# Memory index

- [Grep brace-glob gotcha](tooling-grep-glob-gotcha.md) — mixed root+subdir brace globs silently return zero matches on this Windows repo; use direct paths; Bash/python denied in producer sandboxes
