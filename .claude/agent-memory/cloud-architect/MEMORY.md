# Memory index

- [Grep brace-glob gotcha](tooling-grep-glob-gotcha.md) — mixed root+subdir brace globs silently return zero matches on this Windows repo; use direct paths; Bash/python availability varies by session — try first, record exact denial if blocked
- [Worktree write isolation](tooling-worktree-write-isolation.md) — Write locked to own agent worktree (other task worktrees read-only); use uppercase C: in write paths; disclose transplant need to orchestrator
