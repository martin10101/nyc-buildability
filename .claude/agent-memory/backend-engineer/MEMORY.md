# Backend-engineer memory index

- [Producer sandbox varies per session](env-producer-sandbox-no-exec.md) — probe python/network cheaply at session start; M1-T002 had full exec+network, earlier sessions had none; rm can be denied
- [2026-07-15 no-exec fallback playbook](sandbox-no-python-exec.md) — stale as a universal rule; keeps the Grep-static/orchestrator-capture playbook for sessions where the probe fails
- [Socrata/PLUTO connector gotchas](socrata-pluto-gotchas.md) — checkbox columns are JSON booleans (='Y' gives type-mismatch 400); bbl decimal-serialized in FULL records; SODA omits nulls even under $select
- [Agent-supervisor rotation + model machinery](agent-supervisor-rotation-and-model-machinery.md) — reuse (never fork) broker_permission_handler, rotation.py seam pieces (observe_mid_unit/RotationLedger/decide_pre_dispatch guard), stream-json model+usage carriers; expected_model mismatch probe; threshold in RotationThresholds
- [Worktree vs shared checkout: edit the worktree, mind EOL](env-producer-sandbox-no-exec.md) — a worktree-isolated agent must Edit the worktree path (shared-checkout edits + `git -C` redirects are refused); shared and worktree copies can differ only in line endings (git-normalized in `git diff`), so re-read from the worktree before editing a file whose `diff -q` flags it
- [Supervisor cli.py oversize + F-2 reachability](supervisor-cli-oversize-and-f2-reachability.md) — cli.py is grandfathered-oversized (~26 SLOC headroom); new operator commands go in a focused module via register_*_verbs, not inline; F-2 = owner recovery edges the S7 table defines but no command fires — catch with a mechanical TRANSITIONS reachability test
- [Supervisor launch seam + test gotchas](supervisor-launch-seam-and-test-gotchas.md) — run_unit's single Popen is the worker chokepoint; rotation_pending was reactive-only (no pre-first-dispatch ceiling seam); worktree guards compare cwd vs PACKET worktree not vs checkout; fixture home-prefix hygiene test; CI ruff only lints services/api
- [Modularity SLOC ceiling on supervisor legacy files](modularity-sloc-ceiling-supervisor.md) — claude_runner.py/cli.py now sit AT baseline+10% (0 headroom, supersedes the older ~26 estimate); source_lines counts docstrings not comments; strict-greater boundary; put new machinery in new modules + thin wiring, combine seam checks; modularity_exceptions.json usually not in allowed_paths
- [Supervisor modularity net-zero + command-doc tooth gotchas](supervisor-modularity-and-tooth-gotchas.md) — append a kwarg to an existing call line for NET-ZERO SLOC on at-limit cli.py; tooth SKIPS any `<placeholder>` command and test_runbook_has_a_pinned_start_command needs one concrete pinned start; live protected-config files are readable for digest recompute (not the runtime dir); manifest lives OUTSIDE the tree under %LOCALAPPDATA%\...\ctl24-activation
- [Supervisor run_loop + cmd_start test altitude](supervisor-run-loop-and-cmdstart-test-altitude.md) — drive REAL cli._run_loop with real config + faked cli.ClaudeRunner/CodexReviewer keyed by cwd/repo (proves shared-run_id budget resume, run_closed COMPLETE->IDLE, D6); full cmd_start needs live auth/capability probes + real subprocess (golden-run altitude); exec the VERBATIM cli.py:3069 source line (inspect+ast) to test an inline dispatch expression without refactoring a net-zero file
