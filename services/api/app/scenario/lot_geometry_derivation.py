"""M5-T076 placeholder — the producer authors the lot-geometry derivation module here
(authoritative 2263 ring -> lot_line_segments, provenance quintuple, typed failure
classes, fail-closed). Seeded at the contract seam so the allowed path carries a
tracked file; no importable surface yet by design."""
