"""Proposed-massing validation (task M5-T048, phase B0; placeholder seed).

The proposed_massing INPUT CLASS validator: a proposed building is a THIRD
input class - never a city record and never a rule (D-076-R002). This module
validates the parsed block (closed simple outline, level consistency, unit
sanity, bounded ceilings) with typed refusals; it computes NO allowance and
performs NO derivation (B1 territory).

Placeholder committed at the contract seam; the producer implements it inside
the M5-T048 packet scope.
"""

from __future__ import annotations

__all__: list[str] = []
