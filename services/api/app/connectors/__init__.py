"""Official-source connectors (M1).

Every connector maps official records to canonical source facts that validate
against ``packages/contracts/schemas/v1/source_fact.schema.json`` and never
invents, guesses, or silently defaults a property fact (PRD sections 2, 9, 23.2).
"""
