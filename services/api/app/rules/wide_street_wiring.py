"""B7 wide-street rule wiring (M5-T034) — placeholder seeded at contract time.

The producer replaces this with the wiring module consuming
dcm_street_width_policy + wide_street_buffer_engine (own-module ruling,
M4-T021 G3). Seeded so the packet's allowed_paths carry tracked files
(gate fail-closed rule).
"""
