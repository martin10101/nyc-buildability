"""Placeholder seeded at the M5-T035 contract seam (G0 requires tracked files
for every allowed path). The M5-T035 producer replaces this with the
settings-gated live wide-street-determination provider (orchestration only,
fail-safe None; see the task packet)."""
