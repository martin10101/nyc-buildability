"""CAD interchange + drawing-sheet output (D-087; orchestrator-seeded package).

Modules: dxf_writer (M5-T081), pdf_sheet_writer (M5-T085). Nothing imports this package
until a later gated wiring packet."""
