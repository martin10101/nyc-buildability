# bridge_ring_pairs fixtures — PLACEHOLDER (M5-T073)

Seeded at the contract seam so the `fixtures/bridge_ring_pairs/**` allowed-path
glob carries a tracked file. The producer records one provenance entry per
captured pair here (endpoint URL, BBL, retrieved_at UTC, response sha256,
dataset version if disclosed) alongside the verbatim response fixtures.
