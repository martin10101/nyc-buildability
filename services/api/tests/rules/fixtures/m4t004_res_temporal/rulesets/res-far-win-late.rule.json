{
  "rule_id": "res-far-win-late",
  "rule_version": "0.0.1-draft",
  "family": "residential_far",
  "title": "SYNTHETIC residential-FAR overlapping-window rule - late (effective from 2024-01-01)",
  "jurisdiction": "nyc",
  "status": "extracted_draft",
  "effective_from": "2024-01-01",
  "effective_to": null,
  "description": "SYNTHETIC fixture (M4-T004 FH-2 integration): a residential_far rule with window [2024-01-01, +inf). Overlaps res-far-win-early on [2024-01-01, 2024-06-01); same applicability (SYNTH) and same output. Illustrative value; not a Verified determination.",
  "citations": [
    {"snapshot_id": "zr-demo-m4t004", "section": "00-00-DEMO-FH2", "quote": "SYNTHETIC (not verbatim from any source): FH-2 integration window late.", "last_amended": null}
  ],
  "inputs": [
    {"name": "zoning_district", "type": "string", "required": true, "description": "District governing the lot (scope via applicability)."},
    {"name": "lot_area_sq_ft", "type": "number", "required": true, "unit": "square_feet", "exclusive_minimum": 0, "description": "Zoning-lot area (must be > 0)."}
  ],
  "outputs": [
    {"name": "max_synth_far", "type": "number", "unit": "far", "description": "SYNTHETIC max FAR (late)."},
    {"name": "max_synth_floor_area_sq_ft", "type": "number", "unit": "square_feet", "description": "SYNTHETIC max floor area (late)."}
  ],
  "parameters": [
    {"name": "far_value", "value": 2.0, "citation_ref": "zr-demo-m4t004", "note": "SYNTHETIC late FAR."}
  ],
  "applicability": {"op": "in_set", "input": "zoning_district", "values": ["SYNTH"]},
  "computation": {
    "steps": [
      {"id": "far", "op": "identity", "args": [{"param": "far_value"}], "note": "Late FAR."},
      {"id": "floor_area", "op": "multiply", "args": [{"input": "lot_area_sq_ft"}, {"step": "far"}], "note": "lot area x FAR."}
    ],
    "outputs": {"max_synth_far": {"step": "far"}, "max_synth_floor_area_sq_ft": {"step": "floor_area"}}
  },
  "limitations": ["SYNTHETIC FH-2 integration demonstration; illustrative value; requires official capture + G6 before any use."]
}
