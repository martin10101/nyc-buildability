{
  "rule_id": "syn-comp-far",
  "rule_version": "0.0.1-draft",
  "family": "syn_complementary_far",
  "title": "SYNTHETIC complementary rule - FAR output",
  "jurisdiction": "nyc",
  "status": "extracted_draft",
  "effective_from": null,
  "effective_to": null,
  "description": "SYNTHETIC fixture (M4-T004 FH-2 negative control): same family, same applicability (SYNTH), same (null) window as syn-comp-yard, but emits a DIFFERENT output (max_syn_comp_far). Complementary rules that decide different things are NOT competitors -> NO conflict. Illustrative value; not a Verified determination.",
  "citations": [
    {"snapshot_id": "zr-demo-m4t004", "section": "00-00-DEMO-FH2", "quote": "SYNTHETIC (not verbatim from any source): FH-2 complementary FAR.", "last_amended": null}
  ],
  "inputs": [
    {"name": "zoning_district", "type": "string", "required": true, "description": "District governing the lot (scope via applicability)."},
    {"name": "lot_area_sq_ft", "type": "number", "required": true, "unit": "square_feet", "exclusive_minimum": 0, "description": "Zoning-lot area (must be > 0)."}
  ],
  "outputs": [
    {"name": "max_syn_comp_far", "type": "number", "unit": "far", "description": "SYNTHETIC max FAR (complementary)."}
  ],
  "parameters": [
    {"name": "far_value", "value": 1.5, "citation_ref": "zr-demo-m4t004", "note": "SYNTHETIC complementary FAR."}
  ],
  "applicability": {"op": "in_set", "input": "zoning_district", "values": ["SYNTH"]},
  "computation": {
    "steps": [
      {"id": "far", "op": "identity", "args": [{"param": "far_value"}], "note": "Complementary FAR."}
    ],
    "outputs": {"max_syn_comp_far": {"step": "far"}}
  },
  "limitations": ["SYNTHETIC FH-2 negative-control demonstration; illustrative value; requires official capture + G6 before any use."]
}
