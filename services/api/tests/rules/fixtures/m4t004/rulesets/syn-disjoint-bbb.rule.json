{
  "rule_id": "syn-disjoint-bbb",
  "rule_version": "0.0.1-draft",
  "family": "syn_disjoint_far",
  "title": "SYNTHETIC disjoint-applicability FAR - district BBB",
  "jurisdiction": "nyc",
  "status": "extracted_draft",
  "effective_from": null,
  "effective_to": null,
  "description": "SYNTHETIC fixture (M4-T004 FH-2 negative control): applies only to district BBB. Coexists with syn-disjoint-aaa across an overlapping window but is never simultaneously applicable to the same inputs -> NO conflict. Illustrative value; not a Verified determination.",
  "citations": [
    {"snapshot_id": "zr-demo-m4t004", "section": "00-00-DEMO-FH2", "quote": "SYNTHETIC (not verbatim from any source): FH-2 disjoint BBB.", "last_amended": null}
  ],
  "inputs": [
    {"name": "zoning_district", "type": "string", "required": true, "description": "District governing the lot (scope via applicability)."},
    {"name": "lot_area_sq_ft", "type": "number", "required": true, "unit": "square_feet", "exclusive_minimum": 0, "description": "Zoning-lot area (must be > 0)."}
  ],
  "outputs": [
    {"name": "max_syn_disjoint_far", "type": "number", "unit": "far", "description": "SYNTHETIC max FAR (BBB)."}
  ],
  "parameters": [
    {"name": "far_value", "value": 2.0, "citation_ref": "zr-demo-m4t004", "note": "SYNTHETIC BBB FAR."}
  ],
  "applicability": {"op": "in_set", "input": "zoning_district", "values": ["BBB"]},
  "computation": {
    "steps": [
      {"id": "far", "op": "identity", "args": [{"param": "far_value"}], "note": "BBB FAR."}
    ],
    "outputs": {"max_syn_disjoint_far": {"step": "far"}}
  },
  "limitations": ["SYNTHETIC FH-2 negative-control demonstration; illustrative value; requires official capture + G6 before any use."]
}
