{
  "rule_id": "syn-win-b",
  "rule_version": "0.0.1-draft",
  "family": "syn_window_overlap_far",
  "title": "SYNTHETIC overlapping-window FAR - B (effective from 2024-01-01)",
  "jurisdiction": "nyc",
  "status": "extracted_draft",
  "effective_from": "2024-01-01",
  "effective_to": null,
  "description": "SYNTHETIC fixture (M4-T004 FH-2): window [2024-01-01, +inf). Overlaps syn-win-a on [2024-01-01, 2024-06-01). Same family, same applicability (SYNTH), same output (max_syn_window_far) -> a genuine conflict inside the overlap. Illustrative value; not a Verified determination.",
  "citations": [
    {"snapshot_id": "zr-demo-m4t004", "section": "00-00-DEMO-FH2", "quote": "SYNTHETIC (not verbatim from any source): FH-2 window B.", "last_amended": null}
  ],
  "inputs": [
    {"name": "zoning_district", "type": "string", "required": true, "description": "District governing the lot (scope via applicability)."},
    {"name": "lot_area_sq_ft", "type": "number", "required": true, "unit": "square_feet", "exclusive_minimum": 0, "description": "Zoning-lot area (must be > 0)."}
  ],
  "outputs": [
    {"name": "max_syn_window_far", "type": "number", "unit": "far", "description": "SYNTHETIC max FAR (window B)."}
  ],
  "parameters": [
    {"name": "far_value", "value": 2.0, "citation_ref": "zr-demo-m4t004", "note": "SYNTHETIC window-B FAR."}
  ],
  "applicability": {"op": "in_set", "input": "zoning_district", "values": ["SYNTH"]},
  "computation": {
    "steps": [
      {"id": "far", "op": "identity", "args": [{"param": "far_value"}], "note": "Window-B FAR."}
    ],
    "outputs": {"max_syn_window_far": {"step": "far"}}
  },
  "limitations": ["SYNTHETIC FH-2 demonstration; illustrative value; requires official capture + G6 before any use."]
}
