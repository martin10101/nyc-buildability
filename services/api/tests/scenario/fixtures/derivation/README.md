# Derivation fixtures (M5-T051)

Hand-computed fixture pack - every fixture shows its arithmetic. Placeholder seeded at
the contract seam; the producer adds the hand-computed shapes per the packet SCOPE.
