// M5-T065 placeholder: map outline drawing component (seeded at the contract seam;
// replaced by the producer). Composes over the read-only LotOutlineMap; keyboard-operable
// vertex placement with focus management on delete (DB-043(a) applied from the start).
export const PROPOSAL_OUTLINE_DRAW_PLACEHOLDER = "M5-T065" as const;
