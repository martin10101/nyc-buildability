/**
 * Placeholder seeded at the M5-T036 contract seam (G0 requires tracked files
 * for every allowed path). The M5-T036 producer replaces this with the
 * zoning-context panel (label display only; see the task packet).
 */
export {};
