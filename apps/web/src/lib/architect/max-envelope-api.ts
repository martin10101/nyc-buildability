/**
 * Task M5-T070 placeholder (D-082-R003 + D-083). The typed client for the UNMOUNTED
 * POST /api/v1/max-envelope contract lands with the packet; this stub pins the route
 * path only. No fetch is issued from this module yet.
 */
export const MAX_ENVELOPE_ROUTE = "/api/v1/max-envelope";
