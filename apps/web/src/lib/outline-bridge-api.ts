// M5-T065 placeholder: outline-bridge client (seeded at the contract seam; replaced by
// the producer). Full typed decode matrix per the proposal-checks client discipline.
export const OUTLINE_BRIDGE_CLIENT_PLACEHOLDER = "M5-T065" as const;
