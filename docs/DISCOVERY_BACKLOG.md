# DISCOVERY BACKLOG — mid-work findings tracked to resolution (D-069)

NOT auto-injected. This ledger holds product/domain discoveries made mid-work that are not
yet ledger tasks, blockers, or directive requirements — it FEEDS those systems and never
duplicates them. Statuses: **OPEN** (needs a decision), **QUEUED(task)** (in the pipeline),
**RESOLVED(record)** (closed, with where), **WATCH(trigger)** (deliberately monitored).
Rules (D-069): append at discovery time; sweep OPEN/WATCH at every contract seam and replan;
entries never get deleted — only status changes.

| ID | Date | Discovery | Source | Implication | Status |
|---|---|---|---|---|---|
| DB-001 | 2026-09-17 | 350 Fifth Ave (BBL 1008350041) is genuinely SPLIT-ZONED (C5-3 + C6-4.5 + Special Midtown) — `geometry_uncertain` is correct there; the product cannot yet apportion rules across a split lot | M5-T033 owner-confirmation record; ZTLDB row | Split-lot apportionment support (ZR 77-series class) — a real feature family, needs legal research first | OPEN |
| DB-002 | 2026-09-17 | Condo billing BBLs (lot 7501+) are absent from ZTLDB by design; 3022647515 (condono 1313) honestly reports no data | M5-T033 owner-confirmation; PLUTO/ZTLDB queries | Condo→base-lot resolution step before zoning-lot lookup; also a D-059-R007 benchmark hard class | RESOLVED(research 2026-09-18 + resolver MODULE M5-T042 accepted 2026-09-18, 225th; live wiring remains — DB-029 binds it) |
| DB-003 | 2026-09-17 | The city's ZTLDB source dataset is stale AT THE SOURCE (rows last updated 2026-04-05; >45-day threshold logs on every live request) | Live Render logs; ztldb_soda freshness guard | Data-quality watch; G6-era decision on how stale zoning-lot data is presented to users | WATCH(re-check dataset rowsUpdatedAt at each deploy-verification; escalate if the city updates or the product goes external) |
| DB-004 | 2026-09-17 | Deployed services need `PYTHON_VERSION` pinned (Render default drifted to 3.14 → source-built shapely → geometry-pin guard fail-closed) | Run-38 deploy log; M5-T033 owner-confirmation | Deploy checklist must gain the PYTHON_VERSION pin row (checklist file currently outside every active packet's scope) | RESOLVED(M5-T035 accepted 2026-09-18 — checklist §6d PYTHON_VERSION=3.12.11 row) |
| DB-005 | 2026-09-17 | `PropertyOverview.tsx` builds its ZoLa link inline without the validated `zolaLotUrl()` helper (pre-existing; low risk) | M5-T032 G1 finding F2 + G3 finding 1 | Unify all ZoLa links through the shared validated helper (small web task; AddressConfirmCard too) | RESOLVED(M5-T036 accepted 2026-09-18) |
| DB-006 | 2026-09-17 | `address-search.ts` classifies ANY non-ok non-429 status (incl. 4xx) as retryable `source_unavailable` — practical impact ~nil today | M5-T032 G1 finding F1 | Tighten retry/label gate to `status >= 500` (small hardening) | RESOLVED(M5-T038 accepted 2026-09-18 — distinct never-retried `rejected` outcome) |
| DB-007 | 2026-09-17 | AS-3 timeout outcome lacks a direct test that the full-address /search button is offered on timeout; architect-mode "Not my property" focus return also unit-untested | M5-T032 G4 F1 + journey F3 | Two small test additions in a future web packet | RESOLVED(M5-T038 accepted 2026-09-18) |
| DB-008 | 2026-09-17 | The 6000 ms address-search deadline sits below GeoSearch's observed healthy-but-slow latencies (~6.8 s): a consistently slow service routes every user to manual entry | M5-T032 journey finding 1; handoff §6 | Product-usability watch item for the owner (deliberate no-raised-deadline decision stands) | WATCH(revisit if live smoke or users show persistent >6 s healthy latencies) |
| DB-009 | 2026-09-17 | `malformed` failure copy omits the still-rendered full-address search button (copy/affordance mismatch, minor) | M5-T032 journey finding 2 | One-line copy fix in a future web packet | RESOLVED(M5-T038 accepted 2026-09-18 — shared FULL_ADDRESS_SEARCH_LABEL constant) |
| DB-010 | 2026-09-17 | Named-street §12-10 override table (Broadway W94–97, Allen St Rivington–Delancey) has no implementation: needs a CD+cross-street matcher and distinct override provenance; M5-T034 wiring deliberately refuses exceptions_checked=True near those segments | M4-T018 report §3; M5-T034 packet scope note | Own follow-up module after M5-T034 (was already "OPEN" in Tier 2 prose — now tracked here) | RESOLVED(matcher M5-T039; WIRING M5-T040 accepted 2026-09-18, 224th — full chain live behind the flags) |
| DB-011 | 2026-09-17 | EC-4 boundary-tangency legal tolerance question (zero-area LineString intersects) left open for qualified review | M4-T021 B4 disclosed judgment calls | G6-era legal question; keep with the G6 queue | WATCH(G6 activation) |
| DB-012 | 2026-09-17 | Loop review-round budget (4) is one round short for report-heavy tasks; honesty-bar packet block mitigates but the limit itself is owner-config | Runs 36/37 breaker analysis | Owner option: raise `consecutive_revision_loops` in the protected config; revisit if a run trips the breaker despite the honesty bar | WATCH(FIRED 2026-09-17 run-40: breaker tripped at 4 despite the honesty bar — mid-build churn (an investigation-only checkpoint + a stale lint claim cost two rounds), not report polish; run-41 relaunched with a RUN-40 CARRYOVER packet block; owner option to raise the protected-config budget surfaced 2026-09-17; escalate again on the next trip) |

| DB-013 | 2026-09-17 | Buffer engine: lot-polygon vertex count unbounded before shapely construction; per-segment PATH count unbounded before MultiLineString build (both currently safe — server-derived inputs) | M5-T034 G5 findings 1-2 | Two cheap defense-in-depth ceilings when the engine sits behind a live request path | RESOLVED(M5-T035 accepted 2026-09-18 — MAX_LOT_VERTICES + MAX_PATHS_PER_SEGMENT typed fail-closed) |
| DB-014 | 2026-09-17 | Wide-street outcome (far_row/governing_far/provenance) deliberately NOT serialized in rule_evaluation contract v1.0.0 — clients still see only the conservative FAR even on a confident WITHIN | M5-T034 G3 INFO-B | Additive contract bump to surface the wide-street result + provenance to the UI | RESOLVED(M5-T037 accepted 2026-09-18 — rule_evaluation v1.1.0 optional wide_street block + DevelopmentLimits/CalculationEvidence/ReportView surfaces; D-073-R003 first verification) |
| DB-015 | 2026-09-17 | The wide-street provider seam defaults to None — no live data source feeds real determinations through /rule-evaluation yet | M5-T034 G3 INFO-C; producer report §6.1 | Live provider wiring task (fetch DCM segments + lot geometry server-side per request, bounded) — the step that makes wide-street FAR real for users | RESOLVED(M5-T035 accepted 2026-09-18 — settings-gated live provider, fail-safe None) |

| DB-016 | 2026-09-17 | OWNER OBSERVATION: the product surface shows only residential FAR while ZoLa displays rich per-lot context (special districts, overlays, landmark status, neighbor-lot highlighting) — much of which our connectors ALREADY FETCH (e.g., the ZTLDB row carries zoning_district_1/2 + special_district_1 + zoning_map_number) but never display | Owner question 2026-09-17 ("what's the point if the government site shows all this?") | Context-panel parity task: surface the designations we already retrieve (with provenance + ZoLa link) alongside computed answers; label-display carries no computation risk and closes the perceived gap; the computed-answer engine remains the differentiator | RESOLVED(M5-T036 accepted 2026-09-18) |

| DB-017 | 2026-09-17 | Client-side profile mapped_features carries flags the new context panel does not render (2007 FIRM / 2015 pFIRM flood flags, transit zone, split-zone flag, zoning map number) — panel scope was districts/overlays/special districts/landmark+historic | M5-T036 producer discovery D1 (run persistent2-local-01, cross-session return) | Decide: extend the panel or keep AdditionalZoningFlags as the carrier for these; small follow-up either way | OPEN |
| DB-018 | 2026-09-17 | Once the context panel mounts, the architect overview surface carries TWO ZoLa affordances (the panel's validated link + PropertyOverview's existing one) | M5-T036 producer discovery D2 (same return) | Consolidation is a design call (product-design-director class), not a defect; judge after the panel is accepted and visible | OPEN |

| DB-019 | 2026-09-17 | M5-T036 review-wave polish trio (all advisory, none blocking): (a) "↗" glyph inside the two ZoLa link texts lacks aria-hidden (G3 F1 + HJ F6; matches a pre-existing sibling); (b) PropertyOverview renders NOTHING on a null BBL while the panel and confirm card show an honest absent note (G5 observation — consistency nuance); (c) mapped-feature coverage_status not surfaced on the panel's landmark/historic rows (G1 O1; full coverage table stays on the zoning tab + brief) | M5-T036 gate returns (G1/G3/G5/HJ) | One small web polish packet, or fold into the DB-018 design-director pass | RESOLVED(M5-T038 accepted 2026-09-18 — aria-hidden glyphs, PropertyOverview honest note, landmark coverage badges) |

| DB-028 | 2026-09-18 | M5-T040 review advisory cluster (ALL SIX reviewers PASS, none blocking; recorded at acceptance): (a) wide_street_wiring.py at 723 SLOC, 27 short of the justify threshold — extract a named_street_override_status.py submodule with facade re-export on the NEXT touch, before the alternate-width leg or CD/cross-street connector (G3 cohesion ruling); (b) stale module docstring ("OUT OF SCOPE here") + branch-4 reason understating that the matcher IS now consulted (G3 A1/A2); (c) contract hardening: make the safe attestation path require override_table_implemented=True, not just the boolean pair (G3 A3); (d) all(()) is True on the no-decisions professional-review branch — cosmetic attestation nit (G3 A4); (e) matcher-load-failure fail-safe branch has no direct test (G3 A5); (f) complete-span heuristics guard for future snapshots with abbreviation periods / Title-cased qualifiers (G5 A1); (g) NFKC-before-casefold if user-supplied street names ever reach the matcher fully resolved (G5 A4); (h) CalculationEvidence null-FAR defensive fallback should read "Not calculated" to match DevelopmentLimits (HJ A1); (i) OWNER question: does the DB-025(a) intent extend to the pre-existing whole-evaluation "Draft - Professional review required" banner at the top of the evidence panel? (HJ A2); (j) a review-state-with-non-null-FAR test probe if the server invariant is ever relaxed (G4 #1); (k) MATCHED_OVERRIDE is production-unreachable by design until a future snapshot flips disposition AND a CD/cross-street connector exists — that connector is the named follow-up capability (G1 A3); (l) run-46 producer report kept a stale PARTIAL framing (record-only, G4 #5) | M5-T040 gate returns (G1 A1-A3, G3 A1-A6, G4 #1-5, G5 A1-A6, HJ A1-A3) | One hardening/extraction touch on the wiring module (a-e), display polish (h), owner check (i), future-capability rows (f/g/j/k) | RESOLVED(build items a-e,h = M5-T043 accepted 2026-09-19, 226th; surviving riders f/g/i/j/k moved to DB-030) |

| DB-029 | 2026-09-18 | M5-T042 review cluster — the condo WIRING packet's BINDING preconditions + riders (the DB-023 pattern): (a) G5 F1 MUST-FIX-BEFORE-WIRING — anchor-leaky shape guards (^\d{10}$ matches trailing newline; Unicode \d) and RAW input interpolated into the URL: switch to re.ASCII fullmatch and interpolate normalize_bbl(bbl).canonical, plus F2 (same fix on the response-side base-BBL guard) and F3 negative tests (trailing-newline/Unicode-digit inputs asserting typed refusal, no transport call) — close BEFORE any consumer import; (b) source_registry record + live freshness fetch BEFORE any live traffic (G1 A2; backend rule "no connector without a source_registry record"); (c) official null-billing fixture = live-era C-class row on the wiring packet (G1 A3, orchestrator disposition recorded at acceptance); (d) retrieved_at stamped post-response per query, matching the pluto precedent (G3 #1); (e) BBLValidationError "wrong_shape" code = taxonomy drift — use a documented code or extend the vocabulary (G3 #2); (f) resolve_by_condo_key overloads input_bbl/lot_class — dedicated input_kind (G3 #3 / G4 #5); (g) dtm-local error-branch tests: 400-drift split, JSON guards, terminal typed errors (G3 #4 / G4 #2); (h) unit-path branch tests: unit-empty→UNRESOLVED, unit-row-without-condo_key (G4 #3); (i) C10 runtime-drift note-path test (G4 #4); (j) optional $limit defense-in-depth (G5 F4); (k) hoist shared soda_errors/soda_request helpers if the file grows toward 750 (G3 #5) | M5-T042 gate returns (G1 A1-A5, G3 #1-6, G4 #1-5, G5 F1-F4) | The condo wiring packet's contract MUST cite this row; (a) closes first, (b)/(c) gate live traffic; (d)-(k) ride along | RESOLVED(fully: a,d-i = M5-T044 accepted 227th; b,c + the T044-wave riders + j = M5-T045 accepted 2026-09-19, 230th - registry records for both datasets, the REAL key-absence fixture, both consumer seams wired fail-closed, display increment; k routed to the orchestrator as a cross-connector change; ALL FIVE reviewers + DCV PASS zero blocking) |

| DB-030 | 2026-09-19 | Surviving riders from the M5-T043/M5-T044 hardening waves (none blocking): (a) OWNER QUESTION (HJ, M5-T040 wave): does the DB-025(a) intent extend to the pre-existing whole-evaluation "Draft - Professional review required" banner atop the evidence panel?; (b) future-snapshot heuristics guard for the complete-span binding (abbreviation periods / Title-cased qualifiers) if the snapshot corpus grows (G5); (c) NFKC-before-casefold if user-supplied street names ever reach the matcher fully resolved (G5); (d) review-state-with-non-null-FAR display probe if the server invariant is ever relaxed (G4); (e) the CD/cross-street connector = the named future capability that would make MATCHED_OVERRIDE production-reachable (G1); (f) named_street_override.py (matcher, 853 lines) above the modularity warn tier - extraction/cohesion ruling due on the next task touching it; (g) OVERRIDE_CLEAR test-fixture semantics changed meaning under the same name (reader note, G3 M5-T043 A2) | M5-T040/T043/T044 gate returns | (a) one owner check; (b-e) future-capability/watch riders; (f) binds the next matcher-touching packet; (g) record-only | OPEN |

Sweep log: seeded 2026-09-17 from M5-T032/T033 gate findings, owner-confirmation records,
M4-T018/T021 carried notes, and runs-36/37 analysis. +DB-013..015 from the M5-T034 review
wave (same day). +DB-016 owner product-gap observation (same day).
Sweep 2026-09-17 (M5-T035 contract seam): DB-015 + DB-013 + DB-004 → QUEUED(M5-T035) (live
provider packet folds in the engine ceilings and the checklist PYTHON_VERSION/flag rows).
All other OPEN entries reviewed and stay OPEN (DB-016 = the owner-ruled next lane after
M5-T035; DB-005/006/007/009 = small-web-task cluster, candidates to bundle with DB-016;
DB-001/002/010/014 need their own packets or legal research). WATCH entries unchanged
(DB-003 deploy-verification trigger, DB-008 latency, DB-011 G6, DB-012 next breaker trip).
Sweep 2026-09-17 (M5-T036 contract seam, loop-2 lane per D-071): DB-016 + DB-005 →
QUEUED(M5-T036) (context panel folds in the ZoLa-link unification on the same surface).
DB-007/009 stay OPEN (small address-flow items, next small web packet). Other entries
unchanged since the same-day M5-T035 sweep.

| DB-020 | 2026-09-18 | G1 F1: `wide_street_live_provider.py:356` passes `source_raw_digest=entry.segment.streetwidth_raw` (the width TEXT, e.g. "80") where the engine documents a raw-body-digest passthrough; real digests exist (`SegmentGeometryPage.raw_digest`); lot side correct; latent while the structured block is unserialized | M5-T035 G1 finding F1 | MUST be fixed inside the DB-014 serialization packet before the provenance block reaches any response | RESOLVED(M5-T037 accepted 2026-09-18 — source_raw_digest = geometry-page raw digest, regression-tested; G1 confirmed the M5-T035 F1 closed exactly) |
| DB-021 | 2026-09-18 | M5-T035 review hardening cluster (all advisory, none blocking): (a) G5 LOW-1 provider-side `MAX_LOT_VERTICES` check before its own first `canonical_to_shapely` (:282); (b) G5 LOW-2 cap `wide_object_ids` before the geometry-fetch loop (engine's 512 ceiling runs after fetch); (c) G5 LOW-3 docstring note on the two-layer ZR 12-10 exceptions attestation (width-policy "checked" vs EC-5 "unattested") + routed to the G6 legal queue; (d) G3 O1 optional wrap of pure-assembly steps; (e) G4 branch-coverage: `segment_geometry_unexpected_error` + `_attested_lot` sub-branches untested directly | M5-T035 gate returns (G1/G3/G4/G5) | Fold into the DB-014 packet or a later hardening pass; LOW-3's legal half joins DB-011 at G6 | RESOLVED(M5-T040 accepted 2026-09-18 — a/b/c/e landed and review-verified; (d) stays advisory-unqueued; (c) legal half with DB-011) |

| DB-022 | 2026-09-18 | zr-12-10.snapshot.json v1 (both copies) is a STALE July-2026 draft predating the 3/26/2026 amendment: only the basic wide/narrow definition, no named-street table, no alternate-width text, section_last_amended 2024-12-05, extraction_status extracted_draft, raw_html_verified false — while the ACCEPTED amended capture lives in project-control/reports/M4-T018-zr1210-wide-street-reconciliation.md (sha-pinned channel 4a75e22f) | M5-T039 run-01 consolidated blocker report (worker refused to fabricate provenance — correct) | Snapshot refresh QUEUED into M5-T039 phase 0 (verbatim transcription from the accepted capture, digest recompute, both copies synced); sweep other v1 snapshots for the same staleness class at the next research seam | RESOLVED(M5-T039 accepted 2026-09-18 — zr-12-10 repaired both copies; the OTHER-v1-snapshots staleness sweep stays a WATCH item for the next research seam) |
| DB-023 | 2026-09-18 | M5-T039 review hardening cluster — BINDING precondition on the named-street WIRING packet (G5 ruling: required before the module gains a consumer): (a) G5 M1 MEDIUM — disposition_when_located is outside the digest cover and not source-anchored; a crafted snapshot commit could flip real rows to MATCHED_OVERRIDE while the digest stays valid; fix structurally (refuse matched_override whenever the row carries an unresolved qualifier) or digest-cover the structured fields; (b) G5 L1 / G3 A2 — construction doesn't validate block provision_id/section_anchor → KeyError escape at match() on a malformed snapshot (validate at construction, fail closed); (c) G5 L2 — bound the raw-query reprs in MatchResult.reason before wiring to user input; (d) G3 A1 — word-boundary anchoring in _anchored_in | M5-T039 gate returns (G3 A1-A5, G5 M1/L1/L2, G4 obs 1-4) | The wiring packet's contract MUST cite this row and close (a)-(c) before any consumer import lands; (d) advisory | RESOLVED(M5-T040 accepted 2026-09-18 — G5 independently confirmed M1/L1/L2 CLOSED; (d) landed too) |

| DB-024 | 2026-09-18 | M5-T038 review polish cluster (all advisory, none blocking): (a) HJ F1 circular recovery copy when the full-address search itself fails (copy optimized for the autocomplete-failed path); (b) HJ F2 / G3 advisory — AddressConfirmCard absent-BBL note wording differs from the byte-matched panel/overview pair (share one constant on a future touch); (c) HJ F3 — decorative glyphs outside DB-019a scope still un-hidden (address-hint ↗, PropertyIssuesSummary →); (d) HJ F4 — ZoLa link accessible-name inconsistency (pre-existing); (e) G4 obs 12 — packet parenthetical about network-failure retry wording reconciled by orchestrator ruling (distinct unretried `unavailable` stands, code governs) | M5-T038 gate returns (G3 advisory, G4 obs 12-13, HJ F1-F4) | One small copy/a11y polish touch, or fold into the DB-018 design-director pass | RESOLVED(M5-T041 accepted 2026-09-18 — HJ confirms all four prior findings closed) |

| DB-025 | 2026-09-18 | M5-T037 review advisory cluster (none blocking; all recorded at acceptance): (a) G1 F1 — CalculationEvidence status line renders 'Professional review required' unconditionally for every wide_street block (over-cautious mislabel for confident within/not-within; make it conditional on determination_state); (b) HJ F3 — gate both surfaces uniformly on determination_state (currently state vs null-FAR, agreeing only via the server invariant); (c) HJ F4 — 'Wide-street conditional FAR' heading over the conservative value in the not-within case; (d) G4 F1 — multi-page DB-020 fixture (assert a page-2 segment carries page-2's digest); (e) G4 F2 / G5 INFO-1 — web validator is positive-shape only (no client-side additionalProperties rejection; server authoritative) — tighten or correct the wording claim; (f) G5 INFO-2 — pre-existing target_blank evidence link noted | M5-T037 gate returns (G1 F1, G3 obs, G4 F1/F2, G5 INFO-1/2, HJ F3/F4) | One small display/test polish packet, or fold (a)-(c) into the named-street wiring packet's display touch | RESOLVED(a-d M5-T040 accepted 2026-09-18 — G4 confirmed the (d) multi-page fixture present; e M5-T041; f note-only) |

Sweep 2026-09-18 (M5-T035 acceptance seam): DB-015 + DB-013 + DB-004 → RESOLVED(M5-T035
accepted 2026-09-18, 219th). +DB-020/021 from the review wave (above). Next-lane queue per
D-072/D-073: DB-014 screen-wiring packet (folds DB-020, DB-021a/b candidates) = loop-1;
validation-collection + four-regression-property packet (D-073-R004/R005;
docs/research/live-case-regression-properties.md seeded) = loop-2; DB-002 condo→base-lot
resolution = loop-3. DB-001/006/007/009/010/017/018/019 reviewed, stay OPEN (DB-006/007/009/
019 remain the small-web cluster; DB-010 own module after wiring; DB-017/018 design calls).
WATCH unchanged (DB-003 deploy trigger, DB-008 latency, DB-011 G6, DB-012 breaker — owner
decision package delivered 2026-09-18, answer pending).
Sweep 2026-09-18 (M5-T040/M5-T041 contract seam, successor session per seq-117 handoff):
DB-023 → QUEUED(M5-T040) (binding precondition cited in the contract; (a)-(c) close before any
consumer import, (d) folded as advisory); DB-021(a,b,c,e) → QUEUED(M5-T040) (provider ceilings
+ attestation docstring + branch tests; (c)'s legal half stays with DB-011 at G6; (d) G3 O1
optional wrap deliberately not queued — advisory only); DB-025(a-d) → QUEUED(M5-T040) (display
gating + multi-page digest fixture ride the wiring packet — (d) lives in the provider test file
the wiring packet owns), DB-025(e) → QUEUED(M5-T041), DB-025(f) → RESOLVED(note-only: the
CalculationEvidence external link already carries rel="noopener noreferrer" — verified this
seam); DB-024 → QUEUED(M5-T041). DB-002 → orchestrator-dispatched network research this
session (loops are no-network; handoff step 3). D-072 third-lane shortfall recorded: every
remaining OPEN entry is research (DB-001/002), design-director class (DB-017/018), or
owner-decision class (DB-012 WATCH) — no third pairwise-disjoint local build lane exists, so
this seam runs TWO loops (stepwise 3→2 per D-072). DB-001/017/018 reviewed, stay OPEN. WATCH
unchanged (DB-003, DB-008, DB-011, DB-012 — owner answer still pending).
| DB-026 | 2026-09-18 | Address→DTM-lot identity gap: a real corner/range address with NO PLUTO address string ("1279 37th Street" Brooklyn) resolves to no lot, though the building exists as lot 43 with address-of-record "3622 13 Avenue" (BBL 3052960043) — the review's live stop on that property was identity mismatch (+ unsupported MX-12 coverage), not a FAR-label issue | D-073-R004/R005 research (docs/research/live-case-regression-properties.md case 1; PLUTO 26v2 block-5296 enumeration) | Needs an authoritative address→DTM-lot step for range/corner addresses that surfaces the address-of-record so the user sees which lot was analysed; MX/Article XII FAR coverage is a separate campaign-tracked wave | RESOLVED(recon 2026-09-19: the integrated server Geoclient path handles PAD range/corner frontages — the "no lot" stop was the PLUTO-address-string lookup class, not the product's resolver; + M5-T046 accepted 2026-09-19, 228th: entered-vs-analysed identity display live, equality-gate library + corpus + geosearch registry binding future promotion. RESIDUAL: the record-address display channel = DB-032; MX coverage stays campaign-tracked) |

| DB-027 | 2026-09-18 | M5-T041 review advisory cluster (none blocking; all recorded at acceptance): (a) three residual bare "→" glyphs inside link accessible names — PropertyOverview.tsx:26 "Review conflicting source values", :33 "Review missing inputs" (PropertyIssuesSummary), :77 "All property facts" — inconsistent with the DB-019a pattern (HJ F3 advisory + G3 F7 + G4 obs; pre-existing, outside the DB-024 binding letter-map); (b) confirm-card absent-note "this lot" framing nuance pre-confirmation (HJ F2); (c) new-tab nature of ZoLa links not announced to screen readers (HJ F4; pre-existing); (d) development-limits.test.tsx:185/:198 raw-regex /Open ZoLa/ should import ZOLA_LOT_LINK_LABEL (G4 #2 — silent-drift risk on label change); (e) FULL_SEARCH_ERROR_MESSAGES path has unit-only coverage, e2e candidate (G4 #3); (f) if the shared-copy constant set grows, split a presentation-strings module (G4 #1); (g) two unclaimed test-completeness gaps: typed-failure-then-full-search-failure edge unforced, confirm-card literal-null absent-note path untested (G4 #4) | M5-T041 gate returns (G3 F7/F8, G4 #1-4, HJ F2-F4 advisories) | One small a11y/test polish touch, or fold (a) into the DB-018 design-director pass; (d) is a one-line test hygiene fix for any packet owning that file | OPEN |

Same-day update: DB-002 RESEARCH DELIVERED (orchestrator-dispatched official-source-researcher;
docs/research/condo-base-lot-resolution-sources.md, HIGH confidence) — verified path DOF DTM
Condominiums p8u6-a6it billing→base-lot set (worked case 3022647515 → {3022640032, 3022640033},
loop-closed to ZTLDB R7-1 both), units table eguu-7ie3 reverse path, ArcGIS DTM failover parity,
PLUTO appbbl proven structurally incomplete (single-valued), 14-row contract-test pack C1-C14,
8 open questions. Row stays OPEN until the connector task is contracted (next-seam candidate);
the condo-spanning-zoning-lots divergent case is a G6-class legal surface (never auto-collapse).
Same-day update 2: D-073-R004/R005 RESEARCH DELIVERED (second dispatch) —
docs/research/validation-collection-expectations.md (12 pre-registered cases: 5 flat R6-R12
positives, 1 C-overlay, 3 street conditions incl. the Broadway W94-97 named-override refusal,
3 correct-refusals; expectations fixed BEFORE any product consultation) +
live-case-regression-properties.md EXTENDED with authoritative Part-1 records. Two seed
candidates CORRECTED: 401 Columbia (BBL 3005250001) split-zoning REFUTED (single R5 in ZTLDB +
PLUTO SplitZone=N + nyzd centroid; boundary-confidence class); 1279 37th = identity mismatch +
unsupported MX-12 (→ new row DB-026). 69-02 Kessel confirmed single R2 boundary-confidence.
298 Wallabout confirmed condo-identity (joins DB-002 path). Exact internal stopping step for
the two boundary-confidence lots stays PROVISIONAL pending a permitted server-log re-run
(research was product-consultation-barred by design).
Sweep 2026-09-18 (M5-T040 acceptance seam, 224th): DB-010 chain COMPLETE (matcher M5-T039 + wiring M5-T040); DB-021(a,b,c,e), DB-023, DB-025(a-d) all RESOLVED at this acceptance; +DB-028 advisory cluster (above). The street-dependent conditional FAR now runs matcher -> attestation seam -> provider -> screen AND printed brief, fail-closed at every layer, behind the existing flags. M5-T042 (condo resolver module) submitted, five-reviewer wave running. WATCH unchanged (DB-003, DB-008, DB-011, DB-012 — trips 11 and 12 today, owner answer still pending).
Sweep 2026-09-18 (M5-T042 acceptance seam, 225th): DB-002 RESOLVED (research + resolver module; live wiring remains, bound by the new DB-029 precondition row above). Session tally: three acceptances (223/224/225) + two research deliveries + the D-073-R007 release request prepared. WATCH unchanged (DB-003, DB-008, DB-011, DB-012 — owner answer pending).
Sweep 2026-09-19 (M5-T043 acceptance seam, 226th): DB-028 build items RESOLVED (extraction + guard tightening + display fallback all review-verified; both owned rules modules off the modularity warn list); surviving riders consolidated into DB-030 (above). DB-029 gains the T044-wave riders for the condo wiring packet: response-side condo_key-guard negative test, optional $limit, _ERROR_CODE_SAFE_RE fullmatch consistency, invalid_component documented-code assertion, condo_key canonical-normalizer consistency note. M5-T044 delta-attestations all CARRY/EXTENDED on the tagged F1 correction; acceptance pending corrected-head CI.
Sweep 2026-09-19 (M5-T044 acceptance seam, 227th): DB-029(a,d-i) RESOLVED at this acceptance (the G5 must-fix and every in-scope rider closed, incl. the G3 F1 required correction applied as tagged a471e376 and delta-attested CARRIES by all four reviewers); DB-029(b,c) + the T044-wave riders remain the OPEN preconditions binding the future condo WIRING packet (row unchanged, still OPEN for that purpose). Both loops idle at clean breaker stops; next lanes at the next seam.
Recon note 2026-09-19 (for the condo WIRING packet contracting, DB-029): the condo->base-lot step slots in BEFORE the zoning-lot lookup at TWO consumer seams - profile/zoning_crosscheck.py (imports ztldb_soda at :65-71; consumed by profile/builder.py) and spatial/live_provider.py (:47) - plus a multi-lot presentation surface (the divergent-zoning boundary makes multi-lot display a D-073-R006-class display contract). Packet also needs: source_registry records for p8u6-a6it + eguu-7ie3 (DB-029b), the official null-billing fixture (capture DISPATCHED 2026-09-19, orchestrator subagent), and the DB-029/DB-030 riders. D-072 shortfall recorded: both loops idle at clean stops overnight - the remaining lane is this design-heavy wiring packet, deliberately contracted fresh at the next seam with the capture + this recon in hand rather than rushed (breaker economics: a hasty packet burns four review rounds).
Capture landed 2026-09-19 (DB-029c): docs/research/condo-null-billing-fixture-capture.md - byte-faithful official fixture for condo_key 103343 (THE 128 HESTER STREET CONDO, single base lot 1003030019; NO multi-lot null-billing condo exists - both counts = 28). LOAD-BEARING FINDING for the wiring packet: SODA OMITS null columns entirely - the real null-billing wire shape is KEY ABSENCE (7-or-8-key object), so the resolver synthetic fixture modeling an explicit "condo_billing_bbl": null is byte-infaithful; the wiring packet must pin the REAL captured fixture and assert key-absence tolerance. Bonus: response header X-SODA2-Truth-Last-Modified == rowsUpdatedAt (free per-request freshness stamp).
Recon delivered 2026-09-19 (DB-026, session seq 119): docs/research/db026-address-to-lot-source-recon.md (official-source-researcher, saved verbatim). ROOT CAUSE VERIFIED: PLUTO/PTS deliberately stores ONE address per lot (low-number-of-range; PLUTO dict 26v2 p.13) - corner lot 3052960043's record address is "3622 13 AVENUE"; the "1279 37 STREET" frontage lives only in PAD/Geosupport. LOAD-BEARING: GeoSearch v2 (keyless, PAD-backed) resolves the live case correctly BUT match_type/confidence are NON-DISCRIMINATING (fallback/0.8 on the true hit AND on nonsense) - the only success test is housenumber+street field equality; PAD Open Data rowsUpdatedAt is frozen 2015 (freshness = addendum.pad.version, live 26c); LION has NO lot field (bounded negative). Recommendation: Geoclient v2 primary (OWNER ACTION: free subscription key at api-portal.nyc.gov - blocks the Geoclient leg only) + GeoSearch cross-check/degraded-mode + PLUTO.address as the displayed address-of-record. Byte-faithful fixture capture DISPATCHED same day; DB-026 packet contracts GeoSearch-first once the capture lands (Geoclient leg = follow-up packet behind the key).
ORCHESTRATOR CORRECTION same day (recon flag #10 confirmed material): the researcher's worktree was STALE - the repo ALREADY carries the full Geoclient v2 /address connector (app/connectors/geoclient_address.py, M2-T021, GRC handling + recorded fixtures + GEOCLIENT_SUBSCRIPTION_KEY env read) wired into api/v1/address_resolution.py, AND the web address arc already consumes GeoSearch directly (apps/web/src/lib/address-search.ts /search + /autocomplete; AddressAutocomplete.tsx). The recon's owner-key ask stands ONLY IF the deployed env lacks the key (env-scoped, owner-visible). The REAL DB-026 build, sharpened by the capture: address-search.ts trusts the top /search feature while the corpus proves nonsense input returns a plausible WRONG lot at identical confidence/match_type (fallback/0.8) - the equality gate (returned housenumber+street vs parsed input) is the only honest no-match test - plus the confirm-arc address-of-record display. Capture landed: docs/research/db026-address-to-lot-fixture-capture.md (8 sections, all keyless, clean).
Sweep 2026-09-19 (M5-T045 contract seam, session seq 119): DB-029(b,c) + the T044-wave riders -> QUEUED(M5-T045) (the condo WIRING packet is now contracted: registry record + real key-absence fixture + condo_base_lot seam-policy module + both recon-named consumer seams + the D-073-R006 records-vs-allowances multi-lot display + riders; row stays OPEN until acceptance). DB-030 reviewed: (a) stays the pending owner question; (f) binds the next matcher-touching packet (NOT M5-T045 - matcher out of scope); (b-e,g) unchanged WATCH/record. All other OPEN entries reviewed and stay OPEN (DB-026 second lane deliberately deferred pending its own source recon - do-not-rush ruling carried from the seq-118 recon note). WATCH unchanged (DB-003, DB-008, DB-011, DB-012 - owner answer pending, trips at 14).
DB-012 trip 15 (2026-09-19, M5-T045 run 48): consecutive_revision_loops hit 4 with the backend scope BUILT (uncommitted in-worktree, 10 files) and the 4th REVISE demanding the remaining display/e2e/report scope - the same after-substantive-work pattern as trips 9-14. Between-runs drill executed: audit-chain fork (orchestrator deny raced the live run, dup seq 336) repaired via repair_forked_audit_chain (archived as forked-evidence-20260919-043354; loop-2 chain untouched), 5 parked asks denied on the fresh chain, run 49 relaunched on the same packet (worker edits survive). Owner decision (raise 4 to 6) unchanged, now 15 trips.
DB-012 trip 16 (2026-09-19, M5-T046 run 09): same pattern on the SECOND lane - breaker at 4 with 9 of 10 scoped files built and the 4th REVISE demanding fine-grained polish (trim-only-blank-check, whitespace-exact journey assertion, evidence-packet organization). Two worker-flagged items to assess at harvest: a claimed integration conflict on the address-search surface, and the record-address channel gap (the packet's predicted discovery). 3 asks denied post-close; run 10 relaunched on the same packet. Trips now 16.
| DB-031 | 2026-09-19 | Multi-lot condo RECORDS-VIEW display split out of M5-T045 (orch scope disposition at the run-49 wave): presenting a multi-lot condo's recorded base lots and their recorded zoning on screen requires a real production data channel (profile/evaluation contract or route surface) that M5-T045 rightly forbids touching; in M5-T045 the multi-lot/unresolved outcomes stay fully fail-closed with the professional-review fail-safe as their visible surface | M5-T045 run-49 review wave + ORCH-SCOPE-DISPOSITION in the packet | One follow-up packet that DESIGNS the channel properly (additive contract work under the accepted tooling, or an existing-channel proof), then the records-view display per D-073-R006 | OPEN (binds the follow-up; cite with D-073:R006) |

| DB-032 | 2026-09-19 | Record-address display channel (the M5-T046 OQ-5 discovery + HJ A1): the confirm card shows entered-vs-matched but NOT the lot's PLUTO address-of-record when it differs (corner/vanity lots: two analysts entering different frontages of the same lot see different matched addresses with no record-address anchor); no existing channel carries PLUTO.address to the confirm arc | M5-T046 producer report §7 + HJ A1 + geosearch.json OQ-5; corpus §6 (PLUTO.address="3622 13 AVENUE" for bbl 3052960043) | One bounded packet: surface PLUTO.address (+ version) to the confirm card through a properly designed channel (existing per-BBL route extension, additive), shown as a labeled city record; carries the T046 reviewer riders (G3 A1 borough disambiguation, A2 ambiguity vocab, A3 bounded strings if the gate goes live; G4 A1 /search abbreviation fixture; G5 A1 length bound; HJ A2 layout-neutral copy, A3 display-trim) where in-scope | RESOLVED(M5-T047 accepted 2026-09-19, 229th: additive record-address route + typed client + city-record card line + the in-scope riders; ALL FIVE reviewers + DCV PASS zero blocking; remaining polish riders -> DB-033; future-promotion riders G3 A1/A2/A3 stay with any GeoSearch-promotion packet) |

Sweep 2026-09-19 (M5-T046 acceptance seam, 228th): DB-026 RESOLVED (above); +DB-032 (the record-address channel + T046 reviewer riders). All five reviewers PASS zero blocking; DCV PASS with the six-condition restamp pre-authorization reproduced at record. D-076 plan authored same seam (phase-B queue: B0 contract first); DB-031 (multi-lot records-view channel) and DB-032 are channel-design siblings — contract them mindful of B0's proposal-contract conversation so no competing shapes fork.
| DB-033 | 2026-09-19 | T047-wave polish/hardening riders (all advisory, none blocking): (a) record-line late-insert CLS on the confirm card (HJ A1); (b) aria-label on name-prohibited <strong> - the raw-input a11y exposure mechanism (HJ A2, also flagged on the T046 wave); (c) entered-note copy references "the city-matched address" in the rare no-normalized-street case where that line is absent (HJ A3); (d) corner-lot "why they differ" note would help first-timers (HJ A4); (e) G5-A1 length-bound tests are vacuous (empty-features path masks the guard; exact-512 boundary untested - G3 A1/G4 A1); (f) stale lot_geometry.py module docstring (G3 A2); (g) RA-3 reason assertion non-binding or-tail (G3 A3); (h) client covers 3 of 6 error states (G4 A3); (i) credentials:omit consistency + 422 json-safety parity + title-attr length bound (G5 A1-A3) | M5-T047 gate returns | One small polish/test-hardening packet, or fold into the next confirm-arc touch | OPEN |

Sweep 2026-09-19 (double-acceptance seam, 229th+230th): M5-T047 (record-address channel) and M5-T045 (condo billing-BBL live wiring, amended contract) BOTH accepted - ten reviewer verdicts + two DCV rulings, all PASS zero blocking. DB-032 and DB-029 RESOLVED (above); +DB-033 (T047 polish riders). DB-031 remains the named successor carrying the T045-wave riders: guard-coherence (identity-notice withhold vs resolved-single allow-path once substitution is wired end-to-end - G3 A2, the load-bearing design question), route propagation of condo_resolution_report (G1 A2/G3 A1/HJ A1), cross-boundary token pin (G4 A1/HJ A2), unit-BBL resolution increment (G3 A3), substrate-record of the substitution (G3 A4), records-section heading semantics (HJ A3). Lanes: T048 (proposal contract) + T049 (matcher extraction) building.
DB-012 trip 17 (2026-09-19, M5-T045 run 49): breaker at 4 (run launched before the D-074 config landed); the 4th REVISE carried the scope finding that produced DB-031 + the ORCH disposition. Asks denied post-close; run 50 relaunches under limit 6 - the FIRST run to carry the D-074 raise.
Plan authored 2026-09-19 (D-076, owner "Plan it"): docs/PROPOSAL_EDITOR_PHASED_PLAN.md - phases A (in-flight) / B (proposal editor: B0 contract, B1 derivation, B2 rule wiring, B3 outline+heights UI, B4 wall-nudge propagation, B5 brief+compare; owner checkpoint after B3) / C (assisted PDF import, real-file corpus first) / D (DWG later, unauthorized). Scoped hold release cross-referenced in the hold notice (§2.2). Phase-B packets cite D-076:R002 and feed the loops as current lanes close; DB-031's channel design pairs with B0's contract conversation.
DB-012 trip 18 (2026-09-19, M5-T045 run 50): FIRST trip at the RAISED limit - six rounds consumed (two more than the old limit allowed) mid display-test work; remaining in-scope work is the whole-component negative tests + bounded excerpts for the DB-031 disposition; run 51 relaunched, worker edits survive. The raise is working as designed (runs get further before stopping); the after-substantive-work pattern persists at 6.
DB-012 DECISION RESOLVED (2026-09-19, D-074): the owner authorized the raise in-session ("I give u the permission to raise it to 6"), captured as D-074 BEFORE acting; applied = owner's elevated one-line copy put [limits] max_consecutive_revision_loops = 6 into the immutable config, launch manifest re-recorded (147 files, round-trip verified, verify-controller PASS). Effective at each loop's NEXT launch (live runs keep 4); harness evidence (budget counter_limits = 6) captured at the next relaunch. DB-012 leaves WATCH once that evidence lands and the DCV verifies R001.
Sweep 2026-09-19 (M5-T050 contract seam, session seq 119): DB-033 -> QUEUED(M5-T050) (all nine T047-wave riders in one bounded polish/test-hardening packet; row stays OPEN until acceptance). M5-T049 material HARVESTED at b5d3343f after runs 52-53 both died at S14 missing_checkpoint POST-edits (extraction complete in-worktree; one tagged mechanical ruff import-sort correction; 697+93+439 tests reproduced green by the orchestrator) - loop-1 lane retargets to M5-T050, T049's remaining demands are orchestrator-side (CI capture -> submit -> wave). DB-031 stays deliberately held for B0's contract shape (channel-design siblings rule, 228th-seam sweep). All other OPEN entries reviewed and stay OPEN; WATCH unchanged (DB-003, DB-008, DB-011; DB-012 harness evidence captured, awaiting DCV).
Sweep 2026-09-19 (M5-T049 acceptance seam, 231st): DB-030(f) RESOLVED (the matcher cohesion ruling delivered - named_street_override.py split into value-layer + engine modules behind a byte-identical facade; all three reviewers proved the pure extraction structurally via AST symbol comparison, zero blocking; DCV PASS under the four-condition restamp pre-authorization, all reproduced at 0c7b17f1). M5-T048 material harvested SAME seam at 0c7b17f1 (runs 13+14 both stopped at model_downgrade rotation seams post-work; all six documented suites reproduced green) - submit + five-reviewer wave next once CI lands. Loop-1 building M5-T050 run 54; loop-2 idle pending B1 contracting on the accepted B0 shape. All other OPEN entries stay OPEN; WATCH unchanged.
| DB-034 | 2026-09-19 | B0 wave riders binding the B1/B2/B3 wiring packets (all advisory at B0 - no live route reaches the validator; they become LIVE when editor input is wired): (a) GLOBAL vertex budget across base+per-level outlines (G5: worst case ~2.5e8 segment tests ~ minutes CPU with 500 per-level outlines at the per-outline ceiling) or lower per-level ceilings; (b) MAX_STRING_LEN ceiling on every user-authored string field (author, editor_version, wall id, parent_scenario_id - non_empty_string $ref has no maxLength) + bounded-repr on any embedded user value (DB-023c precedent; wall_id embeds via !r today); (c) degenerate zero-length wall via the closing-vertex index (G3: bounds-checked against len INCLUDING the repeated closing vertex, distinctness by INDEX not point) - bind when walls get consumed; (d) read-side 1.1.0-without-block leniency - the emits-1.1.0-only-when-block-present invariant is builder-side, enforce at B1; (e) one collinear/spike-outline test binding the cross==0/dot<0 reversal branch of _ring_is_simple (G4 mutation-proved unbound) - fold into the next scenario-touching packet | M5-T048 gate returns (G1/G3/G4/G5) | Bind (a)+(b) as PRECONDITIONS on the first packet that wires editor input to the validator (B1 or B3); (c)+(d) on the packet that consumes walls/emits versions (B1); (e) next scenario-touching packet | OPEN (binds B1/B3; cite with D-076:R002) |
Sweep 2026-09-19 (M5-T048 acceptance seam, 232nd): PHASE B0 ACCEPTED - the proposal contract (scenario 1.1.0 optional proposed_massing; the D-076-R002 third-input-class discipline schema-enforced) with four reviewers + DCV ALL PASS zero blocking; the one lost CI round (contracts 2/11) root-caused to the schema-vs-validator layering fact and closed by the semantically_invalid/ third fixture class ([ORCH-CORRECTED]). AS-3 wording adjudicated as the layered gate ([ORCH-SCOPE-DISPOSITION] in M5-T048-G2.md). DB-034 (above) binds the B1/B3 wiring packets. B1 (derivation) is now contractable on the accepted shape - next lane for loop-2; DB-031 channel design pairs with the accepted B0 contract. M5-T050 harvested + integrated (bcfcc538), CI web proof in flight, submit + wave next. WATCH unchanged.
Sweep 2026-09-19 (M5-T051 contract seam, session seq 119): phase B1 contracted for loop-2 on the accepted B0 shape (derivation module + DB-034(c)/(e) closures in-packet; DB-034(a)/(b) stay bound to the first editor-input packet, (d) to B2/B3 emission). DB-033 -> in review (M5-T050 four-reviewer wave running at ee1b361c). All other OPEN entries reviewed and stay OPEN; WATCH unchanged.
| DB-035 | 2026-09-19 | T050-wave residuals (all advisory): (a) the definitive PIXEL-level CLS proof for the record-note late insert (CTA Y-position byte-stable at 360/768/1280 incl. a long record address) needs a Playwright measurement in apps/web/e2e/responsive-a11y.spec.ts - outside T050's allowed_paths by design, orchestrator-side; (b) HJ optional IA refinement: move the record note to after Not-my-property but BEFORE <Meta> (restores content-before-footer reading order; only the non-interactive footer would shift) - optional, the accepted bottom placement is sound; (c) >512-char entries announce a truncated accessible name while visible text shows full length (benign edge); (d) rider-c no-street copy mildly redundant | M5-T050 gate returns (G3/G4/G5/HJ) | Fold (a) into the next e2e-touching packet as a small orchestrator-authored spec; (b)-(d) into the next confirm-arc touch | OPEN |
Sweep 2026-09-19 (M5-T050 acceptance seam, 233rd): DB-033 RESOLVED (all nine T047-wave riders closed - every originating reviewer confirmed its own riders CLOSED on this wave; HJ explicitly accepted the rider-a bottom placement); +DB-035 (above, the wave residuals). Lanes: loop-2 building M5-T051 (phase B1 derivation, run 15); loop-1 idle - next packet DB-031 (multi-lot condo records-view successor, now contractable against the ACCEPTED B0 contract shape per the channel-design-siblings rule). WATCH unchanged.
Sweep 2026-09-19 (M5-T052 contract seam, session seq 119): DB-031 -> QUEUED(M5-T052) (records-view channel + display, guard-coherence ruling required FIRST; the T045-wave riders G1-A2/G3-A1/A3/A4/G4-A1/HJ-A1/A2/A3 all in-scope; row stays OPEN until acceptance). Contracted against the ACCEPTED B0 contract shape per the channel-design-siblings rule (no competing shapes: the route carries the accepted resolver's own report shape, no schema change). Lanes: loop-1 -> M5-T052 (run 55 next), loop-2 building M5-T051 run 15. All other OPEN entries reviewed and stay OPEN; WATCH unchanged.
Sweep 2026-09-19 (M5-T053 contract seam, session seq 119): DB-034 items (a)+(b) -> QUEUED(M5-T053) (the B3-scaffold slice-1 input gate + stateless validation route; the disjointness-correct wrapper design keeps proposal.py with the live M5-T051 lane; items (c)/(e) already closed in-flight by M5-T051's packet, (d) stays bound to B2/B3 emission). M5-T052 material HARVESTED at 46bf1105 (runs 55-56 through the raised breaker - DB-012 trip 19, same after-substantive-work pattern at 6; guard-coherence ruling documented first as contracted; the producer's routed known-red consumer fix applied as the tagged [ORCH-CORRECTED] commit) - CI in flight, submit + six-reviewer wave next. All other OPEN entries reviewed and stay OPEN; WATCH unchanged.
| DB-036 | 2026-09-19 | T052-wave riders (all advisory, none blocking): (a) SANITIZER PRECONDITION on the recorded-zoning propagation packet - recorded_zoning flows through boundedToken whose [A-Za-z0-9._-] charset strips the '/' in special mixed-use districts (M1-5/R7-2 class); dormant today (field universally null) but corrupts silently the moment ZTLDB/spatial zoning lands - add a boundedZoningDistrict helper (the boundedTimestamp precedent) BEFORE that wiring (G1-4a + G5-1, independently converged); (b) 'Condo lot you entered' label binds to billing_bbl and renders 'not recorded (unknown)' for a real unit-BBL input - parse entered_bbl in the client and label billing separately (HJ-1); (c) dangling 'professional-review determination above' reference when the withheld panel shows no such label (HJ-2); (d) COUPLING TEST/DECISION when substrate-substitution wiring stamps scenarios with the base-lot BBL: AnalysisIdentityNotice vs the substitution record read independent signals and could contradict on the brief (HJ-3 - bind to that future packet); (e) client parses neither recorded_zoning_status nor recorded_zoning_dependency (the named ZTLDB gap never surfaces; divergent notice shows even with no zoning recorded) (HJ-4); (f) records-section headings are <strong> not h2/h3 - absent from the heading outline (HJ-5, carried from T045); (g) retrievedAt round-trip not value-pinned at either client level - a selective boundedTimestamp revert would escape the suite (G4-1); (h) 422 detail.raw_value repr uncapped - length-cap as defense-in-depth (G5-2) | M5-T052 gate returns | Bind (a)+(d) as PRECONDITIONS on the zoning-propagation / substrate-substitution packets respectively; fold (b)/(c)/(e)/(f)/(g)/(h) into the next condo-surface or records-view touch | OPEN (cite (a) with D-073:R006) |
Sweep 2026-09-19 (M5-T052 acceptance seam, 234th): DB-031 RESOLVED - the multi-lot condo records view is real (per-BBL channel on the accepted resolver's own shape + the ONE deriveCondoSurface decision on both screen and brief under the unchanged fail-safe; the T045 G3-A2 guard-coherence question ANSWERED and test-proven; the T045-wave riders G1-A2/G3-A1/A3/A4/G4-A1/HJ-A1/A2/A3 all closed in-scope). Five reviewers + DCV PASS; the G3 PASS-with-required-corrections cycle (two false report claims) closed with all five delta-attestations at the re-frozen head; one lost CI round (the boundedToken timestamp corruption, 2/1151) root-caused + fixed tagged. DB-036 (above) carries the wave riders incl. the slash-district sanitizer PRECONDITION on the future zoning-propagation packet. Lanes: loop-1 M5-T053 run 57 (validation route, submit-stage checkpoints), loop-2 M5-T051 run 16 (derivation). WATCH unchanged.
Sweep 2026-09-19 (M5-T053 acceptance seam, 235th): DB-034 items (a)+(b) RESOLVED - the input gate + stateless validation route wire editor input to the B0 validator with the G5 gate formally ruling BOTH its originating T048 findings CLOSED (worst case 5e6 segment tests vs the ~2.5e8 admitted before; dual-layer string bounds). Three reviewers + DCV ALL PASS zero blocking, first CI round. Wave residuals (route 400-cap truncation branch untested, 500-path untested, surrogate half of the strict-JSON guard untested, shared-flag coupling note) fold into the next route-touching packet. DB-034 (c)+(e) closed by M5-T051 (in review); (d) stays bound to B2/B3 emission. T051-wave G5 adds two NEW wiring preconditions to the DB-034 class: a LotContext list-size ceiling (unbounded lot-line/street-line lists = the DoS axis once derive_proposal is wired) and caller-coordinate finiteness guards (a NaN coordinate can silently reach an EvidenceRecord) - BIND BOTH on the packet that wires derive_proposal (B2/B3), alongside the area_sq_ft repr instance for the DB-034(b) fold-in. WATCH unchanged.
Sweep 2026-09-19 (M5-T051 acceptance seam, 236th - the session's closing seam): PHASE B1 ACCEPTED (the deterministic proposal-derivation module: facts with EvidenceRecords, never allowances; hand-computed fixture pack independently RE-COMPUTED by the G4 reviewer; DB-034(c)+(e) RESOLVED at this acceptance). Three reviewers + DCV ALL PASS zero blocking, first CI round. The G4-1 api-count stale-base correction recorded in G2 ([ORCH-CORRECTED]); the G5 wiring preconditions (LotContext list ceiling + coordinate finiteness + the area repr instance) already recorded at the T053 seam - BIND on the B2/B3 packet that wires derive_proposal. Session seq 119 closes with SIX acceptances (231st-236th: T049 extraction, T048 B0 contract, T050 polish, T052 records view, T053 validation route, T051 derivation); next lanes: B2 rule wiring (needs B1 - now accepted; rules-engineer producer per the plan) and B3 UI; DB-035/DB-036 rider clusters; the owner release pass STILL PENDING (one ~10-minute sitting, release-request section 8+). WATCH unchanged.
Sweep 2026-09-20 (D-077 three-lane contract seam, session seq 121): OWNER DIRECTIVE D-077 captured (release pass now + three loop lanes for the remaining MVP). Three packets contracted/claimed/launched at 92b0d265 (G0 pairwise disjointness EMPTY x3): M5-T054 = phase B2 rule wiring (rules-engineer; FIRST production caller of derive_proposal - the T051-G5 preconditions incl. LotContext list ceiling + coordinate finiteness + area repr AND the DB-034(d) emission disposition bind IN-PACKET -> DB-034(d) QUEUED(M5-T054)); M5-T055 = DB-035 rider cluster -> DB-035 QUEUED(M5-T055) (note: responsive-a11y.spec.ts is the EXISTING M2-T002 S6 suite - the packet EXTENDS it); M5-T056 = DB-036 riders (b)/(c)/(e)/(f)/(g)/(h) -> QUEUED(M5-T056), while DB-036(a) slash-district sanitizer stays OPEN bound to the zoning-propagation packet and (d) to substrate-substitution. Loops 1/2/3 relaunched fresh run-ids (58/19/05); loop-3 audit fork (dup seq 13) archive-repaired; loop-2 unit_dispatch_unreconciled reconciled (T051 fully accepted = effects known). B3 UI contracts at the B2 seam per the plan order. All other OPEN entries reviewed and stay OPEN; WATCH unchanged.
| DB-037 | 2026-09-20 | T054-wave outputs binding future packets: (a) G5 BP-1..BP-7 (M5-T054-G5.md) = the route-boundary preconditions - QUEUED(M5-T057) which enforces all seven at the check endpoint; BP-7's authn/tenancy half stays OPEN for the public-exposure packet; (b) G4-1..G4-4 advisory test bindings - QUEUED(M5-T057) scope 3/3; (c) rear-lot-line designation as a future caller input to B1 derivation (unlocks a TRUE commensurable yard check; today the yard check is honestly COULD_NOT_CHECK - the AS-1 disposition) - bind to a future derivation-extending packet with G6-aware design (designating a rear lot line is a legal interpretation, so the input must be a professional designation, never inferred); (d) multi-lot condo SITE-DEFINITION flow (owner conversation 2026-09-20, live 298 Wallabout evidence: billing 3022647515 -> multi_lot_set base lots 3022640032/3022640033): show the parcels, professional confirms the zoning-lot assembly, compute on the confirmed combined land with the confirmation recorded - pairs with the substitution-stamp packet (recon: project-control/reports/condo-substrate-substitution-recon-2026-09-20.md) and the DB-001 split-lot class | M5-T054 gate returns + owner conversation | (a)/(b) land with M5-T057's wave; (c) future derivation packet; (d) contract after T056 accepts (surface conflicts recorded in the recon section 5) | OPEN |
Sweep 2026-09-20 (M5-T054 acceptance seam, 237th): PHASE B2 ACCEPTED (proposal-conditioned rule checks; first derive_proposal caller; the T051-G5 preconditions + DB-034(b) instance CLOSED in-code and G5-ruled; DB-034(d) disposition recorded - the emission invariant rides to the emitting packet). Four reviewers + DCV ALL PASS zero blocking, first CI round; the AS-1 yard-commensurability [ORCH-SCOPE-DISPOSITION] concurred by all four reviewers (G2 record). DB-037 (above) carries the wave outputs. Lanes: T055 + T056 harvested and submitted at db0cf0f8 (T055 five-reviewer wave RUNNING; T056 wave next), CI on both materials in flight; M5-T057 (B3 route slice) contracted at this seam for loop-1. WATCH unchanged.
| DB-038 | 2026-09-20 | T055/T056-wave residuals (all advisory, none blocking): (a) G5-1 MEDIUM PRE-EXISTING - the condo-records 422 `message` field echoes caller input UNCAPPED (root: app/connectors/bbl.py f-strings :106/:113/:137; web re-bounds at 600; route flag-gated) - PRECONDITION-grade fold-in for the FIRST packet allowed to touch connectors/bbl.py or the condo error body; (b) stale test labels (address-confirm S11 describe still says DB-033 a-d,i; api docstring still calls the divergent notice "permanent/stays visible") - fold into next touches of those files; (c) recorded-zoning branch dormant + divergent notice suppressed live until zoning propagation (HJ A1/A2) - reword the docstring + revisit gating AT the zoning-propagation packet (with DB-036(a)); (d) uniform reflected-text bound if enteredInput ever reaches a persistent/shared/logged/LLM sink (G3-2/G5-2) - bind on that future sink; (e) CLS-spec coupling note: .architect-map-status rename would time out visibly (fail-safe) - carry with any LotOutlineMap status-line rename; (f) billing-class label redundancy (HJ A4) + substitution allow-path entered fallback via boundedBbl vs validateBblInput (G3-1) - fold into the substitution/site-definition packets (D-078) | M5-T055 + M5-T056 gate returns | (a) first bbl.py-touching packet; (c) zoning propagation; (f) D-078 packets; rest fold-ins | OPEN |
Sweep 2026-09-20 (M5-T055 + M5-T056 double acceptance seam, 238th + 239th): DB-035 RESOLVED (all four riders closed; the CLS pixel proof survived a REAL CI catch - the spec detected a 27px environmental settle race, root-caused to the LotOutlineMap status line, fixed [ORCH-CORRECTED per G4-1 + CI 35493851185] with four peer delta-attestations + the DCV extension; run 35494571792 green). DB-036 riders (b)/(c)/(e)/(f)/(g)/(h) RESOLVED (HJ formally closed its four originating T052 riders; guard coherence preserved); (a) slash-district sanitizer and (d) substitution coupling stay OPEN bound to their packets. DB-037(a)/(b) riding with M5-T057 (loop-1 building now). DB-038 (above) carries the wave residuals. D-078 capture recorded; its packets contract NOW that T056 is accepted (R003 sequencing satisfied). WATCH unchanged.
Sweep 2026-09-20 (M5-T057 harvest + D-078 pair contract seam, seq 122): M5-T057 harvested at the revision-loop breaker stop (unit complete; supervisor-recorded 'no scoped code defect'; orchestrator-captured suites ruff 0 / api 540 / rules 726 / modularity 0 with explicit cwd; material cherry-pick 3dbd056d; submit + independent wave at the CI-green head next). D-078 pair contracted at this seam (R003 sequencing satisfied): M5-T058 substitution stamp (loop-2) - DB-036(d) QUEUED(M5-T058); M5-T059 site-definition confirmation slice 1 (loop-3) - DB-037(d) QUEUED(M5-T059) for the record substrate (confirm UX + multi-parcel map + calculations-on-combined-land are later slices; platform verified STATELESS today - store ships as ABC + in-memory under the B-001 deferral precedent, route unmounted); DB-038(f) both riders QUEUED(M5-T059). Registry binds: D-078 R001-R003, D-077 R002/R003, D-066 R001, digest resyncs same commit; pairwise disjointness EMPTY x3 (T057xT058, T057xT059, T058xT059) recorded in both G0 reports. WATCH unchanged.
| DB-039 | 2026-09-20 | M5-T057 wave advisories (all non-blocking, route to B3 slice 2 / the public-exposure packet): (a) G3-F5 _derive_enum_domains recomputed per request over the whole registry - memoize beside the registry cache; (b) G3-F6 import-time vocabulary guard fails the WHOLE app at boot (blast radius exceeds the flag) - docstring note; (c) G3-F7 three private cross-module imports (_bounded_message/_declared_content_length/_read_body_within_ceiling) - promote to a shared app/api/v1/_request_bounds.py at public exposure; (d) G5-F3 registry numeric InputSpec bounds (minimum/maximum/exclusive_*) unapplied at the boundary (huge/negative numerics fed; evaluator fail-closed) - extend to _derive_input_domains; (e) G5-F5 domain check ordered after the expensive input gate - reorder beside the type check; (f) G5-F6 module-global registry seam carry-forward (T054 G5-5; benign); (g) G4 gap 3 at-cap-exact coverage for walls/street-lines/label/body boundaries + gap 4 _build_lot_context shape-refusal tests | M5-T057 G3/G4/G5 gate returns | B3 slice 2 (a,b,d,e,g); public-exposure packet (c); watch (f) | OPEN |
Sweep 2026-09-20 (M5-T057 acceptance seam, 240th, seq 122): T057 accepted at 2fbd661e after a FULL rework cycle - first wave G3 FAIL/G4 PASS/G5 FAIL/DCV FAIL(D-066-R001 regen conjunct) at frozen 2b5edbc3; one tagged [ORCH-CORRECTED] cluster ca5c8895 (field/id/provenance/fact-key bounds, outline-position cap + threadpool offload, 12 binding tests, honesty corrections incl. the withdrawn D-066 evidence claim); delta re-attestation by the SAME four reviewers ALL PASS at a0cb3b79; fresh DCV pre-auth reproduced at the accept head. DB-039 EXTENDED with the delta-wave advisories: (h) G3-delta charset asymmetry - street wall_id now charset-bound but exterior_walls[].id is only 512-capped (a matching pair can diverge; slice-2 contract alignment); (i) G3-delta one-comment note - registry resolution must STAY before the threadpool hop (lazy global race hazard if moved); (j) G4-delta F-2 - the BP-1 source-grep clause 'check_proposal,' is satisfied by the import block alone (spy owns the property; optional tightening); (k) G5-delta F7 - 200-path caller strings are length-bounded but not charset-bounded via block wall ids (<=512, DB-034(b) charset-free) and provided_provenance values (<=2048B); report sentence 'no unbounded/markup caller string' overbroad on markup - correct at slice 2 with the render-escaping obligation (T054 G5-4 lineage); (l) G5-delta F8 - ~9.3s threadpool CPU per at-cap request; rate limit + per-request timeout ride the public-exposure packet with authn/tenancy. M5-T059 harvested (loop-3 breaker close; material 48fe290b cherry-picked at this seam; suites ruff 0/site_def+api 578/rules 726/modularity 0) - submit + wave after CI at the pushed head; its ACCEPT stays sequenced AFTER M5-T058 per D-078-R003. Loop-2 (T058) building on run 22 after a run-21 CLI startup-hang recovery (clear-recovery drill; single retry rule - recurrence goes to /deficit-convergence). WATCH unchanged.
| DB-040 | 2026-09-20 | M5-T059 wave + delta advisories (all non-blocking; blocking C1/C-1/C-2 CLOSED in-packet by the [ORCH-CORRECTED] cluster): MOUNT-PACKET PRECONDITIONS (the slice-2/mount packet MUST close these before the main.py mount line): (a) G3-delta F-9 + G5-delta INFO - revoke checks status before the condo binding (409 vs 404 = a cross-property status oracle; swap the checks so all cross-property probes read 404); (b) G3-delta F-10 + G4-delta A-9 - revoke now requires a LIVE multi-lot resolution, so a degraded resolver blocks the only human withdrawal path (fail-direction wrong per the D-051 lesson; bind on the stored record's condo_key w/o requiring the multi-lot outcome, degraded-case tests); (c) G5 F2 - length caps on note (~2000) + confirmer.name (~200) + list-result cap; (d) G5 F3 - chain-depth/list caps + condo_key index (O(n2) today); (e) G5 F4 - record-id collision guard in _record_insert (typed refusal on existing id); (f) G5 F5 - read-authorization posture for confirmer name/note (self-attested personal data) decided with B-001 auth; (g) G5 F6 - confirmation free text is untrusted at any future AI-drafting boundary; (h) G5 F7 - uuid4 ids are NOT capabilities (never build access control on them); (i) G3 F-2/A-6 - parcel-shape refusals misnamed invalid_confirmer (distinct error class); (j) G3 F-4 - condo_key-None write/read asymmetry (+ test); (k) G3 F-5 - SITE_DEFINITION_STATUS_STATE_MATRIX unenforced (bind emissions); (l) G3 F-6 - raw fractional-second timestamp inside the decimal-gated records section (format or annotate the gate before the confirmed branch is reachable); (m) G3 F-7 - unit-class entry has no write path (close at the confirm-button slice); (n) G4 A-1..A-8 (flag-off sentinel on 3 more routes, streaming-413 branch, billing_bbl provenance conflation + entered!=billing fixture, silent parcel drop in boundedParcels, named 298-Wallabout fixture not exercised end-to-end, frozen-ness/isolation gaps, brittle substring guards); (o) HJ-1/2/4/7 to the slice-2 packet (dormant-branch copy, jargon line, supersession detail, zoning-lot legal framing) + HJ-3/5/6/8/9/10/11 backlog (role token display map, provenance attribution, recorded-vs-current parcel labeling, h3 headings, refusal visual weight, unconditional calculation-posture copy, history-clause gating); (p) DCV F-6 - the seven-vs-eight unchanged-path arithmetic in SS12.1/coordinator notes | M5-T059 wave + delta returns | slice-2/mount packet (a)-(m,o); backlog fold-ins (n,p) | OPEN |
Sweep 2026-09-20 (M5-T059 delta-wave close, seq 122): T059 FULLY REVIEWED at the corrected identity 03882888 - delta wave 5/5 PASS (G3 C1 closed both layers + its F-3/F-8 closed; G4 C-1/C-2 closed mutation-proven; G5 F1 closed, all rulings carry; HJ blob-level carry; DCV fresh rulings + fresh pre-auth). ACCEPT parked on the D-078-R003 order: M5-T058 (loop-2 run 24, building) accepts FIRST. DB-040 (above) carries the wave advisories. WATCH unchanged.
| DB-041 | 2026-09-20 | M5-T061 wave advisories (all non-blocking; every blocking DB-039 item closed in-packet): (a) G4 G-1 - clear_input_domain_cache has no mutation-pinning test (test-only helper; probe-verified working) - fold into the next route-touching packet; (b) G4 G-2 - numeric out-of-bounds refusal ordering covered transitively via the shared call site, not independently spy-asserted - same fold-in; (c) G5 F-1 - _bounded_field caps length but not control chars (unexploitable today: every user-derived field component is charset-pre-validated) - keep the invariant explicit at the public-exposure packet; (d) G3 OBS-2 - refusal precedence changed for multi-defect payloads (domain refusal now wins; intentional per DB-039(e), documented); (e) G3 OBS-3 - non-string exterior_walls[].id deferred to the B0 validator (intentional; equal-string pairs aligned) | M5-T061 gate returns | (a)/(b) next route-touching packet; (c) public-exposure packet; (d)/(e) recorded dispositions | OPEN |
Sweep 2026-09-20 (M5-T061 acceptance seam, 241st, seq 123): T061 ACCEPTED first CI round - DB-039 items (a)/(b)/(d)/(e)/(g)/(h)/(i) RESOLVED (G5 ruled each CLOSED with the route's threat surface walked end-to-end incl. an independent adversarial numeric-bypass probe; G3 traced the ordering claim in code; G4 mapped all six AS to mutation-sensitive tests); DB-039 stays OPEN for (c) public-exposure promotion, (f) watch, (j)/(k)/(l) slice-2/public-exposure items. The harvest pattern was the seq-122 class again x2: BOTH loop-2 and loop-3 closed at the S13.8 consecutive_revision_loops breaker with the in-loop reviewer demanding supervisor-owned explicit-cwd evidence (the workers' in-run ruff/pytest outcomes were cwd artifacts; T061's six REVISE cycles + T058's rev7 evidence-handoff) - the orchestrator harvest supplied digest-bound transcripts and all three T061 reviewers + DCV independently reproduced green. DB-041 (above) carries the wave advisories. M5-T058 submitted at the CI-green corrected head 817a6546 (material 7d02eddc + the [ORCH-CORRECTED] frozen-contract guard admission of 1.2.0, M5-T037 precedent); its G3/G5 returned PASS (G3 F1 LOW stale 1.0.0 docstrings in rule_evaluation.py routed to the orchestrator as a follow-up doc fix; G5 F-1 LOW provenance-string maxLength defense-in-depth for a future additive contract task), G4 running. T059 accept next once T058 accepts (D-078-R003; DCV section 7 pre-auth). Loop-1 building T060 run 64 after a silent-start single-retry. WATCH unchanged.
| DB-042 | 2026-09-20 | T058 HJ advisories (all non-blocking; G3/G4/G5/DCV zero corrections): (a) HJ-1 - mixed_substrate split (base-lot geometry vs billing-lot PLUTO identity) is machine-readable only; DevelopmentLimits shows a billing-lot reference FAR beside a base-lot evaluated FAR with no on-screen caveat - product discovery for human surfacing (recon Q5 made it machine-readable deliberately); (b) HJ-2 - the condo_base_lot_unresolved refusal names site confirmation as the next step but no live control exists until the T059 slice-2/mount packet - offer the informational path (condo-records base-lot list) from the refusal surface at that packet; (c) HJ-3 - OutcomeAnnouncer copy trails the visible label (generic missing_evidence/applicable_draft; lib/rule-evaluation.ts is outside the packet) - fold into the next announcer-touching packet; (d) HJ-4 - two parallel base-lot surfaces (identity notice vs condo-records channel) are redundant but fail-safe; (e) HJ-5 - billing/base jargon without inline definition (acceptable for the professional audience); plus G3 F1 - stale 1.0.0 docstrings at rule_evaluation.py lines 21/255 (orchestrator-routed doc fix, fold into the next rule_evaluation touch); G5 F-1 - maxLength bounds on source-derived provenance strings as a future additive contract task | M5-T058 HJ/G3/G5 returns | (b)+(a) bind to the T059 slice-2/mount packet (with DB-040); (c)/(d)/(e) fold-ins; G3-F1 next rule_evaluation touch; G5-F1 future contract task | OPEN |
Sweep 2026-09-20 (M5-T058 + M5-T059 DOUBLE acceptance seam, 242nd + 243rd, seq 123): THE D-078 PAIR IS LIVE-COMPLETE AT THE RECORD LAYER - T058 (substitution stamp, contract 1.2.0) accepted with G2-G5 + HJ + DCV all PASS zero required corrections (the DCV ruled both flagged defects clean: the [ORCH-CORRECTED] guard admission SANCTIONED, the uncommitted-worktree cherry-pick HONESTLY-RECORDED); T059 (site-definition confirmation slice 1) accepted immediately after under its DCV delta section-7 pre-auth (all four conditions reproduced at bc703fcc: 14-path zero-diff vs 03882888, digests carried, validator 0 settled, T058-first). DB-036(d) closure re-ruled correct by three reviewers (the correspondence-gated stampLegitimate guard never relaxes the withhold). DB-037(d) slice 1 delivered (record substrate); row stays OPEN for the confirm-UX/map/combined-land slices behind the DB-040 mount preconditions. DB-038(f) riders rode T059's accepted wave. DB-042 (above) carries the T058 HJ advisories. Lanes: loop-1 building T060 run 64; loops 2/3 idle pending re-feed (candidates: T059 slice-2/mount under DB-040 (a)-(p) + DB-042 (a)/(b), zoning propagation w/ DB-036(a) sanitizer precondition, or the B4/B5 plan order). WATCH unchanged.
| DB-043 | 2026-09-20 | T060 wave advisories (all non-blocking; five verdicts PASS zero corrections): (a) HJ-2 MEDIUM a11y - no focus management on row DELETION in the proposal editor (Delete button unmounts under focus; the CODING_RULES nonce+useEffect remedy applies; e2e never deletes by keyboard) - highest-value fold-in for the next editor touch; (b) G3-F1 + HJ-10 - a saved variation's stored draft can diverge from its later-updated report (snapshot draft-at-check-time in a follow-up); (c) HJ-1 - EPSG:2263 labeled but not explained in plain language; (d) HJ-3 - boundary refusals lack an edit-and-rerun next-step + draftIsRunnable not wired to gate the Run button (doomed degenerate-outline round-trip); (e) HJ-4/5 - shortfall phrasing says "short" for maximum overages (AS-pinned; needs a coordinated AS+server-field rethink) and the PASS note ignores direction; (f) HJ-6 - "coverage" vocabulary overload; (g) HJ-7 - failure cards omit the captured correlation id (support reference); (h) HJ-8 - heading-level skip in the report region; (i) HJ-9 - variation compare omits numeric deltas; (j) G4 F1 - mirror numeric-value drift unguarded (add cheap value-pin asserts), F2 e2e happy-path-only, F3 dead fixture export, F4 recoverable-outcome partial assertions, F5 no flag-off negative for the proposal view; (k) SEC F-1/F-2 - boundedText relies on React escaping (keep the no-raw-HTML-sink invariant visible) + fetchImpl stays in the production signature (test-only discipline) | M5-T060 wave returns | (a)/(b)/(d)/(j) one editor polish/test-hardening packet AFTER the owner checkpoint; (c)/(e)/(f)/(g)/(h)/(i) fold into the same or the B4 touch; (k) standing invariants | OPEN |
Sweep 2026-09-20 (M5-T060 acceptance seam, 244th, seq 123): PHASE B3 ACCEPTED - the proposal editor UI (numeric-2263 table-first editor + full-matrix client + arithmetic-pinned report + ephemeral variations + keyboard e2e) with five verdicts PASS zero required corrections (G3 value-by-value mirror-vs-route reproduction; G4 all-seven-AS mapping; SEC eight-surface walkthrough; HJ full journey w/ ten advisories; DCV all five requirements on reproduced evidence w/ the thin-client evidence chain ruled HONEST). **THE D-076-R003 NAMED POST-B3 OWNER-REVIEW CHECKPOINT IS REACHED**: the owner review is QUEUED; B4+/map-drawing/PDF-import/emission work is NOT contracted on this acceptance's authority (DCV section-7 sequencing flag; D-080-R003 wait-for-owner rule). DB-043 (above) carries the wave advisories - the editor polish packet contracts AFTER the checkpoint. Lanes: loop-1 IDLE pending the owner checkpoint (no released non-B candidate is currently free of the live T062/T063 surfaces); loop-2 building T062 run 27; loop-3 building T063 run 09. WATCH unchanged.
Sweep 2026-09-21 (M5-T066 contract + M5-T065 submit seam, seq 124): M5-T066 contracted at c40c60db (map-CLICK interaction slice per the recorded T065 [ORCH-SCOPE-DISPOSITION]; map surface IN scope; claim-then-hold - launch waits for the T065 accept over three shared files); DB-043(a) focus remedy carried as a T066 binding input (T065 applied it from the start). T065 SUBMITTED at 65faee54: the d3793c2e CI red root-caused NOT to material but to the SHARED e2e tabUntil helper false-matching <body> textContent at the Tab wrap ([ORCH-CORRECTED] a3173987, outside the task surface; latent for every wrapping textContains journey; CI 35554705276 green). T062 G3/G4 CLI gate records made from the seq-123 verbatim PASS reports (identity notes; task surface byte-stable across disjoint peers); T062-G5 + T062-DCV + T063-DCV re-dispatched fresh post-reset. Loop-1 relaunched run-66 (T064 mid-build preserved, both ask stores swept); loop-3 retargeted to T066 run-13 HELD; loop-2 idle pending the T062 accept. All other OPEN entries reviewed and stay OPEN; WATCH unchanged.
| DB-044 | 2026-09-21 | M5-T063 wave advisories (all LOW/non-blocking; every blocking finding closed in-packet at 853a1d25): (a) HJ-1 - CONDO_BASE_LOT_UNRESOLVED_ANNOUNCEMENT (rule-evaluation.ts:517-518) enumerates two of the three documented causes (omits upstream error); generalize to "did not resolve to a single recorded base lot" at the next announcer touch; (b) HJ-2 - residual two-surface redundancy (AnalysisIdentityNotice substitution branch + CondoRecordsChannelSection render the same base-lot fact); eliminating one needs the forbidden T060-lane edit - product discovery; (c) HJ-3 - recorded/mixed zoning states DORMANT in prod until ZTLDB lands (only the "none" state live); revisit copy gating AT the zoning-propagation packet (with the DB-038(c) note); (d) HJ-4 - RuleEvaluationPanel consumes the enriched substitution announcement without an equally-prominent visible identity notice (legacy surface SR/visible prominence asymmetry); (e) G5 F-1 - analyzed_bbl is client-validated as non-empty string only (BBL-shape pattern lives in out-of-scope rule-evaluation-contract.ts); inert in the escaped aria-live sink; add client-side shape validation at the next contract/announcer touch | M5-T063 gate returns | (a)/(d)/(e) next announcer/contract touch; (b) product discovery; (c) zoning propagation | OPEN |
Sweep 2026-09-21 (M5-T063 acceptance seam, 245th, seq 124): T063 ACCEPTED at 9a3d4883 after the full FAIL->fix->delta cycle (G3-F1/G4-C-1 the SAME announcer-correspondence defect independently; [ORCH-CORRECTED] 853a1d25; all five verdicts + fresh post-reset DCV PASS with validator EXIT 0 confirmed at the accept seam). DB-036 FULLY RESOLVED - (a) the slash-district sanitizer landed as boundedZoningDistrict (bounded.ts:90-99, the M1-5/R7-2 class preserved for display) and (e) closed here; (b)/(c)/(f)/(g)/(h) closed at the 239th, (d) at the 242nd. DB-038(b)/(c) closed here ((a) bbl.py precondition, (d) future-sink bound, (e) rename-carry stay OPEN). DB-042(c)/(d)/(e) closed here ((a)/(b) stay bound to the T059 slice-2/mount packet; the api-side G3-F1 stale docstrings + G5-F1 provenance maxLength stay). DB-044 (above) carries the T063 wave advisories. WATCH unchanged.
Sweep 2026-09-21 (M5-T062 acceptance seam, 246th, seq 124): T062 ACCEPTED at e9283558 (the DB-040 api mount-hardening slice; all five gates + fresh post-reset DCV PASS; the seq-123 G3/G4 verbatim reports CLI-recorded with identity notes; G5 arrived in a two-part transmission joined verbatim). DB-040 api-side preconditions (a)-(e), (i)-(l), (n-api) CLOSED at this acceptance (G5 walked the revoke scope-before-status closure, the fail-closed matrix, caps, collision guard, chain-depth, degraded-safe revoke; DCV reproduced AS-1/AS-2 in source). DB-040(f) auth stays DEFERRED behind the default-OFF flag (B-001-bound); (m)/(o)/(p) UI-side items stay for the confirm-UX slice. DB-040 EXTENDED: (q) G5 LOW-1 - supersede retains the status-before-scope oracle (store.py:312-337 checks ACTIVE before the condo_key binding; reorder to mirror revoke) and (r) G5 LOW-2 - revoke's two not-found branches differ in message TEXT (existence oracle given a known uuid4 id; emit identical messages) - both route to the follow-up auth/hardening packet with the G3 F1/F2 auth LOWs. Loop-2 re-feed contracts next. WATCH unchanged.
| DB-045 | 2026-09-21 | M5-T065 wave advisories (five verdicts PASS; none blocking): MOUNT-SEAM CLUSTER - (a) G3-F1 real-lot ring-densification efficacy: the bridge needs equal vertex counts + 2ft RMS between the two connectors' rings; differently-densified real-lot rings would refuse fail-safe - validate against real parcel pairs BEFORE mounting; (b) SEC-A1 flag-reuse + no per-request authN/rate-limit - the mount seam must consciously confirm exposure + consider rate limiting; (c) G4-gap2 server (500, internal_error) branches unproven (ring_crs_mismatch seam test recommended); (d) HJ-3 the drawing surface renders ungated on the bridge being mounted (Convert dead = feature_unavailable in every real env) - gate the visible surface on the mount or land the mount first. T066-BOUND (folded into the packet at this seam): (e) HJ-1 MAJOR copy-vs-gesture mismatch ('Place points over the lot below' describes a map gesture that is numeric entry until click lands); (f) HJ-2 MEDIUM dangling walls on adoption - adoptOutlineVertices replaces only vertices while DraftWall keeps start/end_vertex_index (orchestrator-verified; proposal-draft.ts widened INTO T066); (g) HJ-4 no visible >=3-points hint while Convert disabled; (h) G3-F3 setPendingFocus-inside-updater idiom. ADOPTION/TEST-HARDENING BACKLOG: (i) G4-gap1 MODERATE client browser-fault branches untested (network_error/client_timeout/aborted/over-budget); (j) G4-gap3 smaller request/UI branches; (k) G3-F2 outline_bridge.py 826-line review_signal - split before any growth; (l) HJ-5 no dedicated Retry control (T060 parity); (m) SEC-A2 bounded self-BBL reflection (posture note) | M5-T065 wave returns | (a)-(d) the future mount seam packet; (e)-(h) M5-T066 (folded); (i)/(j) next bridge/editor test touch; (k) standing modularity watch | OPEN |
Sweep 2026-09-21 (M5-T065 acceptance seam, 247th, seq 124): T065 ACCEPTED at a3b62c90 (map-drawing input slice: keyboard vertex entry + correspondence bridge w/ necessary-not-sufficient trust rule + additive adoption; five verdicts PASS - G3/G4/HJ/SEC/DCV all fresh full-report transmissions joined at exact truncation points; the honest click-to-place scope gap governed by the recorded [ORCH-SCOPE-DISPOSITION] and every reviewer explicitly concurred). DB-043(a) confirmed applied-from-the-start (HJ formally closed its T060 HJ-2 MEDIUM here). DB-045 (above) carries the wave advisories; the T066 packet is widened at this seam with proposal-draft.ts + its test and binding inputs for (e)/(f)/(g) before the loop-3 launch. WATCH unchanged.
| DB-046 | 2026-09-21 | M5-T064 wave advisories (five verdicts PASS; none blocking): TEST-HARDENING CLUSTER -> QUEUED(M5-T068): (a) G3-1 multi-floor (>100ft) height path not driven through the full generator-checker proof; (b) G3-2 _conflict_advisory populated branch untested (needs a conflict-producing registry double); (c) G3-3 _LOT_AREA_INPUT redefined without a drift-guard vs the checker's constant; (d) G4-1 MEDIUM the generator-checker FAIL-CLOSED raise untested (monkeypatch check_proposal->FAIL, assert MaxEnvelopeError field=candidate.*, + the route's 500 mapping); (e) G4-2 MEDIUM the (500, internal_error) matrix row never exercised live (force one 500, assert bounded generic body + correlation header); (f) G4-3 LOW-MED out-of-EPSG:2263-bounds lot branch untested. BACKLOG/WATCH: (g) G4-4 other typed fail-safe branches (LOW; several construction-guaranteed); (h) G4-5 cross-process determinism observation; (i) SEC informational area_sq_ft repr echo (bounded, flag-gated - posture note); threadpool sizing + per-request timeout + authn/tenancy ride the public-exposure packet (SEC finding 4/5 dispositions); the max-envelope MOUNT + max-renders-FIRST UI surfacing (D-082-R003) contract AFTER the T066 lane frees the editor surface | M5-T064 gate returns | (a)-(f) M5-T068 (contracted this seam); (g)/(h) watch; (i) public-exposure packet | OPEN |
Sweep 2026-09-21 (M5-T064 acceptance seam, 248th, seq 124): T064 ACCEPTED at 715cbaa1 (max-envelope engine slice 1 - the D-082-R002 maximum-first capability: deterministic derivation w/ binding-rule provenance, honest typed gaps, and a GENUINE generator-checker consistency proof reusing the accepted checker; five verdicts PASS incl. the fresh G5's twelve-area walkthrough at zero defects; the run-65 opus-kill -> run-66 resume -> breaker-close harvest arc recorded end-to-end). DB-046 (above) carries the wave advisories; M5-T068 (test-hardening a-f, test-only) contracts at this seam for loop-1. WATCH unchanged.
Sweep 2026-09-21 (M5-T067 acceptance seam, 249th, seq 124): T067 ACCEPTED at 273e3ae0 (DB-040 (q)+(r) RESOLVED - supersede scope-before-status + revoke message unification, both proven by the G4 reviewer's LIVE mutant harnesses elevating the producer's honest [PREDICTED] claims to OBSERVED; G5 judged both closures as the original finder). DB-040 EXTENDED: (s) G5-LOW-1 - supersede's OWN not-found branches remain non-unified (missing echoes the id vs static foreign text; the supersedes_id-mismatch message is a third divergent text firing only on existing records) - a message-text existence oracle of the (r) class, reproduction in M5-T067-G5.md; QUEUED(M5-T069) at this seam; (t) G5-INFO-1 - get/list_for_condo_key return without a store-boundary property-scope check (caller/route responsibility; unmounted; on record for the mount/auth packet). PROGRAM NOTE: the approval broker runs documented commands from the worktree root while api suites need cwd services/api - the [BLOCKED]-evidence class (T067 G2); controller backlog. Loop-2 re-feeds with M5-T069. WATCH unchanged.
| DB-047 | 2026-09-21 | M5-T066 wave advisories (G3/G4/HJ/SEC PASS + fresh DCV; none blocking): (a) G3-CR-1 LATENT - adoption reconciles exterior_walls but NOT street_lines (DraftStreetLine.wall_id can orphan); unreachable today (no street-line editor/seed); when a street-line editor lands, adoptOutlineVertices + the dropped-wall announcement MUST reconcile street_lines by wall_id or the HJ-2 dangling class returns; (b) G4-gap-1 - the ProposalEditor dropped-wall ANNOUNCEMENT ("Removed N walls...") is untested (lib reconciliation is mutation-pinned; the never-silent honesty copy is not) - a silencing regression reddens nothing; (c) G4-gap-2 - real-pointer SELECT/MOVE has no e2e proof (only PLACE clicks a real canvas; select/move prove in jsdom w/ stubbed hits; keyboard equivalents fully proven); (d) HJ-1 - the drawing-section instruction copy renders "Click the lot map..." even when no interactive map exists (condo unit lot / multiple_features / invalid_geometry / no-WebGL render the typed fallback) - gate the lead copy on map render state; (e) HJ-2+SEC-A1 - Convert enables on point COUNT only; keyboard rows added-but-untyped (NaN lng/lat) enable a doomed bridge round-trip and the wrapper status counts points the overlay filters out - add a finiteness gate (same family as T060 HJ-3 draftIsRunnable); (f) HJ-3 - stacked role=status live regions (wrapper status + lot-outline-summary + architect-map-status + OutcomeAnnouncer + min-hint) fire competing SR announcements on one action - consolidate at the next a11y touch; (g) HJ-4 - dropped-wall announcement names internal wall ids (W-N, W-W), opaque to non-developers (pairs with (b)); (h) HJ-5 NIT - clicking the selected vertex's own circle deselects instead of moving while copy says "click the map to move it"; (i) G4-gap-3 NOTE - AS-2 "delete via map" is delivered as select-on-map -> table Delete (keyboard-accessible; the literal map-delete gesture does not exist, recorded as reasonable); (j) G3-CR-2/CR-3 NITs - side-effecting setSelectedIndex updater in moveSelectedPoint; draftRef announcement-count staleness under back-to-back adoptions (not a realistic path). | OPEN |
Sweep 2026-09-21 (M5-T066 acceptance seam, 250th, seq 125): T066 ACCEPTED at the post-restamp head (map-CLICK slice; G0/G2/G3/G4 + fresh HJ/SEC/DCV all PASS; the lost pre-handoff HJ tail superseded by a FRESH complete re-walk saved verbatim; DCV restamp pre-auth exercised a234a508 -> 5aad9007 after the disjoint T068 material landed - all four predicate conditions re-verified incl. 11/11 blob-MATCH + validator exit 0, 12m07s run). D-083 CAPTURED this seam (owner-relayed architect-lens assessment; 13 requirements incl. the claim-class vocabulary + interim "Preliminary development limits" wording that BINDS the T070 surfacing packet). T068 harvested from the loop-1 run-67 breaker close (fresh suites ruff-clean/63-passed/modularity-0; ALL-MATCH x3 cherry-pick 4e49d8e7 -> 5aad9007; 10 stale asks denied BOTH stores); submit rides the seam-push CI. All other OPEN entries reviewed and stay OPEN; WATCH unchanged.
| DB-048 | 2026-09-22 | FLAKY ACCEPTED SPEC (found at the a66cf818 seam-push CI): lot-outline-map.test.tsx AS-1 sync (M5-T066, ACCEPTED) asserts an exact mock call COUNT ('expected 4, got 6' - setData/addLayer-class counting) and went red on a head whose ONLY web delta was four inert T070 placeholder files; the zero-delta attempt-2 rerun was GREEN, so the assertion is timing-nondeterministic (extra effect/update pass class), not a behavior change. Harden at the next lot-outline-map touch: assert call ARGUMENTS/final state or a >=/monotonic count, never an exact cross-render tally; until then treat a solo red of THIS spec on unrelated heads as the flake class (one zero-delta rerun, then deficit-convergence if it repeats). | OPEN |
Sweep 2026-09-22 (M5-T068 acceptance seam, 251st, seq 125): T068 ACCEPTED (max-envelope test hardening; G0/G2/G3/G4 + DCV all PASS, zero blocking anywhere; the G4 producer=qa-engineer structural rule handled by ADDING backend-engineer as the independent QA-gate reviewer w/ G0 re-record - the DCV judged the amendment sound, never a gate dodge; three mutants OBSERVED red->green incl. the fail-closed raise engine+route). DB-046 (a)-(f) ALL RESOLVED at this acceptance. Four of the wave's five reviewer streams died in the ~21:0x-21:29Z provider incident and were RESUMED via SendMessage to clean complete returns - the resume-not-redispatch drill worked 4/4.
Sweep 2026-09-22 (M5-T069 acceptance seam, 252nd, seq 125): T069 ACCEPTED (DB-040(s) supersede not-found unification; G0/G2/G3/G4/G5 + DCV all PASS, zero blocking; G5 by the ORIGINAL T067 LOW-1 finder declares the last store existence oracle CLOSED; the run-29 PAUSED_RECOVERY missing-checkpoint close harvested via the producer's [BROKER-LIMIT->harvest] recipe - six-mutation matrix RED at the predicted :878/:907 firsts, independently re-killed by G4 LIVE and the DCV's own constant mutation). DB-040 (s) RESOLVED; (t) get/list posture note stays OPEN. NEW riders from the wave (all advisory): G3-A2 - no test pins revoke's raised text to the unified literal (a revoke-only fork would redden nothing; one-line pin at the next api test touch); G3-A3/G5-A-1/G4-ADV-2 - the store-level reason-guard-before-scope ordering is a theoretical store-only oracle, route-unreachable, deferred to the B-001 auth/hardening seam. All other OPEN entries reviewed and stay OPEN; WATCH unchanged.
| DB-049 | 2026-09-23 | M5-T071 wave-1 advisories (G3 PASS / G4 PASS / HJ FAIL-then-corrected; B1/B2 + the :568 flake-class tally + the G4-4 signal pins CLOSED at [ORCH-CORRECTED] 0014ad8f): DRAWING-SURFACE A11Y/DISCLOSURE CLUSTER (post-#253 packet on the freed surface): (a) HJ-A1/G4-5 MEDIUM - Convert silently drops untyped rows once >=3 finite exist (status says 3 against 4 visible rows; no disclosure; no spec covers the mix) - one-line disclosure + spec; (b) HJ-A2 - the disable hint counts incomplete rows but does not identify them (no aria-invalid/row marker); (c) HJ-A3 - the map-render copy flip is unannounced; fold into the wrapper's ONE status region, never a sixth live region; (d) HJ-A5 - selecting a non-drawn point: status silent while instructions say "move it"; (e) HJ-A7 - disabled Convert out of tab order (pre-existing; aria-disabled remedy); (f) HJ-A4 - the interactive label paints before the canvas is clickable (inverse of B2; needs a leaf readiness signal). TEST/RIDER: (g) G4-2 - AS-1 sync no longer proves no-rebuild; add expect(mapInstances).toHaveLength(1) (deterministic today); (h) G4-3/G3-F1 residue - four exact addLayer tallies remain in single-render specs (:248/:530/:721/:765-era anchors; flake trigger absent there) - sweep at the next file touch; (i) G4-6/HJ knowledge gap - no real-browser proof of the copy flip or the MutationObserver cost under MapLibre churn; no e2e asserts the instruction copy - route to the next e2e-touching increment; (j) G3 F2-F8 tail lost to a provider safeguards error twice - re-request or re-derive at the DB-049 packet's contract seam. | M5-T071 wave returns | (a)-(f) the post-#253 drawing-surface packet; (g)/(h) next lot-outline-map touch; (i) next e2e increment; (j) DB-049 contract seam | OPEN |
Sweep 2026-09-23 (T072/T073 contract seam + T070 increment-2 harvest, seq 126): D-084 captured (owner: "run 3 codex loops") - THREE lanes live: lane-1 M5-T072 (DB-040-family G3-A2 revoke-text pin, the seq-125 rider QUEUED here), lane-2 M5-T073 (DB-045(a) real-parcel ring-densification validation QUEUED here - the mount-seam gate), lane-3 closed run-17 cycle_closed and its T070 increment-2 (AS-5 e2e + AS-6 entry) harvested ALL-MATCH x3 at 075e9149; 5 stale loop-3 asks denied from piped JSON. The f58edd19 CI red converged in 3 clusters at c0b71938 (secret-scan pragma; the T070 copy-wall comment self-trip; the T071 DB-047(e) consumer-sweep miss - the M5-T065 adoption test now types finite coords). DB-047(d)/(e) + DB-048 closures ride the T071 accept sweep. All other OPEN entries reviewed and stay OPEN; WATCH unchanged.
| DB-050 | 2026-09-23 | M5-T070 wave findings (G3 FAIL F1-F4 + G4 FAIL F1-F4, convergent; blocking set CLOSED by the [ORCH-CORRECTED] rework at this seam): GEOMETRY-THREADING SEAM (the load-bearing rider): (a) one-action adoption is production-UNREACHABLE until a seam supplies authoritative EPSG:2263 lot geometry to the max-envelope engine (the client rightly sends none; the server rightly refuses to fit without lot lines - G3-F2/G4-F1 proof chain max-envelope-api.ts:538-547 -> max_envelope.py:632-637 -> :795-800); the RIGHT design is server-side derivation from the accepted mappluto_geometry_arcgis connector by BBL, never client-sent display geometry - contract it as its own packet before or with the route mount. ADVISORY CLUSTER (both reviews): (b) Content-Length mandatory fails closed behind chunked/compressing proxies (inherited outline-bridge precedent; mount-seam flag); (c) aborted outcome renders a reasonless card (latent); (d) binding/gap XOR unenforced - null renders as a limit value if the server violates its own invariant; (e) re-adopt silently discards manual edits (unguarded data loss on an explicit action); (f) no-client-CRS-math grep guard absent (AS-5 wording-vs-evidence); (g) e2e contract failures surface as timeouts; (h) entry.test.tsx:183 mounts the panel with unstubbed fetch (suite-time creep 7.1s->12.3s; stub it); (i) untested panel branches (client_timeout/aborted/payload_too_large/invalid_request, loading card, Retry nonce, superseded-request guard, boundCandidate non-srid rejects); (j) dead parameter street_width_class always ''; (k) producer-report "Implementation surface" line anchors stale ~12-21 lines (S1-S5 segments verified accurate); (l) copy-wall matches exact lowercase phrases only (the hyphenated form is invisible - seam noted at c0b71938); (m) gap token-to-copy map lives in the panel - keep it in sync if EnvelopeGapReason grows. | M5-T070 wave returns | (a) its own pre-mount packet; (b)-(m) the post-#254 max-surface hardening touch | OPEN |
| DB-051 | 2026-09-24 | M5-T076 wave riders (G3 FAIL->rework->delta-PASS + G4 PASS + DCV PASS cycle): GEOMETRY-THREADING TEST/CONSUMER CLUSTER: (a) G4-F1 MODERATE - the malformed-segments parametrize is truthy-only ("abc"/42/{...}/True); the idiomatic weakening `if lot.get(...)` ships GREEN while 0/""/{}/False/0.0 get silently derived-over - extend the parametrize with the falsy set (one line, next file touch); (b) G3-delta null-path asymmetry - with a BBL and a FAILING provider: absent/[] -> 200 honest gap but null -> 422 on lot.lot_line_segments with the derivation outcome discarded; docstrings claim equivalence that holds only on success - normalize None->[] on failure OR drop None from the derivable set + fix both docstrings; (c) G4-F2 - BBL_UNRESOLVABLE is the only outcome whose honest detail is asserted nowhere (blanking it ships green); (d) G4-F3 - the at-cap boundary (count == max_segments) untested; a >-to->= off-by-one ships green (fail-closed direction); (e) G4-F4 - a non-scalar lot.bbl (dict/list) yields 200 with NO derived_lot_geometry block at all - no test; (f) G3-F6 + DCV-F5 PAIRED, mount-seam class - the web client sends no lot.bbl (max-envelope-api.ts:50-53/:542, though it holds the BBL at :546) so derivation is client-unreachable, AND GEOMETRY_OVER_CAP is a new derived_lot_geometry.outcome vocabulary value with no consumer today: the FIRST consumer must wire lot.bbl and treat over_cap as distinct-from-invalid in ONE increment, never separately; (g) G3-F8 - no request-scoped budget on the new upstream fetch (30s client default + retries) - an explicit decision at the DB-045(b)/B-001 mount seam. | M5-T076 wave returns (reports M5-T076-G3.md/-G3-delta.md/-G4.md/-DCV.md) | (a)-(e) next geometry-threading file touch; (f)+(g) the mount packet | OPEN |
| DB-052 | 2026-09-24 | LOCAL DIRECTIVE-HARNESS SLOWNESS (owner-raised 2026-09-24: "16 hour to run a test"): the full tools/test_directive_compliance.py suite (129 tests) runs in 3m01s on CI Linux (control-plane job step, run 35963219466 at ed49b2d8) and validate_directive_compliance.py --check in 8s there, vs ~7.6 min/TEST (~16h suite, measured by the T076-era DCV F3) and ~12 min per validator run on the owner's Windows PC. Measured locally at seq 128: 10 sequential `git rev-parse HEAD` = 1.40s (~140 ms per git process). HYPOTHESIS (unverified - measure first): the per-call subprocess pattern in tools/directive_registry.py `_run_git` (ls-tree per pathspec, cat-file per blob) multiplies the Windows process-start cost across thousands of calls. Candidate fix: batch (one `git ls-tree -r -z` per commit + one `git cat-file --batch` stream) with fail-closed semantics and byte-identical digests preserved; before/after timing on the owner PC as the acceptance number. Interim rule stands: DCVs never run the full suite locally (CI runs it on every push). | owner message 2026-09-24; CI step timings of run 35963219466; local git timing | control-plane tooling packet (G5-class review; producer != reviewer) after this round's accepts | OPEN |
| DB-053 | 2026-09-24 | D-087 wave-1 accept riders (M5-T084/T085/T087 accepted 260th-262nd; every verdict PASS, none blocking): PDF SHEET WRITER (T085, unwired): (a) G3-A1 + G5-F1 - `_validate_ring` float()/len() on a malformed vertex RAISES ValueError/TypeError instead of returning a typed SitePlanRefusal (docstring promises never-raises); (b) G5-F2 - `_num` has no finite guard (nan/inf would print as tokens; unreachable today behind the ring finite check); (c) G3-A2 - no claim-class-word screen on caller-supplied address/bbl title-block passthrough (T081 has one) - REQUIRED before any wiring packet feeds untrusted address text; (d) G4-F1 - the escaper round-trip fixture uses only BALANCED parens; an unbalanced-paren address is the real risk - add a fixture. FOOTPRINT SOURCE (T087 research -> the connector packet): (e) G1-F1/G3-F1 - the schema table labels shapefile-truncated names (HEIGHTROOF, GROUNDELEV, MPLUTO_BBL...) as the ArcGIS names; the connector must use the live FeatureServer field names; (f) G3-F2 - base_z = GROUND_ELEVATION (NAVD88) is not yet reconciled with the massing model's local frame - one declared vertical datum rule before any mesh uses it; (g) G3-F3 - the_geom is MultiPolygon and courtyards appear as holes, but the massing prism builder takes ONE ring and refuses holes - the connector/massing seam must disclose or refuse, never silently drop parts; (h) DCV-F3/RQ-1 - HEIGHT_ROOF feet is a well-supported INFERENCE with no per-field unit tag - the connector G1 must close it before any measurement reliance. 3D DEPENDENCY PRE-SCREEN (T084 -> the admission packet): (i) G3-F1/F2 - the note does not argue necessity (policy section 5) and covers 4 of 6 provenance fields - the G5 admission packet must supply necessity + maintainer-change assessment; (j) G3-F4 - ezdxf (if ever proposed, even as test tooling) is a NEW package with full G5 admission; (k) DCV-F2 - R2000+ OBJECTS minimal root dictionary stays [NEEDS G1 RE-VERIFICATION] if any writer ever emits R2000+ (the R12 target avoids it). | M5-T084/T085/T087 wave + DCV reports (M5-T08{4,5,7}-G*.md, -DCV.md) | (a)-(d) the CAD export wiring packet; (e)-(h) the footprint connector packet; (i)-(k) the 3D dependency admission packet | OPEN |
| DB-054 | 2026-09-24 | M5-T082 massing truth-object riders (accepted 263rd; G3/G4/G5/DCV all PASS, none blocking) - CLOSE BEFORE WIRING TO USER GEOMETRY: (a) G4-F1 strong - concave triangulation under-tested: the L-shape fixture is star-shaped, so a naive fan passes every concave assertion (a fan on a U-shape gives cap area 9400 vs 5200) - add a non-star concave fixture (U/plus/comb) to the cap-area + closure + count parametrizations; (b) G4-F4 strong - fail-closed containment tolerance boundary unpinned (only a 200-ft shift is tested; tol=50 accepts a 30-ft overhang) - add a ~0.5-ft-outside case and pin on-lot-line behaviour; (c) G4-F2 + G3-F2 - clockwise ring normalization untested (real MapPLUTO rings are often CW); (d) G4-F3 - collinear-vertex collapse untested; (e) G4-F5 - volume local-origin conditioning unverified (integer fixtures make world == local); (f) G4-F6 - lot-with-hole test is misnamed (1-vertex ring -> invalid_source; the interiors branch is unreachable from a ring list); (g) G4-F7/F8 - no multi-floor golden; MAX_TOTAL_FLOORS cap untested and its refusal reads over_cap_vertices; (h) G5-F1 MEDIUM - lot_ring magnitude unbounded (finiteness + vertex count only) - bound it like the writers' 1e8; (i) G5-F2 MEDIUM - _triangulate worst case O(n^3) on 1000-vertex rings, run per level - bound or budget before user input reaches it; (j) G5-F3 - output size (2000 floors x 1000-vertex footprint) - consider a total-vertex ceiling; (k) G3-F1 - vertical unit declared us_survey_foot = horizontal CRS unit (B0 floor_to_floor_ft has no foot variant) - keep explicit; (l) G3-F3 - per-level outline checked against the LOT, not nested within the floor below (cantilevers allowed) - revisit with buildable-region primitives; (m) G3-F4 (producer discovery, confirmed) - stacked prisms share coincident interface caps; the index-based closure check cannot see position-coincident faces - GLB/DXF export must dedupe or disclose; (n) G3-F5 - exterior/party wall distinction not carried into the mesh; (o) string coordinates from MapPLUTO fail closed as non_finite - parse-to-float belongs in the wiring packet. | M5-T082 wave + DCV (M5-T082-G3.md, M5-T081-G5.md joint, M5-T082-G4.md, M5-T082-DCV.md) | (a)-(j) a massing hardening packet BEFORE the first route/GLB wiring; (k)-(o) the GLB writer + massing wiring packets | OPEN |
| DB-055 | 2026-09-24 | M5-T083 architect-sheet PDF reader riders (G5 FAIL -> rework 5467318e -> G1/G3/G4/G5 re-rulings all PASS): (a) G4-rework F6 ADVISORY test vacuity - test_mid_path_cm_does_not_corrupt_curve_start asserts points[0] (the moveto point) and points[-1], both INVARIANT under the literal pre-fix revert (p0 = apply(ctm, *current)); the bug moves points[1] (real (14.16,13.34) vs mutant (23.12,20.31)) and no test of the 37 reddens - strengthen it to assert the reopened curve's first interior point is continuous with the moveto point (one line); production code is correct; (b) G3-rework MODULARITY - sheet_reader.py is 962/1000 SLOC (JUSTIFY band): a split (object-graph/stream-decode vs the _StreamRun interpreter into a third module, compatibility facade kept) is REQUIRED before ANY further growth, in a packet that grants the third path; (c) G1-rework R1 - point/op/decoded-byte budgets are per DOCUMENT (a very large legitimate multi-page set could refuse on the sum) - disclose the bound downstream; (d) G1-rework R2 - inline images (BI/ID/EI) refuse the whole document; /Rotate not applied; MediaBox recorded un-normalized (page user space by contract); (e) G1-rework R3 - apply_matrix cites section 8.3.3 where the equation is 8.3.4 (doc nit); (f) G5-rework residual LOW - a few refusal details still embed an XObject /name untruncated (lexer-bounded to 64 KB, not attacker-unbounded). (g) DCV-F2 MEDIUM (unrouted producer discoveries): cross-reference STREAMS (PDF 1.5+) are refused by the reused strict reader (read_object_table) and the `sh` shading operator is refused - the two positive vector corpus items (Acrobat PDFMaker 9.0 for AutoCAD) may use xref streams, in which case the sheet reader reads NEITHER; (h) DCV-F1 MEDIUM: M5-T083 does NOT discharge D-087-R005's real-file harness (all 37 tests are synthetic) - the next reader packet (C1 or wiring) must first run corpus items 1-2 and 4-6 (by sha256) through read_sheet, with (g) checked first; until then nobody may tell the owner that real architect PDFs can be read; (i) DCV-F3 LOW: re, v, y, s, f, F, f*, B*, b, b*, n, W, W*, TL, Td, TD, T*, TJ, ' and " are never executed by any test (G1 verified them by reading) and the packet's 30-degree rotated RECTANGLE test is a rotated line; (j) DCV-F4 LOW: the evidence map's R005 rework row over-generalizes 'attacker tokens are truncated' (only the operator token is; XObject /name echoes are the DB-055 (f) residual). | M5-T083 rework wave + DCV (M5-T083-DCV.md; M5-T083-G1-rework.md / -G3-rework.md / -G4-rework.md / -G5-rework.md) | (a)+(e)+(i) the next touch of test_sheet_reader.py / sheet_primitives.py; (b) BEFORE any growth of sheet_reader.py (the C2 confirm packet must split first); (c)+(d) the C2 user-confirm packet; (g)+(h) PRECONDITION of the next reader packet (real-corpus run before any wiring or owner claim) - (h) DISCHARGED by M5-T093 with a negative result (0 of 6 real drawings read; every file uses a PDF 1.5+ cross-reference stream plus object streams); M5-T093 G3 advisories (k) the C1 xref-stream/object-stream resolver goes in a NEW profile-level module under app/drawings/ and must NOT widen the shared read_object_table (its second consumer is the accepted survey pipeline); (l) P3 is reframed as refusing scan-only files that would otherwise 'succeed' with zero geometry after P1 (raise it toward P2); (m) the (b) split stays justified on its own; (n) M5-T093 G1-A1/A2 - in items 3-4 the leaf /Page objects are raw and only the /Pages nodes are compressed (conclusion unchanged; sharpen the note at the next touch) and confirm the SLOC band with modularity_check at the split | OPEN |
| DB-056 | 2026-09-24 | M5-T077 accept riders (264th; G3/G4/DCV PASS, none blocking) + a G0-record hygiene item raised by two DCVs: (a) G3-F1 - GEOMETRY_OVER_CAP is the one fail-closed outcome with no route-level placement.detail assertion (unit detail pinned; route unmounted); (b) G3-F2 - the detail-append reads placement['detail'] directly in the un-try/excepted assembly (engine guarantees it; a .get would be belt-and-braces); (c) G4-F1 - the scalar-bbl edge guards (bool exclusion; empty/whitespace string) are unpinned - add bbl in {True, False, '', '  '} to the non-scalar/no-derive test; (d) dcv-t077 F1 + dcv-cad F6 - G0 neighbour tables list only a filtered subset of active tasks and the overlap check never expands '**' globs or strips prose suffixes (two dormant pre-regime July packets declare overlapping globs) - list every active task or state the filter, and make the checker glob-aware. | M5-T077-G3.md / -G4.md / -DCV.md; M5-T081/T086 DCV (dcv-cad F6) | (a)-(c) the max-envelope mount packet (with DB-050/051 (f)/(g)); (d) the next control-plane tooling touch | OPEN |
| DB-057 | 2026-09-24 | D-087 DXF writer + reader accept riders (M5-T081 266th / M5-T086 267th; every verdict PASS, none blocking): WRITER (T081, unwired): (a) DCV-F1 MEDIUM - D-087-R004 asks for a documented AutoCAD-open check the owner can perform; nothing provides it yet (no reviewer can run AutoCAD) - R004 stays OPEN at directive level until the export/wiring packet commits a sample DXF plus a plain-English opening check, and the owner's R008 'DXF = the middleman' confirmation is still pending; (b) G1-A2 - TABLES emits only LTYPE+LAYER (no STYLE/VPORT) and TEXT uses the undeclared STANDARD style (modern AutoCAD rebuilds both; a strict/old importer may warn) - add STYLE/VPORT in the export packet; (c) G1-A1 INFO - pre-2017 readers treat $INSUNITS 21 as unitless-for-insertion (benign, strictly better than code 2); (d) G5-rework F1 RESIDUAL LOW - _normalize_ring coerces the full raw lot/building input to a list before the edge-count cap (bounded by the caller today; cap the raw length first when wiring user geometry); (e) G3-delta ADVISORY - MAX_COORD_ABS also governs elevations/text height, so a pathological floor-height sum surfaces as coordinate_out_of_range rather than a height code; (f) DCV-F3 LOW - the T081 evidence-map R007 row says stdlib-only is 'asserted by test_as5_module_imports_stdlib_only', but that test rejects only six named libraries (a DWG-library import would pass it) - the fact is true in source; widen the test to an allowlist at the next touch; (g) DCV-F5 INFO - the original section of the T081 producer report shows the superseded $INSUNITS=2 and golden dbef79e1 (the appended Rework section supersedes both; not edited, frozen identity). READER (T086, unwired): (h) DCV-F2 LOW - D-087-R006's round trip is proven only ad hoc (G1 + DCV in-memory runs) - commit a writer->reader round-trip test in the next DXF packet (R006 stays open at directive level until then); (i) G3-F1 + G5-A2 - _to_pairs uses str.splitlines(), which also splits on \x0b \x0c \x1c-\x1e (a group value containing one shifts pairing) - split on CR/LF only; (j) G5-A4 - splitlines materializes the full line list before the max_lines check (bounded by max_bytes); (k) G5-A1 - DxfLimits has no upper clamp (a permissive caller could disable the size/line/entity bounds) - clamp at the wiring seam; (l) G5-A3 - the str-input branch skips the binary-sentinel and non-ASCII checks (test convenience; make it bytes-only or re-apply both); (m) G4-F1..F3 - finiteness pinned only for inf (add nan), bulge refusal only for positive LWPOLYLINE bulge (add negative + VERTEX bulge), closed-flag mask only for 0/1 (add 128 -> open, 129 -> closed); (n) G1 advisory - the INSUNITS map omits the official 22/23/24 (US survey inch/yard/mile), reported honestly as unknown_insunits_N. PROCESS: (o) DCV-F4 - M5-T086-G0.md says 815 graph files (copied from T081) where the 86e2f98b tree has 817 (seam commit + evidence map say 817); (p) DCV-F6 - both G0 neighbour tables list a subset of the in-flight packets (14 active M0/M4 packets omitted; zero overlap in fact) - future G0 tables list every active task or state the filter | M5-T081-DCV.md / M5-T086-DCV.md (dcv-cad), M5-T081-G1.md, M5-T081-G3-delta.md, M5-T081-G5-rework.md, M5-T086-G3.md / -G4.md / -G5.md | (a)+(b)+(h) the CAD export/wiring packet (R004/R006 close only there); (d)+(k) BEFORE any wiring of user geometry or user DXF; (i)+(j)+(l)+(m)+(n) the next dxf_reader touch (DXF reader hardening packet); (f) the next test_dxf_writer.py touch; (o)+(p) G0 practice from now on | OPEN |
| DB-058 | 2026-09-24 | M5-T089 official building-footprint connector riders (G1/G3/G5 all PASS, none blocking; the connector is unwired) - CLOSE IN THE WIRING PACKET: (a) G5-A1 - untrusted source text (attributes dict, geom_source, last_status_type) is kept verbatim for provenance; every report/3D/web consumer must escape it on render; (b) G5-A2 - a hostile or broken official host could make one call retain a few hundred MB (10 MB/page x 50 pages, 2,000 verbatim geometries) - add a per-geometry vertex cap and a cumulative-bytes ceiling; (c) G5-A3 - no wall-clock deadline across paging - thread an AnalysisBudget and lower max_attempts on interactive paths; (d) G5-A4 - correlation_id is logged as given - pass a server-generated id (or strip control characters) so a newline cannot forge a log line; (e) G3-A1 - the internal_error branch records only the exception class name - add logger.exception so a real bug is findable in production; (f) G3-A2 + G1-A2 - GROUND_ELEVATION has two conflicting official definitions (City dictionary: lowest ground-level elevation; FGDC: centroid-interpolated bare-earth DTM) and NAVD88 is stated only by the dictionary; a zero ground elevation is kept as a real 0.0 with ground_elevation_zero_unverified - the massing consumer must choose the ground datum knowingly; (g) G3-A3 - the source-registry record is deferred to the wiring packet; (h) G3-A4 - a feature missing its geometry key is proven only by a unit case, not end to end; (i) G1-A1 - earlier packet/G1 wording said short field names return HTTP 400; the live service returns HTTP 200 carrying an ArcGIS error object (the connector refuses both); (j) G1-A3 - the height unit (US survey feet) stays an INFERENCE (RQ-1; no official per-field unit tag; ~2 ppm vs international feet); (k) G1-A4 INFO - the FGDC states two capture thresholds (>400 vs >500 sq ft) ; (l) G4-A1 - the CRS page gate requires wkid 102718 AND latestWkid 2263 but no fixture has only one half wrong (a one-half simplification survives) - add a {wkid:102718, latestWkid:9999} page expecting wrong_crs; (m) G4-A2 - a zero CONSTRUCTION_YEAR emits the gap and nulls the year, but no test asserts construction_year is None (a false 'year 0' survives) - assert the displaced value | M5-T089-G1.md (dc-t089), M5-T089-G3.md (cr-t089), M5-T089-G5.md (sec-t089) | (a)-(h) the footprint wiring / 3D context packet; (l)-(m) the next test_building_footprints_arcgis.py touch; (i)-(k) by note (no code change) | OPEN |
| DB-059 | 2026-09-24 | D-087 PDF-writer hardening + GLB writer accept riders (M5-T091 268th / M5-T092 269th; every verdict PASS, none blocking; both unwired): (a) T091 G3-F3 + G5-LOW - caller text has no length bound (a 200k-char address yields a ~201 kB PDF) - bound every caller field at the wiring seam; (b) T092 G5-MEDIUM - the GLB name screen (_check_name) is a raw substring match, so separator variants such as 'As_of_right' / 'Maximum_allowed' pass on caller mesh NAMES (the accepted DXF writer has the same gap; the writer's own emitted strings are clean and pinned) - one shared claim-word module with the PDF writer's separator-collapsing key; (c) T092 G5-LOW + G4 - the GLB CLAIM_CLASS_WORDS list is a literal copy with no drift guard and only 2 of 11 words tested - fold into (b) with a drift test; (d) T091 G4 survivor - the non-finite check is tested on x only (production checks x AND y) - add a y-only probe; (e) T092 G3-F1 - the GLB writer raises typed errors while the PDF writer returns typed refusals - reconcile at the consumer; (f) T092 G1/G3-F2 - the official Khronos glTF-Validator was not run (npm/Dart; thin client) - run it in CI or a later packet; the golden was reproduced by two independent parsers; (g) T092 G3-F4 - app/cad/__init__.py's docstring does not list glb_writer (orchestrator-owned housekeeping); (h) T092 G5-INFO - GLB errors echo the caller-supplied name - redact at the wiring seam | M5-T091-DCV.md / M5-T092-DCV.md (dcv-w2) and the T091/T092 G1/G3/G4/G5 reports | (a)+(e)+(h) the CAD/3D export wiring packet; (b)+(c) the shared claim-word module packet; (d) the next test_pdf_sheet_writer.py touch; (f) a CI-side validator packet; (g) the next app/cad seam | OPEN |
| DB-060 | 2026-09-24 | Proposal-map / max-envelope web rework accept riders (M5-T078 270th / M5-T079 271st; G3/G4/HJ re-reviews + DCV all PASS, none blocking) + the M5-T080 HJ re-review advisories: (a) G3-A4 second half + HJ-8 - link each aria-invalid outline input to its row marker (aria-describedby) and mark a freshly added empty row invalid; (b) G4 F3 note - the single finite-point predicate is proven structurally only (the pre-fix form is byte-equivalent) - add a source-scan test; (c) HJ-A PRE-EXISTING - on a real refusal the role=alert card and the polite announcer carry the same text (announced about twice), and (d) HJ-6/D4 - the failure card alert and the aggregate role=status can double-read at mount - both go to the D-086 announcement slice; (e) HJ-4 - candidateIsAdoptable ignores contract violations, so Adopt could appear beside a withheld limit (latent-unreachable today: no candidate without lot geometry) - close before the max-envelope route is mounted; (f) T080 HJ ADV-1 - E12's print cell treats CalculationEvidence:52-55 as wholly raw, but the outer non-raw details opens in print and shows an empty 'Width-policy decisions' summary (cosmetic); (g) T080 HJ ADV-2 - LS-C06/LS-C27 should cross-reference that the cap + review meaning's print duty lives in A06 ; (h) T080 G3 Fadv-1 LOW - architect.css is missing from P0-RECONCILIATION's in-flight re-pin register although it changed after the ledger pin (its print rules at lines 140/187/188 are unmoved, so no print citation is stale today) - add it at the next ledger touch | M5-T078/T079 -G3-rework / -G4-rework / -HJ-rework, M5-T078-DCV.md (dcv-web), M5-T080-HJ-rework.md (hj-t080b), M5-T080-G3-rework.md (cr-t080b) | (a)-(d) the D-086 accessibility/announcement slice; (e) the max-envelope mount packet (mount precondition); (f)-(h) the next ledger touch (P5 print gate) | OPEN |
| DB-061 | 2026-09-24 | M5-T088 massing truth-object hardening accept riders (272nd; G3/G4/G5/DCV PASS, none blocking; unwired): (a) G4 ADVISORY-1 STRONG - the coordinate-magnitude bound is tested on X only (a y-only violation survives as mutant MD; production checks x OR y) - add a y-only probe BEFORE wiring; (b) G4 ADVISORY-2 low - scan units charged before vs after the inner scan are indistinguishable to the suite (mutant ME); (c) G3-A2 + G5 F-LOW-2 - the lot ring gets only the 1e8 magnitude bound, no NYC-2263 unit/range check (a 4326 lot would be mislabelled) - add the B0 NYC_2263 check before wiring user geometry; (d) G5 F-LOW-3 - shapely GEOSException is not a MassingModelError subtype (degenerate input is prevented upstream today) - wrap at the boundary; (e) G3-A1 - resource-bound precedence is check order, not severity - a UX packet surfacing reasons must know it; (f) G3-A3 - _check_output_size intentionally does not content-dedupe (each floor emits its own prism) - never 'optimize' it into an undercount; (g) F-HIGH-1 (the upstream proposal.py all-pairs simplicity check) - QUEUED(M5-T095) - M5-T095 producer discoveries: (h) services/api/app/scenario/derivation.py:314-363 carries its OWN copy of the same all-pairs simplicity logic (bounded today by re-validated input) - adopt the shared GEOS-backed check in a derivation packet; (i) the route-level fix (a cancellable job with a per-request wall-clock timeout + rate limit, M5-T088 G5 fix b) is still REQUIRED at the wiring/public-exposure packet even with the in-process bound | M5-T088-G3.md, -G4.md, -G5.md, M5-T088-DCV.md (dcv-t088) | (a)+(c)+(d) BEFORE any wiring of user geometry into the massing model; (b)+(e)+(f) the next massing_model touch; (g) M5-T095 | OPEN |
| DB-062 | 2026-09-24 | M5-T090 3D web library admission accept riders (276th; three 0.186.0 + @react-three/fiber 9.7.0 admitted under the full dependency-security policy, no waiver; G2/G4/G5/DCV PASS, none blocking): (a) G5-A2 - three@0.186.0 ships no type declarations; @types/three is a REQUIRED future admission and pulls about six runtime dependencies including a WASM physics engine (@dimforge/rapier3d-compat) - OWNER DECISION before admitting it, or a reviewed local declaration file for the used API surface instead; (b) G5-A1 WATCH - the fiber maintainer set shrank 22 -> 8 with a new account at 9.8.0; the pinned, integrity-matched 9.7.0 tarball is unaffected - re-check provenance before any fiber upgrade; (c) G5-A3 - a React 19.3+ upgrade must bump fiber in the same change (fiber peers react >=19 <19.3); (d) G5-A4 - fiber's @types/webxr dependency uses the floating range '*' (0.5.24 today, clean) - every lock regeneration re-verifies it through the age gate and audit; (e) G5-A5 INFO - @babel/runtime, @types/react and csstype flipped dev -> production scope (no version change); (f) DCV-F2 / G4 - no committed test pins the two exact versions or rejects range characters in apps/web/package.json (the whole-tree age gate and audit still protect the tree); (g) DCV-F1 cosmetic - the bot lock commit carries the generate-lockfile workflow's fixed template label '(M0-T019 security tree, validated)' | M5-T090-G4.md, M5-T090-G5.md, M5-T090-DCV.md (dcv-t090) | (a) OWNER decision, then the 3D viewer packet; (b)-(d) any later web dependency change; (f) the next apps/web dependency packet; (e)+(g) by note | OPEN |
| DB-063 | 2026-09-24 | The committed controller-update PowerShell contract suite is RED at HEAD, latently (CI does not run tools/controller_update/ps_tests): test_runbook_parse.ps1 hardcodes $pinnedSha = 3f4cee86 (line 26) and asserts source_binding.json commit_sha == it (line 86) and that docs/CONTROLLER_UPDATE_RUNBOOK.md section 4 names it (line 87; the runbook line 84 still says 3f4cee86); the binding moved to e0dd4a3a (M0-T146) and a5886dab (M0-T148) without either file following. OBSERVED by the orchestrator at 327f487c: `powershell -NoProfile -ExecutionPolicy Bypass -File tools/controller_update/ps_tests/test_runbook_parse.ps1` -> 'test_runbook_parse: 1 assertion failure(s)', exit 1. The M0-T160 producer (commit 28878be2, binding-only re-pin to a3f24ff3) returned NEEDS_SPLIT for this reason; its isolated sandbox also refuses every powershell run of the suite (the fixtures run git), so the suite must be run from the main session at harvest | M0-T160 producer return; orchestrator run | QUEUED(M0-T160): re-scope M0-T160 to the 3-file lockstep (binding + runbook section 4 + test $pinnedSha), G0 re-record, re-claim, rework producer, orchestrator runs run_ps_tests.ps1 at harvest; also consider adding the controller_update ps_tests to CI | RESOLVED (M0-T160 accepted 285th: the 3-file lockstep landed and run_ps_tests.ps1 went 7/7 green; residuals -> DB-071) |
| DB-064 | 2026-09-24 | M5-T094 architect-sheet reader split accept riders (278th; G3/G4/G5/DCV PASS, none blocking; byte-identical split, unwired): (a) G4 A-1 - no corpus case decodes two DISTINCT Form XObjects, so a wrong-key form memo survives - add a two-distinct-forms success case; (b) G4 A-2 - the decoded-bytes budget's per-DOCUMENT accumulation is never exercised (a per-stream charge survives) - add a multi-stream document whose streams are each under the cap but sum over it; (c) G5 A1 - the unsupported-/Filter refusal detail embeds the filter name without _preview truncation (pre-existing, lexer-bounded; pairs with DB-055 (f)); (d) G5 A2 - the document decoded-bytes total is checked after the increment, so it can overshoot by up to one per-stream cap before refusing (pre-existing, bounded); (e) G3 A1 - the report's stated reason for the facade-resident coordinator is off (the real constraint is the <600-SLOC sub-cap, not a runtime cycle); (f) G3 A2 - some private names (_decode_contents, _resolved_int, _preview, _*_KEY) no longer import from sheet_reader (not public, no importer); (g) producer D1 - a committed reusable 3.11 bare-package shim for the drawings/extraction suites would stop per-task re-authoring | M5-T094-G3.md / -G4.md / -G5.md / -DCV.md | (a)-(d) PKT-K (the C1 real-file resolver; the next sheet_* touch); (e)-(f) by note; (g) a tools/dev-helper packet | OPEN |
| DB-065 | 2026-09-24 | M5-T099 export + 3D-viewer plan accept riders (279th; G3 FAIL -> [ORCH-CORRECTED] 78eb0269 -> G3/G5 delta PASS; DCV PASS 9/9; docs only): the plan (docs/design/d087-export-and-3d-viewer-plan.md) is now the contracting source for PKT-A..PKT-M and carries its own mandatory criteria (the PKT-D filename allowlist; the PKT-K inflated-bytes cap + ratio guard; the PKT-F import parse-time controls; DB-061 (i) on PKT-D/E/F, verified by PKT-H). Residual advisories: (a) G5-delta nit - the PKT-D filename test should also cover a sanitized token that ends up empty or only '.'/'-' (fall back to a server default such as the correlation id); (b) G3 advisory 1 - a few +/-1 line anchors (decorator vs def lines); (c) G3 AS-4 advisory - PKT-K lists a dependency on M5-T097 with no file overlap (over-specified, harmless); (d) the section 0.1 present-tense 'in flight' wording is dated once T094-T100 land - refresh at the first plan touch; (e) G5 could-not-verify - the claim that @types/three pulls ~6 runtime deps incl. a WASM engine is unverified on the thin client (feeds the owner's PKT-G decision, not a gate); (f) DCV F-obs-1 - the evidence map's harvest line is boilerplate per row | M5-T099-G3.md / -G3-rework.md / -G5.md / -G5-rework.md / -DCV.md | (a) PKT-D; (b),(d) the next plan touch; (c) the PKT-K contract seam; (e) PKT-G owner decision; (f) by note | OPEN |
| DB-066 | 2026-09-24 | M5-T100 footprint-connector rider accept (280th; G3/G4/G5/DCV PASS, none blocking; unwired) - DISCHARGES DB-058 (e),(h),(l),(m). New before-wiring advisories: (a) G3 A1 - the shared transport allowlist regex ends in `$`, which matches before a TRAILING newline, so a class __name__ like 'Foo\n' keeps a raw newline through _sanitized_bounded (unreachable today - real class names are identifiers) - strip CR/LF outright at the wiring seam; (b) G3 A2 = G5 advisory - the caller-supplied correlation id is logged raw at both log sites (connector + transport) - generate it server-side or sanitize it (this IS DB-058 (d)); (c) G4 A1 - no probe pins that a short ALPHANUMERIC exception message (e.g. 'AdminSecret123', which the allowlist passes verbatim) is never logged - inject it and assert absence; (d) G4 A2 - no assertion that a TYPED refusal emits no ERROR record (a double log survives) - assert an empty error-record list on a typed refusal; (e) G3 A3 INFO - the logged fixed message wording differs from the returned refusal message (cosmetic) | M5-T100-G3.md / -G4.md / -G5.md / -DCV.md | (a)-(d) PKT-C (the connector wiring-hardening packet, with DB-058 (a)-(d),(f),(g)); (e) by note | OPEN |
| DB-067 | 2026-09-24 | M5-T096 owner CAD samples + DXF STYLE/VPORT accept riders (281st; G1/G3/G4/G5/DCV PASS, none blocking): DISCHARGES DB-057 (a), (b), (f). Residual advisories: (a) G4 A1 - the dxf_writer import-allowlist test is blind to __import__('name') calls (no Import node); importlib IS caught - fold into PKT-A (M5-T102, AS-5); (b) G4 A2 / G1 A1 - the producer's mutation-table row M6 and the word 'canonical' for the TABLES order overstate (R12 mandates only LTYPE before LAYER) - wording only; (c) G4 A3 - the README-sha test hashes checkout bytes (fails safe via the binary marking; G1 verified raw-blob == README) - optional git cat-file hashing; (d) G3 advisory - dxf_writer.py is 671 SLOC (WARN band) - split model/serialization/builder behind a facade at the next non-scope-locked touch; (e) G5 LOW - the README's optional third-party web glTF viewer wording must not be reused for REAL building geometry (data-handling); (f) DSC-1 - the GLB writer's title-case honesty stamp vs the DXF/PDF upper-case | M5-T096-G1.md / -G3.md / -G4.md / -G5.md / -DCV.md | (a) M5-T102 (PKT-A); (b),(c),(f) by note / the next app/cad touch; (d) the next dxf_writer growth packet; (e) the export-wiring packet's user-facing copy (PKT-D/PKT-I) | OPEN |
| DB-068 | 2026-09-24 | M5-T095 proposal-validation time-budget accept riders (282nd; G3/G4/G5/DCV PASS, none blocking): DISCHARGES DB-061 (g) (M5-T088 G5 F-HIGH-1 closed IN-PROCESS; the route-level defence stays DB-061 (i)). Advisories for the next proposal-validation touch: (a) G3 A1 - proposal_input_gate.py has its OWN total bound (MAX_TOTAL_VERTICES=5000, applied to the API routes first) and a now-STALE docstring (':18 runs its O(n^2) simplicity test'); the two bounds are unreconciled in writing and the total-count logic is triplicated (proposal.py + proposal_input_gate + derivation) - one shared helper; (b) G3 A2 - add a few EPSG:2263-magnitude FRACTIONAL boundary rings to the committed equivalence corpus (today only small integer grids; identity held in the reviewer's 520k probe); (c) G3 A3 - a GEOS error surfaces as 'self-intersecting'; say 'could not be verified simple'; (d) G3 A4 - a single outline over 20,000 positions now refuses on field proposed_massing (the cheap total check fires first) instead of the per-outline vertex field - disclose; (e) G4 - the 180-degree spike check is an EQUIVALENT (redundant) guard behind GEOS validity; line 287 `assert 0 <= WORK_BUDGET_PAIRWISE_TESTS` is vacuous; the mutation-table row for the all-pairs restore names the wrong killing test; (f) G5 F-LOW-1 - _check_total_positions walks an uncapped levels list before MAX_LEVELS applies (linear, ~0.28 us/level) - an O(1) level-count ceiling first; (g) G5 F-LOW-2 - broaden the fail-closed catch from (GEOSException, ValueError) to shapely.errors.ShapelyError | M5-T095-G3.md / -G4.md / -G5.md / -DCV.md | (a)-(g) the next proposal.py / proposal_input_gate.py packet (DB-061 (h) derivation duplicate stays separate) | OPEN |
| DB-069 | 2026-09-24 | M5-T098 massing pre-wiring accept riders (283rd; G3/G4/G5/DCV PASS, none blocking; unwired): DISCHARGES DB-061 (a)-(d). Before-wiring advisories: (a) G3 ADVISORY-1 - the lot NYC-range check runs on the collinear-COLLAPSED ring, so an out-of-range collinear spike vertex (e.g. x=2e6) is silently collapsed and never range-checked (B0 checks raw vertices before collapse) - range-check the raw vertices; (b) G3 ADVISORY-2 = G5 F-LOW-1 - _wrap_geos_errors catches only GEOSException; ShapelyError siblings (TopologicalError, GeometryTypeError, DimensionError) would escape untyped and the docstrings over-claim 'no untyped error can escape' - catch ShapelyError or correct the wording; (c) G4 ADVISORY-A - the lot NYC check's Y clause and its later vertices are under-probed (dropping the Y clause or checking only vertex[0] keeps all 65 green) - add an in-X/out-of-Y lot and a later-vertex-out lot; (d) G5 F-LOW-2 - refusal messages echo raw caller input unbounded (a 200,000-char vertex string -> a 200,055-char message; the lot path has no total-positions gate) - truncate the repr; (e) G3 ADVISORY-3 - NYC_2263_* are imported from proposal.py though outside its __all__ (explicit import unaffected); (f) G4 ADVISORY-B - exclusive-bound / check-after-Polygon mutants survive but are behaviour-free (generous unit guards) | M5-T098-G3.md / -G4.md / -G5.md / -DCV.md | (a)-(d) BEFORE any wiring of user geometry into the massing model (the PKT-E scene assembler seam); (e),(f) by note | OPEN |
| DB-070 | 2026-09-24 | M5-T097 DXF reader hardening + round trip accept riders (284th; round-1 G3/G5 FAIL on one root cause -> rework e04c2b5b -> same-reviewer delta PASS; DCV PASS 6/6; unwired): DISCHARGES DB-057 (h), (i), (j), (l), (m), (n). For the future DXF import route (PKT-F): (a) G5 A1 MEDIUM - ASCII control characters (incl. ESC) are KEPT inside TEXT values, layer names and $ACADVER for provenance - the consuming route must escape on output and never log raw primitive text; (b) G5 A2 - refusal detail can carry up to max_line_chars of repr-escaped raw content - never reflect it verbatim/unbounded to users; (c) G5 A3 - huge-integer group codes rely on the interpreter's int-digit limit + max_line_chars - an explicit small digit cap on group codes is defense in depth; (d) G5 A4 - only exact bytes/str are accepted (bytearray/memoryview refused) and DxfLimits must never be built from untrusted input; (e) G5 delta residual - the parse is linear but ~8 s at the default cap (2M lines) and ~27 s at the clamp ceiling - run read_dxf off the request thread in a cancellable job with a deadline and an upload byte limit (already a PKT-F criterion). For the next dxf_reader touch: (f) G3 A3 - the round-trip perturbation couples to the writer's POLYLINE/VERTEX serialization; (g) G3 A4 - dxf_reader.py ~613 SLOC (WARN band) - the lexer/model split (DISC-1) before growth; (h) G4 delta - the two scaling-ratio time guards (ceiling 8.0 vs real 3.4-5.7x) - if CI ever flakes, widen toward ~10 or add reps, never delete | M5-T097-G3.md / -G3-rework.md / -G4.md / -G4-rework.md / -G5.md / -G5-rework.md / -DCV.md | (a)-(e) PKT-F (the DXF import service + route); (f)-(h) the next dxf_reader touch | OPEN |
| DB-071 | 2026-09-24 | M0-T160 controller install-source re-pin accept riders (285th; G3/G5/DCV PASS, none blocking; nothing installed): (a) G3 ADVISORY 1 / producer D1 - test_runbook_parse.ps1 hardcodes $pinnedSha, forcing a manual 3-file lockstep on every re-pin (the third stale-pin drift) - have the test READ commit_sha from source_binding.json and assert the runbook states it; (b) G3 ADVISORY 2 / producer D2 - the tools/controller_update ps_tests are not in CI (why the drift stayed latent for two re-pins) - wire them into a CI job; (c) G5 ADVISORY-1 - docs/CONTROLLER_UPDATE_RUNBOOK.md line 19's expected protected-config SHA predates the B-025 config edit (today raw 610ce9d5..., LF 34f4fe90...) and lines 122-124 still point the owner at the M0-T142 freeze record - both fail SAFE (a mismatch makes the owner stop, never a wrong install); the commissioning path is M0-T159-recertification.md section 5, which names the correct values | M0-T160-G3.md / -G5.md / -DCV.md | (a),(b),(c) one controller-update doc/test consistency packet (tools/controller_update/ps_tests + the runbook + a CI job) before the NEXT re-pin | OPEN |
| DB-072 | 2026-09-24 | M5-T102 shared claim-word module accept riders (286th; G3/G4/G5/DCV PASS, none blocking): DISCHARGES DB-059 (b), (c), DB-053 (c) and DB-067 (a). Remaining: (a) G4 A1 - the DXF writer's per-word coverage is transitive (shared-function 11-word test + identity pin), not a direct DXF 11-word test - add one parametrized over all 11 via a monkeypatched GENERATOR_NOTE; (b) G4 A2 - the producer report cites the by-value pin as per-word coverage (label only); (c) G3/G4 A3 - the import-allowlist AST walk skips relative imports (level > 0; no current exposure); (d) G5 LOW - single-word INTERNAL-separator forms (A_P_P_R_O_V_E_D, APP-ROVED) still pass (inherent to collapse-to-space matching; not a regression) - weigh a stricter separator-stripped key if caller names are ever rendered; (e) G5 INFO WIRING PRECONDITION - add NFKC + confusable folding before the screen if any surface relaxes ASCII-only; screen the concatenation if several caller fields render as one visible line; (f) G5 INFO - 'AS-OF-RIGHT' is a redundant tuple entry (normalises to 'AS OF RIGHT'); (g) DB-059 (h) (GLB/DXF refusal messages echo the caller name) stays OPEN for the export seam | M5-T102-G3.md / -G4.md / -G5.md / -DCV.md | (a)-(c),(f) the next app/cad test touch; (d),(e),(g) PKT-D (export service) | OPEN |
| DB-073 | 2026-09-24 | M5-T101 footprint-connector wiring-hardening accept riders (287th; G1/G3/G4/G5/DCV PASS, none blocking; unwired): DISCHARGES DB-058 (b), (c), (d), (f), (g), DB-066 (a)-(d), and DB-058 (a) on the connector side (source text is now DECLARED untrusted; consumers must still escape on render). For the wiring packet (PKT-E): (a) G5 L-1 - the correlation-id sanitiser strips C0/C1/DEL/NEL/ESC but not U+2028/U+2029, and result.correlation_id is not in untrusted_text_fields; (b) G5 L-2 = G3 A1 - the sanitised cid is not length-bounded at the log sites (reuse _LOG_FIELD_MAX); (c) G5 T-1 = G3 A3 - the deadline is checked at each page top only, not before the metadata fetch nor during retry sleeps (a non-interactive caller can overshoot by one retry budget) - the user-facing scene route must pass interactive=True plus a deadline; (d) G3 A2 - a tz-naive deadline raises TypeError -> internal_error; reject it as a typed disallowed_request; (e) G3 A4 / producer discovery - the AS-5 not-wired test greps app/** for the literal module name and will trip on wiring by design; replace it with an import-graph/AST check first; (f) G4 A1 - add a multi-page advancing-clock deadline test (a hoisted once-only check survives today); (g) G4 A2 - add a cumulative-bytes ceiling between the one-page and two-page sums (a per-page reset survives today); (h) G1 ADV-1..4 - registry-draft wording: the truncated-name note wrongly lumps SODA (SODA uses full lowercased names), RQ-4 is stale (M5-T089-G1 captured the org usage header), the SODA 'about a day behind' gloss is directionally imprecise, and the >400 vs >500 sq ft completeness thresholds; (i) G5 BACKLOG - the shared transport (app/resilience/transport.py 460-495) logs correlation ids raw at five sites - sanitise and bound centrally | M5-T101-G1.md / -G3.md / -G4.md / -G5.md / -DCV.md | (a)-(g) PKT-E (the scene assembler + route, before it consumes the connector); (h) the next registry-draft touch; (i) a shared-transport hardening packet (app/resilience) | OPEN |
| DB-074 | 2026-09-24 | M5-T104 sheet-reader test-debt accept riders (288th; G3/G4/DCV PASS, none blocking; tests + one citation): DISCHARGES DB-055 (a), (e), (i) and DB-064 (a), (b) (the M5-T094 G4 survivors - the constant-key form memo and per-stream byte accounting - now die). Advisories for the next sheet_* touch: (a) G3 A1 / G4 A1 - the v, y and mid-path-cm interior-point literals are flattener-derived (de Casteljau at DEFAULT_FLATTEN_TOLERANCE 0.25), so a correct flattener refactor would redden them - later prefer semantic guards (max deviation from the true Bezier + a start-tangent check); (b) G4 A2 - once the sheet_* internals stabilize, commit consuming-namespace mutation tests (today the red runs are recorded, not committed); (c) G3 A2 - the fixed apply_matrix docstring dropped the '[recalled - verify]' marker the neighbouring citations keep; (d) G3 A3 - the Matrix type-alias comment still cites section 8.3.3; (e) producer D1 - path operators dispatch through the import-time dict sheet_interpreter._PATH_HANDLERS (functions held by value): patch the dict entry, not the class method, in mutation work | M5-T104-G3.md / -G4.md / -DCV.md | (a)-(e) the next sheet_reader / sheet_primitives touch | OPEN |
Sweep 2026-09-23 (T070 wave-2 close, seq 126): T070's full wave complete at 2b8963d9 - G3/G4 wave-1 FAIL -> [ORCH-CORRECTED] 0625c19b -> both ATTESTED-PASS; HJ PASS (0 blocking, 12 advisories - split-district silent omission is the TOP pre-mount rider; adoption invisible-feedback/silent-replace/undescribed-option, live-region stack, unstyled panel, raw label tokens, nav label - full text M5-T070-HJ.md; HJ names 1/2/3/6/7 as pre-mount closers); SEC PASS (F1 MEDIUM prototype-chain flaw in the orchestrator's own gap-copy map CLOSED same-wave at 6ea21d1d + the G4 AS-1-restore 6ac8b468, all three reviewers re-attested; F2-F7 riders: declared-Content-Length trust, unbounded array counts, unproven fail-closed paths, unbounded bbl in label, alert-for-permanent-state, discarded server message - full text M5-T070-SEC.md). DB-050 EXTENDED in place by these rider sets (the row's (b)-(m) absorb them; the geometry-threading packet (a) is LIVE as M5-T076 on lane-1). PROVIDER-INCIDENT DRILL EXTENDED: reviewer streams died repeatedly on safeguards flags mid-report; micro-chunk recovery (<=150-word asks, resumed at exact cut phrases, agents can heal minutes later) delivered every report complete - the pattern is now standard for truncation-chasing. All other OPEN entries reviewed and stay OPEN; WATCH unchanged.
Sweep 2026-09-23 (M5-T071 acceptance seam, 253rd, seq 126): T071 ACCEPTED at 9b7c30a5 (drawing-surface hardening; G0/G2/G3/G4 all PASS at the corrected identity after the HJ B1/B2 FAIL->[ORCH-CORRECTED] 0014ad8f->three-delta-PASS cycle; the DCV returned ALIVE post-handoff - slow, not lost - with PASS 5/5, a blob-level restamp predicate, and its own full-surface consumer re-sweep). DB-047 (d)+(e) RESOLVED at this acceptance; DB-048 RESOLVED (the AS-1 sync spec rewritten w/ retained teeth; the four single-render tallies stay as DB-049(h), openly not-claimed-closed). NEW from the DCV: the packet-authoring drift - navigation blocks stopped carrying the query.py --no-regen consultation sentence after M5-T065 - restore it in every future packet (orchestrator duty). D-084 note: R001/R002/R003 lane evidence now carries through the T072/T075/T076 + T073 + T074 deliveries recorded above. All other OPEN entries reviewed and stay OPEN; WATCH unchanged.
Sweep 2026-09-23 (M5-T070 acceptance seam, 254th, seq 126): T070 ACCEPTED at 2c15ab5f (the Preliminary-development-limits panel; G0/G2/G3/G4 all PASS at the final identity after the wave-1 G3+G4 FAIL -> [ORCH-CORRECTED] 0625c19b -> ATTESTED-PASS x2 + the SEC-F1 same-wave fix chain; HJ PASS 0-blocking; DCV PASS 11/11 incl. D-083's FIRST verification rows, with the AS-4 reachability limitation verified at the server and the honesty regime judged satisfied). DB-050 dispositions at this accept: (a) LIVE as M5-T076 (submitted); the HJ/SEC rider sets (b)-(m) stay OPEN routed to the post-#254 max-surface hardening touch and the MOUNT packet, whose PRECONDITIONS are now recorded in three places - the DCV's finding 5 (split-district disclosure MUST close pre-mount), SEC's mount list (authN/tenancy, rate limit, real-deployment Content-Length, the F1 fix ALREADY landed), and HJ's 1/2/3/6/7. D-083-R012 (three-property uncoached demo) stays OWED at the next demo seam. Evidence-map hygiene: anchor drift + one row mislabel caught by the DCV and [ORCH-CORRECTED] pre-accept - refresh anchors when maps are written, and verify relabels row-by-row. All other OPEN entries reviewed and stay OPEN; WATCH unchanged.

Sweep 2026-09-23 (M5-T072 acceptance seam, 255th, seq 127): T072 ACCEPTED at bc851ae3 (the T069-G3-A2 revoke-text pin; G0/G2/G3/G4 all PASS; DCV PASS 1/1 with the lane-1 journal corroboration closing G3-F6, the AS-2 mutant independently reproduced in-process, and a blob-level restamp predicate re-verified ALL-MATCH pre-accept). The seq-125 revoke-text-pin rider (QUEUED at the seq-126 contract-seam sweep) is RESOLVED at this acceptance. DCV-F2 disposition recorded: the packet's explicit G0,G2,G3,G4 gate list (vs the backend-default G5) was a contracting choice for a zero-production-path test-only packet - no security surface; noted per the DCV's one-line duty. Advisories G3-F3/F4/F5 + G4-F4/F5/F7 stay advisory (hoist/direct-pin/store-get() notes) - fold into the next site-definition file touch, no new DB number. All other OPEN entries reviewed and stay OPEN; WATCH unchanged.

Sweep 2026-09-24 (M5-T076 acceptance seam, 256th, seq 127): T076 ACCEPTED at 72eb3801 through the FULL FAIL->rework->delta cycle (G3 FAIL w/ five required corrections incl. two live-reproduced route defects; orchestrator-dispatched rework producer applied F1-F5+F7 mutation-verified per fix; same-reviewer delta-PASS at the corrected head; G4 PASS w/ the two-mutant guard lesson; DCV PASS 1/1 w/ the rework-provenance ruling and the 2080-entry audit chain walked unbroken). DB-051 opened above w/ the full rider cluster. DCV process notes recorded: (i) the seq-126 handoff prose called the lane-1 run 'COMPLETE-CLASS' while the journal says PAUSED_RECOVERY - handoff prose is orientation only, the ledger wins (standing rule reaffirmed); (ii) G2 self-check sits at the pre-rework identity (CLI-legal; substance covered by the [ORCH-HARVEST] rework verification + both independent gates + the DCV's own reruns) - future rework cycles SHOULD re-record G2 at the corrected head; (iii) the '2 stale asks' evidence-map wording merged pre-launch sweep and post-run cleanup (cosmetic). The G4 two-mutant lesson + the in-process-mutant technique are persisted in agent-memory (orchestrator relay; reviewer writes are guard-blocked). All other OPEN entries reviewed and stay OPEN; WATCH unchanged.

Sweep 2026-09-24 (M5-T074 acceptance seam, 257th, seq 128): T074 ACCEPTED at 40978922 (DB-045(c) bridge server-500 proof; G0/G2/G3/G4 PASS; fresh DCV PASS 1/1 - Ruling 1 closed G3-F6 on the lane-3 journal outside the repo, Ruling 2 confirmed the [ORCH-CORRECTED] literal material_commit ea2cdb65 with nil identity motion). DCV advisories recorded, no row opened: F3 (LOW) - lane-occupancy evidence-map rows should cite the run-id + journal path (adopted as the evidence-map convention for future lane packets); F4 (LOW) - explicit --gates G0,G2,G3,G4 on a test-only backend packet leaves G5 N/A (same class as the accepted M5-T072 F2). The first seq-128 DCV ran the validator ONCE and never the full directive suite (the DB-052 / 16h-trap prohibition held). All other OPEN entries stay OPEN; WATCH unchanged.

Sweep 2026-09-24 (M5-T075 + M5-T073 acceptance seam, 258th + 259th, seq 128): both ACCEPTED at 46cc1e6a on fresh DCVs (PASS 1/1 each). T075 (lane-1 run-70 browser-fault specs): DCV F2 (LOW) stale producer-report line anchors dispositioned BY NOTE - the report sits inside the frozen identity, so an edit would force a re-freeze; source-side anchors verified correct. T073 (lane-2 run-31 DB-045(a) real-parcel bridge validation): DCV F3 folds the reviewer riders into DB-045 for the MOUNT packet - G3 F6/F7 advisories, G4 F1-F6 (incl. the by-value-import mutation lesson), data-contract F3/F4 informational - alongside DB-045(b), DB-050(b), DB-051(f)/(g); the DB-045(a) verdict stays bounded to its 8-pair sample (3/8 pass; refusals = capacity + ambiguity classes). Process note: both verifiers addressed their reports to 'team-lead' (undelivered) - recovered verbatim from their transcripts; future DCV prompts name the recipient explicitly. All other OPEN entries stay OPEN; WATCH unchanged.

Sweep 2026-09-24 (M5-T084 + M5-T085 + M5-T087 acceptance seam, 260th-262nd, seq 128): all three ACCEPTED at f2870507 on one joint fresh DCV (dcv-w1: PASS 7/7, 5/5, 4/4) whose blob-level restamp predicate the same verifier re-confirmed after the D-087 source-002 amendment (744154df; R011/R012 bound only to D-087-BOOTSTRAP). Riders opened as DB-053 (a)-(k), each routed to the packet that first touches its surface (CAD export wiring, footprint connector, 3D dependency admission). DCV F1 (B-026 names no task; the loop-lane share of D-087-R001 stays with M0-T159) and F4 (confirm a settled control-plane CI at the accept head: CI run 35977663858 SUCCESS at f2870507) dispositioned BY NOTE.

Sweep 2026-09-24 (M5-T082 acceptance seam, 263rd, seq 128): T082 ACCEPTED at 10def4ef (the D-087 3D-1 massing truth object; G0/G2/G3/G4/G5 PASS; fresh DCV dcv-t082 PASS 5/5 with a blob-level predicate re-verified at the accept head incl. max_envelope.py f0abf884). DCV F1: its one validator run straddled the D-087 source-002 capture (transient c14 digest race, reproduced clean by hand at the settled head; CI control-plane green) - dispositioned BY NOTE. Riders opened as DB-054 (a)-(o); the (a)/(b) strong test gaps and (h)/(i) bounds must close before any wiring to user geometry.

Sweep 2026-09-24 (M5-T077 acceptance seam, 264th, seq 128): T077 ACCEPTED at 114e5e56 (DB-051(a)-(e) geometry-threading hardening; G0/G2/G3/G4 PASS; fresh DCV dcv-t077 PASS 3/3 with a blob-level predicate re-verified at the accept head). Riders DB-056 (a)-(d). Same seam: M5-T078 / M5-T079 G3+G4 FAIL and M5-T080 G3 FAIL recorded (plus HJ FAILs on T078 and T080), all three in rework; M5-T078 scope corrected by one stylesheet (G0 re-record).

Sweep 2026-09-24 (M5-T083 acceptance seam, 265th, seq 128): T083 ACCEPTED (architect-sheet PDF reader profile; G5 FAIL -> rework -> G1/G3/G4/G5 re-rulings PASS; fresh DCV dcv-t083 PASS 5/5). The D-087-R005 verification row is SCOPE-LIMITED (synthetic tests only; the real-file harness NOT discharged). Riders DB-055 (g)-(j) added; (g)+(h) are a precondition of the next reader packet.

Sweep 2026-09-24 (M5-T081 + M5-T086 acceptance seam, 266th-267th, seq 128): T081 (DXF writer, AC1009, $INSUNITS 21, through a G3/G4 FAIL -> rework -> delta PASS cycle) and T086 (DXF reader) ACCEPTED; joint fresh DCV dcv-cad PASS 7/7 + 6/6 with a blob-level predicate re-verified at the accept head. D-087-R004 and R006 stay OPEN at directive level (no owner-runnable AutoCAD-open check yet; no committed round-trip test yet); R008 still awaits the owner. Riders DB-057 (a)-(p).

Sweep 2026-09-24 (review-record seam, seq 128): M5-T089 G1 (live re-fetch byte-identical) / G3 / G5 PASS -> riders DB-058 (a)-(k); M5-T093 G3 PASS -> DB-055 (k)-(m) added and (h) marked discharged with the negative result; M0-T159 G3 (corrections) + G4 (identity carry) PASS at the re-frozen identity.

Sweep 2026-09-24 (M5-T091 + M5-T092 acceptance seam, 268th-269th, seq 128): T091 (PDF sheet writer hardening, DB-053 riders) and T092 (zero-dependency glTF 2.0 GLB writer, EPSG:2263 survey feet -> metres by a declared axis map) ACCEPTED; joint fresh DCV dcv-w2 PASS 5/5 + 6/6 with a blob-level predicate re-verified at the accept head. Riders DB-059 (a)-(h).

Sweep 2026-09-24 (M5-T078 + M5-T079 + M5-T088 acceptance seam, 270th-272nd, seq 128): T078 (proposal outline drawing + bridge rework) and T079 (max-envelope panel rework) ACCEPTED on the joint fresh DCV dcv-web (PASS 5/5 + 8/8; the D-085 model deviation ruled immaterial); T088 (massing truth-object hardening, DB-054 (a)-(j)) ACCEPTED on dcv-t088 PASS 5/5. Riders DB-060 (a)-(g) and DB-061 (a)-(g); F-HIGH-1 is QUEUED(M5-T095). M5-T080 journey re-review PASS saved (its G3 re-review is in flight).

Sweep 2026-09-24 (M5-T089 + M5-T093 acceptance seam, 273rd-274th, seq 128): T089 (official OTI building-footprint + height connector, unwired) ACCEPTED on dcv-t089 PASS 5/5 - its riders stay in DB-058 (a)-(m) for the wiring packet; T093 (real-corpus reader trial, NEGATIVE result: 0 of 6 real drawings read) ACCEPTED on dcv-t093 PASS 5/5 with the D-087-R005 row SCOPE-LIMITED - directive-level R005 stays OPEN until the C1 reader packet (after the M5-T094 split) reads real files; DB-055 (k)-(n) carry the C1 design points.

Sweep 2026-09-24 (M5-T090 acceptance seam, 276th, seq 128): T090 (three 0.186.0 + @react-three/fiber 9.7.0 admitted to apps/web under the full dependency-security policy - exact pins, >=7-day age, zero advisories, registry integrity, G5 provenance review, bot-generated lock; no waiver) ACCEPTED on dcv-t090 PASS 6/6. Riders DB-062 (a)-(g); (a) is an owner decision before the 3D viewer can use typed three.

Sweep 2026-09-24 (M5-T080 acceptance seam, 277th, seq 128): T080 (D-086 P0 - the per-file drift table, route/state map, backlog dispositions, boundary check, Section-13 verification matrix and ONE disclosure-migration ledger over all 381 inventory rows) ACCEPTED through a G3 + HJ FAIL -> two-producer rework -> fresh re-review PASS cycle; dcv-t080 PASS 7/7. Its advisories live in DB-060 (a)-(h). D-086 P1-P7 remain to be contracted in order (P0 done).

Sweep 2026-09-24 (M5-T094 acceptance seam, 278th, seq 129): T094 (the architect-sheet reader split into sheet_objects + sheet_interpreter behind the sheet_reader facade; byte-identical, proven by two independent reproductions of the 62-case golden from the pre-split module) ACCEPTED on dcv-t094 PASS 5/5. DB-055 (b) discharged by this split; riders DB-064 (a)-(g).

Sweep 2026-09-24 (M5-T099 acceptance seam, 279th, seq 129): T099 (the D-087 export-wiring + 3D-viewer PLAN, docs only) ACCEPTED on dcv-t099 PASS 9/9 after a G3 FAIL (seven unmapped rider sub-items) -> tagged [ORCH-CORRECTED] commit 78eb0269 -> re-freeze -> same-reviewer G3/G5 delta PASS. The plan is now the contracting source for the batch-1 packets (PKT-A shared claim-word module, PKT-C connector wiring + source_registry, PKT-K C1 resolver) as their in-flight dependencies are accepted. Riders DB-065 (a)-(f).

Sweep 2026-09-24 (M5-T100 acceptance seam, 280th, seq 129): T100 (building-footprint connector riders - findable non-leaky internal-error log, end-to-end missing-geometry, one-half-wrong CRS pages, zero-year value) ACCEPTED on dcv-t100 PASS 5/5; DB-058 (e),(h),(l),(m) DISCHARGED; the three old G4 survivors (MY1a, MY1b, MY7) are now killed. New riders DB-066 (a)-(e) go to PKT-C with DB-058 (a)-(d),(f),(g).

Sweep 2026-09-24 (M5-T096 acceptance seam, 281st, seq 129): T096 (owner-openable CAD samples + plain-English AutoCAD checklist, DXF STYLE/VPORT tables, stdlib import allowlist) ACCEPTED on dcv-t096 PASS 7/7; DB-057 (a),(b),(f) DISCHARGED. The owner's AutoCAD-open check (R004) and the R008 'DXF = the middleman' confirmation are now owner actions on docs/samples/cad/. Riders DB-067 (a)-(f).

Sweep 2026-09-24 (M5-T095 acceptance seam, 282nd, seq 129): T095 (proposal-validation time budget - a request-level position bound + GEOS-backed simplicity; worst request ~597 s -> <=119 ms, decision-identical) ACCEPTED on dcv-t095 PASS 6/6; DB-061 (g) DISCHARGED; the route-level cancellable job + deadline + rate limit (DB-061 (i)) remains REQUIRED at public exposure. Riders DB-068 (a)-(g).

Sweep 2026-09-24 (M5-T098 acceptance seam, 283rd, seq 129): T098 (massing truth-object pre-wiring guards - y-axis magnitude probe, lot NYC EPSG:2263 range check, typed GEOS wrap, charge-before-scan) ACCEPTED on dcv-t098 PASS 5/5; DB-061 (a)-(d) DISCHARGED (the M5-T088 survivors MD and ME now die). Riders DB-069 (a)-(f) go to the PKT-E scene-assembler seam.

Sweep 2026-09-24 (M5-T097 acceptance seam, 284th, seq 129): T097 (DXF reader hardening + the committed writer->reader round trip) ACCEPTED on dcv-t097 PASS 6/6 after a full FAIL -> rework -> delta cycle (G3 and G5 independently found an O(n^2) line splitter; round 2 made it a single linear pass guarded by scaling-ratio tests; 200k LF lines 6.3 s -> 0.5 s). DB-057 (h),(i),(j),(l),(m),(n) DISCHARGED; riders DB-070 (a)-(h). PKT-K (the C1 real-file PDF resolver) is now unblocked on both of its in-flight dependencies (M5-T094 + M5-T097 accepted).

Sweep 2026-09-24 (M0-T160 acceptance seam, 285th, seq 129): M0-T160 (the controller install source re-pinned to the recertified candidate a3f24ff3 as a 3-file lockstep: binding + runbook section 4 + the runbook-parse test) ACCEPTED on dcv-m0t160 PASS 3/3; DB-063 RESOLVED. The repository side of the B-026 remedy is complete; B-026 stays OPEN until the owner types the commissioning sequence in M0-T159-recertification.md section 5 (backup, install from a3f24ff3, record + verify the manifest, propagate to lanes 2-3, verify, doctor, live probe) and the lanes relaunch on fresh manifests. Riders DB-071 (a)-(c).

Sweep 2026-09-24 (M5-T102 acceptance seam, 286th, seq 129): T102 = PKT-A (one shared claim-word module for the DXF, PDF and GLB writers; separator variants refused everywhere; valid output byte-identical) ACCEPTED on dcv-t102 PASS 8/8; DB-059 (b),(c), DB-053 (c), DB-067 (a) DISCHARGED. PKT-B1 (PDF writer wiring-hardening) is now unblocked on pdf_sheet_writer.py. Riders DB-072 (a)-(g).

Sweep 2026-09-24 (M5-T101 acceptance seam, 287th, seq 129): T101 = PKT-C (footprint connector split + memory/time bounds + newline-safe logging + datum/untrusted-text declarations + the source_registry draft) ACCEPTED on dcv-t101 PASS 5/5; DB-058 (b),(c),(d),(f),(g) and DB-066 (a)-(d) DISCHARGED. PKT-E (the scene assembler + route) is now unblocked on its connector dependency. Riders DB-073 (a)-(i).

Sweep 2026-09-24 (M5-T104 acceptance seam, 288th, seq 129): T104 = PKT-K2 (the sheet-reader test debt: operator coverage, a true rotated rectangle, a non-vacuous mid-path-cm assertion, two distinct forms, per-document byte accounting, one citation fix) ACCEPTED on dcv-t104 PASS 5/5; DB-055 (a),(e),(i) and DB-064 (a),(b) DISCHARGED. Riders DB-074 (a)-(e).
