# Session Handoff — resume state as of 2026-07-17 (end of session 8)

Written by the orchestrator. Open this folder (`nyc-development-feasibility-claude-pack`) as the workspace root, then follow CLAUDE.md's start-of-session routine. This file is the conversation-independent resume point; the ledger (`project-control/`) remains the source of truth.

## Paste-ready prompt for the new session

> Read CLAUDE.md, docs/SESSION_HANDOFF.md, and the always-loaded rules files (.claude/rules/expansion-agent-dispatch-hold.md and 3d-ui-expansion.md attach unconditionally; .claude/rules/project-control.md attaches on project-control reads). Run `python tools/project_control.py status`, reconcile with git (`git status -sb` + `git ls-remote origin refs/heads/main` — main should MATCH remote; there is NO intentional divergence anymore). Resume the Immediate queue top to bottom. HARD RULES: (1) protected-main PR workflow — NEVER push to main directly (locally denied); every change goes task/control branch → push branch → `gh pr create` → `gh pr checks --watch` → `gh pr merge --merge --delete-branch` after green → `git pull origin main` → verify `git ls-remote`; (2) the HARDENED project_control.py governs the ledger (G2 recorded as `--reviewer orchestrator` = self_check role; independent gates G1/G3/G4/G5 need a rostered reviewer ≠ producer; accept requires awaiting_gate + all gates PASS + deps accepted + zero open blockers; accepted tasks are IMMUTABLE — no post-acceptance gate records); (3) evidence atomicity — each task's producer/gate reports live on ITS OWN task branch/PR, ledger records go in per-task control PRs; (4) ADR-005 — only the orchestrator runs project_control.py/git/gh; reviewers are read-only and return verbatim reports; preserve returns VERBATIM in project-control/reports/. Permission posture: auto mode, ask only for deletions — do not re-add broad ask rules. Pause only for secrets, billing, production approvals, professional legal review, or the owner decisions listed in this handoff.

## Exact repository state

- **main = remote main** (verify with ls-remote; at handoff: `9de9320` + the session-close handoff PR). No divergence policy anymore — the single-final-push rule is retired; protected-main PR workflow replaced it (owner directive 2026-07-17).
- **Open PRs at handoff:** #7 (task/M1-T008-dob-legacy-research — DO NOT MERGE until rework + G3 re-review pass). PR history this session: #1 B-008, #2 audit consolidation, #3 owner-approval adjustments, #4 M0-T014, #5 M2-T004, #6 M0-T015, #8/#9/#10 control PRs — all merged.
- **Worktrees/branches:** `.claude/worktrees/M1-T008` @ task branch (ACTIVE — rework). `.claude/worktrees/M0-T014`, `M0-T015`, `M2-T004` + their local branches are MERGED and awaiting cleanup (worktree remove + branch -d — deletion prompts are expected and wanted). Remote task branches already deleted by PR merges.
- **Accepted: 24** — M0: T000–T006 (incl. T005-R1), T009, T011, T012, T013, T010, **T014**, **T015**; M1: T001–T007; M2: T001, **T004**. Blocked: M0-T007/T008 (B-001; their packets need producer/reviewer rosters added BEFORE they can leave blocked — owner rule, encoded in M0-T016). Rework: **M1-T008**. Backlog: M0-T016, M2-T003, M2-T002, M1-T009.

## The hardened control plane (M0-T014 — READ BEFORE ANY LEDGER OPERATION)

`tools/project_control.py` is hardened and enforces: progress transition enum (can never set accepted); accept preconditions (awaiting_gate + every required gate PASS + deps accepted + zero open blockers referencing the task); gate classes — G2 self-check (`--reviewer orchestrator`, stored role `self_check`, can NEVER satisfy an independent gate), G0/G7 administrative (orchestrator), G1/G3/G4/G5/G6 independent (reviewer must be in the packet's `reviewer_agents` AND ≠ producer); task-id regex `^M\d+-T\d{3}(-R\d+)?$`; report paths contained under project-control/reports/; atomic writes. Operational consequences: **accepted tasks are terminal/immutable** (post-acceptance re-checks need a new task id); gate overwrites keep history (that's how FAIL→rework→PASS is recorded); claim requires ready (G0 PASS auto-promotes backlog→ready); CLI writes a submission record `reports/<task-id>.json` at submit — commit it with the task's control PR.

## Immediate queue (in order)

1. **M1-T008 rework** (docs-only reconciliation; research data verified sound): redispatch official-source-researcher against worktree `.claude/worktrees/M1-T008` with the EXACT rework scope from `project-control/reports/M1-T008-G3-code-review.md` (D1 retrieval-timestamp/anchor/catalog-claim reconciliation or commit the missing 16:15Z-session evidence; D2 complete 44-file README inventory + correct counts/sizes + scope the "verbatim" claim; D3 surface the HTTP 500 + quantified 6bgk/3h2n ranges from the committed key-probes log; D4 optional) and G1 report D1/D2. Fixtures stay AS-IS. Then: commit+push on the branch, delta re-review by the SAME G3 charge (fresh code-reviewer, delta-only), G3 PASS overwrite, merge PR #7, pull, accept, control PR.
2. **M2-T003 packet update BEFORE dispatch** (owner control 2, verbatim requirements): contract **1.2.0 is the current canonical contract** (M2-T004 published it); backward compatibility with accepted 1.1.0 consumers remains tested; backend runtime validation uses the correct selected contract; TypeScript types generated from the canonical **1.2.0** schema; the version is never silently hard-coded to stale 1.1.0; unsupported contract versions produce an explicit bounded error; the existing Property screen continues functioning; the Confirm screen consumes only documented 1.2.0 fields. Edit the packet inputs/outputs/scenarios accordingly (current text still says 1.1.0), commit via control PR, THEN claim + worktree + dispatch backend-engineer. Reviewers: data-contract-verifier (G1) + code-reviewer (G3/G4).
3. **M2-T003 → then M2-T002 (Confirm) and M1-T009** per the owner-approved critical path. M2-T002 producer frontend-engineer; reviewers human-journey-reviewer + visual-quality-reviewer + security-reviewer (all dispatchable). CI-only heavy execution stands.
4. **M0-T016** (owner-scoped hardening follow-up; packet in backlog): commit the packet (currently UNCOMMITTED untracked file `project-control/tasks/M0-T016.json` — include in the next control PR) and dispatch backend-engineer when a slot is free. Scope: reject orchestrator in reviewer rosters/independent gates; `--gates` enum validation; blocked-task roster precondition for M0-T007/T008 (do NOT alter their status now).
5. **Hygiene batch:** remove merged worktrees/branches (M0-T014, M0-T015, M2-T004 — deletes prompt); verify post-merge main CI green after each merge; `%TEMP%` has stale report .bak copies (deletable); validator-tests CI wiring (M1-T006 D1), .gitattributes CRLF determinism (M0-T010 G3 O4), Dependabot, Python lockfile.

## Session 8 log (what shipped)

1. **Owner code-audit consolidation executed end-to-end:** 6 packets (M0-T014/T015, M2-T002/T003/T004, M1-T009) + owner-review report (PR #2); owner approved with clarifications (PR #3): G2 codified structurally as orchestrator/self_check; **critical path reordered M2-T004 → M2-T003 → M2-T002/M1-T009** with repo evidence (M2-T004 changes the schema/status/identity/lineage M2-T003 consumes); Wave-1 path exclusivity; M0-T015 CORS constraints.
2. **M0-T014 ACCEPTED (22nd)** — hardened CLI, G3+G4+G5 PASS zero blocking defects, merged FIRST per owner order (PR #4), accepted by its own hardened CLI. Follow-ups D1/D2/OBS-3 → M0-T016; rest in maintenance backlog.
3. **M0-T015 ACCEPTED (23rd)** — health path /api/v1/health, pinned requirements.txt, phantom worker/cron removed w/ restoration note, CORS env allowlist (wildcard+credentials structurally impossible — startup crash), D8 resolved via NEXT_PUBLIC_API_BASE_URL (PR #6). CARRY-FORWARDS: **interpreter-version pinning (PYTHON_VERSION/NODE_VERSION) REQUIRED before B-002 provisioning**; reject non-https origin entries incl. literal `null` next time main.py is touched; add API_CORS_ALLOWED_ORIGINS to services/api/.env.example + SECRETS_POLICY inventory via the next task allowed there.
4. **M2-T004 ACCEPTED (24th)** — contract 1.2.0 additive; 6 independent status dimensions; 19-column documented completeness basis (per-column official PLUTO 26v1 citations, G1-re-verified against the official PDF); fact_key vs observation_id; canonical-json-1 digests (independently reproduced); gap-free fail-loud lineage. Gate history: G1 PASS w/ corrections C1/C2, G3/G4 FAIL (D1: two M2-T001 web tests asserted the OLD defective completeness value) → orchestrator-applied corrections under documented scope amendment @151f2e5 → delta re-review G3+G4 PASS, CI 12/12 (PR #5). Forward notes: raw-bytes digest at M2 Supabase persistence; observation_event_id uniqueness guard at persistence; builder still declares 1.0.0 — M2-T003 owns the declaration decision.
5. **M1-T008 REWORK** — G1 PASS (every headline claim live-reproduced: ic3t 32.6% BBL pollution, e98g empty, g76y frozen 2023-04, 855j dedup key) with blocking D1/D2; G3 FAIL (provenance narrative contradicts committed artifacts; unreported HTTP 500). PR #7 open unmerged. Rework = documentation reconciliation ONLY.
6. **Process shipped:** protected-main PR workflow (owner directive; PRs #1–#10; local deny on direct main pushes + force pushes); B-008 blocker (server-side ruleset plan-gated: private repo on GitHub Free — owner must upgrade to Pro or make repo public); permission posture v3 (auto mode both levels, blanket Bash/PowerShell allow, ask ONLY deletes/destructive, deny credentials+disk+main-push); M0-T016 contracted; CP-0017 + CP-0018 recorded.

## Blockers

B-001 (HIGHEST, Supabase token — blocks M0-T007/T008, which ALSO need packet rosters per M0-T016 scope), B-002 (Render owner-gated; now ALSO gated on M0-T015 carry-forward interpreter pinning), B-004 (Geoclient), B-006 (push protection before ANY real credential), **B-008** (main ruleset plan-gated — owner: GitHub Pro upgrade OR make repo public; interim orchestrator-side enforcement active). B-005/B-007 resolved (2026-07-17).

## Owner decisions pending

1. B-008: GitHub Pro upgrade (paid) or make the repo public — until then no server-side branch protection exists.
2. Credentials when ready: B-001 (highest), B-002, B-004; B-006 push protection first.
3. GDS/expansion planning review (counter-notice §2 hold on 19 tasks/9 contracts/P1–P8) — unchanged.

## Environment/process lessons (session 8 additions)

- **PR-flow mechanics:** `gh pr merge --delete-branch` cannot delete a local branch attached to a worktree (remote still deleted — fine; clean local later). After merging a PR whose files you copied UNTRACKED into the main checkout (report copies for CLI validation), `git pull` aborts — `Move-Item` the copy to `%TEMP%` and pull. state.json conflicts between control branches resolve by taking the CLI-latest superset (`git checkout --ours` on the branch that ran the transitions). GitHub recomputes mergeability after you push a conflict-resolution merge — retry `gh pr merge` once.
- **Auto-mode classifier still prompts on `git push`** unless a blanket `Bash`/`PowerShell` allow exists in settings.local.json (it does — ask rules override allow for deletes only).
- Producer agents get harness-isolated `agent-*` worktrees regardless of assigned worktree; transplant via robocopy/Copy-Item to the task worktree (gitignore keeps caches out; commit exact paths).
- Reviewer sandboxes this session could execute python/pytest/gh read-only — full-lockdown fallback (return-channel delivery) still applies when they can't.
- Big verbatim reviewer returns are a context-loss risk: write them to the task worktree IMMEDIATELY after the agent returns, before anything else.
