# Session Handoff — NYC Buildability (current-only)

**Authoritative state:** the `project-control/` ledger + git + CI. On resume, read it live —
`python tools/project_control.py status` and `python tools/current_state.py` — and reconcile against
the remote: **origin/main may have advanced, so do not trust any SHA here as still-current.** This
file is orientation only. Rules/gates/workflow routes live in `CLAUDE.md`. Old blocks via
`git log -p docs/SESSION_HANDOFF.md`. Keep CURRENT-ONLY: the `context-budget` CI check fails > ~4000 tok.

## Handoff (D-024/D-025 session 1 — updated after M0-T086 acceptance)

**Since the block below was generated:** M0-T097 was ACCEPTED (G2/G3 PASS, DCV 34/34 at `daabf2c`;
checkpoint `CP-D025-M0-T097`; pushed `41113f5`) and **M0-T086 was ACCEPTED** (reviewed identity
`372b4f7`: G2 self-check; independent G3 code PASS, G4 QA PASS — full 1870/2/0 suite re-run by the
reviewer, G5 security PASS, DCV 33/33 PASS; capability probe + fixtures + freeze-rule D-024
recognition delivered). Consolidated non-blocking findings carried into **M0-T088** as a named
hardening bundle (word-boundary flag matching, _run failure-branch tests, generic matrix==live
cross-check, probe_meta home-prefix redaction — note **the GitHub repo is PUBLIC**, G5-S1
adjudication in `M0-T086-G5-security-review.md`). **The canonical next-action pointer is now the campaign record**:
`project-control/campaigns/D-024-fable-codex-loop.json` (validate/read it via
`python -m tools.agent_supervisor.campaign_continuity --status`; fail-closed, exactly-once
advance, ledger+git still win). M0-T087 delivered it and passed the successor-orientation proof
(fresh campaign-ignorant context → READY TO RESUME with the correct next action). Items 2/4/6/16 of the block below are
superseded accordingly; the Gate-0 block and item 14 restrictions stand unchanged.

## Handoff (generated by the /session-handoff real-operation test, D-025 unit landing)

1. **Generated:** 2026-08-25 UTC · session: Fable 5 orchestrator, `session_01GjYMcezy7AJ9kKwBBPwJhm` ·
   reason: real-operation test of the new `/session-handoff` skill at the M0-T097 safe seam.
2. **Identity:** root/worktree `C:\Users\MLFLL\Downloads\nyc-zoning\ctl24`, branch
   `control/D-024-fable-codex-loop`, base HEAD `0d9f6b1` (this handoff ships in the checkpoint commit on top).
3. **Git status at generation:** dirty, fully enumerated, all included in the checkpoint commit:
   `M` D-024 manifest (scope binding fix), `M` directives/index.json, `M` state.json (claim),
   `??` .claude/skills/session-handoff/, `??` directives/D-025-session-handoff-skill/,
   `??` gates/M0-T097-G0.json, `??` reports/M0-T097-G0-readiness.md, `??` reports/M0-T097-session-handoff-skill.md,
   `??` tasks/M0-T097.json, this file. Nothing intentionally left uncommitted.
4. **Active:** directive **D-025** (bounded utility) task **M0-T097 = claimed** (lease: orchestrator,
   this worktree; G0 PASS recorded). Campaign **D-024** captured + pushed at `0d9f6b1`: 128 reqs,
   tasks **M0-T086..T096 all backlog**, none claimed. D-023 remains active elsewhere (M0-T080 round-3).
5. **M0-T097 objective:** owner-only `/session-handoff` skill (inline, dry-run capable, 18-item
   handoff contract, HANDOFF READY/BLOCKED, successor prompt) reusing existing machinery only.
6. **Completed (evidence):** D-024 v4 capture corrected + committed + pushed (`0d9f6b1`; validator
   EXIT=0 ×3). D-025 captured (34 reqs; source sha256 `9b563532…`). SKILL.md written; structural
   + dry-run tests passed (terminal log; report §2). G0 PASS (`reports/M0-T097-G0-readiness.md`).
   Both manifests scope-bound for the s19/R118 claim guard (D-023 precedent).
7. **Attempted, not finished:** M0-T097 independent review chain (G2 self-check → submit → G3
   code-reviewer → DCV → accept) — not yet run at the frozen checkpoint HEAD.
8. **Decisions:** campaign re-sized 8→11 tasks per D-024 §5.5/§15 (manifest applicability_note);
   real-operation test doubles as this seam's landing (honest single-use of the sequence); D-024
   manifest fix done as orchestrator duty outside M0-T097 scope (recorded in its audit_log).
9. **Rejected/failed:** first claim failed on covers_governance (fixed via manifest scope, not by
   weakening the guard); Python `write_text` CRLF broke digest stability (rewritten LF; digests
   recomputed; never hand-write directive JSON without LF + staged-blob hash check).
10. **Files changed:** see §3; plus memory note `d024-fable-codex-loop-campaign.md` (outside repo).
11. **Validation run:** `validate_directive_compliance.py --check` EXIT=0 (3× for D-024 state;
    1× re-run over the D-025/M0-T097 state in this landing — result recorded in terminal + report);
    `evaluate_task_refs` ok:True for all 12 tasks (T086–T096 + T097); staged-blob sha256 == recorded digests.
12. **Sub-agents:** none active, none recently completed, none stopped — nothing to resume or replace.
13. **Outstanding:** M0-T097 review/accept chain (§7); no pending permission prompts; no ambiguous
    external effects; last push confirmed (`0d9f6b1`); no PR opened for the control branch yet.
14. **Standing owner restrictions:** NEVER merge PR #241 or any pre-existing PR; continuous-mode
    activation stays owner-gated (D-024 §18); no D-024 directive rewrite and no continuous-loop
    implementation under M0-T097 (D-025-R002/R003); Bootstrap Gate 0 before any write in every
    session (D-024-R125–R128: primary cwd = ctl24, `/mcp` empty/allowlisted); ctl23 read-only;
    stale main worktree untouched; expansion-planning hold (`.claude/rules/expansion-agent-dispatch-hold.md`).
15. **Successor must read:** root `CLAUDE.md`; this file; `project-control/tasks/M0-T097.json`;
    `project-control/directives/D-025-session-handoff-skill/source-001.md`;
    `project-control/directives/D-024-fable-codex-loop/source-001.md` (before any D-024 unit);
    `project-control/reports/M0-T097-session-handoff-skill.md`.
16. **Exact next action:** finish M0-T097 at the frozen checkpoint HEAD — record G2 self-check,
    `submit` with the 34-requirement evidence map, independent G3 (code-reviewer, read-only) and
    DCV (directive-compliance-verifier) at `--sha` == HEAD, `accept`, commit control records, push.
    Then claim **M0-T086** (D-024 A1 capability baseline) per its task file.
17. **Stop/change conditions:** any Bootstrap Gate 0 failure (no writes; fresh session); validator
    non-zero; reviewer FAIL/BLOCKED (consolidated rework round, re-freeze, re-review); any action
    needing owner authority (credentials, payment, production, legal, PR #241, activation).
18. **Successor prompt:** see `COPY INTO THE NEW SESSION` in the terminal HANDOFF READY block, or
    equivalently: *"Work from durable repository evidence only. Verify root=ctl24, branch
    control/D-024-fable-codex-loop, HEAD, and `/mcp` empty before any change. Read CLAUDE.md,
    docs/SESSION_HANDOFF.md, tasks/M0-T097.json, and the §15 files. Run `python
    tools/project_control.py status`. Reconcile this handoff against live git + ledger (they win).
    Detect stale/duplicated/already-done work. Report READY TO RESUME or BLOCKED. If ready,
    continue from §16 exactly; do not repeat completed work or broaden scope; stop for anything
    needing owner approval."*
