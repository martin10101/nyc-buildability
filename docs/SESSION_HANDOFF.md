# Session Handoff — resume state as of 2026-07-20 (session 15: M0-T020 implemented + independently reviewed, awaiting owner acceptance)

> **P0 DEPENDENCY-SECURITY WAVE — M0-T020 THROUGH REVIEW, AWAITING ACCEPTANCE.** M0-T018 accepted (38th). **M0-T020** (Python dependency-policy enforcement follow-up) was contracted, owner-reviewed, bounded-amended (pytest 9.0.3), implemented by cloud-architect, and INDEPENDENTLY REVIEWED — **G3/G4 (code-reviewer) PASS + G5 (security-reviewer) PASS, zero blocking** — with **task PR #60 OPEN and unmerged** at head `9da5449`. **>>> RESUME POINT: wait for the owner to authorize M0-T020 acceptance, then run the acceptance procedure below, then dispatch M0-T019.** Owner boundary 2026-07-20: do NOT merge PR #60, record gate/acceptance ledger changes, accept M0-T020, advance CP-0031, dispatch M0-T019/M2-T013/M2-T014, or release any hold WITHOUT explicit owner authorization. The ledger (`project-control/`) is the source of truth; this file is the conversation-independent resume pointer.

Written by the orchestrator. Open this folder (`nyc-development-feasibility-claude-pack`) as the workspace root, then follow CLAUDE.md's start-of-session routine.

## Paste-ready prompt for the new session

> Read CLAUDE.md, docs/SESSION_HANDOFF.md, and the always-loaded rules (.claude/rules/expansion-agent-dispatch-hold.md + 3d-ui-expansion.md attach unconditionally; project-control.md attaches on project-control reads). Run `python tools/project_control.py status`; reconcile with git (`git status -sb` + `git ls-remote origin refs/heads/main` — main should MATCH remote at `0572d09`). Expected: ledger 38 accepted / 2 blocked (M0-T007/T008) / 9 backlog / 0 claimed; **1 open PR (#60, the M0-T020 task PR, head `9da5449`)**; checkpoint CP-0031. YOU ARE POST-REVIEW ON M0-T020: implemented + independently reviewed (G3/G4 code-reviewer PASS + G5 security-reviewer PASS, zero blocking); verbatim reviewer reports at `project-control/reports/M0-T020-G3-G4-code-review.md` and `M0-T020-G5-security-review.md`; producer report at `project-control/reports/M0-T020-producer-report.md`. **The next action needs OWNER AUTHORIZATION: accept M0-T020** (procedure below), then dispatch M0-T019. Until then, hold. HARD RULES: (1) protected-main PR workflow — NEVER push to main; task/control branch → push → `gh pr create --head <branch>` → wait checks green (FOREGROUND polls) → `gh pr merge --merge --delete-branch` → `git pull origin main` → verify ls-remote AND remote-branch deletion. (2) HARDENED project_control.py — G2 = `--reviewer orchestrator` self_check; independent gates need a rostered reviewer ≠ producer; orchestrator REJECTED in rosters; accepted tasks IMMUTABLE; report paths must EXIST in the checkout when the CLI runs (record gates AFTER the task PR merges). (3) ADR-005 — only the orchestrator runs project_control.py/git/gh; reviewer returns preserved VERBATIM (byte-scan: python bytes < 9 or 14–31, before committing). SHELL: Bash and PowerShell SHARE a persisted cwd — Set-Location/cd to repo root first, or use `git -C`. TEXT: the tool-call JSON layer decodes backslash-u escapes into literal bytes — write word forms. IDE: background-task completion does NOT re-invoke the session — run CI waits in the FOREGROUND (bounded polls in-turn). Permission posture: auto mode, ask only for deletions. Pause only for secrets, billing, production approvals, professional legal review, or pending owner decisions.

## Exact repository state

- **main = remote main** at `0572d09` (session-15 merges: planning PR #58 → `403d7f4`; pytest-amendment PR #59 → `0572d09`). **1 open PR: #60** (M0-T020 task PR, head `9da5449`, unmerged, MERGEABLE, all 20 checks green).
- **Ledger: 38 accepted / 2 blocked / 9 backlog / 0 claimed.** Checkpoint **CP-0031** (state.json `last_checkpoint`). NO checkpoint recorded this session — owner boundary: the next checkpoint is recorded ONLY after M0-T020 AND M0-T019 are both accepted, Phase 2c reconciliation is complete, and both complete Python and npm audits pass on reconciled main.
- **M0-T020 — IMPLEMENTED + REVIEWED; ledger status `backlog`; task PR #60 OPEN, unmerged.** Producer cloud-architect, branch `task/M0-T020-python-dependency-policy` (head `9da5449`). Gates **G3/G4 code-reviewer PASS + G5 security-reviewer PASS, zero blocking, bound to `9da5449`**. Ledger still shows `backlog` because the gate records + acceptance are DEFERRED to the owner-authorized acceptance control PR (the M0-T018 pattern). Deps: [M0-T018 ✓ accepted]. Content: bounded pyproject `pytest>=8,<9`→`pytest>=9.0.3,<10` (CVE-2025-71176 / PYSEC-2026-1845 fix — the ONLY pyproject change, + comment); hash-pinned tooling lock (`requirements-tools.in` 11 direct pins → `requirements-tools.lock` 42 pkgs, 389 sha256) via one generator `scripts/lock_tools.sh` (byte-identical `--check`); every Python CI job installs tools ONLY from the two locks (`--require-hashes`), app via `--no-deps` (removed unpinned pip upgrade, `.[dev]`, ranged `pytest>=`/`httpx>=`/`pyyaml`/`pip-audit`, `pipx run --spec`); dual pip-audit (runtime + tooling) blocking ZERO; machine release-age gate `scripts/dependency_age_gate.py` (live PyPI UTC, newest admitted-hash artifact, ≥604800s, fail-closed) + 17 tests (604800 pass / 604799 fail + fail-closed branches); uv 0.11.29→0.11.28 (age-clean; **production lock `requirements.txt` UNCHANGED / byte-identical**); `requirements.in` header fixed; egg-info gitignored; new CI job `api-tooling-lock-verify`. **3 non-blocking review observations** (full tooling lock installed where one tool suffices [deliberate, `--require-hashes` rejects single-pkg form]; `api` pip cache key references pyproject [neutral, installs are hash-verified]; age-gate could `urllib.parse.quote` the version segment [defense-in-depth, already mitigated]).
- **M0-T019 — backlog; HELD.** Frontend Next/React security upgrade + permanent dependency-admission policy (`project-control/tasks/M0-T019.json`); deps [M0-T018 ✓, M0-T020]. Dispatch only after the owner authorizes (after M0-T020 accepted). Target: next==15.5.20, react/react-dom==19.1.2, eslint-config-next==15.5.20, postcss override ==8.5.10; npm 11.18.0, min-release-age=7, save-exact=true; blocking npm audit zero; adds docs/DEPENDENCY_SECURITY_POLICY.md + a permanent CLAUDE.md rule. Producer frontend-engineer.
- **M0-T018 (38th, ACCEPTED, IMMUTABLE)** — backend production dependency parity + Python supply-chain enforcement (task PR #55 `7ffd542`, acceptance PR #56 `270e81b`). Do not reopen/rewrite.
- **Repo PUBLIC** (owner 2026-07-20); ruleset `protect-main` active; secret scanning + push protection ON. Blockers B-005/B-006/B-007/B-008/B-009 RESOLVED.
- **Worktrees (M0-T020):** `agent-a501f3e0a11bdd091` holds the STOPPED Phase-B run's producer research (pre-amendment); `agent-ab14f31e8c53fac99` is on branch `task/M0-T020-python-dependency-policy` (the PR #60 implementation). Leave both until M0-T020 is accepted, then clean up. Plus older harness-managed stale `agent-*` worktrees (two locked — leave them).
- **Remote branches:** `task/M0-T020-python-dependency-policy` (PR #60) + the 8 older standing-hygiene branches (M0-T010/T014/T015/T016, M1-T007/T008, M2-T003/T004).
- **Working tree:** clean except `.claude/agent-memory/**` (permitted).

## >>> M0-T020 acceptance procedure (run ONLY after the owner authorizes "accept M0-T020")

1. Confirm PR #60 head is still `9da5449` and all required checks green. If ANY new commit landed after the reviews, the G3/G4/G5 reports are stale for the changed scope — rerun CI and redispatch the affected reviewer(s) against the new SHA first.
2. Merge PR #60 via the protected-main workflow (`gh pr merge 60 --merge --delete-branch`); `git checkout main`; `git pull origin main`; verify local==remote and the remote branch is deleted (`git push origin --delete` if it survives).
3. Create an **acceptance control PR** that, via `tools/project_control.py`, records for M0-T020: G0 PASS (`--reviewer orchestrator`, a readiness note), `claim` (producer cloud-architect), G2 PASS (`--reviewer orchestrator`, self_check = the producer report), G3 + G4 PASS (`--reviewer code-reviewer`, `--report project-control/reports/M0-T020-G3-G4-code-review.md`), G5 PASS (`--reviewer security-reviewer`, `--report project-control/reports/M0-T020-G5-security-review.md`), then `accept --task-id M0-T020 --agent orchestrator` (39th). The three report files are ALREADY on main (committed this session) so the paths exist when the CLI runs.
4. Per the owner boundary, HOLD the checkpoint: record it only after M0-T019 is also accepted + Phase 2c reconcile + both audits green (unless the owner directs otherwise). Update this handoff.
5. Then (owner-authorized) dispatch **M0-T019** (frontend-engineer, isolated worktree) → task PR → gates G3/G4 code-reviewer + G5 security-reviewer → owner acceptance. Then **Phase 2c**: reconcile main + ledger + reports + handoff, re-run BOTH complete dependency audits (Python via the M0-T018/M0-T020 workflows; npm via the M0-T019 workflow) on reconciled main. Then **Phase 3** (only if both P0 accepted + exact-main green): M2-T013 + M2-T014 in parallel isolated worktrees.

## Session 15 log (what shipped)

1. **Owner "next controlled wave" continued.** Contracted M0-T020 (planning PR #58 `403d7f4`: created the M0-T020 packet, made M0-T019 depend on M0-T020, updated handoff sequencing). Owner planning-review amendments (same PR, 7 items) tightened scope (remove generate-lockfile.yml, add tooling source manifest, enumerate every Python CI download, production-lock immutability, precise age-gate artifact semantics, checkpoint timing, superseded session-14 log).
2. **M0-T020 producer STOP #1 (blocked, pre-implementation):** cloud-architect found pytest 8.x (the only versions the pyproject `pytest>=8,<9` cap allows) carries CVE-2025-71176 / PYSEC-2026-1845, fixed only in pytest 9.0.3 (needs a pyproject amendment = forbidden path). Independently corroborated (OSV + pyproject grep). Also proved Phase-2 production-lock BYTE-IDENTICAL under uv 0.11.28 (the feared conflict resolved).
3. **Owner authorized a BOUNDED pyproject amendment** → control-amendment PR #59 `0572d09` (M0-T020.json only: allow ONLY the pytest specifier `pytest>=8,<9`→`pytest>=9.0.3,<10`; require pytest==9.0.3 pin + hash-pinned lock; TP-S14; PYTEST 9 COMPATIBILITY STOP). No advisory waiver, no audit split.
4. **M0-T020 producer run #2 (COMPLETED)** from amended main: full implementation, re-verified every version live, production lock unchanged, dual audits zero, age gate + tests, pytest 9.0.3 collection byte-identical (538) + suite 538 passed with no weakening. Task PR #60 (head `9da5449`); all 20 CI checks green (incl. new `api-tooling-lock-verify`, dual `pip-audit`, `api` on Python 3.12).
5. **Independent review:** G3/G4 code-reviewer PASS + G5 security-reviewer PASS, zero blocking, bound to `9da5449`. Reports preserved verbatim at `project-control/reports/M0-T020-G3-G4-code-review.md` + `M0-T020-G5-security-review.md`.
6. **Returned to owner; awaiting acceptance authorization.** No merge/accept/checkpoint/dispatch performed (owner boundary honored).

## Prior sessions (condensed; full detail in git history + agent-memory)

- **S14:** accepted M0-T018 (38th) — backend production dependency parity + Python supply-chain enforcement (hash-pinned uv runtime lock, jsonschema on the exact Render path, starlette 0.46.2→1.3.1, new `api-lock-verify`/`exact-production-install`/blocking `pip-audit` CI).
- **S13:** accepted M2-T011 (36th, shared connector transport/retry consolidation) + M2-T010 (37th, client+backend supported-version derivation from the schema), both at CP-0031.
- **S12:** accepted M2-T007 (33rd, DCP ArcGIS zoning-features connector), M2-T008 (34th, ZTLDB SODA connector; repo made PUBLIC, ruleset + secret scanning enabled), M2-T009 (35th, per-BBL MapPLUTO ArcGIS geometry). CP-0028/0029/0030.

## Hardened control plane (READ BEFORE ANY LEDGER OPERATION)

Orchestrator REJECTED in rosters/independent gates; G2 recorded `--reviewer orchestrator`; `--gates` enum-validated; blocked tasks with empty rosters stay blocked until packet amendment; accepted tasks IMMUTABLE; `submit` writes `reports/<task-id>.json`; report paths repo-relative and MUST EXIST in the checkout when the CLI runs (gate/submit therefore record AFTER the task PR merges — the M2-T007 / M0-T018 pattern). `new-task` creates a validated skeleton only (empty inputs/outputs/paths/scenarios); the orchestrator authors the rich fields by editing the backlog packet (immutability applies only to accepted tasks).

## Blockers

**B-001** (HIGHEST — Supabase token; blocks M0-T007/T008 AND persistence/citywide-import slices; also needs packet roster amendment), **B-002** (Render), **B-004** (Geoclient). B-005/B-006/B-007/B-008/B-009 all resolved.

## Owner decisions pending

1. **Accept M0-T020** (task PR #60, reviewed PASS/PASS) — the immediate resume gate.
2. Review the session-13 planning report to release the DISPATCH HOLDS on the survey workstream (M2-T014/T015/T016), M2-T013, and M4-T001.
3. Credentials when ready: B-001 (highest), B-002, B-004.
4. GDS/expansion planning review (counter-notice §2 hold) — unchanged.
5. CORS/proxy decision (M2-T001 D8) — deploy-blocking; no urgency while B-001 no-deploy stands.
6. M2-T016 professional-confirmation role definition — needed before that state can be granted in production.

Decisions RESOLVED 2026-07-20: next-wave approval; M2-T013 policy knobs C1–C4; first M4 rule family = R5; OQ-3 owner-deferred/non-blocking (keep recording ZTLDB source age + possible_vintage_skew; do not resurface unless production-blocking or unresolved near launch); M0-T020 pytest 9.0.3 bounded amendment authorized.

## Holds this run (do NOT dispatch)

M2-T012, M2-T015, M2-T016, M4-T001, M6-T001; survey Packets B/C (M2-T015/T016; B-001 still blocks production storage). M4-T001 remains R5-first but waits; M2-T012 and M4-T001 both may touch contract tooling/artifacts — never run concurrently without a later owner-approved non-overlap plan.

## Standing carry-forwards (non-blocking; unchanged)

M0-T020 review non-blockers (3, above). M2-T007/T008/T009 G-review LOW items; M2-T005 accessibility release carry-forwards (NVDA/VoiceOver session, rendered focus-ring pixels — must not disappear before release) + the M2-T005-era web-e2e a11y focus-race flake. Standing hygiene: 8 older remote task branches; validator-tests CI wiring (M1-T006 D1); .gitattributes CRLF determinism; Dependabot.

## Environment/process lessons (this session's additions; prior lists live in agent-memory `orchestration-lessons`)

- **Producer STOP on a real advisory → bounded owner amendment → re-dispatch** is the clean flow. Never let a producer waive an advisory or split the audit to hide it; surface + independently corroborate (OSV `POST /v1/query`), let the owner authorize a minimal forbidden-path exception, re-dispatch from amended main.
- **CRLF-vs-git-blob byte-identity gotcha:** on the Windows PC (`core.autocrlf=true`) a regenerated hash lock shows a FALSE "differs" vs the working copy. Compare against the git blob (`git show HEAD:<file>` → sha256) or `git ls-files --eol`; Linux CI is LF so `api-lock-verify` passes. uv 0.11.28 ≡ 0.11.29 resolver output (production lock unchanged by the age downgrade).
- **Deferred-ledger dispatch under a review-only boundary:** keep the task-PR head == the reviewed SHA; do NOT commit reviewer reports onto the task branch afterward (moves the head, un-binds reviews). Preserve reviewer reports separately; record gates + accept together in the later owner-authorized acceptance control PR. Dispatch reviewers WITHOUT isolation and point them at the producer's task-branch worktree (absolute path) + `gh pr diff/checks` — a fresh isolation:worktree would branch from main and lack the task-branch code.
