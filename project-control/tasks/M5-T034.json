{
  "task_id": "M5-T034",
  "title": "B7: wire the wide-street determination into the R6-R12 conditional-FAR rules (own module) + close the B4 input-bounds gap",
  "task_type": "backend",
  "milestone_id": "M5",
  "objective": "Create the B7 rule-wiring as its OWN module consuming the accepted stack (dcm_street_width_policy + wide_street_buffer_engine): the r6_r7_r8_wide_street_conditional_far rule rows fire ONLY on a policy decision of wide, narrow rows on narrow, and ANY non-{wide,narrow} outcome yields honest UNRESOLVED/professional_review_required (never keyed solely off routed_to). Close the G5 A1 gap with a typed fail-closed input bound in the buffer engine (segment count, per-path vertex count, EPSG:2263 extent sanity). Honor the elevated exceptions_checked criterion; the 2-row named-street override table is OUT OF SCOPE (follow-up) and affected segments must not claim exceptions checked. Add the T019 leftover inequality/conflicting-records/fallback-direction tests; validate and document this rule's own D-051 fallback direction. Rule stays DRAFT/needs_review (D-045-R009).",
  "business_reason": "D-045-R002 A2 geometry mechanics; next packet after M5-T033 per handoff; first production consumer of the accepted wide-street stack",
  "inputs": [
    "EXECUTION ENVIRONMENT (binding, thin client - ~6GB free): api task; NO npm/npx/node, NO WebFetch, NO network. Edit within allowed_paths using native tools, run ONLY the documented python checks (ruff FIRST - it is the api CI job's first step), git add/commit IN YOUR WORKTREE ROOT (plain git, never -C; git writes surface as ASKs the orchestrator answers - propose once and continue), write the producer report, checkpoint with CI-authority evidence deferred to the orchestrator seam.",
    "CHECKPOINT ENVELOPE DISCIPLINE (controller S8.3, binding): in your final checkpoint JSON set starting_sha, current_sha, branch, and worktree to the empty string \"\" - the controller fills authoritative values; any value you supply must match EXACTLY or the run stops. Blank is always safe.",
    "REVIEWER HONESTY BAR (binding - the Codex reviewer enforces this and the run has at most 4 review cycles; write the report to this bar in DRAFT 1): state observations separately from conclusions; never claim exhaustiveness ('exactly two', 'the only cause') unless proven; no 'most likely' rankings; every claim carries its evidence (test name, file:line, or command output); separate what YOU ran from what CI will prove; mark anything unverified as UNVERIFIED; never claim completion or compliance - the gates decide.",
    "CODE-GRAPH NAVIGATION BLOCK (D-066-R001; regenerated 2026-09-17 at this seam: 728 files, 15271 nodes, 6762 edges). The wide-street stack currently has ONLY test consumers - this task creates its FIRST production consumer. Producers: dcm_street_width_policy.py (D-052 decisions: <75 narrow, =75 WIDE, one-sided bounds UNRESOLVED, exceptions-checked-first, provenance quintuple), wide_street_buffer_engine.py (B4: 100.0-ft buffer intersect lot, EPSG:2263), dcm_street_centerline_geometry.py (B3 typed polylines). Rule target: services/api/app/rules/rulesets/r6_r7_r8_wide_street_conditional_far.rule.json (currently cites zr-23-22 ONLY, performs no wide-street determination). Evaluator seam: app/rules/integration.py (FAILSAFE_SPATIAL_ABSENT at :81), route app/api/v1/rule_evaluation.py. For who-consumes/impact questions run 'python tools/code_graph/query.py --no-regen impact <path>' BEFORE broad sweeps. Graph is ADVISORY - verify every material conclusion in source.",
    "B7 BINDING PRECONDITIONS (from the M4-T021 gate reviewers - docs/WORKING_KNOWLEDGE.md 'A2 build map'; binding here): (1) G3 modularity ruling - do NOT split wide_street_buffer_engine.py and do NOT add wiring into it; the wiring lands as its OWN module consuming it (four-file DCM split is the precedent). (2) G5 A1 - the engine has no input-size/coordinate-magnitude bound; before wiring it behind a request path, add a typed fail-closed bound (len(wide_segments), per-path vertex count, plausible-EPSG:2263-extent alongside the existing finiteness check). (3) G3 A1 elevated - exceptions_checked=True ONLY when exceptions were checked AND (none apply OR each applicable one implemented/resolved). (4) G3 A4 - any non-{wide,narrow} policy outcome = 'not classified'; never key routing solely off routed_to.",
    "B4 disclosed judgment calls to respect (docs/WORKING_KNOWLEDGE.md): Ec5AttestedPreconditions is required/no-default and does not gate on attested VALUES; quad_segs=8 pinned for determinism; BOUNDARY_TOLERANCE_FT proven never imported; EC-4 tangency legal-tolerance question stays OPEN for G6 - do not resolve it in this task.",
    "T019 leftovers to include: three inequality cases ('<=74'->narrow is WRONG - verify against D-052: one-sided bounds stay UNRESOLVED; the pinned cases are <=74 -> narrow?, <80 -> UNRESOLVED, >60 -> UNRESOLVED - implement the cases exactly as the D-052 policy module decides them and cite its tests as the oracle); a genuine conflicting-records test; per-rule fallback-direction tests. D-051: fail-closed-to-narrow must be VALIDATED for this specific rule (for the 23-22 conditional-FAR rows, narrow yields the lower FAR - state and test why that direction is conservative HERE) and documented in the rule wiring.",
    "D-045-R009 preservation (prohibition): the rule stays DRAFT/needs_review pending G6 qualified-human approval; provenance quintuple on every decision; no published/verified claim anywhere."
  ],
  "outputs": [
    "New module services/api/app/rules/wide_street_wiring.py: consumes the policy decision + buffer-engine result and produces the typed wide-street determination input for the conditional-FAR rule rows; honest UNRESOLVED path; exceptions_checked semantics per the elevated criterion; named-street override segments (Broadway W94-97, Allen St Rivington-Delancey) explicitly NOT claimed as exceptions-checked (follow-up task noted)",
    "Typed fail-closed input bound added inside wide_street_buffer_engine.py (bound constants + typed failure, no split, no wiring in that file) with deterministic tests",
    "r6_r7_r8_wide_street_conditional_far.rule.json updated to consume the real determination (provenance quintuple; zr-23-22 citation kept; DRAFT/needs_review preserved)",
    "Evaluator/route seam wiring in app/rules/integration.py (+ app/api/v1/rule_evaluation.py if needed) so the determination reaches rule evaluation server-side; every failure stays typed and fail-safe",
    "Tests: wiring module suite (wide/narrow/UNRESOLVED/exceptions/named-street honesty), engine bound suite, T019 inequality + conflicting-records + fallback-direction cases, evaluator-level regression",
    "project-control/reports/M5-T034-producer-report.md (verbatim producer return, written to the REVIEWER HONESTY BAR)",
    "CI api job on the pushed head is the executable authority; the orchestrator captures CI evidence at the seam. REVIEW GUIDANCE (for the Codex reviewer): use the navigation block and 'python tools/code_graph/query.py --no-regen impact services/api/app/connectors/wide_street_buffer_engine.py' to check blast radius and STATE whether the graph information was accurate and useful."
  ],
  "dependencies": [],
  "allowed_paths": [
    "services/api/app/rules/wide_street_wiring.py",
    "services/api/tests/rules/test_wide_street_wiring.py",
    "services/api/app/rules/rulesets/r6_r7_r8_wide_street_conditional_far.rule.json",
    "services/api/app/connectors/wide_street_buffer_engine.py",
    "services/api/tests/connectors/test_wide_street_buffer_engine.py",
    "services/api/app/rules/integration.py",
    "services/api/tests/rules/test_rules_integration.py",
    "services/api/app/api/v1/rule_evaluation.py",
    "services/api/tests/api/test_rule_evaluation_api.py",
    "project-control/reports/M5-T034-producer-report.md"
  ],
  "forbidden_paths": [],
  "acceptance_scenarios": [
    {
      "id": "AS-1",
      "description": "The wiring is its OWN module: wide_street_buffer_engine.py gains ONLY the typed input bound (no wiring, no split); modularity check passes; the wiring module consumes policy + engine through their public interfaces."
    },
    {
      "id": "AS-2",
      "description": "Typed fail-closed input bound: exceeding segment count, per-path vertex count, or plausible-EPSG:2263 extent yields a typed failure that surfaces as honest UNRESOLVED - never a crash, hang, or silent computation - with deterministic tests at and beyond each bound."
    },
    {
      "id": "AS-3",
      "description": "Rule behavior: wide decision -> conditional-FAR wide rows fire; narrow -> narrow rows; UNRESOLVED/not-classified/any non-{wide,narrow} -> professional_review_required with no FAR bonus granted; routing never keyed solely off routed_to."
    },
    {
      "id": "AS-4",
      "description": "exceptions_checked=True only under the elevated criterion; lots whose frontage may touch the two named-street override segments do NOT get exceptions_checked=True (override table is an explicitly-noted follow-up)."
    },
    {
      "id": "AS-5",
      "description": "T019 leftover cases implemented with the D-052 policy module as the oracle (one-sided bounds stay UNRESOLVED), plus a genuine conflicting-records test and per-rule fallback-direction tests proving and documenting why fail-closed-to-narrow is conservative for THESE FAR rows (D-051)."
    },
    {
      "id": "AS-6",
      "description": "Provenance quintuple present on every determination; the rule remains DRAFT/needs_review; no published/verified language anywhere (D-045-R009)."
    },
    {
      "id": "AS-7",
      "description": "ruff and the documented python suites pass; api CI green on the pushed head at the seam (orchestrator-captured)."
    },
    {
      "id": "AS-8",
      "description": "The producer report meets the REVIEWER HONESTY BAR: observations separated from conclusions, no exhaustiveness or ranking claims, every claim evidence-bound, unverified items marked UNVERIFIED."
    }
  ],
  "required_gates": [
    "G0",
    "G1",
    "G2",
    "G3",
    "G4",
    "G5"
  ],
  "producer_agent": "rules-engineer",
  "reviewer_agents": [
    "data-contract-verifier",
    "code-reviewer",
    "qa-engineer",
    "security-reviewer",
    "directive-compliance-verifier"
  ],
  "status": "claimed",
  "progress_percent": 20,
  "risks": [
    "Scope magnet: the named-street override table and EC-4 legal tolerance are OUT of this packet - route them to follow-ups, never resolve them here",
    "The evaluator seam must stay fail-safe: a wiring failure can never produce a guessed district or FAR bonus",
    "api CI runs ruff first - run it before every checkpoint",
    "At most 4 review cycles per run: write the report to the honesty bar in draft 1"
  ],
  "blockers": [],
  "created_at": "2026-09-17T09:58:42.955725+00:00",
  "updated_at": "2026-09-17T10:00:21.441509+00:00",
  "directive_refs": [
    {
      "directive_id": "D-045",
      "requirement_ids": [
        "D-045-R002",
        "D-045-R008",
        "D-045-R009"
      ]
    },
    {
      "directive_id": "D-066",
      "requirement_ids": [
        "D-066-R001"
      ]
    }
  ],
  "directive_regime_version": "1.0",
  "directive_regime_entered_at": "2026-09-17T09:58:42.955725+00:00",
  "documented_test_commands": [
    "python -m ruff check services/api",
    "python -m pytest services/api/tests/rules -q",
    "python -m pytest services/api/tests/connectors/test_wide_street_buffer_engine.py -q",
    "python -m pytest services/api/tests/api/test_rule_evaluation_api.py -q",
    "python tools/modularity_check.py --check",
    "python tools/validate_directive_compliance.py --check"
  ],
  "worktree": "C:\\Users\\MLFLL\\Downloads\\nyc-zoning\\wt-m5t034",
  "progress_log": [
    {
      "at": "2026-09-17T10:00:21.441509+00:00",
      "agent": "rules-engineer",
      "percent": 20,
      "status": "claimed",
      "message": "Contract, G0, bindings complete; awaiting supervisor-loop producer (run 38)"
    }
  ]
}
