{
  "task_id": "M5-T077",
  "title": "DB-051(a)-(e) geometry-threading hardening: ONE null-segments contract (only an absent field or an empty list invites derivation), falsy-malformed net, BBL_UNRESOLVABLE detail pin, at-cap boundary, non-scalar bbl pin - route stays unmounted, engine byte-untouched",
  "task_type": "backend",
  "milestone_id": "M5",
  "objective": "Close DB-051 items (a)-(e) (the M5-T076 wave riders) in ONE bounded change on the geometry-threading seam: (b) make the null-segments contract single - ONLY an ABSENT lot.lot_line_segments or an EMPTY LIST invites server-side derivation; `null` joins every other non-array on the existing typed 422 refusal in every BBL/provider state, and both docstrings stop claiming null==absent; (a) extend the malformed-segments parametrize with the falsy set so the idiomatic `if lot.get(...)` weakening reddens; (c) assert BBL_UNRESOLVABLE's honest detail and that each route-level failure reason reaches placement.detail; (d) pin the at-cap boundary; (e) pin the non-scalar lot.bbl fail-closed behavior. max_envelope.py byte-untouched; the route stays UNMOUNTED; DB-051 (f)/(g) stay with the mount packet.",
  "business_reason": "The T076 G4 proved the malformed-input net covers only half of the exact defect class that caused the T076 G3 FAIL, and the G3-delta found the null spelling behaving differently on the failure path than its docstring promises; closing both before the mount packet keeps the geometry-threading contract single and mutation-guarded.",
  "inputs": [
    "CHECKPOINT ENVELOPE (binding): in every checkpoint, OMIT the four envelope fields entirely - the controller fills them. If you supply one it must be EXACT full-length (40-hex SHA, FULL worktree path).",
    "REPORT DISCIPLINE (binding): reference files and line anchors, never embed long verbatim sections; every checkpoint carries IMPLEMENTATION; one concise evidence pass; the [OBSERVED]/[BLOCKED]/[PREDICTED] evidence-status form is the standard - route what you cannot run to harvest with an exact recipe, never predict-as-observed; explicit cwd on every recorded command.",
    "COMMAND CWD (binding): api work runs from services/api - `python -m ruff check .` FIRST, then the documented pytest; `python tools/modularity_check.py --check` from repo root. KNOWN BROKER LIMIT: documented commands may be runnable only from the worktree root - route blocked runs to harvest with an exact recipe, never predict-as-observed.",
    "SPEC SOURCE (binding): docs/DISCOVERY_BACKLOG.md DB-051 items (a)-(e) (verbatim in the row) + project-control/reports/M5-T076-G4.md F1-F4 (lines 19-28: the truthy-only parametrize at tests/api/test_max_envelope_api.py:668-694; the BBL_UNRESOLVABLE detail gap at tests/scenario/test_lot_geometry_derivation.py:423-428 and route :591/:608; the at-cap boundary at lot_geometry_derivation.py:177; the non-scalar bbl at max_envelope_api.py:177-180) + project-control/reports/M5-T076-G3-delta.md line 22 (the null-path asymmetry). Re-verify every anchor in actual source before editing.",
    "ORCHESTRATOR RULING FOR (b) (binding): 'drop None from the derivable set'. _should_derive_lot_geometry returns True ONLY when lot_line_segments is ABSENT or an EMPTY LIST (and the BBL is a usable scalar); `null` stays on _build_lot_context's typed 422 (`lot.lot_line_segments must be an array`) in EVERY state - no BBL, BBL + passing provider, BBL + failing provider - with NO provider call. Why: one rule and one refusal class, the same refusal null already gets without a BBL (the pre-T076 contract); no client sends null (max-envelope-api.ts sends []). Update test_absent_or_null_segments_with_a_bbl_still_derive accordingly (absent and [] derive; null refuses) and correct the docstrings in max_envelope_api.py AND lot_geometry_derivation.py so no text claims null==absent.",
    "HARD MODULARITY BOUND (binding): services/api/app/scenario/max_envelope.py (998 SLOC, AT the 1000-line hard threshold) stays BYTE-UNTOUCHED - prove by blob comparison in the report. Production edits are limited to the guard + docstrings in max_envelope_api.py and docstrings in lot_geometry_derivation.py; everything else is tests.",
    "TESTS + MUTATION OBLIGATIONS (binding; offline only, recorded fixtures, no network): (a) the malformed parametrize = the existing truthy set PLUS 0, \"\", {}, False, 0.0 - the idiomatic weakening `if lot.get(\"lot_line_segments\"): return False` must redden >=1 case; (b) re-admitting None to the derivable set must redden a named test; (c) blanking the BBL_UNRESOLVABLE detail must redden; (d) the cutter's `>` -> `>=` off-by-one must redden (count == max_segments derives, max_segments + 1 is geometry_over_cap); (e) the non-scalar bbl test asserts NO provider call via the injected provider spy. Record each mutant run (red/green) in the report.",
    "CODE-GRAPH NAVIGATION BLOCK (D-066-R001; graph REGENERATED at this contract seam: 817 files/17027 nodes/7441 edges): max_envelope_api.py consumers = tests/api/test_max_envelope_api.py ONLY (the route is UNMOUNTED - app/main.py does not import it; keep it that way); lot_geometry_derivation.py consumers = max_envelope_api.py:76 + tests/scenario/test_lot_geometry_derivation.py. mappluto_geometry_arcgis.py, max_envelope.py, derivation.py, app/main.py and every web file are FORBIDDEN. Run `python tools/code_graph/query.py --no-regen impact <path>` before sweeps; graph ADVISORY - verify every material conclusion in actual source.",
    "DISCOVERY ROUTING (D-069, binding): out-of-scope product/domain/defect findings go in the report's DISCOVERIES section - never fixed in-packet; the orchestrator records them in docs/DISCOVERY_BACKLOG.md at the seam."
  ],
  "outputs": [
    "services/api/app/api/v1/max_envelope_api.py: the single null-segments contract (absent or [] alone invite derivation) + corrected docstring",
    "services/api/app/scenario/lot_geometry_derivation.py: docstring alignment only (no behavior change)",
    "Offline test additions in tests/api/test_max_envelope_api.py + tests/scenario/test_lot_geometry_derivation.py covering DB-051 (a)-(e), each with a recorded mutant run",
    "project-control/reports/M5-T077-producer-report.md (one concise evidence pass; digest-bound)"
  ],
  "dependencies": [
    "M5-T076"
  ],
  "allowed_paths": [
    "services/api/app/api/v1/max_envelope_api.py",
    "services/api/app/scenario/lot_geometry_derivation.py",
    "services/api/tests/api/test_max_envelope_api.py",
    "services/api/tests/scenario/test_lot_geometry_derivation.py",
    "project-control/reports/M5-T077-producer-report.md"
  ],
  "forbidden_paths": [
    "services/api/app/scenario/max_envelope.py",
    "services/api/app/scenario/derivation.py",
    "services/api/app/connectors/",
    "services/api/app/main.py",
    "apps/web/",
    "packages/contracts/"
  ],
  "acceptance_scenarios": [
    "AS-1 (one null contract): absent lot_line_segments and [] derive with a passing provider; `null` returns 422 validation_error on lot.lot_line_segments with NO provider call in each of three states (no BBL; BBL + passing provider; BBL + failing provider); both docstrings name only 'absent or empty list'; mutation - re-admitting None to the derivable set reddens a named test.",
    "AS-2 (falsy-malformed net): the malformed parametrize covers the truthy set plus 0, \"\", {}, False, 0.0 - each 422 with no provider call; mutation - the idiomatic `if lot.get(...)` weakening reddens at least one case (run recorded).",
    "AS-3 (taxonomy details): BBL_UNRESOLVABLE's honest detail is asserted at unit level (non-empty, names the unresolvable-BBL reason); for each derivation failure class the route test asserts the reason reaches placement.detail; mutation - blanking the BBL_UNRESOLVABLE detail reddens.",
    "AS-4 (boundaries): a ring with exactly max_segments exterior segments derives all of them; max_segments + 1 is geometry_over_cap; a non-scalar lot.bbl ({\"x\": 1} and a list) returns 200 with the honest lot_geometry_unsupported gap, NO provider call, and no derived_lot_geometry block (pinned as the documented fail-closed behavior); mutation - `>` -> `>=` reddens.",
    "AS-5 (scope + modularity): exactly the allowed_paths change; max_envelope.py byte-untouched (blob compare in the report); ruff clean; modularity exit 0; the route stays unmounted; no new dependency."
  ],
  "required_gates": [
    "G0",
    "G2",
    "G3",
    "G4"
  ],
  "producer_agent": "backend-engineer",
  "reviewer_agents": [
    "code-reviewer",
    "qa-engineer",
    "directive-compliance-verifier"
  ],
  "status": "accepted",
  "progress_percent": 100,
  "risks": [],
  "blockers": [],
  "created_at": "2026-09-24T07:41:56.146023+00:00",
  "updated_at": "2026-09-24T09:54:29.832848+00:00",
  "directive_refs": [
    {
      "directive_id": "D-066",
      "requirement_ids": [
        "D-066-R001"
      ]
    },
    {
      "directive_id": "D-087",
      "requirement_ids": [
        "D-087-R001",
        "D-087-R002"
      ]
    }
  ],
  "directive_regime_version": "1.0",
  "directive_regime_entered_at": "2026-09-24T07:41:56.146023+00:00",
  "documented_test_commands": [
    "python -m ruff check .",
    "python -m pytest tests/scenario/test_lot_geometry_derivation.py tests/api/test_max_envelope_api.py -q",
    "python tools/modularity_check.py --check"
  ],
  "worktree": "C:\\Users\\MLFLL\\Downloads\\nyc-zoning\\wt-m5t077",
  "progress_log": [
    {
      "at": "2026-09-24T07:43:27.717121+00:00",
      "agent": "backend-engineer",
      "percent": 20,
      "status": "claimed",
      "message": "Contract seam complete: G0 PASS at f57fccd5; worktree wt-m5t077 at the claim-seam commit; launch/dispatch next"
    },
    {
      "at": "2026-09-24T07:55:42.867158+00:00",
      "agent": "orchestrator",
      "percent": 20,
      "status": "claimed",
      "message": "Scope correction (B-026): the lane launch refused - Claude Code 2.1.281 is not yet admitted to the certified controller - so this packet runs as an orchestrator-dispatched subagent producer in its claimed worktree; D-084 lane-occupancy binding removed (same-commit digest resync); D-087-R001/R002 and every other binding, scope, gate and acceptance scenario unchanged."
    },
    {
      "at": "2026-09-24T08:21:08.245371+00:00",
      "agent": "backend-engineer",
      "percent": 95,
      "status": "claimed",
      "message": "Unit harvested (f63cfad1 MATCH x5; primary re-run ruff clean + 64 passed); submitting for the independent wave"
    }
  ],
  "accepted_by": "orchestrator",
  "accepted_at": "2026-09-24T09:54:29.830842+00:00"
}
