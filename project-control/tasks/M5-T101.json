{
  "task_id": "M5-T101",
  "title": "D-087 PKT-C: building-footprint connector wiring-hardening before any wiring (DB-058 a-d, f, g; DB-066 a-d) - split the geometry helpers out first, bound per-call memory and wall-clock, server-side-safe correlation ids, a declared ground datum, and the source_registry draft",
  "task_type": "backend",
  "milestone_id": "M5",
  "objective": "Harden services/api/app/connectors/building_footprints_arcgis.py (907 SLOC / 50 symbols, WARN + symbol ceiling) BEFORE any route or scene assembler wires it. Step 1 (split first, no behaviour change): move the ring/geometry helpers (_ring, parse_footprint_geometry, classify_query_relation and their private helpers) into a NEW module services/api/app/connectors/building_footprints_geometry.py behind compatibility re-exports so every name the tests import still resolves from the connector; the 117 existing tests pass unchanged. Step 2 (the riders): (b) a per-geometry vertex cap and a cumulative decoded-bytes ceiling across pages, typed refusals; (c) an optional wall-clock deadline across paging (a caller-supplied budget object or deadline, checked before each page) and a lower max_attempts for interactive callers - declare the interface; (d) + DB-066 (a)(b): never log a caller correlation id raw - strip CR/LF and control characters (or replace with a server-generated id) at the connector log sites, and make the class-name log field strip CR/LF outright (the allowlist regex's `$` passes a trailing newline); DB-066 (c)(d): add a short alphanumeric secret-message no-leak probe and a typed-refusal-emits-no-ERROR-log probe; (f) declare the ground-elevation datum semantics on the record (the City dictionary's NAVD88 lowest-ground definition vs the FGDC centroid-interpolated DTM definition stay DISCLOSED, never silently reconciled; a zero stays a typed unverified value); (a) mark attribute/geom_source/last_status_type text as UNTRUSTED for consumers (a typed flag or documented contract field) - escaping itself happens at render in PKT-E/PKT-I; (g) author docs/research/source-registry-drafts/building-footprints.json mirroring the existing drafts (dcm-street-centerline.json, dtm-condo.json) from the accepted research note and the connector's own constants.",
  "business_reason": "The official footprint connector (D-087-R003) supplies the 3D context buildings; the accepted plan requires these guards and the source_registry record before any scene/route packet (PKT-E) may consume it, and the file must split before it grows past the modularity cap.",
  "inputs": [
    "PRODUCER MODE (binding): you are an orchestrator-dispatched subagent producer working ONLY inside your task worktree (named in the dispatch). Every file path you write is under that worktree; commit your work there as ONE commit and report its sha. Never touch the primary checkout, the ledger, or any path outside allowed_paths.",
    "REPORT DISCIPLINE (binding): reference files and line anchors, never embed long verbatim sections; every checkpoint carries IMPLEMENTATION; one concise evidence pass; the [OBSERVED]/[BLOCKED]/[PREDICTED] evidence-status form is the standard - route what you cannot run to harvest with an exact recipe, never predict-as-observed; explicit cwd on every recorded command.",
    "COMMAND CWD (binding): api work runs from services/api - `python -m ruff check .` FIRST, then the documented pytest; `python tools/modularity_check.py --check` from repo root. KNOWN BROKER LIMIT: documented commands may be runnable only from the worktree root - route blocked runs to harvest with an exact recipe, never predict-as-observed.",
    "ZERO NEW DEPENDENCIES (binding; D-087-R009 + the dependency-security policy): stdlib plus already-admitted packages only (shapely/numpy/pydantic/fastapi are admitted); no lockfile or requirements change; no network access in code or tests.",
    "NO EXPOSURE (binding): no route, endpoint, app/main.py, or web change - this packet ships a tested module only; wiring is a later gated packet. The max-envelope route stays UNMOUNTED (its mount preconditions are unchanged).",
    "PLAN SOURCE (binding): the ACCEPTED M5-T099 plan docs/design/d087-export-and-3d-viewer-plan.md (sections 1, 5, 6 and its rider map) names this packet; its riders and mandatory criteria are the scope. Where the plan and the backlog row differ, the backlog row's exact wording wins and the difference goes in the report.",
    "SPEC SOURCE (binding): docs/DISCOVERY_BACKLOG.md DB-058 (a), (b), (c), (d), (f), (g) and DB-066 (a)-(d) (exact wording); DB-053 (e) is already discharged (live field names) and DB-053 (f)/(h) are the datum/inference notes; project-control/reports/M5-T089-G1.md, -G3.md, -G5.md and M5-T100-G3.md, -G4.md, -G5.md; the research note docs/research/building-footprints-source-2026-09.md (accepted M5-T087) for the registry draft. Never guess a field meaning, unit or datum: an item the sources do not settle is recorded as an open question in the draft.",
    "SPLIT FIRST (binding; docs/CODE_MODULARITY_POLICY.md + .claude/rules/code-architecture.md): step 1 is a behaviour-preserving extraction committed in the SAME single commit as step 2, but provable on its own: after the split alone, all 117 existing tests pass unchanged and every recorded fixture stays byte-identical (the sha256-pinned fixture test). The connector must end below the 1000-SLOC hard cap with the symbol ceiling cleared or justified.",
    "CODE-GRAPH NAVIGATION BLOCK (D-066-R001; graph REGENERATED at this contract seam, 827 files / 17839 nodes / 7656 edges): the connector is imported only by tests/connectors/test_building_footprints_arcgis.py (two import lines); app/resilience/transport.py (shared by every connector) and mappluto_geometry_arcgis.py are READ-ONLY here; the recorded fixtures under tests/fixtures/building_footprints/ are byte-frozen. Run `python tools/code_graph/query.py --no-regen impact <path>` before sweeps; graph ADVISORY - verify every material conclusion in actual source.",
    "DISCOVERY ROUTING (D-069, binding): out-of-scope product/domain/defect findings go in the report's DISCOVERIES section - never fixed in-packet; the orchestrator records them in docs/DISCOVERY_BACKLOG.md at the seam."
  ],
  "outputs": [
    "services/api/app/connectors/building_footprints_geometry.py - extracted ring/geometry helpers (new)",
    "services/api/app/connectors/building_footprints_arcgis.py - riders (b)(c)(d)(f)(a) + compatibility re-exports",
    "services/api/tests/connectors/test_building_footprints_arcgis.py - rider tests + DB-066 (c)(d) probes",
    "docs/research/source-registry-drafts/building-footprints.json - the source_registry draft (DB-058 g)",
    "project-control/reports/M5-T101-producer-report.md"
  ],
  "dependencies": [
    "M5-T100"
  ],
  "allowed_paths": [
    "services/api/app/connectors/building_footprints_arcgis.py",
    "services/api/app/connectors/building_footprints_geometry.py",
    "services/api/tests/connectors/test_building_footprints_arcgis.py",
    "docs/research/source-registry-drafts/building-footprints.json",
    "project-control/reports/M5-T101-producer-report.md"
  ],
  "forbidden_paths": [
    "services/api/tests/fixtures/building_footprints/",
    "services/api/app/resilience/",
    "services/api/app/connectors/mappluto_geometry_arcgis.py",
    "services/api/app/scenario/",
    "services/api/app/api/",
    "services/api/app/main.py",
    "services/api/requirements.txt",
    "services/api/requirements.in",
    "apps/",
    "packages/"
  ],
  "acceptance_scenarios": [
    "AS-1 (split, byte-identical): the geometry helpers live in building_footprints_geometry.py; every name the existing tests import still resolves from building_footprints_arcgis; the 117 existing tests pass unchanged and the fixture pack stays byte-identical; imports acyclic; modularity_check exit 0 with the connector below 1000 SLOC.",
    "AS-2 (memory + time bounds): a per-geometry vertex cap and a cumulative decoded-bytes ceiling refuse typed before unbounded retention; a caller deadline stops paging with a typed refusal; interactive max_attempts is lower and declared; one mutation per guard reddens (cap removed, ceiling removed, deadline ignored).",
    "AS-3 (log safety): a caller correlation id carrying CR/LF/control characters never reaches a log line raw (or is replaced server-side - declare which); a class __name__ with a TRAILING newline is logged without a raw newline; a short alphanumeric secret in an exception message never reaches the log; a typed refusal emits no ERROR record; mutation per probe reddens.",
    "AS-4 (datum + untrusted text, honest): each record discloses the ground-elevation datum semantics (both official definitions, none silently chosen) and marks source text as untrusted for consumers; no default replaces a gap.",
    "AS-5 (registry draft): docs/research/source-registry-drafts/building-footprints.json mirrors the existing drafts' shape, its source_id equals the connector's SOURCE_ID, every field/unit/cadence statement cites the accepted research note or the live service metadata, and open questions (height unit inference, datum) are listed as open.",
    "AS-6 (scope): zero new dependencies; unwired; no route/main.py/web change; exactly the allowed paths."
  ],
  "required_gates": [
    "G0",
    "G1",
    "G2",
    "G3",
    "G4",
    "G5"
  ],
  "producer_agent": "geospatial-engineer",
  "reviewer_agents": [
    "data-contract-verifier",
    "code-reviewer",
    "qa-engineer",
    "security-reviewer",
    "directive-compliance-verifier"
  ],
  "status": "accepted",
  "progress_percent": 100,
  "risks": [],
  "blockers": [],
  "created_at": "2026-09-24T21:27:24.129344+00:00",
  "updated_at": "2026-09-25T00:38:12.428059+00:00",
  "directive_refs": [
    {
      "directive_id": "D-087",
      "requirement_ids": [
        "D-087-R001",
        "D-087-R002",
        "D-087-R003",
        "D-087-R009"
      ]
    },
    {
      "directive_id": "D-066",
      "requirement_ids": [
        "D-066-R001"
      ]
    }
  ],
  "directive_regime_version": "1.0",
  "directive_regime_entered_at": "2026-09-24T21:27:24.130665+00:00",
  "documented_test_commands": [
    "python -m ruff check .",
    "python -m pytest tests/connectors/test_building_footprints_arcgis.py -q",
    "python tools/modularity_check.py --check"
  ],
  "worktree": "C:\\Users\\MLFLL\\Downloads\\nyc-zoning\\wt-m5t101",
  "progress_log": [
    {
      "at": "2026-09-24T21:27:57.578693+00:00",
      "agent": "geospatial-engineer",
      "percent": 20,
      "status": "claimed",
      "message": "Claimed at the wave-7 contract seam 40169b9c; worktree C:\\Users\\MLFLL\\Downloads\\nyc-zoning\\wt-m5t101 created at the claim-seam commit; orchestrator-dispatched subagent producer follows."
    },
    {
      "at": "2026-09-24T22:13:24.653192+00:00",
      "agent": "geospatial-engineer",
      "percent": 90,
      "status": "in_progress",
      "message": "Harvested: 6df9abad cherry-picked to 6a0f7f53 (5 files MATCH); harvest checks green; evidence map + G2 written; submitting."
    }
  ],
  "accepted_by": "orchestrator",
  "accepted_at": "2026-09-25T00:38:12.428059+00:00"
}
