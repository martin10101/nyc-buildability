{
  "task_id": "M5-T048",
  "title": "Proposal scenario contract (phase B0, D-076): the proposed_massing input class - additive scenario-contract bump + validation module + typed refusals",
  "task_type": "backend",
  "milestone_id": "M5",
  "objective": "Open phase B of the proposal-editor plan (docs/PROPOSAL_EDITOR_PHASED_PLAN.md): give the scenario contract a proposed_massing INPUT CLASS so an architect-proposed building becomes a first-class, clearly-labeled scenario input - never a city record, never a rule. (1) Contract: additive scenario-contract version bump per the M5-T037 recipe (append the new version to the closed enum in BOTH schema copies byte-identically; the optional proposed_massing block under additionalProperties:false; TS types regenerated byte-identically via the typegen check; documents emit the new version ONLY when the block is present; prior-version documents stay valid - recorded fixtures green). (2) The block's shape (from the plan): lot-anchored building outline (EPSG:2263 feet, bounded vertex list), per-level records (floor count, floor-to-floor heights, optional per-level outline where it differs), named exterior wall segments, and a provenance stamp (author, kind=proposed, editor version, optional parent scenario id). (3) Validation in a NEW focused module services/api/app/scenario/proposal.py: closed non-self-intersecting outline, positive finite heights, level consistency, unit sanity, bounded counts (the DB-013 ceiling pattern), every failure a TYPED refusal - no evaluation or derivation change anywhere yet (that is B1/B2). Valid + invalid fixtures added under packages/contracts/fixtures",
  "business_reason": "Owner-authorized phase B start (D-076 'Plan it'): everything in the proposal editor depends on this contract; B1 derivation and B3 UI consume it",
  "inputs": [
    "CHECKPOINT ENVELOPE (binding): in every checkpoint, OMIT the four envelope fields (branch, worktree, starting_sha, current_sha) entirely - the controller fills the authoritative values. If you supply one it must be EXACT full-length (40-hex SHA, FULL worktree path).",
    "REPORT DISCIPLINE (binding): reference files and line anchors, never embed long verbatim sections; every checkpoint carries IMPLEMENTATION; one concise evidence pass; ask for per-file digest-bound collection if evidence is large. The review-round budget is spent on code.",
    "COMMAND CWD (binding): ruff + api/scenario pytest from services/api; the typegen check and contracts-script tests from the REPO ROOT (they are repo-root commands, see documented_test_commands); modularity from the repo root. Never chain onto a documented command. No local npm/npx/node ever.",
    "CODE-GRAPH NAVIGATION BLOCK (D-066-R001; graph regenerated at the seam, 743 files/15656 nodes). The scenario package: contract.py (the 1.0.0 contract assembly), models/builder/derive/comparison/ranking/breakeven/unused_floor_area (NOT in scope - consumers to keep green), tests/scenario/* suites. Schema copies: services/api/app/_contract_schemas/v1/scenario.schema.json AND packages/contracts/schemas/v1/scenario.schema.json (must stay byte-identical); generated TS: packages/contracts/generated/scenario.ts (typegen check regenerates byte-identically); fixtures: packages/contracts/fixtures/{valid,invalid}/scenario/. The M5-T037 rule_evaluation 1.1.0 bump (commit a6875731) is the proven recipe - read its diff shape first. Run `python tools/code_graph/query.py --no-regen impact <path>` before any sweep; graph ADVISORY - verify in source.",
    "SCOPE 1/2 - CONTRACT BUMP (binding; read docs/PROPOSAL_EDITOR_PHASED_PLAN.md phase B + B0 row, and the M5-T037 recipe FIRST): append '1.1.0' to the CLOSED contract-version enum in BOTH scenario.schema.json copies (byte-identical); add the OPTIONAL proposed_massing block under additionalProperties:false with: outline (EPSG:2263 feet vertex list, closed ring, bounded count), levels (count, per-level floor-to-floor height ft, optional per-level outline), named exterior wall segments (id + the two outline vertex indices they span), and the provenance stamp (author, kind - the literal 'proposed', editor_version, optional parent_scenario_id). 1.0.0 documents stay valid (all recorded fixtures green); a document emits 1.1.0 ONLY when the block is present; regenerate packages/contracts/generated/scenario.ts via the typegen script so the --check passes byte-identically; add at least TWO valid and THREE invalid fixtures under packages/contracts/fixtures/{valid,invalid}/scenario/ (open ring; self-intersecting outline; non-finite height).",
    "SCOPE 2/2 - VALIDATION MODULE (binding): NEW focused module services/api/app/scenario/proposal.py - validate_proposed_massing(...) consuming the parsed block: closed ring (first==last or explicit closure rule - document which), NON-self-intersecting outline (a deterministic segment-intersection check; shapely is available - is_valid/is_simple acceptable with the exact predicate documented), positive finite heights, level consistency (count matches records; per-level outlines each valid), unit sanity (2263 feet magnitudes - coordinate bounds for NYC), bounded ceilings (MAX_OUTLINE_VERTICES, MAX_LEVELS - the DB-013 typed fail-closed pattern), every failure a TYPED refusal naming the exact field; NO evaluation/derivation/builder change anywhere (B1/B2 territory); contract.py changes limited to admitting + threading the validated optional block and version. Answer the seven modularity boundary questions in your plan. PROPOSALS ARE A THIRD INPUT CLASS (D-076-R002 binding): never a city record, never a rule; kind stays the literal 'proposed'; nothing in this packet computes or implies an allowance.",
    "PRESERVATION (binding): every existing scenario behavior byte-identical for 1.0.0 documents (all scenario + api suites green unchanged); schema copies byte-identical to each other; typegen deterministic; no new dependency; no network in tests; NO builder/derive/api-route change; fixtures labeled; empty test files fail vitest-side only (py empties are fine, but seed real probes).",
    "Discovery routing (D-069): surface out-of-scope findings in the report; never fix in-packet. No WebFetch. Thin client. Run `python -m ruff check .` from services/api before every checkpoint.",
    "[ORCH-PACKET-FIX 2026-09-19] The two fixture-directory allowed_paths entries were written as bare directory prefixes, which the broker's glob matcher does not treat as prefixes (S4.1 refused the first fixture write) - corrected to '**' globs in BOTH packet copies. No scope change: the same two fixture directories, now matchable."
  ],
  "outputs": [
    "scenario contract 1.1.0: optional proposed_massing block in BOTH schema copies (byte-identical) + regenerated TS types + valid/invalid fixtures; 1.0.0 documents untouched-valid",
    "services/api/app/scenario/proposal.py validation module with typed refusals and bounded ceilings; contract.py admits + threads the validated block",
    "Scenario + api + contracts-script suites green; typegen --check byte-identical",
    "project-control/reports/M5-T048-producer-report.md (one concise evidence pass)"
  ],
  "dependencies": [
    "M5-T046"
  ],
  "allowed_paths": [
    "packages/contracts/schemas/v1/scenario.schema.json",
    "packages/contracts/generated/scenario.ts",
    "packages/contracts/fixtures/valid/scenario/**",
    "packages/contracts/fixtures/invalid/scenario/**",
    "services/api/app/_contract_schemas/v1/scenario.schema.json",
    "services/api/app/scenario/contract.py",
    "services/api/app/scenario/proposal.py",
    "services/api/tests/scenario/test_scenario_contract.py",
    "services/api/tests/scenario/test_scenario_proposal.py",
    "project-control/reports/M5-T048-producer-report.md"
  ],
  "forbidden_paths": [
    "services/api/app/scenario/builder.py",
    "services/api/app/scenario/derive.py",
    "services/api/app/api/",
    "services/api/app/rules/",
    "services/api/app/spatial/",
    "services/api/app/profile/",
    "services/api/app/connectors/",
    "apps/web/"
  ],
  "acceptance_scenarios": [
    "AS-1 (primary): a valid proposed_massing block (closed simple outline, consistent levels, named walls, provenance kind='proposed') validates, threads through contract.py, and the document emits version 1.1.0 with the block; the same document WITHOUT the block emits 1.0.0 byte-identically to the prior shape.",
    "AS-2 (schema): both schema copies byte-identical; typegen --check passes with zero diff; every recorded 1.0.0 fixture still validates.",
    "AS-3 (invalid shapes): open ring, self-intersecting outline, non-finite/negative height, level-count mismatch, over-ceiling vertex/level counts each yield the TYPED refusal naming the exact field - and the matching invalid fixtures fail schema validation.",
    "AS-4 (third-input-class honesty, D-076-R002): nothing in the block or module computes, stores, or implies an allowance or a city-record value; kind is the literal 'proposed'; the module docstring states the records/rules/proposals distinction.",
    "AS-5 (bounds): NYC-plausible 2263 coordinate bounds enforced; MAX ceilings typed fail-closed (DB-013 pattern) with tests at the ceiling and one past it.",
    "AS-6 (regression): full tests/scenario suite + tests/api suite green unchanged; contracts-script tests green; no builder/derive/api change (git status proves the file set).",
    "AS-7: ruff clean from services/api; modularity exit 0 (new module focused; boundary answers in the plan).",
    "AS-8: CI green on the pushed head (orchestrator captures)."
  ],
  "required_gates": [
    "G0",
    "G1",
    "G2",
    "G3",
    "G4",
    "G5"
  ],
  "producer_agent": "backend-engineer",
  "reviewer_agents": [
    "data-contract-verifier",
    "code-reviewer",
    "qa-engineer",
    "security-reviewer",
    "directive-compliance-verifier"
  ],
  "status": "in_progress",
  "progress_percent": 20,
  "risks": [
    "Schema copies drifting is fail-closed in CI - edit both, byte-identically, in the same change.",
    "Typegen must be run via the repo script only (deterministic output; --check is the proof).",
    "Do not let validation grow into derivation - footprint area, coverage, FAR are B1, not here.",
    "Disjointness: vs live M5-T045 (spatial/profile/connectors/architect) and in-review M5-T047 (lot_geometry route + address components) - zero overlap; forbidden_paths pin the neighbors."
  ],
  "blockers": [],
  "created_at": "2026-09-19T07:59:00.098287+00:00",
  "updated_at": "2026-09-19T08:01:00.782290+00:00",
  "directive_refs": [
    {
      "directive_id": "D-066",
      "requirement_ids": [
        "D-066-R001"
      ]
    },
    {
      "directive_id": "D-076",
      "requirement_ids": [
        "D-076-R001",
        "D-076-R002",
        "D-076-R003"
      ]
    }
  ],
  "directive_regime_version": "1.0",
  "directive_regime_entered_at": "2026-09-19T07:59:00.098287+00:00",
  "documented_test_commands": [
    "python -m ruff check .",
    "python -m pytest tests/scenario -q",
    "python -m pytest tests/api -q",
    "python packages/contracts/scripts/generate_ts_types.py --check",
    "python -m pytest packages/contracts/scripts/tests -q",
    "python tools/modularity_check.py --check"
  ],
  "worktree": "C:\\Users\\MLFLL\\Downloads\\nyc-zoning\\wt-m5t048",
  "progress_log": [
    {
      "at": "2026-09-19T08:01:00.782290+00:00",
      "agent": "backend-engineer",
      "percent": 20,
      "status": "in_progress",
      "message": "Contracted + claimed at the T047-harvest seam (G0 PASS at 7e7dd851; pairwise disjoint vs T045 and T047, intersections empty); scenarios AS-1..AS-8 in-packet; awaiting loop-2 run 12"
    }
  ]
}
