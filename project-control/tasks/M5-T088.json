{
  "task_id": "M5-T088",
  "title": "D-087 3D-1b: massing truth-object hardening BEFORE any wiring (DB-054 a-j) - non-star concave, clockwise, collinear and near-boundary fixtures; lot-ring magnitude bound; triangulation and output budgets; floor-cap refusal (module + tests; no route)",
  "task_type": "backend",
  "milestone_id": "M5",
  "objective": "Close the accepted M5-T082 massing model's review riders DB-054 (a)-(j) in services/api/app/scenario/massing_model.py and its test file before any route, GLB or viewer consumes user geometry: add the missing fixtures (a non-star-shaped concave footprint such as a U or comb, clockwise lot and footprint rings, an exact collinear vertex, a footprint about 0.5 ft outside the lot and one exactly on the lot line, non-integer EPSG:2263 coordinates for the volume conditioning) and the missing bounds (a lot-ring and footprint coordinate magnitude bound with a typed refusal, a triangulation work budget that refuses before the worst-case O(n^3) ear scan runs away, a total output vertex ceiling, and a floor-count cap whose refusal reason names floors). Rename or repair the misnamed lot-with-hole test. Keep every accepted behaviour; the golden digest changes only if output bytes change (explain).",
  "business_reason": "The owner asked for real 3D handling of buildings and lots (D-087-R003); the accepted truth object is correct on every probed shape but its reviewers named test gaps and resource bounds that must close before real user geometry reaches it.",
  "inputs": [
    "PRODUCER MODE (binding): you are an orchestrator-dispatched subagent producer working ONLY inside your task worktree (named in the dispatch). Every file path you write is under that worktree; commit your work there as ONE commit and report its sha. Never touch the primary checkout, the ledger, or any path outside allowed_paths.",
    "REPORT DISCIPLINE (binding): reference files and line anchors, never embed long verbatim sections; every checkpoint carries IMPLEMENTATION; one concise evidence pass; the [OBSERVED]/[BLOCKED]/[PREDICTED] evidence-status form is the standard - route what you cannot run to harvest with an exact recipe, never predict-as-observed; explicit cwd on every recorded command.",
    "COMMAND CWD (binding): api work runs from services/api - `python -m ruff check .` FIRST, then the documented pytest; `python tools/modularity_check.py --check` from repo root. KNOWN BROKER LIMIT: documented commands may be runnable only from the worktree root - route blocked runs to harvest with an exact recipe, never predict-as-observed.",
    "ZERO NEW DEPENDENCIES (binding; D-087-R009 + the dependency-security policy): stdlib plus already-admitted packages only (shapely/numpy/pydantic/fastapi are admitted); no lockfile or requirements change; no network access in code or tests.",
    "HONESTY (binding; D-073-R006/D-076-R002/D-083 vocabulary): proposal-derived geometry is 'Proposed - not a city record'; a generated option is a 'Generated building option'; nothing is ever labeled permitted, approved, or 'maximum allowed building'; no legal conclusion is drawn.",
    "NO EXPOSURE (binding): no route, endpoint, app/main.py, or web change - this packet ships a tested module only; wiring is a later gated packet. The max-envelope route stays UNMOUNTED (its mount preconditions are unchanged).",
    "SPEC SOURCE (binding): docs/DISCOVERY_BACKLOG.md DB-054 (a)-(j) and the reviews they cite - project-control/reports/M5-T082-G4.md (F1-F8 with the exact probes, e.g. a naive fan on a U-shape gives cap area 9400 vs 5200; tolerance 50 accepts a 30-ft overhang), M5-T081-G5.md section M5-T082 (F1 lot_ring magnitude, F2 _triangulate O(n^3), F3 output size) and M5-T082-G3.md. DB-054 (k)-(o) belong to the GLB/wiring packets - do NOT change the vertical-unit declaration, per-level nesting, interface caps or wall semantics here.",
    "TESTS + MUTATION (binding): each new guard gets a test that reddens under its named mutation - naive fan triangulation (on the NEW concave fixture, not only the golden), dropped CW normalization, dropped collinear collapse, containment tolerance 1e-6 -> 50, world-coordinate volume, removed magnitude bound, removed triangulation budget, removed floor cap. Mutate the CONSUMING namespace; record each mutant RED -> restore GREEN in the report.",
    "CODE-GRAPH NAVIGATION BLOCK (D-066-R001; graph REGENERATED at this contract seam): massing_model.py has no production importer yet (only its test); services/api/app/scenario/max_envelope.py and every other scenario module are FORBIDDEN; the GLB writer (M5-T092) will consume the mesh later through a generic interface. Run `python tools/code_graph/query.py --no-regen impact <path>` before sweeps; graph ADVISORY - verify every material conclusion in actual source.",
    "DISCOVERY ROUTING (D-069, binding): out-of-scope product/domain/defect findings go in the report's DISCOVERIES section - never fixed in-packet; the orchestrator records them in docs/DISCOVERY_BACKLOG.md at the seam."
  ],
  "outputs": [
    "services/api/app/scenario/massing_model.py - bounds + typed refusals added, accepted behaviour kept",
    "services/api/tests/scenario/test_massing_model.py - the DB-054 fixtures and mutation-sensitive tests",
    "project-control/reports/M5-T088-producer-report.md"
  ],
  "dependencies": [
    "M5-T082"
  ],
  "allowed_paths": [
    "services/api/app/scenario/massing_model.py",
    "services/api/tests/scenario/test_massing_model.py",
    "project-control/reports/M5-T088-producer-report.md"
  ],
  "forbidden_paths": [
    "services/api/app/scenario/max_envelope.py",
    "services/api/app/scenario/lot_geometry_derivation.py",
    "services/api/app/api/",
    "services/api/app/main.py",
    "services/api/requirements.txt",
    "services/api/requirements.in",
    "apps/",
    "packages/"
  ],
  "acceptance_scenarios": [
    "AS-1 (concave): a non-star concave fixture (U or comb) joins the cap-area, closure and triangle-count parametrizations; mutation - a naive fan triangulation reddens a concave-specific assertion on that fixture (not only the golden).",
    "AS-2 (orientation + collinear): clockwise lot and footprint rings normalize to CCW with positive volume; an exact collinear vertex collapses; mutations - dropping CW normalization / dropping the collinear collapse - each redden a named test.",
    "AS-3 (containment boundary): a footprint about 0.5 ft outside the lot is refused footprint_outside_lot and on-lot-line behaviour is pinned; mutation - tolerance 1e-6 -> 50 reddens.",
    "AS-4 (bounds): coordinate magnitude, triangulation budget, total output vertices and floor count each refuse with a typed reason BEFORE heavy work (a floor-count refusal names floors); mutation - removing each bound reddens its test.",
    "AS-5 (conditioning + scope): a non-integer-coordinate case proves the local-origin volume reduction (world-coordinate mutant reddens); the lot-with-hole test is renamed or repaired honestly; zero new dependencies; not wired; ruff + modularity clean; exactly the allowed paths."
  ],
  "required_gates": [
    "G0",
    "G2",
    "G3",
    "G4",
    "G5"
  ],
  "producer_agent": "3d-massing-engineer",
  "reviewer_agents": [
    "geospatial-engineer",
    "qa-engineer",
    "security-reviewer",
    "directive-compliance-verifier"
  ],
  "status": "awaiting_gate",
  "progress_percent": 85,
  "risks": [],
  "blockers": [],
  "created_at": "2026-09-24T09:39:28.321165+00:00",
  "updated_at": "2026-09-24T10:50:06.760081+00:00",
  "directive_refs": [
    {
      "directive_id": "D-087",
      "requirement_ids": [
        "D-087-R001",
        "D-087-R002",
        "D-087-R003",
        "D-087-R009"
      ]
    },
    {
      "directive_id": "D-066",
      "requirement_ids": [
        "D-066-R001"
      ]
    }
  ],
  "directive_regime_version": "1.0",
  "directive_regime_entered_at": "2026-09-24T09:39:28.321165+00:00",
  "documented_test_commands": [
    "python -m ruff check .",
    "python -m pytest tests/scenario/test_massing_model.py -q",
    "python tools/modularity_check.py --check"
  ],
  "worktree": "C:\\Users\\MLFLL\\Downloads\\nyc-zoning\\wt-m5t088",
  "progress_log": [
    {
      "at": "2026-09-24T09:47:09.067880+00:00",
      "agent": "3d-massing-engineer",
      "percent": 20,
      "status": "claimed",
      "message": "Claimed at the wave-2 claim seam; orchestrator-dispatched subagent producer in the isolated worktree C:\\Users\\MLFLL\\Downloads\\nyc-zoning\\wt-m5t088 (created at the claim-seam commit)."
    },
    {
      "at": "2026-09-24T10:49:31.894698+00:00",
      "agent": "3d-massing-engineer",
      "percent": 90,
      "status": "in_progress",
      "message": "Harvested: 353b919d cherry-picked to 9f355fe1 (3 files MATCH); ruff clean, 54 passed, modularity exit 0; evidence map + G2 written; submitting."
    }
  ]
}
