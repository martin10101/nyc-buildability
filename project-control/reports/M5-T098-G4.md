# M5-T098 — G4 test-adequacy review (qa-engineer "qa-t098", read-only)

> Transmission history: pinned at c4d27e1e, delivered as three SendMessage parts (1/3..3/3) ending with END-OF-REPORT;
> no truncation. Joined verbatim by the orchestrator (transport wrapper tags removed only). Verdict PASS: all four
> producer mutations redden (MD and ME, the M5-T088 survivors, now die); the forced edit of the M5-T088 test is genuinely
> forced with inclusivity preserved. ADVISORY-A (the lot NYC check's Y clause and later vertices are under-probed) is routed
> to the wiring packet at the accept seam.

---

M5-T098 G4 test-adequacy review (qa-t098, independent, read-only). PART 1/3.

SETUP / IDENTITY
- PIN: HEAD at start c4d27e1e (== expected). cwd services/api; Python 3.11.9, shapely 2.0.7, numpy present. CI is 3.12 (authoritative; suite deterministic/offline, goldens runner-stable).
- Harness: my Write and shell file-writes are guard-blocked, so I mutated IN-PROCESS — text-mutate a copy of massing_model.py, inject it into sys.modules["app.scenario.massing_model"] + set the app.scenario attr BEFORE the test imports, purge any sys.modules key containing "massing_model" (this also purges the test module, whose name contains it) between runs. Every mutation asserts count==1 (no silent no-op survivors).
- FAITHFULNESS PROVEN: baseline 65 passed; after 4 mutants a re-run baseline = 65 passed again; a final baseline after 7 more mutants = 65 passed. No cache leak. Documented suite `pytest tests/scenario/test_massing_model.py -q` = 65 passed in ~1s.

PRODUCER'S 4 REQUIRED MUTATIONS — all REDDEN their exact targets; MD and ME were the T088 survivors and are now DEAD:
| Mut | Change | Target | Result |
| MD | drop `or abs(y) > MAX_COORD_ABS` in _prepare_ring | test_t098_as1_y_only_magnitude_violation_is_refused | RED — "DID NOT RAISE" |
| lot_range | `_require_lot_ring_in_nyc_bounds(ring)` -> pass | as2 4326 / metres / reuses_b0 | RED x3 — reason became footprint_outside_lot (the exact mislabel the check prevents) |
| geos_wrap | remove `@_wrap_geos_errors` | as3 geos_exception_is_wrapped + generated_option_path_also_wraps | RED x2 — raw shapely.errors.GEOSException escaped BOTH build paths |
| ME | move `budget.charge(m-3, field)` to AFTER the inner scan | test_t098_as4_scan_units_charged_before_the_inner_scan | RED — "assert 5 == 0" (inner scan ran n-3=5 before the charge) |

Reddened sets match the producer report exactly. The generated-option path has its OWN independent test (not just transitive), and it reddened separately under geos_wrap.

Continues 2/3 (my weakenings + forced-edit + goldens).

---

M5-T098 G4 review (qa-t098). PART 2/3.

MY OWN WEAKENINGS (each a fresh in-process load; count==1 verified):
| Wk | Change | Run | Result |
| W1 | lot NYC check drops the Y-axis clause (X only) | full | SURVIVED 65 passed -> ADVISORY-A |
| W2 | lot NYC check tests only ring[0] (`for x,y in ring[:1]`) | full | SURVIVED 65 passed -> ADVISORY-A |
| W3 | lot NYC check exclusive bounds (`<` not `<=`) | full | SURVIVED 65 passed -> immaterial |
| W4 | lot NYC check moved AFTER `Polygon(...)` construct | full | SURVIVED 65 passed -> immaterial |
| W5 | GEOS wrap `except Exception` (broad) instead of GEOSException | -k as3 | RED x3 — typed_refusal_passes_through_the_geos_wrap_unchanged + T088 footprint_just_outside[half_foot,thousandth] (geometry_engine_error == footprint_outside_lot) |
| W6a | x magnitude bound `>=` | -k inclusive | RED — test_t088_as4_coordinate_magnitude_bound_is_inclusive |
| W6b | y magnitude bound `>=` | -k inclusive | RED x2 — t088 inclusive + test_t098_as1_y_only_magnitude_bound_is_inclusive |

So: the GEOS-wrap SELECTIVITY (W5) and the magnitude-bound INCLUSIVITY on both axes (W6a/W6b) are genuinely pinned. The lot-check y-axis + all-vertices completeness (W1/W2) are NOT — see ADVISORY-A in part 3.

FORCED EDIT (test_t088_as4_coordinate_magnitude_bound_is_inclusive) — genuinely forced, coverage preserved:
- Reverted to the literal pre-change lines and ran them against the REAL module: the +/-1e8 lot `model = build_massing_model(...)` now RAISES MassingModelError(reason=lot_ring_out_of_nyc_bounds, field=lot_ring). So the old "±1e8 lot builds, area==4e16" assertion can no longer hold — AS-2's lot gate and that assertion are irreconcilable. The edit was forced, not gratuitous.
- What the NEW lines assert differently: inclusivity is proven on `_prepare_ring([[1e8,1e8],[1e8-1,1e8],[1e8,1e8-1]])` -> len==3 (accepted at exactly 1e8), i.e. on the CRS-agnostic bound where it actually lives. That value IS the value the bug displaces: W6a/W6b flip it from len==3 to a raised exception. So magnitude-bound inclusivity is genuinely retained.
- The subsequent over-bound refusals (lot[1]=1e8+0.5 -> coordinate_out_of_range/lot_ring[1]; _prepare_ring([[1e9..]]) -> f[0]) are UNCHANGED and green.

GOLDENS — both byte-identical AND still asserted:
- The material-commit diff never touches the golden literals (only the one forced hunk + the appended block).
- golden1 sha256:b7fa98…5133b asserted at L336 (test_as1) AND L964 (new test_t098_as2_in_range_lot_is_unaffected_golden_unchanged); golden2 sha256:e23b5c…a6c5 at L883 (test_t088_as5). All green at HEAD.

Continues 3/3 (vacuity, scope, findings, verdict).

---

M5-T098 G4 review (qa-t098). PART 3/3 — vacuity, scope, findings, verdict.

VACUITY (M5-T083 class) — none found:
- Spies observe the REAL path: as3 geos test asserts calls["n"]>=1 (spied Polygon ran) and geos_wrap mutant reddens it; as4 charge-ordering test spies _point_in_triangle and ME reddens "assert 5==0"; typed-passthrough test reddens under W5. No raises-only checks — lot tests pin reason AND field=="lot_ring"; y-only test pins reason + field=="f[0]"; goldens pin exact sha256.

SCOPE / HYGIENE (independently confirmed):
- Material commit 4b25c60c touches EXACTLY 3 files: massing_model.py, test_massing_model.py, M5-T098-producer-report.md. No forbidden path (no proposal.py / max_envelope.py / cad / api / main.py / requirements).
- massing_model has NO production importer (grep app/ = only its own .pyc); module stays UNWIRED. No permitted/approved/maximum-allowed wording (test_as5 green).

FINDINGS
- ADVISORY-A (low; recommend for the wiring packet). The NEW lot NYC-range check is under-probed on two axes of its own logic: dropping its Y-clause (W1) and testing only vertex[0] (W2) both keep all 65 green, because every lot fixture (4326, metres, below-X-min) violates the X-axis on the first vertex. This is the SAME single-axis gap as T088's ADVISORY-1 (MD) that THIS packet closed for _prepare_ring — the fix was applied to the magnitude bound but the sibling lot guard it adds re-opens the pattern. Code is CORRECT today and AS-2's required whole-check-removal mutation reddens (3 tests), so this is finer than required. Trivially closable: add one lot in-X-range/out-of-Y-range (e.g. x≈1e6 in [9e5,1.1e6], y=4.507e6 > 3e5) and one lot with an in-range vertex[0] but an out-of-range later vertex, each asserting lot_ring_out_of_nyc_bounds.
- ADVISORY-B (trivial / non-material): W3 (exclusive bound) and W4 (check after Polygon) survive but have no behavior effect — the NYC bounds are deliberately generous unit-guards (not a precise city boundary), and _prepare_ring's ≤1e8 magnitude guard already runs before any shapely construct, so edge-inclusivity at the exact NYC bound and this ordering are immaterial. No fixture needed.

BLOCKING: none.

BASIS: all 5 AS required assertions + named mutations hold; all 4 producer mutations reddened their exact targets (MD and ME, the T088 survivors, now die); the forced edit is genuinely forced with inclusivity preserved (W6a/W6b redden it); both goldens byte-identical and asserted; no vacuity; scope clean and unwired. The 2 surviving weakenings are finer than the AS-2-required mutation, act on correct code, break no required behaviour, and are trivially closable — advisories, not blockers.

M5-T098 G4 VERDICT: PASS
END-OF-REPORT
