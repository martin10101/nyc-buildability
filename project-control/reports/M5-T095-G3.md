# M5-T095 — G3 code review (code-reviewer "cr-t095", read-only)

> Transmission history: pinned at a612652b, delivered as two SendMessage parts (1/2, 2/2) ending with END-OF-REPORT;
> no truncation. Joined verbatim by the orchestrator (transport wrapper tags removed only). Verdict PASS: decision
> identity re-proven by an independent 520,000-case adversarial probe at EPSG:2263 magnitudes (0 disagreements);
> advisories A1-A5 are non-blocking (routed at the accept seam).

---

M5-T095 G3 CODE REVIEW (independent, read-only) — PART 1/2

PIN: HEAD at start = a612652b (matches expected). Material commit a2dcc60f; `git show --stat` = exactly 3 files, all in allowed_paths (proposal.py, tests/scenario/test_proposal_validation_budget.py, reports/M5-T095-producer-report.md). No forbidden path touched.

VERIFIED GREEN ON THIS MACHINE (cwd services/api, Py 3.11.9 / shapely 2.0.7):
- `python -m ruff check .` -> All checks passed!
- `pytest tests/scenario/test_proposal_validation_budget.py -q` -> 18 passed (includes the 24,159-ring equivalence corpus).
- Full documented suite (7 files) -> 312 passed. Consumer decision identity holds unchanged.

DECISION IDENTITY (the core question) — SOUND.
- The hybrid keeps conditions (1) zero-length-edge and (2) 180-deg reversal-spike byte-identical to the old predicate (proposal.py:176-187 vs old); ONLY condition (3) non-adjacent self-intersection is delegated to `Polygon([*ring, ring[0]]).is_valid` (proposal.py:147-148). So the ONLY possible disagreement surface is rings that already passed (1)+(2), where GEOS validity vs the all-pairs test could differ.
- For such rings GEOS is_valid == "no non-adjacent edge intersection": vertex-on-edge/T-touch, collinear overlap, and non-adjacent repeated vertices all make a single ring invalid, matching the old test. Winding/orientation and holes are N/A (single closed ring). Collinear/degenerate triangles are caught by condition (2) before GEOS. In production `_ring_is_simple` only ever sees >=3 DISTINCT, finite, bounds-checked vertices (_validate_outline proposal.py:274-285), so the "shapely tolerates consecutive duplicates" divergence the producer found is excluded by condition (1) first.
- Independent verification of the near-degenerate concern (the committed corpus fuzzes not-simple cases only at SMALL integer coords): I ran 520,000 adversarial cases at EPSG:2263 magnitudes (~1e6 ft) with FRACTIONAL coords, incl. deliberate T-touch and near-collinear-overlap epsilon sweeps (eps 0, +/-1e-6, 1e-4, 1e-3, 0.5). Both outcomes exercised (376,915 not-simple by old). Disagreements = 0.

GEOSException/ValueError -> False path (proposal.py:149-150): fail-closed is correct; the try body only builds a Polygon + reads is_valid, so it cannot swallow a ProposedMassingError (which is a ValueError) — none is raised inside. Safe.

Removed helpers (_orientation/_on_segment/_segments_intersect): no module imports them (grep clean); derivation.py keeps its OWN independent copies (derivation.py:314-330). Removal safe.

NO BLOCKING FINDINGS. Advisories in part 2.

---

M5-T095 G3 CODE REVIEW — PART 2/2 (ADVISORY only; none block acceptance)

A1 (context the report omits) — a SECOND total bound already exists: `proposal_input_gate.MAX_TOTAL_VERTICES = 5000` (proposal_input_gate.py:57,166-171) with its own `_outline_position_count`/`_total_position_count`, added for the SAME O(n^2) concern. The API routes (proposal_validation, proposal_checks) go through that gate, so they refuse at 5000 FIRST — the new 20000 validator bound is a backstop for DIRECT callers (derivation/max_envelope/massing_model/contract) that bypass the gate. That is the correct target of F-HIGH-1 (the minutes-long case = direct 501x1000 path) and 20000 is safe only because simplicity is now O(n log n). But the report never reconciles the two values, and proposal_input_gate.py:18 docstring ("runs its O(n^2) simplicity test") is now STALE (forbidden path -> route to a follow-up). Total-count logic now triplicated (proposal.py + input_gate + derivation geometry) — extends producer DISCOVERY #1; a shared helper would remove it.

A2 (test durability) — the committed equivalence corpus fuzzes not-simple/boundary cases only at small INTEGER coords; production coords are fractional at ~1e6. Identity still holds (my 520k probe above), but recommend adding a few 2263-magnitude fractional boundary fixtures to the committed corpus so the guarantee is durable in CI, not reliant on my out-of-tree probe.

A3 (message honesty) — the GEOSException path surfaces "self-intersecting" (proposal.py:287-291). On a finite, bounds-checked, distinct-vertex ring a GEOS error is essentially unreachable, but the label asserts a definite cause for what is really "could not be verified simple." Fail-closed and correct field; a mildly more precise message would be more honest. Low severity.

A4 (refusal-field shift, disclosure) — because the total bound runs before per-outline checks, a SINGLE outline with >20,000 positions now refuses with field `proposed_massing` instead of `proposed_massing.outline.vertices` (MAX_OUTLINE_VERTICES). Both typed; no fixture exercises a >20k-vertex outline; by design the cheap pre-check fires first. Disclosure only.

A5 (flake) — the 0.5s/2.0s wall-clock guards (test lines 311,322) can flake on contended CI, but the deterministic construction-count (==1) and pairwise work-count (497,502 > 50,000 budget) guards are the real mutation protection, so a timing flake cannot mask a restored-all-pairs regression. Low severity.

Scope/public surface: `__all__` gains ONLY MAX_TOTAL_OUTLINE_POSITIONS; validate_proposed_massing/ProposedMassingError/all MAX_* unchanged; zero new deps (shapely admitted, already used by massing_model.py). Confirmed.

M5-T095 G3 VERDICT: PASS

END-OF-REPORT
