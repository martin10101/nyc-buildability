# M5-T120 — G4 test review (qa-engineer "qa-t120", read-only)

> Transmission history: round 1 pinned at b0eb29f7 (material 5ae5700e, identity before the correction), delivered as
> 3 SendMessage parts ending with END-OF-REPORT; then a DELTA attestation after the [ORCH-CORRECTED per M5-T120 G1]
> doc-only commit 793fed27 (re-frozen at seam 858989c1, identity 80c33f6d) - the verdict carries to the corrected identity.
> No truncation. Joined verbatim by the orchestrator (transport wrapper tags removed only). Verdict PASS; the six real PDFs independently re-read in memory
> (4 read, 2 honest scan-only refusals; item 2's unblock proven by reverting the orphan rule); all mutants red.

---

M5-T120 G4 review (qa-engineer, READ-ONLY) — Part 1/3

PIN at start: HEAD b0eb29f7. Material 5ae5700e verified: the 9 touched files are byte-identical 5ae5700e→HEAD; the later 8ecdbeb2 touches only ledger/report/gate files. All 9 files ∈ allowed_paths, none forbidden. Forbidden paths (pdf_object_streams.py, test_pdf_object_streams.py, sheet_marked_content.py, sheet_objects.py) unchanged 0c638f88→HEAD.

Environment: drawings suite via the 3.11 shim = 322 passed (matches harvest). Mutants run in-process via python stdin (my reviewer identity is denied repo AND scratch writes; no repo mutation performed, no orchestrator write requested).

AS-1 SPLIT — PASS.
- Claim seam 0c638f88 had sheet_path_state.py as a 1-line PLACEHOLDER; interpreter was 748. Now sheet_interpreter.py=577, sheet_path_state.py=248 — the split is real, not cosmetic.
- Split-equivalence diff (0c638f88→5ae5700e) shows EXACTLY one intended change: case refuse_l_no_current (`5 5 l S`) → refuse_curve_no_current (`5 5 10 10 20 20 c S`), plus the _OVERALL recapture (137837…→264b54…). All 62 other per-case digests UNCHANGED. test_equivalence_overall_digest_matches_pre_split_golden / _every_case_* pass inside the 322 run — so the goldens match the CURRENT module output.
- Split-facade seam (the seq-130 trap) AUDITED across tests/drawings — every monkeypatch bites at the correct namespace:
  • Moved paint-flag sets patched at sheet_path_state (test_sheet_p4_features.py:264/270/276); these names no longer exist in sheet_interpreter, so a stale sheet_interpreter patch would AttributeError — none exists.
  • Names that stayed are still read where the tests patch them: _PATH_HANDLERS (sheet_interpreter.py:282), _SKIP_SHADING (:318), skip_inline_image (:478), read_inline_dict (:247), _StreamRun._place_form/_paint/_orphan_lineto, _is_finite_point (:276).
  No vacuous patch found; the producer added explicit new-namespace positive controls (test_sheet_p4_features.py:258-287).

(part 2/3 next: AS-2 /DP, AS-3 citations, AS-4 orphan mutants)

---

M5-T120 G4 review — Part 2/3

AS-4 ORPHAN LINETO — PASS. sheet_path_state.py:83-100 _orphan_lineto starts a subpath at its OWN mapped point (no segment from the pre-paint point), increments page_orphan_subpaths+orphan_subpaths, charges 1. Per-page reset at sheet_reader.py:240 (begin_page); stamped :500; summed by SheetDocument.orphan_subpaths (sheet_primitives.py:238-242). Mutants re-run RED, all restored:
- reopen-at-last-point (patch _PathState._paint keep-current) → poly becomes 5,5→10,10→20,20 (INVENTED segment), count 0. RED.
- drop-the-count → geometry identical, count 0. RED.
- orphan-curve-accepted (patch _curveto) → curve-with-no-current now READS (real refuses feature=path). RED.
- MINE #1 orphan-fires-when-current-exists (patch _lineto→always orphan) → `0 0 m 5 5 l S` loses its from-point, 0 polylines (real 1). RED.
- MINE #2 count-not-reset-per-page (patch begin_page) → 2-page doc leaks page1's orphan into page2 (page2=1, doc=2; real page2=0, doc=1). RED.
Orphan curves c/v/y stay typed refusals (v guarded separately at _op_v, sheet_path_state.py:207).

AS-2 /DP DICTIONARIES — PASS. sheet_inline_image.py: _nested_dict_allowed (:309) = DP/DecodeParms only; _MAX_INLINE_DP_DICT_DEPTH=1 (:87); _skip_dict enforces one level (:382). Mutants RED:
- allow-any-nested-dict → non-/DP /CS<<>> accepted+skipped (real refuses).
- raise-depth-cap→2 → a second /DP level accepted (real refuses).
- disallow-dp-dict → the /DP-dict image refuses the whole doc (proves acceptance, not a silent drop, is what lets it read).
Linework after a skipped /DP-dict image still draws (verified: inline_image_skips=1 + the trailing line surfaces).

AS-3 CITATIONS — PASS (one advisory). No "8.9.5.2" remains anywhere. Row alignment now §8.9.3 (:20/:224); BitsPerComponent §8.9.5.1 / Table 89 (:73); colour-space abbreviations Table 93 (:6-7/:100). The primary-source correctness of the ISO section numbers is the G1/DCV domain; I confirm the three flagged wrong pointers are gone.

(part 3/3 next: AS-5 corpus table + item-2 causal proof, AS-6 scope, advisory, VERDICT)

---

M5-T120 G4 review — Part 3/3

AS-5 REAL CORPUS — PASS (independently re-run in memory via urllib, nothing written to disk). All 6 sha256 MATCH the note; 2 runs each, all deterministic:
1 VA Div23-1  sha✓  READ 88pp, 332057 poly, orphan 0, img 5
2 VA Div23-2  sha✓  READ 94pp, 299548 poly, ORPHAN 1, inline 8154, img 8
3 VA DwgDeliv sha✓  READ 14pp, 1611 poly, orphan 0, shading 4, img 19
4 Brooklyn HAER sha✓ READ 8pp, 11 poly, orphan 0, inline 3, img 8
5 Frantz-Dunn HABS sha✓ REFUSED "scan only" (zero vector geometry)
6 Rothschild HABS  sha✓ REFUSED "scan only"

4 read (1,2,3,4), 2 refuse (5,6) — matches the producer's claim exactly. Items 1/3/4 read with orphan=0 (unchanged from T118).
ITEM-2 CAUSAL PROOF (owner-facing claim): I reverted _lineto to pre-T120 behaviour (refuse when no current point) and re-ran item 2 → it REFUSES feature='path'; restored → READ orphan=1, 94pp. So the orphan-lineto rule is the SOLE unblocker for item 2 — confirmed by revert, not merely asserted by an attribution diagnostic.

AS-6 SCOPE — PASS. Zero new deps (requirements.txt/.in unchanged 0c638f88→HEAD; only stdlib+app imports in the 5 source files). Unwired (no route/app/main.py/api change; forbidden paths unchanged). test_pdf_object_streams.py unchanged and green (in the 322). ruff `check .` from services/api = All checks passed. modularity_check.py --check exit 0 (no T120 file flagged). Exactly the 9 allowed_paths touched, no stray/forbidden file. test_sheet_reader.py's 5-line change is a non-weakening docstring update pointing at the new split home.

ADVISORY (non-blocking): sheet_inline_image.py:436 docstring says "colour space (Table 92)" — the CS key is Table 92 but the value abbreviations mapped there (G/RGB/CMYK) are Table 93; a G1 wording nuance, not a test defect. No flake risk observed (every read deterministic across 2 runs; big vector reads ~40s each — a runtime note, not a defect).

No BLOCKING findings.

M5-T120 G4 VERDICT: PASS
END-OF-REPORT

---

M5-T120 G4 delta attestation (qa-engineer, READ-ONLY).

Verified `git diff 5ae5700e..858989c1` across ALL 9 T120 files (not just the correction commit):
(a) DOC-ONLY confirmed. Exactly two hunks, both entirely inside triple-quoted docstrings:
  • sheet_path_state.py `_orphan_lineto` — the reader-of-record citation (now: WHATWG Canvas 2D lineTo "ensure there is a subpath" that pdf.js inherits; MuPDF stricter/warns-and-ignores, marked [ORCH-CORRECTED per G1]).
  • sheet_inline_image.py `_components` — the colour-space table pointer (now: /CS key = Table 92, value abbreviations = Table 93; my advisory, tagged).
  The surrounding executable lines (`point = self._map(x, y)`, `for key in _CS_KEYS:`) are unchanged CONTEXT. The other 7 files (incl. all 4 test files) have ZERO diff. No code, no test, no behaviour change — so my corpus reproduction (4 read / 2 refuse; item-2 revert proof) and all 22 mutant runs still stand on byte-identical executable code.
(b) Independently re-ran tests/drawings via the 3.11 shim at the current head = 322 passed. HEAD has since advanced to e392a7cb (a disjoint M5-T122 review-record seam); `git diff 858989c1..HEAD` over the 9 T120 files is EMPTY, so identity 80c33f6d at seam 858989c1 is byte-stable.

My G4 PASS carries to content identity 80c33f6d at seam 858989c1.

M5-T120 G4 DELTA: PASS
END-OF-REPORT
