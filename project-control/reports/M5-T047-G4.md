<!-- Orchestrator capture: qa-engineer G4 return for M5-T047 saved VERBATIM
     (transport entity-decoding only). Pinned at frozen head ca94b2c3; reviewer read the
     frozen surface via git plumbing and traced mutation-sensitivity per scenario. -->

G4 review complete. I verified every acceptance scenario against the actual frozen-head source (read via git plumbing at `ca94b2c3`, since the worktree sits on a different branch), traced mutation-sensitivity through the route, client, card, and gate sources, and confirmed the stored CI/api evidence applies (the 9 T047 files are byte-identical to the CI-green head `d27a6a83`; one source-section digest recomputed and matched). Here is my gate report.

---

# Gate Report

- **Gate ID:** G4 (QA / test adequacy)
- **Task ID:** M5-T047 (record-address display channel, DB-032)
- **Reviewer:** qa-engineer (read-only)
- **Producer:** backend-engineer
- **Result: PASS** (advisory findings only; no blocking defects)
- **Clean environment / worktree used:** Read-only at frozen SHA `ca94b2c3` via `git show`/`git diff` plumbing (worktree HEAD is on an unrelated branch `worktree-agent-…` @ `d8b3899f`; I did not check out or mutate anything). Verified `ca94b2c3` is the submission-evidence commit and the live branch head `21ee7379` ("SUBMITTED awaiting_gate") is exactly one control-plane-only commit later. Web tests not executed locally (thin client); verified the stored CI evidence instead, which is valid because the 9 T047 surface files at `ca94b2c3` are byte-identical (`git diff --stat` empty) to the CI-green head `d27a6a83` (run 35431045720, web-e2e success). One digest recomputed: `record-address.ts` LF-sha256 `c9a15f82…eeb589` matches the source-sections index.

## Acceptance criteria reviewed (AS-1..AS-8) — adequacy verdict per scenario

| AS | Coverage | Mutation-sensitivity verdict |
|---|---|---|
| AS-1 shown/distinct | backend `test_ra1` (`test_lot_geometry_api.py:487`) asserts address `"3622 13 AVENUE"`, outcome `address_of_record`, full provenance; client `record-address.test.ts:51`; card `address-confirm.test.tsx:722` (S10 AS-1) terminal `shown` + "City record address"/"PLUTO"/distinct-from-matched | STRONG |
| AS-2 equal → no line | differ unit `record-address.test.ts:179`; card S10 AS-2 `:740` waits terminal `equal` then asserts `queryByTestId("record-address")` null | STRONG (waits on settled status, kills the transient-loading false pass) |
| AS-3 absence + connector error | backend `test_ra2` `:515`, `test_ra2b` `:534`, `test_ra3` `:551`, `test_ra4_*` `:568-608`; client `:64/:78/:87/:94/:101`; card S10 AS-3 `:757` (`absent`) + `:771` (`error`, card unaffected) | STRONG |
| AS-4 records + A2 + A3a | A3a display-trim S9 `:634-636` asserts BOTH trimmed textContent AND raw preserved in `title`/`aria-label`; records-label in S10 AS-1 | A3a STRONG; **A2 copy has no probe (adv. A2)** |
| AS-5 riders | G4-A1 abbreviation `address-search.test.ts:340` STRONG; G5-A1 length bound `:366/:378/:388` | G4-A1 STRONG; **G5-A1 over-length probes vacuous (adv. A1)** |
| AS-6 compat | full api 439 green (stored); prior-assertion regression diff shows ONLY the A3a assertion changed (S1-S8 untouched); consumer fix reviewed (item 5) | STRONG |
| AS-7 ruff/api/modularity/web CI | ruff clean, 33 route + 439 api-wide (I counted 33 test items in the file — matches), modularity exit 0, web CI green at byte-identical head | Verified via stored records |
| AS-8 walkthrough | G3/human-journey gate — outside G4 surface; pending per producer §10 | Not a G4 obligation |

## Directive/requirement verification (G4-relevant view; DCV owns the authoritative pass)

| Requirement ID | Reviewed identity | Verdict | Reproduced evidence |
|---|---|---|---|
| D-066-R001 | `ca94b2c3` | PASS (advisory only) | Navigation block present in packet; the LotOutlineMap prohibition is honored — no forbidden path touched; record-address line lives on `AddressConfirmCard` directly (`AddressConfirmCard.tsx:198`). This is code-graph/provenance, tangential to G4 test-adequacy. |
| D-073-R006 | `ca94b2c3` | PASS | Records-vs-computed distinction is testably enforced: card renders the line only on `showRecordAddress` (`AddressConfirmCard.tsx:137-139`), labels it a city RECORD naming PLUTO (`:199-202`), honest-absent otherwise; S10 AS-1 asserts the record labeling, AS-2/AS-3 assert honest no-line. |

## Steps independently executed

1. Resolved the frozen SHA and its relation to the live head; confirmed the intervening commit is control-plane only (`M5-T047.json`, `state.json`, `reports/M5-T047.json`).
2. Read the packet, producer report, source-sections index, CI-evidence, evidence-map at `ca94b2c3`.
3. Read all 8 in-scope source/test files + `bounded.ts` (the blank→null helper the client tests depend on) at the frozen SHA.
4. Traced each acceptance scenario to its probe and each probe to the real code path (route `get_record_address`, client `fetchRecordAddress`/`documentView`/`recordAddressDiffersFromMatched`, card status derivation, gate `resolveLotFromGeoSearch`).
5. Ran two mutation thought-experiments per the surface (fabricated empty-string address; connector fault → absence) — both are killed (backend `test_ra2b`, `test_ra4`).
6. Diffed the confirm test vs its pre-T047 version to confirm no prior assertion silently changed beyond A3a; reviewed the tagged consumer-test fix.
7. Confirmed T047-surface byte-identity between `ca94b2c3` and the CI-green `d27a6a83`; recomputed one LF-normalized digest.

## Expected versus actual (mutation checks)

- **Fabricated empty-string address:** route `_record_address_from_result` returns None on blank (`lot_geometry.py:345`); client `documentView` maps `""`→null (`record-address.ts:165`, via `boundedText("   ","")`→`""`). Probes `test_ra2b` (`:534`) and `record-address.test.ts:87` assert null → a fabricating mutant FAILS. **Confirmed killable.**
- **Connector fault → absence:** route emits typed 5xx with no `outcome` key (`:411-421`); `test_ra4_source_unavailable` (`:568`) asserts 502 + `"outcome" not in body`. A fault→200-`no_record` mutant FAILS. **Confirmed killable.**
- **Flag-off 404 before correlation / invalid-BBL 422 with zero fetch:** `test_ra5_flag_off` (`:616`) asserts 404 + no `X-Correlation-ID`; `test_ra5_malformed_bbl` (`:625`) installs a *landmine* fetcher (`install_record_landmine`, raises if invoked) proving zero network on the 422 path. **Confirmed non-vacuous.**
- **Record line ONLY on differs / equal→none:** S10 AS-1 (`shown`) vs AS-2 (`equal`, no line); a mutant that showed the line when equal flips the status to `shown` and materializes `record-address` — both asserted. **Confirmed killable.**

## Evidence paths

- `project-control/tasks/M5-T047.json`, `project-control/reports/M5-T047-producer-report.md`, `-source-sections.md`, `-ci-evidence.md`, `-evidence-map.json` (all @ `ca94b2c3`).
- Source under test @ `ca94b2c3`: `services/api/app/api/v1/lot_geometry.py`, `apps/web/src/lib/record-address.ts`, `apps/web/src/components/address/AddressConfirmCard.tsx`, `apps/web/src/lib/address-search.ts`.
- Tests @ `ca94b2c3`: `services/api/tests/api/test_lot_geometry_api.py`, `apps/web/src/lib/__tests__/record-address.test.ts`, `apps/web/src/components/address/__tests__/address-confirm.test.tsx`, `apps/web/src/lib/__tests__/address-search.test.ts`, and the tagged fix in `…/__tests__/address-resolution.test.tsx` (`d27a6a83`).

## Regression / consumer-fix findings (surface item 5)

The tagged out-of-scope fix (`d27a6a83`, `address-resolution.test.tsx`) is a single-predicate widening of the existing `lotGeometryStub` side stub to also divert `/record-address` to the benign flag-off 404 (`route_absent`). It changes **no assertion**, mirrors the file's own M5-T023 precedent for `/lot-geometry`, and preserves the resolution spy's call order/count/queued responses — each test's original intent is intact. **PASS.** The confirm-test regression diff shows only the S9 A3a assertion changed (`typed`→`typed.trim()` plus `title`/`aria-label`), plus the additive S10 block; S1-S8 are byte-stable. AS-6 preserved.

## Defects

None blocking.

## Findings

**F (blocking): none.**

**A1 (advisory) — G5-A1 length-bound probes are vacuous** (`address-search.test.ts:366`, `:378`). Both over-length tests use `features: []`. The bound returns `{kind:"no_match", input: parsedInput}` at `address-search.ts:320`; with the bound removed, execution falls through the empty feature loop to the identical return at `address-search.ts:357` — same `kind`, same `input.street === longStreet`. The tests therefore pass whether or not the bound exists and do not regression-guard it. This is low-severity because (a) the bound is verifiably present and functioning in source, (b) its removal changes no user-visible outcome (it is a pure processing/DoS guard — an over-length string can never equality-match a real short feature), and (c) the at-bound positive test (`:388`, with a real feature) proves legitimate addresses are not regressed. Suggested future strengthening: assert normalization was not invoked, or use an oversized input that would otherwise match a supplied feature. Not blocking.

**A2 (advisory) — AS-4 "HJ A2 layout-neutral copy" has no dedicated automated probe.** The A2 wording change lives only in source (`AddressConfirmCard.tsx:191-194`); no test asserts the note avoids directional ("above"/"below") language, so a mutant reverting the copy would pass G4. This is appropriately the human-journey walkthrough's surface (AS-8/G3), and the *testable* part of AS-4 (A3a display-trim) is well covered, so it is not a G4-blocking gap.

**A3 (advisory) — client typed-error coverage is representative, not exhaustive.** `record-address.test.ts` covers 3 of 6 declared error states (`source_unavailable`/`rate_limited`/`validation_error`); `timeout` (504), `schema_drift` (502), and `internal_error` (500) have `DOCUMENTED_PAIRS` entries (`record-address.ts:50-59`) but no client probe — removing one would silently reclassify that response as `unexpected_response` uncaught. Low severity: they route through identical membership logic, the server side is covered (`test_ra4_timeout`), and the misclassification is fail-safe (still no line, no crash). Non-blocking.

Minor note (below advisory threshold): `test_ra3` reason assertion (`test_lot_geometry_api.py:559`) is weakened by an `or body["reason"]` tail that makes the substring check non-load-bearing; the outcome/`address is None` assertions in the same test remain strong.

## Required rework

None required for G4 PASS. A1/A2/A3 are recommended as future-packet test-hardening riders (route to the discovery backlog), not corrections blocking acceptance.

## Reviewer conclusion

All eight contracted acceptance scenarios have probes, and every correctness-bearing behavior the packet promises — the three honest route outcomes with provenance, SODA key-absence and blank-string honest absence, connector-fault-is-never-absence, the differ-gated record line (shown/equal/absent/error via settled terminal status), and the A3a display-trim with raw value preserved in `title`/`aria-label` — is exercised by a mutation-sensitive test that I traced to the real code path and confirmed would fail under the relevant mutant. The two vacuity/gap items fall on a non-correctness resource guard (A1) and a copy nicety owned by the human-journey gate (A2), plus a representative-vs-exhaustive error enumeration (A3); none is a correctness path and none blocks. Backend counts reconcile (33 route tests present in the file; 439 api-wide and modularity-0 per the reproduced records), and the frozen T047 surface is byte-identical to the CI-green head, so the stored web-e2e success applies.

**Verdict: PASS** (advisory findings A1, A2, A3 for backlog; no rework required).
