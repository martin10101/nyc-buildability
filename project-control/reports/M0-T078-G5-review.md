# M0-T078 G5 security/control-plane review (verbatim reviewer return)

Saved VERBATIM by the orchestrator from the read-only reviewer agent's return channel
(report-preservation rule, .claude/rules/project-control.md). Reviewer: t078-g5-reviewer
(independent; reviewer ≠ producer ≠ verifier).

---

G5 SECURITY / CONTROL-PLANE REVIEW — M0-T078

VERDICT: PASS (two minor findings; no must-fix, no important)
Reviewed identity: 48a5da9c60d6ab1fb09b83dcffd9c08884a0f032
Task commit b1fb384, merge 409386c. Read-only review; no files written, no git mutations, no tools/project_control.py invocations.

Summary: the change is documentation-only and inert from a control-plane standpoint — four files, 327 added lines, zero executable content, zero external references. Both new files subordinate themselves to the gates, the lifecycle documents, and any active owner hold in their opening paragraphs.

ANSWERS TO THE SIX QUESTIONS

1. Governance safety — no weakening, no contradiction. docs/ENGINEERING_RELIABILITY_STANDARD.md:8-11 states the standard is "Engineering guidance only," that CLAUDE.md, docs/GATES_AND_CHECKPOINTS.md, docs/PROJECT_CONTROL_PROTOCOL.md, ADR-005/ADR-006 and any active owner hold win on conflict, and that it "confers no authority to accept a task, waive a gate, merge, or release a hold." The skill repeats this verbatim in its Rules of engagement. Its Section 0 non-duplication table cites nine canonical homes rather than restating them; I verified all fourteen cited paths plus both ADRs resolve at HEAD, so there is no dangling citation that could be read as license to improvise. Against the one active hold (.claude/rules/expansion-agent-dispatch-hold.md, the owner hold on expansion planning) there is no interaction — neither file mentions expansion planning, the master plan, or the proposed pack tasks. Dependency security is cited as canonical in Section 0 and nowhere qualified. The MCP default-deny policy is not mentioned at all. The directive-compliance system is untouched.

2. Skill-injection surface — clean. Frontmatter contains exactly one key, `description`, matching all eleven pre-existing project skills. No allowed-tools, no model, no hooks, no tool invocations, no URL, no executable content, no code fences. The description is purely a routing sentence naming trigger conditions; it makes no authority claim, so there is nothing in the auto-loaded surface to disclaim. Nothing can trigger it automatically — this project has no skill-rules.json and no hook under .claude/hooks/ references the skill or any SKILL.md. It is invoked only on demand via /engineering-reliability, so it cannot be triggered in a way that overrides a hold.

3. Prompt-injection / authority claims — disclaimers hold. I scanned both files for permission-granting language (you may, authorize, waive, override, bypass, exempt, self-certify, proceed without). Every hit is a denial of authority, except one benign technical use ("Retry-After ... override the computed backoff," line 176, about HTTP behavior). The document consistently pushes toward more verification, not less: Section 8.9 requires independent final review, Section 8.8 requires frozen-SHA identity, Section 9.4 states severity changes no authority. I found no sentence a future agent could cite to merge, accept, waive a gate, release a hold, or contact an external system. The Section 5/6 passages about payments and outbound messages are design constraints on code being written, not permissions for agent behavior, and are restrictive in direction ("Never retry an ambiguous effect without reconciling first").

4. Supply chain — zero. No third-party or community code, no downloads, no new dependencies, hooks, scripts, or agents. Grepping added lines for Superpowers|secondsky|wshobson|obra|awesome-claude|marketplace|plugin install|community returns nothing. Grepping for any URL, domain, package-manager invocation, or @scope/ returns one false positive only: the word "install" inside the phrase "not only fresh install" (line 278). git diff --name-only b9e7459 409386c over .claude/agents, .claude/hooks, tools/, scripts/, package.json, requirements.txt, pyproject.toml is empty. The text also forecloses future drift: the skill states it "adds no scripts, assets, agents, or dependencies, and adopts no third-party framework or plugin." Satisfies D-023-R005, R006, R038.

5. Secrets / leakage — none. No secrets, tokens, API keys, credentials, private hostnames, IP addresses, local file paths, or personal data. I also checked the subtler injection vector: neither file has a BOM and neither contains a single invisible or format-category Unicode character. Complete non-ASCII inventory is ordinary typography — section sign, en dash, em dash, ellipsis, rightwards arrow, not-equal sign.

6. Control-plane integrity — intact. Both validators PASS read-only at this identity:
   - python tools/validate_mcp_policy.py -> exit 0, "MCP default-deny policy intact: claude.ai connectors disabled, empty allowlist, audited identifiers denied, .mcp.json servers rejected, auto-approval off, pre-existing settings preserved."
   - python tools/validate_directive_compliance.py --check -> exit 0.
   .claude/settings.json, .claude/settings.local.json, .claude/hooks/** and .mcp.json are untouched across the full merge range (git diff --stat b9e7459 409386c over those paths is empty). I additionally ran the context-budget validator that D-023-R015's acceptance harness requires: python tools/context_budget_check.py -> PASS, eager context ~2956 tok against a 6000 tok budget. The standard is not eager-loaded anywhere — reachable only via the on-demand skill and a prose pointer in AGENTS.md. The producer stayed inside the packet's allowed_paths (four of five permitted paths touched).

FINDINGS

Minor 1 — docs/ENGINEERING_RELIABILITY_STANDARD.md:256-257 (Section 9.2): passive downgrade with no named adjudicator. The sentence "Unexplained severity is downgraded when challenged" does not say who performs the downgrade. Read with the Section 9 table, where Minor is marked "Never blocks. Optional." (line 251), a producer who is an interested party could cite it to argue a thinly-worded reviewer must-fix has been downgraded and therefore no longer blocks. Consequence is bounded rather than live: the downgrade is conditioned on the finding lacking a stated reason; Section 9.4 (lines 259-261) explicitly denies that severity changes authority and reserves task status to the gate and orchestrator; and the gates doc's reviewer-independence section requires an owner-approved documented exception to waive a failed gate. Suggested hardening if a correction round opens: name the adjudicator — the reviewer of record or the gate, never the producer.

Minor 2 — docs/ENGINEERING_RELIABILITY_STANDARD.md:253-255 (Section 9.1): the instruction not to "promote a preference to a defect by asserting it 'could' cause a problem — name the mechanism or file it as minor" sits slightly awkwardly with security review, where defense-in-depth findings legitimately describe a plausible mechanism rather than a demonstrated exploit. Largely self-correcting: the clause already accepts a named mechanism, and the must-fix row categorically includes "security or tenancy exposure; secret leakage" without requiring proven exploitation. Recorded so the phrasing is not later cited against a G5 reviewer.

Neither finding blocks. Both are wording-level and become behavioral only if Section 9.4 is ignored, which the document itself forbids.

COMMANDS RUN
git rev-parse HEAD; git status --porcelain; git show b1fb384 --stat; git show 409386c --stat; git show b1fb384:.claude/skills/engineering-reliability/SKILL.md; git show b1fb384:docs/ENGINEERING_RELIABILITY_STANDARD.md; git show b1fb384 -- CLAUDE.md AGENTS.md; git diff --stat b9e7459 409386c -- .claude/settings.json .claude/settings.local.json .claude/hooks .mcp.json; git diff --name-only b9e7459 409386c (full, and over .claude/agents .claude/hooks tools/ scripts/ package.json requirements.txt pyproject.toml); git cat-file -e HEAD:<path> for all 14 cited docs; git ls-tree -r --name-only HEAD | grep adr-00[56]; grep sweeps over added lines for URLs/domains, third-party pack names, executable content, and secret patterns; grep for authority-claim language in both new files; grep -rn ENGINEERING_RELIABILITY across md/json/py to confirm no eager-load; grep -rln for hook references to the skill; python tools/validate_mcp_policy.py; python tools/validate_directive_compliance.py --check; python tools/context_budget_check.py; Python Unicode/BOM scan of both new files; Python inspection of project-control/tasks/M0-T078.json allowed_paths and D-023 requirements.json rules R005/R006/R015/R023/R038; frontmatter key/description-length comparison across all 12 skills.
