# M5-T120 — G5 security review (security-reviewer "sec-t120", read-only)

> Transmission history: round 1 pinned at b0eb29f7 (material 5ae5700e, identity before the correction), delivered as
> 3 SendMessage parts ending with END-OF-REPORT; then a DELTA attestation after the [ORCH-CORRECTED per M5-T120 G1]
> doc-only commit 793fed27 (re-frozen at seam 858989c1, identity 80c33f6d) - the verdict carries to the corrected identity.
> No truncation. Joined verbatim by the orchestrator (transport wrapper tags removed only). Verdict PASS; 0 Critical / High / Medium; three carry-forward
> advisories (L1 the document ceiling at the future mount, L2 the filtered early-EI residual, LOW-1 /UserUnit isfinite).

---

M5-T120 G5 SECURITY REVIEW (READ-ONLY, sec-t120) — Part 1/3. VERDICT: PASS.

PIN: HEAD b0eb29f7 at start. Material 5ae5700e (cherry-pick of wt-m5t120 3d08a559). Identity CONFIRMED: `git diff --stat 5ae5700e HEAD` for all 5 source files is EMPTY — reviewed files are byte-identical to material. Method: full diff of the 5 files vs the split baseline, a forward-progress proof of the new loops, 9 in-process hostile probes, and the drawings suite (322 passed via the 3.11 shim; CI 3.12 is authority). No writes made.

0 Critical / 0 High / 0 Medium / 0 Blocking. Three carry-forward advisories (Part 3), none introduced by T120.

/DP DICTIONARY SKIP (sheet_inline_image.py:182-196, 309-396) — bounded, fail-closed. New `_skip_dict` + `_skip_array(allow_dicts=)`; `_nested_dict_allowed` gates on `_DECODE_PARMS_KEYS={DP,DecodeParms}` ONLY. Probe-verified via skip_inline_image directly:
- Depth bounded to ONE level: `/DP << /K << /Deep 1 >> >>` -> REFUSE "nests deeper than one dictionary level" (:381-386). Load-bearing.
- /DP-only: `/Foo << /K -1 >>` -> REFUSE "a nested dictionary is only allowed as a /DP or /DecodeParms value" (:185-189).
- No smuggling via a nested array: `/DP << /X [ << >> ] >>` -> REFUSE "only allowed in a /DP value array" (:338-342) — the inner array's `_skip_array` runs WITHOUT allow_dicts, so the 2nd structural level refuses.
- Byte cap enforced: an unclosed /DP dict (2000 tokens) AND an over-4096 /DP dict both -> REFUSE "inline-image /DP dictionary is too long" (:367-368); it never walks the whole stream. The cap `max_dict_bytes-(pos-start)` is threaded in (:190), so outer+nested share the one 4096 budget.
- NO desync: `/DP << /K -1 >> /Cs /Val ID … EI` -> SKIP with the trailing key/value parsed and the correct EI (`_skip_dict` returns exactly past `>>`). A /DP dict rides with /F (forces the EI scan); its sentinel value (`_DICT_VALUE`) is never read by the geometry logic.
- Refusal details are CONSTANT strings — no attacker bytes echoed.

(Part 2: recursion/forward-progress bound, orphan-lineto, split equivalence.)

---

M5-T120 G5 — Part 2/3 (recursion bound, forward progress, orphan-lineto, split).

RECURSION / DoS BOUND. `_skip_dict` and `_skip_array` mutually recurse, but depth is bounded: `_skip_dict(level=1)` refuses any nested `<<` (level >= _MAX_INLINE_DP_DICT_DEPTH=1) BEFORE recursing, and its array values call `_skip_array` WITHOUT allow_dicts (no dict beneath). Max chain `_parse_dictionary -> _skip_array(True) -> _skip_dict(1) -> _skip_array(False) -> refuse` = <=4 frames. No quadratic rescan: each nested skip consumes a contiguous sub-region and the caller jumps `pos=end`, so the whole dictionary parse is O(max_dict_bytes)=O(4096).

FORWARD PROGRESS proven (no infinite loop). Each loop iteration returns or strictly advances `pos`. `lex_primitive` (pdf_lexer.py:117-148) returns either a PdfSyntaxError (callers refuse) or a LexedToken with end_offset > offset — every delimiter `()<>[]{}/%` is dispatched to a refusal or a consuming sub-lexer BEFORE `_lex_regular_run`, which only runs on a non-ws non-delimiter byte and so advances >=1. The byte-cap check at each loop top then bounds iterations to <= remaining_bytes.

ORPHAN LINETO (sheet_path_state.py:71-100) — charged, bounded, no invented segment. Probe-verified with a stub host at a 5-point budget:
- EVERY orphan charges exactly 1 point via `_charge_points(1)` (asserted each iteration); with budget 5 the 6th orphan pushed point_count=6 and REFUSED (refused_at=5). So the per-page/per-document point budgets stop a `paint l paint l …` flood.
- No unbounded memory: `_subpaths` stayed length 0 (each paint clears it), polylines 0 (a single-point orphan subpath, len<2, is never emitted).
- Fresh OWN point: each orphan set `_cur_points=[(x,y)]` (asserted), never appending to a stale list; a following real `l` drew from the orphan's own point -> polyline `((0,0),(3,0))`, never a segment from the undefined pre-paint point. Count is a Python int (no overflow), bounded by the point budget.
- Orphan CURVES still refuse: `_curveto`/`_op_v` keep "curve with no current point" (:105-106, :207-208).

SPLIT EQUIVALENCE. The moved path-group functions (`_flush_open/_open_after_close/_moveto/_curveto/_close/_charge_points/_paint/_op_*`) are byte-identical between the deleted interpreter block and the new `_PathState`; the ONLY behavioral change is `_lineto`'s None-branch (refuse -> `_orphan_lineto`). No budget, finite-check, or refusal lost. Goldens + split-equivalence suite pass.

(Part 3: scope/isolation, carry-forward advisories, verdict.)

---

M5-T120 G5 — Part 3/3 (scope/isolation, carry-forward advisories, verdict).

SCOPE / ISOLATION (source + grep):
- No new I/O, network, logging, subprocess, or `open` in sheet_path_state.py or sheet_inline_image.py (grep clean; the only `open` hits are the `_flush_open` name). No document content is ever logged.
- ZERO new dependencies: sheet_path_state imports only in-repo sheet_objects/sheet_primitives; sheet_inline_image adds no import.
- UNWIRED: no app/api or app/main.py imports read_sheet / sheet_interpreter / sheet_path_state / sheet_inline_image. sheet_path_state is imported only by sheet_interpreter (facade `_PATH_HANDLERS`/`_PathState`) and tests.
- Cross-tenant / service-role / private storage / SSRF / injection / upload / prompt-injection / log-redaction / least-privilege: all N/A for this pure in-memory parser — no DB, auth, tenant, secret, outbound I/O, storage, or LLM in path; all ingested PDF bytes are untrusted DATA, never executed. Upload ceilings + concurrency remain the future mount's job.
- Citations (AS-3) are comment-only (8.9.3; 8.9.5.1/Table 89; Table 93); no code behavior.

CARRY-FORWARD ADVISORIES (ADVISORY, non-blocking; none introduced by T120):
1. L1 (from M5-T118-G5): the ~100x document ceiling (<=50M points / 20M ops -> multi-GB/multi-second worst case) still rides on the reader being unwired. The orphan-lineto rule does NOT change that bound (each orphan charges a point under the same budgets) but it makes MORE adversarial documents READ instead of refusing early, so the future import/mount route MUST gate a wall-clock deadline + a per-request points/memory budget well below 50M + concurrency limiting before exposure.
2. L2 (from M5-T118-G5): the filtered-inline-image early-EI desync residual is unchanged; probe I confirms /DP parsing adds NO new desync (parse stays in sync past `>>`).
3. LOW-1 (from M5-T121-G5): sheet_reader.py still validates `/UserUnit > 0` but not `math.isfinite`. T120 touched sheet_reader.py only to add the orphan_subpaths counter, NOT the /UserUnit check, so this stays OPEN — non-blocking (provably finite today via the number lexer per T121); route to the reader/mount owner.

No Critical/High/Medium findings; budget enforcement, depth/desync bounds, forward progress, and orphan boundedness independently verified. Reviewer read-only; no memory file written (Write disabled this session).

M5-T120 G5 VERDICT: PASS

END-OF-REPORT

---

M5-T120 G5 DELTA ATTESTATION (sec-t120, read-only). Verified `git show 793fed27` at HEAD 858989c1.

(a) DIFF IS DOC-ONLY. Both code-file hunks are entirely inside docstrings; no code line, bound, refusal, or control flow changed:
- sheet_inline_image.py: only the `_components` docstring — "(Table 92)" -> clarified "/CS key is Table 92; its value abbreviations are Table 93 [ORCH-CORRECTED per M5-T120 G1]". The body (`for key in _CS_KEYS: …`) is unchanged. `_COMPONENTS`, `_VALID_BPC`, `_skip_dict`, `_skip_array`, `_nested_dict_allowed`, the byte cap and every refusal are byte-identical.
- sheet_path_state.py: only the `_orphan_lineto` docstring — the reader-of-record citation prose (Canvas 2D / pdf.js verified; MuPDF is STRICTER, warns-and-ignores — not a match). The body (`_map` -> fresh own-point subpath -> counter increments -> `_charge_points(1)`) is unchanged. Curves still refuse.
- Third file is the producer report (.md) — no code.

(b) IDENTITY CARRY. 793fed27 is an ancestor of HEAD 858989c1; `git diff --stat 793fed27 HEAD` for all 5 source files is EMPTY, and seam 858989c1 touched only project-control/* (gates/reports/state/task). So the reviewed parser identity is stable and my earlier PASS carries unchanged to content identity 80c33f6d at HEAD 858989c1. Harness at the new head (322 passed via shim) is consistent; a docstring edit cannot alter behavior. All Part-1/2/3 findings (the one-level /DP depth bound, byte cap, no-desync, forward progress, orphan charging/boundedness, unwired, zero new deps) stand; the three carry-forward advisories (L1 mount ceiling, L2 filtered early-EI, LOW-1 /UserUnit isfinite) are likewise unchanged.

M5-T120 G5 DELTA: PASS

END-OF-REPORT
