# M5-T064 G4 gate — independent QA/test-adequacy review (verbatim reviewer return)

Reviewer: qa-engineer (claude-opus-4-8 per D-064), dispatched by the orchestrator at the
seq-124 T064 wave, pinned to the frozen submit head 812a5860 (material 409a8a7d). Recorded
by the orchestrator; the reviewer is read-only and records no gate itself. Arrived complete
with the END marker (no truncation). Verbatim return follows.

---

VERDICT: PASS

# M5-T064 — G4 QA / test-adequacy review (D-082-R002 max-envelope engine slice 1)

Reviewer: qa-engineer (independent, read-only). Producer: scenario-optimization-engineer. This verdict binds to the frozen path-scoped content of the 5 allowed_paths; disjoint control-plane commits may land mid-review and touch none of them.

## Identity verification
- HEAD at review = `a371cae9` (advanced past submit head via disjoint peers). Byte-stability confirmed: `git diff 812a5860..HEAD -- <5 paths>` is EMPTY.
- Material `409a8a7d` own diff = exactly the 5 allowed_paths (999+343+643+275 lines + report), zero files outside scope. main.py untouched (route ships UNMOUNTED, verified by `test_route_is_unmounted_in_the_real_app`).
- Executed at HEAD: `python -m pytest tests/scenario/test_max_envelope.py tests/api/test_max_envelope_api.py -q` → **56 passed in 7.52s** (matches producer claim; case count = 37 engine + 19 route). `python tools/modularity_check.py --check` → **failures 0; warnings 22; EXIT 0**; `max_envelope.py` is a `review_signal` warning (999 lines, 1 under the 1000 hard-fail), cohesion justification recorded in the producer report §Cohesion.
- CI SUCCESS at 366a50be is the orchestrator's record (outside my sandbox); the byte-stable content passes locally at HEAD.

## AS → test mapping (producer anchor table verified against actual tests; all mutation-sensitivity judged)

| AS | Tests (verified present) | Mutation-sensitive? |
|---|---|---|
| AS-1 registry-derived binding, no hand-copied constant, unbounded→gap | `test_as1_binding_values_from_fixture_registry` (0.5/60.0 pinned to fixture rule ids), `_far_and_rear_yard_are_non_commensurable`, `_unbounded_direction_is_a_gap`, `_unattested_input_is_allowance_unresolved`, `_no_applicable_rule_gap`; drift guard `test_usable_coverage_matches_accepted_checker` | YES — the AS-2 flip test (below) changes the registry value 0.5→0.4 and the binding_value follows, proving no hardcoded constant. Drift guard binds `me._USABLE_COVERAGE == pc._USABLE_COVERAGE`. |
| AS-2 provenance names the BINDING rule + out-competed; flip flips the id | `test_as2_tightest_maximum_binds_and_names_out_competed`, `test_as2_mutation_flips_the_binding_rule_id`, `test_as2_minimum_direction_takes_the_largest_floor` | YES (strongest tooth) — flips tighter rule 0.5→0.4: binding_value, binding_rule_id AND out_competed list all follow. Pins the BINDING rule specifically, not any rule. |
| AS-3 honest gaps typed; every dimension enumerated; nothing omitted | `test_as3_all_declared_dimensions_are_enumerated` (binding XOR gap for each), `_gaps_survive_serialization`, route `test_200_returns_the_envelope` L117-120 | YES — enumerate-all asserts emitted set == declared set and exactly-one-of. All 4 EnvelopeGapReason values each have a dedicated test. |
| AS-4 generator-checker consistency; +epsilon FAILs | `test_as4_candidate_passes_the_checker_at_cap` (engine record + INDEPENDENT re-run of `check_proposal`), `_footprint_saturates_the_cap_and_is_contained`, `test_as4_epsilon_mutation_fails_the_checker` (x2: height +1e-6, east edge +1.0ft), `_tolerates_ambiguous_checker_outcome`, `test_multi_floor_height_saturation` | YES for the affirmative claim — epsilon mutation FAILs the checker; an over-cap candidate regression is caught by the independent re-run asserting PASS at 0.5. See gap #1 for the fail-closed direction. |
| AS-5 determinism + route discipline | `test_as5_deterministic` (json.dumps x2 equal), route `test_route_output_is_deterministic`, flag-off 404, UNMOUNTED, 413, 422 matrix (10 route refusal tests) | YES — matrix membership asserted on each; flag-off returns byte-identical generic 404. |
| AS-6 preservation | `test_route_is_unmounted_in_the_real_app`, `test_as6_no_scenario_document_or_contract_version` (source-token + doc-key scan), self-check rows | YES — asserts URL absent from real_app.routes AND openapi; scans module source for forbidden scenario/builder tokens. |

Consistency-proof property (G4 focus #1): removing `_verify_consistency` entirely makes `candidate_consistency` None → `test_as4_candidate_passes_the_checker_at_cap` L325 goes RED. An engine regression producing an over-cap candidate → the independent `check_proposal` re-run (L331-340) returns FAIL → RED. Both caught.

## Coverage gaps (with severities) — all NON-BLOCKING; recommended adds

1. **[MEDIUM] The generator-checker FAIL-CLOSED raise is untested.** `_verify_consistency` (max_envelope.py:919-925) raises `MaxEnvelopeError(field="candidate.<dim>")` when the accepted checker FAILs the engine's own candidate — the packet's DEFINING failure mode (risk #4). No test forces the checker to FAIL (e.g. monkeypatching `check_proposal` to return a FAIL for `lot_coverage_ratio`) and asserts the engine raises. The affirmative direction is proven three ways and is mutation-sensitive, and the branch is unreachable-by-construction — but a mutant that turned the `raise` into a silent pass would survive the suite. The route's 500-mapping of `candidate.*` (max_envelope_api.py:317-320) is likewise untested. Recommend one monkeypatch-the-checker test asserting `MaxEnvelopeError(field="candidate.max_lot_coverage_ratio")` and one route test asserting the resulting 500.

2. **[MEDIUM] The `(500, "internal_error")` route state is in the SSOT matrix but never exercised.** No test produces a 500 — not registry-unavailable (max_envelope_api.py:291-293), not generator-checker inconsistency (317-320), not the generic derive exception (330-333), not serialization-unsafe (337-341). Every other matrix pair (200/404/413/422) is asserted by a live response; the 500 row is asserted only by frozenset inclusion. Test L222's comment "never a 500" only asserts the bad-area case is a 422. A regression leaking `str(exc)`/traceback on the 500 path would pass CI. Recommend one test forcing a 500 (e.g. monkeypatch `_effective_registry` to raise) asserting the bounded generic body + `X-Correlation-ID`.

3. **[LOW-MEDIUM] The out-of-2263-bounds (wrong-unit/wrong-CRS) lot branch is untested.** `_build_candidate` (max_envelope.py:800-809) returns `LOT_GEOMETRY_UNSUPPORTED` "outside the accepted EPSG:2263 NYC bounds" — a real guard against a wrong-CRS lot silently producing a candidate. All test lots anchor in-bounds (985000/195000, 1010000/250000); grep confirms no out-of-bounds anchor. The geometry-unsupported family is otherwise well tested (absent, diagonal, interior-vertex, area-mismatch, footprint-exceeds — 5 sibling branches), so this specific branch is the odd one out. Recommend a lot placed outside NYC bounds asserting the typed gap.

4. **[LOW] Other typed fail-safe branches unexercised:** the ProposalCheckError/ProposalDerivationError/ProposedMassingError→422 branch (max_envelope_api.py:324-329, from the consistency proof's checker run over a bad lot line); `_shrink_depth_to_cap`→None→CONSTRUCTION_INVALID (838-846); the non-finite-coord (642-646), degenerate-bbox (652-656), missing-side (677-681) branches of `_lot_rectangle`. Several construction-guaranteed branches are appropriately `# pragma: no cover`; these are genuinely hard to reach. LOW.

5. **[LOW / observation, not a defect]** Determinism is proven WITHIN one process against one registry object (`test_as5_deterministic` calls twice in-process; route pops correlation_id). AS-5 as written ("same lot_context + same registry = byte-stable") is satisfied; a cross-load/cross-process determinism test would strengthen it.

## Strengths (teeth that genuinely bite)
- AS-2 flip test is the strongest anti-fabrication tooth: registry value change propagates to value + named id + out-competed, proving values are registry-derived not hardcoded AND provenance names the tightest (binding) rule.
- Anchor/translation test (`test_candidate_is_anchored_to_the_actual_lot_and_translates_with_it`) explicitly asserts two differently-located lots do NOT collapse to one placement — catches the fixed-anchor regression the deviation note references.
- All 4 EnvelopeGapReason values + the checker's `could_not_check` each have a dedicated honest-gap test; `test_real_registry_compatibility` proves honest gapping against the actual production registry (no fabricated coverage).
- Provenance fidelity is STRONG: binding_rule_id/version pinned to exact ids, out_competed pinned, rule_citations asserted carried, every binding value flows through `RuleResult.export()` (fail-closed on missing provenance).

## Judgment
Six acceptance scenarios each map to concrete, mutation-sensitive tests that pass at the frozen identity; the anti-fabrication and generator-checker-consistency core proofs (AS-1/AS-2/AS-4) are the strongest-covered. The gaps are on fail-closed / defense-in-depth / declared-but-unexercised branches where the positive behavior is well covered — none leaves an acceptance scenario's affirmative claim unproven, and no mutant of the CORE behavior survives. Test adequacy meets the G4 bar. Recommended (non-blocking) adds: gaps #1, #2, #3.

## Commands run
- `git diff 812a5860..HEAD -- <5 paths>` → empty (byte-stable).
- `git show --stat 409a8a7d` → exactly the 5 allowed_paths.
- `cd services/api && python -m pytest tests/scenario/test_max_envelope.py tests/api/test_max_envelope_api.py -q` → 56 passed in 7.52s.
- `python tools/modularity_check.py --check` → failures 0; warnings 22; EXIT 0 (max_envelope.py = review_signal, justified).

--- END OF REPORT ---
