# M0-T091 — G5 Independent Security Review

> Saved VERBATIM by the orchestrator from the security-reviewer agent-return channel (transport
> entity-decoding only). Verdict: PASS (PASS-with-findings; 5 LOW/advisory L1–L5; none blocking;
> all five carried M0-T090 findings M1–M4/N1 verified REMEDIATED).

I have completed my independent review. All evidence is gathered: deliverables byte-identical to the frozen SHA, 54/54 tests pass, modularity exit 0, imports are stdlib+siblings only, no actuation surface, no external importer wiring, no sensitive content, and every prior finding remediation hand-traced and cross-checked against the passing test pack. Here is my gate report.

---

# Gate Report

- **Gate ID:** G5 (independent security review)
- **Task ID:** M0-T091 (D-024 Phase C2 — invisible runtime supervision)
- **Reviewer:** security-reviewer (independent; read-only; ≠ producer)
- **Producer:** orchestrator
- **Result:** **PASS** (PASS-with-findings; 5 LOW/advisory; none blocking)
- **Clean environment / frozen identity:** Reviewed at frozen content commit `ee564dd`. `git diff --stat ee564dd..b4f7e40` touches only control-plane files (`gates/`, `reports/*-G2*`, `evidence-map`, `state.json`, `tasks/M0-T091.json`); the diff for the deliverables (`tools/agent_supervisor`, `tools/test_agent_supervisor_runtime_supervision.py`, `project-control/reports/M0-T091-runtime-supervision.md`) is **empty** — working tree is byte-identical to the frozen SHA for every reviewed file. Live HEAD `b4f7e40` verified.

## Acceptance surface reviewed

`acceptance_scenarios` is empty; the security acceptance surface is the six charter dimensions plus verification of the carried M0-T090 remediations (M1–M4, N1). Files reviewed: `tools/agent_supervisor/{lease_runtime,runtime_health,runtime_detectors,extension_gate,child_handoff}.py` (new), `{subagent_contracts,workload_classifier,workload_sizing}.py` (edited), `tools/test_agent_supervisor_runtime_supervision.py`, `project-control/reports/M0-T091-runtime-supervision.md`.

## Prior-finding remediation verification (each independently re-derived at `ee564dd`)

| Finding | Verdict | Reproduced evidence |
|---|---|---|
| **M1** — spaced/spelled percent leak forms (fail-open leak-guard variant) | **REMEDIATED (PASS)** | `HealthBands.numeric_strings()` (subagent_contracts.py:316–332) now emits `repr(v)`, `{v:g}`, `{p}%`, `{p} %`, `{p} percent` for every threshold; `assert_no_envelope_leak` matches all forms. Hand-trace against default `prepare=0.70`: `"you are at 70 % now"` → `"70 %"` ∈ leak_values → raises `envelope_leak`. Independent second net: new `_QUOTA_PATTERNS` class `percent_numeric` (`\b\d[\d,_.]*\s*(?:%|percent(?:age)?s?\b)`) catches any numeric percent regardless of spacing/spelling. Test `test_leak_guard_catches_spaced_and_spelled_percent_thresholds` + `test_quota_guard_catches_paraphrased_pressure` — pass. |
| **M2** — leak-guard false positive on bare `observe`/`land` (word-boundary fix) | **REMEDIATED (PASS)** | `_BAND_LEAK_PATTERNS` (subagent_contracts.py:646–652) replaced the `if band in lowered` substring test with four word-boundary regexes (`\bprepare[\s_-]+to[\s_-]+land\b`, `\bemergency[\s_-]+stop\b`, `band(s) <name>`, `<name> band(s)`). Hand-trace: `"Repair the landing page hero section."`, `"island"`, `"England"`, `"observe the failing test"` no longer match; `"prepare_to_land"`, `"emergency_stop"`, `"observe band"`, `"health band land"` still raise. Tests `test_leak_guard_word_boundary_passes_common_english`, `test_leak_guard_still_catches_band_vocabulary` — pass. |
| **M3** — lease path canonicalization (dot-segments, absolute/traversal/root) | **REMEDIATED (PASS)** | `_normalize_lease_path` (subagent_contracts.py:468–500) rejects absolute forms via `_ABSOLUTE_LEASE` (`^(?:[/\\]|[a-z]:[/\\]?)` — POSIX root, UNC/backslash root, drive letter incl. drive-relative `C:foo`), rejects any `..` segment after backslash→slash conversion, normalizes dot-segments with `posixpath.normpath`, and refuses paths normalizing to root (`""`/`"."`). Hand-traced M0-T090 dodges now closed: `pkg/../other` refused; `./pkg`≡`pkg` overlap; `pkg/./sub`≡`pkg/sub` overlap; mixed-separator `pkg\..\other` refused. Case-fold (`.lower()`) is the fail-closed direction for overlap (can only merge distinct strings, never split equal ones → never misses a real overlap). Tests `test_root_lease_rejected_not_dodged`, `test_lease_paths_normalize_dot_segments_and_reject_traversal` — pass. |
| **M4** — snapshot-vs-lock documentation + `LeaseLedger` serialized fold (TOCTOU) | **REMEDIATED (PASS)** | Documentation: `assert_grantable` docstring (subagent_contracts.py:523–531) now states "SNAPSHOT, NOT A LOCK … two candidates validated against the same snapshot can both pass … `lease_runtime.LeaseLedger` serializes grants and folds each granted envelope into the active set before the next candidate is checked." Implementation: `LeaseLedger.grant` (lease_runtime.py:74–100) validates against `active_envelopes()` (the LIVE set) then folds atomically — no check→fold yield window in the single-threaded single-controller model, so two overlapping candidates cannot both pass. Nested children pass the same fold (cap + overlap enforced); `release` refuses undrained children; duplicate/unknown-parent/unknown-grant are typed errors. Test `test_grant_ledger_serializes_where_snapshot_validation_cannot` proves the snapshot hole first, then the ledger closing it; `test_nested_children_cannot_evade_producer_cap_or_leases`, `test_ledger_exactness_duplicate_unknown_parent_and_drain_rules` — pass. |
| **N1** — `worker_text_fields` fail-closed on unscannable types | **REMEDIATED (PASS)** | `worker_text_fields()` (subagent_contracts.py:176–210) now raises `unscannable_field` on any non-`str`/non-`tuple[str]` field, and on any non-str tuple element; `bool` explicitly exempt. Hand-trace: a `dict`/`None`/`list` worker-facing field raises instead of silently escaping the guard. Test `test_worker_text_fields_fail_closed_on_unscannable_types` — pass. |

The two other edited files carry the remaining carried corrections and are security-neutral/additive: `workload_classifier.SIZE_CLASS_ERROR_CODES` (G3 MINOR-5, a closed error-code registry, no rename of pinned codes) and `workload_sizing.NON_OMITTABLE_CATEGORIES` + `packet_plan` `non_omittable_category` refusal (G4 ADV-1, adds a fail-closed refusal — strictly tightens).

## Steps independently executed

- `git log --oneline -5`; `git rev-parse HEAD` (b4f7e40); `git diff --stat ee564dd..b4f7e40 -- <deliverables>` → **empty** (working tree == frozen SHA).
- `python -m pytest tools/test_agent_supervisor_runtime_supervision.py -q` → **54 passed in 0.25s** (Python 3.11.9).
- `python tools/modularity_check.py --check` → **failures 0; warnings 5; EXIT=0**. None of the 5 new modules is flagged (largest, `runtime_health.py`, well under the 600 warn line; the 5 warnings are pre-existing files outside this diff).
- Import inventory of all 5 new modules: only `dataclasses`, `collections.abc.Mapping`, `typing.Any`, and package siblings (`.subagent_contracts`, `.telemetry_records`, `.runtime_detectors`, `.lease_runtime`, `.runtime_health`). **No third-party dependency; no `subprocess`/`os`/`socket`/`urllib`/`threading`/`asyncio`/`open`/`Popen`/`exec`/`eval`/`Task`/`Agent`.**
- Actuation/IO token scan across all new + edited modules → **no actuation or IO call** (NOTHING spawns/resumes/stops/messages/reconfigures an agent).
- External-importer scan of the 5 new module names across `tools/` → only docstring/comment mentions in `subagent_contracts.py`; **no `import` wires them into any dispatch loop** (`cli.py`, `loop.py`, `resume_scheduler.py`, package `__init__.py` unmodified). Shadow-only confirmed.
- Sensitive-content scan → no secrets/home paths/usernames (the `sk-` regex hits were the substring in "ta**sk-**stop"/"ta**sk-**graph"/"sdk-ta**sk-**cumulative", benign).
- Adversarial leak-guard probes: hand-traced deterministically (the read-only guard blocks arbitrary `python -c`, allowing only pytest/modularity; the load-bearing cases are covered by the passing 54-test pack, and each residual below was derived from the exact regex source).

## Security dimensions

1. **R045/R056 leak surfaces — exactly ONE worker-visible text path.** The only text any runtime module can send a worker is the fixed constant `LANDING_DIRECTION_TEXT` ("Finish the current atomic step, save and test what is coherent, and return the bounded handoff." — no digits, no bands), produced only when `evaluate_band` yields `ACTION_SEND_LANDING`, gated once-per-assignment by `SupervisionState.apply`, and re-proven through **both** `assert_worker_text_clean` + `assert_no_envelope_leak` at send (runtime_health.py:343–344). `TurnoverCoordinator.land_child` returns the same constant. Hand-traced bypass attempts: `DetectorFinding.detail`, `BandEvaluation.reasons`, `ExtensionDecision.reasons`, `BacklogEntry.summary`, and `ChildHandoff` fields are all controller records — none is routed into a worker message. No threshold can leak by reformatting the new `numeric_strings` forms in any real path (rendered prompt and landing message both run the worker-text guard, whose `percent_numeric` catches every numeric percent).
2. **Lease/scope enforcement.** TOCTOU closed (M4). Canonicalization edges (Windows drive/UNC/backslash/case/trailing-slash/`..`-smuggling) handled (M3). Release/drain exact (`children_not_drained`), nested-child evasion prevented (same fold + cap), duplicate/unknown ids typed. Read-only grants correctly excluded from the producer cap (`test_read_only_agents_do_not_consume_the_producer_cap`).
3. **Fail-closed discipline.** Every validator raises a typed error (`ContractError`/`LeaseRuntimeError`/`HealthRuntimeError`/`DetectorError`/`ExtensionError`/`HandoffError`, all `ValueError`). Unknown = `None` never zero (`TelemetrySnapshot`, `model_context_window`, occupancy `None`→conservative `observe`). Config loaders reject unknown keys — `HealthBands.from_controller_config` and `bands_for_model` (`[subagent_model_bands]`) both raise `unknown_band_key`, and re-validate band ordering/range on construction.
4. **Actuation surface.** Nothing spawns/resumes/stops/messages an agent. `ACTION_EMERGENCY_STOP`/`TaskStop` are recorded action strings only. `validate_platform_caps` refuses **any** non-`None` `max_turns`/`max_budget_usd` with `routine_cap` (airtight — even `0` is refused); the only representable ceiling is a `CatastrophicCeiling` requiring `ceiling ≥ 5× normal_range`. R595 / D-024 §18 shadow-only posture intact.
5. **Public-repo data hygiene.** No secrets/paths/usernames; no network; no new dependency (stdlib + siblings only); deterministic digests reused unchanged. Prompt-injection posture (prior N3): the runtime adds no new untrusted interpolation — it **reduces** the worker-visible surface to a single constant — so the N3 markdown-fencing risk is not increased by this unit.

**Standard G5 checklist items Not Applicable to this diff (stated honestly):** cross-tenant RLS isolation, service-role secret handling, private Storage access, SSRF, SQL/command injection, and upload controls — none apply (no DB/RLS surface, no network client, no Storage/HTTP, no untrusted external input, no file upload). Prompt-templating is the only injection-adjacent surface and is covered above.

## Defects (by severity)

**Critical: none. High: none. Medium: none.**

**Low / advisory (non-blocking; for the activation / runtime-enforcement package):**

- **L1 (LOW — functional over-block, fail-closed/safe direction).** `_QUOTA_PATTERNS` `conserve_synonym` (`(conserve|save|saving|spare|sparing|economical|economiz*|frugal*) … within 60 chars … (tokens|context|budget|window|capacity)`) flags legitimate engineering prose: hand-traced `"Add spare-capacity monitoring to the connection pool."`, `"Save the window state to disk."`, `"Economical context switching in the scheduler."` all raise `quota_language`. Over-blocking is the safe direction for R045 and the input is controller-authored, so this is not a security hole; but once contracts are actuated it will spuriously refuse valid assignments. Remediation: require a stronger conserve-verb/target coupling (e.g., possessive "your", or "to/of" preposition) before flagging, and add a small allow-list of common false friends.
- **L2 (LOW — backstop under-block on trusted input).** Conserve-pressure paraphrases outside the synonym list evade: hand-traced `"don't waste tokens"`, `"limit your context usage"` pass `assert_worker_text_clean`. Non-exploitable because the primary R045 control is structural (the worker prompt is built only from assignment fields via a fixed template; the envelope is never interpolated) and the controller is the trusted author; the guard is a defense-in-depth net for accidental phrasing. Remediation: add `waste`/`limit`(-yourself) synonyms.
- **L3 (LOW — band-name bare-word under-block, documented trade).** `_BAND_LEAK_PATTERNS` deliberately no longer flags bare `land`/`observe`; unusual band-context phrasings like `"your band is now land"` are not caught. The numeric thresholds are independently caught and the envelope is never interpolated, so this is the intended false-negative trade to eliminate the M2 false positives. No action required beyond the recorded note.
- **L4 (informational — error-surface consistency).** `LeaseLedger.assert_write_within_scope` normalizes the write path with `_normalize_lease_path`, so an **absolute/traversal write path** raises `ContractError("bad_lease_path")` rather than `LeaseRuntimeError("scope_violation")`. Still fully fail-closed (both are `ValueError`; the write is refused), but a caller catching `LeaseRuntimeError` specifically would miss it. Consider catching/re-raising as `scope_violation` at the runtime boundary.
- **L5 (informational — backstop completeness).** `numeric_strings()` omits non-round percent forms of non-integer thresholds (e.g., `"50.5%"` for a `0.505` band). Covered in every real path by the worker-text `percent_numeric` pattern (run alongside `assert_no_envelope_leak` in both `render_worker_prompt` and `SupervisionState.apply`), so no functional leak.

## Required rework

**None for this gate.** All five carried prior findings (M1–M4, N1) are genuinely and completely remediated with red/green tests. L1–L5 are hardening/consistency items to fold into the follow-up runtime-enforcement/activation unit; none is exploitable in this shadow-only unit and none blocks acceptance. (Per the project's "PASS with recommended corrections" convention, the orchestrator may choose to carry L1–L2 as blocking for the activation unit, where these validators graduate from backstop to live enforcement.)

## Reviewer conclusion

**PASS (PASS-with-findings).** At frozen content commit `ee564dd` (deliverables byte-identical to live HEAD `b4f7e40`), M0-T091 delivers five focused, leaf-only, fail-closed runtime-supervision modules with **no actuation surface** (R595 / §18 shadow-only preserved — verified by import inventory, actuation-token scan, and the absence of any external importer wiring), a single guard-proven worker-visible text path (the fixed landing sentence — R045/R056 intact), a serialized `LeaseLedger` that closes the M4 TOCTOU, hardened lease canonicalization (M3), fail-closed field scanning (N1), broadened but deliberately-over-blocking quota/leak guards (M1/M2), no new dependency, no network, clean public-repo hygiene, modularity green, and 54/54 tests passing. All five carried M0-T090 security findings are verified remediated. No critical, high, or medium defect; five LOW/advisory items recorded for the activation package.

**VERDICT: PASS** (non-blocking LOW findings L1–L5 recommended for the runtime-enforcement follow-up).
