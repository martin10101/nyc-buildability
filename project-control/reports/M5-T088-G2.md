# M5-T088 - G2 producer self-check record (recorded by the orchestrator, role self_check)

Producer: 3d-massing-engineer (orchestrator-dispatched subagent) in wt-m5t088; one in-worktree commit 353b919d (parent 114e5e56), cherry-picked to 9f355fe1 with every file MATCH by git blob identity.

- **Scope:** exactly massing_model.py, its test file and the producer report; max_envelope.py and every other scenario module untouched; no dependency change; unwired.
- **Producer self-checks (report, OBSERVED):** ruff clean; 54 passed; modularity exit 0 with one new warning (massing_model.py 663 SLOC - cohesion justification in the report); 14 mutants in a fresh interpreter each (naive fan on the new concave fixtures, dropped CW normalization, dropped collinear collapse, tolerance 50, world-coordinate volume, removed bounds and budget, off-by-one boundaries, and the accepted reverse-bottom-cap guard) all RED; output bytes of 12 valid fixtures identical to the accepted module.
- **[ORCH-HARVEST] at the cherry-picked head (Python 3.11.9, cwd services/api): `python -m ruff check .` -> All checks passed!; `python -m pytest tests/scenario/test_massing_model.py -q` -> 54 passed; repo root modularity exit 0 (massing_model.py 663 SLOC now in the warning band - producer discovery 3 routes a triangulation split).**
- **Behaviour changes disclosed:** floor-cap reason over_cap_vertices -> over_cap_floors (runs up front); lot containment now runs before triangulation; identical per-level outlines triangulated once; an extreme 998-vertex spiral is now refused (fail-closed limit).
- **Discoveries routed (producer):** the proposal validator's O(n^2) simplicity check is itself a CPU sink (~22 min worst case per request) before any massing bound; no NYC 2263 range check on the lot ring; split massing_model.py (triangulation module) before it grows; shapely GEOSException is not a ValueError; reflex-only ear clipping would be faster; stale code-graph cache.

G2 verdict: PASS (self_check; G3 geospatial-engineer, G4 qa-engineer, G5 security-reviewer and the DCV follow).
