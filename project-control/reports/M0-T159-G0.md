# M0-T159 G0 — readiness gate (orchestrator, administrative)

Task: **M0-T159 — B-026 remedy: Claude Code 2.1.281 admission + recertification on the CERTIFIED controller tree a5886dab (runbook section 13 / R287; M0-T132 precedent) - recapture the live fixture pack, re-point the drift teeth, whole-suite recert, manifest bound to the current config in a TEMP path; commissioning steps stay owner-typed**

## Authority

- B-026 remedy: the controller refuses every lane launch until Claude Code 2.1.281 (installed
  under owner directive D-085-R002) is admitted per D-024-R287 (standing: 'update intentionally,
  recapture fixtures, recertify, and only then repin'); the lanes are D-087-R001 capacity.
  Procedure = docs/CONTROLLER_UPDATE_RUNBOOK.md section 13; precedent = accepted M0-T132.
- Directive refs: D-024-R287, D-087-R001, D-066-R001 - bound via applicability.task_ids with same-commit digest
  resyncs + audit_log entries; `evaluate_task_refs`: **ok=true, applicable == cited**.
- Scope source: B-026 (consolidated deficit-convergence blocker) + the M0-T132 fixture set; the
  candidate tree is the CERTIFIED controller commit a5886dab (uncertified M0-T149/T152 changes
  stay out).
- Boundaries: the live controller (three lane checkouts, Program Files config, wt-controller-src,
  every lane journal) is READ-ONLY for the producer; the activation-manifest overwrite, the
  controller re-point and --repin-cli-identity stay OWNER-TYPED commissioning (runbook section 12,
  M0-T132 section-5 precedent); PR #241, Tier D and every other hold unchanged.

## Packet readiness

- allowed_paths: 12 entries (event_drift.py and the four test packs are tracked; the four new fixtures and three
  reports are created by the producer - NOT seeded, because a placeholder fixture would be read
  by the fixture globs and fail the drift teeth).
- 5 acceptance scenarios, each with a named mutation or
  grep/CI proof obligation; documented_test_commands are plain single commands.
- Producer: backend-engineer; reviewers: code-reviewer,qa-engineer,directive-compliance-verifier; gates G0,G2,G3,G4.
- Code graph regenerated at this seam (817 files/17027 nodes/7441 edges); the navigation block names consumers,
  forbidden neighbors, and the `query.py --no-regen impact` instruction (D-066-R001).

## Disjointness (live/frozen neighbors)

| Neighbor | State | Overlap |
|---|---|---|
| M5-T001 | awaiting_gate | none - EMPTY overlap |
| M5-T077 | claimed | none - EMPTY overlap |
| M5-T078 | claimed | none - EMPTY overlap |
| M5-T079 | claimed | none - EMPTY overlap |
| M5-T080 | claimed | none - EMPTY overlap |
| M5-T081 | awaiting_gate | none - EMPTY overlap |
| M5-T082 | awaiting_gate | none - EMPTY overlap |
| M5-T083 | awaiting_gate | none - EMPTY overlap |
| M5-T084 | awaiting_gate | none - EMPTY overlap |
| M5-T085 | awaiting_gate | none - EMPTY overlap |
| M5-T086 | awaiting_gate | none - EMPTY overlap |
| M5-T087 | awaiting_gate | none - EMPTY overlap |

## Seam records

- Packet + binds + digest resyncs + placeholder + this report land in ONE contract-seam
  commit; G0 is recorded at that head; claim with the FULL worktree path; the worktree is
  created AT the claim-seam commit.

Verdict: **PASS** — packet is contract-ready.
