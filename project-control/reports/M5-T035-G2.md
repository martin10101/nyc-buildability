# M5-T035 — G2 producer self-check record (recorded by orchestrator)

G2 is the producer self-check gate; the CLI rejects the producer's own name, so the
orchestrator records it over the producer's documented self-check evidence (established
convention, PROGRAM_KNOWLEDGE).

- **Producer self-checks (report §2, run 41 in wt-m5t035, producer-run):** all six documented
  commands executed by the producer with observed outputs — `python -m ruff check
  services/api` clean; `tests/spatial` 76 passed; `test_dcm_street_centerline_arcgis.py` 69
  passed; `test_wide_street_buffer_engine.py` 54 passed; `test_rule_evaluation_api.py` 38
  passed; `python tools/modularity_check.py --check` failures 0. The producer report
  (`project-control/reports/M5-T035-producer-report.md`) separates observations from
  conclusions, marks unverifiable items UNVERIFIED (AS-7 CI half, ENVELOPE_MARGIN_FT
  adequacy), and claims no completion.
- **Orchestrator harvest re-run (same tree, before the material commit):** all six commands
  re-run green (identical tallies).
- **Identity:** self-checked tree == material commit `97fa2eea` content (the cherry-pick was
  clean; the four independent G1/G3/G4/G5 reviews at the same pinned identity reproduced the
  same six-command results).
- **AS-3/DB-014 disposition:** the producer correctly left the structured-provenance
  sub-criterion unresolved for orchestrator ruling rather than closing or waiving it (report
  §4); ruled at dispatch (decision effect via existing contract fields; structured block =
  DB-014 follow-up).

**G2 verdict: PASS.**
