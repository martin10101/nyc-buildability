# M5-T038 — G3 code review return (VERBATIM; code-reviewer, read-only, pinned d94bbcb6)

Orchestrator note: returned complete (untruncated) via the agent-return channel
2026-09-18T09:56:20Z. VERDICT: PASS (one non-blocking advisory — recorded for the backlog).

---

G3 code review — M5-T038 (address-flow + context-panel polish: DB-006/007/009/019a-c). VERDICT: PASS (one non-blocking advisory).

IDENTITY / SCOPE
- All 13 packet allowed_paths byte-stable d94bbcb6..HEAD (git diff empty for each). Frozen submission identity holds.
- Out-of-packet correction 8c089343 verified: a single line in apps/web/e2e/architect-workspace.spec.ts:223 updating the rate-limited copy assertion to the new shared-constant wording, precedent-tagged, outside allowed_paths — does not move the frozen identity.
- Material commit d94bbcb6 = 8 web files + report, +730/-16. No new computation/schema/data source; ADDRESS_SEARCH_TIMEOUT_MS stays 6000 (address-search.ts:9, untouched). development-limits.test.tsx (M5-T037 lane) is NOT among the 9 files — untouched.
- modularity_check.py --check: 442 files, failures 0; none of the 4 touched source files in the 19 (all pre-existing api/tools/surveyReview) warnings.

FINDINGS (all judge criteria)

1. Retry/label state machine (DB-006) — CORRECT. address-search.ts:128-139: 429→rate_limited (returned before the >=500 check, not retried); status>=500→source_unavailable; every other non-2xx→rejected. Retry loop is gated strictly on reason==="source_unavailable" (address-search.ts:175), so a 4xx spends one attempt and can neither be retried nor borrow the transient label. Tests prove it red/green: 404/422/400 assert calls===1 + reason "rejected"; 500 asserts calls===ADDRESS_SEARCH_MAX_ATTEMPTS + "source_unavailable" (address-search.test.ts:156-189). Typed-union extension is safe: the ONLY consumer that branches on the reason is ERROR_MESSAGES: Record<AddressSearchErrorReason,string> (AddressAutocomplete.tsx:24) — TS-enforced exhaustive, includes the new `rejected` line naming the button; use-address-suggestions.ts passes the outcome through without branching; no switch-with-default anywhere. The non-architect resolve flow uses a SEPARATE AddressErrorOutcome.state type (AddressOutcomeCards.tsx:211) and is unaffected — the "rejected" name-collision is across two distinct type systems, not a real overlap. No silent swallow; `rejected` surfaces honest copy.

2. PropertyOverview additive (DB-019a/b) — CONFIRMED additive. PropertyOverview.tsx:58-65 adds a data-testid + aria-hidden ↗ on the present branch and an absent-note span on the previously-null branch; public props/markup contract preserved. Authoritative proof: vitest was fully green 42/42 at the material head (CI evidence), which exercises all named consumers (ArchitectEntry, ProfileViews, PropertyFacts, ReportView, ScenarioWorkspace) and the M5-T037 development-limits test — any breakage would have shown as a vitest FAIL.

3. Copy/affordance single-source (DB-009) — PASS. FULL_ADDRESS_SEARCH_LABEL (AddressAutocomplete.tsx:16) is the single source for the button label (:174) AND every failure line that names it (:25-31); AS-4 asserts both the button and the status read the same constant (autocomplete.test.tsx:83-94) so label/copy cannot drift.

4. Accessibility (DB-019a/c) — PASS. aria-hidden sits only on the two decorative ↗ glyphs (PropertyOverview.tsx:59, ZoningContextPanel.tsx:58); accessible link names preserved ("Open ZoLa"/"Open in ZoLa"). CoverageBadge (CoverageBadge.tsx:9-18) renders enum value text + SR gloss + aria-hidden symbol — never colour alone. DB-019c badge is guarded (ZoningContextPanel.tsx:119-121: only present designation rows with a coverageStatus), read from the existing mappedFeatureView.coverageStatus (contract.ts:254/265) — display only, nothing computed.

5. No new computation/schema/data source; deadline untouched — PASS (see scope above).

6. Test quality per CODING_RULES — PASS. Typed DOM queries (querySelector<HTMLElement>/querySelectorAll<HTMLElement>, zoning-context-panel.test.tsx); getByRole with exact-string names, no substring regex traps; deliberate invalid-shape probe as `null as unknown as string` (zoning-context-panel.test.tsx:264); AS-3 focus-return asserted via waitFor on document.activeElement, not a synchronous focus (address-resolution.test.tsx:1005-1025); AS-2 asserts the timeout button is present AND not disabled (autocomplete.test.tsx:68-81).

ADVISORY (non-blocking, no rework required)
- DB-019b absent-note copy: PropertyOverview.tsx:64 and ZoningContextPanel.tsx:62 are byte-identical ("The city map link needs a valid BBL, which this lot did not provide.") and a runtime test asserts their equality (zoning-context-panel.test.tsx:252/271). But AddressConfirmCard.tsx:146-147 reads "The city's map link needs a valid BBL, which this result did not provide." — meaning-consistent, NOT byte-matched. The d94bbcb6 commit message and the packet objective say "copy-matched to panel + confirm card"; only the panel is byte-matched. AS-6 requires only the panel match (which holds), and the confirm-card wording is contextually appropriate ("this result" for a resolved address vs "this lot"), so this is not a defect — the claim just slightly overstates. Also structural: the absent note is a duplicated string literal across three surfaces rather than a shared constant (DB-009 got a constant; DB-019b did not); drift between the two architect surfaces is caught by the equality test, but the confirm card is independent. Worth aligning or correcting the claim on a future touch; not blocking here.

CI (orchestrator-captured, verified against committed evidence): vitest 42/42 green at d94bbcb6; one Playwright journey failed only on the pre-change rate-limited sentence; corrected out-of-scope at 8c089343 → all 20 checks green. Web behavior proof is CI-only per thin-client rule; I did not run npm/vitest.

VERDICT: PASS.
