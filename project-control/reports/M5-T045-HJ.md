<!-- Orchestrator capture: human-journey-reviewer return for M5-T045 saved VERBATIM
     (transport entity-decoding only). Pinned at frozen head 2b851c18; the reviewer
     verified byte-identity + two digests and walked all six journeys. -->

Memory recorded. Here is my G3 human-journey review report to return to the orchestrator.

---

# G3 Human-Journey Review — M5-T045 (condo billing-BBL live wiring, D-073-R006 display increment)

**Reviewer:** human-journey-reviewer (read-only, ADR-005)
**Task:** M5-T045 — amended AS-3 / AS-8 (records-vs-allowances condo display)
**Frozen reviewed head:** `2b851c18` (branch `candidate/D-024-mrl-option-b`; live HEAD `7c935d25`, control-plane-only commits later)
**Verdict: PASS** — zero blocking findings; 4 advisories (A1–A4).

## Identity / evidence verification (done first, via git plumbing — no writes)

- Material cherry-pick `28082288` is **byte-identical** to the frozen reviewed head `2b851c18` for all display + backend seam files (`git diff --stat 28082288 2b851c18` empty), and `2b851c18` → HEAD is empty for the same set. Reviewed files = CI-tested files.
- Verified two pinned LF-normalized digests at `2b851c18` against `M5-T045-source-sections.md`: `PropertyOverview.tsx` = `649f8704…1371d` ✓; `AnalysisIdentityNotice.tsx` = `97b0f0ef…c9561` ✓.
- Stored CI evidence (`project-control/reports/M5-T045-ci-evidence.md`): run `35431367744` **success** at `21ee7379`, covering web-e2e (vitest + Playwright vs recorded-official-fixture API). The T045 web surface is byte-stable from `28082288` through `21ee7379`, so that CI covers the reviewed files. I relied on the stored CI artifact (no local npm/node), as instructed.

I therefore judge behavior from source + the executable tests + the stored CI evidence, all at one verified content identity.

## Journey narratives (walked as a skeptical analyst)

**J1 — Resolved-single condo.** A profile carrying `condo_resolution: [resolved_single_base_lot] billing BBL … resolved to the single recorded base lot 1003030019 … RECORD, not a computed allowance.` On PropertyOverview, `CondoResolutionRecords` (PropertyOverview.tsx:135) renders a records section titled "Condo billing lot — recorded base lot(s)" with the note text verbatim (machine prefix stripped, PropertyOverview.tsx:145), and the guard `condoWithholdsAllowances` returns **false** (the token equals the one allow-listed success), so DevelopmentLimits still shows the computed allowances (evaluated FAR 1.50, cap 15,000 sq ft). Tests: condo-resolution-display.test.tsx:185-200, 303-310; printed-brief control report-view.test.tsx:331-347. The story is legible: a record of the substitution, allowances presented separately, "RECORD, not a computed allowance" stated explicitly. **Honest.**

**J2 — Multi-lot condo.** Profile carries a `condo_base_lot_resolution` conflict with ≥2 base-lot values. `condoWithholdsAllowances` returns **true** via `condoHasMultiLot` (PropertyOverview.tsx:97-98,117), so PropertyOverview nulls scenario+evaluation (PropertyOverview.tsx:176-177) → DevelopmentLimits shows "Not calculated" for evaluated FAR and cap. `CondoResolutionRecords` renders each base lot as a record ("Recorded base lot … — recorded zoning: … / not recorded (unknown)") under "Multiple recorded base lots — no computed allowance" plus "no single base lot is presented as the answer and no allowance is computed here." UNKNOWN zoning is preserved, never fabricated (PropertyOverview.tsx:152; test 260-267). No decimal/FAR figure is emitted from the records (test asserts `not.toMatch(/\d+\.\d+/)`, 213, 257). The withhold is **explained**, never a silent blank. Screen tests 279-292; brief tests 375-391. **Honest.**

**J3 — Unresolved + fail-safe token cases (the core promise).** The guard is a fail-safe **allow-list** (PropertyOverview.tsx:116-123): allowances survive ONLY when *every* present `condo_resolution` note carries the success token AND there is no multi-lot conflict. I verified each failure class withholds on **both** surfaces:
- unresolved note → withhold (screen 294-301; note "matched no base-lot record … no reference number is presented as a computed result");
- MISSING token (no bracket → `outcome` null) → withhold (screen 312-328; brief 349-361);
- MALFORMED bracket (regex `/^condo_resolution:\s*\[([a-z_]+)\]\s*/` fails on spaces → null) → withhold (screen 343-354);
- UNKNOWN token (well-formed but ≠ success) → withhold (screen 330-341; brief 363-373).
Answer to the central question — *would an analyst ever see a computed allowance on a condo whose resolution is not the recognised success?* **No.** The allow-list fails safe on ambiguity, and independently the backend live-provider (`build_live_substrate`, live_provider.py:262-267) returns `None` on any `is_fail_safe` outcome so multi-lot/unresolved never even produce an evaluation to display. Two independent fail-safes, both in the withhold direction.

**J4 — Identity notice.** `AnalysisIdentityNotice` (AnalysisIdentityNotice.tsx:20) renders only on a mismatch/absent analyzed BBL; copy is neutral: "The identifiers are recorded as entered versus analyzed only — no relationship between them is inferred — and no calculated allowance is shown while they differ." No billing/base-lot/condo inference (test asserts `not.toMatch(/base lot|billing|condo|resolved/i)`, 136). `data-identity-state` = differs|absent. This is consistent with the T046/T047 entered-vs-matched identity story and does not overclaim protection. Cases covered: null-doc/equal (renders nothing), differing, absent, unrelated-mismatch (tests 104-176; brief 275-317).

**J5 — Screen/brief parity.** ONE shared guard function `condoWithholdsAllowances` is imported by PropertyOverview (:175) and ReportView (:38); `CondoResolutionRecords` and `AnalysisIdentityNotice` are the **same** components on both surfaces. There is no second data path, so the two surfaces cannot disagree on the withhold decision or the records. On withhold, both feed DevelopmentLimits `scenario=null, evaluation=null` → identical "Not calculated". Verified structurally + by the parallel screen/brief test pairs. **Parity holds.**

**J6 — Accessibility.** `CondoResolutionRecords` is a `role="group"` with `aria-label="Condo billing lot resolution record"` (accessible name present). `AnalysisIdentityNotice` is `role="alert"` — appropriate for an important withhold announcement, and it renders on the screen for all analysis views except the report (ArchitectEntry.tsx:168-172) and on the report via ReportView (:75-76), never double-rendered. Reading order is correct on both surfaces: the condo records / identity notice appear **before** the (withheld) Development limits, so the reader sees the explanation before the "Not calculated" values. One minor gap (A3 below).

## Findings

**No blocking findings (F): none.**

**A1 (advisory) — the records-view + withhold guard are DORMANT in production; only the identity notice is a reachable increment.** Producer report §6/§10 and the code comment (PropertyOverview.tsx:52-55) disclose that `condo_resolution_report` (zoning_crosscheck.py:524) has **no production caller**: the four profile-build routes call `build_property_profile(...)` without `additional_conflicts`/`additional_notes`, so no `condo_resolution:` note or `condo_base_lot_resolution` conflict reaches a real profile today. Consequently `CondoResolutionRecords` renders nothing and `condoWithholdsAllowances` is inert for real users. The genuinely reachable surfaces are (a) `AnalysisIdentityNotice` (wired via ArchitectEntry + ReportView) and (b) the backend professional-review fail-safe (`build_live_substrate → None`). This matches the recorded ORCH-SCOPE-DISPOSITION (records-view split to DB-031) and amended AS-3, so it is **not a defect of this packet** — but producer report §8 marks AS-3/AS-4 "PASS … CondoResolutionRecords multi-lot records view, screen + brief," which reads as *reachable* when it is *built + unit/CI-tested against synthetic fixtures only*. Recommend the orchestrator (i) read AS-3/AS-4 PASS as satisfied by the backend fail-safe (the amended visible surface), not the records view, and (ii) confirm DB-031 is tracked so the built display does not sit unreachable indefinitely. No production honesty is harmed today because the backend independently withholds.

**A2 (advisory) — "the two sides can never drift" is overstated.** The success token `resolved_single_base_lot` and the field name `condo_base_lot_resolution` are duplicated as independent string literals in Python (condo_base_lot.py:76; zoning_crosscheck.py:491) and TypeScript (PropertyOverview.tsx:56,78), with no shared contract fixture or cross-language test binding them (comments at PropertyOverview.tsx:66 and zoning_crosscheck.py:518-520 claim they "can never drift"). They **can** drift. Mitigation that makes this advisory-only: every drift direction (renamed backend token, new outcome) fails **safe** — an unrecognised token is `outcome !== success` → withhold, never fail-open. Recommend a cross-boundary fixture test (e.g. assert the display's allow-list token against the backend `OUTCOME_RESOLVED_SINGLE` value via a shared fixture) in the DB-031 packet, and softening the "can never drift" wording to "drift fails safe."

**A3 (advisory) — records section uses `<strong>`, not headings.** `CondoResolutionRecords` titles ("Condo billing lot — recorded base lot(s)" PropertyOverview.tsx:143; "Multiple recorded base lots — no computed allowance" :147) are `<strong>` inside the group, so they do not appear in the screen-reader heading outline that the sibling DevelopmentLimits (`<h2>/<h3>`) populates. The `role="group"` + `aria-label` keeps the section landmark-navigable, so this is minor. Consider `<h3>` for consistency with the surrounding cards. (Consistent with the A3-class note I raised on M5-T047's aria-label-on-strong; the name-on-strong prohibition does not apply here since the group name is on the section, not the strong.)

**A4 (advisory) — in the multi-lot records view the billing lot's "Residential FAR · city record" remains visible.** Because PropertyOverview nulls only scenario/evaluation (not the profile), DevelopmentLimits still renders `development-reference-far` (the billing lot's PLUTO `residfar`) even while everything else reads "Not calculated" (DevelopmentLimits.tsx:117). It is honestly labelled "Residential FAR · city record" / "PLUTO reference" — a recorded field, not a computed allowance — so it is consistent with the records-vs-allowances distinction, and this state is not production-reachable today (A1). Worth noting only because a reader could momentarily see a FAR-shaped number adjacent to "no computed allowance." No change required.

## Gate-relevant checks

- Amended **AS-8** (entered-vs-analysed identity display, screen + brief, neutral, differing/equal/absent/unrelated-mismatch, records separate from allowances): **met** — AnalysisIdentityNotice on both surfaces, tests 104-176 / 275-317.
- Amended **AS-3** (multi-lot → typed outcome, provider None, no fabrication/collapse, professional-review fail-safe as the visible surface; records-view = DB-031): **met** — live_provider.py:262-267; records-view built+tested, split disclosed.
- **AS-4** (unresolved → provider None, honest display, no reference-number-as-result): **met**.
- Screen/brief single-guard parity: **met** (one shared `condoWithholdsAllowances`).
- Scope discipline: `AnalysisIdentityNotice.tsx` (in allowed_paths) modified +26 lines; its screen wiring `ArchitectEntry.tsx` (NOT in allowed_paths) untouched — correct. No forbidden path touched.

**Verdict: PASS.** The condo records-vs-allowances display is honest and fail-safe on both surfaces; the central promise (no computed allowance on a non-success condo resolution) holds via the display allow-list AND, independently, the backend fail-safe. Advisories A1–A4 are non-blocking; A1 (production reachability / DB-031 follow-up tracking) is the one the orchestrator should carry forward so the built-but-dormant records view is not misread as currently protecting condo users.
