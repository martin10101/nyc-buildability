# M5-T091 - G2 producer self-check record (recorded by the orchestrator, role self_check)

Producer: backend-engineer (orchestrator-dispatched subagent) in wt-m5t091; one in-worktree commit d480fdb0 (parent 114e5e56), cherry-picked to d0919de6 with every file MATCH by git blob identity.

- **Scope:** exactly pdf_sheet_writer.py, its test file and the producer report; dxf_writer.py / glb_writer.py / app/documents untouched; no dependency change.
- **Producer self-checks (report, OBSERVED):** ruff clean; 180 passed (19 original + the new AS-1..AS-4 specs); modularity exit 0; golden unchanged; 13 in-memory mutants (type/sequence/arity/container guards, finite guard, screen disabled/raw-only/no-normalisation/address-only, paren and backslash escaper weakenings) all RED.
- **[ORCH-HARVEST] at the cherry-picked head (Python 3.11.9, cwd services/api): `python -m ruff check .` -> All checks passed!; `python -m pytest tests/cad -q` -> 262 passed (dxf 24 + pdf 180 + glb 58); repo root `python tools/modularity_check.py --check` -> exit 0 (selected 486; failures 0; warnings 23, none on app/cad).**
- **Deviations disclosed:** numeric strings, bool and Decimal coordinates now refused (float() used to coerce them); generators/sets/dicts/arrays refused as rings; non-string caller text refused; five new refusal codes; the text screen runs before ring checks.
- **Discoveries routed (producer):** D1 shared claim-word module; D2 caller text has no length limit (close before wiring); D3 substring matching refuses innocent words like PARALEGAL; D4 letter-spaced / look-alike evasion; D5 DXF writer still coerces numeric strings; D6 a stray inspect.py in the shared scratchpad shadowed the stdlib.

G2 verdict: PASS (self_check; G3 code-reviewer, G4 qa-engineer, G5 security-reviewer and the DCV follow).
