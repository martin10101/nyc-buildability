# G4 QA Gate Report — M0-T116 (second golden re-certification, rows R273/R275/R276)

**Verdict: PASS** (one non-blocking bookkeeping observation)
**Reviewer:** t116-g4-qa (independent, read-only)
**Frozen review identity:** `869b313c583cc98107ce6dda96cf2e6973c9babd` — verified as HEAD, working tree clean.
**Environment:** local sandbox, Python 3.11.9, pytest 8.4.2. Golden/supervisor packs collect and pass cleanly on 3.11 (no PEP 695 dependency in this tree).

Every producer claim in `project-control/reports/M0-T116-recertification.md §3` was reproduced independently. No failures observed in any pack I ran. The unit made zero code changes; the certification is a re-run at the frozen post-repair identity.

## Command table — exact observed results

| # | Check | Command (abridged) | Report claim | Observed | Match |
|---|---|---|---|---|---|
| 0 | HEAD + clean tree | `git rev-parse HEAD` / `git status --porcelain` | 869b313, clean | `869b313…`, empty status | ✓ |
| 1 | FULL golden pack | `pytest tools/test_agent_supervisor_golden_run.py -q` | 41 passed | **41 passed** (15.74s) | ✓ |
| 2 | Golden pack collection | `… --collect-only -q` | 41 | 41 collected | ✓ |
| 3 | All 10 affected packs (incl. turnover_live_seam, telegram_sink) | `pytest <10 files> -q` | 705 passed | **705 passed** (62.73s) | ✓ |
| 4 | Whole-suite collection (`-k agent_supervisor`) | `pytest tools/ -k agent_supervisor --collect-only -q` | 2712 | 2712/3272 (560 non-supervisor deselected) | ✓ |
| 5 | Whole-suite collection (explicit glob cross-check) | `pytest tools/test_agent_supervisor_*.py --collect-only -q` | 2712 | 2712 collected | ✓ |
| 6 | Whole-suite chunk 4/4 (sorted files 46–59) | `pytest <files 46-59> -q` | 616 passed | **616 passed** (26.32s) | ✓ |
| 7 | Whole-suite chunk 3/4 (files 31–45) — extra | `pytest <files 31-45> -q -rs` | 689 passed, 2 skipped | **689 passed, 2 skipped** (51.99s) | ✓ |
| 8 | Code drift across unit span | `git diff --name-only 87091a5..869b313` | project-control/** only | only `project-control/**`; **0** files under `tools/` | ✓ |
| 9 | Supervisor tree object @ HEAD | `git rev-parse 869b313:tools/agent_supervisor` | 7487901c | `7487901cea729f…` | ✓ |
| 10 | Golden pack blob @ HEAD | `git rev-parse 869b313:tools/test_agent_supervisor_golden_run.py` | cf03caaa | `cf03caaa261da9…` | ✓ |
| 11 | Last supervisor-code commit | `git log -1 -- tools/agent_supervisor/` | f89aa29 (M0-T114) | `f89aa29…` M0-T114 | ✓ |
| 12 | Delta T115 span added `def test_` | `git show 91664bb / d89d740` | +14 | 8 + 6 = **14** | ✓ |
| 13 | Delta T114 added `def test_` | `git show f89aa29 / a22e34a` | +2 | 2 + 0 = **2** | ✓ |
| 14 | Test removals across span | `git diff 87091a5..869b313 -- tools/**` | none | **0 removed** (0 added — no tools/ change by this unit) | ✓ |
| 15 | Golden pack additive-only since T112 cert | blob diff `d2946392` → `cf03caaa` | +1 register test only | **+1** `test_the_source_record_key_is_sanitized_not_raw`, **0 removed** | ✓ |
| 16 | CI at pushed tip | `gh api …/commits/07233f5/check-runs` | 20/20 green | **20 total, all success/completed** | ✓ |
| 17 | CI tip vs frozen head identity | tree/blob @ 07233f5 vs 869b313 | same code identity | tree `7487901c` and golden blob `cf03caaa` identical; 07233f5 is ancestor of 869b313; only control-plane commits between | ✓ |

Whole-suite decomposition reconciles: report chunks 680 + 725 + 689 + 616 = 2,710 passed + 2 skipped = 2,712 collected. I independently confirmed collection = 2712, chunk 3 (689/2), and chunk 4 (616); therefore chunk1+chunk2 = 2712 − 691 − 616 = 1,405 = 680 + 725 (report). The affected-pack (705) and golden (41) runs draw from these chunks with zero failures.

## Findings

**F1 — Baseline delta arithmetic verified independently (PASS).** Baseline 2,696 + 14 (M0-T115) + 2 (M0-T114) = 2,712. My own count of added `def test_` lines: M0-T115 span `91664bb`(8) + `d89d740`(6) = 14; M0-T114 `f89aa29`(2) + pragma `a22e34a`(0) = 2. Total +16, 0 removals ⇒ 2,712 − 16 = 2,696 baseline. Consistent, and corroborated by the T115 G3 delta reviewer's 2,708+2=2,710 at the pre-T114 identity (this chain minus T114's 2).

**F2 — Golden pack is re-run-only, additive since T112 cert (PASS).** The golden pack test file (`tools/test_agent_supervisor_golden_run.py`, the file that actually carries the report's "golden pack blob" — not `tools/agent_supervisor/golden_run.py`) changed since the T112-certified blob `d2946392` by exactly one additive test (the M0-T114 register-key redaction test) and 0 removals. The 40 certified scenarios are byte-untouched. Confirms report §2 "Not edited by this unit — re-run only."

**F3 — Zero code drift by this unit (PASS).** `87091a5..869b313` touches only `project-control/**`; nothing under `tools/`. The certification identity is fixed; supervisor tree object `7487901c` and golden blob `cf03caaa` are stable across the CI tip and the frozen head.

**F4 — CI applies to the frozen identity (PASS).** All 20 checks are `success`/`completed` at `07233f5`, including `supervisor-bridge (pytest tools/test_agent_supervisor_*.py)`. `07233f5` is an ancestor of the frozen head with an identical supervisor tree and golden blob; the only intervening commits (`f102d2d`, `869b313`) are control-plane records. So CI's whole-suite run exercised the exact code present at the frozen head.

**F5 — The 2 whole-suite skips are genuine platform guards, not masked failures (PASS).** With `-rs` on chunk 3/4: `test_agent_supervisor_policy.py:449` skips because symlink creation needs a privilege the Windows client lacks (WinError 1314); `test_agent_supervisor_process.py:448` is a "POSIX-only guard". Both are environment-conditional skips; neither hides a failure. On CI (Linux) these run rather than skip, and CI is green.

**F6 (non-blocking observation) — superseded intermediate test count in a commit subject.** The `91664bb` commit subject says "9 new tests", but the actual added `def test_` count in that commit is 8. This is fully reconciled: the `d89d740` correction round explicitly re-derived the span total to "14 new tests: 6 defect + 7 guards + 1 hardening", which exactly matches my independent tally (8 + 6 = 14). The "9" is a superseded self-count in an intermediate commit message, not a discrepancy in the certified figure. No action required; noted for transparency.

## R275 coverage-completeness judgment (step 7)

The whole 59-file supervisor suite (2,712 collected) is a strict superset of the 10 affected packs (705) and the golden pack (41). Sorted files 1–15 / 16–30 / 31–45 / 46–59 = all 59 files with no gap, so nothing supervisor-side is missed by construction. The validator pack and other non-supervisor tests are correctly out of scope for a *golden supervisor* re-cert (M0-T114/T115 only moved `tools/agent_supervisor/**`); they are covered by CI's `control-plane`, `contracts*`, `api`, and `Scan repository for credentials` checks, all green at `07233f5`. I found nothing the certification should have run but did not.

## Conclusion

All 41 golden tests, all 705 affected-pack tests, the 2,712-test collection, and the sampled whole-suite chunks (616 + 689/2) reproduce exactly at the frozen identity `869b313`. Code drift is zero, the golden pack is additive-only since the T112 cert, the +14/+2 delta is independently confirmed, and CI is 20/20 at a tip sharing the frozen identity's code. The single observation (F6) is a reconciled commit-message count with no effect on the certified totals.

**G4 verdict: PASS at frozen SHA `869b313c583cc98107ce6dda96cf2e6973c9babd`.**
