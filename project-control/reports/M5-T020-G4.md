# M5-T020 — G4 QA Gate Report (verbatim)

Orchestrator preservation note: report returned by the independent qa-engineer agent
("G4 QA review M5-T020") on 2026-09-12 and preserved verbatim below (transport
entity-decoding only). Reviewer was read-only, worktree-isolated at the frozen HEAD.
Verdict: PASS WITH REQUIRED CORRECTIONS — recorded as PASS per gate-verdict semantics
with BLOCKING-1 (LOT05 holes-preservation assertions) and BLOCKING-2 (generated-TS
fidelity guard + CI-evidence wording correction) as blocking conditions; both applied
as the one bounded test-only commit a3d59e03 (+ evidence correction 7a4897b7) and
discharged via the same-reviewer delta attestation appended below the report.

---

I have everything needed. Let me do one final confirmation of the flag-reuse import (config.py untouched, helper reused) via the diff I already ran and the passing tests, then compose the report.

All evidence is gathered. Here is my independent G4 QA gate report.

---

# G4 QA Gate Report — M5-T020 (D-040-R001 server half)

- **Gate ID:** G4 (QA / test-adequacy)
- **Task ID:** M5-T020 — MapPLUTO lot-outline 4326 transport + flag-gated route + closed contract
- **Reviewer:** qa-engineer (independent; read-only)
- **Producer:** backend-engineer (producer commit `683b6095`)
- **Result:** **PASS WITH REQUIRED CORRECTIONS** — record as PASS; the two required corrections below are BLOCKING for acceptance and for the next gate (repo gate-verdict semantics, `.claude/rules/project-control.md`). No reproducible runtime defect was found; the feature works correctly.
- **Clean environment/worktree used:** Yes. Isolated worktree `agent-a94e39aabf1f3e2ec`, detached-checked-out to the frozen submitted HEAD `adf9b9dc`; clean tree; Python 3.11.9.

## Frozen identity

- Producer material `683b6095` (base `a5e5e408`); submitted HEAD `adf9b9dc`.
- `git diff --name-only 683b6095..adf9b9dc` = only `project-control/{reports,state.json,tasks/M5-T020.json}`. **None of the 12 deliverable allowed_paths changed after the producer commit** — the reviewed bytes are identical to `683b6095`. Frozen identity CONFIRMED.
- `git diff --name-only a5e5e408..adf9b9dc` (excluding project-control) = exactly the 12 allowed_paths (2 modules + additive `main.py` + 2 schema copies + generated TS + 7 contract fixtures + 8 raw fixtures + MANIFEST + 2 test packs + registry draft). **No pre-existing test file was modified** → S6 "no weakened pre-existing assertion" holds by construction.
- Forbidden byte-immutability: `git diff --stat a5e5e408..adf9b9dc -- mappluto_geometry_arcgis.py config.py` = **empty**. Authoritative 2263 connector and `config.py` byte-unchanged (S4 satisfied; flag reused, no new flag).

## CI evidence (internal consistency)

- Run `34698587781` at head `6f091098` (a commit inside `683b6095..adf9b9dc` that touches only project-control → CI ran the identical deliverable bytes). All jobs SUCCESS except `web-dependency-security` (owner-gated D-040-R004 Next.js item; this task touches no package file — forbidden_paths bars `package.json`/`apps/web/**`). Consistent.
- I independently reproduced the local numbers: **connectors 486 passed / api 417 passed / new packs 67 passed / modularity EXIT 0** — matching the producer and CI evidence exactly.

## Steps independently executed (real output)

```
python -m pytest services/api/tests/connectors -q   → 486 passed in 4.15s
python -m pytest services/api/tests/api -q           → 417 passed in 19.23s
python -m pytest .../test_mappluto_lot_outline.py .../test_lot_geometry_api.py -v → 67 passed in 4.02s
python tools/modularity_check.py --check             → selected 397 files; failures 0; warnings 16; exit=0
  (16 warnings all pre-existing; neither new module appears)
sha256 both schema copies                            → 7893be81…  ==  7893be81…  (byte-identical)
sha256 LOT01/LOT06/LOT96/LOT80 vs MANIFEST           → 9e5e1261 / 28a79e78 / 205ef0ae / ab72a8fe  ALL MATCH
7 contract fixtures validated vs lot_geometry schema → all VALID
LOT05 ring structure = Polygon, 3 rings [321,107,38]; LOT06 = MultiPolygon, 2 parts, 1 ring each (NO holes)
all raw fixtures carry crs.properties.name == "EPSG:4326"; LOT96 = 2 features OBJECTID 8814/8815 same BBL
```

## Directive/requirement verification (in-regime: D-040 → D-040-R001)

| Requirement | Reviewed identity | Verdict | Reproduced evidence |
|---|---|---|---|
| **D-040-R001** display-only 4326 transport (f=geojson&outSR=4326 via immutable connector's injection-proof discipline; 4 honest outcomes incl. honest condo-unit no-outline; display_only + ±20ft note + DCP attribution on every outcome; no measurement from 4326 coords) — SERVER half | `683b6095`/`adf9b9dc` | **PASS** | `mappluto_lot_outline.py` imports (never edits) `normalize_bbl`, constants, `_condo_classification`; `build_outline_query_url` int-interpolates canonical BBL + appends `&f=geojson&outSR=4326`; `geometry=geom` verbatim, no shapely/area; `test_every_outcome_carries_display_markers` (all 8), grep test, empty forbidden diff. Web-rendering + MapLibre admission correctly deferred to follow-on packets per packet + §2.1. |

D-040-R001's server portion is satisfied; the web-rendering portion is a separate, correctly-scoped follow-on packet (the directive requirement is not fully *closed* until that packet lands — a program-tracking matter, not an M5-T020 defect; the formal requirement-closure verdict is the directive-compliance-verifier's `verification.json`, producer ≠ verifier).

## S1–S6 coverage table

| Scenario | Verdict | Basis / gaps |
|---|---|---|
| **S1** single_lot 200 | PASS | Assertions are fixture-derived (`test_single_lot_coordinates_are_plausible_nyc_lnglat` iterates real positions; `test_geometry_is_verbatim…` compares to raw fixture; `dataset_version=="26v2"`, block 835, lot 41 from fixture). Provenance/display_only/accuracy/attribution asserted on **every** outcome (connector `test_every_outcome_carries_display_markers`, parametrized ×8) — not just single_lot. |
| **S2** typed outcomes honest | PASS | All four outcomes each from its own fixture (single_lot ×4 incl. Polygon/MultiPolygon/condo-billing; no_outline via condo-unit LOT04 + no-feature LOT02; multiple_features LOT96; invalid_geometry LOT80). Condo-unit honest-empty asserted as a **200** (`test_s2` LOT04) not an error. multiple_features never-first-pick pinned (geometry `None` + review_required True). |
| **S3** route posture | PASS | Flag-off → generic `404 {"detail":"Not Found"}`, **no** X-Correlation-ID leak, `install_landmine` proves no fetch (`test_s3_flag_off…`, `…_unknown_token…`). Malformed bbl → 422 with correlation, pre-fetch (landmine). Injection resistance **actually asserted**: `test_url_builder_refuses_non_canonical_bbl` [`OR 1=1`, `&f=json`, `'; DROP`, newline, short] + int-interpolation guard prove raw input cannot reach the URL. GET-only (405 on POST); `include_in_schema=False` (`test_s3_not_in_openapi`). |
| **S4** authoritative 2263 untouched | PASS | Empty forbidden diff (independently reproduced). No measurement: `test_module_computes_no_area_and_does_not_import_shapely` greps for `import shapely`/`.area`/`compute_area`/`area_sq_ft` (load-bearing static assertion — see A3 for its inherent limit). Existing connector suite green (486). |
| **S5** recorded-official fixtures | PASS | MANIFEST complete (URL/timestamp/sha256/BBL per fixture + re-runnable procedure). Two fixture sha256s recomputed and matched (four checked). Synthetic negatives marked `classification:"synthetic"` + `derived_from` and enforced by `test_synthetic_fixtures_declare_derivation_and_raw_do_not`. Fully offline via injected seam. |
| **S6** contract + regression + parity | PASS (with corrections) | Both schema copies byte-identical (independently verified); `test_s6_bundled_schema_is_byte_identical_to_canonical` reads raw bytes → would fail on a 1-byte divergence (load-bearing). Renderer-parity per outcome at API (`_assert_renderer_parity_safe` both `json.dumps` forms in `test_s2` ×7 + `test_s1`). All existing suites unchanged; modularity EXIT 0. **BUT** generated TS "drift jobs green" is hollow — see BLOCKING-2. |

## Named-mutant table

| # | Mutant | Killed? | Killing test / survivor |
|---|---|---|---|
| 1 | first-pick on multiple_features | KILLED | `test_multiple_features_is_review_never_first_pick` + api `test_s2` (geometry None, review_required True) |
| 2 | drop display_only on one outcome | KILLED | `test_every_outcome_carries_display_markers` (×8) + schema `display_only` const `true` on every 200 |
| 3 | swap condo unit/billing classification | KILLED | `test_condo_billing…` (condo_billing_lot, condo_no 1025) + `test_condo_unit…` (condo_unit_lot_query) |
| 4 | accept lat/lng order swap | KILLED | `test_single_lot_coordinates_are_plausible_nyc_lnglat` + api S1 (lng∈[-74.3,-73.6], lat∈[40.4,41.0]) |
| 5 | route leaks internals on flag-off | KILLED | `test_s3_flag_off_is_generic_404_no_leak` (exact body, no X-Correlation-ID) |
| 6 | schema copies allowed to diverge | KILLED | `test_s6_bundled_schema_is_byte_identical_to_canonical` (raw-byte compare) |
| 7 | present wrong CRS accepted | KILLED | `test_wrong_crs_is_rejected_before_transport` + api `test_wrong_crs_maps_to_502` |
| 8 | silently trust returned feature w/ wrong BBL | KILLED | `test_result_mismatch_when_returned_bbl_differs` |
| 9 | condo-unit empty → 500/error | KILLED | `test_condo_unit_lot_is_honest_no_outline_not_error` + api LOT04 → 200 |
| 10 | invalid geometry → 500 | KILLED | `test_invalid_geometry_is_typed_outcome_not_500` + api LOT80 → 200 |
| 11 | raw BBL injected into query URL | KILLED | `test_url_builder_refuses_non_canonical_bbl` + int-interpolation |
| 12 | drop a multipolygon part | KILLED | `test_geometry_is_verbatim_from_the_recorded_fixture` (LOT06, 2 parts, deep equality) |
| **13** | **drop interior rings (holes) from a Polygon** | **SURVIVES** | LOT05's only assertion is `type=="Polygon"` + truthy coords (stays schema-valid, `minItems 1`); the one verbatim test uses LOT06, which has **no** interior rings, so it cannot detect the drop. **Severity: moderate (BLOCKING-1).** |
| 14 | non-finite (NaN/Inf) / >2-ordinate coordinate accepted | SURVIVES (edge) | `_finite_position` (`math.isfinite`, `len==2`) is a deliberate defensive branch with no fixture; Python `json.loads` accepts `NaN`, so this path is reachable but untested. Severity: low (A2 advisory). |

## Untested-invariant sweep

- **Holes preserved in rendering-ready geometry** — the entire documented purpose of LOT05 ("proves holes transport verbatim"; research flags Governors Island) — has **no assertion that would fail if interior rings were dropped**. LOT05 is checked only for type+truthy coords and is **absent from the API pack** entirely. The verbatim-equality test that would catch it runs on a hole-free MultiPolygon (LOT06). This is a deliberately-designed, product-central behavior with effectively zero coverage → **REQUIRED** correction per the M5-T017 precedent (BLOCKING-1). Mitigant: the transport is a single type-agnostic `geometry=geom` assignment, so no live defect exists — this is a regression-protection gap, not a bug.
- **Multipolygon part count** — covered (LOT06 verbatim equality asserts 2 parts).
- **Generated-TS fidelity/drift** — zero automated coverage; see BLOCKING-2.
- **Non-finite coordinate → invalid_geometry** — designed branch, untested (A2).

## Findings

**BLOCKING-1 (required, in-scope) — holes-preservation has no regression coverage.** Add a direct interior-ring assertion for LOT05 within `services/api/tests/connectors/test_mappluto_lot_outline.py` (allowed path): include LOT05 in `test_geometry_is_verbatim_from_the_recorded_fixture` and/or assert the transported geometry has 3 rings / interior rings equal to the fixture. Kills mutant #13. (Optionally add LOT05 to the API `OUTCOME_200` pack.)

**BLOCKING-2 (required) — generated `lot_geometry.ts` has zero automated verification, and the CI-evidence claim of "in-suite" enforcement is inaccurate.** Verified: `generate_ts_types.py` hardcodes a schema list excluding `lot_geometry` (grep: no `lot_geometry` in `packages/contracts/scripts/`); there is no `test_generate_lot_geometry_ts.py`; and no pytest reads the `.ts`. So `contracts-typegen SUCCESS` and S6's "drift jobs green" are hollow for the new artifact, and the CI-evidence line "the new file's generation fidelity is enforced in-suite" is **false**. The generator script is outside allowed_paths (full drift-check wiring is a legitimate follow-up), **but** a minimal key-coverage/structural assertion for the `.ts` was achievable inside the allowed test files and was not added. Required: either (a) add such an in-suite assertion now, or (b) correct the CI-evidence note **and** create+bind a follow-up contract-scoped task (wire `generate_lot_geometry()/check_lot_geometry()` + CI drift + generator test) that MUST land before the web packet consumes `lot_geometry.ts`. Note: the TS artifact itself is currently **correct** (I read it against the schema — all fields/enums/`display_only:true`/`crs:"EPSG:4326"`/optional `_expected_failure?` match).

**Advisories (non-blocking):**
- **A1 — schema lacks the outcome↔geometry cross-field binding** ("geometry non-null ONLY on single_lot"). Enforced by code + per-outcome tests but not by the schema (add an `if/then`/`allOf` for defense-in-depth).
- **A2 — non-finite / >2-ordinate coordinate rejection** (`_finite_position`) is a designed branch with no fixture; add a synthetic NaN-coordinate fixture asserting `invalid_geometry`.
- **A3 — "no measurement" is enforced by token-absence grep** (`import shapely`/`.area`/`compute_area`/`area_sq_ft`) + `display_only` marker + review; a hand-rolled area computation (e.g. shoelace) would not be caught. Acceptable, but the guard is lexical, not behavioral.
- **A4 — TS emits `LngLatPosition = number[]`**, losing the schema's exactly-2 (`minItems/maxItems`) constraint; runtime schema still enforces it (standard generator limitation).
- **A5 — several `STATUS_STATE_MATRIX` rows are declared but unexercised** at the API level (`502 result_mismatch`, `500 internal_contract_error`); `test_s6_emitted_pairs_are_in_the_matrix` asserts `seen ⊆ MATRIX`, not `==`. Low risk (simple dict lookups; connector raises are tested).
- **A6 — producer report line counts (~415/~210) understate actual (596/244)** by ~40%. Evidence-accuracy only; modularity passed and neither module is flagged.

## Reviewer conclusion

The server half is well-engineered and provenance-disciplined: forbidden 2263 connector and `config.py` byte-unchanged, injection-proof URL builder, honest typed outcomes with no silent first-pick and no fabricated shapes, display-only boundary held (no measurement, no shapely), live-captured fixtures with hash-verified integrity, byte-identical closed schema copies, and full green suites (486 / 417 / 67, modularity EXIT 0) reproduced independently at the frozen SHA. Two **required corrections** remain before acceptance: (1) a direct holes-preservation assertion for LOT05 (a surviving mutant on a deliberately-fixtured, product-central behavior — M5-T017 precedent), and (2) closing/correcting the generated-TS fidelity gap and its inaccurate CI-evidence claim. Neither is a reproducible runtime defect.

**VERDICT: PASS WITH REQUIRED CORRECTIONS** (orchestrator records PASS; BLOCKING-1 and BLOCKING-2 must be applied, validated, and committed before acceptance and before dispatching the next gate).

---

**Relevant absolute paths** (all at frozen SHA `adf9b9dc`, worktree `C:\Users\MLFLL\Downloads\nyc-zoning\nyc-development-feasibility-claude-pack\.claude\worktrees\agent-a94e39aabf1f3e2ec`):
- `services\api\app\connectors\mappluto_lot_outline.py`
- `services\api\app\api\v1\lot_geometry.py`
- `services\api\tests\connectors\test_mappluto_lot_outline.py`
- `services\api\tests\api\test_lot_geometry_api.py`
- `services\api\tests\fixtures\mappluto_lot_outline\MANIFEST.json` (+ LOT01–LOT06, LOT80, LOT96)
- `packages\contracts\schemas\v1\lot_geometry.schema.json` / `services\api\app\_contract_schemas\v1\lot_geometry.schema.json`
- `packages\contracts\generated\lot_geometry.ts`
- `docs\research\source-registry-drafts\pluto-mappluto.json`

Note for the orchestrator: I could not write my agent-memory note — the guard reported the `.claude/agent-memory/qa-engineer/` path as a shared-checkout path and refused the write. No blocker to the gate; recording it here instead.

---

# APPENDED DELTA ATTESTATION (verbatim, same reviewer agent, after correction commits a3d59e03 + 7a4897b7)

Verified. Here is the delta attestation to append verbatim to the G4 gate report.

---

## G4 delta attestation — M5-T020 corrections (appended)

**Reviewed:** `a3d59e03` (test-only, one commit after `adf9b9dc`) + `7a4897b7` (CI-evidence wording). Detached-checkout of `a3d59e03`, clean tree, Python 3.11.9.

**(1) Scope.** `git show a3d59e03 --stat`: 2 files, both in allowed_paths (`tests/connectors/test_mappluto_lot_outline.py`, `tests/api/test_lot_geometry_api.py`), +69/−3, **test-only and additive** — the verbatim test was *strengthened* (parametrized over LOT05+LOT06), not weakened. `7a4897b7`: 1 file (`M5-T020-ci-evidence.txt`), now accurately states the guard was ADDED by the correction and full generator wiring remains a bound follow-up.

**(2) Mutant #13 (drop interior rings) — KILLED (empirical).** Real LOT05 = 3 rings; simulated exterior-only mutant = 1 ring. New `test_holes_interior_rings_transport_verbatim` assertions on the mutant fail: `['ring-count 1!=3','ring[1]-mismatch','ring[2]-mismatch']`; on the real verbatim geometry: `[]`. The parametrized LOT05 deep-equality is a second kill vector.

**(3) TS guard — load-bearing (empirical).** `test_s6_generated_ts_covers_every_schema_key_enum_and_const` on the real TS → `missing=[]`; with enum `"multiple_features"` removed from the TS text → `missing=["enum 'multiple_features'"]` (test fails). A schema change unaccompanied by TS regeneration is caught.

**(4) Packs:** `70 passed` (67 + holes test + TS-guard test + the added LOT05 param case), matching the expected count.

**Disposition:** BLOCKING-1 **DISCHARGED**; BLOCKING-2 **DISCHARGED**. The delta is test/report-only and touches none of A1–A6, which **remain advisory-only**. **VERDICT: G4 PASS — now stands unconditionally** (no remaining required corrections).
