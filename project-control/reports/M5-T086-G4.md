# M5-T083 + M5-T086 — G4 independent test-adequacy review (qa-engineer "qa-drw", read-only; joint review)

> Transmission history: one joint review pinned at 1414e1a7 (HEAD advanced to 71ab388a mid-review; the
> reviewer proved all four reviewed blobs byte-identical), delivered as two SendMessage parts ending with
> END-OF-REPORT; no truncation. Joined verbatim by the orchestrator; saved as M5-T083-G4.md and
> M5-T086-G4.md (each task's gate cites it).

---

G4 test-adequacy review — M5-T083 PASS, M5-T086 PASS (both with ADVISORY coverage adds; no BLOCKING). Part 1/2. Pinned HEAD 1414e1a7; verified read-only, git status --short services/ clean (in-memory mutation only, no repo/ledger/memory writes).

IDENTITY NOTE: HEAD advanced 1414e1a7 -> 71ab388a mid-review (orchestrator integrating disjoint wave units). All 4 review files are BYTE-IDENTICAL at both heads (blob SHAs match: sheet_reader 3353cae2, dxf_reader 6dbc33b7, test_sheet_reader 09894fef, test_dxf_reader 2503baae). Review holds at the pinned target.

METHOD: identity-preserving in-process mutation (inspect.getsource -> exec into the module's OWN namespace so real classes/isinstance stay valid; per the mutate-the-consuming-namespace rule). Baselines green: dxf 28 passed (3.11 native); sheet 23 passed via the producer's bare-package shim (app.documents.extraction pre-registered so pdf_lexer/objects/xref load without units.py PEP695). CI authority: `api (ruff + pytest)` job = SUCCESS on HEAD 71ab388a — under 3.12 both suites (23+28) collect and pass, ruff clean.

=== M5-T086 (DXF reader) mutation table ===
N1 drop entity-count bound (_handle_entity `count>max`->False) [NAMED] -> KILLED by test_mutation_entity_count_bound_reddens.
N2 drop pair-parity (_to_pairs `len%2!=0`->False) [NAMED] -> KILLED by test_as4_odd_pair_count_refused AND test_mutation_pair_parity_check_reddens (2 teeth; induced IndexError caught by the defensive guard).
M6 units default->feet (_resolve_units) [idiomatic] -> KILLED by test_as2_no_units_header_is_unitless_never_feet ("never assume feet" has teeth).
M3 finiteness half-guard (`not isfinite`->`isinf`) [idiomatic] -> *SURVIVED*. Under it a "nan" coordinate reaches a primitive (LINE.start=(nan,0,0)).
M4a LWPOLYLINE bulge `!=0`->`>0` [idiomatic] -> *SURVIVED*. A NEGATIVE bulge (equally an arc) is silently straightened.
M4b POLYLINE/VERTEX bulge `!=0`->`>0` [idiomatic] -> *SURVIVED*. The VERTEX-bulge refusal path has NO test at all.
M5 closed flag `&1`->`bool(flags)` [idiomatic] -> *SURVIVED*. flags=128 (not closed) misreported closed=True; only flags 0/1 tested.

FUZZ: read_dxf over 6696 inputs (every prefix truncation str+bytes, line drops, junk insertions, 4000 random group-code streams, 2000 random byte blobs, pathological repeats, unterminated structures) -> 0 escapes / 0 bad returns. Re-fuzzed with the broad `except Exception` guard neutralized: 11000 more inputs, 0 escapes — the structured _Refuse path is comprehensive; the broad guard is belt-and-suspenders (proven load-bearing only on the N2 induced-IndexError path).

T086 ADVISORY findings (production code is CORRECT & unwired; required named mutations have teeth — these are recommended coverage adds, not defects):
F1 finiteness only half-pinned: test_as4_non_finite_coordinate_refused probes only `inf`. Add a `nan` coordinate probe.
F2 bulge refusal pinned only for POSITIVE bulge, and no VERTEX-bulge test exists. Add a negative-bulge LWPOLYLINE probe + a VERTEX(POLYLINE)-bulge probe.
F3 closed-flag mask pinned only for 0/1. Add flags=128 (expect closed=False) and flags=129 (expect closed=True) probes.

(continued in part 2/2)

---

G4 review part 2/2 — M5-T083 table, findings, isolation, verdicts.

=== M5-T083 (sheet reader) mutation table ===
N1 drop tolerance subdivision (sheet_reader._flatten_cubic no-subdiv) [NAMED] -> KILLED (AS-1 primary deviation>tol).
N2 drop CTM multiply (sheet_reader._concat_matrix = lambda m,ctm: m) [NAMED] -> KILLED by test_as2_rotation_and_translation_compose_exactly.
DISCIPLINE CHECK: rebinding the DEFINING module's leaf concat_matrix (sheet_primitives.concat_matrix) -> SURVIVED (false survivor) while the CONSUMING-ns rebind kills — independently confirms the producer's mutate-the-consuming-namespace choice is correct (not false-negative-prone). (Note: flatten_cubic is self-recursive via its module global, so its defining-ns rebind also reaches the recursion — expected.)
I2 depth bound loose off-by-one (`self._depth+1 > MAX` -> `self._depth > MAX`) [idiomatic] -> KILLED. Evidence: the same FmA->FmB->line 2-level chain is ACCEPTED at MAX=2, so the loose mutant at MAX=1 behaves like MAX=2 and reddens test_as3_form_recursion_depth_bound_is_refused (which asserts a refusal at MAX=1).
I1 cycle check immediate-parent-only [idiomatic] -> SURVIVED*. Self-reference (A->A) is caught by BOTH the correct accumulated-ancestor set AND an immediate-parent weakening (a self-ref's parent is itself), so the only cycle test cannot distinguish them. A multi-hop A->B->A cycle is untested; real code correctly refuses it as "xobject cycle" (I verified), and fail-closed still holds even under the weakening via the depth bound. So limited impact -> ADVISORY.
I3 cubic_flat `and`->`or` (flat if EITHER control near the chord) [idiomatic] -> *SURVIVED* AS-1. This is the highest-value gap:

F4 (ADVISORY, highest value): AS-1's declared-tolerance guarantee is validated ONLY on a SYMMETRIC quarter-circle, which cannot distinguish `and` from `or`. Proof — an asymmetric cubic p0(0,0) p1(1,0)-on-chord p2(50,40)-far p3(100,0): control distances d1=0.000, d2=40.000, declared tol=0.5. Real `and`=False -> subdivides (correct); `or`=True -> emits the straight chord; true curve max deviation from that chord = 17.778 = 36x over the declared 0.5. A plausible simplification to `or` ships a flattener that violates the very tolerance AS-1 records, with no red test. RECOMMEND an asymmetric-Bezier flatness probe (one control on the chord, one far off). Production code is CORRECT and the required no-subdivision mutation has teeth, so PASS — but this probe should be added.
F5 (ADVISORY): add a multi-hop A->B->A cycle test asserting feature=="xobject cycle" (per I1).

FUZZ: read_sheet over 12238 inputs (prefix truncations, 4000 byte-flip/delete/insert mutations of a valid curves+CTM+text+XObject PDF, 2000 random byte blobs, 4000 random content-stream operator streams, truncated streams) -> 0 escapes / 0 bad returns. AS-4 "no exception escapes" holds.

=== AS-5 isolation/scope (read-only, both tasks) ===
- git status --short services/ EMPTY (my in-memory testing left no trace).
- grep app/: nothing imports sheet_reader/dxf_reader/sheet_primitives (not wired).
- git diff --name-only: app/documents tree UNTOUCHED by the wave; requirements.txt/.in UNTOUCHED (zero new deps; modules use stdlib + already-admitted app.documents.extraction submodules only).
- Introducing commits (7acb3359 T083, 719d96f8 T086) changed only allowed_paths files.

Overall: every acceptance scenario is validated by real, non-tautological assertions; all 4 packet-required NAMED mutations reddened; ~18.9k combined fuzz inputs produced zero exception escapes; isolation/deps verified; CI 3.12 api job green at byte-identical content. All surviving mutants are on CORRECT, unwired code -> ADVISORY coverage adds, not gate failures.

M5-T083 G4 VERDICT: PASS (advisories F4, F5)
M5-T086 G4 VERDICT: PASS (advisories F1, F2, F3)

END-OF-REPORT
