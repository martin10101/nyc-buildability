# M5-T088 — G3 independent geometry review (geospatial-engineer "geo-mass2", read-only)

> Transmission history: pinned at 6c79cd88, delivered as two SendMessage parts (1/2, 2/2) ending with
> END-OF-REPORT; no truncation. Joined verbatim by the orchestrator (transport wrapper tags removed only).
> Independent re-derivation incl. byte-level comparison against the accepted module blob 255de6ec.

---

M5-T088 G3 GEOMETRY REVIEW (independent, read-only) — geospatial-engineer. PART 1/2.

PIN: HEAD 6c79cd88 recorded at start (== expected). Material 9f355fe1 (cherry-pick of wt-m5t088 353b919d) touches EXACTLY the 3 allowed paths; module blob 42545728, test blob c55e2cd7 (both == producer report). Method: full source read; documented suite `python -m pytest tests/scenario/test_massing_model.py -q` = 54 passed (cwd services/api); independent in-memory re-derivation loading the accepted blob 255de6ec as a module; shapely + exact-rational probes.

VERIFIED PASS (no blocking findings):

(1) Non-star fixtures genuinely discriminate [independently re-derived]. U (area 4800) and comb (area 4820): star-vertex set = [] — NO vertex sees the whole polygon, so they are truly non-star; even the best-case fan over-covers (U by 2400, comb more). Module ear-clip: U→6, comb→10 triangles = n−2, every triangle CCW and shapely-`covers`ed by the ring, Σtri-area == polygon area exactly. A naive (0,i,i+1) fan gives U 8400 / comb 11610 → over-covers → mutation M1 reddens the concave-specific assertions, not only the golden. The old L-shape is star from vertices [0,3] (a fan from 0 is exact) — confirming why it could not catch M1. massing_model.py:283-344; test_massing_model.py:391-419,617-631.

(2) Every bound fires BEFORE the heavy work [source order + tests]. Builder order massing_model.py:682-713: floor cap (:683 over_cap_floors) → lot magnitude (:684 via _prepare_ring:225-229) → footprint magnitude (:687) → output ceiling (:691 over_cap_output_vertices) → containment (:694-706) → triangulate_distinct (:708) → prisms (:709). The budget has BOTH an up-front least-work refusal (_triangulate_distinct:598, exact lower bound _min_ear_clip_work:283-287) AND per-candidate metering (charge 1 at :319; worst-case m−3 charged BEFORE the scan at :326). The heavy_work spy proves heavy_work==Counter() for the up-front refusals and Counter({"triangulate":1}) for the metered mid-scan case (test :715-818). All pass.

(3) Accepted behaviour unchanged [byte-level, independent]. Loaded accepted blob 255de6ec in-memory; for rect/lshape/ushape/comb/five_floor/cw_lot/collinear/nonint, accepted.to_json() == current.to_json() byte-for-byte. RECT golden b7fa9862… and 5-floor golden e23b5c… reproduce on BOTH the accepted and the current module. Golden not re-anchored; no output regression.

Continues in 2/2.

---

M5-T088 G3 GEOMETRY REVIEW — geospatial-engineer. PART 2/2.

(4) Containment-before-triangulation is honest [source-confirmed]. B0 validate_proposed_massing runs FIRST (massing_model.py:676) and refuses a self-intersecting footprint OR per-level outline as invalid (proposal.py:279 _ring_is_simple, all-pairs) AND a wrong-CRS / out-of-NYC footprint (proposal.py:247-257) before containment or triangulation is reached. So a non-triangulable footprint can never reach the containment check to be mislabeled — every footprint that reaches :694 is already B0-simple, so footprint_outside_lot is a true statement about a valid-but-misplaced ring. Among multiple simultaneous resource violations the surfaced reason follows check order; each typed reason is individually true (honest fail-closed).

(5) Disclosed changes / discoveries accurately characterized. Validator O(n²): proposal._ring_is_simple:143 is all-pairs _segments_intersect:119, run per footprint AND per per-level outline → real CPU sink (Discovery 1, confirmed). Lot ring has NO NYC-2263 range check — B0 range-checks footprint/levels (proposal.py:247-257) but the separate lot_ring argument gets only the 1e8 magnitude bound in _prepare_ring, so a wrong-CRS lot passes then mislabels footprint_outside_lot (Discovery 2, confirmed from source). 663 SLOC modularity WARN (not fail; <750 justify), cohesion justification recorded (Discovery 3). shapely GEOSException (not ValueError) on degenerate geometry (Discovery 4). All correctly routed to the wiring packet, none fixed in-packet; none reachable while unwired.

(6) Scope clean. 9f355fe1 changed only massing_model.py under scenario/; max_envelope.py blob f0abf884 identical parent↔child (byte-untouched); every other scenario module untouched; zero new dependencies (no requirements/lockfile in the diff); no route/main.py/web; the only importer of massing_model is its own test (grep) → unwired.

ADVISORY (all non-blocking):
A1. Resource-bound precedence is order-of-checks, not severity — fine for an unwired module; a wiring/UX packet that surfaces the reason to users should know the precedence.
A2. Close the lot_ring NYC-2263 gap (Discovery 2) before wiring — matches the packet's own "before user geometry" framing.
A3. _check_output_size:577-585 intentionally does NOT content-dedupe (unlike _triangulate_distinct) — this is CORRECT (each floor emits its own prism); noted so it is not "optimized" into an undercount.

M5-T088 G3 VERDICT: PASS
END-OF-REPORT
