<!-- Reviewer return preserved VERBATIM by the orchestrator (report-preservation rule,
.claude/rules/project-control.md; transport entity-decoding applied: &gt; -> >).
Reviewer: independent qa-engineer subagent (G4), returned 2026-09-12, reviewed HEAD e919d9ec. -->

All checks are complete. Every verification item passes. Here is my gate report.

---

VERDICT: PASS

# G4 Quality Gate Report — M4-T011 (family-expansion consumer-test adaptation)

**Reviewer:** qa-engineer (independent; did not author this work)
**Gate:** G4 (coverage/executability review of a test-only adaptation)
**Reviewed HEAD:** e919d9ec (branch candidate/D-024-mrl-option-b)
**Producer material commit:** 40cd4544 (4 test modules + report); ledger/state side-effects in b35feae0, 04947efe, e919d9ec
**Discipline:** read-only; static + read-only git + `python -c` probes with `PYTHONDONTWRITEBYTECODE=1`; no pytest/ruff/project_control.py; nothing written to the repo. CI is the execution authority for the RED/GREEN; I verified statically.

## Scope confirmation
- `git show --name-only 40cd4544` and `git diff --name-only 223c69dc..e919d9ec -- services/api/app apps packages supabase tools` confirm the producer changed **only** the four `services/api/tests/api/*` modules plus `project-control/reports/M4-T011-producer-report.md`. **No production code, no forbidden path** changed anywhere in the range. Matches `allowed_paths` exactly.

## S1 — mapping: exactly the six failing tests adapted, and only those (+helpers) — PASS
- Diff adds **zero** `def test_` lines and removes none (`git diff … | grep 'def test_'` empty). Only functions added are module-local helpers `_applicable_trace` (all four modules) and `_applicable_group` (evidence only). Test-function counts at HEAD: ev=32, re=23, sc=18, sa=41 — collected count unchanged, so `371 = 365 + 6 repaired` holds (RED was 6 failed/365 passed = 371 collected; GREEN 371 passed).
- Six adapted sites correspond one-to-one to the six named CI failures: `test_rule_evaluation_api::test_as3_confident_supported_family_is_200_draft`, `::test_m2t020_s2_flag_on_live_substrate_reaches_evaluation_via_default_seam`; `test_evidence_api::test_as1_…travel_with_it`, `::test_as2_cap_and_citations_are_byte_identical_to_the_trace`; `test_scenario_api::test_as1_confident_r5_cap_surfaces_trace_value_verbatim`; `test_scenario_analysis_api::test_as1_cap_equals_rule_evaluation_trace_value_verbatim`. No other test body changed.

## S2 — value identity (walked hunk by hunk) — PASS
Every expected value present before is present after; the diff changes only SELECTION and the count-assertion form (both explicitly permitted by S1/S2):
- `1.5` preserved: `_applicable_trace(doc)["outputs"]["max_residential_far"] == 1.5` (re, both sites).
- `15000.0` preserved: `doc["draft_zoning_floor_area_cap_sq_ft"] == trace_cap == 15000.0` (sc).
- `TRACE_CAP` preserved: `group["outputs"][…] == TRACE_CAP` and `evidence_cap == trace_cap == TRACE_CAP` (ev); `envelope["scenario_cap_sq_ft"] == trace_cap == TRACE_CAP` (sa).
- The AS-2 group↔trace zip (`group["citations"] == trace["citations"]`, `group["coverage_status"] == trace["coverage_status"]`, `strict=True`, ev:520) is **untouched**; `doc["family_coverage"]["coverage_status"] == cov.COVERAGE_CONDITIONAL`, `cap_label`, disclaimer assertions (ev:755/980/1130/1316) untouched.
- Removed lines carried no standalone caps/coverage/citation value; the only value literals removed reappear verbatim. The `== 1` → `== len(doc["family_coverage"]["rule_ids"])` change is the count-form change S2 permits, not a value change.

## S3 — mutant-killing (selection by applicability, never by position) — PASS
- (a) Production regressing to `evaluations[0]` in scenario/builder: the scenario/analysis tests anchor `trace_cap` to `_applicable_trace(rule_eval)` (the rule-eval endpoint's applicable trace) and chain-assert the builder's cap `== trace_cap == 15000.0/TRACE_CAP`. The RED KeyError (`max_residential_floor_area_sq_ft` absent at `evaluations[0]`) proves the applicable R5 trace is **not** at position 0, so a builder regression to `[0]` yields an absent/different cap → chained equality fails. Mutant killed.
- (b) A second family member becoming applicable for R5: `_applicable_trace`/`_applicable_group` assert `len(applicable) == 1` with the offending sorted `rule_id`s as the message → fires loudly. Mutant killed.
- (c) Family shrink/grow: `len(doc["evaluations"]) == len(doc["family_coverage"]["rule_ids"])`. Verified `family_coverage["rule_ids"]` = `[r.rule_id for r in self._by_family[family]]` (registry.py:213) and evaluations is built per family member (integration.py:600) — the count follows the document's own family list. A divergence between the two is caught; legitimate co-growth is intentionally tolerated (behavior-neutral de-brittling).

## S4 — no hardcoded 5 — PASS
- The only "5" introduced in the diff is the preserved cap literal `1.5`. The count assertion uses `len(doc["family_coverage"]["rule_ids"])`, the authoritative in-document family list. No hardcoded family size.

## S5 — executability statics — PASS
- `ast.parse` succeeds for all four modules under **Python 3.11.9** (no 3.12-only syntax): ev/re/sc/sa all `PARSE_OK`.
- Line lengths ≤ 100 (max: ev=100, re=100, sc=96, sa=100; zero lines >100).
- Helpers are module-local (`_`-prefixed, module scope), pure dict operations. **No imports added/removed**, **no fixtures added/changed**, **no new file I/O** — nothing reads outside committed files.

## Item 6 — the two disclosed untouched positional sites — PASS (disclosure accurate)
- `test_rule_evaluation_api::test_as6…` (re:407, `doc["evaluations"][0]["outputs"] == {}`, function begins ~:393): missing/non-positive lot-area path where **no member computes outputs**, so `outputs == {}` holds for every family member regardless of position/applicability. Not among the RED six; passes under the 5-member family. Correctly left out of the exact-six scope.
- `test_evidence_api::test_as8…` (ev:1132, `len(document["rule_citations"]) == len(rule_eval["evaluations"])`, function begins ~:1105): a count-equality (5==5) that co-grows, not a single-member positional selection; passes under the family. Disclosure ("~:392 / ~:1108") is accurate to function region.

## Notes / non-blocking
- The `re:407` positional `[0]` remains by design (out of the six-failure scope, property is family-invariant, honestly disclosed). Not a finding under this packet's scope; a future family whose members can differ in `outputs` on that path would want the applicable-trace idiom there too — worth a backlog note, not a gate blocker.
- Modularity/ruff/full-suite green are G1/CI concerns (this task's required gates are G0, G1, G4); the change adds no production source and two trivial test helpers, well within test-module norms.

**Conclusion:** All acceptance scenarios S1–S5 and reviewer items 1–6 verified statically at HEAD e919d9ec. The adaptation is a byte-value-identical, selection-only de-brittling that kills the positional-selection, second-applicable, and family-drift mutants, with no production or out-of-scope test change. **VERDICT: PASS.**
