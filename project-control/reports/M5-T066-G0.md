# M5-T066 G0 — readiness gate (orchestrator, administrative)

Task: **M5-T066 — D-082 map-CLICK interaction slice** (the recorded M5-T065
[ORCH-SCOPE-DISPOSITION] follow-up: click-to-place/adjust/delete outline vertices ON the
lot map, map surface IN scope this time, feeding the accepted drawing-bridge-adoption path).

## Authority

- **D-082-R001** (owner post-B3 checkpoint authorization) covers the map-drawing input
  capability; the T065 progress log records the orchestrator scope disposition: "the
  map-CLICK interaction layer contracts as the immediate follow-up slice WITH the map
  surface in scope (the owner already authorized the capability under D-082-R001)".
- Cited requirements bound at this seam with same-commit digest resyncs:
  D-082-R001/R003, D-066-R001, D-076-R001/R002, D-077-R002/R003.
  `evaluate_task_refs`: **ok=true, applicable == cited (7 ids)**.
- Phase C/D remain unreleased; scenario emission stays deferred; no held surface enters
  scope (the expansion §2 hold minus the D-040/D-076/D-082 releases stands).

## Packet readiness

- allowed_paths: 10 entries — the map display file + its spec (additive-optional
  interaction only), a new-wrapper glob pair (`ProposalOutlineMap*.tsx` — glob format per
  the launch-time matcher, no bare dir prefixes), the three T065 integration files + their
  specs, the e2e spec, and the producer report. All existing entries are tracked files;
  the glob pair covers optional new modules (no seeding needed — the surface has tracked
  files, and empty-spec seeding is prohibited for web anyway).
- forbidden_paths pin every adjacent surface: the ACCEPTED address-confirm consumer
  (AddressConfirmCard), the T063 surface, the T065 lib spine (one draft model / one bridge
  / one adoption path), `e2e/helpers.ts` (the a3173987 body-wrap guard is load-bearing),
  all of `services/api/`, and `packages/contracts/`.
- 5 acceptance scenarios (click placement, click adjust/delete + keyboard parity,
  display-surface byte-equivalence, end-to-end pointer journey, no-new-math/deps).
- Documented test command: `python tools/modularity_check.py --check` (thin client — web
  behavior proves only in CI on the pushed head; the packet's inputs carry the binding).

## Disjointness (four live/frozen neighbors)

| Neighbor | State | Overlap with T066 allowed_paths |
|---|---|---|
| M5-T064 (loop-1 LIVE, run-66) | in_progress | none — T064 is `services/api/app/scenario/**` + its tests; T066 forbids all of `services/api/` |
| M5-T062 | awaiting_gate (frozen 0366210c) | none — api-only surface |
| M5-T063 | awaiting_gate (frozen 853a1d25) | none — T066 forbids every T063 file; `address-confirm.test.tsx` (T063's) is NOT in T066 scope |
| M5-T065 | claimed 85%, unsubmitted | **THREE SHARED FILES** (ProposalOutlineDraw.tsx, ProposalEditor.tsx, e2e/proposal-editor.spec.ts) — deliberate: the disposition puts the wiring in this slice. **SEQUENCING HOLD: T066 executes only after M5-T065 is accepted** (packet dependency + binding input; the orchestrator holds the loop-3 launch on that boundary). |

## Seam records

- Code graph REGENERATED at this seam before authoring: 793 files / 16728 nodes / 7304
  edges. LotOutlineMap consumers verified: AddressConfirmCard (accepted, forbidden),
  PropertyOverview (T063, forbidden), ProposalOutlineDraw (in scope), its spec (in scope).
- Validator: one full `validate_directive_compliance.py --check` run at this settled seam
  (direct exit code recorded in the seam commit message).
- Reviewer roster: code-reviewer, qa-engineer, security-reviewer, human-journey-reviewer,
  directive-compliance-verifier; gates G0,G2,G3,G4 (mirrors the accepted T060/T065 shape).

**G0 verdict: PASS** — packet ready; backlog → ready. Claim follows at this seam
(claim-then-hold per D-070: the loop-3 re-feed launches only after the T065 accept).
