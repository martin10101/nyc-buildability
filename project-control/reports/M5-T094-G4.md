# M5-T094 — G4 test-adequacy review (qa-engineer "qa-t094", read-only)

> Transmission history: pinned at a612652b, delivered as four SendMessage parts (1/4..4/4) ending with END-OF-REPORT;
> no truncation. Joined verbatim by the orchestrator (transport wrapper tags removed only). Verdict PASS: the git-extracted
> PRE-split module reproduces all 62 pinned digests; 6 of 8 reviewer weakenings killed; the two survivors (a wrong-key form
> memo; per-stream vs per-document byte accounting) are INHERITED corpus-coverage gaps over byte-identical code (advisories
> A-1/A-2, routed to the C1 packet at the accept seam).

---

M5-T094 G4 test-adequacy review (read-only, qa-engineer) — part 1/4: method + KEY CHECK.

PIN: HEAD a612652b (candidate/D-024-mrl-option-b). Material commit 89128c62; git diff 89128c62..a612652b on the 5 task paths is EMPTY, so the worktree == reviewed content.

METHOD: reused the SHIPPED corpus+serializer+pinned golden from test_sheet_reader_split_equivalence.py (its run_corpus/digests/overall_digest take the module-under-test as a parameter) and ran them against (a) the git-extracted PRE-split monolith and (b) post-split HEAD, via the outside-repo 3.11 bare-package shim.

KEY CHECK — is the golden really PRE-split behaviour? PASS.
- Pre-split parent 89128c62^ = eccca03e: sheet_reader.py is the 1096-line monolith; sheet_objects/sheet_interpreter exist only as identical seeded placeholders (blob 3a724a09) — confirmed pre-split state.
- Byte-untouched deps VERIFIED: git diff 89128c62^..a612652b on app/documents/extraction and sheet_primitives.py is EMPTY, so running the monolith against current-tree deps is a faithful pre-split env.
- The pre-split monolith reproduces the pinned _OVERALL 767766eb… AND all 62 per-case golden digests EXACTLY (0 mismatch, 0 extra, 0 missing).
- Post-split HEAD reproduces the same overall + all 62 cases → post == pre byte-identical across curves, full-affine CTM, q/Q, forms incl. cycle/depth, images, every text op, every refusal class, every budget. The golden is genuinely pre-split (NOT captured from post-split) → the equivalence test is NOT vacuous.

SUITE: full shim run = 46 passed (37 frozen + 9 new), 0 failed, 0 skipped. Frozen test_sheet_reader.py is byte-identical 89128c62^..HEAD (git diff EMPTY) → AS-1 "the 37 tests are not edited" is git-true, and they pass unchanged through the facade.

Continues in part 2/4.

---

M5-T094 G4 — part 2/4: my own weakenings (in-memory, consuming-namespace, save/restore; measured against the corpus overall + per-case digests).

| # | Weakening (consuming namespace) | Expect | Result | Cases reddened |
|---|---|---|---|---|
| 1 | concat_matrix operand order swapped (sheet_reader._concat_matrix) | kill | KILLED | overall digest diverges (all CTM/text/form cases) |
| 2 | /Contents join "\n"→"" glue (sheet_objects._StreamDecoder.decode_contents) | kill | KILLED | contents_array (glued "S20" → unsupported-operator refusal) |
| 3 | refusal detail changed (sheet_interpreter._refuse) | kill | KILLED | 17 interpreter-origin refusal cases |
| 4 | finiteness gate forced True (sheet_interpreter._is_finite_point) | kill | KILLED | refuse_nonfinite_coord (+ curve cases) |
| 5 | Q restores CTM only, drops text state (sheet_interpreter._StreamRun._execute) | kill | KILLED | q_q_font_size |
| 6 | flatten no-subdivision (sheet_reader._flatten_cubic) | kill | KILLED | asym_bezier, mid_path_cm_device, quarter_circle_tol05, v_curve, y_curve |
| 7 | form memo under a CONSTANT key (sheet_objects._StreamDecoder.decode_form) | GAP probe | SURVIVED | 0 cases |
| 8 | decoded-bytes charge per-stream not per-document, identical detail (charge_decoded) | GAP probe | SURVIVES (by inspection) | 0 cases |

BY-VALUE PARTITION (validates [[mutate-consuming-namespace]]): changing detail via each module's own _refuse reddens a DISJOINT set — interpreter 17, objects 9, facade 2, overlap 0 = 28 profile refusals (the other 2, refuse_malformed_header/refuse_encryption, flow through _wrap_strict). So refusal `detail` is fully in the equivalence surface, and a single defining-namespace patch cannot reach all three modules' refusals — the corpus is sensitive across all three.

Note on probe 8: the classifier blocked every in-memory write to the decoded_bytes budget counter, so I confirmed it by source inspection (see part 4), not a live run. Probes 1-7 are live results.

Continues in part 3/4.

---

M5-T094 G4 — part 3/4: producer AS-4 guard mutants + the in-process seam.

PRODUCER'S 6 AS-4 GUARD MUTANTS (new equivalence file) — all load-bearing, all correctly built (each mutates the CONSUMING namespace, no by-value false survivors, each asserts the value the bug displaces):
- flatten point budget: real → "path points"; budget-ignoring _flatten_cubic → SheetDocument.
- decoded-bytes budget: real → "decoded bytes budget"; _StreamDecoder.charge_decoded no-op → SheetDocument.
- form depth guard: real → "xobject recursion"; _StreamRun._place_form resets depth → SheetDocument.
- form cycle guard: real → "xobject cycle"; _place_form clears the ancestor stack → refusal feature ≠ "xobject cycle" (asserts the DISPLACED feature — good, non-vacuous).
- top-level backstop: real → "unexpected error" with attacker detail ABSENT; bypass via _read_sheet → RuntimeError propagates (pytest.raises).
- finiteness gate: real → "non-finite coordinate"; _is_finite_point → True → SheetDocument.
Namespaces are correct: charge_decoded on sheet_objects._StreamDecoder; _place_form/_is_finite_point on sheet_interpreter; _flatten_cubic/_catalog_pages_root on the facade. I re-derived each real-branch feature from source and all 6 are in the 46-green run.

IN-PROCESS MUTANT SEAM PRESERVED (the split's headline risk): the 10 frozen facade-patch tests — patching sheet_reader MAX_CONTENT_OPERATORS / MAX_PATH_POINTS / MAX_Q_DEPTH / MAX_XOBJECT_DEPTH / MAX_TOTAL_DECODED_BYTES / _flatten_cubic / _concat_matrix / _decode_stream / _catalog_pages_root — all still KILL through the facade. Verified in _read_sheet (sheet_reader.py:260-277): read_sheet re-reads the facade globals per call and threads them into the freshly-built _SheetInterpreter/_StreamDecoder, so a facade patch still bites the split interpreter/decoder. Confirmed by the 46-green suite + my probes 1,3,4,6 which are facade/consuming-threaded and reddened.

AS-3 (out of my lane; G3/CI owns modularity_check) — quick corroboration only: runtime import DAG is acyclic (facade→interpreter→objects→primitives; the sole interpreter→facade edge is TYPE_CHECKING-only, lines 57-58); logical SLOC 314/245/584 all <600 (raw wc 359/290/663 differs because modularity_check counts logical lines).

Continues in part 4/4 (findings + verdict).

---

M5-T094 G4 — part 4/4: findings + verdict.

No BLOCKING findings.

ADVISORY A-1 (corpus mutation-coverage; inherited from M5-T083, NOT introduced by this split): no corpus or frozen case decodes two DISTINCT Form XObjects successfully — form_twice/form_flate reuse one form; refuse_xobject_depth's second form is never decoded (refused at depth first). So a wrong-key form memo (two forms sharing decoded bytes) SURVIVES the equivalence corpus (probe 7 = 0 cases changed). Not a defect here: the memo is byte-identical to correct pre-split behaviour and AS-4 requires no memo-correctness mutant (the security-relevant cycle/depth guards ARE tested). Suggest the C1 follow-on add a two-distinct-forms success case.

ADVISORY A-2 (corpus mutation-coverage; inherited): the decoded-bytes budget's per-DOCUMENT accumulation is never exercised. charge_decoded (sheet_objects.py:236-245) is the sole accumulator; the only lowered-total-cap case (refuse_decoded_bytes, MAX_TOTAL_DECODED_BYTES=4) is a single 15-byte decode that refuses on the first charge — identical under per-stream or per-document. So a per-stream-instead-of-per-document charge SURVIVES. AS-4's decoded-bytes guard is still load-bearing (the no-op-charge mutant is killed), so AS-4 is satisfied; only the accumulation semantic is under-covered. Suggest a multi-stream doc with each stream < cap but sum > cap.

Neither advisory undermines the byte-identical claim (independently proven via the pre-split golden reproduction) or AS-4 compliance.

VACUITY / honesty: assertions are non-tautological — digests are computed from the module under test and pinned to independently-reproduced PRE-split values; the AS-4 mutants assert the displaced value (M5-T081/T083 lesson satisfied, incl. the cycle mutant asserting feature ≠ "xobject cycle"). AS-1's "37 not edited" is git-confirmed, not just prose.

Environment note: local writes (scratch + agent-memory) and any in-memory patch of the budget counter were blocked by the read-only-reviewer guard; all live evidence came from git-read + python test execution. If useful, please record the reviewer lesson: "byte-identical-split anti-vacuity = run the git-extracted PRE-split module through the SHIPPED corpus/serializer (module-as-param) and reproduce the pinned golden; verify deps byte-identical parent..HEAD first."

M5-T094 G4 VERDICT: PASS
END-OF-REPORT
