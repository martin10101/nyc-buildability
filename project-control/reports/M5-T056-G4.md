# M5-T056 G4 — independent QA review (saved VERBATIM from the reviewer return; this header line is the orchestrator's)

All checks complete. Here is my gate report.

---

# GATE REPORT — G4 (Independent QA) — M5-T056

**Task:** M5-T056 — DB-036 condo-surface rider cluster (b/c/e/f/g web + h api)
**Reviewer:** qa-engineer (independent; read-only)
**Repo/branch:** `C:\Users\MLFLL\Downloads\nyc-zoning\ctl24` @ `candidate/D-024-mrl-option-b`
**Reviewed content identity:** frozen submission `db0cf0f8`; live head `bd408ab9` (disjoint peers only); material commit `5b7b4b30`
**Verdict: PASS** (no blocking findings; 2 advisories)

## Identity / freeze verification (all confirmed)

- **Byte-stability:** `git diff --stat db0cf0f8 bd408ab9` contains NONE of the 8 material files — every difference is a disjoint peer (M5-T055 e2e, M5-T057 packet/gate, directives index/D-078, `proposal_checks_api`). The 8 material files are byte-identical between the frozen and live heads.
- **Blob binding:** all 7 code/test files at `bd408ab9` and in the ctl24 working tree hash exactly to the producer report §1–§7 claimed new-blobs (e.g. `condo-records.ts` = `35abf328…`, `PropertyOverview.tsx` = `3d61696b…`, `condo_records.py` = `73d4e504…`, `test_condo_records_api.py` = `2f004119…`). `ReportView.tsx` = `a0cd258a…`.
- **Scope discipline:** material commit `5b7b4b30` touched exactly the 8 allowed files (7 source/test + report). `ReportView.tsx` is NOT in the commit → the "UNCHANGED" claim is true. No consumer file (ArchitectEntry/ProfileViews/PropertyFacts/ScenarioWorkspace) and no forbidden path (`address/`, `bbl.ts`, `bounded.ts`, `e2e/`, `connectors/`, `rules/`, `scenario/`, `contracts/`) was touched.

## Executed evidence (reproduced against the verified ctl24 checkout)

```
$ cd services/api && python -m ruff check .
All checks passed!                                    # exit 0

$ python -m pytest tests/api/test_condo_records_api.py -q
19 passed in 2.91s

$ python -m pytest tests/api -q
501 passed in 21.39s

$ cd .. .. && python tools/modularity_check.py --check
selected 456 files; failures 0; warnings 20          # exit 0
```

All match the producer report §10 (only the modularity selected-count differs, 455→456, explained under advisories). No task file appears in the 20 warnings; the sole condo-adjacent warning is `connectors/dtm_condo_soda.py` (pre-existing, forbidden-path).

## AS → binding test → mutation that fails it

| AS | Binding test (file:line) | Plausible mutation that FAILS |
|---|---|---|
| **AS-1** entered vs billing | `condo-records.test.ts:193-214,416-433`; `condo-resolution-display.test.tsx:722-742` | Reuse `enteredBbl` as the billing fallback → `billing.not.toContain("1003031001")` (`:731`) fails. Skip `validateBblInput` → malformed `entered_bbl` no longer nulls (`:432`) fails. |
| **AS-2** dangling reference | `condo-resolution-display.test.tsx:435-454,744-749` | Reintroduce "professional-review determination above" → `.not.toMatch(/…/i)` (`:450,:747`) fails; drop "development limits above" → `toHaveTextContent` (`:449,:748`) fails. |
| **AS-3** zoning honesty | `condo-records.test.ts:435-457`; `condo-resolution-display.test.tsx:751-776` | Remove the `anyZoningRecorded` gate on the divergent notice → no-zoning fixture (which DOES carry a divergent string) now renders it → `queryByTestId("condo-divergent-notice")).toBeNull()` (`:754`) fails. Non-scope-aware gap copy → `"not yet shown for every base lot"` / `"recorded districts…unaffected"` (`:774-775`) fails. |
| **AS-4** heading outline | `condo-resolution-display.test.tsx:778-788`; `report-view.test.tsx:483-485` | Change `<h2>` to `<p>`/`<div>` → `getByRole("heading",{level:2,…})` throws. |
| **AS-5** retrievedAt value pins | `condo-records.test.ts:181-187,459-470`; `condo-resolution-display.test.tsx:790-795`; `report-view.test.tsx:486-491` | `boundedTimestamp`→`boundedToken` strips `:`/`+` → `"2026-09-01T140556Z"` ≠ pin; unit `.toBe` fails and render `.toHaveTextContent("…14:05:56Z")` substring fails. |
| **AS-6** 422 repr cap | `test_condo_records_api.py:572-577,580-593,596-612,615-625` | No-op `_capped_raw_value` → truncate assert fails; wrong ceiling → exact-length asserts fail; not applied in 422 branch → e2e oversized (`:596`) fails. |
| **AS-7** guard coherence | `condo-resolution-display.test.tsx:514-658` (withhold suite, unchanged) + `:668-705` (deriveCondoSurface) | Add a second withhold decision / weaken the withhold → monotonic test (`:699-704`) or the withhold-on-multi/unresolved/error/missing/unknown/malformed tests fail. |
| **AS-8** proof | reproduced above | — |

## Duty-specific verifications

- **(g) char-for-char end-to-end / selective-revert kill — CONFIRMED.** `bounded.ts` `boundedToken` allowlist is `[A-Za-z0-9._-]` (strips `:` and `+`); `boundedTimestamp` keeps `[0-9A-Za-z.:+-]`. So the revert genuinely diverges the fixture value at both parser (`.toBe`, strict) and render (`.toHaveTextContent`, substring). Both usage sites are pinned — `provenance.retrievedAt` (`condo-records.ts:340`) and `provenance.queries[].retrievedAt` (`:313`) — so a selective revert of either site is caught (`condo-records.test.ts:464,468`). Brief surface independently pinned (`report-view.test.tsx:486-491`) through the ONE shared component.
- **(h) byte-identical + exact ceiling+marker + both directions — CONFIRMED.** `_capped_raw_value` (`condo_records.py:231-238`) is a pure length cap applied only in the 422 branch (`:596`); short repr returned unchanged, oversized → `raw[:256] + "...[truncated]"`. Unit + e2e cover both directions; `install_billing_landmine`/`install_unit_landmine` prove no resolver runs on the 422 path.
- **Regression / public-props stability — CONFIRMED.** `PropertyOverview` signature `{profile, scenario, evaluation, onInspect}` unchanged; the +39 lines are the `deriveCondoSurface`/`CondoSurfaceDecision` additions and render-branch locals, no prop change. Consumers definitionally untouched (not in the material commit). CI reports web green at `bd408ab9`, run `35494571792`.
- **Guard coherence — CONFIRMED.** One monotone `deriveCondoSurface` decision (`PropertyOverview.tsx:197-214`, `:308`); the new locals `anyZoningRecorded`/`anyZoningUnknown`/`enteredLot`/`billingLot`/`zoningGapNote` are render-branch derivations from already-parsed view fields, not a second withhold/allow point. `withholdAllowances = profileWithholds || channelWithholds` unchanged; `boundedToken` charset untouched.
- **Fixture hygiene — CONFIRMED.** New `channelMultiLotNoZoningDoc` uses `recorded_zoning:null / status:"unknown"` on both base lots (honest). Recorded districts (`R6`, `R7-2`) are api-supplied fixture inputs the parser only reflects (`boundedToken(record.recorded_zoning)`); production code invents no city value.

## Advisories (non-blocking)

- **G4-A1 (advisory):** Web suites cannot be executed here (thin client); I relied on the CI conclusion supplied in the prompt (green at `bd408ab9`, run `35494571792`) plus digest-bound identity. The three web test files' content is verified byte-identical to the reviewed head, so the CI conclusion is bindable to this identity.
- **G4-A2 (advisory):** `modularity_check` selected-count is 456 at review head vs 455 in the producer report — attributable to the M5-T057 disjoint-peer files added between freeze and live head; `failures 0` and no task file flagged, so immaterial. Both headings are real `<h2>` (AS-4 asks "h2/h3"; two sibling section h2s satisfy it — no defect).

**Recommendation: record G4 PASS.** No corrections required.
