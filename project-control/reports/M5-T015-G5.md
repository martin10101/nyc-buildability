<!-- Reviewer return preserved VERBATIM by the orchestrator (report-preservation rule,
.claude/rules/project-control.md; transport entity-decoding applied: &lt; -> <, &gt; -> >, &amp; -> &).
Reviewer: independent security-reviewer subagent, returned 2026-09-12 (UTC). -->

# G5 SECURITY GATE REPORT — M5-T015 (Address entry UI, Packet 1)

**Task:** M5-T015 — Address-entry UI consuming GET /api/v1/address-resolution
**Reviewed HEAD:** `9f679ff7de62658189dacb4db9ba1ea824b30ab1` (branch `candidate/D-024-mrl-option-b`) — verified via `git rev-parse HEAD`, equals the frozen SHA.
**Production commits reviewed:** `352aa9de` (producer material) + `6787445e` (test-module-only repair; apps/web production content byte-identical between 352aa9de and HEAD).
**Gate:** G5 (security). **Reviewer:** security-reviewer (read-only, ADR-005).

## VERDICT: PASS

## Summary

This packet is the escape/containment layer for an endpoint that deliberately transports unsanitized caller-typed text and names its reflected surfaces in `_REFLECTED_INPUT_WARNING.fields`. The containment is correct, complete, and defense-in-depth. Every reflected field named by the endpoint either renders exclusively as a bounded JSX text node (React auto-escaping is the primary defense, `boundedText`/`boundedToken` add length/control-char/charset caps) or provably does not render in Packet 1 (`provenance.request_params` and `source_facts[].original_value/normalized_value` are never mapped into the view — producer claim verified in source). No reflected value reaches any attribute/URL context: the only anchor is the constant `href="#bbl-input"` (AddressResolutionScreen.tsx:159); the ZoLa/attribute URL context is correctly deferred to Packet 2. `dangerouslySetInnerHTML` appears in zero new/changed files and the test's source-scan asserts its absence across all six. The `rawStreetName` verbatim member is contained to exactly two safe sinks — `URLSearchParams` (percent-encoded request URL) and a controlled `<input value>` (React property assignment, not attribute markup) — and never reaches a render. Correlation ids and `retry_after` are boundedToken'd client-side (belt-and-suspenders over the server's local re-bounding). No secrets, no service-role key, no Geoclient key material, no client logging (`console.*` absent). The flag-off posture makes the surface unmountable and the fetch unreachable (fail-safe default, two independent gating conditions). `package.json`/`package-lock.json` are untouched (`git diff 2ecb06ac..HEAD` empty) and no import specifier outside `react`/`@/`/`./` exists. The hostile-fixture tests would catch a real escape (element-absence sweep + single-anchor href assertion + `window.__pwned` sentinel + fetch-seam verbatim assertion). CI `web` and `web-e2e` jobs SUCCESS at the reviewed head (31 address tests pass). Findings are two Minor observations only; nothing Critical/High/Major.

## Reflected-surface disposition table

Endpoint-named surfaces (`_REFLECTED_INPUT_WARNING.fields`, address_resolution.py:209-227):

| Reflected field | Render site (Packet 1) | Containment |
|---|---|---|
| `input_echo.house_number` | ResolvedCard `line` (216); EchoLine (not_found/rejected) | `textOrNull`→`boundedText`, JSX text node |
| `input_echo.street` | EchoLine (AddressResolutionScreen.tsx:120-125) | `textOrNull`→`boundedText`, JSX text node |
| `input_echo.borough` | EchoLine area (117-124) | `textOrNull`→`boundedText`, JSX text node |
| `input_echo.zip` | EchoLine area fallback (117) | `textOrNull`→`boundedText`, JSX text node |
| `grc_message` | GrcLines (139); warnings block (202-204) | `textOrNull`→`boundedText`, JSX text node |
| `grc2_message` | GrcLines (140); warnings block (205-207) | `textOrNull`→`boundedText`, JSX text node |
| `suggestions[]` (`street_name`) | SuggestionChooser `{suggestion.streetName}` (SuggestionChooser.tsx:46) | `boundedText`, JSX text node; `key={index}` (37); `rawStreetName` never rendered |
| `canonical.street_name_normalized` | ResolvedCard `line` (169) | `textOrNull`→`boundedText`, JSX text node |
| `canonical.borough_name` | ResolvedCard `line` (171) | `textOrNull`→`boundedText`, JSX text node |
| `provenance.request_params` | NOT RENDERED | Never mapped into `AddressDocumentView` (address-api.ts:234-239 maps only sourceId/retrievedAt/connectorCorrelationId/responseDigest); deferred to Packet 2 |
| `source_facts[].original_value` / `normalized_value` (street/borough) | NOT RENDERED | Only `sourceFactsCount` (a number) and `sourceFactsNotEmittedReason` are mapped/rendered; individual fact values never mapped |

Additional transported values, all contained (JSX text nodes, bounded): raw `status`→`boundedToken` statusToken (UnrecognizedStatusCard 349); `canonical.bbl/bin/zip_code`→`boundedToken`; `grc/grc2` codes→`boundedToken`; HTTP + connector correlation ids→`boundedToken`; `error.message`→`boundedText` (invalid_input card 454); `retry_after`→server-bounded + client `boundedToken` (address-api.ts:352); `unexpected_response.receivedState`→`boundedToken`.

## Checklist verification (all 9)

1. **Every reflected field is a bounded text node or provably unrendered — PASS.** See table. Producer's "source_facts/provenance render nowhere" claim verified: grep of AddressResolutionScreen.tsx for `provenance`/`request_params`/`source_facts` finds no render site (only `sourceFactsNotEmittedReason` and count are surfaced).
2. **No reflected value in any attribute/URL context — PASS.** Every non-constant attribute in the new JSX is safe: `href` is the constant `#bbl-input` only (159); `data-testid` uses constants/numeric index/typed-enum state; React `key` is the array index (SuggestionChooser.tsx:37); `value` is form state (see #4); `id/name/className/placeholder/role/aria-*` are all constant. No `src`/`style`/`srcset` with reflected content anywhere.
3. **No `dangerouslySetInnerHTML` — PASS.** Grep across all address files + address-api.ts + announce.ts: none. Test source-scan asserts absence for all six files (test lines 673-675).
4. **rawStreetName containment — PASS.** Consumers: SuggestionChooser `onPick(...rawStreetName)` (SuggestionChooser.tsx:66) → AddressResolutionScreen `onPickSuggestion` (620-632), which (a) `setValues(... street: rawStreetName)` → controlled `<input value>` and (b) `runResolve({ ...base, street: rawStreetName })` → `resolveAddress` → `params.set("street", ...)` → `URLSearchParams.toString()` (percent-encoded request URL). Never reaches a render. **Judgment on the controlled input:** a hostile suggestion in `<input value={...}>` is NOT a risk — React assigns the DOM `.value` IDL property (element.value = string), never serialized HTML/attribute markup, so `"><script>` in the value is inert literal text in the field. On the round trip the server echoes it in `input_echo.street`, which is re-bounded via `textOrNull` before render. URLSearchParams encoding makes the request-URL context safe (no query/header injection).
5. **Correlation ids and retry_after re-bounded client-side — PASS.** correlationId = `boundedToken(response.headers.get("X-Correlation-ID"))` (address-api.ts:293); retry_after = `boundedToken(error?.retry_after, 32)` (352), a client re-bound over the server's fullmatch+cap guard (`_bounded_retry_after`, address_resolution.py:286-298). Client charset [A-Za-z0-9._-] is stricter than the server charset — display-fidelity-only, not a security concern.
6. **No secrets — PASS.** Only `NEXT_PUBLIC_API_BASE_URL` referenced (address-api.ts:171). No service-role key, no SUPABASE/SERVICE_ROLE references, no Geoclient key material (Geoclient appears only in copy text/comments). No `console.*` / client logging anywhere.
7. **Flag containment — PASS (with Minor note F-1).** `PropertyLookup.tsx:265` mounts `<AddressResolutionScreen />` only when `ruleEvalEnabled`; default false; the boolean is computed server-side from non-public env `INTERNAL_RULE_EVAL_UI` + `?ruleeval=on` (rule-evaluation.ts:88-95, property/page.tsx:30) — two independent conditions, fail-safe off, never inlined into the browser bundle. Flag off = surface unmountable, no fetch. See F-1 on the frontend/backend flag divergence.
8. **Hostile-fixture tests would catch an escape — PASS.** S6 test 1 (test lines 590-615) proves inertness three ways: `container.querySelectorAll("script,img,iframe,style").length === 0`, `anchors.map(href) === ["#bbl-input"]` (the href-sink assertion), and `window.__pwned === undefined`. S6 test 2 proves a hostile suggestion renders as text and the RAW name reaches only the fetch seam verbatim (URL-decoded equality). A real escape would require either `dangerouslySetInnerHTML` (grep-asserted absent) or an attribute sink (none exist for reflected text; the href-sink is asserted). See F-2 for a coverage-thoroughness note.
9. **Dependency surface — PASS.** `git diff 2ecb06ac..HEAD -- apps/web/package.json apps/web/package-lock.json` is empty. Import-specifier allowlist test (test lines 681-699) is sound (regex `from "..."` captures static + re-export + type imports; no dynamic import/require in these files). Manual inspection confirms all imports across all six files are `react`/`@/`/`./`. No new npm dependency.

## Findings (Minor only)

**F-1 (Minor / Informational) — Frontend and backend feature flags are distinct env vars, so the endpoint's flag-off 404 is reachable under a specific misconfiguration.**
- Evidence: UI gate = `INTERNAL_RULE_EVAL_UI` + `?ruleeval=on` (apps/web/src/lib/rule-evaluation.ts:75-95); endpoint gate = `INTERNAL_RULE_EVAL_ENABLED` (services/api/app/api/v1/address_resolution.py:119,420). The design spec §6 states both should ship "behind `INTERNAL_RULE_EVAL_ENABLED`, the same flag."
- Impact: In the fail-safe default (frontend flag off) the surface never mounts and the fetch never fires — the primary requirement holds. Only if an operator explicitly enables the frontend flag while the backend flag is off does the fetch fire and receive the generic 404. That 404 renders as the honest `unexpected_response` card ("This is not an 'address not found' result — the response body was not trusted or rendered", AddressResolutionScreen.tsx:513-549), NOT as a `not_found`/coerced address answer. Because the 404 has no `state`, `DOCUMENTED_PAIRS.has("404|")` is false (address-api.ts:334) → `unexpected_response`. The endpoint's "never coerced into not_found" intent is preserved; the leak only occurs in a session where the operator already deliberately turned the surface on. Not a code defect in this packet; the dual-flag scheme is the reused M4-T005 precedent. Recommend the orchestrator/owner note the flag-coupling divergence from the design spec for Packet 2 (where the ZoLa/attribute URL context lands).

**F-2 (Minor / Test thoroughness) — S6 hostile coverage exercises only the not_found and ambiguous cards.**
- Evidence: S6 test 1 drives hostile content through the not_found card; test 2 through the ambiguous chooser. It does not render hostile content through the resolved card (`street_name_normalized`/`borough_name`), the `resolved_with_warnings` grc block, or `unexpected_response.receivedState`.
- Impact: None to runtime safety — those sites use the identical `boundedText`/`boundedToken` + JSX-text-node mechanism (verified by source inspection), and the element-absence sweep + `window.__pwned` sentinel would catch an escape in any rendered card. This is a suggestion to add a hostile-`resolved` fixture in a future increment, not a defect.

## Non-applicable / satisfied-by-construction

- **Cross-tenant isolation, service-role secrecy, private storage:** N/A to this frontend packet — no Supabase, no storage, no tenancy, no service-role key (grep-confirmed absent).
- **SSRF:** The fetch host is the fixed deploy-time `NEXT_PUBLIC_API_BASE_URL`; the path is the constant `/api/v1/address-resolution`; only query params vary and are URLSearchParams-encoded. Reflected content cannot steer the request to another host. Safe.
- **Prompt-injection:** No LLM in this path; reflected text is never fed to a model or as a tool instruction — rendered as escaped text only. Satisfied.
- **Log redaction / least privilege:** No client-side logging added; client holds only form/HTTP state and the public base URL. Satisfied.

## CI evidence verified (orchestrator-captured, thin-client)

Per `project-control/reports/M5-T015-ci-evidence.txt`: CI run 34675912878 on head `6787445e` — `web` (lint+typecheck+build) SUCCESS, `web-e2e` (vitest+Playwright) SUCCESS, address suite 31 tests PASSED, 27 web test files green. The two failing jobs (`web-dependency-security` — Next.js RCE fix awaiting owner authorization; `control-plane` — digest-normalization owner-gated decision) are pre-existing, owner-gated, and unrelated to this packet's diff (package.json untouched). These are tracked outside M5-T015 and do not fall within this render-safety gate's scope; the owner-authorization dependency posture is governed by CLAUDE.md policy #15.

## Relevant file paths
- C:\Users\MLFLL\Downloads\nyc-zoning\ctl24\apps\web\src\lib\address-api.ts
- C:\Users\MLFLL\Downloads\nyc-zoning\ctl24\apps\web\src\components\address\AddressResolutionScreen.tsx
- C:\Users\MLFLL\Downloads\nyc-zoning\ctl24\apps\web\src\components\address\AddressForm.tsx
- C:\Users\MLFLL\Downloads\nyc-zoning\ctl24\apps\web\src\components\address\SuggestionChooser.tsx
- C:\Users\MLFLL\Downloads\nyc-zoning\ctl24\apps\web\src\lib\announce.ts
- C:\Users\MLFLL\Downloads\nyc-zoning\ctl24\apps\web\src\components\property\PropertyLookup.tsx
- C:\Users\MLFLL\Downloads\nyc-zoning\ctl24\apps\web\src\app\property\page.tsx (flag wiring, not in diff)
- C:\Users\MLFLL\Downloads\nyc-zoning\ctl24\apps\web\src\lib\bounded.ts (containment primitives, read-only dependency)
- C:\Users\MLFLL\Downloads\nyc-zoning\ctl24\apps\web\src\components\address\__tests__\address-resolution.test.tsx
- C:\Users\MLFLL\Downloads\nyc-zoning\ctl24\services\api\app\api\v1\address_resolution.py (endpoint contract, read-only dependency)

**Verdict: PASS.** No Critical/High/Major security findings. Two Minor observations (F-1 flag-coupling divergence for the orchestrator/owner to note toward Packet 2; F-2 optional test coverage addition). The render-safety contract of docs/design/address-entry-confirm-design-spec.md §3 is fully and correctly implemented as the containment layer for the unsanitized endpoint. The orchestrator should record this G5 result.
