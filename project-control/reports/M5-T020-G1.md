# M5-T020 — G1 Data-Contract Gate Report (verbatim)

Orchestrator preservation note: report returned by the independent data-contract-verifier
agent ("G1 data-contract review M5-T020") on 2026-09-12 and preserved verbatim below
(transport entity-decoding only). Reviewer was read-only with live official-source access;
frozen review identity: producer commit 683b6095. Verdict: PASS (4 non-blocking advisories
→ recommended follow-up task).

---

All verification complete. Here is my full gate report.

---

# G1 GATE REPORT — M5-T020

**Gate ID:** G1 (data-contract / official-source verification)
**Task:** M5-T020 — D-040-R001 server half: MapPLUTO lot-outline 4326 transport + flag-gated `/lot-geometry` route + new `lot_geometry` contract
**Reviewer:** data-contract-verifier (read-only, ADR-005)
**Frozen identity reviewed:** producer material commit `683b6095` (base `a5e5e408`); HEAD `adf9b9dc`
**Precedent:** this reviewer's M2-T009 G1 hold on the authoritative connector
**Result:** **PASS** (with 4 non-blocking advisories → follow-up task recommended)

---

## Scope containment (frozen identity)

- `git show 683b6095 --stat`: 26 files, all mapping to the 12 `allowed_paths`. No file outside scope.
- Commits after `683b6095` up to HEAD touch **only** ledger/evidence (`project-control/reports/M5-T020-*`, `state.json`, `tasks/M5-T020.json`). None touch any of the 12 source `allowed_paths`. Working-tree blobs of the two source modules + `main.py` byte-match the frozen commit.
- **Forbidden paths byte-immutable (S4):** blob hash of `mappluto_geometry_arcgis.py` is identical (`5db3ba04…`) at base / producer / HEAD; `config.py` identical (`b302fcd3…`) at base/HEAD; `pluto_soda.py`, `scenario.schema.json`, `scenario.ts` diff EXIT 0. **The authoritative 2263 connector and config.py are untouched.**

## Live official-source verification (2026-09-12, WebFetch)

Layer `…/MAPPLUTO/FeatureServer/0?f=json`: name `MAPPLUTO`; currentVersion 12; `esriGeometryPolygon`; wkid 102718 / latestWkid 2263 (native); maxRecordCount 2000; supportsPagination true; **supportedQueryFormats "JSON, geoJSON, PBF"** (→ `f=geojson` supported); `Version` field = `esriFieldTypeString` len 6; capabilities `Query` (keyless). Matches the pinned research exactly.

Live re-fetch of LOT01's **exact manifest URL** (`…query?where=BBL%3D1008350041…&f=geojson&outSR=4326`): `type=FeatureCollection`; `crs={"type":"name","properties":{"name":"EPSG:4326"}}`; 1 feature; Polygon; first vertex `[-73.9867337745617, 40.7485748733075]`; BBL `1008350041`; Version `26v2`; Borough `MN`, Block `835`, Lot `41`. **The recorded fixture's first vertex, CRS, and per-feature Version match the live response byte-for-byte** — confirming the fixtures are genuine live-captured 4326 official bytes, not reprojected/derived from the 2263 esriJSON fixtures. `outSR=4326` server-side reprojection and lng/lat order are confirmed against the live service.

## Field / schema fidelity

- **Imported constants faithful:** `SOURCE_ID=nyc-dcp-mappluto-arcgis`, `SERVICE_ROOT`, `LAYER_NAME=MAPPLUTO`, `BOUNDARY_TOLERANCE_FT=20.0`, `MAX_FEATURES_PER_LOT=10`, `OUT_FIELDS` all imported from the immutable connector (no fork).
- **Condo semantics match research §2.5:** `_condo_classification` (unit 1001–6999 → `condo_unit_lot_query` no polygon; billing 7501–7599 → `condo_billing_lot` merged complex) imported verbatim and reshaped into the closed `condo_classification` fields. Fixtures confirm: LOT03 (lot 7501) → single Polygon billing; LOT04 (lot 1001) → empty/no_outline unit.
- **Contract closed & fully specified:** root and every `$def` object carry `additionalProperties:false`. 17 required root keys. Module-emitted envelope keys ≡ schema required set (both directions verified: nothing emittable is schema-invalid; the only schema-legal-but-unemitted key is `_expected_failure`, an intentional fixture-only annotation). Enums (outcome, no_outline_reason, contract_version, document_kind, `display_only:true`, `crs:EPSG:4326`, source_id) all match module output.
- **Both schema copies byte-identical:** sha256 `7893be81…` for `packages/contracts/schemas/v1/…` and `services/api/app/_contract_schemas/v1/…`; `cmp` IDENTICAL. Load-bearing in-suite guard `test_s6_bundled_schema_is_byte_identical_to_canonical` reads both as bytes and asserts equality (verified present and real).
- **Generated TS faithful:** `lot_geometry.ts` `LotGeometry` interface + all `$def` types match the schema (enums, `display_only:true`, `crs:"EPSG:4326"`, OutlineGeometry union, source_id const, optional `_expected_failure`).
- **7 valid contract fixtures validate** against the schema+common (independently re-run: all 7 VALID, covering all four outcomes + polygon/multipolygon/condo-billing/condo-unit/no-feature).

## Recorded-official discipline (S5)

- All 8 fixture sha256 **recomputed and match MANIFEST exactly**. LOT02/LOT04 legitimately share one sha256 (byte-identical empty FeatureCollections; distinguished by BBL/condo classification, not body — M2-T009 MPG03/MPG05 precedent, documented).
- 6 raw captures: coordinate order `[lng,lat]`, all vertices within NYC ranges (lng ∈ [−74.03,−73.74], lat ∈ [40.62,40.75]); LOT05 = 3-ring holed Polygon (Governors Island), LOT06 = 2-part MultiPolygon (Queens). Verbatim transport confirmed.
- 2 synthetic negatives (LOT80 null-geometry, LOT96 two-feature) correctly classified `synthetic`, `derived_from=LOT01`, with explicit MANIFEST note that the service does not emit these; `test_synthetic_fixtures_declare_derivation_and_raw_do_not` enforces the raw/synthetic boundary. **No 4326 byte derived from 2263 fixtures** (confirmed by live byte-match above).

## Provenance & display-only boundary (S1, S4)

- Every outcome envelope carries `source.source_id`, `service_root`, `layer`, `endpoint` (exact bounded query URL), `dataset_version` (per-feature `Version`, e.g. `26v2`), `retrieved_at`; plus `display_only:true`, the ±20 ft `accuracy_note`, NYC DCP `attribution`, and DCP `disclaimer`. Verified in module code and in the validating fixtures.
- **No measurement from 4326 coordinates:** grep of both new files finds `area/dimension/measure/shapely` only in comments/docstrings/note-strings; no `import shapely`, no area/perimeter/distance computation. `_transportable_geometry` is a structural drawability check only. Registry-draft update is additive and accurate (adds `display_outline_usage` block + second `last_successful_ingestion` date; original preserved).

## Null / ambiguity / pagination / rate-limit / drift behavior

- **Null/ambiguous:** condo-unit vs no-feature distinguished by classification (not body); `multiple_features` → review, geometry withheld, never a first-pick; `invalid_geometry` → typed 200 outcome, never a 500. All exercised by passing tests.
- **Injection/URL discipline (S3):** directly exercised — canonical BBL → exactly one URL ending `&f=geojson&outSR=4326` with `int()` where-clause; `'1008350041 OR 1=1'`, over/under-length, and `'abc'` all refused with `BBLValidationError` before any I/O. Flag-off → generic `404 {detail:"Not Found"}` (no correlation leak); malformed BBL → typed 422; GET-only; `include_in_schema=False`.
- **Pagination:** single-BBL query capped at `resultRecordCount=10`; >1 feature → `multiple_features` review (anomaly surfaced, not paged). Appropriate for one-lot lookup.
- **Schema drift / rate-limit:** non-FeatureCollection → `malformed_response` 502; present-but-wrong CRS → `wrong_crs` 502; returned-BBL mismatch → `result_mismatch` 502; non-200 (incl. 429) → `upstream_error` 502; assembled doc failing the closed schema → typed `internal_contract_error` 500; renderer-parity guard (`allow_nan=False` + utf-8 encode) before send.

## Test / tooling results (Python 3.11.9 sandbox)

- `test_mappluto_lot_outline.py` → **48 passed**; `test_lot_geometry_api.py` → **19 passed** (67 total, matches producer).
- Full `services/api/tests/connectors` → **486 passed**; full `services/api/tests/api` → **417 passed** (no regressions).
- `python tools/validate_directive_compliance.py --check` → **EXIT 0**.
- `python tools/modularity_check.py --check` → **EXIT 0** (only pre-existing warnings on other files; new modules 596 + 244 SLOC, both below the 600 warn threshold; no new warning).
- `main.py` diff = additive `include_router(lot_geometry_v1_router)` + import only.
- CI evidence (`M5-T020-ci-evidence.txt`, CI Python 3.12): api / contracts / contracts-typegen / contracts-schema-bundle / control-plane all SUCCESS; only unrelated `web-dependency-security` (owner-gated D-040-R004, no package file touched) fails. Disclosed 3.11-only PEP 695 contracts failure is pre-existing M2-T015 code, out of scope, green on CI 3.12.

## S1–S6 verdicts

- **S1 single_lot 200** — PASS (contract-valid; provenance incl. Version 26v2; display markers; X-Correlation-ID; NYC-plausible lng/lat).
- **S2 typed outcomes honest** — PASS (condo-unit no-outline, billing merged caveat, multiple→review no first-pick, invalid→typed; all contract- and renderer-parity-valid).
- **S3 route posture** — PASS (flag-off generic 404; malformed BBL 422; GET-only; injection-proof URL builder; not in OpenAPI).
- **S4 authoritative 2263 untouched + no measurement** — PASS (byte-immutable blobs; grep-proven no 4326 measurement; display_only on every outcome).
- **S5 recorded-official fixtures** — PASS (8/8 sha256 match; live byte-match proves genuine capture; synthetics documented; suite offline).
- **S6 contract + regression** — PASS (byte-identical closed schema copies; faithful TS; renderer-parity guards; full suites green; modularity EXIT 0).

## Defects / required rework

**None blocking G1.** All contract, field-fidelity, provenance, and official-source dimensions verified.

## Advisories (non-blocking — recommend orchestrator open a small contract-scoped follow-up task)

1. **TS-generator entrypoint + CI drift not wired for `lot_geometry.ts`** (disclosed limitation #1). `contracts-typegen --check` does not yet cover the new artifact, so schema→TS drift has no automated fail-closed guard. Mitigation is adequate for now (TS verified faithful today; in-suite byte-identity guards the two *schema* copies). Fix requires editing `packages/contracts/scripts/generate_ts_types.py`, which is outside this packet's `allowed_paths` — genuinely a follow-up, not in-scope rework.
2. **`sync_contract_schemas.py` hardcoded `SCHEMA_FILES` does not include `lot_geometry`** (disclosed #2). Mitigated by the load-bearing in-suite byte-identity test; same follow-up as (1).
3. **`default_fetch` has no retry/circuit/rate-limit/LKG** resilience (disclosed #4), unlike the authoritative connector. Acceptable for an internal, flag-gated, display-only route (faults → typed 502); recommend wrapping in `app.resilience` in a follow-up.
4. **Minor over-fetch:** `OUT_FIELDS` (imported from the immutable connector) requests `Shape__Area`/`Shape__Length`, which the outline envelope never reads. Harmless and **not** a display-only violation (no measurement is computed from 4326 coords); noted only for cleanliness.

## VERDICT

**G1 PASS** — data contract, field/unit meanings, provenance, recorded-official discipline, display-only boundary, and typed failure behavior are all independently verified at frozen commit `683b6095`; the official ArcGIS FeatureServer live-confirms `f=geojson&outSR=4326` support and the recorded LOT01 fixture matches the live response byte-for-byte. Forbidden 2263 connector and `config.py` are byte-immutable. Four non-blocking advisories are carried forward as a recommended follow-up task (TS-generator/drift wiring chief among them); none affect the correctness of the shipped contract or its provenance.

Relevant files:
- `C:/Users/MLFLL/Downloads/nyc-zoning/ctl24/services/api/app/connectors/mappluto_lot_outline.py`
- `C:/Users/MLFLL/Downloads/nyc-zoning/ctl24/services/api/app/api/v1/lot_geometry.py`
- `C:/Users/MLFLL/Downloads/nyc-zoning/ctl24/packages/contracts/schemas/v1/lot_geometry.schema.json`
- `C:/Users/MLFLL/Downloads/nyc-zoning/ctl24/services/api/app/_contract_schemas/v1/lot_geometry.schema.json`
- `C:/Users/MLFLL/Downloads/nyc-zoning/ctl24/packages/contracts/generated/lot_geometry.ts`
- `C:/Users/MLFLL/Downloads/nyc-zoning/ctl24/services/api/tests/fixtures/mappluto_lot_outline/MANIFEST.json`
- `C:/Users/MLFLL/Downloads/nyc-zoning/ctl24/services/api/tests/connectors/test_mappluto_lot_outline.py`
- `C:/Users/MLFLL/Downloads/nyc-zoning/ctl24/services/api/tests/api/test_lot_geometry_api.py`

---

# APPENDED DELTA ATTESTATION (verbatim, same reviewer agent, after correction commits a3d59e03 + 7a4897b7)

## G1 DELTA ATTESTATION — M5-T020 (data-contract reviewer)

Independently verified at new HEAD `7a4897b7`:

**(1) Test-only + evidence-only, additive, no contract byte changed.** `git show a3d59e03 --stat`: only `test_lot_geometry_api.py` (+39) and `test_mappluto_lot_outline.py` (+33/-3), both in `allowed_paths`. `7a4897b7`: only `M5-T020-ci-evidence.txt` (+11/-3). Diff `adf9b9dc..7a4897b7` over all schema/bundled-schema/generated-TS/contract-fixture/raw-fixture/production paths is **EMPTY** — no contract, schema, TS, fixture, or production byte changed. Forbidden 2263 connector + `config.py` remain byte-immutable (unchanged since my report).

**(2) New TS guard is real and load-bearing.** `test_s6_generated_ts_covers_every_schema_key_enum_and_const` recursively walks the canonical schema and asserts every property name, enum string, and const literal appears in the committed `lot_geometry.ts`. It captures the dominant drift risk this advisory targeted — a schema property/enum/const added without regenerating the TS now fails in-suite. It is a token-containment check (not full structural equivalence: it won't catch a removed-token/stale-extra or a token re-nested under the wrong interface), but that is exactly the interim coverage needed until the generator+CI wiring lands. Both packs rerun: **70 passed** (was 67).

**(3) G1 PASS stands** at `7a4897b7`. Advisories **1–2 now partially mitigated** (in-suite schema→TS structural drift guard + LOT05 holes-preservation assertions added; full `generate_ts_types.py` entrypoint + CI drift wiring remains the recommended follow-up). Advisories **3 (no fetch resilience) and 4 (Shape__Area over-fetch) unchanged**. No new defects.
