# M5-T018 — G1 Code Review Gate Report (verbatim)

Orchestrator preservation note: report returned by the independent code-reviewer agent
("G1 code review M5-T018") on 2026-09-12 and preserved verbatim below (transport
entity-decoding only). Reviewer was read-only; frozen review identity: producer commits
1b2495da + ee05ad17. Verdict: PASS (no defects; advisories A1–A2 non-blocking).

---

All verifications complete. The reviewed working-tree content is byte-identical to the frozen commit 1b2495da, which is contained in the green CI head 44048007. Here is my gate report.

---

# G1 GATE REPORT — M5-T018 (C1 web contract-mirror validator + Compare rendering)

**Gate:** G1 (independent code review)
**Task:** M5-T018 — "C1 unused draft zoning floor area — web contract mirror + Compare rendering"
**Directive regime:** in-regime — `D-041 → D-041-R001`
**Reviewer:** code-reviewer (read-only, ADR-005)
**Date:** 2026-09-12
**Verdict: PASS** (2 non-blocking advisories; no defects, no required corrections)

## Frozen identity (verified)

- Producer commits: **1b2495da** (implementation + tests, 5 files, +789/-4) + **ee05ad17** (report only). Base **6919dab2** confirmed as the parent of 1b2495da.
- HEAD **f4f01352**. Commits after the producer material (44048007, 574298e8, f4f01352) touch **only** `project-control/state.json`, `project-control/tasks/M5-T01{7,8}.json`, and `project-control/reports/M5-T017-*` + `M5-T018-{ci-evidence,evidence-map}` — **none** touch the task's `allowed_paths`. `git diff 1b2495da HEAD` over `scenario-contract.ts` + `components/compare/**` is empty: my working-tree reads are byte-identical to the frozen commit.
- Scope containment: exactly 5 files changed, all inside `allowed_paths` (`apps/web/src/lib/scenario-contract.ts`, `apps/web/src/components/compare/**`). No forbidden path touched (`packages/contracts/**`, `services/api/**`, `scenario-api.ts`, `scenario-bounds.ts`, `package*.json` all untouched).
- CI containment: 1b2495da → ancestor of CI head **44048007** → ancestor of HEAD. CI evidence (`M5-T018-ci-evidence.txt`) verified against source claims.

## Mirror fidelity verdict — BOTH directions: PASS

Verified field-by-field against `packages/contracts/schemas/v1/scenario.schema.json` `$defs/unused_draft_zoning_floor_area` + `$defs/unused_floor_area_inputs` (read-only), and cross-checked against the actual server output shapes in `services/api/app/scenario/unused_floor_area.py` + `constants.py`. The producer's fidelity table is accurate.

**Looser direction (does it accept undocumented content?) — NO holes:**
- `checkNoUnknownKeys` fires at every object level: section (11 keys), `inputs` (2), `inputs.draft_zoning_floor_area_cap` (3), `inputs.existing_building_floor_area` (5), and each `assumptions[]` record (5) — matching every `additionalProperties:false` in the schema.
- Every schema-required key is effectively required: a missing key fails its own type check (`checkNullableFiniteNumber`/`checkNullableString`/`checkNonEmptyString`/`checkBoolean`/`isRecord`/`checkBoundedArray` all reject `undefined`). Verified for all 11 section keys, both inputs sub-objects, and their fields.
- Enums pinned exactly: `state` (computed/over_built/not_computable) and `not_computable_reason` (3 typed reasons or null), both guarded at compile time by the two new `MutuallyEqual` rows in `ScenarioEnumAssertions` so the runtime arrays cannot drift from the generated union.
- Pre-existing top-level unknown-key rejection preserved (the section key was **added** to `SCENARIO_KEYS`; nothing removed) — S1's "still rejects undocumented TOP-LEVEL key" corroborates.

**Stricter direction (does it reject anything the server can legally emit?) — NO false rejections.** Traced every branch of `build_unused_floor_area_section` against the mirror:
- `value` exactly 0 (computed) → accepted (`checkNullableFiniteNumber`); negative (over_built) → accepted (explicitly, never clamped); null (not_computable) → accepted.
- `unit`/`coverage_status` non-null string while `value_sq_ft` is null (the `inputs_missing` shape on not_computable) → accepted (fields are independently nullable).
- `provenance` object present even when `value_sq_ft` is null (server resolves existing-area provenance regardless) → accepted (`checkNullableObject`, arrays excluded via `isRecord`).
- empty `assumptions` on not_computable, and `[zoning_lot_extent_assumption()]` on computed/over_built → accepted. Verified the server assumption's `key` ("zoning_lot_extent") is token-representable so it survives `checkTokenRepresentable`, and its string `value` passes `checkRequiredScalar`.
- All **four shared M5-T017 fixtures** (preliminary + 3 no_scenario/unsupported, all not_computable) inspected directly and traced clean through the mirror; preliminary cap = 15000.0 confirmed.
- Note (correct design, not a defect): the mirror deliberately imposes **no cross-field state coupling** beyond the schema (the JSON Schema declares `value` as number-or-null on every state). This is the right mirror behavior — re-imposing the server's honest coupling would risk rejecting a legal document; that coupling is the server's responsibility (M5-T017). The finite-number check is stricter than the bare schema `number`, but is defense-in-depth against a caller-constructed NaN/Inf and cannot reject a JSON-transported body (JSON.parse never yields non-finite).

## Findings by review item

1. **Diff shape / scope** — PASS. 5 files, all in scope; frozen identity intact.
2. **Mirror fidelity** — PASS both directions (above).
3. **`checkAssumptions(arrayPath,…)` refactor** — PASS. Diffed against base 6919dab2: root call now passes `"assumptions"`, reproducing the old hardcoded prefix exactly. Emitted paths (`assumptions`, `assumptions[i].key`, …) are byte-identical; the only new caller is the section (`unused_draft_zoning_floor_area.assumptions`). Existing assumptions tests need no change.
4. **`UnusedFloorAreaSection.tsx` render** — PASS. `boundedText` applied at read to `label`, `scope_note`, and `over_built_statement` (consistent with the module's "bounded where it is read" doctrine, with `scenario-bounds.ts` correctly left out of scope). Three honest states render; negative value shown via `formatValue` (minus sign, never clamped/hidden/restyled); review notice is TEXT in a `role="status"` region (not color-only); not_computable → "No supported estimate" + plain-language gloss derived from the typed enum, no number, no invented zero. Forbidden-vocabulary scan of all 5 files is clean — the only "verified" hits are honest negations (`not_verified_disclaimer`, "(DRAFT — … not Verified)"); no "maximum buildable area"/"remaining development rights"/"remaining capacity"/"compliant".
5. **`ScenarioResult.tsx` mount** — PASS. `<UnusedFloorAreaSection document={document} />` mounted once at line 267, outside the `isPreliminary` conditional → present on every branch. `CompareScreen` renders `ScenarioResult` only on the validated `outcome.kind === "scenario"`, so direct `section.label` access is safe.
6. **Tests S1–S4 / no weakening** — PASS. Values are fixture-derived (S2 `=== 5000` from cap 15000 − 10000; S3 `=== -5000` from 15000 − 20000; DOM literals "5,000"/"-5,000" catch formatter regressions). Local variants built to the schema; shared `packages/contracts/` fixtures untouched. Confirmed via `git diff --name-only` that the three previously-red suites (`compare-screen`, `compare-entry`, `scenario-contract.test`) were **not edited** — red→green is achieved purely by teaching the validator the required key + structural checks, with the M5-T017 fixtures already carrying the section.
7. **TypeScript soundness** — PASS by reading; `MutuallyEqual` exhaustiveness proofs cover both new enums, `NOT_COMPUTABLE_PLAIN_LANGUAGE` is exhaustive over the non-null reason union, narrowing in `NotComputableLine` is sound. Corroborated by the green CI `web` (tsc/eslint/build) job.

## Directive D-041-R001 (web half) — satisfied

Independently re-derived from `project-control/directives/D-041-c1-development-rights/requirements.json`. Applicability is a conjunction over D-041-BOOTSTRAP + M5-T017 (server) + M5-T018 (web); the web half's obligations are all met: own clearly-labeled line (document's precise-noun label, verbatim, avoiding the forbidden marketing noun the requirement text itself uses); draft-label discipline present; over-built renders honestly + routes to professional review, never clamped/hidden; missing inputs → standard no-supported-estimate treatment, never estimated; required four-case harness present (normal remainder S2, over-built negative S3, missing existing-area fact + missing/unsupported FAR rule S4), fixture-derived. (The formal `verification.json` pass is the independent directive-compliance-verifier's to record; this is the G1 corroboration.)

## Modularity — PASS

`python tools/modularity_check.py --check` → `selected 395 files; failures 0; warnings 16`, EXIT 0 (independently reproduced). `scenario-contract.ts` (787 physical lines, heavily docstring-commented; SLOC below the warn threshold) is **not** among the 16 warnings; the new `UnusedFloorAreaSection.tsx` (204 lines) is a focused, single-responsibility module. Growth is cohesive with the file's single responsibility (scenario vocabulary + mirror validator).

## CI evidence (corroborated)

Run 34690484292 at head 44048007: `web` SUCCESS (tsc/eslint/build), `web-e2e` SUCCESS (vitest + Playwright), plus api/contracts/control-plane/modularity all SUCCESS. Only failing job is `web-dependency-security` — the owner-gated Next.js item (D-040-R004), unrelated to this task. Red→green shape confirmed: same three suites red at 08bf965b (validator rejected the new required key), green at 44048007, with the new `unused-floor-area.test.tsx` S1–S4 pack green in the same job.

## Defects / required corrections

**None.**

## Advisories (non-blocking; no action required for this gate)

- **A1 (module boundary, scope-driven):** two generic vocabulary-free primitives (`checkNullableFiniteNumber`, `checkNullableObject`) were added to `scenario-contract.ts` rather than the generic `scenario-contract-checks.ts`, because the latter is outside `allowed_paths`. Justified and disclosed; a future task could hoist them into the checks module.
- **A2 (test hardening):** S2's `label`/`scope_note` assertions (`textContent === section.<field>`) rely on `boundedText` being a no-op on the current single-line fixture strings (holds today). No explicit test for the computed **exact-zero** remainder path, though the logic handles it. Optional future hardening.

**VERDICT: PASS.**

Relevant absolute paths:
- `C:\Users\MLFLL\Downloads\nyc-zoning\ctl24\apps\web\src\lib\scenario-contract.ts`
- `C:\Users\MLFLL\Downloads\nyc-zoning\ctl24\apps\web\src\components\compare\UnusedFloorAreaSection.tsx`
- `C:\Users\MLFLL\Downloads\nyc-zoning\ctl24\apps\web\src\components\compare\ScenarioResult.tsx`
- `C:\Users\MLFLL\Downloads\nyc-zoning\ctl24\apps\web\src\components\compare\__tests__\scenario-fixtures.ts`
- `C:\Users\MLFLL\Downloads\nyc-zoning\ctl24\apps\web\src\components\compare\__tests__\unused-floor-area.test.tsx`
- `C:\Users\MLFLL\Downloads\nyc-zoning\ctl24\packages\contracts\schemas\v1\scenario.schema.json` (reference)
- `C:\Users\MLFLL\Downloads\nyc-zoning\ctl24\services\api\app\scenario\unused_floor_area.py` (reference)
