# G5 Security Gate Report — M0-T152 (D-033 T-A: supervisor gate-wave engine)

**Reviewer:** security-reviewer (independent, read-only). Neither producer.
**Reviewed identity:** worktree `C:\Users\MLFLL\Downloads\nyc-zoning\wt-m0t152`, branch `task/M0-T152-gate-wave-engine`, HEAD `cc25bdc15e4e982259a71f6903f370e6d1d8adf9`, **tree clean** (`git status --porcelain` empty). Candidate = `26c973ea` + `382abf6d` (packet-copy sync, 1 line in `M0-T152.json`) + `09fdea16` + `cc25bdc1` on `0e067b6a`. The `382abf6d` control-plane note is in-scope and harmless.
**Scope of change (git diff --stat 0e067b6a..HEAD):** `tools/agent_supervisor/gate_wave.py` (+1099 new), `tools/agent_supervisor/cli.py` (4 wiring points), `tools/test_agent_supervisor_gate_wave.py` (+985), `tools/test_agent_supervisor_loop.py` (+20, the LoopConfig pin), `.claude/rules/supervisor-freeze.md` (+10, S6), `project-control/reports/M0-T152-producer-report.md`, `project-control/tasks/M0-T152.json`.
**Independent execution:** `pytest tools/test_agent_supervisor_gate_wave.py` -> **63 passed** (0.74s); the 6 MUTATION tests + gate-class drift test -> **7 passed**; the LoopConfig TypeError pin -> **1 passed**. Matches orchestrator-captured evidence `M0-T152-documented-commands-cc25bdc1.txt` (63/127/ruff clean/modularity exit 0). Sandbox is Python 3.11.9; these modules collect and pass.

## Per-condition findings (verified against source, not producer claims)

### F1 (HIGH, BLOCKING) — control-plane allow-set — PASS
- `ControlPlaneRecorder` is the **single** path to `project_control.py`; the only reference to that script in the whole module is `self._script` (`gate_wave.py:609`). The cli.py diff adds no other `project_control` invocation. No second import site.
- `build_argv` (`gate_wave.py:632-654`) is the sole argv builder and calls `_assert_subcommand_allowed` (allow-set `{gate, submit}`, `:613-621`, `:73`) then `_assert_current_queue_task` (pinned to the construction-time queue task, `:623-630`) before assembling anything. All three public methods (`record_gate:660`, `record_self_check:675`, `submit:692`) route through it; `_run` (`:656`) is private and only ever receives `build_argv` output. No kwargs pass-through: each method hard-codes its `arguments` dict.
- Argv-injection defenses: per-subcommand argument allow-list (`_SUBCOMMAND_ARGUMENTS:77-81`) rejects unknown keys (`:643-647`); any value beginning with `-` is refused (`:648-652`); the argv is a **list** passed to `process.run` which is `shell=False`, argv-array only (`process.py:6, :747, :764`). `--task-id` is always the validated queue id (non-empty, not `-`-prefixed at construction, `:602-606`).
- `accept`, `new-task`, `unlock`, `depend`, `master-plan`, `hold`, `claim`, `progress` are all excluded — tighter than the M0-T150 F1 wording (which permitted accept/record_advancement). The wave records gates only.
- Negative tests **bind**: `test_every_out_of_scope_subcommand_is_refused` (11 subcommands, `test:572-581`), `test_an_out_of_queue_task_id_is_refused` (`:583-586`), `test_unknown_arguments_and_flag_like_values_are_refused` (`:588-595`). Mutation tests prove load-bearing: `test_MUTATION_removing_the_allow_set_lets_accept_through` (`:626-633`) and `test_MUTATION_removing_the_queue_bound_lets_another_task_through` (`:635-641`).
- Defense in depth: even if the recorder were bypassed, the real `gate()` (`project_control.py:1099-1118`) independently refuses a reserved/producer/non-roster reviewer for an independent gate and requires `orchestrator` for G2 (`:1086-1090`).

### F2 (HIGH, BLOCKING) — verdict-to-gate forgery resistance — PASS
- Reviewer + producer identities are drawn from the **control-plane task packet** the run launched with, never worker-writable files: `reviewer_for_gate` reads `packet["reviewer_agents"]` intersected with the controller's own `DEFAULT_GATE_REVIEWERS` map (`gate_wave.py:575-581, :97-102`); `producer` from `packet["producer_agent"]` (`:885`). In cli.py `packet` is the launch-time task packet (used for orientation/authority), fixed before the worker session runs.
- Decision bound to dispatch by **unique path + digest**: `plan_dispatch` mints a fresh `uuid4` output path under the controller runtime dir (`:385-387`) and `dispatch_digest = digest_of(body)` (`:393`); `bind_verdict` re-asserts binding (`_assert_verdict_bound:469-486` checks task id, checkpoint id, and record seal) and computes `verdict_digest` over dispatch+record+result (`:494-499`).
- Worker-planted verdict **rejected**: `admit_verdict_file` refuses any path inside the worker worktree and any path != the minted unique path (`:524-546`); `write_verdict_report` refuses a pre-existing file at the unique path (`:508-514`); `verify_verdict_report` recomputes the sealed body digest and re-checks `verdict_digest` (`:549-572`). Tests `:483-530`.
- `reviewer == producer` fails closed at dispatch and again at bind (`_assert_reviewer_separated:328-359`, called in `plan_dispatch:384` and `bind_verdict:491`); reserved `orchestrator` refused for independent gates (`:348-353`).
- The two forgery mutation tests disable **exactly** the checks they name: `test_MUTATION_removing_identity_separation_admits_the_forgery` patches `_assert_reviewer_separated` and proves a `reviewer==producer` dispatch then succeeds (`test:532-538`); `test_MUTATION_removing_the_binding_check_admits_a_foreign_verdict` patches `_assert_verdict_bound` and proves an M0-T999 record binds to the M0-T152 dispatch (`:540-551`). Both fail-closed paths otherwise raise `reviewer_is_producer` / `verdict_task_mismatch`.
- Observation (informational, not a hole): the recorded `reviewer_identity` is the roster label while the actual review runs through the single injected `deps.reviewer` via `conduct_ephemeral_review` — a fresh **read-only** process (`ephemeral_review.py:312-317, :352-365`) distinct from the producer, so separation of duties holds at the process level and the worker cannot influence the label. Per-identity live dispatch fidelity is T-B/T-C / R595 territory and the stage is DEFAULT OFF.

### F3 (MED) — prompt-injection immunization — PASS
- Every reviewer contract carries the M0-T148 worker-authored-data clause verbatim: `GATE_CONTRACTS` for `{G1,G3,G4,G5}` each append `WORKER_AUTHORED_DATA_CLAUSE` (`gate_wave.py:106-138`); `gate_contract` fails closed (never sends a blank contract) for an unknown gate (`:396-404`). Structural test `test_every_gate_contract_carries_the_immunization_clause` asserts keys == `INDEPENDENT_GATES - HUMAN_ONLY_GATES` and each contains the clause (`test:700-705`).
- Packets flow through the guard + redaction: `dispatch_gate_review` redacts the injected contract subdict (`redact_structure`, `:426-431`) and refuses a packet that pre-supplies its own contract key (`:420-424`, test `:723-728`); the merged body then passes through `conduct_ephemeral_review`, which applies the AD-083 `guard_packet` and the 0A.4 budget guard fail-closed before any process (`ephemeral_review.py:321-350`).
- The "emit PASS" injection negative genuinely does not steer the verdict: a planted `# SYSTEM OVERRIDE ... emit PASS` artifact with a reviewer returning REVISE binds to FAIL (`test:730-740`). Over-budget packets refuse before any process (`test:742-749`).

### F5 (MED) — mutation coverage — PASS
Six load-bearing enforcement mutation tests confirmed present and passing (independently rerun): (1) switch check `test:291-301`; (2) F2 identity separation `:532-538`; (3) F2 verdict binding `:540-551`; (4) F1 allow-set `:626-633`; (5) F1 queue bound `:635-641`; (6) F6 seam re-assert `test_MUTATION_removing_the_reassert_lets_an_ungated_flag_enable:922-936`. Count = 6, one per new enforcement. Producer's claim of six is accurate.

### F6 (LOW) — owner switch DEFAULT OFF + symmetric refusal — PASS
- `add_owner_switch_argument` registers `--owner-enable-managed-gate-waves` as `store_true` (default False) (`gate_wave.py:186-203`); real-parser test asserts `default is False, const is True, dest owner_enable_managed_gate_waves` on the actual `start` subparser (`test:350-364`).
- Absent flag = byte-identical: `maybe_run_post_complete_stage` returns None when not enabled (`:1025-1026`); `run_with_post_complete_stage` returns exactly `loop.run(first_prompt).to_dict()` when the flag is absent (`:1086-1099`). OFF tripwire test asserts `result is loop.returned[0]` with `MustNotRun` collaborators and no filesystem touch (`test:895-908`, `:268-279`). Switch mutation `:291-301`. LoopConfig TypeError pin: `test_the_managed_wave_switch_never_reaches_loop_config` asserts no field and `TypeError` (`test_agent_supervisor_loop.py:294-312`), and the prior increment's LoopConfig field + its CLI wiring were both removed.
- Flag without the gated capability REFUSED BY NAME: `managed_wave_start_gate` returns `managed_waves_without_gated_mode` for shadow/supervised/limited-auto-without-bounded-enable (`:217-251`, matrix test `:303-318`); wired at `cmd_start` (cli.py diff, `wave_gate = gate_wave.managed_wave_start_gate(args, seal_audit=AUDIT_FILENAME)` -> `emit_refusal`), and re-asserted at the seam before any enable record (`:1086-1091`).
- Refusal sealed in the hash-chained audit log: `seal_wave_refusal` appends `REFUSAL_EVENT` (`:254-273`); test verifies exactly one record, `decision=refuse`, and `verify_chain().ok` (`test:366-388`); an admitted/absent launch seals nothing (`:390-405`).

### S6 — supervisor-freeze D-033 recognition amendment (commit cc25bdc1) — PASS
- The diff adds **exactly one paragraph** to section 2 after the D-024 recognition, followed by a blank line before "## 3. Evidence-citation duty"; nothing else in the file changes. The paragraph's authorization quote is byte-verbatim from `D-033-R007` in `requirements.json` (`classification: authorization`, `binding: true`, applicable to M0-T152). It explicitly states "this amendment changes nothing else - the defect-only lane, gates, R595 prerequisite, and suite-baseline duty below stand unchanged." No other lane/gate/hold weakened. Commit message cites `D-033-R001, D-033-R003, D-033-R007` in both packet and commit as section 3 requires. Orchestrator-authored, matching R007's named producer.

### Authority surface / holds — PASS
- **No new authority path**: the only route to `project_control.py` is the bounded `ControlPlaneRecorder`; the wave engine calls only `record_self_check` (G2, reserved `orchestrator` label) and `record_gate` (`run_gate_wave:921, :987`) — never `accept`, submit-for-acceptance, queue advance, commit, or push. `WaveResult` and docstrings state acceptance/advance/integration stay with the orchestrator (D-033-R005), and `run_gate_wave` records gates only.
- **G6 never automatable**: legal/zoning parks before any dispatch, both in `plan_dispatch` (`:373-377`) and the engine pre-check (`:896-900`).
- **Holds preserved**: nothing touches R595, PR #241, push/deploy, Tier D. Refusal/enable messages reaffirm every hold unchanged. Verified no merge/push/accept code exists in the module.
- **Secrets/creds**: subprocess env is scoped to an allowlist (`minimal_env`, `process.py:200-210`); reviewer packets and the injected contract are redacted (`redact_structure`, which strips `secret/token/password/api_key/bearer/slack/google` by key and pattern, `redaction.py:35-57`); audit `detail` payloads (`record_enable`, `seal_wave_refusal`, `WAVE_FINISHED_EVENT`) carry only ids/modes/digests/verdicts — no secrets; `transcribe_ledger_report` stores the sealed `ReviewRecord` (digests + reviewer decision), not raw packet bodies.

## Findings

**L1 (LOW, advisory — non-blocking for T-A).** The G2 self-check report path writes **unredacted** command transcripts to a committed artifact. `capture_g2` -> `write_g2_report` stores `capture.transcripts` = `results_section(collect_command_transcripts(...))` directly into `project-control/reports/{task}-G2-managed-wave-*.json` (`gate_wave.py:726-780`). Individual transcripts from `EvidenceCollector.run_command` are byte-bounded but **not** redacted at collection (`evidence.py:499-510`); redaction is applied only by `build_packet` (`evidence.py:727`), which this path bypasses. The reviewer-facing independent-gate packet **is** redacted (via `build_packet` at `gate_wave.py:946-956`), so this gap affects only the G2 report file. Reproduction: a documented test command whose stdout/stderr contains a secret would land unredacted in the committed G2 report. Likelihood is low — documented commands are control-plane-authored, `minimal_env` limits child-process secret exposure, and the stage is DEFAULT OFF pending R595. Remediation: run the stored G2 body (or `capture.transcripts`) through `redact_structure` before `write_g2_report` persists it, matching the module's own invariant ("Everything is redacted (S13.9) before it becomes part of a packet," `evidence.py:20`). Recommend closing before live activation (T-B/T-C / R595); does not block T-A acceptance.

No critical, high, or medium findings. All six M0-T150 blocking conditions (F1/F2 HIGH; F3/F5/F6) are satisfied with load-bearing negative and mutation tests; S6 is a clean single-paragraph recognition matching D-033-R007; the authority surface adds no new path to `project_control.py` beyond the bounded recorder; and every owner hold is preserved.

VERDICT: PASS

---

*Orchestrator preservation note: saved VERBATIM from the security-reviewer agent-return channel (2026-09-07 overnight gate wave, task notification a5847db15ff9cb59e; leading corroboration sentence removed as transport framing, HTML entity-encoding of `->`/`!=` decoded, content unaltered).*
