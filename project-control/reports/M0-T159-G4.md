# M0-T159 — G4 independent test-adequacy review (qa-engineer "qa-m0", read-only)

> Transmission history: pinned at 4de4f5ec (HEAD advanced to a57bb8de during the review via four disjoint
> commits; all 12 material blobs identical at a3f24ff3, cfc3d22c and both heads), delivered as four
> SendMessage parts (1/4..4/4) ending with END-OF-REPORT; no truncation. Joined verbatim by the
> orchestrator (transport wrapper tags removed only). A separate follow-up message from the same reviewer
> (agent-memory notes it could not write itself) is NOT part of this report. Verdict: PASS (advisories
> A1-A7, all pre-existing).

---

M0-T159 G4 (test adequacy) report from qa-m0, part 1 of 4. Read-only review.

PIN: ctl24 HEAD at start was 4de4f5ec. During the review it moved to a57bb8de through four disjoint commits (f4b4ef28, 5e9dbd07, 114e5e56, a57bb8de). None of them touched an M0-T159 path. All 12 material files have the same blob ids at a3f24ff3 (wt-m0t159), cfc3d22c and both HEADs. wt-m0t159 was clean before and after every run.

Method: I mutated in memory with a pytest plugin. It re-points the consuming module's pointer, or swaps in a stand-in path object whose read_text returns mutated JSON. For the baseline I made a git-archive copy of a5886dab in my session scratchpad, outside the repo. I wrote nothing to either checkout and ran no supervisor verb.

(1) Teeth: point-back and wrong-version mutants
| ID | Mutation | Result |
|---|---|---|
| B0 | a5886dab copy, installed 2.1.281, four packs | 3 failed, exactly the capability, S8 and native live teeth; 147 passed |
| C0 | candidate, four packs | 150 passed, 0 skipped (wt-m0t159 AND ctl24) |
| M1 | capability CURRENT_FIXTURE pointed back at d032 (2.1.252) | KILLED: live tooth + repurposed test |
| M2 | capability fixture version set to 2.1.280 | KILLED (same 2) |
| M3 | event_drift pointer pointed back at 2_1_252 | KILLED: S8 live + S8 valid_and_masked. Reproduces the producer's focused proof |
| M4 | catalog version set to 2.1.282 | KILLED (same 2) |
| M5 | DETECTION_FIXTURE pointed back at m0t132 | KILLED: live + shape |
| M6 | detection version set to "2.1.28" (prefix trap) | KILLED (same 2) |
| M7 | routing FIXTURE_PATH pointed back at m0t132 | KILLED: versioned-shape + package-fixture-name tests (not a live tooth) |
| M7b | routing version set to "2.1.28" | KILLED: shape test |

Content-strength mutants:
| M10 | catalog drops PostModelSwitch | KILLED (len 32 + drift) |
| M11 | Setup renamed to a new event (still 33) | KILLED (drift) |
| M12 | Setup replaced by a duplicate SessionStart (33 entries) | KILLED (drift: removed Setup) |
| M13 | routing verdict shell_observed | KILLED |
| M14 | routing measured=false | KILLED |
| M16b | detection verb respawn set to unsupported | KILLED |

Survivors. All come from test structure that predates this task; see part 3.
| M9 | event_bus test CATALOG_FIXTURE left at 2_1_252 | SURVIVED 38/38 |
| M15 | one claude_flags entry flipped in the current capability fixture | SURVIVED 19/19 |
| M16 | detection flag --worktree set to unsupported | SURVIVED 58/58 |
| M17 | wrong routing cli_identity on disk (fixture + dir loader) | SURVIVED 35/35 in the pack; the runtime gate refuses it (part 2) |

---

M0-T159 G4 report part 2 of 4: the repurposed test, the fixture checks, and the runtime gate.

(2) Repurposed capability test: compared with a5886dab, the assertion shape is the same. It has 3 version equalities (current claude now 2.1.281, current codex 0.153.4, post codex 0.146.0), the filename token (now "m0t159"), the 4-token leak loop, and the check that binaries are non-empty and start with [HOME]. This is the same in-place pattern as the M0-T132 and D-032 repurposes (checked in git log). Strength is kept, and M1/M2 show it now bites on the claude line. One side effect, consistent with precedent: no test asserts the retired d032 capability, m0t132 detection or 2_1_252 catalog fixtures any more.

(3) Do the fixtures encode what the tests assert? I re-derived each one independently:
- Catalog: 33 entries, all 33 unique. The set and the order match 2_1_252 exactly. catalog_drift against KNOWN_HOOK_EVENTS (31 events) gives +PostModelSwitch and +PreModelSwitch, none removed, which equals the recorded drift.
- Docs, re-fetched by me today: the HTML returns 200 at 2,900,678 B, the same byte count the producer recorded. hooks.md has 37 PascalCase headings: the 33 catalog events plus Configuration, Timeouts, Limitations and Disclaimer. Every event has a heading. The only other event-shaped tokens are CronCreate and TaskCreate, and in context both are tool names.
- Capability: the live cp.build_record() body equals the committed body exactly. Against d032, the body differs only in the claude_version line and its sha, and the claude_help sha.
- Detection: a live detect_native_capabilities() run rebuilds a fixture exactly equal to the committed one. Against m0t132, only the version and task differ.
- Routing: native 3, shell 0, other 0, total 3; verdict native_preferred; 3 provider calls against a ceiling of 3; no worker write. The stream matches the per-assignment tool uses.
- Routing digest: I recomputed executable_identity on the installed claude.exe and it matches cli_identity 92af5806... The m0t132 digest e713c5a6... matches the retired claude.exe.old. The two builds' heads differ at byte 126, so the head+size digest tells them apart.
- event_drift.py: its LF sha256 e85c775c... matches the recert record.
- Manifest: it covers 146 package files at both base and candidate, plus config.toml, for 147. Fixtures are not covered.

Runtime R295 gate, called in memory with the live digest:
- real fixtures dir: PASS
- 2.1.281 record removed: routing_evidence_stale
- wrong digest on disk: stale
- version 2.1.282: stale
- unknown digest: stale
- retired 2.1.252 digest: PASS (append-only history)

So a CLI bump is caught at PREFLIGHT; B-026 was exactly that event.

---

M0-T159 G4 report part 3 of 4: count reconciliation and findings A1-A4.

(4) Count reconciliation: I ran pytest --collect-only once on each tree. The a5886dab copy collects 3664 and the candidate collects 3664. The node-set diff is exactly one rename: test_current_fixture_records_codex_0_153_4_masked_and_shaped became test_current_fixture_records_claude_2_1_281_masked_and_shaped. Nothing was removed or added. The arithmetic holds: 3659 + 3 + 2 = 3664, and 3662 + 2 = 3664. B0 confirms the 3 baseline failures are exactly the three teeth, and C0 shows the teeth run (0 skips) at the candidate. I did not rerun the whole suite, as instructed; the recorded run is consistent with all of the above. The golden pack at the candidate: 42 passed in 150 s, matching the recorded 42.

FINDINGS. BLOCKING: none.
ADVISORY. All of these predate this task; M0-T159 neither introduced nor widened any of them. I suggest one follow-up task, done before the next CLI bump.

A1. The routing pack has no live tooth. test_the_committed_package_fixture_matches_the_installed_version passes the literal "2.1.281". test_the_committed_fixture_passes_for_the_real_claude_digest reads the digest from the fixture itself, so the "real installed digest" claim in its docstring is never tested. That is why M17 survives; only the runtime gate catches a wrong or stale digest. Fix: add a requires_claude test that passes executable_identity(shutil.which("claude")).digest to probe_shell_routing_evidence.

A2. R295 ignores the verdict. I checked in memory: a measured shell_observed record for the pinned digest PASSES. The pack pins native_preferred for one FIXTURE_PATH only. The stock "routing_probe --out" writes shell_observed fixtures too; only the one-off capture scripts filter them out. So a future admission could add a shell_observed fixture and stay green in both the tests and the runtime gate. Fix: assert that every measured shell_routing_*.json is native_preferred, or make R295 check the verdict.

A3. Nothing tests the flag bodies of the current fixtures (M15, M16). The flag invariants bind to the 2.1.220 LIVE_FIXTURE, and the live teeth compare only the version line. A future admission could commit a regressed flag surface with every test green. Fix: a requires_claude test that the live body equals the CURRENT fixture (this holds today).

A4. There are two catalog pointers (M9). The event_bus CATALOG_FIXTURE is independent of ed.CATALOG_FIXTURE_PATH. If the test pointer lags, the masking checks read the old file and nothing turns red. Fix: assert the two are equal.

---

M0-T159 G4 report part 4 of 4: findings A5-A7 and the verdict.

A5. Skip when claude is absent. Four tests are claude-gated: the three teeth plus the live agents listing. They all skip when claude is not on PATH, which is always the case in CI. A recert run from such a shell would report 0 failed with 6 skipped. Today's evidence is sound (the four packs showed 0 skipped), but the only guard is count arithmetic, not a check. Suggest the recert record name the three teeth as PASSED.

A6. The teeth use whichever claude is first on PATH, while the controller launches its configured absolute path. They agree today. probe_meta.claude_binaries went from 3 entries (including two npm shims) to 1. The producer's "only X moved" diff table leaves this out; that is a disclosure nit only.

A7 (for G5). The R295 evidence directory, tools/agent_supervisor/fixtures, falls outside the manifest's COVERED_PATTERNS. A routing fixture dropped into the installed controller after certification would satisfy R295 and verify-controller would not see it. The C10 pinned identity plus the owner repin still guard a CLI bump.

Scratch: one git-archive copy of a5886dab in my session scratchpad (outside the repo), used for B0 and the collection count. Nothing else was written.

M0-T159 G4 VERDICT: PASS

END-OF-REPORT
