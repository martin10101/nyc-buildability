# M5-T042 — G2 producer self-check record (recorded by the orchestrator)

G2 is the producer self-check gate; the CLI records it under the orchestrator's name (it
rejects the producer's own label). Basis, 2026-09-18 seq-118:

1. **Producer self-checks (loop-2 run persistent2-local-07, worker claude-opus-4-8):** the
   producer report documents all four documented commands recaptured in-worktree with exact
   cwd discipline — ruff "All checks passed!", 26 passed, 852 passed connectors-wide,
   modularity failures 0 with the dtm_condo_soda.py warn-tier signal disclosed — and an
   honest working-tree-vs-committed-head evidence distinction plus two honest deferrals
   (live-era C-rows; official null-billing fixture routed to the orchestrator).
2. **Orchestrator reproduction at harvest (in wt-m5t042):** the same four commands re-run
   independently green before the material commit (ruff clean; 26 passed; 852 passed;
   modularity exit 0 from the repo root).
3. **One [ORCH-CORRECTED] repair at harvest:** an inline gitleaks:allow marker on the
   fixture-alias line (scanner false positive on a variable name; comment-only; suite
   re-run green before commit) — disclosed in the material commit message and CI evidence.
4. **CI at the harvest head `c87aacbf`:** run 35407815825 success (all jobs), secret-scan
   35407815581 success, context-budget 35407815610 success (orchestrator-captured:
   `M5-T042-ci-evidence.md`); product-path diff from material 0a6c43c9 to that head empty.
5. **Scope check at harvest:** the material diff = exactly the 3 allowed_paths files; zero
   consumer imports (leaf confirmed).

**G2 verdict: PASS.**
