# M5-T042 — G1 data-contract review (verbatim reviewer return; transport entity-decoding only)

Saved by the orchestrator from the data-contract-verifier agent return, 2026-09-18 (seq-118
wave, pinned at submission sha 3bd6e304). Nothing added or removed beyond decoding transport
HTML entities.

Orchestrator ruling on A3 (recorded here for the acceptance record): the official
null-billing fixture is DISPOSED as a live-era C-class row binding the FUTURE wiring packet
(no one-shot network capture now); the module's labeled-synthetic coverage of the code path
stands as honest. A2's gating condition (source_registry record before any live traffic)
also binds the wiring packet's contract.

---

All checks complete and independently reproduced. Verdict below — return this verbatim as the G1 gate report.

---

# G1 Data-Contract Gate Report — M5-T042

**Task:** M5-T042 — DTM condo billing-BBL → base-lot-set resolver (MODULE ONLY)
**Reviewer:** data-contract-verifier (independent; read-only)
**Reviewed identity:** submission `3bd6e304` (branch `candidate/D-024-mrl-option-b`); material `0a6c43c9`; current HEAD `08d4d201`
**Sole source:** `docs/research/condo-base-lot-resolution-sources.md` (DB-002, retrieved 2026-09-18)
**Date:** 2026-09-18

## VERDICT: PASS

The module implements the verified DB-002 research path faithfully. No BLOCKING findings. All five advisory items are disclosed by the producer and/or already routed to the orchestrator; none prevent acceptance of an offline, leaf, module-only packet.

## Identity / evidence provenance (verified)

- All three allowed-path files are **byte-identical** across submission `3bd6e304`, material `0a6c43c9`, and current HEAD `08d4d201` (`git diff --stat` empty for each pair); working tree clean. The only commit between submission and HEAD is the submit-at-evidence-head commit itself. Reviewing disk == reviewing the frozen submission.
- CI evidence (`project-control/reports/M5-T042-ci-evidence.md`) captures CI run `35407815825` **success** at head `c87aacbf`; I confirmed `git diff 0a6c43c9..c87aacbf` for the two source files is **empty**, so the green CI (26-test suite + 852 connectors-wide) speaks for the reviewed content identity. Import resolution (`Transport` alias, `request_with_retry`, etc. from `app/resilience/transport.py`) is proven by that green suite.

## Findings

**(1) Dataset identities + URL construction — PASS.** `CONDO_DATASET_ID="p8u6-a6it"`, `UNIT_DATASET_ID="eguu-7ie3"` (`dtm_condo_soda.py:104-105`) match research §2/§3.1/§4.1/§10 exactly. URL template `https://data.cityofnewyork.us/resource/{id}.json` (`:106-107`) plus query params `?condo_billing_bbl=`, `?unit_bbl=`, `?condo_key=` (`:598,635,667,742`) match the research request lines §3.3/§4.2 verbatim.

**(2) Schema-shape guards — PASS.** `CONDO_COLUMNS` (`:140-152`) is exactly the 9 fields of research §3.2; `UNIT_COLUMNS` (`:153-172`) is exactly the 16 fields of research §4.1 (7 base-side + 9 unit-side). I compared each frozenset member against the research field tables — no additions, omissions, or renames. `test_c10_column_inventories_match_research_counts` asserts the 9/16 counts (`test:310-312`); `test_c10_fixture_keys_within_declared_columns` asserts fixture keys ⊆ declared columns (`test:315-321`).

**(3) Fixtures byte-faithful; synthetics labeled — PASS.**
- `UNIT_3022642601_BODY` (`test:73-79`) is **character-perfect** vs research §4.2 line 99 (field order, values, keys).
- `BILLING_3022647515_BODY` (`test:63-70`) and `C3_UNIT_SPLIT_BODY` (`test:104-107`) match research §3.3/§4.3 in every key, value, and field order; the ONLY difference is the inter-object separator (`},{` in the test vs `},\n {` in the research markdown). That whitespace is a JSON-insignificant markdown line-wrap artifact — the test form is actually closer to the compact Socrata wire response. Non-defect (ADVISORY A4).
- `CONDO_KEY_301313_BODY` is an explicit alias of the billing body (`test:81-85`), a faithful derivation justified by two verified research facts (§3.3: both rows carry key 301313; §4.3: key 301313 → {32,33}); the `# gitleaks:allow` marker on `:85` is the disclosed [ORCH-CORRECTED] fixture-alias false-positive suppression, comment-only.
- Both synthetics (`DRIFT_DECIMAL_BODY` `test:90-98`; `SYNTHETIC_NULL_BILLING_CONDO_KEY_BODY` `test:109-125`) are clearly labeled SYNTHETIC and never presented as official.

**(4) §7 algorithm — PASS.** Classification ranges billing `(7501,7599)` / unit `(1001,6999)` / else NOT_A_CONDO (`:120-121,326-346`) match §7 step 1. Billing → SET of `condo_base_bbl`, never collapsed (`:597-632`, `_resolved` multi-lot note `:812-816`). Unit → base + `condo_key`, then `?condo_key=` expansion to the FULL set (`:634-702`); when no `condo_key` is on the unit row the incompleteness is surfaced, never guessed (`:693-698`). Empty billing response correctly cannot run step-4a (no key available) and returns honest UNRESOLVED (`:617-628`) — exactly §7's precondition for 4a. `resolve_by_condo_key` implements the step-4a null-billing fallback (`:705-797`). Honest `STATUS_UNRESOLVED`, never a fabricated lot, never an exception for well-formed input (`:832-849`). §7 step 6 boundary honored: NO zoning determination; `DIVERGENT_ZONING_NOTICE` (`:188-195`) carried on every result; `test_resolved_result_carries_divergent_zoning_notice_and_no_zoning` asserts no zoning fields exist (`test:414-421`).

**(5) BBL handling — PASS.** `^\d{10}$` enforced on input (`classify_lot` `:332`, plus `normalize_bbl` re-validates borough/block/lot ranges as defense-in-depth `:339-341`) and on every returned `condo_base_bbl` (`_collect_base_lots` `:488-494`). PLUTO decimal serialization `"….00000000"` is REJECTED as `SchemaDriftError` (`test_c7_decimal_serialization_rejected_fail_closed` `test:290-293`) and as malformed input (`test:388,396-398`). PLUTO `appbbl`/`64uk-42ks` is NEVER consulted — grep of the module finds it only in the docstring ban note (`:33`); `RoutedTransport` raises on any non-DTM URL and `test_c9_appbbl_independence` asserts no requested URL contains `64uk-42ks` or `appbbl` (`test:296-306`).

**(6) Provenance — PASS.** `_provenance_entry` records dataset_id, query_kind, request_url, retrieved_at, record_count, rows_updated_at (`:511-532`); results carry condo_number/condo_key (`:272-282`). `rowsUpdatedAt` is never asserted fresh: `resolve`/`resolve_by_condo_key` default `dataset_rows_updated_at=None`, so provenance `rows_updated_at` is `None` unless a caller injects live values. `RESEARCH_OBSERVED_ROWS_UPDATED_AT` (`:114-117`) is a module-level HINT only — not wired as a default into either public function; it is exercised solely by the explicit-injection test (`test:203-209`). NOT_A_CONDO carries empty provenance correctly (zero lookups, no material value to attribute).

**(7) Leaf status + registry deferral — PASS.** Grep of `services/api/app` for `dtm_condo_soda` finds only in-module self-references (logger name, log label) — **zero consumer imports**. The `source_registry` deferral is DISCLOSED at `:103` (`# source_registry record deferred to the wiring packet`) and in the report's "Deferred to the later wiring packet" section. Module-only-without-registry is acceptable here (M5-T039 module-only precedent; the leaf serves no live traffic) — see ADVISORY A2 for the gating condition on the wiring packet.

**Independent reproductions:** modularity_check `--check` → **exit 0**; `dtm_condo_soda.py` shows only a `review_signal` warn *"above the warning threshold"* (600 tier — notably NOT the 750 "justification threshold" tier that its sibling connectors carry), consistent with the report's SLOC claim despite 850 physical lines (docstring/comment-heavy). Schema-drift error code `"query.soql.no-such-column"` matches the accepted `pluto_soda.py`/`ztldb_soda.py` idiom. 26 tests confirmed by function/param count.

## Advisory findings (non-blocking)

- **A1 (ADVISORY):** §7 step-4b PLUTO `appbbl` cross-check *hint* is not implemented — intentional and consistent with the appbbl-source ban; disclosed in docstring (`:33-36`) and report. Belongs to the wiring packet.
- **A2 (ADVISORY):** `source_registry` record + live `/api/views` freshness fetch are deferred. Acceptable for this leaf module, but the FUTURE wiring packet MUST add the registry record before any live traffic (`.claude/rules/backend-api.md`: "No connector without a source_registry record"). Track as a gating condition on the wiring task.
- **A3 (ADVISORY / owner-decision, already routed):** the research embeds no byte-faithful raw response for a specific NULL-billing condo (only aggregate count 28, §3.4). Covered by the real `condo_key=301313` mechanism test plus a clearly-labeled synthetic null-billing scenario. The producer honestly routed this to the orchestrator (authorize a one-shot capture OR make an explicit contract disposition carrying the official fixture as a live-era C-class row). Orchestrator should record the disposition at/before acceptance; the module and tests are honest and not fabricated.
- **A4 (ADVISORY, informational):** fixture inter-object whitespace differs from the research markdown for §3.3/§4.3 (JSON-insignificant; test form matches the compact wire response). §4.2 is character-perfect.
- **A5 (ADVISORY, theoretical):** the returned-base-BBL guard `_STRICT_BBL_RE = ^\d{10}$` (`:178`) uses Unicode-aware `\d`; a Unicode-digit base BBL from a drifted source could pass the `_collect_base_lots` guard (`:488`). `classify_lot` is protected because it also calls ASCII-only `normalize_bbl`, but the base-BBL guard is regex-only. Purely theoretical for ASCII Socrata text fields and it matches the research's literal `^\d{10}$`; could tighten to `[0-9]{10}` in the wiring packet.

## Read-only commands run (exact)

- `git rev-parse HEAD` / `--abbrev-ref HEAD`; `git show --stat 0a6c43c9`; `git cat-file -t 3bd6e304` / `c87aacbf`
- `git diff --stat 3bd6e304 HEAD -- <3 allowed_paths>`; `git diff --stat 0a6c43c9 HEAD -- <3 files>`; `git diff --stat 0a6c43c9 c87aacbf -- <2 src files>`; `git status --porcelain -- <3 files>`; `git log --oneline 3bd6e304..HEAD`
- Grep `dtm_condo_soda` under `services/api/app`; grep `64uk-42ks|appbbl` in the module; grep `query.soql.no-such-column|SCHEMA_DRIFT_ERROR_CODE` across connectors; grep transport symbols in `app/resilience/transport.py`
- `python tools/modularity_check.py --check` (exit 0) and `--report | grep dtm_condo`
- Read: `project-control/tasks/M5-T042.json`, research doc, module, test, producer report, CI evidence, `app/connectors/bbl.py`

**Note:** I did not re-run the pytest suite locally (thin-client policy; heavy connectors suite). Per the evidence-capture division of labor I verified the orchestrator-captured CI evidence and confirmed by `git diff` that it pins the reviewed content identity — not a BLOCKED condition.

## Relevant absolute paths

- `C:\Users\MLFLL\Downloads\nyc-zoning\ctl24\services\api\app\connectors\dtm_condo_soda.py`
- `C:\Users\MLFLL\Downloads\nyc-zoning\ctl24\services\api\tests\connectors\test_dtm_condo_soda.py`
- `C:\Users\MLFLL\Downloads\nyc-zoning\ctl24\project-control\reports\M5-T042-producer-report.md`
- `C:\Users\MLFLL\Downloads\nyc-zoning\ctl24\project-control\reports\M5-T042-ci-evidence.md`
- `C:\Users\MLFLL\Downloads\nyc-zoning\ctl24\docs\research\condo-base-lot-resolution-sources.md`
- `C:\Users\MLFLL\Downloads\nyc-zoning\ctl24\project-control\tasks\M5-T042.json`
- `C:\Users\MLFLL\Downloads\nyc-zoning\ctl24\services\api\app\connectors\bbl.py`
