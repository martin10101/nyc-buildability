# M5-T053 G5 — independent security review (security-reviewer, saved VERBATIM from the agent return)

G5 SECURITY REVIEW — M5-T053 (proposal-validation route + DB-034(a)/(b) input gate) — VERDICT: PASS. Zero BLOCKING, one INFORMATIONAL. Reviewed at pinned HEAD 31cbc74b (verified). Material 25617bbd is an ancestor of HEAD; the 4 code files are byte-identical between the CI head (25617bbd, run 35454834946 green: 43 route + 497 api tests, secret-scan) and HEAD, so CI evidence covers the reviewed content exactly. Material touches ONLY the 5 allowed paths; proposal.py and all requirements/lock files untouched (git-confirmed).

## My two originating T048-G5 findings

FINDING 1 (aggregate quadratic cost) — CLOSED.
- The global vertex budget MAX_TOTAL_VERTICES=5000 is enforced in validate_proposed_massing_input BEFORE any delegation (proposal_input_gate.py:166-173). _total_position_count (:102-113) is O(1) per outline (bare len) walking levels once, examines no coordinate, runs no simplicity test — it precedes all O(n^2) work. The route calls ONLY the gate (proposal_validation.py:297), never the validator directly, so there is no quadratic path that bypasses the count.
- Worst-case arithmetic now admissible: each single outline is still capped at MAX_OUTLINE_VERTICES=1000 by the accepted B0 validator; the gate additionally caps the SUM at 5000. sum(m_i^2) under sum(m_i)<=5000 with each m_i<=1000 is maximised by the fewest largest outlines = 5 x 1000^2 = 5e6 segment tests (plus the base outline's own <=1e6). Firm sub-second bound, down from the ~2.5e8-5e8 the per-outline ceilings alone admitted (500 levels x 1000). The docstring arithmetic (:15-24) is correct.
- The specific "many levels each under the per-outline cap" attack is defeated and proven: test_many_levels_probe_refused_before_quadratic_work_gate_level (500 levels x 50 pos = 25005 summed) asserts the gate raises its OWN ProposedMassingInputError, i.e. it refused BEFORE delegating. The gate counts exactly the outlines the validator quadratic-tests (base + every level.outline); walls carry no outline. No uncounted quadratic surface.

FINDING 2 (unbounded strings / repr embedding) — CLOSED.
- MAX_STRING_LEN=512 applied to all four user-authored string fields: provenance.author, editor_version, parent_scenario_id, and every exterior_walls[].id (_check_string_ceilings, :129-152), each refusal naming the exact field.
- Bounded-repr on every embedded user value at TWO layers: the gate bounds the fields it owns via _bounded_repr (cap 80 + truncation marker naming true length, :80-88); the route bounds EVERY refusal via _bounded_message (cap 400, :120-129) applied to str(exc) at :303 — this backstops user values the accepted validator itself embeds via repr for fields the gate does not own (srid, kind, vertex, level fields). I confirmed the validator embeds {srid!r}/{vertex!r}/{kind!r}/{wall_id!r} unbounded (proposal.py:211,243,386,444), and all of those flow through _bounded_message before reaching the response. No user string is ever reflected unbounded in any response or log. Proven by test_attacker_length_wall_id_uses_bounded_repr (2000-char id: full value + z*100 absent, marker present, len<=512).

## Checklist findings (all non-blocking)

3. ROUTE SURFACE — PASS. Request-size ceiling before parse is two-layer and robust: declared Content-Length fast path (:243-250) PLUS authoritative bounded-streaming accumulation (_read_body_within_ceiling, :203-222) that refuses the instant the aggregate crosses MAX_BODY_BYTES=256KiB WITHOUT draining the tail or buffering past the ceiling — so chunked/absent-Content-Length and under-reporting lengths are both caught (proven: test asserts pull-count < total chunks, 413 not 422 => parser never reached). Strict-JSON/renderer-parity guard refuses NaN/Infinity/unpaired-surrogate with the SAME encoder settings the renderer+digest use (:285-292), so guard and renderer cannot disagree and nothing malformed reaches the digest. Deeply-nested body -> RecursionError caught by except at :268 -> typed 422. No stack traces/paths/internals: refusals carry only str(exc) (bounded, typed field path) or constant messages; the generic 500 logs correlation_id only, never str(exc)/traceback (:304-309); test_refusal_leaks_no_server_internals asserts no traceback/site-packages/path leakage. Logs carry only ints, field paths (constructed from names+integer indices, never user string VALUES), and correlation ids — no unbounded user value. Correlation-id on every non-disabled response; the disabled 404 sentinel deliberately omits it to stay byte-indistinguishable from an unmounted path. Flag-gating is consistent with the platform: reuses INTERNAL_RULE_EVAL_ENABLED, always-registered, include_in_schema=False, generic 404 when off — matching lot_geometry (M5-T020) and condo_records (M5-T052) exactly per main.py:156-186 and config.py fail-safe-off semantics.

4. ECHO SAFETY — PASS. Echo = {result:"accepted", kind:"proposed", block_digest:"sha256:<hex>", correlation_id}; no raw user string reflected, no derived value (test greps allowance vocab clean). Digest = sha256 over canonical JSON (sort_keys, compact separators, ensure_ascii=False, allow_nan=False) of the INPUT block — an identity fingerprint, not a zoning value, computed only after step-5 proved the block encodable. Two distinct blocks canonicalize to distinct bytes by construction; the digest is echoed only (no storage/authz/dedup use here), so collision is not a security surface.

5. DEPENDENCIES/NETWORK/SECRETS — PASS. No new dependency (gate imports app.scenario.proposal; route imports stdlib hashlib/json/logging/uuid/collections.abc + already-present fastapi + app.config); no requirements/lock file in the material commit (git-confirmed). No network in tests (synthetic ASGI receive + in-process TestClient). Secret-scan/gitleaks green at CI head; fixtures use RFC-2606 architect@example.com.

6. MONOTONICITY — PASS (a security property, held). The gate only ADDS refusals then ALWAYS delegates to validate_proposed_massing unchanged as its LAST step (:179), so it returns None iff (budget met AND ceilings hold AND validator accepts) — it can never accept anything the accepted validator refuses. The gate counts positions (>= distinct-vertex count), so the budget is a conservative upper bound on real quadratic work. Proven across 5 refusal classes with same-field assertions (test_gate_refuses_whenever_validator_refuses) plus a delegated bowtie (test_gate_delegates_a_defect_it_does_not_own).

## Informational (not blocking; pre-existing, correctly matched)
INFO-1: The route inherits the platform's no-auth internal/dev posture (main.py docstring; M1-T005). Mitigation is the fail-safe-off flag gate, which this route implements identically to its siblings. It MUST NOT be publicly exposed until the auth/organization layer lands — same standing condition as every other internal route, not a T053 defect.

Basis: The gate closes both of my T048-G5 originating findings at the code and test level — a global O(n)-counted vertex budget bounds the aggregate quadratic cost to a sub-second worst case before any simplicity test runs, and dual-layer string ceilings + bounded-repr (gate cap 80, route cap 400 over str(exc)) guarantee no user value is echoed or logged unbounded, including validator-embedded reprs on fields the gate does not own. The route adds fail-closed request-size, strict-JSON, and leak-free error discipline consistent with the accepted sibling routes, the acceptance echo carries zero derived values (D-076-R002), and the gate is provably monotone. CI-green at the byte-identical reviewed head. VERDICT: PASS.
