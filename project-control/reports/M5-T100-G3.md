# M5-T100 — G3 code review (code-reviewer "cr-t100", read-only)

> Transmission history: pinned at 83f86ce0, delivered as two SendMessage parts (1/2, 2/2) ending with END-OF-REPORT;
> no truncation. Joined verbatim by the orchestrator (transport wrapper tags removed only). Verdict PASS; advisories
> A1 (a TRAILING newline passes the allowlist regex's `$`), A2 (the correlation id is logged raw - DB-058 (d)) and A3
> (message wording) are non-blocking and routed to the wiring packet at the accept seam.

---

M5-T100 G3 code review (read-only) PART 1/2. PIN: HEAD at start 83f86ce0 (>= expected). Material 2624adec (3 files). Independent runs from services/api: `ruff check .` -> All checks passed; `pytest tests/connectors/test_building_footprints_arcgis.py -q` -> 117 passed; `modularity_check.py --check` (repo root) -> failures 0, warnings 27. I also re-derived the sanitizer edge case against the real functions.

VERDICT PREVIEW: PASS. No BLOCKING findings. Three advisories, all LOW/INFO, non-blocking.

DB-058(e) - findable AND non-leaky: CONFIRMED.
- Findable: logger `app.connectors.building_footprints_arcgis` (:86), level ERROR, correlation id present (:1095-1098). Only UNEXPECTED (non-typed) exceptions log; a typed BuildingFootprintConnectorError is NOT re-logged (:1083-1085 `error = exc`, no log branch) - no double-logging (the shared transport already logs its own retries at WARNING on the same logger, so `_error_records` filtering on ERROR sees exactly one line).
- Non-leaky: `str(exc)` is NEVER rendered. The only exception-derived field logged is the CLASS name; the `message=` field is a FIXED constant (:410, :1098). The real leak vector - upstream body text carried in the exception message - is fully closed. Proven by `test_t100_internal_error_is_findable_and_logs_no_upstream_text` (injects RuntimeError carrying a fake secret + a forged-newline body; asserts none of it reaches the log and detail stays class-name-only).
- Deviation from G3-A1's `logger.exception` suggestion is SOUND: `logger.exception` renders the full traceback + message = message text + stack paths leak. Logging class + fixed-message + cid instead is the correct call, and the M5-T089 G5 "class-name-only refusal is the safe choice" is preserved. Documented as a deviation, not silent.

Returned refusal BYTE-IDENTICAL: error_type "internal_error", message "unexpected internal failure; no data is returned", detail {"exception": <class name>} - unchanged vs the pre-fix branch (:1099-1101; test asserts detail == {"exception": "RuntimeError"}).

Continued PART 2/2.

---

M5-T100 G3 PART 2/2.

DB-058 (h)/(l)/(m) tests - built from the recorded fixture WITHOUT editing it: `synthetic_page` (test:136-140) does `json.loads` of a FRESH copy of the recorded page-1 body, mutates it in-memory, then `json.dumps` - the on-disk fixture is never touched; the sha256-pinned fixture-integrity test (test:148) still passes.
- (h) end-to-end: a feature missing its 'geometry' key -> typed null_geometry, record KEPT (not dropped, not untyped). Kills MU-H.
- (l) both one-half-wrong CRS pages ({wkid:102718,latestWkid:9999} and {wkid:3857,latestWkid:2263}) refused wrong_crs. Kills G4 survivors MY1a/MY1b.
- (m) CONSTRUCTION_YEAR 0 -> construction_year None AND the zero_not_available gap (asserts the DISPLACED VALUE, per the M5-T081/T083 lesson). Kills MY7.

NO DRIFT: commit = exactly 3 files (connector/test/report); NO fixture touched; the module diff is only (i) +1 import `sanitize_retry_after` from the already-imported transport block, (ii) `_sanitized_bounded` + 2 constants (:410-414), (iii) the except-branch split (:1083-1101). Every other code path byte-identical; 110 pre-existing tests unchanged; 117 pass.

MODULARITY: 907 SLOC / 50 symbols = WARNING (symbol_ceiling + review_signal); `modularity_check --check` failures 0 - NOT a CI failure. +16 SLOC (one 4-line log helper + 2 consts + a 9-line if/else), one read-only connector responsibility, peer dcm connector 902 SLOC. Cohesion justification ACCEPTABLE, consistent with the M5-T089 G3 ruling that accepted 891 SLOC.

SCOPE: exactly the 3 allowed paths; zero new dependencies (only new import comes from the already-imported transport); connector unwired.

ADVISORY (non-blocking):
A1 (LOW, verified). `_sanitized_bounded`/`sanitize_retry_after` do NOT neutralize a TRAILING newline. I ran the real functions: `sanitize_retry_after("RuntimeError\n")` returns it verbatim - the regex `^[A-Za-z0-9,: +\-]{1,64}$` (transport.py:69) uses `$`, which matches just before a trailing newline - and a class whose `__name__` is `"Foo\n"` is constructible with `_sanitized_bounded` keeping the raw `\n`. So the report/test claim "a newline cannot forge a log line" holds only for MID-string newlines (correctly repr-escaped; the two sanitize tests use only those). Non-blocking: real exception class names are identifiers (never a newline), reaching this field needs code that builds an exception type from untrusted text (absent here), the packet is unwired, and str(exc) is never logged. Recommend the wiring packet strip `\r`/`\n` outright rather than rely on repr-via-allowlist. Refs: building_footprints_arcgis.py:414-420, :1096-1098; transport.py:69,216-221.
A2 (LOW). cid is logged RAW (:1098), not through `_sanitized_bounded` - the more realistic forge vector - but this is ALREADY tracked as DB-058(d) for the wiring packet and the producer's DISCOVERIES section forwards it; the unwired default is uuid4().hex. Correctly deferred, not a new item.
A3 (INFO). The logged fixed message (hyphen, :410) differs from the returned refusal message (semicolon; the semicolon fails the allowlist so it would be repr-quoted). Cosmetic - class name + cid are the correlation keys; the `message=` field carries no bug-specific info.

M5-T100 G3 VERDICT: PASS
END-OF-REPORT
