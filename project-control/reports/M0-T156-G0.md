# M0-T156 — G0 readiness (administrative, orchestrator)

- Task: M0-T156 — Directive-registry digest normalization (CRLF/LF) — retire the control-plane CI red
- Packet commit: a98d174d (branch candidate/D-024-mrl-option-b)
- Date: 2026-09-12 (UTC)

## Readiness checklist

1. **Authorization**: D-040:D-040-R003 (owner scoped unblock, captured a5694aea, validator EXIT 0).
   Requirement text quoted verbatim in the packet's path_notes; constraint honored by design
   (sources immutable; append-only manifest mechanisms).
2. **Directive regime**: task stamped in-regime at creation (`--directive-refs D-040:D-040-R003`).
   Selective-citation guard pre-checked: `evaluate_task_refs` → ok=true, applicable_ids=[],
   cited=[D-040-R003], missing/invalid/unresolved all empty.
3. **Packet completeness**: exact requirement identifiers (D-040-R003 + the CI job log as the
   authoritative failure list), exact evidence inputs (7, by path/run id), exact outputs (8),
   EXACT-FILE allowed_paths (8), non-overlapping forbidden_paths (sources/requirements/
   verification/index/schema immutable; project_control.py + supervisor frozen), six executable
   acceptance scenarios (AS-1..AS-6 incl. red/green and adversarial tamper case), risks (4),
   documented test commands (5 local stdlib + the CI control-plane job).
4. **No unresolved blockers reference M0-T156**; dependencies: none (control-plane tooling,
   independent of product tasks).
5. **Scope exclusivity**: no other active task holds any of the 8 allowed paths (M5-T016 accepted
   and closed; no other in-flight packets).
6. **Modularity boundary answers** (per the policy): (1) responsibility = registry-artifact
   digest identity; (2) owning modules = tools/directive_registry.py (shared primitive) and
   tools/validate_directive_compliance.py (its two direct hash sites); (3) neither file is above
   a warn threshold for this change class (verify with modularity_check before submit);
   (4) no extraction needed — the change is one normalization primitive + call-site adoption;
   (5) stable public interface = validator CLI exit-code contract + registry load/evaluate API;
   (6) protecting tests = tools/test_directive_compliance.py (extended); (7) modularity CI check
   must pass before submission.
7. **Producer/reviewer independence**: producer = orchestrator; reviewers code-reviewer (G1),
   qa-engineer (G4), security-reviewer (G5) — all ≠ producer. No UI → no G3.

**G0: PASS (administrative readiness).** Task moves backlog → ready for claim.

## G0 addendum (post-recording correction, orchestrator)

After the initial G0 recording, the claim guard (s19 / D-001-R118) correctly fail-closed twice:
(1) cited D-040-R003 was not APPLICABLE to M0-T156 — resolved by the established
applicability-append mechanism (M0-T156 appended to D-040-R003 task_ids; requirements digest
resynced 0399f470 → 27d6d1b8; audit_log entry; validator EXIT 0); (2) governance coverage
requires a cited ACTIVE directive whose manifest scope.task_types includes "governance" covering
the task — resolved by correcting task_type control-plane → governance (the M0-T151..T155
precedent) and citing D-001:ALL (D-001 scope.task_types=["governance"], and its scope.paths
names tools/directive_registry.py, tools/validate_directive_compliance.py,
tools/test_directive_compliance.py exactly; all 136 D-001 requirements remain NON-applicable to
M0-T156 by task_ids conjunction — evaluate_task_refs: ok=true, applicable=cited=[D-040-R003]).
Both guard trips were the system working as designed; readiness conclusions above otherwise
unchanged. Claimed thereafter.
