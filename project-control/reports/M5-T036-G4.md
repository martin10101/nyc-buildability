# M5-T036 G4 review — qa-engineer return (VERBATIM)

Reviewer agent: qa-engineer (spawn rev-g4-m5t036), read-only, pinned HEAD
657b238dce8af8df1bfff2e22e454caab1749413. Saved verbatim from the agent-return channel
(transport entity-decoding only). Orchestrator context note (not part of the return): the web CI
run on this head completed AFTER dispatch and BEFORE this save — its result (web-e2e vitest FAIL,
two ambiguous-query failures in the new panel suite, "Found multiple elements") is the AS-6
orchestrator-captured evidence this review names as outstanding; it confirms rather than
contradicts this review's UNVERIFIED discipline.

---

G4 (test adequacy / regression) verdict for M5-T036: **PASS**, with AS-6 (web vitest) named as required orchestrator-captured CI evidence — not executable on this thin client and not a basis to fail.

HEAD verified = 657b238dce8af8df1bfff2e22e454caab1749413. Read-only throughout; the only commands I ran were `git show`, read-only `grep`/`python` inspection, and `python tools/modularity_check.py --check`.

METHOD NOTE: I cannot run vitest/tsc (no node_modules). "PASS-mappable" below means I traced each assertion to the actual production contracts and fixture data and confirmed they are mutually consistent; runtime green stays UNVERIFIED until web CI.

## AS-1..AS-8 -> proof mapping (all evidence-bound)
- AS-1 (every present designation class + provenance, nothing computed): zoning-context-panel.test.tsx "AS-1" block (4 tests). Backed by the M1-T005 fixture (packages/contracts/fixtures/valid/property_profile/builder_output_m1_t005.json): districts ["R3-2","C4-1"], special_districts ["GI"], mapped_features landmark="INDIVIDUAL LANDMARK"/histdist="Governors Island Historic District", commercial_overlays []. Verified via `python -c json`. PASS-mappable.
- AS-2 (honest absence): "AS-2" block (3 tests) — empty overlay text (base fixture has [] overlays), all-three-empty, mapped_features=[] -> exactly 2 "Unknown — not supplied" rows (DESIGNATION_FLAGS has exactly landmark+histdist) + no disclosure + labels still visible. PASS-mappable.
- AS-3 (validated ZoLa link, DB-005): "AS-3" block (2) + address-confirm.test.tsx lines 247-266 (2) + development-limits.test.tsx lines 175-200 (2). zolaLotUrl (lib/provenance-link.ts:53) returns null unless `/^[1-5][0-9]{9}$/` and length 10, so "12345" -> null. Canonical 1000010010 -> https://zola.planning.nyc.gov/bbl/1000010010. Independent grep: NO raw `zola.planning.nyc.gov`/`ZOLA_BBL_URL_PREFIX`/`${bbl}` template remains in PropertyOverview.tsx, ZoningContextPanel.tsx, or AddressConfirmCard.tsx. PASS-mappable.
- AS-4 (mount + print-exclusion both directions): "AS-4" block (2). Verified in source: ReportView.tsx:11 imports only PropertyIssuesSummary (a named export), NOT the PropertyOverview component that mounts the panel; ReportView renders its own ZoningSection (:79) + AdditionalZoningFlags (:80). Only ArchitectEntry.tsx:95 renders <PropertyOverview> in production. Panel present via PropertyOverview / absent via ReportView both asserted. PASS-mappable.
- AS-5 (scope disjoint): `git show --stat` = 7 files, all under apps/web/** + the report; nothing under services/api/** or apps/web/src/lib/**. DB-005 uses the existing zolaLotUrl by import only. PASS by inspection.
- AS-6 (web CI on pushed head): NOT locally executable (thin client, no npm). REQUIRED orchestrator-captured evidence. Named, not failed.
- AS-7 (modularity): I ran `python tools/modularity_check.py --check` myself -> "selected 441 files; failures 0; warnings 19". All 19 warnings name pre-existing files; the only apps/web one is lib/surveyReview/types.ts (untouched). Matches producer's 441/0. PASS (independently verified).
- AS-8 (report honesty bar): producer report separates OBSERVATION/CONCLUSION, marks all web behavior UNVERIFIED, surfaces D1/D2 as discoveries without fixing. Consistent (primarily the directive-compliance reviewer's remit).

## Assertion-strength findings
1. AS-1 district-provenance is NOT tautological: ProvenanceDisclosure.tsx:26 renders the "Source for X" <summary> label ONLY when records is non-empty (empty records render a different "not linkable" paragraph with no label). So getByText("Source for R3-2"/"C4-1") genuinely proves the D5 fallback join resolved (FALLBACK_COLUMNS.districts includes zonedist1/zonedist2, fixture normalized_values R3-2/C4-1). Same guarantee makes AS-2's queryByText("Source for Landmark")==null a real honest-absence check.
2. Fixture fidelity — overlay case is faithful to production: the AS-1 populated-overlay test pushes a provenance record with original_field_name "overlay1"/normalized_value "C1-4", which is exactly what resolveDistrictProvenance (lib/provenance.ts, FALLBACK_COLUMNS.commercial_overlays=["overlay1","overlay2"]) joins in the live 1.0.0 path. No invented shape.
3. .architect-map-card scoping keeps the two ZoLa affordances independent: development-limits DB-005 tests scope to `.architect-map-card` (Site-context "Open ZoLa ↗"), while the panel's link ("Open in ZoLa ↗") sits in a separate section; the /Open ZoLa/ regex also does not match "Open in ZoLa". Both affordances asserted separately, no blur.
4. DB-005 unification proven at the surface on the two migrated links: address-confirm (:249) and development-limits (:186) assert href === zolaLotUrl(bbl) directly. The panel's own link asserts the exact literal URL (appropriate — it's a new link, not a migration).

## Regression assessment (mandate item 3)
No regression detected in source. The panel mounts only inside <PropertyOverview>, and in production only ArchitectEntry.tsx:95 renders that component (ProfileViews/PropertyFacts/ReportView import PropertyIssuesSummary; ScenarioWorkspace imports DraftHeadline — none get the panel). This is why workspace.test.tsx, ProfileViews.tsx, and AdditionalZoningFlags.tsx correctly stayed untouched.
Existing development-limits overview-view tests (lines 44, 142, 446) now mount the panel but remain safe: they scope with `within(getByRole("region",{name:"Development limits"}))` or assert on testids/text unique to DevelopmentLimits/PropertyOverview that the panel deliberately never emits (architect-cap, development-evaluated-far, development-reference-far, "Zoning boundary check unavailable", "Existing building information"). The existing-building disclosure asserts table rowheaders while the panel uses a <dl>. The panel test itself asserts none of the development-limits result testids appear in the panel. LotOutlineMap is stubbed module-level in both suites. No single-match query becomes ambiguous from the panel's added text (district codes/zoning headings were not previously on the overview). Residual: render-time green is UNVERIFIED until CI.

## CODING_RULES conformance
Typed DOM queries used (querySelector<HTMLElement> in both suites; getByRole/getByText/within otherwise). No empty .test.tsx — both placeholders replaced with real suites. No local npm/node invoked or documented.

## Limitations / UNVERIFIED
- All runtime pass/fail is UNVERIFIED (no local vitest/tsc). My analysis is source-level consistency, not execution. Web CI on the pushed head is the executable authority (AS-6) and must be captured by the orchestrator before acceptance.
- I did not independently run tsc; the new panel's type usage (ZoningValueList props, mappedFeatureView fields feature/hasValue/value/provenanceRef, formatValue(unknown), null-guarded resolveFactProvenance) matches existing in-repo patterns, but type-check green is a CI claim, not mine.

No implementation defect found in the tests as they stand. Verdict: PASS (AS-6 web CI + tsc are the outstanding orchestrator-captured gates).
