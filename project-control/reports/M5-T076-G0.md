# M5-T076 G0 — readiness gate (orchestrator, administrative)

Task: **M5-T076 — DB-050(a) geometry-threading pre-mount packet**: server-side derivation
of authoritative EPSG:2263 lot-line segments from the accepted MapPLUTO connector by BBL,
wired at the route layer, so the geometry-free client request can elicit a FITTED candidate
from the real engine. Fail-closed on every derivation failure; engine byte-untouched; route
stays unmounted.

## Authority

- The requirement is DB-050 item (a) — the T070 wave's load-bearing rider, recorded with
  both reviewers' endorsement of exactly this design direction (server-side derivation from
  mappluto_geometry_arcgis by BBL, never client-sent display geometry; G3 delta: "correctly
  identifies server-side derivation … as the right design"). Lane-1 occupancy authorized by
  owner directive D-084, R001 bound with a same-commit digest resync.
- `evaluate_task_refs`: **ok=true, applicable == cited (D-084-R001)**.
- Sequencing: depends on M5-T070 (awaiting_gate at contract time — the dependency is on its
  MATERIAL, all landed and CI-green at 395d6727; the T070 acceptance rides its own wave).

## Packet readiness

- allowed_paths: 5 entries — the new module (non-empty docstring placeholder seeded), the
  route file (existing), two test files (the new unit suite seeded as a py placeholder;
  test_max_envelope_api.py existing), and the report (placeholder seeded).
- The HARD MODULARITY BOUND is a binding input: max_envelope.py (998 SLOC, at the 1000-line
  hard threshold) is FORBIDDEN and must be proven byte-untouched in the report (AS-4).
- 4 acceptance scenarios: derivation correctness w/ display-ring mutation guard,
  route-level reachability (the fitted candidate on the F01-class fixture through the
  engine's own consistency proof), byte-identity + per-failure-class fail-closed, and the
  modularity/scope bound. All tests offline on recorded fixtures.

## Disjointness (live/frozen neighbors)

| Neighbor | State | Overlap |
|---|---|---|
| M5-T070 (HJ+SEC running) | awaiting_gate | none — apps/web + its report; this packet is api-side |
| M5-T071 (DCV out) | awaiting_gate | none |
| M5-T072 / M5-T075 (submitted) | awaiting_gate | none — site_definition / web lib tests |
| M5-T073 (lane-2 LIVE) | claimed | none — tests/connectors fixtures/harness |
| M5-T074 (lane-3 LIVE) | claimed | **tests/api/test_max_envelope_api.py is NOT in T074's paths** (T074 = tests/api/test_outline_bridge.py only) — EMPTY overlap |

Pairwise overlap of allowed_paths vs every live/frozen lane: EMPTY.

## Seam records

- Graph regenerated at the seq-126 seam; the navigation block pins the connector's
  consumers and the read-only boundary; the route seam anchor (:201-206) and LotContext
  (derivation.py:255) named.
- The packet + R001 bind + digest resync + placeholders + this report land in ONE
  contract-seam commit, G0 recorded at that head.

Verdict: **PASS** — packet is contract-ready.
