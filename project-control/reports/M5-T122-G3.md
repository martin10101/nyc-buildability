# M5-T122 — G3 code review (code-reviewer "cr-t122", read-only)

> Transmission history: pinned at 9d5311a8 (material b2e833fd; CI run 36116891984 green at 9d5311a8), delivered as
> 2 SendMessage parts ending with END-OF-REPORT; no truncation. Joined verbatim by the orchestrator (transport
> wrapper tags removed only). Verdict PASS; the move verified verbatim; four ADVISORY items
> (1 the strip's 'see City records below' pointer can dangle during a condo-records channel outage, 2 D1, 3 D2, 4 C07).

---

M5-T122 G3 CODE REVIEW (read-only) — PART 1/2. PIN: HEAD 9d5311a8 at start (material b2e833fd, parent b0eb29f7 == claim-seam file state; b2e833fd is the ONLY commit touching these paths in range — no interleaving).

CI @ 9d5311a8 (run 36116891984): web (lint+typecheck+build) ✓ 1m12s; web-e2e (vitest+Playwright) ✓ 5m14s; api ✓; modularity ✓; control-plane ✓. Only supervisor-bridge (tools/test_agent_supervisor_*.py) still running — UNRELATED (this task touches zero tools/ or services/ files). Known flake a11y-announcements.spec.ts:152 did NOT manifest.

THE MOVE (AS-2) — VERBATIM, verified against claim-seam PropertyOverview.tsx (401 lines):
- The four pure-logic units are byte-identical old→new: condoResolutionNotes, deriveCondoDisplay, condoWithholdsAllowances, deriveCondoSurface + types (CondoRecordsSection.tsx:65-168). Only stale comments were dropped (e.g. the "channels unpopulated today" note now false). No branch/predicate changed.
- Facade complete: PropertyOverview.tsx:25-40 imports the moved names locally and re-exports CondoRecordsChannelSection, deriveCondoSurface, deriveCondoDisplay, condoWithholdsAllowances + the two types; DraftHeadline + PropertyIssuesSummary stay in-file. ReportView.tsx:11 (`{ PropertyIssuesSummary, CondoRecordsChannelSection, deriveCondoSurface } from "./PropertyOverview"`) resolves unchanged; report-view.test.tsx + condo-resolution-display.test.tsx untouched and GREEN via the facade (web-e2e). No importer edited.

§5.4 PASS (AS-1) — each element maps to a C row, records never read as allowances:
- C01 "City record" tag as an h2 SIBLING (CondoRecordsSection.tsx:232-235); h2 name "Recorded base lot for this condo" unchanged.
- C02 "Reference only" sibling (285-288); h2 "City records for this condo" unchanged.
- C04 "Human record" tag (192).
- C06 "Parcels differ." + comparison <details> (196-202) reading REAL schema fields — parcelDiscrepancy {recordedParcels:string[]; currentResolverParcels:string[]} verified at lib/condo-records.ts:175-178 (NOT guessed; the old code never read sub-fields). Record not revoked.
- C08 "Zoning missing for N lots" display count (253, 299).
No allowance/permitted/approved wording (tests assert the negative). No number computed in UI; no backend/contract change.

SCOPE: exactly the 9 allowed paths; zero forbidden paths in the commit; lib/, ReportView, ArchitectEntry, AnalysisIdentityNotice, report-view.test.tsx all untouched. Modularity fine (317/128/120 lines).

Part 2 (strip riders + advisories + verdict) next.

---

M5-T122 G3 CODE REVIEW — PART 2/2.

STRIP RIDERS (AS-3) — all verified against real code:
- A2 one heading: h2#overview-exception-strip-heading + region aria-labelledby, no aria-label (OverviewExceptionStrip.tsx:66,69). Test asserts single heading, labelledby=heading.id, NOT aria-label, status resolves once (strip test + both e2e).
- A4 stale link: propertyHref(bbl,"evidence") — "evidence" IS a valid WorkspaceView (navigation.ts:2), so it typechecks and targets the real Evidence view (OverviewExceptionStrip.tsx:61,115).
- A5 condo clause: unresolved partitioned into generic vs condo_base_lot_resolution (53-54); condo row keeps heading/field/issues-link but gets the TRUE clause "the condo's base lot is not resolved to a single lot; see 'City records for this condo' below" (83-95); generic keeps "official sources disagree". Reachable: zoning_crosscheck.py emits this conflict with resolution='unresolved' for multi_lot. Combined test proves both clauses coexist.
- A7 identity alert: CSS-only .architect-alert[data-identity-state] rose treatment — selector matches the REAL role=alert element (AnalysisIdentityNotice.tsx:72, data-identity-state differs|absent) and correctly EXCLUDES PropertyIssuesSummary's plain .architect-alert. Not a dead selector.
- Grid comment fix (G3 advisory): new comment is factually accurate vs the CSS — left track minmax(320px…) base / 280px at ≤1200px (architect.css:79,177) / right floors at 0 / single column at ≤950px (178). The old "both tracks 0 floor" was wrong; correction is true.

TESTS: behavior-focused, strict-TS idioms (as unknown as Response/typeof fetch), present AND absent for every new element (C04:218-222, C06:247-250, C08:200-205, A2-absent). Facade proven by reference identity (toBe, 137-140). C06 drives the REAL fetchCondoRecords parse (snake→camel), not a hand-built view. e2e stubs the conflict via page.route + a 360px no-overflow check.

ADVISORY (non-blocking):
1. Dangling pointer edge: the strip's "see 'City records for this condo' below" reads profile.conflicts, but the records section renders from the independent live useCondoRecords channel (showRecords = channel.kind==="multi_lot", CondoRecordsSection.tsx:162,242). If the profile carries the condo conflict while the channel is loading/unavailable (outage), the section is honestly absent and the pointer dangles. No test co-renders both (unit tests render each in isolation; e2e asserts the strip text, not the section). Clause substance stays true and the issues-link works; design tolerates channel outage. Recommend softening/gating the pointer or accepting knowingly.
2. D1: a profile with a true source conflict AND a condo conflict shows two rows both headed "Unresolved data conflicts" (each clause true; tested). Minor UX oddity; producer honored "keeps its heading" literally (OQ-1). HJ judgment call.
3. D2: h2 kept exact (report-view.test.tsx locks it) with "Reference only" as a sibling — correctly avoids editing a forbidden locked test; name preserved, intent visible. Acceptable.
4. C07 history correctly routed to backlog (SiteDefinitionView exposes only confirmationCount; needs a lib contract addition, read-only here). Not a defect.

No guessed schema, no hard-coded legal value, no hidden default, no meaning weakened, no allowance vocabulary. web + web-e2e GREEN.

M5-T122 G3 VERDICT: PASS

END-OF-REPORT
