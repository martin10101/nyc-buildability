# M5-T052 HJ — independent human-journey review (human-journey-reviewer, saved VERBATIM from the agent returns)

M5-T052 (DB-031) HUMAN-JOURNEY (G3) REPORT — VERDICT: PASS (5 ADVISORIES, 0 BLOCKING). Read-only; pinned HEAD 6e3e1a400450091dcdf0c7f3429e9dee1fdd5e6e verified.

BASIS (one paragraph): Walking the three journeys as a professional analyst, the design is honest and coherent. ONE shared decision (deriveCondoSurface, PropertyOverview.tsx:197) is consumed by BOTH the screen (PropertyOverview.tsx:283) and the printed brief (ReportView.tsx:46), and both render the SAME CondoRecordsChannelSection under the unchanged fail-safe (DevelopmentLimits fed null scenario/eval → "Not calculated"), so screen and brief cannot tell different stories. Multi-lot: heading "City records for this condo" is RECORD-class (no analysis/zoning-result words; grep-tested at condo-resolution-display.test.tsx:367-368), each base lot is a bare record (BBL + recorded zoning, null → "not recorded (unknown)"), the divergent-zoning notice shows, no allowance vocabulary and no computed decimal appear. Resolved-single is the allow path: the substitution record reads as an explanation ("The analysis runs on the land parcel the city records for this condo. You entered X; the city records base tax lot Y…") — entered vs analyzed, not an apology or a computation — and allowances still show. Unresolved/error/outage are honest absence (null section); a resolver outage correctly does NOT withhold on its own. Raw outcome literals (multi_lot_set etc.) never leak into copy — they drive branching only. The route is mounted (main.py:32,174) behind INTERNAL_RULE_EVAL_ENABLED and now resolves per-BBL independently of profile-build, so the records view is genuinely REACHABLE for a real multi-lot condo (the "missing half" of T045, which was dormant). No blocking defect: the fail-safe holds, nothing is fabricated, absence is honest, and the cross-boundary token is pinned on both sides.

FINDINGS:

1. ADVISORY (copy/edge, multi-lot unit input) — The line "Condo lot you entered: {billingBbl}" (PropertyOverview.tsx:242) binds the "…you entered" label to the BILLING lot, not the entered BBL. For a UNIT-class multi-lot input the route returns billing_bbl=null (billing_status "unknown", condo_records.py:410-420), so the screen renders "Condo lot you entered: not recorded (unknown)" even though the professional demonstrably entered a real unit BBL. The document carries `entered_bbl` (always present, condo_records.py:513) but the web client's documentView (condo-records.ts:300) never parses it. The allow path already uses substitution.enteredBbl correctly (PropertyOverview.tsx:228), so the two paths are inconsistent. Fix: parse `entered_bbl` and render it for the "lot you entered" line; show the billing lot on its own labelled line.

2. ADVISORY (dangling reference) — The records/conflict copy says "under the professional-review determination above" and "The professional-review determination above governs" (PropertyOverview.tsx:241,255), but the withheld DevelopmentLimits panel above shows "Draft assessment · envelope incomplete" + "Not calculated" with NO explicit professional-review label (STATUS_LABELS professional_review_required only renders when scenario is non-null; withheld → scenario null). Defensible (withheld = deferred to a human) but a reviewer looking for the named determination won't find matching wording above. Consider "the withheld development limits above" or surfacing an explicit professional-review line.

3. ADVISORY (coherence, brief only, dormant) — On the printed brief, AnalysisIdentityNotice (ReportView.tsx:84-85) and the substitution record read INDEPENDENT identity signals: the notice compares returnedScenario/Evaluation.evaluated_input.bbl vs profile.identity.bbl; the substitution comes from the separate condo-records channel. Tests set evaluated_input.bbl == identity.bbl so they never collide, but if future backend wiring stamps the returned scenario with the substituted BASE-lot BBL, the brief would show "identity mismatch / results withheld" directly beside the substitution record's "analysis runs on that recorded base lot" + shown allowances — a contradiction. Not couplable in this packet (profile/scenario forbidden); dormant today (condo substrate fail-safes to None). Recommend a coupling test/decision when the substrate-substitution wiring lands.

4. ADVISORY (dropped labelling) — recorded_zoning is ALWAYS null from the real route (_recorded_zoning_for returns None unconditionally, condo_records.py:368-377), so every real base lot renders "recorded zoning: not recorded (unknown)". The route emits a richer `recorded_zoning_status` label and a named `recorded_zoning_dependency` (the ZTLDB/spatial gap, condo_records.py:141-150,527), but the web client parses NEITHER, so the display never surfaces the named dependency (only its own honest "unknown" fallback). Also the divergent_zoning_notice renders on every multi-lot even though no zoning is recorded for any base lot, so it reads slightly disconnected. Honest and conservative; nothing fabricated — minor.

5. ADVISORY (a11y, carried from T045) — CondoRecordsChannelSection uses `<strong>` for its section headings ("City records for this condo" / "Recorded base lot for this condo") inside role="group"+aria-label, not h2/h3 (PropertyOverview.tsx:231,240), so the records section stays absent from the heading outline DevelopmentLimits populates. HJ-A3's RECORD-class WORDING rider is satisfied; the heading-level gap is unchanged from M5-T045.

No agent-memory write attempted (read-only posture); findings returned here in full.

## Delta-attestation (verbatim)

CARRIES. My HJ PASS holds at e9abeaaf15ce9cff2c6405c96da8bbe8e2e31ef0 — I reviewed only component/copy/test surfaces (PropertyOverview.tsx, ReportView.tsx, condo-records.ts, AnalysisIdentityNotice.tsx, condo-resolution-display.test.tsx, condo-records.test.ts, condo_records.py), all byte-identical under the report-only diff (6e3e1a40..e9abeaaf = two report files + control-plane records, zero code change). Bottom line unchanged: records view honest, three journeys coherent, 5 advisories / 0 blocking.
