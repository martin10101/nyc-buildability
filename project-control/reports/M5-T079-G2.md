# M5-T079 - G2 producer self-check record (recorded by the orchestrator, role self_check)

Producer: frontend-engineer (orchestrator-dispatched subagent after the B-026 lane refusal) in wt-m5t079;
one in-worktree commit cherry-picked to 006f3589 with every file MATCH by git blob identity.

- **Scope:** exactly the seven allowed paths (MaxEnvelopePanel.tsx, max-envelope-api.ts, their specs,
  entry.test.tsx, proposal-editor.spec.ts, the producer report); services/api untouched; route UNMOUNTED;
  no package.json/lockfile change.
- **Producer self-checks (report):** thin client - modularity exit 0; the strengthened copy wall's matcher
  replayed in Python over the five real scanned files (all pass) with four injected banned variants caught
  and a comment-only mention not caught [OBSERVED]; every web behavior claim marked [PREDICTED] for CI.
- **[ORCH-HARVEST] CI proof on the pushed head:** CI run 35975846444 at acb224b2 SUCCESS on all 18 jobs
  incl. `web (lint + typecheck + build)` and `web-e2e (vitest + Playwright vs recorded-official-fixture
  API)`; 006f3589 is an ancestor of acb224b2.
- **Honest gaps disclosed by the producer:** the aborted `.then` guard is covered only indirectly (not
  observable in RTL); D1 one-action adoption stays production-unreachable until the mount seam; D2 the
  file-wide entry fetch stub; D3 '0 of N' aggregate copy when only a violation exists.

G2 verdict: PASS (self_check; never independent review - G3 code-reviewer, G4 qa-engineer, the
human-journey walk and the DCV follow).
