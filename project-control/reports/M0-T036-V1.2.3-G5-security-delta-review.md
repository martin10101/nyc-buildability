# M0-T036 V1.2.3 — G5 security delta review (R207 + resume-pending-prompt)

- Gate: G5 (security-reviewer) | Task: M0-T036 | Reviewed SHA: **4ff4d88 (HEAD)** | Delta: b95ebf7..HEAD
- Reviewer model: claude-opus-4-8 / xhigh | Producer: backend-engineer
- **VERDICT: PASS** (no acceptance-blocking findings). Recorded by the orchestrator; reviewer read-only, wrote nothing.
- (Condensed faithful record; full verbatim return is in the agent transcript.)

## Security posture verified (delta = 6 files, +457/-3)
- (a) **Digest-bound, one specific prompt** — `cli.py:1607-1613` refuses (return 1, no mutation) when supplied `--approve-prompt-digest` (required=True) ≠ the durable `pending_prompt/<run_id>` digest. No wildcard/bypass.
- (b) **Approval semantics unchanged / WAIT_FOR_OWNER not weakened** — fires the identical edge the loop fires inline (`state_machine.py:250`; loop `loop.py:1596,1612`); controller-CLI operator affordance, structural twin of accepted `cmd_clear_recovery`; not worker/reviewer-reachable (workers go through the bounded `can_use_tool` broker, never the controller CLI).
- (c) **Fail-closed** on: unreadable journal, emergency stop, state≠WAIT_FOR_OWNER, no/malformed pending record, digest mismatch, missing arg — all 6 covered by passing tests.
- (d) **First-class redacted, hash-chained audit** — `operator_resume_pending_prompt` with `input_digest` (a SHA-256 hash, not prompt text), routed through `redact_structure`, emitted AFTER the durable transition.
- (e) **R207 knobs additive/fail-closed** — per-day counters excluded from RESET_ON_PROGRESS (progress can't reset the daily cap); `record_daily` fails closed on empty/non-str day + non-per-day counter; `Limits.from_mapping` rejects 0/negative/non-int (`bad_limit`) + unknown keys (`unknown_limit`). Tighter caps can only tighten.
- (f) **Shadow-only intact** — command only advances a parked journal's state, dispatches nothing, sets no mode; a `pending_prompt` record exists only in SUPERVISED mode; `limited-auto` refused by name.

## Independently run: phase1 80 passed; loop 103 passed; resume-pending-prompt 6 passed. No regression. Working tree over reviewed scope empty (wrote nothing).

## Activation-only findings (NOT acceptance-blocking; for the owner-gated R595 rehearsal)
- **LOW — stale `pending_prompt` record + reason-agnostic WAIT guard** (`cli.py:1586-1613`, mirrored in `loop.py`): the command gates on state==WAIT_FOR_OWNER + a matching-digest `pending_prompt/<run_id>` record, but does not verify the *current* WAIT was entered via the supervised-approval edge, and neither it nor the loop consumes/clears the record after use. In an *active supervised multi-cycle run*, a later WAIT for a different ASK could still carry a prior cycle's `pending_prompt`; an operator supplying that (genuine, system-recorded) digest would fire `owner_approved_pending_prompt`. Bounded by digest-binding + operator-only invocation + owner-gated supervised activation (unreachable under shadow-only) → not a trust-boundary bypass. Mirrors the accepted `clear-recovery` shape. **Recommend before R595 activation:** consume/clear `pending_prompt/<run_id>` on a successful resume, or record + check the trigger that entered WAIT.
- **INFO** — R207 per-day counters + CPU/memory gauges defined + unit-tested but not yet sampled in `loop.py` (consistent with the existing gauge pattern; a not-yet-enforced tighter cap cannot weaken enforcement). Activation/completeness item, not a security regression.

## Conclusion: PASS. Digest-bound, fail-closed, audited, non-activating operator resume preserving the WAIT_FOR_OWNER trust model; R207 knobs additive/fail-closed. Two forward notes recorded for the R595 rehearsal; neither blocks acceptance.
