# M5-T096 - G2 producer self-check record (recorded by the orchestrator, role self_check)

Producer: backend-engineer (orchestrator-dispatched subagent) in wt-m5t096; one in-worktree commit 33a90617 (parent 2e0b2351), cherry-picked to d59cbfca with every file MATCH by git blob identity (9 files: docs/samples/cad/.gitattributes ab95c38b, docs/samples/cad/README.md c3e98f73, docs/samples/cad/example-massing.glb 21c1e306, docs/samples/cad/example-site-plan.dxf 2c80881b, docs/samples/cad/example-site-plan.pdf d7220466, project-control/reports/M5-T096-producer-report.md e2efa4df, services/api/app/cad/dxf_writer.py 6f57cdc4, services/api/tests/cad/test_cad_owner_samples.py c0383c49, services/api/tests/cad/test_dxf_writer.py 79ad39af).

- **Scope:** dxf_writer.py (STYLE + VPORT tables; TABLES order VPORT, LTYPE, LAYER, STYLE; golden re-pinned 2d8988d6 -> 6a8dbd94 with the reason recorded), test_dxf_writer.py (table tests + import allowlist), test_cad_owner_samples.py (sample regeneration + honesty + size + README-hash tests), docs/samples/cad/ (README, 3 samples, .gitattributes) and the producer report; pdf_sheet_writer.py / glb_writer.py and their tests untouched.
- **Producer self-checks (report, OBSERVED):** ruff clean; 277 passed across the four cad suites (pdf/glb writer tests unchanged); modularity exit 0 (dxf_writer.py now a warning-band review signal at ~671 SLOC, cohesive); six mutations RED (drop STYLE, VPORT centre, table order, STYLE font, writer-constant drift vs samples, import json); STYLE codes checked against the live 2024 Autodesk STYLE page; VPORT R12 codes and the inter-table order marked '[recalled - verify]' for the G1 verifier.
- **[ORCH-HARVEST] at the cherry-picked head d59cbfca (Python 3.11 local): `python -m ruff check .` (cwd services/api) -> exit 0: All checks passed!; `python -m pytest tests/cad/test_dxf_writer.py tests/cad/test_cad_owner_samples.py tests/cad/test_pdf_sheet_writer.py tests/cad/test_glb_writer.py -q -p no:cacheprovider` (cwd services/api) -> exit 0: 277 passed in 1.48s.**
- **Discoveries routed (producer):** DSC-1 the GLB writer's honesty stamp is title-case ('Proposed - not a city record') while DXF/PDF are upper-case - normalize at a future app/cad touch; DSC-2 the R004 owner-runnable check is now delivered but R008 (owner confirmation) and any export route remain open; DSC-3 the Autodesk 'About the DXF TABLES Section' page was not fetchable - the table order is for the G1 verifier to confirm.

G2 verdict: PASS (self_check; the independent reviews and the DCV follow - dispatched by the successor session per the handoff).
