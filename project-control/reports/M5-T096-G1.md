# M5-T096 — G1 format/source verification (data-contract-verifier "dc-t096", read-only)

> Transmission history: pinned at a612652b, delivered as three SendMessage parts (1/3..3/3) ending with END-OF-REPORT;
> no truncation. Joined verbatim by the orchestrator (transport wrapper tags removed only). Verdict PASS: both producer
> "[recalled - verify]" items (VPORT R12 codes 40/41; TABLES order) VERIFIED against the primary AutoCAD Release 12 DXF
> Reference; raw-blob sha256 of all three samples match the README. Advisories A1-A3 non-blocking.

---

M5-T096 G1 (D-087 CAD-2) verification part 1/3 — read-only data-contract-verifier; PIN a612652b; bottom line PASS.

Self-contained. PIN: HEAD = a612652b1e777218dd7d0d6de82ab5cf9648dee5 (== expected a612652b), branch candidate/D-024-mrl-option-b. Worktree dxf_writer.py carries the STYLE/VPORT tables; the committed sample DXF is present.

Method: every new group code re-checked against (a) the PRIMARY archival AutoCAD Release 12 DXF Reference PDF (© 1993 Autodesk, "u12.1.02"; damassets.autodesk.net/.../acad_r12_dxf.pdf), (b) the live 2024 STYLE (DXF) and VPORT (DXF) pages, (c) ezdxf DXF-internals. I parsed the committed DXF by a group-code walk and ran both allowed test files. Retrieval 2026-09-24 UTC.

Bytes (git cat-file raw blobs at HEAD — files are marked binary, so raw-blob hash):
- example-site-plan.dxf 7936 B sha256 4e19c669…65c03e — matches README + producer report
- example-site-plan.pdf 1839 B sha256 8f28f12d…fb0c726 — matches
- example-massing.glb 2460 B sha256 0bc7a37d…d5816bf — matches
All three < 200 KB. .gitattributes marks each binary (text:unset), so no CRLF smudge can break the digests. The README sha256 table equals the committed bytes.

DXF group-code walk (committed example-site-plan.dxf): $ACADVER = AC1009 ✓; $INSUNITS = 21 ✓; TABLES present in order VPORT, LTYPE, LAYER, STYLE ✓; one VPORT *ACTIVE, one STYLE STANDARD, LTYPE CONTINUOUS, 4 LAYERs (LOT / BUILDING_OUTLINE / MASSING_3D / ANNOTATION) ✓; 4 TEXT entities, NONE carry a group 7 → all resolve to STANDARD, which the STYLE table now defines ✓; file ends 0/EOF ✓. VPORT view centre (988050.0, 210033.5) = midpoint of $EXTMIN(988000, 209987) / $EXTMAX(988100, 210080) ✓; view height 110 ≥ extents height 93 and 110 × aspect 1.0 ≥ width 100 → the *ACTIVE view frames the extents ✓.

Tests (cwd services/api, Python 3.11.9): `python -m pytest tests/cad/test_dxf_writer.py tests/cad/test_cad_owner_samples.py -q` → 39 passed. [OBSERVED]

Table, findings and verdict follow in parts 2/3 and 3/3.

---

M5-T096 G1 part 2/3 — verification table (claim / official source / result).

| Claim | Official source | Result |
|---|---|---|
| VPORT 40 = view height (R12) | R12 DXF Ref p.18 verbatim "40 (view height)" | CORRECT |
| VPORT 41 = viewport aspect ratio (R12) | R12 p.18 "41 (viewport aspect ratio)" | CORRECT |
| VPORT 42 = lens length; 43/44 = front/back clip | R12 p.18 "42 (lens length), 43 and 44 (front and back clipping planes)" | CORRECT |
| VPORT shared 2,70,10-37,50,51,71-78 | R12 p.18 full list; 68/69 "never saved" correctly omitted | CORRECT |
| Modern page moved view height to 45, no 40/41 | live 2024 VPORT page: 45 = view height, 40/41 absent | CONFIRMED — that is the R2000+ subclass form; does NOT apply to an AC1009 file |
| *ACTIVE = current viewport name | R12 p.18 "…all have the name *ACTIVE. The first such entry describes the current viewport" | CORRECT |
| STYLE 2,70,40,41,50,71,42,3,4 | R12 p.17 + appendix p.57 + live 2024 STYLE page — all 9 agree | CORRECT |
| STYLE 40=0 → per-TEXT height; 3="txt"; 4 blank | R12 "40 fixed text height; 0 if not fixed … 4 big-font blank if none" | CORRECT |
| TABLES order VPORT,LTYPE,LAYER,STYLE | R12 p.16 "The order of the tables may change, but the LTYPE table always precedes the LAYER table" | VALID (LTYPE<LAYER satisfied) |
| $ACADVER AC1009 = R12; $INSUNITS 21 | R12 p.4 / §1; prior M5-T081-G1 ruling | CORRECT (unchanged) |
| every TEXT resolves to a defined STANDARD | parsed file: no group 7 on any TEXT; STYLE defines STANDARD | CORRECT — closes M5-T081-G1 advisory A2 |
| 3 samples byte-identical; README sha256 honest | 39-pass test suite + raw-blob hashes I recomputed | CORRECT |

Both producer "[recalled - verify]" items — VPORT R12 40/41, and the TABLES order — are now VERIFIED CORRECT against the primary R12 reference (not merely recalled). Every VPORT and STYLE group code the writer emits matches the R12 spec exactly, with correct value types (10–59 float, 60–79 int, 0–9 string).

Findings and verdict in part 3/3.

---

M5-T096 G1 part 3/3 — findings, README honesty, and verdict PASS.

BLOCKING: none.

ADVISORY (non-blocking):
- A1 (wording only): the writer comment and a test name call VPORT,LTYPE,LAYER,STYLE the "canonical R12 write order." The R12 reference mandates only LTYPE-before-LAYER; order is otherwise free. The order used is spec-valid and matches AutoCAD's own output convention — behavior is correct; only the word "canonical" overstates. No change required for PASS.
- A2 (worst case; producer already disclosed as D4): if a reader wrongly applied the modern group-45 VPORT schema to this AC1009 file it would not find view height on 40 and would fall back to a default zoom. Effect = initial zoom only — never geometry, never openability. R12 schema (40/41) is the correct one for AC1009, so this is the expected-correct path.
- A3 (carried from M5-T081, unchanged, already G1-accepted): no BLOCKS section; $INSUNITS 21 predates strict R12 readers. AutoCAD tolerates a missing BLOCKS section (HEADER/TABLES/ENTITIES/EOF opens) and skips an unknown $INSUNITS by name. Not blocking; CRS is also stated verbatim on the ANNOTATION layer.

README honesty (item 5): the checklist matches the parsed file — lot 100×80 ft, building 60×40 ft, ~42 ft/4 floors, 4 named layers, US survey feet, and the three stamps (PROPOSED - NOT A CITY RECORD, the EPSG:2263 note, Example lot - not a real property) are all present. It is explicitly honest that AutoCAD-open is a pending OWNER check ("so you can check that our drawings open," "Opening the files only proves the export works," "Did each file open?") and never claims AutoCAD has already opened the file; the producer report marks AutoCAD-open [BLOCKED]. The GLB's title-case honesty label vs the DXF/PDF upper-case is disclosed (D1/DSC-1) and harmless.

Conclusion: every new STYLE and VPORT group code, the table order, AC1009 / INSUNITS 21, the STANDARD-style resolution, the VPORT framing, the three digests/sizes, and the README are correct and honest against the primary official sources; the two recalled items are verified.

M5-T096 G1 VERDICT: PASS

SOURCES (retrieved 2026-09-24 UTC):
- AutoCAD Release 12 DXF Reference (© 1993 Autodesk, u12.1.02): https://damassets.autodesk.net/content/dam/autodesk/www/developer-network/platform-technologies/autocad-dxf-archive/acad_r12_dxf.pdf (VPORT p.18; STYLE p.17 + appendix p.57; TABLES order p.16)
- Live STYLE (DXF) 2024: help.autodesk.com/cloudhelp/2024/ENU/AutoCAD-DXF GUID-EF68AF7C-13EF-45A1-8175-ED6CE66C8FC9
- Live VPORT (DXF) 2024: GUID-8CE7CC87-27BD-4490-89DA-C21F516415A9
- ezdxf VPORT internals: ezdxf.readthedocs.io/en/stable/dxfinternals/tables/vport_table.html

END-OF-REPORT
