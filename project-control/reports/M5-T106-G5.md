# M5-T106 — G5 security review (security-reviewer "sec-t106", read-only)

> Transmission history: pinned at f4c4ab3a (material 966cca75 byte-identical to it), delivered as two SendMessage parts
> (1/2, 2/2) ending with END-OF-REPORT; no truncation. Joined verbatim by the orchestrator (transport wrapper tags removed
> only). Verdict PASS for the unwired builder. MED-1 (a huge-integer coordinate escapes untyped as OverflowError) and LOW-1
> (the :812 footprint echo) are before-wiring preconditions for PKT-E, taken into the required-corrections round.

---

M5-T106 G5 SECURITY REVIEW (read-only, sec-t106) — massing pre-wiring 2 (DB-069 a-d; DB-061 b) — PART 1/2

PIN: HEAD f4c4ab3a (matches expected). Material 966cca75: exactly the 3 allowed paths (massing_model.py, test_massing_model.py, producer report); +146/-43 in the module. Both source files BYTE-IDENTICAL 966cca75 → HEAD (empty diff; no later commit touches them). Module blob at HEAD 6bee1f79, test 75138a0a. Method: clean-diff read + in-memory hostile probes from services/api (shapely, Py3.11) + the delivered suite.

SCOPE — clean. Zero new deps (import changed GEOSException→ShapelyError, both admitted shapely; AST allowlist test enforces it). Unwired: grep finds NO importer of massing_model under services/api/app — only its own test. proposal.py (forbidden) untouched. Green at reviewed content: ruff clean (module+test); pytest test_massing_model.py = 75 passed; both accepted goldens (b7fa98…, e23b5c…) byte-identical (test_t106_as6).

POSTURE — pure offline deterministic geometry. No secrets/env, no network/SSRF, no SQL/shell/eval, no filesystem/storage, no logging sink, no AI/ingested text (no prompt-injection surface), no auth/privilege. Cross-tenant/upload/redaction remain the WIRING packet's duty (enforce tenant/auth on scenario_id / property_geometry_version_id / rule_release_id; treat lot+outline as untrusted upload) — carried forward, unchanged.

VERIFIED GUARDS (all probe-confirmed):
- DB-069 a (raw-range check): NYC-2263 check runs on `raw` (all parsed vertices) at _prepare_ring:344, BEFORE the closing-duplicate drop and collinear collapse. Probe: an out-of-range value (2e6) in the collinear-spike slot AND in the closing-duplicate slot each refuse lot_ring_out_of_nyc_bounds (no bypass). Magnitude-first precedence preserved (1e154 → coordinate_out_of_range).
- DB-069 b (wrap widening): `except ShapelyError` (:198) wraps the whole family — probe confirms TopologicalError (genuine non-GEOS ShapelyError) → geometry_engine_error; MassingModelError (ValueError, disjoint from ShapelyError) passes through with its own reason; re-raise uses `from exc` — nothing swallowed.
- DB-069 c: in-X/out-of-Y and later-vertex-out probes present and pass.
- DB-069 d (direct echoes): _preview (:211) bounds the lot-vertex, source and height echoes — 200k-char lot vertex → 184-char message; source 200k → 198; repr escapes CR/LF (no log-forging).
- Resource use: _require_raw_vertices_in_nyc_bounds is one O(n) pass, n bounded by MAX_OUTLINE_VERTICES (checked at :315 before the loop). No new unbounded work.

(continues 2/2)

---

M5-T106 G5 SECURITY REVIEW — PART 2/2 — FINDINGS + VERDICT

All findings ADVISORY / non-blocking for THIS unwired packet; MED-1 and LOW-1 are BEFORE-WIRING preconditions that must be closed in/before PKT-E (M5-T107, which feeds USER geometry in) — route to DISCOVERY_BACKLOG as DB-069 extensions. This mirrors how M5-T088 F-HIGH-1 and M5-T098 F-LOW were classed: ADVISORY for the unwired builder, BLOCKING for the wiring packet. No regression: both are PRE-EXISTING (present through accepted T088/T098) and outside DB-069 (a)-(d)'s enumerated scope; this packet touched neither site.

MED-1 (typed boundary incomplete — untyped OverflowError escapes both public paths). A huge-INTEGER coordinate (a JSON integer literal beyond float range: 10**400, any int ≳10**309) makes _is_finite_number (:230-237, reached at :327) call math.isfinite(bigint) → OverflowError "int too large to convert to float". It escapes build_massing_model UNTYPED: @_wrap_geos_errors catches only ShapelyError (:198); the B0 try/except catches only ProposedMassingError (:810). Probe-confirmed on lot AND footprint. A float 1e300 is fine (magnitude bound → coordinate_out_of_range); only out-of-float-range ints trip it. Reachable once wired if the route hands json.loads big-ints (or Any-typed coords) to the builder. Fix: in _is_finite_number treat out-of-float-range ints as non-finite (magnitude compare before math.isfinite, or try/except OverflowError), OR PKT-E rejects numeric literals outside MAX_COORD_ABS before the call. Note: the general docstring (:800-801 "Every failure is a typed MassingModelError") is pre-existing and over-claims here.

LOW-1 (echo bound incomplete — footprint path). The B0 error is re-echoed UNBOUNDED at :812 (f"…B0 contract validation: {exc}"). Probe: a 200,000-char footprint vertex → a 200,144-char MassingModelError (log/response amplification). The NEW docstring (:66-68, "every refusal echo of caller input is length-bounded … a huge pasted vertex cannot amplify a message") is not literally true for the footprint path. Root cause proposal.py:250 {vertex!r} (out of scope; already flagged M5-T098 F-LOW-2); in-scope one-line mitigation: {_preview(exc)} at :812.

INFO: _preview calls repr() unguarded — a deeply-nested list → RecursionError, a hostile __repr__ → propagates (probes). NOT reachable via a typed JSON route; noted only if an in-process caller passes non-JSON objects. __float__ never called (isinstance short-circuits); bytes/NaN/inf → typed non_finite.

M5-T106 G5 VERDICT: PASS

END-OF-REPORT
