# M5-T063 G2 gate — producer self-check (recorded by the orchestrator)

G2 is the producer self-check gate; the CLI records it under --reviewer orchestrator (it
rejects the producer's own name). Recorded at the corrected identity 853a1d25 (material
f33b752c + the [ORCH-CORRECTED per G3-F1/G4-C-1] cluster).

## Producer self-check record (loop-3 worker, run persistent3-local-09)

The loop worker built the 11-file rider material in one run through an in-run REVISE arc that
included ONE test-only assertion correction (the hostile-markup residue expectation — the
sanitizer charset was already correct; the corrected expectation is explained in the producer
report's "Rework correction" section and was verified TRUE by both G3 and G4 independently).
The final in-run revisions converged the report to the cap-safe evidence-repair form with the
supervisor packet specified for harvest. The producer report (part of the material) is the
self-check artifact.

## Orchestrator corroboration at harvest + the wave cycle

- `python tools/modularity_check.py --check` (wt-m5t063 repo root) — exit 0, failures 0
- LF-normalized digests of the 11 files verified byte-equal after cherry-pick to f33b752c
  (ALL-MATCH); the 12th allowed_path (PropertyOverview.tsx) deliberately byte-unchanged
- CI at f33b752c: all green incl. the web jobs (the material's only web proof, thin client)
- Wave: G5 PASS; G3 FAIL (F1) + G4 FAIL (C-1) — BOTH independently the SAME single defect
  (the announcer's missing entered_bbl === evaluated_input.bbl correspondence gate); ONE
  tagged [ORCH-CORRECTED] cluster 853a1d25 (gate + comment + mutation-sensitive regression
  test); CI at 853a1d25 all green; delta re-attestations by the SAME three reviewers ALL PASS
  at the corrected identity (G3 F1 CLOSED, G4 C-1 CLOSED, G5 carry - pure tightening).

G2 verdict: PASS — the self-check obligation is satisfied by the producer's honest report,
the corroborating evidence chain, and the closed correction cycle; independent review is the
G3/G4/G5 wave + deltas (recorded separately) and the HJ/DCV batch at the corrected identity.
