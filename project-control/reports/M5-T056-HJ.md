# M5-T056 HJ — independent human-journey review (saved VERBATIM from the reviewer return; this header line is the orchestrator's)

Review complete. Returning the G3 report.

---

# G3 Human-Journey Gate Report — M5-T056 (DB-036 condo-surface rider cluster)

**Reviewer:** human-journey-reviewer (independent, read-only)
**Task:** M5-T056 — close DB-036 riders (b)/(c)/(e)/(f)/(g) on the condo surfaces + (h) on the condo-records API
**Directive regime:** in-regime — D-066-R001, D-073-R006, D-077-R002/R003
**Frozen reviewed identity:** frozen head `db0cf0f8`; material `5b7b4b30` (8 files); live `bd408ab9` = disjoint peers (M5-T055/D-078/M5-T057).
**Verdict: PASS** (4 advisories, none blocking)

## Identity / provenance verification

- `git merge-base --is-ancestor 5b7b4b30 HEAD` → YES; `db0cf0f8` also ancestor of HEAD `bd408ab90847d52fa926fbe83b7a5249663f5361`.
- `git diff --stat 5b7b4b30 HEAD` over all 8 M5-T056 paths → **empty** (zero drift); `git status --porcelain` for those paths → clean. Reading the working tree therefore reads the frozen material content.
- Producer report's "This pass changes no code" is a resubmission artifact — the material is committed in `5b7b4b30` (8 files, +635/-26) and is byte-stable at HEAD. Verified against source, not the producer's map.
- Web behavior proves only in CI (thin client, no npm). I verified the test **code** asserts the correct behavior and relied on the orchestrator's stated CI-green at HEAD for execution evidence.

## Journey walked (298 Wallabout: billing lot OR unit BBL entered)

The condo-records surface is a **live** channel (`useCondoRecords(bbl)` in `PropertyOverview.tsx:307` and `ReportView.tsx:45`), unlike the M5-T045 profile channel that had no production caller. Both surfaces derive from the ONE `deriveCondoSurface` decision (`PropertyOverview.tsx:197`) and render the same `CondoRecordsChannelSection`.

## Rulings on items 1–5

**1. Rider (b) — entered vs billing, with a UNIT BBL: PASS / rider CLOSED.**
`PropertyOverview.tsx:258-266`: `enteredLot = recordsView.enteredBbl ?? "not recorded (unknown)"`; `billingLot` renders the BBL only when `billingBblStatus === "recorded" && billingBbl`, else "not recorded (unknown)". A unit input therefore shows the entered unit BBL under "Condo lot you entered:" and moves the unknown to "Condo billing lot:" — never the unit BBL relabelled as billing. The API emits `entered_bbl` unconditionally (`condo_records.py:533`) and `billing_bbl_status` (`:537`); the client parses `entered_bbl` through the shared `validateBblInput` read-only (`condo-records.ts:269-273`), null on malformed. Label language is plain ("Condo lot you entered" / "Condo billing lot") — my HJ-1 ask satisfied. Proven by `condo-resolution-display.test.tsx:722-742` and `condo-records.test.ts:416-433`.

**2. Rider (c) — dangling reference: PASS / rider CLOSED.**
"professional-review determination above" is fully removed from production (grep finds only negative test assertions). Replacements: records intro "shown for reference **under the development limits above**" (`PropertyOverview.tsx:264`) and withheld/conflict panel "**The development limits above govern.**" (`:280`). The reference is **accurate on both surfaces**: `DevelopmentLimits` renders a real `<h2>Development limits</h2>` (`DevelopmentLimits.tsx:112`) that is always present and sits directly above the condo section on the screen (`PropertyOverview.tsx:316` → `:336`) and on the brief (`ReportView.tsx:91` → `:97`), and it shows "Not calculated" under the withhold — so the analyst genuinely sees the section it points to. My HJ-2 ask satisfied. Proven by `condo-resolution-display.test.tsx:444-453, 744-749`.

**3. Rider (e) — zoning-status honesty: PASS / rider CLOSED.**
Divergent notice gated `anyZoningRecorded && recordsView.divergentZoningNotice` (`:270`); ZTLDB gap gated `anyZoningUnknown && recordsView.recordedZoningDependency` (`:271`). The gap copy names the concrete source in plain English ("Zoning Tax Lot Database (ZTLDB)") and, in the mixed case, scopes to "not yet shown for **every** base lot" and states "recorded districts shown above are **unaffected**" (`:251-253`). The no-zoning fixture (`channelMultiLotNoZoningDoc`, `condo-resolution-display.test.tsx:751-761`) proves the divergent notice is absent and the gap note present; the mixed fixture (`:763-776`) proves both show. Parser + fallback proven in `condo-records.test.ts:435-457`. My HJ-4 ask satisfied. (See Advisory 1/2 on production reachability.)

**4. Rider (f) — headings outline: PASS / rider CLOSED.**
`<h2>City records for this condo</h2>` (`:263`) and `<h2>Recorded base lot for this condo</h2>` (`:231`) are real h2 elements (they were `<strong>` in T045 — my A3 advisory). They read as sensible peers of the "Development limits" and "Site context" h2s on the screen, and of the property-label + Development-limits h2s on the brief. Outline asserted via `getByRole("heading",{level:2})` on the screen (`condo-resolution-display.test.tsx:778-788`) and the brief (`report-view.test.tsx:477-492`). My HJ-5/T045 carry satisfied.

**5. Journey coherence: PASS.**
ONE `deriveCondoSurface` governs both surfaces; the section component is shared; the condo decision inputs (profile + bbl) are identical on screen and brief, so nothing renders on one that is withheld on the other. The new locals (`anyZoningRecorded`, `anyZoningUnknown`, `enteredLot`, `billingLot`, `zoningGapNote`) are presentation derivations inside the section from already-parsed view fields — not a second withhold/allow decision; `withholdAllowances` is unchanged (AS-7 preserved). The multi-lot case lists **all** base lots (`:268`), never collapsed, and withholds every computed allowance ("Not calculated"). Confirmed by `condo-resolution-display.test.tsx:525-546, 668-704`.

Riders (g) and (h) (not human-journey-visible) are implemented: (g) the retrievedAt value is pinned char-for-char including colons at the parser and on both surfaces, killing a `boundedTimestamp→boundedToken` revert (`condo-records.test.ts:459-470`; screen `:790-795`; brief `:486-491`); (h) the 422 `detail.raw_value` repr is length-capped at 256+marker, short reprs byte-identical, oversized truncated, bound end-to-end (`test_condo_records_api.py:572-625`).

## Advisories (non-blocking)

- **A1 — Recorded-zoning branch is dormant in production.** `_recorded_zoning_for` (`condo_records.py:396-397`) always returns `None`, so every base lot is `recorded_zoning_status="unknown"` today. The divergent notice, the partial-gap copy, and any "recorded zoning: R6/R7-2" value are exercised **only by synthetic fixtures**; a real analyst always sees "not recorded (unknown)" for every lot. This is honest and by design — the gap names `RECORDED_ZONING_DEPENDENCY` (ZTLDB/spatial lookup, out of scope, routed to the orchestrator). Parallels my M5-T045 dormancy finding; flag for the future zoning-propagation packet.
- **A2 — Divergent-notice suppression is a behavior change.** T056 gates the "permanent no-collapse boundary" divergent-zoning notice behind `anyZoningRecorded`; since zoning is always unknown today, that notice is never shown live. I judge this defensible (raising "divergent zoning is a legal question" while displaying zero zoning would be premature/vacuous) and it directly satisfies AS-3. The no-collapse honesty is preserved by the full base-lot list + the withhold + the ZTLDB gap note. Owner should be aware the API docstring still calls the notice "permanent … stays visible."
- **A3 — Whole surface is flag-gated (`INTERNAL_RULE_EVAL_ENABLED`).** When off, the route 404s → `route_absent` → `non_condo` → the section renders nothing. This is the same flag as the internal property flow, so it is reachable when an analyst is in that flow. The live 298 Wallabout multi-lot resolution depends on the resolver + live SODA, which I cannot exercise on a thin client.
- **A4 — Billing-class redundancy.** A billing-class input shows the identical BBL under both "Condo lot you entered" and "Condo billing lot" (honest but visually redundant; a parenthetical could clarify).

## Originating T052 riders — disposition

- HJ-1 (rider b, entered-vs-billing labels + plain language): **CLOSED**
- HJ-2 (rider c, dangling professional-review reference): **CLOSED**
- HJ-4 (rider e, ZTLDB gap surfacing + divergent-notice gating): **CLOSED**
- HJ-5 (rider f, real h2 headings in the outline): **CLOSED**

**Verdict: PASS.** All four of my originating T052 riders plus (g)/(h) are closed, the ONE-decision guard coherence is preserved, both surfaces stay in lockstep, and the multi-lot condo reads honestly (full set, never collapsed). The advisories are dormancy/design observations for the orchestrator's discovery sweep, not defects.
