# M5-T104 — G4 test-adequacy review (qa-engineer "qa-t104", read-only)

> Transmission history: pinned at cde8fc0e, delivered as three SendMessage parts (1/3..3/3) ending with END-OF-REPORT;
> no truncation. Joined verbatim by the orchestrator (transport wrapper tags removed only). Verdict PASS: 12/12 producer
> mutants and 5/5 reviewer weakenings killed (one equivalent: B* vs B is not modelled); all 20 DB-055 (i) operators
> executed; the two M5-T094 G4 survivors now die. Advisories A1-A3 are routed to the next sheet_* touch.

---

M5-T104 (D-087 PKT-K2) G4 test-adequacy review — qa-engineer "qa-t104", read-only. Part 1/3: method + producer mutants.

PIN: HEAD cde8fc0e at start (== expected). Material test/citation content == c0ce1ae3 (the cde8fc0e harvest recorded a 3-file MATCH). git diff c0ce1ae3^..cde8fc0e on source = sheet_primitives.py (1 line: apply_matrix §8.3.3→§8.3.4) + test_sheet_reader.py (+178, 37→46 tests); exactly the three allowed paths, zero new deps.

METHOD: local 3.11 via a copy of the outside-repo bare-package shim. In-memory mutation only, each on the CONSUMING namespace (never a .py written to the repo/scratch — my Write and shell file-writes are guard-blocked, so I ran the harness through `python -c` execution). BASELINE = 55 passed (46 sheet_reader + 9 split-equivalence), and every one of the 10 target tests GREEN before mutating.

PRODUCER MUTANTS re-run (each must move an asserted value → RED): 12/12 KILLED [RED-assert].
| # | consuming-ns mutant | target test | result |
|---|---|---|---|
| M1 | _StreamRun._curveto pre-fix p0=apply(ctm,current) | mid_path_cm | KILLED |
| M2 | sheet_interpreter._apply_matrix=identity | rotated_rectangle | KILLED |
| M3 | _PATH_HANDLERS['v'] ctrl1_is_current=False | v_curve | KILLED |
| M4 | _PATH_HANDLERS['y'] ctrl2≠endpoint | y_curve | KILLED |
| M5a | _PAINT_STROKE=∅ | all_paint_operators | KILLED |
| M5b | _PAINT_FILL=∅ | all_paint_operators | KILLED |
| M5c | _PAINT_CLOSE_FIRST=∅ | all_paint_operators | KILLED |
| M6 | _IGNORED−{W,W*} | clip_operators | KILLED |
| M7 | sheet_reader._concat_matrix=(m,ctm→m) | text_line_moves | KILLED |
| M8 | sheet_reader._concat_matrix=(m,ctm→m) | text_show_TJ/'/" | KILLED |
| M9 | _StreamDecoder.decode_form constant key | two_distinct_forms | KILLED |
| M10 | _StreamDecoder.charge_decoded per-stream | multi_stream_budget | KILLED |

The TWO M5-T094 G4 survivors (A-1 wrong-key form memo; A-2 per-stream vs per-document byte accounting) are now DEAD: M9 and M10 both redden. DB-064 (a)/(b) closed. Continues 2/3.

---

M5-T104 G4 — part 2/3: my own weakenings, operator coverage, by-value trap.

MY WEAKENINGS (in-memory, consuming-ns): 5 KILLED + 1 equivalent.
| id | weakening | target | result |
|---|---|---|---|
| W-A | TL never sets leading (T* uses stale leading) | text_line_moves | KILLED (D→640≠646) |
| W-B | single-quote ' does NOT advance the line | text_show | KILLED (P stays 470≠460) |
| W-C | W/W* actually CLIP (drop the pending path) | clip_operators | KILLED (0 polylines≠1) |
| W-D | re builds corners with x−w (negative-width-style sign bug) | rotated_rectangle | KILLED (verts move) |
| W-F | TJ joins only the first array piece | text_show | KILLED ("X"≠"XY") |
| W-E | B* treated as B | all_paint | EQUIVALENT (survives-by-design) |

W-E note: _PAINT_STROKE/_PAINT_FILL treat B and B* identically and _PAINT_CLOSE_FIRST excludes both; the profile does not model even-odd vs nonzero fill, so "B* treated as B" is byte-identical output — no test can or should kill it. Not a defect. (This directly answers the four suggested probes: ' not moving, W* clip, re negative width, TJ number-adjust — all caught; TL/T* leading also caught.)

OPERATOR COVERAGE (AS-2): I traced _StreamRun._execute across the six new coverage tests. All 20 DB-055(i) operators are actually EXECUTED, not merely present in source: re v y s f F f* B* b b* n W W* TL Td TD T* TJ ' " — MISSING: NONE. Each is also meaningfully asserted (both directions where relevant: M6 proves W/W* aren't refused, W-C proves they aren't dropped; M5a/b/c prove each paint facet). The "true rotated rectangle" replaces the old rotated-LINE: len(points)==4 with all four vertices pinned == _rotate_then_translate(corner), killed by M2 and W-D.

BY-VALUE TRAP re-confirmed (validates the producer's central methodology): patching the CLASS METHOD _StreamRun._op_v with a raising stub left test_v_curve GREEN — a FALSE survivor, because _PATH_HANDLERS['v'] holds the original function object by value (`_PATH_HANDLERS['v'] is _op_v` → True). The producer correctly mutated the dict entry (M3), which reddens. So the mutation record is honest about the effective consuming namespace. Continues 3/3 (semantics-vs-impl, committed-mutation-test question, findings, verdict).

---

M5-T104 G4 — part 3/3: expected-value provenance, committed-mutation question, findings, verdict.

EXPECTED VALUES — semantics vs implementation:
- Rotated-rectangle vertices, paint (stroked/filled/closed) flags, clip path-kept, and ALL text origins (A700/B680/C660/D646; XY470/P460/Q450) are DERIVED FROM PDF SEMANTICS (re corners × rotation CTM; operator definitions; Td/TD/T*/TL/'/" positioning under identity CTM) — hand-derivable and refactor-stable. Good.
- The three pinned INTERIOR points — v points[1]=(21.7188,19.2578), y points[1]=(4.1943,7.041), mid_cm points[1]=(14.1602,13.3398) — are COPIED FROM THE IMPLEMENTATION: I confirmed each curve subdivides to 10–14 flattened points, so points[1] is a midpoint-de-Casteljau output, not a PDF-Bezier value. The semantic ANCHORS (points[0]=start, points[-1]=endpoint) are asserted separately and are semantic; the interior literal is mutant-proven load-bearing (M1/M3/M4) and is a hardcoded literal, not an imported constant (so not tautological). Net: correct + non-vacuous, but BRITTLE to a legitimate flattener refactor.

NO COMMITTED MUTATION TEST — acceptable here: (1) the interpreter internals the mutants patch (_PATH_HANDLERS, _PAINT_*, _IGNORED, _StreamDecoder methods) are owned by the parallel M5-T103 and forbidden to this packet; committing patches on them would couple across packets; (2) precedent M5-T083/T094 recorded (not committed) proofs; (3) the committed POSITIVE assertions are the durable guards and I independently re-ran every mutant to confirm they redden. So the "record red runs, don't commit mutation tests" choice is sound.

FINDINGS
- BLOCKING: none.
- ADVISORY A1 (test brittleness): the 3 interior-point literals are flattener-implementation-derived; a correct flattener refactor would falsely redden them. LATER (post-M5-T103 stabilization) prefer semantics-derived guards — max-deviation-from-true-Bezier (as test_asymmetric_bezier already does) PLUS a start-tangent/continuity check that distinguishes v's first-control-is-current — which are non-vacuous AND refactor-stable, and need no internal names.
- ADVISORY A2: when the sheet_* internals stabilize, optionally commit consuming-namespace mutation tests; until then the recorded+independently-re-run red runs suffice.
- ADVISORY A3: sheet_primitives.py still cites §8.3.3 on the Matrix type-alias comment (line 28); DB-055(e) named only apply_matrix, so correctly out of scope — already logged as producer discovery D2. §8.3.4 is the right section for the point-mapping equation.

Baseline 55 green; 12/12 producer mutants + 5/5 my weakenings KILLED; 20/20 operators executed; scope clean; zero new deps; AS-1..AS-4 all satisfied. Advisories are non-blocking and route to the next sheet_* touch.

M5-T104 G4 VERDICT: PASS
END-OF-REPORT
