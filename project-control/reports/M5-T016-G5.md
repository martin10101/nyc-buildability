# G5 Security Gate Report — M5-T016 (Address Confirm card + ZoLa deep-link + handoff, address-entry Packet 2)

> Preserved VERBATIM by the orchestrator from the security-reviewer agent return
> (transport entity-decoding only, per the report-preservation rule). Reviewer preamble:
> "I have completed the full independent G5 security review. All evidence is gathered and
> verified. Here is my gate report."

- Gate: G5 (security), independent reviewer, read-only (ADR-005)
- Reviewed content identity: producer commit `65d72bfd` (parent `ffaa85dc`); apps/web verified byte-identical through head `ed110e6a`
- Frozen-identity check: `git diff 65d72bfd..ed110e6a -- apps/web` → **empty** (confirmed)
- Producer commit scope: exactly the 7 allowed paths (6 apps/web files + producer report); `package.json`/`package-lock.json` **not** in the diff stat and are in `forbidden_paths` (confirmed)
- Verdict: **PASS** (2 advisories, 0 required-corrections, 0 blocking)

## Data-flow trace — the two new href sinks

### Sink 1 — ZoLa external link (`AddressConfirmCard.tsx:122-139`, href at :126)
`href={`${ZOLA_BBL_URL_PREFIX}${encodeURIComponent(canonicalBbl)}`}`
- `ZOLA_BBL_URL_PREFIX` (`:36`) is a module constant `"https://zola.planning.nyc.gov/bbl/"` — host+path prefix never concatenated from response data.
- `canonicalBbl` (`:48`) = `validation?.ok ? validation.canonical : null`; `validation` (`:47`) = `validateBblInput(view.canonical.bbl)` (only when non-null).
- `view.canonical.bbl` = `boundedToken(canonical.bbl, 32)` (`address-api.ts:263`) — already charset-restricted to `[A-Za-z0-9._-]`.
- `validateBblInput` (`bbl.ts:36-72`) returns `ok:true` **only** for `^[0-9]+$` AND `length===10` AND first digit 1-5; `canonical` is that trimmed 10-ASCII-digit string. A value matching `[1-5][0-9]{9}` cannot carry a URL path/query metacharacter — no path-segment or query break-out is possible.
- Anchor renders **only** when `canonicalBbl` is truthy (`:122`); the invalid/hostile branch renders `<p class="section-note" data-testid="zola-link-absent">` (`:135-138`) — **no anchor, not a disabled/empty one**.
- `encodeURIComponent` applied (redundant belt-and-suspenders on pure digits); `target="_blank"` + `rel="noopener noreferrer"` present (`:127-128`). **PASS.**

### Sink 2 — Continue handoff (`AddressConfirmCard.tsx:217-230`, href at :220)
`href={`/property/confirm?bbl=${encodeURIComponent(canonicalBbl)}`}` via `next/link`.
- Same validated `canonicalBbl`; `encodeURIComponent` applied; internal navigation (no `target`/`rel` needed).
- Renders **only** when `canonicalBbl` truthy; else `<p ... data-testid="confirm-continue-absent">` (`:226-229`) — no link. Nothing else from the document reaches the query string (only the 10-digit BBL). **PASS.**

## Numbered findings

1. **(advisory)** In-suite source-scan test asserts absence of `dangerouslySetInnerHTML` only (`address-resolution.test.tsx:717`); it does not additionally assert absence of `eval` / `new Function` / `window.open`. My independent scan (`Grep` over `apps/web/src/components/address` and `address-api.ts`) confirms **none of these sinks exist in the current source**, so this is a test-completeness note, not a code defect. Optional: extend the scan set. No action required for this gate.

2. **(advisory, cross-boundary)** The provenance disclosure faithfully renders whatever the backend places in `provenance.request_params` and `provenance.endpoint`. Secret-secrecy therefore depends on the (read-only, out-of-scope) backend never emitting the Geoclient subscription key there. Verified in `services/api/app/connectors/geoclient_address.py`: key is sent only via the `Ocp-Apim-Subscription-Key` header from `GEOCLIENT_SUBSCRIPTION_KEY` (`:135-136`, `:590-591`), and `:739` states `"request_params": dict(params),  # key is NEVER a param`; endpoint is the constant `ENDPOINT_URL` (`:210,:738`), of which the card shows HOST only. No leak; recorded so the invariant stays visible.

## Threat-model checklist (all confirmed)

- **(1) href sinks:** both traced above; hostile/invalid path renders honest section-note absence, never an anchor. **PASS.**
- **(2) Provenance injection surfaces:** `sourceId`/`retrievedAt`/`connectorCorrelationId`/`grc`/`grc2` = `boundedToken`; `responseDigest`/`sourceFactsNotEmittedReason`/echo/normalized = `boundedText`; `endpointHost` = `hostOrNull` → `new URL().host` → `boundedToken` (host only, no full URL/params — `address-api.ts:223-230`, rendered `AddressConfirmCard.tsx:152-154`); `requestParams`/`sourceFacts` rendered as bounded key/value **text nodes** (`:177-203`). No caller/external value reaches a URL-ish/attribute context. **PASS.**
- **(3) Source scan:** no `dangerouslySetInnerHTML`, `innerHTML`, `eval`, `new Function`, `window.open`, `document.write`, `setAttribute`, `insertAdjacentHTML` in any new/changed source (independent `Grep`). Only `target="_blank"` (ZoLa) carries `rel="noopener noreferrer"`. Import-specifier allowlist enforced by test (`react` | `next/link` | `@/` | `./` only, `:736-744`) — extended only to `next/link` as the producer log states. **PASS.**
- **(4) S6 adequacy:** hostile payloads cover `street_name_normalized`, `borough_name`, `request_params.street`, `source_facts.original_value` (confirm S6, `:358-392`) and `canonical.bbl` (confirm S2 hostile-BBL, `:236-253`). Both do a full anchor sweep over `container.querySelectorAll("a")` + element-absence sweep (`script,img,iframe,style` length 0) + `window.__pwned` undefined; S6 asserts every anchor href ∈ {`#bbl-input`, ZoLa prefix, `/property/confirm?bbl=`}. Hostile-RESOLVED fixture discharges M5-T015 G5 **F-2**. **PASS.**
- **(5) Bounding additive:** `address-api.ts` diff (`65d72bfd^..65d72bfd`) is additive-only — new interfaces/fields/helpers; existing bounding lines byte-preserved. **PASS.**
- **(6) No new deps / secrets / logging:** package files untouched (diff stat + forbidden_paths); only public ZoLa host constant; no `console.*`, no `service_role`/`Bearer`/`apikey`/secret literals; the only `INTERNAL_RULE_EVAL`-adjacent match is a pre-existing test prop `ruleEvalEnabled` (`:242`), not env wiring. **PASS.**
- **(7) Carried M5-T015 items:** F-2 discharged (above); **F-1** (flag-name divergence `INTERNAL_RULE_EVAL_UI` vs `INTERNAL_RULE_EVAL_ENABLED`) carried as report-only owner note (producer report §3.6, task `path_notes[4]`) with **no** env-wiring change in the diff. **PASS.**

## CI evidence (verified stored, not re-executed — thin client)
`project-control/reports/M5-T016-ci-evidence.txt`: run 34679638323 — `web` SUCCESS, `web-e2e` SUCCESS; address tests 32 (resolution) + 11 (confirm) passed, whole web suite 28 files green. `web-dependency-security` FAILURE and `control-plane` FAILURE are pre-existing owner-gated items; this packet touched no dependency surface (confirmed: `package.json`/lockfile absent from the diff), so they are out of this packet's G5 scope.

**VERDICT: PASS**

---

# G5 Delta-Attestation — M5-T016 at corrected head `b1129f34`

> Preserved VERBATIM by the orchestrator from the same security-reviewer agent, resumed for
> the delta (transport entity-decoding only). Reviewer preamble: "Delta verified — the change
> at `b1129f34` is exactly as described and confined to the test module."

- Delta reviewed: `git diff ed110e6a..b1129f34 -- apps/web` → **one file, +11 lines**, `__tests__/address-confirm.test.tsx` S5 test 1 only. No production source in the apps/web delta (the other two commits `5dc283ac`/`4cae06bd` are control-plane only, outside apps/web).
- Nature of the change: two additive `expect(body).toContain(...)` presence assertions discharging G4 C1 —
  1. `retrieved ${boundedRetrievedAt}`, where `boundedRetrievedAt = doc.provenance.retrieved_at.replace(/[^A-Za-z0-9._-]/g, "")`;
  2. `Geosupport return codes: ${doc.grc} / ${doc.grc2}`.

## Security assessment of the new test code
- The `replace(/[^A-Za-z0-9._-]/g, "")` is **assertion-side only**: it is applied to a hard-coded fixture string (`doc.provenance.retrieved_at`) to compute an expected bounded value that mirrors `boundedToken`'s charset. It sanitizes nothing in production and touches no untrusted input path.
- Both additions are `toContain` presence checks — they can only make S5 stricter, never weaker. No assertion was removed or relaxed.
- No new sink, no href construction, no import/specifier change (the import allowlist and `next/link`-only extension are unchanged), no dependency change, no `dangerouslySetInnerHTML`/`eval`/`window.open` introduced.
- The two production href sinks (ZoLa, Continue), the provenance-disclosure bounding, and the additive `address-api.ts` view are byte-identical to the reviewed `65d72bfd` content (apps/web delta contains no non-test file).

## Result
- G5 **PASS** verdict **stands unchanged** at `b1129f34`.
- Advisory A1 (source-scan test asserts only `dangerouslySetInnerHTML`) — **stands unchanged** (the new assertions are unrelated to the scan test; sinks remain absent).
- Advisory A2 (card faithfully renders backend `provenance.request_params`; secrecy rests on `geoclient_address.py` never emitting the subscription key — verified `:739`) — **stands unchanged**.
- CI evidence at corrected head (run 34680826441, web + web-e2e SUCCESS, vitest 32/32 + 11/11, 28/28 files): consistent with additive test-only assertions passing; no security-relevant regression.

**VERDICT: PASS (unchanged at b1129f34)**
