# M5-T027 Independent Directive-Compliance Verification (D-001) — DCV Report

> Saved VERBATIM by the orchestrator from the directive-compliance-verifier agent return
> (2026-09-14; transport entity-decoding only, per the report-preservation rule).
> Verifier ≠ producer ≠ gate reviewers.

## Restamp ruling

**Pinned head at task assignment:** 2231227a. **Live HEAD at verification time:** ddfaedf90a64 (one commit later, exactly the "possibly one later control-plane-only commit" scenario anticipated in my instructions). `git show --name-only ddfaedf90a64` touches only `.claude/rules/PROGRAM_KNOWLEDGE.md` and `docs/WORKING_KNOWLEDGE.md` — neither in M5-T027's `allowed_paths`.

I independently recomputed the task's content identity via `project_control._task_git_identity(reg_mod, task)` (no `reviewed_sha` argument, i.e. computed live): result `manifest_sha256 = 7d1697b004c9751842e5c9c26806162ae25861c398435b29175f3c3d7c79f58d`, `resolved_sha = ddfaedf90a64…`, `error = None` (tree clean, `require_clean=True` satisfied). **This is byte-identical to the `content_manifest_sha256` recorded in both G3 and G4** (`project-control/gates/M5-T027-G{3,4}.json`, reviewed_sha `f1955348…`). I also ran `git diff f1955348 ddfaedf90a64 -- <all 7 allowed_paths>` directly → 0 lines of diff. The task file's own diff over that range touches only `updated_at`; `allowed_paths` and `directive_refs` are byte-unchanged.

**Ruling:** any head H is safe to restamp at without re-review **iff** (1) no commit after f1955348 up to H touches an M5-T027 allowed_path (checked directly for ddfaedf90a64: true), (2) `_task_git_identity` recomputed at H reproduces the gate-recorded manifest sha with a clean tree (checked: true, identical), and (3) the task's `allowed_paths`/`directive_refs` are byte-unchanged (checked: true — only `updated_at` moved). **All three hold at the current live head ddfaedf90a64.** Acceptance may restamp `reviewed_sha = ddfaedf90a64` (or 2231227a) on this basis without re-gating.

## Applicability check

Ran `DirectiveRegistry().load().evaluate_task_refs(task)` myself: `applicable_ids == cited_ids == ['D-046-R001', 'D-046-R002', 'D-059-R001', 'D-059-R002', 'D-059-R003', 'D-059-R011']`, `missing_ids: []`, `unresolved: []`. Confirmed exactly the six IDs, nothing more, nothing less. `python tools/validate_directive_compliance.py --check` exits 0, no output.

## Per-requirement verdicts

| Requirement | Verdict | Evidence I personally inspected |
|---|---|---|
| **D-059-R001** (recorded-data semantics) | **PASS** | `git show 2231227a:services/api/app/scenario/constants.py` — `UNUSED_FLOOR_AREA_LABEL`/`UNUSED_FLOOR_AREA_SCOPE_NOTE` state "a RECORDED-DATA COMPARISON, not a ZR 12-10 zoning floor-area difference," name condo net-vs-gross semantics, state a positive/negative result does not "by itself establish unused legal development rights"/"zoning noncompliance," and name the rights-calc preconditions (compatible existing zoning-area input + confirmed zoning-lot extent) — matches source-001.md finding 1 verbatim and requirements.json text. `test_s6_no_forbidden_nouns_and_no_verified_compliant_language` reproduced passing (431/431 scenario suite green, `python -m pytest services/api/tests/scenario -q`). |
| **D-059-R002** (bldgarea-zero fail-closed) | **PASS** | Read `unused_floor_area.py`'s `_vacancy_basis`/branch (b2) directly: bldgarea==0 is COMPUTED only when `numbldgs` is present, usable coverage, and ==0; otherwise routes to `EXISTING_BUILDING_AREA_UNUSABLE`, `professional_review_required=True` (section+root), original zero + numbldgs basis in `assumptions`. Reproduced the reviewer's exact case via `test_s1_zero_with_positive_numbldgs_fails_closed_red_on_old` (ran it myself, passes) and independently confirmed red-on-old by diffing `b57998a6^` (pre-fix) against the fix — old code has no numbldgs awareness (matches G4's independent swap-and-rerun: `5 failed, 26 passed`, reproduced by me via direct code read, consistent). Corrected vacant-lot fixture verified: `test_s1_established_vacancy_zero_existing_area_is_usable_and_computed` now supplies `numbldgs={"value":0.0,...}`. |
| **D-059-R003** (labels from actual evaluation) — **for M5-T027's own scope** | **PASS, scope-limited (see ruling a below)** | `builder.py::_cap_citation_section` derives the section from `cap_provenance["citations"][0]["section"]` (the actual evaluated rule's own citation); `constants.py::draft_cap_label`/`coverage_matrix_rows`/`preliminary_cap_reason` all take the derived section/rule_id, never a hardcoded one. Ran `test_as_r003_low_density_r5_derives_coverage_and_section_labels` and `test_as_r003_higher_density_r6_r12_derives_coverage_and_section_labels` myself (pass): the R5 doc contains "23-21" and NOT "23-22"; the R6-R12/R9A doc (built from the real `r6_r12_residential_far.rule.json` FAR 7.52/§23-22) contains "23-22" and NOT "23-21", and `coverage_matrix` governs-text names "(R5)" vs "(R6-R12)" correctly. |
| **D-059-R011** (external fact, PLUTO zero-with-buildings) | **PASS — independently re-verified by me against the LIVE current official document (see ruling b below)** | Live-fetched `https://www.nyc.gov/assets/planning/download/pdf/data-maps/open-data/pluto_datadictionary.pdf` myself today (2026-09-14; 403 on plain curl, 200 with a browser UA — sha256 `455ebe1f…` on file in scratchpad). Header confirms **"May 2022 (22v1)"** — same document version the directive originally cited, unchanged. Read the BldgArea field description (dictionary p.20-21) verbatim: *"A TOTAL BUILDING FLOOR AREA of zero can mean it is either not available or not applicable. If NUMBER OF BUILDINGS is greater than zero, then a TOTAL BUILDING FLOOR AREA of zero means it is not available. If NUMBER OF BUILDINGS is zero, then... it is not applicable."* — this is the exact rule the code implements (numbldgs>0 → fail closed/unavailable; numbldgs==0 → legitimately computable zero, "not applicable" ≡ no building exists). Also cross-checked the NumFloors sibling rule (p.26) the producer cited from `pluto_soda.py:784-791` — matches verbatim. Also confirmed `pluto_soda.py:201-202`'s "condo net not gross… NOT ZR 12-10 zoning floor area" note against the dictionary's own gross/net-condo and §12-10 language (p.20). No conflict found; connector citations accurate. |
| **D-046-R001** (scale up, ≤3 concurrent, disjoint) | **PASS** | M5-T027 (`4cfdac3c` contracted) ran concurrently with M4-T021 (claimed earlier at `a0303c52`, still in-flight through M5-T027's full G0→G3→G4 cycle) — 2 concurrent producers, within the 3-ceiling. Confirmed via `git log --oneline` interleaving and both task files' status/timestamps. |
| **D-046-R002** (disjointness mandatory) | **PASS** | Compared `allowed_paths` directly: M5-T027 = `services/api/app/scenario/{unused_floor_area,constants,builder}.py` + 3 test files + its report; M4-T021 = `services/api/app/connectors/wide_street_buffer_engine.py` + its test + its report. Zero path overlap, confirmed by direct set comparison, not by trusting either task file's prose. |

## Ruling (a) — D-059-R003 scope

**M5-T027's PASS covers ONLY the two files the requirement text itself names** ("in `services/api/app/scenario/constants.py` (emitted by `builder.py`)") — confirmed fixed and tested (R5 vs R6-R12 derivation both directions). **It does NOT close the directive-level requirement project-wide.** I independently confirmed the G3 finding by grepping the live head myself: `derive.py:82`, `breakeven.py:155`, `comparison.py:118/129`, `ranking.py:114`, `sensitivity.py:128` all still hardcode `"ZR 23-21"`, verbatim. I further confirmed these are genuinely live-wired (not dead code) by tracing imports myself: `services/api/app/api/v1/scenario_analysis.py` imports and calls `analyze_scenario_sensitivity`, `rank_scenario_assumption_sets`, `compare_scenario_assumption_sets`, `find_scenario_threshold` (lines 110-114, 676-800), all of which import `derive_practical_usable_range` from `derive.py` (confirmed in `breakeven.py`, `comparison.py`, `ranking.py`, `sensitivity.py` source). `requirements.json`'s own `applicability.task_ids` for D-059-R003 lists both `M5-T027` AND `M5-T028` — the directive anticipated two contributing tasks. M5-T028 (`project-control/tasks/M5-T028.json`, `status: claimed`, `allowed_paths` = exactly those five files + their tests) is contracted but **not yet accepted**. **Directive-level D-059-R003 remains OPEN project-wide until M5-T028 completes.**

## Ruling (b) — D-059-R011 re-verification method

The producer's own action (re-citing the already-captured directive finding + the connector's in-repo source notes, no live PDF fetch) does **not**, by itself, constitute the "re-verify against the current official dictionary" the requirement demands — G3 correctly flagged this as a minor non-blocking gap (A4). However, my role is to locate primary evidence myself rather than accept any party's claim. **I performed the live re-fetch myself** (see D-059-R011 row above) and independently confirmed, against the actual current nyc.gov document, that: the document is unchanged (still 22v1/May 2022 — no newer dictionary version has superseded it), the connector's citations are accurate, and the code's semantic treatment (numbldgs>0→unavailable/fail-closed, numbldgs==0→legitimately-zero/computable) matches the dictionary's own "not available" vs "not applicable" distinction exactly. **Ruling: D-059-R011 is SATISFIED — not on the strength of the producer's re-citation, but on my own direct, reproducible primary-source verification performed today.** Had I been unable to reach the live document, this would have been UNVERIFIABLE (naming the missing item: a live fetch of the nyc.gov PLUTO data dictionary PDF); that gap did not in fact exist for me.

## Material-identity statement

- Producer commit `b57998a6` and integration/material commit `77558eb5`: `git diff b57998a6 77558eb5 -- <allowed_paths>` = 0 lines — byte-identical cherry-pick, confirmed by me.
- `77558eb5` touches exactly: `project-control/reports/M5-T027-producer-report.md`, `services/api/app/scenario/{builder,constants,unused_floor_area}.py`, `services/api/tests/scenario/{test_scenario_foundation,test_unused_floor_area}.py` — exactly within `allowed_paths`, zero `forbidden_paths` touch (confirmed: no `packages/**`, no `connectors/**`, no `rules/**`, no `apps/web/**`).
- Integration seam commit `bb1b37b8` carries only `project-control/state.json` + `project-control/tasks/M4-T021.json` — confirmed by direct `git show --name-only`, matching the framing given to me.
- `packages/contracts/schemas/v1/scenario.schema.json` untouched (`test_s6_both_schema_copies_are_byte_identical` reproduced passing).
- Live-run self-checks reproduced by me directly: `pytest services/api/tests/scenario -q` → 431 passed; `pytest services/api/tests/scenario/test_scenario_contract.py -q` → 28 passed; `ruff check app/scenario tests/scenario` → clean; `python tools/modularity_check.py --check` → 0 failures, 17 pre-existing warnings, none on the three touched files; `pytest services/api/tests -q --ignore=tests/documents` → 1 failed/2435 passed, the 1 failure and the 15 `tests/documents` collection errors are a pre-existing Python-3.11-vs-3.12 (PEP 695) sandbox gap in `app/documents/units.py:276`, unrelated to and untouched by this task (confirmed via `git status --porcelain` on that file).

## Blocking discrepancies

None found. No gap remains unverifiable — the one candidate gap (R011's live-source re-verification) was resolved by performing the check myself against the primary source.

## Overall verdict: **PASS**

All six applicable requirement IDs (D-059-R001, D-059-R002, D-059-R003 [scope-limited], D-059-R011, D-046-R001, D-046-R002) are SATISFIED on primary evidence I personally reproduced. Restamp to the current live head (ddfaedf90a64) or to 2231227a is safe under the stated conditions. D-059-R003's directive-level (project-wide) closure remains explicitly open pending M5-T028 — this PASS does not close it, and should not be read as doing so.
