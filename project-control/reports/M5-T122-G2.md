# M5-T122 - G2 producer self-check record (recorded by the orchestrator, role self_check)

Producer: frontend-engineer (orchestrator-dispatched subagent) in wt-m5t122; one in-worktree commit 9d9ad38e (parent 55276f2d), cherry-picked to b2e833fd with every file MATCH by git blob identity (9 files: apps/web/e2e/architect-workspace.spec.ts ac0db8ac, apps/web/e2e/responsive-a11y.spec.ts 2f649002, apps/web/src/app/property/architect.css 8235b6ee, apps/web/src/components/architect/CondoRecordsSection.tsx 64b4d685, apps/web/src/components/architect/OverviewExceptionStrip.tsx b49c7475, apps/web/src/components/architect/PropertyOverview.tsx 320c0050, apps/web/src/components/architect/__tests__/condo-records-section.test.tsx c00a834f, apps/web/src/components/architect/__tests__/overview-exception-strip.test.tsx ea2ae851, project-control/reports/M5-T122-producer-report.md d2eeee41).

- **Scope:** Web presentation on the loaded /property overview: CondoRecordsSection.tsx (NEW; the condo surface moved verbatim + the P1 section 5.4 pass: 'Reference only' / 'City record' / 'Human record' tags, a 'Zoning missing for N lots' count, 'Parcels differ' with a recorded-vs-current comparison disclosure); PropertyOverview.tsx (a facade re-exporting every moved name; 401 -> ~120 lines); OverviewExceptionStrip.tsx (a real h2 + aria-labelledby, a stale-row link to the Evidence retrieval status, a true clause for the condo base-lot conflict); architect.css (the identity-withhold alert visually distinct; strip heading; condo tags; the grid comment corrected); unit tests (condo-records-section NEW, overview-exception-strip) and e2e (architect-workspace, responsive-a11y).
- **Producer self-checks (report, OBSERVED):** modularity exit 0 [OBSERVED]; git diff --stat = only allowed paths [OBSERVED]; banned-wording grep clean [OBSERVED]; web unit + e2e results [PREDICTED] until CI on the pushed harvest head (thin client: no local npm/node).
- **[ORCH-HARVEST] at the harvested head b2e833fd (cherry-pick b2e833fd; Python 3.11 local): `python tools/modularity_check.py --check` (cwd .) -> exit 0:   warn review_signal: tools/agent_supervisor/next_task.py - above the warning threshold; consider the module boundary before growing it further.**
- **Discoveries routed (producer):** OQ-1 the condo conflict row keeps the literal 'Unresolved data conflicts' heading (a profile with both a source conflict and a condo conflict shows two rows with that heading, each with a true clause); OQ-2 'Reference only' is a sibling tag because report-view.test.tsx locks the h2 name; OQ-3 the C06 comparison disclosure prints in the brief; D4 the identity alert's new tint also reaches print (colour only); DISCOVERY C07 - the revoked / superseded history list cannot be rendered because SiteDefinitionView exposes only a confirmation count (a lib/condo-records.ts contract addition).

G2 verdict: PASS (self_check; the independent reviews and the DCV follow - dispatched by the successor session per the handoff).
