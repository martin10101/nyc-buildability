# M5-T069 — G4 QA review record (qa-engineer return, VERBATIM)

[Orchestrator record header — not part of the reviewer return: dispatched 2026-09-22 ~21:31 UTC
(a retry — the first dispatch attempt was refused at isolation-worktree creation and never ran)
at frozen 141007c37c58c1a0e3d1297c582b015ca0f20bb1. This reviewer ran in an isolated agent
worktree and executed on a throwaway git-archive extraction, leaving the repo untouched. Return
received COMPLETE in one part with the required END-OF-REPORT marker; transport entity-decoding
only. Verdict PASS; zero blocking; ADV-1..ADV-3; three mutation rows LIVE-OBSERVED matching the
producer harvest table exactly.]

---

All verification is complete. Here is my G4 gate report.

---

# G4 QA-GATE REPORT — M5-T069 (DB-040(s) supersede not-found message unification)

**VERDICT: PASS**

Reviewer: qa-engineer (independent; producer = backend-engineer). Read-only; all execution done on a throwaway `git archive` extraction of the frozen tree in scratchpad — repo untouched (`git status services/api` clean; scratchpad `store.py` restored to `89f998f5`).

**Frozen identity verified (OBSERVED):** frozen submit `141007c3`, material `463f98f9`, CI `a66cf818` all resolve. Extracted files hash-match frozen blobs exactly: `store.py`=`89f998f5`, `test_site_definition_records.py`=`25481bea`, `test_site_definition_api.py`=`5c40b02c`.

## 1. Scoped suite — OBSERVED
`cd services/api && python -m pytest tests/site_definition tests/api/test_site_definition_api.py -q` → **77 passed, exit 0** (ran to completion; matches expected 74 baseline + 3 new). Test files are pure additions (numstat `52/0` api, `71/0` records) — no existing T067 test modified or deleted.

## 2. Mutation matrix — 3 rows LIVE (OBSERVED), 3 ASSESSED
Each row: `sed` the single `raise` arg, run affected tests `-x --tb=line`, restore, re-hash `== 89f998f5`.
- **A1** — store.py:328 (missing) → divergent literal: FIRST fail `test_site_definition_records.py:878` (`str(missing)==str(foreign)`). **LIVE OBSERVED.**
- **B1** — store.py:334 (supersedes_id mismatch) → divergent literal: FIRST fail `:907` (`str(mismatch)==str(missing)==_UNIFIED`). **LIVE OBSERVED.**
- **C1** — store.py:350 (foreign) → divergent literal: FIRST fail `:878`. **LIVE OBSERVED.**
- **A2/B2/C2** (re-echo mutants): **ASSESSED** — a re-echo makes the same equality asserts (:878/:907) diverge first; consistent with the harvest's OBSERVED rows. Restore hash verified `== 89f998f5` after every row and finally.
All three LIVE firsts match the producer's harvest table exactly.

## 3. Residual oracle hunt — ASSESSED (source at frozen SHA)
No caller-visible existence oracle remains beyond message text:
- All three supersede not-found branches raise the **same class** `ConfirmationNotFoundError` → `(404,"not_found")`, `reject_code="confirmation_not_found"` — a fixed class attribute (`records.py:303`), not message-derived. No status/code/field divergence.
- Route builds `new_confirmation` with `supersedes_id=record_id` (`site_definition.py:621`) BEFORE `store.supersede` (:623) → the mismatch branch (store:334) is **route-unreachable**; reachable only at store level, where AS-2 proves identical text.
- Reason path: `create_confirmation` raises `TransitionReasonRequiredError` (422) when `supersedes_id` set and reason empty (`records.py:603-604`→`_require_reason:523`) — BEFORE the store lookup, so it fires identically for missing vs existing `old_id`. No route oracle.
- `ConfirmationNotActiveError` (409) reachable only after the scope match (own record); foreign/missing 404 first — the accepted T067 scope-before-status design, not a foreign-existence oracle.
- Route 404-uniformity is asserted by `test_site_definition_api.py:840` (full-body equality minus `correlation_id`).

## 4. Equal-treatment completeness — OBSERVED
All **five** branches — supersede `:328/:334/:350` + revoke `:428/:441` — reference the single constant `_NOT_FOUND_FOR_ADDRESSED_PROPERTY` (`store.py:72`). The test constant `_UNIFIED_NOT_FOUND_TEXT` (`records test :853-856`) is byte-identical to it. Byte-identity across supersede AND revoke is structural (one constant), matching the DB-040 discipline. Old supersede literals fully removed from code/tests.

## 5. AS mapping — all covered
- AS-1 → `records:863` (store) + `api:840` (route). ✓
- AS-2 → `records:891` + route-unreachability of mismatch branch. ✓
- AS-3 → six-mutation matrix (LIVE A1/B1/C1 + harvest A2/B2/C2). ✓
- AS-4 → store diff changes only `raise`-arg literals (no reorder; guards untouched); test diffs pure additions → T067 tests unchanged and green within the 77. ✓
- AS-5 → 77 green (LIVE); ruff `All checks passed!` on the 3 changed files (LIVE, ruff 0.13.0); modularity exit 0 (harvest OBSERVED; CI green `a66cf818`); no pre-existing assertion adjusted. ✓

## Advisory (non-blocking)
- **ADV-1:** `store.py:282` `get()` not-found still echoes `record_id!r`. This is the read path, explicitly OUT OF SCOPE for T069 (tracked as G5-INFO-1), noted by the producer — not a supersede/revoke not-found-class branch.
- **ADV-2:** Store branch 3 (`reason=None`, `TransitionReasonRequiredError`) is a store-only theoretical existence oracle (404 missing vs 422 reason-required existing) reachable only by a hand-built record bypassing `create_confirmation`. Route-unreachable, defense-in-depth, outside T069's not-found-class scope. No action.
- **ADV-3:** I linted only the 3 changed files (clean), not the whole `services/api` tree; CI-authoritative `ruff check .` was green at `a66cf818` per the identity — corroborated, not a gap.

**No blocking findings.** Test adequacy and mutation proof are sound; the not-found-class unification is structurally complete and oracle-free at the store and route.

END-OF-REPORT M5-T069-G4
