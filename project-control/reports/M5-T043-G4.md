# M5-T043 — G4 QA review (verbatim reviewer return; transport entity-decoding only)

Saved by the orchestrator from the qa-engineer agent return, 2026-09-19 (seq-118 wave,
pinned at submission sha 20c597c1). Nothing added or removed beyond decoding transport
HTML entities.

---

Review complete. All checks reproduced independently at the material SHA. Returning the gate report.

---

# G4 QA GATE REPORT — M5-T043

**Task:** Wide-street wiring module hardening + extraction (DB-028 a–e,h)
**Reviewer:** qa-engineer (independent, read-only)
**Pinned submission:** `20c597c1` · **Material:** `f6e88222` · **Combined head:** `a380a956` (branch `candidate/D-024-mrl-option-b`)
**Verdict: PASS** (2 advisory findings, 0 blocking)

## Combined-head CI validity (does the run genuinely prove THIS task?)

I reproduced the disjointness gate independently:
- `git diff --name-only f6e88222 a380a956` → touches only `services/api/app/connectors/dtm_condo_soda.py`, `services/api/tests/connectors/test_dtm_condo_soda.py`, `M5-T044-producer-report.md`, and control-plane bookkeeping (`state.json`, `tasks/M5-T043.json`, `tasks/M5-T044.json`). **No M5-T043 allowed_path is touched.**
- Therefore the M5-T043 material tree is byte-identical at `f6e88222` and `a380a956`; CI run `35412920210` = success at `a380a956` (incl. web + web-e2e jobs) is a valid conclusion for this task's material, including the web-only AS-6/AS-8 proof. The superseded per-task run at `876ea2b8` is correctly disclosed and irrelevant. **Combined-head conclusion accepted.**

Material stat independently confirmed: 7 files, +829/−249; all 7 inside `allowed_paths`; no `connectors/**` (M5-T044 disjointness preserved).

## Scenario adequacy table

| AS | Item | Adequate? | How I forced/verified it |
|----|------|-----------|--------------------------|
| AS-1 | (a) facade/extraction | YES | `test_db028a` asserts `getattr(facade,n) is getattr(submodule,n)` — **identity (`is`)**, not importability, plus `__all__` membership on both modules. Consumer `wide_street_live_provider.py:110-114` imports the moved symbols from the **facade** and its source is **unedited** (diff empty) — the re-export is what keeps it resolving. Old submodule was a docstring-only placeholder (no duplication). Private `_fully_resolved_typed_inputs` moved with its only caller; no dangling reference in the facade. |
| AS-2 | (c) guard hardening | YES (mutation-sensitive) | `test_db028c` feeds hand-built `implemented=False/may_touch=False` + wide decision + within-segment. Reconstructed OLD guard from parent `eb74d42a`: `named_pending = may_touch AND NOT implemented` → False; `_elevated` → `all([wide])` → True. Old code would yield WITHIN_WIDE / exceptions_checked=True / pending=False — **fails all 3 assertions**. New code → PROFESSIONAL_REVIEW / False / True. Guard is monotonically ≥ strict for every builder-emitted shape; only the illegitimate hand-built shape changes outcome (tighten-only confirmed). |
| AS-3 | (d) empty-decisions | YES (mutation-sensitive) | `test_db028d` passes `[]` decisions + `OVERRIDE_CLEAR`. Branch 1 (`if not policy_decisions`) surfaces `exceptions_checked=_elevated(...)` (not hardcoded). Old `_elevated([], ...)` → `all(())` → **True** (vacuous); new `bool([]) and ...` → False. The `is False` assertion fails on old code. Forced. |
| AS-4 | (b) doc/reason accuracy | YES | Reproduced the grep myself over BOTH rules modules (`wide_street_wiring.py`, `named_street_override_status.py`) for `not implemented / out-of-scope / follow-up / TODO / stub / placeholder / not yet` → **zero matches**. Read branch-4 reason (`:421-430`) and module docstring — both state the matcher "was consulted but could not resolve." |
| AS-5 | (e) matcher-load-failure | YES | `test_db028e` calls `provider._named_street_matcher.cache_clear()` BEFORE monkeypatching a raising `load_default_matcher` (essential — the matcher is `@lru_cache(maxsize=1)`; without the clear the branch never runs), and `cache_clear()` again in a `finally` (no test-order pollution). Verified the provider's real fail-safe branch exists (`wide_street_live_provider.py:194-206`: try/except → `_fail_safe("named_override_matcher_unavailable",…)` → unresolved status). Asserts unresolved shape (implemented=False, may_touch=True, matched=None, note "could not be loaded") AND the typed log event + `error_type=RuntimeError`. |
| AS-6 | (h) web fallback | YES (mutation-sensitive; CI-proved) | `wide-street-provenance` testid is rendered by the **edited** `CalculationEvidence.tsx:39`. Test sets `determination_state=within_100ft_of_wide_street` (→ `wideReview=false`) and `governing_max_residential_far=null`, forcing the `?? "Not calculated"` branch. Asserts BOTH the positive `"…: Not calculated"` (exact match to `{wideValueLabel} (dimensionless ratio): …`) AND absence of both "withheld — professional review required" and "Professional review required". Old fallback was `?? "withheld — professional review required"` → both negative asserts would fail. Proven green in the combined-head CI web job. |
| AS-7 | ruff/suites/modularity/contract | YES (modularity corroborated, not locally rerun) | Consumer + contract files (`test_rules_integration.py`, `test_rule_evaluation_api.py`, `development-limits.test.tsx`, `integration.py`, `rule_evaluation.py`) all **byte-untouched** (diffs empty). Modularity improvement independently corroborated by line count: `wide_street_wiring.py` 723→**539** (was warn-tier at 723; now well under), submodule 251. Ruff/pytest/modularity exit-0 are orchestrator-reproduced (report §4) + green CI. See Limitation L1. |
| AS-8 | CI green + DRAFT grep | YES | CI success validity established above. I reproduced the DRAFT-preservation grep on added source lines: no `published/verified/compliant/compliance/final determination/approved` claim introduced. |

Test-count deltas verified against the diff: `test_wide_street_wiring.py` **+3** (`test_db028a/c/d`, 98→101 with matcher suite), `test_wide_street_live_provider.py` **+1** (`test_db028e`, 38→39). Exactly the DB-028 tests; consumer suite 475 unchanged (zero consumer edits).

## Findings

1. **ADVISORY** — `project-control/reports/M5-T043-producer-report.md` embeds ~407 lines of verbatim source hunks (§3.1–§3.6), the opposite of the packet's "REPORT DISCIPLINE" input (reference files/anchors, never embed). The report itself discloses this was forced by a prior reviewer demand (DB-012 report-reviewability churn). Report hygiene only; no bearing on code or test adequacy.
2. **ADVISORY** — `report/M5-T043-ci-evidence.md` and evidence-map are accurate as written; I note only that AS-6/AS-8 web proof is inherently CI-only (thin client) — verified here via source-level mutation analysis + the disjointness diff, which is the correct channel.

## Limitations (verification context)

- **L1** — I could not execute `python tools/modularity_check.py --check`, `ruff`, or `pytest` at `f6e88222` myself: my isolated worktree is checked out at an unrelated HEAD and I am read-only (cannot check out the material tree). Those AS-7 results are orchestrator-reproduced (report §4) and green in CI; I independently corroborated the modularity IMPROVEMENT via the 723→539 line-count drop and verified behavior preservation by reading source + confirming the facade identity test and unedited consumers. This is a context limitation, not a blocker.

## Exact read-only commands run

- `git rev-parse HEAD` / `git branch --show-current` / `git log --oneline` — orientation
- `git cat-file -t {20c597c1,f6e88222,a380a956}` — all `commit`
- `git ls-tree -r --name-only 20c597c1 | grep M5-T043` — packet file set
- `git show 20c597c1:project-control/tasks/M5-T043.json` (+ producer-report.md, ci-evidence.md, evidence-map.json)
- `git diff --name-only f6e88222 a380a956` — disjointness (no M5-T043 allowed_path touched)
- `git show --stat f6e88222` — 7 files, +829/−249
- `git show f6e88222:services/api/app/rules/named_street_override_status.py` / `…/wide_street_wiring.py` / `…/spatial/wide_street_live_provider.py`
- `git show f6e88222:…/tests/rules/test_wide_street_wiring.py` / `…/tests/spatial/test_wide_street_live_provider.py`
- `git show eb74d42a:…/wide_street_wiring.py` — parent guard/`named_pending` reconstruction (mutation-sensitivity)
- `git show eb74d42a:…/named_street_override_status.py` — confirmed old placeholder stub
- Grep (read-only) over both rules modules for stale/deferred phrasing → no matches; over added source for published/verified claims → no matches
- `git diff eb74d42a f6e88222 -- <consumer/contract paths>` → all empty (unedited)
- `git diff eb74d42a f6e88222 -- CalculationEvidence.tsx / report-view.test.tsx` — reviewed the (h) change + new assertion
- `git grep -n _fully_resolved_typed_inputs f6e88222` — self-contained in submodule

**Recommendation to orchestrator: record G4 = PASS.** All eight scenarios have adequate tests; AS-2/AS-3/AS-6 are mutation-sensitive against the pre-hardening code; the extraction is behavior-preserving with a genuine identity-asserting facade test; consumers/contracts are byte-untouched; the combined-head CI validly covers this task's material.
