# M5-T094 — G5 security review (security-reviewer "sec-t094", read-only)

> Transmission history: pinned at a612652b, delivered as two SendMessage parts (1/2, 2/2) ending with END-OF-REPORT;
> no truncation. Joined verbatim by the orchestrator (transport wrapper tags removed only). Verdict PASS: every guard
> is logic-identical and the patched bounds thread at read time; advisories A1/A2 are PRE-EXISTING (byte-identical to
> the pre-split file) and are routed to the C1 growth packet at the accept seam.

---

M5-T094 G5 SECURITY REVIEW (read-only) — PART 1/2

PIN: HEAD a612652b at start. Material commit 89128c62. Pre-split = `git show 89128c62^:...sheet_reader.py` (1096 lines). Commit touched only 5 files (report, interpreter, objects, reader, split-equivalence test).

METHOD: full line-by-line OLD monolith vs NEW split of every security guard; ran both suites via the 3.11 shim (46 passed); ran an independent in-memory red/green seam probe (python -c, bare-package setup).

FINDING (core): the split is behaviourally faithful. Every guard's LOGIC is identical; the only change is binding — bounds/algebra/decode-hook moved from module globals (read at runtime) to values captured at `_read_sheet()` start and THREADED into `_InterpreterLimits` (sheet_reader.py:133-142, built 260-277) and `_StreamDecoder` (sheet_objects.py:212-264). The facade constants are read from sheet_reader's own namespace at read time, so patch-before-read still bites (the documented mutant seam is preserved).

THREAT MODEL — each item verified:
1. Decoded-bytes budget: still ONE document-wide `_StreamDecoder` per read, shared by pages/forms/`/Contents` (charge on `decoder.charge_decoded`, sheet_objects.py:236-245). Charged after each decode exactly as pre-split; per-stream 8MB cap bounds transient allocation before the total is charged. No new decode bypasses the charge — decode_stream/decode_form/decode_contents all route through the threaded `_decode_stream` (sheet_objects.py:247-290). Probe: patch `MAX_TOTAL_DECODED_BYTES=4` → SheetRefusal "decoded bytes budget"; `MAX_DECODED_STREAM_BYTES=4` → "flate output bound"; unpatched → SheetDocument (green).
2. Stream-size cap, operator count, path-point budget (bounded DURING Bezier flatten via threaded remaining budget, sheet_interpreter.py:289-305), q-depth, xobject depth + cycle guards: all present, identical, threaded from facade; covered by the 37 UNCHANGED facade-patch mutation tests (all green).
3. Finiteness gates: present in `_map` (241-240), post-flatten loop (300-302), `_map_via` (537-543) — identical.
4. Backstop: `read_sheet` try/except → `_refuse("unexpected error", f"unexpected {type(error).__name__} during read")` (sheet_reader.py:227-230) — TYPE only. Probe: helper raising ValueError("SECRET…") → detail "unexpected ValueError during read", "SECRET" absent.

Part 2 next.

---

M5-T094 G5 SECURITY REVIEW — PART 2/2

5. Patched-bound threading (the marquee risk): VERIFIED. Facade constants `MAX_CONTENT_OPERATORS`, `MAX_PATH_POINTS`, `MAX_Q_DEPTH`, `MAX_XOBJECT_DEPTH`, `MAX_DECODED_STREAM_BYTES`, `MAX_TOTAL_DECODED_BYTES` and hooks `_flatten_cubic`/`_concat_matrix`/`_decode_stream` are read at read_sheet() start and threaded into the running decoder/interpreter. No stale by-value copy makes a bound unpatchable. Existing tests patch the FACADE (`sheet_reader.*`) for `_flatten_cubic`, `_concat_matrix`, MAX_XOBJECT_DEPTH, MAX_CONTENT_OPERATORS, MAX_PATH_POINTS, MAX_Q_DEPTH, `_decode_stream`, MAX_TOTAL_DECODED_BYTES — all green against the split. My probe independently confirmed the byte-budget path.
6. No new dependency: imports are stdlib (zlib/math/dataclasses/typing) + in-repo only. No logging/print/open/socket/urllib/subprocess/eval/exec (only `_flush_open` matched, a method name). sheet_primitives.py AND app/documents byte-untouched (git diff 89128c62^..HEAD empty). Not wired to any route (only the drawings `__init__` docstring references it). Runtime import graph acyclic — the interpreter's `from sheet_reader import _SheetInterpreter` is under `if TYPE_CHECKING:` (sheet_interpreter.py:57-58).

FINDINGS
- Critical: none
- High: none
- Medium: none
- Low / ADVISORY (2, both PRE-EXISTING and byte-identical to the pre-split file — NOT introduced by M5-T094, non-blocking; raise on the C1 growth packet, not here):
  A1 (sheet_objects.py:159-160): the unsupported-`/Filter` refusal detail embeds the filter name (`filter_value.value` / `repr(filter_value)`) without `_preview` truncation. Value is a lexer-bounded PdfName, not raw stream bytes; exposure limited. Identical in OLD sheet_reader.py.
  A2 (sheet_objects.py:236-245): the document decoded-bytes total is checked AFTER increment, so it can overshoot by up to one per-stream cap (~8MB) before refusing. Bounded; identical to pre-split.

Neither advisory is a regression, and both are out of scope for a byte-identical refactor. No behaviour change reaches a route (unwired).

M5-T094 G5 VERDICT: PASS

END-OF-REPORT
