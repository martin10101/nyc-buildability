# M5-T063 HJ — independent human-journey review (verbatim reviewer return)

Reviewer: human-journey-reviewer (opus-4-8 xhigh per D-064), dispatched by the orchestrator at
the f42a94a0 delta-wave-close seam, pinned to the corrected identity 853a1d25 (material
f33b752c + the [ORCH-CORRECTED] cluster). Recorded by the orchestrator; reviewer is read-only
and records no gate itself. The reviewer's own originating T058 riders are formally ruled
CLOSED inside this report.

---

Now my G3 human-journey review report.

---

**VERDICT: PASS** (4 non-blocking advisories routed to discovery)

# M5-T063 G3 Human-Journey Gate Report — web condo rider cluster (DB-042/DB-036(a,e)/DB-038(b,c))

## Identity (read-only plumbing, verified)
- Corrected material identity reviewed: `f33b752c` (11 files) + tagged correction `853a1d25` (`[ORCH-CORRECTED per M5-T063 G3-F1/G4-C-1]`), reviewed together.
- `git cat-file -t` confirms both commits exist. `git diff --stat 853a1d25 HEAD` over all 11 material paths returns **empty** — material is byte-stable to current HEAD `f42a94a0` (which adds only control-plane records). I read the working-tree files as the reviewed identity.
- Thin client: no npm/node run. Web behavior proves in CI (orchestrator-captured, green at `f33b752c` and `853a1d25` per the evidence map and the G3/G4/G5 delta re-attestations). I verified source and specs directly.

## Method
Read as a skeptical screen-reader-and-sighted analyst on the architect flow: does the announced (aria-live) text match the visible surface in specificity AND truthfulness; do the recorded-zoning states read honestly with no dead ends; does the jargon land plainly; does the slash district survive display. Traced both announcer consumers (ArchitectEntry, RuleEvaluationPanel/PropertyLookup) and the two base-lot surfaces.

## Per-rider closure judgment

### YOUR HJ-3 (announcer trails visible labels) — CLOSED
`rule-evaluation.ts:520-554`. Two branches now carry the visible specifics:
- `condo_base_lot_unresolved` → `CONDO_BASE_LOT_UNRESOLVED_ANNOUNCEMENT` (:517-518, :551-552): names "condo billing lot", "recorded base lot", and "a site-definition confirmation is required" — matching the visible "Condo base lot needs site confirmation" label; strictly more specific than the old generic "missing_evidence" string (test `:512`, `not.toBe(GENERIC_MISSING_EVIDENCE)`, no "verified"/"best").
- substitution → `:545-549`: names both BBLs, "recorded base tax lot", "condo billing lot … you entered", "a city record of the resolution, not a computed allowance" — mirrors the visible `AnalysisIdentityNotice.tsx:66`.

**Truthfulness (the correction) verified, not just asserted.** The announcer's substitution branch (`:535-543`) now gates on `substitution.entered_bbl === document.evaluated_input.bbl`, exactly the in-document correspondence the visible `stampLegitimate` guard enforces (`AnalysisIdentityNotice.tsx:54-61`). I confirmed the truthfulness at the call site: in `ArchitectEntry.tsx:75-78`, branch (A) catches `evaluated_input.bbl !== profile.identity.bbl` first with a WITHHELD announcement, so the announcer's "evaluation" case runs only when `evaluated_input.bbl === profile.identity.bbl === requestedBbl`; combined with the new gate, `entered_bbl === requestedBbl` transitively — the full visible correspondence. The requestedBbl clause correctly stays upstream (out of the pure function's reach), as the F1 note said. The reproducible drift I would have filed (announcer legitimizing a stamp the sighted surface renders as nothing) is eliminated. The regression test (`rule-evaluation.test.ts:551-565`) mutates `entered_bbl → 4000010001` and asserts `GENERIC_APPLICABLE_DRAFT` with no "base tax lot"/BBLs — genuinely mutation-sensitive (deleting the gate re-enters the branch and flips it red). **Announced text now matches the visible surface in both specificity and truthfulness.**

### YOUR HJ-4/HJ-5 (parallel surfaces redundancy; billing/base jargon) — CLOSED
- **HJ-5 (jargon) fully closed:** `AnalysisIdentityNotice.tsx:66` defines both terms inline on first use: "a condo billing lot (the single tax lot a condo is billed under)" and "the land parcel the city records as this condo's base". Reads plainly for a first-time analyst.
- **Consistency holds across the two surfaces:** both `AnalysisIdentityNotice.tsx:66` and `CondoRecordsChannelSection` (`PropertyOverview.tsx:266`) now use the same "city record … / entered versus analyzed / not a computed allowance" framing. No contradictory vocabulary between them.
- **HJ-4 (redundancy) closed as scoped:** the packet harmonized wording rather than deleting a surface (eliminating one would require the forbidden T060-lane `ArchitectEntry` edits or relaxing the accepted withhold guard). The two surfaces still co-render for a single-base-lot condo on the overview screen; residual redundancy is advisory HJ-2, not a defect. The `stampLegitimate` gate and `role="alert"` withhold branch are behaviorally unchanged (copy-only edit inside the already-legitimate branch — diff-confirmed).

### DB-036(e)/DB-038(c) (divergence gates on actually-recorded zoning; ZTLDB gap honest) — CLOSED
Walked all three states against `PropertyOverview.tsx:277-320` and `condo-resolution-display.test.tsx:754-779`:
- **none:** `anyZoningRecorded=false` → divergent notice absent (:319); gap note (:320) renders the all-lots copy naming the "Zoning Tax Lot Database (ZTLDB)" in plain language, "zoning left as an explicit unknown rather than guessed"; no "out of scope" jargon leak (test `:763`).
- **mixed:** divergent notice present + PARTIAL gap ("not yet shown for every base lot", "recorded districts shown above are unaffected" — protects shown districts, never claims recorded zoning is globally unavailable).
- **recorded:** `anyZoningUnknown=false` → gap note absent; districts render.
Honest, plain, no dead ends. The interface doc reword (permanent→dormant-unless-recorded, `condo-records.ts:210-216`) now matches the gate. Caveat (advisory HJ-3): recorded/mixed states are DORMANT in prod (ZTLDB not connected; only the "none" state is live today) — honest by-design.

### DB-036(a) (recorded districts render byte-exact incl. M1-5/R7-2 slash) — CLOSED
`boundedZoningDistrict` (`bounded.ts:90-99`), charset `[A-Za-z0-9/-]` admits the slash, length-capped 32, empty→null. Wired at `condo-records.ts:349` (explicitly NOT `boundedToken`, which strips the slash). Rendered as an escaped text-node child at `PropertyOverview.tsx:316`. Strictly additive — every pre-existing helper byte-unchanged; the `boundedToken("M1-5/R7-2") === "M1-5R7-2"` spec-pin still holds, proving the two vocabularies stay distinct. Tests pin `recorded zoning: M1-5/R7-2` and `.not.toContain("M1-5R7-2")` through both the parser and the real component.

### DB-038(b) (S11 describe-label hygiene) — CLOSED
`address-confirm.test.tsx:809` describe now names its true scope ("rider-i cap superseded by rider-c a11y honesty"); label/comment-only change, no test-logic delta, no empty spec files.

## Findings (all non-blocking; route to discovery)

**HJ-1 (LOW):** `CONDO_BASE_LOT_UNRESOLVED_ANNOUNCEMENT` (`rule-evaluation.ts:517-518`) enumerates only two of the three documented causes — "resolves to more than one recorded base lot, or to none" — and omits "or an upstream error" that the visible fail_safe prose carries. For a transient upstream failure the screen-reader text presumes an unresolvable multiplicity while the sighted prose is more careful. Recommend generalizing to e.g. "did not resolve to a single recorded base lot." Minor SR-vs-visible precision gap in the exact HJ-3 spirit; the announcement is still far more specific and truthful than the old generic string.

**HJ-2 (LOW):** Residual two-surface redundancy — `AnalysisIdentityNotice` substitution branch and `CondoRecordsChannelSection` "Recorded base lot for this condo" both render the same entered/analyzed base-lot fact for a single-base-lot condo on the overview screen. Now consistent in wording (this packet's scoped closure), but still the same information twice. Eliminating one is a forbidden-lane change; left as discovery.

**HJ-3 (LOW):** Recorded/mixed zoning states are fixture-only in production (ZTLDB unconnected; `_recorded_zoning_for` → None). Only the "none" gap-note state is live today. Honest and by-design, but the divergence-notice and partial-gap copy paths are unexercisable by a real analyst until ZTLDB lands. Consistent with the T056 dormancy note.

**HJ-4 (LOW):** The second announcer consumer, `RuleEvaluationPanel` (mounted in `PropertyLookup.tsx:165`, `ruleEvalEnabled`-gated), calls `announcementForRuleEvaluation` but renders `RuleEvaluationResult`, not `AnalysisIdentityNotice`. So the newly-enriched substitution announcement there is not mirrored by an equally-prominent visible identity notice (the document is still in the DOM via `CapturedRecord`). Not harmful — the announcement is truthful about the document (the branch requires `entered_bbl === evaluated_input.bbl`, and the panel fetches for `profile.identity.bbl`) — but a minor SR/visible prominence asymmetry on the legacy property surface. Discovery.

## Preservation / a11y verdict
- Accepted withhold guard intact: `stampLegitimate` correspondence gating and the `role="alert"` withhold branch (`AnalysisIdentityNotice.tsx:54-79`) behaviorally unchanged; both-directions withhold specs untouched and green in direction. Softening-while-editing regression did NOT occur.
- No forbidden/T060-lane file touched; `recorded_zoning` stays display-only (D-073-R006, single text-node sink); no new dependency; PropertyOverview export/props byte-compatible.
- a11y: announcer sink is `role="status" aria-live="polite"` escaped text; substitution notice stays non-alarming (no role), withhold keeps `role=alert`; recorded-district value renders inert.

## Ruling on my originating T058 riders
All CLOSED: **HJ-3 CLOSED** (announcer specificity + truthfulness, correspondence-gated and mutation-proven at `853a1d25`); **HJ-5 CLOSED** (billing/base defined inline); **HJ-4 CLOSED AS SCOPED** (consistency achieved across both surfaces; residual redundancy is advisory HJ-2, requires a forbidden-lane change to remove). The four advisories are enrichment/discovery items, none blocking.

**G3 = PASS** at corrected identity `853a1d25` (material byte-stable to HEAD `f42a94a0`).

Relevant files (absolute):
- `C:\Users\MLFLL\Downloads\nyc-zoning\ctl24\apps\web\src\lib\rule-evaluation.ts`
- `C:\Users\MLFLL\Downloads\nyc-zoning\ctl24\apps\web\src\components\architect\AnalysisIdentityNotice.tsx`
- `C:\Users\MLFLL\Downloads\nyc-zoning\ctl24\apps\web\src\lib\condo-records.ts`
- `C:\Users\MLFLL\Downloads\nyc-zoning\ctl24\apps\web\src\components\architect\PropertyOverview.tsx`
- `C:\Users\MLFLL\Downloads\nyc-zoning\ctl24\apps\web\src\lib\bounded.ts`
- `C:\Users\MLFLL\Downloads\nyc-zoning\ctl24\apps\web\src\components\architect\ArchitectEntry.tsx` (call-site truthfulness, read-only)
- `C:\Users\MLFLL\Downloads\nyc-zoning\ctl24\apps\web\src\components\rule-evaluation\RuleEvaluationPanel.tsx` (HJ-4 context, read-only)
- Memory written: `.claude\agent-memory\human-journey-reviewer\m5t063-db042-condo-riders.md`
