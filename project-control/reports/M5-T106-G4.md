# M5-T106 — G4 test-adequacy review (qa-engineer "qa-t106", read-only)

> Transmission history: pinned at 436e0e34, delivered as three SendMessage parts (1/3..3/3) ending with END-OF-REPORT;
> no truncation. Joined verbatim by the orchestrator (transport wrapper tags removed only). Verdict PASS: all 6 producer
> mutants redden their specific assertion; the DB-069 (c) probes are non-vacuous. ADVISORY-1 (a second ShapelyError sibling
> in AS-2) and ADVISORY-2 (the :926 echo) are taken into the required-corrections round.

---

M5-T106 G4 test-adequacy review (qa-engineer, READ-ONLY) — part 1/3

PIN: HEAD 436e0e34 at start (as expected). Material commit 966cca75 (massing_model.py; test_massing_model.py +10 tests; producer report). 3 files, zero forbidden paths.

METHOD: in-process mutation — a text-mutated copy of massing_model.py injected into sys.modules BEFORE the test module's by-value imports resolve (consuming-namespace rule), test module loaded fresh, targets called directly. Non-vacuity guard: every mutation pattern must match exactly 1x or the run aborts. Baseline (unmutated inject) GREEN across 11 targets → harness faithful. Full suite from services/api: 722 passed (matches accepted baseline). python 3.11.9.

PRODUCER'S 6 MUTANTS — all reproduce, each reddens its SPECIFIC assertion:
| mutant | target | result |
|---|---|---|
| raw_after_collapse (check collapsed ring) | as1 spike | RED — DID NOT RAISE (spike collapsed away, builds) |
| geos_only (except GEOSException) | as2 non-geos | RED — TopologicalError escapes untyped |
| drop_y (drop the Y clause) | as3 in-X/out-Y | RED — wrong reason (footprint_outside_lot) |
| vertex0_only (vertices[:1]) | as3 later-vertex | RED — DID NOT RAISE (tall in-X lot builds) |
| unbounded_echo (_preview returns raw) | as4 vertex+source | RED — huge in msg / len>300 |
| me_charge_after (charge after scan) | t098_as4 | RED — pit calls != 0 |

DB-069(c) probes are NON-VACUOUS: drop_y and vertex0_only each redden ONLY via the new in-X/out-of-Y and later-vertex probes; a drop_lot_check mutant (guard removed) reddens all three lot probes → the guard is load-bearing, not decorative.

DB-069(d) bound is PINNED: test_t106_as4_preview asserts MAX_ECHO_CHARS == 120 (hardcoded literal, non-tautological) and both echo tests assert len(msg) <= 300 on the real 200,000-char case; unbounded_echo reddens both.

DB-061(b) mutant ME: the EXISTING test_t098_as4 genuinely kills me_charge_after (RED, pit-calls != 0) — correctly discharged, no new test needed.
(continued in 2/3)

---

M5-T106 G4 review — part 2/3

MY 6 ADDED WEAKENINGS:
| mutant | target | result |
|---|---|---|
| drop_lot_check (remove lot raw check) | 3 lot probes | RED — load-bearing confirmed |
| incl_to_excl_xmax (<= → < on NYC X-max) | lot probes | GREEN survives — inclusive NYC-max boundary not pinned; IMMATERIAL (generous guard; matches accepted DB-069(f)/T098 G4 ADVISORY-B) |
| echo_off_by_one (text[:limit+1]) | echo tests | GREEN survives — 121 vs 120 uncaught, still bounded; IMMATERIAL |
| echo_cond_flip (<= → < in _preview) | echo tests | GREEN survives — exactly-limit length never exercised; IMMATERIAL |
| wrap_geos_plus_one (except (GEOSException, TopologicalError)) | as2 non-geos | GREEN survives — see ADVISORY-1 |

BEHAVIORAL PROBES (direct, unmutated code):
PROBE1 — build_from_generated_option no-candidate path echoes {detail or ''} RAW: a 200,000-char detail → 200,113-char message, NO truncation marker; NOT routed through _preview. Untested.
PROBE2 — real widened wrap (except ShapelyError) correctly types GeometryTypeError (a sibling that is neither GEOSException nor TopologicalError) as geometry_engine_error → PRODUCTION CODE IS CORRECT; the AS-2 test simply never exercises this sibling.

SCOPE / AS-6 VERIFIED INDEPENDENTLY:
- No existing test edited: git diff of the test file has ZERO removed lines (additions only).
- Goldens byte-identical: test_t106_as6 PASS; both accepted golden hashes hold; the guards touch only refusal paths.
- ruff app/scenario/massing_model.py tests/scenario/test_massing_model.py → All checks passed (rc 0).
- modularity_check --check → failures 0, EXIT 0 (massing_model.py 958 SLOC, warning-band < 1000 hard cap; cohesion note recorded in report).
- Public API stable: build_massing_model / build_from_generated_option / MassingModel / MassingModelError / __all__ / public constants UNCHANGED; only private helpers changed (_prepare_ring gained keyword-only nyc_range_check=False default; _require_lot_ring_in_nyc_bounds replaced) → M5-T107 dependency safe.
(continued in 3/3)

---

M5-T106 G4 review — part 3/3

FINDINGS:

ADVISORY-1 (test adequacy, AS-2 family claim under-pinned): the widened wrap's only non-GEOS sibling exercised is TopologicalError, so a maintainer narrowing to `except (GEOSException, TopologicalError)` ships GREEN while GeometryTypeError / DimensionError / EmptyPartError would escape untyped. Production code is CORRECT (catches the ShapelyError base — proven by PROBE2); the natural regression (geos_only) DOES die. Suggested tightening: inject a SECOND, different sibling (e.g. GeometryTypeError) in test_t106_as2 to lock the base-class catch. NON-BLOCKING.

ADVISORY-2 (residual echo + report overclaim): build_from_generated_option's no-candidate `{detail or ''}` is an unbounded caller-input echo (PROBE1: 200k → 200,113 chars) NOT routed through _preview and untested. The report's AS-4 claim "Applied to every caller-input echo" is therefore inaccurate. DB-069(d)'s NAMED sites (lot vertex, source) ARE bounded and tested with the 200k case, `detail` is engine-sourced, and the module is unwired — so NON-BLOCKING for this rider's scope. Recommend: PKT-E must bound this echo (fold into the producer's own D-OBS-2 carry-forward) and correct the report's universal wording.

IMMATERIAL SURVIVORS (3): incl/excl NYC-max, echo bound ±1, echo <=/< at exactly-limit — all behaviour-free bound mutants, consistent with the accepted DB-069(f) note; not defects.

CONCLUSION: all 6 producer mutants redden the specific assertion each claims; the DB-069(c) probes are non-vacuous (drop_lot_check proves the guard load-bearing); goldens byte-identical with no existing test edited; the DB-069(d) bound is pinned; DB-061(b) mutant ME is genuinely killed by the existing test; ruff / modularity / public-API / scope all verified. The two ADVISORY items are non-blocking for the rider-scoped acceptance criteria.

M5-T106 G4 VERDICT: PASS
END-OF-REPORT
