# G4 QA Review — M0-T115 (broker ask-row resolution + recovery-probe reconciliation; D-024 R272/R273/R274)

**Verdict: PASS** (2 MINOR advisory corrections, non-blocking)
**Frozen review identity:** `4d3760e10aae9b0e1d074dbe95760702dc36518b` — verified HEAD, working tree clean (`git status --porcelain` empty).
**Reviewer:** qa-engineer (independent; read-only; did not produce this work). Base for diffs: `871cab8`; producer deliverable commit: `91664bb`.

## Command table (exact observed results)

| # | Command | Expected | Observed |
|---|---------|----------|----------|
| 0 | `git rev-parse HEAD` / `git status --porcelain` | frozen SHA, clean | `4d3760e10aae…`, clean (empty) |
| 1 | `pytest test_agent_supervisor_command_authority.py test_agent_supervisor_recovery_probes.py -q` | 117 passed | **117 passed in 14.97s** |
| 2 | `pytest` + broker/recovery/crash/start_reentry/phase1 (7 packs) `-q` | 353 passed | **353 passed in 33.05s** |
| 3 | `git diff 871cab8..4d3760e` new `def test_` in the two test files | — | **8** (3 command_authority + 5 recovery_probes) |
| 4 | same diff, content-deletion (`^-`) lines in the two test files | none | **none** (additions only) |
| 5 | RED-style `-k` selection at HEAD (fixed code) | all pass | 7 passed, 110 deselected (see MINOR-2) |
| 6 | `git show 871cab8:…/broker.py` grep `resolve_ask` | only in `revoke_all` | **line 684 only** (`revoke_all`) |
| 7 | `git show 871cab8:…/recovery_probes.py` grep reconciliation symbols | absent | **absent** (no `all_state`/`_owner_answered`/`APPROVAL_PREFIX`) |
| 8 | `git show 91664bb --stat` | 5 allowed paths, additions only | **5 files, 247 insertions, 0 deletions** |
| 9 | `git diff 871cab8..4d3760e` content-deletion lines in the two source modules | none | **none** (additions only) |
| 10 | `python tools/modularity_check.py --check` | EXIT=0 | **EXIT=0** |

## Findings

**1. Fix correctness — root cause at both boundaries (PASS).**
- *Write side (`broker.py`):* `approve_once` (line 644) and `deny_request` (line 674) now call `journal.resolve_ask("ask_<request_id>", …)` — the literal `revoke_all` M0-T070 pattern (line 699). Both calls are correctly placed on the **success path only**: `approve_once` after the digest-match (line 620) and `status==PENDING` (line 627) guards; `deny_request` after the digest-match guard (line 656). The digest-mismatch/not-pending refusals return before the new call, so a refused answer never resolves the row. Ask-id→record-key mapping is exact: `defer` mints `ask_<request_id>` (line 518); `_key` = `APPROVAL_PREFIX + request_id` (line 330); the reconstruction round-trips.
- *Read side (`recovery_probes.py::probe_pending_requests`):* the new `_owner_answered` predicate (lines 446–451) is a faithful mirror of the cli.py status-command reconciliation (`cli.py` 1499–1503). I checked all four cases (non-`ask_` id, missing record, PENDING record, answered record) — the probe's `[ask for ask in open_asks if not _owner_answered(ask)]` is the exact logical negation of cli.py's "stays open" condition. `STATUS_PENDING == "PENDING_OWNER"`, so DENIED/APPROVED_ONCE/REVOKED/CONSUMED all count as answered, matching `revoke_all`'s treatment.
- *Fail-closed:* both read boundaries (`open_asks()` line 424; `all_state()` line 441) return `_unknown(..., "pending_requests_unreadable")` on any exception — an unreadable queue is never an empty one. Correct.

**2. R273 (no runtime-journal edit) — HONORED.** The read-side fix mutates nothing; `test_a_pre_fix_denied_request_journal_does_not_block_restart` asserts the raw row is *still* unanswered (`len(open_asks())==1`) after the probe passes. The write-side fix writes only through the existing `resolve_ask` surface on **new** answers, never touching pre-fix journals.

**3. R274 (red→green→revert-proof + path decomposition) — SATISFIED.**
- GREEN independently reproduced: 117/117 both packs; 353/353 affected sweep.
- Removal-sensitivity independently confirmed. Per the team lead's instruction I could not stash (write), so I verified by pre-fix-source inspection: at `871cab8`, `broker.py` had `resolve_ask` **only** in `revoke_all` (command #6) and `probe_pending_requests` had **no** reconciliation (command #7). Therefore the 2 broker defect tests (assert `open_asks()==[]`) and the 2 probe defect tests (assert `result.passes`) provably fail against pre-fix code — matching the recorded RED (4 failed) and stash→pop revert-proof.
- Coverage of the required matrix: deny→restart and approve-once→restart are each covered against **both** the new-shape journal (broker tests: row resolved at answer time) and the pre-fix-shape journal (probe tests: read-time reconciliation). The tested probe boundary (`probe_pending_requests` → `approval_pending`) is exactly the step that refused live ("revalidation failed `['pending_requests']`").
- **Provenance match (strong):** the live defect (M0-T113 addendum) denied three asks `9f45b2ca`, `c73f9247`, `7e4b33d8` whose rows showed `answered_at_utc=""` while records were `denied`. The tests reuse those exact ids and that exact shape (`9f45b2ca`→pre-fix DENIED test, `c73f9247`→APPROVED_ONCE test, `7e4b33d8`→PENDING still-blocks guard). The unit shape is the live shape.

**4. Gate-not-weakened — CONFIRMED.** Four guard tests exist and pass: `test_a_digest_mismatch_deny_leaves_the_ask_row_open` (refused answer leaves row open), `..._pending_record_still_blocks`, `..._no_approval_record_still_blocks`, `..._non_broker_ask_still_blocks_regardless_of_state`. No existing test was modified or deleted (command #4: zero content-deletion lines in test files) — additions only. No source logic was removed (command #9: zero content-deletion lines in the two modules). I found **no** ASCII-punctuation-normalization deletion either; the delivered diff is purely additive, cleaner than the pre-brief expectation.

**5. Scope — CLEAN.** Deliverable commit `91664bb` touches exactly the 5 allowed paths and nothing else (command #8). `cli.py` and all other forbidden paths untouched; no code outside the two owning modules. The base→HEAD range also contains orchestrator control-plane commits (task/gate/directive files) — expected and outside the producer deliverable.

**6. Modularity — PASS (EXIT=0).** `broker.py` at 785 SLOC is above the 750 justify threshold; the repair record §5 records the cohesion justification (+14-line mirror of its own `revoke_all` pattern, no new responsibility, under the 1000 hard threshold). Checker passes.

## MINOR advisory corrections (non-blocking; recommend fixing in the M0-T116 recert bundle)

**MINOR-1 — test-count overstatement.** The commit message and progress log state "9 new tests … 5 guards"; the evidence map / repair record are consistent with that. The deliverable actually adds **8** test functions: 4 defect-named (deny/approve-once row resolution; pre-fix denied/approved probe reconciliation) + **4** guards (digest-mismatch, pending, no-record, non-broker). Substance is complete and correct; only the tally is off by one. Recommend correcting the record for accuracy.

**MINOR-2 — RED reproduction command not literally reproducible.** Repair record §3 documents the RED as a single `pytest` invocation with **two** `-k` flags across the two files, claiming "4 failed, 6 passed". pytest applies only the **last** `-k` globally, so the command as written selects 7 tests at HEAD (command #5), not the 4+6=10 the shorthand implies. The RED *claim* is substantively true (independently confirmed via pre-fix source + live evidence), but the documented one-liner does not reproduce as written. Recommend splitting it into two per-file invocations.

## Observation (not a defect)

End-to-end deny→clear-recovery→restart against the **real runtime journal** (and the `recovery.classify` aggregation returning "safe to start" on a reconciled pre-fix journal) is not exercised in this deliverable; the repair record §4 explicitly and honestly defers it to the R276 resume after the M0-T116 recertification. For this bounded defect-lane fix that is sufficient — the exact refusing boundary is unit-covered with removal-sensitive tests, and R271 mandates the recert before autonomous resume regardless. `clear-recovery` itself is out of scope (it succeeded live; the defect was not there).

## Conclusion

The root-cause fix is correct, minimal, additions-only, in-scope, and independently verified against the reproduced live defect; all recorded evidence (117/117, 353/353, modularity EXIT=0, removal-sensitivity, R273 read-only proof) reproduces at the frozen identity `4d3760e`. The two MINOR items are documentation/reproducibility accuracy only and do not weaken the gate.

**G4 verdict: PASS** — with MINOR-1 and MINOR-2 recommended as non-blocking corrections to the evidence record before the M0-T116 recertification bundles this change.
