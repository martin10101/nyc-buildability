# M5-T109 — G4 test-adequacy review (qa-engineer "qa-t109", read-only)

> Transmission history: pinned at 3cd8bdb0, delivered as two SendMessage parts (1/2, 2/2) ending with END-OF-REPORT;
> no truncation. Joined verbatim by the orchestrator (transport wrapper tags removed only). Verdict PASS: 11 of 13 own
> mutations caught, every binding acceptance bound has reddening coverage. Advisories A1 (the text screens for bbl /
> generated_at / generator_version are untested), A2 (floor-cap edge) and A3 (include_in_schema on a mounted app) are
> routed at the accept seam.

---

M5-T109 G4 test-adequacy review (READ-ONLY, in-memory mutation) — PART 1/2

PIN: HEAD at start = 3cd8bdb0 (matches expected). Baseline: `cd services/api && python -m pytest tests/cad -q` -> 429 passed. Method: each mutation applied in-memory to the CONSUMING namespace (export_service `es` / export_api `mod`), driving the EXISTING test fns, then restored. No repo file edited (Write + shell file-writes are blocked for this identity; ran via `python -c`). Memory write blocked -> skipped per protocol.

MUTATION TABLE (caught = an existing test reddened):
| ID | mutation | caught |
|--|--|--|
| M-A | pdf ring cap set to the LOOSEST (dxf) value | YES (test_dxf_uses_its_own_looser_ring_cap) |
| M-B | service screens only `address` (drop bbl/generated_at/generator_version) | NO — see A1 |
| M-C | floor cap off-by-one (`>=` not `>`): refuse exactly MAX_FLOORS | NO — see A2 |
| M-D | `_reconcile` leaks a coordinate into detail | YES (redaction test) |
| M-E | content_disposition omits `filename*=UTF-8''` | YES |
| M-F | filename allowlist widened to include `;` and `"` | YES (all 4 hostile values) |
| M-G | rate limiter `allow()` always True | YES (429 test) |
| M-H | per-request deadline neutralized (`wait_for` passthrough) | YES (503 test) |
| M-I | flag-off 404 leaks X-Correlation-ID | YES |
| M-K | 500 path leaks an internal string | YES |
| M-L | GLB made floor-COUNT sensitive (dedupe broken) | YES (single-extrusion test) |
| M-M | `_reconcile` hardcodes a frozen code | YES (via GLB code + route bad-geometry) |
| M-N | route `include_in_schema` | effective (absent from mounted-app OpenAPI); not pinned on mounted app — see A3 |

Producer's 6 named mutation tests reproduced: ring_cap / floor_cap / text_cap / claim_word_screen / filename_token / narrow_overflow_only -> 6 passed. DB-057(d) raw-length-before-normalization ordering: PINNED (neutralizing `_raw_ring_length` reddens the non-numeric-over-cap test).

Continued in part 2.

---

M5-T109 G4 — PART 2/2

BLOCKING: none.

ADVISORY:
- A1 (M-B): the service claim-word + length screens for `bbl`, `generated_at`, `generator_version` have NO reddening test — only `address` does. Full non-parametrized sweep of test_export_service with `_TEXT_FIELDS=("address",)` reddened NOTHING. Impact is bounded: PDF is defended by the writer's own `_screen_caller_text`; for DXF/GLB those fields reach only the allowlisted filename token + the returned provenance dict, so a claim word (e.g. "APPROVED" in bbl) would survive into the filename token as plain text — a latent D-083 honesty risk if a future refactor narrows `_TEXT_FIELDS`. Code is correct today. Recommend parametrizing the claim-word and text-length tests across all four `_TEXT_FIELDS`.
- A2 (M-C): floor-cap exact boundary (`>` vs `>=` at 2000) unpinned; `test_floor_cap_mutation_reddens` proves the cap is load-bearing but not its edge. Low severity (2000-floor boundary; rendering 2000 floors is costly).
- A3 (M-N): `include_in_schema=False` is not directly pinned on the mounted app (only real-app absence is tested — the binding AS-4 requirement while UNMOUNTED). Re-verify at the PKT-H mount seam.

CONFIRMED (team-lead checklist):
- Tests assert the DISPLACED value, not just "an error": honesty labels assert literal strings (not imported constants); redaction asserts the specific coordinate/caller text ABSENT + the exact reject_code; dedupe asserts byte-identity + the DB-054 disclosure substring; filename asserts the safe grammar + exact structural quote/`;` counts.
- Deterministic, no wall-clock flake: deadline uses 0.5s sleep vs 0.02s deadline (25x margin); limiter is count-based; ran deadline+rate tests 3x -> stable.
- PDF golden, owner samples (10), DXF/GLB goldens UNCHANGED (17 + 5 targeted tests pass).
- Corrected test_glb_writer AS-5: passes at baseline; REDDENS on a 2nd app importer of glb_writer AND on main.py importing export_api (demonstrated in-memory). grep corroborates: sole app importer is app/cad/export_service.py; main.py references neither export_service nor export_api.
- DB-075(a): narrow OverflowError-only catch reddens the hostile-`__float__` test; broadened catch verified.

The three advisories are test-adequacy refinements, not defects; the implementation is correct and every binding acceptance bound has reddening coverage.

M5-T109 G4 VERDICT: PASS
END-OF-REPORT
