# M5-T095 — G5 security review (security-reviewer "sec-t095", read-only)

> Transmission history: pinned at a612652b, delivered as three SendMessage parts (1/3..3/3) ending with END-OF-REPORT;
> no truncation. Joined verbatim by the orchestrator (transport wrapper tags removed only). Verdict PASS: M5-T088
> F-HIGH-1 verified CLOSED (worst validator-valid request measured at <=119 ms, was ~597 s); advisories F-LOW-1 (an O(1)
> level-count ceiling before the walk) and F-LOW-2 (broaden the catch to ShapelyError) are routed at the accept seam; the
> route-level cancellable job + deadline + rate limit stays REQUIRED at exposure (DB-061 (i)).

---

M5-T095 G5 SECURITY REVIEW (read-only, security-reviewer) — PART 1/3. Verdict PASS; F-HIGH-1 verified closed.

PIN: HEAD a612652b (matches expected). Material commit a2dcc60f touches EXACTLY the 3 allowed paths: services/api/app/scenario/proposal.py, services/api/tests/scenario/test_proposal_validation_budget.py, project-control/reports/M5-T095-producer-report.md. No app/api/ or main.py touched (no route change). Zero new deps: shapely==2.0.7 already pinned (requirements.txt:508, requirements.in:51); commit does not touch requirements. proposal.py has no network/eval/exec/logging/subprocess/open/getenv surface (grep clean).

METHOD: code read + independent adversarial in-memory probes from services/api (shapely 2.0.7; this PC, CI is Py 3.12) + re-ran the delivered suite + an independent fresh-seed fuzz.

F-HIGH-1 CLOSURE — VERIFIED CLOSED.
- The O(n^2) all-pairs scan is gone. _ring_is_simple (proposal.py:153) keeps the O(n) zero-length-edge + reversal-spike checks and delegates non-adjacent self-intersection to shapely Polygon.is_valid via _ring_boundary_is_simple (proposal.py:129-150), a GEOS sweepline ~O(n log n).
- The request-level MAX_TOTAL_OUTLINE_POSITIONS=20000 (proposal.py:89) is charged in _check_total_positions (proposal.py:481-504), called BEFORE any per-outline geometry in validate_proposed_massing (proposal.py:518). I proved ordering independently: an over-budget request (21000 positions) whose footprint is ALSO out-of-bounds is refused at _check_total_positions:498 with field="proposed_massing" — the out-of-bounds vertex check never runs.
- Worst validator-valid request, MEASURED BY ME through the public validator: 20 outlines x 1000 = 65 ms; 500 x 40 = 119 ms (was ~597 s).

(continues 2/3)

---

M5-T095 G5 — PART 2/3 (required checks PASS; two ADVISORY findings).

REQUIRED CHECKS — PASS.
1. Adversarial worst case re-derived INDEPENDENTLY (single outline, n<=1000, MY shapes, not the producer's): convex 2.5ms; near-coincident on a 0.5-ft circle 1.8ms; near-collinear thin sliver 6.7ms; high-sweepline comb 1.3ms; {999/2} self-crossing star (invalid) 1.4ms; dense self-cross 1.0ms; 8-turn self-touching spiral (invalid) 10.0ms. No GEOS pathology — every case single- to low-double-digit ms; is_valid short-circuits on invalid rings.
2. No OTHER O(k^2) across outlines in the validator: validate_proposed_massing does the O(L) position sum, per-outline O(n log n) checks, an O(L log L) index sort (L<=MAX_LEVELS=500), and O(W<=4000) walls — NO containment/overlap/inter-outline cross-check. Confirmed by read.
3. Bound value/placement: 20000 charged before geometry (proven above); malformed outlines count as 0 in _outline_position_count (proposal.py:465-478, never raises) and keep their exact typed field errors downstream.
4. Decision identity: I re-ran the committed suite (test_proposal_validation_budget.py + test_scenario_proposal.py = 74 passed), which includes the 24k new-vs-old equivalence corpus; PLUS an independent fresh-seed (4242) 30,000-case small-integer-grid fuzz = 0 disagreements (5367 true / 24633 false). Identity holds.
5. Fail-closed: GEOSException/ValueError -> False -> typed ProposedMassingError "self-intersecting" (suite AS-4 + my probe). Memory bounded: <=20000 positions total, one Polygon per outline.

F-LOW-1 (ADVISORY, non-blocking): _check_total_positions walks the attacker-controlled levels list (proposal.py:493-496) with NO count cap before the walk; schema (scenario.schema.json) has no maxItems on levels; MAX_LEVELS is enforced only afterward. Measured LINEAR ~0.28us/level: 1e6 empty levels = 281ms, ~2.8s per 1e7. This is NOT the O(n^2) HIGH and needs an INVALID (>500-level) request; it is subsumed by the route body-size limit + wall-clock timeout already marked REQUIRED. Optional defense-in-depth: an O(1) level-count ceiling before the walk.

(continues 3/3)

---

M5-T095 G5 — PART 3/3. Final verdict: M5-T095 G5 VERDICT: PASS.

F-LOW-2 (ADVISORY, non-blocking): the fail-closed catch is `except (GEOSException, ValueError)` (proposal.py:149). All shapely errors subclass shapely.errors.ShapelyError; TopologicalError / GeometryTypeError / DimensionError are ShapelyError but NOT GEOSException (and GeometryTypeError is a TypeError, not ValueError), so they would escape untyped. NOT reachable on the pre-validated path (inputs are finite, in-NYC-bounds, >=3 distinct, strictly 2D, so Polygon.is_valid returns a bool and construction raises at most ValueError/GEOSException). Defense-in-depth: broaden the catch to shapely.errors.ShapelyError.

ROUTE DEFENSE STILL REQUIRED — correctly recorded, NOT claimed closed. Producer report DISCOVERY #2 keeps M5-T088 G5 fix (b) (cancellable job + per-request wall-clock timeout + rate limit; DB-034(a)) OPEN as a precondition at public exposure; the in-process bound reduces but does not remove it. Confirmed.

DISCOVERY #1 disposition: derivation.py carries its own O(n^2) _segments_intersect (derivation.py:324-370). FORBIDDEN path here, correctly routed out. Note: derive_proposal re-validates via validate_proposed_massing on entry (derivation.py:704), so it now transitively inherits the <=20000 total-positions bound — a net improvement. It should still adopt the shapely check if ever run on larger/less-trusted geometry (derivation-touching packet).

SECURITY POSTURE (pure offline deterministic geometry validator): no secrets/env/credentials; no network/SSRF; no SQL/shell/eval/injection; no filesystem/storage I/O; no logging (no redaction risk); no AI/ingested text (no prompt-injection surface); no privilege. All N/A/clean. Cross-tenant isolation N/A at this layer — the module carries no auth; the WIRING route must enforce tenant/auth and treat outline+lot as untrusted upload (M5-T088 G5, unchanged).

VERDICT RATIONALE: F-HIGH-1 fully and correctly closed — the O(n^2) is replaced by a bounded shapely check, the total-positions bound fires before any geometry, worst validator-valid request drops from ~597s to <=119ms (my measurement), decision identity holds (0 disagreements incl. my independent fuzz), fail-closed typed, exact allowed paths, zero new deps, no route change. The two LOW items are advisory defense-in-depth and do not reintroduce the finding.

M5-T095 G5 VERDICT: PASS

END-OF-REPORT
