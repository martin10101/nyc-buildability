<!-- Saved VERBATIM from the qa-engineer agent return (transport entity-decoding only),
     orchestrator seq 122, 2026-09-20. Ruling identity: frozen submission f58f4d89.
     (The harness transcript file for this agent was empty; saved from the delivered return.) -->

# GATE REPORT — G4 (QA / test adequacy) — M5-T059

**Task:** M5-T059 — D-078 pair 2/2: multi-lot condo site-definition confirmation, slice 1
**Reviewer:** qa-engineer (independent; producer was backend-engineer) — read-only
**Date:** 2026-09-20
**Verdict:** **PASS with required corrections** (C-1, C-2 below block acceptance; they are not gate failures)

## 1. Frozen identity verification

All checks from `C:\Users\MLFLL\Downloads\nyc-zoning\wt-m5t059` via read-only git plumbing.

| Check | Result |
|---|---|
| Worktree HEAD | `48fe290b1bc9edcccc2963821369f0ffdcf39c43`, `git status --porcelain` empty |
| Material commit | `2fbd661e` (parent `fd62b08d`), ancestor of `candidate/D-024-mrl-option-b` tip `f51fb0fc` |
| Frozen submission | `f58f4d89`; branch order is `2fbd661e → fc2910af → f58f4d89 → f51fb0fc` |
| **allowed_paths f58f4d89 vs branch tip** | `git diff --stat f58f4d89 f51fb0fc -- <14 allowed paths>` → **empty** (identical; not BLOCKED) |
| **allowed_paths worktree vs material** | `git diff --stat 2fbd661e 48fe290b -- <14 allowed paths>` → **empty** (blob-identical) |
| Material diff scope | `git diff --stat 2fbd661e^ 2fbd661e` = 13 files, all inside `allowed_paths`; no forbidden path touched |
| `main.py` byte-unchanged | `git diff 2fbd661e^ 2fbd661e -- services/api/app/main.py` → empty |
| `bbl.ts` / `bounded.ts` byte-unchanged | `git diff 2fbd661e^ 2fbd661e -- apps/web/src/lib/bbl.ts apps/web/src/lib/bounded.ts` → empty |
| Web material at CI head | `git diff --stat 2fbd661e fc2910af -- <4 web paths>` → empty, so CI 35502941971 at `fc2910af` exercised byte-identical web material |

Worktree `48fe290b` differs from `2fbd661e` **only** in out-of-scope M5-T057 files (`proposal_checks_api.py`, its tests, `M5-T057.json`) — the worktree base predates T057's `ca5c8895` correction cluster. This does not affect any T059 surface; it does mean the local `578` count is the worktree's count, which is exactly what the producer and SS10.1 captured.

**Digest binding verified independently** — LF-normalized sha256 of 5 of the 12 SS9(b) rows recomputed at the worktree; all 5 match the report table byte-for-byte:

```
cddcb3b819f7a758ba91fa2ad17f891564e242b82925f9e5a56cce24cd7e8fd5  app/site_definition/records.py
b24e3c5f3075eacdabbbbc6fc8c1bc88a9ab5dcddc579886841a1c539fd1e5e8  app/site_definition/store.py
bdb4f90487486feded57d96a2b05a4a0af58cc3b1ec573e68dab11a7dbb5472a  app/api/v1/site_definition.py
c27d7559245579b2e18aeec5566f313c7ed090733e539639fb01172066c906ab  apps/web/src/lib/condo-records.ts
8028be9d36ec9f27957421b155c089179b72f11c4fc314595ad1d40d0c707baf  .../PropertyOverview.tsx
```

## 2. Required commands — independent execution

| # | Command | cwd | Result | Expected | Match |
|---|---|---|---|---|---|
| T1 | `python -m ruff check .` | `wt-m5t059/services/api` | `All checks passed!`, `RUFF_EXIT=0` | 0 | YES |
| T2 | `python -m pytest tests/site_definition tests/api -q -rsxX` | `wt-m5t059/services/api` | **578 passed in 37.92s**, exit 0, `-rsxX` summary empty (**no skips, no xfails, no xpasses**) | 578 no-skips | YES |
| T3 | `python -m pytest tests/rules -q` | `wt-m5t059/services/api` | **726 passed in 32.69s**, exit 0 | 726 | YES |
| T4 | `python tools/modularity_check.py --check` | `wt-m5t059` (root) | `selected 460 files; failures 0; warnings 20`, `MOD_EXIT=0` | 0 | YES |

`condo_records.py` (684 raw lines) is **not** in the 20-warning list; neither is `app/site_definition/**`. The seven modularity answers in the packet reconcile with the actual diff: record/store/facade split with `store.py` importing `records.py` only, HTTP wiring isolated in `api/v1/site_definition.py`, and `condo_records.py` grown by an import + a dependency + a 10-line additive block (`condo_records.py:100-104`, `:222-238`, `:667-676`).

**Broadened regression (beyond the documented commands):** `python -m pytest -q --ignore=tests/documents` from `services/api` → **3099 passed, 1 failed in 82.26s**. The single failure is `tests/contracts/test_contract_serializers.py::test_serializer_imported_exactly_at_the_profile_write_boundary`, and a bare `python -m pytest -q` interrupts with 15 `tests/documents` collection errors. Both are the **sandbox Python-version skew**, not a T059 regression: the sandbox is `Python 3.11.9` and `services/api/app/documents/units.py:276` uses PEP 695 syntax (`def _match_unit[UnitT: enum.Enum](`) requiring 3.12. `units.py` last changed in `561d2899` (M2-T015) and is a forbidden path here. CI runs 3.12 and is authoritative.

## 3. AS-by-AS adequacy

**AS-1 (primary, multi-lot end-to-end) — SATISFIED, one advisory.** Value-pinned, not shape-pinned: `test_site_definition_records.py:93-114` asserts `attestation_status is UNAUTHENTICATED_SELF_ATTESTED`, `refused_for_calculation is True`, the full parcel tuple, `provenance == _provenance()` (dataclass equality = byte-match of the snapshot), and `confirmed_at == "2026-09-20T08:23:30.089123+00:00"` **char-for-char with fractional seconds retained**. Route leg: `test_site_definition_api.py:174-192` (201, provenance byte-copied from the re-read resolution, tz-aware parse). List-active: `:297-313`. Document leg: `test_condo_records_api.py` `test_multi_lot_document_surfaces_recorded_confirmation` pins the same fractional timestamp on the surfaced block. Web render leg: `condo-resolution-display.test.tsx` "renders an active confirmation…". **Advisory A-5:** AS-1 names the `3022647515 → 3022640032+3022640033` class; grep shows those values appear only in `tests/connectors/test_condo_base_lot.py:44-49` and `tests/connectors/test_dtm_condo_soda.py:125-132`. Every new T059 test uses a synthetic Manhattan stand-in (`test_site_definition_records.py:56-58`, `test_site_definition_api.py:56-58`). Same resolution class, but the named fixture is not exercised end-to-end.

**AS-2 (strict parcel binding) — SATISFIED.** Subset / superset / divergent each separately typed (`test_site_definition_records.py:148-155`); mismatch names exact sets (`:157-161`); order-only accepted at both the record layer (`:143-145`) and over HTTP (`test_site_definition_api.py:228-233`); route-level divergent returns `reject_code == "parcel_set_mismatch"` (`:218-226`). *Mutation reasoning:* deleting `records.py:465-466` (`if submitted != expected: raise`) kills all four refusal tests; deleting the `sorted(...)` at `records.py:382` kills `test_reordered_but_equal_parcel_set_is_accepted` and `test_normalize_parcels_rejects_malformed_sets`'s final assert. Both lines are individually pinned.

**AS-3 (append-only + chain) — SATISFIED, two required corrections.** `test_site_definition_records.py:198-222` is the strongest assertion in the pack: `old_view.record is original` proves object identity (no in-place rewrite), status is derived from the appended transition, back-link correct, exactly one active, chain listable. Duplicate-active refused (`:185-189`, and 409 over HTTP at `test_site_definition_api.py:238-245`); supersede reason required at construction (`:192-195`) and at the route (`:254-257`); revoke terminal + reason required (`:231-257`, `:270-294`); post-revoke re-create allowed (`:260-269`); unknown id not-found (`:272-276`); frozen-dataclass mutation raises (`:225-228`).

*Probe — "is there ANY store/record API that mutates in place and lacks a refusing test?"* No in-place mutator exists: `InMemorySiteDefinitionStore` exposes only `create`/`get`/`list_for_condo_key`/`supersede`/`revoke`; `supersede` and `revoke` append to `_transitions` and never rewrite `_records[...]`. **But two contract holes surfaced** (reproduced live, see §4): `create` accepts an orphan `supersedes_id`, and the "newest first" contract is violated on a timestamp tie.

**AS-4 (prohibitions / D-078-R002 harness) — SATISFIED.** (a) `deriveCondoSurface` (`PropertyOverview.tsx:197-214`) and `channelWithholdsAllowances` (`condo-records.ts:780-786`) are **byte-unchanged** — the `condo-records.ts` diff has exactly 5 hunks, all in lines 124-533, none near 780; the `PropertyOverview.tsx` hunk at `@@ -212,6 +214,38 @@` adds *after* `deriveCondoSurface`'s closing brace. Behaviorally bound on the api side by `test_condo_records_api.py::test_prohibition_surfacing_is_additive_only` (fetch the multi-lot document with and without an active confirmation, pop `site_definition`, assert the remainders are **equal** — a genuine byte-identity assertion that would catch any leakage into the refusal), and `test_single_document_has_no_site_definition_block` pins the multi-lot-only guard at `condo_records.py:670`. Web side by `condo-resolution-display.test.tsx` "regression: a recorded site-definition confirmation NEVER unlocks the withheld allowances" — with an ACTIVE confirmed block, `development-evaluated-far` and `architect-cap` still read `"Not calculated"`. (b) No selected site absent a confirmation: `build_site_definition_block` (`records.py:514-524`) sets `confirmed` only from an ACTIVE view, and the web parser fails safe twice over (`condo-records.ts:471-482` requires the confirmation's **own** derived status to be `active` plus a record id, `:484-509` requires **both** that and the exact top-level literal) — pinned by five hardening tests at `condo-records.test.ts` (empty `{}`, `revoked`, missing `record_id`, non-object, and the block-lies-confirmed case at `condo-resolution-display.test.tsx` "a malformed / non-active confirmation can never render as active"). (c) Provenance + supersession covered above. (d) Refused-for-calculation label: server-set and unraisable (`records.py:488` has no caller input; `test_site_definition_api.py:195-212` posts a hostile body claiming `attestation_status: "authenticated"` both top-level and nested and still gets `unauthenticated_self_attested` / `refused_for_calculation: true`), fail-safe on the web (`condo-records.ts:438-441` forces `true` for self-attested; `condo-records.test.ts` "defect 2", "missing flag defaults to refused", and — good discipline — "the refusal override is SCOPED to self-attestation" proving the override is not a blanket `true`).

**AS-5 (route discipline) — SATISFIED, two advisories.** Flag-off generic 404 with no correlation leak (`test_site_definition_api.py:133-140`, asserts `resp.json() == {"detail": "Not Found"}` and `X-Correlation-ID` absent); malformed BBL 422 (`:143-148`); over-ceiling 413 (`:151-159`); non-multi-lot 422 (`:162-168`); **router unmounted** pinned by `test_router_is_not_mounted_in_main_app` (`:319-321`), which scans `app.main.app.routes` — a real mutation catcher for a stray mount, corroborated by the `main.py` git-diff above. The local-FastAPI precedent is **confined to the new modules**: `FastAPI()` construction appears only in `test_site_definition_api.py:102-109` and `:334-350`; `test_condo_records_api.py` still drives `app.main.app` through the existing `client` fixture (`:221-232`, which clears `dependency_overrides` after yield — no cross-test leakage). Advisories A-1 and A-2 below.

**AS-6 (DB-038(f) riders) — SATISFIED.** Rider 1 (HJ A4 collapse) bound at `condo-resolution-display.test.tsx` "(f)-1 a billing-class input COLLAPSES entered and billing into ONE line": asserts the merged parenthetical text **and** `queryByTestId("condo-billing-lot")` is null, while the pre-existing unit-input case above it keeps the two lines distinct — the guard at `PropertyOverview.tsx` (`enteredEqualsBilling` requires `billingBblStatus === "recorded"`) is therefore pinned in both directions. Rider 2 (G3-1) bound at `condo-records.test.ts` "a non-canonical substitution entered_bbl is an explicit null": `"abcd"` survives the looser `boundedBbl` token cleaner but is rejected by `enteredBblValue`, so reverting `condo-records.ts:361` to `boundedBbl` turns the expected `null` into `"abcd"` — a clean single-line mutation catcher. `bbl.ts` / `bounded.ts` byte-untouched (git-verified above).

**AS-7 (proof) — SATISFIED** per §2, with the web leg proven only in CI 35502941971 at `fc2910af` (byte-identical web material, verified above). No npm/npx/node was run.

## 4. Findings

### Required corrections (block acceptance; recorded, not gate-failing)

**C-1 — `list_for_condo_key` violates its own "newest first" contract on a timestamp tie, and the tie-break comment is false.**
`store.py:176` is `views.sort(key=lambda view: view.record.confirmed_at, reverse=True)`. The comment at `store.py:174-176` claims "then by the reverse of insertion order so a same-instant pair is still deterministic" — Python's sort is stable and `reverse=True` does **not** reverse equal elements, so a same-instant pair keeps **insertion** order, i.e. oldest first. The ABC contract at `store.py:63-65` and packet SCOPE 2/4 both state "newest first". Reproduced:

```
=== PROBE B: same-instant chain ordering (store.py:174-176 comment claim) ===
  confirmed_at equal: True
  list order: ['AAA', 'BBB']  (docstring claims NEWEST first)
  active: ['BBB']
```

Active-record selection is unaffected (`records.py:514-516` scans for status, not position), so no confirmed/unconfirmed decision changes. Trigger requires two records at the identical microsecond, which `datetime.now(UTC)` per HTTP request makes effectively unreachable today — but the durable B-001 store will implement this same ABC, possibly with a coarser clock. Fix: add a deterministic secondary key (e.g. reversed insertion index) **or** correct both `store.py:63-65` and `store.py:174-176` to state the real guarantee; either way add a same-instant ordering test. Existing tests (`test_site_definition_records.py:216-221`, `test_site_definition_api.py:297-313`) both use distinct timestamps and cannot see this.

**C-2 — `store.create` admits an orphan `supersedes_id`, an append-only-integrity hole in the published ABC.**
`store.py:148-158` validates only the one-active-per-`condo_key` invariant. A record carrying `supersedes_id` pointing at an unknown (or another condo's) record is accepted as ACTIVE with no transition appended and no chain flip. Reproduced:

```
=== PROBE A: store.create accepts a record claiming supersedes_id (no chain flip) ===
  created orphan-supersede record: ConfirmationStatus.ACTIVE supersedes_id= some-other-record
```

Not reachable through the route today (`site_definition.py:374-385` never passes `supersedes_id` on create; `store.supersede` **does** guard the reference at `store.py:183-199`). But `SiteDefinitionStore` is the public interface the durable implementation will satisfy, and `store.py:44-51` states "Implementations MUST … record every status change as an appended transition". Fix: refuse a `supersedes_id`-carrying record in `create` (route the caller to `supersede`), plus a covering test.

### Advisory (record for the ledger; no fix required in this packet)

- **A-1 — flag-off sentinel bound for 1 of 4 routes.** Only `create` has a flag-off test (`test_site_definition_api.py:133-140`). Deleting `site_definition.py:408-409` (list), `:448-449` (supersede), or `:503-504` (revoke) leaves the whole suite green. Probe D confirms all three guards work today (`GET 404 / supersede 404 / revoke 404`) — the gap is mutation detection, not behavior.
- **A-2 — the 413 ceiling's two guards are covered by one indistinguishable test.** `site_definition.py:269-271` (declared `Content-Length`) and `:272-274` (streaming accumulation) both produce `413 payload_too_large`; `test_site_definition_api.py:151-159` uses `TestClient` with `content=`, which always sets `Content-Length`, so it exercises only the declared branch and cannot detect the loss of either. Reproduced by neutralizing `_declared_content_length`: `unmutated: 413` → `declared-length guard DELETED: 413`. The streaming guard exists precisely for a chunked / under-reporting body, and that case is never sent. Also, the ceiling is tested on `create` only, not `supersede` / `revoke`.
- **A-3 — `billing_bbl` provenance conflation, invisible to every fixture.** `site_definition.py:375-377` sets `billing_bbl=canonical` **and** `entered_bbl=canonical` from the same path BBL. `CondoResolution` (`condo_base_lot.py:105-118`) carries no billing-BBL field — only `resolution_path` — so the route has no authoritative billing value to record. Every fixture in the pack uses entered == billing, so no test distinguishes the two fields; a unit-entered path reaching a multi-lot outcome would record a *false* `billing_bbl` on a legally-sensitive provenance record. Suggest documenting the field as "the entered BBL, which equals the billing BBL on the billing resolution path", and pinning an entered ≠ billing case.
- **A-4 — silent parcel drop on the display parser.** `condo-records.ts:411-417` `boundedParcels` filters out any entry `boundedBbl` rejects, so a partially-malformed `parcels` array renders a **shorter** recorded-lot list ("Recorded base lots: 1003030019") with no unknown marker, unlike sibling surfaces that print "not recorded (unknown)". Neither `parcels` nor `parcel_discrepancy.recorded_parcels` / `current_resolver_parcels` has a partial-malformation test. The doc comment "de-duplication-free list" is also unclear.
- **A-5 — AS-1's named fixture class not exercised.** See §3 AS-1.
- **A-6 — misattributed `reject_code` on parcel-shape refusals.** `records.py:364-381` raises `InvalidConfirmerError` (`reject_code = "invalid_confirmer"`) for a non-list, blank-entry, or empty `parcels` value, so the API's 422 body names the confirmer for a parcel defect. `test_site_definition_records.py:164-171` pins the current (wrong-signal) behavior.
- **A-7 — immutability and isolation gaps.** Frozen-ness is probed only on `SiteDefinitionConfirmation.parcels` (`test_site_definition_records.py:225-228`); `StatusTransition`, `ConfirmationView`, `SiteConfirmer`, `ResolutionProvenanceSnapshot` have none. No test proves `list_for_condo_key` isolates by `condo_key` (a record under condo B must neither appear under condo A nor block A's create).
- **A-8 — fragile substring guards.** `test_site_definition_api.py:376` (`assert "far" not in str(after).lower()`) and the matching `json.dumps(body).lower()` guard in `test_condo_records_api.py` would also trip on any word containing "far". Directionally safe today, but brittle.

## 5. Limitations of this review

1. Sandbox interpreter is **Python 3.11.9**; the repo targets 3.12. `tests/documents` cannot be collected and one `tests/contracts` test fails on PEP 695 syntax in `app/documents/units.py:276` (last changed `561d2899`, M2-T015). Pre-existing and out of scope; CI 3.12 is authoritative.
2. Web (vitest) behavior was **not** executed locally per thin-client policy. I verified the four web allowed-path files are byte-identical between `2fbd661e` and `fc2910af`, so CI run 35502941971 at `fc2910af` is a valid proof head for them; I did not independently query the CI run (gh is reviewer-forbidden) and rely on the orchestrator's recorded success.
3. Directive-requirement satisfaction (D-066-R001, D-077-R002/R003, D-078-R001/R002/R003) is the `directive-compliance-verifier`'s pass; I assessed only D-078-R002's *executable-test* expression (AS-4), which reproduces.
4. Read-only discipline held: no repo file was created, edited, or deleted; no `git` write verb, no `tools/project_control.py`, no `gh`, no npm. Probe scripts live in the session scratchpad (`…/scratchpad/g4_probe.py`) and only import the worktree package.

## 6. Verdict rationale

All four required commands reproduce their expected results exactly (0 / 578 with no skips / 726 / 0). Every acceptance scenario AS-1..AS-7 has at least one value-pinned, mutation-sensitive binding test, and the SCOPE 4/4 prohibitions are genuinely executable — the api-side byte-identity comparison and the web-side "Not calculated" regression are the two assertions that would actually catch a D-078-R002 breach, and both are present and specific. The digest binding in the producer report is reproducible and honest; the report's own limitations (unmounted route, ephemeral store, self-attested identity, web-CI-pending) are stated plainly rather than papered over. The two required corrections are contract/comment defects on a production-unreachable surface with no acceptance-scenario impact, and the advisories are coverage gaps rather than wrong behavior.

VERDICT: PASS


---

<!-- DELTA RE-RULE saved VERBATIM from the resumed qa-engineer agent (transport entity-decoding only), orchestrator seq 122, 2026-09-20. Ruling identity: frozen resubmission 03882888. -->

---

# GATE REPORT (DELTA) — G4 re-rule — M5-T059 rework

**Reviewer:** qa-engineer (independent, read-only) · **Date:** 2026-09-20
**Scope:** the C-1 / C-2 remediation delta and the new tests' adequacy. Prior AS-1..AS-7 rulings carried forward.
**Verdict:** **PASS** — both blocking findings closed; no corrections outstanding.

## 1. Corrected identity

| Check | Result |
|---|---|
| Worktree HEAD | `4afcebd08cc9aa2bb75cd17df4e47b9a4d7042b1`, `git status --porcelain` empty |
| Worktree vs rework material `30bc5349`, all 14 allowed_paths | `git diff --stat` → **empty** (blob-identical) |
| Frozen resubmission `03882888` vs branch tip `56814549`, allowed_paths | **empty** (identical; not BLOCKED) |
| Rework commit's own diff (`30bc5349^..30bc5349`) | 7 files: the 6 named material files + the producer report. No out-of-scope file |
| The other 7 allowed paths, `2fbd661e` → `30bc5349` | **empty** — `condo_records.py`, `test_condo_records_api.py`, `tests/site_definition/__init__.py` and all 4 web files byte-unchanged |
| `main.py` / `bbl.ts` / `bounded.ts` | **empty** across the rework — still untouched |
| SS12.1 digests | All **6** LF-normalized sha256 recomputed at the worktree; all 6 match the report table byte-for-byte |

Because the 7 carried-forward paths are byte-unchanged, my prior **AS-4** (prohibitions, both sides), **AS-6** (both DB-038(f) riders) and the document/web legs of **AS-1** are undisturbed and stand as ruled.

## 2. Commands re-run at the rework head

| # | Command | cwd | Result | Expected | Match |
|---|---|---|---|---|---|
| T1 | `python -m ruff check .` | `wt-m5t059/services/api` | `All checks passed!`, exit 0 | 0 | YES |
| T2 | `python -m pytest tests/site_definition tests/api -q -rsxX` | `wt-m5t059/services/api` | **582 passed in 23.08s**, exit 0, `-rsxX` summary empty (no skips/xfails/xpasses) | 582 | YES |
| T3 | `python -m pytest tests/rules -q` | `wt-m5t059/services/api` | **726 passed**, exit 0 | 726 | YES |
| T4 | `python tools/modularity_check.py --check` | `wt-m5t059` (root) | `selected 460 files; failures 0; warnings 20`, exit 0 | 0 | YES |

Broadened regression: `python -m pytest -q --ignore=tests/documents` → **3103 passed, 1 failed** (was 3099 + 1 pre-rework; exactly +4 = the four new bindings). The single failure is the unchanged sandbox artifact — Python 3.11.9 here vs the repo's 3.12, `ast.parse` choking on PEP 695 syntax at `app/documents/units.py:276` (last changed M2-T015, a forbidden path). Not a T059 regression.

## 3. C-1 — newest-first on a timestamp tie: **CLOSED**

`store.py` now maintains `_insertion_seq` / `_next_seq` populated by a single `_record_insert` helper used by **both** `create` and `supersede`, and `list_for_condo_key` sorts on the tuple `(confirmed_at, insertion_seq)` with `reverse=True`. The false comment is gone, replaced with the real mechanism plus the `[ORCH-CORRECTED per T059 G4-C-1]` marker; the ABC docstring at `store.py:63-65` is now honest.

Reproduced — my original failing probe now inverts, and the fix is proven load-bearing by neutralizing the secondary key:

```
=== D1: C-1 REPAIRED? same-instant chain order ===
  order: ['BBB', 'AAA'] expected ['BBB','AAA'] (newest first)
  chain[0] active: True

=== D2: is the C-1 test a real catcher? neutralize the secondary key ===
  with secondary key DELETED: ['AAA', 'BBB'] -> test_same_instant_pair_lists_newest_first FAILS (catcher confirmed)
  restored: ['BBB', 'AAA']
```

`test_same_instant_pair_lists_newest_first` (`test_site_definition_records.py:378-397`) is adequate: it asserts the tie precondition explicitly (`superseding.confirmed_at == first.confirmed_at`) before asserting order, so it cannot silently degrade into a distinct-timestamp test the way the two pre-existing ordering assertions did; and `chain[0].status is ACTIVE` pins the semantic consequence, not just the list order.

## 4. C-2 — orphan `supersedes_id` admitted by `create`: **CLOSED**

`store.create` now refuses any `supersedes_id`-carrying record with the new typed `OrphanSupersedeError` (`records.py:193-202`, `reject_code = "supersede_via_create_refused"`), exported through the package facade (`__init__.py`). The guard runs before the duplicate-active check, so the refusal names the real cause.

```
=== D3: C-2 REPAIRED? create refuses an orphan supersedes_id ===
  refused: OrphanSupersedeError reject_code= supersede_via_create_refused

=== D4: C-2 guard did NOT break the legitimate supersede path ===
  new active: B1 active | old: superseded | back-link: B1
```

D-4 is the regression I most wanted: `supersede` inserts via `_record_insert` rather than routing through `create`, so the new guard does not block the legitimate chain — status flip and back-link both still correct. `test_create_refuses_a_supersedes_id_carrying_record` (`test_site_definition_records.py:400-411`) catches deletion of the guard directly.

## 5. Folded G3-C1 / G5-F1 (revoke binding) — adequately bound

I reviewed this fold-in since it lands in my surface. `SiteDefinitionStore.revoke` now takes a **required** `condo_key`; the impl raises `ConfirmationNotFoundError` on mismatch (`store.py:274-279`); the route re-reads the resolver for `{bbl}` and passes `resolution.condo_key or canonical`. Public-interface change is safe: `grep -rn "\.revoke("` over `services/api` finds exactly one production call site (the route) and seven test call sites, all updated; `InMemorySiteDefinitionStore` is the only ABC implementer.

```
=== D5: cross-condo revoke binding + POSITIVE control ===
  cross-condo refused: ConfirmationNotFoundError | record still: active
  bound revoke succeeds: revoked (positive control - a refuse-everything mutation would fail here)
```

`test_cross_condo_revoke_is_a_typed_refusal_and_leaves_the_record_active` (`test_site_definition_api.py:381-432`) is the best-constructed test in the pack: two local apps over **one shared store** with different `condo_key`s, asserting the typed `404 / state == "not_found"` (distinct from the flag-off `{"detail": "Not Found"}` sentinel), that the record survives ACTIVE via a follow-up list, **and** that the correctly-addressed revoke still returns 200. That last assertion is the positive control a "refuse everything" mutation would fail — without it the negative half alone would be satisfiable by breaking revoke entirely.

## 6. New advisory raised by the remediation (not blocking, for the record / slice 2)

**A-9 — binding revoke to a live multi-lot resolution blocks the only human withdrawal path when the resolver degrades.** `site_definition.py:520-523` now runs `_resolve_multi_lot` on the revoke route, which previously needed no resolver at all. Reproduced end-to-end:

```
=== D6: NEW EDGE - revoke now depends on a live MULTI-LOT resolution ===
  created: 201 4978c62b54754d50ab8e2bb66425613f
  revoke when no longer multi-lot: 422 validation_error
  revoke when the resolver is DOWN:  500 internal_error
  record status after both attempts: active (still active -> cannot be withdrawn)
```

If the city records later collapse the condo to a single base lot, or the DTM channel is unavailable, an active confirmation becomes **un-revokable** and stays ACTIVE. Blocking `create` / `supersede` on a degraded resolver is fail-safe; blocking `revoke` fails in the *unsafe* direction — it is the repo's own D-051 lesson that fail-closed is not universally conservative and each path must validate its own fallback direction. Impact today is nil (router unmounted, no calculation reads confirmations, and `build_site_definition_block` still surfaces the parcel discrepancy honestly). Suggested for slice 2 before the mount: bind revoke on the stored record's own `condo_key` against a resolution that need not be multi-lot, or let a resolver failure fall through to the record-scoped binding. No test covers either degraded case.

My earlier A-1..A-8 advisories were explicitly not taken; they remain accurate against this head and route to the backlog. Read-only discipline held throughout: no repo file created, edited, or deleted; no `git` write verb, no `tools/project_control.py`, no `gh`, no npm. Probes live in the session scratchpad (`…/scratchpad/g4_delta_probe.py`) and only import the worktree package.

## 7. Rationale

Both blocking findings are genuinely repaired at the source level, each is bound by a test I independently proved is a mutation catcher, the legitimate supersede path is regression-free, the folded revoke binding carries a positive control, all four required commands reproduce their expected values exactly (0 / 582 no-skips / 726 / 0), the +4 test delta reconciles against the broad suite, and all six SS12.1 digests verify. The one new edge I found is an advisory on an unmounted, calculation-free surface, not a blocking defect.

VERDICT: PASS
