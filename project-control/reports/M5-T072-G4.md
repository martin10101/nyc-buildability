# M5-T072 — G4 test-adequacy review (backend-engineer, read-only)

> Transmission history: returned in two transmissions — the initial return truncated inside
> F7 at the phrase "`stor`"; the remainder was requested from the SAME reviewer FROM that
> exact cut phrase and arrived as micro-chunks 1/5–5/5. Joined verbatim at the cut point
> (the chunk labels are transport artifacts, left in place inside the continuation). Saved
> verbatim by the orchestrator (transport entity-decoding only).

Pinned head verified: `git rev-parse HEAD` = `4ed4d3d737c9755750dc1969cb289eae49017c31`. Working tree carries only agent-memory files (no material drift).

**Part 1 — findings F1–F3**

**F1 (PASS, `services/api/tests/site_definition/test_site_definition_records.py:841-842`).** AS-1 is closed for both revoke branches. The added pin `assert str(missing.value) == _UNIFIED_NOT_FOUND_TEXT` (:842) sits after the pre-existing `assert str(missing.value) == str(foreign.value)` (:836) in the same test body, executing in that order, so the foreign branch is bound to the constant transitively. I confirmed both revoke raise sites (`app/site_definition/store.py:428` missing, `:441` foreign) pass the one shared constant `_NOT_FOUND_FOR_ADDRESSED_PROPERTY` (`store.py:72`), and that the test-local `_UNIFIED_NOT_FOUND_TEXT` (`:855`) is a byte-copy rather than an import of it — the correct oracle form, since importing the store constant would make the assertion tautological.

**F2 (INFO — mutation evidence corroborated, not re-run).** My broker refused shell file-writes outright ("'t072-g4' is operationally read-only: repository/GitHub/control-plane mutation and shell file-writes are blocked"), so I could not reproduce the mutant even in a scratchpad copy. I corroborated the recorded `[ORCH-HARVEST]` result analytically instead, and it holds exactly: under a both-branch fork to `"revoke-only divergent not-found text"`, :836 still holds (both branches emit the same new literal), :837 is untouched (reject_code unchanged), and :839/:840 hold because that literal contains neither `"does-not-exist"` nor the record id — so the pre-pin suite is green, which is precisely the A2 gap. With :842 present the same mutant fails there and only there. The recorded transcript (43 passed without the pin, AssertionError at the pin with it) is decisive.

**F3 (LOW — raised then closed by me).** The harvest's step (b) ran only `tests/site_definition` (43 tests), not the full 77-test scoped suite, so "the pin is the SOLE catcher" was unproven for the 34 route tests. I closed it: the literal appears nowhere but `store.py:73` and `test_site_definition_records.py:856`, and the route-level counterpart `tests/api/test_site_definition_api.py:788` asserts only cross-branch response equality plus no-id-echo — it stays green under the both-branch fork. The sole-catcher claim stands as written.

**Part 2 — findings F4–F7, assessment, verdict**

**F4 (LOW, advisory — no rework).** The foreign branch's binding is transitive, resting on :836. If a future edit weakened or removed :836, the foreign branch would silently unpin while the test kept passing. An explicit `assert str(foreign.value) == _UNIFIED_NOT_FOUND_TEXT` would make each branch independently pinned. I am not requiring it: the packet's behavior-preservation bound allows one assert plus one comment, and this is byte-for-byte the structure the T069 G3 review already accepted for supersede (`:880` + `:886`). The same advisory applies equally to supersede, so any follow-up should cover both rather than singling out revoke.

**F5 (LOW, advisory).** `_UNIFIED_NOT_FOUND_TEXT` is consumed at :842 but defined at :855, under the "M5-T069 — DB-040(s) supersede" section banner. This is runtime-safe (module-level global resolved at call time) and the suite proves it, but the revoke test now depends on a constant that reads as owned by the supersede section. A future touch could hoist it to the module's constant block above the revoke test.

**F6 (INFO — no over-fitting).** The pin constrains exactly one thing: the not-found wording that :886 and :909 already pin. It adds no coupling to reject_code, status, ordering, or any unrelated behavior, and a deliberate wording change costs one constant edit — the intended friction for an oracle-hardening invariant.

**F7 (INFO — out of scope, for the DB-040 line).** `stor

**Micro-chunk 1/5**

e.py:281-283` (`get()`) is the one remaining `ConfirmationNotFoundError` whose text diverges from the unified constant and echoes the probed record id. It is not route-reachable today — the v1 routes call only `create`, `list_for_condo_key`, `supersede`, and `revoke` — so it is not an oracle now, but it would become one if `get()` were ever wired to a route. No T072 action.

**Micro-chunk 2/5**

**Self-run evidence (cwd `services/api`, at the pinned head).** `python -m ruff check .` → `All checks passed!` (exit 0). `python -m pytest tests/site_definition tests/api/test_site_definition_api.py -q` → `77 passed`. `python -m pytest tests/site_definition -q` → `43 passed`.

**Micro-chunk 3/5**

That 43 matters for F3: the `tests/site_definition` directory contains only the single records test file, so the harvest's 43-test mutant run was the whole store-layer suite, not a fragment. No mutants were applied by me; no repository file was written — I could not even update my own agent-memory (`Edit` refused: "'t072-g4' is operationally read-only and may not use Edit"), so this session produced no memory write.

**Micro-chunk 4/5**

**Assessment.** The change is a genuine, minimal, non-cosmetic closure of T069 G3 Advisory A2: it converts a structurally-enforced-only invariant into a test-enforced one, and the recorded mutation evidence is both internally consistent and independently reproducible by reading the assertions against the named mutant. Scope discipline is clean (+2/-0, one file, zero production edits, every existing assertion byte-unchanged).

**Micro-chunk 5/5**

Suite hygiene is good; ruff and the full scoped suite are green at the pinned head. The two advisories (F4, F5) are maintainability notes that mirror the already-accepted supersede structure and should not gate acceptance.

G4 VERDICT: PASS

END-OF-REPORT
