# M5-T107 — G3 code review (code-reviewer "cr-t107", read-only)

> Transmission history: pinned at 7f5c82fa, delivered as three SendMessage parts (1/3..3/3) ending with END-OF-REPORT;
> no truncation. Joined verbatim by the orchestrator (transport wrapper tags removed only). Verdict PASS with advisories
> A1 (attribute keys and diagnostic fields not HTML-escaped; the disclosure over-claims), A2-A5; carried into the rework
> inventory.

---

M5-T107 G3 code review (READ-ONLY) — PART 1 of 3. HEAD pinned at start: 7f5c82fa. Material reviewed: d3c47a85 (7 files, exactly allowed_paths).

EVIDENCE (all run here):
- `ruff check .` (cwd services/api): All checks passed.
- `pytest tests/scenario tests/connectors -q`: 1823 passed in 62.8s (matches harvest).
- `modularity_check.py --check`: exit 0 (connector above the 750 justify signal, below the 1000 hard cap; cohesion note recorded in the producer report §Cohesion note).
- `git show --stat d3c47a85`: exactly the 7 allowed files. app/main.py, massing_model.py, proposal.py NOT in the commit (the massing_model delta vs the claim-seam is M5-T106's parallel work, correctly excluded). UNMOUNTED and forbidden paths respected.

VERIFIED CORRECT (no defect):
- Assembler is pure/deterministic/offline: no I/O, no clock, no logger (AST-tested test_as3_assembler_does_not_log; test_as1_assembler_is_deterministic). scene_assembler.py:472-502.
- Section 2.1 shape: MassingModel.as_dict() carried verbatim + context_buildings; vertical_unit explicit (scene_assembler.py:487); ONE declared datum GROUND_DATUM_DECISION (:100-133) with BOTH official GROUND_ELEVATION definitions disclosed, neither reconciled, NAVD88 attributed to the City dictionary only — recorded in provenance.scene (:491-501). Matches massing_model.as_dict() keys (massing_model.py:579-594).
- Typed refusals: SceneAssemblyError wraps MassingModelError preserving reason+field (:323-324; massing_model.py:171-172). String coords parsed or typed-refused (parse_ring :224-246). MultiPolygon holes disclosed, never dropped (:392-397, DB-053 g).
- Honesty (D-083): test_as2_the_assemblers_own_labels_carry_no_barred_claim_word runs the shared claim-word screen over every emitted label/disclosure.
- Route: flag-gated generic 404 sentinel byte-identical to unmounted (scene_api.py:152-155,236-237); include_in_schema=False (:230); real-app absence test passes; bounded body via reused T053 primitives (:246-254); off-loop cancellable job under asyncio.wait_for (:312); connector called interactive=True with a matching wall-clock deadline (:292-305, DB-073 c); typed 413/422/429/504/500; server correlation id; SCENE_STATUS_STATE_MATRIX is the single source of truth.

DB-073 (a)-(g) — all verified:
(a) U+2028/U+2029 added to _CONTROL_CHAR_DELETE (arcgis:432-438); (b) cid length-bounded at source (_safe_correlation_id:453-469); (c) _check_deadline before metadata fetch (:1078) AND during retry sleep — _sleep_within_deadline (:640-648); the injected sleep is called OUTSIDE the retry try/except (transport.py:505), so the raise propagates to the fail-closed path (behaviourally proven by test_pkte_deadline_refuses_during_a_retry_sleep); (d) tz-naive deadline → typed disallowed_request before any I/O (_validate_deadline:966-980; transport.calls==[]); (e)-(g) below in part 2 tail. Part 2 next.

---

M5-T107 G3 — PART 2 of 3.

DB-073 (e)-(g) verified:
(e) The grep-for-literal not-wired test is REPLACED by an import-graph/AST walk from app.main (test line ~811). I reproduced the walk: 60 modules reachable from app.main; building_footprints_arcgis and scene_api both correctly ABSENT; the assembler→connector edge asserted present. It is genuinely load-bearing — mounting scene_api in main.py (or importing the connector from any reachable module) would add it to the reachable set and redden. Not a false-green.
(f) Per-page deadline: advancing-clock test trips on page 2 (replay.calls == [metadata, P1]; page 2 never fetched).
(g) Cumulative-bytes ceiling accumulates across pages (ceiling set between the 1-page and 2-page sums → refuses on page 2).
Robustness to M5-T106: the assembler codes ONLY against the massing public API (build_massing_model / build_from_generated_option / as_dict / MassingModelError.reason+field / SOURCE_PROPOSED / VERTICAL_UNIT). T106's changes (raw-vertex NYC check, GEOS wrap, bounded echoes) are internal to build_massing_model and surface through MassingModelError, which the assembler re-raises as a typed SceneAssemblyError — so new T106 reasons propagate cleanly as 422s. No reliance on massing internals; forbidden path untouched.

ADVISORY findings (none gate-blocking for G3):

A1 (security-relevant; hand to sec-t107 for the DB-058(a) severity call). scene_assembler.py:345-348 `_escape_attributes` escapes attribute VALUES but NOT KEYS, justified by the comment "Keys are the pinned official field names (not caller text)" — that premise is INACCURATE: the connector drift-signals unknown attribute keys and passes them through raw (`attributes=dict(attrs)` arcgis:930; drift-only at :794-797), so a novel/hostile upstream KEY string reaches `attributes_escaped` keys un-escaped. Likewise `gaps[].raw`, `drift_signals` (`unknown_attribute:<name>`), and `geometry_findings` are emitted un-HTML-escaped (they are `_safe_repr` = repr+length-bound, NOT HTML-escaped — geometry.py:55-60). The assembler's own disclosure `untrusted_source_text_escaped` (:167-174) over-claims ("HTML-escaped … raw strings are not emitted"). MITIGATIONS keeping this advisory not blocking: official source with a fixed key schema in practice; the primary carrier (values) IS escaped and tested; and the payload carries the connector's UNTRUSTED_TEXT_NOTICE ("consumers MUST escape these fields on render", arcgis:212-215), so a correct consumer escapes keys anyway. RECOMMEND: escape keys too (cheap: `_escape_untrusted(k)`) OR soften the disclosure to state keys + diagnostic fields (gaps/drift/findings) are not HTML-escaped and must be escaped per the carried notice. TEST GAP: test_as3 exercises a hostile VALUE only, never a hostile attribute KEY. Part 3 (rate limiter, design note, minors, verdict) next.

---

M5-T107 G3 — PART 3 of 3.

A2 (rate limiter — scene_api.py:91-138). Correctness: sliding window, reads MAX/WINDOW at call time so tests can tighten; prunes per-key on each call; 429 typed. Thread-safety: SAFE — `_rate_limit_allows` runs entirely on the event-loop thread BEFORE any await (:242, before the threadpool handoff at :312), so the read-modify-write on `_rate_state` cannot interleave under asyncio. Memory bound: `_rate_state` is UNBOUNDED in the number of caller-host keys (each key's list is window-pruned, but keys are never evicted). Acceptable while UNMOUNTED + flag-gated; producer disclosed this as DISCOVERY DB-1. Must bound the tracked-key count before public exposure (PKT-H).

A3 (DESIGN NOTE, as you requested). This is the THIRD route-local rate limiter (M5-T108/T109 built their own). Recommend extracting ONE shared route rate-limit module at PKT-H — a bounded/pruned store with the per-caller key chosen at the auth seam — instead of a fourth copy. Matches the producer's DB-1 discovery; route it there.

A4 (minor). scene_api.py:287 `context = body.get("context") if isinstance(..., dict) else {}` silently treats a non-dict `context` as no-context (honest `not_requested` layer, but a caller-fault context could arguably be a 422). Producer disclosed the sibling case (invalid context params → disclosed refusal inside a 200) as DB-2. Low priority; fine for the internal route.

A5 (honesty nuance, not a defect). For a zero-but-unverified ground, `base_z_ft` is a real number and `base_z_grounded=True` while `ground_status="ground_zero_unverified"` (scene_assembler.py:356-388; parametrized test row 2 pins it). Honesty rests on `ground_status`; a consumer keying only off `base_z_grounded` would treat an unverified ground as grounded. Faithful passthrough of the connector contract and disclosed — acceptable; worth a one-line consumer note that ground_status is authoritative.

Tests overall: behaviour-focused, offline (injected fakes/monkeypatch), every new guard has a reddening in-process mutation (consuming-namespace mutations for the strip table and the rate limit). Zero new dependencies; ruff clean on the 6 code files; app/main.py untouched. Scope is exactly the allowed paths.

Summary: correct, well-tested, in scope, honest. The only substantive item is A1 (attribute-key / diagnostic-field escaping vs the assembler's stated guarantee) — I flag it to sec-t107 for the DB-058(a) severity call; from a G3 code-review standpoint the behaviour is honest (notice carried, values escaped) and correct for the realistic threat, so it does not block G3.

M5-T107 G3 VERDICT: PASS

END-OF-REPORT
