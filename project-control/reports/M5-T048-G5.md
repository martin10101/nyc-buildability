# M5-T048 G5 — independent security review (security-reviewer, saved VERBATIM from the agent return)

G5 SECURITY REVIEW — M5-T048 (proposal scenario contract, phase B0) — VERDICT: PASS (two ADVISORY, zero BLOCKING). Reviewed at pinned HEAD 15c1b3a4 (verified). Material 0c7b17f1 + 93f194c0.

BASIS: New validator services/api/app/scenario/proposal.py (pure-stdlib, `import math` only) correctly gates the untrusted editor-authored proposed_massing block: NaN/Infinity refused at two layers, integer fields bounded, no eval/subprocess/os/regex/network/shapely, provenance treated as claimed data (never auth), no new dependency, secret-scan/gitleaks green, fixtures PII-free. Two latent hardening gaps for the wiring phase (no user-input route reaches the validator yet), neither blocking a validation-only B0.

FINDINGS
1. ADVISORY — aggregate quadratic geometry cost. _ring_is_simple is O(n^2); for a SINGLE outline it is correctly gated behind MAX_OUTLINE_VERTICES=1000 via an O(1) len() check placed BEFORE the parse loop and before the simplicity check (proposal.py:220-225 precede 279), so the task's specific "huge single vertex list before the ceiling" concern is handled. BUT _validate_levels runs _validate_outline (its own O(n^2)) for each of up to MAX_LEVELS=500 per-level outlines (proposal.py:352-354), so worst case ~500 x ~5e5 segment tests ~= 2.5e8 per document (order minutes CPU). Bounded, not unbounded. Recommend a GLOBAL vertex budget across base+per-level outlines (or lower per-level ceilings) before B1/B3 exposes a network route. Refs: proposal.py:143-183, 295-298, 352-354.

2. ADVISORY — unbounded string length on user-authored fields (DB-023c bounded-repr precedent). common.schema.json non_empty_string enforces minLength:1 but NO maxLength; it is the $ref for author, editor_version, and wall id. parent_scenario_id is [string,null], also unbounded. The module adds no length ceiling. wall_id is embedded via `{wall_id!r}` in the duplicate-id refusal (proposal.py:386); srid/vertex/kind reprs are similarly embedded on the direct-call path. Recommend a MAX_STRING_LEN ceiling on every string field + bounded-repr on any embedded user value before editor input is wired. Refs: proposal.py:380-388, 410-435; common.schema.json non_empty_string. Live-impact mitigation today: see calibration.

CALIBRATION (not a defect) — both consumers, scenario.py:294 and scenario_analysis.py:547, validate a SERVER-CONSTRUCTED build_scenario(...) document as a response-side belt-and-suspenders guard; build_scenario emits no proposed_massing block in B0, so validate_proposed_massing is unreached by any live route. Findings 1-2 are therefore LATENT (become live when B1/B3 wires editor input) — hence ADVISORY. Both routes log ONLY exc.location (the field path) + correlation_id and return a generic 500; no user value flows into logs or client responses today, so no live repr/log amplification.

VERIFIED CLEAN
- Numeric safety: contract.py:114-121 strict-JSON guard json.dumps(document, allow_nan=False) refuses NaN/Infinity at <root> FIRST; module re-checks via _is_real_number/math.isfinite (proposal.py:191-198, 331-339). srid/level_index/floor_count/vertex-index are isinstance-int excluding bool, all bounded (floor_count<=500, indices in range, level_index by contiguity {0..N-1}). No non-finite or unbounded int can pass.
- Injection: no eval/exec/subprocess/os/open/regex; no user value into paths/SQL/eval. Only unbounded repr is finding #2.
- shapely: NOT used (pure stdlib) — no WKT/WKB parsing, no pathological-geometry parser DoS class. Better than the schema's suggested shapely option.
- Provenance honesty (D-076-R002 / AS-4): kind is literal "proposed"; author/editor_version recorded as strings, never identity/auth; validate_proposed_massing returns None and computes no allowance; docstrings state records/rules/proposals distinction.
- Supply chain: only `import math` added; no requirements/lockfile change in the diff (git stat); no network; secret-scan + gitleaks green (CI run 35439235554).
- Fixtures: provenance uses architect@example.com (RFC-2606 example domain) + placeholder strings; no real secrets/PII.

Files: services/api/app/scenario/proposal.py; services/api/app/scenario/contract.py; services/api/app/_contract_schemas/v1/{scenario,common}.schema.json; services/api/app/api/v1/{scenario,scenario_analysis}.py (consumers, unchanged by this task).
