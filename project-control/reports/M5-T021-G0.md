# M5-T021 — G0 Contract/Packet Gate

Gate: G0 (packet completeness). Result: PASS. Recorded by: orchestrator (contract gate;
producer for this dependency task is the orchestrator itself — gh workflow dispatch is
orchestrator-only authority and the material edit is a few registry-fact-driven pins;
reviewers code-reviewer + security-reviewer are independent, ≠ producer).

## Checklist

1. **Exact requirement identifiers** — D-042-R001 verbatim (upgrade to exactly 15.5.24 +
   whole-tree advisory sweep + full machinery + CI green), with R003 (no deploy) and R004
   (no 16.x) pinned as preserved prohibitions; B-022 named as the blocker this discharges
   (the R002 resolution step follows acceptance).
2. **Exact directive references** — `D-042:D-042-R001`, bound via append-only applicability
   (digest 7fd8d5c2 → 2a1b8b0c, audit-logged); `evaluate_task_refs` ok=true, applicable ==
   cited == ["D-042-R001"]; validator EXIT 0.
3. **Exact source/evidence files** — seven pinned inputs: the directive, the blocker, the
   authoritative generate-lockfile workflow (with the bot-push-no-CI trap), the current
   package.json pin/override inventory, the audit output naming all 7 advisories (incl.
   the js-yaml-override-blocks-its-own-fix mechanic), the registry research (in flight,
   to be preserved at M5-T021-registry-research.md), and the policy documents.
4. **Exact allowed/forbidden paths** — 4 allowed (the two dependency files + two reports;
   the lock is bot-committed on the branch and included in the material); forbidden fences
   all app code, config, workflows, render.yaml, services, packages, control plane.
   No overlap with any in-flight task (none in flight).
5. **Acceptance scenarios** — S1 exact pin + minimal evidenced bumps; S2 the workflow-proven
   lock (the four-check chain before the bot commit); S3 CI green with the LAST standing red
   retired; S4 prohibitions preserved (no deploy, no 16.x, no app-code change).
6. **Gates & reviewers** — G0/G1/G5; code-reviewer + security-reviewer (D-042-R001 names the
   G5 review; the machine gates in the workflow + CI are the QA authority, so no separate G4).
7. **Modularity** — no production source; two dependency manifests + reports. N/A beyond the
   minimal-diff discipline stated in path_notes.

## Notes

- Age-gate discipline is fail-closed with no waiver: any needed fix version younger than 7
  days stops the task honestly (recorded, not waived).
- The bot's lock commit is part of the task material; the bot-push-no-CI trap and its
  empty-commit remedy are documented in the packet.

VERDICT: PASS — packet is claimable.
