# GATE REPORT — M5-T016 (G1 independent code review)

> Preserved VERBATIM by the orchestrator from the code-reviewer agent return
> (transport entity-decoding only, per the report-preservation rule). Reviewer preamble:
> "I have completed my independent G1 review. All evidence verified against the frozen
> review identity. Here is my gate report."

- **Task:** M5-T016 — Address Confirm card + ZoLa deep-link + handoff (address-entry Packet 2)
- **Reviewer:** code-reviewer (read-only, ADR-005)
- **Gate:** G1 (correctness / provenance / contract / maintainability)
- **Frozen identity verified:** producer material `65d72bfd` (parent `ffaa85dc`). `git diff 65d72bfd..ed110e6a -- apps/web` is **empty** (byte-identical confirmed) — apps/web content reviewed is exactly the producer material; later commits are control-plane/docs only.
- **Submit record:** `project-control/reports/M5-T016.json` (reviewed_sha `069c3d40`, identity `8add6d7b`, applicable requirement set EMPTY).
- **CI evidence verified:** `project-control/reports/M5-T016-ci-evidence.txt` — run 34679638323: `web` SUCCESS, `web-e2e` SUCCESS on first execution (address-resolution 32 tests PASS, address-confirm 11 tests PASS, whole web suite 28 files green). Only reds are the two standing owner-gated jobs (`web-dependency-security`, `control-plane`).
- **Scope:** 7 files, all inside `allowed_paths` (`git show --stat 65d72bfd`); no forbidden path touched; `package.json`/lockfile untouched.

## Verification method
Re-derived each acceptance criterion from the packet + design specs (not from the producer conclusion); read the full diff at `65d72bfd`; byte-compared the extracted module against the removed screen code; diffed the test module against its parent for weakened assertions; traced the two href contexts through `validateBblInput` (`bbl.ts`) and the bounding primitives (`bounded.ts`); confirmed the handoff target contract in `confirm/ConfirmScreen.tsx:485` (reads `?bbl=`, re-validates, hydrates); confirmed D-038 applicability against `requirements.json`.

## Findings

**1. Href discipline — the core risk — CORRECT (no finding).**
Both URL contexts are built exclusively from `validateBblInput(view.canonical.bbl).canonical`. Triple containment: `view.canonical.bbl` is already `boundedToken(...,32)` (safe charset) → `validateBblInput` (digits-only, length 10, borough 1–5) → `encodeURIComponent`. ZoLa host is the module constant `ZOLA_BBL_URL_PREFIX = "https://zola.planning.nyc.gov/bbl/"` (`AddressConfirmCard.tsx:31`), never concatenated from response data; `target="_blank" rel="noopener noreferrer"` present. On any validation failure both anchors are replaced by honest-absence copy (`zola-link-absent`, `confirm-continue-absent`, `resolved-bbl-absent`). The hostile-BBL path (`boundedToken` strips markup, remainder fails `validateBblInput`) provably yields no anchor — confirmed by the S2/S6 href-sweep tests (`address-confirm.test.tsx:236-253, 379-388`). `/bbl/` route matches the confirmed research (`zola-deeplink-url-confirmation.md` §2 recommendation) and the packet input line 10; it correctly supersedes the spec's tentative `/l/lot/` form.

**2. address-api.ts additive-only — CORRECT (no finding).**
New `RequestParamView`/`SourceFactView` interfaces, new `sourceFacts`/`provenance.endpointHost`/`provenance.requestParams` fields, and new pure helpers `scalarOrNull`/`hostOrNull`/`requestParamViews`/`sourceFactViews`. Existing outcome discrimination and all prior canonical/echo/provenance bounding (`address-api.ts:255-283`) are byte-preserved. `confidence` deliberately not mapped. `endpointHost = hostOrNull(...)` returns `new URL(v).host` bounded to 64 — HOST only, full URL with params never surfaced (packet risk 3 closed).

**3. Extraction is behavior-neutral (S7) — CORRECT (no finding).**
`AddressOutcomeCards.tsx` is a byte-preserving move of the removed cards + failure primitives (`FailureTitle`/`Meta`/`RetryButton`/`EchoLine`/`GrcLines`/`RecoveryActions`) and the full error-copy matrix; markup/copy/testids identical to the removed screen code, only `export` keywords added and `Meta` exported for reuse. `ResolvedCard` was correctly superseded (not moved) by the confirm card. Test-module diff shows exactly: (a) legitimate stub→confirm routing replacement (this packet's S1), (b) mechanical `address-resolved`→`address-confirm-card` retargets, (c) additive strengthening (per-state `bodySnippet` column; source scan extended to the two new files; `next/link` allowlisted). **No assertion weakened.**

**4. Screen state handling (S1/S4) — CORRECT (no finding).**
`resolved`/`resolved_with_warnings` route to `AddressConfirmCard` (`AddressResolutionScreen.tsx:201-208`); warnings block renders above Continue and never gates (DOM-order asserted). `notMyProperty` (`:183-186`) clears result and focuses the always-mounted street input — form values retained (no `setValues`), no fetch, announcer collapses to `""`. Focus persists because the focused target is not the unmounting node. Matches the existing `editAddress` idiom.

**5. Provenance discipline (S5) — CORRECT (no finding).**
Collapsed-by-default `<details>`; source id, endpoint HOST, retrieval timestamp, both GRCs, connector correlation id **labeled distinctly** from the HTTP reference id, response digest, bounded request-param key/value rows, and source-fact rows. Not-verified posture line verbatim from spec §4. `confidence` never rendered; no "Verified" badge.

**6. Carried items discharged.** G3 F4 (card extraction) ✔; G4 finding-3 (per-state body-copy assertions via the `ERROR_MATRIX` 4th column) ✔; G5 F-2 (hostile-resolved fixture — S6 puts hostile text in normalized street/borough/params/fact values) ✔. G5 F-1 (flag divergence `INTERNAL_RULE_EVAL_UI` vs `INTERNAL_RULE_EVAL_ENABLED`) carried as a report-only owner note (producer report §3.6, task path_notes) — **no env/flag wiring changed in the diff** (confirmed). Correct handling.

**7. Directive D-038 — applicable set EMPTY is CORRECT (no finding).** `requirements.json` scopes every requirement by explicit `task_ids` with empty `task_types`/`milestones`/`paths`: R001/R002/R005–R007 → `D-038-BOOTSTRAP`; R003/R004 → an enumerated list ending at `M5-T013`. `M5-T016` appears in none, so the conjunction yields zero applicable requirements — the submit record's empty set is verifiable. (Formal `verification.json` remains the control-plane-verifier's separate pass; no requirement is violated by this code.)

**8. Modularity / deps.** `AddressResolutionScreen.tsx` 282 lines, `AddressOutcomeCards.tsx` 414, `AddressConfirmCard.tsx` 242 — all under the 600 warn threshold; CI `modularity` job SUCCESS. No new dependency (`next/link` is existing Next.js).

### Advisory (non-blocking)
- **A1 (docs accuracy):** prose line/test counts disagree with reality — producer report says "10 tests"/"306 raw lines"; commit + progress log say "264 lines"; actual is 11 tests and 282 lines. Cosmetic; all under threshold.
- **A2 (test robustness):** the no-badge assertion `container.textContent).not.toMatch(/\bVerified\b/)` (`address-confirm.test.tsx:338`) is case-sensitive. No real gap (confidence is never rendered), but `/verified/i` would be stronger.
- **A3 (external-source caveat):** per the research doc, ZoLa may render an empty client-side state for well-formed condo/billing-lot BBLs; the card cannot detect this without PLUTO data (out of scope — Packet 3 on the expansion hold). The lot-outline honesty placeholder mitigates. No action.
- **A4 (repo hold, not this task):** `web-dependency-security` FAILURE (Next.js RCE awaiting owner authorization) is pre-existing and owner-gated; this task introduces no dependency change and does not affect it. It remains a repo-level merge/deploy hold for the orchestrator — outside this task's G1 scope.

## Conclusion
Every acceptance scenario S1–S8 is satisfied in code and pinned by reproducible tests at the frozen SHA; the two href contexts are provably safe; the extraction is byte-neutral with no weakened tests; provenance and not-verified discipline are correct; additive-only address-api extension preserves existing behavior; no forbidden path or dependency touched; carried findings discharged; directive applicability empty-set verified. No blocking or required-correction findings.

**VERDICT: PASS**
