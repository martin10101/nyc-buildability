# M5-T017 — G1 Code Review Gate Report (verbatim)

Orchestrator preservation note: report returned by the independent code-reviewer agent
("G1 code review M5-T017") on 2026-09-12 and preserved verbatim below (transport
entity-decoding only). Reviewer was read-only; frozen review identity: producer commit
45f63efb. Verdict: PASS (zero required corrections; advisories A1–A4).

---

I have completed all review steps and independent reproductions. Here is my gate report.

---

# GATE REPORT — G1 Independent Code Review

**Gate ID:** G1
**Task:** M5-T017 — C1 unused draft zoning floor area (FAR-derived) — scenario document section
**Directive regime:** in-regime — `directive_refs: D-041 → D-041-R001`
**Reviewer:** code-reviewer (independent; read-only)
**Producer:** scenario-optimization-engineer
**Frozen review identity:** producer material = single commit `45f63efb` (base `462c8ad1`); current HEAD `f4f01352`
**Date:** 2026-09-12
**Result: PASS** (zero required corrections; four advisories, one of them a merge-sequencing constraint already handled by the orchestrator)

---

## Frozen-identity verification

`git log 462c8ad1..f4f01352` restricted to the 17 allowed_paths returns exactly one commit: `45f63efb`. Every later commit (ledger records, CI evidence, M5-T018 web material) leaves all 17 allowed_paths byte-unchanged. Review was conducted at `45f63efb` content.

`git show 45f63efb --stat`: 17 files, +1697/-4. The changed set equals the packet's 17 `allowed_paths` exactly; **no `forbidden_paths` entry is touched** (verified: no `apps/web/**`, `derive.py`, `profile/**`, `api/**`, `tools/**`, etc.). `models.py` and `constants.py` diffs contain **no deletion lines** (purely additive; existing symbols untouched).

---

## Steps executed (reproduced from source, not from the report)

| # | Command | Result |
|---|---|---|
| 1 | `git show 45f63efb --stat` + per-file diffs | 17 files = allowed_paths; scope clean |
| 2 | `python -m pytest services/api/tests/scenario -q` | **422 passed** |
| 3 | `python -m pytest services/api/tests/api -q` | **398 passed** |
| 4 | `python tools/modularity_check.py --check` | **EXIT 0**, failures 0; builder.py and unused_floor_area.py NOT among the 16 warnings |
| 5 | `python -m ruff check services/api/app/scenario …` | All checks passed |
| 6 | `python -m pytest packages/contracts/scripts/tests -q` | 29 passed |
| 7 | `python -m pytest …/test_unused_floor_area.py -q` | 24 passed |
| 8 | `diff` of both schema copies | **byte-identical** |
| 9 | Independent direct-call reproduction (10 self-chosen cases incl. NaN/Inf/negative/bool/no-mutation/JSON) | **ALL PASS** |

My local reproduction (422 / 398 / EXIT 0) matches the orchestrator pre-gate capture and the CI `api` job in `project-control/reports/M5-T017-ci-evidence.txt`.

---

## Acceptance-criteria table (S1–S6)

| ID | Case | Verified evidence | Verdict |
|----|------|-------------------|---------|
| S1 | computed normal remainder | `value == cap - existing` unrounded, unit `square_feet`; label/scope/formula constants verbatim; cap provenance echoed == `document["cap_provenance"]`; existing provenance resolved from root `provenance[]` (source_id/original_field_name); ZR 12-10 assumption record; PRR False. Independently reproduced (cap100−existing40=60). | PASS |
| S2 | over-built honest negative | state `over_built`; `value == cap-existing < 0`, `!= 0`, `!= abs()`; `over_built_statement` present; section PRR True AND root PRR True. Independently reproduced (100−130 = **-30.0**, never clamped). | PASS |
| S3 | zero boundary | `value == 0`, state `computed` (not over_built/not missing), no PRR. Independently reproduced. | PASS |
| S4 | missing/unusable existing area | (a) no fact / null → `not_computable`/`missing_existing_building_area`, value null; (b) `data_conflict`/`unsupported` coverage → `existing_building_area_unusable` with coverage echoed verbatim; non-numeric/NaN/neg → unusable. Independently reproduced NaN, Inf, negative, bool. | PASS |
| S5 | no draft FAR cap | 6 no-cap factories + degenerate `{}` → `not_computable`/`no_draft_far_cap`, value null, formula null, assumptions `[]`. All no-scenario `_assemble` branches pass `cap_value=None`. Independently reproduced. | PASS |
| S6 | contract/JSON/precise-noun regression | both schema copies byte-identical (25992 B); new key required + closed (`additionalProperties:false` on section and inputs); every state survives `json.dumps(allow_nan=False)` and utf-8 encode; no forbidden noun / no "verified"/"compliant" in C1 constants. | PASS |

---

## D-041-R001 requirement verification (re-derived from `requirements.json`)

| Sub-requirement (verbatim intent) | Reproduced evidence | Status |
|---|---|---|
| C1 output = existing built floor area vs draft residential FAR cap | `value = cap - existing_area` (unused_floor_area.py:312); cap consumed VERBATIM (`_positive_finite_float(cap_value)`, never recomputed) | PASS |
| Its own clearly labeled line | `UNUSED_FLOOR_AREA_LABEL` precise-noun constant, rendered on every state | PASS |
| Standard draft-label + per-fact provenance discipline | draft framing in label/scope; `inputs` block: cap provenance echoed, existing provenance resolved from `provenance_ref` against root `provenance[]` (mirrors `_lot_area_provenance`) | PASS |
| Over-built (negative remainder) renders HONESTLY as its own explicit statement | state `over_built` + `over_built_statement` constant | PASS |
| …and routes to professional-review flag | section PRR True + builder ORs into root `professional_review_required` | PASS |
| Never clamped to zero, never hidden | negative preserved exactly (independently: -30.0); section present on every document | PASS |
| Missing existing-area fact → standard missing treatment, never estimated | `missing_existing_building_area`, value null | PASS |
| No applicable FAR rule → standard missing/unsupported treatment, never estimated | `no_draft_far_cap`, value null, on every no-cap path | PASS |
| Contracted as gated ledger task citing D-041:D-041-R001 | M5-T017 carries `directive_refs D-041→D-041-R001`; required_gates G0/G1/G4/G5 | PASS |
| Required harness (normal, over-built, missing fact, missing/unsupported FAR; fixture-derived) | S1/S2/S4a/S5 present; assertions derived from `S.trace_cap(...)`, no retyped magic numbers | PASS |

(The recorded `verification.json` is authored by the independent `directive-compliance-verifier`, per the directive registry; the above is the G1 code-review reproduction.)

---

## Detailed findings

**Module correctness (unused_floor_area.py).** Decision order is exactly no-cap → missing fact/null → unusable (coverage not in `{"conditional"}` OR value not finite non-negative) → subtract. Cap is echoed verbatim (a separate float subtraction; the echoed `value_sq_ft`/provenance are the untouched cap). Value is unrounded; negative preserved; exact zero classified `computed`. Guards `_finite_float`/`_positive_finite_float`/`_nonnegative_finite_float` reject bool, non-numeric, NaN, ±Inf, and negative — reproduced independently. Profile consumed read-only (no-mutation test + my own deepcopy check pass).

**Schema / profile fidelity (no guessing).** The fact shape read (`value`/`units`/`coverage_status`/`provenance_ref`) matches `profile/builder.py::_fact_value`; `existing_building_facts` is keyed by field name via `facts_for(BUILDING_FACT_COLUMNS)` so `bldgarea` is correct; coverage vocabulary (`conditional`/`data_conflict`/`unsupported`) matches `_coverage_status`; the resolved-provenance subset matches `_lot_area_provenance`; the assumption record matches `$defs/assumption`. No schema was guessed.

**Builder wire-in.** +15 lines (935→950), ~13 logic lines confined to `_assemble`; no C1 logic in builder. Root `professional_review_required = professional_review_required OR unused_section["professional_review_required"]`. I reproduced the consumer analysis: no consumer branches on the scenario document's root PRR boolean — `scenario_analysis.py:180` lists it only as a forbidden caller-input key (the new key is server-computed, not a caller input, so no forgery surface); `evidence.py` reads `rule_evaluation` coverage, not the scenario root flag; `scenario.py` mentions it only in a docstring. The widening is safe and semantically correct.

**Contract & ratified expansion.** Both schema copies byte-identical; section + inputs closed and fully specified. The 4 fixtures carry only the new not_computable/computed section (preliminary→`missing_existing_building_area` with cap provenance echoed; 3 no-cap→`no_draft_far_cap`, all-null inputs); root PRR unchanged on fixtures. `scenario.ts` = 26 insertions, scenario.ts only, matching the schema; typegen/bundle CI green. These 5 artifacts are the honestly-disclosed, orchestrator-ratified mechanical consequence of the required-key change.

**Tests.** The three pre-existing test files are extended purely additively (no assertion weakened or removed). New assertions are fixture-derived.

---

## Advisories (non-blocking)

- **A1 — merge-sequencing constraint (already handled).** M5-T017 makes `unused_draft_zoning_floor_area` a REQUIRED key in the CLOSED contract. The hand-written web mirror validator `apps/web/src/lib/scenario-contract.ts` rejects it, so `web-e2e` is red on M5-T017-alone and **green on the combined C1 head (run 34690484292, M5-T017+M5-T018)**. `apps/web` is `forbidden_paths` for this task, so this is not a defect in M5-T017's in-scope code — but **M5-T017 must not merge to main independently of M5-T018.** The orchestrator has already contracted M5-T018 and confirmed the combined head green.
- **A2 — structural (out of scope).** `apps/web/src/lib/scenario-contract.ts` being a hand-maintained closed-key mirror forces a web change on every additive server contract change. Worth a future task to have web consume the generated contract; not this task.
- **A3 — minor semantics.** On `no_draft_far_cap` with a present bldgarea fact, `inputs.existing_building_floor_area.value_sq_ft` is null while coverage_status/provenance are populated. Defensible (no subtraction performed) and schema-valid; asymmetric but not misleading.
- **A4 — informational.** builder.py is 950 lines (over WARN 600, pre-existing); the modularity policy was honored by routing all C1 logic to the new 346-line focused module. A future builder.py decomposition is worth considering.

CI note: the only red on the combined C1 head is `web-dependency-security` (owner-gated Next.js RCE, D-040-R004) — unrelated to M5-T017.

---

## Conclusion

M5-T017's frozen material (`45f63efb`) is in-scope, correct, deterministic, pure, honestly provenance-bound, and precise-noun-clean. All S1–S6 acceptance criteria and every D-041-R001 sub-requirement are independently reproduced. Both schema copies are byte-identical; the ratified fixture/typegen expansion is minimal and legitimate; tests are strong and additive; local pytest (422/398), modularity (EXIT 0), ruff, and contracts-scripts all pass, matching the CI evidence. No code correction is required. The single integration constraint (co-land with M5-T018) is an orchestrator merge-sequencing matter already satisfied by the green combined head.

**VERDICT: PASS**

---

# APPENDED DELTA ATTESTATION (verbatim, same reviewer agent, after correction commit ae38a169)

26 passed, as expected. Here is the attestation, for verbatim appendix to my gate report.

---

## DELTA ATTESTATION — M5-T017 G1 (code-reviewer), 2026-09-12

Re: commit `ae38a169` (single commit after `f4f01352`), discharging G4 required corrections C1 and C2.

**Independently verified:**
1. `git show ae38a169 --stat` = **one file changed, `services/api/tests/scenario/test_unused_floor_area.py`, +39 / -0** (inside allowed_paths). `git diff 45f63efb ae38a169` over the other 15 reviewed files is empty — **all production code, both schema copies, `scenario.ts`, and the 4 fixtures remain byte-identical to `45f63efb`.** No production change; no pre-existing assertion modified or weakened; the diff is purely two appended test functions.
2. `python -m pytest services/api/tests/scenario/test_unused_floor_area.py -q` → **26 passed** (was 24).
3. Both added tests are genuine mutation discriminators, not tautologies: `test_s1_vacant_lot_zero_existing_area_is_usable_and_computed` (bldgarea `0.0` → `computed`, value `== cap`, existing input `0.0`, PRR False) kills the `_nonnegative_finite_float → _positive_finite_float` mutant; `test_s1_fractional_remainder_is_unrounded` (bldgarea `= cap − 4999.5` → value `== 4999.5` exactly and `!= round(...)`) kills a `round()` mutant at the subtraction site. Both assertions are fixture-derived from `S.trace_cap(...)`.

**Conclusion:** The change is test-only, bounded, and additive; it strengthens coverage without altering reviewed behavior. My prior G1 findings are unaffected.

**My G1 PASS stands at the new content** (M5-T017 test file now at `ae38a169`; all other reviewed files byte-identical to `45f63efb`). The four advisories in the original report are unchanged, including A1 (M5-T017 must co-land with M5-T018).

**VERDICT: PASS**
