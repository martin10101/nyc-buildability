# M5-T034 producer report (placeholder)

Seeded at contract time so the packet's allowed_paths carry a tracked file. The loop
producer replaces this with its verbatim return, written to the packet's REVIEWER
HONESTY BAR.
