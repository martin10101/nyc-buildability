# G5 Security/Privacy Gate Report (Round 2) — M0-T023 / Directive D-001 amendment 3

- Task: M0-T023 (Owner Directive Compliance System — D-001 amendment 3), post-round-1 rework
- Reviewer: independent security-reviewer (read-only; not the producer)
- Round-1 head (PASS): `bd8e10119f7c23cfe81bcc7c6323f4ca304b05c0`
- NEW reviewed head: `ed8a6776f89c9f2b17ecad35e528a0180fe6bfd1` (== HEAD)
- Rework delta: `git diff bd8e1011..ed8a6776` — 3 files, +98/-13: tools/directive_registry.py, tools/project_control.py, tools/test_project_control.py

## Verdict: PASS

The rework implements the three git-surface hardening items raised in round 1 plus fail-closed control-flow tightening. Every change is a security improvement or a fail-closed tightening; the delta introduces no new external input surface, no new dependency, no network, and no lockfile change. All three test suites pass at the new head.

## Verification of the four requested items

1. Three git-hardening changes correct, no new issue — CONFIRMED.
   - GIT_LITERAL_PATHSPECS=1 (_run_git ~line 651): valid git env var; forces literal pathspec matching, disabling pathspec magic (:(exclude) etc.) in allowed_paths. Does not alter `--` handling; does not weaken any fail-closed path.
   - timeout=60 + except subprocess.TimeoutExpired -> fail closed: bounds every git call; subprocess kills the child on expiry. Reduces DoS surface.
   - --end-of-options before rev in resolve_commit (~line 678): valid git-wide option (git >= 2.24) guaranteeing rev is parsed as a revision, never an option, even for a leading-`-` operator --sha. Fail-closed preserved on old git; SHA40_RE output check still gates the result.
   - Additional tightening — the new commit==HEAD guard in frozen_git_identity(require_clean=True) (~line 775): a "clean live" stamp is only meaningful at HEAD; a non-HEAD reviewed_sha with require_clean now fails CLOSED. Closes a potential false-clean (N2). reviewed_sha=None->HEAD and submit/gate passing current HEAD still satisfy it.
   - Additional robustness — relevant_working_tree_dirty skips the rename/copy origin field when R/C appears in EITHER status column. Fail-closed semantics preserved.

2. import os + os.environ copy safe — CONFIRMED. env={**os.environ, "GIT_LITERAL_PATHSPECS": "1"} shallow-copies the parent env for the child git process — the SAME env git already inherited implicitly (subprocess.run without env= inherits os.environ), so no new secret exposure and PATH preserved. The env dict is passed to the child only; never printed/logged/serialized. No secret leakage.

3. Still stdlib-only, no new dependency/network/lockfile — CONFIRMED. os is stdlib and already in the AST-enforced StdlibOnlyTests allowlist (test passed). No dependency manifest / lockfile in the delta; no network call.

4. No new injection/DoS surface — CONFIRMED. All three git-surface changes REDUCE risk (timeout bounds DoS; GIT_LITERAL_PATHSPECS removes pathspec-magic injection; --end-of-options removes argument-confusion). The two project_control.py changes are control-flow only: accept per-directive intersection (requirement ids namespaced per directive; no requirement dropped; missing directive still fails closed — not a bypass); the new progress() regime-entry guard is an additive fail-closed restriction (canceled exempt).

## Findings

No Critical, High, or Medium. Round-1 items LOW-2 (--end-of-options) and the two INFO items (GIT_LITERAL_PATHSPECS, subprocess timeout) are RESOLVED. Two non-blocking items carry forward (both out of scope of this delta):
- LOW-1 (carried forward): requirements_file/verification_file are read without the _within() containment guard applied to sources[].file and the index->manifest path (mirrored in the validator). Impact limited (registry content trusted, integrity-checked, reviewed; non-JSON target fails closed; nothing exfiltrated). Remediation: add _within() checks or a bare-filename schema pattern.
- INFO (carried forward): _run_git resolves git via PATH; on Windows CreateProcess also searches cwd. Negligible under the local-tool trust model. shutil.which("git") would remove the theoretical vector.

Round-2 verdict: PASS, bound to head ed8a6776. No blocking defects. Stdlib-only, no network/dependency/lockfile impact; no new injection or DoS surface.
