# M5-T099 — G3 review (code-reviewer "cr-t099", read-only)

> Transmission history: pinned at a612652b, delivered as three SendMessage parts (1/3..3/3) ending with END-OF-REPORT;
> no truncation. Joined verbatim by the orchestrator (transport wrapper tags removed only). Verdict FAIL on ONE blocking
> finding (B1: the section-6 rider map leaves seven open sub-items unmapped - DB-053 (i)(j)(k), DB-055 (f)(j), DB-061
> (h)(i), the last a safety rider). AS-2..AS-5 PASS. The task moves to rework.

---

M5-T099 G3 review (code-reviewer, read-only) — part 1/3. PIN: HEAD a612652b at start (matches expected). Material commit cd04fddb; plan pinned to claim seam 2957e40d.

SCOPE (AS-5 scope leg): PASS. cd04fddb changes exactly the two allowed paths (docs/design/d087-export-and-3d-viewer-plan.md, project-control/reports/M5-T099-producer-report.md); no forbidden path touched (git show --stat cd04fddb).

AS-1 INVENTORY TRUTH: PASS. Spot-checked 15+ quoted APIs/limits/consumers against `git show 2957e40d:<path>` — all accurate:
- dxf_writer.py :79/:117/:118/:119/:124/:144/:147/:152/:159/:184/:196/:229/:573/:604/:665/:775, SLOC 787 — all exact.
- glb_writer.py US_SURVEY_FOOT_TO_METRE = 1200/3937 (:131, NOT 0.3048), AXIS_MAPPING :135, caps :164-:176, write_glb :578 RAISES GlbValidationError (verified). The "GLB raises vs PDF returns a value" contrast is real — load-bearing for the DB-059(e) reconciliation.
- pdf_sheet_writer.py :87/:90/:146/:160/:221/:224/:252/:424/:442/:447, SLOC 506; the single production import is byte-exact: :54 `from app.cad.dxf_writer import CLAIM_CLASS_WORDS`.
- dxf_reader.py read_dxf :622 never-raises verified (try/except _Refuse/except Exception -> DxfRefusal VALUE); DxfLimits :166-:170.
- massing_model.py build_from_generated_option :770 refuses (no fabrication) at :791-:794; content_hash :487.
- connector fetch_context_buildings :1017; field names exact :117-:121; SLOC 1075; HEIGHT_ROOF-feet-is-an-inference confirmed :150.
Line offsets are at most ±1 (decorator vs class-def line); immaterial.

STALENESS AT HEAD (in-flight T094/T096/T097/T098/T100 landed — submitted, not accepted): §1 anchors are now stale for dxf_writer (787->932), dxf_reader (641->738), sheet_reader (1096->359, the split), massing_model (825->894), connector (1075->1107). glb_writer (597) and pdf_sheet_writer (506) are byte-stable, so their §1.2/§1.3 anchors still hold at HEAD.
The plan treats this HONESTLY: §0 pins all anchors to 2957e40d; §0.1 tables every in-flight packet + the files it locks and states the pre-landing condition ("STYLE/VPORT not yet in dxf_writer.py; sheet_reader still 1096 SLOC monolithic ... these are the in-flight packets, not accepted state"). Correct for a design note that must not treat unaccepted material as accepted — NOT a defect. (Note only: the §0.1 present-tense phrasing is literally out-of-date at HEAD since the material is physically present-but-unaccepted; the pinning + labeling keep it honest.)

Part 2/3 next.

---

M5-T099 G3 review — part 2/3 (the one BLOCKING finding).

BLOCKING B1 — AS-1 RIDER MAP INCOMPLETE. AS-1 requires every open DB-053..DB-062 sub-item mapped to exactly one future step, an in-flight packet, a discharge, or "by note". Cross-checking plan §6 (d087-export-and-3d-viewer-plan.md:376-412) against docs/DISCOVERY_BACKLOG.md:182-191, SEVEN open sub-items are unmapped:

- DB-053 (i),(j),(k) [backlog:182]: (i) the 3D dep-admission must argue necessity + maintainer-change assessment; (j) ezdxf-as-a-new-package G5 (contingent — "if ever proposed"); (k) R2000+ OBJECTS re-verify (moot under the plan's R12/AC1009 target). §6 maps only DB-053 (a)-(h).
- DB-055 (f),(j) [backlog:184]: (f) LOW security residual — some refusal details still embed an untruncated XObject /name (lexer-bounded to 64 KB); (j) an evidence-map over-generalization doc nit. §6 maps a,b,c,d,e,g,h,i,k,l,m,n.
- DB-061 (h),(i) [backlog:190]: (h) derivation.py:314-363 carries a DUPLICATE all-pairs simplicity check (backlog routes it to a derivation packet); (i) SAFETY — "a cancellable job with a per-request wall-clock timeout + rate limit ... still REQUIRED at the wiring/public-exposure packet even with the in-process bound." §6 maps only DB-061 (a)-(g).

WHY BLOCKING: DB-061 (i) is a safety rider. The plan carries a per-request wall-clock deadline (§2) and a bounded request body (§7.1), but the RATE LIMIT and the CANCELLABLE off-event-loop JOB are carried into NO export/scene/mount packet (PKT-D/E/H) and appear nowhere in §6. The task's stated purpose is a complete map "so ... builders wire ... without ... skipping a safety rider" — an unmapped safety rider defeats that, which is exactly what G3 should catch before packets are contracted off this map.

FIX (small): add the 7 rows to §6; attach DB-061 (i)'s rate-limit + cancellable-job requirement explicitly to PKT-D/PKT-E (and/or PKT-H mount). DB-053 (i) substance is arguably met by PKT-G's "full dependency-security policy + G5 provenance review" but must be CITED; DB-053 (j)/(k) and DB-055 (f)/(j) can be "by note (contingent/moot/LOW)".

NO problematic double-maps: DB-058 (a) spanning PKT-C + PKT-E/I render-escape is ONE rider with legitimate multiple enforcement points (connector keeps text verbatim for provenance; consumers escape on render), not a conflict. Every other §6 row maps its items once.

Part 3/3 next (AS-2/3/4/5 + verdict).

---

M5-T099 G3 review — part 3/3 (remaining scenarios, advisories, verdict).

AS-2 (export/scene contracts): PASS. Flag reuse INTERNAL_RULE_EVAL_ENABLED + generic-404 sentinel verified (lot_geometry.py:100); the write/import dedicated default-off flag pattern verified (main.py:203-218 — literally "needs BOTH this registration flag AND that handler flag"). Bounds, Content-Disposition + media types (glb_writer.GLB_MEDIA_TYPE = model/gltf-binary), server-generated X-Correlation-ID, typed refusals reconciling GLB-raises vs PDF-returns-value (DB-059(e)), no-caller-text-in-logs, escaping, provenance quintuple — all present and source-consistent.

AS-3 (viewer): PASS. Data path, GLB-as-single-canonical-converter (2263 ft -> m via 1200/3937 + AXIS_MAPPING), perf caps (<=1000 meshes / 500k verts), non-visual text alternative, print = text + accepted PDF site plan, CI-only web proof. Stack pins verified EXACT at 2957e40d: apps/web/package.json three "0.186.0" (:23) + @react-three/fiber "9.7.0" (:18), no range chars. §3.1 lays out BOTH @types/three options with trade-offs (incl. the @dimforge/rapier3d-compat WASM physics dep) and chooses NEITHER (owner's call) — matches DB-062(a).

AS-4 (packet breakdown): PASS. Dependency-correct ordering; each parallel batch pairwise file-disjoint (Batch1 {A,C,K} disjoint, B1 serializes after A on pdf_sheet_writer.py; Batch2 {D,E,F} all new modules + own tests; Batch3 H/J serialize on app/main.py); route modules ship UNMOUNTED; gates listed per packet; each packet depends on the in-flight packet locking its file. ADVISORY: PKT-K lists a dependency on M5-T097 (dxf_reader) with no file overlap against PKT-K's sheet_reader/pdf_object_streams scope — likely over-specified; harmless.

AS-5 (honesty + scope): PASS. Sheet reader reads 0/6 real drawings and nothing is wired — stated (§0); max-envelope route confirmed UNMOUNTED at HEAD (git show HEAD:app/main.py has NO include) with preconditions listed-not-relaxed (§7 == route docstring :10-36); D-083 words correct ("Preliminary development limits"/"Generated building option"; no "approved"/"maximum allowed building"/"demonstrated maximum"); native DWG stays a Tier D owner licensing decision (§4/§8.2). Scope leg passed (part 1).

ADVISORY (non-blocking): (1) a few ±1 line anchors on decorator-vs-def lines; (2) DB-059(a) reads as PKT-B1 in §6 but §2 prose also invokes it in the export-service defense — pick one seam to avoid ambiguity.

NET: inventory accuracy, contracts, viewer design, packet ordering, honesty and scope are all strong and source-true. The single blocking gap is AS-1 rider-map completeness (7 unmapped open sub-items, one safety-relevant: DB-061 (i)). Fix is ~7 rows — narrow rework, not a redesign.

M5-T099 G3 VERDICT: FAIL

END-OF-REPORT
