<!-- Saved VERBATIM from the security-reviewer agent return (transport entity-decoding only),
     orchestrator seq 122, 2026-09-20. Ruling identity: frozen submission 2b5edbc3.
     One annotation added at commit time: an inline gitleaks:allow HTML comment on the BP-7
     "Live parity" line (generic-api-key false positive on reviewer prose; no secret exists). -->

# G5 SECURITY GATE REPORT — M5-T057 (phase B3 slice 1: proposal-check route)

**Reviewer:** security-reviewer (independent, read-only)
**Task:** M5-T057 | **Milestone:** M5 | **Gate:** G5
**Frozen submission sha:** `2b5edbc3b0a6231adbeec0cd1dc0a765b6cc160e` | **Material commit:** `3dbd056d` | **Live HEAD:** `18616ff5` | **Worktree used for execution:** `C:\Users\MLFLL\Downloads\nyc-zoning\wt-m5t057` @ `fcc58e24`
**Judged as:** INTERNET-FACING-IN-WAITING (flag-gated internal today; the packet states this route is the future public trust boundary)

## VERDICT: FAIL

Two of the seven recorded preconditions are **not closed at this seam** and both are reproducible in ≤1 request against the frozen code. The packet's own risk statement sets the bar: *"G5 will verify each BP CLOSED at this seam — a missed cap or an uncapped error path fails the wave."* An uncapped error path exists (F1) and a compute path escapes the BP-4 product bound (F2). Everything else is genuinely well built — BP-1, BP-2, BP-5, BP-6, BP-7 are closed, several of them unusually thoroughly.

---

## 1. Frozen identity — CONFIRMED

```
git diff 2b5edbc3 HEAD -- services/api/app/api/v1/proposal_checks_api.py \
  services/api/app/api/v1/proposal_validation.py services/api/app/main.py \
  services/api/tests/api/test_proposal_checks_api.py \
  services/api/tests/api/test_proposal_validation_api.py \
  services/api/tests/rules/test_proposal_checks.py \
  project-control/reports/M5-T057-producer-report.md
→ EMPTY (exit 0)
```
Blob identity at `2b5edbc3` == HEAD == worktree `fcc58e24` for all six:
`proposal_checks_api.py` `e7712e20`, `main.py` `bf2c658d`, `test_proposal_checks_api.py` `eb2d9e2d`, `test_proposal_validation_api.py` `eddd9655`, `test_proposal_checks.py` `83135b71`.
`git diff 2b5edbc3 HEAD` overall touches only `project-control/state.json` and `tasks/M5-T057.json` (ledger transition). Not BLOCKED.

**Review surface reviewed:** `C:\Users\MLFLL\Downloads\nyc-zoning\ctl24\services\api\app\api\v1\proposal_checks_api.py` (593 lines), `…\services\api\tests\api\test_proposal_checks_api.py`, the one `…\services\api\app\main.py` mount (+1 import, +13 comment/mount lines; no other line changed — verified by full diff). Read read-only as dependencies: `app/api/v1/proposal_validation.py`, `app/scenario/proposal_input_gate.py`, `app/scenario/proposal.py`, `app/scenario/derivation.py`, `app/rules/proposal_checks.py`, `app/rules/registry.py`, `app/rules/models.py`, `app/config.py`.

---

## 2. Per-precondition rulings (BP-1..BP-7, verbatim set from `project-control/reports/M5-T054-G5.md`)

### BP-1 — single engine entry (`check_proposal` only; no `derive_proposal` reachable): **CLOSED**
Import block (`:68-72`) pulls `CALLER_RULE_INPUT_NAMES`, `ProposalCheckError`, `check_proposal` only. `derive_proposal` is neither imported nor referenced (grep over the module: the only hits are the two docstring/comment mentions at `:34` and `:110`). `check_proposal` is invoked exactly once (`:563-570`), inside the try that caps its errors. The registry is resolved once (`_effective_registry`, `:188-200`) and passed in, so the BP-5 domain vocabulary and the engine evaluate against the identical registry — a genuine improvement over passing `None`. Bound by `test_bp1_only_check_proposal_entry_is_called` (`:481`), which also asserts `not hasattr(mod, "derive_proposal")`. All B2 wiring preconditions (list ceilings, coordinate/area finiteness) therefore apply.

### BP-2 — `scenario_label` / `proposal_id` caps + conservative charset, typed refusal: **CLOSED**
`_require_label` (`:258-274`): non-empty-after-strip, `len ≤ MAX_LABEL_LEN (200)`, then `_LABEL_CHARSET.fullmatch` over `[A-Za-z0-9 ._:\-]+`. Refusals echo only the length, never the value. `proposal_id` is optional and gets identical treatment when present.

I probed for bypasses (18 vectors, live against the frozen route, flag on). Every one refuses `422 field=scenario_label`; only the exact 200-char boundary value passes:

| vector | result | vector | result |
|---|---|---|---|
| `scen\u0130ario` (dotted capital I) | 422 | trailing `\n` | 422 |
| `scenario\u0000` (NUL) | 422 | `\r` | 422 |
| `scenario\u200b` (zero-width) | 422 | `\t` | 422 |
| `ＡＢＣ` (fullwidth, NFKC→ASCII) | 422 | `\u2028` (line sep) | 422 |
| `\u0660\u0661` (Arabic-Indic digits) | 422 | `\ufb01le` (NFKC ligature) | 422 |
| `e\u0301` (combining) | 422 | `scenario/../x` | 422 |
| `sc'en"ario` | 422 | `<b>x</b>` | 422 |
| `" "` (whitespace only) | 422 | `"s"*201` | 422 |
| `"s"*200` | **200** (exact boundary) | `proposal_id="\u0130d"` | 422 field=`proposal_id` |

The `fullmatch`-not-`$` choice is correct and deliberately documented (`:104-106`); an anchored-`$` regex would have accepted `"foo\n"`. No normalization, control-char, empty-vs-whitespace, or length bypass found.

### BP-3 — error-detail capping on EVERY error path: **OPEN** → finding **F1 (MEDIUM)**
The message half is correct everywhere: all 422s route through `_validation_error` → `_bounded_message` (400 + marker). The specific class BP-3 names — the B0 validator's uncapped bad-vertex `repr` (G5-3) — is genuinely closed (verified below). **But `_validation_error` serializes `exc.field` raw**:

```python
# services/api/app/api/v1/proposal_checks_api.py:222-229
body: dict[str, object] = {
    "state": "validation_error",
    "message": _bounded_message(message),   # capped
    "correlation_id": correlation_id,
}
if field is not None:
    body["field"] = field                    # NOT capped
```

and the route builds `LotContext` without validating the caller ids at all:

```python
# :379-383
LotLineSegment(id=cast(str, seg.get("id")), start=_to_point(seg.get("start")), …)
```

`cast` is a typing no-op. The B2 engine then embeds that id verbatim in the *field path*:

```python
# app/rules/proposal_checks.py:458
_require_finite_point(seg.start, f"lot.lot_line_segments[{seg.id!r}].start")
```

The DB-034(b) string ceiling (`MAX_STRING_LEN = 512`) covers only `proposed_massing` strings — the `lot` object is entirely outside the input gate. So an uncapped, uncharted, attacker-controlled value reaches the client.

**Reproduction (executed at the frozen code, worktree `fcc58e24`, `INTERNAL_RULE_EVAL_ENABLED=1`):**
```python
EVIL = "<script>alert(1)</script>" + "A"*100000
POST /api/v1/proposal-checks
{"proposed_massing": <valid rectangle block>,
 "lot": {"area_sq_ft": 8000.0, "area_provenance": {},
         "lot_line_segments": [{"id": EVIL, "start": [null, 0], "end": [1, 1]}],
         "street_lines": []},
 "lot_rule_facts": {"zoning_district": "R5"}, "scenario_label": "scenario-A"}
```
Observed:
```
HTTP 422 | response bytes=100594
    state:   'validation_error'
    field:   len=100056  head="lot.lot_line_segments['<script>alert(1)</script>AAAA…"   <-- UNCAPPED
    message: len=434     (correctly capped)
    '<script>' present in response: True
```
Same class via `lot.street_lines[].wall_id` (`proposal_checks.py:461`): `HTTP 422 | field len=100051`.

**The same uncapped ids also reach the 200 SUCCESS path.** `_lot_line_setback` (`derivation.py:608`) puts `f"lot_line:{nearest_id}"` into `EvidenceRecord.input_ids`, which `CheckResult.as_dict()` publishes as `provenance.provided_input_ids`; `lot.area_provenance` is republished verbatim as `provided_provenance` (`derivation.py:458`); unmapped `lot_rule_facts` keys are republished raw. Reproduced:
```
HTTP 200 | response bytes=156408 (request 150748)
    longest provided_input_id len: 100034
    unmapped_lot_facts: ['<img src=x onerror=alert(1)>']
    provided_provenance keys: ['evil'] max value len: 50000
    '<script>' in body: True | '<img' in body: True
```

**Why this matters despite the ~1:1 size ratio.** (a) It falsifies the module's own stated invariant, `:36-39` *"BP-3 — EVERY error path length-caps any embedded value"*, and BP-3's verbatim text *"Length-cap ALL exception detail … before serializing into any client response"*. (b) It defeats BP-2's purpose through a different key: BP-2 restricts the label charset *because* B3 renders these strings, yet `field` — the key a UI is most likely to render beside a validation error — accepts `<script>`, quotes, and any code point, unbounded. (c) It is logged unredacted (F4). Mitigating: the API sets `X-Content-Type-Options: nosniff` and `Content-Security-Policy: default-src 'none'` (`main.py:54-58`), so the JSON response itself cannot execute in a browser; the exposure is to the B3 consumer and to logs.

**Confirmed CLOSED sub-case (the class BP-3 literally names):** a 60,000-char string inside an outline vertex →
```
HTTP 422 resp_bytes=573 msg_len=433 field='proposed_massing.outline.vertices[1]'
```
The B0 `{vertex!r}` is correctly truncated; all validator/gate `field=` values are literal-path + integer-index. The gap is exclusively the lot-side ids, which this route introduced.

**Remediation (both halves):**
1. `_validation_error` (`:227-228`): `body["field"] = _bounded_message(field)` — or a dedicated short field cap (≈120 chars), since a field path should never be long.
2. Durable root-cause fix, because these ids also ride the 200 path: apply a length + conservative-charset ceiling to `lot.lot_line_segments[].id` and `lot.street_lines[].wall_id` inside `_build_lot_context` (`:370-406`) — the route owns the `lot` object, and no gate does — and bound the size/depth of `lot.area_provenance` and `street_lines[].attestation`. Add a binding test asserting `len(body["field"]) < cap` and that an over-length lot-line id cannot appear in a 200 body.

### BP-4 — compute budget tighter than library ceilings, no path escaping the product bound: **OPEN** → finding **F2 (MEDIUM)**
The *product* half is implemented well: `ROUTE_MAX_EXTERIOR_WALLS=500`, `ROUTE_MAX_LOT_LINE_SEGMENTS=800`, `ROUTE_MAX_STREET_LINES=400` (`:122-124`), each strictly below its library counterpart (4000/1000/500), enforced by O(1) `len` before any heavy work (`_enforce_route_caps`, `:415-440`), typed refusal naming the exact cap, arithmetic shown (`:109-121`: `500 × (800+400) = 600,000 < ~1e6`), tested at `:313/328/340/354/366`. I re-derived it against `derivation.py`: the wall loop (`_derive_wall_setbacks:665`) × `_lot_line_setback:597` is exactly `walls × lot_line_segments` distance tests, and the street term is an O(1)-per-comparison `wall_id` scan plus one distance test per wall. The product is not escapable via many small polygons or attested street lines. That sub-requirement is CLOSED.

**But the requirement is "keeping worst case under ~1e6 segment tests" and "verify no path escapes the product bound" — and one does.** The O(n²) ring-simplicity test (`proposal.py:_ring_is_simple:174-183`) is bounded only by the *inherited* DB-034(a) budget (`MAX_TOTAL_VERTICES = 5000` → ≤5e6 segment tests per pass), and this route runs that validation **twice**: once explicitly (`:529 validate_proposed_massing_input`) and again inside `check_proposal` → `derive_proposal` → `validate_proposed_massing` (`derivation.py:704`). Worst case ≈ **1e7 segment tests**, ~17× the capped product. The route comment acknowledges the separate budget exists but neither quantifies it nor notes the double pass, and `test_bp4_documented_worst_case_product_under_1e6` asserts only the 600,000 product.

**Measured, at the frozen code, all returning HTTP 200 (single request, inside `MAX_BODY_BYTES`):**

| payload (all ≤ 256 KiB) | request bytes | single-request CPU |
|---|---|---|
| at-cap product: 500 walls × (800 lot-line + 400 street), 501-vertex outline | 158,278 | **4.57 s** |
| DB-034(a)-budget outlines: 5 × ~1000 positions, 1 wall | 205,807 | **14.24 s** |
| **combined at-cap**: 5×999-vertex outlines + 500 walls + 800 lot lines + 400 street lines (compact integer coords) | **196,035** | **17.31 s** |

The combined worst case is reachable within the body ceiling. Compounding it: `post_proposal_checks` is `async def` (`:444`) and `check_proposal` / `validate_proposed_massing_input` are synchronous CPU-bound calls, so this runs **on the event loop** — one such request stalls every other endpoint in the worker process for ~17 s.

**Remediation (either satisfies BP-4's own recorded remediation text):**
- Move the compute off the loop — declare the handler `def` (FastAPI runs it in the threadpool) or `await run_in_threadpool(check_proposal, …)`; and/or
- add a route-level total-outline-position cap tighter than the inherited 5000 (e.g. 1200 → ≤2×1.44e6), enforced with the same O(1) pre-count pattern as `_enforce_route_caps`, with a typed refusal naming the cap; and
- correct the `:109-121` arithmetic comment and the BP-4 test to state the route's *actual* worst case, including the double validation pass.

### BP-5 — `lot_rule_facts` value/domain validation before feeding; unmapped surfaced never fed: **CLOSED** (with LOW residual F3)
`_assert_fact_type_table_covers_vocabulary` (`:150-161`) is an import-time partition guard against a future mapped input silently skipping validation — good design. `_derive_enum_domains` (`:296-315`) reads `InputSpec.enum` from the live registry and narrows a name only when *every* declaring rule constrains it — never an invented list, and conservative in the right direction. I verified against the 18 production rulesets: effective domains are `site_class` → {qualifying_residential_site, standard_zoning_lot} and `street_width_class` → {narrow, wide}; `zoning_district`/`overlay_present`/`special_district_present`/`historic_district`/`large_site` are declared enum-less by at least one rule and correctly left un-narrowed. So the domain half is non-vacuous in production.

Type-confusion probes (live):

| fact | result | fact | result |
|---|---|---|---|
| `{"large_site": [1,2]}` | 422 `lot_rule_facts.large_site` | `{"large_site": 1}` (int-as-bool) | 422 |
| `{"zoning_district": {"a":1}}` (nested dict) | 422 | `{"lot_depth_ft": "12"}` | 422 |
| `{"overlay_present": null}` | 422 | `{"street_width_class": "WIDE"}` | 422 (domain) |
| `{"street_width_class": "wide"}` | 200 | `{"site_class": "standard_zoning_lot"}` | 200 |
| `{"lot_depth_ft": 10**400}` | 200 (fed) | `{"lot_depth_ft": -5}` | 200 (fed) |
| NaN / Infinity | 422 at the strict-JSON guard (`:493-500`) | `{"zoning_district": "R5"*300}` | 200 (fed) |

Unmapped keys verified surfaced-and-never-fed (`unmapped_lot_facts: ['<img src=x onerror=alert(1)>']`, that key absent from `rule_input_bindings`). I rule BP-5 CLOSED: types, enum domains, and the no-silent-merge property are all enforced before the engine runs, with typed field-named refusals.

**F3 (LOW, residual):** `InputSpec` also carries `minimum` / `maximum` / `exclusive_minimum` / `exclusive_maximum` (`app/rules/models.py:29-32`) and the route ignores them, so huge (`10**400`) and negative (`-5`) numerics pass the boundary and are fed to the evaluator; free-form strings are fed with no length ceiling. The evaluator is accepted and fail-closed and no leak or 500 resulted, so this is advisory, not a BP-5 failure. Remediation: extend `_derive_enum_domains` into a `_derive_input_domains` that also applies the declared numeric bounds, same registry-derived discipline.

**F5 (LOW, ordering):** the cheap registry-derived domain check (`:552-557`) runs *after* the expensive O(n²) input gate (`:529`), so an out-of-domain fact still pays full validation cost. The module docstring claims enforcement "in order, fail-closed"; moving the domain check up beside `_validate_lot_rule_fact_types` costs nothing.

### BP-6 — no emission (no scenario document / contract_version; DB-034(d)): **CLOSED**
Response is `report.as_dict()` + `correlation_id` only (`:586-593`). Grep over the module for `contract_version|builder|build_scenario|emit|1.1.0|scenario_id`: only docstring/comment prose. Live 200 body top-level keys: `['correlation_id','fact_mapping','outline_digest','proposal_id','results','rule_input_bindings','scenario_label','source_class','summary','unmapped_lot_facts']`; tokens `contract_version`, `scenario_id`, `constraint_completeness`, `1.1.0`, `"kind"` all absent from the serialized body. Bound by `test_bp6_response_emits_no_scenario_document_or_contract_version` (`:488`). The DB-034(d) input-gate-bypass hazard is not created here. Disposition recorded in the producer report §5.

### BP-7 — bounded body + posture; authn/tenancy as a recorded disposition: **CLOSED (posture)** / **N-A-WITH-DISPOSITION (authn/tenancy)**
- Flag check is the **first** statement (`:449`), before a correlation id is minted or the body is touched; `internal_rule_eval_enabled` is true-token-only, absent/empty/unknown → disabled (`config.py:38,52-57`). `include_in_schema=False` (`:443`). <!-- gitleaks:allow (generic-api-key FP on "token" prose; no secret) -->
- Live parity with T053's route, flag off: both return `404 {"detail":"Not Found"}` with **no** `X-Correlation-ID`; no `proposal*` path in `/openapi.json`. Bound by `test_residual_d_both_routes_share_one_flag_and_off_behaviour`, which also documents the deliberate shared-flag coupling in both directions (off → identical 404; on → both reachable). No new flag introduced. <!-- gitleaks:allow (generic-api-key FP on prose; no secret) -->
- Bounded body: reuses the accepted T053 primitives — Content-Length fast path plus chunk-by-chunk accumulation that stops at the first over-ceiling chunk (`proposal_validation.py:203-222`), so chunked/absent-length bodies are caught and nothing is buffered past 256 KiB.
- **Disposition verified recorded** at `project-control/reports/M5-T057-producer-report.md` §5 (lines 163-168): authentication, tenancy, and per-user ownership of the supplied lot geometry arrive with the public-exposure packet; not implemented here; route unreachable without the internal flag. That is the expected recorded disposition, and it is accurate — the route has no identity concept and no data store, so there is no cross-tenant surface to leak today.

---

## 3. Standard G5 checklist

- **Cross-tenant isolation — CLOSED.** No DB, session, cache, or tenant state; every input arrives as a request parameter and every output is derived from that same request. The only process-global is `_PRODUCTION_REGISTRY` (`:185-200`), which holds public legal-rule definitions only. No cross-request data path exists.
- **Service-role secrecy — CLOSED.** No env access beyond the boolean feature flag; no credential, key, token, connection string, or service-role reference anywhere in the module (grep clean for `getenv|environ|secret|token|password`).
- **Private storage / uploads — N/A.** No storage client, no file upload, no signed URLs, no filesystem writes. Request-body size is the only ingest control and it is enforced pre-parse (`test_oversized_body_is_413_before_parse`).
- **SSRF / outbound — CLOSED.** No `requests`/`httpx`/`urllib`/socket/`open()`/`Path()`; no caller value is ever used to build a URL or path. The registry loads bundled JSON from `app/rules/rulesets/` only, never request-bound (`get_proposal_check_registry()` takes no arguments and has no request binding).
- **Injection (SQL / shell / eval / exec / dynamic import / pickle) — CLOSED.** None present. Every payload string is treated strictly as data: the only control-flow uses of caller strings are membership tests against fixed vocabularies (`CALLER_RULE_INPUT_NAMES`, registry-declared enums) and the anchored charset regex. `_LABEL_CHARSET` is a fixed literal pattern (no caller-built regex → no ReDoS). Responses are `JSONResponse` under `nosniff` + `default-src 'none'`.
- **Prompt-injection — N/A/CLOSED.** No LLM call, no prompt construction, no ingestion of untrusted text into a model. Fully deterministic.
- **Least privilege — CLOSED for the internal posture** (off-by-default flag, hidden from schema, generic 404, no privileges held) **+ disposition** for authn/tenancy per BP-7.
- **Log redaction — OPEN-LOW → finding F4.** No secret or PII is logged; log lines are correlation-id-anchored and no `str(exc)`/traceback is ever logged (`:550,580,591`). But `logger.info(… field=%s …)` at `:533,539,555,572,576` writes the **uncapped** `exc.field`. Measured: a single refusal carrying a 50,000-char lot-line id emitted a **50,126-character** log record; at the body ceiling that is ~250 KB per request. `repr` neutralizes newlines so log-line injection is not possible, but this is unbounded untrusted payload content in application logs. Fixed by the same cap as F1.
- **No new dependency — CLOSED.** The material commit `3dbd056d` touches exactly 6 files; no requirements/lockfile change. All imports are stdlib or existing in-repo modules.
- **No new flag — CLOSED.** Reuses `INTERNAL_RULE_EVAL_ENABLED`; the coupling is deliberate and documented by `test_residual_d`.
- **Modularity — PASS.** `proposal_checks_api.py` is 593 lines, below the 600 warn tier and absent from the warn list. `python tools/modularity_check.py --check` → `selected 456 files; failures 0; warnings 20`, exit 0; all 20 warnings are pre-existing out-of-scope files. Single responsibility (boundary enforcement + serialization), all legal calculation delegated, T053 primitives reused rather than forked.
- **F6 (INFO, carry-forward of T054 G5-5).** `get_proposal_check_registry()` is a module-global test-injection seam in production code, and `_PRODUCTION_REGISTRY` is a lazily-populated process global. Neither is request-settable; the cache holds public rule data only and a first-call race is benign/idempotent. No action required. Note `_derive_enum_domains` re-walks all 18 rulesets on every request (negligible, but trivially cacheable alongside the registry).

---

## 4. Reproduction commands (all read-only; executed in the free worktree)

```
cd C:/Users/MLFLL/Downloads/nyc-zoning/wt-m5t057/services/api
python -m pytest tests/api/test_proposal_checks_api.py tests/api/test_proposal_validation_api.py -q
    → 82 passed in 2.52s
python -m ruff check .                      → All checks passed!
cd C:/Users/MLFLL/Downloads/nyc-zoning/wt-m5t057
python tools/modularity_check.py --check    → selected 456 files; failures 0; warnings 20 (exit 0)
```
The F1/F2/F3 probes were run via `python -` (stdin, no files written) from `wt-m5t057/services/api` with `INTERNAL_RULE_EVAL_ENABLED=1`, driving `fastapi.testclient.TestClient(app, raise_server_exceptions=False)` against `/api/v1/proposal-checks`; payloads and verbatim outputs are quoted inline above. Sandbox: Python 3.11.9, fastapi 0.128.0. (CI run 35498218087 is green at `f86ae735`; none of my findings depend on rerunning the suite — they are reproduced directly against the frozen code, and the suite passing is consistent with them because no existing test asserts a bound on the `field` key or on the route's true compute worst case.)

---

## 5. Findings summary

| ID | Severity | Precondition | Summary | File:line |
|---|---|---|---|---|
| F1 | MEDIUM | BP-3 OPEN | `_validation_error` serializes `exc.field` uncapped; `lot.*` ids are never length/charset-bounded, so 100 KB of attacker content (incl. `<script>`) reaches a 422 `field` and a 200 `provided_input_ids` / `provided_provenance` | `proposal_checks_api.py:227-228`, `:379-383`, `:399-405`; root path `app/rules/proposal_checks.py:458,461`, `app/scenario/derivation.py:608,458` |
| F2 | MEDIUM | BP-4 OPEN | Outline-simplicity path escapes the 600k product bound: ~1e7 segment tests (double validation under the inherited 5000-vertex budget); **17.31 s** single-request CPU inside the body ceiling, run on the event loop by an `async def` handler | `proposal_checks_api.py:109-121`, `:444`, `:529`, `:563`; `app/scenario/proposal.py:174-183`; `app/scenario/derivation.py:704` |
| F3 | LOW | BP-5 residual | Declared numeric `InputSpec` bounds (`minimum`/`maximum`/`exclusive_*`) ignored; huge/negative numerics and unbounded free strings are fed | `proposal_checks_api.py:296-315`; `app/rules/models.py:29-32` |
| F4 | LOW | log redaction | Uncapped `exc.field` written to application logs (50,126 chars measured for one refusal) | `proposal_checks_api.py:533,539,555,572,576` |
| F5 | LOW | ordering | Cheap registry-derived domain check runs after the expensive O(n²) input gate | `proposal_checks_api.py:529` vs `:552-557` |
| F6 | INFO | — | Module-global registry test seam + process-global registry cache (public rule data only; benign) | `proposal_checks_api.py:174-200` |

**Blocking for this gate:** F1 and F2. Both are contained fixes inside the packet's own allowed path (`proposal_checks_api.py`) plus one test each; neither requires touching a forbidden or accepted module. F3–F6 are advisory and may ride to the B3-slice-2 packet.

**What is genuinely strong and should not be relitigated on rework:** BP-1 (single entry + single registry resolution), BP-2 (survived 18 bypass vectors including NFKC, zero-width, Arabic-Indic digits, and the `$`-vs-`fullmatch` newline trap the producer called out explicitly), BP-5's import-time vocabulary partition guard and registry-derived domains, BP-6's negative proof, BP-7's byte-identical fail-safe 404 parity, and the four T053 residual tests — `test_residual_b` in particular asserts the 500 path leaks no `boom`/`RuntimeError`/`traceback`/`/services/api` and I confirmed the generic-500 body is `{state, message, correlation_id}` only.

---

**VERDICT: FAIL**


---

<!-- DELTA RE-RULE saved VERBATIM from the resumed security-reviewer agent (transport entity-decoding only), orchestrator seq 122, 2026-09-20. Ruling identity: frozen resubmission a0cb3b79. -->

---

# G5 DELTA GATE REPORT — M5-T057 (rework re-rule of BP-3 and BP-4)

**Reviewer:** security-reviewer (independent, read-only)
**Frozen resubmission sha:** `a0cb3b795ebc84792673ab5f43d3300f0ea90f33` | **Rework material:** `ca5c8895` | **Verified at worktree** `wt-m5t057` @ `c09f87d6` | **Live HEAD:** `fd62b08d`
**Scope:** delta only — re-rule BP-3 and BP-4. All other rulings from the base G5 report stand (re-verified for regression, see §4).

## VERDICT: PASS

---

## 1. Identity — CONFIRMED

`git diff a0cb3b79 c09f87d6` over the six packet files → **empty**; `git diff a0cb3b79 HEAD` touches only `project-control/state.json` and `tasks/M5-T057.json`. LF-normalized digests and line counts match the coordinator's claims exactly:

| file | LF-sha256 (16) | lines |
|---|---|---|
| `services/api/app/api/v1/proposal_checks_api.py` | `ccfe7c993a73ce6d` | 721 |
| `services/api/tests/api/test_proposal_checks_api.py` | `0d1cd1acfe6ed566` | 767 |
| `services/api/tests/api/test_proposal_validation_api.py` | `cd3bf12ebc044107` | 714 |

`git diff --stat 2b5edbc3 a0cb3b79 -- services/api/app/main.py services/api/tests/rules/test_proposal_checks.py` → **empty** (both byte-unchanged from the original frozen identity, as claimed). Material `ca5c8895` touches exactly 4 files.

## 2. BP-3 — re-ruled **CLOSED**

All three of my original F1 reproductions, replayed byte-for-byte against the repaired route:

| original reproduction | before | now |
|---|---|---|
| A — `lot.lot_line_segments[].id` = `<script>…` + 100 000 `A` → 422 | `field` len **100 056**, resp **100 594 B**, `<script>` present | **422**, `field='lot.lot_line_segments[0].id'` (27 ch), resp **209 B**, `<script>` **absent** |
| B — `lot.street_lines[].wall_id`, same payload | `field` len **100 051** | **422**, `field='lot.street_lines[0].wall_id'`, resp **209 B**, `<script>` **absent** |
| C — 200-path: id + 50 KB `area_provenance` + markup fact key | **HTTP 200**, resp **156 408 B**, `provided_input_id` 100 034 ch, `<img…>` in `unmapped_lot_facts` | **422** at the boundary, resp **199 B**, `<script>` **absent** |

Each sub-vector independently closed: oversize `area_provenance` → 422 `lot.area_provenance`; oversize `attestation` → 422 `lot.street_lines[0].attestation`; markup fact key → 422 `lot_rule_facts`; over-length fact key → 422; short markup lot id → 422; missing lot id → 422. A conforming unmapped key still surfaces normally (`unmapped=['some_unmapped_key']`), so BP-5's unmapped-never-fed behavior is preserved.

**`_bounded_field` is load-bearing, not dead defense-in-depth.** A *conforming* 200-char lot id still produces an engine field path of 231 chars (`lot.lot_line_segments['<200 ch>'].start`), and I confirmed it is truncated: `field len=231, truncated=True, tail='…<truncated; 231 chars total>'`, message 386 ch. The block-side `exterior_walls[].id` path (≤512 ch via the accepted DB-034(b) gate) is likewise now capped. So both the root cause and the backstop are real.

**F4 (log redaction) closed by the same cap** — measured: a 50 000-char id refusal now emits a **109-char** log record (was **50 126**); the 231-char engine-field path emits 326 chars.

## 3. BP-4 — re-ruled **CLOSED**

All three prongs of my recorded remediation text were taken:

**(a) Off the event loop — verified empirically, not just by source.** Concurrent ASGI test (`httpx.ASGITransport` + `asyncio.gather`): `/api/v1/health` returned at **t=0.58 s** while an at-cap `/proposal-checks` request ran to **t=7.85 s**. The worker's other endpoints are no longer stalled — that was the sharp edge of F2.

**(b) Outline cap, counted before any validation pass.** `ROUTE_MAX_TOTAL_OUTLINE_POSITIONS=1200` via the gate's own `_total_position_count` in `_enforce_route_caps` (so the route and the DB-034(a) budget can never count differently), plus an import-time guard against cap rot. My escape probes:

| probe | counted | result |
|---|---|---|
| 1201 positions (1 over) | 1201 | 422 `proposed_massing`, message names `ROUTE_MAX_TOTAL_OUTLINE_POSITIONS` |
| 1200 positions (at cap) | 1200 | 422 — per-outline `MAX_OUTLINE_VERTICES (1000)` catches it |
| split base + level outline | 1299 | 422 (aggregate counted) |
| many small polygons (3 levels) | 1299 | 422 (aggregate counted) |

No split-across-outlines or many-small-polygons escape remains.

**(c) Corrected arithmetic + measured worst case.** At the true at-cap payload (1200 positions, 500 walls, 800 lot lines, 400 street lines, 133 053 B): **HTTP 200 in 9.34 s**, down from **17.31 s** at the previous identity. Operation bound falls from ~1.06e7 to ~1.64e6 (600 000 distance tests + ~1.04e6 simplicity pairs across the two passes). The module's stated ~2.0e6 is a *conservative over-estimate* of the true ~1.64e6 — it errs high, which is the correct direction for a security bound — and it now discloses the double validation pass, which was the substance of F2. BP-4's binding product clause (walls × lot-line segments) remains 600 000 < 1e6, tighter than the library, typed refusal naming the cap.

## 4. Regression sweep — no other ruling disturbed

- **BP-1 CLOSED:** `hasattr(mod, "derive_proposal")` → False; single entry unchanged.
- **BP-2 CLOSED:** all 16 bypass vectors (dotted-I, NUL, zero-width, fullwidth, LF/CR/tab, U+2028, Arabic-Indic digits, combining, NFKC ligature, traversal, quotes, markup, whitespace-only, len-201) still 422; exact 200-char boundary still 200; non-ASCII `proposal_id` 422.
- **BP-5 CLOSED:** types, registry-derived enum domains (`WIDE` 422 / `wide` 200), unmapped surfacing — all intact.
- **BP-6 CLOSED:** 200 keys unchanged; zero emission tokens.
- **BP-7 CLOSED (posture) / N-A-WITH-DISPOSITION (authn+tenancy):** flag-off `404 {"detail":"Not Found"}`, no `X-Correlation-ID`, byte-identical to the sibling route, absent from OpenAPI.
- **AS-1 arithmetic preserved:** coverage FAIL 0.625 vs 0.5 shortfall **0.125**; height PASS 30.0/60.0; two COULD_NOT_CHECK; summary `{pass:1, fail:1, could_not_check:2, total:4}`.
- **Checklist items** (cross-tenant, service-role secrecy, private storage, SSRF, injection, uploads, prompt-injection, least privilege, no new dependency, no new flag) — unchanged by this diff; all stand as ruled.
- **Suites, reproduced independently:** `python -m ruff check .` → All checks passed; `pytest tests/api -q` → **552 passed**; `pytest tests/rules -q` → **726 passed**; `python tools/modularity_check.py --check` → `selected 456 files; failures 0; warnings 20`, exit 0. Matches the orchestrator capture exactly.
- **Modularity (route 593 → 721 lines):** `proposal_checks_api.py` is still **absent** from the warn list and the warning count is unchanged at 20 (all pre-existing, out of scope). The three new helpers (`_bounded_field`, `_require_bounded_object`, `_validate_lot_rule_fact_keys`) are all request-boundary-enforcement responsibilities, correctly placed; no responsibility mixing, no dumping ground. Worth watching as it approaches the tier — the producer already routed the `_request_bounds.py` extraction to DB-039(c).
- **F3/F5/F6 routing verified:** DB-039 exists in `docs/DISCOVERY_BACKLOG.md:153` at `a0cb3b79` and carries all three verbatim. Correct disposition (advisory, out-of-packet).

## 5. New residual (LOW, advisory — does not block)

**F7 — 200-path markup is length-bounded but not charset-bounded.** The producer report's claim that the 200 path "can carry no unbounded/markup caller string" is accurate for *unbounded* but overbroad for *markup*. Reproduced: `HTTP 200`, markup appears **twice** in the body via (i) `provenance.provided_input_ids` → `wall:<block exterior_walls[].id>` (≤512 ch, charset unrestricted by the accepted DB-034(b) gate, not this packet's to change) and (ii) `provided_provenance` ← `lot.area_provenance` values (≤2048 B under the new `MAX_PROVENANCE_BYTES`). Longest `provided_input_id` 430 ch; response 8 663 B. This is a materially different risk class from the original F1 (everything is now length-bounded, and the API sets `nosniff` + `default-src 'none'`), and "escape caller provenance on render" is already the recorded B3-slice-2 obligation from the T054 G5-4 lineage. Suggested: append to DB-039 and correct the report sentence to "no unbounded caller string".

**F8 — threadpool saturation at public exposure (LOW, advisory).** An at-cap request is ~9.3 s of threadpool CPU; the default anyio threadpool is bounded (~40 workers), so sustained at-cap traffic would queue. Rate limiting and a per-request timeout belong with the public-exposure packet alongside authn/tenancy — same bucket as the BP-7 deferral, not a defect here.

---

## Per-precondition rulings at this identity

- **BP-1: CLOSED** — **BP-2: CLOSED** — **BP-3: CLOSED** (re-ruled; was OPEN) — **BP-4: CLOSED** (re-ruled; was OPEN) — **BP-5: CLOSED** — **BP-6: CLOSED** — **BP-7: CLOSED** (posture) **/ N-A-WITH-DISPOSITION** (authn + tenancy, recorded in producer report §5).

All seven recorded preconditions are now closed at this seam. F1, F2 and F4 are repaired at the root cause with binding tests; F3/F5/F6 correctly routed to DB-039; F7/F8 are new LOW advisories for the same backlog.

**VERDICT: PASS**
