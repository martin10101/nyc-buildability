# M5-T017 — G5 Security Gate Report (verbatim)

Orchestrator preservation note: report returned by the independent security-reviewer agent
("G5 security review M5-T017") on 2026-09-12 and preserved verbatim below (transport
entity-decoding only). Reviewer was read-only; frozen review identity: producer commit
45f63efb. Verdict: PASS (advisories LOW-1, LOW-2; one process note — the DCV row —
satisfied by the orchestrator's acceptance flow).

---

All checks complete. No working-tree drift on the 17 files (content identity == frozen SHA 45f63efb), and evidence.py reads the rule_evaluation document's PRR, not the widened scenario-document flag. Here is my G5 gate report.

---

# Gate Report

- **Gate ID:** G5 (security review)
- **Task ID:** M5-T017 — C1 unused draft zoning floor area (FAR-derived) scenario document section
- **Reviewer:** security-reviewer (read-only, independent)
- **Producer:** scenario-optimization-engineer
- **Result:** **PASS** (2 LOW advisories, non-blocking; 1 process note)
- **Clean environment/worktree used:** Reviewed at frozen producer commit `45f63efb` (base `462c8ad1`); repo HEAD `f4f01352`, branch `candidate/D-024-mrl-option-b`. Python 3.11.9 sandbox.

## Frozen-identity check — PASS

- `45f63efb` and base `462c8ad1` exist; `45f63efb` is an ancestor of HEAD `f4f01352`.
- Producer commit `45f63efb` touches **exactly 17 files**, all within `allowed_paths`; none in `forbidden_paths`.
- `git diff --name-only 45f63efb..HEAD` restricted to the 17 allowed_paths is **empty** — the six later commits (M5-T018 web half + evidence commits) do **not** re-touch any reviewed file.
- `git status --porcelain` on the reviewed paths is **empty** — no uncommitted working-tree drift; reviewed content identity == `45f63efb`.

## Acceptance criteria reviewed

S1 computed, S2 over-built, S3 zero-boundary, S4 missing/unusable, S5 no-cap, S6 contract/JSON-safety/precise-noun/modularity — all reproduced independently (see below).

## Directive/requirement verification

| Requirement ID | Reviewed SHA / content identity | Verdict | Reproduced evidence |
|---|---|---|---|
| D-041-R001 | `45f63efb` (working tree clean) | **PASS** | Re-derived from `project-control/directives/D-041-c1-development-rights/requirements.json`. All atomic obligations reproduced: (1) computes `cap − existing built floor area` (`unused_floor_area.py:312`); (2) own clearly-labeled line via `UNUSED_FLOOR_AREA_LABEL`; (3) draft-label + per-fact provenance (cap echoed verbatim + bldgarea provenance resolved from `provenance_ref`); (4) over-built renders honest explicit statement (`over_built` state + `UNUSED_FLOOR_AREA_OVER_BUILT_STATEMENT`); (5) routes to professional review (section flag + root OR); (6) negative preserved exactly, never clamped/hidden (`test_s2` value `<0`, `!=abs`); (7) missing inputs → typed `not_computable`, value `null`, never estimated (`test_s4`, `test_s5`); (8) four-case harness present with fixture-derived assertions (S1/S2/S4/S5, plus S3/S6). R002/R003 apply only to `D-041-BOOTSTRAP`, not M5-T017 (correctly not bound). |

**Process note (not a G5 security defect):** `verification.json` for D-041 has `task_verifications: []` / `verified_by: null`. The independent `directive-compliance-verifier` must record a PASS row for M5-T017 at the reviewed SHA before `accept()` (separate required pass; `accept()` reads it as blocking evidence). My independent re-derivation finds R001 substantively satisfied.

## Steps independently executed

```
git rev-parse HEAD                     -> f4f01352…
git merge-base --is-ancestor 45f63efb HEAD -> ancestor confirmed
git show --stat 45f63efb               -> 17 files, +1697/-4
git diff --name-only 45f63efb..HEAD -- <17 paths> -> empty
sha256sum <both schema copies>         -> identical (4ad3bcae…)
python tools/validate_directive_compliance.py --check -> EXIT 0
services/api $ pytest tests/scenario/test_unused_floor_area.py -> 24 passed
services/api $ pytest tests/scenario                 -> 422 passed
services/api $ pytest tests/api/test_scenario_api.py -> 27 passed
python tools/modularity_check.py --check             -> EXIT 0 (builder.py & unused_floor_area.py NOT flagged)
```
Plus: adversarial numeric/surrogate probe against `build_unused_floor_area_section`, and byte-recomputation of all 4 fixtures with the real function.

## Threat-by-threat findings

**1. Input trust — PASS.** The section is built only inside `_assemble` from `property_profile` + builder-internal `cap_value`/`cap_provenance`. `GET /properties/{bbl}/scenario` (`scenario.py:161`) takes only `bbl` + injected deps — **body-less**. The POST analysis endpoints rebuild the scenario server-side in `_rebuild_scenario` (never consulting the body); the request body reaches engines as `assumptions` only, and `_finish` surfaces only cap/label/kind/coverage — **not** the authoritative section. The new key `unused_draft_zoning_floor_area` is not in `FORBIDDEN_FACT_KEYS`, but there is no forgery surface: the authoritative section is never in the analysis envelope, and the section's sensitive sub-key `professional_review_required` is already rejected at any depth. No caller-controlled path reaches the section or its inputs. `test_builder_does_not_mutate_profile_with_bldgarea` confirms read-only consumption.

**2. Honesty-as-security — PASS.** Over-built value is returned verbatim (`value = cap − existing_area`, `unused_floor_area.py:312,324-334`) — never clamped/nulled/abs'd (`test_s2`). All three `not_computable` returns hard-set `value=None` (`:244,278,297`) — no path emits a number. Cap is consumed byte-verbatim: `_positive_finite_float` returns the identical float, `cap_provenance` echoed by passthrough; the document's `draft_zoning_floor_area_cap_sq_ft` and the section's echoed cap both derive from the same `_assemble` `cap_value` — no recompute drift (`test_direct_pure_function_computed_and_verbatim_cap`).

**3. JSON/renderer parity — PASS (numeric); LOW advisory (surrogate string).** Numeric smuggling is fully guarded: my adversarial probe confirms NaN, +/-Inf, negative, huge-int (`10**400` overflow, `10**308` finite), and bool `bldgarea` all fail closed to `not_computable`/`null` or a finite value; NaN/Inf cap → `no_draft_far_cap`. Every value is finite-or-null, so both `json.dumps(allow_nan=False)` and `.encode("utf-8")` succeed. `test_s6_every_state_survives_strict_and_utf8_json` exists and passes for all states. See LOW-1 for the surrogate-string sub-case.

**4. professional_review_required widening — PASS.** Root flag is `professional_review_required or unused_section["professional_review_required"]` (`builder.py`). The section flag is `False` in every state except `over_built` (`:247,282,302,340` False; only `:328` True). The OR can therefore only **add** True — it can never lower a review obligation (`test_root_prr_or_semantics_both_triggers`). Downstream check: no code consumes the **scenario document's** root PRR boolean for an authorization/access decision — `evidence.py` reads the **rule_evaluation** document's coverage/`needs_review` (`:248,301`); `scenario.py` returns 200 regardless; `scenario_analysis.py:180` only lists the name in `FORBIDDEN_FACT_KEYS`. Fail-safe direction correct.

**5. Contract surface — PASS.** Both schema copies are byte-identical (matching sha256), each closed (`additionalProperties:false` on the section and `unused_floor_area_inputs`), new key `required`. `generated/scenario.ts` diff is type-only/additive and matches the schema. I recomputed all 4 fixtures with the real `build_unused_floor_area_section` using each fixture's own cap/cap_provenance — every embedded section **byte-matches** (`preliminary_r5_cap` → `missing_existing_building_area`; the 3 no-cap → `no_draft_far_cap`). No invented official values; the cap provenance in the preliminary fixture is echoed verbatim from the canonical trace.

**6. Dependencies / I/O / secrets — PASS.** New module imports only `math`, `typing`, and local `.constants`/`.models`. Grep for network/file/env/secret/subprocess/eval tokens in the new module + constants: none. No lockfile/`requirements*.txt`/`pyproject.toml`/`package.json` in the diff. Facade export is additive only. The single CI red is owner-gated `web-dependency-security` (D-040-R004, Next.js RCE) — unrelated to this server half; all server jobs (api/contracts/typegen/bundle/modularity/control-plane) SUCCESS on the combined green head `44048007`.

**7. Commands — PASS.** `validate_directive_compliance.py --check` → EXIT 0; S6 tests pass (24 in the C1 pack; 422 scenario; 27 api scenario).

## Regression/security/provenance findings

- No assertions weakened; existing suites pass with additive shape updates only.
- Modularity boundary answers verified against the actual diff: new focused module (346 SLOC), builder.py wire-in +13 net lines (not flagged), stable public interface, additive facade, boundary tests present, checker EXIT 0.
- Never-Verified discipline intact: no "verified"/"compliant" language and no forbidden nouns ("maximum buildable area" / "remaining development rights" / "remaining capacity") in any C1 constant (`test_s6_no_forbidden_nouns…`).

## Defects

None blocking.

**LOW-1 (advisory, pre-existing, out-of-scope):** The section echoes profile string fields (`unit`, `coverage_status`, `provenance_ref`, and resolved provenance sub-fields) guarded only by `isinstance str`. My probe shows a lone surrogate in a profile string (`units="sq\ud800ft"`) yields a `computed` section that passes `json.dumps(allow_nan=False)` but fails `.encode("utf-8")` (Starlette's renderer settings). This is **not new to M5-T017**: it is identical to the accepted `_lot_area_provenance`/`_cap_provenance` echo pattern already in the same document, the echoed fields here are server-assigned constants (not PLUTO free-text), the profile builder/connectors are in `forbidden_paths`, and the numeric threat-3 vector is fully closed. Recommend a future document-wide encode guard on `GET /scenario` (which, unlike the analysis `_finish`, has no pre-send encode check) — track outside this task.

**LOW-2 (advisory, defense-in-depth):** Consider adding `unused_draft_zoning_floor_area` to `scenario_analysis.FORBIDDEN_FACT_KEYS` when that file is next in scope (M5-T018/follow-up). No live forgery surface today.

## Required rework

None. Both advisories are non-blocking and outside this task's allowed_paths.

## Reviewer conclusion

**VERDICT: PASS.** Frozen identity is intact (producer `45f63efb`, 17 allowed_paths, no later drift, clean working tree). All seven threat-model items reproduce as safe: no caller-controlled path reaches the section; over-built honesty and not-computable nulls are guaranteed; numeric NaN/Inf/huge-int smuggling is fully guarded and every state is strict+utf8 JSON-safe; the PRR OR widens (never lowers) and no downstream authorization consumes the scenario-document root flag; both schema copies are byte-identical with type-only TS drift; all 4 fixtures byte-match independent recomputation; no new dependencies/IO/network/secrets; validator EXIT 0 and the S6 suite passes. D-041-R001 is independently satisfied. Two LOW advisories and one process note (the independent `directive-compliance-verifier` must still record the D-041 `verification.json` row for M5-T017 before `accept()`) are recorded for the orchestrator; none blocks this gate.

---

# APPENDED DELTA ATTESTATION (verbatim, same reviewer agent, after correction commit ae38a169)

Delta verified. Attestation:

---

**G5 DELTA ATTESTATION — M5-T017 @ ae38a169 (parent f4f01352)**

I independently confirm commit `ae38a169` is a single, test-only, purely additive change: +39 lines to `services/api/tests/scenario/test_unused_floor_area.py` (inside allowed_paths), no other file. `git show` confirms two added regression tests (`test_s1_vacant_lot_zero_existing_area_is_usable_and_computed`, `test_s1_fractional_remainder_is_unrounded`) discharging the G4 mutants M15/M16. No production code, schema, fixture, or dependency change; no new imports (both tests reuse the module's existing symbols); no I/O; no weakened assertion. The C1 pack now runs 26 passed (was 24).

All six threat-model conclusions are unaffected — nothing in the diff touches input trust, honesty, JSON parity, PRR widening, contract surface, or dependencies. The vacant-lot test additionally pins the `bldgarea == 0.0` boundary I probed adversarially (`_nonnegative_finite_float`, not `_positive_`), and the fractional test locks the unrounded subtraction against a `round()` regression — both strengthen the honesty-as-security guarantee.

My **G5 PASS stands** at content `ae38a169`. LOW-1/LOW-2 advisories and the pending directive-compliance-verifier `verification.json` row are unchanged.
