<!-- Reviewer return preserved VERBATIM by the orchestrator (report-preservation rule,
.claude/rules/project-control.md; transport entity-decoding applied: &amp; -> &, &gt; -> >, &lt; -> <).
Reviewer: independent qa-engineer subagent (G4), returned 2026-09-11, reviewed HEAD 79f06fde. -->

All verification complete. Here is my gate report.

---

# Gate Report

- **Gate ID:** G4 (QA — coverage & executability)
- **Task ID:** M4-T010 (Rule-citation content-digest binding: additive schema property + fail-closed loader verification)
- **Reviewer:** qa-engineer (independent; did not author)
- **Producer:** rules-engineer/orchestrator (producer commit `7bac23c1`)
- **Result: VERDICT: PASS**
- **Clean environment/worktree used:** Read-only static review at the frozen reviewed HEAD `79f06fde3a56aae2ffef7016e24a5f138fc0940f`. Source inspected in the ctl24 checkout and cross-checked against git blobs at 79f06fde (see below). No pytest/ruff run (CI is execution authority); only `ast.parse`, offline `jsonschema` probes, and read-only git via the shared object store.
- **Recomputed collected-case count: 8** (matches producer claim).

## Acceptance criteria reviewed

S1–S6 from `project-control/tasks/M4-T010.json`. Every source file reviewed is byte-identical to the frozen SHA:
- `test_rule_citation_digest.py` working tree = blob `63087f95…` = `79f06fde:…/test_rule_citation_digest.py`
- `dsl.py` working tree = blob `47955fc0…` = `79f06fde:…/dsl.py`
- `rule_definition.schema.json` working tree = blob `a3a2ae75…` = `79f06fde:…/schema.json`
- Producer commit `7bac23c1` is an ancestor of `79f06fde`; the two intervening commits (`79734f11`, `79f06fde`) touch only control-plane files (`M4-T010.json`, evidence map), never the three source files.

## Directive/requirement verification

| Requirement ID | Reviewed SHA / content identity | Verdict | Reproduced evidence |
|---|---|---|---|
| D-038: ALL | 79f06fde | UNVERIFIABLE-BY-THIS-GATE (applicable set empty) | `project-control/reports/M4-T010-evidence-map.json` = `{"requirements": {}}`; orchestrator record notes "D-038 applicable set computes empty for M4-T010 (M2-T021/M0-T057 precedent)". Authoritative directive-compliance verification is owned by the independent `directive-compliance-verifier` (producer ≠ verifier) and recorded in the directive's `verification.json`, not by this QA gate. This gate scope is coverage/executability only. |

## Steps independently executed

1. `git show --stat 7bac23c1` → exactly 4 files: producer report, `dsl.py` (14±), schema (3±), new test module (148+). `git diff --name-only 7bac23c1 79f06fde` → only control-plane files.
2. `git hash-object` of all three ctl24 source files == frozen blobs at 79f06fde.
3. `ast.parse(..., feature_version=(3,12))` on `dsl.py` and the test module → OK; no line > 100 chars in either.
4. Offline `jsonschema` 4.26.0 probe against the real schema file, replicating `_schema_validate`'s loc/message formatting, for S3-shape, S4 (all 4 shapes), and a reconstructed pre-change schema.
5. Computed `sha256(verbatim_excerpt)` for `zr-23-21` and compared to the stored snapshot digest.
6. Enumerated all 11 committed rules' snapshot/citation refs against the fixture's snapshot dir (`docs/research/zr-snapshots/v1`).

## Expected versus actual

**1. Scenario coverage & recomputed count.**
- S1 → `test_s1` (loads all committed rules; floor `>= 11`, actual inventory = 11).
- S2 → `test_s2` (matching digest loads).
- S3 → `test_s3` (mismatch fails closed).
- S4 → `test_s4` parametrized ×4 (ids `uppercase`, `63-chars`, `65-chars`, `non-hex`).
- S5 → `test_s5` (missing-snapshot precedence).
- S6 → command-level (`documented_test_commands`). **Disposition acceptable, not a gap:** S6's assertions (engine.py untouched, rulesets untouched, requirements unchanged, full suite passes, modularity exit 0) are diff/CI invariants, not unit assertions — encoding "engine.py untouched" as a pytest assertion would be wrong. The diff-level half is independently verified here (commit touches only the 4 allowed paths; none is engine.py/rulesets/requirements). The suite-pass and modularity halves are CI/command authority.
- **Recomputed count = 1+1+1+4+1 = 8.** Matches producer.

**2. Anti-tautology.**
- S2: the expected digest is loaded from the snapshot via `_snapshot_digest` and cross-checked against `sha256(verbatim_excerpt)`; the test file contains **no 64-hex literal** and `dsl.py` hardcodes no digest. Probe confirms `zr-23-21` stored digest `b52771e6…` == `sha256(verbatim_excerpt)` (True). No assertion restates a literal the code hardcodes.
- S3: probe confirms a valid-hex flipped digest **passes** schema (validator returns no error), so only `_check_refs` can raise. Its `DSLError` message = "…records content_digest_sha256 <flipped> but the snapshot on disk stores <actual>…". The S3 `match=` `records content_digest_sha256 .* but the snapshot on disk stores` matches that message and **cannot** match any schema-shape rejection (which reads "rule schema violation at …"). S3 also asserts rule_id (`r5-residential-far`) and snapshot_id (`zr-23-21`) appear in the message — both do. Confirmed the risk "mismatch test could pass vacuously against a schema-shape rejection" is closed.

**3. Mutant-killing power.**
- (a) Drop the digest comparison from `_check_refs`: mismatch no longer raises → `test_s3` `pytest.raises(DSLError, match=…)` gets no error → **FAILS**. Caught. ✓
- (b) Compare against a rule-side recomputed digest instead of the stored one: **no test catches this, and it does not matter.** `SnapshotStore.load`/`load_snapshot_file` set `content_digest_sha256 = sha256(verbatim_excerpt)` and raise `SnapshotError` if the on-disk stored digest diverges — a tampered snapshot never loads. So "stored" and "rule-side recomputed" are equal for every loadable snapshot; the distinction is unobservable by construction, and the tamper path is owned by the snapshot store's own tests. Honest gap noted; not a coverage defect for this unit's scope ("compare against the snapshot's STORED digest").
- (c) Make the schema property `required`: all 11 committed rules (none carries the field) would fail schema → `test_s1` **FAILS**. Caught. ✓
- (d) Loosen the pattern (e.g. allow uppercase / wrong length): the malformed value would pass schema and route to the load-time mismatch `DSLError` instead of "rule schema violation at citations/0" → `test_s4` `match=r"rule schema violation at citations/0"` **FAILS**. Caught. ✓ (This is precisely why S4 asserts the schema-violation-specific message, not a bare `raises(DSLError)`.)

**4. Red/green coherence** (mechanically reproduced via the reconstructed pre-change schema):
- Pre-change, citation items have `additionalProperties: false` with no `content_digest_sha256`. Probe: a doc carrying the field → "rule schema violation at citations/0: Additional properties are not allowed ('content_digest_sha256' was unexpected)".
  - S2 pre-change: schema rejects → `test_s2` expects successful load → **RED**. ✓
  - S3 pre-change: `DSLError` raised but message ≠ mismatch pattern → `pytest.raises` match fails → **RED**. ✓
  - S5 pre-change: schema rejects the added field before `_check_refs` runs → `DSLError`, not `SnapshotError` → `pytest.raises(SnapshotError)` → **RED**. ✓
  - S1 pre-change: committed rules carry no field → **GREEN**. ✓
  - S4 pre-change: additionalProperties message = "rule schema violation at citations/0: …" → S4 `match=` substring matches → **GREEN** (×4). ✓
- Pre-change tally: 3 failed (S2, S3, S5), 5 passed (S1 + S4×4). Matches producer's "3 failed, 5 passed". Post-change 367 = 359 + 8 is arithmetically consistent (367 − 8 = 359). Full-suite/zero-regression is a CI claim deferred to CI per gate discipline.

**5. Executability statics.** Both changed Python files `ast.parse` clean under `feature_version=(3,12)`; no line > 100. Parametrize ids are explicit/deterministic. The module reads only committed files (rule files, `docs/research/zr-snapshots/v1` snapshots), deep-copies documents in memory, performs no writes/tmp/network. Fixture `store` is module-scoped and used read-only (frozen `SectionSnapshot`; all mutation is on deep-copied docs) — sound. Imports resolve (`DSLError`, `build_rule_definition` in `dsl.py`; `SnapshotError`, `SnapshotStore` in `snapshots.py`). S1 executability confirmed: all 11 committed rules' snapshot/citation refs resolve within the fixture's `_DOCS_SNAPSHOT_DIR`.

**6. Regression surface.** `7bac23c1` touches nothing under `services/api` except `dsl.py`, the schema, and the new test module (report is outside `services/api`). No `engine.py`, no `rulesets/**`, no requirements change. The 359 existing tests' surface (loading committed rules via `build_rule_definition`) is exercised by S1. `dsl.py` is 299 lines (far under the 600 warn threshold); the change is a cohesive 6-line addition to the existing citations loop — no modularity concern.

## Evidence paths

- Task: `project-control/tasks/M4-T010.json`; Producer report: `project-control/reports/M4-T010-producer-report.md`; Evidence map: `project-control/reports/M4-T010-evidence-map.json`
- Source: `services/api/app/rules/dsl.py`, `services/api/app/rules/schemas/v1/rule_definition.schema.json`, `services/api/tests/rules/test_rule_citation_digest.py`, `services/api/app/rules/snapshots.py`
- Snapshot: `docs/research/zr-snapshots/v1/zr-23-21.snapshot.json`; Base rule: `services/api/app/rules/rulesets/r5_residential_far.rule.json`

## Human-style walkthrough findings

N/A — backend schema/loader unit, no UI surface.

## Regression/security/provenance findings

None. The change is additive and fail-closed: absent field loads exactly as before; a recorded digest that disagrees with the resolved snapshot refuses to load with a typed `DSLError` naming rule id, snapshot id, and both digests; `SnapshotError` resolution precedence is preserved. Provenance discipline (binding transcription digest at the rule file) is strengthened, not weakened.

## Defects

None.

## Required rework

None.

## Reviewer conclusion

**VERDICT: PASS.** Reviewed HEAD `79f06fde3a56aae2ffef7016e24a5f138fc0940f`; recomputed collected-case count **8**. All six review dimensions pass: S1–S5 map to specific, executable, non-tautological tests; S6's command-level disposition is correct for its diff/CI-invariant nature and its diff half is independently verified; the anti-tautology guards on S2/S3 hold (confirmed by offline jsonschema probe); the tests kill mutants (a), (c), (d); the one unobservable mutant (b) is unobservable by construction of `SnapshotStore.load`'s own tamper check and is not a gap for this unit's scope; red/green counts (3 fail / 5 pass → 367 = 359 + 8) are mechanically and arithmetically coherent; and the change's regression surface is confined to the three allowed source files with no engine/ruleset/requirements impact. Full-suite pass, `ruff`, and `modularity_check` remain CI/command execution authority per gate discipline; nothing in the static review contradicts the producer's recorded green.

**One transparency note for the record (not a defect):** mutant class (b) — comparing a rule-side recompute vs the stored snapshot digest — is untestable here because `SnapshotStore.load` guarantees the stored digest equals `sha256(verbatim_excerpt)` and fails closed otherwise; the two values can never diverge for a loadable snapshot.
