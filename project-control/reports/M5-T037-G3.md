# M5-T037 — G3 code review return (VERBATIM; code-reviewer, read-only, pinned a6875731)

Orchestrator note: returned via the agent-return channel 2026-09-18T10:15:02Z; the transport
truncated the first delivery inside point (3). Part 1 below is the truncated delivery
verbatim; Part 2 is the resent remainder, verbatim. VERDICT: PASS (one advisory,
non-blocking).

---

## Part 1 (first delivery, verbatim)

I have completed the review. All the material claims verified against source. Returning my G3 gate report.

---

# G3 Code Review — M5-T037 — VERDICT: PASS (one advisory, non-blocking finding)

Reviewed material commit `a6875731` (16 files), identity-stamped `f954aa59`, current HEAD `fa2aecab`. Read-only; no files modified.

## Identity / scope checks (all clean)
- **allowed_paths byte-stable `a6875731..HEAD`**: `git diff` over all 20 allowed_paths is empty. The later commits are the sanctioned out-of-scope `[ORCH-CORRECTED]` guard update (`be0a7062`, touches only `services/api/tests/scenario/test_scenario_contract.py`, correctly outside allowed_paths and routed to the orchestrator per the consumer-sweep rule) and the CI-evidence report (`fa2aecab`). Neither alters material code identity.
- **Both schema copies byte-identical** at HEAD (canonical `packages/contracts/schemas/v1/` == bundled `services/api/app/_contract_schemas/v1/`), diff empty.
- **CI**: `M5-T037-ci-evidence.md` records all 20 green at `be0a7062` incl. web-e2e, contracts-typegen, contracts-sync, exact-production-install. Modularity `--check` re-run here: no M5-T037 file flagged (only unrelated `tools/agent_supervisor/*` warnings). AS-8 preservation grep: the only "verified" token in the display additions is the negated "not verified" (`DevelopmentLimits.tsx:73`); no compliance/approved claim introduced.

## Judge points 1–6

**(1) DB-020 first + order-of-work — CORRECT.** `wide_street_live_provider.py` `_attested_wide_segments` now walks `geom_result.pages` and, per page, iterates `page.entries` binding `source_raw_digest=page.raw_digest`, `source_retrieved_at=page.retrieved_at`, `crs=page.crs`. **No page/segment mismatch is possible**: `SegmentGeometryPage` (`dcm_street_centerline_geometry.py:326-349`) owns its own `entries`, `crs`, `retrieved_at`, `raw_digest`; each attested segment is drawn from within its page loop, so a segment can only ever receive its own page's digest. The regression test (`test_wide_street_live_provider.py::test_db020_attested_segment_carries_geometry_page_digest_not_width_text`) asserts the sha256 shape (`re.fullmatch(r"sha256:[0-9a-f]{64}", ...)`), exact equality with `raw_body_digest(page body)`, `!= "80"`, and that the source `streetwidth_raw` is preserved untouched. Strong red-before/green.

**(2) Serialization boundaries — CORRECT and fail-closed.** The shape change is centralized in `PropertyRuleEvaluation.as_dict` (`integration.py:193-194`): `wide_street` emitted only when `wide_street_determination is not None`. The three sibling surfaces (`evidence.py`, `scenario.py`, `scenario_analysis.py`) source is unmodified; shape can only change through the shared serializer, and each calls `validate_rule_evaluation_document` (evidence.py:518, rule_evaluation.py:346, scenario.py:277, scenario_analysis.py:535), which validates against the closed jsonschema and raises on any defect — so a malformed block yields no 200 anywhere. `_wide_street_summary` (`integration.py:552-573`) emits **exactly the 15 keys the schema `required` lists, no extras**, so `additionalProperties:false` + `required` both hold. Enum-constrained fields are guaranteed in range: `far_row` ∈ the three `FAR_ROW_*` constants; `determination_state` ∈ the three `DETERMINATION_*` constants (`wide_street_wiring.py`). No residual exact-`1.0.0` rule_evaluation assertion remains in any unmodified consumer suite (the one that had it, `test_scenario_contract.py`, was the sanctioned ORCH correction).

**(3) Display honesty — CORRECT.** `WideStreetResult` (`DevelopmentLimits.tsx`) renders nothing when no block (no invented value); shows the FAR only via `farValue(...)` when non-null else "Not calculated"; states units (dimensionless ratio; floor area = FAR × lot area sq ft), conditions (within/outside 100 ft), DRAFT marker + "Pending qualified legal review (not verified)"; on `professional_review_required` it prints the escalation and **withholds the higher FAR** (test asserts `quer[TRANSPORT TRUNCATION POINT]

---

## Part 2 (resent remainder, verbatim; delivered 10:15:56Z — report COMPLETE)

Orchestrator note on convergence: G3's advisory F1 below is the SAME defect the HJ review
required as its blocking F1 (the report's ungated DevelopmentLimits feed), with the SAME
suggested minimal fix — which was applied as the tagged rework commit 75daaebd before this
remainder arrived. Two independent reviewers converged on the defect and the remedy.

Resending from where it truncated in point (3):

**(3) Display honesty — CORRECT (cont.).** On `professional_review_required` `WideStreetResult` (`DevelopmentLimits.tsx`) prints the escalation and WITHHOLDS the higher FAR — the test asserts `screen.queryByText(/3\.44/)` is null. When present it shows the FAR only via `farValue(...)` when non-null else "Not calculated"; states units (dimensionless ratio; floor area = FAR × zoning-lot area sq ft), conditions (within / outside 100 ft), the DRAFT marker + "Pending qualified legal review (not verified)" (`DevelopmentLimits.tsx:73`), plus a "Wide-street sources and provenance →" link to the evidence view. Renders nothing when no block (no invented value). No legal text hardcoded beyond UI labels/condition copy; the substantive strings (reason, draft_label, fallback_direction_note) come from the document, not the component.

**(4) Report parity — met structurally for the healthy path; see F1 for the edge.** `ReportView.tsx` is UNMODIFIED and reuses the same `DevelopmentLimits` (L68) and `CalculationEvidence` (L92) — one validated document, no forked data path. Correct approach. `report-view.test.tsx` validates the shared fixture (`validateRuleEvaluationDocument(doc).ok === true`), renders both surfaces from it, and asserts `reportValue === screenValue` (both contain "3.44"), that the report's provenance surface shows "OBJECTID=12345", that professional-review shows no fabricated FAR, and that a 1.0.0 doc adds no wide-street surface on either. Caveat in F1.

**(5) Strict validator — CORRECT.** `checkWideStreet` (`rule-evaluation-contract.ts:337`) treats `undefined` as valid (1.0.0 body stays valid), shape-checks all 15 keys (enums via `checkEnum`; number|null; non-empty strings; booleans; string vs string|null arrays matching the schema's per-array nullability), and any single problem fails the WHOLE document (`problems.length>0 → {ok:false}`) — no partial render. `contract_version` is now `checkEnum`'d against `["1.0.0","1.1.0"]` (was a hard `!== "1.0.0"`). Generated TS (`rule_evaluation.ts`) is `"1.0.0" | "1.1.0"` + optional `wide_street?` with all 15 typed keys; CI contracts-typegen proves byte-identity.

**(6) Modularity + test quality — CORRECT.** Typed DOM queries (`container.querySelector<HTMLElement>(...)`, `within(panel)`); exact-string assertions where drift matters ("3.44", the evidence href, "OBJECTID=12345", "Professional review required"); the `report-view.test.tsx` placeholder is replaced with real suites (no empty-suite risk); only `LotOutlineMap` is mocked (jsdom/MapLibre avoidance), not the units under test. Local modularity `--check` here flags no M5-T037 file (only unrelated `tools/agent_supervisor/*` warnings); CI modularity job green.

## Finding F1 (advisory, non-blocking) — parity holds only for INSPECTABLE documents
`ReportView.tsx:68` feeds `DevelopmentLimits` the UNGATED `identityEvaluation`, but the real screen (`ArchitectEntry.tsx:73` → PropertyOverview/ZoningView) feeds it the inspectability-GATED `evaluation` (`evaluationIsInspectable(identityEvaluation) ? identityEvaluation : null`, def at `development-limits.ts:95`). `WideStreetResult` gates only on block presence (`const wide = evaluation?.wide_street; if (!wide) return null;`), not inspectability. So a document that is contract-VALID but NON-inspectable (a trace fails the stricter `readableTrace` guard while the block passes the schema) would render the wide-street FAR on the report but NOT on the screen; and within the report itself `DevelopmentLimits` (L68, `identityEvaluation`) vs `CalculationEvidence` (L92, gated `evaluation`) gate differently, so the FAR headline would show while its D-052 provenance drops. That is the class of screen/report disagreement D-073-R003 targets. The parity test does not catch it: it renders `DevelopmentLimits` directly (bypassing the screen gate) and uses `draftApplicableDoc()`, which is inspectable, so both surfaces trivially agree.

WHY ADVISORY, NOT FAIL: not reachable through normal server output — a real wide-street-bearing evaluation carries well-formed R6–R12 traces and is therefore inspectable; the fixture is representative of that healthy path. The divergent direction shows a valid, DRAFT-marked block (no false compliance claim). The producer disclosed the L68/L92 wiring in its report but concluded "no gap to record" without analyzing the inspectability asymmetry.

SUGGESTED MINIMAL FIX (in-scope; `ReportView.tsx` is in allowed_paths): pass the gated `evaluation` to `DevelopmentLimits` at L68 to match the screen (safe — `DevelopmentLimits` already re-gates its other values via `evaluationIsInspectable`, so only `WideStreetResult` behavior changes), plus a non-inspectable-block edge test. Orchestrator's call whether to require before acceptance.

## Minor observations (not defects)
- `CalculationEvidence.tsx` status line prints "Professional review required" for every wide_street block regardless of `determination_state`; defensible as the DRAFT-pending-G6 posture (actual state shown in the `<dl>`), slightly redundant.
- Web validator requires non-empty strings for coverage_hint/draft_label/fallback_direction_note/reason (schema allows empty) and does not enforce additionalProperties:false within the block; both harmless — stricter-than-schema on emptiness, and the authoritative closed check is the server jsonschema wired at all four endpoints (evidence.py:518, rule_evaluation.py:346, scenario.py:277, scenario_analysis.py:535).

VERDICT: PASS. F1 is a genuine but narrow parity edge; recommend the orchestrator weigh the one-line L68 alignment + edge test, but it does not block on code-correctness grounds.

---

## Delta-attestation round 1 (VERBATIM; delivered 10:23:27Z at head 8240a497 — does-not-carry pending the casing fix + CI; the fix landed at 1c89922c)

Delta-attestation for rework 75daaebd → head 8240a497. VERDICT: my G3 PASS does NOT carry cleanly — the 3-file delta introduces one BLOCKING new defect (a failing existing test). Report remainder already delivered; the orchestrator saved it verbatim as M5-T037-G3.md Part 1 + Part 2 — nothing owed there.

IDENTITY OUTSIDE THE 3 FILES — CONFIRMED UNCHANGED.
`git diff --stat a6875731 8240a497` over all reviewed api/schema/contract code (integration.py, response.py, wide_street_live_provider.py, both schema copies, generated rule_evaluation.ts, rule-evaluation-contract.ts, CalculationEvidence.tsx, the three api test files, contract-versions.test.ts) is EMPTY — byte-identical. The only other changed files are control-plane/gates/reports/docs for M5-T037 and the interleaved M5-T038, plus the already-known sanctioned out-of-scope guard `test_scenario_contract.py` (be0a7062, part of my original review context). No api/schema/contract change. Good.

F1 CORRECTION — CLEAN (exactly my suggested fix). `ReportView.tsx:68` now feeds `DevelopmentLimits` the inspectability-gated `evaluation` (line-30 form `evaluationIsInspectable(identityEvaluation) ? identityEvaluation : null`), matching every screen surface and the report's own `CalculationEvidence`. This is a strict tightening; for a non-inspectable doc both surfaces now withhold the panel. Its new guard test in report-view.test.tsx (rule_id set to `123 as unknown as string` → non-inspectable → panel withheld) is sound and uses the correct invalid-shape idiom.

F2 CORRECTION — INTRODUCES A NEW DEFECT (BLOCKS). The panel copy at `DevelopmentLimits.tsx:81` changed to:
  `Draft — pending qualified legal review (not verified).`   (lowercase "pending")
The rework updated its OWN new test to match — `report-view.test.tsx:135` asserts exactly `"Draft — pending qualified legal review (not verified)."` — but it did NOT update the pre-existing assertion in `development-limits.test.tsx:563`, which still reads:
  `expect(within(panel).getByTestId("wide-street-draft")).toHaveTextContent("Pending qualified legal review");`   (capital "Pending")
`development-limits.test.tsx` is outside the rework's 3-file set and was not touched. `toHaveTextContent(string)` is a case-sensitive substring check, so "Pending qualified legal review" (capital P) is NOT contained in the rendered lowercase text → this assertion FAILS. That test ("shows the within-wide conditional FAR with units, conditions, DRAFT marker and a sources link") will go RED in the vitest/web CI job at 8240a497. The two test files now literally disagree about the same copy (report-view.test.tsx:135 lowercase-correct vs development-limits.test.tsx:563 capital-stale).

REPRODUCTION (read-only):
- `git show 8240a497:apps/web/src/components/architect/DevelopmentLimits.tsx` line 81 → lowercase "pending".
- `git show 8240a497:apps/web/src/components/architect/__tests__/development-limits.test.tsx` line 563 → asserts capital "Pending qualified legal review".
- Contradicted by the rework's own `report-view.test.tsx:135`.

ALSO: no CI evidence exists for the rework head. The latest captured run (`M5-T037-ci-evidence.md`) is green at `be0a7062`, which PREDATES 75daaebd. Since the rework touches web files, web-e2e/vitest must be re-run and captured at the corrected head — and as above it would currently fail on development-limits.test.tsx. My original AS-5/6/7 PASS rested on CI green; it does not transfer to an unproven, currently-red head.

FIX (orchestrator/producer's call; both in-scope — development-limits.test.tsx IS in the packet allowed_paths):
1. Update `development-limits.test.tsx:563` to assert the new copy (e.g. the full `"Draft — pending qualified legal review (not verified)."`, matching report-view.test.tsx:135), OR restore capital "Pending" in the panel — pick one so the copy and both test files agree.
2. Re-run and capture web CI at the corrected head (currently no green evidence exists past be0a7062).

Everything else in the delta is clean: F1 is correct, the jargon-guard test is sound (asserts D-045-R009/D-051/needs_review/within_100ft_of_wide_street/ZR 23-22 never reach the panel), the dropped draft_label/reason/fallback strings remain available behind the CalculationEvidence disclosure, and no api/schema/contract identity moved. Once the development-limits.test.tsx:563 mismatch is fixed and CI is green at the corrected head, my G3 PASS carries.

Orchestrator note: the named fix (option 1, exact wording) was applied at 1c89922c BEFORE this
round-1 message arrived (it crossed with the HJ's identical finding); the round-2 carry
confirmation at 1c89922c + green CI completes the condition.

---

## Delta-attestation round 2 (VERBATIM; delivered ~10:25Z at head 1c89922c — CARRIES)

Delta-attestation for head 1c89922c (rework 75daaebd + F1/F2 + casing fix d532928f). VERDICT: my G3 PASS CARRIES to 1c89922c on code-correctness grounds. One evidence-capture item remains (web CI at the corrected head), which is the orchestrator's to capture, not a code defect.

IDENTITY OUTSIDE THE CHANGED FILES — CONFIRMED UNCHANGED. `git diff a6875731 1c89922c` over all reviewed api/schema/contract code (integration.py, response.py, wide_street_live_provider.py, both schema copies, generated rule_evaluation.ts, rule-evaluation-contract.ts, CalculationEvidence.tsx, contract-versions.test.ts, and the three api test files) is EMPTY — byte-identical. The only code changes in range are the two display files (DevelopmentLimits.tsx, ReportView.tsx) + two web test files (development-limits.test.tsx, report-view.test.tsx) + the already-known sanctioned guard test_scenario_contract.py, plus control-plane/docs/M5-T038 files. No material contract identity moved.

CASING DEFECT I FLAGGED — FIXED. Commit d532928f changed `development-limits.test.tsx` line 563 from `toHaveTextContent("Pending qualified legal review")` (capital, stale) to `toHaveTextContent("pending qualified legal review")` (lowercase), plus a 2-line explanatory comment. All three surfaces now agree on the copy:
- Panel `DevelopmentLimits.tsx:81`: `Draft — pending qualified legal review (not verified).`
- development-limits.test.tsx: asserts substring `"pending qualified legal review"` — contained, passes.
- report-view.test.tsx:135: asserts the full exact `"Draft — pending qualified legal review (not verified)."` — matches.
Grep confirms zero remaining capital "Pending qualified" in any web test at 1c89922c, and no panel assertion references the F2-dropped reason/fallback strings (the only hits are the fixture object's own property definitions at development-limits.test.tsx:543-544, which are inputs, not panel assertions — dropping those strings from the calm panel breaks nothing; they still render behind CalculationEvidence).

F1 + F2 — SOUND, NO NEW DEFECT. F1 (ReportView.tsx:68 now feeds the inspectability-gated `evaluation`) is exactly my advisory fix, a strict tightening; its non-inspectable guard test uses the correct `as unknown as string` invalid-shape idiom. F2 (dropping raw draft_label/reason/fallback jargon from the calm panel, kept behind the evidence disclosure) is honest and correct; its jargon guard asserts D-045-R009/D-051/needs_review/within_100ft_of_wide_street/ZR 23-22 never reach the panel. The DRAFT/not-verified marker remains on the panel.

ONE OUTSTANDING ITEM (evidence, not code). No web CI has been captured at the corrected head. `M5-T037-ci-evidence.md` still records green only at `be0a7062`, which PREDATES the rework and the casing fix. Because the delta is web-only (2 components + 2 vitest suites), web-e2e/vitest must be re-run and captured green at 1c89922c before acceptance — I cannot run vitest locally (thin-client; CI is the web authority). By source inspection the two test suites now agree with the panel copy and should pass, but that must be proven on the pushed head, not asserted from my reading.

BOTTOM LINE: the code delta a6875731→1c89922c is clean and my G3 PASS carries; acceptance is gated only on capturing green web CI at 1c89922c (standard for a web-only delta).

(Orchestrator correction of a reviewer typo for the record: the casing commit's sha is the one recorded in the ledger — the reviewer's "d532928f" refers to the same single-line commit verified by content above.)
