# M0-T159 - G2 producer self-check record (recorded by the orchestrator, role self_check)

Producer: backend-engineer (orchestrator-dispatched subagent) in wt-m0t159 at the CERTIFIED controller base
a5886dab; one in-worktree commit a3f24ff3, cherry-picked to cfc3d22c with every file MATCH by git blob
identity (12 files; the only controller-manifest-tracked change is tools/agent_supervisor/event_drift.py).

- **Scope:** exactly the 12 allowed paths; four NEW 2.1.281 fixtures; older fixtures untouched; lane
  journals byte-identical before/after every probe and suite (sizes + mtimes recorded in the report);
  nothing written under C:\SupervisorController*, wt-controller-src, Program Files config or the lane
  runtime dirs (record-manifest and doctor wrote only under %TEMP%\m0t159).
- **Producer self-checks (report, OBSERVED):** baseline at a5886dab = 3 failed (exactly the three CLI-drift
  live teeth) / 3659 passed / 2 skipped; recert at the frozen candidate = 3662 passed / 2 skipped / 0
  failed (3664 collected); golden pack 42 passed; four re-pointed packs 150 passed; removal-sensitivity
  proven (catalog pointer reverted to 2_1_252 -> 2 teeth RED, restored GREEN); ruff clean;
  verify-controller PASS; non-live doctor PASS 41/41.
- **[ORCH-HARVEST] primary re-run at cfc3d22c (this checkout, Python 3.11.9, repo root):**
  `python -m pytest tools/test_agent_supervisor_capability_probe.py tools/test_agent_supervisor_event_bus.py
  tools/test_agent_supervisor_native_adapter.py tools/test_agent_supervisor_routing_probe.py -q` -> 150
  passed in 63.19s (the ctl24 controller tree carries later supervisor commits than a5886dab; the four
  packs still pass here).
- **Disclosed deviations:** the raw `claude --help` sha changed (every probed flag still supported); the
  mrl_* measured-behaviour facts pinned to 2.1.252 were not re-measured (mocked logic tests, not live
  teeth - a separate task if wanted); the routing-capture script needs PYTHONPATH when run bare.
- **Owner-typed commissioning (presented, never run):** install from the admitted commit after a reviewed
  source_binding re-pin, record + verify the activation manifest, clear-recovery on lane 1, then each
  lane once with --repin-cli-identity (M0-T159-recertification.md section 5).

G2 verdict: PASS (self_check; never independent review - G3 code-reviewer, G4 qa-engineer and the DCV follow).
