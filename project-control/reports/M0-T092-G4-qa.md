# M0-T092 — G4 independent QA review (verbatim reviewer return)

Saved VERBATIM by the orchestrator from the qa-engineer agent-return channel
(report-preservation rule; transport entity-decoding only). Reviewer: qa-engineer
(read-only). Recorded by: orchestrator.

---

# Gate Report

- **Gate ID:** G4 (QA / test-quality)
- **Task ID:** M0-T092 — D-024 Phase D unit F: controller state machine, safe seams, exact-once succession, outage handling
- **Reviewer:** qa-engineer (independent, read-only)
- **Producer:** fable-orchestrator-session
- **Result:** PASS
- **Clean environment/worktree used:** Reviewed the deliverable content at `C:\Users\MLFLL\Downloads\nyc-zoning\ctl24` (deliverable identity b940c90; live HEAD 8234479 carries control-plane records only). Ran pytest myself (writes temp dirs only); ran no git/gh/project_control write commands.

## Acceptance criteria reviewed
Scenario pack S1–S15 in `project-control/reports/M0-T092-controller-succession.md` §1 (the section-16.3 matrix), plus the producer claims: matrix 70/70; drift files green; 13/13 mutants killed; T1 full-suite 2911/3/3 with the three failures being exactly the live drift teeth; composed T2 baseline via change-graph; task outputs present.

## Directive/requirement verification (QA-scoped — the R-ids the matrix exercises)
The exhaustive 65-requirement pass is the directive-compliance-verifier's lane (separate reviewer in the packet). I reproduced the scenario->requirement coverage the matrix claims, at the deliverable identity:

| Requirement cluster | Scenario/tests | Verdict | Reproduced evidence |
|---|---|---|---|
| R028/R030/R031 renewable epoch lease, exact-once succession, boot reconciliation | S2 (6), S3 (4), S5 (5), S7 (4), S8 (5) | PASS | 70/70 matrix run; S2/S7/S8 verbose all PASS; cross-connection CAS single-winner reproduced |
| R026/R027 stop-intent precedence survives restart | S4 (6) | PASS | precedence emergency>graceful>pause + reopen persistence reproduced |
| R066/R067 §7 safe-seam + smallest-complete handoff | S6 (6) | PASS | 11 unsafe conditions each refuse; children-unreconciled seam input reproduced |
| R032 host-restart auto-resume / truthful blocker | S9 (3) | PASS | deterministic share reproduced (live resume owner-gated) |
| R024/R025 Codex preflight fail-closed | S10 (5) | PASS | missing/malformed/mismatched dispatch nothing |
| R033 outage backoff/blocked/bounded-idle | S11 (8) | PASS | bounded attempts, cap, jitter, MAX_IDLE ceiling reproduced |
| R125–R128 Bootstrap Gate 0 | S12 (7) | PASS | added-dir non-equivalence, unknown-MCP fail-closed, R128 adoption reproduced |
| R160/R180 one backend, resume-not-a-seam | S13 (5) | PASS | context-shedding reorients despite verified resume |
| R045/R042 no token pressure / telemetry honesty | S14 (2), S15 (3) | PASS | quota text fails closed; missing usage unknown-never-zero |
| R029 §3 state set | S1 (5) | PASS with note | 16/18 distinctions map to real states; 2 composites asserted tautologically (see ADVISORY-1) |

## Steps independently executed
1. `python -m pytest tools/test_agent_supervisor_controller_succession.py -q` -> **70 passed** (re-run under `-p no:randomly` -> 70 passed; deterministic).
2. `python -m pytest tools/test_agent_supervisor_{capability_probe,event_bus,native_adapter,phase1}.py -q` -> **195 passed, 0 failed**.
3. Verbose S7/S8/S2 -> 15 passed (race, all five crash windows, lease lifecycle).
4. `python tools/modularity_check.py --check` -> **failures 0; warnings 5** (none of them the unit-F files).
5. Read all new modules (`epoch_lease`, `stop_intent`, `outage_policy`, `bootstrap_gate`) and the additive edits (`durable_state.compare_and_swap_state`, `state_machine` +4 states/+17 transitions, `rotation.children_unreconciled`, `turnover_seam`).
6. Grepped the change-graph closure for §4.4; read the committed T1 full-suite log.

## Expected versus actual
- Matrix 70/70 — **confirmed** (70 passed, twice, deterministic).
- Drift files green — **confirmed** (195/195; fixtures now at 2.1.248, matching installed CLI).
- T1 full-suite 2911/3/3, failures = the three teeth — **confirmed**: `M0-T092-full-suite-T1.txt` lines 127–129 name exactly `capability_probe::test_live_reprobe…`, `event_bus::test_s8_live_version…`, `native_adapter::test_live_detection…`, each asserting `'2.1.248' == '2.1.247'`.
- 13/13 mutants killed — **credible and meaningful** (verified by mapping each mutant to a killing test; not strawmen — see below). I could not re-run a mutation engine (no tooling in sandbox); verified by construction.
- Task outputs present/non-empty — **confirmed** (4 new modules + 4 additive edits + test file 1046 lines + report).

## Regression/security/provenance findings
- **Determinism:** every clock is an injected POSIX float and every rng an injected callable; no `time`/`random`/`sleep` imports in the test file. Wall-clock `to_utc_iso()` metadata is stored but never asserted, and CAS compares round-tripped stored dicts (not freshly generated timestamps) — no nondeterminism leaks into the single-winner comparison. No flake across repeated runs.
- **S7 read-before-write interleaving (QA focus #3):** genuinely proven. `test_the_cas_primitive_is_single_winner_across_connections` exercises the real CAS across two `DurableJournal` connections on the same file (`BEGIN IMMEDIATE`, absence-vs-null handled). `test_a_lost_cas_is_a_typed_refusal_never_a_silent_win` models the exact interleaving the epoch-check cannot catch: a `LosingJournal` whose CAS returns False and whose `set_state` raises — proving the loser raises `succession_race_lost` and writes nothing (no succession-log entry). Note: `test_two_racing_successors…` is sequential and exercises the epoch-mismatch guard, not the CAS; the CAS path is covered by the other two.
- **Crash-window matrix S8 (QA focus #5):** all five windows exercised — pending before-effect -> AMBIGUOUS_EFFECT; verified after-effect -> SAFE_CHECKPOINT (resume not permitted); rolled-back journal -> integrity_check `rolled_back`; armed-gate/rotation-incomplete -> `require_ready` refuses post-reopen; lease-commit -> same-epoch resume. Genuine assertions, not tautologies.
- **Mutation meaningfulness (QA focus #4):** the 13 mutants each have a real killing test — live-takeover (S2 predecessor_live), lost-CAS ignored (S7 LosingJournal), CAS-always-wins (S7 cross-connection + null-not-absence), precedence swap (S4), non-owner clears graceful (S4), unlimited retries (S11 attempts_exhausted), backoff cap dropped (S11 capped.delay_for), unknown-cause-fails-open (S11 classify), idle bound dropped (S11 MAX_IDLE+1), unknown-MCP passes (S12), added-dir equivalent (S12), every-transition-legal (S1 illegal raises), children-seam-check dropped (S6). Not strawmen.
- **Change-graph closure (QA focus #6):** sound. `event_drift.py` (the moved `CATALOG_FIXTURE_PATH` pointer) is imported only by `test_agent_supervisor_event_bus.py`; the three new fixtures (`…2_1_248.json`, `native_runtime_detection_…json`, `hook_event_catalog_2_1_248.json`) are referenced only by the three drift test files (grep-verified across `tools/`). The T1->T2 delta therefore closes over exactly the three re-run files; composed with the T1 log (2911 passed + the 3 teeth) the T2 baseline is 2914/0/3.
- **Modularity:** checker passes 0 failures; unit-F files not flagged (rotation.py's 826 raw lines are docstring-heavy; SLOC is under threshold). New modules are small and single-responsibility.
- **Scope signal:** state-count `27` is hardcoded only in `phase1.py:432` (updated 23->27 with an R029/R033 citation) and the new matrix file — matching the producer's grep claim; `cli.py` reads `len(STATES)` dynamically.

## Defects
None blocking. Coverage/robustness notes only:

- **LOW-1** — `epoch_lease.py:91` `expired()` boundary (`>` vs `>=`) is not pinned: no test sets `now == renew_by_epoch_seconds`, so a boundary mutant would survive. One-second semantic; either interpretation is defensible.
- **LOW-2** — `epoch_lease.py:205` idempotent double-`release()` branch (`return stored`) is untested (no test releases twice).
- **LOW-3** — `epoch_lease.py:350–363` `may_dispatch_writes()` reconciliation gating (`unreconciled_children` non-empty / `external_effects_reconciled=False`) is not exercised; only the `OWN_LEASE_LIVE` default-True and `OTHER_LEASE_LIVE`-False paths are tested.
- **ADVISORY-1** — `test_agent_supervisor_controller_succession.py:140–141`: two of the 18 R029 distinctions ("correcting", "temporary lower-model bridge") are asserted tautologically (the test checks its own MAPPING string contains `"composite:"`); no test exercises the journaled REVISE decision or the `effective_model` bridge record. Documentation-backed in report §4.1, but not hard-tested.
- **ADVISORY-2** — the clean T2 full-suite (2914/0/3) is argued via the composed change-graph, not a single captured run (two full runs were externally stopped). Closure is sound; I did not re-run the 32-minute suite. Recommend the orchestrator confirm CI green at frozen SHA b940c90.
- **ADVISORY-3** — `acquire_first()` refusal against an already-released/expired lease record is documented but only the live-lease refusal is directly tested.

## Required rework
None to pass G4. The LOW/ADVISORY items are optional hardening — consider folding a boundary test (`now == renew_by`), a double-release test, and a `may_dispatch_writes` reconciliation-gating test into the matrix, and letting the orchestrator attach the CI whole-suite confirmation at b940c90 for ADVISORY-2.

## Reviewer conclusion
Every producer claim in scope for a QA gate reproduces at the deliverable identity: matrix 70/70 (deterministic across two runs), drift files 195/195, all five S8 crash windows and the S7 single-winner CAS genuinely exercised, 13 meaningful mutants each with a killing test, a sound change-graph closure behind the composed full-suite baseline, and all named outputs present and non-empty. Injected clocks/rng make the suite deterministic; no vacuous or over-mocked passes were found beyond the two documented S1 composite tautologies (ADVISORY-1). No blocking defect. The exhaustive per-requirement directive pass over the 65 applicable R-ids remains the directive-compliance-verifier's separate gate.

**VERDICT: PASS**
