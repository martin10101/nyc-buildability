<!-- Orchestrator capture: code-reviewer G3 return for M5-T045 saved VERBATIM
     (transport entity-decoding only). Pinned at frozen head 2b851c18; reviewer hashed
     all eight material files and reproduced the focused + consumer suites. -->

# Gate Report

- **Gate ID:** G3 (independent code review)
- **Task ID:** M5-T045 — Condo billing-BBL live wiring (DB-029)
- **Reviewer:** code-reviewer (read-only)
- **Producer:** backend-engineer
- **Result: PASS**
- **Clean environment/worktree used:** Reviewed at branch `candidate/D-024-mrl-option-b`. The eight material source files were independently hashed (LF-normalized sha256) against `M5-T045-source-sections.md` — **all 8 MATCH**, confirming the on-disk surface is byte-identical to frozen head `2b851c18` / cherry-pick `28082288` (CI-proven at `21ee7379`). Reviewed content, not a bundled diff.

## Acceptance criteria reviewed

All ten scenarios (AS-1..AS-10), including the AMENDED AS-3 and AS-8 per the recorded ORCH-SCOPE-DISPOSITION. Focused on the six surfaces in the review brief: the seam-policy module, the live-provider pre-lookup, the profile crosscheck helper, the display increment (screen + printed brief), the connector riders, and change-set/disposition consistency.

## Directive/requirement verification

| Requirement ID | Reviewed content identity | Verdict | Reproduced evidence |
|---|---|---|---|
| D-066-R001 (code-graph nav block; verify-in-source discipline) | frozen `2b851c18` (digests match) | PASS | Nav-block consumer claims independently re-derived: `resolve_condo_billing`/`condo_resolution_report`/`condo_base_lot` appear ONLY in the three in-scope modules + tests (Grep of `services/api/app`); `build_live_substrate` route consumer `api/v1/rule_evaluation.py` and `test_rule_evaluation_api.py` pass unbroken (55 passed). No forbidden consumer touched. |
| D-073-R006 (records-vs-allowances; records never presented as computed allowances) | frozen `2b851c18` | PASS | Backend `condo_resolution_report` (`zoning_crosscheck.py:524`) emits multi-lot base lots as `resolution:"unresolved"` RECORDS, never a chosen answer; web `condoWithholdsAllowances` (`PropertyOverview.tsx:116`) is a fail-safe allow-list; screen (`PropertyOverview`) and brief (`ReportView`) share one guard. Reproduced by the focused suites + inspected source. |

Note: the authoritative in-regime requirement verification is the independent `directive-compliance-verifier`'s `verification.json`; the above is the code-level corroboration within my G3 scope.

## Steps independently executed

From `services/api` (packet-specified cwd), reproduced offline:
- `python -m ruff check` on the four reviewed api modules → **All checks passed! (exit 0)**
- `pytest tests/connectors/test_dtm_condo_soda.py tests/connectors/test_condo_base_lot.py tests/spatial/test_live_provider.py tests/profile/test_ztldb_crosscheck.py -q` → **124 passed (exit 0)**
- `pytest tests/profile/test_wave_integration.py tests/api/test_rule_evaluation_api.py -q` (named consumer suites) → **55 passed (exit 0)**
- From repo root: `python tools/modularity_check.py --check` → **exit 0** (warnings only; the sole reviewed-file signal is `dtm_condo_soda.py` justify-band `review_signal`, cohesion-justified in producer report §4; no warning on the other three reviewed files).
- LF-normalized sha256 of all 8 material files vs the stored evidence table → **8/8 MATCH**.

Web tests were not run locally (thin client); CI evidence (`M5-T045-ci-evidence.md`) shows the web-e2e-inclusive CI run **35431367744 success** at head `21ee7379`, the first head covering this material.

## Expected versus actual

- **Seam module** (`services/api/app/connectors/condo_base_lot.py`): typed exhaustive outcome vocabulary (`OUTCOME_*` :75-79); frozen `CondoResolution` dataclass with `is_pass_through`/`substitutes_base_lot`/`is_fail_safe` predicates; single/multi split `_from_result` :237-252; unresolved/unknown-status → fail-safe `OUTCOME_UNRESOLVED` :254-270; typed transport error → `OUTCOME_ERROR` :186-198; malformed/non-billing BBL → pass-through with zero I/O :163-182. A degenerate `RESOLVED`-with-empty-set defensively routes to a multi-lot fail-safe. `resolved_base_bbl` is set ONLY on single — a consumer physically cannot pick one lot from a multi-lot set. Matches contract. 270 lines, well under warn threshold; clean single responsibility (policy seam; transport stays in `dtm_condo_soda`). Seven modularity boundary answers hold against the actual diff.
- **Live provider** (`spatial/live_provider.py:237-310`): condo pre-lookup runs BEFORE the ztldb/lot fetch; `is_fail_safe` → `None` + payload-only fail-safe log; `substitutes_base_lot` → base lot flows to BOTH downstream fetches (proven by `test_live_provider.py` asserting `ztldb_calls == [(_BASE_LOT, CID)]`, `lot_calls == [(_BASE_LOT, CID)]`); non-condo → substrate = input, byte-identical (`ztldb_calls == [(BBL, CID)]`). New `condo_resolver` param is keyword-only with a module-seam default — backward-compatible; the route's `default_live_substrate` signature is unchanged. Payload-only logging preserved (digit BBLs / condo_key / own correlation id only; no `str(exc)`). Any unexpected resolver exception is caught by the outer fail-safe boundary → `None`.
- **Profile crosscheck** (`profile/zoning_crosscheck.py:524-633`): maps the typed outcome onto the EXISTING contract-1.3.0 conflict/note channels; machine outcome token `condo_resolution: [<OUTCOME>]` (:511-521) equals `CondoResolution.outcome`, so the two sides cannot drift; multi-lot → `resolution:"unresolved"` conflict listing every base lot as a record + note; resolved-single/unresolved/error → notes; pass-through → empty report. Field `condo_base_lot_resolution` is deliberately outside the builder's identity/critical gating set (the real fail-safe is the live-provider seam). No contract-shape change; `additional_conflicts`/`additional_notes` are pre-existing additive builder params (`builder.py:618-619,767,784`), so `builder.py` was not modified.
- **Display** (`PropertyOverview.tsx`, `ReportView.tsx`, `AnalysisIdentityNotice.tsx`): `condoWithholdsAllowances` is a genuine fail-safe allow-list — allowances survive ONLY when there is no `condo_base_lot_resolution` conflict AND every `condo_resolution` note carries the `resolved_single_base_lot` token; a multi-lot conflict, an unresolved/error note, a MISSING/malformed token (outcome null), and an UNKNOWN future token all withhold. I traced every branch: **no fail-open path exists** (the OK token is emitted only by `OUTCOME_RESOLVED_SINGLE`; multi-lot is independently caught by the conflict channel). Screen and brief consume the SAME exported guard and `CondoResolutionRecords` component — **one data path, so they cannot disagree**. `AnalysisIdentityNotice` renders neutral entered-vs-analysed wording, infers no billing/base-lot relationship, and withholds on any differing/absent identity; it is wired on the screen via the pre-existing `ArchitectEntry.tsx:169-170` and on the brief via `ReportView.tsx:75-76` (its public interface is unchanged, so the out-of-scope `ArchitectEntry` consumer is unaffected).
- **Connector riders** (`dtm_condo_soda.py`): `_ERROR_CODE_SAFE_RE.fullmatch` (:354) with negative test; optional `$limit` off-by-default / non-positive-fails-closed (:359-373) with tests; response-side `condo_key` `fullmatch` guard before URL interpolation (:737) with a negative test; documented `invalid_component` code (:818) asserted against `bbl.py` vocabulary; canonical-normalizer consistency test. Each rider closed by a named test.
- **Registry draft** (`docs/research/source-registry-drafts/dtm-condo.json`): two records (p8u6-a6it, eguu-7ie3) mirroring the committed `ztldb.json` top-level shape field-for-field (same 19 keys incl. `last_successful_ingestion: null`); freshness mechanism (`X-SODA2-Truth-Last-Modified == rowsUpdatedAt`) documented; column counts match `CONDO_COLUMNS` (9) / `UNIT_COLUMNS` (16). No invented top-level keys.
- **Real null-billing fixture** (`test_dtm_condo_soda.py:180-195`): byte-faithful 8-key (103343) and 7-key (100355) bodies with `condo_billing_bbl` KEY-ABSENT (never explicit null); parametrized key-absence test asserts `"condo_billing_bbl" not in record`; multi-lot-null-billing is a clearly LABELED synthetic (documented citywide negative, 28==28).

## Evidence paths

Reviewed modules and tests as listed in the return (seam module, live provider, profile crosscheck, connector, three web components, registry draft, and the seven test files).

## Human-style walkthrough findings

Not the primary lens for G3 (HJ reviewer covers AS-8). At the code level the reachable AMENDED display is coherent: the entered-vs-analysed identity record renders on both surfaces from one component; the condo records-view logic is fail-safe and never presents a reference number or a computed figure as an allowance (the multi-lot test asserts the rendered text contains no `\d+\.\d+`).

## Regression/security/provenance findings

- **Regression:** non-condo and flag-off behavior byte-identical (proven by `test_m5t045_non_condo_passthrough_default_resolver_byte_identical` and the S1 zero-call tests); named consumer suites green. No forbidden path touched — `builder.py`, `adapter.py`, `_contract_schemas/`, `rules/`, `packages/contracts/` do not import the new symbols (Grep-confirmed), consistent with the change-set proof in the disposition.
- **Security:** payload-only logging discipline preserved on both new log helpers (no `str(exc)`, only digit strings + own correlation id); response-side shape guards (`fullmatch`, `re.ASCII`) fail closed on Unicode-digit / trailing-newline / prefix-injection inputs; app token never logged.
- **Provenance:** every outcome carries source_id, dataset_ids, retrieved_at, condo_key, per-query provenance, and the divergent-zoning notice; profile notes/conflicts embed the provenance suffix. Fail-closed everywhere; uncertainty never collapsed; divergent zoning never adjudicated.

## Defects

None blocking.

## Required rework

None.

## Advisory (non-blocking) — for the recorded DB-031 / route-propagation follow-up

- **A1 — `condo_resolution_report` has no production caller (dead-until-wired).** The profile note/conflict channel it feeds is populated only by injected fixtures; the four profile-build routes still call `build_property_profile(...)` without `additional_conflicts`/`additional_notes`. Therefore the condo records-view portions of AS-3/AS-4/AS-8 are proven at the unit level only, and the production display renders nothing for condos today (non-condo byte-identical). This is honestly disclosed (producer report §6/§10) and explicitly scoped out by the ORCH-SCOPE-DISPOSITION; the reachable surfaces — the live-provider fail-safe (→ None → professional review) and the entered-vs-analysed identity notice (via `ArchitectEntry`/`ReportView`) — ARE wired. Acceptable for this packet; the successor must wire the note propagation to make the records-view reachable.
- **A2 — guard-coherence risk to reconcile in the successor.** When resolved-single substitution is wired end-to-end, the analysis runs on the base lot, so `evaluated_input.bbl` (base lot) will differ from the entered billing BBL. `AnalysisIdentityNotice` withholds on ANY identity mismatch, which would suppress the very allowances the `resolved_single_base_lot` allow-path is designed to permit — the two fail-safe guards can disagree once wired. Not a defect here (nothing wired; both are individually fail-safe), but the follow-up must define which BBL becomes `profile.identity.bbl` for a substituted condo, or the resolved-single allowance path stays unreachable.
- **A3 — unit-BBL (1001-6999) pass-through** dead-ends at the zoning-lot lookup rather than resolving; documented as a later increment and fail-safe (no fabrication). Advisory.
- **A4 — live-provider resolved-single substitution is recorded only in a log line**, not in the returned substrate record (`LotIntersectionRecord.bbl` becomes the base lot with no carried record of the entered billing BBL). Not user-facing today (live path is flag-disabled); ties into A1/A2 for the successor.

## Reviewer conclusion

**PASS.** The in-scope code is fail-safe throughout (multi-lot / unresolved / typed-error → absent substrate → professional review; divergent zoning never collapsed; no fabricated base lot), provenance is carried end-to-end, the display guard is a genuine fail-safe allow-list with no fail-open path and a single shared data path across screen and brief, the registry draft mirrors the committed shape, the real null-billing fixture is byte-faithful with key-absence tolerance, and all riders are closed with named tests. No forbidden path was touched and no contract shape changed. All reproduced checks are green (ruff clean; 124 + 55 focused/consumer tests pass; modularity exit 0), the eight material digests match the frozen evidence, and CI is green on the covered head. The ORCH-SCOPE-DISPOSITION is a legitimate application of the packet's own STOP+discovery rule and is honestly recorded; the remaining reachability work is transparently routed to DB-031 / a follow-up. The four advisory items are future-packet concerns, not defects in this packet.
