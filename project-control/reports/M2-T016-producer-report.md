# M2-T016 — Producer report (AOS §6) — Survey review UI, correction workflow, profile integration (Packet C)

**Milestone:** M2. **Producers:** product-design-director (design), backend-engineer (U1 API), frontend-engineer (U2 UI); orchestrator integration. **Directive regime:** D-010 (`directive_refs: D-010:ALL`). First product task under the lean operating process. **Integration branch:** `task/M2-T016-survey-review`.

## Objective delivered
The survey review experience: a reviewer opens a `needs_review` survey, sees extracted lines/boundaries/labels/measurements overlaid on the original, accepts/corrects/rejects each material item with a full audit trail, dependent buildability conclusions rerun, unresolved items propagate visibly as blocked/provisional, and only a defined professional role may grant `professionally_confirmed` (never an AI/automatic path).

## Deliverables (this branch)
- **`docs/SURVEY_REVIEW_WORKFLOW.md`** — canonical workflow spec: 6 states reconciled 1:1 with shipped `DocumentState`; 12-edge transition table (from `ALLOWED_TRANSITIONS`); two state layers (document lifecycle vs per-fact confirmation); professional-confirmation mechanism (role identity flagged as a pending owner/Tier-D decision, rec. default NY-licensed PLS); correction/audit model; rerun + downstream honesty; profile 1.4.0 **consume-only** integration (authoritative survey-geometry promotion flagged as a new contracted decision — STOP); full UX contract. §12.1 reconciles the confirmation rule with the implemented slice.
- **Backend (`services/api/app/documents/review_{actions,authz,events}.py` + tests)** — 7 pure-Python handlers (read; accept/correct/reject fact; confirm/reject/reopen document), per-action authorization (closed principal/role enums, fail-closed), append-only correction history, audit events, dependent-recalc trigger, immutability, `ReviewStore` Protocol seam. Every gated edge routes through the shipped `promotion_gated_transition`; raw `transition()` is never called from a handler; the security machinery is unmodified (called only). **190 tests pass; ruff 0.13.0 clean.**
- **Frontend (`apps/web/src/{lib/surveyReview,components/survey-review,app/survey/review}` + tests)** — flag-gated review screen: overlay (with non-color status cues), per-item accept/correct/reject, explicit two-layer state visibility, blocked/provisional indicators, non-dismissible conflicts, immutable side-by-side correction history, capability+H5-gated confirm surfacing blocking rejected facts, recovery + a11y (live region, keyboard, no color-only status). Server communication isolated in a typed api-client reconciled 1:1 to the backend contract.

## Independent gates (all PASS; verbatim reports in `project-control/reports/`)
- **Backend U1 (`57d8574`):** G3 code-review PASS (`M2-T016-backend-G3-code-review.md`) → blocking **C1** (rejected fact silently overwritten to confirmed) → reworked → **G3 delta PASS, C1 RESOLVED** (`…-G3-delta-review.md`); G5 security PASS (`…-G5-security-review.md`) → **F1** (silent post-confirmation edit) → reworked → **G5 delta PASS, F1 RESOLVED** (`…-G5-delta-review.md`). Both reproduced 190 tests, gate modules byte-unchanged, forge/principal defenses intact.
- **Frontend U2 (`dc8c5de`):** human-journey PASS (`…-frontend-human-journey-review.md`) → required **F1** (dominant-action never points to Confirm) + **F3** (stale reload loses draft) → reworked → **human-journey delta PASS, F1+F3 RESOLVED** (`…-human-journey-delta-review.md`, honesty core intact & stronger); G3 code-review **PASS** (`…-frontend-G3-code-review.md`) — the hand-rolled optimistic-concurrency fingerprint is **fail-safe** (worst case a retryable `concurrent_review_modification`, never corruption), reconciliation matches with no drift, no XSS/secrets/new-deps.

## Honesty / safety invariants proven
No path lets unconfirmed or rejected evidence read as authoritative; no silent default of an unresolved item; no auto/AI path to `professionally_confirmed`; a professionally-rejected fact **blocks** confirmation (never overwritten); post-confirmation edits require an audited reopen; originals immutable; corrections append-only + attributed; conflicts non-dismissible; profile contract 1.4.0 unchanged (consume-only); no production storage/auth assumed (B-001-honest).

## Engineering judgment / owner decisions surfaced (do not block this build; mechanisms built)
1. **Professional-confirmation role identity** — which real-world license may grant `professionally_confirmed` is a **pending owner / qualified-human (Tier-D legal) decision** (mechanism built role-gated; rec. default NY-licensed PLS for boundary facts).
2. **Authoritative survey-geometry as a profile input** — profile 1.4.0 has no home for it; promoting confirmed survey geometry as authoritative is flagged as a **new contracted decision (STOP)**; consumed here only as honesty signals.

## Tracked follow-ups → survey-review HTTP-route + production-`ReviewStore` binding task (blocked by B-001)
- Backend F2 (atomic multi-fact confirm), F3 (CAS on the confirmation field), F5 (route MUST populate the principal from the authenticated channel — never the payload — and validate the digest + compute promotion verdicts solely server-side).
- Read-model assembly the route owes: per-fact `history_fingerprint` (so the FE echoes it and the hand-rolled reproduction is deleted), a per-action principal-capability surface (SC-S3 UI clarity, finding J1), per-check `expected`/`observed` (full SC-S6 numeric detail), a review-inbox endpoint; plus a Python↔JS canonical-string CI cross-vector once the fingerprint is exposed.

## Verification / CI notes
Python 3.12 target (repo `requires-python>=3.12`; PEP-695). Local sandbox is 3.11 → the `documents` pytest suite cannot fully collect (15 pre-existing collection errors in `units.py`, unrelated); the 3 new backend test files are 3.11-clean and pass (190). Frontend vitest/Playwright not runnable locally (thin client). SC-S8 (full CI green) verified via CI on this branch's PR. ruff 0.13.0 clean on the backend slice.
