# M5-T044 — G3 code review (verbatim reviewer return; transport entity-decoding only)

Saved by the orchestrator from the code-reviewer agent return, 2026-09-19 (seq-118 wave,
pinned at submission sha 20c597c1). Nothing added or removed beyond decoding transport
HTML entities. Per gate-verdict semantics the verdict records as PASS with Finding 1 as a
BLOCKING-for-acceptance required correction; the orchestrator applies it as a tagged
[ORCH-CORRECTED per M5-T044-G3 F1] edit after the in-flight reviewers return, then reworks/
resubmits for delta-attestation.

---

I have everything needed. All findings verified in source at the pinned content identity. Here is my gate report.

---

# G3 Code Review Gate Report — M5-T044

**Task:** M5-T044 — Condo resolver pre-wiring hardening (DB-029 a, d–i)
**Reviewer:** code-reviewer (independent; read-only)
**Reviewed identity:** submission sha `20c597c1`; **material commit `1fd07e9a`** (3 files, +583/−37). Live branch HEAD is `11bed5f7` (disjoint peer/M5-T043 material); task files are byte-stable, so the review is faithful to the submission.
**Gate:** G3 code review · **Date:** 2026-09-18

## Content identity (verified)
Both material files on disk are byte-identical (LF-normalized sha256) to `1fd07e9a`:
- `dtm_condo_soda.py` = `d2c289df…9e02` (commit == disk)
- `test_dtm_condo_soda.py` = `3a1082e1…30f` (commit == disk)

## VERDICT: PASS — with one blocking-for-acceptance required correction (Finding 1)

The security-critical must-fix (G5 F1/F2/F3 — the wiring precondition) is fully and correctly closed with genuine negative tests. One contracted rider, item (d)/G3 #1, is closed for `resolve()` but silently left incomplete for `resolve_by_condo_key`. Everything else is closed, correct, and preserving.

Per this repo's gate-verdict semantics (`.claude/rules/project-control.md`), record **PASS**; **Finding 1 is BLOCKING for acceptance** until the one-line correction lands or is explicitly disclosed-and-deferred to the wiring packet.

---

## Origin-ruling closure status (explicit)

- **G5 F1 — CLOSED.** `_STRICT_BBL_RE` / `_CONDO_KEY_RE` are now `re.compile(r"\d{10}", re.ASCII)` / `r"\d{6}"` matched with `.fullmatch` at input (`:350-359` classify_lot; `:785` resolve_by_condo_key) and response sides. Canonical value interpolated at every URL: `canonical_bbl = normalize_bbl(bbl).canonical` (`:619`) at billing `:633` and unit `:674`; the two condo_key URL sites (`:717`, `:795`) interpolate a value that is `_CONDO_KEY_RE.fullmatch`-guarded immediately before interpolation (`:708`, `:785`), so no raw/unvalidated input reaches any URL. Grep confirms exactly four query-URL f-strings (`:633/:674/:717/:795`) — none interpolate raw input. The G5 reproductions now fail closed: `resolve('3022647515\n')`, trailing-space, and Unicode-digit inputs raise `BBLValidationError("non_numeric")` before I/O with zero transport calls (`test:508`, `_never_called`, reproduced green).
- **G5 F2 — CLOSED.** `_collect_base_lots` response-side base-BBL guard now uses `_STRICT_BBL_RE.fullmatch(base)` (`:514`); a Unicode-digit / trailing-`\n` / trailing-space `condo_base_bbl` raises `SchemaDriftError` (`test_a_response_base_bbl_leaky_shape_rejected`, `test:565-572`). A new response-side condo_key guard (`:708`) additionally refuses a leaky-shape condo_key from a unit response before the expansion URL.
- **G5 F3 — CLOSED.** Negative parametrizations for trailing-newline, trailing-space, and Unicode-digit BBL (`test:500-513`) and condo_key (`test:516-522`), asserting typed refusal + zero transport.
- **G3 #1 — PARTIALLY CLOSED (see Finding 1).** `resolve()` stamps post-response, per query (`:638` billing, `:676` unit, `:721` expansion; NOT_A_CONDO stamps at classification with a disclosed comment `:604-606`; the old pre-I/O stamp is removed). `test_d` (`test:576-596`) proves two distinct post-response stamps with an advancing clock. **But `resolve_by_condo_key` still stamps pre-response** — see Finding 1.
- **G3 #2 — CLOSED.** `classify_lot` raises `"non_numeric"` (`:358`) and `resolve_by_condo_key` raises `"invalid_component"` (`:787`); both are in the documented `bbl.py:44-46` vocabulary; ad-hoc `"wrong_shape"` is gone. `test_e` (`test:600-605`) asserts the code is documented and `!= "wrong_shape"`.
- **G3 #3 — CLOSED.** New `INPUT_KIND_BBL/INPUT_KIND_CONDO_KEY` (`:141-143`), `CondoBaseLotResult.input_value` + `input_kind` + `lot_class: str | None` (`:294-299`); condo_key results carry `input_kind=condo_key`, `lot_class=None` (`:829-831`, `:844-846`) instead of overloading `input_bbl`/`not_a_condo`. Clean rename verified leaf-safe: repo-wide `.py` grep for `dtm_condo_soda`/`input_bbl` returns only the module + its own test; no `.input_bbl` remains in the test; `CondoBaseLotResult` has no serialization method emitting `input_bbl` (`to_payload` at `:230` belongs to the error class). `test_f_*` (`test:609-638`) cover resolved/unresolved/BBL identities.
- **G3 #4 / (g) — CLOSED.** `test_g_*` (`test:645-701`) force 400-drift vs 400-other, non-JSON/non-array/non-object bodies, and terminal 429/timeout/network via `_AlwaysTransport`; `test_g_rate_limited_terminal` asserts `transport.calls >= 2` (retry budget genuinely spent — not a happy-path shadow).
- **(h) G4 #3 — CLOSED.** `test_h_unit_empty_is_unresolved_single_query` (single `unit_bbl` provenance) and `test_h_unit_without_condo_key_returns_direct_lot_no_expansion` (RoutedTransport would `AssertionError` on any expansion URL → proves no expansion; asserts the incompleteness note).
- **(i) G4 #4 — CLOSED.** `test_i_unknown_columns_note_is_recorded` and `test_i_multiple_condo_keys_and_numbers_notes` (ambiguous identifiers surfaced, `condo_key`/`condo_number` left `None`, never guessed).

## Preservation (criterion 2) — CONFIRMED
The 26 pre-existing tests are preserved and pass alongside the 28 new ones (54 total). Billing SET, unit→condo_key expansion, condo_key fallback, and honest UNRESOLVED are behaviorally unchanged; under `FIXED_CLOCK` the new post-response stamping yields byte-identical `retrieved_at` (e.g. `test_c1` still asserts `2026-09-18T12:00:00Z`). No preserved test asserted `input_bbl`, so the rename does not alter any prior assertion.

## Scope / dependency / modularity
- **Exactly 3 files** in `1fd07e9a` (module, test, report), all in `allowed_paths`. Module remains a **leaf** (zero consumer imports). **No new dependency** (stdlib + `app.connectors.bbl` + `app.resilience.transport`; the added test imports `TransportFailure/TransportTimeout` already exported).
- **(j) $limit deferral is disclosed, not silent** — producer report §(j) and "Deferred to the wiring packet" route it to the wiring packet per D-069 (ADVISORY 3 below notes the asymmetry with the undisclosed (d) residual).

---

## FINDINGS

**1. BLOCKING (required correction; low actual severity) — `resolve_by_condo_key` still stamps `retrieved_at` BEFORE I/O, so G3 #1 / item (d) is not fully closed and criterion (3) is violated on a query path.** `dtm_condo_soda.py:792` computes `retrieved_at = _rfc3339(clock())` *before* the single `_fetch_rows` call at `:796`; the resolved/unresolved provenance entries (`:810`, `:834`, `:852`) then carry that pre-request stamp. The M5-T042 G3 #1 finding explicitly named this site (M5-T042 `:739`) alongside `resolve()` `:569`, and this packet's scope (d) is "retrieved_at stamped **after each successful response, per query**." `resolve()` was fixed; `resolve_by_condo_key` was not, and the residual is **undisclosed** — the producer report's (d) section lists only `:638/:676/:721` and silently omits this path (contrast the disclosed (j) deferral). Impact is bounded (provenance-timestamp precision on a no-consumer leaf, deterministic under a fixed clock, still honest/non-fabricating), which is why it does not fail the whole gate — but it must not be certified as closed. *Remediation (one of):* move the stamp to after `_fetch_rows` (mirroring `:638`) and add a post-response-timing test for `resolve_by_condo_key`; **or** the orchestrator explicitly binds this residual to the wiring packet with a disclosed note (parallel to how F1 itself was deferred in M5-T042). Silent partial closure is not acceptable.

**2. ADVISORY — module at 910 SLOC (justify tier).** `modularity_check --check` exits 0 (justified: single cohesive responsibility, comparable to sibling SODA connectors). Above the 750 justify threshold, under the 1,000 hard threshold. Named future extraction seam (shared `soda_errors`/`soda_request` helpers) still applies if it grows further; keep as one module now. Not blocking.

**3. ADVISORY — canonical-value asymmetry for condo_key URLs.** BBL sites use `normalize_bbl(bbl).canonical`; condo_key sites (`:717`, `:795`) interpolate the `fullmatch`-validated raw condo_key (no condo_key normalizer exists). This is safe — `re.ASCII` `fullmatch(r"\d{6}")` guarantees a clean canonical 6-digit shape — so it is not a raw-input leak; noted only for interpolation-consistency documentation.

**4. ADVISORY — `"non_numeric"` used for all `classify_lot` shape failures.** A wrong-length all-digit input (e.g. 9 digits) reports `"non_numeric"` where `"wrong_length"` would be more precise. Both are documented codes, so item (e) is satisfied; minor semantic imprecision only.

---

## Read-only commands run
- `git show --stat 1fd07e9a`; `git show 1fd07e9a -- services/api/app/connectors/dtm_condo_soda.py`
- `git rev-parse HEAD` (`11bed5f7…`); `git show 1fd07e9a:<file> | tr -d '\r' | sha256sum` vs on-disk LF-normalized digest for both files (identity match)
- `Grep` URL f-strings / `CONDO_BASE_URL|UNIT_BASE_URL|resource/` in the module (4 query sites); `Grep dtm_condo_soda|input_bbl` over `**/*.py` (leaf: module + test only); `Grep \.input_bbl|input_bbl=` in the test (none)
- `Read` of `M5-T044.json`, producer report, `M5-T044-ci-evidence.md`, `M5-T042-G5.md`, `M5-T042-G3.md`, `bbl.py` (code vocabulary `:44-46`), and the module/test ranges cited above
- `cd services/api && python -m pytest tests/connectors/test_dtm_condo_soda.py -q` → **54 passed** (reproduced)
- AS-8 committed-head CI relied on orchestrator-captured evidence (`M5-T044-ci-evidence.md`: CI api job `35412920210` success at `a380a956`; `1fd07e9a..a380a956` empty over `apps/ services/ packages/`).

**Recommendation to orchestrator:** record **G3 = PASS**. Findings 2–4 are non-blocking. **Finding 1 is a blocking-for-acceptance required correction**: either apply the one-line `resolve_by_condo_key` post-response stamp + test, or record an explicit disclosed deferral of that residual onto the wiring packet — do not accept it as a silently-closed (d)/G3 #1.
