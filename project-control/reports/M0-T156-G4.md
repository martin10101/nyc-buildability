# M0-T156 — G4 QA Gate Report (verbatim)

Orchestrator preservation note: report returned by the independent qa-engineer agent
("G4 QA review M0-T156") on 2026-09-12 and preserved verbatim below. Reviewer was
read-only, worktree-isolated; frozen review identity: producer commits 2bdae13f + 012ba064.
Verdict: PASS (findings 7/8/9 INFO, finding 10 LOW non-blocking; no required rework).

---

I have completed all independent verification. Here is my G4 QA gate report.

---

# G4 QA Gate Report — M0-T156 (Directive-registry digest normalization CRLF/LF)

**Reviewer:** qa-engineer (independent, read-only)
**Task:** M0-T156 — "Directive-registry digest normalization (CRLF/LF) — retire the control-plane CI red"
**Frozen reviewed identity:** producer commits `2bdae13f` (material) + `012ba064` (report-only); CI head `012ba064`
**Rubric:** acceptance scenarios AS-1..AS-6 (packet)
**Method:** worktree-isolated; commits inspected from shared object store; frozen tree exported via `git archive 2bdae13f` and re-run in scratchpad; all conclusions re-derived from source, not from the producer's report.

## Verdict basis — what I ran (all at the reviewed identity)

| Check | Result |
|---|---|
| Independent recompute of all 6 normalized digests from frozen blobs | ALL 6 == recorded manifest value (full 64-hex) AND CI-reported prefix; line-ending-independent |
| `validate_directive_compliance.py --check` on extracted **LF** tree (= CI side) | EXIT 0 |
| `validate_directive_compliance.py --check` on synthesized **CRLF** tree (= local Windows side) | EXIT 0 |
| Red-half: normalization monkeypatched to identity, CRLF tree | 316 c2/c14 digest mismatches (fix is load-bearing) |
| `test_directive_compliance.py LineEndingNormalizationTest -v` | Ran 3, OK (7.8s) |
| `test_directive_compliance.py NegativeValidatorTests.test_c2_s6_source_rewritten_without_hash_change RequirementsBodyDigestTest.test_body_edit_detected` | Ran 2, OK (pre-existing tamper guards intact) |
| `test_project_control.py` | EXIT 0 (all 23 groups) |
| `test_directive_reminder.py` / `test_readonly_agent_guard.py` / `test_agent_dispatch_guard.py` | EXIT 0 / EXIT 0 / EXIT 0 |
| Frozen-identity: any commit after `012ba064` (through branch tip `038b5e6d`) touching the 8 allowed_paths | NONE (git log range empty) |
| Byte-identity `2bdae13f`..`012ba064` over `tools` + `directives` | empty diff (identical) |

## Numbered findings

1. **[PASS / no defect] AS-1 red/green is genuinely load-bearing via an independent oracle.** `test_crlf_lf_and_lone_cr_hash_identically` computes `expected` as `hashlib.sha256(b"line one\nline two\n")` — a literal-LF oracle *independent* of the production helper — then asserts LF, CRLF, and lone-CR all equal it, plus `assertNotEqual(raw CRLF hash, expected)`. Red walk with normalization removed: `assertEqual(sha256_text_artifact(lf), expected)` still passes (lf is already LF), so the **first** assertion to fail is the second one — `assertEqual(sha256_text_artifact(crlf), expected)` — exactly the load-bearing case. Because the oracle is independent, an over-forgiving mutant (e.g. stripping spaces) is also killed here (it wouldn't equal the space-bearing oracle). Confirmed running green.

2. **[PASS / no defect] AS-2 representation-flip test is correctly scoped and cannot false-pass.** `test_representation_flip_validates_clean` guards `assertNotEqual(flipped, raw)` (so a no-newline fixture fails loudly rather than silently), and its clean-validation assertion filters specifically `"source-001.md" in e and "digest mismatch" in e` before asserting `== []`. A real c2 error for that file is formatted `"<did>: source 'source-001.md' digest mismatch ..."` and contains both substrings, so it **would** be caught — the test cannot pass while a genuine content mismatch exists. Independently confirmed: I ran `validate --check` EXIT 0 on both the LF-extracted real registry (CI side) and a fully-CRLF-converted copy (local side). CI control-plane green is corroborated (see finding 8).

3. **[PASS / no defect] AS-3 — all six re-stamped digests verified by independent recomputation.** Reading each artifact's frozen blob bytes and computing `sha256(replace(\r\n,\n).replace(\r,\n))`:
   - D-032 source-004-amendment.md → `3704add2…7f3533` — matches recorded + CI prefix
   - D-032 requirements.json → `d8158188…983888` — matches
   - D-033 source-002-amendment.md → `d0ae01b4…2506bf` — matches
   - D-033 requirements.json → `d2876943…3af2fd5` — matches
   - D-034 requirements.json → `dfab73c0…a31e5c` — matches
   - D-038 requirements.json → `0e9de4cc…4c9b34` — matches
   Every value equals both the recorded manifest field and the CI-reported "actual" prefix, and each is line-ending-independent (CRLF-normalized == LF hash).

4. **[PASS / no defect] AS-4 — sources immutable, append-only, byte-preserved.** Producer commit `2bdae13f` changes only: the 3 tools files, the 4 manifests, and the report. Each manifest diff = the intended digest value(s) + `updated_at` + exactly one appended `digest_normalization_restamp` audit entry (D-032/D-033 = 2 digests each incl. an amendment source digest; D-034/D-038 = 1 requirements digest each; 6 total). **Zero** byte changes to any source-*.md, amendment, requirements.json, verification.json, or index.json; `locked_requirement_ids` untouched. Byte-format preserved per file (D-032/033/034 indent-2 / no trailing NL; D-038 indent-1 / trailing NL — all round-trip-parse as JSON). The producer's four audit notes are pure ASCII; D-038's 3 non-ASCII bytes are a **pre-existing** em-dash (U+2014) at an identical offset in the parent commit, byte-preserved (not producer content).

5. **[PASS / no defect] AS-5 — tampering still detected; security meaning is sound.** `test_content_tamper_still_detected_under_crlf` (one content char changed, delivered CRLF) still raises c2 — confirmed green — and both pre-existing tamper tests still pass. Security analysis of `replace(b"\r\n",b"\n").replace(b"\r",b"\n")`: it forgives **only** the choice of CR/LF/CRLF as line-ending representation. A lone CR inserted mid-line becomes a real `\n` (a content/line-break change → still caught); line-break count and position are preserved; CR-vs-LF carries no semantic channel in JSON or Markdown. The only forgiven class is pure representation, never content. Correct.

6. **[PASS / no defect] AS-6 — whole control-plane suite green.** All five documented local commands + the dispatch-guard EXIT 0 at the reviewed identity (table above), and the new `LineEndingNormalizationTest` 3/3. The producer's changed code touches none of the guard/reminder/project_control paths, so those are regression-clean by diff as well as by run.

7. **[INFO] Content-manifest material-identity path deliberately kept raw.** `directive_registry` line ~1204 (`entries.append((rel, sha256_file(f)))`) and `content_sha256` still hash raw bytes — documented in the new helper's docstring and consistent with the packet risk note ("frozen identities must not shift"). This is out of scope for M0-T156 (which targets c2/c14/migration), and CI green confirms no regression. Not a defect; noted for completeness. (`requirements_id_digest_sha256` — a joined-string hash — is correctly left untouched.)

8. **[INFO] CI evidence relied upon per reviewer division-of-labor, and corroborated.** I cannot execute `gh` (read-only reviewer). I verified the orchestrator-captured `M0-T156-ci-evidence.txt`: run `34685572048` job `103531898219` (control-plane) SUCCESS at `012ba064`, retiring the standing red (prior red run `34681382456` job `103520545751` listed the six c2/c14 mismatches). This is internally consistent and **independently corroborated** by my own reproduction of the LF-side `validate --check` EXIT 0, the six-digest recomputation, and the red-half demonstration. The only failing job on the run (`web-dependency-security`) is the owner-gated Next.js item preserved by D-040-R004 — unrelated to this task. I did **not** re-run the entire `test_directive_compliance.py` single-pass locally (packet-documented 30-60+ min under AV/disk pressure); CI is the single-pass whole-suite authority and I ran all AS-relevant classes targeted.

9. **[INFO] Packet's AS-4 command over-captures; correct scoping used.** `git diff ebd39767..012ba064 -- project-control/directives/` also shows D-041 additions + index.json — but these come from **interleaved unrelated commits** (`2fa8c332` D-041 capture, docs commits) between the claim and the producer material, not from M0-T156. The producer's own commit `2bdae13f` (verified via `git show --name-status`) is clean. No action needed; flagged so the orchestrator doesn't misread the range diff.

10. **[LOW] No dedicated CRLF unit test for the migration-manifest site specifically.** The `migration_manifest_sha256` check adopted the shared `sha256_text_artifact` (defensively correct), but there is no unit test that flips the migration manifest's line endings the way AS-2's flip test does for a source. It is covered transitively (same helper as AS-1's tested function) and by `validate --check` on both representations of the real registry (both EXIT 0), and the migration manifest is LF-on-disk today so normalization is identity there. Non-blocking; a future hardening opportunity, not a gap in AS-1..AS-6 coverage.

## Named-mutant table (§6) — independently confirmed killed

| Mutant | Killer (verified) |
|---|---|
| Normalization removed (raw hashing) | AS-1 CRLF `assertEqual` fails; red-half showed 316 real-registry mismatches |
| Six values not re-stamped | `validate --check` EXIT 1 (computed LF value ≠ old CRLF-recorded value) |
| Over-forgives (strips >CR) | AS-5 tamper test + AS-1 independent oracle both fail the mutant |
| Only some sites normalized | red-half shows both c2 AND c14 errors; local CRLF `validate --check` catches the unconverted class |
| Fixture digests left raw | fixture author now uses `sha256_text_artifact`; raw would break on Windows `write_text` CRLF-translation |

## Scenario-coverage table

| AS | Requirement | Verification (independent) | Result |
|---|---|---|---|
| AS-1 | line-ending-independent digest, load-bearing red/green, lone-CR decision | independent-oracle test runs green; red walk traced (CRLF assertEqual fails first); lone-CR normalized + documented | PASS |
| AS-2 | real registry green both representations; CI is LF authority | `validate --check` EXIT 0 on extracted LF tree AND synthesized CRLF tree; flip test correctly scoped; CI green corroborated | PASS |
| AS-3 | six re-stamps == LF actuals == CI prefixes | all six recomputed from frozen blobs; match recorded 64-hex + CI prefix; line-ending-independent | PASS |
| AS-4 | sources immutable, append-only, byte-preserved | `git show 2bdae13f`: only 6 digest values + updated_at + 1 ASCII audit entry per manifest; zero source/req/verification/index changes; format byte-preserved | PASS |
| AS-5 | tampering still detected; only representation forgiven | tamper test green; 2 pre-existing tamper tests green; security semantics sound | PASS |
| AS-6 | whole control-plane suite green (local + CI) | 5 local commands + 3 guards + new test class all EXIT 0 at reviewed identity; CI whole-suite single-pass green | PASS |

## Frozen-identity integrity

Confirmed intact: code/manifests byte-identical between `2bdae13f` and `012ba064` (empty diff); no commit after `012ba064` through the branch tip `038b5e6d` touches any of the 8 allowed_paths. The later commits (`a27b1ecc` CI-evidence capture, `b89fc5ba` submit, docs) add new report files and ledger records only.

## Split-run posture (acceptability)

Acceptable. Tests use isolated per-test tempdir fixtures (each `Fixture` copies the registry into a fresh temp dir), so there is no shared mutable state that ordering could mask; the local A–L (75) + M–V (53) split is a documented performance workaround (registry copytree per Fixture under AV/disk pressure), and CI runs the entire file single-pass green as the authority. In my scratchpad export the same classes ran in seconds, confirming the slowness was environmental, not a test defect.

## Notes for the orchestrator

- No blocking defects. Findings 7, 8, 9 are INFO; finding 10 is LOW/non-blocking (future hardening).
- This G4 verdict covers acceptance-scenario/test adequacy (AS-1..AS-6). The in-regime directive verification (D-040-R003, D-001:ALL) is the `directive-compliance-verifier`'s pass and must be recorded separately in the directive `verification.json` before acceptance.
- I could not run `gh`; CI facts are from orchestrator-captured evidence, independently corroborated — not a BLOCKED condition.

**VERDICT: PASS**
