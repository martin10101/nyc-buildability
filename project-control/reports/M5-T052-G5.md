# M5-T052 G5 — independent security review (security-reviewer, saved VERBATIM from the agent returns)

G5 SECURITY REVIEW — M5-T052 (DB-031 condo records channel + display). VERDICT: PASS (2 ADVISORY, 0 BLOCKING). HEAD verified 6e3e1a40. Read-only; both agent-memory write paths (Bash and Write) are disabled this session, so no memory file was recorded — flagging that here.

SCOPE: material 365f492f touches exactly the 9 allowed_paths files; 46bf1105 is the routed consumer-sweep test fix (report-view.test.tsx, test-only, tagged, outside allowed_paths as the producer disclosed); a6f35a0f is the timestamp fix in condo-records.ts. No forbidden path touched (condo_base_lot.py, profile/, spatial/, connectors/, scenario/, contracts/). No package.json/lockfile change.

CHECKS PASS:
1. ROUTE INPUT (condo_records.py) — flag `internal_rule_eval_enabled()` checked FIRST (config.py:52, fail-safe: absent/empty/unknown->False->generic 404 before a correlation id is minted). normalize_bbl runs BEFORE any resolver call (condo_records.py:561); malformed->typed 422; 404 sentinel is byte-identical to an unmounted path. No stack traces/internals leak: _internal_error_500 (:221) logs type+correlation_id only, body is generic. No user value logged unbounded — validation log emits payload["code"]+correlation_id, never raw_value (:564). Response size bounded by resolver semantics (base_lots = condo base-lot set; provenance = one entry per SODA query). _assert_json_safe (:236) rejects NaN/Inf/unpaired-surrogate before send.
2. INFORMATION EXPOSURE — exposes only records the platform already carries (base-lot identity + provenance quintuple); recorded_zoning is always null today (_recorded_zoning_for returns None; DTM channel has no zoning column, honestly LABELLED unknown). No secrets, no connector keys reachable; resolver consumed read-only via injected seams; typed resolver failure is a NORMAL 200 `error` doc, never 5xx.
3. CLIENT (condo-records.ts) — the single fetch sends credentials:"omit" (:353), cache:"no-store", cancellable+time-bounded. Every reflected field bounded before render; boundedTimestamp caps at 40 (:207); typed error states echo only boundedText(message)+boundedToken(correlationId), never raw bodies; the 422 detail.raw_value is NOT read by the client at all.
4. SANITIZER-BOUNDARY SWEEP (the systemic ask) — audited every boundedToken site. Only ONE charset/vocabulary mismatch remains (ADVISORY-1 below). All others fit [A-Za-z0-9._-]: BBLs/condo_key numeric, dataset_id SODA-4x4 hyphenated, source_id/query_kind/error_type snake_case, correlation uuid. Free text (note/reason/divergent_zoning_notice/message) correctly uses boundedText (preserves spaces, strips only control chars). The fixed timestamp path now uses boundedTimestamp.
5. DISPLAY — no dangerouslySetInnerHTML in apps/web/src (grep confirms the only hits are the bounded.ts comment, LotOutlineMap MapLibre attribution note, and the address test's negative assertion). React escaping sound; all interpolated values pre-bounded. The records section can NEVER render an allowance: withholdAllowances = profileWithholds || channelWithholds is monotonic (PropertyOverview.tsx:206), nulls scenario/evaluation into DevelopmentLimits; showRecords is gated to channel.kind==="multi_lot" (:208); CondoRecordsChannelSection renders only recordsView fields (bbl, provenance, recorded-zoning label) — no computed value. AnalysisIdentityNotice byte-unchanged (absent from the material diff).
6. DEPENDENCIES/TESTS — no new dependency or lockfile change; tests offline via Depends-injected resolver seams and stubbed fetch; per M5-T052-ci-evidence.md, CI run 35451055372 at a6f35a0f = all jobs success incl. secret-scan green (verified the stored evidence per read-only protocol; I cannot run gh).

FINDINGS:
- ADVISORY-1 (sanitizer-boundary, LATENT) — condo-records.ts:242 `boundedToken(record.recorded_zoning, 32)`. NYC zoning district codes legitimately contain `/` (special mixed-use e.g. `M1-5/R7-3`, overlay-over-residence). boundedToken's [A-Za-z0-9._-] allowlist strips the slash => silent corruption (`M1-5/R7-3`->`M1-5R7-3`) — the SAME class as the timestamp bug this task just fixed. NOT currently exploitable: the API always emits recorded_zoning:null (condo_records.py:_recorded_zoning_for:368), so no live value flows through it today. But the module is explicitly designed to pass zoning through this exact seam unchanged when the ZTLDB/spatial lookup is wired, at which point slash-districts corrupt silently. Remediation (before that wiring): a `boundedZoningDistrict` helper permitting `/` and the district charset, mirroring the boundedTimestamp precedent. Not blocking because dormant.
- ADVISORY-2 (minor, defense-in-depth) — bbl.py:63 to_payload emits `repr(raw_value)` with no length cap; condo_records.py:574 reflects it into the 422 detail.raw_value. It is self-reflected to the same caller only, repr()-escaped, never logged, and never read/rendered by the web client, so no XSS/log-injection surface — but a length cap on the reflected raw_value would be tidy defense-in-depth.

No BLOCKING findings. Cross-tenant isolation N/A (no auth/tenancy; internal flag-gated route). Least privilege and log redaction hold. PASS.

## Delta-attestation (verbatim)

YES — G5 security PASS CARRIES to e9abeaaf. Verified git diff 6e3e1a40..e9abeaaf independently: only the two report files (producer-report.md, source-sections.md) + control-plane records (M5-T052.json, state.json, tasks/M5-T052.json) changed; every code/security-surface file I reviewed (condo_records.py, condo-records.ts, PropertyOverview.tsx, ReportView.tsx, main.py, the test files) is byte-identical. Both advisories still stand as-is (neither was a code defect). PASS at e9abeaaf.
