# M2-T015 unit 2 — independent security review (verbatim reviewer return)

Saved verbatim by the orchestrator from the independent security-reviewer agent's return channel
(2026-08-08; reviewer model claude-opus-4-8 effort xhigh; transport entity-decoding only). This is
the unit-level review the supervised loop's Codex CONTINUE decision required before unit 3; the
full task-level G5 gate runs at task submission. Reviewer preamble: "I have completed my
independent review. I verified the frozen SHA (`a059b5000a6cd5d6e9061347066d7ec79166dfd5`, clean
worktree), read all four artifacts under review plus the M2-T014 hard input and the producer
report, reproduced the terminology sweep myself (not trusting the producer's claim), and checked
the threat/control matrix against the packet's G5-mandatory list and SB scenarios."

Orchestrator note on the reviewer's final evidence item: the requested belt-and-suspenders
`validate_contracts.py` capture at this SHA already exists —
`ACTIVATION-2026-08-08-supervised-auto/m2t015-u2-validation-at-a059b50.md`
("Checked 10 schema file(s); 0 failure(s)." EXIT:0).

---

# M2-T015 UNIT 2 — Independent Security Review (unit-level, pre-implementation)

**Reviewer:** security-reviewer (read-only, ADR-005)
**Reviewed SHA:** `a059b5000a6cd5d6e9061347066d7ec79166dfd5` (branch `task/M2-T015-survey-ingestion`, worktree `C:/Users/MLFLL/Downloads/nyc-zoning/wt-m2t015`; `git status` clean)
**Scope:** unit-2 artifacts — `docs/SURVEY_DOCUMENT_INGESTION_ARCHITECTURE.md` (361 lines), `docs/UPLOAD_THREAT_MODEL.md` (140 lines), plus the 2-line isolation-terminology reconciliation in `packages/contracts/schemas/v1/survey_evidence.schema.json` (line 141 `extraction_run_id` description) and `docs/SURVEY_EVIDENCE_CONTRACT.md` (§3 identity table row). Producer report `project-control/reports/M2-T015-producer-report.md` read as claims to reproduce, not as evidence.
**Nature:** design/threat-model unit. No executable implementation exists yet; this is the CONTINUE gate the supervised loop requires before unit 3.

## VERDICT: CONTINUE

No Critical/High/Medium findings. Three Low/informational forward-notes for the implementation unit (none blocking). The prior Codex REVISE items (honest parser-isolation boundary; T11 abandoned-job cleanup) are both fully resolved.

## Review-question answers

**Q1 — Parser-isolation honesty (the REVISE item). PASS.** I reproduced the sweep independently (Grep, case-insensitive, all four artifacts). The word "sandbox" now appears in the reviewed set only in two deliberately NEGATIVE statements: `SURVEY_DOCUMENT_INGESTION_ARCHITECTURE.md:111` ("A plain child process is not that boundary and is never described as sandboxed") and `UPLOAD_THREAT_MODEL.md:108` ("is not called a sandbox and never enables parsing by itself"). The schema and contract doc contain zero "sandbox" occurrences (Grep "sandbox" on `SURVEY_EVIDENCE_CONTRACT.md` → no matches; on the schema → the sole match at line 141 is now "isolated processing job"). The old positive wording ("one sandboxed processing job") is gone from both (diff confirmed: schema `d1a370b..9127a97`, contract `74a8e1e..63d947b`). The boundary is defined with two kernel-enforced properties (Landlock FS allowlist, seccomp socket-syscall denial under `no_new_privs`), proven by a fail-closed startup capability probe, self-verified per process, with "no fallback to unisolated parsing" (§5). Parsing (S2–S4, and dependent S5–S8) is explicitly DISABLED until either the probe proves both properties on the running Render substrate or a dedicated no-egress parsing container/jail is available (§5 "Honest substrate statement"; §11 worker row; threat model §5.3 / residual risk 3). The technical claims are accurate (Landlock and seccomp-BPF are unprivileged, self-applicable; the inherited stdout pipe needs no socket; the conditional Landlock-network-rules note is correctly hedged). The unit-1 schema/contract now cite architecture §5 rather than asserting their own model, and add the correct corollary that no `extraction_run_id` is ever minted without a verified boundary. Terminology is consistent across the four reviewed artifacts.

**Q2 — Threat/control matrix completeness. PASS.** All ten G5-mandatory items map to a threat with a control and an acceptance-scenario id: MIME/content validation → T01 (SB-S6); extension-mismatch → T02 (SB-S6); size/page/pixel → T03 (SB-S6); decompression bomb → T04 (SB-S6); malware/dangerous content → T05 (SB-S6, with honest AV-scope residual §5.2); sandboxed/isolated parsing → T06 (SB-S6/SB-S9); private storage → architecture §7 + threat model §5.1 (deferred, honest); immutable originals + file digests → T10 + architecture §6 (SB-S6); safe failure → typed rejections throughout + T11 (SB-S6). SSRF/injection: T06 seccomp network denial + architecture §5 "external-reference/external-entity resolution disabled" + S2 "external references never executed, flagged"; T07 path-traversal/storage-key injection handled by content-addressed keys, filenames-never-in-paths, system-generated temp names, and no-archive-formats (no zip-slip surface by construction). Every threat T01–T11 carries a scenario id. **T11 abandoned-job cleanup is present and deterministic** (architecture §5 items 1–7): ownership `OWNER.json` marker with worker instance id + pid **plus process start time** (pid-reuse-safe) + run id; deterministic liveness rule; bounded startup+periodic scavenging (`JOB_TEMP_TTL_SECONDS`/`JOB_DIR_GRACE_SECONDS`/`SCAVENGE_INTERVAL_SECONDS`/`MAX_SCAVENGE_DELETIONS_PER_SWEEP`); safe path validation (direct child of root, realpath containment, real-directory-not-symlink, name-pattern match, symlinks never followed, validation-failure → typed anomaly never deletion); per-deletion and per-refusal audit records (paths/metadata only, never document bytes); host-crash coverage via next-instance sweep or ephemeral-disk destruction; SB-S6 executable coverage enumerated. Named-constant limits table is complete and marked "initial bounds… changed only through review, never set by AI."

**Q3 — B-001 honesty. PASS.** Architecture §7 states plainly "Nothing below exists in any environment yet: no bucket, no credentials, no migration, no deployment," and separates "Active now (no credentials required)" (storage abstraction interface, temp-dir CI fake, synthetic fixtures, digest discipline) from the deferred production binding. Threat model §5.1 mirrors this ("design exists, nothing is provisioned… no production upload endpoint exists, so the deferred controls protect nothing that is live"). Credentials §7 defers to `docs/SECRETS_POLICY.md` (Render env `sync:false` / GitHub environment secrets, never in Git) and records provisioning as the owner action in B-001. No credential, bucket, or deployment is implied as existing.

**Q4 — Fail-closed doctrine. PASS.** No path promotes model output to canonical geometry or a material input on confidence alone: architecture §1.2, §10 ("Confidence — however high — promotes nothing"), §4 state machine (AI cannot trigger/veto/propose transitions), and contract §6/`confidence` field ("confidence NEVER promotes a value"). AI is confined to bounded, schema-constrained classification of already-extracted content with no tool access/side effects (§10; T09); a successful prompt injection yields only a wrong candidate that fails independent validation visibly. Unresolved conditions are always visible (`validation_results` `unresolved` with mandatory `detail`; empty-array = visible "no check run yet"; state routes to `needs_review`). SB-S7 wrong-address: `address_bbl_match` fail → flagged, routed to `needs_review`, professional-rejection path; `bbl` is documented as upload *intent, never a verified association* (contract §4.1, T08) — consistent with "flagged, not ingested into that property's evidence." MapPLUTO cross-check (`tax_lot_geometry_comparison`) is typed-comparison-only and never silently overrides a survey (§9 rule, contract §6, SB-S4). Log redaction, least privilege, and service-role secrecy are all addressed (parser process holds no secrets; document bytes never logged; storage abstraction exposes no overwrite for originals).

**Q5 — Misstatements / overpromises / inconsistencies.** None that mislead the implementation unit. Notably, the malware item is honest rather than overpromised (residual §5.2 concedes structural-only screening, no AV engine, flags AV as an owner payment decision), and the insider/platform-compromise limit is scoped out honestly (§5.5, ADR-002). The most load-bearing scrutiny — that "parsing disabled until verified" could contradict the SB-S6 screening scenarios — resolves cleanly: S1 gate controls (MIME sniff, extension cross-check, stream size cap, digest, path safety = T01/T02/T03-size/T07/T10-ingest) run synchronously in the API independent of the boundary, while S2–S4 screening code is exercised by CI fixtures (architecture §11 "logic runs identically with the temp-dir storage fake"; threat model §6 "a screening control without a failing fixture is treated as unimplemented"), and only *production decoding of untrusted bytes* is gated on the probe. Coherent fail-closed posture, internally consistent.

## Findings

- **[LOW / informational, out-of-scope, correctly deferred]** `docs/SURVEY_DOCUMENT_FORMAT_POLICY.md` (M2-T014, a hard input, NOT in this unit's `allowed_paths`, unedited here) still uses "sandboxed/sandboxable" positively (e.g., lines 20, 27, 34). The fixture `packages/contracts/fixtures/invalid/survey_evidence/unapproved_extraction_method.json` (unit 1, `cabe128`) paraphrases it ("licensing/sandboxing unproven"). Both are faithful paraphrases about *toolchain-confinement feasibility as a format-admission criterion*, not claims that any runtime process is a sandbox. The producer correctly scoped this out (report §U2.4 meaning 5, §U2.9 item 5). No action required in this unit; a corpus-wide vocabulary migration, if the owner wants it, is an M2-T014-owned follow-up.

- **[LOW / informational, forward-note for unit 3]** Upload-time authorization/tenant scoping (which authenticated principal may upload against which BBL/organization) is deferred with B-001 (threat model §5.1 tenant-isolation RLS; §5.1 "no production upload endpoint exists"). This is acceptable for a design unit, but the implementation unit must not build a live, tenant-blind upload endpoint — the cross-tenant authorization model must land with provisioning when B-001 clears. Already implied; restated so it is not lost.

- **[LOW / informational, forward-note for unit 3]** Because production extraction is entirely availability-gated behind boundary verification, the implementation unit must ensure the SB-S6 S2–S4 screening fixtures (page/pixel bomb, decompression bomb, malformed structure, active-content) exercise the screening code directly in a CI harness so the controls are proven even while production parsing stays disabled. This is already the stated intent (threat model §6; architecture §11); flagged only so the CONTINUE decision carries it into unit 3 as a hard fixture requirement.

## Notes on evidence I could not self-execute
The producer's expected `python .github/scripts/validate_contracts.py` result was NOT re-run in my read-only context; however this unit's schema change is description-text only (diff confirmed keyword-identical: property set, `required`, `$ref`, enums, patterns, `additionalProperties:false` unchanged), so contract-instance verdicts cannot have changed. If the orchestrator wants belt-and-suspenders coverage before unit 3, capture `validate_contracts.py` at this SHA (unit-1 pattern: "Checked 10 schema file(s); 0 failure(s)." exit 0). This does not affect the CONTINUE verdict for a documentation/threat-model unit.

**VERDICT: CONTINUE**
