# M5-T089 — G3 independent code review (code-reviewer "cr-t089", read-only; the ADDED independent G3 reviewer)

> Transmission history: pinned at fa7e5fae, delivered as two SendMessage parts (1/2, 2/2) ending with
> END-OF-REPORT; no truncation. Joined verbatim by the orchestrator (transport wrapper tags removed only).
> Verdict PASS (advisories A1-A4).

---

M5-T089 G3 code review (read-only) — PART 1/2. Pinned HEAD fa7e5fae at start.

VERIFICATION RUN (cwd services/api): `ruff check .` -> All checks passed; `pytest tests/connectors/test_building_footprints_arcgis.py -q` -> 110 passed; `python tools/modularity_check.py --check` (repo root) -> failures 0 (this file is a warning/review_signal, not a CI failure). Independently re-derived the risky claims in source.

CORRECTNESS — no BLOCKING defects.
- Request builder (`_query_url` 522-538): where=1=1, inSR=2263, outSR=2263, esriSpatialRelIntersects, explicit LONG out-field allowlist (never `*`), returnGeometry=true, orderByFields=OBJECTID ASC, deterministic resultOffset. Fully percent-encoded; tests assert byte-for-byte reproduction of every recorded request URL and the field-type pins vs the recorded layer JSON (test:167/189/209). Correct.
- Paging (`_collect_pages` 978-1014): offset = len(collected) under stable OBJECTID ASC ordering is the correct ArcGIS semantics; over-page count, cross/intra-page repeated OIDs, empty-page-claiming-more, page ceiling (50) and feature cap (2000) each raise a typed paging_pathology — never truncation. Stop condition keyed on exceededTransferLimit. Sound.
- Metadata-first schema check (`_validate_metadata` 616-651): validates layer name, geometryType, objectIdField, field presence+esri types, maxRecordCount, supportsPagination/supportsOrderBy before any page; publication-CRS change and missing editingInfo are non-fatal visible drift signals. Not guessed — pinned from the recorded layer JSON and cross-checked. Page-level CRS gate (687-694) refuses any non-empty page not wkid 102718/latestWkid 2263 before a coordinate is read.
- Datum/units: relative_base_z = ground - site_ground, relative_roof = base + height, both None when an input gaps (915-916; exact-on-fixture test:303). HEIGHT_UNIT us_survey_foot and GROUND_DATUM NAVD88 both carry the RQ-1 INFERENCE / RQ-2 caveat and the dictionary-vs-FGDC definition conflict verbatim (148-164) — no guessed unit or legal value. Condo billing-lot range is IMPORTED from mappluto (7501-7599), not re-hardcoded.
- Geometry policy (`parse_footprint_geometry` 747-794): esri winding handled correctly (CW exterior = negative shoelace, CCW hole = positive), holes assigned to smallest containing shell, multipart/has_holes/parts_touch flagged, interior overlap or orphan hole -> review_required, any malformed ring -> invalid; the record is ALWAYS returned with verbatim geometry + digest (never dropped). 16-row RED/GREEN mutation table backs the honest-gap and fail-closed claims.

Continued in PART 2/2.

---

M5-T089 G3 code review — PART 2/2.

FAIL-CLOSED TRANSPORT: uses the shared engine (request_with_retry + standard_retry_hooks + jittered_retry_after_delay via `_Io.get` 564-593) exactly as the accepted MapPLUTO connector does — mirroring confirmed. Every fault (transport error, ArcGIS error object at HTTP 200, malformed JSON, wrong CRS, over-page, schema drift, budget) becomes a typed FootprintRefusal carrying the failed request URL, time and body digest; `fetch_context_buildings` never raises. Verified `canonical_json_digest(None)` returns "null" (no mask risk when a feature lacks geometry).

CATCH-ALL (report deviation #1): the final `except Exception` -> internal_error is acceptable and required by AS-4. It cannot emit wrong data (returns status=refused, zero buildings). Safe.

MODULARITY: 891 SLOC / 49 symbols is above the 750 justification threshold (warning, not the 1000 hard limit; CI failures 0). Cohesion justification is recorded and reasonable — one read-only connector responsibility, peer dcm_street_centerline_arcgis.py is 902 SLOC. Acceptable now; no split required before wiring. `_ring`/`parse_footprint_geometry`/`classify_query_relation` are the natural future extraction behind a facade if it grows.

SCOPE: exactly the 4 allowed paths; imports only stdlib+shapely+app (ast test:788); zero new dependencies; keyless (Accept only); not wired — test:801 rglobs all of app/ to prove no consumer. All test-enforced.

ADVISORY (non-blocking):
- A1. The internal_error branch (1065-1075) does not log the unexpected exception, only records its class name in the refusal detail. Consider `logger.exception(...)` there so a genuine bug is discoverable in production, not just surfaced in the result.
- A2. Zero GROUND_ELEVATION is kept as a real 0.0 and flows into relative_base_z with flag `ground_elevation_zero_unverified` (deviation #8) — the correct provenance-preserving choice (source does not document 0 as "not available" for this field, unlike HEIGHT_ROOF). Forward note: the wiring/massing consumer must treat that flag as a datum gap, not a true 0 ft datum.
- A3. Source-registry record is deferred to the wiring packet (the accepted MapPLUTO peer likewise has no in-module registration; it is outside this packet's allowed_paths). Forward note only.
- A4. The full fetch path with a feature missing the `geometry` key is proven only via the direct `parse_footprint_geometry(None)` unit case, not an end-to-end test; verified safe by inspection.

None of the advisories block. No guessed schemas, no hardcoded legal values, no hidden defaults, no silent uncertainty, tests are strong, contract is additive/compatible.

M5-T089 G3 VERDICT: PASS
END-OF-REPORT
