# M5-T017 — G0 Contract/Packet Gate

Gate: G0 (packet completeness, /start-controlled-task checklist). Result: PASS.
Recorded by: orchestrator (contract gate; producer for the implementation will be a
dispatched scenario-optimization-engineer agent — producer ≠ gate reviewers).

## Checklist

1. **Exact requirement identifiers** — PRESENT. D-041-R001 verbatim text is the requirement
   (inputs[0]); the design contract is `docs/design/astra-presentation-research.md` §3.1
   (precise-noun rule + ZR 12-10), §3.2 (Calculated evidence row), §3.3 (honest result states),
   pinned as inputs[1] with section numbers.
2. **Exact directive references** — PRESENT and mechanically verified: `D-041:D-041-R001`;
   `evaluate_task_refs` → ok=true, applicable == cited == ["D-041-R001"] after the append-only
   applicability bind (task_ids append + requirements digest resync 6800ebea → 7d32c881 +
   manifest audit_log entry; `validate_directive_compliance.py --check` EXIT 0). D-041-R003
   scope limit respected: this packet implements C1 only.
3. **Exact source/evidence files** — PRESENT: eight pinned inputs with line references
   (scenario builder document shape + provenance-resolution pattern, profile bldgarea fact
   shape, constants/models vocabulary, both closed schema copies, the two shape-test files,
   the connector's bldgarea typing + condo caveat).
4. **Exact allowed_paths / forbidden_paths** — PRESENT: 12 allowed (1 new module, bounded
   builder wire-in, constants/models, facade, both schema copies, 4 test files, own report);
   forbidden fences every other engine module, profile/rules/connectors/api/documents,
   apps/web, tools, CI config, dependency files, and the control plane. No overlap with any
   in-flight task (no other task is in flight).
5. **Acceptance scenarios** — PRESENT: S1–S6 covering the D-041 required four-case harness
   (S1 normal remainder, S2 over-built honest negative + professional-review routing, S4
   missing/unusable existing-area, S5 missing FAR cap) plus S3 zero boundary and S6
   contract/JSON-renderer-parity/regression/modularity. Fixture-derived assertions required.
6. **Required gates & reviewers** — G0/G1/G4/G5 with code-reviewer/qa-engineer/
   security-reviewer (all ≠ producer). No UI in this packet → no G3; the UI rendering of the
   line is a follow-on packet.
7. **Modularity boundary answers** — PRESENT in path_notes (new focused module per the
   derive.py precedent; builder.py 935 lines gets a ~20-line wire-in only; stable public
   interface; focused tests; modularity check must pass).

## Notes

- The section/value naming (`unused_draft_zoning_floor_area`) implements the §3.1 precise-noun
  rule and matches the owner's own label example in D-041-R001 ("unused DRAFT floor area");
  "remaining development rights" is deliberately NOT the output name.
- The machine-readable tax-lot-as-zoning-lot assumption (ZR 12-10) is an explicit packet
  obligation (S1), per the owner-supplied research and the peer-relayed design input.
- Risks name the two-copy closed contract, the root professional_review_required semantics
  widening, and the condo-lot bldgarea caveat.

VERDICT: PASS — packet is claimable.
