# M0-T161 — G3 code review (code-reviewer "cr-m0t161", read-only, static)

> Transmission history: pinned at 7f5c82fa, delivered as four SendMessage parts (1/4..4/4) ending with END-OF-REPORT;
> no truncation. Joined verbatim by the orchestrator (transport wrapper tags removed only). Verdict PASS: every
> load-bearing section-5 STOP is faithful and exact, fail-closed throughout. Advisories F1-F5 (F2: the owner guide's
> step 4 wrongly says step 3 prints the digest) are taken into a required-corrections round before the owner runs it.

---

M0-T161 G3 code review (READ-ONLY) — PART 1/4: pin + faithfulness

PIN: HEAD 7f5c82fa (later than 52594a03; acceptable). Material b27fc89b = exactly 4 files, all in allowed_paths (git show --stat confirms). My sandbox blocks powershell.exe even for the read-only parser, so this is a STATIC review + the harvest evidence you cited (parser 0 errors both files; run_ps_tests 8 files passed).

DIGEST FAITHFULNESS (commission_lanes.ps1 L76-87) — every pinned value byte-matches the recert:
- CandidateCommit a3f24ff3… L76 ✓; CandidateSubtree 9c0b14ea… L78 ✓; WrongSubtree 11d43515… L79 ✓; ManifestStop "147 55dc7135…" L80 ✓; ConfigRawHash 610ce9d5… L81 ✓; ChainCheckStop "2.1.281 946eb509…" L83 ✓; ClaudeVersion 2.1.281 L84 ✓.
- The two STRONGEST identity STOPs use EXACT string equality (-ne after Trim), not substring: 5.4 manifest readback == ManifestStop (L437) and 5.11 chain == ChainCheckStop (L704). ✓
- 5.3 install (Step-Install L402-410) asserts the candidate commit present, the candidate subtree present, AND the wrong subtree 11d43515 ABSENT — a solid three-way identity guard. ✓
- 5.1 config raw hash exact (L300). ✓ 5.7 verify-controller expected line asserted (L497). ✓ doctor PASS / doctor --live VERIFIED asserted with exit-code as the PRIMARY guard (L513/L529).

ADVISORY F1 (loosening vs the recert text — 4 pinned values defined but NEVER asserted; grep: 1 occurrence each = definition only):
- CandidateTree 82432361 (commit tree) — recert 5.3 expects "commit tree 82432361…" in the install output; the helper never checks it.
- InstalledFileCount 204 — recert 5.3 "files 204 byte-identical" and 5.5 "installed files re-compared 204" — never checked.
- CoveredFileCount 146 — recert 5.5 "covered files 146" — never checked. Step-VerifyManifest (L444-452) asserts ONLY 'MANIFEST VERIFIED against the accepted source' (L447), not the count line nor the "…at a3f24ff3…" commit.
- ConfigLfHash 34f4fe90 — defined, never used (benign: covered transitively by verify-controller/doctor).

WHY NOT BLOCKING: the update-transaction script (update_controller_from_candidate.ps1) does the full bidirectional 204-file compare in -Phase install and the full digest re-comparison in -Phase verify-manifest, and EXITS NONZERO on any mismatch; the helper checks `$r.code -ne 0` on both (L396, L447). So install/manifest integrity is enforced by exit code + the exact manifest-digest STOP (5.4) + the commit/subtree identity (5.3). The dropped items are corroborating echoes, not the load-bearing gate. You may still choose to require them for literal recert parity.

Continued in part 2.

---

M0-T161 G3 — PART 2/4: fail-closed + lanes 4-5

FAIL-CLOSED (Invoke-Ext L110-148): correct.
- EAP='Continue' is set FUNCTION-SCOPED (L123) only, so native stderr can't become a terminating error before the raw exit code is read; it does NOT leak to callers (PS dynamic scoping). Args pass as a splatted ARRAY (@ExtArgs, L131), never concatenated into a command string.
- $global:LASTEXITCODE reset to $null before the call (L126); a command that produces NO exit code THROWS "STOP … produced no exit code (fail closed)" (L143-146). ✓
- Every LOAD-BEARING caller checks `$r.code -ne 0`: backup L372, install L396, record-manifest L429/L437, verify-manifest L447, checkout-key L203, robocopy `-ge 8` L475 (correct robocopy semantics: 0-7 ok, 8+ fail), verify-controller L497, doctor L513, doctor --live L529, lane status L665, clear-recovery L674, draft L697, chain L704, pending-approvals L743, resume L756, git-worktree L826. No path treats a nonzero/stderr result as success.
- Informational-only calls that intentionally don't gate on code (check-phase status L291/L294; PostChecks L539) match the recert, which marks those informational. No fail-OPEN anywhere.
- STOP wiring proven by the offline test: 2a forces the 5.4 readback wrong and asserts verify-manifest/verify-controller/robocopy/doctor NEVER ran after it (test L71-74); 2b forces the chain wrong and asserts the detached start never fired (L92); 2c forces the config hash wrong (L109). Top-level dispatcher catch prints the message and `exit 1` (L915-918); `exit 0` only on success.

LANES 4-5 STAND-UP (Step-Propagate L454-486): faithful + correctly scoped.
- Writes ONLY C:\SupervisorController4/5 (New-Item L462-463 gated to $NewLanes) + their wrapper (L466) + their mrl folder. robocopy /MIR targets `tools\agent_supervisor` only, so lanes 2/3 mrl\ is untouched; lanes 4/5 mirror into fresh empty dirs. ✓
- Checkout key comes from the controller's OWN checkout_key via python from wt-controller-src (Get-CheckoutKey L195-208), never re-implemented, never hardcoded (D-066-R001). runtime_dir_for likewise (L181-193). ✓
- config.toml / model_selection.toml immutability is snapshot-guarded before/after ALL steps (Invoke-UpdatePhase L336-356): STOP if either raw SHA changes. The activation manifest is the only thing touched, and only by the 5.4 re-record (which reproduces the same certified digest). ✓ Matches D-088-R003 "WITHOUT changing any of them".
- Assert-SameTree proves all five copies byte-identical to wt-controller-src (L483-485) + verify-controller from all five + wt-controller-src (Step-VerifyController L488-503). ✓ Matches R003 evidence requirement.

Continued in part 3.

---

M0-T161 G3 — PART 3/4: wrapper, lane starts, plan, owner guide

WRAPPER (New-LaneWrapperContent L550-616): derives lane 4/5 from lane 3, changing ONLY lane-specific values (key, SupervisorController3→N, autostart3-→N, supervisor-3→N, INSTANCE 3→N) + blanking the ACTIVE-TASK block to NOT-YET-FED (so an accidental launch fails closed on --repo NOT-YET-FED). Global .Replace() is safe: the shared `C:\SupervisorController\model_selection.toml` and the shared manifest path have no "3", so they're preserved (test asserts this L199-202). Set-NotYetFedBlock markers can't self-collide (start "# ---- ACTIVE-TASK" ≠ end "# -{10,}"). ✓

LANE STARTS (Start-LaneFirstLaunch L657-724): faithful to recert 5.11 + MRL §1/2/5.
- order: status→clear-recovery ONLY on PAUSED_RECOVERY (L671)→fresh mrl_launch_draft (all 12 args match, L683-696)→exact chain STOP→detached start.
- draft --mode and start --mode are the SAME $Mode variable (equal, as required). --max-cycles 1 always. --repin-cli-identity ONLY when Repin=$true: lane/lanes pass $true (first start), approve passes $false (L767). limited-auto adds --owner-enable-bounded-auto (L712). ✓
- lane 1 forced supervised canary (Get-LaneMode L622-637 rejects any other mode); Start-Detached hidden with named logs + PID printed (L238-252). ✓
- approve (L730-769): pending-approvals PRINTS the held prompt (L745), REQUIRES -PromptDigest (STOP L747), and verifies the digest is actually held (L749) before resume; re-start WITHOUT repin. ✓
- lanes (L876-897): Assert-ValidLanePlan (L788-860) enforces each field present, lane∈2-5, no dup lane, no dup worktree (normalized), each a real git worktree, packet claimed/in_progress, allowed_paths present + pairwise-disjoint; then starts sorted, ≥60 s apart (Start-LaneSpacing L885-887). ✓ Matches D-088-R004/R005.
- PS 5.1: no &&/ternary/??, no Invoke-Expression, no caller text in any command string. Owner input (PacketId/Worktree/Digest) always flows through the ExtArgs ARRAY, never into a python -c string or IEX. Every -c string uses only script constants. ✓

ADVISORY F2 (owner-guide accuracy, owner-guide.md step 4, L59): "The command in step 3 will print a long code called a 'digest'. Copy that code." — WRONG. Step 3 (lane phase) launches DETACHED and returns before the async run parks in WAIT_FOR_OWNER, so it cannot print the digest. The digest is surfaced by the APPROVE phase's pending-approvals output — i.e. the owner runs step 4 once WITHOUT a digest, which prints pending approvals + STOPs "pass -PromptDigest <the digest printed above>" (L745-747), then re-runs step 4 with it. Machine behavior is SAFE and self-correcting (fail-closed STOP guides them), but the prose misdirects a non-technical owner. Recommend rewording step 4 (this is the top advisory to fix before the owner runs it). D-088-R007.

Continued in part 4 (remaining advisories + test adequacy + verdict).

---

M0-T161 G3 — PART 4/4: remaining advisories, test adequacy, verdict

ADVISORY F3 (overlap check gap, Get-PathOverlap L862-874): it does literal prefix matching (equal, or one an ancestor via `+ '/'`). It correctly catches exact dups and `services/api` vs `services/api/app/x.py`, BUT it would NOT catch overlaps expressed through `**`/`*` glob suffixes — e.g. `services/api/**` vs `services/api/app/x.py`, or `services/api/**` vs `services/api/app/**` — which this repo's packets commonly use (PROGRAM_KNOWLEDGE: "write dir/**"). Test 5 only exercises exact-file paths, so this gap is unproven-either-way. Mitigation: the orchestrator does the AUTHORITATIVE pairwise-disjointness check (D-088-R005 is the orchestrator's own obligation); this validator is secondary defense. Recommend normalizing trailing `/**`/`/*` before comparison, or requiring plan packets to use non-glob directory prefixes.

ADVISORY F4 (STOP-prefix consistency): four throws in the wrapper-generation path do NOT start with "STOP" (L557 key-not-64hex, L562 no lane-3 key, L587 no ACTIVE-TASK markers, L596 no $Py/$WorkDir). They ARE fail-closed (halt update, surfaced by the dispatcher catch), but the owner guide's rule is "if any line starts with STOP…"; these wouldn't match that heuristic (the missing "UPDATE PASSED" would still tip the owner off). Recommend prefixing them "STOP [update 5.6]:". Minor.

ADVISORY F5 (test hardening): in test 2a, Step-Propagate's real New-Item/WriteAllText (lane 4/5 folders) are NOT stubbed — they're merely UNREACHABLE because the 5.4 STOP fires first. The suite's no-machine-write property therefore depends on that STOP. The test proves the STOP fires, so this is sound today, but stubbing New-Item/WriteAllText too would be belt-and-suspenders.

TEST ADEQUACY (test_commission_lanes.ps1): genuinely proves its claims. All machine-changing calls are stubbed or unreachable at each test — Invoke-Ext (every supervisor verb), Start-Detached, Ensure-ActivationDir, Assert-SameTree, Get-FileSha256Raw, Get-FreeGiB, Get-BoundCommit are replaced; the only filesystem writes are under %TEMP% sandboxes, cleaned at the end. STOP-halts-everything-after (2a/2b/2c), check-no-write (before/after snapshot, test 3), wrapper-diff (contains-checks + strong region diff, test 4), and plan-validator accept/refuse×5 (test 5) are all real. Harvest evidence corroborates: parser 0 errors both files, run_ps_tests 8 files pass.

SCOPE/AS-5: exactly the 4 allowed files; producer changed nothing on the machine (report §5, [BLOCKED] AS-4 with the exact orchestrator recipe, matching the packet HARNESS note).

All findings are ADVISORY; none BLOCKING. Load-bearing section-5 STOPs are faithful and exact, fail-closed throughout, PS 5.1-clean, no IEX, no caller-text injection, correct canary/repin/mode/spacing/plan behavior.

M0-T161 G3 VERDICT: PASS

END-OF-REPORT
