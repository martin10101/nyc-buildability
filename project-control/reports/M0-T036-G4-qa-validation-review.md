# G4 QA/Validation Gate — M0-T036 (Codex–Claude Supervisor Bridge)

> Preserved VERBATIM by the orchestrator from the g4-qa-engineer-m0t036 agent-return channel
> (2026-08-04, frozen SHA 43848bd). Transport entity-decoding only; no edits.

G4 QA/VALIDATION GATE — M0-T036 (Codex–Claude Supervisor Bridge), frozen SHA 43848bd5158cb74184964f93d97ef109fad1ed19 (verified rev-parse; HEAD never moved; no repo writes, no git mutation, no project_control.py, no gh).

VERDICT: PASS — with one required report correction (PILOT-LOG run-4 root cause, below) and ranked activation-forward gaps. Validation at this SHA is adequate for the shadow/supervised scope it claims; nothing here re-opens F-1..F-7, the diagnostics triage, F-2/F-4 as V1.1 conditions, or the F-5 doctor-tz recommendation (all cited as settled).

(1) REPRODUCED TOTALS (my own runs)
- Suite: `python -m pytest tools/test_agent_supervisor_*.py -q` (all 19 files) = 1065 passed, 2 skipped, 60.60s — exactly the expected totals. Rerun with -rs: identical totals, 83.94s. The 2 skips: (a) test_agent_supervisor_policy.py:449 symlink-escape — host lacks SeCreateSymbolicLinkPrivilege; MITIGATED: the Windows junction-escape twins (policy.py test line 468, adversarial line 393) ran and passed, exercising the same realpath/reparse-point denial (`symlink_or_junction_escape`); (b) test_agent_supervisor_process.py:423 — POSIX-only guard, correctly skipped on Windows.
- Doctor: `python -m tools.agent_supervisor doctor` = overall PASS, exit 0, controller 0.4.0-phase4; limited-auto reported NOT IMPLEMENTED and disabled; two checks self-declare UNVERIFIED-live (control_response wrapper bytes) — see gap 1.
- Replay: `python -m tools.agent_supervisor replay` = 8/8 cases reproduce recorded stop/continue behaviour, exit 0, 0 model calls, 0 writes. Corpus contains all 8 §15-required cases (clean_continuation, review_required_correction, ci_failure, stale_sha_mismatched_review, owner_gated_stop, m0_t031_accepted_lifecycle, b015_sentinel_failure, m0_t028_detection_only_stop); doctor verified corpus digest 768eea1ec6bb9e83… with provenance against this checkout.

(2) §15 ADEQUACY — SAMPLED AND CONFIRMED
Tier-policy matrix: covered incl. the subtle items — shadow owner-touch counter cannot trigger policy widening (loop.py test line 654), ASK-independence five-clause check (policy line 816), DENY_AND_CONTINUE vs DENY_AND_HALT, injection labelled-not-obeyed. Model-selection matrix: unlisted-model refused in every role and even as provider default, ordered fallback + NOTIFY, exhausted chain queues ASK, per-provider lists never cross-satisfied, advisory model refused for every reserved purpose, IPC worker/descendant denial, unconfirmed change never applied, replayed confirmation refused (reviewer lines 635-739, ipc 132-364). Adversarial essentials: push-timeout-remote-succeeded reconciliation, notification/approval matrix (replay, wrong nonce/digest/identity, expired binding, moved head, exactly-once), restore drills x3, junction/hard-link races, reviewer-write refusal. Recovery matrix: crash before/after every journal/effect/outbox/audit/state boundary (crash.py 121-405), SAFE/AMBIGUOUS/UNSAFE incl. drift-dominates, ambiguous-effect never retried/never auto-resumes, DST/clock-jump/monotonic-lease tests. Test mass: 1067 tests across 19 files (policy 98, scheduler 96, adversarial 93, endurance 92, reviewer 74…).

(3) PILOT REGRESSION ADDITIONS — THEY PIN THE LIVE FAILURE MODES
SessionCloseTests (6, runner.py 505-571): pins the 900s wall ride (asserts elapsed <60s against a 120s wall — loop must end on the terminal result event, not the watchdog); pins kill-flush by asserting the checkpoint IS captured when graceful close fails; pins the F-3-fix regression risk that stdin must stay open for control_responses until the terminal result; multi-turn terminal results; watchdog still owns a runaway unit. ProviderRejectionTests (6) + SchemaStrictSubsetTests (4) + FlattenedConstraintTests (9) = 19: pins the allOf HTTP-400 surfaced as `provider_rejected_request` with the REAL child returncode (F-7 unmasking), audit carries redacted+bounded provider reason, genuinely-absent file stays `missing_decision_file`; forbids every composition keyword at any depth plus strict-subset closure (additionalProperties:false, full required) and freezes the decision enum; every constraint the old allOf carried now enforced in validate_decision, including the whitespace-prompt edge the old `const:""` missed. Adequate and well-aimed.

(4) PILOT EVIDENCE CHAIN — VERIFIED, WITH ONE DISCREPANCY
All five preserved audit chains (runtime-run1/3/4/5/6) hash-chain-verify via AuditLog.verify_chain (ok=True), head.json anchors match tail digest and sequence, run_ids match the PILOT-LOG rows exactly. Run 1: missing_checkpoint stop, touch counted — supported. Run 3: invalid_checkpoint `bad_status: 'awaiting_gate'` verbatim in the chain — supported. Run 5: unit clean (timed_out=false, ~F-3 fix live-proven), 3x codex_review_invalid_output then blocking_ask — supported, and its masked message ("the reviewer wrote no last-message file") is itself the preserved evidence OF F-7. Run 6: journal independently confirms POLICY_CHECK→COMPLETE on decision_complete, decision COMPLETE by gpt-5.6-sol attempt 1 correlated to cp-m0t035-accept-ready-r6 with the 4 gate records as evidence_refs, zero owner touches — fully supported; timeline 39s dispatch-to-checkpoint vs run 4's exactly 900.1s.
DISCREPANCY (required correction, report-accuracy only): PILOT-LOG says run 4's checkpoint text was "lost to the timeout kill" (and F-3 says "run 3 captured it, runs 1 and 4 lost it"). Run 4's own audit contradicts this: its claude_unit_completed event records checkpoint_id `cp-m0t035-accept-ready-r4` with empty error_category — and claude_runner.py:775 only writes that field from a successfully extracted, schema-valid checkpoint. So in run 4 the checkpoint SURVIVED and validated; the S4.5 stop ("the worker exited without a valid checkpoint") was driven by timed_out=true, i.e. the fail-closed timeout rule, not checkpoint loss. Run 3 is the inverse (checkpoint_id empty because validation failed). F-3 itself stands (all three runs demonstrably rode the wall; fix live-validated by runs 5/6), but the run-4 root-cause line and the "two lost their checkpoint" count are wrong and should be corrected by appended erratum (PILOT-LOG is an append-per-run log). The SessionCloseTests docstring repeats the claim — cosmetic.
Run 2 has NO preserved runtime evidence (no runtime-run2 dir); its F-2 claim rests on the log narrative. I corroborated it from code: state_machine.py:241 defines PAUSED_RECOVERY→PREFLIGHT on `owner_cleared_pause` and cli.py contains no reference — no command fires it. Acceptable given F-2 is a settled V1.1 condition, but note the evidential asymmetry.

(5) COVERAGE GAPS RANKED BY ACTIVATION RISK (§15 family; concrete missing scenario)
1. HIGH — Live broker allow-path never observed. Every pilot run's audit shows permission_decisions: []; doctor's control_response_live_probe self-reports UNVERIFIED (wrapper shape implemented per SDK docs, exercised only against fakes; Phase 1 probe captured the request payload and a deny round-trip, not the response wrapper bytes). Family: parsing/processes + approval broker. Missing scenario: `doctor --live` bounded run — one in-scope tool call ALLOWED through the real CLI (tool executes) and one denied (tool does not) — before any live supervised/limited-auto worker run.
2. HIGH — Limited-auto is not implemented (loop refuses it by name; doctor confirms). The target mode's forwarding, standing-grant, and NOTIFY-never-blocks behavior has no executable form at this SHA; activation requires new code and its own gate. Family: tier-policy matrix / S12 modes. Missing scenario: post-implementation full fake-loop tier matrix in limited-auto plus a supervised live rehearsal.
3. MEDIUM — No live forwarding at all: shadow forwarded nothing (correct), so FORWARD_PROMPT with the five required S9 elements is fake-tested only. Family: tier policy / S9 tier mapping. Missing scenario: one supervised live cycle forwarding one CONTINUE prompt end-to-end.
4. MEDIUM — Rotation/handoff never exercised live (all pilot units short, single-checkpoint; 67 rotation tests all fake). Family: rotation matrix. Missing scenario: live rotation drill with thresholds forced low — verified handoff, Codex handoff review with review_model, fresh-session READY, no old-session resume.
5. LOW-MEDIUM — Durable wake never drilled on real Task Scheduler (install-autostart is owner-gated; scheduler matrix is fake/simulated, incl. DST/rollover/clock-jump). Family: recovery and scheduling. Missing scenario: one owner-approved one-shot wake drill verifying trigger identity, post-wake full revalidation, expired-trigger cleanup.
6. LOW — Codex fallback engagement never live-observed (run 5's chain shared the broken schema so it could not engage; fallback order/NOTIFY/exhausted-ASK are fake-tested). Family: model-selection matrix. Missing scenario: live probe with primary forced unavailable, verifying terra engages with NOTIFY.
7. LOW — Symlink-escape denial not executed on this host (privilege skip); junction twin passed and both share the realpath resolution path. Family: approval broker / adversarial. Missing scenario: one privileged/dev-mode or POSIX CI execution of the symlink variant.

(6) COULD NOT RUN / VERIFY
- `doctor --live` (launches the real Claude CLI) — outside my authorized command set; gap 1 therefore remains open, not merely unassessed.
- Run 2: nothing preserved to verify; corroborated from code only.
- The pilot controller checkout `C:\SupervisorController` is outside my working directories — its isolation/manifest claims rest on the PILOT-LOG and run audits, not my inspection.
- No live provider calls of any kind; everything I executed was the suite (twice), doctor (offline), replay, and read-only inspection of the committed pilot artifacts. Doctor wrote only under %LOCALAPPDATA%\NYCBuildabilitySupervisor (outside the repo); `git status` porcelain shows no new repo modifications from my session.
