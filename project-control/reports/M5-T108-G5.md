# M5-T108 — G5 security review (security-reviewer "sec-t108", read-only)

> Transmission history: pinned at f4c4ab3a, delivered as four SendMessage parts (1/4..4/4) ending with END-OF-REPORT;
> no truncation. Joined verbatim by the orchestrator (transport wrapper tags removed only). Verdict FAIL on MEDIUM 1:
> huge-but-finite coordinates make the measured dimensions overflow to inf and POST /candidates returns an untyped 500.
> LOW 2 and LOW 3 are PKT-H mount items. One rework round covers the consolidated inventory.

---

M5-T108 G5 security review (READ-ONLY) — Part 1/4. VERDICT: FAIL.

HEAD pinned f4c4ab3a. Material code identity is entirely in 2df81013; harvest f4c4ab3a touched only project-control files (verified git show --stat). Baseline: ruff clean on the 4 target files; 41/41 tests pass; modularity exit 0.

Summary: one MEDIUM defect reachable from the untrusted upload surface that falsifies the module's own typed-error contract, plus two LOW advisories for the PKT-H mount. The gating (ships UNMOUNTED + double-flag default-off + rate limit) means NO live production exposure today — so this is a fix-now-vs-track call for you — but as the security gate certifying the submitted code and its AS-1/AS-4 claims, those claims do not hold on this input, hence FAIL.

BLOCKING — MEDIUM 1: POST /candidates emits non-finite measured dimensions → unhandled ValueError → uncontrolled HTTP 500 (bypasses the typed error model; no correlation id).
- Repro (in-memory probe, both flags on): a closed LWPOLYLINE ring with large-but-finite coords (e.g. 1e300) is accepted by read_dxf (each coordinate is individually finite). measured_dimensions (services/api/app/drawings/dxf_import.py:219-229) then computes area = |Σ x0*y1 − x1*y0| / 2 = inf (the products overflow ~1.8e308); bbox_width/height and perimeter also overflow at 1e308.
- list_candidates (dxf_import.py:317) puts that raw measured dict on the Candidate; post_dxf_candidates (services/api/app/api/v1/dxf_import_api.py:365, 376) emits it in the 200 body via _json (dxf_import_api.py:179-182). Starlette 0.46.2 JSONResponse.render uses allow_nan=False → raises ValueError("Out of range float values are not JSON compliant") DURING render, inside the handler's return, which has no try/except.
- Observed: status 500, body "Internal Server Error", NO X-Correlation-ID; with raise_server_exceptions=True the ValueError escapes the app.
- This contradicts the service docstring ("raises nothing to a caller; every failure is a typed ImportRefusal VALUE"), AS-4, and the documented (500,"internal_error") typed matrix — the client gets Starlette's bare untyped, untraceable 500. POST /draft is SAFE (validate_proposed_massing rejects the same coords → 422).

---

M5-T108 G5 — Part 2/4 (MEDIUM 1 remediation + two LOW advisories).

MEDIUM 1 remediation (all within allowed_paths — dxf_reader.py needs NO change):
- In the service, after computing measured dimensions, guard every value with math.isfinite; if any is non-finite, return a typed ImportRefusal (reuse "unreadable_dxf", or add e.g. "coordinate_out_of_range"/"degenerate_geometry") so the route maps it to a 422 with a correlation id. Equivalent: bound coordinate magnitude at the seam before measuring.
- Add a /candidates test with overflow coordinates asserting a typed 4xx (never 500) + a reddening mutation. Consider a defense-in-depth allow_nan=False pre-render guard mirroring proposal_validation.py.
Security relevance on an upload trust boundary: untrusted input → uncontrolled server error (a DoS primitive) + loss of the correlation-id traceability the module guarantees for its 500 path.

LOW 2 (advisory; PKT-H mount, NOT reachable in this packet): the "generic 404 byte-indistinguishable from an unmounted path" guarantee is POST-only. Probe with the route MOUNTED + flag off: POST → 404 {"detail":"Not Found"} (correct sentinel), but GET → 405 {"detail":"Method Not Allowed"} — distinguishable from a truly unmounted path (which 404s for every method), leaking feature existence. This packet ships unmounted (app/main.py untouched — verified), so both methods 404 in prod today. PKT-H must make non-POST methods on the disabled path also return the generic 404.

LOW 3 (advisory; PKT-H mount): rate-limiter identity + memory.
- POSITIVE: _client_key (dxf_import_api.py:174-176) uses request.client.host (the socket peer) ONLY — no X-Forwarded-For, so NO header-spoof bypass of the limiter (verified grep).
- But _RateLimiter._hits (dxf_import_api.py:147) never evicts idle keys, so distinct source IPs grow the dict unbounded (minor memory-DoS; per-key timestamp lists ARE bounded to ~max_requests via the prune-before-append in check(), dxf_import_api.py:149-158).
- And behind Render's reverse proxy, request.client.host is the proxy IP → all callers collapse to ONE key → the per-caller limit becomes GLOBAL (one noisy caller throttles everyone) rather than a bypass. For the mount: derive identity from a trusted-hop X-Forwarded-For + add idle-key eviction / a max-key cap.

---

M5-T108 G5 — Part 3/4 (PASSED checks, verified in source + probe).

Upload ceiling: the flag gate is checked FIRST, before any body read (dxf_import_api.py:237-239) — a disabled feature does zero work. Body bounded BEFORE materialization via the reused T053 primitives: a declared-Content-Length fast path (dxf_import_api.py:247-253) AND a streamed accumulator that stops one chunk past the ceiling without draining the rest (proposal_validation.py:203-222) — so chunked / absent / under-reporting Content-Length are all caught. Content-type + magic-byte gate (dxf_import.py:233-276): the binary-DXF sentinel is refused first on raw bytes, non-DXF/non-ASCII refused; only then do the exact bytes reach read_dxf. _IMPORT_DXF_LIMITS is a fixed module constant DxfLimits(max_bytes=MAX_BODY_BYTES) (dxf_import_api.py:98), never request-derived; the reader additionally hard-clamps every field via _LIMIT_CEILINGS (dxf_reader.py:190-222) so a permissive caller cannot disable a bound.

Job safety: read_dxf runs off the event loop in run_in_threadpool under asyncio.wait_for(READ_DEADLINE_SECONDS) (dxf_import_api.py:223-230); TimeoutError → typed 503 (dxf_import_api.py:269-274); asyncio.CancelledError is a BaseException and is NOT swallowed by the following except Exception. The worker thread is not force-killed after timeout (disclosed), but the work is bounded: read_dxf is O(n) (single-pass line split, dxf_reader.py:543-587) over a ≤256 KiB body, and the 30-req/60s rate limit caps concurrency — no thread-pool exhaustion and no algorithmic-complexity DoS path. The limiter is stdlib, monotonic-clock, thread-safe.

Untrusted drawing text: _escape_drawing_text (dxf_import.py:183-202) renders control chars as \xNN and length-bounds to 200 on EVERY drawing-derived output — layer, $ACADVER, disclosed-unknown type names and skipped-section names (dxf_import.py:339-347), and the reader-refusal detail (dxf_import.py:283-290); route refusal messages are bounded to 400 (proposal_validation.py:120-129). Logging is correlation-id-only across all log lines — no raw drawing, caller, or upstream text is ever logged (confirmed by reading every logger call).

---

M5-T108 G5 — Part 4/4 (remaining PASSED checks, baseline, final verdict).

Query params: int/float parse errors → typed 422 (dxf_import_api.py:280-309); candidate indices range-checked (dxf_import.py:440-459); floors and floor_to_floor_ft are bounded by validate_proposed_massing (int 1..500; finite, >0, ≤100 ft — proposal.py:320-357); known_length_ft/measured_length required finite and strictly positive (dxf_import.py:408-414); confirmed_units restricted to the feet-family map; author required non-empty and escaped. Probe confirms NaN/Inf query values cannot reach a /draft 200 body (→422, incl. an overflowing known/measured scale, which the outline finiteness+NYC-bounds check rejects). The draft is built through the SAME validate_proposed_massing contract (dxf_import.py:480-489).

Exposure: both endpoints include_in_schema=False; the route ships UNMOUNTED — app/main.py has no reference (verified grep) and test_route_is_unmounted_in_the_real_app asserts absence from routes + OpenAPI. The local DXF_IMPORT_ENABLED flag reads os.environ only (dxf_import_api.py:125-134), default-off, not caller-controllable; BOTH flags must be true or → generic 404. The draft path stays fail-safe for bad params too (dxf_import_api.py:384-389: bad params while disabled → 404, not 422).

Persistence: no storage/file/db/network/temp anywhere in either file (verified grep + read) — in-memory only. Zero new dependencies; requirements untouched (forbidden path). Honesty vocabulary intact; input-precision provenance not upgraded.

Cross-tenant / service-role / private-storage / SSRF: N/A — no DB, no auth surface, no outbound I/O, no storage; the route is a pure parse→validate→echo with no tenant or credential context. Prompt-injection: N/A (no LLM in path); ingested drawing text is treated as untrusted data and escaped.

Baseline (cwd services/api): ruff clean on the 4 target files; pytest tests/drawings/test_dxf_import.py test_dxf_import_api.py → 41 passed; python tools/modularity_check.py --check → exit 0.

M5-T108 G5 VERDICT: FAIL

Blocking: MEDIUM 1 (non-finite measured dimensions → uncontrolled 500 on the untrusted upload path; fixable entirely within allowed_paths). Advisories: LOW 2 and LOW 3 (both for the PKT-H mount). No critical/high findings. No live production exposure today given the route is unmounted + default-off + rate-limited.
END-OF-REPORT
