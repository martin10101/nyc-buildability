# M5-T120 — G1 format / data-contract review (data-contract-verifier "g1-t120", read-only)

> Transmission history: round 1 pinned at b0eb29f7 (material 5ae5700e, identity before the correction), delivered as
> 4 SendMessage parts ending with END-OF-REPORT (parts 1-4 were queued twice, byte-identical; the first copy is joined); then a DELTA attestation after the [ORCH-CORRECTED per M5-T120 G1]
> doc-only commit 793fed27 (re-frozen at seam 858989c1, identity 80c33f6d) - the verdict carries to the corrected identity.
> No truncation. Joined verbatim by the orchestrator (transport wrapper tags removed only). Verdict PASS with ONE REQUIRED doc-only
> correction (the MuPDF clause of the orphan-lineto citation - applied in 793fed27, confirmed by the delta) and one
> advisory (the colour-space table pointer - also applied).

---

M5-T120 G1 data-contract/format-authority review (data-contract-verifier, READ-ONLY) — PART 1/4. PIN: HEAD b0eb29f7 at start. Material 5ae5700e (cherry-pick of wt-m5t120 3d08a559). Verdict in Part 4.

METHOD: read the 9 files at HEAD; isolated the single commit 5ae5700e (diff vs parent d1970b06, not the claim seam — peer T121/T123 landed between); compared moved code vs parent d1970b06; verified ISO pointers in-file; verified the orphan-lineto reader-of-record from PRIMARY sources (WHATWG Canvas 2D, pdf.js reliance, MuPDF path.c). Did not re-fetch the real corpus (network/out-of-tree, files deleted by producer — as the M5-T118 G1 also did).

SCOPE / AS-6 — PASS. Commit 5ae5700e touches EXACTLY 9 files, all inside M5-T120.json allowed_paths. Forbidden byte-UNCHANGED in this commit (empty diff): sheet_objects.py, pdf_object_streams.py, test_pdf_object_streams.py, sheet_marked_content.py, app/documents/, app/api/, app/main.py, app/cad/. Zero new deps; unwired (no route/main change). Modularity: sheet_interpreter.py 577 (was 749; under the 750 ceiling), sheet_path_state.py 249, sheet_inline_image.py 444.

AS-1 (split first, byte-identical) — PASS. The path-state group is now the `_PathState` mixin in sheet_path_state.py; `_StreamRun(_PathState)`. I diff-compared every moved function against parent d1970b06: `_flush_open`, `_open_after_close`, `_moveto`, `_curveto`, `_close`, `_charge_points`, `_paint`, `_op_m/l/c/v/y/h/re` are BYTE-IDENTICAL. `_lineto` differs by EXACTLY one branch — old `if self._current is None: return _refuse("path","'l' with no current point")` → new `return self._orphan_lineto(x,y)` (sheet_path_state.py:71-73); the AS-4 feature, isolated to the case that previously refused. The interpreter's only ADDED lines are docstrings, the import `from app.drawings.sheet_path_state import _PATH_HANDLERS, _PathState` (sheet_interpreter.py:51), and `class _StreamRun(_PathState)` — no change to the scan loop, `_execute`, `_map`, text, or Form/inline handling. `re` was always dispatched separately from `_PATH_HANDLERS` (old and new alike). `_execute` reads `_PATH_HANDLERS` as a sheet_interpreter global (imported), so the monkeypatch seam bites; committed positive controls `test_split_path_handlers_bite_at_new_namespace` and `test_split_paint_flag_sets_bite_at_new_namespace` (patch the NEW consuming namespace) prove the moved constants are not vacuous. Split-equivalence: exactly ONE case substituted, 62 other per-case digests unchanged. Continues in Part 2.

---

M5-T120 G1 — PART 2/4.

AS-2 (/DP dictionaries) — PASS. sheet_inline_image._parse_dictionary now accepts a nested `<< >>` VALUE, or a `[ <<>> null ]` array, ONLY for /DP // /DecodeParms via `_nested_dict_allowed` (:309-314), consuming exactly ONE level (`_skip_dict`, capped by `_MAX_INLINE_DP_DICT_DEPTH=1`, :355-396) under the same 4096-byte dict cap. The value is stored as the `_DICT_VALUE` sentinel (:193), never a geometry key, so `_determinable_length` arithmetic is untouched and NO sample byte is decoded. This is faithful to ISO 32000-1 §8.9.7 / §7.4.4: /DP or /DecodeParms may be a decode-params dictionary, or an array of such dictionaries/nulls (one per filter) — the standard CCITTFax `/DP <</K -1 /Columns..>>` or Flate-predictor shape. A nested dict under any OTHER key (:185-189) and a SECOND nesting level (:381-386) stay typed refusals. Skipping still never decodes: a /DP dict rides with a /F filter, which already forces the EI scan (`_scan_for_ei`); `_skip_dict` only lexes-and-discards for balance. This directly resolves the M5-T118 G1 FINDING A (DP-dict was refused, not skipped). All three mutants are genuine red/green: disallow-dp-dict (→refuse), allow-any-nested-dict (/CS `<<>>` → wrongly skips), raise-the-depth-cap=2 (2nd level → wrongly skips). Confirmed the `_nested_dict_allowed` and `_MAX_INLINE_DP_DICT_DEPTH` reads are module-global so the patches bite.

AS-3 (citations) — PASS, one minor ADVISORY. The three flagged pointers are corrected, and I grep-confirmed NO residual "§8.9.5.2" anywhere in the file:
- row byte-alignment now §8.9.3 "Sample Representation" (docstring :20-21; `_determinable_length` :224) — correct vs ISO 32000-1.
- permitted /BitsPerComponent now §8.9.5.1 / Table 89 (:73) — correct.
- colour-space abbreviations now Table 93 (`_COMPONENTS` :100; docstring :6-7,:21) — correct. Table 92 retained only where correct (KEY abbreviations, :91).
- ADVISORY (non-blocking): `_components` docstring (:436) still reads "colour space (Table 92)" — the SAME value-abbreviation→count mapping that :100 correctly cites as Table 93. Defensible (the /CS KEY is genuinely Table 92) but internally inconsistent with :100; recommend aligning :436 to Table 93 for consistency. Does not affect behaviour (a wrong CS fails safe via the EI-position check). Continues in Part 3.

---

M5-T120 G1 — PART 3/4.

AS-4 (orphan lineto) — PASS, with ONE REQUIRED citation correction.

BEHAVIOUR verified correct and safe. `_orphan_lineto` (sheet_path_state.py:83-100): when `_current is None`, map (x,y), start a NEW subpath at that own point (`_cur_points=[point]`, `_current=point`, `_subpath_start=point`), draw NO segment to it, increment page+document `orphan_subpaths`. Plumbing clean: sheet_reader.py adds the doc counter (:224), the per-page reset in `begin_page` (:241), and the stamp in `_build_page` (:501); sheet_primitives.py adds `SheetPage.orphan_subpaths`/`SheetDocument.orphan_subpaths` defaulted 0 (byte-stable). The reopen-at-last-point MUTANT (test_sheet_p4_features.py:102-124) is a real red/green: it re-enables `_open_after_close` reopening at the stale pre-paint point and the test catches the invented segment ((5,5)→(10,10)→(20,20) vs the real (10,10)→(20,20)). Orphan curves c/v/y STAY typed refusals (`_curveto` :105, `_op_v` :207 — byte-identical to parent).

CITATION check (the [recalled-verify] item the packet asked G1 to resolve) — verified from PRIMARY sources:
- ISO 32000-1 §8.5.2 / §8.5.3, Table 59: the current point is undefined after a painting operator / at stream start — CORRECT (also predecessor-verified in M5-T118 G1).
- WHATWG Canvas 2D (HTML Living Standard, CanvasRenderingContext2D `lineTo`): "when a path has no subpaths, lineTo must ensure there is a subpath for (x,y)" = create a NEW subpath with (x,y) as its first-and-only point, AS IF moveTo had been called (no segment). This is an EXACT match for `_orphan_lineto`. VERIFIED CORRECT.
- pdf.js renders to an HTML canvas and inherits that Canvas rule — accurate.
- MuPDF — INCORRECT as cited. I read source/fitz/path.c: `fz_lineto` with no current point WARNS AND IGNORES: `if (path->cmds_len == 0){ fz_warn(ctx,"lineto with no current point"); return; }`. MuPDF does NOT start a subpath — it drops the op (stricter). So the comment at sheet_path_state.py:90 ("...and MuPDF's handling [recalled - verify]") and the producer report §STEP 4 / OPEN QUESTION 2 MISCHARACTERIZE MuPDF.

REQUIRED CORRECTION (blocking for acceptance, doc-only): fix sheet_path_state.py:90 (and the report) to keep the now-VERIFIED Canvas-2D/pdf.js citation (drop its [recalled-verify] marker) and correct the MuPDF clause — MuPDF warns-and-ignores (drops the orphan lineto), so it is stricter and does NOT match; the reader deliberately follows the Canvas/pdf.js reading (what Acrobat/Chrome display). No behaviour/test change needed. Continues in Part 4.

---

M5-T120 G1 — PART 4/4.

AS-5 (real corpus, honest) — PASS to the extent a read-only format reviewer can verify. The report claims 4 of 6 read (items 1-4), items 5-6 scan-only refuse; item 2 (94pp VA HVAC vector set) NEWLY reads, unblocked by the orphan-lineto rule, with 8,154 skipped inline images and exactly 1 orphan subpath. The § classifications and the non-geometry disclosure (orphan/inline/sh counts) are internally consistent and correctly typed. The attribution diagnostic honestly states STEP 2 (/DP) is NOT exercised by the corpus (item 2's inline images carry no /DP dict) — so /DP handling is proven by SYNTHETIC tests only — and that no real file hits an orphan curve. No overclaim. I did not re-fetch the corpus (out-of-tree, network, files deleted), consistent with the M5-T118 G1; every format claim I can check holds.

FINDINGS
- BLOCKING: none. The format/data-contract interpretation is correct: the orphan-lineto rule is grounded in a VERIFIED primary source (Canvas 2D) and never invents geometry (mutant-proven); the /DP-dict skip is faithful to §8.9.7/§7.4.4 and never decodes samples; the split is a pure move with no semantics drift; the three citation pointers are corrected.
- REQUIRED CORRECTION (record PASS; blocks acceptance until applied — doc-only, no re-run): sheet_path_state.py:90 + producer report — the MuPDF citation is inaccurate; MuPDF's fz_lineto warns-and-ignores (drops the orphan lineto, does NOT start a subpath). Keep the verified Canvas-2D/pdf.js citation (drop its [recalled-verify]); correct/remove the MuPDF clause. (Detail in Part 3.)
- ADVISORY (non-blocking): sheet_inline_image.py:436 `_components` docstring still says "colour space (Table 92)" while :100 correctly says Table 93 for the same value-abbreviation→count mapping — align to Table 93 for internal consistency. (Detail in Part 2.)

Files reviewed (all absolute): C:\Users\MLFLL\Downloads\nyc-zoning\ctl24\services\api\app\drawings\{sheet_path_state.py, sheet_interpreter.py, sheet_inline_image.py, sheet_primitives.py, sheet_reader.py}; ...\services\api\tests\drawings\{test_sheet_p4_features.py, test_sheet_reader.py, test_sheet_reader_split_equivalence.py}; ...\project-control\reports\M5-T120-producer-report.md. Memory write was BLOCKED this session (Write disabled) — skipped per rule.

M5-T120 G1 VERDICT: PASS

END-OF-REPORT

---

M5-T120 G1 DELTA attestation (data-contract-verifier, READ-ONLY). Inspected `git show 793fed27`; confirmed 793fed27 is an ancestor of HEAD 858989c1 and the corrected text is present at HEAD.

(a) DOC-ONLY — confirmed. 3 files, changes confined to docstrings / report prose; every executable line (`_orphan_lineto` body `point = self._map(...)` ...; `_components` `for key in _CS_KEYS:` ...) is unchanged context. No code, test, or behaviour change; no golden/digest touched.

(b) REQUIRED CORRECTION satisfied as written — confirmed. sheet_path_state.py `_orphan_lineto` docstring now: Canvas 2D `lineTo` "ensure there is a subpath" (new subpath, no segment), pdf.js inherits it — [recalled - verify] REMOVED; MuPDF "STRICTER (`fz_lineto` with no current point warns and ignores the operator)", i.e. NOT a match — exactly my finding. Report STEP 4 + OPEN QUESTION 2 corrected identically. No residual "recalled - verify" in either touched source file. My ADVISORY is also resolved: `_components` docstring now says the /CS key is Table 92, its value abbreviations Table 93.

(c) G1 PASS CARRIES to content identity 80c33f6d at HEAD 858989c1 — confirmed. The delta is doc-only; the format/data-contract interpretation and all behaviour I verified for PASS are unchanged, and the only substantive edits are exactly the correction I required plus the advisory. Both G1 findings are now closed with nothing outstanding.

M5-T120 G1 DELTA: PASS

END-OF-REPORT
