# M5-T093 — G1 source/corpus verification (data-contract-verifier "dc-t093", read-only)

> Transmission history: pinned at fa7e5fae, delivered as three SendMessage parts (1/3..3/3) ending with
> END-OF-REPORT; no truncation. Joined verbatim by the orchestrator (transport wrapper tags removed only).
> Verdict PASS: 6/6 corpus files re-fetched with sha256 MATCH; the per-file structure table reproduced by an
> independent byte walk; the real read_sheet refuses all six identically at the cross-reference stage.
> Advisories A1 (leaf /Page objects are raw in items 3-4; only the /Pages nodes are compressed - the
> operative conclusion stands) and A2 (confirm the SLOC band with modularity_check at the C1 split) are
> routed to DB-055 (n).

---

M5-T093 G1 verification Part 1/3 — PIN fa7e5fae, method, provenance (6/6 sha256 MATCH), per-file structure table.

PIN: repo HEAD at start = fa7e5fae (matches expected). Material commit ab2a6730 is an ancestor of HEAD; git show --name-only = exactly the 2 allowed docs, 301 insertions/4 deletions, both docs, zero code/corpus files.

METHOD (read-only; NO disk writes — the Write tool is disabled and shell file-writes are sandbox-blocked for this agent, so I drove everything through curl/urllib piped into Python over stdin, nothing committed). Re-fetched all 6 items from the recorded URLs (curl pipe AND urllib, both hosts); independent byte scan via my OWN raw byte-walk + stdlib zlib inflation of every FlateDecode stream (NOT the producer's scripts); ran the REAL app.drawings.sheet_reader.read_sheet under the bare-package 3.11 shim, each file twice.

PROVENANCE (AS-1) — 6/6 HTTP 200, bytes == note, sha256 MATCH all six:
1: 6,490,778  daf0fbb3…c7e6b7f4 ✓
2: 6,804,710  f8384c4d…3c67c6de ✓
3: 885,378    2288a94e…7bd3bb527 ✓
4: 148,470    8f80f6d2…9cc552c2 ✓
5: 15,158     d0355d6f…a9faf75b5 ✓
6: 11,800     8756f638…5bfaeaf ✓

PER-FILE STRUCTURE (AS-2) — my byte scan vs note §2 (# | ver | xref | ObjStm | shading | inline | enc | producer):
1 | 1.7 | XREF-STREAM(/XRef+/W @6488119) | 10 | no | no | no | PDFMaker 9.0 for AutoCAD ✓
2 | 1.7 | XREF-STREAM @6801263 | 76 | no | YES | no | PDFMaker 9.0 for AutoCAD ✓
3 | 1.5 | XREF-STREAM @885035 | 32 | YES(/Shading+4 sh-ops) | no | no | Adobe PDF Library 11 / PDFMaker 11 for Word ✓
4 | 1.6 | XREF-STREAM @116 | 13 | no | YES | no | Canon iR-ADV C5840 → Adobe Acrobat Pro (32-bit) 23 Paper Capture Plug-in ✓
5 | 1.6 | XREF-STREAM @14795 | 3 | no | no | no | Acrobat 11.0.3 Paper Capture / I.R.I.S. ✓
6 | 1.6 | XREF-STREAM @11463 | 3 | no | no | no | Acrobat 11.0.3 Paper Capture / I.R.I.S. ✓

Every column reproduces note §2. ObjStm counts match EXACTLY (10/76/32/13/3/3). Shading confirmed on item3 only; inline images (ordered BI·ID·EI) confirmed on items 2 and 4 only; no encryption anywhere. Item4's dual producer independently confirmed incl. the exact XMP string. Continued Part 2.

---

M5-T093 G1 verification Part 2/3 — reader refusal evidence (AS-3), DB-055(g) ruling (AS-4), C1 scope.

READER OUTCOME (AS-3) — read_sheet imported from the REAL module app.drawings.sheet_reader. All SIX files returned the IDENTICAL typed refusal, run1==run2=True each:
  origin=strict_reader  reject_code=unsupported_pdf_feature  feature='cross-reference stream'
Verbatim detail (item1; others differ only in offset): "startxref offset 6488119 begins with an integer object header (a PDF 1.5+ cross-reference stream); only classic 'xref' tables are in the supported subset". The offsets in the six refusals (6488119/6801263/885035/116/14795/11463) EXACTLY match my independent last-startxref scan. Source-traced: app/documents/extraction/pdf_xref.read_object_table emits this the moment the last startxref target begins with a digit; sheet_reader._wrap_strict wraps it into the SheetRefusal. The refusal fires BEFORE any object table / catalog / page / content is materialised, so shading and inline images are never reached — the "latent" framing is correct.

DB-055 (g) RULING (AS-4) — all independently supported:
- Cross-reference streams block 6/6 across BOTH drawing classes and THREE distinct toolchains (PDFMaker-for-AutoCAD; PDF Library 11 / PDFMaker 11; Acrobat Paper Capture / Canon). The M5-T083 DCV worst case ("reader reads NEITHER positive vector fixture") is confirmed — items 1 AND 2 both refuse.
- Object streams are an inseparable co-requisite: in ALL six the ROOT /Pages node lives only inside a compressed object stream (my raw scan: /Type /Pages = 0 in raw for every file; ≥1 only after inflation), so resolving /Root→/Pages is impossible without decoding object streams. Confirmed.
- Shading (item3) + inline images (items 2,4) are real reader gaps but LATENT: sheet_reader has no 'sh'/'BI' handler (neither is in _IGNORED nor dispatched → both would hit the "unsupported operator" refusal), but the xref refusal fires first. Confirmed by source + reader run. Encryption absent in all six.
- pdf_xref.py source also confirms the note's claim that the reused strict reader reads exactly one classic xref section and refuses /Prev (incremental) and /XRefStm (hybrid) — so C1's "substantial extension" framing is accurate.

C1 SCOPE (§5): P1 (xref-stream + object-stream parsing, blocking), P2 (inline/sh handling, reachable only after P1), P3 (scan-only refusal reason for items 4-6), deprioritise encryption — all supported by the evidence; nothing generalised beyond these 6 files (the binding scope-honesty statement holds). Continued Part 3.

---

M5-T093 G1 verification Part 3/3 — scope (AS-5), findings, VERDICT.

SCOPE (AS-5): material commit ab2a6730 changes EXACTLY the two allowed doc paths — docs/research/architect-corpus-reader-trial-2026-09.md + project-control/reports/M5-T093-producer-report.md; no code, no fixture, no corpus binary committed. All corpus downloads stayed in my scratch subfolder (nothing on disk was even writable to me) and are discarded; I committed nothing.

FINDINGS:
BLOCKING: none.

ADVISORY (non-blocking — do NOT gate acceptance):
A1. §2 note-1's parenthetical "(and hence the page objects) live only inside compressed object streams" slightly over-generalises: for items 3 and 4 the LEAF /Page objects are in RAW (uncompressed) bytes (item3: 14 leaf /Page in raw, item4: 8), only the intermediate /Pages nodes are compressed. BUT the note's explicitly-cited numbers (item1 0/19, item3 0/3, item5 0/1) are all about /Pages nodes and are CORRECT, and the operative conclusion is fully intact — the ROOT /Pages node is compressed in ALL six (raw /Type /Pages = 0 everywhere), so object-stream decoding is genuinely required to walk /Root→/Pages. The note's very next sentence states exactly that reachability argument. Wording nuance, not a substantive error; affects no acceptance scenario and no C1 item. (If anything, the leaf-in-raw detail could be added to sharpen §2.)
A2. §5 sequencing note calls sheet_reader.py the "JUSTIFY" modularity band; the file is ~1097 physical lines. Whether that is JUSTIFY (750–1000 SLOC) or HARD (>1000) depends on the SLOC metric, which I did not measure (tangential to the corpus trial). Orchestrator can confirm with tools/modularity_check.py when contracting the C1 split, if it matters.

SUMMARY: All five acceptance scenarios independently verified — provenance exact (6/6 sha256 MATCH), per-file structural facts reproduced column-for-column, the headline negative result ("reader reads none of the six") reproduced by the REAL reader on real bytes with a deterministic typed refusal, conclusions and C1 scope supported, and scope clean (2 docs, nothing from corpus committed). No blocking defects.

M5-T093 G1 VERDICT: PASS

END-OF-REPORT
