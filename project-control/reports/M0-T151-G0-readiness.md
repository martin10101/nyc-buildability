# M0-T151 G0 readiness (administrative, orchestrator)

- **Task:** M0-T151 — D-034 implementation: repo-root ARCHITECTURE.md + `.claude/skills/pr-review/SKILL.md`.
- **Authority:** owner directive D-034 (source-001.md sha256 24c33759..., rows R001-R004),
  captured from the owner's practice screenshots + "can u implementat it".
- **Packet completeness:** objective enumerates both artifacts and the no-duplication rule;
  5 acceptance scenarios (six-question completeness, forbidden-paths+invariants accuracy,
  stop-condition protocol, skill output discipline incl. UNPROVEN marking + advisory
  disclaimer, real trial run); allowed_paths = 3 new files (path_free_governance declared);
  reviewers code-reviewer / security-reviewer / directive-compliance-verifier (≠ producer);
  gates G0/G2/G3/G5; in-regime `D-034:ALL` (validator exit 0).
- **Result:** PASS — contractible; producer may claim.
