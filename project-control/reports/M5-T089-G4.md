# M5-T089 — G4 test-adequacy review (qa-engineer "qa-t089", read-only)

> Transmission history: pinned at fa7e5fae, delivered as three SendMessage parts (1/3..3/3) ending with
> END-OF-REPORT; no truncation. Joined verbatim by the orchestrator (transport wrapper tags removed only).
> Verdict PASS: all 16 producer mutants and 4 of the reviewer's 7 own mutants killed; the 3 survivors are
> test gaps over correct production code (advisories A1/A2), routed to DB-058 (l)-(m).

---

M5-T089 G4 test-adequacy review (read-only) — PART 1/3. PIN HEAD fa7e5fae (recorded at start). Scope: services/api/tests/connectors/test_building_footprints_arcgis.py (110 tests) vs app/connectors/building_footprints_arcgis.py; material fe254eee.

METHOD (read-only): Write + shell file-writes are guard-blocked for me, so each mutant ran in a fresh Python process that reads the REAL connector source, applies ONE in-memory text mutation, injects the mutated module under the real dotted name into sys.modules BEFORE pytest collects the real test file (the test module's by-value imports and bf.* then bind to the mutant), then runs the full 110-test suite from services/api. The connector file on disk was never modified. Baseline (no mutation) = 110 passed → harness faithful.

PRODUCER'S 16 MUTANTS — re-run, ALL KILLED (rc=1, test file green only when the defect is absent):
M1 zero-height passthrough → RED (3: as3_zero_height, as3_missing_heights[0.0], as3_placeholder)
M2 NULL height→0.0 → RED (as3_missing_heights[None])
M3 missing ground→site ground → RED (as3_placeholder)
M4 relbase=absolute ground → RED (2)
M5 outSR=2263 dropped → RED (3)
M6 short name HEIGHTROOF → RED (58, URL cascade)
M7 exceededTransferLimit ignored → RED (10)
M8 wrong-CRS gate removed → RED (2)
M9 over-page check removed → RED (1)
M10 ArcGIS error object accepted → RED (1)
M11 generic catch removed → RED (1, exception escapes)
M12 holes silently filled → RED (2: courtyard, multipart)
M13 multipart→first part → RED (2)
M14 boundary touch=overlap → RED (2)
M15 condo billing-lot flag removed → RED (2)
M16 placeholder gap removed → RED (1)
All 16 producer claims corroborated. Continued in part 2.

---

M5-T089 G4 — PART 2/3. REVIEWER'S OWN MUTANTS (7 idiomatic weakenings, exceeds the ≥4 ask):

KILLED (4):
MY2 relative_base sign flip (ground−site → site−ground) → RED (as2_relative_base_z). Sign is pinned by the 794314 = −2.0 and shifted 195.5 cases (the two equal-ground records are sign-invariant, but the unequal one catches it).
MY3 joins_subject_lot keyed on BASE_BBL instead of MAPPLUTO_BBL → RED (as3_condo_billing_lot_join, subject_bbl=4068157501 → True vs mutant False). Join key pinned to MAPPLUTO_BBL (recon §9).
MY4 metadata validate skips the field-TYPE drift check (presence only) → RED (as4_layer_schema_drift[GROUND_ELEVATION retyped to string]). Type-drift pinned.
MY5 over_page_count off-by-one (`> page_size` → `> page_size+1`) → RED (as4_over_page_count). Boundary pinned.

SURVIVED (3) — GREEN, full suite still 110-pass with the defect present:
MY1a CRS page-gate checks ONLY latestWkid (drop the wkid==102718 half) → SURVIVOR.
MY1b CRS page-gate checks ONLY wkid (drop the latestWkid==2263 half) → SURVIVOR.
MY7 zero CONSTRUCTION_YEAR: gap emitted but `year = None` removed (value kept as 0) → SURVIVOR.

Important: the PRODUCTION code is CORRECT in all three survivor cases — the connector requires both CRS halves and does null the zero year. These are TEST-adequacy gaps only (missing discriminating assertions), detailed in part 3. Continued.

---

M5-T089 G4 — PART 3/3. FINDINGS.

ADVISORY A1 (MY1a/MY1b) — CRS page-gate two-part assertion not independently pinned. The gate correctly requires wkid==102718 AND latestWkid==2263, but the only wrong-CRS fixture (web-mercator) has BOTH wrong and valid pages have BOTH right, so a maintainer who simplifies the gate to one half stays green. The representative AS-4 case IS proven (M8 + test_as4_recorded_web_mercator both kill). Recommend: add a synthetic page with one half wrong (e.g. spatialReference {"wkid":102718,"latestWkid":9999}) expecting wrong_crs.

ADVISORY A2 (MY7) — zero CONSTRUCTION_YEAR value-leak not asserted. _build_building emits the zero_not_available gap AND nulls year; the param test asserts only the GAP, and no fixture drives year==0 through the None path, so keeping year=0 (a false "year 0") survives. This is the packet's "gap emitted but value also defaulted" class and the M5-T081/T083 "assert the value the bug displaces" lesson. Contrast HEIGHT_ROOF zero, where height_roof_ft is None IS asserted (M1 kills). Recommend: assert construction_year is None in the CONSTRUCTION_YEAR-0 case.

No BLOCKING findings.

VACUITY / COVERAGE
- Honesty/label tests assert HARDCODED literals ("INFERENCE","RQ-1","RQ-2","NAVD88","centroid","not height above sea level","us_survey_foot", FEATURE_CODE_LABELS values, the full crs dict) — not tautological by-value comparisons. Satisfies the hardcoded-literal rule.
- No by-value-import false survivors: mutations were applied to the connector SOURCE and re-exec'd, so the consuming namespace always saw the mutant (no false "survivor" from rebinding an already-imported copy).
- All 11 fixtures load-bearing: each is asserted by ≥1 test that fails if the body were wrong (layer_metadata, subject p1/p2, lot_polygon, condo, courtyard, placeholder, zero_height_condo, empty, web_mercator, error_object).

Assessment: 16/16 producer mutants and my 4 real-defect mutants all die; the 3 survivors are low-severity defense-in-depth test gaps with correct production behavior. The suite adequately guards AS-1..AS-5.

M5-T089 G4 VERDICT: PASS

END-OF-REPORT
