# M5-T037 — Human-journey review return (VERBATIM; human-journey-reviewer, read-only, pinned a6875731)

Orchestrator note: returned via the agent-return channel 2026-09-18T10:13:50Z; the transport
truncated the first delivery inside J6, BEFORE the findings. VERDICT: **PASS with required
corrections (F1, F2 block acceptance until applied; F3-F4 advisory)** — per the recorded
gate-verdict semantics this records as PASS with the corrections BLOCKING acceptance. Part 1
below is the truncated delivery verbatim; Part 2 is the resent remainder, verbatim.
(The reviewer's opening line notes its agent-memory write was refused by the read-only guard —
expected; no repository write occurred.)

---

## Part 1 (first delivery, verbatim)

Agent-memory write was refused (read-only), so I'll skip it per protocol and return the full report.

---

# G3 Human-Journey Review — M5-T037 (wide-street conditional FAR to screen + report)

**Verdict: PASS with required corrections.** Reviewed read-only from source at pinned material `a6875731`. Walking the six journeys against a real (server-shaped, inspectable) rule-evaluation document, the wide-street conditional FAR reaches the architect honestly and completely: the computed figure appears with what/units/conditions/sources and a DRAFT-pending-review posture, the higher figure is clearly conditional (never promised), the professional-review escalation withholds any unearned number, a documentless 1.0.0 property is untouched, and the report renders the same value from the same document. Two corrections should be applied before acceptance (F1, F2); F3–F4 are advisory. None of the findings are honesty/correctness failures on real data — they are a latent parity gap and a presentation (jargon) regression.

## Journey-by-journey

**J1 — Within 100 ft of a wide street (conditional row applies): PASS.** `DevelopmentLimits.tsx:62-81` (`WideStreetResult`) renders, in a labeled region ("Wide-street conditional FAR", `aria-label` present), a "Draft" badge, the governing FAR (e.g. 3.44), a units line ("a dimensionless ratio. Floor area = FAR × zoning-lot area (sq ft)", :73), the condition ("Applies within 100 ft of a wide street", :74), the not-verified draft marker (:76), and a "Wide-street sources and provenance →" link to `?view=evidence`. WHAT/UNITS/CONDITIONS/DRAFT/SOURCES are all present, and the higher figure is unambiguously marked conditional and draft, not promised.

**J2 — Why / the sources: PASS.** Following the link lands on the evidence view, which defaults `selection="calculation"` (`ArchitectEntry.tsx:79,83,87`) → `CalculationEvidence.tsx:24-41` renders the "Wide-street determination · D-052 provenance" section: reason, governing-FAR note, a definition list, and the full D-052 quintuple (source_versions, matched_geometry_refs, interpreted_bounds_summaries, classification_reasons, policy_decision_states) inside a collapsed `<details>` `CapturedRecord`. Calm and discoverable — the answer is on the limits card, the provenance is one click / one disclosure away.

**J3 — Professional review required: PASS (honest).** `WideStreetResult` gates on `determination_state === "professional_review_required"` (:65,69-70) and shows "Professional review required — the higher wide-street floor-area ratio is withheld…", rendering no number. I confirmed the server invariant that makes this safe: `professional_review_required ⟹ far_row=none ⟹ governing_max_residential_far=None` (`wide_street_wiring.py:466-500` `select_far_row_value`; every professional-review branch sets `FAR_ROW_NONE`). No unearned number is shown.

**J4 — No street determination (1.0.0 doc, no block): PASS.** `WideStreetResult` returns `null` when `evaluation.wide_street` is absent (:63); `CalculationEvidence` renders no wide-street section. Nothing invented. Serializer confirms the key is emitted only when a determination folded in (`integration.py:193-194`), and the web validator keeps a blockless body valid (`rule-evaluation-contract.ts:337-338`).

**J5 — Report parity: PASS on real data, with a latent gap (F1).** For an inspectable document, `ReportView` embeds the same `DevelopmentLimits` (:68) and `CalculationEvidence` (:92) fed the same document, and `report-view.test.tsx` asserts the identical `wide-street-result` text and the shared provenance render on both surfaces. A reader of the brief and a viewer of the screen agree — for documents the current server actually produces. See F1 for the non-inspectable edge case where they diverge.

**J6 — Three distinct concepts: PASS.** On the limits card the recorded city reference ("Residential FAR · city record / PLUTO reference", `development-reference-far`), the evaluated draft FAR ("Evaluated residential FAR / Dra[TRANSPORT TRUNCATION POINT]

---

## Part 2 (resent remainder, verbatim; delivered 10:15:29Z — report COMPLETE)

Resending the truncated remainder of the M5-T037 G3 report. Verdict: PASS WITH REQUIRED CORRECTIONS (F1 and F2 block acceptance until applied; F3/F4 advisory). I am read-only and changed nothing.

## J6 — Three distinct concepts (rest): PASS
On the limits card the recorded city reference ("Residential FAR · city record / PLUTO reference", testid `development-reference-far`), the evaluated draft FAR ("Evaluated residential FAR / Draft rule result", testid `development-evaluated-far`), and the new computed conditional row (its own bordered "Wide-street conditional FAR" panel with a Draft badge) are verbally and visually distinct. Built FAR stays behind the "Existing building information" disclosure, not on this card (development-limits.test.tsx:154 asserts 2.61 is absent from the region). D-073-R006's separation of recorded record vs city reference vs computed allowance holds.

## Accessibility / posture: PASS
Both new surfaces carry meaning by text, not color: "Draft" is a textual `architect-status` span; the escalation and DRAFT state are text; the wide-street region is labeled (aria-label="Wide-street conditional FAR"); provenance sits in native keyboard-operable <details>; the sources link is a real focusable <Link> with visible text; heading order is h2 (Development limits / How this was calculated) -> h3 (wide-street) with no skips. No color-alone signaling in the new code. DRAFT / not-verified vocabulary is reused, not reinvented; no published/verified/compliance claim is introduced on these surfaces. (One jargon caveat: see F2.)

## F1 (REQUIRED CORRECTION) — Report feeds DevelopmentLimits an UNGATED evaluation; the screen gates it, so screen and report can quietly disagree.
Defect: On every screen surface, DevelopmentLimits receives the inspectability-GATED evaluation (ArchitectEntry.tsx:73 `evaluationIsInspectable(...) ? id : null`, passed at :95/:101/:110 -> null when not inspectable). On the report, ReportView.tsx:68 passes the UNGATED `identityEvaluation` to DevelopmentLimits, while CalculationEvidence at ReportView.tsx:92 gets the gated `evaluation`. `WideStreetResult` reads `evaluation.wide_street` with NO inspectability guard (DevelopmentLimits.tsx:62-81) — unlike every other value-bearing reader, which flows through `evaluationIsInspectable` (development-limits.ts:95-97,116).
Consequence: for a validator-valid but non-inspectable document that still carries a `wide_street` block, the REPORT shows the wide-street FAR panel (a number) WITHOUT provenance (CalculationEvidence gets null) and WHILE the report's own IncompleteEvaluationNotice (ReportView.tsx:67) says "Numerical summaries are unavailable" — self-contradictory — whereas the SCREEN shows nothing. That is exactly the "screen and report quietly disagree" D-073-R003 forbids, on the very value this packet exists to keep in parity. Latent: not reachable via current honest server output (server always emits readable traces alongside the block) and not exercised by any test (report-view.test.tsx uses the inspectable draftApplicableDoc()).
Location: apps/web/src/components/architect/ReportView.tsx:68 (feed) + DevelopmentLimits.tsx:62-65 (unguarded reader).
Fix that satisfies me: in ReportView, pass the gated `evaluation` (not `identityEvaluation`) to DevelopmentLimits so it matches the screen; OR guard `WideStreetResult` with `evaluationIsInspectable`. Then extend report-view.test.tsx with a non-inspectable-but-carries-wide_street fixture asserting the report withholds the panel exactly as the screen does.

## F2 (REQUIRED CORRECTION) — The calm answer-first panel renders raw server strings that leak internal identifiers, and the tests mask it with sanitized fixtures.
Defect: WideStreetResult prints `{wide.draft_label}` (DevelopmentLimits.tsx:76), `{wide.reason}` (:77), and `{wide.fallback_direction_note}` (:78) verbatim in the PRIMARY panel. The ACTUAL server strings carry engineer/legal tokens into a first-time architect's calm surface:
- draft_label = DRAFT_LABEL_NOTICE -> "…(D-045-R009). This wide-street determination feeds a needs_review draft rule and is subject to G6 qualified-human legal review…" (wide_street_wiring.py:130-135).
- fallback_direction_note = FALLBACK_DIRECTION_NOTICE -> "D-051 fallback direction (validated for the ZR 23-22 conditional-FAR rows only)…" (wide_street_wiring.py:144-149).
- reason = the fold reason -> "wide-street determination within_100ft_of_wide_street: the wide-street (higher) conditional-FAR row governs (max_residential_far 3.44); DRAFT pending G6. …B4 aggregate intersects…" (integration.py:538-543 + wide_street_wiring.py:429-433).
So on real data the panel shows directive IDs (D-045-R009, D-051), a ZR section number, raw enum tokens (within_100ft_of_wide_street, max_residential_far), and internal engine phrasing — contrary to the DISPLAY CONTRACT's "calm presentation" and the frontend rule "keep legal text and legal logic out of components." The vitest fixtures use hand-cleaned strings ("DRAFT — not a verified legal determination"; "On uncertainty the higher wide-street FAR is withheld; the conservative row governs"; "Wide-street row governs: within 100 ft of a wide street; DRAFT pending G6") in development-limits.test.tsx:528-544 and report-view.test.tsx:19-36, so the green tests do NOT reflect what a real property renders — the jargon is masked.
Location: apps/web/src/components/architect/DevelopmentLimits.tsx:76-78.
Fix that satisfies me: keep only clean, human-phrased text in the primary panel — the panel already carries sufficient clean lines (the units line :73, "Applies within 100 ft of a wide street" :74, "Pending qualified legal review (not verified)" :76-tail). Move the raw reason/draft_label/fallback_direction_note strings to the detail-on-demand area (CalculationEvidence already renders them behind disclosure). And make at least one test assert against the REAL server-produced strings (or the actual constants) so the jargon can't silently return.

## F3 (advisory) — Divergent gating relies on an unenforced server invariant.
WideStreetResult withholds the number by determination_state (screen), but CalculationEvidence.tsx:29 shows/withholds by `governing_max_residential_far != null` (evidence). They agree only because the server guarantees professional_review_required -> far_row=none -> null FAR (wide_street_wiring.py select_far_row_value). The web validator checkWideStreet (rule-evaluation-contract.ts:337-386) does not enforce that cross-field rule. Consider gating both surfaces uniformly on determination_state.

## F4 (advisory) — "Wide-street conditional FAR" heading over a conservative value in the not-within case.
For not_within_100ft_of_wide_street, the panel shows governing_max_residential_far (the standard/conservative FAR) under the heading "Wide-street conditional FAR" with the clarifying note "Outside 100 ft of a wide street; the conservative floor-area ratio governs" (DevelopmentLimits.tsx:68-74). The note disambiguates, but a first-time reader could momentarily read the number as a wide-street bonus. Consider a heading/label that reflects the governing row in the not-within case.

## Closing
Journeys 1-5 (within-wide, sources/why, professional-review escalation, documentless 1.0.0, report parity) and journey 6 (three distinct concepts) all render honestly and correctly for real, inspectable server documents; units, conditions, DRAFT posture, honest escalation, and screen/report parity are present. The two required corrections are display-only (ReportView.tsx / DevelopmentLimits.tsx) and do not touch the backend, schema, or validator, whose correctness (including the professional_review -> null-FAR invariant) I confirmed from source at a6875731.

---

## Delta-attestation round 1 (VERBATIM; delivered 10:22:08Z at head 8240a497 — F1 satisfied; F2 casing regression found; the recommended one-line fix was applied at 1c89922c immediately after)

Delta-attestation delivered to the team lead. Summary of the resubmission review at head 8240a497:

**F1 — satisfied exactly.** `ReportView.tsx:72` now feeds `DevelopmentLimits` the inspectability-gated `evaluation` (defined at `ReportView.tsx:30`), matching the screen and `CalculationEvidence`. The new guard test at `report-view.test.tsx:92` forces a non-inspectable document and asserts the report withholds the wide-street panel exactly as the screen does. Closed.

**F2 — content intent met, but the change introduces a CI-red regression.** The raw server strings (`draft_label`/`reason`/`fallback_direction_note`) are correctly gone from the calm panel, and the new jargon-guard test (`report-view.test.tsx:110`) uses the verbatim wiring constants to assert D-045-R009 / D-051 / needs_review / raw enum tokens / ZR 23-22 never reach the panel. However, the new static line at `DevelopmentLimits.tsx:81` reads lowercase "pending", while the pre-existing, un-updated assertion at `development-limits.test.tsx:563` requires capital "Pending qualified legal review" via case-sensitive `toHaveTextContent`. The rework (commit 75daaebd) touched only three files and did not update `development-limits.test.tsx`, so the two suites now contradict each other on casing — the within-wide test will fail and the web vitest CI job on 8240a497 will go red (a concrete prediction, not just "CI pending").

**One-line fix I recommend:** update `development-limits.test.tsx:563` to lowercase "pending qualified legal review" (that file is in allowed_paths; it also matches the new `report-view.test.tsx:136` assertion).

**Verdict on the resubmission:** F1 PASS; F2 needs that single casing fix before my PASS stands unconditionally. Nothing else in the corrections needs changing. F3/F4 remain backlog advisories. I made no file changes (read-only).

Two other reviewers' returns landed on disk while I worked (G1 data-contract PASS, G5 security PASS, G3 code-review PASS) and independently converged on the same F1 defect and remedy; none of them flagged the F2 casing regression, so my prediction is the load-bearing new signal for the orchestrator to act on before waiting out CI.

---

## Delta-attestation round 2 (VERBATIM; delivered 10:23:31Z at head 1c89922c — FINAL: PASS UNCONDITIONAL)

Confirmed at HEAD 1c89922c466444ee90bce32ab70f208e088862db (verified read-only). F2 is now closed and my PASS stands unconditionally, subject only to the orchestrator-captured CI green you record separately.

Verification:
- The diff 8240a497..1c89922c is exactly one material line — development-limits.test.tsx:563 now asserts lowercase "pending qualified legal review" (tagged [ORCH-CORRECTED per M5-T037 HJ delta-attestation]); everything else in the diff is control-plane (M5-T037.json, state.json, M5-T037 report json). No production code changed: DevelopmentLimits.tsx and ReportView.tsx are byte-identical to 8240a497, so my F1 (gated report feed) and F2 (jargon removed from the calm panel) verifications carry forward unchanged.
- The casing contradiction is resolved: the assertion "pending qualified legal review" is a case-sensitive substring of the component's line "Draft — pending qualified legal review (not verified)." (DevelopmentLimits.tsx:81), and it now agrees with report-view.test.tsx:136. The within-wide test (development-limits.test.tsx:555) will pass; the predicted CI-red failure is removed.

Final G3 human-journey verdict for M5-T037 at 1c89922c: PASS (unconditional). Required corrections F1 and F2 both applied and verified from source; F3 (uniform determination_state gating across the two surfaces) and F4 ("Wide-street conditional FAR" heading over the conservative value in the not-within case) remain non-blocking backlog advisories. I made no file changes (read-only).
