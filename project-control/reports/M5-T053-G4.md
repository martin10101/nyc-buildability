# M5-T053 G4 — independent QA review (qa-engineer, saved VERBATIM from the two agent returns)

**G4 QA VERDICT: PASS** (3 ADVISORY findings, 0 BLOCKING)

All four permitted commands executed green at HEAD `31cbc74b` (byte-identical to material head `25617bbd` on T053's five allowed_paths — `git diff` empty; the later T051 cherry-pick `f1ec64a8` is disjoint, touching zero `tests/api` files).

**Executed counts (services/api cwd; repo root for modularity):**
- `python -m ruff check .` → `All checks passed!` (exit 0)
- `python -m pytest tests/api/test_proposal_validation_api.py -q` → **43 passed**
- `python -m pytest tests/api -q` → **497 passed**
- `python tools/modularity_check.py --check` → failures 0; 20 pre-existing warnings, none an M5-T053 file
- 43 = 29 test functions, several parametrized (verified by counting the file). CI run 35454834946 = success / 0 non-success jobs at head `25617bbd`; consistent with my local reproduction.

**AS-by-AS (binding tests + hardest probes):**
- **AS-1 (global budget):** `test_global_budget_at_budget_accepted:250` (5000 → 200), `_one_over_refused:259` (5001 → 422, field `proposed_massing`, "MAX_TOTAL_VERTICES"), `_many_levels_probe_refused_before_quadratic_work_gate_level:271` (500×50 = 25005), `_via_route:283`. **Mutation-check answered:** remove the global budget and all three refusal tests fail — their blocks (`[45]×110+[46]`=111 levels; `[50]×500`=500 levels) each sit under `MAX_OUTLINE_VERTICES` and at/under `MAX_LEVELS`, so the validator alone would ACCEPT them. The many-levels probe asserts the gate's OWN type `ProposedMassingInputError` (raised at gate:167 before the `validate_proposed_massing` delegate at gate:179), which structurally proves refusal happens BEFORE the quadratic simplicity test. Strong.
- **AS-2 (string ceilings + bounded-repr):** `test_string_ceiling_exact_boundary:316` parametrized over all four fields (author, editor_version, parent_scenario_id, wall[0].id) — each 512 → 200, 513 → 422 at the exact field (matches gate `len > MAX_STRING_LEN`). `_attacker_length_wall_id_uses_bounded_repr:332` — 2000-char id → full value absent, `"z"*100` absent, "truncated" present, message ≤ 512 (gate `_REPR_DISPLAY_CAP=80`). Exact boundary + echo assertion both solid.
- **AS-5 (B0 passthrough):** `test_b0_valid_fixture_accepted:529` (both valid fixtures → 200), `_b0_semantically_invalid_fixture_refused_same_field:537` (both → 422 at `proposed_massing.outline`). Independently confirmed the field pin against the B0 unit pack: `test_open_ring_refused` and `test_self_intersecting_outline_refused` both assert `proposed_massing.outline` (test_scenario_proposal.py:204,220). All four fixture files exist. Fields match.
- **AS-6 (monotone):** proven by **construction** — `validate_proposed_massing_input` delegates to the unchanged validator unconditionally-last (gate:179), so it can only ADD refusals — plus 5 spot cases (`test_gate_refuses_whenever_validator_refuses:575`, parametrized wrong_srid/open_ring/bad_kind/dup_wall/neg_height) asserting `gate.field == validator.field == expected`, plus `_delegates_a_defect_it_does_not_own:594` (bowtie reaches validator) and `_input_error_is_a_validator_error_subclass:611`. Adequate: construction makes exhaustive proof unnecessary; spot cases confirm same-field.
- **AS-4 echo:** `test_valid_block_accepted_echo:216` asserts `set(body) == {result,kind,block_digest,correlation_id}` (exact key-set — stronger than grep), `_carries_no_derived_value:229` greps 9 allowance-vocab terms absent. No derived value. AS-3/AS-7/AS-8 all bound and green (413 declared+streamed+boundary, 422 malformed/empty/non-object/NaN/Inf, no-leak on 422, no mutation, one mount line, matrix).

**Findings:**
1. **ADVISORY** — The `(500, "internal_error")` path has no behavioral test. Both emit sites (`proposal_validation.py:304-309` validator-raises-unexpected; `:321-327` echo-serialization-failure) are unexercised; only matrix *membership* is asserted (test:618). The 500 body's no-leak property (generic message, correlation-id-only log at `proposal_validation.py:305`) is untested. Defense-in-depth catch-all, hard to trigger without monkeypatching — not an enforcement gap.
2. **ADVISORY** — The route's `_bounded_message` truncation branch (`proposal_validation.py:124-129`) is never exercised; every tested refusal message stays under the 400-char cap. This branch is the sole bound on a validator-embedded value for a field the gate does NOT ceiling — notably `provenance.kind` (enum-validated, correctly excluded from the gate's DB-034(b) enumerated list of author/editor_version/parent_scenario_id/wall-ids). Verified safe by inspection: a long `kind` → validator refusal → route caps to 400 at `:303`; the refusal log at `:299-302` emits only field+correlation_id (no message), so no unbounded echo or log. Enforcement present in code; only the direct test is missing.
3. **ADVISORY** — The strict-JSON guard's non-encodable-string (unpaired-surrogate) sibling (`proposal_validation.py:285-292`) is documented but only the NaN/Infinity half of the same `json.dumps(allow_nan=False)` guard is tested (`test_...py:495,503`). Same code path; partial coverage.

**Basis:** All DB-034(a)/(b) security controls — global vertex budget (pre-delegation, mutation-proven), per-field string ceilings at exact boundaries, bounded-repr on the gate's own fields, oversized-body 413 via bounded streaming, strict-JSON NaN/Inf refusal, 422 no-leak, acceptance-echo purity (exact key-set, zero derived values), monotone-by-construction delegation, and B0 fixture passthrough with independently-confirmed field pins — carry binding tests that ran green and, where probed, catch the removal mutation. The three advisories are untested defense-in-depth branches, not gaps in enforcement. Recommend acceptance; the advisories are optional follow-up test coverage, not corrections. **Verdict: PASS.**
