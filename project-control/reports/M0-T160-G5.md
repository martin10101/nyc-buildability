# M0-T160 — G5 security review (security-reviewer "sec-m0t160", read-only)

> Transmission history: pinned at 481e51ff, delivered as three SendMessage parts (1/3..3/3) ending with END-OF-REPORT;
> no truncation. Joined verbatim by the orchestrator (transport wrapper tags removed only). Verdict PASS: the binding pins
> the FROZEN recertified candidate a3f24ff3 (not the integrated ledger tree); the installer's fail-closed checks are intact;
> no owner command retypes a hash. ADVISORY-1 (two stale runbook reference lines - the M0-T142 freeze-record pointer and
> the pre-B-025 config SHA; both fail safe) is routed at the accept seam; commissioning uses recertification section 5.

---

M0-T160 G5 SECURITY REVIEW (read-only) — Part 1/3: identity integrity.

HEAD at start 481e51ff (expected b4fa0b53 or later — OK). Material: 8df2bec5 (round-1 binding) + 83fdb5bd (round-2 runbook+test) + harvest b4fa0b53. Blob identity at HEAD matches the G2 record EXACTLY: source_binding.json 4f6fdfdb, CONTROLLER_UPDATE_RUNBOOK.md 4ee5a21b, test_runbook_parse.ps1 2288cb62. No commit after b4fa0b53 touches any of the 4 allowed paths — the reviewed material identity is stable at HEAD.

IDENTITY INTEGRITY — VERIFIED via git plumbing (shared object store):
- commit_sha a3f24ff3825c126c038f59b1ea6d2352f29d724a == a3f24ff3^{commit}
- commit_tree_sha 82432361540c3c2a11c55ffa3d6d485426cd45f7 == a3f24ff3^{tree}
- subtree_tree_sha 9c0b14eaa56ce32d241c77f789266035b584380c == a3f24ff3:tools/agent_supervisor (204 files)
- a3f24ff3 reachable on origin (origin/task/M0-T159-cli-2-1-281-admission); parent = a5886dab (the superseded source)
- all 12 required_modules exist at a3f24ff3 (git cat-file -e each)
- a5886dab→a3f24ff3 diff (a5886dab is the direct parent; via git show): 12 files, ALL M0-T159 paths — 3 reports, 4 new 2.1.281 JSON fixtures, 4 tests, and one production file tools/agent_supervisor/event_drift.py. That file is a BENIGN fixture-pointer re-point (hook_event_catalog_2_1_252.json → _2_1_281.json + comment); no logic/IO/import/network change.

NOT-A-LOOK-ALIKE — VERIFIED: M0-T159 is accepted (status=accepted, 100%); recert report records 3662 passed / 2 skipped / 0 failed on the certified tree; M0-T159 gates G0/G2/G3/G4 all PASS + DCV PASS 3/3.

SUPPLY-CHAIN GUARD (key finding): the binding pins the FROZEN CANDIDATE a3f24ff3 / subtree 9c0b14ea — NOT the integrated ledger commit cfc3d22c (subtree 11d43515), which additionally carries UNCERTIFIED M0-T149/M0-T152 supervisor code (cli.py/evidence.py/policy.py/gate_wave.py). The G3/G4-reviewed sha fa7e5fae carries subtree 11d43515 (integrated), but the recert suite (3662/0) ran on the frozen a3f24ff3 branch cut from certified a5886dab, and the installer's subtree_tree_sha check (ps1:388-390) refuses 11d43515. This is exactly the guard recert §5.0 mandates — the binding correctly names the isolated recertified content, not the integrated tree. Continued in Part 2/3.

---

M0-T160 G5 — Part 2/3: installer fail-closed + section-4 commands + lockstep.

INSTALLER FAIL-CLOSED VERIFICATION — INTACT. update_controller_from_candidate.ps1 is NOT touched by M0-T160 (forbidden path; absent from every diff). Confirmed still binding:
- reads all binding keys incl. the 3 identity values + required_modules + source_worktree (ps1:293-307)
- refuses any of the 3 SHAs not matching ^[0-9a-f]{40}$ (ps1:320-330) → no mutable ref, no short SHA
- verifies origin identity (358), commit-exists+is-commit (365-372), commit-tree match (374-382), subtree match (383-390), every required_module at the pinned commit (392-397), and source worktree DETACHED at exactly commit_sha + clean (405-419, 649)
- schema stays controller_update_source_binding/v2; every transaction key (controller_root, backup_root, a1_runtime_dir, backup/rollback/manifest/evidence) byte-identical. Only the 3 identity fields + 1 appended authority sentence moved (8df2bec5). AS-2 holds.

SECTION-4 OWNER COMMANDS — NO RETYPED HASH. The only commit SHA in the install step is in the PROSE pin paragraph (line 84, full 40-hex). The fenced ```powershell blocks (112-114 -Phase install; 118-120 worktree remove) carry NO commit SHA — the installer reads it from the binding; the owner never retypes it. Fenced blocks are byte-identical to 83f86ce0 (that commit is the G0 re-record and does NOT touch the runbook; the only runbook-touching commit 83fdb5bd changed ONLY prose lines 83-88 — no ``` fence). No angle-bracket placeholder in section 4.

TEST IS LOAD-BEARING + MEANINGFUL (test_runbook_parse.ps1): asserts no 'origin/main' anywhere (39), no '<' in section 4 (82), no 64-hex runtime key retyped in section 3 (101), no manual robocopy / no newest-backup auto-select, schema v2, and the 3-way lockstep commit_sha == $pinnedSha == runbook-contains-SHA (87-88). Change = only $pinnedSha (line 27) + one comment (26).

LOCKSTEP + HARVEST: 3-way static equality at HEAD confirmed — binding commit_sha, test $pinnedSha, runbook line 84 all byte-identical a3f24ff3825c126c038f59b1ea6d2352f29d724a. G2: orchestrator ran run_ps_tests.ps1 at 777e6b81 → EXIT 0, 7/7 files PASS; RED at pre-rescope a612652b (1 assertion) proves the lockstep bites. Old full SHA 3f4cee8680… gone from runbook+test; short refs a5886dab/e0dd4a3a/3f4cee86 remain only as correct supersession provenance (line 87).

SCOPE / NO MACHINE CHANGE: exactly the 4 allowed paths changed; no forbidden path touched. Reviewed contract commit only — nothing installs, B-026 stays open, commissioning stays owner-typed (recert §5). My review ran read-only git plumbing + reads only; no supervisor verb, no update script. Continued in Part 3/3 (findings + verdict).

---

M0-T160 G5 — Part 3/3: findings + verdict.

AUTHORITY SENTENCE — claims verified: D-024-R287 (2.1.281 admission), task M0-T160, candidate a3f24ff3, "M0-T159 accepted" (status=accepted confirmed), recert "3662 passed / 2 skipped / 0 failed" (matches recert §2), "superseding a5886dab" (the prior binding value). The a5886dab (M0-T148) LOW-1/LOW-2 G5 residuals stay preserved in the append-only authority field. Runbook pin-paragraph supersession chain accurate.

FINDINGS — no CRITICAL / HIGH / MEDIUM.

ADVISORY-1 (LOW; non-blocking; PRE-EXISTING + out of M0-T160 scope) — runbook static references lag current state:
(a) docs/CONTROLLER_UPDATE_RUNBOOK.md:122-124 still tells the owner to compare the installer's printed identity against project-control/reports/M0-T142-freeze.md (an OLD candidate's freeze record; last touched by M0-T142 commit 8c604681; stale since M0-T146).
(b) line 19 "Expected protected-config SHA-256" 4c67875b… predates the B-025 config edit (current LF 34f4fe90…, per recert §5.1).
Neither was in M0-T160's DB-063 re-scope (pin paragraph + test + binding only). Both fail SAFE: a mismatch makes the owner STOP, never a wrong install; and the authoritative commissioning path is recert §5.0/§5.3, which names a3f24ff3 correctly. RECOMMEND a follow-up doc-consistency task (point line 123 at M0-T159-recertification.md §5.0; refresh line 19). Does not block this task.

INFO-1: M0-T159 has no standalone G5 file; its required gates were G0/G2/G3/G4 + DCV (all PASS). Security-benign: the a5886dab→a3f24ff3 delta adds no dependency, external I/O, secret, or network surface (fixtures/tests/reports + one benign fixture-path re-point); the installed tree was G5-reviewed at base a5886dab (M0-T148-G5). This M0-T160 G5 covers the re-pin that makes a3f24ff3 the live install source.

INFO-2: the ordinal "275th" is not security-material; M0-T159 is confirmed accepted (exact ordinal is a ledger/DCV detail).

CHECKLIST: cross-tenant isolation / service-role secrecy / private storage / SSRF / injection / upload controls / prompt-injection — N/A to this change (a 3-value binding re-pin + doc/test lockstep; no runtime code path, no secret, no I/O added). Least privilege: producer stayed in 4 allowed paths; installer authority unchanged; reviewers read-only. Log redaction: N/A. No machine change: confirmed.

M0-T160 G5 VERDICT: PASS

END-OF-REPORT
