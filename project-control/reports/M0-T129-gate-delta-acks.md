# M0-T129 - G3/G4 delta-ack confirmations on the C1/N1/N2 protocol fixes - VERBATIM agent returns (transport entity-decoding only)

Orchestrator capture note: both independent reviewer identities confirmed their PASS verdicts
stand at the corrected protocol text (report-only edit `1d4ecea`).

---VERBATIM-G3-BEGIN---
All three corrections are correctly applied.

**CONFIRMED.** At material-stable identity (tools unchanged; report-only edit at `1d4ecea`), the delta `git diff 9e60d05 1d4ecea` shows all three of my findings correctly resolved: (C1) the fact-6 row now cites the **actual live mechanism** - "`run_task_queue`'s ordered-queue iteration + `evaluate_eligibility` (eleven fail-closed categories) + the `is_advanced` already-done skip - LIVE since M0-T128 (`record_advancement` drives exactly-once)" - and explicitly labels "the standalone `select_next_packet` helper remains **simulation-only with zero production callers**," matching the code I verified (grep confirmed `run_task_queue` calls `evaluate_eligibility`/`is_advanced`/`record_advancement`, never `select_next_packet`); (N1) the category-2 prerequisite is added verbatim - "the queue entry's `task_id` equals the packet's own `task_id`" - matching next_task.py:578; and (N2) "is clean" is removed from the eligibility list and reworded as a preparation duty - "Worktree git-CLEANLINESS is a PREPARATION duty, not an eligibility category," which is accurate since `evaluate_eligibility` checks `isdir`/not-primary/launch-seam binding but not git-cleanliness. No overclaim introduced, no other text disturbed, and the commissioning package is now code-accurate. My prior M0-T129 PASS stands with C1/N1/N2 now cleared.
---VERBATIM-G3-END---

---VERBATIM-G4-BEGIN---
**CONFIRMED** - my PASS verdict stands at the revised text (identity `1d4ecea`, tip at/after `5a4c96b`). The diff is report-only (protocol .md plus control-plane files; no supervisor code or test touched), the revised `M0-T129-commissioning-protocol.md` is ASCII-clean (0 non-ASCII bytes), and the consistency sweep is still clean (no "orchestrator executes", no "autonomy already proven", no "fewer than seven facts" claims - all seven facts still require the owner-run live journey). All three G3 corrections match exactly what I verified at M0-T128: (1) the fact-6 mechanism now correctly cites `run_task_queue`'s ordered-queue iteration + `evaluate_eligibility` (eleven fail-closed categories) + the `is_advanced` skip, with `record_advancement` driving exactly-once, and labels `select_next_packet` simulation-only with zero production callers - which I re-confirmed by grep (its sole reference is inside `advance_and_select`, which itself has no production caller); (2) the category-2 `task_id`-match prerequisite is now listed in s3, matching `evaluate_eligibility`'s `task_id_mismatch` check; and (3) worktree git-cleanliness is correctly reframed as a preparation duty rather than an eligibility category, since the engine checks existence/not-primary/launch-seam-binding but not cleanliness. The corrections make the protocol strictly more accurate with no regressions.
---VERBATIM-G4-END---
