# M0-T159 — G3 independent code review (code-reviewer "cr-m0", read-only)

> Transmission history: pinned at 4de4f5ec (HEAD advanced to 114e5e56 during the review via disjoint
> commits; the 12 reviewed paths and tools/agent_supervisor + tools/controller_update verified
> unchanged), delivered as five SendMessage parts (1/5..5/5) ending with END-OF-REPORT; no truncation.
> Joined verbatim by the orchestrator (transport wrapper tags removed only). Verdict: PASS with REQUIRED
> corrections F1-F3 (F4/F5 folded) - recorded PASS; the corrections block acceptance and any
> commissioning hand-off (2026-07-16 gate-verdict rule).

---

M0-T159 G3 review, part 1/5: pins and independent checks (cr-m0, read-only).

PINS: ctl24 HEAD was 4de4f5ec94f7 at start and 114e5e56 at send. The 3 peer commits in between are disjoint: `git diff --stat 4de4f5ec 114e5e56` over the 12 reviewed paths, tools/agent_supervisor and tools/controller_update is empty. The material commit a3f24ff3 (wt-m0t159, clean, parent a5886dab) equals cfc3d22c on all 12 files (`git diff --stat a3f24ff3 cfc3d22c -- <12 paths>` is empty). Versus a5886dab, the only change under tools/agent_supervisor apart from the 4 fixtures is event_drift.py.

INDEPENDENT CHECKS (no file writes; pytest run with -B -p no:cacheprovider):
1. The four re-pointed packs at a3f24ff3: 150 passed (62 s).
2. In-process mutants (pointers monkeypatched, no file edits):
   - The S8 live tooth, the capability live tooth and the native live tooth all go RED when fed the 2.1.252-era fixtures (catalog 2_1_252 / d032 / m0t132), and GREEN on the new ones.
   - The routing package tooth goes RED (routing_evidence_stale) when the 2.1.281 fixture is hidden.
3. Installed CLI:
   - `claude --version` = 2.1.281.
   - process.executable_identity = 92af58066c9c...4a2c, sha256_head+size, 240,767,648 B. This equals the routing fixture's cli_identity.
   - The runtime digest-keyed probe, probe_shell_routing_evidence(installed_identity=<real digest>), PASSES against shell_routing_2026-09-24_m0t159_2_1_281.json.
4. Manifest recomputed from the a3f24ff3 tree plus the reported config digest 34f4fe90...: 147 entries, manifest_digest 55dc71350b9f6b0a57e8e4121c62a9d2a4fe8914eea8bcd1ca4acdddbcec74b9. Exact match. event_drift.py e85c775c... also matches.
5. Hooks docs re-fetched: HTTP 200, 2,900,678 B (the same size the producer recorded). All 33 catalog events appear as page headings; no other event heading.
6. Test-node reconciliation by diff: a5886dab->a3f24ff3 renames exactly one test def (capability). No skip/xfail marker and no conftest was added or removed.
7. 2.1.281 `claude --help` lists every flag the live MRL worker launch uses: --restricted, --json-schema, --allowedTools, --disallowedTools, --agents, --tools, --settings, --strict-mcp-config, and --permission-mode (choices still include dontAsk).

---

M0-T159 G3 review, part 2/5: blocking findings F1-F2. Both are corrections to project-control/reports/M0-T159-recertification.md section 5; no code change.

F1 BLOCKING - wrong-commit hazard.
recertification.md:66-67 defines `<admitted-commit>` as "this unit's integrated commit sha". The integrated commit here is cfc3d22c. Its tools/agent_supervisor subtree (11d43515..., 148 covered files) carries the UNCERTIFIED M0-T149/M0-T152 changes: cli.py, evidence.py, policy.py and the new gate_wave.py.
The recertified candidate is a3f24ff3:
- commit a3f24ff3825c126c038f59b1ea6d2352f29d724a
- commit_tree 82432361540c3c2a11c55ffa3d6d485426cd45f7
- subtree 9c0b14eaa56ce32d241c77f789266035b584380c (147 covered files)
The installer would accept either commit, because it only checks that the binding is self-consistent. record-manifest, verify-controller and verify-manifest would all pass on a wrong install.
Fix:
- Name a3f24ff3 and both tree SHAs explicitly. The section-1 row at :14 also lacks them.
- Add a stop condition: the re-recorded activation manifest must show 147 files and digest 55dc71350b9f6b0a...; otherwise stop.
Checked for the re-pin: a3f24ff3 is in the bound source_repo and on origin, all 12 required_modules exist at it, and the origin URL matches.

F2 BLOCKING - the sequence is incomplete.
Section 5 claims to follow runbook sections 4->5->6->7->13.4 (:66), but it is missing:
- Runbook section 3 `-Phase backup`. Install refuses without backup evidence that matches the CURRENT live controller (update_controller_from_candidate.ps1:624-643: backup_evidence_missing / backup_stale).
- The section 4 removal step. If wt-controller-src still exists, install refuses with source_worktree_exists (ps1:645-648; runbook:114-118).
- Section 5a `-Phase verify-manifest`, the R611 source-provenance cross-check (runbook:155-173).
- The section 7 doctor, and the section 8 doctor --live (or a stated reason to skip it).
All of these fail closed, so nothing unsafe happens. But step (a) as written would be refused.

---

M0-T159 G3 review, part 3/5: F3 (blocking) and F4.

F3 BLOCKING - lanes 2 and 3 are not covered.
The installer's only destination is C:\SupervisorController\tools\agent_supervisor (source_binding.json:25). Lanes 2 and 3 run from C:\SupervisorController2 and C:\SupervisorController3 (three-loop-launch-2026-09-18.md:10-11; lane 3 is a "NEW byte-copy of instance 2"). The MRL start must run with cwd = its checkout (MRL_LAUNCH_RUNBOOK.md:88). So `python -m tools.agent_supervisor` loads that instance's own copy (cli.py:308, PACKAGE_ROOT).
Those copies stay at a5886dab after step (a):
- Their fixtures/ has no 2.1.281 routing fixture. The tooth reads the package's own directory (recovery_probes.py:600-605), so it refuses with routing_evidence_stale.
- If they bind the shared activation manifest (B-026 detail), event_drift.py differs and the controller_manifest check refuses.
Section 5(c) relaunches each lane without a step for this.
Fix: add a verified propagation step for instances 2 and 3: copy the installed subtree, byte-compare it against the source worktree, then run verify-controller from each instance. Or cite evidence that they load C:\SupervisorController's copy. I did not open those directories, per my instructions; the orchestrator can check read-only.

F4 ADVISORY (fold into the same edit).
- Section 5(c) uses placeholders. Each lane needs a FRESH launch manifest drafted after (a) and (b).
- OneShotRunner checks dispatch.claude_chain_sha256 and dispatch.claude_version against the live CLI (mrl_one_shot.py:249-254). A manifest drafted before 2.1.281 is refused there regardless of --repin-cli-identity, because the repin only rewrites the journal pin (recovery_probes.py:358-384).
- Also add a Set-Location to each lane's checkout.
- Side note (pre-existing, for the discovery backlog): MRL_LAUNCH_RUNBOOK.md:71-75 and :90 say --repin-cli-identity cures claude_version_mismatch. In fact that refusal comes only from the launch-manifest pin.

---

M0-T159 G3 review, part 4/5: advisory findings F5-F7.

F5 ADVISORY - one diff not disclosed.
- producer-report.md:24 says only claude_version and the help sha moved in the capability fixture. probe_meta.claude_binaries also went from 3 entries to 1.
- Cause: the npm `claude` and `claude.cmd` shims are gone from %APPDATA%\npm. I checked; only the codex shims remain.
- This is benign (it removes a PATH-shadowing candidate), but AS-2 asks for every diff to be stated.
- The routing fixture's assistant_events also dropped (2->1 and 3->2). That is stream granularity only: the tool sequence, the brokered denial, rc=1 and the verdict are all identical to M0-T120/M0-T132.

F6 ADVISORY - the "benign" help-sha ruling is sound, but its basis is narrow.
- Precedent supports it: the raw help sha also moved at the 2.1.246, 2.1.248 and 2.1.251 admissions while the flags stayed the same.
- But the capability probe's 12 flags and the native detection list skip the flags the live MRL lanes depend on (mrl_subagent_contract.py:426-436, mrl_one_shot.py:268).
- My 2.1.281 --help check (part 1, item 7) found all of them present. Suggest recording that check in the report.

F7 ADVISORY - pre-existing, not introduced here.
- The routing "drift tooth" is version-hardcoded. test_agent_supervisor_routing_probe.py:245-249 passes the literal "2.1.281", and :251-258 reads the digest from the fixture itself.
- So it never reads the installed CLI and would stay green on a future bump. Only the digest-keyed runtime gate protects.
- Also, fixtures/*.json fall outside the manifest's covered patterns (manifest.py:50-59), yet the start gate trusts fixtures/shell_routing_*.json.
- Both are worth a discovery-backlog entry.
- Wording nit: recertification.md:17 says the fixtures are "OUTSIDE the manifest root". They are inside the root; they just are not matched by the covered patterns.

---

M0-T159 G3 review, part 5/5: answers to the six questions, and the verdict.

(1) event_drift.py: only the comment block and CATALOG_FIXTURE_PATH changed (event_drift.py:37-47). Only tests read it; nothing uses it at runtime. Correct.

(2) Teeth: real and removal-sensitive (part 1, item 2). The capability test was repurposed in place (test_agent_supervisor_capability_probe.py:232-253). It keeps the same six checks: claude version, codex version, post-update codex version, file name, leak loop, [HOME] binaries. This is the same shape as the D-032 re-point e0dd4a3a. Nothing was weakened or deleted.

(3) Fixtures: all four are masked. No Users/MLFLL/drive path or secret; binaries are [HOME]-masked; the argv executable is redacted. The catalog, detection and routing diffs are described honestly; for the capability fixture see F5. The help-sha ruling is acceptable (F6).

(4) The counts reconcile: 3659 + 3 = 3662, 3664 collected and 2 skipped in both runs, no test removed (part 1, item 6).

(5) The order is correct: the repin comes last (recertification.md:88-94). For completeness and safety see F1-F4. As written, the commands fail closed. F1 is the only path to a silent wrong install.

(6) Acceptable, not blocking:
- The scope follows R287 and the M0-T132 four-fixture precedent, and the gap is disclosed (producer-report.md:90-95).
- The code guards against these facts: mrl_subagent_contract.py:381-394 refuses any inventory tool that has no explicit allow or deny rule.
- But these facts govern the actual lane worker launch, and nothing has yet run --restricted, dontAsk or --json-schema live at 2.1.281.
Conditions:
- Record this as a named residual risk.
- Treat the first lane launch as a supervised canary: one cycle, watch the audit log for any tool the profile should deny, stop on any anomaly.
- Open a follow-up to add these flags to the capability probe and re-measure the M0-T140..T142 canary facts at 2.1.281.
The "2.1.252" text in refusal strings is cosmetic.

VERDICT: PASS with required corrections F1, F2 and F3; F4 and F5 fold into the same edit.
- Under the 2026-07-16 rule this is recorded as PASS, and the corrections block acceptance and any commissioning hand-off.
- The corrections touch only recertification.md and producer-report.md, both outside the manifest, so no recertification is needed.
- They still require a re-freeze and a delta attestation from me.

M0-T159 G3 VERDICT: PASS

END-OF-REPORT
