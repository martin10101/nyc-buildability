# M5-T118 — G4 test review (qa-engineer "qa-t118", read-only)

> Transmission history: pinned at a120220e (material 992c47b1), delivered as 2 SendMessage parts ending with
> END-OF-REPORT; no truncation. Joined verbatim by the orchestrator (transport wrapper tags removed only). Verdict PASS; 15/15 own mutants; the six real
> PDFs re-run IN MEMORY by sha256 reproduce the producer's table exactly (items 1, 3, 4 read fully).

---

M5-T118 G4 test review — PART 1/2: PASS (verdict in Part 2). READ-ONLY. HEAD pinned a120220e (matches expected). Material 992c47b1 verified: the 8 material files are byte-identical 992c47b1->HEAD (only ledger/report differ), so I reviewed the code at HEAD.

GREEN BASELINE (orchestrator shim, Py3.11.9 — CI 3.12 is the authority):
- tests/drawings full suite: 272 passed (matches producer; 252 pre-P3, +20).
- test_sheet_p3_features.py: 20/20 passed.
- test_pdf_object_streams.py: 44 passed; pdf_object_streams.py + test_pdf_object_streams.py + sheet_marked_content.py byte-UNCHANGED f2ada974->HEAD (empty diff). Forbidden-path invariant holds.
- sheet_interpreter.py = 748 SLOC (< 750).

INDEPENDENT MUTATION (my own harness, consuming-namespace, in-process; 15/15 PASS — I re-derived PDFs + installed the mutants myself, did not just trust the committed tests):
Re-ran 5 producer mutants — each REAL reads / MUTANT refuses with the EXACT typed feature:
- P1 sheet_interpreter._SKIP_SHADING=frozenset() -> 'unsupported operator' (sh skip, ~L429).
- P2 sheet_interpreter.skip_inline_image->refuse -> whole doc 'inline image' (_op_bi ~L638).
- P3 _SheetInterpreter.begin_page->no-op -> 'operator count' (per-page reset load-bearing; call at sheet_reader.py ~L475).
- P4 skip under-consumes (resume at dict) -> fail-safe SheetRefusal, never wrong geometry (desync guard).
- P5 _StreamDecoder.begin_page->no-op -> 'page decoded bytes' (sheet_objects.charge_decoded).
My OWN 2 mutants:
- Inline-length row-alignment ceil (sheet_inline_image.py:208 row_bytes): W=9/H=1/G/BPC=1 => ceil(9/8)=2 bytes READS; floor=1 (drop the ceil) AND len=5 (overshoot) both refuse 'inline image'. The exact arithmetic is load-bearing.
- Operator-budget boundary (sheet_interpreter.py run() op-count ~L176-183): at cap=3 a 3-op page READS and a 4-op (cap+1) page refuses 'operator count / over 3 operators'. Strict-`>` boundary confirmed (cap admits exactly cap, refuses cap+1).

GOLDEN REVISIONS — exactly the intended cases, non-tautological: golden-dict diff f2ada974->HEAD touches ONLY refuse_unsupported_op content (sh->zz) + its per-case digest + _OVERALL; every other _GOLDEN entry unchanged. split-equivalence test passes (63/63 digests match current code), so the per-page/document budget split is behavior-preserving for the other 62 corpus cases. Continues in Part 2/2.

---

M5-T118 G4 test review — PART 2/2: independent real-corpus check, findings, verdict.

INDEPENDENT REAL-CORPUS CHECK (network live; each file downloaded IN-MEMORY via urllib, sha256-pinned to docs/research/architect-drawing-corpus-2026-09.md, fed to read_sheet; NOTHING written to disk). All six sha256 MATCH the note. My results reproduce the producer table EXACTLY:
1 | VA Div23 1/2 88pp | MATCH | READS 88pp, 332,057 polylines, 1,687,113 segpts, 1,567 text, 5 img, 0 sh, 0 inline
2 | VA Div23 2/2 94pp | MATCH | REFUSED path "'l' with no current point"
3 | VA DwgDelivRqmts 14pp | MATCH | READS 14pp, 1,611 polylines, 6,444 segpts, 2,898 text, 19 img, 4 sh, 0 inline
4 | Brooklyn Bridge HAER 8pp | MATCH | READS 8pp, 11 polylines, 38 segpts, 990 text, 8 img, 0 sh, 3 inline
5 | Frantz-Dunn HABS 1pp | MATCH | REFUSED 'scan only'
6 | Rothschild HABS 1pp | MATCH | REFUSED 'scan only'

Every count matches the producer's report byte-for-byte. The owner-facing headline "3 of 6 real architect PDFs now read fully (items 1,3,4)" is INDEPENDENTLY CONFIRMED, and all three P3 features are load-bearing on REAL files: item 1 reads via the per-page budget (332k polylines/88pp that the old 200k per-DOCUMENT operator budget refused), item 3 exercises 4 real `sh` skips, item 4 exercises 3 real inline-image skips. The producer's honesty holds exactly — no overclaim.

FIXTURE REALISM: synthetic fixtures use valid classic-xref PDFs + realistic operator streams; the real-corpus run confirms the same behaviors fire on genuine files. FLAKE RISK: LOW — committed tests are deterministic (synthetic bytes, monkeypatch-restored, no network/timing/random).

FINDINGS (cite file:line):
- BLOCKING: none.
- ADVISORY A1 (sheet_inline_image.py:212-232 _skip_determinable): a determinable length that overshoots by EXACTLY 1 is leniently accepted (my len+1 probe read) because it skips the single trailing whitespace and still lands on EI. SAFE — it consumes the separator, not a sample byte; undershoot and overshoot>=2 correctly refuse. Matches the producer's OPEN-Q4 fail-safe intent. No action needed.
- ADVISORY A2: item 4 "reads" as OCR/raster text + 11 stray vector paths, not true drawing linework — producer already disclosed this in DISCOVERIES; the vector-vs-scan class label belongs to a later human-confirmed stage. Not a defect.

Tests are adequate, non-tautological, and the owner-facing corpus claim is independently reproduced. (Note: could not write my qa-engineer memory — Write is blocked for this read-only reviewer; skipped per task rules.)

M5-T118 G4 VERDICT: PASS

END-OF-REPORT
