# M5-T053 G3 — independent code review (code-reviewer, saved VERBATIM from the two agent returns)

## G3 CODE REVIEW — M5-T053 — VERDICT: **PASS** (0 BLOCKING, 3 ADVISORY)

Reviewed at pinned HEAD `31cbc74b9c3202ab2a8a7c36db5e46c40e42c9b4` (verified via `git rev-parse`). Material cherry-pick `25617bbd`; `git show --stat` confirms it touches exactly the 5 allowed paths and **neither** `proposal.py` **nor** `tests/scenario/` — disjointness against the live M5-T051 lane holds (AS-7). All conclusions verified in actual source, not the code-graph.

**Basis:** The DB-034(a)/(b) hardening closes the two M5-T048-G5 findings correctly and precisely. The gate is a read-only monotone wrapper; the route is a fail-closed stateless validator that mirrors the accepted sibling internal routes (`condo_records`, `lot_geometry`) exactly. Constants are protective with documented arithmetic that I re-derived. Tests are genuinely binding.

### Findings

1. **GATE CORRECTNESS — verified.** The global vertex budget is counted before any quadratic work: `validate_proposed_massing_input` calls `_total_position_count` (O(number-of-outlines), an O(1) `len()` per outline, no coordinate examined) at `proposal_input_gate.py:166` and refuses at `:167-173` **before** the delegate at `:179`; the validator's `_ring_is_simple` (O(n²), `proposal.py:143-183`) is only reachable through that delegate. No crafted block reaches simplicity work before the count. The gate counts exactly the outlines the validator runs quadratic work on (base `block["outline"]` + each `levels[i]["outline"]`, matching `proposal.py:467-469, 352-354`); it overcounts by including the closing duplicate, which is conservative (monotone-safe). Budget arithmetic is protective and re-derived: with each single outline independently capped at 1000 by the validator's O(1) check at `proposal.py:220` **before** its quadratic test at `:279`, `sum(m_i²)` under `sum(m_i) ≤ 5000, m_i ≤ 1000` maximizes at 5×1000² = **5e6** segment tests (sub-second), versus the ~2.5e8 the per-outline ceilings alone admit (G5 finding 1). String ceilings cover all four field classes — `author`, `editor_version`, `parent_scenario_id`, every wall `id` (`:129-152`). Bounded-repr (`:80-88`, cap 80 + length marker) is applied to every user value the gate embeds in a refusal.

2. **MONOTONICITY — verified.** The gate never returns an acceptance; it either raises (budget/ceiling) or falls through to the unchanged validator at `:179`, so every validator refusal still refuses. Delegation is read-only import of `validate_proposed_massing`/`ProposedMassingError` (`:44`) — no monkeypatching or internal copying. No mutation (`test_gate_accepts_valid_block_without_mutation`). AS-6 is spot-proven same-field across 5 B0 classes (`test_proposal_validation_api.py:548-581`); field paths match the validator's (`proposed_massing.outline.srid`, `.outline`, `.provenance.kind`, `.exterior_walls[1].id`, `.levels[0].floor_to_floor_ft` — all confirmed against `proposal.py`).

3. **ROUTE — verified.** Body size bounded before parse two ways: an over-ceiling declared `Content-Length` fast path (`:243-250`, proven not to touch the stream, `test_...without_touching_stream`) plus authoritative bounded streaming that stops one chunk past the ceiling without draining or parsing (`_read_body_within_ceiling` `:203-222`, proven with a pull-counter on a no-Content-Length body). Strict-JSON NaN/Infinity/unpaired-surrogate refused via the **same** encoder settings the renderer uses (`:286`), so guard and renderer can't disagree; `json.loads` accepts NaN by default and the `allow_nan=False` re-dump catches it (tests confirm). Typed refusals only; generic 500 logs correlation id, never `str(exc)`/traceback (`:304-309`). Acceptance echo (`:315-320`) carries exactly `result`/`kind`/`block_digest`/`correlation_id`; `block_digest` is a SHA-256 identity fingerprint of the **input** (`:176-184`), not a derived zoning value — AS-4 greps allowance vocab clean.

4. **main.py — verified.** Exactly one import (`main.py:36`) + one `include_router` (`:184`) + a posture comment, layered on the accepted T052 `condo_records` mount. Fail-safe posture matches siblings: `internal_rule_eval_enabled()` is default-off for absent/empty/unknown (`config.py:41-49`), `include_in_schema=False`, and the 404 body `{"detail":"Not Found"}` is byte-identical to `condo_records`/`lot_geometry`.

5. **MODULARITY — verified.** Gate 179 SLOC, route 328 SLOC — both below the 600 warn tier, each a single responsibility (input-gate vs route). The wrapper-vs-fold-inward design note is present in the gate docstring (`proposal_input_gate.py:8-10`).

6. **TESTS — binding.** The many-levels probe (`test_..._gate_level`, `[50]*500` = 500 outlines each <1000 and ==MAX_LEVELS, sum 25005) asserts the gate's **own** `ProposedMassingInputError` subtype, structurally proving pre-delegation refusal of a block the validator would otherwise accept — the exact G5-A1 closure. Boundaries are at/over for every ceiling: budget 5000 accepted / 5001 refused; string 512 accepted / 513 refused per field ×4; body exact / +1. Both `semantically_invalid` B0 fixtures refuse at the pinned `proposed_massing.outline` field; both `valid` fixtures pass through (fixtures confirmed present on disk).

7. **PRODUCER-REPORT ACCURACY — sound.** No unexecuted claim: carry-forward blob identity is labelled **unverified** (a producer cannot capture `git hash-object`), the prior wrong-cwd exit-4/exit-1 failures are retained rather than hidden, and full-source collection / commit-digest / scenario-suite+CI are routed to the orchestrator. Honest-uncertainty posture is correct.

### ADVISORY (non-blocking)

- **A1 (test gap).** The route's `_MAX_REFUSAL_MESSAGE_CHARS = 400` truncation (`proposal_validation.py:120-129`) is the **only** bound on user values the *validator* embeds via `!r` (e.g. a duplicate wall id of ~500 chars that passes the gate's 512 ceiling, or a long non-`"proposed"` `kind`). The mechanism is present and correct, but no test drives a validator-embedded value between 400 and 512 chars to assert it truncates — the bounded-repr test (`:332`, 2000-char id) trips the *gate's* 512 ceiling first and never exercises this route-level path. Recommend one test binding the 400-char bound. Not blocking: the 400-cap plus the 256 KiB body cap make unbounded echo unreachable.
- **A2 (design coupling, matches accepted precedent).** The route reuses `INTERNAL_RULE_EVAL_ENABLED` rather than a dedicated flag, so it cannot be toggled independently of rule-evaluation. Consistent with the accepted `condo_records`/`lot_geometry` routes, and `app.config` is out of packet scope; noting only for the eventual fold-inward.
- **A3 (orchestrator verification).** The 43 route + 497 api pass / ruff-clean / modularity-0 claims are green in the material commit message but I did not execute them (read-only, thin client). The CI conclusion at the pushed head and the `tests/scenario` suite remain the orchestrator's to record per the report's routing. [Orchestrator note: recorded — CI run 35454834946 all jobs success at 25617bbd (M5-T053-ci-evidence.md); the orchestrator reproduced 43/497/ruff/modularity at harvest.]

**Verdict unchanged: PASS** (0 BLOCKING, 3 ADVISORY).
