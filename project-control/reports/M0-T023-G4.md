# G4 DIRECTIVE-COMPLIANCE RE-GATE REPORT — ROUND 2 (D-001 Amendment 3, M0-T023)

- Verifier: directive-compliance-verifier (independent; producer = orchestrator; producer != verifier)
- NEW reviewed head: `ed8a6776f89c9f2b17ecad35e528a0180fe6bfd1`
- NEW content-manifest identity: `9ab6c9601b1a60f903d1a399d57192f662d96e912fdc50304ba931894b3004be` — independently reproduced from `frozen_git_identity(M0-T023.allowed_paths, reviewed_sha=ed8a6776, exclude project-control/)`. MATCH.
- Round-1 head `bd8e1011` superseded. Rework delta = 3 files only (directive_registry.py, project_control.py, test_project_control.py); the entire directive registry (manifest/requirements/verification/migration_manifest/sources/schema) is byte-identical to round-1.

## OVERALL VERDICT: PASS (strengthened)

The rework closes the G3 code-lens bypass on R129 and hardens the identity/accept paths, with new passing adversarial proofs. All four suites are green at ed8a6776, every append-only/integrity invariant re-binds cleanly to the new identity, and nothing prohibited occurred. 41/47 new requirements are fully SATISFIED with reproduced primary evidence at 9ab6c960; the 6 Section-4 items are correctly-sequenced PASS-pending-orchestrator-artifact (post-gate), each with a named artifact below.

Suites at ed8a6776: test_project_control.py -> all 14 groups OK (S9 now includes PROOF 7b/7c); test_directive_compliance.py -> 55 OK; test_directive_reminder.py -> 12 OK; validate_directive_compliance.py --check --json -> valid:true, 0 errors.

## Rework re-assessment (coordinator-flagged requirements)

- R129 — SATISFIED (materially STRENGTHENED). Round 1 rested only on the claim-time guard; G3 found `progress()` could launder a not-in-regime legacy task from a non-continuation status into a continuation status, letting `_legacy_grandfather_check` grandfather it at accept. The rework adds a fail-closed guard in project_control.py:708-716 (`if enabled and not _task_in_regime(t) and target != "canceled" and cur not in _CONTINUATION_STATUSES and target in _CONTINUATION_STATUSES -> fail`), after the transition + roster checks, before `t["status"]=target`. New regressions PROOF 7b (blocked->awaiting_gate rejected + accept rejected) and PROOF 7c (rework->in_progress rejected) both pass. Regime entry is now provably claim-only.
- R125 — SATISFIED (boundary reinforced); the launder guard removes the alternate non-claim entry path.
- R126 — SATISFIED; already-active legacy tasks still finish (PROOF4); the continuation set can no longer be gamed.
- R132 — SATISFIED (STRENGTHENED); no bypass/suppression/exemption flag, plus GIT_LITERAL_PATHSPECS=1 and the progress-launder guard. `canceled` remains the only exemption (legitimate abandonment).
- R133 — SATISFIED (STRENGTHENED); nine proofs + PROOF 7b/7c (genuine: assert rc!=0 + "regime" + non-acceptance).
- Collateral (re-verified SATISFIED): R136/R138/R142 (N1 per-directive scoping); R149/R151/R153/R161 (identity hardening: --end-of-options, both-column R/C, N2 non-HEAD guard, 60s timeout). Same shared implementation (R152).

## Refreshed per-requirement matrix — R119..R165 @ identity 9ab6c960

Tally: 41 SATISFIED (7 strengthened), 6 PASS-pending-orchestrator-artifact, 0 VIOLATED, 0 INDETERMINATE-on-code.
- R119, R120 SATISFIED (reconciliation reproduced; 6e41fd8 is ancestor of 33fbb44).
- R121–R155, R157, R161, R163, R165: SATISFIED with reproduced primary evidence at 9ab6c960 (registry byte-identical to round-1; code re-verified at ed8a6776 via passing suites + source inspection). R129/R132/R133/R136/R138/R142/R149/R151/R153 strengthened.
- R156, R158, R159, R160, R162, R164: PASS-pending-orchestrator-artifact (Section-4 steps sequenced after this gate). Named artifacts:
  - R156 -> fresh gate records G3/G4(dc)/G4(control-plane)/G5 re-recorded at reviewed_sha=ed8a6776 + content_manifest_sha256=9ab6c960 (+ this report).
  - R158 -> the GitHub Actions run on pushed ed8a6776 (green web-e2e/api/contracts/locks/control-plane/context-budget/secret-scan).
  - R159 -> PR #97 body distinguishing frozen base 1acb9b5 / reviewed code SHA ed8a6776 / content identity 9ab6c960 / evidence-only commits / current head.
  - R160 -> same PR body with the "final head 6e41fd8" language removed.
  - R162 -> explicit enumerated list separating the reviewed CODE commit ed8a6776 from evidence-only/ledger-only commits.
  - R164 -> the 11-item owner return.

## Append-only / integrity findings @ ed8a6776 — all CONFIRMED

- Registry files byte-identical to bd8e1011 (git diff --stat on project-control/directives/ + schema/ = empty).
- source-001 (f3aa16d9), source-002 (888b3443), source-003 (dc4f2211): declared==actual, working-tree==git-canonical, all LF.
- requirements: 136 ids; requirements_id_digest 0edd2ecf MATCH; requirements_content_digest c93810ad MATCH (wt==git).
- migration_manifest_sha256 e14190fa MATCH (wt==git); 57 entries all reproduce from baseline 1acb9b5 (0 mismatches).
- locked_requirement_ids == requirements set (136); bd8e1011 locked ⊆ new locked (append-only holds; nothing renumbered/deleted).

## Owner-claim independent verification (all TRUE)

57 baseline task ids bound to 1acb9b5 (all digests reproduce); new-task fails closed without --directive-refs when the regime is enabled; v2 multi-task schema real + enforced per-task; identity git-canonical (blob object-ids + mode + type), not working-tree bytes; reminder hook fail-visible (exit 0), not fail-closed; M0-T023 cites D-001:ALL, applicable = all 136 (no selective citation).

## Prohibited-action confirmation (R163)

Rework delta = 3 tool/test files only (no product code/deps/schema/registry change). M0-T023 awaiting_gate, accepted_at:null; PR #97 OPEN, not merged, not closed; expansion hold intact; owner_approval merge_authorized:false, accept_authorized:false. Nothing merged/accepted/deployed/purchased/product-dispatched/closed.

Recommendation: Record round-2 as PASS for the D-001 amendment-3 directive-compliance dimension at reviewed_sha ed8a6776 / identity 9ab6c960. Stamp verification.json per-task identity to 9ab6c960 and flip the 136 rows to PASS at that identity (R156/R157). Do not accept M0-T023 until the six named downstream artifacts exist.
