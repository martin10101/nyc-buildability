# DCV report — M5-T026 (pre-acceptance directive-compliance verification)

> Saved VERBATIM by the orchestrator from the directive-compliance-verifier agent return
> (2026-09-14; transport entity-decoding only, per the report-preservation rule).

Verifier: directive-compliance-verifier (independent; producer = frontend-engineer, gate reviewers = code-reviewer/qa-engineer — I am none of these). Read-only throughout; no writes, no git/gh state changes, no `project_control.py` verbs.

Environment: primary checkout `C:/Users/MLFLL/Downloads/nyc-zoning/ctl24`, `git rev-parse HEAD` = `67af2a209e9250dd8f0d4daf5643824c3820637b` (matches pin). Working-tree dirt is confined to `.claude/agent-memory/**` and `scratchpad/` — zero overlap with M5-T026 scope, and the identity function's own clean-tree guard returned no error.

## (1) Conditional-restamp ruling (pre-authorized)

**Range f5d9719e..67af2a20 — ruled per commit, VERIFIED NOW.** The range contains exactly one commit, `67af2a20` itself ("M5-T026 G3/G4 gate records"), touching only `project-control/gates/M5-T026-G3.json`, `project-control/gates/M5-T026-G4.json`, `project-control/state.json`, `project-control/tasks/M5-T026.json` — none in M5-T026's `allowed_paths`. I recomputed the CLI's own identity (`project_control._task_git_identity(reg, task, reviewed_sha=None)` → `frozen_git_identity` with `require_clean=True`) at the live pin and got exactly `(858707a7a70dcabf054aa84db3a535be9744964a2a79a9b0e163b22a4c2c250f, 67af2a20…, None)` — equal to the `content_manifest_sha256` in both gate records. **Restamp of reviewed_sha from f5d9719e to 67af2a20 is authorized unconditionally.**

**For any later head H (e.g. if the DCV-report commit lands first), restamp is pre-authorized conditionally on exactly this, orchestrator-verifiable:** (a) `git diff --name-only 67af2a20 H` intersected with M5-T026's five `allowed_paths` is EMPTY (control-plane-only commits qualify); (b) `_task_git_identity` recomputed at H returns identity `858707a7a70dcabf054aa84db3a535be9744964a2a79a9b0e163b22a4c2c250f` with `err == None` (which also enforces the clean-tree guard at H); and (c) the task file's `allowed_paths` and `directive_refs` are byte-unchanged from the version verified here (lifecycle fields may move). If any condition fails, restamp is NOT authorized and DCV must re-run.

## (2) Per-requirement verdicts

| requirement_id | verdict | evidence (personally inspected at the pin) | note |
|---|---|---|---|
| **D-057-R001** | **PASS** | `apps/web/src/lib/rule-evaluation.ts:102` (`INTERNAL_RULE_EVAL_DEFAULT_ON_ENV_VAR`, no NEXT_PUBLIC_ prefix), `:114-118` (`ruleEvaluationDefaultOnEnabled`, same `TRUE_TOKENS` set from `:89`, absent/empty/unknown → false), `:131-139` (`ruleEvaluationSurfaceEnabled`: main flag off short-circuits `:134`; param absent → default-on var `:136`; param present → true iff true token `:137-138`, so present-non-true incl. `'off'` → off regardless of both vars); per-request read via `process.env` default args evaluated at call time, invoked from the Server Component `apps/web/src/app/property/page.tsx:38-39`. Harness: full 50-row (5 var-states × 10 param-shapes) matrix `rule-evaluation.test.ts:154-207` + main-flag-OFF-always-wins block `:209-253` incl. `default-on=1, ruleeval=on → false`. Grep of producer diff over the three code files: zero added `NEXT_PUBLIC` lines. | Semantics match the directive's recorded design decision (`source-001.md` lines 23-34) exactly, kill-switch preserved. |
| **D-057-R002** | **PASS** | `git show 0b112357 --name-status` = exactly the 5 allowed files (no e2e, no config, no package.json); explicit equivalence block `rule-evaluation.test.ts:255-302` deletes the var in `beforeEach` (`:263`) and asserts every pre-D-057 output (flag off, param absent, `'off'`, `'on'`, garbage/array forms); pre-existing tests `:57-123` unmodified in the diff hunk structure; zero new dependencies (package.json/lockfile untouched and forbidden). CI `web-e2e` job success at 2e2f7a1f with content byte-identical to 0b112357 (empty diff over allowed paths). | Purely additive; classification is prohibition — nothing prohibited occurred. |
| **D-057-R003** | **PASS** | `docs/RENDER_INTERNAL_WEB_DEPLOY_CHECKLIST.md:150-175` (§2a): OPTIONAL (`:150-153`), exact var name + **WEB service only** (`:155-156`), what it does (`:157-163`), exposure-consequence sentence "anyone holding the web URL sees the full internal flow immediately" (`:164-169`, citing D-057 source), `?ruleeval=off` remains the kill switch (`:170-172`), server-read / no-rebuild / env-save-restart-suffices (`:173-175`); cross-references at `:292-293` and traceability row `:355`. Doc-alignment portion: updated module comment block `rule-evaluation.ts:67-83` and `page.tsx:22-30` docstring (default-on mode + kill switch, no stale two-factor text). | All five required checklist elements present. |
| **D-046-R001** | **PASS** | Task file `project-control/tasks/M5-T026.json:119` `"worktree": "wt-m5t026"`, `path_notes` (`:115-117`) recording the parallel disjoint dispatch; concurrency window reproduced from control-plane timestamps: M4-T020 loop LIVE from 04:31 (progress_log, status still `claimed`, scope `services/api/**`, wt-m4t020) while M5-T026 was claimed 06:00 and its producer returned by 06:15 (progress_log `:126-134`); M5-T025 (wt-m5t025) claimed 05:26 same morning. Concurrent writers ≤ 3, each in its own isolated worktree. | Dispatch shape conforms; producer commit 0b112357 authored in wt-m5t026 per progress log. |
| **D-046-R002** | **PASS** | Pairwise `allowed_paths` intersections computed by me from the three task files: M5-T026 ∩ M5-T025 = ∅ (T025's 9 files are components/lib-provenance paths), M5-T026 ∩ M4-T020 = ∅ (T020 is `services/api/**` only). Producer commit touched only the 5 allowed files; no forbidden path touched. | Prohibition satisfied — no overlapping concurrent scope existed. |

Excluded by design and confirmed: **D-057-R004** binds only sentinel `D-057-BOOTSTRAP` (`requirements.json:114-123`), **D-046-R003/R004/R005** bind only `D-046-BOOTSTRAP` — none applicable to M5-T026. `DirectiveRegistry.load().evaluate_task_refs(M5-T026)` run by me returned `ok: true`, applicable_ids == cited_ids == exactly the five above, no missing/invalid/unresolved.

Intake cross-check: no amendment files exist in either directive directory (source-001.md is sole source in both); D-057's three task-binding rows and D-046's two faithfully decompose their verbatim sources — nothing missing, weakened, combined, or invented. `python tools/validate_directive_compliance.py --check` exit 0 (digest/index/schema integrity). `python tools/modularity_check.py --check` exit 0 (0 failures; 17 pre-existing warnings, none in this task's files; `rule-evaluation.ts` is 539 lines, under the warn threshold).

## (3) Material-identity statement

My own reproduction: producer commit `0b1123574f6a1fa49a89ba9b516c550c7ff5dabb` modifies exactly the 5 allowed files. `git diff` over the allowed paths is EMPTY for 0b112357→2e2f7a1f (CI head), 2e2f7a1f→f5d9719e (gate head), and 0b112357→67af2a20 (pin) — material bytes stable across the whole chain; all three are ancestors of the pin (`merge-base --is-ancestor` confirmed). Recomputed identity at the pin = `858707a7a70dcabf054aa84db3a535be9744964a2a79a9b0e163b22a4c2c250f` == the `content_manifest_sha256` in both gate records (`project-control/gates/M5-T026-G3.json`, `-G4.json`, both PASS at reviewed_sha `f5d9719e7c63f6d667d3dd4d49106ade34667e56`, reviewers code-reviewer / qa-engineer ≠ producer). Executable authority confirmed via `gh run view 34814460843`: headSha `2e2f7a1f…`, conclusion success, all 18 jobs success including `web (lint + typecheck + build)` and `web-e2e (vitest + Playwright…)`. G0 record: PASS at `7b2d90a7…`, matches the stated contract head.

Prohibited-action evidence: task status is `awaiting_gate` (not accepted); no M5-T026 row exists yet in either directive's `verification.json` `task_verifications` (D-057: empty; D-046: rows end at M5-T025); nothing merged, dispatched, deployed, or closed by this review.

## (4) Blocking discrepancies

None. One disclosed judgment call reviewed and endorsed: the checklist's §2a mentions the pre-existing `NEXT_PUBLIC_API_BASE_URL` in a contrast sentence ("unlike … which is build-inlined") — this is documentation of an existing var, not a new NEXT_PUBLIC_ introduction; the three code files' added lines contain zero `NEXT_PUBLIC` matches.

# VERDICT: PASS

All five applicable requirement IDs (D-057-R001, D-057-R002, D-057-R003, D-046-R001, D-046-R002) independently verified PASS at content identity `858707a7a70dcabf054aa84db3a535be9744964a2a79a9b0e163b22a4c2c250f`; restamp f5d9719e→67af2a20 authorized now, later heads per the exact condition in section (1).
