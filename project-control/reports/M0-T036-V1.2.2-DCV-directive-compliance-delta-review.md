# M0-T036 V1.2.2 — DCV directive-compliance delta verification (verbatim reviewer return)

- Role: directive-compliance-verifier | Directive: D-007 | Task: M0-T036 | Reviewed SHA: **b95ebf7** (V1.2.2)
- Reviewer model: **claude-opus-4-8 / effort xhigh** (owner-approved Fable-out fallback, D-007-R609 lifting R607 for M0-T036 only)
- Recorded verbatim by the orchestrator; reviewer was read-only and did not record the gate.
- Orchestrator note: the harness flagged an instruction-shaped fragment in the reviewer output (it was DCV *quoting* `.claude/settings.json` content, `"model":"claude-fable-5"`); benign — treated as a finding, not an instruction.

---

I have completed my independent verification. All primary evidence has been reproduced at the frozen SHA. Let me compose the final verdict.

---

# DIRECTIVE-COMPLIANCE VERIFICATION REPORT

## Header
- **role:** directive-compliance-verifier (independent; producer ≠ verifier)
- **directive:** D-007 (Codex-Claude supervisor bridge)
- **task:** M0-T036
- **reviewed_sha:** b95ebf7 (supervisor scope confirmed byte-identical to working tree; see below)
- **model:** claude-opus-4-8 / effort xhigh (owner-approved Fable-out fallback per reviewer-model-fallback rule 2026-08-05; authorized for this task's held reviews by D-007-R609 / source-014)
- **registry:** `project-control/directives/D-007-codex-claude-supervisor-bridge/`

## Overall VERDICT: **PASS**

The four V1.2.2 **code** requirements (R603, R604, R605, R606) are SATISFIED by primary evidence, and R605 hard-actuation is **mutation-proven**. The governance items (R607, R609) are honored/captured. R608 (a reporting return item) is satisfied for every clause I can reproduce; two sub-items are orchestrator-lane and are called out below (no defect). No requirement is VIOLATED. Nothing was merged, accepted, or dispatched.

---

## Frozen-identity precondition (reproduced)
- `git diff b95ebf7 -- tools/agent_supervisor/ tools/test_agent_supervisor_loop.py tools/test_agent_supervisor_model_chain.py` → **empty** (working tree == b95ebf7 for the entire scope). Commits after b95ebf7 (7ad26d4, 39c90a6, eb6e277) touch only SESSION_HANDOFF + directive capture; `git diff b95ebf7 HEAD -- tools/agent_supervisor/ …` is empty. Inspecting the working tree is therefore inspecting b95ebf7.
- Suites at this SHA: `python tools/test_agent_supervisor_model_chain.py` → **Ran 20 tests … OK**; `python tools/test_agent_supervisor_loop.py` → **Ran 97 tests … OK**; `python tools/validate_directive_compliance.py --check` → **exit 0**.
- Matrix integrity: `requirements.json` requirement_count 614 == 614 actual; `amendments_applied` includes source-002…source-014 (all 13); R603–R609 present.

---

## Per-requirement results

| ID | State | Primary evidence (reproduced) |
|---|---|---|
| **D-007-R603** — detect-and-hold MUST NOT be built (no owner tap at switch) | **SATISFIED** | `loop.py:1028-1123` `_switch_at_seam` is fully automatic — no `approval_gate`/owner-touch gate blocks the switch; `_touch`/ask surfaces appear only on the PAUSE (`_pause_model_unavailable` 979-1026) and STOP (`_stop_chain_exhausted` 1125-1184) fail-closed paths, never the switch. Grep across `tools/agent_supervisor/` for "reply to continue" / "Fable exhausted" / "proceed only on reply" / detect-and-hold → **none** (only affirmative comment `loop.py:1036` "There is no judgement here and no owner tap"). Superseded design absent. |
| **D-007-R604** — record-only defect re-scoped to detect-and-SWITCH actuation | **SATISFIED** | `_actuate_model` (`loop.py:765-789`) rebinds the runner via `with_model` **before** any record; `_switch_at_seam` writes `model_substitution` only after actuation (`loop.py:1061-1062` "ACTUATION FIRST"). Exercised by `RealProcessSwitchTests` / `RealProcessReturnTests`. |
| **D-007-R605** — HARD actuation: relaunch actually launches a REAL process on claude-opus-4-8; a switch-event-only proof is insufficient | **SATISFIED (mutation-proven)** | Test `tools/test_agent_supervisor_model_chain.py:286` launches a **real OS subprocess** (fake CLI run via `sys.executable`; it reads `--model` from its own argv and appends `{pid,model,argv,started}` to `launches.jsonl`). Test reads that file (written by another process) and asserts a started launch on `claude-opus-4-8`, `loop.launched_model()=="claude-opus-4-8"` (line 317), `record["launched_model"]=="claude-opus-4-8"` (326), durable `launched_model` (332). End-to-end via real `cli.main` start path: `CrashResumeTests:614` and `:645`. Probe is a real launch: `claude_runner.py:1137` `subprocess.Popen(argv, shell=False, …)`. **Mutation check (in-process, no repo files touched):** stubbing `SupervisedLoop._actuate_model`→no-op (the exact V1.2.1 record-only defect) → test **FAILS** at line 317 (`'claude-fable-5' != 'claude-opus-4-8'`). Stubbing `effective_model`→pin (the G4 crash-resume defect) → both CrashResume tests **FAIL** at lines 640/682 (resumed process comes up on `claude-fable-5`). The tests genuinely catch the defect. |
| **D-007-R606** — three DISTINCT triggers not conflated | **SATISFIED** | Switch is reached only when `availability.reason_code == QUOTA_EXHAUSTED_REASON` **and** orchestrator role (`loop.py:935-936`); durable record carries **separate** `reason_code=QUOTA_EXHAUSTED_REASON` vs `rotation_reason` (context) at `loop.py:1081-1082`, asserted distinct in test lines 337-338 (`durable["reason_code"]==quota_exhausted`, `durable["rotation_reason"]=="context_threshold"`). Security downgrade is its own reason `model_downgrade` in the rotation taxonomy (`rotation.py:427`), routed to PAUSE (non-quota). 400k context-rotation stays the pre-existing rotation path (`supervisor_rotation_relaunch`). No trigger renamed or absorbed. |
| **D-007-R607** — hold: no Fable-5 re-review until owner's window reset | **SATISFIED (intact + honored)** | Requirement text unchanged in `requirements.json` (classification `hold`, status pending). Honored operationally: this and the held reviews run on the authorized **claude-opus-4-8 xhigh** fallback, **not** Fable-5. Standing hold remains for any other Fable-5-pinned review; overridden for M0-T036 only by R609. |
| **D-007-R608** — return item: on-disk pin audit (19 producer pins + settings default == claude-opus-4-8), `claude --version`, `/model` resolution | **SATISFIED (reproduced) — 2 notes** | Reproduced on disk: **24/25** agent files pinned exactly `model: claude-opus-4-8` (all producers; the 5 reviewer files flipped opus-4-8 per fallback); only `orchestrator.md` = `claude-fable-5`; **zero** `opus-5`/aliased ids anywhere. `claude --version` → **2.1.220 (Claude Code)**. Chain lives in the **immutable, owner-editable-only** controller config: `config.py:82-84` `DEFAULT_ORCHESTRATOR_MODEL_CHAIN=("claude-fable-5","claude-opus-4-8","claude-opus-4-7")`; opus-5 never selectable (`config.py:80` + probe `model_id_mismatch` `claude_runner.py:1110-1114`, test line 471/487). **Note 1:** `.claude/settings.json` `model` = `claude-fable-5` — this is the **D-004-governed** session default (D-004 am.22/23), not a D-007/supervisor defect; R608's "settings default == opus-4-8" wording is superseded for the settings default by D-004. **Note 2:** `/model claude-opus-4-8` picker resolution is an interactive action I cannot execute read-only — request orchestrator-captured evidence for that one report line. |
| **D-007-R609** — override lifting R607 for M0-T036 only; reviews now on claude-opus-4-8 xhigh | **SATISFIED (captured; this review complies)** | Verbatim capture `source-014-amendment.md:26-27,33`; R609 present in `requirements.json` (classification `decision`). This DCV executes on claude-opus-4-8 xhigh at frozen b95ebf7, matching the authorization. The concrete gate-record model/effort stamp is written by the orchestrator after this verdict. |

**Model-governance mirror IDs D-004-R751..R760** (home registry D-004) are corroborated by the same code/tests but are out of my D-007 registry scope; noted, not scored here.

---

## Unsatisfied / stale — called out plainly
- **None VIOLATED. None BLOCKED. None stale.**
- Two orchestrator-lane items to complete the R608 **return** (not code defects, do not block the code gate):
  1. Attach the observed `/model claude-opus-4-8` resolution result (interactive; orchestrator-captured) to the R608 return alongside the reproduced facts (producer pins = opus-4-8 verbatim; `claude --version` = 2.1.220).
  2. Record the reconciliation that `.claude/settings.json` `model` = `claude-fable-5` is the D-004-governed default (R608's opus-4-8 wording applies to the Opus-pinned producer agents, which are all correct; the settings/orchestrator default remains fable-5 by D-004).
- Prohibited-action evidence (reproduced): b95ebf7 is on `task/M0-T036-supervisor-bridge` **only**, **not on main** (main tip e7f5078); `manifest.affected_prs = []`; `manifest.final_reviewed_sha = None`; task not accepted. Nothing merged/accepted/dispatched/deployed.

## Change attestation
I changed **nothing**. All operations were read-only inspection, stdlib test/validator execution, and two **in-process** monkeypatch mutation runs (temp-dir sandboxes, `python -c`; no repo files written). Every attempted shell/file write was blocked by the read-only guard. No git/`gh`/`project_control.py` write command was run. This report is returned to the orchestrator for recording per ADR-005.
