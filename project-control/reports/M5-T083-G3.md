# M5-T083 + M5-T086 — G3 independent code review (code-reviewer "cr-drw", read-only; joint review)

> Transmission history: one joint review pinned at 1414e1a7, delivered as two SendMessage parts (1/2,
> 2/2) ending with END-OF-REPORT; no truncation. Joined verbatim in delivery order by the orchestrator;
> saved as M5-T083-G3.md and M5-T086-G3.md (each task's gate cites it).

---

G3 CODE REVIEW (read-only; HEAD verified 1414e1a7). Part 1/2 — M5-T083.

SCOPE/ISOLATION (all verified): material 7acb3359 touches only the 4 allowed files (report, sheet_reader.py, sheet_primitives.py, test); app/drawings/__init__.py untouched; `git diff --stat f9bfd54d 1414e1a7 -- services/api/app/documents` EMPTY (strict reader byte-untouched); nothing outside app/drawings imports the profiles; zero new deps (math, zlib, app.* only). ruff/modularity: failures 0.

CORRECTNESS (verified against ISO 32000-1): CTM concat = m×CTM (correct `cm` premultiply, checked algebraically at sheet_primitives.py:36); apply_matrix row-vector correct. Bezier c/v/y control-point mapping correct (v reuses current point, y duplicates endpoint); conservative chord-flatness; tolerance applied in post-CTM user space and recorded on page+doc. q/Q save/restore CTM only; unbalanced q/Q refused; point freezing at add-time correct. Form XObject: depth ≤8 enforced (depth+1>8 refuses), cycle guard via ancestor ref-key set catches self + mutual cycles, op/point budgets shared across nesting, form_ctm = M×CTM, resources inherited when absent. Image: counted, dims read from dict keys only, raw_data NEVER touched. Refusals-as-values throughout; read_object_table guarantees /Root is a PdfRef (pdf_xref.py:177) so table.trailer[Root] cannot KeyError; encryption/xref-stream/hybrid refused upstream; all-or-nothing (any page refusal fails whole doc). No unguarded exception path found.

FINDINGS:
F1 (MODERATE; ADVISORY for G3 — fix before wiring; route to G5). Single-curve flatten unbounded transient. `_curveto` (sheet_reader.py:602-613) calls `flatten_cubic` then `_charge_points(len(out))` — the MAX_PATH_POINTS=500000 budget is charged only AFTER the full point list is built and `_cur_points.extend(out)`. A single c/v/y with large-magnitude (valid) coords never satisfies cubic_flat, so recursion hits MAX_FLATTEN_DEPTH=24 → up to 2^24≈16.7M points (~1GB) from ONE operator before the budget refuses. REPRODUCED: control pts (1e14,1e14)/(2e14,-1e14) end (3e14,0), tol 0.25, cap 20 → 1,048,424 points; cap 30 → did not terminate in 300s. No coord-magnitude bound and no in-subdivision budget check. It STILL returns a typed refusal (fail-closed, no exception, all-or-nothing preserved) and no test covers this case. Fix: thread remaining budget into flatten_cubic (bail at cap), or bound coord magnitude.
F2 (LOW; ADVISORY). q/Q does not save/restore text state (font_size, leading) though PDF graphics state includes them — affects only the reported font_size label, not geometry; undocumented simplification.
F3 (NIT). Curve start p0 recomputed as apply(current_ctm, current) not the stored device point; diverges only under exotic mid-path `cm`.

MODULARITY: sheet_reader.py 818 SLOC (JUSTIFY band, warning-only, CI failures 0). Justification ACCEPTABLE — packet grants only 2 production files and geometry is already split to sheet_primitives; the interpreter is one cohesive state machine. A future object-graph/interpreter split is warranted when a packet grants the path.

TESTS: AS-1..AS-5 covered; both named mutations (drop subdivision / drop CTM multiply) confirmed to redden. Local 3.11 collection blocked by app/documents/units.py PEP-695 (env artifact) — CI 3.12 on pushed head is authority.

M5-T083 G3 VERDICT: PASS (F1 should be corrected before any route-wiring packet and weighed by G5).

---

G3 CODE REVIEW. Part 2/2 — M5-T086 (strict-subset DXF reader).

SCOPE (verified): material 719d96f8 touches only the 3 allowed files (report, dxf_reader.py, test); stdlib only (enum, math, dataclasses); zero new deps; ruff/modularity failures 0; dxf_reader.py well under 600 SLOC. Local tests RUN under 3.11: 28 passed.

CORRECTNESS (verified): group codes correct (0/1/2/8/9, points 10-33, height 40, bulge 42, flags 70; closed = bit 1); $INSUNITS carried on a group-70 pair, $ACADVER on group-1 — both correct, marked [recalled-verify] for G1. Subset parse correct: LINE, LWPOLYLINE, POLYLINE/VERTEX/SEQEND (SEQEND body consumed, index advanced past), 3DFACE (3-or-4 corners), TEXT; layer defaults "0"; vertices derived from actual 10/20 pairs (declared group-90 count ignored — robust). Units honesty: absent→unitless/source=absent; $INSUNITS=0→unitless/source=header (declared-vs-absent distinguished); unknown code→verbatim unknown_insunits_N; feet never assumed. Disclose-or-refuse per the written docstring rule: unknown ENTITIES types counted in disclosed_unknown (read succeeds); non-HEADER/ENTITIES sections skipped+recorded (BLOCKS definition skipped, ENTITIES INSERT disclosed). Fail-closed VALUES for binary sentinel, non-ASCII, over-limit bytes/lines/line-length/entities/vertices, odd pair-parity, bad group code, non-numeric/non-finite coord, stray VERTEX/SEQEND, unterminated POLYLINE, no-section-markers, non-zero bulge (curved segment refused, never silently straightened). No exception escapes: private _Refuse marker + final defensive `except Exception` → refusal value (dxf_reader.py:636). Bounds are named, injectable, and asserted verbatim (drift guard, test:400). Results are frozen values.

TESTS: both named mutations confirmed to redden — entity-count bound (test:423) and pair-parity (test:438, where dropping the guard turns ODD_PAIR_COUNT into a defensive-guard MALFORMED_STRUCTURE). AS-1..AS-5 all covered.

FINDINGS:
F1 (LOW; ADVISORY). `_to_pairs` uses `str.splitlines()` (dxf_reader.py:475), which splits on ASCII control chars beyond \r/\n/\r\n (form-feed \x0c, vertical-tab \x0b, \x1c-\x1e). A group value literally containing one would shift line-pairing. Impact is bounded: it fails closed (ODD_PAIR_COUNT or a parse-refusal VALUE), not silent corruption, and such raw control chars are not valid in DXF string values (spec uses caret/\U escaping). No fix required; note for hardening if a real corpus shows them.
NIT: header `pending_var` isn't cleared after $INSUNITS is consumed, but it is reset on the next group-9, so no real-world misread.

No blocking defect. Correct fail-closed doctrine, honest units, sound bounds, teeth-verified mutations.

M5-T086 G3 VERDICT: PASS

END-OF-REPORT
