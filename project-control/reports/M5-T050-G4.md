# M5-T050 G4 — independent QA review (qa-engineer, saved VERBATIM from the agent return)

G4 (QA) gate report — M5-T050 (DB-033 confirm-arc polish, 9 riders). Reviewer: qa-engineer (read-only). Frozen HEAD verified = ee1b361c. Material cherry-pick bcfcc538.

VERDICT: PASS. No blocking findings. Three advisories (all producer-disclosed or out-of-G4-scope).

## Executed at ee1b361c (all match expected)
- services/api `python -m ruff check .` → All checks passed (clean).
- `pytest tests/api/test_lot_geometry_api.py -q` → 33 passed.
- `pytest tests/api -q` → 439 passed.
- repo-root `modularity_check.py --check` → selected 449 files; failures 0; warnings 20 (all pre-existing tools/*.py; AddressConfirmCard NOT among them); exit 0.
- CI run 35440808156 @ headSha 499ae642 → completed success; ALL 19 jobs green incl. web (lint+typecheck+build), web-e2e (vitest+Playwright), api (ruff+pytest), modularity.
- Byte-identity reproduced: `git diff` on the 9 allowed_paths is EMPTY across bcfcc538 → 499ae642 → HEAD ee1b361c. The frozen material = the CI-green head = current HEAD.

## Rider-to-test binding (each: the test that FAILS if the fix is reverted)
- (a) CLS — S11 rider-a, 4 tests. Immediate-shown: `cta.compareDocumentPosition(note) & DOCUMENT_POSITION_FOLLOWING` + `card.lastElementChild===note` + no `record-address-reserved/slot` in any state. Delayed-shown pure-append: `cta===ctaBefore` (same node) + `cta.outerHTML===ctaHtmlBefore` (byte-identical) + `childIds(card).slice(0,-1)===childIdsBefore` (append-only, all prior children keep identity/order) + note follows CTA. Delayed non-shown it.each{equal,absent,error-502,route-absent-404}: `childIds(card)===childIdsBefore`, no note, no reserved box (proves no reservation-collapse on late settle — the prior approach's failure mode). Revert (note back in-flow) → lastElementChild/append-order assertions fail. STRONG, structural. (address-confirm.test.tsx S11).
- (b) a11y — S9 (:641) + S11 rider-b: `role="img"` + `toHaveAccessibleName(raw/trim)` + `getByRole("img",{name})`. Card source AddressConfirmCard.tsx: `<strong role="img" title={enteredTitle} aria-label={enteredTitle}>`. Revert role="img" → accessible-name/getByRole fail. STRONG.
- (c) copy — S11 rider-c: no-normalized-street case asserts `not.toContain("city-matched address")` + `toContain("We show it as the address you searched for")`; normal case asserts the old wording byte-identical. Source is now `addressLine ? "...alongside..." : "...as the address you searched for..."`. Revert to unconditional → fallback test fails. STRONG.
- (d) why-note — S11 rider-d differ + equal: `getByTestId("record-address-why")` present on differ (`not.toMatch(/\d/)` = record explanation, no computed value, D-073-R006), null on equal. Revert (drop span) → fails. STRONG.
- (e) equality-gate de-vacuation — CONFIRMED non-vacuous. Both over-length probes now carry a NON-EMPTY feature whose housenumber+street EQUAL the parsed input (would-match), replacing the old `features:[]` masking path. Exact-512 test: atBound(512)→`resolved`, overBound(513)→`no_match`. The "resolved at 512" assertion proves the feature genuinely equality-matches, so the 513 refusal MUST be the guard. Guard is genuinely present at address-search.ts:316-320 (GEOSEARCH_RESOLVE_INPUT_MAX_LEN=512, before any normalization/compare; address-search.ts byte-UNCHANGED, absent from the diff). Revert (remove guard) → 513 feature resolves → `toBe("no_match")` fails. Old masking path can no longer false-pass. STRONG.
- (f) docstring — lot_geometry.py module docstring names the record-address channel; verified COMMENT-ONLY diff (+9, no code change). No test (documentation rider) — appropriate.
- (g) RA-3 binding — test_ra3: old `assert "no PLUTO record" in body["reason"].lower() or body["reason"]` REMOVED; now `assert body["reason"] == "No PLUTO record exists for BBL 3052960043 in dataset 64uk-42ks."` (exact equality, or-tail gone). Revert (route emits a different reason) → fails. STRONG. (This is the T047 "minor note" fixed.)
- (h) six error states — record-address.test.ts 6 DISTINCT typed tests: source_unavailable/rate_limited/validation_error (:110/:118/:125, T047) + timeout-504/schema_drift-502/internal_error-500 (:135/:143/:151, new) + a taxonomy coverage-guard asserting RECORD_ADDRESS_ERROR_STATES == exactly those six. Revert (drop a DOCUMENTED_PAIRS entry) → that response reclassifies to unexpected_response, `result.kind==="error" && result.state` fails. STRONG. (Closes T047 A3.)
- (i) omit + 422 parity + title bound — record-address.ts `credentials:"omit"` (+5; test captures `init.credentials==="omit"`). 422 json-safety parity: test asserts `four22.message===five02.message`, both 600+"… [truncated]", NUL/BEL stripped (binds the SHARED bounding path — a weaker 422 surface would break parity). Title bound: card `ENTERED_TITLE_ATTR_MAX_LEN=512`, `enteredTitle` cap; test asserts `title.length===513`, `endsWith("…")`, aria===title, visible text stays full-trim. Revert any → the matching assertion fails. STRONG.

## Coverage honesty (Q3) — HONEST
No committed test asserts pixels/layout (no getBoundingClientRect/offsetHeight/style.height/min-height anywhere). All CLS assertions are DOM-structural (position, lastElementChild, outerHTML, child order). The report explicitly discloses jsdom has no layout engine and the pixel proof is outstanding. No overclaim.

## Regression (Q4) — CLEAN
Only two web-test changes: S9 gained ADDITIVE rider-b assertions (the 3 prior S9 assertions unchanged, no `-` on them), and the entirely NEW S11 group. S1–S8 and S10 have ZERO deletions/modifications. The record-note relocation (rider a) required no S10 edits because S10 asserts presence/content, not DOM position — CI green confirms S10 still passes. Rider-c normal-case copy preserved byte-identical (asserted). address-search.ts byte-unchanged. No weakened/deleted assertion outside the riders.

## Findings
- F (blocking): NONE.
- A1 (ADVISORY, producer-disclosed): the definitive pixel-level CLS proof for rider a is NOT in the committed surface. The structural tests are adequate and correct-by-construction (a late insert below every settled element cannot move them), but the real-browser measurement at 360/768/1280 belongs in an e2e spec outside this packet's allowed_paths and is OUTSTANDING — orchestrator/wave must capture the Playwright measurement (responsive-a11y.spec.ts) before the CLS rider is fully proven. Not a G4 defect on the committed tests.
- A2 (ADVISORY, out of G4 scope): rider a relocates the record note to the card's LAST child, below the actions + correlation-id footer — a reading-order/IA tradeoff explicitly for the human-journey reviewer to weigh, not QA.
- A3 (ADVISORY, cosmetic): the producer per-rider map lists rider-i source as "record-address.ts (omit + bounded message)", but the material diff to record-address.ts is only +5 (credentials omit); the 600-char cap / control-char strip is PRE-EXISTING T047 behavior that rider-i's parity test now covers (test is non-vacuous — binds real shared behavior). Map wording only slightly implies a new source change.

Basis: I traced every acceptance scenario AS-1..AS-8 to a mutation-sensitive test against the frozen-head source, confirmed each of the nine riders has a test that fails under the relevant revert (structurally for a, exactly for e/g/h/i), verified the T047 vacuity gaps (A1→rider e, A2→f, A3→g, G4-A3→h) are genuinely closed, executed the four permitted api suites at ee1b361c with matching counts (33/439/clean/0), and confirmed the 9 allowed_paths are byte-identical to the all-green CI head 499ae642. VERDICT: PASS (advisories A1/A2/A3 for the wave/backlog; no rework required for G4).

[Orchestrator identity note at recording time: between the review pin ee1b361c and the gate-record head, the only commits are the M5-T051 contract-seam records — disjoint from every M5-T050 allowed_path; the task's material identity is byte-stable, per the reproduced empty diffs (the reviewer itself reproduced bcfcc538 → 499ae642 → ee1b361c).]
