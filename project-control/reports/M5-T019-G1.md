# M5-T019 — G1 Code Review Gate Report (verbatim)

Orchestrator preservation note: report returned by the independent code-reviewer agent
("G1 code review M5-T019") on 2026-09-12 and preserved verbatim below (transport
entity-decoding only). Reviewer was read-only; frozen review identity: producer commit
b30f709c. Verdict: PASS (advisories A-1/A-2 informational/trivial; no defects).

---

All verifications complete. Every claim in the packet and producer report reproduces from source. Here is my gate report.

---

# GATE REPORT

**Gate ID:** G1 (independent code review)
**Task:** M5-T019 — D-040-R002 flag unification (web adopts canonical `INTERNAL_RULE_EVAL_ENABLED`)
**Directive regime:** in-regime — `directive_refs` = D-040 / D-040-R002
**Reviewed identity (frozen):** producer material commit `b30f709c` (base `b119edf1`); CI head `634f1060`; current HEAD `1c84d779`
**Reviewer:** code-reviewer (read-only)
**Result:** **PASS**

---

## Frozen-identity / scope containment

- `git show --stat b30f709c` = exactly 7 files, all in `allowed_paths`; nothing under `services/api/**`, `packages/**`, `render.yaml`, `.github/**`, `apps/web/e2e/harness/**`, components, or package files. Scope clean.
- `git diff b30f709c 1c84d779` = only `project-control/**` (reports, `state.json`, `tasks/M5-T019.json`, plus the preserved D-040-R001 MapPLUTO research). None of the task's 7 `allowed_paths` are touched by any later commit — the frozen `M5-T019-producer-report.md` (the one report in scope) is untouched after `b30f709c`. Frozen identity holds.
- CI at `634f1060` = `b30f709c` + a project-control-only progress commit, so the 6 code/test files tested in CI are byte-identical to the frozen producer material. CI evidence is applicable to the reviewed identity.

## Acceptance scenarios

| ID | Case | Verdict | Basis (reproduced from source) |
|----|------|---------|--------------------------------|
| S1 | rename_complete_and_exclusive | PASS | `Grep INTERNAL_RULE_EVAL_UI apps/web` = 3 hits, ALL in `rule-evaluation.test.ts` (:87 comment, :89 test-title string, :90 `const OLD` literal) — the mandated inert-guard test; nowhere read as an active flag. `ruleEvaluationFlagEnabled` now reads `process.env[INTERNAL_RULE_EVAL_ENABLED_ENV_VAR]` (rule-evaluation.ts:83,88). `Grep NEXT_PUBLIC…RULE_EVAL apps/web` = none. New test is non-tautological: it deletes the canonical key, sets only the OLD name, and asserts `false` — would FAIL against the un-renamed code. |
| S2 | semantics_byte_preserved | PASS | `git diff b119edf1 b30f709c -- rule-evaluation.ts`: only the env-var name literal + doc comments changed. `TRUE_TOKENS = {1,true,yes,on}`, `.trim().toLowerCase()`, the `typeof rawValue === "string"` absent/empty/unknown→false guard, and `ruleEvaluationSurfaceEnabled`'s two-factor gate (env AND `?ruleeval=on`, array-first coalescing) are byte-identical to base (diffed against `git show b119edf1:…`). Non-`NEXT_PUBLIC_` server-side-only posture preserved. Unit-test diff: only `KEY` literal changed + one test ADDED; no assertion deleted, weakened, or loosened. |
| S3 | e2e_both_processes_one_name | PASS | Next process: `playwright.config.ts:61` env key renamed to `INTERNAL_RULE_EVAL_ENABLED: "1"` (siblings untouched). Fixture-API process: `fixture_api.py:59,253` imports+exports `INTERNAL_RULE_EVAL_ENABLED_ENV_VAR` from `app.config`, whose value is confirmed `"INTERNAL_RULE_EVAL_ENABLED"` (config.py:28) — the identical canonical name flows through both processes. Corroborated by CI run 34694372457 (`M5-T019-ci-evidence.txt`): `web` SUCCESS + `web-e2e` SUCCESS (both rule-evaluation specs green). |
| S4 | owner_return_note | PASS | Producer report §"OWNER RETURN NOTE" names the exact Render action (rename `INTERNAL_RULE_EVAL_UI`→`INTERNAL_RULE_EVAL_ENABLED` wherever set in the dashboard; API service unchanged). Factual claims verified: `render.yaml` carries neither flag and its `nycdf-web` block is WITHHELD (render.yaml:156-168); no `.github/**` workflow sets either name; both `.env.example` files omit them. |

## Directive obligations (D-040-R002)

- Packet cites `D-040:D-040-R002` (`directive_refs`); requirement applicability includes `M5-T019` (requirements.json:48-51). Verified.
- "Reconcile frontend flag with backend `INTERNAL_RULE_EVAL_ENABLED` per one-flag intent (M5-T015 G5 F-1)" — satisfied: frontend now reads the canonical name; the `?ruleeval=on` second factor preserved. Design-spec §6 (line 118) confirmed to name `INTERNAL_RULE_EVAL_ENABLED` as the single flag; F-1 (M5-T015-G5.md:52-54) is exactly the split-name divergence this closes.
- "Deploy-affecting: packet must name the Render env-var change as a return item" — satisfied in objective, `path_notes`, `inputs`, and the producer report's owner-return note.

## Modularity

Pure rename inside the existing focused `rule-evaluation.ts` module; no new file, no structural change, no threshold approached; net change is one doc comment block + one test. No responsibility mixing or coupling introduced. `path_notes` modularity answers match the actual diff.

## Findings

- **Advisory A-1 (informational, not a defect).** S1's literal "appears NOWHERE … in tests" is in tension with the same scenario's mandate to "add a new unit test [that sets] the OLD name." The only reconciliation is the producer's: the old name survives solely as the inert-guard test's literal, never as an active flag read. Correctly handled and transparently disclosed; no action needed.
- **Advisory A-2 (trivial).** Producer report characterizes the 3 surviving `INTERNAL_RULE_EVAL_UI` occurrences as "2 comments + the const OLD literal"; precisely they are 1 comment (:87), 1 test-title string (:89), and 1 `const OLD` literal (:90). Immaterial — all three are inert.
- **Note (not a G1 concern).** The formal in-regime directive verification (`verification.json`, produced by the independent `directive-compliance-verifier`, producer ≠ verifier) is a separate pass and is required before `accept()`. My G1 confirms the code-level obligations of D-040-R002 are satisfied; it does not substitute for that verifier's recorded verdict.
- **Note.** The one failing CI job (`web-dependency-security`, the owner-gated Next.js RCE item) is unrelated and its prohibition (D-040-R004) is preserved — `apps/web/package.json`/`package-lock.json` are untouched by this diff (confirmed; both are in `forbidden_paths` and absent from the change set).

## Conclusion

The change is an exact, exclusive, name-only unification of the web rule-evaluation env flag to the canonical `INTERNAL_RULE_EVAL_ENABLED`. Flag semantics (token set, normalization, absent→false), the two-factor env-AND-`?ruleeval=on` gate, and the deliberate non-`NEXT_PUBLIC_` server-side-only posture are byte-preserved; the const has no external importer; no assertion was weakened; a genuine (non-tautological) test pins the retired name as inert, closing M5-T015 G5 F-1; one canonical name now flows through both e2e processes; the owner-return deployment note is present and factually accurate. CI `web` + `web-e2e` are green at the frozen code identity. Scope and frozen identity are intact.

**VERDICT: PASS**

Relevant absolute paths:
- `C:\Users\MLFLL\Downloads\nyc-zoning\ctl24\apps\web\src\lib\rule-evaluation.ts`
- `C:\Users\MLFLL\Downloads\nyc-zoning\ctl24\apps\web\src\lib\__tests__\rule-evaluation.test.ts`
- `C:\Users\MLFLL\Downloads\nyc-zoning\ctl24\apps\web\playwright.config.ts`
- `C:\Users\MLFLL\Downloads\nyc-zoning\ctl24\apps\web\src\app\property\page.tsx`
- `C:\Users\MLFLL\Downloads\nyc-zoning\ctl24\apps\web\e2e\rule-evaluation.spec.ts`
- `C:\Users\MLFLL\Downloads\nyc-zoning\ctl24\apps\web\e2e\rule-evaluation-flag-off.spec.ts`
- `C:\Users\MLFLL\Downloads\nyc-zoning\ctl24\apps\web\e2e\harness\fixture_api.py` (out of scope; corroborating)
- `C:\Users\MLFLL\Downloads\nyc-zoning\ctl24\services\api\app\config.py` (out of scope; corroborating const value)
- `C:\Users\MLFLL\Downloads\nyc-zoning\ctl24\project-control\reports\M5-T019-producer-report.md`
- `C:\Users\MLFLL\Downloads\nyc-zoning\ctl24\project-control\reports\M5-T019-ci-evidence.txt`
