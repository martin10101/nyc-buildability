# M5-T118 — G5 security review (security-reviewer "sec-t118", read-only)

> Transmission history: pinned at a120220e (material 992c47b1), delivered as 3 SendMessage parts ending with
> END-OF-REPORT; no truncation. Joined verbatim by the orchestrator (transport wrapper tags removed only). Verdict PASS; L1 the ~100x document
> ceiling raise (multi-GB worst case) must be gated by the future import route; L2 the filtered-inline-image early-EI residual.

---

M5-T118 G5 SECURITY REVIEW (READ-ONLY) — part 1/3

PIN: HEAD a120220e (includes material 992c47b1, cherry-pick of wt-m5t118 9378890f, 9 files). Base/claim-seam f2ada974. Method: diff review of the 5 source files, in-process probes (python -c, package-init stubbing), and the drawings suite via the 3.11 shim (272 passed in 8.05s). No writes made.

TOP FINDING — L1 RESOURCE CEILING RAISE (MEDIUM-as-risk, ADVISORY here; not blocking)
sheet_reader.py:146-153 raises the per-DOCUMENT bounds ~100x: operators 200k→20,000,000 and flattened points 500k→50,000,000. Confirmed against f2ada974 where op_count/point_count were DOCUMENT-WIDE 200k/500k. Points are retained as tuple[Point,...] (Point=tuple[float,float], sheet_primitives.py:33) across ALL pages in the returned SheetDocument, so peak resident memory is bounded by the 50M-point document ceiling ≈ 5+ GB (≈100+ bytes/point in CPython), plus tens of seconds–minutes of pure-Python interpreter CPU at the ceiling.

Why NOT blocking: (a) the reader is UNWIRED — grep confirms no app/api or app/main.py import; only dxf_reader/dxf_import are routed. (b) The raise is the declared design intent (read real 88/94-page vector sets, DB-055 c) and the deferral to the future route is explicit (DB-076 d; task inputs). Enforcement here is sound: no unbounded accumulation — the point budget is threaded as min(page_remaining, doc_remaining) INTO flattening (sheet_interpreter.py:323-347) so a curve stops before materializing 2**depth points (preserves the prior G5 F1 fix), and _charge_points also checks page then doc post-hoc (:360-373).

REQUIRED at mount (carry forward to the import-route task, extends M5-T113-G5 A1 + DB-076 d): the future route MUST enforce a wall-clock deadline, a per-request points/memory budget well BELOW 50M (5 GB/doc is not viable per concurrent request), AND concurrency limiting. Recommend the mount packet gate on these before any exposure.

(part 2 follows: inline-image parser + sh)

---

M5-T118 G5 — part 2/3

INLINE-IMAGE PARSER (sheet_inline_image.py) — bounded, fail-closed. All probed in-process:
- Dictionary bounded to max_dict_bytes=4096, refuse over cap (:141-143); no nested dicts; nested arrays refused (:287, probe "nested-array" REFUSE); no-ID-before-EOF refused (:182); array-where-key-expected refused (:150-151). Loop always advances (delimiter/lexer-fail branch yields word="" → typed refuse, no infinite loop) — reviewed :159-181.
- Determinable length ceil(W*comp*bpc/8)*H (:208-209): OVERFLOW is pre-empted TWICE. (1) The strict lexer caps numbers at MAX_NUMBER_BYTES=64, so a 4000-digit W/H/BPC is rejected as "malformed dictionary" before arithmetic (probe: huge-width, huge-bpc both REFUSE, detail_len 43). (2) A lexer-VALID huge geometry (100000×100000 ≈ 10^10 B) is caught by the 8-MiB data cap (:218-221; probe REFUSE "inline-image data over 8388608 bytes"). Non-positive W/H refused (:206-207; probe "zero-height" REFUSE). bpc∉{1,2,4,8,16} refused (:200-201).
- DESYNC PROTECTION verified: determinable length that does NOT land on a whitespace-delimited EI REFUSES (:227-231; probe "det-mismatch" and "no-ei-past-end" both REFUSE) — no silent skip. ID not followed by a whitespace byte refused (:117-120). Data running past stream end refused (:224-225).
- SCAN path (filtered/undeterminable): EI scan bounded by max_data_scan_bytes=8 MiB with a per-byte limit check (:244-249); no-EI-before-EOF refused (:258; probe "scan-no-ei" REFUSE); valid <ws>EI<delim> resumes just past EI (:254-256; probe "scan-ok" SPAN determinable=False). _ei_at requires a trailing whitespace/delimiter/EOF so "EIGHT" does not false-terminate (:261-268). No quadratic rescan: successful skips partition the ≤8-MiB/≤32-MiB stream (each byte belongs to one image's data region); a "BI" inside raw data is treated as bytes, never re-parsed.
- RESIDUAL (L2, ADVISORY/DISCLOSED): a FILTERED inline image (or a determinable image whose geometry is a lie) can end early on a chance <ws>EI<delim> inside raster/compressed bytes. By design this desyncs into a DOWNSTREAM typed refusal (all-or-nothing), never a silent wrong drawing — fundamental to skipping filtered inline images without decoding; documented sheet_inline_image.py:22-26 + producer report. Reader is unwired and coordinates are never auto-trusted. Accepted.

(part 3 follows: sh skip, refusal-detail hygiene, scope, verdict)

---

M5-T118 G5 — part 3/3

sh SKIP (sheet_interpreter.py:69-72, 429-434): `sh` is skipped as non-geometry, page+doc counts incremented, operands cleared, never refused. Hides NO parser failure: `sh` consumes only its stack operands (a shading-dict name), never stream bytes, so clearing operands is exactly correct execution and cannot desync. Skipping means the shading resource is never resolved — not needed (non-geometry). Load-bearing mutation seam present (_SKIP_SHADING rebindable → falls through to unsupported-operator refuse). BI dispatch (:432-433) sits before _IGNORED with no operator-name collision.

REFUSAL-DETAIL HYGIENE: all inline-image refusal details are typed VALUEs (SheetRefusal via _refuse, sheet_objects.py:88-95) with CONSTANT text — no attacker bytes echoed. The one attacker-derived value is the bpc integer in :201; INFORMATIONAL only — bounded to ≤64 chars by the lexer's MAX_NUMBER_BYTES (probe: 64-digit bpc → detail_len 106), and it is a lexer-validated integer, not raw bytes. No action needed.

SCOPE / NON-FUNCTIONAL:
- No new I/O, network, logging, or subprocess in any of the 5 changed files (grep clean; the only hits were `_flush_open`, an in-memory subpath flush, not file I/O). No document content logged.
- ZERO new dependencies (stdlib + in-repo pdf_lexer/sheet_objects/sheet_primitives only).
- UNWIRED confirmed: no route/main import of read_sheet or sheet_inline_image; decoded per-document ceiling UNCHANGED at 128 MiB (only per-page 32-MiB budget added, sheet_objects.py:9).
- Dual page/document budgets enforced and reset per page (begin_page: sheet_reader.py:229-237 + _StreamDecoder.begin_page); SheetPage new fields default 0 (backward compatible).
- 272 drawings tests pass via the 3.11 shim.

SUMMARY: 0 Critical, 0 High, 0 Blocking Medium. Advisory: L1 (document ceiling raise → multi-GB/multi-second worst case; unwired + disclosed; mount route MUST gate deadline+memory/points budget+concurrency), L2 (filtered-inline-image early-EI residual; disclosed, desyncs to refusal). Informational: bpc-in-detail (bounded ≤64). No cross-tenant/secret/storage/SSRF surface in this unwired parser. Budget enforcement, overflow pre-emption, and desync protection independently verified.

M5-T118 G5 VERDICT: PASS

END-OF-REPORT
