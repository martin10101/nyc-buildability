# M5-T111 - G2 producer self-check record (recorded by the orchestrator, role self_check)

Producer: backend-engineer (orchestrator-dispatched subagent) in wt-m5t111; one in-worktree commit d3e303eb (parent 275ef85f), cherry-picked to c3e3df15 with every file MATCH by git blob identity (9 files: project-control/reports/M5-T111-producer-report.md 70a7d045, services/api/app/api/v1/dxf_import_api.py 6a58ae1a, services/api/app/api/v1/export_api.py 4177ba09, services/api/app/api/v1/scene_api.py 9cdc8eb7, services/api/app/resilience/rate_limit.py 637c1b6a, services/api/tests/cad/test_export_api.py ec9f9419, services/api/tests/drawings/test_dxf_import_api.py 233878ef, services/api/tests/resilience/test_rate_limit.py 0f2b0278, services/api/tests/scenario/test_scene_api.py cd6232cf).

- **Scope:** services/api/app/resilience/rate_limit.py (NEW: the one shared thread-safe sliding-window limiter with a declared key ceiling, idle eviction, fail-closed when full of ACTIVE keys, never evicting an active key's history, an injectable clock; caller_key = request.state.principal else host; JobSlots + run_in_job_slot releasing on thread return) and the three UNMOUNTED routes (scene_api.py, dxf_import_api.py, export_api.py) switched to it with limiter-before-parse, job slots, every-method 404 while disabled; tests in tests/resilience + the three route test files.
- **Producer self-checks (report, OBSERVED):** ruff clean; resilience + the three route files 183 passed; modularity exit 0; neighbours green in isolation (scenario 787, cad 435, drawings 125); 11 in-process mutations RED with the baseline restored GREEN (scratch harness, not committed). The producer's combined scenario+drawings+cad run was interrupted at collection under Python 3.11 by the pre-existing PEP 695 syntax in app/documents/units.py (3.12-only; CI runs 3.12).
- **[ORCH-HARVEST] at the harvested head c3e3df15 (cherry-pick c3e3df15; Python 3.11 local): `python -m ruff check .` (cwd services/api) -> exit 0: All checks passed!; `python -m pytest tests/resilience tests/scenario/test_scene_api.py tests/drawings/test_dxf_import_api.py tests/cad/test_export_api.py -q` (cwd services/api) -> exit 0: 183 passed in 5.25s; `python -m pytest tests/scenario tests/cad -q` (cwd services/api) -> exit 0: 1252 passed in 21.68s; `python tools/modularity_check.py --check` (cwd .) -> exit 0:   warn review_signal: tools/agent_supervisor/next_task.py - above the warning threshold; consider the module boundary before growing it further.**
- **Discoveries routed (producer):** DB-D1/DB-D2: the shared 256 KiB body ceiling refuses a legitimate maximum export (two 10,000-vertex rings ~452 KB) and real architect DXF files - kept (fail-closed) and routed to per-route ceilings at PKT-H; job-slot cap 16 per route (below anyio's 40-thread pool); enabled routes answer non-POST with 405 (every method 404 only while disabled).

G2 verdict: PASS (self_check; the independent reviews and the DCV follow - dispatched by the successor session per the handoff).
