# M5-T093 — G3 independent code review (code-reviewer "cr-t093", read-only)

> Transmission history: pinned at fa7e5fae, delivered as three SendMessage parts (1/3..3/3) ending with
> END-OF-REPORT; no truncation. Joined verbatim by the orchestrator (transport wrapper tags removed only).
> Verdict PASS (no blocking findings). Orchestrator disposition of the advisories: (a) the C1 reader packet
> must put the PDF 1.5+ cross-reference/object-stream resolver in a NEW profile-level module under
> app/drawings/ and must NOT widen the shared read_object_table (two production consumers, one of them the
> accepted survey pipeline); (b) P3 is reframed as preventing a false 'successfully read' result on
> scan-only files; (c)+(d) noted. All four are routed to DB-055 at this seam.

---

M5-T093 G3 (code-reviewer) PART 1/3 — refusal path REAL + scope docs-only CONFIRMED

PIN: HEAD at start = fa7e5fae. Reviewed material ab2a6730 (docs/research/architect-corpus-reader-trial-2026-09.md + project-control/reports/M5-T093-producer-report.md). Read-only; no writes.

(1) REFUSAL PATH — REAL, verbatim-exact.
- pdf_xref.py:140-148 returns UnsupportedPdfFeature("cross-reference stream", "startxref offset {N} begins with an integer object header (a PDF 1.5+ cross-reference stream); only classic 'xref' tables are in the supported subset"). This is byte-for-byte the note's §3 verbatim detail (N=6488119). `feature` matches.
- sheet_reader.py:372-374 calls read_object_table FIRST and, on UnsupportedPdfFeature, returns _wrap_strict(table). _wrap_strict (sheet_reader.py:172-177) sets origin=strict_reader, reject_code=refusal.reject_code (= "unsupported_pdf_feature", the ClassVar pdf_xref.py:101), feature=cross-reference stream — exactly the note's §3 table. CONFIRMED.
- The refusal fires before _catalog_pages_root (line 376) and the page walk, so the note's "latent, never reached" framing for sh/inline-image is code-accurate.

(5) SCOPE — docs only, CONFIRMED. `git show --stat ab2a6730` = exactly the two allowed doc paths; nothing under services/apps/packages/tools/.github. Reader files byte-untouched.

SLOC / DB-055 cross-check — the note's "962 SLOC, JUSTIFY band, split before growth" is accurate: sheet_reader.py = 1096 physical / 962 non-blank-non-comment lines; no modularity_exceptions.json entry; matches DB-055 (b) verbatim. DB-055 (g)+(h) scaffolding exists in docs/DISCOVERY_BACKLOG.md (line 184) and the note discharges both as described (note ran all six incl. item 3, which (h) listed as 1-2 and 4-6 — MORE coverage, not less).

(continued in part 2/3)

---

M5-T093 G3 PART 2/3 — object-stream co-requisite CONFIRMED + P1-location recommendation (profile, NOT shared reader)

(1b) OBJECT STREAMS a TRUE co-requisite — CONFIRMED against the resolution path. read_object_table materializes ONLY the classic-xref-listed objects, parsed at byte offsets (pdf_xref.py:212-232); _resolve (sheet_reader.py:206-221) follows refs through table.objects and refuses "unresolvable reference" if a key is absent. So page-tree nodes that live only inside /ObjStm (note §2: item1 0 raw/19 in ObjStm, etc.) are unresolvable even with a type-1-only xref-stream reader. Object-stream decoding is genuinely required to resolve /Root→/Pages→page→/Contents. The note calls it a co-requisite (not the CURRENT blocker), which is precise — the 6/6 blocker that actually fires is the xref stream.

(2) P1 LOCATION — RECOMMENDATION (citable): put P1 in a NEW profile-level resolver under app/drawings/, NOT by extending the shared read_object_table.
- Blast radius: read_object_table has TWO production consumers — the ACCEPTED survey/vector pipeline (vector_pdf_decoder.py:43 → pdf_container.py:109 → read_object_table) and sheet_reader.py:372. Widening it to accept PDF 1.5+ xref streams would silently change what the accepted survey pipeline parses (files it now refuses would succeed) — a behavior-contract change to gated, legally-sensitive code that forces its re-review/re-acceptance. That is exactly the "sweep consumers of a changed contract" hazard.
- Fit: the sheet profile is already a "standalone, read-only consumer" that "relaxes none of the survey decoder's refusals" (sheet_reader.py docstring 6-11). A profile-owned resolver matches that boundary and keeps the strict subset intact for the survey pipeline. It can delegate the classic-table case to read_object_table unchanged and fall back to its own stream/ObjStm resolver only on an xref stream, reusing pdf_lexer/pdf_objects read-only. It must re-apply the strict reader's identity check + all fail-closed bounds (stream-size, decoded-byte, hop/object caps).
- The note is internally ambiguous here ("extension to the reused strict read_object_table" vs "bounds the profile already applies") and does NOT flag the two-consumer risk. ADVISORY: the next packet should resolve P1 to the profile and cite this blast radius. Not sinking — location is the next packet's design call under G0.

(continued in part 3/3)

---

M5-T093 G3 PART 3/3 — P2/P3 ordering sound; no over-generalization; VERDICT PASS

(3) P2/P3 ORDERING — SOUND, verified against code. No BI/ID/EI or sh handler exists (not in _IGNORED sheet_reader.py:137-150, not dispatched 781-819) → each hits line 816 "unsupported operator", refusing the whole doc, so the note's "latent" claims are correct. Image XObjects ARE counted-not-decoded (_place_image 1011-1028), so P2's "mirror the image-XObject treatment" is grounded. P1→P2→P3 is defensible: content ops are unreachable until P1; item 4 (scanned) carries an inline image, so P2's inline-image decision is a prerequisite to classify it scan-only (P3).
- ADVISORY (sharpening, not a defect): P3 undersells the post-P1 risk. For items 5-6 (lone image XObject, no BI/sh), after P1 the reader would _place_image → SUCCEED as a SheetDocument with 0 geometry — a false "successfully read" scan, not merely an "incidental xref" refusal. P3 should be framed as "refuse instead of falsely succeeding," which raises its priority nearer P2.

(4) NOTHING CLAIMED BEYOND THE SIX FILES — scrupulously scoped (binding scope-honesty statement; "derived only from these six files" §5). One mild sentence — "Modern Acrobat/PDFMaker output defaults to it; it is not incidental to one producer" (§4) — leans slightly general but is accurate and bounded by the disclaimer. ADVISORY nit only. Latent claims correctly marked predicted/never-observed.

Inherent, accepted limitation (not a defect): the corpus is licensing-bound and deleted (never committed), so per-file structural facts rest on the note's independent scan (fitz-corroborated, twice-run determinism). The load-bearing conclusion (6/6 refuse at the xref stage) is directly verifiable from the code path and the verbatim detail matches source exactly.

BLOCKING findings: NONE.
ADVISORY: (a) resolve P1 to a profile-level resolver + flag read_object_table's two-consumer blast radius [check 2]; (b) reframe P3 as preventing a false-success on scan-only files [check 3]; (c) one mildly general producer sentence [check 4]; (d) the "split before P1" coupling is looser if P1 is a new module — the DB-055 (b) split is independently justified regardless.

M5-T093 G3 VERDICT: PASS

END-OF-REPORT
