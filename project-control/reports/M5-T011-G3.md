# M5-T011 — G3 QA / Test-Adequacy Review (verbatim reviewer return)

Reviewer: **qa-engineer** (independent ≠ producer scenario-optimization-engineer). Reviewed content verified by blob hash against commit `bbf3267f8be2444c5460742e22792698bdc1307f` (breakeven.py `067b5ecf…`, test `5629e926…`, __init__.py `0f188f11…`). Transport entity-decoding applied; content otherwise verbatim.

---

# GATE REPORT — M5-T011 (G3 QA / test adequacy + mutation sensitivity)

**Task:** M5-T011 — Deterministic scenario break-even / threshold finder (`find_scenario_threshold`)
**Reviewer:** qa-engineer (independent; NOT the producer)
**Reviewed content identity (verified by blob hash, not by trusting the checkout):** breakeven.py `067b5ecfb3c6c49c64646b768cb0e50ac8d5a6f1`, test `5629e92695a4bde21bf468740ee561523f28a9ed`, __init__.py `0f188f11cd8e313c3c89557e74d7a9854d588cdc` — all hash-match the reviewed commit `bbf3267f`. My own worktree was on an older base (`d8b3899f`); I located the frozen content via `git worktree list`, extracted from the reviewed SHA, and confirmed identity by `git hash-object`.

## VERDICT: PASS

No blocking findings. Three LOW / advisory notes below.

## 1. Reproduced counts (frozen content, offline; Python 3.11.9 / pytest 8.4.2)
- `python -m pytest tests/scenario -q` → **384 passed in 1.06s**
- `python -m pytest tests/scenario/test_scenario_breakeven.py -q` → **61 passed**
- Pre-existing = 384 − 61 = **323** (AS-6 "≥323 pre-existing"); **0 regressions** — reviewed commit --stat shows exactly 3 code/test files changed; forbidden derive/sensitivity/ranking/comparison/json_safety test files untouched.
- `python tools/modularity_check.py --check` → **failures 0; warnings 15** (breakeven.py a soft `review_signal`, not a failure). AS-6 satisfied.
- `__init__.py` diff purely additive (one import block + 5 appended `__all__` entries; existing exports/order unchanged) — AS-7.

## 2. AS → concrete test mapping (every AS has real, non-trivial assertions — no theater)

| AS | Tests | Substance |
|----|-------|-----------|
| AS-1 | `test_as1_found_reports_base_lineage_breakdown_and_honest_bracket` | lineage + coverage=="conditional"; metrics == [7500,12000,13500,15000]; `components.canonical_cap_sq_ft == 15000.0` (verbatim cap); crossing lower/upper == 0.8/0.9 + metric values + direction. |
| AS-2 | `test_as2_byte_identical_across_input_reorderings_and_duplicates`, `test_as2_identical_input_is_byte_identical` | `json.dumps` byte-equality across forward/reversed/shuffled + duplicate; ascending order with duplicates preserved; explicit byte-identical assertion. |
| AS-3 | `test_as3_result_is_never_verified_and_honestly_labelled`, `test_as3_crossing_is_grid_bracket_and_midpoint_is_labelled_estimate`, `test_as3_incoming_verified_coverage_is_capped_to_conditional` | No "verified"; disclaimer present; crossing endpoints are real evaluated candidates; midpoint `is_estimate:True, verified:False`; incoming verified capped to conditional. |
| AS-4 | `test_as4_target_already_met`, `_target_never_met`, `_first_crossing_reported_..._monotonic`, `_non_monotonic_multiple_crossings_via_controlled_derive_double`, `_not_derivable_candidates_kept_in_value_order`, `_unknown_or_malformed_variable_is_invalid`(7 params), `_literal_verified_variable_is_invalid`, `_non_numeric_or_negative_target_is_invalid`(9 params), `_empty_or_malformed_domain_is_invalid`(7 params), `_unknown_response_metric_is_invalid`, `_no_cap_document_is_typed_empty`(3 params), `_degenerate_document_is_typed_no_crash` | Typed markers for already-met/no-crossing/empty/invalid; non-monotonic path forces 3 crossings via a derive double on the exact name breakeven calls, asserts non_monotonic True, counts all 3, reports FIRST bracket (0.2→0.4); no raises. |
| AS-5 | `test_as5_malformed_candidate_..._json_safe`(11 params incl nan/inf/-inf/10**5000), `_unserializable_candidate_is_typed`, `_overflow_huge_int_..._deterministic`, `_uses_shared_sanitizer_not_a_local_duplicate` | json.dumps(allow_nan=False) never raises; every number finite & ≥0; no `" at 0x"`; source asserts shared import present and local sanitizer defs absent. |
| AS-6 | `test_as6_consumes_derive_read_only_no_recompute` + full suite + modularity | Each candidate's `derived` re-derived independently and asserted equal. |
| AS-7 | `test_as7_inputs_are_byte_unchanged_and_not_aliased`, `_efficiency_ratio_variable_and_metric_selection`, `_output_is_strict_json_safe_on_mixed_domain`, `_runs_fully_offline_socket_blocked`, `_module_imports_are_contract_free` | Inputs byte-unchanged + un-aliased; socket.socket replaced with a raiser (real offline proof); import allow-list asserted. |

## 3. Mutation sensitivity (in-process against mutated copies of frozen source; source never left mutated)

| Mutation | Result | Evidence |
|----------|--------|----------|
| (a) report INTERPOLATED crossing value instead of honest grid bracket | **CAUGHT** | upper_candidate→0.85 fails test_as1 (==0.9) and test_as3 (`in candidate_values`) |
| (b) off-by-one bracket (`rows[1:]`→`rows[2:]`) | **CAUGHT** | crossing→(0.5,0.9); test_as1 requires (0.8,0.9) |
| (c) pick LATER crossing not FIRST (`crossings[0]`→`crossings[-1]`) | **CAUGHT** | non-monotonic double: first_meeting→0.8; test_as4_non_monotonic requires 0.2/0.4 |
| (d1) disable non-monotonic flag (force False) | **CAUGHT** | flag False with 3 crossings; test requires True |
| (d2) flag boundary `>=2`→`>=3` | **MISSED** (LOW-1) | suite exercises the flag at 1 crossing (False) and 3 crossings (True); no 2-crossing case pins the exact boundary |
| (e) weaken target guard (`_non_negative_finite_float`→`_finite_float`) | **CAUGHT** | negative target −1 yields target_already_met instead of INVALID; test requires INVALID |
| (f) inf/NaN leak | **CAUGHT** (by construction) | every path runs `_strict_json_safe` = json.dumps(allow_nan=False) + finite/≥0 assertions |

The honesty (interpolation), first-crossing, and monotonicity-disable mutations — the three highest-risk — are all caught.

## 5. Findings
- **BLOCKING:** none.
- **LOW-1 (mutation gap):** the `non_monotonic` boundary is only pinned at 1 crossing (False) and 3 crossings (True); a `>=2`→`>=3` mutation survives. Suggest a controlled 2-crossing derive double to pin the exact `>=2` boundary. Non-blocking — core behavior (set on multiple, unset on single) well covered.
- **LOW-2 (advisory, code-review/G4 scope):** breakeven.py (776 lines) trips a modularity `review_signal` warning (not a failure). Single cohesive responsibility, but near the threshold — should not grow further without extraction.
- **LOW-3 (very low):** AS-3's "verified" absence check uses exact full-string membership rather than substring, so it would not catch `"verified"` embedded inside a longer string; meaningful never-Verified paths still covered by coverage-status capping + exact `_coverage_values` checks.

## 6. Notes
- Public signature `find_scenario_threshold(scenario_document, variable, target, domain, response_metric=USABLE_RANGE_POINT)` — `domain` is a required 4th positional (consistent with AS-1's "explicit ordered candidate domain"). Not a defect.
- Directive-compliance (D-038 ALL) is the separate directive-compliance-verifier gate (out of G3 scope); the code honors the D-038 honesty invariants the tests assert (never Verified, verbatim canonical cap, no independent legal recompute).

**Requested gate result:** record **G3 = PASS** for M5-T011 (LOW-1/LOW-2/LOW-3 non-blocking notes for producer/code-review follow-up).
