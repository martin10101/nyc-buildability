# M5-T047 — G0 contract gate (orchestrator)

- **Packet:** record-address display channel (DB-032, the M5-T046 OQ-5/HJ-A1 discovery):
  an ADDITIVE per-BBL extension of the lot-geometry route carrying PLUTO.address +
  version + provenance (normalize_bbl before fetch; accepted connector/resilience path
  read-only; SODA key-absence = typed honest absence, never a fabricated line; existing
  typed error discipline); the confirm-card record-address line as a D-073-R006 records
  display (differs → one labeled city-record line; equal → no duplicate; absent/error →
  honestly absent); the two named HJ polish items (layout-neutral copy, display-trim
  with the raw value in a title/aria attribute, S9 updated with the change); and the two
  bounded T046 riders (equality-gate length bound G5-A1; a /search abbreviation
  fail-closed fixture G4-A1). Cites D-066:R001 + D-073:R006. Depends on M5-T046 (228th,
  accepted).
- **Evidence files named in-packet:** M5-T046-HJ.md (A1-A3), M5-T046-producer-report.md
  §7, docs/research/db026-address-to-lot-fixture-capture.md §6 + assertion 7,
  docs/DISCOVERY_BACKLOG.md row DB-032.
- **Pairwise disjointness (D-072-R003):** mechanical check at this seam — recorded
  output `PAIRWISE DISJOINT T045 vs T047: True | intersection: []` (M5-T045 live on
  loop-1, 17 allowed paths; M5-T047 10 allowed paths). forbidden_paths additionally pin
  LotOutlineMap.tsx and the ENTIRE architect component directory (PropertyOverview
  imports LotOutlineMap — the M5-T045-owned surface is untouchable), plus connectors/,
  profile/, spatial/, and contract schemas. No third lane.
- **Directive binding:** D-066-R001 and D-073-R006 task_ids appended (same-commit digest
  resyncs + audit entries); `evaluate_task_refs` ok=True, applicable == cited.
  `affected_tasks` untouched (verification-time only).
- **Nav block (D-066-R001):** graph regenerated at this seam (743 files / 15656 nodes);
  embedded in inputs — lot_geometry consumers (main.py mount + its api test), the
  LotOutlineMap consumer set including the PropertyOverview edge that drives the
  prohibition, the read-only pluto fetch path, bbl primitives. Advisory clause included.
- **Placeholders (c17):** record-address.ts lib seed, record-address.test.ts with a
  trivial PASSING test (vitest no-suite trap), producer-report placeholder. Every other
  allowed path already tracked with content.
- **Scope sanity:** additive-only route change (contract-schema need = STOP +
  discovery); M5-T046 behavior byte-identical except the two named HJ polish items whose
  tests move with the change; the gate changes only by the additive length bound; api
  suites + ruff documented from services/api; web proves in CI.

**G0 verdict: PASS** — packet is contractable and claimable.
