# M5-T115 - G2 producer self-check record (recorded by the orchestrator, role self_check)

Producer: frontend-engineer (orchestrator-dispatched subagent) in wt-m5t115; one in-worktree commit f94081ac (parent 01856916), cherry-picked to 75c03f25 with every file MATCH by git blob identity (8 files: apps/web/e2e/confirm-journey.spec.ts 30ed69f8, apps/web/e2e/responsive-a11y.spec.ts 3a02a6b4, apps/web/src/app/property/architect.css d917ab02, apps/web/src/components/address/AddressConfirmCard.tsx 4e139d5f, apps/web/src/components/address/__tests__/address-confirm.test.tsx a574ff92, apps/web/src/components/architect/ArchitectEntry.tsx 39979587, apps/web/src/components/architect/__tests__/entry.test.tsx 46eb31b5, project-control/reports/M5-T115-producer-report.md c8b11889).

- **Scope:** Web presentation on the live /property search -> confirm flow: the PropertySearch block of ArchitectEntry.tsx (always-visible environment badge + professional-review line closing the <=700px hide - DB-083 f; the BBL path framed as an alternative; the four lib/bbl.ts errors distinctly styled with aria-invalid, text verbatim), AddressConfirmCard.tsx (a static 'City-matched address' identity label rendered at first paint so Continue stays stable), search/confirm-scoped CSS in architect.css, and new unit + e2e tests (entry, address-confirm, confirm-journey, responsive-a11y).
- **Producer self-checks (report, OBSERVED):** modularity exit 0 [OBSERVED]; git diff --stat = only the 8 allowed paths [OBSERVED]; web unit + e2e results [PREDICTED] until CI on the pushed harvest head (thin client: no local npm/node). The producer states the P2 row's behaviours already existed and are cited; this packet adds a meaning-preserving visual layer only.
- **[ORCH-HARVEST] at the harvested head 75c03f25 (cherry-pick 75c03f25; Python 3.11 local): `python tools/modularity_check.py --check` (cwd .) -> exit 0:   warn review_signal: tools/agent_supervisor/gate_wave.py - above the justification threshold; record a cohesion justification in review.**
- **Discoveries routed (producer):** OQ-1 possible 360px horizontal overflow on /property?ruleeval=on (pre-existing? not asserted); OQ-2 two role=note at >=768px; OQ-3 the environment note paraphrases InternalBanner instead of importing it; DISC-P2-1 the shell env badge/footnote is still hidden <=700px on loaded-workspace surfaces (P3+); DISC-P2-2 the e2e resolver emits only 'resolved' (no warnings / ambiguous street), so the architect error/warning matrix stays unit-only; no e2e 'warnings above Continue' test (unit S1 covers it).

G2 verdict: PASS (self_check; the independent reviews and the DCV follow - dispatched by the successor session per the handoff).
