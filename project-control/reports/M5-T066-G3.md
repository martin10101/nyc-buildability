# M5-T066 G3 gate — independent code review (verbatim reviewer return)

Reviewer: code-reviewer (claude-opus-4-8 per D-064), dispatched by the orchestrator at the
seq-124 T066 wave, pinned to the frozen submit head 84132df5 (material abfd182b). Recorded
by the orchestrator; the reviewer is read-only and records no gate itself. The return
arrived in four parts (the first transmission truncated in critical surface 3; the reviewer
re-sent the remainder in three labeled parts) — joined verbatim at the exact break point.

---

VERDICT: PASS

# M5-T066 — G3 Independent Code Review

**Task:** D-082 map-CLICK interaction slice — click-to-place/select/move/delete outline vertices on the lot map via a new `ProposalOutlineMap` wrapper + an additive-optional interaction prop on the accepted `LotOutlineMap`; clicked display-4326 positions enter the SAME drawn-outline state as keyboard entry (one draft model, one bridge, one adoption). Plus three T065-wave fold-ins: HJ-1 copy honesty, HJ-2 adoption reconciliation (AS-6), HJ-4 persistent ≥3-points hint.

**Frozen identity reviewed:** submit head `84132df5`; material `abfd182b` (cherry-pick of in-worktree `0f1ffc1b`). Verified `git diff 84132df5..HEAD` over all 12 task paths is EMPTY (path-clean). Material own-diff = 11 files, every one inside the packet's `allowed_paths` (the new `ProposalOutlineMap.tsx` and `proposal-outline-map.test.tsx` match the packet's glob pair); no `forbidden_paths` touched (`AddressConfirmCard.tsx`, `PropertyOverview.tsx`, `outline-bridge-api.ts`, `e2e/helpers.ts`, `services/api/`, `packages/contracts/` all absent from the diff). My verdict binds to this frozen path-scoped content; disjoint control-plane commits (T067 seam) do not affect it.

**Verification posture:** Thin client — I did not run npm/node. Web behavior and the mutation-revert reds prove only in CI; I rely on the recorded CI SUCCESS at the harvest head `2578329c` (orchestrator-validated). I independently ran the one documented local check: `python tools/modularity_check.py --check` → **473 files selected, failures 0, warnings 22, exit 0**; none of this packet's files (`LotOutlineMap.tsx`, `ProposalOutlineMap.tsx`, `ProposalOutlineDraw.tsx`, `proposal-draft.ts`) is flagged — the producer's §2 claim is accurate. I read every changed source and test file and judged the wiring/state contracts they encode.

## Critical surfaces (the five the wave flagged)

**1. Additive-optional LotOutlineMap change — PASS.** The interaction is gated purely by `interactive = onOutlineMapClick != null` (LotOutlineMap.tsx:376). The click listener is registered only inside `if (interactive)` (l.487); the overlay effect returns early unless `drawnOverlay && mapReady` (l.581). The map-build effect's dep array gained `interactive` (l.573), which is constant per consumer, so a display consumer sees no churn. I confirmed the real accepted consumer: `AddressConfirmCard.tsx:305` renders `<LotOutlineMap bbl={canonicalBbl} context={architect} />` — none of the three new props — so `interactive` is false and no overlay is added: byte-equivalent to the accepted display. AS-3 is unit-proven (below) and the accepted address-confirm/lot-outline specs are untouched.

**2. One-path invariant — PASS.** Clicked positions travel as `{lng, lat}` straight from `clickEvent.lngLat` and are appended into the SAME `points` state the keyboard `addPoint` fills (`placePoint`, ProposalOutlineDraw.tsx:101). No second math path: grep over the three components for `proj4|epsg|2263|transform|lambert|toMercator|fromLngLat|project|unproject` returns only explanatory comments and one server-response CRS *label* display — no client-side transform (AS-5 grep-clean). The wrapper holds no map instance and no coordinate math beyond assembling the display overlay.

**3. HJ-2 reconciliation in proposal-draft.ts — PASS.** `adoptOutlineVertices` now filters `exterior_walls` through `wallReferencesInRange(w, nextVertices.length)` (integer + `0 ≤ idx < count` on both endpoints), dropping exactly the walls that would dangle; equal- or larger-count adoption keeps every wall (all indices stay in range), preserving the accepted equal-count semantics; the result is always internally consistent; the function stays pure/immutable. The report's claim that `lot_line_segments` cannot dangle is correct — `DraftLotLineSegment` addresses explicit `start_x/y,end_x/y` coordinates, not vertex indices (proposal-draft.ts:40-46). `ProposalEditor.adoptDrawnOutline` computes `danglingWallIds` and announces exactly which walls were removed, so the structural change is never silent (satisfies the "no silent scenario-changing default" rule).

**4. MapLibre interaction wiring + early-click guard — PASS.** The click listener attaches at map construction, before the overlay effect installs the `proposal-drawn-outline-points` layer. The handler guards the hit test with `queryMap.getLayer(DRAWN_OVERLAY_POINTS)` (l.505) so a click in that window falls through to a place instead of querying a missing layer. Defensively correct and mutation-tested (see AS-2 early-click guard). One INFO note: the specific real-MapLibre justification ("fires an `error` event → `failRender` teardown") is version-dependent and cannot be verified in jsdom — but the guard is the right behavior regardless (a query against an uninstalled layer is at best a no-op warning, at worst a teardown), so the exact framing does not weaken the fix.

**5. Keyboard parity + DB-043(a) focus + a11y — PASS.** Every click route has a keyboard equivalent: place = "Add drawn point"; select = a per-row `Select/Deselect` button with `aria-pressed` and a descriptive `aria-label`; move = the row's lng/lat inputs; delete = the aria-labeled Delete button. Delete keeps the DB-043(a) remedy — `pendingFocus` nonce + `useEffect` moves focus after the remove re-render (never a synchronous `.focus()` on a remounting node) — and now also reconciles `selectedIndex` so it never dangles past the removed row. The wrapper carries a `role="status"` point-count/selection announcement and instruction copy that changes with selection state.

## Acceptance scenarios

- **AS-1 (place; one model)** — `proposal-outline-draw.test.tsx` "AS-1: map clicks and keyboard entry append into the SAME table" drives the real wrapper (only leaf LotOutlineMap mocked): two map clicks + one keyboard row land as rows 0/1/2 in one table, overlay renders 3 points, Convert enables. Genuinely proves the one-model invariant at unit level. Plus `proposal-outline-map.test.tsx` pure-builder tests and the e2e pointer journey. PASS (runtime geometry CI-proven).
- **AS-2 (select/move/delete + keyboard-equivalent + focus)** — parent tests: select via vertex click (`aria-pressed`, overlay `selected` flag), move via a subsequent click (count stays 2, coords updated), deselect via re-click then a click PLACES (count → 3); delete-selected keeps focus on the delete control at the slid index and clears the selection. PASS.
- **AS-3 (display-surface byte-equivalence)** — `lot-outline-map.test.tsx` "AS-3 byte-equivalence: with no interaction props, NO click listener attaches and only the 2 lot-outline layers are added" asserts `clickListeners` length 0 and `addLayer` called exactly twice. The accepted address-confirm/T060/T065 specs are untouched and ride CI. PASS.
- **AS-4 (end-to-end)** — e2e "AS-1 pointer" places 3 real clicks → bridge (stub asserts `srid=4326`, `drawn_vertices ≥ 3`) → adopt → check (stub asserts adopted `2263` vertices equal `BRIDGED_2263`); a wrong/missing request cannot be papered over by the response. The keyboard "AS-4" journey is retained unchanged (diff is purely additive after it). PASS (CI).
- **AS-5 (no new math/deps)** — grep-clean of CRS math; lockfile untouched (no dependency file in the diff). PASS.
- **AS-6 (adoption reconciliation)** — `proposal-draft.test.ts`: equal-count (converted from the old 3-vertex T065 test to a genuine 5-vertex case, correctly — the old premise is now false under reconciliation) and larger-count preserve every wall and are immutable; the SMALLER-count test drops to survivors `["W-S","W-E"]` and asserts every surviving wall's endpoints `< adopted.vertices.length`. The mutation claim holds: reverting the reconciliation (keeping `base.exterior_walls`) makes both the `["W-S","W-E"]` equality and the in-range loop red because W-N/W-W reference vertex 3 past the 3-vertex outline. `danglingWallIds` is pinned including the negative/non-integer case. Real teeth. PASS (mutation red is CI-asserted).

## Findings (all advisory — none blocking)

- **CR-1 (LOW, latent completeness — for a future task).** Adoption reconciles `exterior_walls` but not `street_lines`, which also reference walls by `wall_id` (`DraftStreetLine.wall_id`, proposal-draft.ts:47-54). Dropping a wall can orphan a street-line's `wall_id`. This is **unreachable today**: both seed drafts set `street_lines: []` and there is no add/update/remove street-line helper or UI, so a street-line cannot be created. Non-blocking now; when a street-line editor lands, `adoptOutlineVertices` (and the dropped-wall announcement) must also reconcile `street_lines` by `wall_id`, or a dropped wall re-introduces the exact HJ-2 dangling-reference class through street_lines.

- **CR-2 (NIT).** `moveSelectedPoint` (ProposalOutlineDraw.tsx:113) nests `setPoints(...)` inside the `setSelectedIndex` updater to read the current selection. Correct result and idempotent under a strict-mode double-invoke (moving to the same lngLat twice is a no-op), but a side-effecting updater is a smell; reading `selectedIndex` from a ref would be cleaner. Not blocking.

- **CR-3 (NIT).** `adoptDrawnOutline` reads `draftRef.current.exterior_walls` for the dropped-wall announcement while the draft updates via the functional `setDraft`. The draft is always correct; only the announced count could go stale if two adoptions fire before a re-render (not a realistic path — adopt follows a discrete Convert). Not blocking.

- **CR-4 (INFO).** The early-click guard's real-MapLibre failure-mode justification is version-dependent (see critical surface 4); the guard behavior is correct and mutation-tested either way. No action required.

## Bottom line

The slice delivers the map-click interaction the T065 compose-only wall left undeliverable, with the accepted-display safety invariant genuinely preserved (additive-optional prop; the real AddressConfirmCard consumer passes only `bbl`+`context`, so it is byte-equivalent), a real one-draft-model architecture (map clicks and keyboard share one `points` state, proven by parent-integration tests, not just the pure builder), no client-side CRS math, keyboard parity and the DB-043(a) focus remedy intact, and honest proposed-not-a-record labeling. The three fold-ins are correctly implemented and well-tested: HJ-2 reconciliation is mutation-pinned (AS-6), HJ-1 copy now describes an interaction that exists, HJ-4 shows a persistent singular/plural-correct hint. Path scope is clean (diff 84132df5..HEAD empty over all task paths; 11 material files all in allowed_paths; no forbidden path), modularity passes (473 files, failures 0, no packet file flagged), and every advisory is either latent-unreachable (CR-1) or cosmetic (CR-2/3/4). Web and mutation-revert behavior ride the recorded CI SUCCESS at `2578329c`.

VERDICT: PASS (advisory findings CR-1..CR-4 only; none blocking).

--- END OF REPORT ---
