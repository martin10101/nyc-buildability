# G3 Code Delta Re-Review — M0-T036 V1.1 correction unit (frozen c193a52)

> Preserved VERBATIM by the orchestrator from the g3-v11-m0t036 agent-return channel
> (2026-08-04). Transport entity-decoding only; no edits.

# G3 DELTA re-review — M0-T036 V1.1 correction unit

**Frozen content identity:** c193a52e0ac1bf4ceb7e0a1e67bb866d02b2605c (verified via `git rev-parse HEAD` before reading; HEAD not moved, nothing written, no control-CLI / git-mutating / gh commands run).
**Reviewer:** independent G3 code reviewer (read-only; producer != reviewer; same reviewer as the 43848bd review).
**Scope:** ONLY `git diff 43848bd..c193a52` restricted to the 12 code files (loop.py, state_machine.py, claude_runner.py, cli.py, circuit_breakers.py, process.py, audit_log.py + 5 test suites). The other files in the range (project-control ledger/reports/directives, SESSION_HANDOFF) are orchestrator-authored control-plane artifacts and were NOT gated here. The prior review of the whole tree at 43848bd is settled and was not re-opened.

## (1) Verdict: PASS on the delta

All four BLOCKING findings are genuinely fixed with real tests; F-2, F-4, F-5 and the optional items L-1, V-6, V-4-partial are correct and delta-scoped; the two new S7 edges are legal, loop-only, and weaken no refusal. No new blocking defect. Four LOW/INFO observations below, none activation-blocking and none requiring V1.1 rework. Shadow-only remains in force; nothing here is an activation opinion.

## (2) Per-fix confirmation

**B-1 — FIXED.** `ForwardResult.sent_prompt` carries the exact bytes of the outbox row marked sent (loop.py:1008, 1048); `run()` threads it into cycle 2's `run_unit` via `prompt = result.forward.sent_prompt` (loop.py:1087), and fails closed with `LoopError("forwarded_prompt_unavailable")` when a sent forward lacks its bytes (loop.py:1081-1086). The resumed-crash path reads the journaled row's OWN `payload.prompt` (loop.py:1028-1035); envelope shape verified against `enqueue_outbound`/`unsent_outbound` (durable_state.py:560-585) and the payload built at loop.py:991-999. The report carries a digest, not the bytes (`to_dict`, loop.py:322-327). Tests are real: `ForwardedPromptThreadingTests` asserts the SECOND unit's received prompt via `FakeRunner.prompts[1]` (both not-the-original and byte-equality against the actual outbox SQL row), the resumed-row case, and the digest-only report.

**B-2 — FIXED.** New legal edge `POLICY_CHECK -> PREFLIGHT` on `cycle_closed` (state_machine.py:211-214), fired only from the loop after a completed shadow observation (loop.py:860-868) and after DENY_AND_CONTINUE (loop.py:774-779, now an honest `stop("deny_and_continue", ..., PREFLIGHT)`). `CycleResult.continues` narrowed to CLAUDE_RUNNING only (loop.py:353-362, fail-closed direction), with the shadow break re-ordered before `continues` so `shadow_observation_complete` keeps its label (loop.py:1064-1069). `cmd_start` wraps `_run_loop` in `except (LoopError, IllegalTransitionError)` and reports `loop_refusal` with an honest provider-calls note instead of a traceback (cli.py:2065-2092). Design choice endorsed: closing-edge is right — admitting POLICY_CHECK to `CYCLE_ENTRY_STATES` would mean resuming mid-policy-evaluation with the decision object gone, semantics S7 never defined; the entry set stays `{PREFLIGHT, CLAUDE_RUNNING}` (loop.py:112, unchanged). Producer risk #1 (deny_and_continue ends the invocation) assessed SAFE: auto-retrying a hard-denied instruction would be an unbounded provider-call loop over the same refused prompt; the machine is not paused/halted, the journal rests at a legal entry state, and the stop code is honest — S4.4's "the run is not stopped" is honored in the only meaningful sense. DENY_AND_HALT untouched and regression-tested (`test_deny_and_halt_still_pauses_hard`). Tests: restartable-after-shadow (two runs, same journal), deny_and_continue supervised no-crash + resumable, and the real-CLI stranded-journal refusal report (`bad_cycle_entry_state`). The one modified pre-existing assertion (`test_shadow_never_reaches_forward_prompt` now pins PREFLIGHT) is itself part of the B-2 fix and still asserts FORWARD_PROMPT unreached.

**B-3 — FIXED.** `extract_checkpoint` raises `CheckpointError("multiple_distinct_checkpoints")` when `len(by_id) > 1` (claude_runner.py:394-403); same-id conflict refusal and identical-duplicate tolerance both retained and regression-tested. Tests cover the mandated adversarial shape (real BLOCKED checkpoint then rosier fabricated one under a different id, through the real runner via the `distinct_double_checkpoint` fake, asserting `result.checkpoint is None`), the extraction seam directly, and the benign duplicate.

**B-4 — FIXED.** The `codex_reviews_per_checkpoint` verdict is consumed: TRIP transitions to PAUSED_RECOVERY via the new legal `CODEX_REVIEW -> PAUSED_RECOVERY` / `unsafe_condition` edge (state_machine.py:177-181), records a counted synchronous-stop touch, and stops BEFORE `self.reviewer.review(...)` is reached (loop.py:691-706 precedes the call at 707-710); WARN notifies. Counter resets on each newly received checkpoint right after CHECKPOINT_RECEIVED (loop.py:660-664); `reset()` pre-exists (circuit_breakers.py:122). Tests assert the reviewer received zero packets on trip and that a 2-cycle supervised run survives where the unreset counter would trip spuriously. The `claude_runs_per_task -> max_claude_turns_per_run` mapping mismatch is documented, not renamed (circuit_breakers.py:39-50) — acceptable: renaming the config field would invalidate owner-placed manifest-covered configs (S3.1) and renaming the counter would decouple historical audit events; correctly deferred to a separately owner-approved version.

**F-2 — FIXED.** New `clear-recovery` command (cli.py:1475-1517) fires the pre-existing `PAUSED_RECOVERY -> PREFLIGHT` `owner_cleared_pause` edge (state_machine.py:259) through the real StateMachine (journaled + audited; `transition()` validates against the S7 table and does not gate on `assert_can_act`, so the operator edge fires legally — verified at state_machine.py:343-403). Refuses under a durable emergency stop and outside PAUSED_RECOVERY; clears no flags, dispatches nothing. The mandated run-2 scenario is tested end to end through the real CLI (parked journal -> refusal report -> clear-recovery -> the SAME journal dispatches), plus both refusal cases and the S12.1 command-surface registration.

**F-4 — FIXED.** `with_checkpoint_contract` is applied inside `run_unit` (claude_runner.py:~638) so every dispatched unit — operator first prompt and every forwarded prompt — gets the canonical S8.3 block; delivery verified by a fake worker that writes its received first stdin user turn (the prompt IS the first user turn, claude_runner.py:712). The block is derived from the `ClaudeCheckpoint` dataclass and `CHECKPOINT_STATUSES` (required fields = no-default fields), so it cannot drift from what `extract_checkpoint` validates — and a test pins that derivation. Sentinel dedup is idempotent and byte-preserving for authored contracts; `checkpoint_contract_appended` recorded on the result and in the audit detail. Producer risk #2 (outbox `prompt_digest` vs delivered bytes) confirmed as described: delivered bytes = journaled prompt + the fixed, deterministic, supervisor-owned suffix, with the append fact audited. Acceptable for V1.1; see N-4.

**F-5 — FIXED.** `_check_timezone_database` (cli.py:257-283) probes `ZoneInfo('America/New_York')`, wired into doctor (cli.py:1142); failure is a doctor FAILURE naming `tzdata` and "first wake". Both PASS and injected-failure paths tested via a patched `cli.zoneinfo`.

**L-1 — FIXED.** `--flag=value` form of every hard-deny bypass flag denied like the bare token (process.py:146-150), test driven from `HARD_DENY_ARGUMENTS` itself including upper-case forms.

**V-6 — FIXED.** `_read_head_anchor` distinguishes absent from corrupt (audit_log.py:303-321); `verify_chain` fails closed with `head_anchor_corrupt` on a corrupt anchor (audit_log.py:278-289; positional args match the `ChainVerification` field order at audit_log.py:67-77). Five corrupt shapes plus the absent-legitimate case tested.

**V-4 partial — FIXED as scoped.** Precise not-OK reasons for checkpoint+graceful_close_failed and checkpoint+wall-timeout (loop.py:617-634). The runner-side wall-during-grace `timed_out` reclassification is honestly deferred; both paths fail closed. Acceptable residual.

**New S7 edges:** both add stop/close paths only; neither weakens any existing refusal, hold, or hard-deny; triggers are fired only from loop code; no test pins the transition count (grep-verified — only doctor's dynamic `len(TRANSITIONS)` message references it).

## (3) NEW findings introduced by the delta (none blocking)

**N-1 (LOW, not activation-blocking, V1.2 candidate):** the resumed-unsent path silently falls back to the freshly re-rendered prompt when the journaled row's payload lacks a usable `prompt` string (loop.py:1031-1035 — the `isinstance` guards fail open into `sent_prompt = prompt`). Inconsistent with the fail-closed stance of `forwarded_prompt_unavailable`. Mitigation: reachable only via journal corruption or hand-crafted rows, and `forward_message_id` keys on the instruction's approval digest so the same message id pins the same instruction. Suggest refusing instead in a later unit.

**N-2 (LOW, informational):** sentinel-based dedup means a reviewer-authored `next_claude_prompt` containing the literal sentinel line suppresses appending the real contract. Consequence degrades to a fail-closed `no_valid_checkpoint` stop (a bogus contract cannot make an invalid checkpoint pass `extract_checkpoint`), so no refusal is weakened. Note for the threat-model doc.

**N-3 (INFO):** `start` returns exit code 0 with `dispatched: false` on a loop refusal — consistent with the pre-existing recovery-refusal convention in `cmd_start`, but scripting against the exit code alone won't see the refusal. Convention question, not a defect.

**N-4 (INFO):** the outbox payload comment "exact bytes sent" (loop.py:993) is now exact only up to the fixed contract suffix (producer risk #2). The fact is audited via `checkpoint_contract_appended`; recommend a one-line docstring/comment amendment in a later unit so the envelope's own claim matches.

## (4) What I could NOT verify

- I executed nothing (read-only reviewer; pytest writes caches). I rely on the producer's stated evidence: 1065->1091 passed / 2 skipped, doctor PASS, secret-scan PASS. The +26 new tests were verified by READING them, not running them.
- The non-code files in the 43848bd..c193a52 range (ledger, directive registry, reports, SESSION_HANDOFF) were not gated here — orchestrator artifacts outside my mandate.
- The settled 43848bd review was not re-opened; V-1/V-2/V-3/V-5, L-3/L-4, and the minor notes remain as recorded there. The control-response live-probe caveat from the prior review is unchanged by this delta.
- `Limits(max_codex_reviews_per_checkpoint=...)` construction in the new breaker tests was verified by reading, not instantiation.

Shadow-only remains in force; this verdict feeds the owner's record and is not an activation opinion.
