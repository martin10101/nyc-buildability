# M5-T113 — G5 security review (security-reviewer "sec-t113", read-only)

> Transmission history: pinned at 7b3686f0, delivered as 3 SendMessage part(s) ending with END-OF-REPORT;
> no truncation. Joined verbatim by the orchestrator (transport wrapper tags removed only). Verdict PASS; A1 (the pure-Python
> unfilter CPU = DB-076 d, before any mount), A2 (comment skip bounded by the stream cap), A3 (indirect predictor parms fail closed).

---

M5-T113 G5 SECURITY REVIEW (read-only) — part 1/3

PIN: HEAD 7b3686f0 at start. Material fcd0033e (cherry-pick of wt-m5t113 21956e4b). Verified NO material drift on the 4 source files fcd0033e..HEAD (the later 7b3686f0 touched only control-plane files). Pre-change seam 275ef85f present. Scope reviewed: sheet_objects.py, sheet_marked_content.py (NEW), sheet_interpreter.py, sheet_reader.py + the two test files.

METHOD: source read of every changed line + the reused apply_predictor/_resolve/decoder; 252/252 tests/drawings pass on the 3.11 shim; 14/14 targeted security probes pass (run inline via python -c, no files written — shell file-write is sandbox-blocked here, so no agent-memory note either).

CONFIRMED — decompression / predictor bounds (content streams, the new surface):
- Inflation bounded: zlib decompress(raw, cap=8_388_608) + a typed "flate output bound" refusal on unconsumed_tail/not-eof (sheet_objects.py:178-184). Predictor runs on ALREADY-charged bytes; document-wide budget charged BEFORE apply_predictor (sheet_objects.py:185-189; charge_decoded 284-293). Budget-before-predictor is also test-pinned (test_as1_predictor_stream_shares_the_document_decoded_budget).
- Predictor geometry bounded BEFORE any buffer sized (the M5-T103 G5 Finding-1 class): /Predictor, /Columns, /Colors, /BitsPerComponent validated; empty data returns before row_len is derived; row_len>absolute_cap and misaligned rows are typed refusals; only then is a buffer touched (sheet_objects.py:408-422). PROBE: /Columns=10**18 and /Colors=10**18 both refuse in ~0.0000s with NO MemoryError and no large allocation; row_len==cap+1 refuses; TIFF(2) and non-int columns refuse.
- Per-request CPU is FINITE: pure-Python _png_unfilter cost is bounded by the 8 MB per-stream cap and the 128 MB per-document budget (sheet_objects.py:31-32, 432-467). See advisory A1 in part 2.
- New /DecodeParms indirect-ref resolution is bounded: _resolve follows refs under _MAX_RESOLVE_HOPS=32 with cycle/missing refusal (sheet_objects.py:113-129); _content_decode_parms adds at most 2 hops, refuses multi-element arrays and non-dictionaries (sheet_objects.py:189-217). Indirect-ref VALUES inside the parms dict fail closed to a refusal (safe).

(part 2 follows)

---

M5-T113 G5 — part 2/3

CONFIRMED — inline-dict lexer (sheet_marked_content.py, NEW):
- Depth cap 16 (both << and [ count toward depth), byte cap 65_536, iterative (no recursion → no stack blowup), balanced parse; unbalanced/too-deep/too-long each a typed SheetRefusal VALUE, never a partial read (77-171). Bounds are threaded from the facade limits so a patched cap still bites (interpreter 225-238; reader 150-155, 302-306) — test-pinned load-bearing (depth→1, bytes→4).
- NO infinite loop / non-advancement: only << >> [ ] are handled here; every other byte goes to the shared lex_primitive, which returns a LexedToken that consumes ≥1 byte (a regular run's first byte is guaranteed non-delimiter/non-whitespace) OR a PdfSyntaxError→refuse. There is no zero-width-token path, so the scanner always advances (verified in source + probes). One primitive lexer only.
- Refusal details never echo attacker bytes — only our own numeric caps; the malformed-token path returns a generic string. No content logged (no logging import).
- PROBES pass: comment-with-no-EOL (4 MB) terminates in 0.81s as a typed refusal; many-scalars hit the byte cap; deep arrays hit the depth cap; lone '>' refuses; unterminated refuses; hex-string value, escaped-parens + nested (max_depth 3), and #xx name escapes all parse and advance correctly.

CONFIRMED — scan-only honesty (sheet_reader.py):
- A parser failure returns ITS OWN typed refusal at _build_page (sheet_reader.py:352-353) BEFORE the scan-only check runs (post-loop, 365-391). So no hostile file can be mislabelled "scan only" to hide a parser failure, nor the reverse. Gate is load-bearing (mutant test defeats it → succeeds).
- The two DB-076(e) items are DISCLOSED, not hidden: the "scan only" label also fires on text-only pages (pinned by the read-only test_pdf_object_streams suite; detail names the text-only case honestly), and image-only succeeds on the classic path but refuses on the resolver path. Both are fail-safe refusals with characterization tests — not security-relevant.

CONFIRMED — scope: no new I/O/network/logging/subprocess/eval/pickle in any changed file (the only grep hits are the word "open" in a comment and a message); new module imports stdlib + in-repo only; requirements.txt/.in untouched; nothing outside app/drawings imports the reader → UNWIRED (no route/endpoint/main.py change).

(advisories + verdict in part 3)

---

M5-T113 G5 — part 3/3

ADVISORY findings (all NON-BLOCKING; none Critical/High/Medium):

A1 (Low, = DB-076 d, which this packet explicitly defers): the pure-Python PNG unfilter cost scales with the decoded budget — up to 8 MB per stream and 128 MB per document (sheet_objects.py:31-32, 432-467). It is FINITE (not unbounded) and the module is UNWIRED, so there is no live DoS today. Remediation (before any route mount / at the DB-076 d packet): ensure the mounting route's per-request wall-clock/worker CPU budget covers the worst-case per-byte unfilter over the 128 MB document budget, or lower the caps / vectorize. Reproduction: apply_predictor over a valid ~8 MB PNG-predicted stream is O(bytes) pure-Python.

A2 (Low/Info): read_inline_dict evaluates its byte-cap at loop top, so a single token or a '%' comment-with-no-EOL can walk up to the remaining stream length (≤ the 8 MB inflate cap) before the cap fires — probe measured 0.81s for a 4 MB comment (sheet_marked_content.py:95-99, 158-171). It is bounded by the stream cap (not by max_bytes) and occurs at most once per document (it forces a refusal that aborts the read), so it is NOT amplifiable/quadratic. Optional hardening: bound the ws/comment skip by remaining max_bytes. No change required.

A3 (Info): _parm_int does not resolve indirect-ref predictor parameters (e.g. /Columns given as "n g R"); such values fail closed to a typed refusal (sheet_objects.py:425-429). Safe (fail-closed); mild spec-completeness gap only.

Cross-tenant/service-role/private-storage/upload-control/prompt-injection items: N/A — this is an offline, unwired, in-memory byte parser with no auth, storage, network, or AI surface. Least privilege and log redaction: satisfied (no logging, refusal details carry only our own numeric caps, never attacker bytes).

VERIFICATION: 252/252 tests/drawings pass (3.11 shim); 14/14 security probes pass. Material identity stable (no drift fcd0033e..HEAD on source files).

M5-T113 G5 VERDICT: PASS

END-OF-REPORT
