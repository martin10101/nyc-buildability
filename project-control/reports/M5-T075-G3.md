# M5-T075 — G3 independent code review (code-reviewer, read-only)

> Transmission history: returned in three transmissions — the initial return truncated inside
> Part 2's Assessment at the phrase "I traced each fault to a distinct production b"; the
> continuation was requested from the SAME reviewer FROM that exact cut phrase and itself
> truncated after "Nothing found rises to a required correction."; the final lines arrived in
> a third transmission. Joined verbatim at both cut points. Saved verbatim by the
> orchestrator (transport entity-decoding only).

Review complete. Verifications run: pinned HEAD confirmed, material diff, the production client at the pinned head, vitest/eslint config, blob identity across 7d225e2f/490c3413/HEAD, and CI run 35829303244.

---

**PART 1 of 3 — M5-T075 G3 independent code review (pinned head 4ed4d3d737c9755750dc1969cb289eae49017c31, verified by `git rev-parse HEAD`)**

**Findings**

**F1 (LOW — evidence accuracy) `project-control/reports/M5-T075-producer-report.md:33-39` and `:11-13`.** The branch table's test-file line anchors are stale after the re-feed revision. The report cites the timeout spec at 329, pre-flight abort at 341, mid-flight abort at 358, over-budget at 370; the committed file has them at 335, 351, 368, 384 (`grep -n '  it("types' apps/web/src/lib/__tests__/outline-bridge-api.test.ts`). Line 329 is actually the network spec's `.toContain("could not be reached")` assertion, and 341 is the timeout spec's `timeoutMs` assertion — a reviewer following the map lands inside the preceding spec. The "test file lines 263–385" claim is likewise short: the block runs 263–403 (the file is 403 lines). Only the network anchor (319) is correct. All source-side anchors were checked and are right: `:359-361`, `:380`, `:381`, `:382-387`, `:399-401`, `:549-556`. Non-blocking; correctable as an `[ORCH-CORRECTED]` edit, since the report is inside `allowed_paths`.

**F2 (INFORMATIONAL) test file `:322`, `:342`, `:363`, `:377`, `:395`.** Each AS-2 line `expect(REFUSAL_OR_BRIDGED_KINDS.has(outcome.kind)).toBe(false)` follows an exact `expect(outcome.kind).toBe(<kind>)` in the same spec, so it is tautological — it cannot fail while the preceding assertion passes. It reads as documentation of the never-laundered rule rather than added mutation coverage; the actual protection comes from the exact-kind assertions plus mutation predictions 1, 2 and 5. No change requested.

**F3 (LOW) test file `:384` (spec title "…BEFORE parsing").** Nothing in the assertions distinguishes bound-before-parse from parse-then-discard: both orderings return the identical `unexpected_response(httpStatus 200, receivedState null, correlationId "cid")`, because the fixture body is well-formed. The ordering is genuinely unobservable through the client's public surface, so this is a title over-claim rather than a test defect, and mutation prediction 5 (flip `>` to `<` at `outline-bridge-api.ts:399`) does prove the size guard governs the outcome.

**END OF PART 1**

---

**PART 2 of 3**

**F4 (INFORMATIONAL — adjacent coverage gap, correctly out of scope).** The client's second fault surface, the `response.json()` catch at `outline-bridge-api.ts:404-410` with its own `timedOut` / `aborted` sub-branches, plus the blank and non-numeric `Content-Length` variants of the regex at `:396`, remain untested. DB-045(i) enumerates only network_error / client_timeout / aborted / over-budget, so leaving these is correct for this packet. Suggest routing to DB-045(j) ("smaller request/UI branches") or the next bridge-test touch rather than widening M5-T075.

**F5 (INFORMATIONAL — pre-existing, must stay byte-unchanged here) test file `:199`.** The pre-existing absent-Content-Length spec asserts `expect(["unexpected_response", "bridged"]).toContain(outcome.kind)`, which accepts either outcome and therefore proves nothing about the documented fail-closed rule; its comment says "undici may auto-populate it". The new over-budget spec's CI green is the evidence that settles the adjacent question — an explicit `headers.set("Content-Length", …)` on a constructed `Response` is honored end-to-end by the runtime — so a follow-up can tighten line 199 to a single expected kind. Not editable under this packet (additions-only, existing assertions byte-unchanged).

**Assessment**

*Real branch logic, not stubs asserting stubs.* Every spec calls the real exported `fetchOutlineBridge`, injecting only the module's documented seams (`fetchImpl`, `signal`, `timeoutMs`). I traced each fault to a distinct production branch: network TypeError → `:382-387`; timeout AbortError → the `if (timedOut)` guard at `:380`; pre-flight caller abort → the early return at `:359-361`; mid-flight caller abort → the `controller.signal.aborted || externalSignal?.aborted` guard at `:381`; over-budget → the size bound at `:399-401`.

---

The two abort paths are genuinely different branches, and the pre-flight spec additionally asserts `fetchCalls === 0`, proving the request never leaves the client.

---

*Typed-result correctness, no thrown leak.* All five specs pin the exact `kind`; the rejected fetch and both `AbortError` rejections are absorbed into typed outcomes rather than propagating.

---

`timeoutMs` is carried (10). The over-budget outcome asserts `httpStatus 200`, `receivedState null`, `correlationId "cid"`, which matches the client reading the correlation header at `:390` before the size bound at `:400`.

---

The three nonempty announcement literals are byte-identical to the arms at `:550`, `:552`, `:554`; both abort specs assert `""` against `:556`.

**END OF PART 2**

---

**PART 3 of 3**

*Mocking soundness in vitest/jsdom.* `abortAwareFetch` never settles on its own and rejects only when its injected signal aborts, so the timeout spec is deterministic: the internal timer is the only thing that can settle it.

---

`timedOut = true` is assigned before `controller.abort()` at `:365-366`, so the catch always reads it true — `client_timeout`, never `aborted` or `network_error`, with no ordering race.

---

In the mid-flight spec, `fetchOutlineBridge` runs synchronously through the external-signal listener registration (`:363`) and the `fetchImpl` call (`:372`) before its first suspension, so the stub's abort listener is attached before the test calls `controller.abort()`.

---

Even a lost race would land on the pre-flight guard and the same `aborted` assertion, so it cannot flake into a false green.

---

Real timers throughout — `apps/web/vitest.config.ts` is jsdom with `vitest.setup.ts` registering only jest-dom matchers, no global fake timers — and the 30_000 ms budget is cleared in the client's `finally`, leaving no dangling timer.

---

*Strict-TS hygiene.* `REFUSAL_OR_BRIDGED_KINDS` pins `ReadonlySet<OutlineBridgeOutcome["kind"]>` with an explicit `new Set<…>` type argument; an inferred set would have narrowed `T` to the seven literals and rejected `.has("network_error")`.

---

The two `as typeof fetch` casts are valid-shape function casts mirroring the pre-existing `stub` helper at `:37`, not invalid-shape probes, so the CODING_RULES `as unknown as T` rule does not apply. Unused parameters are underscore-prefixed (`_input`, `_resolve`), satisfying both `noUnusedParameters` and the after-used unused-vars rule.

---

*Scope and identity.* `git show --numstat 7d225e2f` gives 143/0 on the test file with zero `^-` lines, so every pre-existing assertion is byte-unchanged.

---

The production client blob is identical at `HEAD` and `7d225e2f^` (b1b36871df6156ecccf3cbd02cde697ad41ef943): zero production edits, honoring the code-graph read-only note for the frozen T071 consumer.

---

*CI evidence.* Run 35829303244 (workflow CI at 490c3413) completed SUCCESS across all 18 jobs, including "web (lint + typecheck + build)" and "web-e2e (vitest + Playwright)".

---

The test file blob is ed57959e4c9fb8e5d56d086f8d2a6f5d215cbf53 at 7d225e2f, at 490c3413, and at the pinned head, so that green covers exactly this content — lint, strict typecheck and the five new specs all proven on the pushed head.

---

The newest head run 35881158851 at 4ed4d3d7 was `in_progress` at review time; noted, not blocking, per instruction.

---

*Directive fit.* DB-045(i) names network_error / client_timeout / aborted / over-budget; all four are covered, with `aborted` split into two distinct source branches. D-084-R001 lane-1 packet fit holds.

---

Nothing found rises to a required correction. F1 is a worthwhile documentation fix; F2–F5 are notes.

G3 VERDICT: PASS

END-OF-REPORT
