# M5-T021 — G5 Security (Dependency-Admission) Gate Report (verbatim)

Orchestrator preservation note: report returned by the independent security-reviewer agent
("G5 security review M5-T021") on 2026-09-12 and preserved verbatim below (transport
entity-decoding only). Reviewer was read-only with live OSV/registry/gh access; frozen
review identity: d62a779e + 3cd3b93c + bot lock d83bcfe5 + 18ee6b79. Verdict: PASS
(residual items LOW/informational, non-blocking).

---

I have completed all independent verification. Here is my full G5 gate report.

---

# Gate Report — G5 (dependency-security admission review)

- **Gate ID:** G5
- **Task ID:** M5-T021 (D-042 Next.js 15.5.24 upgrade + whole-tree advisory sweep)
- **Reviewer:** security-reviewer (independent, read-only)
- **Producer:** orchestrator
- **Result:** **PASS**
- **Clean environment/worktree used:** primary checkout at branch `candidate/D-024-mrl-option-b`, HEAD `b405d350`; material verified at the frozen commit set below. External facts re-derived live from OSV (`api.osv.dev/v1/query`) and the npm registry (`registry.npmjs.org`) on 2026-09-12. Workflow runs corroborated via `gh` read-only.

## Frozen identity — VERIFIED

Material identity = `d62a779e` (package.json pins + preserved registry research) + `3cd3b93c` (browserslist 4.28.9 override) + `d83bcfe5` (bot lock, package-lock.json) + `18ee6b79` (vitest-4 S4 focus correction). HEAD `b405d350`.

- The three material *code/lock/test* files — `apps/web/package.json`, `apps/web/package-lock.json`, `apps/web/src/components/address/__tests__/address-confirm.test.tsx` — are touched **only** by these four commits. Confirmed via per-commit `git show --stat` and a full range name-only diff.
- Post-material commits touch **no** material file: `d3c8e055` (producer report + control state), `60095356` (docs), `14bdcc73` (separate task M0-T157 + `tools/test_directive_compliance.py`), `a53e7244` (CI-evidence reports), `b405d350` (control-plane submit). Identity is frozen and intact.
- `d83bcfe5` (the bot commit) touches **only** `apps/web/package-lock.json` (1 file) — confirmed.
- **Advisory note (non-blocking):** the shared branch also carries M0-T157 (`tools/test_directive_compliance.py` fixture repair) and a `docs/MVP_ARCHITECT_REVIEW_QA.md` edit. These are outside M5-T021's `allowed_paths` but belong to other tracked work committed on the same branch; they are not part of M5-T021's material identity and do not affect it.

## Directive/requirement verification

Only **D-042-R001** is applicable to M5-T021 (directive `requirements.json` applicability `task_ids` includes `M5-T021` for R001; R002/R003/R004 bind `D-042-BOOTSTRAP`). This matches the packet's `directive_refs` and evidence map. R003/R004 prohibitions verified additionally as security checks. Formal `verification.json` remains the independent `directive-compliance-verifier`'s artifact; this row is my security-dimension re-derivation.

| Requirement ID | Reviewed identity | Verdict | Reproduced evidence |
|---|---|---|---|
| D-042-R001 (upgrade next→exactly 15.5.24, regenerate lock, whole-tree advisory-free at every severity: exact pins, integrity match, 7-day age gate, G5, CI incl. web-dependency-security green) | package.json @ `3cd3b93c`, lock @ `d83bcfe5`, test @ `18ee6b79` | **PASS** | next pinned exactly `15.5.24` (no 16.x); OSV clean for all six + `@vitest/mocker`; 6/6 ages ≥ 604800s recomputed; 3 integrity spot-checks MATCH registry; workflow run 34726113984 all proof steps success; CI run 34726577993 `web-dependency-security` + `web` + `web-e2e` success |
| D-042-R003 (prohibition: no deploy) | range `9cf2890b..b405d350` | **PASS** | `.github/**` and `render.yaml` byte-unchanged (empty range diff); no deploy config or host change |
| D-042-R004 (scope: 15.5.24 exactly, no 16.x, Supabase/creds/G6 unchanged) | package.json + lock | **PASS** | single `node_modules/next` = 15.5.24; the two "16." lock hits are `@testing-library/react`/`react-is` (unrelated); react/react-dom unchanged at 19.1.2; no supabase/services/migration files touched |

## Threat-by-threat findings

**1. ADVISORY-FREE — PASS (threat class CRITICAL).** OSV (`/v1/query`, 2026-09-12) returns `{}` (no advisories) for all six changed versions — `next@15.5.24`, `sharp@0.35.4`, `js-yaml@4.3.2`, `vitest@4.1.11`, `browserslist@4.28.9`, `baseline-browser-mapping@2.11.21` — and for the actual advisory-carrying transitive `@vitest/mocker@4.1.11`. Methodology validated: OSV correctly reports the known advisories for the *replaced* versions (next 15.5.21 → GHSA-2xp9-vwfh-vxw4 + GHSA-p293-qw3h-jr36; js-yaml 4.3.1 → GHSA-2883-xcg3-v3hh; browserslist 4.28.6 → GHSA-73wf-gq98-2v4g + GHSA-c83g-rgw3-j3cx; vitest 3.2.7 → GHSA-82fw-gwwq-j7x9; sharp 0.35.3 → GHSA-rgj7-g3m4-5g8c), so `{}` is genuinely clean, not "no data." Machine proof: workflow run 34726113984 steps "Blocking npm audit (--audit-level=low)" and "Blocking npm audit (JSON total vulnerabilities must be 0)" both `success`; CI run 34726577993 job `web-dependency-security` `success`.

**2. AGE GATE — PASS (threat class HIGH).** Recomputed all six from live registry `.time` against a conservative reference of 2026-09-12T23:40:00Z: next 18.31d, sharp 17.58d, js-yaml 17.12d, vitest 25.38d, browserslist 8.42d (727,258s), baseline-browser-mapping 9.25d. Tightest = browserslist at 727,258s, clearing the 604,800s (7-day) threshold by ~122k s. Workflow step "Committed-lockfile release-age gate (>= 7 days, fail-closed)" = `success`. **No waiver granted** — every "waiver" string in D-042/M5-T021 files is policy language ("no waiver"/"do not waiver"); there is no waivers directory and no D-042 age-waiver record. **No gate modification** — `.github/workflows/generate-lockfile.yml` and `apps/web/scripts` (incl. `dependency_age_gate.mjs`) are byte-unchanged this task (empty range diff).

**3. INTEGRITY — PASS (threat class HIGH).** All six lock integrity fields are registry-format `sha512`. Spot-checked three against the live registry `dist.integrity` — next@15.5.24, js-yaml@4.3.2, browserslist@4.28.9 — all **MATCH**. Runner verification: workflow step "Deterministic install check (npm ci; integrity + lock↔package.json)" = `success`. All 552 `resolved` URLs in the lock point to `registry.npmjs.org` (0 rogue registries).

**4. SUPPLY-CHAIN POSTURE — PASS (threat class HIGH).** Bot commit `d83bcfe5` touches only `package-lock.json`. The workflow is the reviewed sanctioned path with SHA-pinned actions (`actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5`, `actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020`), npm pinned to 11.18.0, byte-unchanged this task. No dynamic download outside the lock. **No new package introduced:** the OLD lock (parent `9cf2890b`) already contained `browserslist` (4.28.6) and `baseline-browser-mapping` (2.10.43) as transitive members — the two new `overrides` only force version bumps of existing tree members; the other four are pre-existing direct/override entries. None is a brand-new dependency name requiring separate provenance review.

**5. PROHIBITIONS — PASS.** D-042-R003 (no deploy): `render.yaml` + `.github/**` byte-unchanged. D-042-R004 (no 16.x): next = 15.5.24 exactly. Supabase/G6 untouched. Credential-pattern scan across the full material diff (auth/token/password/secret/api-key/private-key) = **empty**.

**6. VITEST-4 TEST CORRECTION — PASS (no-weakening).** The only change wraps `expect(document.activeElement).toBe(streetInput)` in `await waitFor(...)` inside the already-async S4 test. `waitFor` is imported (line 1) and used identically at line 169 for the sibling focus assertion. The assertion still fails if focus never lands (waitFor throws on timeout) — no weakening or removal. Empirical proof: web-e2e 448/448 green on vitest 4.1.11.

**7. RESIDUAL RISK (advisory, non-blocking).**
- **eslint-config-next stays 15.5.21 while next is 15.5.24 — LOW.** Version skew; not advisory-named. The `web (lint + typecheck + build)` CI job is green, so it doesn't break the build. Recommend aligning to 15.5.24 in a future minimal, age-gated bump.
- **vitest 3→4 major behavioral surface — LOW/informational.** jsdom-timing and mocker API changes; empirically absorbed (448/448 green; the one timing-sensitive assertion is the now-wrapped S4 focus test).
- **browserslist 4.28.9 at 8.42d is nearest the 7-day boundary — informational.** Passes now and at workflow time with ~1.4-day margin; researcher named 4.28.8 (35d) as a risk-free fallback.
- **"7th advisory" identity inferred — informational.** The registry research honestly discloses the sharp `GHSA-rgj7-g3m4-5g8c` mapping could not be confirmed against the original CI audit JSON (job 103547594072 artifact absent). The authoritative fact — regenerated-lock audit JSON total==0 across every severity (workflow step success) — subsumes the counting question.
- **Branch co-mingling (M0-T157 + docs) — informational.** Not M5-T021 material; identity unaffected.

## Steps independently executed (reproduction list)

1. `git show --stat` on each of `d62a779e`, `3cd3b93c`, `d83bcfe5`, `18ee6b79`; `git diff --name-only 9cf2890b b405d350` — frozen identity + no material drift.
2. `git diff 9cf2890b 3cd3b93c -- apps/web/package.json` — the six version changes; react/react-dom unchanged.
3. `git diff --name-only 9cf2890b b405d350 -- .github/ apps/web/scripts render.yaml` — empty (workflow/scripts/deploy untouched).
4. `git show 9cf2890b:apps/web/package-lock.json | grep` browserslist/baseline-browser-mapping — present as transitives in OLD tree (no new package).
5. `grep` current lock for the six `node_modules/<pkg>` version+resolved+integrity; scan for `next@16.x`; count resolved-origin (552/552 official).
6. OSV `POST /v1/query` for all six new versions + `@vitest/mocker@4.1.11` → all `{}`; and for six OLD versions → known advisories returned (methodology validation).
7. Registry `.time` fetch for the six → publish timestamps; `date`-based age computation vs 2026-09-12T23:40Z → 6/6 ≥ 604800s.
8. Registry `/<pkg>/<version>` `dist.integrity` for next/js-yaml/browserslist → 3/3 MATCH lock.
9. `gh run view 34726113984` (jobs+steps) → all steps success incl. audit-JSON-total-0, age-gate, npm-ci-integrity, before bot commit; `gh run view 34726577993` (jobs) → all 18 jobs success incl. web-dependency-security, web, web-e2e.
10. Credential-pattern grep across material diff; waiver-record search; supabase/services/migration search — all clean.

## Evidence paths

- `project-control/tasks/M5-T021.json`, `project-control/reports/M5-T021-producer-report.md`, `project-control/reports/M5-T021-registry-research.md`, `project-control/reports/M5-T021-ci-evidence.txt`, `project-control/reports/M5-T021-evidence-map.json`
- `apps/web/package.json`, `apps/web/package-lock.json`, `apps/web/src/components/address/__tests__/address-confirm.test.tsx`
- `.github/workflows/generate-lockfile.yml`
- `project-control/directives/D-042-nextjs-15-5-24-upgrade/requirements.json`

## Regression/security/provenance findings

No blocking findings. Provenance intact: every pin traces to a registry-verified fixed version with publish-timestamp and OSV evidence; the lock was proven by the sanctioned fail-closed workflow chain before the bot committed it; the prior fail-closed run (34726060266) is the negative proof that a defective lock cannot land.

## Defects

None (blocking). Non-blocking residual items enumerated under Threat 7.

## Required rework

None required for this gate.

## Reviewer conclusion

**VERDICT: PASS.** The M5-T021 dependency admission satisfies policy §G at the frozen identity: all six changed versions are advisory-free today (OSV, independently reproduced and methodology-validated) and machine-proven advisory-free whole-tree at every severity; all six clear the 7-day age gate on recomputation with no waiver and no gate modification; integrity is registry-`sha512` with matching spot-checks and runner `npm ci` verification; the supply-chain path is the SHA-pinned sanctioned workflow with a lock-only bot commit and no new package introduced; the deploy/16.x/Supabase prohibitions hold; the vitest-4 test correction is behavior-preserving; and the last standing CI red (`web-dependency-security`) is retired on a fully green run. Residual risks are LOW/informational and non-blocking.

Recommend the orchestrator record G5 PASS. (This security-dimension verdict does not substitute for the independent `directive-compliance-verifier`'s `verification.json` pass on D-042-R001, which remains a separate acceptance input.)
