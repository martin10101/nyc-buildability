# M5-T068 — G4 QA review record (backend-engineer independent return, VERBATIM)

[Orchestrator record header — not part of the reviewer return: dispatched 2026-09-22 ~20:52 UTC
at frozen 141007c37c58c1a0e3d1297c582b015ca0f20bb1 to backend-engineer as the independent G4
reviewer (roster amendment: producer = qa-engineer cannot self-gate; see M5-T068-G0.md
[ORCH-AMENDED]). The agent's stream dropped mid-run in the ~21:0x-21:29Z provider incident
(server_error) and was RESUMED with context intact to a clean single-part return with the
required END-OF-REPORT marker; transport entity-decoding only. Verdict PASS; zero blocking;
advisories A-1..A-3; three mutants OBSERVED red→restored-green.]

---

All duties complete. Delivering the gate report.

---

**VERDICT: PASS** — M5-T068 G4 (test adequacy + mutation proof). No blocking findings.

Independent G4 review by backend-engineer (roster amendment per `reports/M5-T068-G0.md` §[ORCH-AMENDED]; producer=qa-engineer cannot self-gate). Frozen identity `141007c37c58c1a0e3d1297c582b015ca0f20bb1`. All work done on a `git archive`-extracted throwaway of the frozen `services/api` tree in scratchpad; **zero repository files touched** (so "git diff empty on services/api" holds by construction — my isolation worktree was pruned after the earlier stream drop, git objects remain shared).

**Identity (git plumbing):** Test files byte-identical between material `5aad9007` and frozen `141007c3` (`git diff` empty on both paths). Material commit = exactly 3 files / 443 insertions / 0 deletions (2 test files + producer report) — the TEST-ONLY wall is honored; no production file appears in the change. Engine `max_envelope.py` and route `max_envelope_api.py` are frozen accepted material (unchanged, present via the frozen tree only).

---

**1. Scoped suite (RUN by me, frozen content, offline):**
- `cd services/api && python -m ruff check .` → `All checks passed!` (the api CI job's first step; also ran scoped to the two files — clean).
- `python -m pytest tests/scenario/test_max_envelope.py tests/api/test_max_envelope_api.py -q` → **63 passed in 14.87s** (56 pre-existing + 7 new). Env: py3.11.9 / fastapi 0.128.0 / pytest 8.4.2 — a CORROBORATION of the authoritative CI SUCCESS at `a66cf818` (CI is py3.12).

**2. Mutation proof — 3 OBSERVED (live red→restored green), 3+ ASSESSED:**

| Mutant | Test(s) hit | Result |
|---|---|---|
| **OBSERVED** — AS-1/(d) fail-closed raise silenced: `_verify_consistency` raise→`pass` (`max_envelope.py:919-925`) | `test_fail_closed_raise_on_saturating_checker_fail` (scenario:762) **and** `test_500_generator_checker_inconsistency_is_a_bounded_generic_error` (api:300) | BOTH went **red** (`observed == {}`; no MaxEnvelopeError), restored byte-identical, green again |
| **OBSERVED** — AS-6(c) drift: `_LOT_AREA_INPUT` value changed (`max_envelope.py:113`) | `test_lot_area_input_matches_accepted_checker` (scenario:689) | **red** (`'lot_area_sqft' != 'lot_area_sq_ft'`), restored, green |
| **OBSERVED** — AS-5/(b) conflict-advisory populated branch → `return None` (`max_envelope.py:526-529`) | `test_conflict_advisory_populated_branch_is_surfaced_never_resolved` (scenario:732) | **red** (`assert None is not None`; binding 0.5/cov-a still selected, proving the branch-specific tooth), restored, green |

Clean-restore verified: engine `cmp` byte-identical to the pre-mutation frozen copy; full scoped suite re-run 63 passed.

- **ASSESSED (read + live-green, sound):** AS-2/(e) `test_500_registry_unavailable...` (api:363) — patches `get_max_envelope_registry`→raise; route `except Exception:` at `max_envelope_api.py:291-293`→`_internal_error_500`; asserts fixed body + `secret`/`"RuntimeError"` absent from `resp.text` + X-Correlation-ID (a leak-regression would go red). AS-3/(f) `test_out_of_2263_bounds...` (scenario:787) — pins the BOUNDS branch specifically via `lot_rectangle is not None` (`max_envelope.py:800-809` sets it; the rect-None branch at :797 does not), so it can't be satisfied by a generic geometry gap. AS-4/(a) `test_as4_multi_floor...` (scenario:696) — real checker re-run at the 250ft multi-floor cap, asserts `floor_count>1`, per-floor≤`MAX_FLOOR_TO_FLOOR_FT`, both saturating dims pass.

**3. Coverage vs the six DB-046 gaps — each closed with a defect-sensitive test; no residual hole:**
(a) multi-floor→scenario:696 ✓; (b) conflict advisory→scenario:732 ✓ (OBSERVED); (c) drift guard→scenario:689 ✓ (OBSERVED); (d) fail-closed raise engine+route→scenario:762 + api:300 ✓ (OBSERVED, both); (e) live 500→api:363 ✓; (f) out-of-bounds→scenario:787 ✓. The route's `candidate.*`→500 vs precondition→422 split (`max_envelope_api.py:314-323`) is exactly the intended branch the (d) route test drives (proven by the OBSERVED red).

**4. AS-1..AS-6 → concrete tests at frozen identity:**
- AS-1 → `test_fail_closed_raise_on_saturating_checker_fail` (scenario:762) + `test_500_generator_checker_inconsistency...` (api:300) — OBSERVED.
- AS-2 → `test_500_registry_unavailable...` (api:363).
- AS-3 → `test_out_of_2263_bounds_lot_is_a_geometry_unsupported_gap` (scenario:787).
- AS-4 → `test_as4_multi_floor_height_flows_through_the_full_consistency_proof` (scenario:696).
- AS-5 → `test_conflict_advisory_populated_branch...` (scenario:732) + `test_lot_area_input_matches_accepted_checker` (scenario:689) — OBSERVED (both).
- AS-6 → 63 passed (56 unchanged + 7 new) + `ruff check .` clean + engine/route byte-frozen (verified: not in the material change).

**5. Over-mocking / order / timing:** None blocking. Doubles (`_StubCheck`/`_StubReport`, `_ConflictRegistry`, the check_proposal monkeypatch) are the minimum needed to reach branches that are unreachable-by-construction (the fail-closed FAIL, the populated FH-2 conflict); the affirmative paths use the REAL checker (AS-4). The (d) route spy runs the REAL `derive_max_envelope` and re-raises unchanged — observation, not fabrication — and I proved it bites (OBSERVED red under the silenced-raise mutant). All patches are pytest `monkeypatch` (auto-undone); each test uses fresh app/client fixtures; floats via `pytest.approx`; dict-equality (`observed == {...}`) and self-constructed tuple ordering (`["cov-a","cov-b"]`) are deterministic. No retry counts, sleeps, wall-clock, or exact-count-across-renders — the api-side DB-048 flake class is absent.

---

**Blocking findings:** none.

**Advisory (non-blocking):**
- A-1: `_StubCheck`/`_StubReport` are duplicated across the two test files rather than shared — minor, acceptable for isolation.
- A-2: (a)/(e)/(f) were ASSESSED (live-green + code-read), not isolated-mutant-driven; their assertions would catch the obvious regressions, and the highest-risk MEDIUM gap (the fail-closed raise) was OBSERVED both engine- and route-side.
- A-3: Local run is py3.11/fastapi0.128 vs CI py3.12 — my green corroborates; CI SUCCESS at the frozen lineage stays authoritative.

Relevant files (absolute): `C:\Users\MLFLL\Downloads\nyc-zoning\ctl24\services\api\tests\scenario\test_max_envelope.py`, `C:\Users\MLFLL\Downloads\nyc-zoning\ctl24\services\api\tests\api\test_max_envelope_api.py`, engine `...\services\api\app\scenario\max_envelope.py`, route `...\services\api\app\api\v1\max_envelope_api.py` (both frozen), producer report `...\project-control\reports\M5-T068-producer-report.md`.

END-OF-REPORT M5-T068-G4
