# M0-T121 — Directive-compliance verification (VERBATIM verifier return; saved by the orchestrator, transport entity-decoding only)

All 20 CI check-runs on 6edf820 are now `success` (supervisor-bridge completed green). HEAD still 6edf820. I have reproduced primary evidence for all 19 requirements. Here is my verdict.

---

# DCV REPORT — M0-T121 (restart-channel defect fix), D-024 Amendments 16/17/18

**Reviewed head:** `6edf820` (confirmed `git rev-parse HEAD` = 6edf82022b36d5df6da9a02589b329881d488df5, twice, unchanged during review; branch `control/D-024-fable-codex-loop`, pushed = `origin/control/D-024-fable-codex-loop`).
**Material identity verified:** `668c824` (created the surfaces: restart_channel.py, cli.py +2, test file, README, runbook, reports) → `6432d2d` (test-only rework: only `test_agent_supervisor_restart_channel.py` + producer report; `git show --stat 6432d2d` shows NO production file). `git diff --stat 6432d2d 6edf820 -- tools/agent_supervisor/ <testfile>` is EMPTY, so the code/test identity at HEAD equals the identity G3/G4/G5 reviewed (6432d2d).
**CI on pushed tip 6edf820:** 20/20 check-runs `completed`/`success` (final poll). Note: at first poll `supervisor-bridge (pytest tools/test_agent_supervisor_*.py)` was `in_progress`; it concluded `success` on re-poll. All green.

## Per-requirement verdicts (19/19)

**D-024-R302 — PASS.** Authorization bounded to exactly two ledger tasks. `tasks/M0-T122.json`: exists, `dependencies: ["M0-T121"]`, `status: "backlog"`, `producer_agent: null` (held pending T121). Amendment 16 verbatim ("Ledger tasks allocated under this authority: M0-T121 and M0-T122"). Qualifying evidence `project-control/reports/M0-T107-cycle2-start-refusal.md` present (5828 bytes), cited in the packet objective and G0 report. Window = one defect task + one recert window, as authorized.

**D-024-R303 — PASS.** Reproduced the pre-fix state at `04216dd`: `git grep owner_explicit_restart 04216dd -- tools/agent_supervisor/` returns ONLY `state_machine.py:397` and `:399` (the `_t(...)` table rows) — zero call sites; `git grep owner_answer_validated 04216dd` returns only `state_machine.py:272` (table) plus two `cli.py` comment lines (1899/1906) that note resume-pending-prompt deliberately does NOT cover it — zero call sites. `restart_channel.py` absent pre-fix. Current tree now supplies three surfaces (`owner_restart`, `acknowledge_emergency_stop`, `owner_answer_resume`) → complete F-2 class (3 latent edges) closed.

**D-024-R304 — PASS.** `restart_channel.py:310-322` `owner_restart()` fires `HALTED->IDLE` on `owner_explicit_restart` via `_locked`; `_fire_edge` (lines 270-307) applies exactly one `StateMachine.transition` then appends `EVENT_OWNER_RESTART` ("operator_owner_restart"). `test_AS1_live_shape_journal_restarts_truthfully` asserts one transition (before+1), `verify_chain().ok` before AND after, and the durable event present. Fail-closed via `evaluate_preconditions`.

**D-024-R305 — PASS.** `acknowledge_emergency_stop()` (lines 375-422) requires BOTH `acknowledged=True` AND `confirm_token == emergency_ack_token(journal)` (a 16-char digest of the last transition-into-EMERGENCY_STOPPED provenance, printed on mismatch). Materially stronger than `owner_restart`. Tests: `test_AS4_missing_acknowledgment_refuses` (code `acknowledgment_required`), `test_AS4_wrong_token_refuses` (`confirm_token_mismatch`), `test_AS4_correct_token_and_ack_exits_emergency` (works only with the journal token). All pass.

**D-024-R306 — PASS.** Ordinary `owner_restart` has `expected_from_state=HALTED`, so from EMERGENCY_STOPPED it refuses `wrong_state` (`test_ordinary_restart_refuses_emergency_stopped_state`, `test_AS4_ordinary_surface_refuses_emergency_stopped`). `grep -rn owner_explicit_restart tools/agent_supervisor/` shows the trigger is fired ONLY by the two restart_channel surfaces (via the `OWNER_EXPLICIT_RESTART` constant) — no scheduler/recovery/succession/loop caller; `policy.py` has no restart classification. Emergency exit is impossible via ordinary or automatic path.

**D-024-R307 — PASS.** Independently recomputed from `state_machine.py`: `BLOCKING_STATES = {WAIT_FOR_OWNER, PAUSED_RECOVERY, EMERGENCY_STOPPED, HALTED}`, `TERMINAL_STATES = {COMPLETE, EMERGENCY_STOPPED, HALTED}`. Derived all 11 edges leaving blocking-or-terminal from `TRANSITIONS`; they match the producer-report §3 table row-for-row. `test_the_recovery_edge_set_is_the_expected_owner_edges` pins the derived set.

**D-024-R308 — PASS.** My derivation yields exactly 5 operator-recovery edges (source∈blocking∪terminal, target∉, trigger `owner_*`): `(HALTED,owner_explicit_restart)`, `(EMERGENCY_STOPPED,owner_explicit_restart)`, `(WAIT_FOR_OWNER,owner_answer_validated)`, `(WAIT_FOR_OWNER,owner_approved_pending_prompt)`, `(PAUSED_RECOVERY,owner_cleared_pause)`. `test_every_operator_recovery_edge_has_a_registered_cli_surface` asserts `uncovered==∅`; the three per-surface drop tests prove exactly-one-surface. Terminal-deepening edges (owner_halt/owner_closed_stage/owner_emergency_stop) and `COMPLETE->IDLE run_closed` excluded by the target-rule — no recovery surface, as intended.

**D-024-R309 — PASS.** Ran `python -m pytest tools/test_agent_supervisor_restart_channel.py -q` → **34 passed**. The sweep is edge-granular: `operator_recovery_edges()` returns `(state_from,trigger)` pairs from `TRANSITIONS` (test lines 102-120); `reachable_literals` resolves constant NAMES to string values through each handler's own globals and walks the AST closure within cli+restart_channel; `edge_has_surface` requires ONE handler naming BOTH source state AND trigger (so a sibling on the shared trigger cannot stand in). Per-surface drop tests exist for all three verbs (`test_dropping_only_owner_restart_fails_the_sweep`, `_acknowledge_emergency_stop_`, `_resume_after_answer_`). RED encoded: `test_pre_fix_registration_set_is_red` asserts the pre-fix handler subset leaves exactly the 3 closed edges uncovered.

**D-024-R310 — PASS.** `restart_channel.py` writes only `StateMachine.transition` + `audit.append`; it clears no flag, resets no budget, erases no history. `test_AS8_only_state_and_trigger_keys_change` asserts the durable diff == `{STATE_KEY, LAST_TRIGGER_KEY}` with every other key byte-identical; `test_AS8_no_flag_or_budget_key_is_reset` shows a set `manual_pause` survives. Each verb fires one fixed edge with no target-state argument (no generic command). `state_machine.py`/`recovery.py` NOT edited (668c824 diff-stat). No journal-edit path.

**D-024-R311 — PASS.** `evaluate_preconditions` (lines 202-262), first-failure-wins order: emergency-stop flag, exact source state, open owner asks, pending external effects, surviving/undetermined children, recorded recovery classification (which surfaces `provider_identity_drift` specifically when cli_capability_manifest/auth failed, else `unsafe_recovery_classification`). Each precondition has a real check + individual test: AS-5a open_asks, AS-5b pending_effects, AS-5c surviving_children, AS-5d provider_identity_drift, AS-5e unsafe_recovery_classification, AS-6 emergency_stop_flag; each asserts refusal AND state unchanged (HALTED). Live drift probe documented-deferred to `start` preflight (not a silent skip; `_recovery_classification_precondition` docstring lines 148-172).

**D-024-R312 — PASS.** Every matrix item maps to a named passing test I confirmed in the 34-test run: pre-fix journals → `test_AS1` via `build_halted_live_shape` (HALTED through `PATH_TO_HALTED`/decision_halt_unsafe + 3 denied asks + recorded SAFE_CHECKPOINT + chain verified); current/fresh journals → `test_cmd_owner_restart_refuses_fresh_journal`, `test_recovery_unclassified_refuses`; repeated invocation → `test_AS2`; stale runs → `test_AS7_stale_lock_is_taken_over_not_refused`; concurrent controllers → `test_AS7_live_foreign_lock_refuses` (a live-foreign-pid lock refusal, the sound proxy) + `test_AS7_exactly_once_across_sequential_invocations`; emergency-stop recovery → `EmergencyStopAcknowledgment::*` (incl. wrong/missing token); audit-chain continuity → `verify_chain().ok` before/after in AS-1/AS-4/owner-answer; removal sensitivity → the drop tests + RED. Judged on the owner's wording "concurrent controllers": the coverage item is met by the lock-contention proxy, and both the producer report §5 and the evidence-map row state accurately that this is a proxy, NOT a threaded race — the claim matches the test.

**D-024-R313 — PASS.** `_locked` (lines 425-460) acquires the single-instance lock, re-checks preconditions UNDER the lock, then `_fire_edge` — closing the check-then-act window. `test_AS7_exactly_once_across_sequential_invocations` asserts exactly ONE HALTED->IDLE transition across two calls (second fails `wrong_state`). `_fire_edge` commits the transition (which writes `state_transition`) FIRST, then appends the first-class `operator_owner_restart` durable audit event — durable audited owner-restart record.

**D-024-R314 — PASS.** Gate files: G0 PASS (orchestrator, ef0d476), G2 PASS (orchestrator, 668c824), **G3 PASS (code-reviewer, 6432d2d)**, **G4 PASS (qa-engineer, 6432d2d)**, **G5 PASS (security-reviewer, 6432d2d)** — the three independent reviewers are distinct from the producer `supervisor-restart-producer` (producer ≠ verifier). Mutation proof RED (`test_pre_fix_registration_set_is_red`)/GREEN reproduced (34 passed). Freeze baseline: evidence-map claims 2811 passed/0 failures (orchestrator-run); I reproduced the 34-test restart_channel subset and re-derived the edge set independently. CI on 6edf820 = 20/20 success (all green, incl. supervisor-bridge). Frozen-identity recertification correctly deferred to M0-T122 (held/backlog, depends_on T121). DCV = this pass.

**D-024-R318 — PASS.** Producer not interrupted to capture a directive: `progress_log` shows a single dispatch→completion; the producer transcript has 0 interruption/stop markers and ends on a natural assistant message. Amendments 17/18 capture notes state they were recorded at the safe seam / read-only while the producer ran.

**D-024-R319 — PASS.** `grep -niE` for operability/autonomy claims across producer report, repair record, evidence map, README, runbook: the ONLY hit is the explicit disclaimer in the producer report ("this report does NOT claim continuous operability"); the evidence-map R319 row repeats the disclaimer. No affirmative continuous-operability/full-autonomy claim anywhere.

**D-024-R323 — PASS.** Primary evidence reproduced: `grep -o '"model":"[^"]*"' <producer transcript>.jsonl | sort | uniq -c` on `agent-a5895208e35d99e99.jsonl` → **309 "model":"claude-opus-4-8"** and (explicit second grep) ZERO non-opus-4-8 ids — matches the final re-read in `M0-T121-producer-model-identity.md` §6 (309/309). Durable authority: `.claude/agents/backend-engineer.md` frontmatter `model: claude-opus-4-8`, `effort: high`; `git log -1 --format=%H -- .claude/agents/backend-engineer.md` = `8b1b386` (byte-stable); D-004-R735 assigns Opus 4.8 to all non-reviewer agents.

**D-024-R324 — PASS.** Running producer not interrupted (0 interruption markers, natural completion; single dispatch). The identity evidence was gathered by a read-only grep of the transcript file — no message to or stop of the agent.

**D-024-R325 — PASS.** Determination AUTHORIZED is supported: dispatch passed no `model` override; no quota/unavailability/fallback event exists in the window (orchestrator stayed on Fable 5); the uniform 309/309 opus-4-8 identity is the model D-004-R735 pre-assigned to `backend-engineer`. The R325 fail-closed condition (unauthorized substitution/fallback) is NOT met — no owner disposition required.

**D-024-R326 — PASS.** Authority is claimed only from the explicit D-004-R735 assignment + the checked-in `backend-engineer.md` frontmatter it mandates (model-identity report §3/§4); §4 states no allowlist was consulted or cited. Allowlist-is-not-authorization honored.

## Discrepancies between claims and evidence
None material. Every claim I reproduced matched the reports (edge set, blocking/terminal sets, 34 tests, 309/309 model ids, gate SHAs/reviewers, cli.py +2 diff, byte-stability to HEAD, CI green). One process note (not a defect): the three general control-plane harness suites (`test_directive_compliance.py`, `test_project_control.py`, `test_directive_reminder.py`) exceed the sandbox timeout locally (subprocess/git-heavy on this Windows host); however `tools/validate_directive_compliance.py --check` returns EXIT=0 and the CI `control-plane (workflow regression test, ADR-005)` check on 6edf820 is `success`, so control-plane integrity is confirmed by CI + the validator rather than by the local script runs.

DCV VERDICT: ALL PASS
