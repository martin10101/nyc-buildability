<!-- Orchestrator capture: data-contract-verifier G1 return for M5-T045 saved VERBATIM
     (transport entity-decoding only). Pinned at frozen head 2b851c18; reviewer recomputed
     six digests, proved the forbidden-path negative, and confirmed both dataset
     identities against the live NYC Open Data catalog. -->

I have completed the independent G1 data-contract verification. All checks pass. Here is my gate report.

---

# G1 (Data-Contract) Gate Report — M5-T045

**Task:** M5-T045 — Condo billing-BBL live wiring (DB-029): registry record + real null-billing fixture + base-lot resolution seams + records-vs-allowances display
**Reviewed at frozen head:** `2b851c18144aee5cd84cbfa9b011dfa5346c7a0c` (branch `candidate/D-024-mrl-option-b`)
**Live HEAD at review:** `7c935d25ebd1ba842e4d0418fd82f44963b79ef1` (disjoint control-plane commits past the freeze — see identity note)
**Reviewer:** data-contract-verifier (read-only; ran only git read/plumbing + hashing + WebSearch)
**Contract basis:** reviewed against the AMENDED packet (tagged `ORCH-SCOPE-DISPOSITION 2026-09-19`, AS-3/AS-8 amended)

## VERDICT: **PASS** (zero blocking findings; 3 advisory)

---

## Identity / provenance verification

- Frozen head `2b851c18` exists and is a commit; its subject confirms it carries the "M5-T045 submission evidence (digest index, evidence map, CI green at 21ee7379)".
- **6 of 16 surface digests independently recomputed** (git blob bytes = LF at the frozen head) and matched `M5-T045-source-sections.md` byte-for-byte:
  - `condo_base_lot.py` → `9a816c04…f2ed0e` ✓
  - `dtm_condo_soda.py` → `041ee640…4aa3f158` ✓
  - `live_provider.py` → `1dc3aaca…09229a0` ✓
  - `zoning_crosscheck.py` → `0f30967b…03a03c984` ✓
  - `dtm-condo.json` → `5fe69e09…9ac7507` ✓
  - `test_dtm_condo_soda.py` → `a59b5c82…12727a0a` ✓
  The working-tree files I read are therefore byte-identical to the frozen material; live-HEAD drift is control-plane/report-only (T047 peer), consistent with the CI-evidence note.
- **Change-set proof:** the T045 material cherry-pick `28082288` spans 15 files, all inside `allowed_paths`. Grepping the change-set for `builder.py | _contract_schemas | packages/contracts | spatial/adapter.py | app/rules/` returned NONE. No forbidden contract path is touched → the "NO contract-shape change anywhere" claim is structurally proven, not merely asserted.
- **CI evidence (stored, verified not re-executed):** `M5-T045-ci-evidence.md` pins run `35431367744` (CI incl. web-e2e) success at `21ee7379`, the first head whose CI covers this byte-identical material. Verifying stored evidence is correct here; I do not return BLOCKED for non-execution.
- **Official source independently confirmed (provenance):** WebSearch (network otherwise default-deny for the packet; used only to confirm identifiers) confirms `p8u6-a6it` = DOF "Digital Tax Map: Condominiums" and `eguu-7ie3` = DOF "Digital Tax Map: Condominium Units", both DOF-published, cadence "extracted … last Friday of each month, refreshed on ArcGIS Online" — matching the registry records verbatim.

---

## Surface 1 — `dtm-condo.json` registry draft (AS-6, DB-029b)

**PASS.** Two records (`p8u6-a6it`, `eguu-7ie3`).

- **Field-for-field parity vs committed `ztldb.json`:** both records carry the identical 19 top-level keys in the same order as `ztldb.json` (source_id … open_questions). No invented top-level keys (the fail-closed risk). `authentication` {required, recommended, evidence} and `rate_limits` {without_token, with_token, throttle_signal, evidence} sub-shapes mirror ztldb exactly; `fields_available.columns` is a list-of-strings and `null_semantics`/`note` are strings, same types as ztldb.
- **Column counts vs connector inventories:** registry `p8u6-a6it` columns array = 9, matching `CONDO_COLUMNS` (frozenset of 9, `dtm_condo_soda.py:151`); registry `eguu-7ie3` = 16, matching `UNIT_COLUMNS` (`dtm_condo_soda.py:164`). Column *names* are identical set-for-set between registry and connector, and `test_c10_column_inventories_match_research_counts` (`test_dtm_condo_soda.py:397`) asserts len 9/16.
- **Per-request freshness mechanism:** registry (`dtm-condo.json:20`) states `X-SODA2-Truth-Last-Modified == rowsUpdatedAt`, both `1788271556 = 2026-09-01T14:05:56Z`. I independently verified the epoch arithmetic (1788271556 → 2026-09-01T14:05:56Z) and cross-checked the capture doc: capture (b) header `Tue, 01 Sep 2026 14:05:56 GMT` == capture (c) `rowsUpdatedAt:1788271556`. The `eguu-7ie3` value `1788271541 = 2026-09-01T14:05:41Z` is internally consistent (15 s earlier) and matches `RESEARCH_OBSERVED_ROWS_UPDATED_AT` (`dtm_condo_soda.py:118`).
- **DB-029(b) gating (registry-before-live-traffic):** the registry file lands in the same atomic material commit (`28082288`) as the wiring, and the live path is flag-gated OFF by default (`LIVE_SPATIAL_PROVIDER_ENABLED` absent → disabled). AS-6's "same change-set as (in practice before) any live-traffic wiring" is satisfied.

## Surface 2 — Real null-billing fixture (AS-2, DB-029c)

**PASS.**

- **Byte-faithful:** `REAL_NULL_BILLING_103343_BODY` (`test_dtm_condo_soda.py:180`) is character-identical to capture (b)'s 229-byte body (condo_key 103343, single base lot `1003030019`, 8-key object, `condo_billing_bbl` absent). `REAL_NULL_BILLING_100355_BODY` (`:191`) matches capture (a)'s first record (7-key, no `condo_name`, no `condo_billing_bbl`).
- **Key-absence semantics:** `test_c6_null_billing_key_absence_is_tolerated` (`:461`) is parametrized over the 7- and 8-key shapes and asserts `"condo_billing_bbl" not in record` — the load-bearing fact. The parser (`_collect_base_lots`, `dtm_condo_soda.py:519`) branches on `record.get(...)` key presence, never on an explicit null; no KeyError path exists.
- **Documented negative honored:** the multi-lot null-billing case (which cannot exist citywide, 28==28) is exercised ONLY by the clearly-labeled `SYNTHETIC_MULTI_LOT_KEY_ABSENT_BODY` (`:205`), which is byte-shape faithful (keys omitted, never explicit null) and never presented as an official capture. The prior byte-infaithful explicit-null synthetic is retired.

## Surface 3 — T044-wave riders on `dtm_condo_soda.py` (AS-7)

**PASS.** All five closed with a named test AND a code anchor:

1. Response-side `condo_key` fullmatch guard: `dtm_condo_soda.py:737` + `test_rider_unit_response_condo_key_guard_rejects_bad_shape` (`:954`) — proves SchemaDriftError fires before the expansion URL is reached.
2. Optional `$limit` off-by-default byte-identical URLs: `_limit_suffix` (`:359`, returns `""` on None) + `test_rider_row_limit_off_by_default_and_opt_in_appends_soda_limit` (`:910`) and non-positive fail-closed (`:940`).
3. `_ERROR_CODE_SAFE_RE` `.fullmatch`: `:207`/`:354` + `test_rider_error_code_sanitizer_uses_fullmatch_not_prefix` (`:881`) — trailing-newline prefix injection is `repr()`'d.
4. `invalid_component` documented code: `resolve_by_condo_key` raises it at `:818`; it IS a documented code in `bbl.py:44-47` (I verified the docstring vocabulary); `test_rider_condo_key_invalid_shape_uses_documented_invalid_component_code` (`:977`).
5. Canonical-normalizer consistency note: `canonical_bbl = normalize_bbl(bbl).canonical` (`:647`) + `test_rider_condo_key_canonical_value_interpolated_unchanged` (`:987`).

## Surface 4 — `condo_resolution` channel shape (no contract-shape change)

**PASS.** `condo_resolution_report` (`zoning_crosscheck.py:524`) maps the typed `CondoResolution` onto the SAME contract-1.3.0 channels `crosscheck_lot_zoning` already emits:

- **Conflict entries (multi-lot, `:567`)** use the identical shape as the existing zoning conflict (`:406`): keys `field` / `values[{source_id, value, derivation}]` / `resolution:"unresolved"` / `reason`. No new keys.
- **Notes** are plain strings appended to `CrosscheckReport.notes` (`reproducibility.connector_notes`), carrying a machine-readable outcome token `condo_resolution: [<OUTCOME_*>]` (`_condo_note_prefix`, `:511`). The token equals `CondoResolution.outcome`, so the profile-note producer and the web parser cannot drift.
- **`CondoResolution.outcome`** vocabulary (`condo_base_lot.py:75-79`) is exhaustive and typed; `_from_result` (`:212`) never collapses a multi-lot set (`resolved_base_bbl` set only for single, `:243`; `None` for multi, `:250`) — a consumer physically cannot pick one lot.
- **`CrosscheckReport` dataclass is unchanged** (still compared_fields/conflicts/uncertainties/agreements/notes); `condo_resolution_report` only populates existing fields.
- **Live-provider seam (`live_provider.py:237`)** returns the engine `LotIntersectionRecord` unmodified and records the base-lot substitution only via a payload-only log line (`_record_condo_substitution`, `:221`) — no fabricated fact, no new contract shape; fail-safe → `None` on multi-lot/unresolved/error (`:263-267`); non-condo pass-through byte-identical (`:277-278`).
- **Change-set proof (above)** confirms contract schemas and `builder.py` are untouched.

---

## Findings

No blocking (F) findings.

- **A1 (advisory) — `dtm-condo.json:22,75` `fields_available` nested sub-key set.** Both records' `fields_available` carry {note, columns, null_semantics} but omit ztldb's `assignment_rules_verbatim_basis` and `park_caveat`. Those two are ZTLDB-domain-specific (split-lot assignment %, PARK consolidation) and genuinely N/A to the condo datasets; no *invented* keys are added, and every top-level key is present, so "mirror field-for-field / invented shapes fail closed" is met. Non-blocking; flagged only so a strict-parity reader sees it was considered.
- **A2 (advisory / provenance) — `condo_resolution_report` reachable-by-contract but not production-propagated in this packet.** The helper and its channel mapping are verified in isolation (`test_ztldb_crosscheck.py`), but the four profile-build routes still call `build_property_profile(...)` without `additional_conflicts`/`additional_notes` (producer report §6/§10). This is correctly routed as discovery **DB-031** and is explicitly within the AMENDED `ORCH-SCOPE-DISPOSITION` (multi-lot records-view SPLIT to a named successor; those four route files are outside `allowed_paths`). Not a G1 defect against the amended contract.
- **A3 (advisory) — `last_successful_ingestion: null` on both records.** Appropriate for a not-yet-ingested, flag-gated-off source and mirrors committed `ztldb.json`. Not a defect.

## What I verified vs. relied on
- Independently recomputed 6 LF-normalized digests at the frozen head (all matched); proved the change-set touches no forbidden contract path; verified the freshness epoch arithmetic; compared both real fixtures against the byte-faithful capture doc; confirmed both dataset identifiers/agency/cadence against the live NYC Open Data catalog via WebSearch.
- Relied on orchestrator-captured CI evidence (`21ee7379` green) for web-e2e and full-suite execution, per the read-only evidence-capture division — verified its stored identity, did not re-run.

**Verdict: G1 PASS.** Corrections needed for acceptance: none. Advisory A1–A3 are non-blocking.
