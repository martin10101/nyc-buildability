# M5-T038 — G5 security review return (VERBATIM; security-reviewer, read-only, pinned d94bbcb6)

Orchestrator note: returned via the agent-return channel 2026-09-18T09:55:02Z; the transport
truncated the first delivery inside point (5). Part 1 below is the truncated delivery verbatim;
Part 2 is the resent tail, verbatim. VERDICT: PASS — no findings at any severity.

---

## Part 1 (first delivery, verbatim)

G5 security review complete. All checks pass, no findings. Full report below.

---

## G5 SECURITY REVIEW — M5-T038 (address-flow + context-panel polish; DB-006/007/009/019a-c)

**VERDICT: PASS** — no critical, high, medium, or low security findings.

### Scope & integrity verification
- **Pinned material commit `d94bbcb6`** — diff is 4 web source files + 4 web test files + producer report; all within the packet's `allowed_paths` (`project-control/tasks/M5-T038.json` status `awaiting_gate`).
- **Byte-stability d94bbcb6..HEAD (`33b0c161`): CONFIRMED.** `git diff d94bbcb6 HEAD` over all 12 allowed_path entries returns empty — the reviewed material is byte-identical at HEAD. The only later change touching `apps/web/**` is `8c089343`, which edits `apps/web/e2e/architect-workspace.spec.ts` (NOT in allowed_paths — the orchestrator's out-of-scope e2e consumer-sweep copy fix). Interleaved commits `a6875731`/`33b0c161` are disjoint M5-T037 material. Packet identity intact.

### Security surface assessment (per brief)

**(1) Retry state machine — `apps/web/src/lib/address-search.ts` — no amplification, no injection.**
- The new `rejected` outcome (`address-search.ts:138-139`) returns immediately; the retry loop guard (`:175`) advances only while `outcome.reason === "source_unavailable"`, which is now strictly `status >= 500` (`:136-137`). A 4xx/404/422 therefore spends exactly one attempt and is never retried. Attempts are hard-bounded to `ADDRESS_SEARCH_MAX_ATTEMPTS = 3` (`:14`, `:172`). This change *tightens* the prior surface — previously `!response.ok` routed every non-2xx (including 4xx) into the retried `source_unavailable` branch. No amplification introduced.
- Endpoint stays the hardcoded official constant (`GEOSEARCH_AUTOCOMPLETE`/`GEOSEARCH_SEARCH`, `:4`/`:8`); the only user-controlled component is `text`, which is `encodeURIComponent`'d (`:127`) and length-bounded to 3–200 chars (`:170`). No user-controlled URL/host/path, no new query params — no SSRF or URL injection. Request hardening preserved: `credentials:"omit"`, `referrerPolicy:"no-referrer"`, `Accept: application/json` (`:127`). Response body bounded to 128 KB (`:16`, `:92`); parse is strict (source must be `nycpad`, control-char rejection `:56`, borough/zip allowlists, ≤8 results).

**(2) Error copy — no echo of upstream body or user input.** All six `ERROR_MESSAGES` in `AddressAutocomplete.tsx:23-31` are static template literals built only from the `FULL_ADDRESS_SEARCH_LABEL` constant. No upstream response body or typed text is interpolated. No `dangerouslySetInnerHTML`/`innerHTML`/`document.write` anywhere in the diff (swept, clean). React escaping is the norm.

**(3) DB-019b/c display-only — no data leak, ZoLa via validated helper.**
- `CoverageBadge` (`ZoningContextPanel.tsx:115-118`) renders `view.coverageStatus`, which originates from `mappedFeatureView` reading the already-fetched profile through the `isCoverageStatus` guard (`contract.ts:265`). No new fetch, no new query params, no secrets.
- Both ZoLa links route through `zolaLotUrl` (`PropertyOverview.tsx:47`, `ZoningContextPanel.tsx:38`), which strictly validates a canonical 10-digit BBL (`/^[1-5][0-9]{9}$/`, `provenance-link.ts:57`) and builds from the module-constant `ZOLA_LOT_PREFIX`; it returns null (honest "needs a valid BBL" note) otherwise. No raw template-string URL reintroduced, no server-echoed/reflected URL reaches an `href`.

**(4) Accessibility (aria-hidden) — decorative glyphs only.** Every added `aria-hidden` wraps a decorative glyph (the `↗` arrow on the two ZoLa links; the status symbol in `CoverageBadge`). Link text ("Open ZoLa" / "Open in ZoLa") and the badge value + `visually-hidden` gloss remain exposed to assistive tech. No meaningful content is hidden; the AS-5 test asserts names unchanged and glyphs decorative.

**(5) Secrets / storage — clean.** Secret scan across the full pinned diff (JWT/`sk-`/`service_role`/PEM/`ghp_`/`AKIA`/`password=`) returns no matches. No `localStorage`/`se[TRANSPORT TRUNCATION POINT]

---

## Part 2 (resent tail, verbatim; delivered 09:55:21Z — report COMPLETE)

The rest of point (5) was one sentence, and one closing note followed it. Sending both now.

---

**(5) continued:** No `localStorage`/`sessionStorage` writes, no `process.env`, no `Authorization` header, no API-key or service-role material — the secret scan across the full pinned diff (JWT / `sk-` / `service_role` / PEM / `ghp_` / `AKIA` / `password=`) returned zero matches. The browser holds only the already-fetched profile and typed form state.

**Notes (closing lines):**
- The typed address is sent to the third-party official GeoSearch service — pre-existing intended design (not introduced here), correctly hardened with `credentials:"omit"` and `referrerPolicy:"no-referrer"`. Not a finding.
- `fetchImpl` (`address-search.ts:48`) is a test seam defaulting to real `fetch`; not user-controllable. Not a finding.
- Web execution is out of scope for this read-only source review; CI evidence (all 20 green at corrected head `8c089343`) is recorded at `project-control/reports/M5-T038-ci-evidence.md`.

**VERDICT: PASS** — no critical, high, medium, or low security findings; packet allowed_paths byte-stable `d94bbcb6..HEAD`.
