# G4 QA Gate Report — M0-T112 (D-024 Amendment 8, unit M: final golden re-certification at the frozen post-addition identity)

**Reviewer:** qa-engineer (independent; read-only)
**Gate:** G4 (QA)
**Frozen review identity:** `14d204363c3ad44dff9a96333a9fe0e5662541ca` — confirmed `git rev-parse HEAD` matches.
**Verdict: PASS** (with two non-blocking-to-QA MINOR control-plane reconciliations for the orchestrator to clear before recording the gate / accepting).

The re-certification evidence in `M0-T112-recertification.md` is independently reproduced: every test count I re-ran matches the report exactly, the certified code identity is pristine (zero code/test drift), the +4 baseline delta is exactly the four accepted L-pack tests, the golden-run pack was re-run-only (not edited), and CI on the pushed tip is 20/20 green. R247's "affected/full golden certification re-run at the final frozen identity before the activation package is presented" is satisfied.

---

## 1. Command / verification table (exact observed results)

| # | Command | Observed result | Verdict |
|---|---|---|---|
| 1 | `git rev-parse HEAD` | `14d204363c3ad44dff9a96333a9fe0e5662541ca` | MATCHES frozen identity |
| 2 | `git status --porcelain` | `?? project-control/reports/M0-T112.json` (one **untracked** control-plane submit-record; **no** tracked-file modifications, nothing staged) | Tree not pristine — see MINOR-1 |
| 3 | `git log --oneline a2aec114..14d20436` | 4 commits: `a4f94b7` (G0 readiness) → `615f661` (re-cert + CI tip) → `c2518d4` (CI-confirm + evidence map + G2 self-check) → `14d2043` (HEAD: G2 PASS + submit awaiting_gate at c2518d4) | Coherent lifecycle chain |
| 4 | `git diff --stat a2aec114..14d20436` | 9 files, **all** under `project-control/**` (gates G0/G2, M0-T096-activation-package.md, M0-T112 readiness/self-check/evidence-map/recertification reports, state.json, task file); 363 ins / 87 del; **zero** code/test/config/dependency files | Zero code drift CONFIRMED |
| 5 | `git rev-parse HEAD:tools/test_agent_supervisor_golden_run.py` | `d2946392f1c14ba086d63c60f2e125db6863bc10` | MATCHES spec + report §2 |
| 6 | `git rev-parse HEAD:tools/agent_supervisor` | `132e698c15a9f9412d53905e45ce0ae0724abe15` | MATCHES report §2 supervisor tree |
| 7 | `git log -1 --oneline -- tools/test_agent_supervisor_golden_run.py` | `635fac5` (M0-T096 unit-I correction round) | Golden pack last moved at unit-I; **NOT** touched by M0-T112 |
| 8 | `git show 8574c58 --stat` | `telegram_sink.py` +45, `telegram_sink_cli.py` +8, `test_agent_supervisor_telegram_sink.py` +129; commit msg names "4 new L-pack tests (authorized-canary-no-env, task_id dedup, queue-growth, identifier redaction+cap)" | Delta source confirmed |
| 9 | `git show 8574c58 -- …telegram_sink.py \| grep '^[+-].*def test_'` | **added=4, removed=0, net=+4** — `test_identifier_fields_are_redacted_and_the_total_is_bounded`, `test_the_same_condition_for_a_different_task_is_not_deduplicated`, `test_a_failing_identical_condition_does_not_grow_the_queue`, `test_cli_canary_with_flag_but_no_env_queues_without_a_send` | +4 delta attribution EXACT |
| 10 | `python -m pytest tools/test_agent_supervisor_golden_run.py -q` | **40 passed** in 19.75s | MATCHES report (40/40) |
| 11 | `python -m pytest tools/test_agent_supervisor_telegram_sink.py tools/test_agent_supervisor_codex_channel.py -q` | **91 passed** in 24.84s (telegram L-pack + codex, both within the affected-493 set) | PASS, no failures |
| 12 | `python -m pytest tools/ -k "agent_supervisor" --collect-only -q \| tail` | **2696 collected** / 3256 (560 deselected) in 20.35s | MATCHES report (2,696 collected) |
| 13 | `python -m pytest <files 46–59: rotation…turnover_live_signal> -q` (whole-suite chunk 4/4) | **610 passed** in 81.25s | MATCHES report chunk 4/4 (610) |
| 14 | `gh api …/commits/615f661…/check-runs` | **total 20, every conclusion=success, all completed** (model-routing, web-e2e, web-dependency-security, code-graph, supervisor-bridge, context-pipeline, control-plane, contracts-schema-bundle, product-map, contracts-typegen, modularity, api-tooling-lock-verify, api-lock-verify, context-index-a1, contracts, api, exact-production-install, context-budget, web, credential-scan) | CI 20/20 CONFIRMED on the pushed tip |
| 15 | `git diff --stat c2518d4..14d20436` | 3 files only: `gates/M0-T112-G2.json` (new), `state.json` (timestamp only), `tasks/M0-T112.json` (submit fields) — **zero** evidence/code drift between the declared reviewed_sha and the frozen HEAD | See MINOR-2 (benign) |
| 16 | `grep -n '8574c58\|132e698c\|d2946392\|a4f94b7' M0-T096-activation-package.md` | Items 10–12 cite the post-addition identity (`8574c58`, tree `132e698c`, golden blob `d2946392` "unchanged — re-run only", run head `a4f94b7`); banner states the package is presentable "ONLY once M0-T112 itself is [accepted]"; REFRESH-ONLY | Activation-package refresh CONFIRMED |
| 17 | `git ls-files project-control/reports/*.json` + `git check-ignore` | `M0-T096.json`, `M0-T110.json`, `M0-T111.json` **tracked**; `M0-T112.json` **untracked**; `reports/*.json` **not** gitignored (319 tracked report JSONs) | Peer submit-records were committed; this one is not — MINOR-1 |

**Whole-suite arithmetic reconciliation:** report chunks 677 + 724 + 683 + 610 = **2,694 passed**; + 2 skipped (chunk 3) = **2,696 collected**. I independently reproduced chunk 4/4 (610) and the collect count (2,696); the arithmetic is internally consistent and my two re-run components (610, 40, 91) match. No test removed, no unexplained drift.

---

## 2. Judgement against the gate criteria

**R247 — affected/full golden certification re-run at the final frozen identity (the QA-central row): SATISFIED.**
- Full golden-run pack independently re-run: **40 passed** (report 40/40).
- Whole 59-file supervisor suite: **2,696 collected**, chunk 4/4 independently **610 passed**; the whole-suite run is a superset of the affected-pack set, so **no affected pack is missing** — coverage is complete by construction.
- Two affected packs independently re-run (telegram L-pack + codex) = **91 passed**, both green.

**Golden pack must NOT have been edited — CONFIRMED.** `tools/test_agent_supervisor_golden_run.py` last moved at `635fac5` (unit-I), blob `d2946392…` at HEAD, and it does not appear in the `a2aec114..14d20436` diff. It sits in `allowed_paths` (kept only in case a re-run exposed an identity-stamp defect); none did, so the "re-run only" claim holds.

**+4 baseline delta attribution — EXACT.** `8574c58` added precisely 4 net test functions (added=4/removed=0), and they are exactly the four named L-pack tests. Prior recorded full-suite figure 2,690 + 2 skipped → this run 2,694 + 2 skipped; the +4 is fully accounted for.

**Zero code drift / identity integrity — CONFIRMED.** `a2aec114..14d20436` touches only `project-control/**`; supervisor tree `132e698c` and golden blob `d2946392` match the report; CI 20/20 on `615f661`.

**Activation-package ordering (R247/R232/R246) — PRESERVED.** Items 10–12 refreshed to the certified identity, REFRESH-ONLY, package "activates nothing" and is presentable only after this unit's own acceptance. Prohibitions (R248) hold: diff shows no `.claude/hooks`, no settings, no PR-#241 touch, no source changes.

---

## 3. Findings

**BLOCKER:** none.
**MAJOR:** none.

**MINOR-1 (control-plane hygiene; blocking-for-acceptance, not for QA correctness).** `project-control/reports/M0-T112.json` (the submit-record) is **untracked** at the frozen HEAD, so `git status --porcelain` is not clean. This is the only non-clean item — there are no tracked-file modifications and nothing staged. Peer submit-records (`M0-T096/T110/T111.json`) were committed in their control commits, and `reports/*.json` is not gitignored, so this one should likewise be committed. The certified **code/test identity is fully intact** (supervisor tree and golden blob match every declared digest), and the committed evidence (recertification report, evidence map, gate records) is present and unchanged — so this does not affect QA correctness. **Recommendation:** orchestrator stages/commits `reports/M0-T112.json` before recording the gate / accepting, matching the established pattern.

**MINOR-2 / INFO (reviewed_sha lag; benign).** The submit-record names `reviewed_sha = c2518d4` (HEAD~1) while the frozen review identity is `14d20436` (HEAD). The intervening `c2518d4..14d20436` diff is control-plane-only (G2 gate record + a state.json timestamp + the task's submit fields) with **zero code/test/certification-evidence drift**, so my review at `14d20436` is evidence-equivalent to the declared reviewed_sha — this is the normal "submit commit records the submit-at-prev-HEAD" pattern. **Recommendation:** per the repo's `reviewed_sha==HEAD` discipline, record the G4 gate at the live HEAD `14d20436`.

**INFO.** §5 known non-blocking residuals (`_already_queued` digest normalization; unit-I `live_observation.py:296` one-liner; unit-K boundary notes) are correctly **carried not fixed** — supervisor source is outside this packet's `allowed_paths` and, by R247, any such change would re-invalidate the certification. Disposition is sound; a single bounded follow-up after acceptance is the right lever.

---

## 4. Conclusion

The re-certification evidence is independently verified and reproduces exactly at the frozen identity. All required QA substance passes: full golden-run pack 40/40, whole-suite collection 2,696 with chunk 4/4 610, two affected packs 91, +4 delta exactly the four accepted L-pack tests, golden pack re-run-only (unedited), zero code drift, activation-package refresh-only with ordering preserved, and CI 20/20 on the pushed tip. `test_agent_supervisor_golden_run.py` did not need and did not receive changes.

**G4 QA verdict: PASS.** The two MINOR items are control-plane reconciliations for the orchestrator (commit the untracked submit-record; record the gate at live HEAD `14d20436`); neither affects the certified evidence or the QA correctness of this unit.

Frozen review identity: `14d204363c3ad44dff9a96333a9fe0e5662541ca`.
