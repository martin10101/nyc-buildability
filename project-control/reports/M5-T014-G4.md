<!-- Reviewer return preserved VERBATIM by the orchestrator (report-preservation rule,
.claude/rules/project-control.md; transport entity-decoding applied: &amp; -> &, &lt; -> <, &gt; -> >).
Reviewer: independent qa-engineer subagent (G4), returned 2026-09-12, reviewed HEAD f5a30e51. -->

I have everything I need. Let me record the key verification points to my agent memory, then return the gate report.

All verification is complete. Let me consolidate the findings.

Verification summary of what I confirmed independently at the reviewed content (candidate branch tip f5a30e51; producer content commit 31d3683e):

1. **Central question — weakening vs repair.** Traced the component logic in `NoScenarioBlock.tsx` (lines 127-132) and the field-extraction path in `scenario-display.ts` (`asStringOrNull` → `boundedText`) plus `bounded.ts` (empty/blank → fallback `""`).

2. **Derived date + `as string` cast** — confirmed against the committed fixture.

3. **Cross-test isolation** — `conflictScenarioBody()` = `structuredClone(...)`.

4. **Scope + red/green** — confirmed via read-only git on the producer content commit.

Here is my gate report.

---

VERDICT: PASS

# G4 QA Gate Report — M5-T014 (compare-screen empty-from assertion repair)

**Dimension:** test integrity (independent G4). **Reviewer:** qa-engineer (read-only, ADR-005).
**Reviewed content identity:** candidate/D-024-mrl-option-b tip `f5a30e51` (submit-time control-plane commit); producer *code* material at `31d3683e` ("M5-T014 producer material…"). I did not write this change.
**Verification model:** CI-only per thin-client policy (no node_modules; vitest not executed locally — correct and expected). RED externally recorded; GREEN authority is the web-e2e job on the pushed head.

## Central adjudication — TEST-REPAIR, not test-weakening

The producer chose disposition (b): construct the mixed date state so the per-slot label renders and the original assertion stands. This is the correct choice; disposition (a) (relaxing the expectation to the collapsed phrase) would have abandoned the docstring intent and asserted a state the test does not exist to check.

Evidence the chosen form is a genuine repair:

- **All three original assertions are preserved verbatim** — `"rule id not stated"`, `"version not stated"`, `"from not stated"` are all outside the diff hunk and unchanged. The commit is purely additive: 8 insertions, **0 deletions** in the test file. Nothing relaxed or deleted (S3 satisfied).
- **Docstring intent preserved and now actually reached.** The test's purpose is that an *empty STRING* (not just null) labels as absent. The empty-string inputs are retained (`rule_id="   "`, `rule_version=""`, `effective_from=""`). Before the repair, `effective_from=""` + `effective_to=null` both landed in the absent class, so the component's deliberate both-absent collapse (`"no effective dates are recorded for this rule"`, line 127-128 of `NoScenarioBlock.tsx`, endorsed by the adjacent both-null test at line 437) rendered — the assertion could never pass against correct code. The added real end date routes the rule through the per-slot branch (`isAbsent("") && isAbsent("2024-12-05")` = `true && false` = `false` → else branch → `"recorded effective dates: from not stated, to 2024-12-05"`), so `"from not stated"` genuinely renders.
- **Mutant-killing power added (verified trace).** `from=""` → `stated("", "not stated")`. If a mutant makes `isAbsent` stop treating `""` as absent (`value === null` only): `isAbsent("")` → `false`; the per-slot branch is still reached (because `to` is present), and `stated("","not stated")` returns `""`, so the rendered string becomes `"recorded effective dates: from , to 2024-12-05"`. `"from not stated"` is then absent from the DOM → `toHaveTextContent("from not stated")` FAILS → mutant killed. The test now has a valid green (correct code) and a red (mutant); pre-repair it had neither.
- **Component untouched** — `NoScenarioBlock.tsx` (accepted M5-T004 design) is not in the diff. No accepted work reopened.

## Point-by-point

- **S2 / derived date.** `rules[1].effective_from` in `packages/contracts/fixtures/valid/scenario/no_scenario_conflict.json` is the literal string `"2024-12-05"` (line 41) — a real fixture date, not an invented literal. Both competing rules carry `effective_from:"2024-12-05"`, `effective_to:null`, so blanking `effective_from` on `rules[0]` did put both dates in the absent class pre-repair (mechanism confirmed). The `as string` cast is sound: the record is typed `Record<string, unknown>`, the runtime value is a string, and the assignment target (`effective_to`) is `unknown` — type-valid and semantically correct.
- **S3 / cross-test isolation.** `conflictScenarioBody()` returns `structuredClone(noScenarioConflict)` (`scenario-fixtures.ts` line 86-88); `afterEach` runs `cleanup()` + `vi.unstubAllGlobals()`. The `rules[0]` mutation operates on a deep clone and cannot leak into the adjacent both-null test (line 437, which builds its own fresh clone) or the recorded-dates test (line 425). Confirmed.
- **S1 / RED + arithmetic.** RED externally recorded on every web-e2e run since M5-T004 acceptance (run 34669078475, full DOM diff: expected `"from not stated"`, received the both-absent collapse). Arithmetic checks: 389 passed + 1 failed = 390 collected; GREEN target = 390/390 (the one failing becomes the 390th passing). Consistent.
- **S4 / GREEN.** Acceptable under the thin-client policy (`docs/LOW_STORAGE_CLOUD_DEVELOPMENT_POLICY.md`; CLAUDE.md principle 14): local vitest cannot run, so the web-e2e job on the pushed head is the sole executable authority. The green outcome is deterministically implied by the traced render mechanism above, and the RED is reproducible from gh run logs. **One record-time condition for the orchestrator:** S4's executable proof is the *actual* web-e2e job turning green (vitest 390/390) on the pushed head — confirm that CI result before acceptance. This is a normal thin-client evidence step, not a defect in the change.
- **Statics.** The test file parses as valid TSX by inspection: the hunk inserts a 7-line comment plus one statement between two existing statements inside one function body; braces/JSX untouched; line lengths consistent with the file's ~78-char convention. No other hunk in the commit beyond the one test function + the report.
- **Scope.** Producer content commit `31d3683e` changes exactly two files — `apps/web/src/components/compare/__tests__/compare-screen.test.tsx` (+8) and `project-control/reports/M5-T014-producer-report.md` (+50) — both in `allowed_paths`. The test path matches both the allowed exact path and the forbidden `apps/web/src/components/**/*.tsx` glob; allowed-exact wins (packet path_notes confirm). All forbidden paths untouched by the producer: component, lib, e2e, packages, services, supabase, .claude, tools, docs, tasks, gates. The later commits `68b7d10f` (evidence-map) and `f5a30e51` (report json / state.json / tasks json) are orchestrator submit-time control-plane transitions, not producer material.

## Commands run (read-only)
- `git cat-file -t f5a30e51`; `git show --stat f5a30e51` (submit commit: 3 control-plane files)
- `git log --oneline -6 f5a30e51 -- <test file>` and `-- <producer report>` (producer material = `31d3683e`)
- `git show 31d3683e --stat` and `git show 31d3683e -- <test file>` (2 files, +58/-0; 8-line additive test hunk)
- Read: task packet, test file, `NoScenarioBlock.tsx`, `scenario-display.ts`, `bounded.ts`, `scenario-fixtures.ts`, fixture `no_scenario_conflict.json`, producer report.

## Findings
None blocking. This is a clean, intent-preserving, mutant-strengthening test repair with no component change and no assertion weakening. Recommend PASS. Orchestrator to confirm the web-e2e GREEN (vitest 390/390) on the pushed head at record/accept time as S4's executable proof.
