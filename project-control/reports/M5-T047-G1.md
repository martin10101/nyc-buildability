<!-- Orchestrator capture: data-contract-verifier G1 return for M5-T047 saved VERBATIM
     (transport entity-decoding only). Review pinned at frozen head ca94b2c3; reviewer
     recomputed all nine digests and verified the change-set + official source. -->

Official source sufficiently confirmed: dataset `64uk-42ks` is the official PLUTO dataset, and the `address` field is the tax-lot address-of-record (corpus §6 directly evidences this — same bbl 3052960043 carries PLUTO address "3622 13 AVENUE" vs. matched frontage "1279 37 STREET"). The PDF data dictionary didn't parse in the fetcher, but the captured corpus (dated today) plus the web-confirmed dataset identity and field roster establish the field meaning. I have everything needed for the verdict.

---

# G1 Data-Contract Gate Report — M5-T047

**Task:** M5-T047 — Record-address display channel (DB-032) + T046 in-scope riders
**Gate:** G1 (data contracts) — independent, read-only
**Reviewed identity:** frozen submission `ca94b2c3`; live HEAD `21ee7379` (one control-plane commit later). Verified via git plumbing: `21ee7379` is the child of `ca94b2c3` and touches only `project-control/{reports/M5-T047.json,state.json,tasks/M5-T047.json}` — no T047 surface bytes. The material T047 surface is byte-identical from `d27a6a83` (corrected head) through HEAD.
**Directive refs:** D-066-R001, D-073-R006
**Verdict: PASS** (2 non-blocking advisories)

## Identity / provenance verification

- **All 9 LF-normalized sha256 digests recomputed at HEAD match `M5-T047-source-sections.md` exactly** (`git show HEAD:<path> | tr -d '\r' | sha256sum`). Notably `lot_geometry.py` = `10046e29…`, `record-address.ts` = `c9a15f82…`, `address-search.ts` = `2d3b6ecf…`. The digest-bound surface is authentic.
- **Change-set verified against forbidden paths.** Material commit `7ecdcbf4` touches exactly the 9 `allowed_paths` (8 impl/test + producer report) — no `packages/contracts/**`, no `services/api/app/_contract_schemas/**`, no `connectors/`, no `profile/`, no `spatial/`, no `LotOutlineMap.tsx`/`architect/`. **"No contract-schema file change / no builder/profile change" is CONFIRMED** by the diff, not just claimed.
- **Official-source cross-check:** dataset `64uk-42ks` = NYC Open Data "Primary Land Use Tax Lot Output (PLUTO)" (confirmed on data.cityofnewyork.us); `address` is a documented tax-lot location field. Corpus §6 (captured 2026-09-19) directly evidences the address-of-record semantics: bbl 3052960043 carries PLUTO `address:"3622 13 AVENUE"` while the GeoSearch-matched frontage is `1279 37 STREET` — same lot, two address strings.

## Surface 1 — Backend record-address channel (`services/api/app/api/v1/lot_geometry.py`)

Verified in source (additive sibling endpoint `@router.get(".../record-address", include_in_schema=False)` at :363; `get_lot_geometry` untouched):

- **State matrix honesty.** `RECORD_ADDRESS_STATUS_STATE_MATRIX` (:294–305) frozen set: `(200,None),(404,None),(422,validation_error),(429,rate_limited),(502,source_unavailable),(502,schema_drift),(504,timeout),(500,internal_error)`. RA-6 test (:652–678) asserts every emitted pair ⊆ matrix. PASS.
- **Three honest 200 outcomes, absence = typed null.** `address_of_record` (:456), `no_address_of_record` (:444), `no_record` (:432) — all 200 with `address` verbatim or `None`, never an empty-string line. `_record_address_from_result` (:338–348) returns the value only when `isinstance(str) and value.strip()!=""`, else `None`.
- **Key-absence = null (corpus assertion 7) correctly implemented.** The connector (`pluto_soda.py:803`) emits a fact only for columns present in the SODA record (`record_keys & PLUTO_COLUMNS`); a null-omitted `address` column produces no address fact → route returns `no_address_of_record`. This is the true SODA wire behavior, not a synthetic explicit-null. Verified `address` IS a real PLUTO column (`pluto_soda.py:161 "address":"text"`), so a populated record yields `address_of_record` — no phantom-field defect.
- **Provenance.** `_record_source` (:351–360) → `source_id` (PLUTO_SOURCE_ID="nyc-dcp-pluto-soda"), `dataset_id` ("64uk-42ks"), `dataset_version` (PLUTO release, e.g. "26v2" — distinct from the GeoSearch PAD "26c"), `retrieved_at`, `request_url`. All four packet-named provenance fields present (plus request_url). `dataset_version` maps to the connector's `version_raw` = record `version` (`pluto_soda.py:870`), corpus-faithful.
- **Typed connector-fault taxonomy, never a fake absence.** `_PLUTO_ERROR_STATUS` (:309) maps rate_limited→429, timeout→504, source_unavailable/schema_drift→502, default→502; `normalize_bbl` first → 422 with zero network (:378–396); unexpected → typed 500 (:422). RA-4 asserts faults carry `state` and NO `outcome` key (`test:579`). PASS.
- **Backward-compatible/additive:** flag-gated OFF by default (reuses `INTERNAL_RULE_EVAL_ENABLED` → generic 404, RA-5 :616), `include_in_schema=False` (not in OpenAPI, RA-5 :642). No new dependency.

## Surface 2 — Client mirror (`apps/web/src/lib/record-address.ts`)

- **DOCUMENTED_PAIRS (:50–59) matches the route matrix pair-for-pair** — all 8 pairs, verified line-by-line against `RECORD_ADDRESS_STATUS_STATE_MATRIX`. Any pair outside → `unexpected_response` (:280). PASS.
- `RECORD_ADDRESS_ERROR_STATES` (:36) and `RECORD_ADDRESS_OUTCOMES` (:61) mirror the six server error states and three honest outcomes.
- **`route_absent` is first-class** (`RecordAddressRouteAbsentOutcome` :95; the generic `404 & state===null` → `{kind:"route_absent"}` :276), never an error. Client test :94 asserts it.
- **Bounded display view.** `documentView` (:157) bounds address via `boundedText`, and provenance tokens via `boundedToken` (sourceId/datasetId ≤64, datasetVersion ≤32, retrievedAt ≤40). Branching stays on the RAW `outcome` (:256–272).
- **Blank address = absence.** `documentView` (:161–162) maps `""`→`null`; `recordAddressDiffersFromMatched` (:325–327) returns `false` for null/blank. Mirrors server `_record_address_from_result`. Client tests :87, :184 confirm.
- **`normalizeAddressForCompare` (:309)** deliberately narrow (collapse/trim/upper, no abbreviation/ordinal folding) — correct, since both operands are already city-canonical (the ordinal/abbreviation logic belongs to the equality gate, not the record display).

## Surface 3 — Fixture fidelity (corpus §6)

Backend fixtures (`test_lot_geometry_api.py:412–438`) and client fixtures (`record-address.test.ts:25–42`) both use PLUTO `address="3622 13 AVENUE"`, `version="26v2"`, `bbl="3052960043"`, `source_id="nyc-dcp-pluto-soda"`, `dataset_id="64uk-42ks"`, `retrieved_at="2026-09-19T04:00:03Z"`, `request_url=".../64uk-42ks.json?bbl=3052960043"`. Every value is faithful to corpus §6 and to the connector's actual behavior (`fetch_by_bbl` builds `?bbl={canonical}` at `pluto_soda.py:638`; SOURCE_ID/DATASET_ID at :105–106). **No invented fields.** The `PlutoFetchResult` fixtures use the real dataclass fields (status/facts/dataset_version/…, `pluto_soda.py:362–395`) — the `_address_fact` shape (`original_field_name/original_value`) matches what the connector emits (:816–823). RA-2 (null column) and RA-2b (blank string) exercise key-absence honestly.

## Surface 4 — T046 riders (`address-search.ts` + tests)

- **G5-A1 length bound is a typed refusal.** `GEOSEARCH_RESOLVE_INPUT_MAX_LEN=512` (:220) enforced at gate entry (:316–320) BEFORE any normalization/compare, returning the existing typed `{kind:"no_match"}`. Tests refuse over-length street (:366) and housenumber (:378); a normal input still resolves (:388). Rider is +16/−0, match semantics unchanged.
- **G4-A1 /search abbreviation fails closed.** `normalizeStreetForMatch` (:264) folds only digit-suffixed ordinals (`\b(\d+)(?:ST|ND|RD|TH)\b`), so spaced `"37 st"`→`"37 ST"` ≠ feature `"37 STREET"` → the equality gate refuses to `no_match` (test :340–363). This is corpus-anchored (assertions 1–3: GeoSearch never empties `features`; success is field EQUALITY, not confidence/match_type; identity binds to `addendum.pad.bbl`). Abbreviation expansion was never contracted; the fail-closed posture (D-051 fail-closed-with-care) is honest.

## Findings

- **F — none (no blocking data-contract defects).**
- **A1 (advisory, process/context):** the tagged `[ORCH-CORRECTED]` commit `d27a6a83` modifies `apps/web/src/components/address/__tests__/address-resolution.test.tsx`, which is OUTSIDE the packet `allowed_paths`. This is a documented orchestrator action (CI-evidence.md; consumer-count skew from the new sibling fetch, corrected per the file's own M5-T023 precedent — the record-address side stub joins the lot-geometry stub so the resolution spy's order/counts/queued responses are preserved) and is test-only (no production/contract bytes). Process-correct per CODING_RULES "route out-of-scope test updates to the orchestrator." Not a G1 concern, but the G4/regression reviewer should confirm the correction preserved the consumer test's original intent rather than masking a real regression. (Note: web CI on the corrected head `d27a6a83` is recorded green — run 35431045720 — in CI-evidence.md; I verified the record, not the run.)
- **A2 (advisory, test cosmetics):** `address-search.test.ts:388` ("a normal-length input **at the bound**") uses a 9-char street, not a 512-char input, so it does not actually exercise `length == GEOSEARCH_RESOLVE_INPUT_MAX_LEN`. The gate uses strict `>`, and the +1 refusal cases cover the boundary correctly, so semantics are sound; consider adding an exactly-512 accept case for completeness. Non-blocking.
- **A3 (advisory, cross-gate):** in `AddressConfirmCard.tsx:131` `matchedForCompare` is built from `view.inputEcho.houseNumber` + `view.canonical.streetNameNormalized`. For the corpus corner-lot case entered==matched so AS-2 holds, but the HJ/code reviewer should confirm `inputEcho.houseNumber` is the *matched* (not merely *entered*) house number so the differ compares record-vs-matched per AS-2 in the general case. Not a data-contract (G1) issue.

## Digests I verified
All 9 LF-normalized sha256 in `M5-T047-source-sections.md` recomputed at HEAD and matched (lot_geometry.py, test_lot_geometry_api.py, record-address.ts, record-address.test.ts, AddressConfirmCard.tsx, address-confirm.test.tsx, address-search.ts, address-search.test.ts, producer report). Git parent/child identity `ca94b2c3`→`21ee7379` verified; T047 surface byte-stability across intervening M5-T045/T048 commits verified via change-set (no allowed_path appears in those commits).

**G1 verdict: PASS.** The additive record-address channel and its client mirror are honest, provenance-complete, corpus-faithful (address-of-record ≠ frontage, key-absence = null, typed fault taxonomy never a fake absence), pair-for-pair consistent with the route matrix, additive-only with no contract-schema change, and the two T046 riders are typed/fail-closed. Advisories A1–A3 are non-blocking and A1/A3 route to the G4/HJ reviewers.
