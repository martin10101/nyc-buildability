# G4 QA Gate Report — M0-T114 (D-024 residual fixes; rows R258/R272/R273)

**Verdict: PASS**
**Frozen review identity:** `5e2c8c3497bf1af1dff333a7f4c9290c9aa86eb7` — verified as HEAD with a clean working tree (`git status --porcelain` empty) before any command was run. Reviewer was READ-ONLY throughout; no writes, no git mutations, no stash.

**Code deliverable under review:** commit `f89aa29` (parent/pre-fix `420dd5c`). HEAD (`5e2c8c3`) and its two predecessors are control-plane commits only (gate/report/state/task JSON). The `f89aa29` diff touches exactly the five M0-T114 allowed_paths and nothing else.

## Command table (exact observed results)

| # | Command | Expected | Observed | Result |
|---|---|---|---|---|
| 1 | `git rev-parse HEAD` + `git status --porcelain` | frozen SHA, clean | `5e2c8c3497bf1af1dff333a7f4c9290c9aa86eb7`, empty | PASS |
| 2 | `pytest test_agent_supervisor_telegram_sink.py test_agent_supervisor_golden_run.py -q` | 77 passed (36+41) | `77 passed in 16.97s` | PASS |
| 3 | 8-pack sweep (telegram, golden, adversarial, endurance, phase1, operator_channel, codex_channel, reviewer) | 535 passed | `535 passed in 58.16s` | PASS |
| 4 | `python tools/modularity_check.py --check` | EXIT=0 | `selected 327 files; failures 0; warnings 8`; EXIT=0 | PASS |
| 5 | `ruff check` on the four touched files | clean | `All checks passed!`; EXIT=0 | PASS |

Modularity's 8 warnings are all pre-existing (cli.py, durable_state.py, policy.py, refusal_bridge.py, repair_gate.py, context_benchmark.py, types.ts, mappluto_geometry_arcgis.py); **none** falls on `telegram_sink.py` or `live_observation.py`. The two touched source files introduce zero new lint/modularity debt.

## Findings

**1. Removal-sensitivity — CONFIRMED by pre-fix source inspection.**
- *Residual 1 (telegram_sink):* pre-fix (`f89aa29~1`) `_already_queued(journal, digest)` ran BEFORE `build_notification` using `digest = _dedup_digest(condition, task_id, summary)` computed from the RAW summary (line 313). Stored queue items carry the POST-BUILDER (truncated/redacted) summary, and `_already_queued` re-digests each stored item's own summary. Raw-digest vs built-digest therefore never match for any builder-altered summary, so the new test's 4 repeats would each re-enqueue (queue depth → 5, `repeat.already_queued` False) — the `assertEqual(depth, 1)` and `assertTrue(repeat.already_queued)` deterministically fail pre-fix.
- *Residual 2 (live_observation):* pre-fix line wrote `"source_record_key": str(source.get("source_record_key", ""))` — the RAW value, while `sanitized` (built at line 271-278) already included `source_record_key` in its input set. The new register test embeds `sk-ant-api03-…` in the source key; pre-fix the raw token reaches `rows[0]["source_record_key"]`, so both `assertNotIn(secret, …)` and `assertIn("[REDACTED", …)` fail pre-fix.

**2. Test quality — GENUINE, both name the defect.**
- The builder-altered test uses `"the golden run completed cleanly " * 30` = 990 chars > `MAX_SUMMARY_CHARS=400`, forcing truncation. Its `assertNotEqual(queued[0]["summary"], long_summary)` precondition explicitly proves the stored summary was ALTERED (raw ≠ stored) — the defect only manifests for altered summaries, so this precondition is load-bearing. (Confirmed the builder truncates rather than rejects over-length summaries: the queued-depth-1 assertion would fail otherwise, and the L-pack passes.)
- The register test embeds a real anthropic-key-shaped token (`sk-ant-api03-…`, flagged `gitleaks:allow`/`secretscan:allow` fake sentinel) that the boundary sanitizer genuinely redacts (proven by the green `[REDACTED` assertion). `sanitized.value["source_record_key"]` is the same sanitizer output every other copied scalar uses.
- Both docstrings name the defect ("M0-T114 residual 1", "residual 2").

**3. Additions-only + no cross-unit bleed — CONFIRMED.** The `f89aa29` diff of both test files contains only `+` insertions (new methods `test_a_builder_altered_summary_still_bounds_queue_growth`, `test_the_source_record_key_is_sanitized_not_raw`); zero deletion lines, zero edits to existing scenarios. This unit's diff touches only `live_observation.py`, `telegram_sink.py`, the two test packs, and the report md. None overlaps M0-T115's files (`broker.py`, `recovery_probes.py`, `loop_turnover.py`, `test_command_authority.py`, `test_recovery_probes.py`, `test_turnover_live_seam.py`, `M0-T115-repair-record.md`).

**4. At-least-once / leak-refusal invariants — HOLD.** Traced `notify_condition` new order: (a) closed-vocabulary check → raise, no state; (b) `_dedup_seen(raw digest)` → early return, audit-only, no queue/dedup mutation; (c) `build_notification` (no journal parameter — pure builder; refuses leak shapes by raising `NotificationError` → early return with nothing enqueued); (d) `_already_queued(post-builder digest)` → early return, audit-only, no mutation; (e) `queue.deliver(...)`; `_dedup_record(raw digest)` only on delivered. Every early return is side-effect-free w.r.t. the durable queue/dedup state that governs correctness (audit appends are observability, not dedup state). Because the leak-refusing builder runs BEFORE both the queue check and delivery, no leak shape is ever enqueued. Nothing is dropped: when `already_queued` is true the pre-existing queued item still delivers (at-least-once preserved); for unaltered summaries raw == built so behavior is identical to pre-fix. The delivered-dedup path stays raw-vs-raw (line 313/349) and is internally consistent; the queue-growth path is now post-builder vs post-builder.

## Non-blocking observations (no action required for this gate)
- The dual-digest design (delivered-dedup keyed on the raw summary; queue-growth keyed on the post-builder summary) is internally consistent per path. One theoretical edge: two distinct raw summaries that truncate to an identical post-builder summary would share a `queued_digest` and the second would be suppressed as already-queued. This is benign and in fact the *correct* bounding behavior — the stored items are byte-identical post-builder, the operator sees the same view-only content, and at-least-once is preserved. Not a defect.
- Residual 3 correctly dispositioned no-code: `codex_channel.py` is outside M0-T114's allowed_paths and the write-only/inert boundary queue is by-design for the accepted unit-K contract (drain is owner-gated promotion work). Confirmed no code was touched there in `f89aa29`.

All producer claims verified: L-pack 36 + golden 41 = 77; 8-pack 535/535; ruff clean on the four files; modularity EXIT=0; both fixes red→green with pre-fix-source-confirmed removal sensitivity; additions-only tests; scope-clean; R273 (no runtime-journal writes — both fixes execute only inside future notify/observe calls, tests use temp journals).

**FINAL VERDICT: PASS at frozen identity `5e2c8c3497bf1af1dff333a7f4c9290c9aa86eb7`.**
