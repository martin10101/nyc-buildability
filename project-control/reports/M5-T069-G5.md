# M5-T069 — G5 security review record (security-reviewer return, VERBATIM)

[Orchestrator record header — not part of the reviewer return: dispatched 2026-09-22 ~20:52 UTC
at frozen 141007c37c58c1a0e3d1297c582b015ca0f20bb1, to the SAME reviewer identity that
originally surfaced T067 G5-LOW-1. The agent's stream stalled mid-review in the ~21:0x-21:29Z
provider stream incident; it was RESUMED with context intact and finished cleanly. Return
received COMPLETE in one part with the required END-OF-REPORT marker; transport
entity-decoding only. Verdict PASS; T067 G5-LOW-1 declared CLOSED; zero blocking; advisories
A-1..A-3 all pre-existing/carried.]

---

I have completed all checks. Verified: identity at frozen content, working tree matches the material commit, ruff clean, 77 passed, all line anchors, the enumerate-guess walk, scope-before-status order, the route-unreachability of the mismatch and reason-None branches, and the consumer sweep. Here is the complete report.

---

# G5 SECURITY/PRIVACY GATE REPORT — M5-T069

**Verdict: PASS**

**Task:** M5-T069 — DB-040(s) supersede not-found message unification in the site-definition store (closure of the T067 G5-LOW-1 existence oracle I surfaced).
**Reviewer:** security-reviewer (original finder of T067 G5-LOW-1). Read-only; no writes outside my agent-memory dir.
**Reviewed identity:** material commit `463f98f98c74ef213a5c3c03e3cd844291c01740` (the only material commit); frozen submit `141007c37c58c1a0e3d1297c582b015ca0f20bb1`; CI SUCCESS `a66cf818b5691c2bb9c17f763a9ffcf2ecd6caa3`.

**T067 G5-LOW-1: CLOSED.** The supersede existence oracle I reported in `project-control/reports/M5-T067-G5.md:44-49` is genuinely and structurally closed, mutation-tested both ways, with the frozen scope-before-status order and insert guards untouched.

## Reproduction / verification performed
- `git cat-file -t` on all three cited SHAs → all commits present; branch `candidate/D-024-mrl-option-b`.
- `git diff --stat 463f98f9 -- <3 reviewed source files>` → **empty** (working tree == material commit; no disjoint peer touched the reviewed files).
- `cd services/api && python -m ruff check .` → **All checks passed!**
- `cd services/api && python -m pytest tests/site_definition tests/api/test_site_definition_api.py -q` → **77 passed** (74 pre-existing + 3 new), matching the harvest claim.
- Read frozen `store.py`, both test suites, the route (`app/api/v1/site_definition.py`), and the factory (`app/site_definition/records.py`) via `git show <sha>:<path>`.

## Blocking findings
**None.** No critical/high/medium/low blocking defect in scope.

## Review-surface adjudication (each item from the packet)

1. **Existence-oracle closure (AS-1) — CONFIRMED.** All three supersede not-found-class branches raise the single shared constant `_NOT_FOUND_FOR_ADDRESSED_PROPERTY` (`store.py:72`) with no id echo: missing `store.py:328`, supersedes_id-mismatch `store.py:334`, foreign-scope `store.py:350`. Byte-identical to revoke's two branches (`store.py:428`, `:441`), all referencing the same constant, so the branches structurally cannot drift. A wrong guess (missing id) is indistinguishable from a real record probed from a foreign property.

2. **Residual side channels on the supersede path — NONE reachable.**
   - *Exception→status divergence:* Via the route, the ONLY reachable not-found-class branches are missing (`:328`) and foreign (`:350`), both `ConfirmationNotFoundError` → `(404, not_found, confirmation_not_found)` (`site_definition.py:167-168`, serialized uniformly by `_refuse`, `site_definition.py:302-318`, which emits `str(exc)` + `reject_code`). Same class → identical status, reject_code, and message body.
   - *Mismatch branch (`:334`) is route-unreachable:* the handler always sets `supersedes_id=record_id` (`site_definition.py:621-622`), so `supersedes_id != old_id` can never fire via the route. At store level it is unified anyway (AS-2, see #4).
   - *reason-None branch (`store.py:335-340`, `TransitionReasonRequiredError`→422) is route-unreachable:* the handler passes `reason=block.get("reason")` into `create_confirmation`, which calls `_require_reason(reason)` whenever `supersedes_id is not None` (`records.py:603-604`, `_require_reason` at `records.py:523-529`). That raise happens INSIDE the factory, BEFORE `store.supersede` and without touching the store — so it is existence-independent (same 422 for missing or existing old_id). No oracle. (Carried advisory A-1 notes the store-level residual.)
   - *409 not-active branch (`store.py:353`)* is reachable ONLY on the caller's OWN binding (condo_key matches at `:345` first), echoes only the caller's own `old_id`/status — not a cross-tenant leak. Scope-before-status order preserved.
   - *Headers/body:* every refusal flows the same `_refuse`→`_json` path; the route test asserts full-body equality minus the per-request `correlation_id`. No header/detail-key/body-shape oracle.

3. **Enumerate-guess walk — SAFE.** Authenticated caller probing supersede with guessed ids, addressing their own property: guessed missing id → `(404, not_found, confirmation_not_found, unified message)` (`:328`); existing-but-foreign id → identical `(404, …, unified message)` (`:350`); only an own non-active record yields the 409 (own property). Missing vs foreign are byte-identical (reproduced by `test_supersede_not_found_response_is_identical_for_missing_and_foreign_ids`, `test_site_definition_api.py`, which asserts equal status/state/reject_code/message and full-body equality minus correlation_id, plus no id echo, plus no mutation).

4. **Test lock-in — GENUINE.** `test_supersede_not_found_message_is_identical_for_missing_and_foreign_ids` (records test, equality assert `:878`) and `test_supersede_supersedes_id_mismatch_shares_the_unified_not_found_text` (AS-2, assert `:907`) pin pairwise equality + reject_code equality + no-echo + `== _UNIFIED_NOT_FOUND_TEXT`. The literal is a **hardcoded copy** in the test file (not imported from store.py), so a mutation of the store constant also reddens — real mutation coverage. Spot-checked the six-row matrix (producer report lines 106-111) against frozen sources: each single-branch mutation (divergent literal or re-echo) diverges exactly one of the two compared branches, so the pairwise equality assert reddens first (A1/A2/C1/C2 → `:878`; B1/B2 → `:907`; route → message-equality assert). Claims verified sound; I reproduced 77 passed on the restored candidate.

5. **Standard G5 sweep — CLEAN.** No id logged into any new message (the only logging, `_refuse` `site_definition.py:303-307`, logs reject_code/status/correlation_id, no record id; store adds no logging). No new data exposure — the change strictly REMOVES id echoes. No dependency/import change (diff adds one module-level string constant + comments; no requirements/lockfile touched). Scope-before-status order and `_record_insert` guards byte-untouched (AS-4): the diff changed only `raise` message text inside existing branches; no `if`-condition reordered.

## Advisory (non-blocking, carried-forward — not introduced by T069)
- **A-1 (INFO, pre-existing, route-unreachable):** At the STORE boundary only, the reason-None guard (`store.py:335-340`) fires only after `old` is confirmed to exist, so a direct store caller passing `reason=None, supersedes_id==old_id` would get 422 for an existing id vs 404 for a missing id. Not reachable via the route (factory validates reason first, see #2), unchanged by T069, and consistent with the structure T067's PASS accepted. No action for this task; route with the deferred B-001 auth/hardening packet if the store is ever exposed to a non-factory caller.
- **A-2 (INFO, pre-existing = T067 G5-INFO-1):** `get()` still uses `f"no site-definition confirmation exists for record id {record_id!r}"` with an id echo (`store.py:282`), and get/list lack a store-boundary property-scope check. Out of scope (not supersede), already on record, route-scoped, route unmounted. No T069 regression.
- **A-3 (nil severity):** micro-timing difference between the missing branch (1 dict.get) and foreign branch (dict.get + two attribute compares) on an in-memory dict is not a realistic remote timing oracle. Note only.

## Consumer sweep (packet duty)
Repo-wide grep at frozen content for the three OLD supersede literals: the old missing-text survives only in `get()` (`store.py:282`, out of scope, A-2) and in historical `project-control/reports/` prose; the old mismatch and foreign texts survive only in report prose. **No out-of-scope test suite pins any old supersede message text** — producer's sweep claim corroborated; nothing to route to the orchestrator.

## Notes
- Directive-completeness (D-078/D-066/D-077 requirement-by-requirement) is the `directive-compliance-verifier`'s pass (producer≠verifier); this G5 verdict covers the security/privacy surface only. Security-relevant intent (unify not-found text, no id echo, read-only) is satisfied.
- Frozen submit `141007c…` is the roster-amendment seam commit; material content reviewed at `463f98f…` per the packet's frozen-head instruction. Working tree identical to material for all three reviewed files.

**Verdict: PASS.** T067 G5-LOW-1 is **CLOSED**. The last known store existence oracle is eliminated structurally (shared constant), mutation-locked, route-verified byte-identical, with scope-before-status order and insert guards preserved.

END-OF-REPORT M5-T069-G5
