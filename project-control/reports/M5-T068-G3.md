# M5-T068 — G3 code review record (code-reviewer return, VERBATIM)

[Orchestrator record header — not part of the reviewer return: dispatched 2026-09-22 ~20:52 UTC
at frozen 141007c37c58c1a0e3d1297c582b015ca0f20bb1. The agent's stream stalled mid-review in
the ~21:0x-21:29Z provider stream incident; it was RESUMED with context intact and finished
cleanly. Return received COMPLETE in one part with the required END-OF-REPORT marker; transport
entity-decoding only. Verdict PASS; zero blocking; advisories A-1..A-3.]

---

All checks complete. Modularity exit 0 (only pre-existing `tools/agent_supervisor` warnings, nothing from this task). Here is the complete gate report.

---

# GATE REPORT — M5-T068 (G3 independent code review)

**Verdict: PASS**

Task: Max-envelope test hardening (DB-046 a–f) — TEST-ONLY, seven mutation-sensitive tests over the frozen accepted T064 engine/route.
Reviewer: code-reviewer (read-only). Frozen submit identity: `141007c37c58c1a0e3d1297c582b015ca0f20bb1`. Material commit (only): `5aad9007248b3508656fa2cea4e4aef0806f2e42`.

## Identity / reproduction

- Material commit touches EXACTLY 3 files: `services/api/tests/scenario/test_max_envelope.py` (+160/-0), `services/api/tests/api/test_max_envelope_api.py` (+111/-0), `project-control/reports/M5-T068-producer-report.md` (+172/-0). Pure additions, zero deletions across the whole commit.
- Both test blobs are hash-identical at the frozen submit identity and the material commit (`6d8fe1a1…`, `0c5e5f16…`), and identical at HEAD (`703d4196…`); material is an ancestor of HEAD, so the working checkout carries the frozen content.
- TEST-ONLY wall HONORED: engine `app/scenario/max_envelope.py` (blob `f0abf884…`), route `app/api/v1/max_envelope_api.py` (`c1876148…`), and `app/main.py` (`cbc8f1b6…`) are byte-identical between the material parent and the frozen submit, and unchanged at HEAD. No forbidden path touched.
- Reviewer runs from `services/api` cwd (frozen content on disk): `python -m pytest tests/scenario/test_max_envelope.py tests/api/test_max_envelope_api.py -q` → **63 passed in 4.92s** (56 pre-existing + 7 new). `python -m ruff check .` → **All checks passed!**. `python tools/modularity_check.py --check` (repo root) → **exit 0** (only pre-existing `tools/agent_supervisor` warnings; no test file listed).

## Requirement-by-requirement (DB-046 a–f) — re-derived against frozen source

All six gaps close correctly; each new test verified to have real teeth against the frozen engine/route.

1. **(c) `_LOT_AREA_INPUT` drift guard** — `test_max_envelope.py:689`. Asserts `me._LOT_AREA_INPUT == pc._LOT_AREA_INPUT`; both are `"lot_area_sq_ft"` (engine `max_envelope.py:113`, checker `proposal_checks.py:281`). Divergence in either constant goes red. Mirrors the accepted `test_usable_coverage_matches_accepted_checker`. PASS.

2. **(a) multi-floor >100ft through the FULL consistency proof** — `test_max_envelope.py:696`. Height binds 250.0 via `_FakeRegistry` trace on family `residential_height_setback`/output `max_building_height` (spec `max_envelope.py:237-239`). `MAX_FLOOR_TO_FLOOR_FT = 100.0` (`proposal.py:79`) → `_height_levels(250)` (`max_envelope.py:759-772`) yields `floor_count=3`, `ftf≈83.33 ≤ 100`. Test asserts `floor_count>1`, `ftf≤MAX`, both saturating checks `"pass"` (keyed by check_id in `_verify_consistency` `max_envelope.py:914-929`), and an INDEPENDENT `check_proposal` re-run reports `building_height.provided_value ≈ 250` at PASS. A mutant collapsing the split (floor_count=1, ftf=250>100) fails the `ftf≤MAX` assertion. PASS.

3. **(b) populated `_conflict_advisory` branch** — `test_max_envelope.py:732`. `_ConflictRegistry.detect_conflicts` returns `competing_output_names=("max_lot_coverage_ratio",)` + two competing rules; engine `_conflict_advisory` (`max_envelope.py:513-529`) then emits `{"competing_rule_ids":["cov-a","cov-b"],"note":…}`. Test asserts advisory populated, note present, binding stays TIGHTEST (`0.5`, `cov-a`, `gap_reason is None` — never resolved), the unflagged height dim carries NO advisory, and the advisory survives `as_dict()` serialization. A mutant returning `None`/dropping the branch goes red. PASS.

4. **(d) engine fail-closed raise** — `test_max_envelope.py:762`. Monkeypatches the ENGINE's imported reference `me.check_proposal` (correct seam — `_verify_consistency` calls the module global at `max_envelope.py:909`) to FAIL `lot_coverage_ratio`; asserts `MaxEnvelopeError.field == "candidate.max_lot_coverage_ratio"` (engine raise at `max_envelope.py:920-925`, `_SATURATING_CHECK_IDS` mapping `:269-271`) and `assert calls` proves the patch actually fired (no vacuous pass). A mutant turning the `raise` into a silent pass goes red. The `_StubCheck` carries `provided_value`/`required_value` so the raise's f-string interpolation does not `AttributeError` into the wrong branch. PASS.

5. **(d) route candidate.* → bounded 500** — `test_max_envelope_api.py:300`. Patches `engine.check_proposal`→FAIL AND spies `mod.derive_max_envelope` with `_observing_derive`, which runs the REAL engine (`real_derive`), catches the `MaxEnvelopeError` it genuinely raises, records type+field, and re-raises unchanged. The route calls the module-global `derive_max_envelope` (`max_envelope_api.py:307`), so the patch bites; its handler maps `field.startswith("candidate")` → `_internal_error_500` (`max_envelope_api.py:314-320`). Asserts `observed == {MaxEnvelopeError, candidate.max_lot_coverage_ratio}`, status 500, fixed generic body, `"candidate" not in resp.text` (no invariant leak), and correlation-id header parity. A mutant routing `candidate.*`→422 leaves status≠500 → red; a checker patch that silently missed leaves `observed` empty AND status≠500 → red. Real router mounted, real error path, no over-mock. PASS.

6. **(e) live (500, internal_error)** — `test_max_envelope_api.py:363`. Forces `mod.get_max_envelope_registry`→`RuntimeError(secret)`; `_effective_registry` (`max_envelope_api.py:120-131`) raises on the event loop and is caught at `:291-293` → `_internal_error_500`. Asserts status 500, `(500,"internal_error")` in the matrix, fixed body, `secret not in resp.text` AND `"RuntimeError" not in resp.text` (route logs correlation-id only, `:292`, no `str(exc)`), and correlation-id parity. A mutant leaking `str(exc)`/type goes red. This exercises the row LIVE, not by frozenset membership. PASS.

7. **(f) out-of-2263-bounds typed gap** — `test_max_envelope.py:787`. `_lot(8000.0, anchor=(0,0))` builds a geometry-consistent 80×100 rect anchored far outside NYC bounds; engine bounds check (`max_envelope.py:800-809`) returns `LOT_GEOMETRY_UNSUPPORTED` WITH `lot_rectangle=rect.as_dict()`. Test asserts `candidate is None`, `candidate_consistency is None`, status `LOT_GEOMETRY_UNSUPPORTED`, `"EPSG:2263 NYC bounds" in detail`, and `lot_rectangle is not None` — the last precisely pins the BOUNDS branch, distinguishing it from the parse-failure branch (`:797-798`, which leaves `lot_rectangle=None`). Binding values still stand. A mutant removing the bounds gate would emit a candidate → red. PASS.

## Test-quality / correctness checks (packet items 3–5)

- Seams are the real ones: `me.check_proposal`, `mod.derive_max_envelope`, `mod.get_max_envelope_registry` — all monkeypatched on the actual imported/module references that the frozen code resolves at call time. No route-mounting shortcut: both route tests mount the REAL `router` on a fresh `FastAPI()` (`mounted_app`/`client` fixtures) and drive it through `TestClient`. The AS-1 route spy runs the REAL engine and only observes+re-raises, so the 500 is produced by genuine engine behavior, not fabricated.
- Fixtures verified against frozen source: in-bounds anchor `(985000,195000)` and out-of-bounds `(0,0)` are correct for the NYC 2263 window; area 8000 = 80×100 is bounding-box-consistent so `_lot_rectangle` parses (isolating the bounds gate); the 250ft→3-floor math is correct for `MAX_FLOOR_TO_FLOOR_FT=100.0`. No guessed schema values; every pinned field/enum matches the frozen engine/route.
- No production-behavior pinning contradicts accepted T064 semantics: `candidate.*` field naming, `candidate.*`→500 vs precondition→422 split, `LOT_GEOMETRY_UNSUPPORTED`+`lot_rectangle`, and the `{competing_rule_ids,note}` advisory shape all match the frozen source.
- Modularity boundary answers: N/A — no handwritten production source changed (test-only). Test files are out of production-modularity scope; `modularity_check --check` exit 0.

## Findings

Blocking: NONE.

Advisory:
- A-1 (informational): The producer report withdrew its earlier "ruff clean / 63 passed" api claim as PENDING supervisor (correct-cwd capture). I independently reproduced both from the `services/api` cwd against the frozen content (63 passed; ruff clean). The orchestrator should treat this reviewer run as the corroborating digest-bound capture, or re-capture at harvest.
- A-2 (minor duplication): `_StubCheck`/`_StubReport` are defined twice — `test_max_envelope.py:652/667` and `test_max_envelope_api.py:284/292` — with slightly different shapes. Not a defect (separate test modules, intentional per-file doubles); could be a shared helper later. No action required.
- A-3 (scope handoff): directive_refs D-082-R002, D-066-R001, D-077-R002/R003 require the per-requirement attestation recorded in the directive's `verification.json` by the `directive-compliance-verifier` (a separate packet reviewer). From the code-review lens the changes are consistent with those directives (max-envelope hardening; nav block honored — the two test files have no downstream consumers so no sweep is owed; report references files without long embeds). The formal per-ID pass remains the DCV's remit.

END-OF-REPORT M5-T068-G3
