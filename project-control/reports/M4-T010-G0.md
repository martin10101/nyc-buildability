# M4-T010 — G0 administrative contract check (orchestrator)

Packet completeness verified against the /start-controlled-task checklist before claim:

- Requirement identifiers: M4-T009 AS-5 (packet text), M4-T009 producer report §4.4 (routed
  scope decision), PRD §19 (a material value never surfaces without its citation), C3 digest
  convention (docs/MVP_AGENDA.md — v1 = sha256(verbatim_excerpt)).
- Directive refs: D-038:ALL (in-regime; empty-applicable row expected at accept per the
  M2-T021/M0-T057 precedent — R003/R004 task_ids are M5-only).
- Evidence files: named exactly in inputs[] (schema, dsl.py, both v1 snapshots, the two
  M4-T009 sources).
- Scope: EXACT-FILE allowed_paths, disjoint from open M4-T009 (its tests/rules/** glob vs this
  packet's single new test file; sequential execution, no parallel writer — path_notes record
  the disjointness rationale); rulesets/** forbidden here so family digest adoption stays in
  M4-T009's AS-5 unit.
- Acceptance scenarios S1–S6: primary (S2), boundary (S4), missing/ambiguous (S5), failure
  (S3), back-compat enumeration (S1), no-change proof (S6) — executable, anti-tautology forms
  required in the scenario text.
- Gates: G0/G1/G3/G4 with reviewers code-reviewer, data-contract-verifier, qa-engineer, all
  distinct from the producer. G5 not required: no auth/secrets/upload/network surface; the
  fail-closed load path is G1/G3/G4 territory. G3 is mandatory because this IS a contract
  change (additive, versioned-contract discipline per .claude/rules/backend-api.md).
- Modularity boundary answers: owning responsibility = rule-definition contract + fail-closed
  reference validation at load; owning modules = rule_definition.schema.json + dsl.py
  (_check_refs); dsl.py is 288 lines, far under thresholds; no extraction needed; public
  interface (load_rule_file/build_rule_definition signatures) unchanged; focused tests =
  new test_rule_citation_digest.py; modularity check runs before submission.

G0 verdict: PASS (administrative; recorded by the orchestrator per gate-class rules).
