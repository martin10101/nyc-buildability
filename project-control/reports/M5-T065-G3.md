# M5-T065 G3 gate — independent code review (verbatim reviewer return)

Reviewer: code-reviewer (claude-opus-4-8 per D-064), dispatched by the orchestrator at the
seq-124 T065 wave, pinned to the frozen submit head 65faee54 (material d3793c2e). Recorded
by the orchestrator; the reviewer is read-only and records no gate itself. The return
arrived in three parts (the first transmission truncated; the reviewer re-sent the
remainder from the exact truncation point in two parts) — joined verbatim below.

---

VERDICT: PASS

# G3 Independent Code Review — M5-T065 (map-drawing input slice + 4326→2263 correspondence bridge)

**Reviewer:** code-reviewer (independent, read-only) · **Gate:** G3 (correctness/maintainability) · **Date:** 2026-09-20
**Task:** M5-T065 — D-082-R001/R003 keyboard-operable outline input over the accepted read-only lot map + flag-gated UNMOUNTED 4326→2263 correspondence-bridge endpoint + additive adoption into the accepted proposal draft.
**Producer:** frontend-engineer (loop-3). **Verdict: PASS** (3 advisory findings, none blocking).

## Identity verification (performed)
- Submit head `65faee54`, material `d3793c2e`, base/parent `f98924e5` all exist (`git cat-file -t`).
- **Working tree == frozen surface:** `git diff 65faee54..HEAD -- <12 task paths>` is **empty**; `git status --porcelain` clean for the task paths. Judged against the frozen path-scoped content; bound to it.
- **Material scope is exact.** The report's "base `f98924e5`" is the contract/claim seam, not the commit's direct parent. `d3793c2e`'s real parent is `e085eba5` (the T062 submit seam), so `git diff f98924e5..d3793c2e` spans the intervening T062/T063 commits and shows extra files — a range artifact, not this commit. The material commit's **own** diff (`git show d3793c2e --numstat`) touches **exactly the 12 allowed_paths**, no forbidden path (LotOutlineMap, main.py, site_definition/**, connectors/**, rules/**, the held T063 surface all untouched). Disjoint-peer control commits during review touch none of the 12 paths.

## Local evidence (re-collected)
| Command | cwd | Result |
|---|---|---|
| `python -m ruff check .` | `services/api` | **All checks passed** (exit 0) |
| `python -m pytest tests/api/test_outline_bridge.py -q` | `services/api` | **38 passed** (exit 0) |
| `python tools/modularity_check.py --check` | repo root | **failures 0**, 21 warnings (all pre-existing `tools/**`); `outline_bridge.py` a `review_signal` warning only, under the 1000-SLOC hard cap |
| `git grep -nE "pyproj\|Lambert\|proj4\|transformer.transform" d3793c2e -- <packet py/ts>` | — | only the two **docstring** lines stating the prohibition; **no CRS-library import, no hand-rolled projection math** (AS-3 satisfied) |

Web behavior proves in CI (thin client); CI is reported green at `a3173987` (run 35554705276) and pushed head `93b5943a`.

## Correctness assessment
- **Correspondence math (`outline_bridge.py:378-482`)** is a correct centered least-squares 6-parameter 2D affine. Centering rationale (4326 ~1e0 vs 2263 ~1e6 ill-conditioning) is sound; the singular-scatter guard (`:410`) returns `None` on collinear control points. `fit_correspondence` searches both windings and all cyclic offsets (`:466-473`) — correct, since the two official layers need not share a start vertex or winding — and the drawn vertices are mapped through the fitted affine in the correct src(4326)→dst(2263) direction (`:774-775`). Residual is computed in destination feet, matching the ft bound.
- **"Residual necessary-but-not-sufficient" is enforced in code, not just documented:** a fit is emitted only with ≥4 control points AND RMS ≤ 2.0 ft (`:739`) AND a ≥2.0 ft margin over the runner-up alignment (`:761`); otherwise refused. The symmetric-parcel ambiguity case is the central risk and is handled (unit + route tests below prove a low-residual-but-ambiguous square is refused with no coordinates).
- **Typed distinct refusals** are single-sourced in `OUTLINE_BRIDGE_STATUS_STATE_MATRIX` (`:128-141`); every response helper emits a pair in the matrix. Fail-safe posture verified: flag-off/unmounted → generic `404 {"detail":"Not Found"}` with no correlation id or feature hint (`:561-564,636-637`); body ceiling enforced before parse; BBL/SRID validated before any connector I/O.
- **Client decoder (`outline-bridge-api.ts`)** mirrors the matrix verbatim (`DOCUMENTED_PAIRS :228-238`), routes strictly by (status, state) never by state alone (`:417`), bounds size before parse (`:394-401`), classifies a 200 missing the correspondence block as a distinct `validation_failure` (`:294-296`), and passes the load-bearing residual through **verbatim** (`finiteOrNull`, no rounding). Module boundaries clean — transports/bounds only, computes no coordinate.
- **Adoption semantics** are correct and additive: `adoptOutlineVertices` (`proposal-draft.ts:277-279`) is pure/immutable, replacing only outline vertices; `ProposalEditor.adoptDrawnOutline` (`:102-114` of the diff) lands them in the numeric table as-if-typed, clears the stale check outcome, and keeps the table the editable authority (manual stays the option — D-082-R003). The e2e proves end-to-end that the **adopted** 2263 vertices are exactly what the check route receives.
- **A11y / DB-043(a):** focus-on-delete uses the nonce + `useEffect` remedy (`ProposalOutlineDraw.tsx:78-89,99-107`), never a synchronous `.focus()` on a remounting node (CODING_RULES). Every action is keyboard-operable; labeled inputs; live-region announcer. Honesty labeling (D-076-R002) present on the drawn surface and in the server disclosure ("not a survey and not a city record").

## Test adequacy
Strong and layered: correspondence unit tests (unique/cyclic/reversed recovery, the symmetric-square ambiguity proof, triangle/too-many/mismatch/collinear typed reasons); route tests (each distinct refusal, flag-off 404 sentinel, **UNMOUNTED assertion** that the route is absent from `app.main`, provenance surfacing, offline production-adapter paths incl. multipolygon); mutation-sensitive client matrix tests (never-route-by-state-alone, verbatim residual, missing-correspondence → validation_failure); component tests (proposed label, convert gating, keyboard entry + exact-vertex adoption, focus-on-delete mid-list and empty, distinct residual vs neighborhood refusals); and an e2e that keyboard-drives draw→bridge→adopt→check and asserts the adopted 2263 vertices flow into the check.

## Findings
- **F1 — Low / advisory (discovery, route to backlog + the follow-up M5-T066/mount seam).** The bridge requires the 4326 display ring and 2263 authoritative ring — fetched from two different connectors (`mappluto_lot_outline` vs `mappluto_geometry_arcgis`) — to have **equal vertex counts** and correspond within 2 ft RMS. If those connectors return differently-densified rings for real lots, the endpoint will refuse (`vertex_count_mismatch` / `residual_too_high`) for those lots. Behavior is **fail-safe** (never ships wrong coordinates) and the route is **UNMOUNTED**, so no production exposure, but real-lot efficacy is unverified thin-client. The packet's own risk register anticipates this; the follow-up should validate against real parcel pairs before mounting. Not blocking.
- **F2 — Info (modularity).** `outline_bridge.py` is 826 lines — a `review_signal` warning (0 failures, under the 1000 hard cap), cleanly split into three documented, non-overlapping sections (connector I/O, pure affine math, HTTP). Acceptable now; a further growth should split with a compatibility facade.
- **F3 — Info (React idiom).** `deletePoint` calls `setPendingFocus(...)` inside the `setPoints` functional updater (`ProposalOutlineDraw.tsx:100-106`). It works and is covered by a passing focus test, but queuing a sibling state update from within an updater is non-idiomatic; consider computing the focus target outside the updater in follow-up.

## Scope-ruling compliance
Judged against the recorded [ORCH-SCOPE-DISPOSITION] (M5-T065.json progress_log, 2026-09-21T00:52Z): I did **not** fail for the absence of map click-to-place. The delivered surface — keyboard/numeric vertex entry over the read-only reference map + the full correspondence bridge + additive adoption — is correct, honest, fail-safe, and well-tested. The producer surfaced the click-to-place scope gap honestly rather than faking it (§3.1 of the producer report); that gap is contracted separately as M5-T066.

**VERDICT: PASS** — no correctness, contract-fidelity, or maintainability defect in the delivered material. Findings F1–F3 are advisory and belong to the follow-up slice / backlog, not this gate.

[end of report]
