# M5-T102 — G3 code review (code-reviewer "cr-t102", read-only)

> Transmission history: pinned at cf4fd224, delivered as two SendMessage parts (1/2, 2/2) ending with END-OF-REPORT;
> no truncation. Joined verbatim by the orchestrator (transport wrapper tags removed only). Verdict PASS: claim_key is
> byte-identical in logic to the old PDF key; the 11-word list is unchanged; every caller-text path is screened; the three
> goldens are unchanged. One advisory (relative imports are not counted by the allowlist) is routed at the accept seam.

---

M5-T102 G3 code review (part 1 of 2): claim_key / word-list / AS-1 / input-coverage — all PASS.

Read-only. PIN: HEAD at start = cf4fd224. Material commit 01b52fe8 = exactly the 9 allowed files; no forbidden path touched.

CLAIM_KEY SEMANTICS — PASS. New claim_key (claim_words.py:66-73) = _SEPARATOR_RUN.sub(" ", text.upper()) with _SEPARATOR_RUN = re.compile(r"[^A-Z0-9]+") (claim_words.py:63). Byte-identical logic to the old PDF _claim_key/_CLAIM_SEPARATOR_RUN (01b52fe8^:pdf_sheet_writer.py:221,252-253). Upper-cases first, then collapses every non-alphanumeric run (underscore, hyphen, dots, tabs, space-runs, and the "?" an ASCII-sanitiser emits) to one space; non-ASCII letters (é/ſ) fold to a space exactly as before. contains_claim_word (claim_words.py:76-93) replicates the old PDF screen loop (per-word claim_key, substring, tuple-order return). Guard is `barred is not None` in all three writers — correct (claim words are never empty).

FALSE POSITIVES — no new material risk. Single-word claims (LEGAL/APPROVED/…) match identically under old raw-substring and new collapse (single spaces map to single spaces). For DXF/GLB the collapse only widens the two multi-word phrases ("MAXIMUM ALLOWED"/"AS OF RIGHT") so underscore/dot/multi-space separators now match — the intended DB-059(b) convergence to accepted PDF behaviour; fail-closed (refuse-only; emits nothing new, AS-4 byte identity proven). test_claim_words.py:91 pins "12 PERMIT ST"/"1 LEGACY PL" as CLEAN (guards against over-broad single-word hits).

WORD LIST — PASS. Old DXF (01b52fe8^:dxf_writer.py:173-185) == old GLB (glb_writer.py:146-158) == new shared tuple (claim_words.py:45-57), 11 words unchanged. Pinned by value, hardcoded (test_claim_words.py:32-49).

AS-1 ONE SOURCE — PASS. Only claim_words holds the literal + matcher; AST drift tests (test_claim_words.py:139-148) and identity tests (161-167) green. PDF dropped import re / _claim_key / _CLAIM_SEPARATOR_RUN and imports the shared module (pdf_sheet_writer.py:55); GLB dropped its literal (test_glb_writer.py:552 not hasattr); dxf keeps identity-equal alias (dxf_writer.py:60).

INPUTS / NO UNSCREENED PATH — PASS. GLB _check_name screens the mesh name after the isinstance/charset guard (glb_writer.py:341-357); node names == validated mesh names (glb_writer.py:543), scene name is the PROPOSED_LABEL constant (542). DXF screens each of its 3 emitted constants — the only text it emits, no caller-text param (dxf_writer.py:744-767). PDF screens all 4 caller fields passing raw AND ASCII-sanitised forms (pdf_sheet_writer.py:241; DB-053 c).

(part 1 of 2 — continues)

---

M5-T102 G3 code review (part 2 of 2): byte-identity / allowlist / modularity — VERDICT PASS.

BYTE IDENTITY (AS-4) — PASS. Commit touches only the 9 allowed files; test_cad_owner_samples.py, tests/drawings/test_dxf_roundtrip.py, docs/samples/, requirements.txt/.in untouched. All three golden SHA constants byte-identical old vs new (DXF 6a8dbd94, GLB 30d79d80, PDF c38360f9). I ran (cwd services/api): `python -m ruff check .` → All checks passed; `python -m pytest tests/cad tests/drawings/test_dxf_roundtrip.py -q` → 345 passed (incl. golden-digest, roundtrip, owner-samples).

ALLOWLIST (AS-5) — PASS. _imported_modules (test_dxf_writer.py:555-579) flags __import__("x") by module name, __import__(name) as "__import__", and any importlib reference; load-bearing mutants on the real source (test_dxf_writer.py:600-616). Conservatively over-blocks (safe direction). ADVISORY (non-blocking): the ImportFrom branch is gated on node.level == 0, so a relative import (`from . import x`) is not counted — a theoretical blind spot only; dxf_writer uses absolute imports exclusively, so no current gap.

DISCLOSED DEVIATION (DB-059 h) — acceptable for PKT-A. GLB refusal echoes the caller mesh name (glb_writer.py:356); DXF echoes {text!r} (dxf_writer.py:750) but that text is a module constant — no caller leak. Redaction explicitly routed to the wiring packet; writers are unwired; pre-existing behaviour, not worsened. PDF is already non-echoing (test asserts caller text not in detail, test_pdf_sheet_writer.py:493).

MODULARITY / DEPS / SCOPE — PASS. claim_words.py = 93 lines, focused, stdlib `re` only → no import cycle (imports nothing from app.cad). dxf_writer shrinks 932→928; glb 588; pdf 500. Zero new dependencies (no requirements/lockfile change). Unwired. Exactly the allowed paths.

MUTATION COVERAGE — strong. DXF raw-substring mutant (test_dxf_writer.py:637-654), GLB raw-substring mutant (test_glb_writer.py:516-536), PDF title-block screen-removal mutant (test_pdf_sheet_writer.py:470-482) each prove the shared screen is load-bearing; GLB full 11-word coverage asserts code=="claim_class_word" (test_glb_writer.py:539-546), fixing the former 2/11 (DB-059 c). PDF caller-text refusal asserts the caller string is never echoed (test_pdf_sheet_writer.py:493).

No BLOCKING findings. One ADVISORY: relative-import allowlist blind spot (test_dxf_writer.py:565), no current exposure.

M5-T102 G3 VERDICT: PASS

END-OF-REPORT
