# M5-T090 — G5 security review: section-5 new-package provenance review of all 13 new packages (security-reviewer "sec-3d", read-only)

> Transmission history: pinned at 023294ec (lock byte-identical through 4c999e15), delivered as four
> SendMessage parts (1/4..4/4) ending with END-OF-REPORT; no truncation. Joined verbatim in delivery order
> by the orchestrator (transport wrapper tags removed only). Dispatched without a model override (agent
> file pin claude-opus-4-8).

---

M5-T090 G5 provenance review — PART 1/4 (diff verification)

HEAD pinned 023294ec at start; now 4c999e15. Lock byte-identical across that range (`git diff --stat 023294ec..HEAD -- apps/web/package-lock.json apps/web/package.json` = empty). Read-only throughout; registry/OSV/GitHub queried live; registry clock = Thu 24 Sep 2026 10:31:40 GMT.

DIFF (80f17a86^=114e5e56 vs 2e444920): exactly 13 NEW `node_modules/*` entries, matching the assigned list —
three@0.186.0, @react-three/fiber@9.7.0, @react-three/fiber/node_modules/scheduler@0.27.0 (nested), @types/react-reconciler@0.28.9, @types/webxr@0.5.24, base64-js@1.5.1, buffer@6.0.3, ieee754@1.2.1, its-fine@2.0.0, react-use-measure@2.1.7, suspend-react@0.1.3, use-sync-external-store@1.7.0, zustand@5.0.15.

NO existing package version changed. The only edits to existing entries are three `"dev": true` REMOVALS — @babel/runtime@7.29.7, @types/react@19.2.17, csstype@3.2.3 — versions unchanged; they flip dev→production because fiber's runtime subtree now reaches them (harmless; whole-tree audit already covers them). Root scheduler@0.26.0 untouched; fiber's `^0.27.0` nests 0.27.0 separately.

Lock produced ONLY by the reviewed workflow: 2e444920 authored by github-actions[bot], message "chore(web): regenerate package-lock.json (M0-T019 security tree, validated)" — the cherry-pick of the generate-lockfile.yml bot commit.

UNIVERSAL (all 13, independently verified from registry.npmjs.org): resolve to the official registry; lock dist.integrity MATCHES the official registry integrity; ZERO install/lifecycle scripts (no preinstall/install/postinstall/prepare); OSV version-query CLEAN(0). Per-package specifics + advisory detail in PART 2.

---

M5-T090 G5 review — PART 2/4 (per-package table + advisories)

Per-package (name@ver — age — license — publisher / maintainers). All: integrity MATCH, no scripts, registry.npmjs.org, OSV CLEAN.
1. three@0.186.0 — 15.63d — MIT — mrdoob (Ricardo Cabello); maint 2 (mrdoob, mugen87)
2. @react-three/fiber@9.7.0 — 54.76d — MIT — krispyaa; maint@ver 22, pkg-now 8 (change ruled in PART 3)
3. @types/react-reconciler@0.28.9 — 651.8d — MIT — types (DefinitelyTyped)
4. @types/webxr@0.5.24 — 359.7d — MIT — types (DefinitelyTyped)
5. base64-js@1.5.1 — 2142.6d — MIT — feross
6. buffer@6.0.3 — 2131.3d — MIT — feross
7. ieee754@1.2.1 — 2157.5d — BSD-3-Clause — feross
8. its-fine@2.0.0 — 582.8d — MIT — codyjasonbennett/drcmda (pmndrs)
9. react-use-measure@2.1.7 — 601.8d — MIT — codyjasonbennett/drcmda
10. scheduler@0.27.0 (nested) — 357.5d — MIT — react-bot/fb (React team)
11. suspend-react@0.1.3 — 1198.8d — MIT — drcmda (pmndrs)
12. use-sync-external-store@1.7.0 — 14.72d — MIT — react-bot via GitHub Actions trusted publisher (React team)
13. zustand@5.0.15 — 42.4d — MIT — daishi/jeremyrh/drcmda via GitHub Actions trusted publisher

AGE: every version ≥ 604800s using the registry clock; youngest is use-sync-external-store 1,271,552s = 14.72d. None near the floor.

ADVISORIES (two sources + live positive controls):
- OSV version-query: CLEAN(0) for all 13.
- GitHub Advisory DB (native): three@0.186.0 = 0, @react-three/fiber@9.7.0 = 0.
- Positive controls (prove queries are live, not an outage): OSV three@0.124.0 → GHSA-fq6p-x6j3-cmmq; GitHub three@0.124.0 → [GHSA-7vvq-7r29-5vg3, GHSA-fq6p-x6j3-cmmq]. Both historical GHSAs affect only <0.137.0 / <0.125.0, fixed long before 0.186.0.

TYPOSQUAT: all 13 are the canonical packages from their established maintainers, and every name matches @react-three/fiber's declared dependency manifest — the resolved tree equals the declared graph, no injected/confusable package. CI + rulings in PART 3.

---

M5-T090 G5 review — PART 3/4 (CI evidence + rulings)

CI EVIDENCE:
- generate-lockfile run 35985974452, job "lockfile" (107588608594): status success, and EVERY step succeeded — "Deterministic install check (npm ci; integrity + lock↔package.json)", "Blocking npm audit (--audit-level=low; dev deps included)", "Blocking npm audit (JSON total==0)", "Committed-lockfile release-age gate (>=7 days, fail-closed)", "npm CLI tooling advisory verification (11.18.0, fail-closed)" — with "Commit lockfile" running LAST. So the bot lock was committed only after the full gate suite passed on this exact content.
- Live CI run 35987709331 (head 4c999e15, byte-identical lock): job web-dependency-security (audit + committed-lock age gate + npm CLI advisory), ID 107594249947 = success in 55s. Also green: web (lint+typecheck+build), api (ruff+pytest), exact-production-install (pip-audit). web-e2e still running — irrelevant to dependency security. NOTE: the push-CI at 023294ec itself shows "cancelled" (superseded by later branch pushes — the known own-push-cancels-CI behavior), but the identical lock is validated green on the live head.

MAINTAINER-CHANGE RULING (producer flag / D2): fiber version-level maintainers were 22 at 9.7.0; package-level now 8, with new account dennissmolek added at 9.8.0. Does NOT affect the admitted 9.7.0 tarball: npm cannot republish an existing version, its integrity is registry-matched, and its publisher krispyaa was and remains an authorized maintainer. → non-blocking for 9.7.0; WATCH before any fiber upgrade (esp. 9.8.0).

NECESSITY (policy §5): SATISFIED. three.js is genuinely required — exact legal-envelope triangle geometry (ZR 23-73x sky-exposure / sloped-setback planes are non-prismatic), the GLB deliverable, and cutaway/ghost/isolate/floor interactions that MapLibre fill-extrusion (base+height prisms, layer-constant opacity, no glTF loader, no clipping planes) cannot provide; MapLibre's own 3D-model path itself embeds three.js. @react-three/fiber (+~11 transitive) is explicitly named by owner D-087-R011 and rule 3d-ui-expansion.md item 5, and every transitive package passes all gates. @types/three and drei correctly deferred.

DISCOVERIES: D1 (@types/three later, own admission — it pulls a WASM physics dep @dimforge/rapier3d-compat; scrutinize then): agree, correctly excluded here. D3 (fiber peer react `>=19 <19.3`): valid watch; current react 19.1.2 fits; a 19.3+ bump must bump fiber together. D4 (@types/webxr `*` float): re-gated by the committed-lock age+advisory gates on every regen, npm ci deterministic against the pin — acceptable. Findings + verdict in PART 4.

---

M5-T090 G5 review — PART 4/4 (findings + verdict)

BLOCKING FINDINGS: none.

ADVISORY / WATCH (non-blocking):
A1. @react-three/fiber maintainer set 22→8 with new account dennissmolek (at 9.8.0). Non-blocking for the immutable, integrity-matched 9.7.0 tarball. Recheck maintainers/provenance before ANY fiber upgrade, especially 9.8.0.
A2. @types/three is a REQUIRED future admission (three@0.186.0 ships no .d.ts). It pulls ~6 runtime deps incl. a WASM physics engine (@dimforge/rapier3d-compat) — must get its own G5. Correctly excluded from this admission.
A3. A future React 19.3+ upgrade must bump fiber in the same change (fiber peers react/react-dom `>=19 <19.3`), or npm refuses the peer conflict.
A4. @types/webxr uses the floating range `*` in fiber's deps; currently resolves to 0.5.24 (verified clean). Each lock regeneration may pick a newer one — re-verified by the age+advisory gates each time; committed lock pins it, so npm ci is deterministic.
A5. Informational: @babel/runtime@7.29.7, @types/react@19.2.17, csstype@3.2.3 flipped dev→production (no version change) because fiber's runtime subtree reaches them; whole-tree audit already covers them.

POLICY §1 CHECKLIST — all green: exact pins (package.json 9.7.0/0.186.0; lock single-versions every transitive with integrity); every admitted version ≥ 604800s by the registry clock; advisory-free at every severity (OSV + GitHub + CI npm audit low & JSON-total-0, positive controls live); integrity matches the official registry for all 13; official registry origin; zero lifecycle scripts; no existing package version changed; lock produced only by the reviewed workflow whose full gate suite passed; §5 necessity + owner authorization (D-087-R011) satisfied. Fail-closed conditions: none triggered.

M5-T090 G5 VERDICT: PASS

END-OF-REPORT
