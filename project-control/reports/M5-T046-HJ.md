<!-- Orchestrator capture: human-journey-reviewer return for M5-T046 saved VERBATIM
     (transport entity-decoding only). Note: the reviewer titled its report "G3
     Human-Journey Gate Report"; in this packet's gate scheme the ledger G3 gate is the
     code-reviewer's — this file is the HJ walkthrough report (amended AS-8), the T040
     pattern. Review pinned at frozen head c9113e09. -->

Review complete. Returning my G3 human-journey report below.

---

# G3 Human-Journey Gate Report — M5-T046

**Task:** Address-to-lot identity honesty (DB-026): GeoSearch equality gate + typed no-match + confirm-arc address-of-record display
**Gate:** G3 (independent human-journey walkthrough), amended AS-8
**Reviewer:** human-journey-reviewer (read-only)
**Reviewed identity:** frozen submission `c9113e09`; live HEAD `665d3d0b` (intervening commit adds only `project-control/reports/M5-T046.json`, `state.json`, `tasks/M5-T046.json` — control-plane only; `c9113e09` is an ancestor of HEAD). The `apps/web` + `geosearch.json` source at `c9113e09` is **byte-identical** to the CI-green corrected head `fb7af67d` (empty `git diff fb7af67d c9113e09 -- apps/web docs/research/source-registry-drafts/geosearch.json`), so this walkthrough is against the CI-proven material. CI evidence `project-control/reports/M5-T046-ci-evidence.md`: CI (incl. web-e2e vitest+Playwright) **success** at `fb7af67d`; 1038+1 tests green; modularity check exit 0.

## VERDICT: PASS

No blocking findings. Three advisory findings (all honestly scoped / disclosed). The shipped increment — the entered-vs-matched confirm-arc display with verbatim raw-input preservation, plus the equality-gate library as a future primitive — is honestly valuable on its own, and **nothing on screen claims a protection that does not exist.**

---

## Journey narrative

I walked the two AS-8 journeys as a skeptical analyst, from source + the executable component/journey tests, on the CI-proven head (thin client: no local npm/node per packet WEB VERIFICATION binding).

**1. The corner-lot case — "1279 37th Street, Brooklyn."** In the architect arc I type into the autocomplete (`AddressAutocomplete.tsx`, `role="combobox"`, labelled "Street address"). Permissive GeoSearch `/autocomplete` suggestions appear; I pick one. On pick, `choose()` captures the RAW typed text BEFORE `setText` rewrites the field (`AddressAutocomplete.tsx:110-117`) and passes it as the 2nd `onPick` arg (`:116`). `AddressResolutionScreen` threads it as `ResolutionResult.typedInput` (`:66,159`) into `AddressConfirmCard` (`:251`). The pick re-resolves through the **server Geoclient** route, not GeoSearch. On the confirm card I now SEE: the heading "Is this the right lot?" (focus target), the city-matched line "1279 37 STREET, BROOKLYN 11218" (`confirm-address`, `AddressConfirmCard.tsx:114-121`), and — distinct and below it — "You searched for **1279 37th street brooklyn**. The address above is what the city matched to this lot; the tax lot (BBL) below is the identity we carry forward." (`entered-input`, `:130-136`). The BBL `3052960043` carries forward. The entered value renders verbatim through an escaped React text node (no URL built from it); it is kept distinct from the matched frontage. This reads as a RECORD — no computed value implied (D-073-R006). Verified executably by S8 (`address-confirm.test.tsx:492-517`) and the full pick-journey S9 (`:536-639`, which asserts the exact `<strong>` textContent equals the raw typed string including surrounding whitespace, and that it differs from both the picked city-shaped label and the matched canonical line).

**2. The nonsense case — a wrong/nonexistent address (e.g. "1279 zzqqxx street brooklyn").** I traced the CURRENT arc as shipped. GeoSearch never returns an empty features array for garbage (corpus §4a: "zzqqxx" → a real WRONG lot "1279 53 STREET" at the identical `confidence:0.8 / match_type:"fallback"`). Suggestions stay permissive by design, so a fallback suggestion may appear — but it displays "1279 53 STREET", already showing the substitution at pick time. Crucially, **no live path silently lands the user on a lot**: every promotion requires (a) an explicit user pick (`choose`) or an explicit full-address search that still requires a pick (`runFullSearch` never auto-accepts, `AddressAutocomplete.tsx:126-143`; asserted in `autocomplete.test.tsx:125-154`), and (b) server-Geoclient re-resolution. On the confirm card the entered-vs-matched line is exactly the self-catch cue: "You searched for **1279 zzqqxx street brooklyn**" beside a matched line reading "1279 53 STREET" — the mismatch is legible, and "Not my property" (`:294-301`) returns to entry with form values retained (S4). The equality-gate LIBRARY refuses both nonsense probes to a typed `no_match` with NO lot surfaced (`resolveLotFromGeoSearch`, `address-search.ts:299-342`; AS-4 `address-search.test.ts:275-296`, including a `confidence:1/match_type:"exact"` wrong-street probe that is still refused — proving the fields are never consulted). The library is a FUTURE primitive (no production importer), so this refusal is not yet the live guard; the live honesty rests on the pick + Geoclient + entered-vs-matched display, which is a legitimate transparency increment.

**3. Recovery + clarity.** Absent-identity / manual / BBL paths: `typedInput` is undefined, so `enteredInput` falls back to the server `input_echo` string (`:96-107`) — which IS the verbatim entry on those paths. Blank handling is trim-for-blank-only (`typedInput.trim().length > 0`): whitespace-only typed text falls back to `input_echo`, and if both are empty the entire "You searched for" block does not render (`:130`), so there is no dangling empty prompt. When the BBL fails canonical re-validation, the card shows honest-absence copy and neither URL context renders (S2 `:264-301`), unchanged from the M5-T016/DB-005 discipline. No new copy overstates what was checked.

**4. The ORCH adjudication from the user's seat.** With no live GeoSearch promotion to wire, is the increment honestly valuable, and does anything claim protection that doesn't exist? The entered-vs-matched record + preserved raw input is genuinely useful — it is the mechanism by which an analyst catches a wrong match themselves. And the card makes no protection claim: the provenance disclosure states "This is an official address match from the city's Geoclient service, transported exactly as received. It has not been through the platform's rule review." (`:271-275`); there is no "Verified" badge (S5 asserts `container.textContent` does not match `/\bVerified\b/`, `:400`). The registry draft records the equality gate as the binding match discipline for any future promotion path (`geosearch.json` `match_discipline_verbatim_basis` / `connector_implementation`), and the record-address gap is deferred as OQ-5, not built around. The adjudicated scope is honest: no client-side lot-promotion path was added; server Geoclient stays the sole live lot authority.

---

## Findings

No blocking (F) items.

**A1 (advisory) — the lot's PLUTO address-of-record is not shown for a corner/vanity lot.** For bbl `3052960043` the record address is "3622 13 AVENUE" (corpus §6, assertion 5), which differs from both the entered input and the matched frontage. The confirm card shows entered + matched only. Two analysts entering different frontages of the same lot each see a different matched address with the same BBL and no on-screen signal that an official address-of-record may differ from either. This is honestly scoped: no existing channel carries it, and AS-6's else-branch explicitly permits shipping entered-vs-matched and reporting the gap as a discovery (`AddressConfirmCard.tsx:80-86`; `geosearch.json` `known_limitations` + `open_questions` OQ-5). The card does not falsely present the matched frontage as the official address. Non-blocking; recommend the OQ-5 BBL→PLUTO.address lookup be prioritized in a follow-up so the records display is complete.

**A2 (advisory) — spatial "above/below" references in the entered-input copy.** "The address above is what the city matched… the tax lot (BBL) below is the identity we carry forward." (`:132-135`) uses visual-layout references. DOM reading order matches the copy (matched line precedes the note precedes the BBL), so a linear screen-reader pass encounters them in the stated order; still, spatial wording is a mild accessibility smell. Additionally, in the rare no-normalized-street case (`:123-128`) "the address above" points at a fallback note rather than an address line. Non-blocking; consider layout-neutral phrasing (e.g. "the city-matched address" / "the tax lot (BBL) we carry forward").

**A3 (advisory) — verbatim surrounding whitespace in the bolded entered value, and `typedInput` not surviving retry.** (a) Leading/trailing whitespace in the raw entry renders verbatim inside `<strong>` (S9 confirms "  1279 37 st bk  " renders exactly). This is a deliberate verbatim-honesty choice and correct in spirit, but can look cosmetically odd; a display-only trim while preserving the true value in a title/aria attribute would be cleaner. (b) A retry-after-error that succeeds into a resolved card (`retry` → `runResolve(result.query)` with no `typedInput`, `AddressResolutionScreen.tsx:198-205`) falls back to `input_echo` (the picked components) rather than the raw one-box text — still honest (input_echo is a truthful record of what was submitted), just less granular, on a rare path. Non-blocking.

---

## Corroborating checks (within read-only scope)

- **Equality-gate library correctness** re-derived against the byte-faithful corpus: AS-1 (true hit → bbl 3052960043), AS-2 ("37th"→"37 STREET" via the digit-run ordinal fold, `ORDINAL_SUFFIX` `:249`), AS-3 (reverse frontage + GARAGE sibling bind to the same `pad.bbl`, identity never the address string or bin), AS-4 (both nonsense probes refused, no lot surfaced; fields never consulted), AS-5 (autocomplete shape parses without `confidence`/`match_type`; abbreviated "37 st" fails closed to `no_match`), AS-5b (resolved via explicit reference-parse; absent keys recorded null). The single CI red at `c3c690f7` (AS-5 asserting `resolved`) was a **test-only** correction to the fail-closed contract at `fb7af67d`; no production code changed (CI evidence confirms).
- **Scope discipline:** material commit `3182f0a2` touched exactly the 8 allowed source/doc files + the report; no `services/api/`, `packages/contracts/`, `PropertyOverview.tsx`, `AnalysisIdentityNotice.tsx`, or `ReportView.tsx`. The `onPick` signature change (`(query)` → `(query, typedText)`) reaches only `AddressResolutionScreen` (updated); `PropertyOverview`/`ZoningContextPanel` import only the two shared string constants, not the component — no broken out-of-scope consumer.
- **Preservation:** permissive suggestion UX and all M5-T032 reliability/cancellation tests remain in `autocomplete.test.tsx`; no new dependency; no contract-shape change.

## Evidence anchors
- `apps/web/src/components/address/AddressConfirmCard.tsx:96-136,271-275`
- `apps/web/src/components/architect/AddressAutocomplete.tsx:62-68,110-120,126-143`
- `apps/web/src/components/address/AddressResolutionScreen.tsx:58-67,146-160,247-253,308-311`
- `apps/web/src/lib/address-search.ts:249-342`
- `apps/web/src/components/address/__tests__/address-confirm.test.tsx:492-639` (S8/S9)
- `apps/web/src/lib/__tests__/address-search.test.ts:218-332` (AS-1..AS-5b)
- `apps/web/src/components/architect/__tests__/autocomplete.test.tsx:24-30,125-154`
- `docs/research/source-registry-drafts/geosearch.json:35-39,43-61`
- `docs/research/db026-address-to-lot-fixture-capture.md:16-91,197-208`
- `project-control/reports/M5-T046-ci-evidence.md`

**Recommendation to orchestrator:** record G3 = **PASS**. Advisories A1–A3 are non-blocking; A1 (OQ-5 record-address lookup) is the strongest candidate for a follow-up discovery task so the records display becomes complete.
