# M5-T054 G4 — independent QA/regression review (saved VERBATIM from the reviewer return; this header line is the orchestrator's)

# G4 QA/Regression Gate Report — M5-T054 (phase B2: proposal-conditioned rule checks)

**Verdict: PASS** (4 advisory findings; 0 blocking)

Reviewer: qa-engineer (read-only). Reviewed content identity confirmed at the frozen material commit `ba0cf6b0` (see Content-identity, below). Frozen submission head `622757b1`; live head differs only by a disjoint ledger commit — irrelevant to the 9 material files.

## Content-identity verification
All 9 material files on the ctl24 disk match the producer report's recorded LF-normalized `sha256[:16]` exactly (script `scratchpad/digests.py` → `ALL MATCH`). I therefore reviewed the frozen reviewed content even though my isolated worktree HEAD (`d8b3899f`) predates the material commit and cannot `git checkout` it.

## Reproduced evidence (run locally, offline, from `services/api` cwd)
- `python -m ruff check .` → **All checks passed** (matches report §3).
- `python -m pytest tests/rules/test_proposal_checks.py -q` → **25 passed**.
- `python -m pytest tests/rules -q` → **722 passed** (30.3s) — matches report/CI.
- `python -m pytest tests/scenario -q` → **568 passed** (3.4s) — B1 suites untouched.
- `python tools/modularity_check.py --check` → **exit 0**; `proposal_checks.py` is below the warn tier (not listed; only pre-existing `connectors/`, `integration.py`, `scenario/breakeven.py`, `tools/**` warn).

Regression is clean and reproduces the report's numbers and CI run 35491182686. Note: I could not `git diff` the forbidden paths from an isolated worktree, but (a) the material commit touched only the 9 allowed files (digest-confirmed), and (b) 722 rules + 568 scenario tests pass unchanged — strong evidence `evaluator.py/integration.py/registry.py/response.py/rulesets/**` and `app/scenario/**` are byte-preserved.

## Duty 1 — Independent hand-recomputation (arithmetic verified, fixture NOT self-proving)

| Quantity | Formula (from outline/lot coords) | Hand value | Module/fixture value | Match |
|---|---|---|---|---|
| footprint | 100 (x 1000000→1000100) × 50 (y 200000→200050) | 5000 sq ft | 5000 | ✓ |
| lot coverage | 5000 / 8000 | 0.625 | 0.625 | ✓ |
| coverage ceiling (synthetic R5) | param | 0.500 | 0.500 | ✓ |
| coverage shortfall (MAX: 0.625>0.5) | 0.625 − 0.5 | 0.125 | 0.125 | ✓ |
| gross floor area | 5000 × 3 floors | 15000 sq ft | 15000 | ✓ |
| cumulative height | 10 ft × 3 | 30 ft | 30 | ✓ |
| min wall-to-lot-line setback | west wall x=1000000 − lot line x=999990 | 10 ft | 10 | ✓ |
| synthetic height (wide) | param | 60 ft | 60 (PASS 30≤60) | ✓ |
| real r5-height (23-422) | rule | 45 ft | 45 (PASS 30≤45) | ✓ |
| synthetic rear-yard | param | 5 ft | computed but NOT presented (CNC) | ✓ refusal |
| synthetic residential-FAR area | 8000 × 1.5 | 12000 sq ft | computed but NOT presented (CNC) | ✓ refusal |
| real residential-FAR area | 8000 × 1.5 | 12000 sq ft | NOT presented (CNC) | ✓ refusal |
| summary attested | — | pass 1 / fail 1 / cnc 2 | 1/1/2 | ✓ |
| summary unattested | — | pass 0 / fail 1 / cnc 3 | 0/1/3 | ✓ |

**Not self-proving:** expected outcomes/shortfalls live hand-authored (with formulas) in `rectangle_case.json` (`arithmetic`, `expected_unattested`, `expected_attested`); `test_as1_matches_fixture_expectations` (test lines 156-180) reads them from JSON and asserts against module output. Nothing is generated by running the module. Confirmed.

## Duty 2 — Test adequacy vs AS-1..AS-8

Every acceptance scenario is bound by ≥1 mutation-sensitive test:
- **AS-1** — `test_as1_rectangle_coverage_fail_height_pass` (110), `test_as1_matches_fixture_expectations` (156): exact provided/required/shortfall/direction asserted; a comparison or shortfall mutation fails it.
- **AS-2** — `test_as2_unattested_street_width_could_not_check` (188) + attested (205) + real-registry (549, 518).
- **AS-3** — ceilings (294, 308), NaN/inf/-inf coord (324), NaN/inf area (340), DB-034(b) capped repr (348), finiteness helper unit (586).
- **AS-4** — `test_as4_summary_carries_could_not_check_count` (378): asserts exact summary dict + cnc≥1.
- **AS-5** — `test_as5_declared_mapping_and_unmapped_facts` (400): unmapped `boobytrap_input` and redundant `lot_area_sq_ft` caller copy dropped; lot area sourced only from LotContext; `test_as5_unsupported_family` (427).
- **AS-6** — `test_as6_no_scenario_document_or_contract_version_emitted` (464): source grep for 8 forbidden tokens + asserts report dict lacks scenario-emitting keys. Confirmed by my own read of the imports (only `app.scenario.derivation`).
- **AS-7** — `test_as7_deterministic` (499).
- **AS-8** — reproduced above.

### Unbound branches (probe-confirmed reachable and fail-closed; not exercised by the committed suite)

I wrote a standalone probe (`scratchpad/probe.py`, `PYTHONPATH=services/api`) that reached each branch:

- **G4-1 (advisory)** — `CouldNotCheckReason.AMBIGUOUS_RULE` is **untested**. `proposal_checks.py:585-591` (`_select_allowance`, `len(usable) > 1`). Probe (two coverage rules, same family, both usable) → `could_not_check / AMBIGUOUS_RULE`, `required_value=None` (never picks a winner). This is a **load-bearing D-076-R002 honesty branch** shipping without a regression test; current fixtures have one rule per family so it cannot arise today, but a mutation that let it pick a winner (a legal determination) would survive. Recommend a bound test (route to B3 or a follow-up).
- **G4-2 (advisory)** — `CouldNotCheckReason.NO_APPLICABLE_RULE` is **untested**. `proposal_checks.py:597-599`. Probe (rule applicable to R5, facts supply R6) → `could_not_check / NO_APPLICABLE_RULE`. Fail-closed, correct. AS-5's intent ("never a silent PASS") is covered by the tested `FAMILY_UNSUPPORTED` sibling; this sibling is uncovered.
- **G4-3 (advisory)** — `_compare` **MINIMUM** branch is never exercised via a commensurable check. `proposal_checks.py:610-611`. The only MINIMUM declared check (`rear_yard_depth`) is semantic-gapped → CNC before comparison; `test_provided_fact_absent...` (262) uses a MINIMUM probe but hits `PROVIDED_FACT_ABSENT`. Probe confirms correct output (PASS when provided≥required; FAIL shortfall = required−provided = 3.0 for 12 vs 15), but a mutation to the MINIMUM shortfall direction would survive the suite.
- **G4-4 (advisory, minor)** — two defensive branches untested: street-line coordinate finiteness `proposal_checks.py:460-462` (coordinate finiteness is tested only for `lot_line_segments`), and the `lot must be a LotContext` guard `429-432`. Per-field dotted paths that ARE tested: `lot.lot_line_segments`, `lot.street_lines`, `lot.lot_line_segments['LL'].start`, `lot.area_sq_ft`, `scenario_label`. NaN and inf both bound (coords: nan/inf/-inf; area: nan/inf).

None of these is blocking: every named AS is bound, and each unbound branch behaves correctly and fail-closed (probe-verified).

## Duty 3 — Regression: PASS (see reproduced evidence). Pre-existing rules/scenario suites untouched and green; no jsdom noise in scope (api-only task).

## Duty 4 — AS-1 disposition (report §4): concur

The `[ORCH-SCOPE-DISPOSITION]` is sound. A generic minimum wall-to-lot-line setback is not a rear-yard depth (no rear lot line is designated in the B1 facts; designating one is a legal interpretation the module must never make — permanent principle 1, D-076-R002, D-051). The suite proves the refusal **robustly against a rule that actually produces a number**: `test_rear_yard_not_commensurate_even_with_a_rule_present` (225, synthetic rule computes 5 ft) and `test_real_registry_residential_far_is_not_commensurate` (567, real rule computes 12000) both show `COULD_NOT_CHECK / PROVIDED_FACT_NOT_COMMENSURATE` with `required_value=None` and the provided fact still surfaced. The semantic gate fires FIRST (`proposal_checks.py:649-652`), so it is registry-independent (also proven by `test_as5_unsupported_family`, 427). Substituting the commensurable PASS (`building_height`, feet vs feet) + commensurable FAIL (`lot_coverage_ratio`, exact 0.125 shortfall) satisfies AS-1's intent while honoring the honesty boundary. This is a tested, deliberate outcome, not a coverage gap.

## Duty 5 — Fixture hygiene: PASS

- All four synthetic rulesets are clearly labeled `SYNTHETIC`, `status: needs_review` (never published), each with a `limitations` note "requires official capture + G6 before any use." No fixture claims Verified status.
- Provenance snapshot `pc-demo-synthetic.snapshot.json`: `official_channel: none`, `capture_method: synthetic_test_fixture`, explicit `verification_required` text stating it was NOT captured from any official source; `content_digest_sha256` validates offline (registry loads cleanly in tests). The excerpt says "30 feet" while `pc-rear-yard-demo` cites "5 feet" — deliberately documented in the snapshot `notes` (excerpt reused verbatim from `zr-demo-rear-yard` for offline digest stability; each rule states its own illustrative quote). Not a provenance defect.

## Modularity
`proposal_checks.py` is a single focused module below the warn tier; `modularity_check --check` exit 0; imports only read-only (`app.scenario.derivation`, `app.rules.coverage`, `app.rules.registry`). No forbidden-path edits.

## Summary
Arithmetic independently reproduced and exact; fixtures hand-authored and not self-proving; all AS-1..AS-8 bound by mutation-sensitive tests; full regression reproduced green; fixture hygiene clean; AS-1 disposition concurred. Advisory findings G4-1..G4-4 are test-adequacy gaps on correct, fail-closed branches (notably the AMBIGUOUS_RULE honesty branch, G4-1) — recommend binding tests in a follow-up, but they do not block acceptance.

**Verdict: PASS** (advisory G4-1..G4-4).

Assumptions/limitations: reviewed the ctl24 shared checkout (digest-confirmed identical to frozen material commit `ba0cf6b0`); could not `git diff` forbidden paths from the isolated worktree, mitigated by the digest-confirmed 9-file material scope plus green unchanged regression suites. Directive-compliance (D-066/D-076/D-077) and the G5 wiring-precondition verification are the DCV's and security-reviewer's scope, not this G4 pass.
