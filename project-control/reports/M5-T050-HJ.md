# M5-T050 HJ — independent human-journey review (human-journey-reviewer, saved VERBATIM from the agent return)

# G3 Human-Journey Gate Report — M5-T050 (DB-033 confirm-arc polish riders a–i)

**Reviewer:** human-journey-reviewer (read-only; agent-memory Write was blocked, so this return is the full report). **Pin verified:** `git rev-parse HEAD` = ee1b361ca30c8f66d94c7c84bd96c8f81645994b. Judged from JSX/copy/tests + my T047 walkthrough memory; no local npm (web proves in CI).

## VERDICT: PASS

**THE placement decision — ACCEPTED (bottom placement).** The record-address note (with the rider-d why-note inside it) was relocated to the card's last child, below Continue/Not-my-property and below the correlation-id footer (`AddressConfirmCard.tsx:420-430`). I accept it, and I stand by reversing the grouping I praised in T047, for this reason: the record note is **supplementary transparency, not a decision input.** A first-time analyst answers "is this the right lot?" from the matched frontage (which matches what they searched) plus the BBL — the city-record address never changes that answer. And the note is **self-contained**: it states it "can differ from the matched frontage" and immediately explains why ("a single tax lot can front on more than one street"), so an analyst who scrolls to it gets both the surprise and its reassurance together, while an analyst who presses Continue first proceeds correctly on their matched frontage. There is no journey in which not-seeing-it-before-Continue produces a wrong decision. Against that, the producer's engineering claim is sound: a below-everything append is the only mechanism that is CLS-neutral for the CTA *and* keeps a clean absent presentation (an in-flow reserve only approximates the unknown wrapped height; a max-height reserve leaves a permanent gap; an overlay overlaps). So the trade — stable CTA vs. grouped-but-shifting — resolves in favor of stability. Discoverable-on-scroll and fully honest down there.

**My three other T047 advisories are CLOSED:**
- **(b) aria raw-value exposure — honest for SR analysts.** `<strong role="img" title aria-label>` (`:222`). role="img" is a name-*permitting* role, so the aria-label (raw entered value) is now the accessible name — this fixes the exact T047 A2 gap (aria-label on a bare name-prohibited `<strong>` is ignored). Verified by S9 `:643-645` and S11 rider-b `:1083-1090`.
- **(c) no-normalized-street copy — coherent, no phantom reference.** Conditional note (`:229-231`): normal = "…alongside the city-matched address so you can compare them…"; no-street = "…as the address you searched for…". Read aloud both are clean; the no-street variant no longer points at the absent matched line (my T047 A3). Asserted S11 rider-c `:1093-1121`.
- **(d) why-they-differ wording — plain, neutral, record-not-allowance.** "A single tax lot can front on more than one street, so its address of record can differ from the frontage you searched." No number, no computed value (test asserts no digit, `:1132`); renders only in the differ case.

**Overall journey holds.** Everything above the actions is byte-identical to T047 except the named riders (b/c/i). Every failure path is honest and quiet: the client (`record-address.ts`) maps error/timeout/abort/network/unexpected all to a non-document outcome → no line; `credentials:"omit"` is set on the fetch. S10/S11 wait for the terminal `data-record-address-status` before asserting no-line, ruling out a false pass on the loading-null.

## Findings (all ADVISORY, none blocking)

1. **ADVISORY — IA inversion at the bottom placement.** The record note now sits *after* the correlation-id footer, so for a screen-reader user reading DOM order the debug footer precedes substantive lot-identity content, and the T047 three-line identity grouping is broken (matched/entered stay grouped at top; city-record is at the bottom). If a future refinement wants to improve reading order without reintroducing CLS: place the note immediately after the Not-my-property button but **before** `<Meta>` — that keeps every *interactive* element stable (only the non-interactive footer shifts one line on the async settle) while restoring content-before-footer order. The producer chose strict zero-shift instead; both keep it below the CTA, so this is optional polish, not a gate.
2. **ADVISORY — role="img" nuance (rider b).** The accessible name is whitespace-normalized (fine — surrounding whitespace carries no meaning to an SR), and some screen readers prefix "image"/"graphic" before a text value. Net improvement over T047; minor.
3. **ADVISORY — rider-c no-street copy is mildly redundant.** "You searched for **X**. We show it as the address you searched for…" repeats "searched for." Coherent, just slightly clunky in that rare path.
4. **ADVISORY — rider-i over-length divergence.** For an abusive >512-char paste, the title/aria value is truncated to 513+marker while the visible text shows the full length; benign, never bites a real address.

**Outstanding (already surfaced by the producer, not mine to gate):** the pixel-level CLS proof at 360/768/1280 incl. a long record address is a CI Playwright measurement out of this packet's allowed_paths — the jsdom S11 tests prove only the structural append (last-child, pure-append on delayed resolve, collapse-free non-shown `it.each`), which is the honest jsdom-provable guarantee.

## Basis
The one decision the producer surfaced resolves in favor of the bottom placement because the record-address line is post-hoc transparency rather than a Continue-gating input, and it self-contains its reassurance; the CTA-stability win of a correct-by-construction append outweighs the grouping/reading-order cost, which I log as a non-blocking advisory with a concrete before-`<Meta>` alternative. Riders b/c/d each close the corresponding T047 advisory honestly, and all error/absence outcomes remain silent and non-fabricating. No blocking findings.

[Orchestrator identity note at recording time: between the review pin ee1b361c and the gate-record head, the only commits are the M5-T051 contract-seam records — disjoint from every M5-T050 allowed_path; the task's material identity is byte-stable, per the reproduced empty diffs.]
