# M0-T123 — G0 readiness (orchestrator, administrative)

Recorded 2026-08-30 at the Amendment-19 capture tip. VERDICT: **PASS — ready to claim and
dispatch.**

1. **Authority:** owner authorization verbatim in `source-019-amendment.md` (rows
   D-024-R327..R347): S16.7 excess dispositioned (accepted measurement only, nothing
   granted, touch history preserved) + the bounded rotation-at-seam defect task and fifth
   R247 recert window authorized as the COMPLETE resume-path defect class. Qualifying
   evidence per the supervisor-freeze rule §2/§3: **reproduced live defect** — the cycle-2
   start resumed the 604,772-token session to `context_tokens 640,224` with no rotation
   and cwd on the primary checkout; rc=1; S14 `no_valid_checkpoint`
   (`project-control/reports/M0-T107-cycle2-live-journey.md`); authorized by D-024-R330.
2. **Packet completeness:** objective, 8 acceptance scenarios (AS-1..AS-8 covering the
   R342 12-item matrix, the R340 bypass-sensitive reachability proof, the R341 fixture
   discipline, and the R343/R344 terminal-evidence honesty), 20 allowed_paths (the
   launch/resume seams `session_continuity`/`loop_turnover`/`claude_runner`/`loop`/`cli`
   + a focused new `launch_seam.py` module option + fixtures dir + 8 test files + 2
   report files), forbidden paths (the live runtime dir and preserved transcripts are
   READ-ONLY fixture sources — never written), gates G0/G2/G3/G4/G5, 4-reviewer roster.
3. **Directive binding:** in-regime `D-024:ALL`; resolver dry-run ok=true — applicable =
   cited = **20 rows** (R327–R346 as bound), missing none. M0-T124 (recert +
   presentation, depends on this task) binds its 5 rows incl. the R347 stop.
4. **Preserved-evidence baseline (R329/R341 integrity anchor):** SHA-256 heads recorded
   BEFORE any producer work — journal `a4acb370f3a23fd5…`, audit `e80c057cabc24478…`,
   audit head sidecar `2ffdfe8ea5b39cb2…`, cycle-1 transcript (02b014ee)
   `3a0d1f30664b1deb…`, cycle-2 transcript (798d2f00) `3c9185687f12e86a…`. The identical
   hashes must hold at acceptance (read-only discipline is thereby machine-checkable).
5. **Window prohibitions restated (R345/R328):** no live-loop restart, no clear-recovery,
   no journal edit, no budget reset, no PR #241; the S16.7 disposition grants nothing;
   the R316 one-attempt consumption stands.
