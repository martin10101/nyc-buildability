# M5-T124 — G3 code review (code-reviewer "cr-t124", read-only)

> Transmission history: pinned at f2986fac (material c65a0724; CI green at f2986fac), delivered as 2 SendMessage parts
> ending with END-OF-REPORT; no truncation. Joined verbatim by the orchestrator (transport wrapper tags removed only).
> Verdict PASS; the math verified (proper rotation, centred sums, far-corner probe exact); four ADVISORY items (the docstring's
> input-contract wording, lot-ring distinctness, footprint-only outside-lot, a None outside-lot without a discrepancy row).

---

M5-T124 G3 review — VERDICT PASS (part 1/2): math correct + numerically robust, contract not bypassed, no blocking findings.

Independent G3 code review (READ-ONLY, HEAD pinned f2986fac; material commit c65a0724). Reviewed: drawing_alignment.py, test_drawing_alignment.py, the packet, producer report, the contract app/scenario/proposal.py, and both importers (READ-ONLY).

BLOCKING: none.

MATH (verified in source + independently probed in-process):
- The 2D rigid fit is the Kabsch / orthogonal Procrustes PROPER-rotation solution: cos=dot/hypot, sin=cross/hypot, translation=target_centroid − R·source_centroid (drawing_alignment.py:248-254; _compose :232-245). Proper rotation only, det=+1 (probed =1.000000000000); NO scale, NO reflection applied.
- Numerical robustness: source and target are CENTRED before the cross/dot/sumsq sums (:209-222), defeating the catastrophic-cancellation risk with 1e6-scale 2263 coordinates. Probed a rotation fit at the FAR corner of the NYC bounds (1.09e6, 2.9e5): max residual 0.0, vertex error 0.0, rotation exact (37.3°). Collinear-but-distinct picks (3 points on one wall) fit cleanly (residual 1.2e-10) — correctly accepted, since two distinct points already determine a rigid motion.
- Implied scale (:276-281) and mirror fit (:257-273) are computed SEPARATELY and disclosed only. Probed a genuine 0.5 implied scale: disclosed, local edge length preserved (never rescaled). Mirror disclosed, orientation preserved.

CONTRACT (not weakened):
- The aligned block is re-validated by the UNCHANGED validate_proposed_massing (:677); a still-invalid result is a typed aligned_invalid refusal naming the field (:679-681). Probed srid=2264 → refused at proposed_massing.outline.srid; a near-origin identity fit → aligned_invalid on the NYC bounds. The module imports proposal.py and never re-implements the contract.
- Every failure is a typed AlignmentRefusal VALUE. I traced every path — no exception escapes ("raises nothing" holds): fit degeneracy is guarded BEFORE _rigid_fit (coincident_source :648, indeterminate_rotation :654), _BadBlock and ProposedMassingError are caught (:671, :678), and all shapely work is try/except-guarded (:391-399, :412-416). Divisions by pair count are safe (>= MIN_CONTROL_PAIRS validated first).

Part 2 (honesty, tests, advisories, evidence, verdict line) follows.

---

M5-T124 G3 review (part 2/2): honesty, tests, 4 advisories, evidence, and the PASS verdict line.

HONESTY (D-076-R002 / D-083-R006): all discrepancies disclosed, never applied (residuals, scale, mirror, outside-lot each SHOWN only); precision label kept EXACTLY and never upgraded (:420-428, echoed unchanged); no permit/approved/maximum-allowed/survey wording (full-JSON scan test); the 2-pair fit disclosed as unchecked; user-confirmed pairs only — no corner auto-matching.

INPUT CONTRACT / OQ1 / DISC-A (verified as a real fact, NOT a defect): both importers' build_draft set outline.srid=2263 + kind=proposed and call validate_proposed_massing INTERNALLY (sheet_import.py:592; dxf_import.py same), so a local-frame draft is refused today (draft_invalid) — exactly DB-092. The aligner is duck-typed on the block SHAPE, imports neither importer, and re-validates the aligned block. Nothing here weakens the contract. The docstring (:11-15) + producer Deviation 1 / OQ1 / DISC-A document the future-wiring gap clearly.

TESTS: 39 pass (local 3.11). Each of the 10 in-process mutations patches a distinct seam and asserts the DISPLACED field moves (edge doubles, orientation flips, residual count drops, out-of-bounds returned ok, precision upgraded) — all load-bearing, none vacuous; exact 2263 vertices asserted; behaviour-level, no implementation coupling.

ADVISORY (non-blocking):
1. Docstring says input is "as returned by build_draft", but build_draft refuses a local frame today — it is the shape build_draft assembles BEFORE its own bounds validate. Already flagged in OQ1/DISC-A; a one-line note would aid the mount author.
2. _validate_lot_ring (:457) distinctness uses _distinct_ring, which removes only a closing dup, so [A,A,B] passes the "3 distinct" gate with 2 true points. Benign — a degenerate lot makes outside-lot disclose None, never a false zero; lot_ring feeds only the disclosed outside-lot area.
3. Outside-lot area is footprint-only; per-level outlines are transformed + revalidated but not outside-lot-checked.
4. When a polygon is unusable, outside_lot area is None with no discrepancy row — honest, but a caller reading only `discrepancies` could miss that the check could not run.

EVIDENCE (cwd services/api unless noted):
- ruff check . → All checks passed!
- pytest tests/drawings/test_drawing_alignment.py -q → 39 passed (Python 3.11.9)
- modularity_check --check (repo root) → EXIT=0; drawing_alignment.py NOT in warn list (~592 SLOC)
- CI on pushed head f2986fac (py3.12, authoritative): CI + context-budget + secret-scan all success
- material commit c65a0724 = exactly the 3 allowed paths; f2986fac = ledger-only (code frozen at reviewed head)

Agent-memory note skipped (Write disabled this session).

M5-T124 G3 VERDICT: PASS

END-OF-REPORT
