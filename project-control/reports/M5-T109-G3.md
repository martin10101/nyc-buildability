# M5-T109 — G3 code review (code-reviewer "cr-t109", read-only)

> Transmission history: pinned at 3cd8bdb0, delivered as four SendMessage parts (1/4..4/4) ending with END-OF-REPORT;
> no truncation. Joined verbatim by the orchestrator (transport wrapper tags removed only). Verdict PASS: writer output
> preserved, caps before any writer, one reconciled redacted refusal, filename safety solid, the [ORCH-CORRECTED]
> test_glb_writer AS-5 minimal and with teeth. Advisories A1-A5 (A1: the GLB cap fan triangulation is wrong for concave
> footprints - fail-open, must be fixed before PKT-H mounts GLB export) are routed at the accept seam.

---

M5-T109 G3 code review (READ-ONLY) — part 1 of 4. PIN: HEAD at start = 3cd8bdb0a98ff33171443badaca99ce15fc13705 (matches expected).

EVIDENCE (all run by me):
- `cd services/api && python -m pytest tests/cad -q` -> 429 passed in 9.46s (exit 0), incl. test_cad_owner_samples (owner DXF/PDF/GLB samples byte-identical, unchanged) and both writer goldens.
- `cd services/api && python -m ruff check .` -> All checks passed! (exit 0). (Producer DEV-3 reported 4 stale E501s in other packets' placeholders; those are gone at this HEAD.)
- `python tools/modularity_check.py --check` -> exit 0. Every warn line is a pre-existing file OUTSIDE this packet; export_service.py (505 SLOC) and export_api.py (302 SLOC) are under the 600 warn threshold and not listed.
- Material scope: 154f3abe = exactly the 7 allowed files; 419cdcee = only test_glb_writer.py (the ORCH-CORRECTED edit). No forbidden path touched; app/main.py untouched. Zero new deps (imports = stdlib + admitted fastapi/starlette; requirements not touched).

EXCEPTION-HIERARCHY CHECK (the one real correctness risk): the service catches `dxf_writer.DxfWriterError` (export_service.py:386) and `glb_writer.GlbWriterError` (:416). Confirmed both writers' concrete errors subclass those bases and carry `.code`: GlbValidationError(GlbWriterError) glb_writer.py:204; DxfValidationError/DxfSanitizationError(DxfWriterError) dxf_writer.py:228/240. So writer raises are caught and reconciled, not propagated. The PDF writer RETURNS SitePlanRefusal (:401-402). `getattr(exc,"code",...)` maps every writer code through `_reconcile` (:277-283), which builds a server-side detail and never echoes caller text.

Continued in part 2.

---

M5-T109 G3 — part 2 of 4. Per the review asks (all PASS unless noted):

SERVICE DESIGN
- Format dispatch preserves writer output: DXF/GLB writers + goldens not in the diff; goldens re-asserted green; DXF entry render_site_plan_dxf(lot,building,floor_heights,*,base_elevation) (dxf_writer.py:916) matches the service call (:380). PDF SitePlanInput fields match (:392-399). GLB GlbMesh/GlbLocalFrame/write_glb signatures match (:360-371,:415).
- Caps at the right level: _FORMAT_RING_CAP is the TIGHTEST per format (pdf 1024 / dxf 10000 / glb 10000, sourced from the writers' own constants, export_service.py:104-108); _raw_ring_length caps RAW len() BEFORE any coercion (:245-256, ordered before dispatch at :452-467); refuse, never truncate. AS-1 test seeds non-numeric vertices past the cap and proves ring_cap_exceeded fires first (test:78-86), with a reddening mutant (test:100-110).
- Reconciled refusal: one {reject_code,detail}; GLB raise and PDF return map identically (test:166-174); hostile coordinate never echoed (test:177-184).

FILENAME SAFETY (MANDATORY, AS-3) — solid. build_filename_token allowlists to [A-Za-z0-9._-], caps 80, returns None->fallback (export_service.py:190-202); content_disposition builds both ASCII filename + RFC5987 filename* (:205-214). Because token is allowlisted and ext is a fixed literal, no quote/;/CR-LF/non-ASCII can enter the header. Parametrized hostile-bbl test (quote+;, CRLF, non-ASCII, param injection) asserts the safe grammar + exactly 2 quotes/2 semicolons (test:205-229); reddening mutant passes the raw value and proves the leak (test:261-271). urllib quote of already-safe chars is a valid RFC5987 value — correct.

ROUTE (AS-4) — mirrors the accepted proposal_validation/max_envelope pattern. Flag gate FIRST -> generic 404 with no correlation id, byte-identical to unmounted (export_api.py:227-228,:155-158); include_in_schema=False (:221); reuses the accepted T053 bounded-stream primitives (:54-59,:238-251); off-loop run_in_threadpool under asyncio.wait_for deadline -> typed 503 (:279-290); 500 backstop never leaks str(exc) (test:225-233); per-format media types + nosniff on the 200 file (:188-200). Genuinely unmounted: independent grep shows no app/ importer of export_api and main.py includes lot_geometry+proposal_validation but NOT export.

Continued in part 3 (rate limiter, ORCH-CORRECTED, honesty, advisories).

---

M5-T109 G3 — part 3 of 4.

RATE LIMITER (_RateLimiter, export_api.py:103-143) — correct sliding-window-log: prune events older than the live window, refuse at limit, append after admit; LRU-evict past 8192 keys; window/limit read live for tests. Memory bounded (8192 keys x <=limit floats). Thread-safety: no lock, BUT allow() is called only on the event-loop thread (before run_in_threadpool) with no await inside, so it runs atomically vs other coroutines — safe in this usage. Reddening/behaviour tests: 429 (test:199-207), 503 (test:210-222).

ORCH-CORRECTED test (419cdcee, test_glb_writer.py:584-594) — minimal (5+/1-) and keeps teeth: asserts importers == ["app/cad/export_service.py"] (any other glb_writer importer fails) AND app/main.py contains neither "export_service" nor "export_api". Verified independently.

HONESTY / DETERMINISM — two source labels only; approved/permitted/maximum_allowed/as_of_right refused (test:290-294); no legal conclusion; generated_at is a caller input, never a clock; no-clock determinism proven per format (test:303-317). DB-075(a): pdf_sheet_writer _coerce_vertex catch broadened to (ValueError,TypeError)->non_numeric_coordinate (:353-360, diff is docstring + that branch only); hostile-__float__ test for both ValueError and TypeError (test_pdf_sheet_writer.py:756-766) plus a reddening mutant that reverts to the narrow OverflowError-only catch (:786+). PDF golden re-asserted unchanged (:131-132,:639-640).

ADVISORY findings (none blocking):
- A1 (recommend recording as a DISCOVERY): GLB cap triangulation is a fan from footprint vertex 0 (export_service.py:356-359), which is correct only for convex/star-shaped footprints. A concave-but-non-degenerate footprint (e.g. an L-shaped lot) yields geometrically-WRONG top/bottom caps that are NOT refused (fail-open); only degenerate (collinear/coincident) triangles are refused by the GLB writer. Honestly disclosed in report DEV-5 but not routed to DISCOVERIES, and no concave-footprint test exists. Acceptable now (route UNMOUNTED, massing is a visual "Proposed/Generated" option, no zoning value derives from it, robust triangulator is M5-T106/T107 scope). Recommend: before PKT-H mounts GLB export, either refuse non-convex footprints (fail-closed) or use the robust triangulator.

Continued in part 4 (remaining advisories + verdict).

---

M5-T109 G3 — part 4 of 4.

ADVISORY (continued, none blocking):
- A2 (PKT-H): the in-process _RateLimiter is per-process. A multi-worker deploy multiplies the effective per-caller limit by worker count and shares no state across processes — fine while UNMOUNTED, but public exposure (PKT-H) needs a shared store or a per-process budget. Also fold this and M5-T108's second limiter into one shared module (producer DISC-2 already flags this).
- A3: build_export does NOT re-allowlist `fallback_token` (export_service.py:480); safe today because both call sites pass a safe value (literal "export" / hex correlation id), but it trusts the caller. Consider allowlisting the fallback too, or documenting the "caller must pass a safe token" contract on the parameter.
- A4 (doc nit): the service docstring (export_service.py:38-40) says the GLB dedupe is disclosed "in the file's own asset.extras (via the writer)"; the GLB writer's asset.extras carries origin/axis/label, not a per-floor-cap note. The real disclosure is ExportResult.provenance["glb_massing"] (:494-496). Wording only.
- A5 (DB-075(c), routed-to-G3): the ISO 32000-1 §7.3.4.2 citation stays "[recalled - verify]" in pdf_sheet_writer._escape_pdf_text. The escaping unconditionally backslash-escapes ( ) and \ and ASCII-sanitises the rest — a conservative superset that always yields a balanced literal string, so this is correct regardless of the exact standard wording. The marker is documentation honesty, not a code defect. OK.

Tests test behaviour not implementation (mutations rebind the consuming namespace / cap constants — behaviour-level); modularity fine; exactly the allowed paths; zero new dependencies. The byte-frozen DXF/GLB writers and owner samples are untouched and green.

Nothing above blocks acceptance. A1 is the one item worth recording as a backlog discovery so PKT-H cannot mount GLB export for concave footprints without a fix.

M5-T109 G3 VERDICT: PASS

END-OF-REPORT
