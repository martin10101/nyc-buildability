# G3 Gate Report — M0-T011 "ADR-004: Frontend hosting on Render (drop Vercel)"

**Reviewer:** code-reviewer (independent; producer was cloud-architect)
**Date:** 2026-07-16 | **Commit reviewed:** 8e6bdf4 on main | **Mode:** read-only per ADR-005; this report is the reviewer's only write; gate recording is the orchestrator's action.

## Verdict: PASS

Six defects recorded — all LOW/MEDIUM residuals outside the producer's allowed paths or status-hygiene items; none is an operative instruction that still targets Vercel in any deploy, rollback, or secrets path, and none blocks acceptance. All five acceptance scenarios pass on independent re-execution.

## Summary

ADR-004 is an unambiguous, fully cited decision record: the Next.js frontend becomes Render web service `nycdf-web`, Vercel is dropped, the frontend inherits the accepted M0-T006 Actions-gated deploy model exactly, and the preview strategy is explicitly decided as "no previews initially" with a real cost basis (Pro-gating quoted from the orchestrator-captured research file; service-preview billing honestly declared unverified rather than guessed). The PRD §14.1 deviation is recorded per PRD §34 with the owner decision date, and the single billing-affecting item (any future preview enablement / Pro upgrade) is flagged as a HUMAN ACTION in a dedicated blockquote, not silently assumed. The M0-T006 G3 amendment map was applied at every listed location in ADR-001/002/003 plus both R1 residuals. render.yaml re-validated mechanically: parses, exactly four services, all four `autoDeployTrigger` values are the STRING `'off'`, and the diff shows the three pre-existing services changed only by the off-quoting plus comments — the `nycdf-web` block is pure addition. The repo-wide Vercel grep leaves only history, superseded-marked rows, immutable source docs, and a small set of out-of-scope residuals (below).

## Scenario results

| ID | Case | Expected | Actual | Verdict |
|---|---|---|---|---|
| S1 | normal | Decision matches owner directive; every Render claim cited to research file/official URL; preview strategy decided with cost basis; billing flagged human-only | Read ADR-004 cold before the producer report. Decision §1 unambiguous ("Vercel is dropped from the architecture; no Vercel account, project, vercel.json, or VERCEL_* secret is created"). Spot-checked 7 Render claims against `docs/research/render-nextjs-previews-2026-07-16.md`: (1) SSR Next.js = web service + build/start commands = §1; (2) preview environments Pro-gated, billed like regular services prorated by the second, `sync: false` not copied, auto-destroy on merge/close = §2; (3) service previews exist but plan/billing NOT captured — ADR says "no claim is made here" = §2 pointer; (4) `autoDeployTrigger` values/default/mandatory `"off"` quoting = §3; (5) `.next/cache` not persisted + `$XDG_CACHE_HOME` workaround = §1; (6) free-tier 15-min spin-down = official URL retrieved 2026-07-14 via accepted ADR-001 sources; (7) rollback-without-rebuild = ADR-003 R2 sources. **Zero memory-sourced claims found**; the edge/CDN row even cites "research file (absence)" and treats the benefit as lost rather than asserting an equivalent. Preview strategy: options (a)/(b)/(c) each costed; (c) adopted with re-evaluation triggers; Pro pricing explicitly NOT stated (owner to read render.com/pricing). HUMAN ACTION blockquote covers all billing. PRD §14.1 deviation recorded per §34 with owner decision date 2026-07-14. | PASS |
| S2 | boundary | render.yaml parses; nycdf-web additive; API/worker/cron untouched except off-quoting; all `"off"` values strings; staging/prod model preserved | Executed `python -c "import yaml; ..."`: parse OK; **service count 4** (web nycdf-api py / worker py / cron py / web nycdf-web node); **all four `autoDeployTrigger` == Python string `'off'`** (bare `off` would have parsed as `False` — quoting verified by parse result, not by eye); `previews: {generation: manual, expireAfterDays: 3}` unchanged. `git show 8e6bdf4 -- render.yaml`: existing-service hunks are exactly the three `off` → `"off"` one-liners + header/policy comments; nycdf-web hunk `@@ -168,3 +176,60 @@` is pure addition. Cross-checked `buildCommand: "npm ci && npm run build"` / `startCommand: "npm run start"` against `apps/web/package.json` (`build: next build`, `start: next start`) with `apps/web/package-lock.json` present (npm ci deterministic). `healthCheckPath: /` — root page exists (`apps/web/src/app/page.tsx`). envVars: only the two publishable `NEXT_PUBLIC_*` keys, `sync: false`, with PRD-17 never-privileged comment. | PASS |
| S3 | missing | Amendment map + both R1 residuals applied; no dangling Vercel in ADRs/runbook/README | Walked the M0-T006 G3 amendment map line-by-line against the diffs — every listed location amended: ADR-001 (title, context bullet, decision-table row, "no deviation" → documented-deviation bullet, dashboards bullet, secrets bullet, sources row superseded + research row added); ADR-002 (context bullet superseded-marked, all 3 mapping rows, anon-key row, VERCEL_* secrets row struck visibly, promotion rules 1–3, cost sequencing, verification item 3 withdrawn/replaced, sources); ADR-003 (context, D1 replaced in full, D4, D5 `deploy-vercel` job removed, failed-migration step 1, R1 replaced in full, decision-matrix rows, consequences env-var bullet, 4 sources rows superseded-marked). **R1 residual (a)**: ADR-001 rule 5 now "Render secret stores only... not duplicated to GitHub Actions secrets" ✓. **R1 residual (b)**: `"off"` quoted in render.yaml values AND in ADR-002 §2/rule 3, ADR-003 D1/step 1, runbook §0 prose ✓. Repo-wide grep adjudication below: zero unmarked operative references in ADRs, runbook, README, render.yaml. | PASS |
| S4 | conflict | Frontend gated identically to backend; runbook consistent; rollback concrete | Runbook §0 prod row: `nycdf-web` in same protected project env, `autoDeployTrigger: "off"`, deploy only via its secret deploy hook from the same Actions pipeline — textually mirrors the API column. Deploy order updated to "1) migrations, 2) Render API/worker/cron, 3) Render frontend"; §1.2 step 4 folds the frontend into the same deploy-hook chain (former Vercel CLI step removed); ADR-003 D5 now has one `deploy-render` job including nycdf-web. The M0-T006 Defect-1 resolution is **preserved, not weakened**: failed-migration step 1 (ADR-003 + runbook §2.5) still names the D5 `needs:` chain as the only halt mechanism and now says "trigger no deploy hook — frontend included". Rollback §2.1 replaced with a concrete, executable-as-written Render rollback for `nycdf-web` (dashboard path + API URL `api-docs.render.com/reference/rollback-deploy` — verified this citation pre-existed in ADR-003 L71/L123, not newly memory-sourced), with the R2 env-group caveats and the env-var-incident redirect to §1.3. Failure aspect: a failed hook deploy cannot take traffic (health-check gating 2xx/3xx in 5 s, cited) and §1.2 mandates stop-at-first-failure; recovery = §2.1 rollback or fix-forward. No contradiction found between ADR-002/003/004, render.yaml, and the runbook. | PASS |
| S5 | regression | ADR-004 explicitly supersedes the Vercel plan; B-003 closeable; PRD deviation acknowledged | ADR-004 Consequences: "B-003 (Vercel account) can be closed: this ADR supersedes the Vercel plan (orchestrator action after acceptance)"; rollback path defines B-003 reopening if reversed; dedicated "PRD deviation (explicit)" section. `project-control/blockers/B-003-vercel-account.json` and `docs/HUMAN_ACTIONS_REQUIRED.md` §3 already record the hold and await orchestrator closure — nothing in the deliverable set contradicts closure. B-003 is closure-ready on acceptance. | PASS |

G3 standard checks: normal (S1 cold read — a devops engineer implementing D5 gets an exact job list: one `deploy-render` job, one more deploy-hook secret, no `deploy-vercel`), boundary (S2 YAML-1.1 `off` semantics verified by parser, not inspection), missing/ambiguous (service-preview plan-gating declared unverified instead of guessed; Pro pricing deferred to owner), failure (failed migration → no hooks incl. frontend; failed hook deploy → health-check gate + §2.1 rollback). Clarity for a reader outside the session: good — every amendment is dated and back-referenced ("amended 2026-07-16 per ADR-004"), and superseded text is marked rather than silently deleted. No hidden defaults that materially change behavior (nycdf-web omits `ENVIRONMENT` — unused by the placeholder frontend; no `NEXT_PUBLIC_API_URL` yet — consistent with `apps/web/.env.example`, an apps/web-scoped follow-up when the frontend consumes the API). No large or persistent artifacts written anywhere: text-only commit (~350 lines, < 25 KB), no installs, no account operations.

## Repo-wide Vercel grep — residual adjudication (47 files)

Allowed categories (clean): ADR-004 itself (decision/trade-off/rollback-path record); ADR-001/002/003, runbook, README, render.yaml (every remaining mention explicitly superseded/withdrawn/"Vercel dropped"); PRD.md + docs/PRODUCT_FLOW_AND_AI_BOUNDARIES.md + docs/LOW_STORAGE_CLOUD_DEVELOPMENT_POLICY.md + docs/IMPLEMENTATION_SEQUENCE.md (immutable source docs — same class as PRD); project-control tasks/reports/blockers + docs/BOOTSTRAP_REPORT.md + docs/research/* + tools/enrich_bootstrap_tasks.py (immutable history/fixtures); .claude/agent-memory/* (reviewer memory, self-maintained); disclosed residuals docs/SECRETS_POLICY.md and apps/web/.env.example (producer-forbidden paths, follow-up task recommended in the producer report); docs/SESSION_HANDOFF.md, docs/IMPLEMENTATION_STATUS.md, docs/CONNECTION_AUDIT.md, docs/HUMAN_ACTIONS_REQUIRED.md, docs/MASTER_EXECUTION_PLAN.md (orchestrator-owned status/plan docs that the orchestrator updates at acceptance together with B-003 closure).

Hits NOT covered by the allowed categories → defects below: `services/api/.env.example:15-16` (undisclosed), `CLAUDE.md:23`, `README_START_HERE.md:8,28,56`, `.claude/agents/cloud-architect.md:3`.

## Defects (numbered; none blocking)

1. **MEDIUM (carry-forward; owner/orchestrator-owned) — CLAUDE.md:23** — permanent principle 5 still states "Vercel for the Next.js frontend" as current architecture, and this file is injected into every agent session, so future agents will read a provider set that contradicts ADR-004. Outside the producer's allowed paths. Fix: orchestrator/owner amend principle 5 to "...Render for FastAPI, workers, cron jobs, long-running processing, and the Next.js frontend (ADR-004)".
2. **LOW — services/api/.env.example:15-16** — comment says `VERCEL_TOKEN`/`VERCEL_ORG_ID`/`VERCEL_PROJECT_ID` "live in GitHub environment secrets"; those secrets are now never created. Undisclosed residual (producer disclosed only SECRETS_POLICY.md and apps/web/.env.example). Fix: strike the three VERCEL_* names in the same cleanup follow-up task.
3. **LOW — README_START_HERE.md:8,28,56** — pack overview states "Supabase + Render + Vercel + GitHub" and "Frontend deployments on Vercel" as current architecture. Fix: same cleanup task (3 lines).
4. **LOW — .claude/agents/cloud-architect.md:3** — agent description names Vercel among the domains it designs. Fix: drop "Vercel," from the description line.
5. **LOW — docs/SECRETS_POLICY.md L18/L20/L21/L26/L27/L44/L53 and apps/web/.env.example L3-4** — disclosed by the producer; recorded here so the follow-up task is tracked, not just recommended: re-point frontend rows at Render `nycdf-web` env vars, strike VERCEL_* rows, fix the .env.example header.
6. **LOW (status hygiene, pre-existing) — docs/adr/ADR-001/002/003 headers** still read "Status: Proposed (pending G3 gate review)" although M0-T006 was accepted 2026-07-15, while new ADR-004 says "Accepted"; inconsistent status semantics across the ADR set. Fix: orchestrator flips ADR-001/002/003 status lines to Accepted (with date) when recording this gate.

## Carry-forwards for the D5 deploy-workflow task

- One `deploy-render` job only (no `deploy-vercel`); it must call **four** deploy hooks — API/worker/cron then `nycdf-web` last — each with `ref=<validated commit SHA>`; one new GitHub `production` secret: `RENDER_DEPLOY_HOOK_URL` for nycdf-web.
- The halt-on-failed-migration property still exists ONLY in the D5 `needs:` chain (M0-T006 Defect-1 resolution preserved); D5 remains a precondition for the first production deploy.
- Verify at staging instantiation: staging copies of all four services get `autoDeployTrigger: commit` (ADR-002 verification item 1, unchanged).
- Before any preview enablement: fresh capture of render.com/docs/service-previews (plan-gating/billing unverified — ADR-002 verification item 3 replacement); Pro pricing owner-read from render.com/pricing.
- `[confirm at first use]` residual (pre-existing): whether Blueprint syncs of `render.yaml` changes apply automatically with `autoDeployTrigger: "off"` (runbook §3 infra row).

## Commands run

1. `git show 8e6bdf4 --stat`; `git show 8e6bdf4 -- <each file>` — full diff review.
2. `python -c "import yaml; ..."` on render.yaml — parse OK; 4 services; all `autoDeployTrigger` string `'off'`; outputs match the producer's S2 evidence independently.
3. Grep `vercel` (case-insensitive) repo-wide (47 files) + targeted content greps for adjudication; grep `api-docs.render.com|rollback-deploy` to confirm the runbook §2.1 citation pre-existed in ADR-003.
4. Reads: task packet M0-T011.json; research file; M0-T006-G3-verification.md (amendment map + R1); all four ADRs; render.yaml; runbook; README; apps/web/package.json + .env.example + directory listing; producer report READ LAST.

**Recommended orchestrator action:** record G3 PASS; accept M0-T011; close B-003; apply Defect 1 (CLAUDE.md) and Defect 6 (ADR status lines) directly; contract one small doc-cleanup task for Defects 2–5; update SESSION_HANDOFF / IMPLEMENTATION_STATUS / MASTER_EXECUTION_PLAN / HUMAN_ACTIONS_REQUIRED §3 at acceptance.
