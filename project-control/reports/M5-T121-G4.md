# M5-T121 — G4 test review (qa-engineer "qa-t121", read-only)

> Transmission history: pinned at ed229971 (material d865d977; CI run 36113132489 green at ed229971), delivered as
> 3 SendMessage parts ending with END-OF-REPORT; no truncation. Joined verbatim by the orchestrator (transport
> wrapper tags removed only). Verdict PASS; 6 of 7 independent mutants caught; four ADVISORY items (A1 the
> last-edge scale untested, A2 the display cap untested at value level, A3 honesty strings asserted by value, A4 weak
> never-parsed / scale-bound tests).

---

M5-T121 G4 test/integrated-regression review (qa-engineer, READ-ONLY) — PART 1/3

PIN: `git rev-parse HEAD` at start = ed229971 (expected ed229971+ — OK). Material: sheet_import.py NEW + test_sheet_import.py NEW (31 tests incl. 8 in-process mutants) + producer report. Memory write BLOCKED (qa-t121 read-only) — skipped per rules.

METHOD: ran the suite from services/api → 31 passed (0.59s, py311); loaded the real test module in-process and ran its 23 zero-arg guards under 7 of MY OWN mutations (distinct from the producer's 8), patching the consuming namespace; suite green under a mutation = SURVIVED, ≥1 red = CAUGHT. Baseline: 23 guards, 0 reddened.

AS COVERAGE (every AS has an ASSERTING test, not just a run):
- AS-1: stable-index candidates {1,2} not 0..k, total_closed_rings==2, measured units/bbox_width==200/area==40000 (test_candidates_list_closed_rings_by_stable_index...); open + degenerate disclosed; scale notes escaped+listed; out-of-range + no-closed-ring typed; overflow→coordinate_out_of_range; reader refusal→escaped unreadable_pdf. OK
- AS-2: service-measured edge EXACT (measured==40.0, scale==2.0, outline lands on _RING); NO client channel (dataclass has no measured_length); invalid known_length / bad edge / degenerate edge / bad candidate all typed. OK
- AS-3: assigned ring scaled, closure explicit (verts[0]==verts[-1]), srid 2263, kind stays 'proposed'; USER-ASSIGNED ring not first/largest (the M5-T108 F1 lesson — 2 rings, ring A larger @idx0, building_outline=1 → ring B) + 2 reddening mutants; draft_invalid on the near-origin contract violation naming proposed_massing.outline...; bad index → bad_candidate. OK
- AS-4: input_class asserted vs the LITERAL "imported_pdf"; precision/label/frame_note/page/scale/roles present; forbidden-words scan; precision-not-upgraded mutant. OK
- AS-5: no-persistence (two equal calls); git diff confirms ONLY the 3 material/ledger files, no forbidden path (dxf_import, reader modules, app/scenario untouched); ruff clean.

REGRESSION: DXF suites test_dxf_import.py + test_dxf_reader.py + test_dxf_roundtrip.py → 90 passed, unchanged. Deterministic (pure; no clock/random). CI (py312) is running at the superseding M5-T122 head 6d5a2623 (which includes these files) — orchestrator records it.

Continued PART 2/3.

---

M5-T121 G4 — PART 2/3 (my independent mutation table; consuming-namespace patch, run against the full zero-arg guard suite)

| # | My mutant (beyond producer's 8) | Result | Reddened by |
|---|---|---|---|
| M1 | drop explicit closure (_outline_vertices → no closing vertex) | CAUGHT | test_draft_built...explicit_closure +5 |
| M2 | off-by-one: measure the NEXT edge | CAUGHT | test_scale_is_measured...exact_arithmetic +7 |
| M3 | closing-edge off-by-one: points[i+1] w/o `% n` (CLEAN; zero-guard kept) | SURVIVED | — (A1) |
| M4 | swap scale numerator/denominator | CAUGHT | test_scale_is_measured... +4 |
| M5 | drop display cap (MAX_LISTED_CANDIDATES→1e6) | SURVIVED | — (A2) |
| M6 | leak control byte (_escape_drawing_text→identity) | CAUGHT | test_reader_refusal...escaped +2 |
| M7 | accept OPEN rings as candidates (_closed_ring_candidates→all) | CAUGHT | test_open_polyline... +3 |

6/7 CAUGHT. The producer's 8 in-suite mutants are non-vacuous (each applies a monkeypatch and asserts the FLIPPED outcome; all are in the 31-pass run).

DIRECT PROBES on real code:
- Closing edge is correct: _measure_edge(_RING, idx 3) = 100.0, but a pure-modulo regression IndexErrors at idx 3 (n-1) — untyped, uncaught. No test uses scale_edge=n-1 (source scan confirms).
- Cap LOGIC is correct though untested: cap=1 over 2 rings → lists 1, total_closed_rings discloses 2.
- Scale-note NON-parsing is STRUCTURAL: build_draft/_resolve_scale never reference text_runs; a misleading "SCALE 1:100" note present on the page leaves the geometry-derived scale at exactly 2.0. The existing test's own "never parsed" assertion (result.ok and isinstance tuple) is weak, but the property holds by construction.
- Resolved-scale MIN/MAX bound WORKS (a tiny 1e-9 edge → scale 1e12 > MAX → invalid_scale) but no test pins it; backstopped by the contract's finite/bounds check.

Continued PART 3/3.

---

M5-T121 G4 — PART 3/3 (fixture realism, honest-limit, findings, verdict)

FIXTURE REALISM: keyword-constructed Sheet* values match the frozen reader dataclasses field-for-field (SheetPolyline/Page/TextRun/Image/Document/Refusal). Closure convention is correct — the reader sets closed=True WITHOUT duplicating the first vertex (sheet_primitives.py SheetPolyline docstring), and fixtures use distinct points (4-pt rectangles → 4 edges). measured_dimensions reused correctly (perimeter 360, area 8000 on _RING). No realism gap; no test would pass on fixtures yet fail on real reader output.

HONEST-LIMIT: the near-origin local ring is pinned (test_out_of_bounds_local_ring_refused_by_the_contract → draft_invalid, field proposed_massing.outline...). The detail is the contract's generic "outside plausible NYC EPSG:2263 bounds — likely a wrong-unit or wrong-CRS"; honest and correctly field-named, but not sheet-import-specific (the "local sheet frame; alignment is a later step" wording lives only in the provenance frame_note, which a refusal doesn't carry). Same as accepted DXF DISC-A/B.

FINDINGS — all ADVISORY, none BLOCKING:
- A1 (M3): closing-edge (scale_edge=n-1) untested. Real code correct; a modulo off-by-one ships the whole suite green AND raises an untyped IndexError on a realistic input (user names the ring's last edge). Add a last-edge scale test.
- A2 (M5): display cap untested at value level (no >cap fixture). Add a >MAX_LISTED_CANDIDATES candidate-count test asserting len(candidates)==cap while total_closed_rings discloses the true count.
- A3: precision/label honesty asserted vs the by-VALUE imported dxf constant, not a literal (input_class DOES assert the literal). Non-tautological against a build_draft regression and against the runtime mutant, but a shared-source-constant edit would not redden locally; backstopped by the forbidden-words scan + dxf_import's own literal tests. Optional: assert the literal strings too.
- A4: resolved-scale MIN/MAX bound + scale-note "never parsed" have only weak/no direct tests; both hold structurally / are contract-backstopped.

RATIONALE: every CENTRAL AS behavior is constrained (the M5-T108 F1 gap is closed here), 6/7 independent mutants caught, regression green, fixtures realistic. The survivors are boundary/defensive with correct code and external backstops — not the central-uncovered-behavior the precedent bar FAILs on.

M5-T121 G4 VERDICT: PASS

END-OF-REPORT
