# M5-T105 — G5 security review (security-reviewer "sec-t105", read-only)

> Transmission history: pinned at 259b6d40, delivered as two SendMessage parts (1/2, 2/2) ending with END-OF-REPORT;
> no truncation. Joined verbatim by the orchestrator (transport wrapper tags removed only). Verdict PASS: no refusal echoes
> caller text, 27 hostile address probes all parse balanced, screen order correct. One LOW advisory (F1: a custom
> numbers.Real whose __float__ raises a non-overflow error escapes; not reachable from JSON) is routed to PKT-D.

---

M5-T105 G5 security review (part 1/2) — VERDICT PASS with one LOW advisory. Read-only; HEAD pinned 259b6d40 (matches expected). Material commit e294cf1f verified to touch exactly the 3 allowed paths (app/cad/pdf_sheet_writer.py, tests/cad/test_pdf_sheet_writer.py, reports/M5-T105-producer-report.md); submit commit 259b6d40 touches only control-plane files (orchestrator scope), and pdf_sheet_writer.py is byte-unchanged between them. Method: full source read + in-memory hostile-input probe run from services/api + the tests/cad suite.

SCOPE & INVARIANTS — all PASS
- Zero new deps: imports are math, numbers, collections.abc, dataclasses, app.cad.claim_words (accepted). No new import.
- Unwired: app/cad/__init__.py states nothing imports the package; grep + code-graph confirm only tests import pdf_sheet_writer — no route/main.py.
- Byte-identical: `python -m pytest tests/cad -q` = 368 passed, incl. test_golden_sha256 and test_cad_owner_samples.py (committed sample PDF unchanged).
- Log / refusal redaction: no refusal detail echoes caller text. text_too_long names only the field ("address exceeds the 120-character limit"); claim_class_word returns the BARRED canonical word (from CLAIM_CLASS_WORDS), not caller text; invalid_text / non_finite_* / oversize_input name field or class only. Verified live.
- Cross-tenant isolation, service-role secrecy, private storage, SSRF, upload controls, least privilege: N/A — pure dependency-free function, no I/O, no network, no tenant data.

CHECK 1 — length bound on EVERY caller string: PASS
The 4 caller text fields (address, bbl, generated_at, generator_version) are the ONLY caller-controlled strings that reach the PDF; all other title-block/edge/scale/north text is static or geometry-derived (confirmed in _build_content/_draw_title_block/_label_edges). All 4 are screened in _screen_caller_text BEFORE any drawing (no partial output). _ascii_sanitise is a strict 1:1 char map (ch or "?") so it CANNOT expand length — the raw-len cap therefore also bounds the sanitised length. _escape_pdf_text at most doubles length (each ( ) \ -> 2 chars), still bounded. Worst-case input (2x1024 vertices + 4x120-char hostile text) rendered to 118,776 bytes. Output size is bounded for the worst caller input.

Part 2/2 follows with injection, screen-order, never-raise, and the finding.

---

M5-T105 G5 security review (part 2/2) — injection, screen order, never-raise, and the one finding.

CHECK 2 — PDF literal-string injection: PASS
Probed 27 hostile address values: bare and unbalanced parens, backslash, trailing backslash, "A) Tj ET 0 0 0 rg (B" operator-breakout, ") Tj 100 100 m ... S (", octal \051\050, %comment, <</Evil>>, CR/LF, NUL, DEL(0x7F), high bytes 0x80/0xFF, U+2028/U+2029, and 60x each of ( ) \. For EVERY case the content stream parsed with all literal strings balanced, zero stray ( or ) outside a string, and the sanitised text recovered intact. _ascii_sanitise maps CR/LF/NUL/tab/DEL/high bytes -> "?" (kills the newline-continuation and NUL vectors); ( ) \ are unconditionally escaped (ISO 32000-1 7.3.4.2 conservative superset). No breakout is possible.

CHECK 4 — screen order (length -> claim -> escaping): PASS
Per field: type -> length -> claim-word (pdf_sheet_writer.py:279-291); escaping happens later at draw time. Length-first also keeps the separator-collapse regex off an unbounded string. The claim screen tests BOTH the raw value and its ascii-sanitised form, so a claim word hidden behind a non-ASCII separator (prints "?", collapses to a space in claim_key) is still caught. No order bypass.

CHECK 3 — never-raise contract: PASS with 1 LOW advisory
Confirmed typed refusals with NO raise for: non-str text x7 (invalid_text), nan/inf on x AND y (non_finite_coordinate), huge int 10**400 (oversize_input via caught OverflowError), non-sequence / nested / wrong-arity / str rings, and non-SitePlanInput spec. The _num non-finite path (DB-053 b) correctly raises _RenderRefused caught at _finish -> returned refusal; docstrings now honest.

FINDING F1 — LOW / ADVISORY (non-blocking) — services/api/app/cad/pdf_sheet_writer.py:339-344
_coerce_vertex wraps `float(vertex[0]), float(vertex[1])` in `except OverflowError` only. A numbers.Real subclass whose __float__ raises a NON-Overflow error (ValueError/TypeError) escapes the public API — I triggered a live ValueError, violating the module docstring's absolute "never raises on caller data" (lines 28-29).
Reachability: NOT the export-route surface this packet guards. Geometry is server-derived EPSG:2263, and a JSON caller cannot construct a custom numbers.Real (JSON yields only int/float/str/bool/None/list/dict). It is also outside AS-2's enumerated malformed set (non-numeric/wrong-arity/None/nested), which all pass. Worst impact = a request-scoped 500; no leak, injection, or cross-tenant effect.
Remediation (fold into PKT-D geometry wiring): broaden to `except (OverflowError, ValueError, TypeError)` (or catch Exception) returning a typed non_numeric_coordinate/oversize_input refusal, and add a hostile-__float__ test.

M5-T105 G5 VERDICT: PASS
END-OF-REPORT
