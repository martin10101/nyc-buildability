# M5-T020 — G0 Contract/Packet Gate

Gate: G0 (packet completeness). Result: PASS. Recorded by: orchestrator (contract gate;
implementation producer will be a dispatched backend-engineer agent ≠ gate reviewers).

## Checklist

1. **Exact requirement identifiers** — D-040-R001 verbatim; the pinned research deliverable
   `project-control/reports/D-040-R001-mappluto-research.md` (live FeatureServer facts,
   condo/multipolygon/accuracy limitations, packet implications) is inputs[0]; expansion-hold
   §2.1 scope named.
2. **Exact directive references** — `D-040:D-040-R001`, bound via append-only applicability
   (digest a3fc9d01 → a4145aeb, audit-logged); `evaluate_task_refs` ok=true, applicable ==
   cited == ["D-040-R001"]; validator EXIT 0.
3. **Exact source/evidence files** — eight pinned inputs incl. the BYTE-IMMUTABLE authoritative
   connector (read-only reuse by import), the M2-T009 fixture/manifest pattern and canonical
   BBL set, the body-less flag-gated route precedents, the two-copy contract + TS generator
   pattern, and the source-registry draft posture.
4. **Exact allowed/forbidden paths** — 12 allowed (2 new modules, additive main.py mount, the
   new contract in both copies + generated TS + contract fixtures, recorded fixtures dir, 2
   test files, the registry draft, own report); forbidden fences the authoritative 2263
   connector (byte-immutable), config.py (no new flag), scenario/profile/rules/spatial,
   apps/web (the MapLibre half is a separate packet), scenario contracts, dependency files,
   CI config, control plane. No overlap with any in-flight task (none in flight).
5. **Acceptance scenarios** — S1–S6: single-lot 200 with full provenance/display-only/accuracy
   discipline; the four typed outcomes honest (condo-unit empty, billing merged, multiple
   never-first-pick, invalid typed); route posture (flag-off 404, injection-proof bbl); the
   2263 discipline untouched (grep-provable no-area-from-4326); recorded-official live-capture
   fixture rule with manifest; contract two-copy + renderer-parity + full regression.
6. **Gates & reviewers** — G0/G1/G4/G5 with data-contract-verifier (G1, the M2-T009 connector
   precedent), qa-engineer (G4), security-reviewer (G5); all ≠ producer backend-engineer.
   No UI in this packet → no G3.
7. **Modularity answers** — in path_notes (new focused transport module importing the
   immutable connector's public names; route its own module; main.py additive; the
   display-only measurement boundary stated).

## Notes

- The MapLibre dependency admission and the confirm-card rendering are the two FOLLOW-ON
  packets; this server half has no dependency change (package files forbidden).
- Fixture capture is live HTTP against the keyless public FeatureServer — the same posture
  that produced the accepted MPG01–MPG07 set; never derived/invented bytes.

VERDICT: PASS — packet is claimable.
