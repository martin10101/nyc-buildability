# M5-T049 G4 — independent QA review (qa-engineer, saved VERBATIM from the agent return)

G4 QA review — M5-T049 (named_street_override.py pure-extraction split). VERDICT: PASS. Reviewed at HEAD f3bf4fd3 (verified). Material cherry-pick b5d3343f IS ancestor of HEAD; task-branch commit d52b1402 is not (expected — consistent with source-sections.md).

BASIS: The pure-extraction claim is proven two independent ways, not just by green suites. (1) AST byte-identity: all 26 top-level symbols of the original file (git show e04e6aad:...named_street_override.py, 853 lines) are present across the two new modules — 18 in _table, 8 in _matching — with ZERO byte-different source segments, zero missing, zero duplicated across modules, zero added (decorators included in the comparison). (2) Facade completeness: all 18 consumer import-bindings across the 4 files resolve through the facade at runtime and are all in __all__; zero unresolved. The acceptance suite is byte-identical to baseline (empty git diff e04e6aad→HEAD). Executed suites at HEAD match every claim.

FINDINGS:
1. [BLOCKING-check → CLEAR] Test reconciliation. `git diff e04e6aad HEAD -- tests/rules/test_named_street_override.py` = EMPTY. Acceptance suite: 46 `def test_` / 63 collected — exactly the pin's expected counts, zero edits, not in the diff.
2. [CLEAR] Moved-boundary coverage. test_named_street_override_table.py imports DIRECTLY from app.rules.named_street_override_table (12 tests); test_named_street_override_matching.py imports DIRECTLY from ...named_street_override_matching (2 tests) — 14 collected, exercised against the impl modules, not the facade. The re-export identity test (test_named_street_override_matching.py:36) uses `is` (object identity) to prove facade.X IS table.X / matching.X for all 17 names, and that the engine applies the SAME helper objects it imports from _table. Genuine structural proof.
3. [CLEAR] Executed at HEAD (from services/api): `pytest tests/rules -q` → 697 passed in 23.59s; `ruff check .` → All checks passed!. From repo root: `modularity_check.py --check` → exit 0, failures 0, warnings 20, NONE referencing named_street/_table/_matching (AS-3 size requirement met, matcher warn cleared).
4. [CLEAR] CI run 35435873943 (all-green claim) is consistent with everything I could reproduce locally (rules 697 / ruff / modularity exit 0). tests/spatial (93) and tests/api (439) were outside my permitted local command set so I did not re-execute them — they are covered by that CI run and are guaranteed unable to diverge given the production-code byte-identity + unchanged consumer imports (finding 1/facade-completeness).
5. [CLEAR] Missing-coverage sweep. Grepped every `from app.rules.named_street_override import` site: named_street_override_status.py (MatchStatus, NamedStreetOverrideMatcher, OverrideQuery, _normalize_cd), wide_street_live_provider.py (NamedStreetOverrideMatcher, OverrideQuery, load_default_matcher), test_named_street_override.py (7 names), test_wide_street_wiring.py (4 names) — ALL resolve, all in __all__. No `import *` of the module anywhere (so __all__ is non-restrictive). No name importable pre-split but absent post-split. MATCHED_OVERRIDE production-unreachability is preserved by construction (NamedStreetOverrideMatcher body byte-identical).

ADVISORY (non-blocking):
A1. Producer report §3 calls the facade __all__ "unchanged (same 17 names)". The pre-split original at e04e6aad had NO __all__ at all — the facade introduces it. Harmless (no import-* consumers; every explicit import resolves), but "unchanged" means unchanged vs an intra-worktree increment, not vs the committed baseline. Wording only.
A2. Pin item-1's premise that the 6 normalization tests have "pre-move versions" to diff at e04e6aad is inaccurate: at e04e6aad BOTH focused test files were docstring-only placeholders (verified via git show). The 14 focused tests are entirely ADDITIVE — there were no prior assertions to weaken or delete. The real byte-identity proof is the unchanged acceptance suite (finding 1). The 6 normalization tests carry concrete assertions incl. fail-closed cases (_normalize_cd bool/ambiguous→None, _word_bounded partial-word rejection, _bounded_repr truncation), no weak coverage.

Note: I could not save a QA-method memory — this reviewer identity is operationally read-only (Write and shell file-writes blocked, including under .claude/agent-memory/). No impact on the verdict.

PASS — pure extraction confirmed byte-identical; facade preserves the full import surface; all documented suites green at HEAD; two advisories are report-wording only.

[Orchestrator note on A2: accurate — the "moved" framing in the producer report and the review pin describes the relocation BETWEEN in-task increments (run 51 → runs 52-53 working set), not vs the committed baseline; at e04e6aad both focused test files were placeholders, so the 14 focused tests are additive vs baseline. No evidence file relies on the contrary reading.]
