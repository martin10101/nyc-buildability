# M5-T071 — G0 readiness (orchestrator, seq 125, 2026-09-22)

Task: T066-wave hardening — DB-047(d) no-map instruction-copy gating, DB-047(e) Convert
finiteness gate, DB-048 flaky count-assertion hardening. Drawing-surface only.

## Readiness checks

1. **Dependency accepted:** M5-T066 (250th) is `accepted`; the drawing surface this packet
   hardens is frozen acceptance evidence being deliberately amended under a new packet.
2. **Directive regime:** refs D-082 (R001), D-076 (R002 — the copy-honesty clause DB-047(d)
   enforces), D-066 (R001), D-077 (R002/R003); applicability bound at this seam with
   same-commit digest resyncs; `evaluate_task_refs` ok, applicable == cited (5 ids).
3. **Code-graph seam duty (D-066-R001):** graph REGENERATED at this seam
   (799 files / 16885 nodes / 7381 edges); the navigation block pins the load-bearing fact:
   ProposalOutlineDraw's consumer is ProposalEditor — a LIVE M5-T070 lane file, FORBIDDEN
   here — so the packet carries the finite-flow byte-compatibility wall (AS-3).
4. **Pairwise overlap vs the LIVE T070 lane:** T071 allowed_paths ∩ T070 allowed_paths = ∅
   (T070 forbids ProposalOutlineDraw/ProposalOutlineMap; T071 forbids ProposalEditor/
   ArchitectEntry/proposal-draft/max-envelope files and ALL e2e). Loop-1 lane; loops 2 idle,
   3 live on T070. EMPTY overlap holds.
5. **No new files:** every allowed_paths file except the producer report already exists and
   is tracked — no placeholder seeding needed; the report is the packet's own output.
6. **Fail-safe framing:** DB-047(e) only ADDS a disable condition (non-finite rows);
   DB-048 is test-side only (LotOutlineMap.tsx FORBIDDEN); DB-047(d) only gates copy on the
   already-typed fallback states. AS-5 requires demonstrated teeth so the hardened spec
   cannot go tautological.
7. **Thin client:** documented local check = modularity only; web proofs ride CI on the
   pushed head.

## Verdict

G0 PASS — contractable: walls explicit both directions against the live T070 lane, all six
acceptance scenarios executable, no placeholder debt. Ready to claim for the loop-1 lane
(worktree wt-m5t071 at/past the claim-seam commit).
