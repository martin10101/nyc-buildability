<!-- Reviewer return preserved VERBATIM by the orchestrator (report-preservation rule,
.claude/rules/project-control.md; transport entity-decoding applied: &lt; -> <, &gt; -> >, &amp; -> &).
Reviewer: independent code-reviewer subagent, returned 2026-09-12 (UTC). -->

# Gate Report

- Gate ID: G1 (code review)
- Task ID: M5-T015 (address entry + resolution outcomes + error matrix, design-spec Packet 1)
- Reviewer: code-reviewer (independent; read-only, ADR-005)
- Producer: frontend-engineer/orchestrator
- Result: **PASS** (with Minor, non-blocking recommendations)
- Clean environment/worktree used: reviewed at frozen HEAD `9f679ff7` on branch `candidate/D-024-mrl-option-b`; `git rev-parse HEAD` = `9f679ff7de62658189dacb4db9ba1ea824b30ab1` (matches). No files written outside my agent-memory. CI not re-run (thin client) — committed CI evidence cross-checked for plausibility.

## One-paragraph summary

The client is contract-correct against the accepted endpoint. The `DOCUMENTED_PAIRS` set in `address-api.ts:51-62` byte-matches the endpoint's `STATUS_STATE_MATRIX` (`address_resolution.py:164-177`) across all ten (status,state) pairs; 200-family routing branches on `document.status` after verifying `document_kind === "address_resolution"` (never on field presence), and every consumed field name matches `_success_document`/error-body keys exactly. The state machine is a faithful clone of `PropertyLookup` (abort + monotonic `requestSeq`, D5 last-good survivorship, `retryFocus`, `[data-outcome-heading]` focus, a single persistent announcer cleared in flight); the deliberate "submit not disabled in flight" divergence is consistent with `PropertyLookup` (which also never disables its submit) and is correctly covered by the supersession + seq-guard path. `PropertyLookup.tsx` flag-off renders today's DOM byte-equivalently (the placeholder block moved verbatim inside `{ruleEvalEnabled ? null : (…)}` and the surface renders `null`), the BBL machine is untouched, and flag-on suppresses the placeholder and mounts the surface. `announce.ts` is genuinely additive (existing function byte-unchanged; new mapping exhaustive over all 9 error states + all outcome kinds). Render-safety is sound: only bounded/text-node output reaches the DOM, index keys, no `dangerouslySetInnerHTML`, the sole anchor is a constant `#bbl-input` fragment. Scope is clean (producer commits touched only the 8 allowed paths). The findings below are all Minor: a containment-claim inaccuracy (rawStreetName also flows to the street input value, a safe context), a test-adequacy gap (no fixture makes bounded≠raw, so the "raw only" path is not actually discriminated), and two inaccurate stats in the producer report (line count, test count). None block G1.

## Acceptance criteria reviewed

All S1–S8 verified statically against the frozen source and the committed CI evidence (web + web-e2e SUCCESS on run 34675738338/34675912878, address suite 31 cases — count independently confirmed from the test file).

- S1 flag posture — PASS. `PropertyLookup.tsx` diff: surface `{ruleEvalEnabled ? <AddressResolutionScreen /> : null}` (line ~265) and placeholder `{ruleEvalEnabled ? null : (…unchanged bytes…)}` (lines ~306-330). Flag-off DOM byte-equivalent; test S1 asserts no surface, no fetch (`address-resolution.test.tsx:224-247`).
- S2 resolved→stub — PASS. Disabled `stub-continue` + explicit Packet-2 copy (`AddressResolutionScreen.tsx:236-251`); warnings block `role="status"`, both messages, never gating (lines 192-213).
- S3 ambiguous caller_selects — PASS. No default selection (`SuggestionChooser.tsx:29`), Use disabled until pick (line 62), index keys (line 37), re-resolve with `rawStreetName` + original house/borough (`AddressResolutionScreen.tsx:620-632`).
- S4 visible honest non-success — PASS. Both GRC lines always rendered (lines 130-143); unknown status → `UnrecognizedStatusCard` via `default` (lines 669-670), never coerced to not_found; `boundedToken` status display (line 349).
- S5 error matrix — PASS. All 8 emitted pairs + budget/undocumented/drifted-200 covered; retry only where safe via `ADDRESS_ERROR_COPY[state].retry` (lines 361-435, 467); correlation id on every card via `Meta`.
- S6 render safety — PASS. Bounded text nodes only; source-scan test asserts no `dangerouslySetInnerHTML`, index keys, import allowlist (`address-resolution.test.tsx:642-700`).
- S7 state-machine integrity — PASS. Seq guard proven load-bearing even when transport ignores abort (`AddressResolutionScreen.tsx:585-599`; test 708-734); D5 inert-resubmit survivorship (736-753); focus + announcer (755-807).
- S8 no new deps/tokens — PASS. No `package.json` change (verified via diff); import-specifier allowlist test; reused classes only.

## Directive/requirement verification

This task is in-regime (`directive_refs: D-038:ALL`). Per the run-quality-gate protocol, the requirement-by-requirement independent verification of D-038 belongs to the `directive-compliance-verifier` agent and is recorded in the directive's `verification.json` (producer ≠ verifier); it is out of G1 code-review scope. From the G1 vantage, nothing in the diff contradicts the D-038 product-pivot posture (this is the first user-facing consumer of the accepted backend), and no legal interpretation, schema invention, or AI-into-value path is introduced. I did not attempt to substitute for the directive-compliance pass.

## Steps independently executed

- `git rev-parse HEAD` → `9f679ff7…` (confirmed frozen SHA).
- `git diff 6787445e 9f679ff7 -- apps/web` → empty (CI-tested production code == reviewed code).
- `git diff --name-status <merge-base> 9f679ff7` and `git show --name-status 352aa9de/6787445e` → producer material limited to the 8 allowed paths; repair touched test module only; control-plane files (state.json, task json, ci-evidence, evidence-map) are orchestrator writes.
- Byte-compared `DOCUMENTED_PAIRS` (address-api.ts:51-62) vs `STATUS_STATE_MATRIX` (address_resolution.py:164-177) and `ADDRESS_ERROR_STATES` (address-api.ts:38-48) vs `_ERROR_STATUS` + defaults — exact match.
- Field-name fidelity: every client read (`documentView`, error branch) cross-checked against `_success_document` and the error body — exact match; unread fields (lat/long, grc_reason, connector_correlation_id) are Packet-2/unused, not defects.
- `git diff` of announce.ts and PropertyLookup.tsx — additive/flag-gated only.
- Mutant spot-check (≥5) against the tests — see below.
- `wc -l` of the four new files; inspected `tools/modularity_check.py` thresholds (WARN 600 / JUSTIFY 750 / HARD 1000 SLOC).

## Expected versus actual (mutant spot-check, producer report §4)

1. S1 guard removal → S1 test 1 `queryByTestId("address-resolution-screen")` non-null → FAILS. Killed. ✓
2. S2 live Continue (drop `disabled`) → `expect(stub).toBeDisabled()` FAILS. Killed. ✓
3. S3 default-checked radio (`useState(0)`) → radios-all-unchecked loop + `use-suggestion` disabled assertions FAIL. Killed. ✓
4. S7 seq-guard removal → late first (notFound) overwrites resolved; `waitFor(not-found null)` FAILS (verified the guard is the only catch since the mock ignores abort). Killed. ✓
5. S7 D5 removal (drop both-blank guard) → `expect(fetchSpy).toHaveBeenCalledTimes(1)` FAILS. Killed. ✓
6. S7 announcer-not-cleared → `waitFor(announcer==="")` during retry FAILS. Killed. ✓
7. S3 "re-query with bounded streetName instead of rawStreetName" → **NOT killed** (see Defect 2): no fixture makes `boundedText(name)` differ from the raw name, so all street-seam assertions pass identically for bounded or raw.

## Evidence paths

- `C:\Users\MLFLL\Downloads\nyc-zoning\ctl24\apps\web\src\lib\address-api.ts`
- `C:\Users\MLFLL\Downloads\nyc-zoning\ctl24\apps\web\src\components\address\AddressResolutionScreen.tsx`
- `C:\Users\MLFLL\Downloads\nyc-zoning\ctl24\apps\web\src\components\address\AddressForm.tsx`
- `C:\Users\MLFLL\Downloads\nyc-zoning\ctl24\apps\web\src\components\address\SuggestionChooser.tsx`
- `C:\Users\MLFLL\Downloads\nyc-zoning\ctl24\apps\web\src\components\address\__tests__\address-resolution.test.tsx`
- `C:\Users\MLFLL\Downloads\nyc-zoning\ctl24\apps\web\src\lib\announce.ts`
- `C:\Users\MLFLL\Downloads\nyc-zoning\ctl24\apps\web\src\components\property\PropertyLookup.tsx`
- `C:\Users\MLFLL\Downloads\nyc-zoning\ctl24\services\api\app\api\v1\address_resolution.py` (contract, read-only)
- `C:\Users\MLFLL\Downloads\nyc-zoning\ctl24\services\api\app\connectors\geoclient_address.py` (contract, read-only)
- `C:\Users\MLFLL\Downloads\nyc-zoning\ctl24\project-control\reports\M5-T015-ci-evidence.txt`
- `C:\Users\MLFLL\Downloads\nyc-zoning\ctl24\project-control\reports\M5-T015-producer-report.md`

## Regression/security/provenance findings

- Render safety is sound. No reflected free-text reaches an attribute/URL context. The only `data-testid` template literals interpolate `outcome.state` (a typed union member, not free text) and array `index` — both safe (`AddressResolutionScreen.tsx:448`, `SuggestionChooser.tsx:44`). No `dangerouslySetInnerHTML` in any of the six files.
- No secrets/keys touched; browser holds only form/HTTP state; no AI-into-value path.
- Flag-off leak: none. Surface and its fetch are gated behind `ruleEvalEnabled`; the endpoint's flag-off 404 is unreachable from the UI.
- Contract discipline: no schema guessing; the route-local document shape is consumed verbatim; provenance/source_facts are mapped for Packet-2 disclosure but not rendered now (honest deferral).

## Defects (all Minor / non-blocking)

1. **Minor — rawStreetName containment claim is inaccurate.** `AddressResolutionScreen.tsx:628` (`onPickSuggestion`) does `setValues((current) => ({ ...current, street: rawStreetName }))`, so the verbatim (unbounded) suggestion string also flows into the controlled `<input value={values.street}>` (`AddressForm.tsx:97`), not "ONLY to the fetch seam" as claimed in producer report §3.2. Impact is benign: a React controlled input sets `.value` as a DOM property (no HTML parsing, no attribute-string/URL context), so there is no injection or escape vector, and it does not violate packet risk #2. Recommendation: correct the report claim to "fetch seam + the street input value (safe controlled-input context)."

2. **Minor — the rawStreetName-vs-bounded distinction is functionally untested.** `boundedText` only alters a string that is non-string, blank, contains control chars, or exceeds 600 chars. No S3/S6 fixture triggers any of these (`HOSTILE_SUGGESTION` at `address-resolution.test.tsx:586-587` contains none), so `boundedText(streetName) === rawStreetName` for every fixture. Consequently S6 test 2's "picking sends the RAW name only to the fetch seam" (line 617-640) would pass equally if the code used the bounded `streetName`; the named mutant in report §4 (S3 re-resolve) is not actually killed. The code is correct (it uses `rawStreetName`), but the design is unproven by test. Recommendation (non-blocking): add a fixture where the suggestion name carries a control char or >600 chars so raw≠bounded and the seam assertion discriminates.

3. **Minor — inaccurate stats in the producer report.** §5 states `AddressResolutionScreen.tsx` is "687 raw lines"; actual is 725 (`wc -l`). §1/§4 state "20 tests"; the file has 24 `it`/`it.each` blocks expanding to 31 cases (the CI evidence's "31 tests" is the correct figure). Neither affects the gate: 725 raw is under JUSTIFY (750) and HARD (1000) SLOC and CI modularity passed; the file is cohesive (a single responsibility — the outcome-rendering table + host state machine for one endpoint). Recommendation: correct the numbers so the record is trustworthy.

4. **Minor / context — the CI run is not fully green.** `M5-T015-ci-evidence.txt:20-23` records `web-dependency-security FAILURE` (Next.js advisory awaiting owner authorization) and `control-plane FAILURE` (owner-gated), with web + web-e2e + modularity + api SUCCESS. Both failures are pre-existing, owner-gated, and unrelated: this task adds no dependency (verified — no `package.json`/lockfile change and the import-allowlist test enforces it). Flagged only so "CI green" is not read as unqualified; not attributable to M5-T015.

## Required rework

None blocking for G1. Recommended (may be folded into a later Packet-2 touch or a report correction, at the orchestrator's discretion): fix the two inaccurate report stats (Defect 3), correct the rawStreetName containment wording (Defect 1), and consider a bounded≠raw fixture to actually exercise the verbatim re-query path (Defect 2).

## Reviewer conclusion

G1 **PASS**. The change is contract-correct (byte-verified pair matrix and field names), a faithful `PropertyLookup` state-machine clone with a sound and well-tested deliberate divergence, render-safe, additive where required, flag-off byte-equivalent, and strictly within allowed paths. CI evidence covers the exact reviewed code (apps/web byte-identical to the tested head) and reports web + web-e2e green with the address suite passing. All findings are Minor and non-blocking; I recommend the orchestrator record them as advisory corrections rather than rework. The independent D-038 directive-compliance verification remains a separate pass for the `directive-compliance-verifier` before acceptance.
