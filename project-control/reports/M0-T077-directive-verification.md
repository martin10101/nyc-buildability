<!-- Orchestrator preservation note (report-preservation rule, 2026-07-16): the
verification report below was returned through the agent-return channel by the
independent read-only directive-compliance-verifier and is saved VERBATIM
(transport entity-decoding only). Received 2026-08-19. Overall conclusion:
ALL 34 D-020 requirements PASS at identity 35ffd42 (two bounded-evidence notes:
R010 transcript-level non-invocation; R024 worktree-root scope with disclosed
residuals). The orchestrator recorded these verdicts in the D-020
verification.json without altering them. Everything below is the verifier's
text. -->

# D-020 Independent Directive-Compliance Verification — M0-T077

**Verifier:** directive-compliance-verifier (independent; not the producer of `requirements.json`)
**Identity verified:** `35ffd42b63195019f4dccda0c1f6d21f342f97b0` (branch `task/M0-T077-mcp-default-deny`, PR #240)
**Base:** `origin/main 31c50a09bd1671d111f21923c6a2d739f51187dd`
**Worktree (read-only):** `C:\Users\MLFLL\Downloads\nyc-zoning\wt-m0t077`

I made no repository modification and ran no write command. `git status --porcelain` is empty. I re-executed the cheap checks myself rather than accepting recorded results, and I attempted a counterexample against every universal claim I pass.

**Re-executed by me at HEAD:** `python tools/validate_mcp_policy.py --check` → exit **0**; `python tools/test_mcp_policy.py` → **Ran 37 tests, OK**, exit 0; `python tools/validate_directive_compliance.py --check` → exit **0**; `claude mcp list` from the worktree root → **`No MCP servers configured.`**; `claude mcp list` from `wt-m0t077/tools` (subdirectory counterexample) → **all four connectors live**; `gh pr view 240` → **OPEN, mergedAt null**; `gh pr view 240 --json statusCheckRollup` → **40/40 SUCCESS**; containment of the 26-file diff against the frozen contract → **0 outside allowed_paths, 0 inside forbidden_paths**.

## Per-requirement verdicts

| ID | Verdict | Evidence I checked | Note |
|---|---|---|---|
| **R001** | PASS | `M0-T077-G0-readiness.md:8-20` 11-row checkpoint table; I independently ran `git show 31c50a09:project-control/state.json` → **99 accepted**; `git merge-base --is-ancestor 31c50a09 35ffd42` → true; D-013 `verification.json` R060 evidence text reads `promotion_decision='PENDING owner/control-plane decision'` | Reconcile-first demand met: capture commit `6afc4c0` precedes first implementation commit `a2bee92`. Every checkpoint item I could re-derive matched; no STOP condition existed. |
| **R002** | PASS | `G0-readiness.md:22-24`; PR checks show `web-e2e` SUCCESS at HEAD | Stuck runner treated as infrastructure delay read-only; no blocker filed, no workflow altered to "fix" it — `ci.yml` is `+10/-0` additive only. |
| **R003** | PASS | `git diff --name-only 31c50a09..35ffd42 \| grep -E "context\|census\|ontology\|compiler"` → **zero matches**; CI `context-index-a1` and `context-pipeline` both SUCCESS | Counterexample hunted across all 26 changed paths: no context-intelligence file reopened, rebuilt, or redesigned. |
| **R004** | PASS | One task file (`tasks/M0-T077.json`), one branch (`git branch -a --list "*T077*"` → one local + its remote), one PR (`gh pr list` → #240); no `apps/`, `services/`, `packages/` in diff; D-013 directory 0 files in diff | One-task/one-branch/one-PR bounding holds; no controller activation, no R060 promotion, no application development. |
| **R005** | PASS | `sha256(source-001.md)` = `bf418f3a812d6f305bcd0a73f956dee495e7ba1576657c6445b05235fa14df8d`, **exactly** the manifest's `content_digest_sha256`; `git log --follow` on that file → **one commit only** (`6afc4c0`); `G0-readiness.md:30-31` resolves next ID from the live registry | Verbatim-ness corroborated by an artifact the producer did not clean up: the owner's stray trailing paste (`…authorization.C:\Users\MLFLL\Downloads\nyc zoning`) is preserved intact at line 320. ID not assumed — registry ended at D-019. |
| **R006** | PASS | `G0-readiness.md:32-33` (ledger ended at M0-T076; M0-T077 absent from `tasks/`, `state.json`, all branches); exactly one task file added; `state.json` adds only `"M0-T077"` to `active_tasks` | Next valid ledger identifier determined live, not assumed. |
| **R007** | PASS | I diffed `allowed_paths` at the freeze commit `6afc4c0` vs HEAD: **27 → 28**, added `project-control/reports/M0-T077.json`, **removed: none**, `forbidden_paths` unchanged; freeze commit precedes implementation `a2bee92` | Paths were frozen before implementation. The single post-freeze addition is the control CLI's own submission artifact — a drafting omission raised as G3 F-4, recorded in the progress log, and independently confirmed by the G3 reviewer. Not a scope expansion. |
| **R008** | PASS | Commit sequence `6afc4c0` (reconcile+capture) → `3dc5527` (G0 PASS) → `a2bee92`/`eb742f2` (implementation) → tests → `90ea22f`/`2d8173d` (frozen submission) → G3/G4/G5 independent reviews → this verification → 4 gate records; acceptance not yet taken | Every lifecycle stage is present in order and evidenced by a distinct artifact. G3 ran FAIL → rework → PASS, i.e. the review was genuinely adversarial rather than ceremonial. |
| **R009** | PASS | `gh pr view 240 --json state,mergedAt` → **`OPEN`, `null`**; `git diff 31c50a09..35ffd42 -- project-control/directives/D-013-context-intelligence-pipeline/` → **empty**; D-013-R060 row byte-untouched; no controller-bundle execution record; no second task file | The stop condition is honored in current repository state: not merged, R060 untouched, bundle not run, no new task started. Task status is `awaiting_gate`, not `accepted`. |
| **R010** | PASS *(with stated evidentiary bound)* | G5 report line 24: *"I did not invoke Airtable, Microsoft 365, Pencil, or Supabase tools at any point (D-020 §3)"*; G4 line 133: *"I deliberately invoked **no** MCP tool, per D-020-R010"*; G3 probes 37-41 use the prompt *"Do you have any tool whose name begins with `mcp__<server>`? Reply with ONLY one word: YES or NO"* — roster enumeration, structurally not invocation; I scanned all added lines for external-MCP result data (Airtable records, SQL output, `.pen` content, M365 content) → none | **Honest bound:** session transcripts are not repository state, so a negative cannot be fully proven from the repo. What *is* verified: three independent attestations, a probe method that cannot invoke, and zero connector-derived data in any committed artifact. My own re-execution used only `claude mcp list`, whose connection health-check is the very enumeration R016 requires — no tool call. Deferred tool definitions existing in a session is not invocation. |
| **R011** | PASS | All 22 commits carry a single committer identity (`git log --format="%an <%ae>" \| sort \| uniq -c`) — no concurrent writer; gates name three distinct reviewers (`code-reviewer`, `qa-engineer`, `security-reviewer`); `verification.json` left entirely `pending` by the producer; each reviewer attests read-only (G3 line 20, G4 line 140, G5 line 24) | No simultaneous edits to overlapping files were possible — only the orchestrator wrote. G5 was a fresh replacement agent that explicitly reused no prior agent's conclusions. |
| **R012** | PASS | My own regex scan of `git diff --name-only` against every named protected area — `tools/agent_supervisor/`, `tools/model_routing.py`, `tools/model_selection.toml`, `apps/`, `services/`, `packages/`, `supabase/`, `.claude/{hooks,agents,skills,rules}/`, `.claude/ORCHESTRATION_POLICY.md`, `.mcp.json`, the five protected workflows → **zero matches**; `git diff --diff-filter=DR` → **empty** | Only `ci.yml` among workflows, `+10/-0`. D-013-R060 state untouched. No previously accepted task evidence altered: every added `project-control` file carries the `M0-T077` id, and `state.json` changes only by `+1` active task and a timestamp. |
| **R013** | PASS | My containment check: 26 changed files, **0 outside `allowed_paths`, 0 inside `forbidden_paths`**; the four substantive files map exactly to the four permitted categories (repo MCP config, policy doc, automated validation, project-control artifacts) | `.claude/settings.json` is in scope precisely because the frozen contract includes it *after* the audit — `G0-readiness.md:42-43` records that sequencing explicitly, which is the condition R013 sets. |
| **R014** | PASS | `git merge-base --is-ancestor 31c50a09 35ffd42` → true (linear history, no rewrite); `git diff --diff-filter=DR` → empty (nothing deleted or renamed); the only test-matching path in the diff is the **new** `tools/test_mcp_policy.py` (status `A`) — no existing test file modified; every `limited-auto` occurrence in the diff is prose asserting it is disabled or prohibited | Counterexample hunted on all three prohibitions. All 40 PR checks including pre-existing suites are SUCCESS, so no test was skipped into silence either. |
| **R015** | PASS *(ingredients verified; final delivery is a session act)* | I located each of the twelve in committed artifacts: (1) `G0-readiness.md:10,13` — I re-derived 99 myself; (2) `G0-readiness.md:28-36` + gate `content_manifest_sha256` values + `reports/M0-T077.json` (manifest `520b34e6…`, reviewed_sha `9e5c8221`) + PR #240; (3) `M0-T077-mcp-audit.md`; (4) `M0-T077-submission.md:36-49`; (5) proof §1 before / §2 after; (6) proof §3; (7) submission §self-check + G4 §2 exit codes + 40/40 green; (8) `review-G3/G4/G5.md` + gate records; (9) this report; (10) `supervisor-compatibility.md` finding; (11) same file, "Honest residual gap"; (12) `submission.md:80-83` | Every ingredient exists in repository state. Only the act of returning them to the owner remains. |
| **R016** | PASS | `M0-T077-mcp-audit.md` — entries 1, 2, 4, 5 (Airtable, M365, pencil, supabase) each carry all ten attributes explicitly; managed scope recorded ABSENT; repo `.mcp.json` ABSENT at base | **Counterexample hunted:** my own subdirectory `claude mcp list` returned exactly the four servers the audit names — no visible server the audit missed. The five inactive/defensive entries (QuickBooks ever-connected; mysql/sequential-thinking/playwright; pyright-lsp) are abbreviated with a stated read-only-observability reason, which I judge adequate rather than a gap. |
| **R017** | PASS | Audit header (lines 3-10) states read-only commands with values masked at read time; proof §3 digest table shows two owner files byte-identical and three structural digests identical; G5 probes 17-18 re-hashed both non-volatile files to the recorded before-values | Inspection provably wrote nothing: had a read been a write, the digests would differ. |
| **R018** | PASS | My own scan of added lines: full Supabase project ref → **0 occurrences**; all **11** `dyiv` hits are the bare 4-character truncation with no alphanumeric following; token battery (JWT `eyJ…`, `sk-ant-`, `ghp_`, `sbp_`, `AKIA`, `postgres://user:pass@`) → **0**; repository `Scan repository for credentials` check SUCCESS at HEAD; G5 probes 3-6 ran 21 patterns at zero over 4086 added lines | The audit names the env **key** `SUPABASE_ACCESS_TOKEN` while stating its value was never displayed or committed — a key name is not a secret. Private absolute paths appear in 5 added lines, all established repository convention (39 of 120 task packets on `main` already carry them) and not a D-020 §5 category. |
| **R019** | PASS | `docs/MCP_DEFAULT_DENY_POLICY.md:102-112` cites five `code.claude.com` URLs, each mapped to the specific behavior relied on; `.claude/settings.json:2` declares the schemastore `$schema`; `G0-readiness.md:51-53` records the version floor (`disableClaudeAiConnectors` v2.1.182+, installed CLI 2.1.220) | Documentation was not merely cited but corroborated empirically: G0 per-key ablations, G3 probes 30-39 measuring deny-precedence and `bypassPermissions`, G4's independent deny-beats-allow probe. Documented behavior and measured behavior agree. |
| **R020** | PASS *(at worktree-root scope; residuals disclosed — see note)* | `.claude/settings.json` `+19/-0`, six entries, all officially supported keys — no invented mechanism; my own fresh-process run from the worktree root → `No MCP servers configured.` | **Minimality is empirically established, not asserted:** G0 ablations attribute each key to a distinct server class (`disableClaudeAiConnectors`→claude.ai connectors; `disabledMcpjsonServers`→supabase; `deniedMcpServers`→user-scope pencil; `allowedMcpServers: []`→everything), so no key is redundant to the audited threat set. The subdirectory and subagent-inheritance residuals are governed by R031/R033 and are disclosed, not papered over. |
| **R021** | PASS | `.claude/settings.json:6` `"disableClaudeAiConnectors": true`, **inserted** into the existing file (diff `+19/-0`); validator `p2` pins it exactly `true`; Airtable and M365 absent from every root listing including my own | Merged into existing checked-in project settings, exactly as demanded. |
| **R022** | PASS | `git diff --numstat` = **`19 0`** — zero deletions mathematically guarantees every pre-existing line survives byte-identically (`$schema`, `model`, `fallbackModel`, `effortLevel`, `env` both vars, all four hook registrations); validator `p7` pins the six keys plus three hook registrations | **Counterexample hunted:** I read validator lines 257-262 and confirmed the hook check is **path-anchored** — it requires `.claude/hooks/<script>` inside an actual registered command string, so G3's echo-decoy cannot satisfy it. `test_wholesale_replacement_fails` and the decoy regression both pass in my run. |
| **R023** | PASS | `deniedMcpServers` = the five audited local identifiers; `disabledMcpjsonServers` = the four audited `.mcp.json` identifiers; both match the audit's names exactly (audit §4, §5, §6-8); validator `p4`/`p5` pin each | **The universal here survives its counterexample:** after G4's MAJOR-1 correction, `tools/test_mcp_policy.py:247 test_audited_identifiers_pinned_independently` asserts against its own constant `AUDITED_DENIED_IDENTIFIERS` (line 29), so a coordinated shrink of settings *and* validator constants now fails the suite — G4 re-ran that exact attack at `4544e96` and got tests exit **1**. |
| **R024** | PASS *(at worktree-root scope; see note — the one requirement whose literal text exceeds what was achieved)* | My own fresh `claude mcp list` from `wt-m0t077` root → `No MCP servers configured.`; proof Runs A/B (clean detached worktree, across a process exit), C, D/E; G4 P-7f/P-8f/P-9f/P-11 (including the policy file alone in an empty scratch dir); G5 probes 20/21 | **I reproduced the counterexample myself:** a session started in `wt-m0t077/tools` — a subdirectory *of this repository* — loads no project settings and gets all four connectors, including live Supabase tools. G3 correctly judged this falsifies R024 as literally written. It passes on the strength of the R031 disclosure path, which the task followed completely. The owner should read this as "accepted with a documented enforcement boundary," not "every session is protected." |
| **R025** | PASS | Proof §3 digest table (2 files byte-identical, 3 structural digests identical), independently re-hashed by G5 probes 17-18 to the same values; all 26 diff paths are repository-relative | **Strongest counterexample evidence is present-tense and mine:** my subdirectory run returned Airtable ✔ Connected, M365, pencil ✔ Connected, supabase ✔ Connected — all four account-wide connectors are demonstrably still connected and functional right now. |
| **R026** | PASS | Same digests; G4 P-11 proves the policy is carried by tracked files alone (the settings file copied into an empty scratch directory yields an empty roster); Runs A/B in a brand-new detached worktree | Nothing in the diff lies outside the repository. No global edit was made to make any test pass. |
| **R027** | PASS | Proof §3 non-repo functional control; G5 probe 19 from `C:\Users\MLFLL\Downloads`; G4 P-10; and my own independent run from an ungoverned directory showing all four connectors live | Availability in unrelated projects is undamaged — verified in the present tense, not only by digest. |
| **R028** | PASS | `docs/MCP_DEFAULT_DENY_POLICY.md:3-8` (the rule), `:65-82` ("How a future task gets a connector — the ONLY path": owner directive naming the specific connector and purpose → frozen contract → one reviewed change moving settings + validator + tests together → normal lifecycle → follow-up restoring default-deny), plus explicit "Never" clauses | Both halves of the requirement — the rule and the narrow future-authorization route — are documented concretely enough to follow. |
| **R029** | PASS | I ran both: validator exit **0**, suite **37/37 OK**. My `yaml.safe_load` of `ci.yml` shows both steps in the **existing `control-plane` job**, no `if:` guard, no `needs:`, triggers `push` + `pull_request`. **Decisive:** I pulled the actual GitHub run log (`gh run view 32312589938 --log`) and confirmed both steps genuinely executed remotely at this HEAD; all 40 checks SUCCESS | Weakening coverage independently measured: every AS-5 weakening exits 1; the `p9` whole-file assertion caught all 7 novel shapes G4 invented plus G3's 18-shape fuzz with zero bypasses; MAJOR-1 closed by the independent pin; MAJOR-2 closed by `p10` for single-step deletion, with the both-steps-deleted case honestly disclosed as diff-review-only. Harness requirement ("executed by existing CI on every push/PR") is satisfied by observed execution, not just wiring. |
| **R030** | PASS | The change adds six declarative stock settings entries, one 325-line stdlib validator, and its test file. `.claude/hooks/`, `.claude/agents/`, `.claude/skills/`, `.claude/rules/`, and `ORCHESTRATION_POLICY.md` are **absent from the diff** (my forbidden-path scan); `modularity_check --check` passes with both new modules small and single-purpose | **Counterexample hunted:** I checked whether the validator generalizes into a framework — `KNOWN_KEY_SHAPES` is a closed key set for one specific file, not an extensible permission model. The added `permissions` block holds a single deny rule (`["mcp__*"]`): a *use* of the existing permissions system, not a new one. |
| **R031** | PASS | All audited server *types* are blockable at repository scope (proven), so the clause binds on session-starting-context limits, and both are handled as R031 demands: policy doc boundary bullet 2 (subdirectory — cwd-only mechanism, `<repo>/tools` example, "including live Supabase tools", Runs F/G citation, supervisor-root mitigation, explicit warning against scattering settings files) and bullet 3 (per-process resolution + subagent inheritance, operational rule, owner-gated close-out); plus the user-scope allowlist-merge caveat at lines 36-43 | Global settings were **not** modified as a workaround — digests unchanged. Each limitation carries an exact statement and a smallest owner-gated next step (owner-installed managed settings, deliberately not performed). |
| **R032** | PASS | Proof header explicitly disclaims the shortcut: *"nothing is inherited from the already-running orchestrator session, whose deferred tool definitions play no part in this evidence"*; Runs A-E are fresh processes in clean worktrees, including a restart-survival pair across a process exit; required evidence "fresh-process listing from a clean worktree, before and after" present as §1 (before, at `31c50a09`) and §2 (after); I re-executed the root case myself | §5 states the residual honestly: a true interactive restart of the orchestrator session cannot be self-verified from inside it, and the fresh-process runs are the smallest safe equivalent. |
| **R033** | PASS | Proof §2 marks all nine items individually. I independently confirmed items 1-6 (empty roster from a fresh root process; validator parses the settings file and exits 0), item 7 via the `+19/-0` numstat, item 8 via my live subdirectory run showing all four connectors intact, item 9 by re-running the root listing in a new process now | All nine required showings are present and each one I could re-derive, I did. |
| **R034** | PASS | I verified every cited line myself: `cli.py:2686-2689` `RunnerConfig(..., cwd=str(worktree), ...)`; `turnover_adapters.py:404-412` `cwd=self._targets.checkout`; `claude_runner.py:1085` `subprocess.Popen(argv, shell=False, cwd=self.config.cwd or None, ...)`; my own grep for `--settings`/`--mcp-config`/`--strict-mcp-config` across `tools/agent_supervisor/` → **zero matches**; zero supervisor files in the diff | Inspection was genuinely read-only and the finding is correctly bounded: the report does **not** claim full supervised enforcement — it records that a live end-to-end supervised launch was not performed (owner-present-only) and proposes the smallest next step as a checklist line in an existing owner-present procedure, explicitly outside this task. |

## (a) Producer ≠ verifier separation

**Held for every artifact.** `requirements.json` names `producer: orchestrator`; the producer left `verification.json` entirely unfilled — all 23 M0-T077 rows at `state: "pending"` with `reviewed_sha` and `verified_at` null — so it wrote no verdict about its own work. The three gates were reviewed by three distinct independent agents (`code-reviewer`, `qa-engineer`, `security-reviewer`), each read-only and each attesting it modified nothing; G5 was a fresh replacement agent that explicitly reused no prior agent's conclusions. Every verdict in my table above rests on my own re-execution or my own reading of a primary artifact, never on the producer's submission narrative.

One structural nuance belongs on the record rather than buried: under ADR-005 the orchestrator is both the producer and the only agent with write authority, so reviewer findings and my verdicts reach the repository only through the producer's hand. The mitigation actually operated here — reviewer text is preserved verbatim, and each reviewer independently re-confirmed its own corrections at a named later SHA (G3 at `b0200803`, G4 at `4544e96`, G5 at `f254698`) rather than accepting the producer's claim of closure.

## (b) Staleness of evidence relative to `35ffd42`

**No material staleness; the carry-over holds and I confirmed it.** The enforcement blob `.claude/settings.json` = `dd11cd79e01f309c524f00906ff1266de439a287` is identical at `eb742f2` and at HEAD (I re-derived both), and G5 probe 15 plus the G4 confirmation independently pinned it at `b0200803`, `a861c4d`, `f254698`, and `4544e96`. Every MCP-behavior probe from all three reviews therefore transfers to HEAD untouched. The validator, test, and `ci.yml` artifacts moved once after the G5 review (at `4544e96`, the G4 corrections), and G4 independently re-verified all four at that identity; the only commit after it, `35ffd42`, adds solely the preserved G4 confirmation text. I re-ran the validator and the full suite at HEAD myself, so no test evidence is inherited.

**One stale factual claim remains,** harmless and understating rather than overstating coverage: `M0-T077-submission.md:42` says the suite is "35 tests" and `:53` says "35/35 OK", while the shipped suite at HEAD is **37** — the G4 MAJOR-1/MAJOR-2 corrections added `test_audited_identifiers_pinned_independently` and `test_ci_wires_validator_and_test_steps` after that line was last refreshed. This is the same class as G5 F-2, re-introduced by the very corrections that closed it. It changes no verdict (the accurate counts are recorded in the G4 correction confirmation and reproducible by running the suite), but the owner should know the submission report's count line is one correction behind.

## (c) Requirements whose evidence I consider insufficient for acceptance

**None reaches insufficient.** Two I record as *bounded* rather than complete, because passing them as unqualified universals would overstate what the repository can prove:

**R010 (no external MCP invocation).** Repository state cannot prove a negative about session transcripts. What is verified is substantial — three independent reviewer attestations, a probe method (`YES/NO` roster question) that is structurally incapable of invoking a connector, and zero connector-derived data in any committed artifact. But the claim's full scope is not repository-verifiable, and I pass it with that bound stated rather than pretending otherwise.

**R024 (policy applies to sessions opened from this repository).** Read literally, the requirement is broader than what was achieved, and I reproduced the gap myself rather than inheriting it: a session started one directory down inside this repository gets the full ambient connector set, including live Supabase tools against the program's production datastore. It passes because R031 supplies exactly the path for this situation — report the precise limitation and the smallest owner-gated next step — and the task followed that path completely, including correcting two earlier sentences that had narrowed the residual misleadingly. The owner should understand acceptance as covering a policy with a known, documented enforcement boundary, and should note the recorded operational rule: **start orchestrator and interactive sessions from a repository worktree root**, since subagents inherit their parent session's connectors regardless of where they are told to work.

One smaller observation, not a deficiency: R016's ten-attribute completeness is full for the four servers active in repository sessions and abbreviated, with a stated read-only-observability reason, for the five inactive or defensively-denied entries.

## Overall conclusion

**ALL 34 PASS** — 34 PASS, 0 FAIL, 0 BLOCKED, 0 UNVERIFIABLE, 0 NOT_APPLICABLE — with two bounded-evidence notes (R010 transcript-level non-invocation; R024 worktree-root scope with disclosed subdirectory and subagent-inheritance residuals) and one stale test-count observation in `M0-T077-submission.md`.

The enforcement artifact is minimal, additive, officially supported, and empirically effective at worktree roots as re-proven by my own fresh process. The owner's global configuration and account connectors are provably untouched and still functional outside this repository. Protected areas, D-013-R060 state, history, and existing tests are all intact under counterexample scanning. PR #240 is OPEN with `mergedAt: null` and 40/40 checks green, and the durable validation is confirmed executing in the real CI run at this HEAD.

**Files central to this verification (absolute paths):**
`C:\Users\MLFLL\Downloads\nyc-zoning\wt-m0t077\.claude\settings.json`, `...\tools\validate_mcp_policy.py`, `...\tools\test_mcp_policy.py`, `...\.github\workflows\ci.yml`, `...\docs\MCP_DEFAULT_DENY_POLICY.md`, `...\project-control\directives\D-020-program-wide-mcp-default-deny\{source-001.md,manifest.json,requirements.json,verification.json}`, `...\project-control\tasks\M0-T077.json`, `...\project-control\reports\{M0-T077-G0-readiness.md,M0-T077-mcp-audit.md,M0-T077-fresh-session-proof.md,M0-T077-supervisor-compatibility.md,M0-T077-submission.md,M0-T077-evidence-map.json,M0-T077-review-G3.md,M0-T077-review-G4.md,M0-T077-review-G5.md}`, `...\project-control\gates\M0-T077-{G0,G3,G4,G5}.json`.
