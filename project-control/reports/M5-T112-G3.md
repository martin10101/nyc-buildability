# M5-T112 — G3 code review (code-reviewer "cr-t112", read-only)

> Transmission history: pinned at 1461966b, delivered as 2 SendMessage part(s) ending with END-OF-REPORT;
> no truncation. Joined verbatim by the orchestrator (transport wrapper tags removed only). Verdict FAIL on BLOCKING B1: the facade
> monkeypatch of _point_in_triangle in test_t098_as4 no longer bites after the split (a vacuous mutant-ME guard) -> rework.

---

M5-T112 G3 review — Part 1/2. Read-only. HEAD pinned 1461966bf2aab95085b697dfd8f407d6330c210c (matches expected).

Checks run this session:
- services/api `python -m ruff check .` -> All checks passed!
- services/api `python -m pytest tests/scenario -q` -> 817 passed in 20.14s
- repo root `python tools/modularity_check.py --check` -> selected 502 files; failures 0; warnings 26 (no massing_* file flagged; facade 450 / guards 172 / triangulation 234 / mesh 91 SLOC, all < 600)
- material commit 7413ecf4 touches 7 files, all within allowed_paths (test_massing_model.py left byte-identical); no forbidden path.

Split quality — PASS. DAG proposal -> massing_guards -> massing_triangulation -> massing_mesh -> massing_model(facade) is acyclic (the suite imports clean); one responsibility per module; no dumping-ground helpers; sizes fine.

Behaviour preservation — PASS. Diffed the facade builder against the claim-seam file (275ef85f) line-by-line: _check_floor_cap, _expand_floor_stack, _check_output_size, _triangulate_distinct, _lot_polygon, _local_origin, _crs_frame, build_massing_model, build_from_generated_option, _canonical_block, _passthrough_provenance, MassingModel/_Floor are byte-identical; the moved guards/triangulation/mesh primitives are byte-identical to their claim-seam bodies. The ONLY behaviour delta is the intended DB-079 (b): massing_model.py:416 field=_locate_overflow_field(...) vs the claim-seam constant "proposed_massing.outline"; message + reason unchanged. The equivalence + refusal corpus (test_massing_split_equivalence.py) confirms.

Public triangulate_polygon (massing_triangulation.py:262) — sound: ring in / prepared-ring + CCW indices out, fresh _WorkBudget(MAX_TRIANGULATION_WORK) when budget=None (same budget), typed MassingModelError refusals, no unbudgeted path; the NYC range-check is correctly NOT exposed there. DB-079 (b) locator (massing_guards.py:189) is bounded and correct for the outline cases.

Part 2 has one BLOCKING finding + advisories + the verdict.

---

M5-T112 G3 review — Part 2/2: one BLOCKING finding, advisories, verdict.

BLOCKING B1 — a split-introduced monkeypatch no longer bites, silently defeating an accepted mutation guard. test_massing_model.py:1037 does monkeypatch.setattr(mm, "_point_in_triangle", counting_pit) then calls mm._triangulate(...) directly at :1041. Post-split _triangulate lives in massing_triangulation and resolves _point_in_triangle from THAT module's globals, so the facade patch never reaches it. Proven this session (services/api):
- mm._point_in_triangle IS massing_triangulation._point_in_triangle -> True (re-export)
- Case A facade-patch counter (test_t098_as4 style) -> 0
- Case B consuming-namespace patch counter -> 15
Consequence: the test's `assert calls["n"] == 0` (line 1044) now holds UNCONDITIONALLY, so it can no longer detect mutant ME (charging the m-3 scan units AFTER the inner scan) — the exact regression it was written to catch. It still passes (CI/AS-2 stay green) but is a false-green guard. Pre-split the patch DID bite (same module), so this is a regression from this split. Production behaviour is unchanged — the charge-before-scan order is intact (massing_triangulation.py:228 charge before the :229-234 scan). The producer design note ("keep the builder in the facade so every monkeypatch bites") does not cover this case: test_t098_as4 calls _triangulate directly (not via the builder) and patches a primitive consumed only inside the submodule. Fix (in-scope — test_massing_model.py is in allowed_paths): patch the consuming namespace (import app.scenario.massing_triangulation as mt; monkeypatch.setattr(mt, "_point_in_triangle", counting_pit); real_pit = mt._point_in_triangle). I verified the other 6 patched names (_triangulate, _build_prism, MAX_TOTAL_FLOORS, MAX_TOTAL_MESH_VERTICES, MAX_TRIANGULATION_WORK, Polygon) are all consumed by facade-resident code, so their patches still bite; B1 is isolated to this one test.

ADVISORY A1: DB-079 (b) locator scope — a huge-int floor_to_floor_ft still falls back to proposed_massing.outline (producer OPEN QUESTION 1 / DISCOVERY D-OBS); deliberate, unchanged from claim seam, still fails closed non_finite. Route the residual to the backlog.
ADVISORY A2: triangulate_polygon intentionally not on the facade __all__ (OPEN QUESTION 2); DB-082 (a) must import it from app.scenario.massing_triangulation directly.

M5-T112 G3 VERDICT: FAIL

END-OF-REPORT
