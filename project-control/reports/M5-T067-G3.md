# M5-T067 G3 gate — independent code review (verbatim reviewer return)

Reviewer: code-reviewer (claude-opus-4-8 per D-064), dispatched by the orchestrator at the
seq-124 T067 wave, pinned to the frozen submit head 4ed36c5e (material 4e1491f8). Recorded
by the orchestrator; the reviewer is read-only and records no gate itself. The return
arrived in four parts (the first transmission truncated in the (r) section; the reviewer
re-sent the remainder in three labeled parts) — joined verbatim at the exact break point.

---

VERDICT: PASS

# M5-T067 — G3 Independent Code Review (verbatim reviewer return)

**Task:** M5-T067 — DB-040(q)+(r) site-definition store hardening residuals (close the two LOW security findings the M5-T062 G5 review routed onward): (q) supersede reordered to bind property/condo SCOPE before the ACTIVE-status check (closing the cross-property 409-vs-404 status oracle), and (r) revoke's two not-found branches unified to byte-identical message text (closing the existence-via-message oracle).
**Reviewer:** code-reviewer (independent G3, read-only). Fresh dispatch; the reviewer records no gate itself.
**Verdict:** **PASS** — no blocking defects. Three non-blocking observations, all either honestly disclosed by the producer or explicitly out of packet scope.

## Frozen-identity verification (performed)

- Submit head **4ed36c5e** confirmed == live HEAD.
- `git diff --stat 4ed36c5e..HEAD` over all four allowed paths (`store.py`, `tests/site_definition/test_site_definition_records.py`, `tests/api/test_site_definition_api.py`, `reports/M5-T067-producer-report.md`) is **empty** — the working tree equals the frozen submit surface. All review performed against that content.
- `git show 4e1491f8 --numstat` = **exactly 4 files** (report +261; store.py +41/−22; api test +52; records test +121). Own diff is confined to the allowed_paths; no forbidden path touched. My verdict binds to this frozen path-scoped content; per the disjoint-peer note, any control-plane commit landing mid-review touches none of the four task paths.

## (q) supersede reorder — scope-before-status (PASS)

- The status-first helper `_require_active` is **fully removed**; a repo-wide grep over `services/api` returns **zero remaining references** — no dead call site, no other consumer regressed by its deletion (it was supersede's only caller).
- New order in `supersede()` (`store.py:308-341`): direct record lookup (404 if missing) → `supersedes_id` reference → required `reason` → **SCOPE** (`condo_key != old.condo_key` → 404, `store.py:329-333`) → **STATUS** (`_current_status is not ACTIVE` → 409, `store.py:336-341`). Scope is strictly before status, exactly mirroring the accepted `revoke()` discipline (`store.py:399-437`). A cross-property probe of a KNOWN foreign record carries a server-resolved foreign `condo_key`, so it 404s at the scope check regardless of the record's status; the 409 `ConfirmationNotActiveError` is reachable only on the record's OWN binding. The oracle is closed.
- **Insert guards genuinely unmoved.** The collision and chain-depth caps live in `_record_insert` (`store.py:205-238`), which the diff does not touch and which is still called only at `store.py:346`, after all validation, raising before any mutation. A refused foreign probe mutates nothing.
- **Legitimate-path preservation.** Same-property active supersede: all checks pass → insert + superseded-transition, byte-unchanged. Same-property non-active own record: still the 409 conflict (pinned by `test_supersede_of_a_non_active_own_record_is_still_the_409_conflict`). No cross-property mutation is possible on any path.

## (r) revoke message unification (PASS)

- Both not-found branches (`store.py:410-412` missing-record; `store.py:426-428` foreign-scope) now raise `ConfirmationNotFoundError` with the byte-identical two-line text `"no site-definition confirmation matching that record id exists for the property addressed by the request path"`. Verified identical in the diff and current source.
- No other field leaks existence: both branches are the same exception class → same `reject_code` (`confirmation_not_found`), same 404/`not_found`; the only differing response field is the per-request `uuid.uuid4().hex` `correlation_id`, which discloses nothing. The route-level test asserts full-body equality after removing exactly that field. The unified text echoes neither probed id (the old missing-record branch's id-echo is gone).

## Tests pin the closures mutation-sensitively (PASS)

- **AS-2 (q) is genuinely mutation-sensitive.** `test_supersede_foreign_probe_is_404_whatever_the_record_status` is parametrized `active|superseded|revoked` and expects `ConfirmationNotFoundError`. Reverting the reorder (status before scope) makes the `[superseded]`/`[revoked]` foreign probes raise `ConfirmationNotActiveError`. I verified the exception hierarchy at `records.py:300-312`: `ConfirmationNotFoundError` and `ConfirmationNotActiveError` are **sibling** direct subclasses of `SiteDefinitionError`, not parent/child — so `pytest.raises(ConfirmationNotFoundError)` will **not** swallow the mutated 409 and the test goes red. (`[active]` stays green under the mutation, which is correct: an active foreign probe 404s via scope in both orderings; the two non-active params carry the sensitivity.) The test also asserts the refused probe mutated nothing (`after == before`; foreign condo empty).
- **AS-4 (r) is genuinely mutation-sensitive.** Store-level `test_revoke_not_found_message_is_identical_for_missing_and_foreign_ids` asserts `str(missing.value) == str(foreign.value)` plus equal `reject_code` and neither-id-echoed; route-level `test_revoke_not_found_response_is_identical_for_missing_and_foreign_ids` asserts equal status/state/reject_code/message and direct full-body equality minus `correlation_id`. Diverging either literal by one character breaks the equality → red.
- **No pre-existing assertion adjusted.** Both test diffs are pure additions (0 deletions). Grep confirms no test pinned the removed literals: the old foreign-revoke text `"must belong to the property"` appears only in a stale `store.cpython-311.pyc` bytecode artifact (not committed — the material diff is 4 files; recompiled on any pytest run), and the old missing-record literal is asserted nowhere in `tests/`. The producer's "no adjustments needed" claim is accurate. Consumer sweep confirmed: routes serialize `str(exc)` and never branch on message text; `test_condo_records_api.py` uses `site_definition` additively only.

## Independent evidence reproduced (from `services/api`)

| Command | Outcome |
|---|---|
| `python -m ruff check` on the 3 in-scope source/test files | **All checks passed!** |
| `python -m pytest tests/site_definition tests/api/test_site_definition_api.py -q` | **74 passed** (68 prior + 6 new; matches the packet's expected 74 and the harvest claim) |

`store.py` is 472 lines — well under the 600 modularity warn threshold; the net +19-line change raises no modularity concern.

## Non-blocking observations

1. **Precedence shift for malformed supersede inputs (not a defect).** Moving STATUS after the `supersedes_id`/`reason` checks means a non-active OWN record probed with a mismatched `supersedes_id` (or `reason=None`) now returns 404 / reason-error where the old `_require_active`-first order returned 409. This is not a legitimate path and is unobservable via the route, which always constructs `supersedes_id == old_id` and a reason server-side; it is if anything less disclosive. No test pinned the old precedence. Acceptable.
2. **Supersede's own message oracle remains (out of scope, honestly disclosed).** `supersede()`'s missing-record branch (`store.py:310-312`) still echoes the record id, and its foreign-scope branch (`store.py:329-333`) uses distinct static text — the analogue of revoke's LOW-2, one class lower (message-text existence hint only, on a default-off/unmounted route requiring an already-known unguessable uuid4). The packet scopes (r) to **revoke** only; the producer report (lines 173-183) discloses this residual and routes it as a follow-up. Correct scoping — recommend the orchestrator contract a future increment to unify supersede's messages.
3. **Timing side-channel (negligible, out of scope).** The revoke missing-record branch returns one dict-lookup sooner than the foreign branch (which also runs `_matches_addressed_property`). On an in-memory, default-off route this is immeasurable and outside the packet's message-text scope.

No critical, high, or medium findings. The implementation matches the M5-T062 G5 LOW-1/LOW-2 remediations exactly, preserves every legitimate same-property path, keeps the insert guards unmoved, and is pinned by mutation-sensitive tests I verified structurally and reproduced green.

Relevant files (absolute): `C:\Users\MLFLL\Downloads\nyc-zoning\ctl24\services\api\app\site_definition\store.py`, `...\services\api\app\site_definition\records.py`, `...\services\api\tests\site_definition\test_site_definition_records.py`, `...\services\api\tests\api\test_site_definition_api.py`, `...\project-control\reports\M5-T067-producer-report.md`, `...\project-control\reports\M5-T062-G5.md`.

--- END OF REPORT ---
