# M0-T114 — G5 DELTA re-attestation (verbatim)

**M0-T114 — G5 DELTA VERDICT: PASS** at corrected frozen identity `723f1d8ac4331701e40878ed3edc1b2c664b49d9` (verified == HEAD). The entire range `5e2c8c3..723f1d8` touches only one source line: commit `a22e34a` appends `# gitleaks:allow secretscan:allow fake sentinel proving register-key redaction` to the existing fake token in `tools/test_agent_supervisor_golden_run.py` — `git diff --stat` shows 1 insertion / 1 deletion on that file alone, and the diff over `tools/agent_supervisor/**` and the telegram test pack is empty, so both prod modules and all my Residual-1/Residual-2 conclusions carry over unchanged. It is a comment-only edit: the sentinel value (`sk-ant-api03-FAKEFAKEFAKEFAKE…`) and the `refusal_record`/assertion logic are byte-identical, so test semantics are unaffected and my prior "77 passed" stands without re-run. Pragma-on-a-fake-sentinel is the accepted L-pack convention and no real secret is involved; the CI "Scan repository for credentials" job (a different, stricter scanner than local gitleaks, which was already clean) flagged the unpragma'd token at `5e2c8c3`, and the dual pragma resolves it — your note that CI is 20/20 green at `a22e34a` is consistent. Local gitleaks over the range is still clean (exit 0, no leaks). No security posture change: leak-refusal, one-way geometry, hash-only audit, env-only secrets, and the register sanitization sweep all hold exactly as in my PASS at `5e2c8c3`. Verdict: **PASS**.
