# M5-T076 — G2 producer self-check record (recorded by the orchestrator, role self_check)

Producer: backend-engineer (lane-1 worker, run persistent-local-71-m5t076). The unit closed
clean with the derivation module + route wiring + tests delivered and the scoped pytest
explicitly routed ([BLOCKED → harvest] class — root-cwd `app` import artifact); the
orchestrator executed the exact harvest recipe and appended it verbatim as the report's
[ORCH-HARVEST] section.

- **Scope self-check (report, [OBSERVED]):** `git diff --stat` shows exactly the 4 allowed
  paths (new `lot_geometry_derivation.py`; route wiring in `max_envelope_api.py` — the sole
  production edit; the two test files) + this report; `max_envelope.py` ABSENT from the
  diff → engine BYTE-UNTOUCHED (AS-4); route stays unmounted
  (`test_route_is_unmounted_in_the_real_app` unchanged); no new dependency.
- **Design notes (report):** segment shape mirrors the committed `_build_lot_context`
  contract; holes excluded; provenance quintuple with dataset_version never guessed (`None`
  when absent); derivation only when segments absent AND BBL present; BBL resolvability is
  a pre-network gate; every failure class (bbl_unresolvable | no_feature |
  multiple_features | invalid_geometry | connector_fault | over-cap) keeps the honest
  `lot_geometry_unsupported` gap with the reason — NO fabricated rectangle; derivation runs
  off the event loop via `run_in_threadpool`; display 4326 connector NEVER imported.
- **Harvest execution of the recipe (orchestrator, cwd services/api, wt-m5t076):**
  `python -m ruff check .` → All checks passed (exit 0); scoped pytest over both test files
  → **40 passed** — every [PREDICTED PASS] AS-1/AS-2/AS-3 row elevated to OBSERVED
  (derivation correctness + provenance; the geometry-free fitted-candidate reachability
  proof; byte-identity + fail-closed classes).
- **Modularity:** [OBSERVED] failures 0 / warnings 22 (all pre-existing; the new module far
  under threshold; `max_envelope.py`'s 998-SLOC warning is its pre-existing signal,
  unchanged).
- **Identity:** in-wt commit cherry-picked to b8dc1044, ALL-MATCH x5 (LF-normalized);
  `max_envelope.py` blob unchanged across the material (the AS-4 bound held); CI on the
  pushed head is the backstop.

G2 verdict: PASS (self_check; never independent review — G3/G4 follow).
