# M5-T103 — G4 test-adequacy review (qa-engineer "qa-t103", read-only)

> Transmission history: pinned at 68bc726e, delivered as two SendMessage parts (1/2, 2/2) ending with END-OF-REPORT;
> no truncation. Joined verbatim by the orchestrator (transport wrapper tags removed only). Verdict PASS: every mandatory
> guard is caught by a non-vacuous test. Two own weakenings MISSED (W1 zero-width /W type default, W10 cross-phase budget
> carry) and one PARTIAL (W9 preview sites) became advisories A1-A5; they are carried into the M5-T103 rework inventory.

---

M5-T103 G4 test-adequacy review (independent, read-only, in-memory mutation) — part 1 of 2.

PIN: HEAD 68bc726e (material 3fab28f6). Method: local py3.11 bare-package shim; baseline 84/84 across test_pdf_object_streams (29) + test_sheet_reader (46) + the 62-case split-equivalence golden. Git-verified: app/documents/extraction and both forbidden test files BYTE-UNTOUCHED. All mutations applied to the CONSUMING namespace and restored.

MANDATORY GUARDS — all CAUGHT, non-vacuous:
- absolute inflated-bytes cap / inflate ratio / /Prev depth / objstm count / /N / shared per-document budget: the producer's 7 constant-rebind mutation tests each RED-under-mutation, GREEN-at-baseline, and assert a SPECIFIC feature string (disambiguates sibling guards).
- zip-bomb bounded memory: tracemalloc peak 0.58 MB, IDENTICAL across 3 runs (deterministic on CI); NON-VACUOUS — raising the caps materializes 103.83 MB, proving the cap is load-bearing. Charge-before-materialization confirmed in source (caps+charge precede out+=chunk; chunk <=64 KiB).
- encryption guard: load-bearing (rebinding _ENCRYPT_KEY -> test_encryption RED).

MY WEAKENINGS (>=5 required; 10 run) — mutation | site | result:
W1 /W zero-width type field default 1->0 | _parse_entries | MISSED (no test)
W2 /Index subsections reversed | _parse_entries | CAUGHT (multi_index)
W3 /Prev newest-LOSES | _collect_stream_entries | CAUGHT (prev_wins)
W4 Paeth c-instead-of-b | _paeth | CAUGHT (png4 only; png0-3 unaffected)
W5 PNG Up ignores previous row | _png_unfilter | CAUGHT (png2 only)
W6 objstm offset ignores /First | _load_object_stream | CAUGHT (all objstm tests)
W7 type-2 slot index off-by-one | _load_object_stream | CAUGHT (identity-check fires)
W8 scan-only gate BOTH directions | sheet_reader.any | CAUGHT both (never-fire -> scan_only RED; always-fire -> vector RED)
W9 _preview truncation | O/I/P._preview->identity | PARTIAL: only 2 of ~8 echo sites reddened
W10 shared-budget seed carry dropped | resolve_object_table | MISSED (no test)

Findings (BLOCKING vs ADVISORY) + verdict follow in part 2.

---

M5-T103 G4 review — part 2 of 2 (findings + verdict).

BLOCKING: none. No production defect found; every load-bearing guard and spec feature is caught by a non-vacuous test (11 mutations CAUGHT with precise, minimal blast radius).

ADVISORY (fast-follow test hardening; production code is CORRECT but unprotected):
A1 [W10] AS-2 cross-phase budget CARRY untested. The resolver's charged-bytes seed IS wired into the content decoder (verified in source), but zeroing it reddens NO test — including the shared-budget guard, which fires during resolve regardless. AS-2 explicitly claims "sharing the document-wide budget." Note: the SHARED-CAP half IS tested (facade constant=4 makes the resolver refuse). Add a fixture where xref+objstm decode PLUS content decode jointly exceed the budget while each fits alone.
A2 [W1] Zero-width /W type field default (`if w0 else 1`, ISO 32000-1 7.5.8.2) untested — all fixtures use w0>=1. Worse, test_narrow_field_widths_default_generation's comment CLAIMS "a zero-width type field" but uses widths (1,2,1) => w0=1. Fix the misleading comment and add a real w0=0 fixture (also covers the field3 `if w2 else 0` default).
A3 [W9] _preview truncation spot-checked at only 2 of ~8 echo sites (1 of 5 XObject /name echoes; 1 of 2 filter echoes; the resolver's OWN xref/objstm filter echo and the unsupported-operator echo untested). Low risk (uniform helper) but AS-3/DB-055(f)/DB-064(c) claim broader coverage.
A4 test_reads_uncompressed_xref_stream is a MISNOMER — the fixture FlateDecode-compresses the xref stream ("uncompressed" = no PNG predictor). The resolver's true no-/Filter branch (pdf_object_streams._decode_stream, filt is None) is untested.
A5 test_broken_type1_offset asserts only origin in {STRICT_READER, SHEET_PROFILE}, no feature check — non-vacuous (catches silent acceptance) but loose; tighten to the object/offset refusal.
Minor: 4 guard tests (ratio, objstm_count, objstm_N, shared_budget) lack an in-test green-baseline assertion (green is established via sibling tests); _DecodeBudget.charge's check-before-increment (no-overshoot) property is not independently asserted.

Scope/regression: only allowed paths changed; extraction + forbidden tests untouched; 62-case golden + sheet suites green unchanged.

Rationale: the mandatory security guards (zip-bomb, absolute cap, ratio, per-stream/per-document budget, /Prev depth+cycle, objstm count, /N) are all proven adequate and non-vacuous; every spec feature (entry types 0/1/2, /Index order, /Prev precedence, all five PNG filters, objstm offset/index, hybrid /XRefStm, scan-only both directions, encryption) is caught. The two MISSED items are correctness-preserving coverage gaps, not defects or vacuous tests.

M5-T103 G4 VERDICT: PASS
END-OF-REPORT
