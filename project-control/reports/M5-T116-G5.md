# M5-T116 — G5 security review (security-reviewer "sec-t116", read-only)

> Transmission history: pinned at 7bed25c0 (material 99c8457f), delivered as 3 SendMessage parts ending with
> END-OF-REPORT; no truncation. Joined verbatim by the orchestrator (transport wrapper tags removed only). Verdict PASS; ADVISORY-1
> the undetected distinct-vertex bowtie is a bounded correctness gap, not a security defect.

---

M5-T116 G5 SECURITY REVIEW (read-only, sec-reviewer) — PART 1/3

PIN: HEAD 7bed25c07aa25fecf5e3ca06908adb3b1c639aa4 at start (matches expected 7bed25c0). Material 99c8457f = cherry-pick of wt-m5t116 74e97bb4; `git show --stat 99c8457f` = exactly the 3 allowed paths (export_service.py, tests/cad/test_export_service.py, M5-T116-producer-report.md). Baseline claim seam c375457c.

METHOD: byte-level read of the diff, function-by-function trace of the new triangulation path, exhaustive raise-path audit, plus the offline suite from services/api (Py3.11, shapely/numpy).

EVIDENCE (cwd services/api): `python -m ruff check .` -> All checks passed. `python -m pytest tests/cad -q` -> 458 passed in 4.80s (includes the READ-ONLY owner-samples golden, so DXF/PDF byte-identity holds).

SCOPE CONFIRMED: the READ-ONLY deps are byte-unchanged across c375457c..99c8457f (massing_triangulation.py, massing_guards.py, glb_writer.py, dxf/pdf writers, claim_words.py, main.py, export_api.py — empty diff-stat). main.py has NO `export` reference; the service stays route-free/UNMOUNTED. So I can rely on M5-T109/T112 for the writer + triangulator internals AND re-verified them in source.

BLOCKING findings: NONE. One ADVISORY (correctness, non-security). Detail below.

1. RESOURCE BOUNDS ON THE NEW PATH (PASS). `triangulate_polygon` runs `_prepare_ring` FIRST (triangulation.py:286) — vertex cap MAX_OUTLINE_VERTICES=1000 (:102-105), finiteness (:114), magnitude MAX_COORD_ABS=1e8 (:118) — BEFORE `_triangulate`'s O(n^3) ear scan (:289). So the GLB footprint's effective cap tightens to 1000: build_export's raw cap is still dxf 10_000 (export_service.py:122), and a 1001..10000-vertex footprint now refuses INSIDE the triangulator as the typed `over_cap_vertices` (reconciled), never reaching the scan. Work budget: `_triangulate` charges `budget.charge(m-3)` at :228 BEFORE the `any(_point_in_triangle...)` scan at :229; default MAX_TRIANGULATION_WORK=2_000_000; a 1000-vertex ring costs ~498,501 units — the scan cannot run away. Mesh size is bounded: 2n<=2000 positions; walls 6n + caps 2*(n-2)*3 = 12n-12 <= ~11,988 indices — far under the writer's MAX_TOTAL_VERTICES=500_000 / MAX_TOTAL_INDICES=1_500_000 (glb_writer.py:158/160, re-checked :317-324). Per-request cost bounded; caps enforced before any writer.

(continues 2/3)

---

M5-T116 G5 SECURITY REVIEW — PART 2/3

2. MALFORMED INPUT / BOWTIE (PASS; one ADVISORY). Every triangulator failure is a typed MassingModelError (subclass of ValueError, guards.py:54; every raise sets a FIXED `reason` literal, no caller interpolation). `_render_glb` catches it (export_service.py:477) and reconciles. Every writer failure is GlbValidationError, which IS a subclass of GlbWriterError (glb_writer.py:204), caught at export_service.py:481. I audited every raise in `_prepare_ring`/`_triangulate`/`_WorkBudget` (all MassingModelError) and the arithmetic helpers (`_signed_area`/`_cross3`/`_point_in_triangle` — pure, no raise); `_is_finite_number` swallows the out-of-float-range int OverflowError (guards.py:100-103) so `abs()`/`_q()` only ever run on finite values. No untyped exception can escape.

No caller geometry reaches glb_writer without finiteness/magnitude: mesh positions derive from the PREPARED ring (`_prepare_ring` output — finite, |coord|<=1e8, quantized), NOT the raw footprint. `_coerce_footprint` catches only Type/Value/Overflow (export_service.py:349), so a `float("inf")` slips past it — but `_prepare_ring` finiteness then refuses it (typed), and the writer re-validates finiteness (`_real`/isfinite :271), the local bound (:393), degeneracy (:430-444), index range (:416) and counts. Proven: `test_hostile_float_in_glb_footprint_is_a_typed_refusal`.

ADVISORY-1 (CORRECTNESS, NON-SECURITY, NON-BLOCKING): a crossed bowtie with DISTINCT vertices whose edges self-intersect may NOT be refused — `_prepare_ring` only detects DUPLICATE vertices (triangulation.py:136), and ear-clipping can run to completion on some non-simple rings — yielding a valid-but-geometrically-wrong (self-overlapping) mesh instead of the `self_intersection` refusal AS-2's text implies. Security impact: NONE — the mesh is BOUNDED (<=n-2 cap triangles), finite, valid-index, and passes the writer's degeneracy/finiteness/bound checks, so there is no malformed/huge mesh, no untyped exception, no writer crash, no DoS amplification. It is a geometric-correctness gap (roof/floor drawn wrong for that input) that is G4/QA's domain, is disclosed by the producer, and the route is UNMOUNTED so no caller reaches it today. The committed test exercises the DETECTED (duplicate-vertex) variety (test line 523). Recommend recording as a DB-082/DB-084 correctness note for the mount packet; not a G5 blocker.

3. ERROR SURFACE (PASS). `_reconcile_triangulation` (export_service.py:319-327) keeps ONLY `exc.reason` (fixed code) + a server-built detail; the MassingModelError message — which interpolates the caller coordinate via `_preview` — is DISCARDED. Proven: `test_triangulator_refusal_reason_is_reconciled_without_caller_text` (asserts the coordinate absent) and the mutation `test_glb_triangulation_refusal_mutation_reddens` (removing the reconcile -> untyped raise WITH the coordinate -> reconcile is load-bearing). NO new logging in the module (no logging/print/logger import — verified; the only grep hit is the "footPRINT" substring). No stack/internal detail reaches the caller.

(continues 3/3)

---

M5-T116 G5 SECURITY REVIEW — PART 3/3 — FILENAME, SCOPE, VERDICT

4. FILENAME SAFETY (PASS — closes M5-T109 A1). New single choke point `_allowlist_token` (export_service.py:205-209) keeps only [A-Za-z0-9._-] (`str(raw)` first, so any type coerces) and caps at MAX_TOKEN_LEN=80. BOTH `build_filename_token` (:220) and the new `_sanitize_fallback_token` (:232) route through it; `build_export` now allowlists the fallback (:545-547) instead of the old `builder() or fallback_token` that trusted the RAW fallback — the exact M5-T109 A1 defense-in-depth gap, now closed. Empty/only-punct fallback -> caller-free `_DEFAULT_TOKEN="export"`. So the token in content_disposition/filename/provenance is ALWAYS allowlisted: no quote, ';', CR/LF or non-ASCII reaches the header; `filename*` is percent-encoded (:245-247). Behaviorally equivalent for the route's real uuid correlation-id fallback (hex+hyphen survive unchanged); only hostile fallbacks are hardened. DXF/PDF BODIES are unaffected (token is header/metadata only) -> byte-identity holds. Proven: `test_fallback_token_is_re_allowlisted_before_content_disposition` (hostile `"; DROP TABLE\r\nSet-Cookie: x` -> clean header, exactly 2 quotes/2 semicolons, no CR/LF), its skip-the-allowlist mutation, and `test_empty_fallback_uses_the_server_default`.

5. ZERO NEW DEPS / UNMOUNTED / main.py (PASS). The two new imports (app.scenario.massing_guards.MassingModelError, app.scenario.massing_triangulation.triangulate_polygon) are app-internal already-admitted modules — no PyPI/lockfile/requirements change. main.py byte-unchanged, no `export` reference; export_api.py unchanged; the service stays route-free. Material commit = exactly the 3 allowed paths.

DEFERRED SURFACES (carried, not regressed): the module is pure offline deterministic geometry + serialization — no DB/auth/tenant, no secrets/env, no network/SSRF, no filesystem/storage, no AI or ingested text (no prompt-injection surface), no logging. Cross-tenant isolation, service-role secrecy, private storage, upload controls, least-privilege and log redaction all belong to the PKT-H mount packet (M5-T109 A3 / M5-T112 INFO-2), correctly deferred; nothing here touches them.

SUMMARY: no CRITICAL / HIGH / MEDIUM. The new concave-safe cap path is guarded (prepare-ring FIRST), budget-metered (charge-before-scan), size-bounded, and fails closed through two typed exception families both caught and reconciled to the ONE redacted refusal that echoes no caller value or internal detail. The fallback-token re-allowlist closes M5-T109 A1 at a single choke point. Zero new deps; UNMOUNTED; main.py untouched; ruff clean; tests/cad 458 passed; DXF/PDF byte-identity preserved. The sole ADVISORY (undetected distinct-vertex bowtie -> valid-but-wrong mesh) is a bounded, typed-or-valid CORRECTNESS gap for G4/QA, disclosed and unmounted — NOT a security defect.

M5-T116 G5 VERDICT: PASS

END-OF-REPORT
