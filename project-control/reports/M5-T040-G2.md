# M5-T040 — G2 producer self-check record (recorded by the orchestrator)

G2 is the producer self-check gate; the CLI records it under the orchestrator's name (it
rejects the producer's own label). Basis, 2026-09-18 seq-118:

1. **Producer self-checks (loop-1 runs persistent-local-45 and -46, worker claude-opus-4-8):**
   both unit reports document all five documented commands supervisor-run green in-worktree
   (run 46: ruff clean; 98 passed matcher+wiring; 38 passed provider; 475 passed tests/api +
   test_rules_integration; modularity 0 failures with the wide_street_wiring.py warning-tier
   cohesion signal disclosed). Web deliberately not run locally (thin client) — CI is the
   only web proof channel, as the packet requires.
2. **Orchestrator reproduction at both harvests (in wt-m5t040):** the same five commands
   re-run green independently before each material commit (987930a2 and 42b72b10/751a1826);
   the modularity re-run was corrected to the repo-root cwd (the services/ cwd artifact was
   the orchestrator's own invocation error, not a failure).
3. **CI at the harvest head `4d8847c6`:** run 35407153078 success (all jobs incl. web +
   web-e2e), secret-scan 35407153073 success, context-budget 35407153117 success
   (orchestrator-captured: `M5-T040-ci-evidence.md`); product-path diff from material
   751a1826 to that head is empty.
4. **Scope check at harvest:** the union of both material commits = 11 files, all inside
   allowed_paths; contract files (schemas, generated TS, integration.py, response.py)
   byte-untouched.

**G2 verdict: PASS.**
