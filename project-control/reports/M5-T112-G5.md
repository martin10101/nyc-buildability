# M5-T112 — G5 security review (security-reviewer "sec-t112", read-only)

> Transmission history: pinned at 1461966b, delivered as 3 SendMessage part(s) ending with END-OF-REPORT;
> no truncation. Joined verbatim by the orchestrator (transport wrapper tags removed only). Verdict PASS; ADVISORY-1 is the same
> vacuous test; ADVISORY-2 and INFO-1/2 are carry-forwards for DB-082 a and the wiring packet.

---

M5-T112 G5 SECURITY REVIEW (read-only, sec-t112) — massing_model split (DB-079 a,b) — PART 1/3

PIN: HEAD 1461966b (matches expected). Material 7413ecf4 (cherry-pick of wt f31aeef5); seam commit 1461966b touched ONLY control-plane files (git diff 7413ecf4..1461966b = gates/reports/state/task), so HEAD material == 7413ecf4. Baseline = claim seam 275ef85f (the post-T106/T098 accepted module). Method: byte-level function-by-function compare pre-split -> split, in-process hostile probes, and the live suite, all from services/api (Py3.11, shapely/numpy).

GREEN AT REVIEWED HEAD: ruff clean; pytest tests/scenario = 817 passed. Zero new deps (AST allowlist test test_t098_as5 enforces {functools,hashlib,json,math,collections,dataclasses,typing,numpy,shapely,__future__}). No I/O, no logging, no os/subprocess/socket/eval/exec/open in any massing_*.py (grep-confirmed). UNWIRED: the only in-app importer is scene_assembler.py:63-69 (MassingModel, build_massing_model, build_from_generated_option — all three re-exported via the facade __all__, model.py:83-92). No route/app change.

POSTURE: pure offline deterministic geometry. No secrets/env, no network/SSRF, no SQL/shell, no filesystem/storage, no AI/ingested text (no prompt-injection surface), no auth. Same posture M5-T106 G5 recorded.

NOTHING LOST IN THE MOVE (each guard byte-identical to 275ef85f, verified):
- finiteness+magnitude: _is_finite_number (guards.py:88, incl. out-of-float-range int -> non-finite, the T106 MED-1 fix); MAX_COORD_ABS bound (triangulation.py:118).
- NYC-2263 raw-vertex check BEFORE the collinear collapse: _require_raw_vertices_in_nyc_bounds (guards.py:137) called from _prepare_ring (triangulation.py:131-132) on `raw`, ahead of the collapse at :150. Magnitude-first precedence preserved.
- MAX_TOTAL_FLOORS (model.py:123 / _check_floor_cap:207); mesh-vertex cap MAX_TOTAL_MESH_VERTICES (model.py:129 / _check_output_size:277).
- triangulation budget charged BEFORE the O(n^3) scan: _triangulate charges budget.charge(m-3) at triangulation.py:228 BEFORE the any(_point_in_triangle) scan at :229. Probe F: a (n-3) budget refuses with ZERO point-in-triangle calls. Intact.
- huge-int OverflowError: build_massing_model except-arm (model.py:403-416) still types it non_finite.
- shapely wrap: _wrap_geos_errors (guards.py:106, ShapelyError family) on build_massing_model (model.py:375).
- bounded echo: _preview (guards.py:66, MAX_ECHO_CHARS=120) + its repr-guard.

(continues 2/3)

---

M5-T112 G5 SECURITY REVIEW — PART 2/3 — NEW SURFACE + FINDINGS

NEW public triangulate_polygon (triangulation.py:262): SAFE. _prepare_ring runs FIRST (:286), so ring-prep guards (vertex cap via MAX_OUTLINE_VERTICES, finiteness, magnitude, duplicate, min-3 distinct) ALWAYS apply before any ear clip; budget defaults to a fresh _WorkBudget(MAX_TRIANGULATION_WORK) at :287-288 (and _triangulate re-defaults at :209), so no caller can reach the O(n^3) scan without metering or with an unbounded vertex count; the default budget equals the production 2M. Probe confirms guards + budget on the raw public path.

DB-079(b) _locate_overflow_field (guards.py:189) + helpers _outline_holds_overflow / _is_out_of_float_range_int: SAFE. Returns only the fixed default "proposed_massing.outline" or f"...levels[{pos}].outline" where pos is an int enumerate index — NO caller data echoed. Cannot raise: every branch is isinstance-guarded and math.isfinite is wrapped in try/except (guards.py:166-169) — load-bearing, because it runs INSIDE the except-OverflowError arm, so a raise there would escape untyped; it can't. Adds no new exception path and no unbounded echo (the OverflowError message is fixed). Equivalence test pins footprint-overflow -> outline, level-overflow -> levels[0].outline.

FINDINGS

ADVISORY-1 (test vacuity; security-adjacent DoS guard; NON-BLOCKING) — test_massing_model.py:1037 (test_t098_as4_scan_units_charged_before_the_inner_scan). The split relocated _triangulate into massing_triangulation, but this UNCHANGED test still patches massing_model._point_in_triangle (the facade binding). Because _triangulate.__globals__ IS the triangulation module, it resolves _point_in_triangle from THERE, so the facade patch is a no-op. Probe: facade-patched -> 0 pit calls during a real scan; triangulation-patched -> 3. The test's mutant ME (charge m-3 AFTER the scan) would NO LONGER redden it, so this "charged BEFORE the scan" mutation guard now passes vacuously. The PRODUCTION property is intact (probe F, part 1) — a lost TEST, not a lost bound. The commit-message claim "the accepted tests' facade-level monkeypatches still resolve" is inaccurate for this one seam; the other 6 targets (_triangulate, _build_prism, MAX_TOTAL_FLOORS, MAX_TOTAL_MESH_VERTICES, MAX_TRIANGULATION_WORK, Polygon) have facade-resident consumers and DO resolve. REMEDIATION: re-point the patch to massing_triangulation._point_in_triangle (one line; the file is in this task's allowed_paths). Route to qa-engineer (G4).

(continues 3/3)

---

M5-T112 G5 SECURITY REVIEW — PART 3/3 — REMAINING FINDINGS + VERDICT

ADVISORY-2 (defense-in-depth; NON-BLOCKING) — guards.py:189 _locate_overflow_field does not itself re-check the vertex/level caps; it relies on B0 validating cap BEFORE finiteness (proposal.py:227 len>MAX_OUTLINE_VERTICES=1000 precedes the per-vertex math.isfinite at :248; levels capped at MAX_LEVELS=500, :302). It is reachable only from the except-OverflowError arm — i.e. only after B0 raised — so its scan is bounded (<=1000 footprint + <=500x1000 level vertices), one extra linear pass over already-parsed data: no superlinear blow-up, not a DoS. If B0 ever reordered to check finiteness before the cap, this scan would become unbounded. Note for the B0 owner; no action here.

INFO-1 — triangulate_polygon deliberately OMITS the lot-only NYC range check (docstring :283-285). Correct for its intended consumer (DB-082 a GLB cap export of an already-B0-validated footprint). When DB-082 a wires it, ensure the ring is B0/NYC-validated upstream OR that the check is genuinely unneeded there. Magnitude / finiteness / vertex-cap / budget guards still apply on this path.

INFO-2 (carried forward from M5-T106 G5, unchanged) — wiring-packet duties, NOT this unwired split: enforce tenant/auth on scenario_id / property_geometry_version_id / rule_release_id (carried verbatim into provenance, model.py:474-486); treat lot_ring + outline as untrusted upload; cross-tenant isolation and log redaction belong to the route.

SUMMARY: no CRITICAL / HIGH / MEDIUM. Every input bound survives the move with the same order and strength (byte-identical guards + probe-confirmed charge-before-scan ordering). The one intended change (DB-079 b field label) adds no echo and no new exception path. The new public triangulator is guarded and budget-bounded. Zero new deps; unwired; no logging / secret / PII surface; no circular import (guards <- triangulation <- mesh <- facade). The sole regression is a now-vacuous DoS-guard TEST (ADVISORY-1), not a production posture change — so the monkeypatch-seam security question ("no production path reads a mutable global changing posture") passes.

M5-T112 G5 VERDICT: PASS

END-OF-REPORT
