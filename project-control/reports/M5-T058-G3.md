# M5-T058 G3 gate — independent code review (verbatim reviewer return)

Reviewer: code-reviewer (opus-4-8 xhigh per D-064), dispatched by the orchestrator at the
5f7b032e submit seam, pinned to reviewed sha 817a6546 (material 7d02eddc + the [ORCH-CORRECTED]
consumer fix). Recorded by the orchestrator; reviewer is read-only and records no gate itself.

---

I have completed a thorough cross-layer review and independently reproduced all documented gate commands. Here is my G3 gate report.

---

VERDICT: PASS

# G3 Independent Code-Review Gate Report — M5-T058

**Task:** M5-T058 — D-078 pair 1/2: condo substrate-substitution stamp; rule_evaluation contract 1.2.0
**Reviewer:** code-reviewer (independent, read-only)
**Date:** 2026-09-20

## Pinned identity (verified via read-only git plumbing)

- Reviewed sha: `817a6546` (`817a6546d284…`), the CI-green corrected head.
- Material commit: `7d02eddc` — 19 files, exactly the packet's allowed_paths (`git show --stat` confirmed; no forbidden path touched: grep for condo_records/evidence/scenario/main.py/proposal/profile/rulesets/connectors over `7d02eddc` returned NONE).
- Consumer fix: `817a6546` — 1 file only, `services/api/tests/scenario/test_scenario_contract.py` (+9/-1).
- Submit seam: `5f7b032e` (current HEAD) — control-plane only (state.json + task/report JSON).
- **Stability check:** all 18 code material paths `git diff 817a6546..HEAD` = EMPTY. The review was performed at HEAD, which is byte-identical to the reviewed sha for every material file. Peer commit `2fbd661e` (M5-T059 site-definition lane) landed in the range and is the sole source of the condo_records.py / condo-records.ts deltas vs the claim seam — disjoint from M5-T058, not a scope breach.

## Reproduced evidence (this checkout, explicit cwd, Python 3.11.9)

| Command | cwd | Result |
|---|---|---|
| `python -m ruff check .` | services/api | `All checks passed!` |
| `pytest tests/api/test_rule_evaluation_api.py tests/contracts/test_rule_evaluation_contract.py tests/rules/test_rules_integration.py tests/spatial/test_live_provider.py -q` | services/api | **168 passed** (matches evidence-map) |
| `pytest tests/scenario/test_scenario_contract.py -q` | services/api | 60 passed (consumer fix green) |
| `pytest tests/api/test_evidence_api.py tests/api/test_scenario_api.py tests/api/test_scenario_analysis_api.py -q` | services/api | **257 passed** (AS-3 compat) |
| `python tools/modularity_check.py --check` | repo root | `selected 467 files; failures 0; warnings 20`, EXIT 0 |

Web/TS proves in CI only (thin client); CI at `817a6546` reported all-green by the orchestrator — I verified web↔api coherence by source, not by running vitest.

## Cross-layer coherence (correct)

1. **Schema, both copies byte-identical** — `git rev-parse HEAD:<both paths>` → identical blob `764dd4c1`. `test_runtime_bundle_copy_is_byte_identical_to_canonical` also proves it functionally. Change is strictly ADDITIVE: contract_version enum `["1.0.0","1.1.0"]`→`+"1.2.0"`; fail_safe_reason enum `+"condo_base_lot_unresolved"`; new `substrate_substitution` added to `properties` but NOT to `required`; `additionalProperties:false` at top-level and nested `mixed_substrate`. Old 1.0.0/1.1.0 payloads stay valid (proven by `test_m5t058_old_documents_stay_valid_without_the_block`).
2. **Generated TS** (`rule_evaluation.ts`) consistent with the schema: `contract_version` union, `fail_safe_reason` union, and the optional `substrate_substitution?` block with the 9 fields + nested `mixed_substrate` all match the schema shape and nullability.
3. **API emission path is honest and provenance-carried.** `build_substrate_substitution_stamp` (`live_provider.py:309`) copies every field verbatim from the `CondoResolution` via `getattr` (never fabricated; `dataset_ids` coerced to a list). The stamp is threaded route → `evaluate_property(substrate_substitution=…)` → `PropertyRuleEvaluation.substrate_substitution` → `as_dict()` emits the block only when set (`integration.py`), and `response.py` serializes at `RULE_EVALUATION_CONTRACT_VERSION = "1.2.0"`.
4. **evaluated_input.bbl stays the ENTERED billing BBL** — `response.py:160` pops `evaluation.bbl` (the profile identity = billing BBL); the stamp alone carries entered-vs-analyzed. Proven by `test_m5t058_route_stamps_substrate_substitution` (`evaluated_input.bbl == BBL`, `analyzed_bbl == 1000010050`).
5. **D-078-R002 (no auto-select) honored in source.** `CondoResolution.resolved_base_bbl` is set ONLY for `OUTCOME_RESOLVED_SINGLE` (connector docstring + dataclass), so a multi-lot outcome physically exposes no single lot. `build_live_substrate_resolved` returns absent substrate + `condo_base_lot_unresolved` cause and NO base-lot pick on `is_fail_safe`. `test_m5t058_as2_condo_fail_safe_names_cause_no_auto_pick` asserts `ztldb_calls == [] and lot_calls == [] and layer_calls == []` for multi-lot/unresolved/typed-error — zero geometry lookups.
6. **Honest refusal vocabulary.** Condo-caused absence → `condo_base_lot_unresolved`; genuinely-absent non-condo → generic `spatial_intersection_absent`. Both directions bound at the provider, integration, and route layers (`..._genuine_absent_non_condo_keeps_no_condo_cause`, `..._route_non_condo_absent_keeps_generic_reason`, integration `..._keeps_generic_reason`).
7. **AS-3 additive compatibility.** The three forbidden consumers (`evidence.py`, `scenario.py`, `scenario_analysis.py`) are byte-unchanged (git diff empty vs base) and keep calling the retained `get_spatial_substrate_provider` / `build_live_substrate` `object|None` seam; `build_live_substrate` is now a delegating facade returning `.substrate`. At-most-one SODA call proven (`..._delegates_substrate_only`, `..._flag_on_carries_stamp_single_call`). Their suites: 257 passed.
8. **DB-036(d) closure is correct and safety-preserving.** `AnalysisIdentityNotice.tsx` computes `stampLegitimate` requiring `entered_bbl === requestedBbl === evaluated_input.bbl` and a distinct non-empty `analyzed_bbl`; that branch renders a display-only "analyzed on the base lot" record and fires BEFORE the withhold guard. An unstamped mismatch, a non-corresponding stamp (entered_bbl ≠ opened, or ≠ evaluated_input.bbl), and a degenerate stamp (analyzed == entered) all fall through to the unchanged `role="alert"` withhold. Comprehensive both-directions coverage in `analysis-identity-substitution.test.tsx` (6 cases), typed DOM queries per CODING_RULES.
9. **Web contract lib** adds `condo_base_lot_unresolved`, `1.2.0`, and a positive-shape `checkSubstrateSubstitution` (absent valid; malformed present block fails total validation) wired into `validateRuleEvaluationDocument`; `development-limits.ts` adds the required total-record label. Consistent with the generated types (the `NonNullable<...fail_safe_reason>` and `contract_version` `satisfies` constraints force the additions).
10. **Consumer fix `817a6546`** is exactly the sanctioned enum admission (updates the frozen-contract guard from `["1.0.0","1.1.0"]` to include `"1.2.0"`, M5-T037/`f954aa59` precedent cited in-file), nothing else.

## Contract additive-compat proof (contracts test suite)

`test_rule_evaluation_contract.py` proves: enum appended not substituted; block optional/absent-valid; old 1.0.0/1.1.0 valid; stamped 1.2.0 round-trips with `entered_bbl == evaluated_input.bbl` and the runtime bundle validates the same instance; closed object at both levels; all 9 fields required; new fail_safe_reason admitted, unknown rejected; version outside the closed enum rejected.

## Modularity

`failures 0`, exit 0. `integration.py` is a warn-band `review_signal` (below justify/hard); `live_provider.py` is not among the 20 warns. Both changes are additive threading of one optional record + one boolean through an existing cohesive boundary — no responsibility mixing, no new persistence/I/O/presentation; `build_live_substrate` facade preserves the public interface. Cohesion justification adequate (packet SS6). Pass.

## Findings

**F1 — LOW / advisory (non-blocking).** `services/api/app/api/v1/rule_evaluation.py` docstrings are stale: line 21 ("return a versioned `rule_evaluation` @ 1.0.0 document") and line 255 ("return a rule_evaluation @ 1.0.0 document") still say 1.0.0, but the serializer now emits 1.2.0 (`response.py` correctly documents this). Pre-existing (also wrong at the 1.1.0 bump) but carried into a file edited this packet. No functional impact. Recommend a follow-up doc fix (route the correction to the orchestrator; out-of-scope to fix in-gate).

## Informational (not a finding)

- The resolved-single-condo → base-lot-absent path (base lot has no candidate districts / partial page) intentionally drops the substitution stamp and yields generic `spatial_intersection_absent` rather than surfacing that a billing→base substitution occurred. Deliberate and tested (`test_m5t058_resolved_base_lot_absent_substrate_no_stamp`): the stamp describes a mixed substrate that only exists when composition succeeds. Honest fail-safe; acceptable. Flagged so the paired M5-T059 site-definition team is aware that a successful condo resolution whose base lot has no zoning assignment presents as a generic spatial-absent refusal.
- D-078-R002 (no auto-select) and D-078-R003 (additive 1.2.0 substitution stamp) are visibly honored in source and tests. The full requirement-by-requirement directive verification (D-066/D-077/D-078) is the separate `directive-compliance-verifier` pass (producer ≠ verifier), not this G3 code gate.

## Verdict

**PASS.** Correctness, cross-layer contract coherence (api emission ↔ both schema copies ↔ generated TS ↔ web decode), additive backward-compatibility, honest fail-safe error paths, D-078-R002 no-auto-pick, AS-1..AS-6 coverage, provenance discipline (verbatim carry, byte-matched), and DB-036(d) closure are all satisfied and independently reproduced. F1 is a low-severity documentation staleness that does not block acceptance; recommend an orchestrator-routed doc correction.

Relevant absolute paths:
- `C:\Users\MLFLL\Downloads\nyc-zoning\ctl24\services\api\app\spatial\live_provider.py`
- `C:\Users\MLFLL\Downloads\nyc-zoning\ctl24\services\api\app\rules\integration.py`
- `C:\Users\MLFLL\Downloads\nyc-zoning\ctl24\services\api\app\rules\response.py`
- `C:\Users\MLFLL\Downloads\nyc-zoning\ctl24\services\api\app\api\v1\rule_evaluation.py` (F1 lines 21, 255)
- `C:\Users\MLFLL\Downloads\nyc-zoning\ctl24\packages\contracts\schemas\v1\rule_evaluation.schema.json` and `C:\Users\MLFLL\Downloads\nyc-zoning\ctl24\services\api\app\_contract_schemas\v1\rule_evaluation.schema.json` (byte-identical, blob `764dd4c1`)
- `C:\Users\MLFLL\Downloads\nyc-zoning\ctl24\apps\web\src\components\architect\AnalysisIdentityNotice.tsx`
