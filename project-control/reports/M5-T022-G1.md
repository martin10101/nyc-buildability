# M5-T022 — G1 Code Review Gate Report (verbatim)

Orchestrator preservation note: report returned by the independent code-reviewer agent
("G1 code review M5-T022") on 2026-09-13 and preserved verbatim below (transport
entity-decoding only). Reviewer was read-only with live registry/OSV access; frozen
review identity: pin f17a5869 + bot lock 6d776102. Verdict: PASS (no defects).

---

Independent verification complete. All checks pass. Returning the gate report.

---

# Gate Report

- **Gate ID:** G1
- **Task ID:** M5-T022 (maplibre-gl@6.7.0 dependency admission, D-040-R001)
- **Reviewer:** code-reviewer (independent; read-only)
- **Producer:** orchestrator
- **Result:** PASS
- **Clean environment/worktree used:** primary checkout at frozen HEAD `0bfcc08b`; read-only inspection of the two frozen material commits `f17a5869` (pin) + `6d776102` (bot lock). Live registry/OSV spot-checks via curl.

## Frozen identity confirmed
- HEAD = `0bfcc08b26560dfa97b23bc25b54bf89e49b9572` ✓ (matches contract).
- `git log f17a5869..HEAD -- apps/web/package.json apps/web/package-lock.json` returns **only** `6d776102`. No later commit touches either dependency file ✓. The three commits after the lock (`44c1012d`, `4afa0153`, `0bfcc08b`) touch only `project-control/` reports/state/tasks — nothing CI-relevant.

## Acceptance criteria reviewed
S1 exact_admission, S2 workflow_proven, S3 ci_green, S4 provenance_review — all reproduced independently below.

## Directive/requirement verification

| Requirement ID | Reviewed SHA / content identity | Verdict | Reproduced evidence |
|---|---|---|---|
| D-040-R001 | pin `f17a5869` + bot lock `6d776102` | PASS | D-040-R001 releases the expansion hold for exactly the lot-outline increment and mandates MapLibre GL JS (`.claude/rules/3d-ui-expansion.md:5`). This task admits exactly `maplibre-gl@6.7.0` — the newest age-gate-passing version — as the web-half prerequisite. B-022 is `resolved`; the D-042-R002 discharge (blocker `resolution.consequence`) explicitly authorizes the admission to proceed under its own G5 + age gate. Requirement satisfied at the frozen identity. |

Note: the binding directive verdict is written by the independent `directive-compliance-verifier` in the directive's `verification.json` (producer ≠ verifier). This G1 pass corroborates D-040-R001 is satisfied at the frozen identity.

## Steps independently executed
1. `git show --stat` on both commits (author + diff shape).
2. `git show f17a5869 -- apps/web/package.json` (exact pin).
3. Enumerated added/removed `node_modules/` keys and every added `version`/`resolved`/`integrity` in the lock diff.
4. Traced the single removed line (`minimist` `dev:true`) to its cause.
5. Grep `apps/web/src` for `maplibre|mapbox|maptiler|leaflet`.
6. Live registry age spot-check of `6.7.0/6.8.0/6.9.0` + 9 transitives; OSV query; `6.7.0` manifest (license/repo/integrity/deprecated).

## Expected versus actual

**S1 — exact_admission: PASS**
- `package.json`: exactly ONE added line — `"maplibre-gl": "6.7.0"` (exact pin, no `^`/`~`; save-exact posture). Only package.json change. Root `packages[""].dependencies` in the lock also carries `"maplibre-gl": "6.7.0"`.
- Lock: **22 added nodes, 0 removed** — matches the producer's enumeration table one-for-one. All 22 `resolved` from `registry.npmjs.org` (0 non-npmjs); all 22 `integrity` are `sha512-` (0 non-sha512). `maplibre-gl` = `6.7.0`.
- Single existing-node change: `minimist` loses `"dev": true` (promoted to production because the added `@maplibre/maplibre-gl-style-spec` declares `minimist ^1.2.8`). Its version `1.2.8` and integrity are unchanged — benign dedup/promotion, not version drift. No other existing node changed.
- Supply-chain integrity: registry `dist.integrity` for `maplibre-gl@6.7.0` = `sha512-Y1Q1+UP9WXou0DUrnaFdDsoMqiMCWy6aS968IBUaQ2eZi6H1/kPCfpfZOWXFJZbeKMkX9Wo6OFzY/k48qqPf3Q==`, **exactly matching** the committed lock entry.

**S2 — workflow_proven: PASS (corroborated)**
- Live registry: `6.7.0` published `2026-09-02T14:27:35Z` → age 811,944 s (research date) / ~865k s (bot commit) ≥ 604,800 s → PASS. `6.8.0` = 351,202 s and `6.9.0` = 212,938 s → FAIL. Confirms the research's newest-aged-version claim.
- Transitive spot-check (9 nodes incl. the newest maplibre-scoped): all ≥ 604,800 s; smallest `@maplibre/maplibre-gl-style-spec@26.4.1` = 1,567,091 s.
- Bot lock `6d776102` authored by `github-actions[bot]`, touching ONLY `package-lock.json` (+200/−1). Proof chain recorded as generate-lockfile run `34729750887` SUCCESS before the commit.
- Limitation: I did not re-run `gh` (read-only + skill prohibits gh). Verified via the committed evidence plus independent registry/integrity corroboration, which is consistent with a proper npm-ci-integrity + whole-tree-audit + age-gate proof chain.

**S3 — ci_green: PASS (captured evidence + material coverage)**
- `M5-T022-ci-evidence.txt`: CI run `34729820746` at head `44c1012d` = CONCLUSION SUCCESS, all jobs green incl. `web-dependency-security`, `web`, `web-e2e`.
- The reviewed material (pin + lock) last changed at `6d776102`, which is an ancestor of `44c1012d`; the two commits after `44c1012d` are project-control-only. Therefore CI at `44c1012d` validly covers the frozen material through HEAD `0bfcc08b`. Independently verified the material-coverage relationship; did not re-run gh.

**S4 — provenance_review: PASS**
- `maplibre-gl@6.7.0`: license **BSD-3-Clause** (registry-confirmed), repo `github.com/maplibre/maplibre-gl-js` (official MapLibre org), not deprecated. **OSV: 0 vulns** affecting `6.7.0`; CVE-2026-85061 (fixed 6.4.1) absent. Integrity matches registry (see S1).
- 22-node tree enumerated from the lock diff matches the producer table exactly; every node npmjs + sha512; no rogue registry, no unexplained node.

## Regression/security/provenance findings
- Prohibitions honored: **no** `apps/web/src` file imports `maplibre|mapbox|maptiler|leaflet` (grep: no files) — the unused-dependency window is present and intended. `render.yaml`, `.github/**`, `services/**`, `packages/**`, `apps/web/.npmrc`, `apps/web/e2e/**` untouched by both frozen commits (`--stat` confirmed).
- No guessed schema, no hard-coded legal value, no missing migration/RLS (dependency manifests only), no hidden default, no silent uncertainty. Modularity N/A (manifests only).

## Defects
None.

## Observation (not a defect)
The contracting commit `f17a5869` also writes control-plane files that appear in the packet's `forbidden_paths` (`project-control/tasks/M5-T022.json`, `state.json`, `directives/D-040-scoped-unblocks/*`, `gates/`). These are the orchestrator's control-plane contracting/claim/G0/directive-bind operations under `producer=orchestrator` (recorded in `M5-T022-G0.md`, M5-T021 precedent), not producer deliverable edits. The frozen material identity for this review — the package.json pin + the bot lock — is exactly the two clean commits and is unaffected. Flagged for transparency; consistent with precedent.

## Required rework
None.

## Reviewer conclusion
The admission is exactly one save-exact pin of `maplibre-gl@6.7.0` plus a bot-generated, integrity-matched, fully-npmjs, age-gate-clean 22-node transitive lock, with the only existing-node delta being a benign `minimist` dev→prod promotion. The age gate (independently reconfirmed live: 6.7.0 passes, 6.8.0/6.9.0 fail), OSV cleanliness, BSD-3-Clause license, official repo, and registry-matching integrity all hold; source imports nothing yet (intended window); forbidden production paths are untouched; D-040-R001 is satisfied at the frozen identity with B-022 resolved and the D-042-R002 discharge authorizing the admission.

**VERDICT: PASS**

Relevant paths (all absolute):
- `C:\Users\MLFLL\Downloads\nyc-zoning\ctl24\apps\web\package.json`
- `C:\Users\MLFLL\Downloads\nyc-zoning\ctl24\apps\web\package-lock.json`
- `C:\Users\MLFLL\Downloads\nyc-zoning\ctl24\project-control\tasks\M5-T022.json`
- `C:\Users\MLFLL\Downloads\nyc-zoning\ctl24\project-control\reports\M5-T022-producer-report.md`
- `C:\Users\MLFLL\Downloads\nyc-zoning\ctl24\project-control\reports\M5-T022-ci-evidence.txt`
- `C:\Users\MLFLL\Downloads\nyc-zoning\ctl24\project-control\reports\M5-T022-evidence-map.json`
- `C:\Users\MLFLL\Downloads\nyc-zoning\ctl24\project-control\reports\M5-T022-G0.md`
- `C:\Users\MLFLL\Downloads\nyc-zoning\ctl24\project-control\reports\D-040-R001-mappluto-research.md`
- `C:\Users\MLFLL\Downloads\nyc-zoning\ctl24\project-control\blockers\B-022-whole-tree-advisories-block-maplibre-admission.json`

Note for the orchestrator: I am read-only and did not run `project_control.py`, git-write, or `gh`. Please save this report verbatim and record the G1 result. The `gh` run IDs (generate-lockfile `34729750887`, CI `34729820746`) are corroborated by committed evidence and independent material/registry checks but were not re-fetched via `gh` in this review.
