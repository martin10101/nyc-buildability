# M5-T040 — G4 QA review (verbatim reviewer return; transport entity-decoding only)

Saved by the orchestrator from the qa-engineer agent return, 2026-09-18 (seq-118 wave,
pinned at submission sha b4865d1c). Nothing added or removed beyond decoding transport
HTML entities.

---

I have completed the independent G4 QA review. Verdict below.

---

# G4 QA GATE REPORT — M5-T040 (named-street override WIRING + display gating)

**Verdict: PASS**

Reviewed identity: sha `b4865d1c` (submission); material `987930a2` (DB-023/DB-021 hardening) + `751a1826` (wiring + provider status + DB-025 display). Independently confirmed the reviewed code identity equals the CI-tested identity: `git diff 751a1826 4d8847c6 -- apps/ services/ packages/` is **empty**, and `b4865d1c` adds only two control-plane files over `4d8847c6` — so CI run **35407153078 (success @ 4d8847c6)** genuinely proves the web behavior of the exact reviewed code. API suites orchestrator-reproduced 98/38/475; ruff clean; modularity exit 0 (21 warnings, 0 failures).

## Scenario adequacy table

| AS | What it requires | Proven by (file → test) | Verdict |
|---|---|---|---|
| **AS-1** DB-023a: crafted qualifier→matched_override refused | `test_named_street_override.py`: `test_db023a_matched_override_with_open_question_fails_closed`, `..._real_qualified_snapshot_matched_override_metadata_bypass_refused[remove/null/empty]`, `..._all_qualifier_signals_stripped_still_refused[remove/null/falsely_resolved]`, `..._all_signals_stripped_refusal_is_source_bound` (differential: conditional REFUSED vs unconditional ALLOWED), `..._narrowed_source_quote_to_omit_condition_refused` (match="COMPLETE sentence span"); over-refusal guards `..._without_open_question_still_allowed`, `..._unconditional_row_with_no_qualifier_signals_reaches_override` | ADEQUATE — exemplary; differential + narrowing tests force the failure mode and prove the refusal is source-bound, not merely keyed on `matched_override` |
| **AS-2** DB-023b: malformed provision_id/section_anchor fails closed at construction, no match() KeyError | `test_db023b_missing_or_mistyped_provenance_fails_closed[None/None/7/empty]`, `test_db023b_alt_block_missing_provenance_fails_closed` | ADEQUATE — all fields match() reads are construction-validated (verified in source); "never KeyError" holds structurally |
| **AS-3** DB-023c: bounded MatchResult.reason reprs | `test_db023c_reason_bounds_long_query_input` (5000-char inputs across unknown-street / outside-frontage / wrong-CD paths; asserts len<300/400 and huge not in reason) | ADEQUATE — forces the unbounded-repr failure mode |
| **AS-4** wiring truth table (all rows) | `test_wide_street_wiring.py`: all-NOT_MATCHED→attest (`..._all_clear_lets_exceptions_checked_become_true`); MATCHED_OVERRIDE→PR with provenance (`test_matched_override_forces_professional_review_with_provenance` asserts sha256+provision_id+section_anchor in reason; `..._carries_distinct_provenance` asserts all 4 fields exactly; `..._preempts_all_narrow_standard_row`; `..._wins_over_indeterminate`); INDETERMINATE→refusal (`..._indeterminate_segment_is_unresolved_refusal`); missing/non-string/list-CD→refusal (`..._missing_bounds`, `..._non_string_bounds/borough`, `..._list_community_district`, `..._mixed_*_blocks_clear` order-independent); empty→refusal | ADEQUATE — every truth-table row forced; no unforced row found; provenance asserted exactly |
| **AS-5** ceilings + DB-021(e) + DB-025(d) | `test_wide_street_live_provider.py`: `test_db021a_lot_over_vertex_cap_...` (asserts segments NOT fetched), `test_db021b_wide_segments_over_cap_...` (asserts geometries NOT fetched), `test_db021e_unexpected_wide_geometry_error_returns_none` (event + error_type=RuntimeError), `test_db021e_attested_lot_rejects_every_unusable_subbranch` (5 sub-branches parametrized) + positive companion; **DB-025(d): `test_db025d_multipage_each_segment_carries_its_own_page_digest` is PRESENT** (two distinct page bodies; page-2 seg carries page-2 digest) | ADEQUATE — the carryover "check first, do not duplicate" question resolves: the multi-page page-digest fixture EXISTS and asserts per-page digest binding; **not a gap** |
| **AS-6** display gating on all 3 surfaces + parity | `development-limits.test.tsx` (within/not-within/review each on DevelopmentLimits incl. aria-label both ways); `report-view.test.tsx` (DB-025a within/review scoped to `wide-street-provenance`; DB-025c not-within/within/review incl. `{hidden:true}` closed-`<details>` heading queries; `reportValue===screenValue` for within 3.44 and not-within 2.20) | ADEQUATE — parity structurally proven (one shared component tree); heading-regression-safe |
| **AS-7** ruff/suites/modularity/contract byte-untouched | Material commits touch ONLY allowed_paths (verified `git diff --name-only`); no integration.py/response.py/schema/generated-TS edit; ruff+modularity reproduced | ADEQUATE |
| **AS-8** CI green + DRAFT/not-verified preservation | CI 35407153078 success at material identity; DRAFT markers asserted; `coverage_hint != "verified"`, `determination_state != "published"` | ADEQUATE |

## Findings

All ADVISORY; none BLOCKING.

1. **ADVISORY** — `CalculationEvidence.tsx:17,42` (review label). The DB-025(a) review-label gating is not *independently* forced apart from a null-FAR heuristic: no test uses a review-state document with non-null FAR (or a confident-state document with null FAR), so within/review agreement rides the server invariant. Mitigated: the code manifestly gates on `determination_state`, and `report-view.test.tsx:195`/`development-limits.test.tsx:621` (not-within, non-null FAR 2.20) *do* force determination_state gating for the sibling value-label (a null-FAR heuristic cannot distinguish within from not-within). Suggest a future review-state-with-non-null-FAR probe if the server invariant is ever relaxed.

2. **ADVISORY** — `wide_street_wiring.py:582` (branch 3) + `_elevated_exceptions_checked`. Untested benign combination: an all-narrow lot whose candidate `segment_may_touch_named_override` but is unresolved returns `NOT_WITHIN` (standard/conservative row) with `exceptions_checked=False` rather than escalating to professional review. This is fail-safe and honest (higher FAR withheld; exceptions_checked surfaces the gap), but the combination has no dedicated test.

3. **ADVISORY** — `wide_street_wiring.py:419-424`. The `prov is None` defensive branch on the MATCHED_OVERRIDE path is unreachable (the matcher always attaches `_row_provenance` to MATCHED_OVERRIDE) and untested. Harmless defensive code.

4. **ADVISORY** — `wide_street_wiring.py` is over the modularity WARNING threshold (disclosed by producer; 0 failures, exit 0). Cohesion signal for the G3 code reviewer to weigh before further growth; not a CI failure and outside QA test-adequacy scope.

5. **ADVISORY** — `M5-T040-producer-report.md` is framed "PARTIAL / backend PENDING" while the submission (`b4865d1c`) + evidence-map cover the full backend+web scope. Stale framing (written mid-loop); this G4 wave IS the independent backend review the report defers to. Coherence nit, no defect.

## Read-only commands run (all git-read/Grep/Read; no git-write, no project_control, no npm)
- `git cat-file -t` on b4865d1c/987930a2/751a1826/4d8847c6 (all commits exist)
- `git show b4865d1c:<path>` for the packet, producer report, ci-evidence, evidence-map, and all 12 source/test files (extracted to scratchpad for line-anchored reading)
- `git show --stat` on both material commits; `git diff --name-only <c>^ <c>` (material touches only allowed_paths)
- `git grep -n "Wide-street conditional FAR" b4865d1c -- apps/web` (string confined to the 2 components + 2 test files; no other/e2e suite asserts the old always-on heading)
- `git grep -ln "wide-street-provenance|development-wide-street|wide-street-result"` and e2e sweep (`apps/web/e2e` has zero wide-street refs; no `calculation-evidence.test.tsx` exists — CalculationEvidence proven via report-view.test.tsx)
- `git diff --stat 751a1826 4d8847c6 -- apps/ services/ packages/` → **empty** (CI head == material); `git diff --name-status 4d8847c6 b4865d1c` → only 2 control-plane files

## Verification limits
Web suites were NOT run locally (thin-client policy; web proves only in CI). I verified the CI-tested identity is byte-identical to the reviewed material, so CI run 35407153078 (success) is valid proof; CI-green additionally confirms the `{hidden:true}` heading queries are non-vacuous (a missing element would throw). API suites were orchestrator-reproduced (98/38/475), not re-run in my sandbox (worktree is pinned to a pre-M5-T040 HEAD and git checkout is a read-only-guard-blocked write).

Report artifacts (this reviewer, read-only): memory written at the reviewer's own agent-memory directory (`m5t040-named-street-wiring-gate.md`, `wide-street-display-gating-test-adequacy.md`, `MEMORY.md`). The orchestrator should save this verbatim report and record the G4 gate.
