# M5-T041 — G4 QA review (verbatim reviewer return; transport entity-decoding only)

Saved by the orchestrator from the qa-engineer agent return, 2026-09-18 (seq-118 wave,
pinned at submission sha 843e23c4). Nothing added or removed beyond decoding transport
HTML entities.

---

G4 QA gate review complete. Verdict below, returned verbatim for the orchestrator to record.

---

# GATE REPORT — M5-T041 (G4 independent QA)

**Task:** M5-T041 — Address-flow + validator polish cluster (DB-024 a–d + DB-025(e))
**Reviewer:** qa-engineer (independent; read-only)
**Reviewed identity:** submission sha `843e23c4`; MATERIAL commit `740b6f2f` on `candidate/D-024-mrl-option-b`
**Verdict: PASS** (no BLOCKING findings; 4 ADVISORY)

## Identity / integrity verification
- `git diff --name-only 740b6f2f..843e23c4` → control-plane only: `M5-T041-ci-evidence.md`, `M5-T041-evidence-map.json`, `state.json`, `M5-T040.json`, `M5-T041.json`. `git diff --stat 740b6f2f..843e23c4 -- apps/ services/ packages/` → empty. `git merge-base --is-ancestor 740b6f2f 843e23c4` → true. So CI at the material head speaks for the pinned apps/ identity.
- Material commit touches exactly the 10 allowed apps files + report (+818/-32). `apps/web/src/lib/address-search.ts` NOT in the diff (AS-6). Nothing outside `allowed_paths`.
- AS-7 web proof: CI run `35402081543` **success** at `740b6f2f` (incl. web + web-e2e), per orchestrator-captured `M5-T041-ci-evidence.md`. Accepted as the authoritative web execution evidence.

## Scenario-by-scenario adequacy

| AS | Verdict | Proven by what (independently reproduced) |
|----|---------|-------------------------------------------|
| AS-1 (a) non-circular full-search failure copy | ADEQUATE | `autocomplete.test.tsx` DB-024(a) forces the `fullSearchActive` branch: mock full `/search` → error, click `full-address-search`, assert status `not.toHaveTextContent(FULL_ADDRESS_SEARCH_LABEL)` + `/manual entry/i` + `use-manual-entry` present. I confirmed in `AddressAutocomplete.tsx:151` the copy is selected by `fullSearchActive ? FULL_SEARCH_ERROR_MESSAGES : ERROR_MESSAGES`; an unchanged impl would emit the button label and fail the first assert — genuinely forced, not happy-path. Twin test forces the TYPED path (still names the button). |
| AS-2 (b) shared absent-BBL note, byte-identical, via import | ADEQUATE | Single export `ABSENT_BBL_MAP_LINK_NOTE` (`AddressAutocomplete.tsx:23`). Asserted through the IMPORT (`.toBe(constant)`) on all three: `address-confirm.test.tsx` S7; `zoning-context-panel.test.tsx` AS-6 ×2 (panel + overview, non-canonical and literal-null). No re-typed literal. |
| AS-3 (c) aria-hidden + accessible name for both glyphs | ADEQUATE | address-hint `↗` (`autocomplete.test.tsx` DB-024(c)): span `aria-hidden`, name "NYC Planning address suggestions". ProfileViews `→` (`zoning-context-panel.test.tsx` DB-024(c)): span `aria-hidden`, name "Inspect calculation evidence". Both assert glyph hidden AND accessible name excludes glyph. |
| AS-4 (d) three ZoLa sites, one accessible name | ADEQUATE | `ZOLA_LOT_LINK_LABEL` asserted via import by role+name on all three: confirm-card S7; panel AS-5; overview site link AS-5 (scoped to `.architect-map-card`). |
| AS-5 (e) validator wording matches behavior, no fixture verdict changed | ADEQUATE | Material diff to `rule-evaluation-contract.ts` is **comment-only** (module + fn doc), so behavior is provably unchanged. Grep confirms the only `additionalProperties`/key-rejection reference is the corrected comment — no `Object.keys` rejection exists, so the positive-shape claim is truthful. `contract-versions.test.ts` DB-025(e) proves baseline `draftApplicableDoc()` (real fixture from `@/test-support/rule-evaluation-fixtures`) stays `ok` before and after adding an unknown key; existing admission suite unchanged. |
| AS-6 no retry/outcome change; modularity clean | ADEQUATE | `address-search.ts` untouched (not in diff, not in allowed_paths). Modularity: orchestrator-captured exit 0 + inspection (no new files; largest touched file `AddressAutocomplete.tsx` = 213 SLOC). |
| AS-7 CI green on pushed head | ADEQUATE | Run `35402081543` success at material head; apps/ identical material↔pinned. |

## Findings

1. **ADVISORY — new cross-component import edge, but contract-sanctioned.** `AddressConfirmCard.tsx:8`, `PropertyOverview.tsx:11`, `ZoningContextPanel.tsx:8` now import the shared string constants FROM the `AddressAutocomplete` component (a heavier module pulling `address-search`/hooks). Task input (b) explicitly permits homing "in AddressAutocomplete.tsx or another in-scope home," so this is authorized and passes the checker. If this shared-copy set grows, a dedicated presentation-strings module would reduce coupling. No action required.

2. **ADVISORY — cross-file test coupling on the constant's value.** `development-limits.test.tsx:185,198` (M5-T040 territory, out of this packet's allowed_paths) queries `/Open ZoLa/` and now implicitly depends on `ZOLA_LOT_LINK_LABEL="Open ZoLa"`. Verified safe: `PropertyOverview.tsx:56-73` renders the nested `ZoningContextPanel` OUTSIDE `.architect-map-card`, so `within(siteCard())` still resolves exactly one link, and CI is green. If the shared label ever changes, that raw-regex test could silently drift — worth a future note that it should import the constant.

3. **ADVISORY — full-search failure copy has unit coverage but no e2e.** The new `FULL_SEARCH_ERROR_MESSAGES` path is proven only in `autocomplete.test.tsx`. The one e2e that touches failure copy (`architect-workspace.spec.ts:216-236`) mocks `/v2/autocomplete` (TYPED path) and asserts the unchanged `ERROR_MESSAGES.rate_limited` sentence at line 226 — it never clicks full-search, so the new copy is not exercised end-to-end. Acceptable (unit forces it; CI green), but a candidate for a follow-up e2e.

4. **ADVISORY — two test-completeness gaps the producer did not claim (neither a correctness defect):** (a) the `fullSearchActive` edge "typed failure THEN full-search failure" is not forced — the DB-024(a) test uses empty typed suggestions, not a typed error; behavior is nonetheless correct by construction (`AddressAutocomplete.tsx:100` gives `searchResult` precedence). (b) The confirm-card absent note is tested only on a non-canonical BBL (`address-confirm.test.tsx` S7, bbl="12345"); the literal-null path for the confirm card is not exercised there (it is, for the panel/overview, in `zoning-context-panel.test.tsx` AS-6). Byte-identity holds regardless since it renders the same constant (`AddressConfirmCard.tsx:150`). Also noting pre-existing unwrapped decorative arrows outside AS-3's two named glyphs — PropertyIssuesSummary "Review conflicting source values →" / "Review missing inputs →" (`PropertyOverview.tsx:26,33`) and "All property facts →" (`:77`); these are pre-existing (not in this diff) and outside the binding scope (c)/AS-3, so route as discovery, not a defect here.

## Directive note
D-066-R001 (code-graph navigation block) is a process/navigation requirement; the packet embeds the required navigation block. Authoritative directive verification is the `directive-compliance-verifier`'s call, recorded in the directive `verification.json` — not this QA gate.

## Read-only commands run (from worktree `agent-a1b95c70bce6c65bb`, shared object store)
- `git log --oneline -3 740b6f2f`; `git show 740b6f2f --stat`; `git show 740b6f2f -- apps/web`
- `git diff --name-only 740b6f2f..843e23c4`; `git diff --stat 740b6f2f..843e23c4 -- apps/ services/ packages/`; `git merge-base --is-ancestor 740b6f2f 843e23c4`
- `git ls-files 'apps/web/**e2e/**' '*.spec.ts'` (locate e2e)
- Grep (apps/web): `Open in ZoLa|View this lot on the city|this result did not provide|Open ZoLa`; `needs a valid BBL`; (e2e) `ZoLa|valid BBL|full.?address|manual entry|Search this full address`; (contract file) `additionalProperties|Object\.keys|...`
- Read: `M5-T041.json`, `M5-T041-producer-report.md`, `M5-T041-ci-evidence.md`, `PropertyOverview.tsx`, `AddressAutocomplete.tsx`, `AddressConfirmCard.tsx` (120–159), `contract-versions.test.ts` (1–60), `architect-workspace.spec.ts` (195–237)

Modularity note: I did not re-execute `python tools/modularity_check.py --check` (it would analyze my worktree's tree, not the submitted files); relied on orchestrator-captured exit 0 + inspection. If you want it re-run against the submitted head, capture it from a checkout at `740b6f2f`.

Memory written (my worktree agent-memory only): `feedback_copy_change_old_string_sweep.md` + index update.
