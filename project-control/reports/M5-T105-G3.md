# M5-T105 — G3 code review (code-reviewer "cr-t105", read-only)

> Transmission history: pinned at 259b6d40, delivered as two SendMessage parts (1/2, 2/2) ending with END-OF-REPORT;
> no truncation. Joined verbatim by the orchestrator (transport wrapper tags removed only). Verdict PASS: the 120-character
> cap covers all four caller text fields before any drawing, the extracted _finish boundary is complete and byte-identical,
> golden c38360f9 unchanged. Advisories 1-2 (the owner-sample count nit, the raw-value cap) are routed at the accept seam.

---

M5-T105 G3 code review (read-only) — PART 1/2. PIN: HEAD 259b6d40 at start (as expected). Material commit e294cf1f, 3 files, exactly the allowed paths. Checks I ran (cwd services/api): ruff `All checks passed!`; `pytest tests/cad -q` = 368 passed; modularity (repo root) EXIT=0, pdf_sheet_writer.py NOT flagged (551 lines, under warn-600).

BLOCKING findings: NONE.

AS-1 the cap (DB-059 a) — VERIFIED:
- Covers EVERY caller text field. `_screen_caller_text` bounds address, bbl, generated_at, generator_version (pdf_sheet_writer.py:272-285). These are all 4 str fields on SitePlanInput (:139-144) and the only caller-derived strings in `_draw_title_block` (:435-443); the other title-block rows are literals or the server-computed scale. No unbounded caller string escapes.
- Order is correct: type (:279) → length (:281) → claim-word (:286), and `_screen_caller_text` is called at :184 BEFORE `_validate_ring`/`choose_scale`/`_finish`, so the refusal precedes all drawing. Proven by `build_calls == []` in test_over_long_caller_text_is_refused_nothing_drawn and test_length_cap_precedes_the_claim_word_screen.
- Refusal never echoes caller text: detail = `"{name} exceeds the {_MAX_TEXT_CHARS}-character limit"` (:282-285) — field name only; test asserts `over_long not in result.detail` (:601).
- 120 is reasonable: a real NYC address+unit is ~70 chars, bbl ~13, ISO timestamp ~25 — all comfortably under; bound is inclusive (exactly-120 renders, :606-610); mutation `_MAX_TEXT_CHARS=10**9` reddens per field (:613-623), proving the one shared cap is load-bearing for every field.

AS-2 `_finish` extraction + honesty (DB-053 b) — VERIFIED:
- Byte-identical code path. Old `render_site_plan_pdf` ended with try→`_build_content`→`_assemble_pdf`/except; new `_finish` (:247-251) holds that identical body, with `to_device` passed as the same closure (:228). Golden sha256 c38360f9… is byte-identical in parent (e294cf1f^) and current; test_golden_sha256 green.
- Single boundary is complete. Only raise site is `_num` (:499); only catch is `_finish` (:250). Every `_num` call is inside `_build_content` or `_assemble_pdf` (:386-519), both within `_finish`'s try; no `_num` call sits outside it. `render_site_plan_pdf`/`choose_scale`/`_validate_ring` call `_num` nowhere.
- Docstrings honest: `_num` now states it RAISES (:490-497); module para (:28-36), `_finish` (:238-246), `_escape_pdf_text` (:464-473, uncertainty marked "[recalled - verify]") all match reality. Mutation (a `_finish` without the catch → public API raises) reddens (:656-671).
PART 1 END — part 2 follows.

---

M5-T105 G3 code review — PART 2/2.

DB-053 (a),(d) closed by M5-T091 — AGREE:
- (a) malformed vertex: `_validate_ring`/`_coerce_vertex` (:299-354); H-1 tests sweep non-numeric, numeric-string, None, bool, complex, nested list, Decimal, wrong-arity, overflow, and container-level junk (:317-356) — all typed refusals, never raises. Re-verified green, no code change.
- (d) unbalanced parens+backslash: H-4 round-trips through the REAL in-repo strict reader (:544-552) with paren-skip mutants (:567-586). This task added the 2 combined paren+backslash fixtures (test file :539-540, confirmed `+` in the diff) and the ISO 32000-1 §7.3.4.2 citation.

AS-3 escaping (ISO 32000-1 7.3.4.2) — VERIFIED:
- No double-escape by construction. `_escape_pdf_text` (:475-481) iterates the already-sanitised SOURCE chars one at a time and emits per source char: a source `\`→`\\`, a source `(`→`\(`. It never re-scans its own output, so the "backslash-first ordering" hazard cannot arise — this is safer than a sequential str.replace. Combined fixtures have teeth (paren-skip mutant reddens the round-trip).

AS-4 byte identity + scope — VERIFIED:
- Golden c38360f9 unchanged (parent == current). test_cad_owner_samples.py UNTOUCHED (git clean) and green (10 passed); docs/samples/cad/example-site-plan.pdf tracked/unchanged.
- Zero new deps (source imports math/numbers/collections.abc/dataclasses + app.cad.claim_words; tests stdlib + pytest). Unwired (no route/main.py/__init__; forbidden CAD paths clean). Exactly the 3 allowed paths.

ADVISORY (non-blocking):
1. Producer report line 57 says the owner-sample suite is "11 passed"; actual is 10 passed. The load-bearing claim (untouched + green + sample byte-identity) is verified true; only the count is off by one. Report-accuracy nit only.
2. The 120-char cap is applied to the raw field value, not the composed title-block line (fixed prefixes like "SITE: "/"Generator: " add ≤11 chars). This is correct per DB-059(a) wording and safe (a 120-char value + prefix at 9 pt still fits the sheet width); noted for completeness, no action.

M5-T105 G3 VERDICT: PASS
END-OF-REPORT
