# M5-T117 — G3 code review (code-reviewer "cr-t117", read-only)

> Transmission history: pinned at 675dc7a1 (material cf6526e3), delivered as 2 SendMessage parts ending with
> END-OF-REPORT; no truncation. Joined verbatim by the orchestrator (transport wrapper tags removed only). Verdict PASS; three ADVISORY items
> (a strict AST guard, a 50 ms post-start timing dependency, a duplicated test helper).

---

M5-T117 G3 CODE REVIEW (independent, read-only) — PART 1/2

IDENTITY: HEAD pinned 675dc7a1 at start. Material cf6526e3 (cherry-pick of wt-m5t117 76a9ba28), 7 files. Byte-stable: `git log cf6526e3..675dc7a1 -- <6 code/test files>` is EMPTY; submit commit 675dc7a1 touched only project-control ledger/gate/report files. Working tree clean for all 6. Review identity = cf6526e3.

SCOPE (PASS): exactly the 7 allowed_paths; the only production files are rate_limit.py + scene_api.py (docstring-only). Forbidden export_api.py / dxf_import_api.py / main.py / proposal_validation.py / requirements NOT touched -> routes stay UNMOUNTED. Zero new deps (rate_limit.py adds no import; tests add stdlib `ast`).

EVIDENCE RUN (cwd services/api, local 3.11.9):
- `ruff check .` -> All checks passed.
- pytest harvest (the 4 documented paths) -> 190 passed (183 M5-T111 + 7 new).
- `modularity_check.py --check` (repo root) -> exit 0; rate_limit.py 346 lines, below the 600 warn line and NOT in the warn list.

BLOCKING FINDINGS: NONE.

CORRECTNESS — the slot state machine (rate_limit.py:275-345), CONFIRMED:
- Every exit path releases EXACTLY ONCE: success / work()-raise -> worker finally _release_once (:333); pre-start cancel -> except sees started=False -> _release_once (:341-344); post-start timeout -> except sees started=True -> defers, worker finally releases; SlotsExhausted (:311-312) acquired nothing.
- The race the ask flags (worker starts between the except reading started==False and its release; both sides release): handled. `_release_once` (:319-324) checks-and-sets `released` under `_guard` BEFORE `slots.release()`, so two concurrent callers yield exactly ONE slots.release(). No double release; no leak (at least one owner always releases).
- `started=True` is set under `_guard` BEFORE work() (:327-328), so a started job's release is owned by the worker finally and the except defers -> a started job holds its slot until the THREAD returns.
- No `await` between try_acquire() (:311) and the try (:335) -> no cancellation slips in un-caught. `_guard` released before `slots.release()` -> no lock-order inversion/deadlock. JobSlots.release guards >0 (:259-261). Correctness does NOT rely on abandon_on_cancel=False — the idempotent flag covers any runner overlap; the docstring 40-token pool note is documentation for PKT-H, correctly hedged.

(PART 2: sweep + caller_key + route tests + behaviour + advisories + verdict)

---

M5-T117 G3 CODE REVIEW — PART 2/2

CORRECTNESS (cont.):
- Sweep throttle (rate_limit.py:166-180): `_maybe_sweep` runs the O(keys x stamps) full sweep at most once per window_seconds; the post-sweep ceiling recheck (:142-145) keeps the new-key refusal FAIL-CLOSED even when the sweep is skipped. Throttling only DELAYS reclaiming expired keys by <=1 window — never evicts an active key (_sweep_expired drops empty windows only, :182-189), never wrongly admits. reset() clears _last_sweep (:154).
- caller_key (rate_limit.py:219-224): principal used ONLY when `isinstance(str) and non-empty` (:220); empty-string/non-string/absent -> host; principal:/host: namespacing kept; never reads any header (XFF untrusted by construction).

ROUTE TESTS (item 4), CONFIRMED:
- HEAD/OPTIONS added to the every-method-404 loop in ALL THREE (test_scene_api.py, test_dxf_import_api.py both URLs, test_export_api.py), with the correct HEAD-empty-body assertion (resp.content == b"") and OPTIONS still json {"detail":"Not Found"}, no X-Correlation-ID.
- AST guard `_module_level_limiter_containers` catches module-level dict literals (ast.Dict) AND dict/defaultdict/OrderedDict/deque calls in both bare-id and attribute (collections.X) forms, at module scope only (handles Assign + valued AnnAssign). Verified NO live false positive: the three route modules currently have zero module-level dict/container assignments (grep clean), so `== []` passes legitimately; 190 green.
- Contention tests (test_rate_limit.py): 64 barrier-synced threads on one key/slot pool admit EXACTLY the cap (limiter 10, slots 10) — proves the lock prevents over-admission (G4 gap 1). Each new property carries a described reddening mutation (pre-start no-release, release-on-every-cancel, sweep-on-every-key, empty-principal-accepted, trust-XFF, drop-lock).

BEHAVIOUR UNCHANGED (item 5): scene_api.py diff is docstring/comment only (import path -> :func:`app.resilience.rate_limit.*`); per-route limits / 429 shape / deadlines / correlation ids live in the untouched route bodies. Finding 4 (per-route body ceilings) correctly deferred to PKT-H (out of scope per packet + G5).

ADVISORY (non-blocking):
1. The AST guard is deliberately strict: a FUTURE legitimate module-level dict constant added to any route module would redden the test (fail-closed, over-strict; no false positive today). It also still cannot catch a novel-named class-based limiter — the accepted G4-gap-4 limit, backstopped by the behavioural 429 tests.
2. test_post_start_cancel..._no_double relies on the worker setting started within the 0.05s deadline (thread submitted before wait_for times; started set first). Sub-ms realistically; an extreme >50ms scheduling stall would flip the read (release_calls would be 1, tripping the ==0 assert). Low flake risk, matches the 0.05s dependency G4 already accepted for M5-T111.
3. Trivial: the AST helper + _LIMITER_CONTAINER_CALLS are duplicated verbatim across the 3 test files.

M5-T117 G3 VERDICT: PASS

END-OF-REPORT
