# M5-T090 — G4 independent test-adequacy review (qa-engineer "qa-3d", read-only)

> Transmission history: pinned at 023294ec, delivered as two SendMessage parts (1/2, 2/2) ending with
> END-OF-REPORT; no truncation. Joined verbatim by the orchestrator (transport wrapper tags removed only).
> The reviewer's read-only guard blocked `gh run view --log`; it relied on job-level conclusions, the
> workflow YAML and direct registry/lock inspection, and says so in part 1.

---

M5-T090 G4 review Part 1/2 — AS-1 (pins) and AS-2 (lock) both PASS on verified evidence.

Scope: independent G4 test-adequacy review of M5-T090 (admits three@0.186.0 + @react-three/fiber@9.7.0 as exact pins + regenerated lock; ships no code). Read-only; ran no npm/node. HEAD pinned at start = 023294ec (matches expected). LIMITATION: this identity's read-only guard blocked my `gh run view --log` (and one `gh api .../logs`) calls, so for step-level proof I used the job-level conclusions (`gh run view --json jobs`), the workflow YAML (each gate is a non-continue-on-error `run:` step), and direct registry/lock inspection.

AS-1 (pins) — PASS.
- Diff 80f17a86^..80f17a86 on apps/web/package.json: exactly two adds — "@react-three/fiber": "9.7.0" and "three": "0.186.0" — plus the trailing comma on the unchanged "react-dom": "19.1.2". No other dependency line changed. Both exact; zero range characters (^ ~ > < * x).
- Ages I verified myself against the live registry `time` map vs now (2026-09-24T10:25Z):
  * three@0.186.0 published 2026-09-08T19:25:22Z → 1,350,016 s (15.6 d) ≥ 604800 PASS
  * @react-three/fiber@9.7.0 published 2026-07-31T16:20:20Z → 4,730,722 s (54.7 d) ≥ 604800 PASS
  * fiber 9.8.0 (published 2026-09-22, ~137k s) is correctly excluded as too-new; 9.7.0 is the newest eligible stable.

AS-2 (lock) — PASS.
- Lock came only from bot commit 2e444920 (github-actions[bot]) — the cherry-pick of generate-lockfile run 35985974452 (branch task/M5-T090-3d-web-deps, workflow_dispatch, job "lockfile" success in 41s).
- Both pinned integrities in the lock equal the registry dist.integrity EXACTLY: three = sha512-cr/fIM2ddMSV…HHkQ==, fiber = sha512-EWm9FwcaOZQu…dI1clw==.
- No existing package VERSION changed. The only non-additive lock edits are `dev:true` removed from @babel/runtime@7.29.7, @types/react@19.2.17, csstype@3.2.3 — a correct dev→prod reclassification (fiber's tree pulls them into production); versions unchanged.

Part 2/2 (AS-3, regression risk, predicted-tree match, verdict) follows.

---

M5-T090 G4 review Part 2/2 — AS-3 PASS (the 3 red jobs are CANCELLED, not failed); verdict PASS with one ADVISORY.

AS-3 (whole-tree gates) — PASS.
- generate-lockfile run 35985974452 proves the lock BEFORE committing via its blocking steps (npm ci, npm audit --audit-level=low, npm audit JSON-total-0, committed-lock release-age gate, npm CLI advisory); job conclusion = success.
- CI on HEAD 023294ec (run 35986924667): web-dependency-security = success (52s) [the packet's AS-3 job — runs the SAME gate steps on the committed lock], web = success (1m15s). 15/18 jobs success.
- The 3 non-success jobs — web-e2e, control-plane, supervisor-bridge — have conclusion CANCELLED, not failure (confirmed via `gh run view --json jobs`). They are the 3 slowest jobs (>4.5m), killed by `concurrency: cancel-in-progress` when M5-T083 pushed at 10:27:42Z (cancel at 10:27:55Z). The web-e2e log shows all executed Playwright tests ✓ up to the cancel. No genuine CI failure is attributable to this packet; the green `web` job proves npm ci + build succeed with the new tree.

Point 4 (could a regression slip through?):
- Too-new transitive in a future regeneration: COVERED. dependency_age_gate.mjs enumerates the WHOLE committed lock (direct+transitive+dev+scoped+nested), integrity-matches each vs registry, fails closed <604800 s. Tests pin the 604800/604799 boundary and assert run() over a lock with a too-new entry exits 1 and names it.
- New advisory tomorrow vs a pinned version: COVERED by scheduled-web-audit.yml (daily 06:41 UTC + PRs touching web dep files): npm audit all-severities + release-age gate + npm CLI advisory, all fail-closed.
- GAP (ADVISORY): no committed test pins these two exact versions or rejects range characters in apps/web/package.json. `.npmrc save-exact` only affects new `npm i` writes; npm ci accepts a hand-edited `^`-range if the lock stays in sync. Not a current-safety defect (age gate + audit protect the tree regardless of the spec string), but worth a follow-up guard.

Point 5 (predicted tree = actual lock): PASS. Exactly 13 new packages as predicted (three, @react-three/fiber, buffer, base64-js, ieee754 [BSD-3-Clause], zustand@5.0.15, its-fine, @types/react-reconciler, scheduler@0.27.0, @types/webxr, suspend-react, react-use-measure, use-sync-external-store@1.7.0); scheduler nested at node_modules/@react-three/fiber/node_modules/scheduler.

AS-4 (provenance) is the security-reviewer's G5 lane, not G4 — I defer it. (Producer flagged fiber's maintainer set 22→8 for G5; the fixed 9.7.0 tarball is pinned by integrity.)

No BLOCKING findings; one ADVISORY (package.json exact-pin/range guard gap).

M5-T090 G4 VERDICT: PASS

END-OF-REPORT
