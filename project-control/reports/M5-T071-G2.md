# M5-T071 — G2 producer self-check record (recorded by the orchestrator, role self_check)

Producer: frontend-engineer (loop-1 worker, run persistent-local-68-m5t071). Run-68 closed
PAUSED_RECOVERY missing_checkpoint AFTER four validated checkpoints; the unit was COMPLETE
per its own internal rework-2 report — the recorded harvest class (a delivery, not a
failure). Harvested at seam commit 2396a000; the producer return is saved verbatim as
M5-T071-producer-report.md.

- **Scope self-check:** material 2396a000 = the packet's allowed drawing-surface files only
  (ProposalOutlineDraw/ProposalOutlineMap + their suites + lot-outline-map.test.tsx + the
  report); ProposalEditor untouched by the packet, exactly as the title binds.
- **Behavior self-check (rework-2 report):** DB-047(d) per-fallback-state copy gating with
  an observer transition test; DB-047(e) Number.isFinite gate on both axes with a truthful
  finite-point status + disabled-reason hint, finite flows byte-identical; DB-048 AS-1 sync
  spec rewritten to final-state/argument assertions (teeth retained — the mutation rows in
  the report stay red under the reverted gate).
- **Web proof (authoritative, thin-client rule):** web behavior proves ONLY in CI — run
  35817816845 at c0b71938 SUCCESS (web-e2e green: 55 vitest files incl. the three packet
  suites; secret-scan 35817816896 SUCCESS). T071's allowed files are byte-identical from
  2396a000 through that green head.
- **Consumer sweep:** the producer swept its named surface in-run; the one out-of-packet
  consumer (the M5-T065 adoption test in proposal-editor.test.tsx, red under the new
  DB-047(e) contract) was surfaced by the first completed CI run and closed by the
  orchestrator per the CODING_RULES routing as peer commit c0b71938 (cluster C) — recorded
  honestly in the evidence map's D-066-R001 row; green CI at that head proves the completed
  sweep.
- **Identity:** frozen submit identity at 717562a6 (submit-seam 749064c6 carries only
  state/task files).

G2 verdict: PASS (self_check; never independent review — G3/G4/HJ/DCV follow).
