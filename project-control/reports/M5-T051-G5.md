# M5-T051 G5 — independent security review (security-reviewer, saved VERBATIM from the agent return)

M5-T051 G5 SECURITY REVIEW — VERDICT: PASS (2 advisories bind the future wiring packet; none blocking for this pure/unwired module). HEAD verified ce11d09f; material f1ec64a8.

COMPUTE-COST ARITHMETIC (worst case, validated ceilings MAX_OUTLINE_VERTICES=1000, MAX_LEVELS=500, MAX_EXTERIOR_WALLS=4000; caller lot-lines L, street-lines S):
- footprint shoelace O(V_base) ≤ 1000; coverage O(1).
- per-level areas: shoelace ONLY (no _ring_is_simple in derivation), O(V) per level → ≤ 500×1000 = 5e5 ops. Linear; no block-side quadratic.
- gross/heights O(levels) + sort. Fine.
- wall setbacks = the only super-linear path: for each of ≤4000 walls, _lot_line_setback scans all L lot-lines (O(1) each) and _street_line_setback scans all S → O(walls×(L+S)) = 4000×(L+S) segment-distance ops. Walls ARE bounded (4000); L and S are NOT — LotContext has no ceiling on lot_line_segments/street_lines. With L=1e6 this is 4e9 O(1) ops (minutes–hours CPU) = a DoS seam once wired.
- derive_proposal re-calls validate_proposed_massing (defense-in-depth), so it ALSO pays the validator's O(sum V^2) simplicity cost — DB-034(a)'s ~2.5e8-op / ~minutes worst case is inherited on every call.

FINDINGS
1. ADVISORY (wiring precondition; companion to DB-034(a)) — LotContext.lot_line_segments and .street_lines are UNBOUNDED (derivation.py:266-267; no ceiling). The wall-setback loop is O(4000×(L+S)); an unbounded caller list is a DoS axis when a route is wired. Bind a LotContext list-size ceiling on the packet that wires editor/lot input to derive_proposal (same class as DB-034(a) global vertex budget). Inert today: no route reaches derive_proposal.

2. ADVISORY (numeric safety; wiring precondition or cheap in-module fix) — Caller LotContext COORDINATES are not finiteness-checked, unlike lot.area_sq_ft which IS (derivation.py:438-447). LotLineSegment.start/end and AttestedStreetLine.start/end (derivation.py:224-251) flow straight into _segment_segment_distance → _point_segment_distance. A NaN/Inf coordinate: seg_len_sq becomes NaN, `NaN == 0.0` is False so the zero-length guard (line 353) is skipped, division yields NaN, math.hypot propagates NaN; `distance < best_distance` is False for NaN so it never displaces a good value BUT if the FIRST/only lot-line is NaN, best_distance is set on the `is None` branch (line 601) and a NaN reaches EvidenceRecord.value — a silent guessed-past value into a provenance record, contra the module's stated fail-closed contract. Asymmetric with the area_sq_ft guard, which shows caller-input hardening was intended. Fix: math.isfinite guard on lot-line/street-line coordinates (here at use, or at the wiring boundary).

3. ADVISORY (minor; DB-023c / DB-034(b) territory, already tracked) — Unbounded-repr embedding of caller-supplied values in messages/details: lot.area_sq_ft!r (derivation.py:445, reachable via a pathological huge int → float(inf) → this branch), wall id !r (derivation.py:575-576), lot_line.id !r (derivation.py:610). DB-034(b) already binds MAX_STRING_LEN + bounded-repr to the editor-input packet; the area_sq_ft repr is a new small instance to fold in there.

CLEAN (no defect):
- DB-034(c) fix correct & non-bypassable: _validate_walls gets len(outline_vertices)=full length incl. closing duplicate (proposal.py:467-475); indices bounds-checked 0<=iv<vertex_count and start!=end; modular fold ring_length=vertex_count-1 catches the sole aliasing pair (index 0 ↔ closing index N) both directions (proposal.py:415-427); derivation _wall_segment adds start==end defense-in-depth (:573). Purely additive (0 deletions); B0 semantics byte-preserved. Modulo-by-zero impossible (valid ring ⇒ vertex_count≥4 ⇒ ring_length≥3).
- No derived-allowance leak: source_class='proposed_derivation' on every record; grep-clean — every permitted/required/allowed/maximum/zoning-floor-area hit is a disclaimer or docstring, no field/value computes/implies an allowance (AS-4 satisfied).
- Purity: stdlib-only (hashlib/json/math/collections.abc/dataclasses/enum) + own .proposal; no I/O/network/env/subprocess/eval; no new dependency or lockfile change (diff is .py + fixtures + tests + report); fixtures PII-free (architect@example.com, synthetic BBL/object_id/digests); secret-scan green (CI 35455214032).
- Block-side numeric safety: block coords validated finite + in NYC-2263 bounds; _area_or_refuse refuses non-finite/non-positive area (:407); coverage divisor guarded finite+positive (:443); zero-length segment guarded (:353).

BASIS: This is a pure, dependency-free computation module with no route wiring, re-validating its block as defense-in-depth. Its own arithmetic on the validated block is bounded and fail-closed; the DB-034(c)/(e) hardening is correct and additive; no allowance is computed or implied. The two substantive findings are both on caller-supplied LotContext (unbounded list sizes; unchecked coordinate finiteness) and are reachable only once a caller/route supplies untrusted input — exactly the future-wiring surface, with no reachable attack surface today — so they are recorded as advisory preconditions on the wiring packet (alongside DB-034(a)/(b)) rather than blocking defects, consistent with the project's fail-closed-at-the-boundary discipline. PASS.
