<!-- Reviewer return preserved VERBATIM by the orchestrator (report-preservation rule,
.claude/rules/project-control.md; transport entity-decoding applied: &amp; -> &).
Reviewer: independent data-contract-verifier subagent (G3), returned 2026-09-12,
reviewed HEAD 032dffe7. -->

All verification complete. Every check passes. Compiling the G3 gate report.

---

VERDICT: PASS

# G3 Data-Contract Gate Report — M4-T009 (R1-R12 residential FAR draft rule family)

**Reviewer:** data-contract-verifier (independent; did not author this work)
**Reviewed HEAD:** `032dffe7da8fbf1ef9af35bfda55cef184275b49` (branch `candidate/D-024-mrl-option-b`)
**Gate:** G3 (FIRST full G3 wave) — transcription task, source fidelity IS the gate
**Method:** stdlib-only recompute (`PYTHONDONTWRITEBYTECODE=1 python -c`), raw-JSON compare against the two committed snapshots as primary source. Read-only; no pytest/ruff; no writes; no `project_control.py`/git-write.
**Primary source:** `docs/research/zr-snapshots/v1/zr-23-21.snapshot.json` (R1-R5), `.../zr-23-22.snapshot.json` (R6-R12)

## Result summary — every acceptance scenario in the data-contract scope PASSES

| AS | Result | Evidence |
|---|---|---|
| AS-1 source fidelity (byte-exact, all values) | **PASS** | 95/95 value checks, **0 mismatches** |
| AS-2 complete coverage, computed | **PASS** | 18+27=45 districts, all covered, disjoint, 0 gaps/0 extras; test loads snapshot rows |
| AS-3 conservative on conditionals | **PASS** | lower returned; higher only as always-on `conditional_alternative`, never computed |
| AS-4/AS-9 footnote scope | **PASS** | snapshot marker only on row 0; cap on r1-r2-r3, absent from r2x-r4 & r5 |
| AS-5 digests (both trees) | **PASS** | all digests equal across docs+app trees, rule-recorded, and sha256(excerpt); files byte-identical |
| AS-6 second column surfaced, never applied | **PASS** | qualifying surfaced as `conditional_alternative` on all five; values byte-exact; not a computation input |
| AS-7 honesty labels | **PASS** | all five `needs_review` / `pending` / `pending` / `effective_from 2024-12-05` |
| Evidence map (per-commit / applicability / offline) | **PASS** | reproduced sentence-by-sentence, see below |

## 1. AS-1 SOURCE FIDELITY — 95 values, byte-exact, zero drift

I built the ground-truth district→value map by loading both snapshot `table.rows` and compared every FAR value in all five rules. **95 checks (18+10+8+46+13), 0 mismatches.** Breakdown:
- `r1-r2-r3` first row: 9 districts × (standard 0.75, qualifying 1.00) — match
- `r2x-r4`: R2X 1.00/1.00; R4A/R4B/R4/R4-1 1.00/1.50 — match
- `r5`: R5/R5A/R5B 1.50/2.00; R5D 2.00/2.00 — match (values unchanged per AS-9)
- `r6-r12` flat: all 23 districts, both columns (R6A/R6-1/R7B 3.00/3.90 … R12 15.00/18.00) — match
- `r6-r7-r8` conditional: conservative R6 2.20, R7-1/R7-2 3.44, R8 6.02; wide-street (higher) R6 3.00, R7-1/R7-2 4.00, R8 7.20; qualifying-from-lower-row R6 3.90, R7-1/R7-2 5.01, R8 7.20; R8 footnote-2 8.64 — all match, each drawn from the correct snapshot row.

The acceptance pack (`test_r1_r12_residential_far.py`) is **non-tautological**: helpers `_snapshot_raw`/`_district_rows`/`_row_values` load the snapshot and compare; the module docstring states "no expected FAR literal restated." Independent recompute is stronger and confirms it.

## 2. AS-2 COVERAGE — computed, complete, disjoint

23-21: 18 distinct districts, all covered by r1-r2-r3 ∪ r2x-r4 ∪ r5 (pairwise disjoint), 0 uncovered/0 extra. 23-22: 27 distinct districts, all covered by r6-r12 (flat) ∪ r6-r7-r8 (conditional), disjoint, 0 uncovered/0 extra. Conditional set from snapshot multiplicity = exactly {R6, R7-1, R7-2, R8}, matching the conditional rule's applicability. Empty documented-exclusion list is legitimate (nothing is silently absent). No other `residential_far`-family rule overlaps these districts.

## 3. AS-3 CONSERVATIVE — proven at the computation layer

The conditional rule's `computation` references **only** `standard_far_by_district` (the lower values). `wide_street_far_by_district` and `qualifying_far_by_district` are **not** computation inputs — surfaced via `wide_street_far_alternative` (`condition: null`, effect `conditional_alternative`) and `qualifying_housing`. R8 footnote-2 8.64 is `documented_limitation` scoped to `zoning_district == R8` only. The higher value (3.00/4.00/7.20) can never be the returned result.

## 4. AS-4 / AS-9 FOOTNOTE SCOPE — verified against snapshot markers

Snapshot self-check: `zr-23-21` has `value_footnotes` on **row 0 only** (`{standard_zoning_lots: "1"}`). The 0.60 cap exception `single_dwelling_unit_equivalent_far_cap` is present on `r1-r2-r3` (applicability = exactly the 9 first-row districts) and **absent** from `r2x-r4` and `r5` (confirmed absent for R2X, R4, R4A, R4B, R4-1, R5, R5A, R5B, R5D). The prior R5 misattribution is corrected; R5 FAR values unchanged.

## 5. AS-5 DIGESTS — three-way equality across both trees

For both snapshots, `content_digest_sha256` (docs) == (app) == `sha256(verbatim_excerpt)` (docs & app) == the rule-recorded citation digest:
- zr-23-21 → `b52771e6…a48ad8a` (recorded by r1-r2-r3, r2x-r4, r5)
- zr-23-22 → `943b65f9…8df1a38e` (recorded by r6-r12, r6-r7-r8)

Both snapshot files are **byte-identical** between `docs/research/zr-snapshots/v1/` and `services/api/app/_zr_snapshots/v1/`. All five rules carry exactly one citation with `last_amended: 2024-12-05`.

## 6. Evidence-map — reproduced sentence-by-sentence, no overstatement

- **ae117874** — 11 files (report + 5 rule JSONs + 2 acceptance packs + 3 adapted siblings), all inside `allowed_paths`, **zero** under `tools/`/`.claude/`/supervisor/loop. **TRUE.**
- **197156b9** — 2 files, the two acceptance packs only; diff contains **no** changed `assert`/comparison lines (E501 whitespace wraps only). "No assertion changed" **TRUE.**
- **3c46f923** — 4 snapshot files under docs + app trees (orchestrator base-repair, outside packet scope, disclosed in progress_log). **TRUE.**
- **3433c3bb** — orchestrator merge; all four snapshot blobs **byte-identical** to candidate parent (`git rev-parse` blob compare = YES ×4); the "10 files/1394 insertions vs parent1" is the producer branch content arriving via merge, not content authored in the merge. "No new content" **TRUE.**
- **317df044** — 7 files (5 rules + provenance test + report addendum). Each rule diff is exactly the added `content_digest_sha256` line (+ comma on the prior `last_amended` line); **no FAR/param/value line changed anywhere**. The map's "one added line per rule" is accurate in substance. **TRUE.**
- **Applicability disclosure** — reproduced from `requirements.json`: D-038-R003 `applicability.task_ids = [M5-T003..M5-T013]`, task_types/milestones/paths/lifecycle all empty; M4-T009 not in the set → machine-applicable set computes **EMPTY**. The R003 discharge is voluntary; the empty-applicable form is correct. **TRUE.**
- **Offline** — both packs import `app.rules` + stdlib (json/decimal/pathlib/hashlib) + pytest only; **no** network/supabase/psycopg/os.environ/secret/token references. **TRUE.**

## 7. Non-blocking observations (no action required for this gate)

1. The docstring in `test_r1_r12_residential_far.py` describing packaged-bundle drift ("no zr-23-22 / stale zr-23-21") is now **stale**: base-repair 3c46f923 synced `app/_zr_snapshots/v1` so it contains zr-23-22 and byte-agrees with docs. This is descriptive prose, strengthens (not weakens) the position, and is not a data-contract defect.
2. AS-8 pytest/ruff execution is outside this G3 data-contract mandate and was not run (skill forbids); it belongs to G1/G4. Data-contract fidelity is fully established here from raw-JSON compare independent of suite execution.

## Conclusion

No transcription drift (0/95), no coverage gap (45/45), no footnote mis-scope, no digest mismatch (all three-way equal, both trees byte-identical), and no false record claim in the evidence map. All data-contract acceptance scenarios (AS-1 through AS-7, AS-9) are satisfied at the reviewed HEAD.

**VERDICT: PASS**

Relevant files (absolute):
- `C:/Users/MLFLL/Downloads/nyc-zoning/ctl24/docs/research/zr-snapshots/v1/zr-23-21.snapshot.json`
- `C:/Users/MLFLL/Downloads/nyc-zoning/ctl24/docs/research/zr-snapshots/v1/zr-23-22.snapshot.json`
- `C:/Users/MLFLL/Downloads/nyc-zoning/ctl24/services/api/app/rules/rulesets/r1_r2_r3_residential_far.rule.json`
- `C:/Users/MLFLL/Downloads/nyc-zoning/ctl24/services/api/app/rules/rulesets/r2x_r4_residential_far.rule.json`
- `C:/Users/MLFLL/Downloads/nyc-zoning/ctl24/services/api/app/rules/rulesets/r5_residential_far.rule.json`
- `C:/Users/MLFLL/Downloads/nyc-zoning/ctl24/services/api/app/rules/rulesets/r6_r12_residential_far.rule.json`
- `C:/Users/MLFLL/Downloads/nyc-zoning/ctl24/services/api/app/rules/rulesets/r6_r7_r8_wide_street_conditional_far.rule.json`
- `C:/Users/MLFLL/Downloads/nyc-zoning/ctl24/project-control/reports/M4-T009-evidence-map.json`
