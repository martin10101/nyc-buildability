# G4 QA Delta Re-Review — M0-T036 V1.1 correction unit (frozen c193a52)

> Preserved VERBATIM by the orchestrator from the g4-v11-m0t036 agent-return channel
> (2026-08-04). Transport entity-decoding only; no edits.

G4 QA DELTA RE-REVIEW — M0-T036 V1.1 correction unit, frozen SHA c193a52e0ac1bf4ceb7e0a1e67bb866d02b2605c (verified rev-parse; HEAD never moved; read-only except the suite + offline doctor, which write only under %LOCALAPPDATA%; git status porcelain shows no new repo modifications from my session). Review scope = `git diff 43848bd..c193a52`.

VERDICT: PASS on the delta. All seven mandatory fixes are implemented and each is pinned by at least one test that would fail if the defect returned. Three LOW-severity under-tested spots noted below; none blocks.

(1) REPRODUCED TOTALS (my own runs)
- Suite: `python -m pytest tools/test_agent_supervisor_*.py -q` = 1091 passed, 2 skipped, 61.79s — exactly the producer's claim and exactly +26 over the prior 1065 (settled G4 report, same 2 skips: symlink-privilege and POSIX-only guard). I counted exactly 26 new `def test_` in the diff: 12 loop, 8 runner, 2 audit, 2 phase1, 2 endurance — matching the producer's split.
- Doctor (offline): overall PASS, exit path clean, controller 0.4.0-phase4. The NEW `timezone_database` check is present and PASS ("ZoneInfo('America/New_York') resolved"). limited-auto still reported "NOT IMPLEMENTED and disabled", and loop_modes still refuses it by name. State machine now reports 66 documented transitions (64 + the two new V1.1 edges), every state reachable.

(2) PER-FIX TEST ADEQUACY — all seven PIN their finding
- B-1 PINNED, strongly. `test_cycle_two_receives_the_forwarded_prompt_not_the_original` asserts the SECOND unit's received prompt (via FakeRunner.prompts[1]) is NOT the original and IS the reviewer's rendered forward — the exact regression shape. Better still, `test_the_threaded_prompt_is_the_exact_outbox_row_marked_sent` asserts BYTE-equality between prompts[1] and the outbox envelope's journaled prompt read straight from SQLite — this would catch any re-render drift, not just the original-resent bug. Resumed-unsent rows threading their OWN journaled prompt and the report-carries-digest-not-bytes invariant are each pinned.
- B-2 PINNED. `test_the_journal_is_restartable_after_a_completed_shadow_cycle` runs a SECOND loop against the SAME journal (the pre-fix crash); `test_deny_and_continue_in_supervised_does_not_crash_and_reports` uses a poisoned reviewer decision (--effort flag) with max_cycles=3, asserts stop code `deny_and_continue`, resting state PREFLIGHT, zero forwards, and proves resumability by actually re-running. `test_deny_and_halt_still_pauses_hard` guards the non-weakening side. `test_a_loop_refusal_is_a_report_not_a_traceback` drives a genuinely stranded journal (COLLECT_EVIDENCE) through the real CLI and asserts a named `bad_cycle_entry_state` report at exit 0.
- B-3 PINNED. The `distinct_double_checkpoint` fake emits a real BLOCKED checkpoint then a rosier UNIT_COMPLETE under a different id; the test asserts result.checkpoint is None, error `multiple_distinct_checkpoints`, and BOTH ids named — last-wins cannot silently return. Direct extraction-seam test plus the benign-identical-duplicate regression guard both present. Implementation refuses on len(by_id)>1 BEFORE `shaped[-1]` selection, so refuse-precedes-choose structurally.
- B-4 PINNED. Trip test asserts the run stops with `circuit_breaker_hard_threshold` in PAUSED_RECOVERY with reviewer.packets == [] and provider_calls == 1 — i.e. the pause happens BEFORE the reviewer is contacted, with a counted synchronous-stop touch. Reset test: a 2-cycle supervised run at limit 2 completes (would trip spuriously without the per-checkpoint reset) and asserts the counter value is 1 afterward. The new CODEX_REVIEW→PAUSED_RECOVERY `unsafe_condition` edge backs it in the S7 table. The claude_runs_per_task naming mismatch is documented in circuit_breakers.py rather than renamed — the stated reason (renaming would invalidate owner-placed manifest-covered configs and decouple historical audit events) is sound.
- F-2 PINNED end-to-end. `test_run2_scenario_clear_recovery_then_start_works` reproduces pilot run 2 exactly: journal parked in PAUSED_RECOVERY → `start` refuses with a REPORT naming PAUSED_RECOVERY (this step also exercises the new IllegalTransitionError catch) → `clear-recovery` fires the audited owner_cleared_pause transition to PREFLIGHT → the SAME journal dispatches (asserted via payload["dispatched"], honest `no_valid_checkpoint` stop since sys.executable isn't a worker). Refusal outside PAUSED_RECOVERY, refusal under a standing emergency stop, and S12.1 surface registration all covered. cmd_clear_recovery clears no flags and dispatches nothing, as claimed.
- F-4 PINNED. A `record_prompt` fake worker writes its received first user turn to a file; the test asserts the dispatched prompt starts with the operator's prompt unmodified and carries the sentinel, all 12 required field names, the exact five-word status vocabulary line, and the one-JSON-object rule. Pass-through of an already-authored contract is asserted byte-identical with sentinel count 1; `with_checkpoint_contract` idempotence directly tested; and the derivation test iterates dataclasses.fields(ClaudeCheckpoint) + CHECKPOINT_STATUSES so contract/validator drift is structurally impossible. `checkpoint_contract_appended` asserted on both branches.
- F-5 PINNED both ways. Clean-environment PASS asserted through doctor --json; the failure side patches cli.zoneinfo to raise ZoneInfoNotFoundError and asserts doctor exit 1 with a detail naming both "tzdata" and "first wake". Doctor-level placement matches my original F-5 recommendation.
- Optionals: L-1 test iterates HARD_DENY_ARGUMENTS itself (including upper-case =-forms), so a new deny-list entry is automatically covered — good pattern. V-6 covers five distinct corrupt-anchor shapes (subTests) plus the absent-anchor-legitimate distinction; implementation returns fail-closed `head_anchor_corrupt` and now type-checks sequence/digest.

(3) THE SINGLE MODIFIED PRE-EXISTING ASSERTION — LEGITIMATE
`ShadowTests::test_shadow_never_reaches_forward_prompt` changed exactly one expected value: resting state POLICY_CHECK → PREFLIGHT. The load-bearing assertion (FORWARD_PROMPT never in the transition set) is untouched, and the old expected value WAS the B-2 defect (the stranded state I cited at G3 B-2(b)). This is a defect pin, not a weakening. The only other pre-existing-test edits are additive and behavior-preserving: the endurance S12.1 expected-command set gains "clear-recovery" (surface grew, correctly), and RunnerTestBase.run_fake gains a `prompt` parameter defaulting to the old literal.

(4) UNDER-TESTED SPOTS / NEW GAPS (none blocking; all LOW)
a. `forwarded_prompt_unavailable` (loop.py:1083) — the B-1 fail-closed guard has NO test. Traced it: sent_prompt initializes to the passed render and run() guards non-empty before threading, so the branch is near-unreachable defensive code; still, an untested fail-closed branch is worth a follow-up test (a ForwardResult stub with sent=True, sent_prompt="").
b. Resumed-unsent corner (loop.py ~1028): if the previously journaled row's payload lacks a usable prompt string, the code silently falls back to the FRESH re-render rather than refusing — slightly at odds with the stated "the row is resumed, never re-rendered". Mitigated: digest-keyed message_ids force byte-identical renders anyway; only approval_id-keyed rows with a corrupted journal payload could diverge. Untested corner; suggest a follow-up test or a fail-closed tightening in V1.2.
c. V-4 partial: the two new precise not-OK reason strings (valid-checkpoint + graceful_close_failed, valid-checkpoint + wall timeout) have no test asserting them (grep confirms no test references the new strings), and the review-breaker WARN path ("circuit_breaker_warning" notify) is likewise not explicitly pinned. Both fail-closed either way; optional-scope; LOW.
d. Producer risk 2 is real but acceptable and honestly disclosed: the F-4 contract is appended at the runner AFTER the outbox prompt_digest is computed, so "prompt_digest = exact bytes sent" now holds only up to the fixed supervisor-owned suffix; the delta appended is deterministic and recorded via checkpoint_contract_appended. Note the B-1 byte-equality test operates at loop level (FakeRunner), so it does not conflict with this — the two facts compose.
e. `deny_and_continue` becoming a run stop code (producer risk 1): the S4.4 reading is defensible (machine not paused/halted, journal immediately resumable, and auto-retrying a hard-denied prompt would be an unbounded provider-call loop); the new test pins the machine-not-paused + resumable facts. Semantics call for G3/owner, not a QA defect.
No regression found: DENY_AND_HALT, identical-duplicate tolerance, same-id-conflict refusal, and the cycle-entry set ({PREFLIGHT, CLAUDE_RUNNING}, unchanged) all re-verified in the diff; CycleResult.continues narrowed (POLICY_CHECK removed) — fail-closed direction.

(5) COULD NOT RUN / VERIFY
- `doctor --live` — outside my authorized command set; the control_response live-probe gap from my settled G4 report remains open (unchanged by this delta, which correctly did not touch it).
- No live provider calls, no live forwarding — the B-1/F-4 pins are fake-worker/fake-runner based (appropriate for this scope; my settled activation-forward gaps 1-4 still stand for any live supervised run).
- The replay corpus was not extended (producer disclosed; the corpus replays recorded policy decisions, which the delta does not change — verified doctor still reports 8/8 cases reproducing with provenance intact).
