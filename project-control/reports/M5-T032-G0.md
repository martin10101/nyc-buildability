# M5-T032 — G0 contract gate (orchestrator, 2026-09-17)

Task: Address-search reliability under GeoSearch failure + ZoLa-first human-readable property links.
Recorded by the orchestrator at the contract seam; gate SHA = the commit carrying this report,
the completed packet, and the D-064 applicability binding.

## Contract completeness

- **Objective and business reason**: present; scoped to the handoff §6 defect plus the D-064-R005
  link change; no backend, no deployment, no legal-rule content.
- **Inputs**: D-064 source (verbatim owner instruction), handoff §6 observations (dated 503s, the
  663 ms non-timeout failure, 6.8 s vs 6000 ms deadline), official GeoSearch docs URL, the four
  implementation files named by the handoff, existing constant-prefix link discipline
  (provenance-link.ts, AddressConfirmCard ZoLa precedent), preserved M5-T030 behavior contract.
- **Outputs**: typed failure outcomes, bounded recovery, explicit /search action, preserved input,
  fallback route; zolaLotUrl-style helper + primary/secondary link ordering with honest absence;
  regression tests incl. the handoff's named cases; producer report with a dated live smoke check.
- **Allowed paths**: 19 existing tracked files (implementation + their test files). No new files
  required, so no placeholder seeding applies; every allowed path exists and is tracked at HEAD.
- **Acceptance scenarios**: AS-1..AS-11 executable and mapped to the handoff §6 required
  regressions (503-then-recovery, repeated 503s, slow valid reply, full-address submission without
  selection, preserved input, ambiguity, Queens hyphenated numbers, five boroughs, cancellation,
  malformed payload) plus the R005 link cases and the dated live smoke check (AS-11).
- **Test commands**: documented_test_commands follow the M5-T030 command profile; per D-064-R007
  the producer runs the focused test subset; full build/e2e run at the integration seam.
- **Gates/reviewers**: G0,G1,G2,G3,G4,G5 with data-contract-verifier, code-reviewer, qa-engineer,
  security-reviewer, human-journey-reviewer, directive-compliance-verifier — every required gate
  has its reviewer in reviewer_agents.
- **Directive regime**: directive_refs = D-064:R005,R007; registry evaluate_task_refs returns
  ok=true with applicable_ids == cited_ids == [D-064-R005, D-064-R007] after the same-commit
  applicability bind + digest resync (manifest audit_log entry recorded).
- **Holds**: no expansion-pack content (the §2 hold is untouched); PR 241 / main-branch holds not
  affected; work is frontend-only on the candidate branch.

## Verdict

PASS — contract is complete, bounded, and in-regime; ready for claim by frontend-engineer via the
supervisor loop (claude-opus-4-8 @ xhigh per D-064-R001).
