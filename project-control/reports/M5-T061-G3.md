# M5-T061 G3 gate — independent code review (verbatim reviewer return)

Reviewer: code-reviewer (opus-4-8 xhigh per D-064), dispatched by the orchestrator at the
caddbf59 submit head, pinned to material 1f1db078. Recorded by the orchestrator; reviewer is
read-only and records no gate itself.

---

All independent checks reproduced green. Here is my G3 gate report.

---

**VERDICT: PASS** (no required corrections; three non-blocking observations)

# G3 Gate Report — M5-T061 (DB-039 proposal-checks route hardening)

## Identity verified (read-only git plumbing)
- Pinned material commit `1f1db078` confirmed present and an ancestor of working HEAD `caddbf59` (branch `candidate/D-024-mrl-option-b`).
- `git diff 1f1db078 HEAD` for all four material files is empty and the working tree is clean for them — the bytes I reviewed ARE the pinned identity.
- All four LF-normalized sha256 digests MATCH the evidence-map values exactly (report, `_proposal_fact_domains.py`, `proposal_checks_api.py`, test file). Reviewed at HEAD `caddbf59`; the M5-T058 lane's disjoint peer commits do not touch this surface.
- `1f1db078` changed exactly the four allowed_paths files (261/382/373/262 line deltas); no forbidden path (incl. `main.py`, `app/rules/**`, `app/scenario/**`) was touched.

## Commands reproduced (cwd services/api, explicit)
- `python -m ruff check .` → All checks passed.
- `python -m pytest tests/api/test_proposal_checks_api.py -q` → 67 passed.
- `python -m pytest tests/api -q` → 591 passed (independent AS-6 preservation confirmation).
- `python tools/modularity_check.py --check` (repo root) → selected 467 files; failures 0; neither packet file appears in the 20 (pre-existing, unrelated) warnings.

These independently supersede the producer report's deliberately-hedged in-run outcomes (see OBS-1).

## Correctness findings (evidence-bound)

Domain/bounds logic — CORRECT:
- `_rule_lower`/`_rule_upper` (`_proposal_fact_domains.py:202-232`) pick each rule's MOST-RESTRICTIVE own bound (highest floor / lowest ceiling; exclusive wins a value tie) — verified against the min/max key functions.
- `_combine_lower`/`_combine_upper` (`:235-256`) keep the MOST-PERMISSIVE bound across rules (lowest floor / highest ceiling; inclusive wins a tie) — mathematically consistent so a boundary refusal only rejects values EVERY declaring rule would already reject.
- `derive_input_domains` (`:259-330`) enforces the every-declaring-rule-per-direction conservatism (`lower_all`/`upper_all` flags) and copies each kept bound from an `InputSpec` field — no invented limit. Enum side keeps the union only when no declaring rule leaves it free (`enum_free`).
- `_NumericBounds.refusal_reason` (`:166-177`) is correct; at most one lower and one upper field is ever set, so no double-report; never echoes the value.

Ordering claim (AS-2 / DB-039(e)) — VERIFIED by code trace: in `post_proposal_checks` the registry is resolved (`proposal_checks_api.py:553-557`) and `_validate_lot_rule_fact_domains` runs (`:558-563`) BEFORE `run_in_threadpool(validate_proposed_massing_input, …)` (`:571`) and before `_build_lot_context`. The only O(n²) surface is inside the gate; the earlier `_enforce_route_caps` uses the gate's own O(n) `_total_position_count`. Test `test_domain_check_runs_before_the_input_gate` (gate spy = 0 calls) and `test_registry_unavailable_500_is_recorded_before_the_gate` (gate not called on 500) both pass.

Memoization (AS-3 / DB-039(a)) — CORRECT: `_INPUT_DOMAIN_CACHE` is a `weakref.WeakKeyDictionary` keyed by registry identity (`:337-349`); `clear_input_domain_cache` hook; no cross-registry leakage; mutated only on the event loop (never inside `run_in_threadpool`), so the threadpool race the DB-039(i) note describes is genuinely avoided. Proven by `test_domain_derivation_memoized_once_per_registry` and `test_test_injected_registry_derives_its_own_domains`.

Wall-id charset (AS-4 / DB-039(h)) — CORRECT: `_validate_exterior_wall_ids` (`:321-335`) applies the same `_require_label` (200-char + `[A-Za-z0-9 ._:-]` fullmatch, length-only echo) as street `wall_id` gets in `_build_lot_context`. For any equal STRING pair the decision is identical regardless of ordering, so a matching pair cannot diverge. Non-string ids are intentionally left to the B0 validator (OBS-3).

Public surface / contract compatibility — UNCHANGED: `__all__`, `PROPOSAL_CHECKS_STATUS_STATE_MATRIX`, the four ROUTE caps, `get_proposal_check_registry`, `router`, and `post_proposal_checks` are byte-stable in the diff; `MAX_LABEL_LEN` is now imported from the extracted module and re-exported, so external importers (test file line 40; `main.py:36` mount) are unaffected. The (200,None)/(404,None)/413/422/500 matrix is intact. Consumer sweep: route is imported only by `main.py`, the test file, and the new helper (no cycle — helper imports nothing from the route).

Provenance / no-guessing discipline — CLEAN: numeric fields the derivation reads (`minimum/maximum/exclusive_minimum/exclusive_maximum`) are real `InputSpec` fields (`app/rules/models.py:29-32`), not guessed. Independent ruleset scan confirms the AS-6 safety claim: no production rule declares an enum or numeric bound on `zoning_district` or `lot_depth_ft` (so the new numeric path narrows nothing in production today and cannot fabricate a legal limit); `site_class`/`street_width_class` enum narrowing is pre-existing T057 behavior. Refusals echo accepted range/set or length only, never the offending value. Route emits no scenario document / contract version (BP-6). No stale `_derive_enum_domains` references remain.

Modularity — PASS: route reduced 721→622 raw lines via the packet-permitted extraction into a cohesive sibling module (380 lines, single BP-5 responsibility); checker failures 0; neither file flagged.

Acceptance-scenario coverage — all six materially covered: AS-1 (`test_derive_input_domains_*`, `test_route_numeric_fact_out_of_bounds_refused_before_engine`), AS-2 (ordering + 500-before-gate spies), AS-3 (memo spies), AS-4 (charset/length/non-string/round-trip), AS-5 (at-cap-exact 200 for walls=500/street_lines=400/label=200/body==MAX_BODY_BYTES + above-cap refusals + `test_build_lot_context_shape_refusals_each_bound` covering every shape branch), AS-6 (independently reproduced 591 passed + ruff + modularity). Each new guard has a mutation-sensitive assertion.

## Non-blocking observations
- OBS-1 (provenance/reporting, not code): the producer report's Evidence section records its authoritative in-run outcomes as "ruff exit 1 / pytest exit 4 (no tests found)" and marks green ruff/pytest/CI as PENDING — a deliberate honesty artifact of the loop worker's unrecorded-cwd run. Those PENDING items are now corroborated GREEN by the orchestrator evidence map AND my independent reproduction (ruff 0, 67 passed, 591 passed, modularity 0). Recommend the orchestrator note this so a reader of the report alone is not misled by the standalone "exit 1/exit 4" language.
- OBS-2: the reorder changes WHICH refusal wins for a multi-defect payload (domain refusal now precedes the gate/validator refusal). Intentional per DB-039(e), documented, (status,state) matrix unchanged, zero test regressions.
- OBS-3: a non-string `exterior_walls[].id` is left to the B0 validator while a non-string street `wall_id` is refused by `_require_label`. Intentional and documented; the charset-divergence risk concerns only equal STRING ids (which are aligned), and the gate's wider 512 ceiling remains upstream. Not a divergence bug.

## Directive regime
Task is in-regime (`directive_refs`: D-066-R001, D-076-R001/R002, D-077-R002/R003). Within G3 code-review scope I confirmed the code-level facts those requirements assert (closed code-graph seam, hardening inside the released route without new work class/flag/dependency, typed-bounded refusals, scope confined to the four allowed_paths, M5-T058 surface untouched). Per-requirement independent attestation is the `directive-compliance-verifier`'s pass (producer ≠ verifier) and is not duplicated here.

## Relevant paths (absolute)
- C:\Users\MLFLL\Downloads\nyc-zoning\ctl24\services\api\app\api\v1\proposal_checks_api.py
- C:\Users\MLFLL\Downloads\nyc-zoning\ctl24\services\api\app\api\v1\_proposal_fact_domains.py
- C:\Users\MLFLL\Downloads\nyc-zoning\ctl24\services\api\tests\api\test_proposal_checks_api.py
- C:\Users\MLFLL\Downloads\nyc-zoning\ctl24\project-control\reports\M5-T061-producer-report.md
- C:\Users\MLFLL\Downloads\nyc-zoning\ctl24\project-control\reports\M5-T061-evidence-map.json
- C:\Users\MLFLL\Downloads\nyc-zoning\ctl24\project-control\tasks\M5-T061.json

**G3 verdict: PASS.** No required corrections; OBS-1..OBS-3 are advisory (OBS-1 is a suggested orchestrator note, not a code change). Recording of the gate result remains with the orchestrator.

---

Orchestrator note per OBS-1 (recorded here, no material change): the producer report's standalone
"ruff exit 1 / pytest exit 4" lines are that unit's honest in-run cwd-artifact outcomes; the
corroborated truth at the reviewed identity is the evidence map's explicit-cwd transcripts
(ruff 0, targeted 67 passed, tests/api 591 passed, modularity 0), independently reproduced by
G3 and G5.
