# M5-T022 — G5 New-Package Provenance Gate Report (verbatim)

Orchestrator preservation note: report returned by the independent security-reviewer agent
("G5 provenance review M5-T022") on 2026-09-13 and preserved verbatim below (transport
entity-decoding only). Reviewer was read-only with live registry/OSV/gh access; frozen
review identity: pin f17a5869 + bot lock 6d776102. Verdict: PASS (LOW-1 ignore-scripts
posture + LOW-2 minimist promotion note, non-blocking).

---

I have completed all independent verification. Here is my G5 gate report.

---

# Gate Report

- **Gate ID:** G5 (new-package provenance review, Policy-15)
- **Task ID:** M5-T022 — maplibre-gl@6.7.0 dependency admission (D-040-R001)
- **Reviewer:** security-reviewer (independent; read-only)
- **Producer:** orchestrator
- **Result:** **PASS**
- **Clean environment/worktree used:** primary-checkout at HEAD `0bfcc08b`; dependency material read from the frozen commits `f17a5869` (pin) + `6d776102` (bot lock). Registry/OSV/GitHub facts fetched live (network available); GH runs corroborated read-only via `gh`.

## Frozen identity — VERIFIED

- Material = `apps/web/package.json` @ `f17a5869` (one added dependency: `maplibre-gl` = `6.7.0`) + `apps/web/package-lock.json` @ `6d776102` (bot commit, `+200/-1`, touches ONLY the lock — confirmed via `git show --stat`).
- Every commit AFTER the bot lock (`44c1012d`, `4afa0153`, `0bfcc08b`) touches only report/evidence/control-plane files. `git diff --name-only f17a5869^..0bfcc08b` shows the dependency files (`package.json`, `package-lock.json`) unchanged since the bot lock. The dependency material at HEAD is byte-identical to the frozen material.
- The only non-control-plane changes across the whole task span are the pin + the lock. `git diff --stat f17a5869^..0bfcc08b -- render.yaml .github/ services/ apps/web/src/ apps/web/e2e/ packages/` = empty (no prohibited paths touched).

## Acceptance criteria reviewed

| Scenario | Verdict | Evidence |
|---|---|---|
| S1 exact_admission | PASS | `dependencies["maplibre-gl"]=="6.7.0"` exact; next 15.5.24 / react 19.1.2 unchanged; lock resolves 6.7.0 + tree; all new nodes registry.npmjs.org + sha512 (automated: 0 non-registry, 0 non-sha512). |
| S2 workflow_proven | PASS | `gh run view 34729750887` = conclusion **success** on headSha `f17a5869`; workflow file shows npm-ci integrity → 2× blocking audit → age gate → npm-CLI-advisory ALL precede the commit step. |
| S3 ci_green | PASS | `gh run view 34729820746` = conclusion **success** at post-lock head `44c1012d`; orchestrator CI evidence corroborates web-dependency-security re-proven, web/web-e2e unaffected. |
| S4 provenance_review | PASS | This report (below). |

## Directive/requirement verification

| Requirement ID | Reviewed SHA / content identity | Verdict | Reproduced evidence |
|---|---|---|---|
| D-040-R001 | material f17a5869 + 6d776102 (HEAD 0bfcc08b) | **PASS** (for the part applicable to M5-T022) | R001 = scoped release of the expansion hold for the lot-outline increment mandating **MapLibre GL JS**; M5-T022 admits `maplibre-gl` — consistent with the enabling clause. R001's preserved-hold clause (3D massing, 19-task pack, GDS P1-P8, master-plan changes REMAIN HELD) is not violated: no `master_plan.json` change, no expansion tasks/code in the diff. `evaluate_task_refs` claim reproduced: R001's `applicability.task_ids` includes M5-T022; cited == applicable == {D-040-R001}. |

Selective-citation check (independent): D-042 (the Next.js-upgrade directive that discharges B-022) has **no** requirement applicable to M5-T022 — all D-042 requirements are `task_id`-scoped to `D-042-BOOTSTRAP`/`M5-T021` and none is path-based (`paths: []`). So citing only `D-040:D-040-R001` is complete; there is no un-cited applicable requirement.

Authorization chain (valid): D-040-R001 (scoped MapLibre release) + D-042-R002 (sequencing: "upon the tree becoming advisory-free … the maplibre-gl@6.7.0 admission MAY proceed under its own G5 provenance review and age gate") → M5-T022. B-022 was resolved at `636bab06` **before** the pin `f17a5869`. Sequencing respected.

**Acceptance precondition (NOT a G5 blocker — flagged for the orchestrator/DCV):** D-040 `verification.json` currently holds DCV rows for M0-T156 (R003) and M5-T019 (R002) but none for M5-T022/R001 (it predates this task). accept() requires the independent `directive-compliance-verifier` to record a PASS row for D-040-R001 / M5-T022 at `reviewed_sha==HEAD` before the task is accepted. That is the DCV verifier's step, distinct from this G5.

## Steps independently executed

1. `git show --stat` on f17a5869 / 6d776102 / 44c1012d / 4afa0153 / 0bfcc08b — frozen-identity + bot-touches-only-lock.
2. `git show 6d776102 -- apps/web/package-lock.json` — full lock diff; independent enumeration of added nodes; registry/integrity scan; hidden-change scan (removed/version-bump/flag lines).
3. Live registry: `https://registry.npmjs.org/maplibre-gl` (dist-tags, per-version `.time`, license, repository, dist.integrity, scripts, `_npmUser`, maintainers) and per-version endpoints for all 22 nodes (integrity + scripts + publisher).
4. OSV: batch query all 22 nodes at exact versions; single-query negative control across maplibre-gl 6.0.0/6.3.0/6.4.0/6.4.1/6.7.0; minimist@1.2.8.
5. Age computation for the required spot-checks against the bot-lock timestamp (2026-09-13T01:08:39Z).
6. `gh run view 34729750887` and `34729820746` (read-only).
7. Read workflow `.github/workflows/generate-lockfile.yml`, `apps/web/.npmrc`, root `.npmrc`; `grep maplibre` over `apps/web/src`.
8. Read D-040 + D-042 `source`/`requirements`/`verification` records.

## Expected versus actual (provenance table)

**maplibre-gl@6.7.0**
- License: BSD-3-Clause (registry) == BSD-3-Clause (lock) == task claim. ✓
- Repository: `git+https://github.com/maplibre/maplibre-gl-js.git`; homepage maplibre.org. ✓
- Publisher: MapLibre org; maintainers `nyurik, harelmazor, maplibreorg, birkskyum` (multi-maintainer). Published via **npm Trusted Publishing / GitHub OIDC** (`_npmUser.trustedPublisher.id=github`, `npm-oidc-no-reply@github.com`) — cryptographically attested provenance. Strong positive.
- dist.integrity registry `sha512-Y1Q1+UP9WXou0DUrnaFdDsoMqiMCWy6aS968IBUaQ2eZi6H1/kPCfpfZOWXFJZbeKMkX9Wo6OFzY/k48qqPf3Q==` == lock. ✓
- Age: published 2026-09-02T14:27:35Z; at bot-lock = **902,464 s ≈ 10.45 d** ≥ 604800. PASS. It IS the newest age-passing version — 6.8.0 (5.1 d) and 6.9.0 (3.5 d, current `latest`) both FAIL the age gate and are correctly excluded.
- Advisory history: CVE-2026-85061 / GHSA-jrc7-96c5-q579 present on 6.0.0/6.3.0/6.4.0, **fixed in 6.4.1**, absent from 6.7.0 (OSV negative-control confirmed the mechanism reports real vulns). ✓
- Scripts: no `preinstall`/`install`/`postinstall`; only a dev `prepare` (does not run for registry-tarball installs). ✓

**Transitive tree (21 transitives + maplibre-gl = 22 nodes)** — producer's section-2 table reproduced exactly against my own lock diff (22 added `node_modules/` entries, identical set/versions; 0 removed nodes):

- Registry + integrity: all 22 resolve from `registry.npmjs.org` with sha512; **all 22 dist.integrity values match the registry** (independent per-node fetch: ALL OK).
- OSV (exact versions): **all 22 CLEAN**.
- Licenses: all permissive (BSD-3-Clause, BSD-2-Clause, MIT, ISC, MIT-OR-Apache-2.0) — no copyleft/unusual.
- Install scripts: **none** on any of the 22 nodes.
- Age spot-check: @maplibre/mlt 1.2.0 = 24.1 d PASS; @maplibre/maplibre-gl-style-spec 26.4.1 = 18.1 d PASS; earcut 3.2.3 = 72.6 d PASS (also geojson-vt 75 d, vt-pbf 100 d). The tightest node in the whole tree is maplibre-gl itself at 10.45 d — the age gate was not fooled.
- Publisher spot-check: OIDC trusted-publishing for maplibre-gl / @maplibre-* / @mapbox/jsonlint / pbf; remainder by recognized maintainers (mapbox/mbx-np, mourner, toji, lydell, mafintosh, DefinitelyTyped). No typosquat-adjacent names; all under `@maplibre`/`@mapbox`/`@types` orgs or established authors.

## Threat-by-threat

- Cross-tenant isolation / service-role secrecy / private storage / SSRF / injection / prompt-injection: **N/A** — dependency-lock admission only; no runtime code, config, secrets, or logs changed.
- Supply-chain (rogue registry / integrity tamper): none — all registry.npmjs.org + sha512, integrity matches upstream.
- Malicious install-time code: none — zero install/preinstall/postinstall scripts across the tree (and the promoted minimist).
- Advisory exposure: none — whole tree OSV-clean; the one historical maplibre CVE is fixed pre-6.7.0.
- Least privilege (workflow): third-party Actions SHA-pinned (`checkout@34e11487… v4.3.1`, `setup-node@49933ea… v4.4.0`); npm pinned to 11.18.0 with hard verification; `permissions: contents: write` only; the commit step does `git add apps/web/package-lock.json` ONLY; the four/five-check proof (npm ci integrity → blocking audit → audit-JSON total==0 → committed-lock age gate fail-closed → npm-CLI advisory fail-closed) all precede the commit. gh corroborated.
- Attack surface: `grep maplibre apps/web/src` → no files; admission is inert until the rendering packet.

## Evidence paths

- `apps/web/package.json`, `apps/web/package-lock.json`
- `.github/workflows/generate-lockfile.yml`, `apps/web/.npmrc`
- `project-control/reports/M5-T022-producer-report.md`, `-ci-evidence.txt`, `-evidence-map.json`
- `project-control/tasks/M5-T022.json`
- `project-control/directives/D-040-scoped-unblocks/{source-001.md,requirements.json,verification.json}`
- `project-control/directives/D-042-nextjs-15-5-24-upgrade/requirements.json`
- gh runs `34729750887` (generate-lockfile), `34729820746` (CI)

## Regression/security/provenance findings

- **CRITICAL:** none.
- **HIGH:** none.
- **MEDIUM:** none.
- **LOW-1 (posture, non-blocking):** `apps/web/.npmrc` sets `min-release-age=7`, `save-exact=true`, `package-lock=true` but does **not** set `ignore-scripts=true` (no root `.npmrc` either). For this admission the install-time code risk is nil because all 22 nodes (and the promoted minimist) declare no install scripts — verified against the registry — but the protection is per-tree, not blanket. Recommend adding `ignore-scripts=true` as defense-in-depth for future admissions. Honest posture note per the task requirement.
- **LOW-2 (completeness, non-blocking):** the producer's "22 added nodes, 0 removed" is accurate for node additions, but the lock also promotes `minimist` from dev-only to a **production** dependency (pulled by `@maplibre/maplibre-gl-style-spec` → `minimist ^1.2.8`); the only removed line in the whole diff is that `"dev": true`. Benign: minimist 1.2.8 is OSV-clean and already-admitted. Worth recording that the production dependency surface grew by one already-vetted node beyond the 22 enumerated.

## Defects

None blocking. No CRITICAL/HIGH/MEDIUM findings. Two LOW advisory notes above.

## Required rework

None required for PASS. Advisory (optional, non-blocking): (a) add `ignore-scripts=true` to `apps/web/.npmrc`; (b) note the minimist dev→prod promotion in the producer report for completeness.

## Reviewer conclusion

**PASS.** maplibre-gl@6.7.0 and all 21 transitives are advisory-free, registry-sourced with sha512 integrity matching upstream, age-gate-compliant (tightest = 10.45 d), install-script-free, permissively licensed, and carry strong provenance (OIDC trusted-publishing on the MapLibre/Mapbox core; recognized maintainers elsewhere). The producer's 22-node table matches the actual lock diff; no rogue-registry, typosquat, downgrade, or hidden version bump. The generate-lockfile workflow is sound (SHA-pinned actions, pinned npm, fail-closed proof chain before a lock-only bot commit) and gh-corroborated; CI is green at the post-lock head. The admission is inert (no source imports maplibre) and all prohibitions hold (no deploy, no 16.x drift — next stays 15.5.24, Supabase/G6 untouched, expansion hold otherwise preserved). Two LOW advisory notes; one acceptance precondition outside G5 scope: the directive-compliance-verifier must record the D-040-R001 / M5-T022 DCV PASS row at reviewed_sha==HEAD before accept().

**VERDICT: PASS.**
