# M0-T036 V1.2.3 — DCV directive-compliance delta re-verification (for acceptance)

- Role: directive-compliance-verifier | Directive: D-007 | Task: M0-T036 | Reviewed SHA: **4ff4d88 (HEAD)**
- Reviewer model: claude-opus-4-8 / xhigh | Producer: backend-engineer (producer ≠ verifier)
- **VERDICT: PASS — acceptance-ready (shadow-only): YES.** Recorded by the orchestrator; reviewer changed nothing.
- (Condensed faithful record; full verbatim return is in the agent transcript.)
- Content-identity note: M0-T036 `allowed_paths` carry parenthetical annotations, so the mechanical content-identity (`08f8db0e`) does not move with code — verified against the ACTUAL HEAD code, not the stamp.

## Results
- **D-007-R207 → PASS** (was UNVERIFIABLE). All 4 previously-absent members present, bounded, configurable, fail-closed: `config.py` Limits `max_model_calls_per_day/max_external_writes_per_day/max_cpu_percent/max_memory_bytes` (from_mapping refuses unknown_limit/bad_limit); `circuit_breakers.py` per-day counters + `record_daily`/`_roll_day` (fail-closed on non-per-day counter + missing/empty day) + cpu/memory gauges; config.example.toml knobs; 9 tests green. Live loop sampling is the disclosed Phase-2/3 boundary (consistent with peers). Also completes **R615**.
- **D-007-R593 → NOT_APPLICABLE** (never PASS, per R621). not_applicable_justification (recorded in verification.json): *"R593 is NOT_APPLICABLE to the shadow-only acceptance of M0-T036 per owner directive D-007-R618 (Option A, source-016-amendment.md, captured 2026-08-06T19:41:13Z). R593's third live leg — live context-threshold rotation-seam actuation (QA-gap-4) — is an EXPLICITLY ACCEPTED RESIDUAL deferred to the R595 supervised rehearsal. It is structurally infeasible as a synthetic probe under R594 (the seam requires a real forward; R594 keeps shadow-only in force and forbids anything forwarding to a real task), so the R616 STOP-and-report branch was correctly taken and NO substitute evidence was fabricated. The requirement is NOT waived; per R619 the R595 supervised rehearsal is a MANDATORY BLOCKING prerequisite before any supervised-auto/limited-auto/product-task execution or any claim that live rotation is proven. 2 of 3 V1.2 live legs are independently proven (live ALLOW round-trip = QA-gap-1 CLOSED; live model-mismatch detection); the rotation seam is unit- and real-process-proven but not live-actuated. Independently approved on the strength of the owner directive; recorded NOT_APPLICABLE, never PASS (R621)."*
- **575 carry-forward** — delta `b95ebf7..HEAD` touches exactly 6 files (R207 + resume-command + their tests), strictly additive (the −3 is docstring re-wording); no existing field/counter/gauge/method/command renamed/removed/weakened → the 575 prior PASS carry forward. Specifically re-verified reqs the resume command bears on: **R338, R147/R151/R152, R341, R436/R203/R456** — all remain PASS (additive operator affordance, digest-bound, fail-closed, fires only the pre-existing legal edge).
- **10 newly-applicable amendment reqs** (R608, R609, R615, R616, R617, R618, R619, R620, R621, R622) — each SATISFIED (R608 non-gating; R619 activation-hold active+honored; R622 verifier-scope clause discharged, accept-time clauses pending orchestrator).

## Harness (read-only): validate_directive_compliance --check exit 0; test_directive_compliance 102 OK; test_project_control 22 OK; supervisor suite 1163 passed / 2 skipped (+15 = 9 R207 + 6 resume, reconciles to b95ebf7 baseline 1148).

## Posture at HEAD — CLEAN: not accepted (in_progress); not on main (supervisor code absent from origin/main; no PR from the code branch — #149/#150 are control-plane capture packets); shadow-only (default_mode=shadow; limited-auto refused at boot).

## OVERALL: applicable set = all PASS or justified-NOT_APPLICABLE. No VIOLATED / UNVERIFIABLE remain. No residual gating id. Forward-looking only: R619 (R595 rehearsal = mandatory blocking prerequisite before ANY activation) + R622/R618 accept-time orchestrator actions. **Verifier verdict: PASS.**
