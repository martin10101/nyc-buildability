# M5-T113 — G3 code review (code-reviewer "cr-t113", read-only)

> Transmission history: pinned at 7b3686f0, delivered as 2 SendMessage part(s) ending with END-OF-REPORT;
> no truncation. Joined verbatim by the orchestrator (transport wrapper tags removed only). Verdict PASS; four ADVISORY items
> (a shim count in the report, a corpus-note citation, cap/cap+1 fixtures, count-balanced type mismatch).

---

M5-T113 G3 code review (READ-ONLY) — PART 1/2

PIN: HEAD = 7b3686f027b0e71aacdff4056daa456659ac2a79 at start. Material fcd0033e (cherry-pick of wt-m5t113 21956e4b), 7 files, all inside allowed_paths; forbidden paths untouched. Drift check: sheet_objects/sheet_interpreter/sheet_reader/test_split_equivalence are byte-identical between claim seam 275ef85f and fcd0033e^ (no intervening drift).

BLOCKING findings: NONE.

(a) Predictor content streams — CORRECT. sheet_objects.py:190 reuses the existing bounded apply_predictor (defined 387-422); no second implementation. TIFF(2) refused at :405, out-of-range at :407, empty-data returns before any geometry-sized buffer at :408-409, row_len bounded by absolute_cap BEFORE sizing at :418, alignment re-checked in _png_unfilter :437. Budget charged BEFORE the predictor (:185-190) — proven by test_as1_predictor_stream_shares_the_document_decoded_budget. New _content_decode_parms (:193-214) refuses multi-element arrays / non-dict / parms-without-filter (:158-162). Predictor output ≤ charged inflated bytes (PNG output = rows*row_len < rows*(row_len+1) = input), so no under-charge. Round-trip verified across all 5 row filters (test_as1_...[0-4]).

(b) sheet_marked_content.py — clean, bounded, downward-only imports (strict lex_primitive + _refuse). depth+byte caps (:45-46), balanced <<>> and [] via a uniform depth counter, scalars delegated to lex_primitive, MarkedContentDict discards content (holds 2 ints only). Wired minimally: sheet_interpreter.py:150-156 routes a leading << to _read_inline_dict; BDC/DP are in _IGNORED (:79) and _execute clears the operand (:398-399) — paints nothing. A stray <<>> not consumed by BDC/DP → "dangling operands" refusal (_finish :178-185; test_as2_inline_dict_wrong_position). Single '<' hex strings correctly NOT captured (test_as2_hex_string_operand_still_lexes). Inline images (BI/ID/EI) stay refused per DB-055 d. Bounds are facade-threaded via _InterpreterLimits and proven load-bearing (depth/byte cap mutations redden).

(c)/(e) scan-only — _scan_only_refusal (sheet_reader.py:365+) is behavior-preserving vs the old inline gate (same conditions, feature kept "scan only", detail enriched only). Reachable + load-bearing: test_as3_scan_only_reachable_through_marked_content, _reaches_a_real_raster_page, _gate_is_load_bearing (mutant → empty-page success). Both DB-076 (e) inconsistencies DISCLOSED via characterization tests. Disclosure is FORCED, not convenient: the read-only test_pdf_object_streams.py:446-448 feeds a TEXT-ONLY page and asserts feature=="scan only", so relabeling would break a must-pass-unchanged suite. Rationale in the reader docstring and report OPEN QUESTIONS is accurate.

(continued in part 2)

---

M5-T113 G3 code review — PART 2/2

Golden revision (test_sheet_reader_split_equivalence.py) — CORRECT and exactly as intended. The single refuse_decode_parms case is replaced by decode_parms_png_predictor (drawn success) + refuse_decode_parms_tiff (TIFF still refused); _OVERALL recomputed 767766eb→887a0a68; corpus 62→63; the diff touches ONLY those _GOLDEN entries — the other 61 per-case digests are byte-unchanged. Cross-check: decode_parms_png_predictor digest == flate_content digest (f1b2f835…), proving the predictor yields the same drawing as plain flate. The equivalence fixture uses row-filter tag 2 (not 0) so the leading 0x02 tag byte would be an illegal token if the predictor did NOT run — the success is genuinely predictor-load-bearing.

Modularity: `python tools/modularity_check.py --check` → failures 0 (exit 0). sheet_interpreter.py 690 SLOC (warn band, non-blocking, < 750); new sheet_marked_content.py 171. Scope: exactly the 7 allowed paths; zero new deps (stdlib + internal only); unwired (no route/main/api change). Mutation namespace verified: threaded hook is sheet_objects._decode_stream (sheet_reader.py:94,314), so the apply_predictor patch bites — confirmed by the passing mutation test.

EXECUTABLE EVIDENCE (ran myself, shim311, cwd services/api):
- ruff on the 6 changed files → All checks passed!
- test_sheet_reader_split_equivalence + test_sheet_content_features + test_pdf_object_streams → 81 passed (=9+28+44). Confirms the recomputed golden matches live code, all 28 new tests pass incl. all 5 load-bearing mutations, and the pinned read-only suite is unchanged-green.
- full tests/drawings → 252 passed, 0 failed (no sibling regressions).

Real corpus (AS-4): report is honest — 0/6 read fully, six DISTINCT correct content-level outcomes, explicit "nobody may tell the owner real architect PDFs can be read." Provenance file docs/research/architect-drawing-corpus-2026-09.md exists.

ADVISORY (non-blocking):
- ADV-1: report says "245 passed" via shim (line 124); authoritative run at pinned HEAD is 252 passed. Undercount, not a false pass — every claimed-green suite is green.
- ADV-2: report cites architect-drawing-corpus-2026-09.md for sha256s while the packet SPEC SOURCE named architect-corpus-reader-trial-2026-09.md; both files exist (prose citation only; docs/research read-only for the producer).
- ADV-3: cap tests prove the bounds are threaded/load-bearing via mutated caps, not a default-cap boundary fixture (exact >/>= at the default cap unpinned) — consistent with the carried DB-076 (j); add a cap/cap+1 fixture at the next touch.
- ADV-4: read_inline_dict counts <<,[ and >>,] uniformly and returns only when depth hits 0 via >>; a type-mismatched-but-count-balanced (invalid) PDF refuses or over-consumes within the byte cap rather than pinpointing the mismatch — safe (bounded, typed refusal, no exec); real PDFs are well-typed.

M5-T113 G3 VERDICT: PASS

END-OF-REPORT
