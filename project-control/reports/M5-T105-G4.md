# M5-T105 — G4 test-adequacy review (qa-engineer "qa-t105", read-only)

> Transmission history: pinned at 259b6d40, delivered as two SendMessage parts (1/2, 2/2) ending with END-OF-REPORT;
> no truncation. Joined verbatim by the orchestrator (transport wrapper tags removed only). Verdict PASS: 8 own weakenings
> killed where expected (both cap edges, per-field coverage, screen order, escaping proven through the reader, the y-only
> probe, the never-raise boundary); byte identity genuine. Advisories A-1, A-2 are routed at the accept seam.

---

M5-T105 G4 (independent test-adequacy, read-only) — PART 1/2

PIN: HEAD 259b6d4036652e2b5a23e9ca184c01eff937c77d at start. Material commit e294cf1f. Repo C:\Users\MLFLL\Downloads\nyc-zoning\ctl24.

ENV: services/api, Python 3.11.9 (reportlab absent; writer is stdlib-only, reader sideloaded per the test's 3.11 path). Baseline: `python -m pytest tests/cad -q` = 368 passed; test_pdf_sheet_writer.py = 211 passed.

METHOD: file writes (Write tool + shell) are guard-blocked for this reviewer, so I mutated IN-MEMORY via `python -c`, importing the real test module and rebinding writer module globals (production looks them up at call time), then invoked the REAL test functions and recorded genuine green→red. Consuming-namespace rebinds throughout.

MUTATION TABLE (all against the real suite):

| # | Mutation | Target real test(s) | Result |
|---|---|---|---|
| M-A | length cap `>=` not `>` | test_exactly_at_cap_caller_text_still_renders ×4 | KILLED ×4 |
| S-2 | cap allows cap+1 (`> cap+1`) | test_over_long_caller_text_is_refused_nothing_drawn ×4 | KILLED ×4 |
| M-B | one field (bbl) skipped by cap | test_over_long…[bbl] | KILLED bbl only; other 3 SURVIVE = every field individually covered |
| M-C | length checked AFTER claim screen | test_length_cap_precedes_the_claim_word_screen | KILLED |
| M-D | escape ( ) but NOT backslash | test_unbalanced_parens_round_trip (backslash fixtures) | KILLED backslash fixtures; paren-only survive (correct) |
| M-E | escape backslash LAST / double-escape | test_unbalanced_parens_round_trip (paren fixtures) | KILLED all paren fixtures (lone-backslash survives = coincidentally correct escaping; paren fixtures cover it) |
| M-F | non-finite CLAMPED to 0.0 not raised | test_number_formatter_refuses_non_finite ×3, test_public_api_converts_internal_raise, test_non_finite_drawing_number_not_a_token | KILLED all |
| EX2 | finiteness on X but NOT Y (the M5-T091 survivor) | test_y_only_non_finite_vertex_is_non_finite_coordinate ×3, test_invalid_rings y-only nan/inf params | KILLED; x-only nan param SURVIVES = mutation is genuinely y-specific |

Producer self-mutation tests re-confirmed passing unmutated: y_only_finiteness_check_is_load_bearing, boundary_conversion_is_load_bearing, escaper_bypass_mutant_reddens, paren_skipping_escaper_mutant. (part 2 follows)

---

M5-T105 G4 — PART 2/2

CHECKLIST:
- M5-T091 x-only survivor dies via the y-only probe? YES — test_y_only_non_finite_vertex_is_non_finite_coordinate KILLED for nan/inf/-inf under the x-only mutant; specificity proven (x-only nan param survives). It asserts the SPECIFIC displaced code (non_finite_coordinate → becomes non_finite_value/oversize_input), not just isinstance.
- ≥4 own weakenings? YES — 8 (M-A, S-2, M-B, M-C, M-D, M-E, M-F, EX2).
- exactly-at-cap and cap+1 boundaries pinned? YES — M-A kills exactly-at-cap (120), S-2 kills cap+1 (121). Bound is inclusive and both edges have teeth.
- Escaping proof PARSES the content stream (not string-matching the escaper output)? YES — independent probe: real escaper → reader-parsed text_run "SITE: a)b(c" present True; no-backslash mutant on "R\D" → reader returns content-refused. The round-trip asserts on interpret_content().text_runs.
- Byte identity genuine? YES — golden sha256 == fresh render; committed docs/samples/cad/example-site-plan.pdf (1839 B) regenerates byte-identically via test_cad_owner_samples.build_sample_pdf(); that suite has a strict one-byte-diff anti-vacuity guard. Sample's longest caller field = 33 chars vs cap 120 (headroom).
- Vacuity? Assertions move under their bug: over_long asserts field-in-detail + caller-text-NOT-echoed + build_calls==[]; the clean-text test asserts build_calls==1 so the spy is live; order/y-only assert the displaced code value.

SCOPE: `git diff e294cf1f..HEAD` for both code files = EMPTY (no drift). e294cf1f changed exactly the 3 allowed paths (report + writer + test); no forbidden path touched.

FINDINGS:
- BLOCKING: none.
- ADVISORY A-1: the _num non_finite_value path and the _finish boundary catch are reachable in production today only via monkeypatched constants (writer unwired; all real drawing numbers derive from validated finite inputs). Honestly disclosed as defense-in-depth; tested correctly via in-process mutation. When PKT-D wires caller geometry, add one end-to-end test that a pathological-but-bounded caller input returns the typed refusal through the real route.
- ADVISORY A-2 (informational): ISO 32000-1 §7.3.4.2 is marked "[recalled - verify]"; implementation escapes all three metacharacters unconditionally (safe superset), so correctness does not depend on the recalled wording. Verify the citation at wiring time.

Every acceptance scenario (AS-1..AS-4) is backed by a mutation that genuinely reddens a specific assertion; boundaries, ordering, per-field coverage, the y-only probe, the never-raise boundary, escaping-by-parser, and byte identity all hold.

M5-T105 G4 VERDICT: PASS
END-OF-REPORT
