# M5-T104 — G3 code review (code-reviewer "cr-t104", read-only)

> Transmission history: pinned at cde8fc0e, delivered as two SendMessage parts (1/2, 2/2) ending with END-OF-REPORT;
> no truncation. Joined verbatim by the orchestrator (transport wrapper tags removed only). Verdict PASS: every committed
> literal reproduced against live production; the DB-055 (a) pre-fix revert moves points[1] exactly as recorded; the
> section 8.3.4 citation is right. Advisories A1-A4 (flattening-coupled literals, marker consistency, the type-alias
> citation) are routed at the accept seam.

---

M5-T104 (D-087 PKT-K2) — G3 independent code review (read-only). Part 1/2.

PIN: HEAD at start = cde8fc0e (as expected). Material commit c0ce1ae3, exactly 3 files (git show --stat): sheet_primitives.py (1 line), tests/drawings/test_sheet_reader.py (+178), reports/M5-T104-producer-report.md. Method: full-diff read of all 3 files; read of the 4 production modules (sheet_primitives/interpreter/reader/objects); shim run; ruff; inline live-production value + mutant reproduction (no repo writes — read-only guard blocked both file-write paths, so mutants were run inline via `python -c`, never committed).

BLOCKING: none.

VERIFIED CORRECT (independently reproduced against live production):
- AS-1 non-vacuity (DB-055 a) — the core requirement. test_sheet_reader.py:580 pins points[1]=(14.160156,13.339844). I reran the literal pre-fix revert (`p0=_apply_matrix(self._ctm,*self._current)` in _StreamRun._curveto, sheet_interpreter.py:277): points[1] moves to (23.125,20.3125) — EXACTLY the report's M1 value — while points[0]=(10,10) and points[-1]=(40,40) stay invariant. So the prior [0]/[-1]-only assertion was provably vacuous and the new pin has teeth. This is the exact CODING_RULES "assert the field the bug displaces" trap (M5-T083 G4-F6), correctly satisfied. tolerance abs=1e-4 vs a ~9-unit gap — appropriate.
- Committed flattened literals match live production exactly: mid=(14.16015625,13.33984375), v=(21.71875,19.2578125) (test:684), y=(4.1943359375,7.041015625) (test:697). Not wrong.
- Rotated rectangle (AS-2, test:650): re→4 exact affine-mapped corners, asserted vertex-by-vertex via _rotate_then_translate; v0=(-1.33974596,22.32050808) matches an independent rotation-matrix computation. abs=1e-9 correct (no flattening on straight edges). len==4, closed/stroked/not-filled all correct vs _paint.
- Paint flags (test:706): every (stroked,filled,closed) tuple matches _PAINT_STROKE/_PAINT_FILL/_PAINT_CLOSE_FIRST (interpreter.py:65-68). Clip W/W* (test:718) are in _IGNORED (interpreter.py:82); kept+painted by n. Correct.
- Text origins (test:730, :751): I hand-derived every origin from Td/TD/T*/TL/'/"/TJ concat semantics — A(100,700) B(100,680) C(100,660) D(100,646); XY(50,470) P(50,460) Q(50,450). All exact and correct; TJ correctly skips the numeric kern.
- DB-064(a) two forms (test:764): distinct ref keys (5,0)/(6,0) → distinct memo entries; FmA=[0,0,5,0], FmB=[100,0,100,9]. DB-064(b) (test:786): decode_contents charges per-DOCUMENT via _StreamDecoder.charge_decoded (objects.py:236-244); two 13B streams under cap=20 but sum 26 refuses "decoded bytes budget" — genuinely exercises accumulation the single-stream cap=4 test (test:487) cannot. Not duplicates.

Continued in part 2.

---

M5-T104 G3 review. Part 2/2.

CITATION (DB-055 e) — sheet_primitives.py:52: apply_matrix §8.3.3→§8.3.4. Correct. §8.3.4 "Transformation Matrices" is where ISO 32000-1 gives the point-mapping equation x'=a·x+c·y+e / y'=b·x+d·y+f that apply_matrix implements; it now matches concat_matrix (line 37, already §8.3.4 for cm). Internally consistent. The whole file marks these citations "[recalled - verify]", so the exact clause is a documented residual, not a load-bearing value.

MUTATION-NOT-COMMITTED — acceptable. The 10 mutants (report M1-M10) all patch M5-T103-owned internals (_PATH_HANDLERS, _PAINT_*, _IGNORED, _StreamDecoder methods) in this packet's forbidden_paths. Committing them would create a brittle cross-task dependency on names M5-T103 may rename. The task input says "record each mutant and its red run" (record, not commit); the M5-T083 G4/DCV precedent did the same. The durable guard is the committed assertions passing against production + recorded red runs; I reproduced M1 myself. Correct call.

SCOPE (AS-4) — clean. Exactly the 3 allowed_paths; zero forbidden paths touched (split_equivalence golden untouched, 9/9). No new imports/deps in the diff. Existing tests unchanged except the one DB-055(a) strengthening (points[0]/points[-1] assertions preserved; points[1] inserted). Count 37→46 (9 new + 1 strengthened). Shim: 55 passed (46 sheet_reader + 9 split). `python -m ruff check .` from services/api: All checks passed.

ADVISORY (non-blocking):
- A1: the v/y/mid-path points[1] literals are de Casteljau flattening-derived dyadics (not hand-derivable from pure operator semantics); they couple the tests to DEFAULT_FLATTEN_TOLERANCE=0.25 + the midpoint-subdivision algorithm, so a legitimate flattening change would redden them. Mitigated: endpoints anchor the operator semantics and mutants M1/M3/M4 prove the interior pin is non-vacuous. Fine for a regression guard; worth a one-line comment that these lock flattening too.
- A2: the fixed apply_matrix docstring (primitives.py:52) drops the "[recalled - verify]" marker that concat_matrix:38 and the Matrix type-alias comment:29 still carry — cosmetic inconsistency.
- A3 (= producer D2): the Matrix type-alias comment (primitives.py:28-29) still cites §8.3.3 while stating the §8.3.4 mapping equation; correctly left out of scope (DB-055 e named only apply_matrix) and flagged. Follow-on nit if all affine citations should point at §8.3.4.
- A4: producer D1/D3 discoveries (dispatch-table by-value mutant trap; DB-064 c/d + DB-055 f still open, pre-existing) are correctly routed to the orchestrator, not fixed in-packet.

Verdict basis: expected values derived/correct, AS-1 mutant reproduced, scope clean, tests green, ruff clean, no BLOCKING defects.

M5-T104 G3 VERDICT: PASS

END-OF-REPORT
