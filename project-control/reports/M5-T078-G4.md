# M5-T078 + M5-T079 — G4 independent test-adequacy review (qa-engineer "qa-web", read-only; joint review)

> Transmission history: one joint review pinned at 33662211, delivered as six SendMessage parts (1/6..6/6)
> ending with END-OF-REPORT; no truncation. Joined verbatim in delivery order by the orchestrator
> (transport wrapper tags removed only); saved identically as M5-T078-G4.md and M5-T079-G4.md. A separate
> follow-up message from the same reviewer (agent-memory lessons it could not write itself) is NOT part of
> this report. Verdicts: M5-T078 FAIL (F1-F3 BLOCKING), M5-T079 FAIL (F1-F2 BLOCKING).

---

M5-T078/M5-T079 G4 report (qa-engineer, read-only) — part 1/6: pin, CI proof, M5-T078 caught mutants.

PIN: HEAD 33662211137a36f0ff4b7c8ac8ae779f75943f8a. All 11 listed blobs are identical at the material commits (5879cdfa, 006f3589), at acb224b2 and at HEAD. Both material commits are ancestors of acb224b2, and acb224b2 is an ancestor of HEAD. T078 touched exactly its 5 files + report; T079 exactly its 6 files + report. No forbidden file changed (ProposalEditor, proposal-editor.test, e2e for T078: empty diff).

CI 35975846444 @ acb224b2 (web-e2e job log, read via gh): all green —
- lot-outline-map 27
- proposal-outline-draw 14
- proposal-outline-map 21
- max-envelope-panel 24
- max-envelope-api 32
- entry 42
- proposal-editor unit 7
- vitest 1396/1396
- Playwright 120 passed, incl. proposal-editor.spec.ts :130/:282/:350/:544
The per-file counts match the spec sources.

M5-T078 mutants, traced CAUGHT (mutant -> failing assertion):
- Hint re-gated on finiteCount<MIN (AS-1's named mutant) -> draw:358: getByTestId("outline-draw-min-hint") throws.
- Counts line drops the omitted clause -> draw:368 "1 row". Converted count taken from points.length -> draw:367 "3 points converted".
- aria-invalid dropped on latitude (AS-2's named mutant) -> draw:394. Dropped on longitude -> draw:398. Row-level aria-invalid on both inputs -> draw:392.
- Blanket "longitude and latitude" row marker -> draw:390/391.
- New live region inside the wrapper, or aria-live on the instructions <p> (AS-3's named mutant) -> map:378 (count 2). Readiness not folded into the status -> map:375. Readiness made unconditional -> map:369.
- `disabled` restored (AS-4's named mutant) -> draw:422.
- Blocked click routed into convert() -> draw:426. fetchImpl is called synchronously inside fetchOutlineBridge, before its first await, so the synchronous zero-call check is sound.
- No announcement on a blocked click -> draw:428.
- Untyped selection says "move" -> map:394/397. Selection dropped at zero points -> map:402.
- Overlay change rebuilds the map -> lot:739 (mapInstances length 1). This is deterministic: stable fetchImpl, callbacks held in interactionRef, build deps [geometry, context, interactive].
- Dropped getSource guard, or a stale setData -> the unchanged DB-048 teeth.
- canConvert reverted to count-only -> draw:139. The migrated aria-disabled assertions keep this check.

---

M5-T078 G4 — part 2/6: surviving mutants and three BLOCKING findings.

Survivors:
- S1: the too-few hint arm (ProposalOutlineDraw.tsx:194) reverted to the pre-fix blanket "need(s) a longitude and latitude"
- S2: an untyped row selected while the map is not "present" (this is a defect at HEAD, see F2)
- S3: readiness gated on `mapSurface !== "unknown"`
- S4: `&& !converting` dropped from canConvert
- S5: `[bbl]` changed to `[]`
- S6: a new live region added in ProposalOutlineDraw, outside the wrapper
- S7: the omitted count set only when > 0

F1 BLOCKING — the F7 rider is not tested where the defect was. M5-T071 G3 F7 was the too-few arm saying "needs a longitude and latitude" for a half-typed row. T078 fixed that arm, but the only F7 spec (draw:402) exercises the NEW omission arm (4 rows, 3 finite). No spec renders the too-few arm with one half-typed row:
- draw:140 asserts only "coordinates filled in";
- draw:375 has two incomplete rows ("their coordinates") and does not assert the hint.
So S1 stays green. Required spec: 2 finite rows + 1 row with longitude only. The hint must contain "1 row still needs a latitude" and must not contain "longitude and latitude".

F2 BLOCKING — honesty defect at HEAD. ProposalOutlineMap.tsx:204 appends " — it has no coordinates yet, so a map click will place it" to the status for any untyped selection, with no check that mapSurface === "present". This fires on every fallback lot (condo, no-WebGL, multiple_features, invalid, render error) and while loading. There, selecting an untyped row with the table's Select button makes the screen-reader status promise a map click. The visible instructions meanwhile say "no interactive drawing surface". This is the DB-047(d)/HJ-B1 class (B2 while loading). The only nearby spec, map:261, selects a finite row and checks only the instructions. Required: show the clause only when "present" (keyboard wording otherwise). Add a status negative for each FALLBACK_STATES variant and for loading. Please route to G3/HJ to confirm.

F3 BLOCKING — the AS-5 grep evidence does not prove the property. The producer's pattern matches only the one-line form. ProposalOutlineDraw.tsx:286-288 recomputes the pair check outside isDrawnPointFinite: two Number.isFinite calls, then `lngFinite && latFinite`. AS-5 forbids exactly this. If the shared predicate is ever tightened, the hint will count a row as omitted that shows no marker, and oneMissingPhrase becomes "". Required:
- `rowComplete = isDrawnPointFinite(p)`, keeping the per-ordinate booleans only for the label and aria-invalid;
- a grep that finds split-line pair checks, not only the one-line form.

---

M5-T078 G4 — part 3/6: advisories and verdict.

A1 (S6) AS-3 scope: map:362 counts live regions inside the ProposalOutlineMap container only (mocked leaf, baseline 1). AS-3 and DB-049(c) ("never a sixth") name the whole drawing section. Fix: count inside proposal-outline-draw before and after the flip.

A2 (S3): no status negative in fallback states. The draw HJ-B1 check only looks for "Click the lot map", but the readiness copy says "click it to place points". Fix: add a status negative to the FALLBACK_STATES it.each.

A3 (S4): with `disabled` removed, `!converting` is the only double-submit guard. Dropping it survives: two bridge POSTs, two onAdopt calls. The new copy "Conversion is already in progress." is never asserted. Fix: a deferred-fetch spec — two clicks while pending give exactly one fetch plus that announcement.

A4 (S7): no spec converts twice. Also, the fixture returns 3 vertices for 3 sent, so the server count is never told apart from the client count.

A5: lot:539's title promises "only the 2 lot-outline layers", but presence checks plus overlay-ID negatives let a stray extra layer through. Fix: `expect(new Set(addedLayerIds())).toEqual(new Set(["lot-outline-fill","lot-outline-line"]))` — it tolerates rebuilds and keeps "only".

A6 (S5): the F8 comment overstates its guard. Both specs it names keep bbl constant, so `[]` passes them. The packet asked only for the comment.

A7: the copy table does not list "Conversion is already in progress." word for word.

A8 (for HJ, DB-049(i)): the status re-announces the readiness sentence on every point or selection change, because role=status is atomic.

Checks:
- Every new T078 assertion is a hardcoded literal; no by-value tautology.
- No exact call count on mocks.addLayer; only comments match.
- DB-048 duplicate-source and stale-payload checks intact.

M5-T078 G4 VERDICT: FAIL

Required corrections, all inside allowed_paths:
- F1: one spec.
- F2: gate the clause + spec.
- F3: one-line change + corrected grep evidence.

---

M5-T079 G4 — part 4/6: mutation table (mutant -> spec -> result).

1. XOR branch removed from dimensionRowKind (AS-2's named mutant) -> api:461-462 + panel:367/387 -> CAUGHT.
2. Violation term dropped from envelopeAggregateIsComplete (named mutant) -> api:485 + panel:374/391 -> CAUGHT.
3. DimensionRow reverted to the pre-fix `gapReason !== null ? gap : value` -> panel:367/387 (no violation testid) -> CAUGHT.
4. Aggregate violation clause dropped -> panel:375 -> CAUGHT.
5. Value text leaked into the violation branch, e.g. "withheld: 20000 sq_ft" -> only the value testid's absence is asserted -> SURVIVES (A1).
6. Truthy classifier `Boolean(d.bindingValue)`, which withholds a real 0 limit -> no 0-valued fixture -> SURVIVES (A2).
7. Claim wall weakened: no lowercasing (lowercase-phrase-only), hyphen-only or space-only collapse, or no comment stripping -> panel:316-319, :298, :328-329 -> CAUGHT.
8. Banned phrase split across JSX {" "} or "…" + "…" -> SURVIVES (A3).
9. Gap copy set to the raw token, a key removed, or a token added without copy -> panel:339, tsc (web job), panel:337/:345 -> CAUGHT.
10. Gap copy reworded to a misleading meaning (e.g. "no limit applies") for any of the 3 tokens without a literal pin -> SURVIVES (A4).
11. proj4, "ol/proj" or @turf import injected -> api:525 -> CAUGHT. 'ol/proj.js' or a new helper module -> SURVIVES (A8).
12. Active-token guard removed -> panel:450 -> CAUGHT.
13. Aborted guard removed (MaxEnvelopePanel.tsx:288, the literal pre-fix of DB-050(c)) -> SURVIVES (F2).
14. announcementForMaxEnvelope returns "" for client_timeout, payload_too_large or invalid_request (a reasonless typed card) -> SURVIVES (F1).
15. Loading branch or aria-busy removed -> panel:405/406 -> CAUGHT. Retry no-op or double fetch -> panel:423/426 -> CAUGHT.
16. timedOut branch or the pre-flight aborted check dropped -> api:425 / api:442-443 -> CAUGHT.
17. beforeEach fetch stub removed -> entry:215 -> CAUGHT. Stub narrowed to the entry:209 test only -> SURVIVES (A6); entry:206 also passes on a real network_error card.
18. e2e JSON body mismatch -> explicit toEqual diff with its message -> CAUGHT, no timeout.
19. Seed street_width_class set to "narrow" -> no assertion -> SURVIVES (A7).

---

M5-T079 G4 — part 5/6: two BLOCKING findings, plus a mutant to run.

F1 BLOCKING (AS-1 wording). AS-1 requires client_timeout, payload_too_large and invalid_request to each "render their typed failure card with reason text — one spec each". The evidence is API classification only (api:248-255, api:412-425). No spec renders any of the three in the panel. None of their reason strings ("took too long", "too large to send", "lot context was refused") appears in any spec. So mutant 14 survives for these three kinds — the reasonless-card class DB-050(c) is meant to prevent.

Required: a panel it.each.
- 413 payload_too_large and 422 validation_error via the stub.
- client_timeout: vi.useFakeTimers, a pending fetch that rejects on abort, then advanceTimersByTimeAsync(12_000) (DEFAULT_TIMEOUT_MS). Assert synchronously; findBy does not advance vitest fake timers.
- Assert the literal phrase in the card's first <p>.
- Assert Retry follows maxEnvelopeOutcomeIsRecoverable: present for client_timeout, absent for the other two.

F2 BLOCKING (the DB-050(c) fix is untested). Deleting MaxEnvelopePanel.tsx:288 leaves all 24 panel specs green:
- panel:429 supersedes through the token guard, and its fetch ignores the signal, so no aborted outcome reaches the panel;
- panel:454 is a 500;
- api:376 tests only the announcement text.

The producer's limitation says the guard is not observable in RTL, because aborted-on-the-active-token arises only from supersession or unmount. That is wrong. When request becomes null, activeRef is not bumped (:270-275). The aborted result then passes the token guard while the panel stays mounted — exactly the path the :283-287 comment describes. It is observable:
1. render(REQ) with a pending fetch that rejects on abort;
2. await act: rerender(null), then flush (setTimeout 0, as panel:448 does);
3. attach a MutationObserver (childList, subtree) to the container;
4. rerender(REQ2), then call takeRecords();
5. assert no added node has data-testid="envelope-failure".
The pre-fix code commits the reasonless card before the effect sets loading.

Executed mutant to run on a throwaway CI branch:
(a) delete :288 and run max-envelope-panel.test.tsx — I predict 24/24 green, i.e. the mutant survives;
(b) add the spec above — I predict green at HEAD and red with (a).

---

M5-T079 G4 — part 6/6: advisories and verdict.

A1 (5): add row-text negatives: not.toHaveTextContent("20000") on the BOTH row and not.toHaveTextContent("null") on the NEITHER row.

A2 (6): add a binding_value: 0 row, which must classify as "value", and a gap_reason "" case (two-mutant rule: include falsy values).

A3 (8): the wall scans source text, so prettier-wrapped JSX ({" "}) and "a " + "b" splits evade it. A `//` inside a rendered URL also blanks the rest of that line. A wall over rendered textContent across the fixtures does not depend on source formatting. The (l) requirement itself (case, hyphen, space) is met.

A4 (10): pin all four gap-copy literals as hardcoded strings. `gapReasonCopy(token) === GAP_REASON_COPY[token]` compares against the imported constant (the M5-T081 lesson). Only allowance_unresolved is pinned today (panel:136).

A5 e2e: failures are explicit for JSON mismatches, so AS-4 is met. Three timeout-only paths remain:
- postDataJSON() throws on a non-JSON body before fulfill;
- a missing request times out on the disclosure wait with no contract message;
- only the last request body is kept.
Fix: read raw postData(), fulfill first, then parse in try/catch. Add `expect.poll(() => capturedMethod, {message})` before the panel wait. Capture bodies into an array and assert length 1.

A6 entry: the stub answers every URL with a 404, and only entry:209 checks routing; make it fail closed on unexpected URLs. Suite time proves nothing either way: entry ran 11.9 s @acb224b2, 8.5 s @33662211 and 7.3 s @f2870507 with T079, and 8.8 s @f5e51d71 before it.

A7 (19): assert draft.street_width_class === "" in the panel adoption spec (D-051: a client default of "narrow" is not conservative). The byte-identical seed claim itself holds by reasoning plus api:396.

A8: the CRS guard covers only 3 files and a fixed marker list.

A9 (copy, for HJ): with only a contract violation, the aggregate reads "Could not check 0 of 2…" and the announcement says "0 of 2 could not be checked". Producer D3 names only the aggregate.

Checks:
- By-value tautology only in A4.
- The Retry exact counts are per click and deterministic; not the DB-048 flake class.
- Non-srid candidate rejection is covered (api:310).

M5-T079 G4 VERDICT: FAIL
Required: F1 + F2, test-only, inside allowed_paths.
END-OF-REPORT
