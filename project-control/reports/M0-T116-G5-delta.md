# M0-T116 — G5 DELTA re-attestation (verbatim)

**M0-T116 — G5 DELTA VERDICT: PASS** (corrected frozen identity `11cfe8bbd58d5c80655ccc0dc57c0a3e3c4a5c84`, verified == HEAD). The correction delta `869b313..11cfe8b` (commit `9edbc47`) is doc/control-plane only — `git diff --name-only` for tools/apps/services/packages/supabase/.claude returned empty; the certification runs are untouched. The rewritten activation-package TOP banner satisfies the honesty charter: (a) it claims no activation or resume beyond the owner's already-exercised Amendment-9 decision — it records the completed Amendment-8 chain, the owner's exercised R187/R595 activation decision (Amendment 9, limited-auto), and the first live run stopping fail-closed, all as genuinely-past events consistent with my round-1 review; no NEW resume is asserted; (b) it gates the R276 RESUME correctly — "the authorized loop resumes ONLY once M0-T116 itself is ACCEPTED through its gates AND the complete activation preflight (manifest re-record, verify-controller, doctor, doctor --live, CI) passes again; on any failure the loop stays stopped" — matching the task objective's preflight, plus "Nothing in this package activates or resumes anything by existing"; (c) no secrets introduced (gitleaks over `869b313..11cfe8b`, 2 commits/~35KB: no leaks) and no owner gate weakened — the change removes a stale Amendment-8/M0-T112 contradiction (G3 MAJOR-1: the old banner read as "already presentable" on an invalidated cert) and strengthens, not loosens, the resume gating. Recertification §4 rewording accurately describes the G3 correction and restates the R276 gating. Control-plane bookkeeping is clean (submit-record repointed to `9edbc47`/new content-manifest, one accurate rework progress_log entry, timestamp bumps in state/task JSON). The committed `M0-T116-G5-security.md` faithfully reproduces my round-1 PASS at `869b313`. No new critical/high/medium/low findings; the prior INFO-1 (PREFLIGHT runtime label) is unchanged and remains non-actionable. My round-1 PASS stands; this correction is clean.
