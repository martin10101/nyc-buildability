# M2-T006 — G2 Producer Self-Check Evidence

- **Task:** M2-T006 — Property-profile contract 1.3.0 (additive): reproducibility.staleness, correlation_id description refresh, open-schema key documentation (amendment A1)
- **Gate:** G2 (producer self-check; recorded by orchestrator per the hardened CLI convention `--reviewer orchestrator` = self_check role)
- **Producer:** backend-engineer (resumed run under packet amendment A1; the original run's STOP is recorded in the packet progress log and the producer report §1)
- **Date:** 2026-07-17 (session 11)
- **Branch/commit under check:** `task/M2-T006-contract-1-3-0` @ `e500ea5` (PR #34)

## Producer-local execution (thin client; verbatim results in producer report §7)

- `python -m pytest services/api/tests -q` → **276 passed** (baseline 264 + 12 new, incl. `tests/resilience/test_staleness.py` 7 tests)
- `python packages/contracts/scripts/generate_ts_types.py --check` → OK (generated file regenerated by running the pipeline, never hand-edited)
- `sync_contract_schemas.py --check` → OK (packaged bundle re-synced)
- `validate_contracts.py` → 6 schemas, all fixtures OK, 0 failures, both validation engines agree
- contracts script tests → 6 passed; `ruff check app tests` → clean
- NOT executable locally (disclosed): tsc/vitest/Playwright — no Node toolchain; CI is the proof (below).

## CI evidence (PR #34, commit e500ea5 — all 8 jobs PASS on both push and pull_request events)

| Job | Result | Duration |
|---|---|---|
| api (ruff + pytest) | pass | both events |
| contracts (JSON Schema validation) | pass | 7s / 8s |
| contracts-typegen (TS drift, byte-identical) | pass | 11s / 10s |
| contracts-schema-bundle (byte-identical) | pass | 6s / 6s |
| web (lint + typecheck + build) | pass | 57s / 57s |
| web-e2e (vitest + Playwright vs recorded-official-fixture API) | pass | 2m36s / 2m27s |
| control-plane (ADR-005 regression) | pass | 23s / 22s |
| Scan repository for credentials | pass | both events |

The green `web-e2e` job is the executable proof of the A1 rationale: the harness serves REAL builder output now declaring 1.3.0, and the client (with the A1 vocabulary addition) accepts it across all 53 Playwright journeys. The green byte-identity jobs prove typegen and schema-bundle were regenerated, not hand-edited.

## Scenario self-check mapping (S1–S7)

- S1 schema validity + byte-identical typegen/bundle: contracts, contracts-typegen, contracts-schema-bundle jobs green.
- S2 stale serve: LKG path stamps staleness (served_from_cache true, stale true, upstream_error_type, original_retrieved_at, age_seconds via injected clock); valid fixture `staleness_lkg_m2_t006.json`; human STALE connector note retained — tested in `test_staleness.py`.
- S3 fresh serve: exact two-boolean marker (served_from_cache false, stale false); absence = pre-1.3.0 producer — schema-documented and tested.
- S4 version: builder declares 1.3.0; declared-vs-emitted enforced atomically; unsupported-version bounded error unchanged; rejected-exemplar fixture advanced 1.3.0→1.4.0 in the same commit.
- S5 compatibility: 1.1.0/1.2.0 fixtures still validate; only the two A1 client files changed outside contracts/api; web-e2e green with the 1.3.0-declaring harness.
- S6 correlation_id: description refreshed to match actual generation/propagation (for G1/G3 direct verification).
- S7 regression: full api suite 276 passed; connector field mappings untouched (additive `staleness` dataclass field only in pluto_soda.py).

## Disclosed deviation from the pre-A1 design

`if`/`then` is NOT in `validate_contracts.py`'s keyword allowlist — conditional requirements are encoded with `allOf`/`anyOf`/`const` instead, enforced identically by both validation engines and proven by the two new invalid fixtures (`staleness_cached_without_age.json`, `staleness_stale_without_error_type.json`).

## Scope verification (orchestrator)

Worktree diff contains exactly: `packages/contracts/**`, `services/api/**`, the two A1 files (`apps/web/src/lib/contract.ts`, `apps/web/src/lib/__tests__/validate-profile.test.ts`), and the producer report. No other `apps/web` file, no docs/workflows/other project-control changes.

## G2 verdict

Producer self-check evidence complete and green. G2 permits submission to independent review; it does not accept the task.
