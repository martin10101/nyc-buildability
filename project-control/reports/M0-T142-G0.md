# M0-T142 G0 scoping gate (orchestrator, 2026-09-02)

Task: M0-T142 — D-024 Amendment 45 settlement runtime-identity + explicit Bash restriction
corrective package.

- **Authority:** owner directives 2026-09-02 captured verbatim as D-024 Amendment 45
  (`source-045-amendment.md`, three messages: discharged stop-order causal trace R684;
  the one-bounded-package authorization R685–R698; the exact-`claude-fable-5`
  model-identifier correction R699). Registry validator exit 0 at capture. R685 authorizes
  exactly ONE new task — this one.
- **Qualifying evidence (supervisor-freeze AD-093):** reproduced live defect + provider CLI
  drift + explicit D-024 requirements — the settlement REFUSED a schema-valid WorkerResult
  (`contract_violation: runtime reported no model`; preserved `canary-b5-02r1` artifacts)
  because 2.1.252 reports `modelUsage` as a session aggregate (owner official correction
  R686), and unlisted read-only Bash EXECUTED under `dontAsk` (R688). Causal trace:
  `project-control/reports/M0-T140-canary-b502r1-causal-trace.md` (clusters A/B/C).
  Cited requirement IDs: D-024-R684..R699 (packet + commit message per freeze §3).
- **Scope:** exactly the causal trace's ONE minimal repair boundary — (A) the settlement
  identity seam (new focused `mrl_runtime_identity.py` + the one wiring site in
  `mrl_one_shot.py`; correlation-bound transcript source with the binding explicitly
  added AND tested, per official correction R690), (B) the explicit Bash restriction
  (profile builder refuses neither-allowed-nor-denied inventory tools; existing
  deny channel becomes load-bearing), (C) canary evidence-reader rows (script-side,
  regenerated post-acceptance). Proven surfaces are forbidden paths (schema hotfix,
  worker-result contract, transport, model_selection). No repo-wide audit; no accepted
  task reopened; no live provider call (R685/R699 "no live launch yet").
- **Dependencies:** M0-T141 (accepted).
- **Test discipline (R696):** focused tests while editing; ONE affected-suite
  (supervisor tests) verification at the frozen candidate; no repeated full-suite runs.
- **Reviews:** separate foreground read-only G3 code-reviewer + G4 qa-engineer + DCV;
  producer (orchestrator) ≠ every verifier.

Verdict: PASS (task is atomic, authorized, scoped, dependencies accepted).
