# M0-T143 — Independent directive-compliance verification (DCV), preserved VERBATIM

Returned by the read-only `directive-compliance-verifier` agent through the agent-return channel
on 2026-09-03; saved verbatim by the orchestrator (transport entity-decoding only).

---

# DCV Report — M0-T143 vs D-024 Amendment 46 (R700–R713)

**Verifier:** directive-compliance-verifier (read-only, producer ≠ verifier)
**Reviewed head:** `59f572c1410cd25df520bb9b58ffd75695fb7b2e` (confirmed `git rev-parse HEAD`)
**Frozen CODE candidate:** `3f4cee8680ba5c9a167327507af387d316f5dbc3` — `git diff --quiet 3f4cee86 HEAD -- tools/agent_supervisor` **exits 0**; `HEAD:tools/agent_supervisor` tree = `209026fd` = `3f4cee86:tools/agent_supervisor`. Code identity preserved; all post-`3f4cee86` commits are control-plane/docs/binding only.
**Scope guards:** `git diff 56848d98 HEAD -- M0-T141.json M0-T142.json` empty (accepted packets byte-unchanged); branch `candidate/D-024-mrl-option-b` has **no upstream** (no push); every non-control-plane file touched is inside the packet `allowed_paths`.

## Per-requirement verdicts

| ID | Verdict | Primary evidence I reproduced |
|---|---|---|
| **R700** | **PASS** | Exactly one new task `project-control/tasks/M0-T143.json` (`status=awaiting_gate`, bounded `allowed_paths` = schema/validator/reviewers/tests/fixture/binding/script/docs). M0-T141/M0-T142 `accepted` and byte-unchanged in range; M0-T140 still `claimed`. `M0-T143-G0.md` lines 12-15 cite probe `codex-schema-probe-20260902-191655532` verbatim (`invalid_json_schema`, 400, `text.format.schema`, `'uniqueItems' is not permitted`). |
| **R701** | **PASS** | Verified by absence: `git diff 56848d98 HEAD` touches only schema, controller validator, pre-spawn inspection, reviewer failure paths, tests, fixture, binding, owner script, docs. No auth/model-availability/executable-identity/budget/Fable/repo-state probing added anywhere in the diff. |
| **R702** | **PASS** | `git diff 8d6fa2db 3f4cee86 -- schemas/review_verdict.schema.json` removes `minLength`,`maxLength`,`minItems`,`maxItems`,`uniqueItems` **together in the single commit** `3f4cee86` (only commit in range touching the file). After-state (`git show HEAD:…`) retains only types/enum/required/array-item-type/`additionalProperties:false`. |
| **R703** | **PASS** | `mrl_codex_decision.py::ReviewVerdict.from_provider` (lines 65-111): `validate_instance` over flattened schema (shape/types/enum/no-extra-keys) + nonempty & ≤4096 rationale, 1..64 ids, nonempty & ≤128 per-id, uniqueness with duplicates named, issued-ids-only. Ran `pytest tools/test_agent_supervisor_mrl_codex_decision.py` → **24 passed**. |
| **R704** | **PASS** | `ReviewVerdictContractTests` (`test_unknown_and_forged_fields_rejected`, `test_wrong_type_and_bad_enum_rejected`, `test_lengths_and_cardinality_rejected`, `test_non_controller_evidence_id_rejected`, `test_more_than_max_ids_rejected`) + `M0T143ControllerEnforcedBoundsTests` (empty/overlong rationale, empty id, duplicate-named, wrong-item-type, non-object). Covers all 8 named mutation classes controller-side; 24/24 pass. |
| **R705** | **PASS** | `mrl_provider_schema.assert_codex_output_schema_strict` called **before any spawn** in `mrl_one_shot_review.py:219` (before `resolve_chain`/`observe_version`) and `codex_reviewer.py:558` (spawn `self._run` at line 651). Ran `test_unsupported_schema_keyword_refuses_before_any_spawn` → PASS, asserting `rh.calls==[] and rh.version_calls==[]` (zero child processes). Sweep `test_every_provider_facing_schema_passes_its_provider_inspection` present; 47 provider_schema tests pass. |
| **R706** | **PASS** | `codex_reviewer.py`: `bounded_stream_tail` (redact-then-bound, `[TRUNCATED …]` marker, 600 chars), `failure_tails` (stdout+stderr), `no_decision_error` uses `provider_failure_reason` which parses `PROVIDER_FAILURE_EVENT_TYPES={"turn.failed","error"}` first code+message + returncode + tails; `provider_rejected_request` never a bare `no_decision`. Ran R706 tests (`…surfaces_parsed_error_and_tails`, `…never_reduced_to_bare_no_decision`, `…truncated_with_marker`, `…redacted_in_tails`, `…timeout_failure_carries_tails`) → all PASS. |
| **R707** | **PASS** | No model-selection edit in code diff; grep of `tools/ docs/` diff shows no `fable` alias / `claude-fable-5-1` introduced (only doc narrative + pre-existing `resolve_model` import). Owner-script PV phase (lines 179-192) VERIFIES exactly one `model = "claude-fable-5"`, rejects `claude-opus-4-8`/`claude-fable-5-1`/`fable`, requires `fallback_models = []`, and never edits the selection; P5 doctor asserts `claude model 'claude-fable-5'`. |
| **R708** | **PASS** | `M0-T143-evidence.md` s7 (lines 130-142) records the split: Claude auth PASS (session `fc7ddb4c`), Codex the only failed leg, item 8 already PASS, item 10 NOT_RUN. Owner-script P9 (lines 310-335) implements it: row 3 splits Claude/Codex legs and surfaces parsed `codex_review_failed` category/message; row 7 requires a settled post-review verdict; item 10 run by P10. |
| **R709** | **PASS** | Determination in `evidence.md` s6 is code-accurate: `loop.py:191 CYCLE_ENTRY_STATES = frozenset({PREFLIGHT, START_CLAUDE, CLAUDE_RUNNING})` (state_machine.py:48 `CODEX_REVIEW` is NOT a cycle-entry state → no resume-from-review). `restart_channel.py:326-340,514` `resume-after-answer` fires `WAIT_FOR_OWNER→PREFLIGHT` on `owner_answer_validated` (full re-preflight); `resume-pending-prompt` needs a `pending_prompt` record. Falls back to ONE successor canary `canary-b5-02r3` via audited surfaces (script P6); no bypass, no hand-edited state. |
| **R710** | **PASS** | Reproduced focused suites (24/47/81). **Independently ran `pytest tools/test_agent_supervisor_*.py` at HEAD (code-identical to 3f4cee86) → 3626 passed, 2 skipped, 0 failed, exit 0** (matches the claimed counts exactly). Fixture `fixtures/codex_schema_probe_20260902.jsonl` sha256 `3c40d9e9af9e228c…` (matches claim) carries owner probe facts; `test_owner_probe_fixture_names_the_rejected_keyword` asserts `invalid_json_schema`/400/`text.format.schema`/`uniqueItems`. No live provider call (all tests use injected harness/static fixture). |
| **R711** | **PASS** | Binding `tools/controller_update/source_binding.json`: `commit_sha=3f4cee86`, `commit_tree_sha=04688351`, `subtree_tree_sha=209026fd` — each **verified against real git objects** (`git rev-parse 3f4cee86^{tree}` = 04688351; `3f4cee86:tools/agent_supervisor` = 209026fd). `ps_tests/test_runbook_parse.ps1` `$pinnedSha` and `docs/CONTROLLER_UPDATE_RUNBOOK.md:84` both pin `3f4cee86`. Owner script exists at `%LOCALAPPDATA%\NYCBuildabilitySupervisor\ctl24-activation\run_m0t143_codex_schema_repair_and_canary.ps1`, **sha256 `096ee3ea749ca3b8654561a20aa442b0bac472ad6f1635e430da0116ebfed5de` — matches the claimed hash exactly**; it implements transactional install+verify (P1-P4), claude-fable-5 confirmation (PV/P5), canonical recovery + single successor canary (P6-P8), item 10 (P10), and the consolidated ten-item table + token. (Independent review = this gate wave; G3/G4 gate records are produced by the orchestrator as part of the same wave — the two producible deliverables are present and correct.) |
| **R712** | **PASS** | No upstream configured → no push; commits `56848d98..HEAD` are local-only on the candidate branch; no PR possible without a push; owner-script deployed but **never invoked** (no `canary-b5-02r3` runtime dir; script's own P0 guard would refuse to overwrite). All touched files inside `allowed_paths` — no unrelated work. |
| **R713** | **UNVERIFIABLE-BY-VERIFIER** | The obligation is that the implementing session's **final reply** ends with exactly `CODEX_SCHEMA_REPAIR_READY`. That is the orchestrator's final message, which I cannot observe from repository state; per the task instruction this row is satisfied by the orchestrator's final message and is not a defect in the reviewed artifacts. |

## Overall

**Verdict: PASS.** 13/14 requirements independently verified **SATISFIED** against primary evidence (files, real git objects, and tests I reran read-only, including the full 3626-passed affected-suite at the frozen code identity). The one remaining row, **R713**, is **UNVERIFIABLE-BY-VERIFIER by design** (it constrains the orchestrator's own final reply, outside repository state) — it is not a VIOLATION and does not indicate a producer gap. No requirement is VIOLATED. Frozen code identity, accepted-packet immutability, no-push/no-execute prohibitions, and the owner-script/binding pins to `3f4cee86` all hold.

Relevant paths: `C:\Users\MLFLL\Downloads\nyc-zoning\ctl24\project-control\directives\D-024-fable-codex-loop\source-046-amendment.md`, `…\requirements.json`, `…\project-control\reports\M0-T143-evidence.md`, `…\tools\agent_supervisor\schemas\review_verdict.schema.json`, `…\tools\agent_supervisor\mrl_codex_decision.py`, `…\tools\agent_supervisor\mrl_provider_schema.py`, `…\tools\agent_supervisor\codex_reviewer.py`, `…\tools\agent_supervisor\mrl_one_shot_review.py`, `…\tools\agent_supervisor\loop.py`, `…\tools\controller_update\source_binding.json`, and the external `C:\Users\MLFLL\AppData\Local\NYCBuildabilitySupervisor\ctl24-activation\run_m0t143_codex_schema_repair_and_canary.ps1`.
