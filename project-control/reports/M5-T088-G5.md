# M5-T088 — G5 security review (security-reviewer "sec-mass2", read-only)

> Transmission history: pinned at 6c79cd88, delivered as three SendMessage parts (1/3..3/3) ending with
> END-OF-REPORT; no truncation. Joined verbatim by the orchestrator (transport wrapper tags removed only).
> Verdict PASS; F-HIGH-1 (the upstream proposal validator's O(n^2) check) is recorded as a BLOCKING
> precondition of any wiring packet, not of this one.

---

M5-T088 G5 SECURITY REVIEW (read-only) — massing truth-object hardening — PART 1/3

Reviewed HEAD (PIN): 6c79cd88 (matches expected). Material: 9f355fe1 — massing_model.py git blob 42545728 (== producer report; byte-identical between 9f355fe1 and HEAD, verified). Method: code read + in-memory probes run from services/api (shapely 2.x/numpy 2.x, Py3.11) + the delivered suite.

SCOPE CONFIRMATION
- Zero new dependencies: imports are stdlib + already-admitted numpy/shapely + internal .proposal. No requirements change.
- Exactly the allowed paths in 9f355fe1 (git show --stat): services/api/app/scenario/massing_model.py, services/api/tests/scenario/test_massing_model.py, project-control/reports/M5-T088-producer-report.md.
- Unwired: git grep finds no importer of massing_model besides its own test.
- Green at reviewed head: pytest tests/scenario/test_massing_model.py = 54 passed; ruff check app/scenario/massing_model.py = clean.

M5-T082 FINDINGS CLOSED (all three, independently reproduced)
- F1 lot_ring magnitude unbounded -> CLOSED. MAX_COORD_ABS=1e8 (massing_model.py:123) checked in _prepare_ring:225-229 before any shapely construct. Probe: lot coord 1e9 -> coordinate_out_of_range with 0 triangulate / 0 prism calls (spy).
- F2 _triangulate O(n^3) -> CLOSED. _WorkBudget:264-280 + _min_ear_clip_work:283-287 up-front check (_triangulate_distinct:597-602) + m-3 charged BEFORE the scan (:326). Metered to MAX_TRIANGULATION_WORK=2,000,000 units.
- F3 unbounded output -> CLOSED. MAX_TOTAL_MESH_VERTICES=100_000 (_check_output_size:577-585) + MAX_TOTAL_FLOORS=2000 (_check_floor_cap:508-518).

REQUIRED CHECKS — PASS (1-2 of 4)
1. Bounds fire BEFORE heavy work: probes show coordinate_out_of_range, over_cap_floors, and over_cap_output_vertices each raised with triangulate=0 / prism=0 (spy). Builder order massing_model.py:683 (floor cap) -> :691 (output cap) -> :708 (triangulation) confirms it. over_cap_floors probe used a B0-VALID 5x500=2500-floor input, so it passed B0 then hit the massing cap = defense in depth confirmed.
2. Worst-case triangulation wall time bounded: measured 1.84 us/unit x 2,000,000 = ~3.7s here (producer 4.23s). _triangulate_distinct refuses 5 distinct 1000-gons up front in 0.000s; budget=1 on a 999-gon refuses with 0 point-in-triangle calls (scan charged before it runs). +containment ~0.23s/500 rings + bounded prism build -> module worst case ~5s.

(continues 2/3)

---

M5-T088 G5 SECURITY REVIEW — PART 2/3

REQUIRED CHECKS — PASS (3-4 of 4)
3. No NaN/inf/huge reaches shapely: _prepare_ring:221-229 rejects non-finite (non_finite) and abs>1e8 (coordinate_out_of_range) before EVERY Polygon/centroid/buffer. Probe: 1e154 AND 1e300 -> typed coordinate_out_of_range (the OLD untyped shapely GEOSException path is closed); NaN / +inf / -inf -> non_finite. Backstop to_json(allow_nan=False):484. Every shapely input in the module is a prepared ring (lot :617-618, origin :631, containment :700, prism :420, buffer :694) — no bypass.
4. Refusals typed, no partial output: every gate raises MassingModelError(reason=...); build_massing_model returns a complete MassingModel or raises. No yield / print / partial write.

FINDING F-HIGH-1 — B0 O(n^2) simplicity DoS (rules on producer Discovery 1)
Severity HIGH (availability). ADVISORY for M5-T088 (out of scope + unwired); BLOCKING for the wiring packet.
- Root cause: proposal.validate_proposed_massing runs FIRST in build_massing_model (massing_model.py:675), BEFORE every massing bound. B0's _ring_is_simple (services/api/app/scenario/proposal.py:143) is O(n^2) and runs once per outline; _validate_levels validates each per-level outline (proposal.py:352-354). With MAX_LEVELS=500 (proposal.py:63), MAX_OUTLINE_VERTICES=1000 (:62), and NO total-positions bound, a single B0-VALID request (500 detailed setback bands x 1000-vtx) forces up to 501 x O(1000^2). NO massing bound can gate it — it is upstream.
- Independently measured: _ring_is_simple on a 999-vtx ring = 0.90s here -> x500 ~= 7.5 min single-threaded (producer measured 2.62s -> ~22 min). Realistic single-request worker-exhaustion DoS once user geometry is wired; the input is within B0's own stated limits, so not even obviously malicious.
- WHERE TO FIX (must land before/with wiring, NOT in this packet):
  (a) primary, proposal.py: add a request-level TOTAL-positions bound in _validate_levels / validate_proposed_massing (sum of top-level + all per-level outline vertices), AND/OR replace the hand-rolled O(n^2) _ring_is_simple with the already-admitted shapely validity (GEOS sweepline ~O(n log n)) — note massing_model.py:623 already trusts shapely is_valid for the lot.
  (b) required at the route: run geometry validation in a cancellable job with a per-request wall-clock timeout (backend-api rule: jobs cancellable + dead-letter). Do NOT call validate_proposed_massing synchronously on untrusted input without a budget.

(continues 3/3)

---

M5-T088 G5 SECURITY REVIEW — PART 3/3

LOWER-SEVERITY FINDINGS (ADVISORY, for the wiring packet — not defects in this packet)
- F-LOW-2 wrong-CRS lot mislabelled: the lot ring gets only the 1e8 magnitude bound (massing_model.py:123 / _prepare_ring); no NYC-2263 unit check (B0 has NYC_2263_* for the proposal; the lot has none). A 4326 lon/lat lot passes, then fails as footprint_outside_lot — the wrong cause. Wiring/connector packet should add a NYC-bounds unit check on the lot. (= producer Discovery 2.)
- F-LOW-3 GEOSException not a ValueError subtype: degenerate shapely geometry raises shapely.errors.GEOSException, not MassingModelError. In-scope I verified _prepare_ring prevents degenerate/over-magnitude input reaching shapely (>=3 non-collinear, magnitude-bounded, is_valid checked at :623), so nothing escapes THIS module today; but any wiring route mapping only ValueError/MassingModelError to 4xx must also catch GEOSException or it 500s. (= producer Discovery 4.)
- INFO: the containment loop (:694-706) and prism build are bounded by validated input (levels<=500, output-cap 100k verts), not explicitly budget-metered — acceptable; measured ~0.23s/500.

SECURITY POSTURE (pure offline deterministic geometry function)
- No secrets/env/credentials; no network/SSRF; no SQL/shell/eval/injection surface; no filesystem/storage I/O; no logging (no redaction risk); no AI/ingested text (no prompt-injection surface); no elevated privilege. All N/A and clean.
- Cross-tenant: the module carries scenario_id / property_geometry_version_id / rule_release_id through provenance verbatim with no auth — correct for a pure builder; the WIRING route must enforce tenant/auth on those ids and treat the outline + lot as untrusted upload. Upload controls present at this layer: vertex cap, coordinate magnitude, floor cap, output cap, triangulation budget — the ONE gap is the B0 time bound of F-HIGH-1.

VERDICT RATIONALE: the three M5-T082 findings are fully and correctly closed; the delivered module's own resource bounds are complete, typed, fire before heavy work, and bound worst-case wall time to ~5s; zero new deps; exact allowed paths; unwired; suite green + ruff clean at 6c79cd88. F-HIGH-1 is a correctly-disclosed UPSTREAM (proposal.py) prerequisite this in-scope, unwired packet cannot fix — it must gate the WIRING packet (record it as a wiring blocker/discovery), not this one.

M5-T088 G5 VERDICT: PASS

END-OF-REPORT
