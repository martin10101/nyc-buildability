# M0-T036 Control-Plane Integrity Review — frozen head 43848bd

> Preserved VERBATIM by the orchestrator from the cpv-m0t036 agent-return channel
> (2026-08-04, frozen SHA 43848bd). Transport entity-decoding only; no edits.

**Reviewer:** control-plane-verifier (read-only, independent; not the producer, not the orchestrator)
**Frozen SHA verified:** `git rev-parse HEAD` = `43848bd5158cb74184964f93d97ef109fad1ed19`; `git log origin/task/M0-T036-supervisor-bridge` head = 43848bd (local = remote; HEAD not moved).

## VERDICT: PASS — with two required corrections, BLOCKING for acceptance per the project's gate-verdict semantics (.claude/rules/project-control.md)

No falsification, no illegal status transition, no authority breach, and no self-approval was found anywhere in the record. Two mechanism-level discipline deviations must be corrected before acceptance.

## Findings, ranked

**F-CP-1 (VIOLATED — required correction C1): the last two progress_log entries were written to the ledger task file outside `tools/project_control.py`.**
- Record: `project-control/tasks/M0-T036.json`, entries at `2026-08-03T23:11:06` (82%) and `2026-08-04T00:56:13` (85%), commits `bbab407` and `758e09f` (both authored/committed by martin10101; 758e09f touches only the task file).
- Evidence the CLI did not write them: (a) `tools/project_control.py:1007` unconditionally sets the header `t["progress_percent"] = a.percent` on every `progress` call, yet the header remains `"progress_percent": 80` (task file line 63) after the 82% and 85% entries — 758e09f's diff shows the entry appended with no header change; (b) `save()` at `tools/project_control.py:217` serializes with `indent=2`, but bbab407 reformatted the whole file to 1-space indentation (all prior CLI-written commits through fdca6ba are indent-2); (c) `save()` triggers `sync_state()`, yet `project-control/state.json` `updated_at` is frozen at `2026-08-03T20:42:21` — the last genuine CLI write (the second 80% entry). The CLI itself was not modified in this window (`tools/project_control.py` last changed at dbf0a88, pre-task).
- This violates the packet's own `allowed_paths` constraint — "project-control/tasks/M0-T036.json (CLI lifecycle writes only)" — and the standing rule "never by hand-editing control files". Materiality is limited: no status change occurred in either write (status stayed `in_progress`), and the CONTENT is truthful and externally corroborated (PR #152 mergedAt `2026-08-04T00:55:17Z` per the GitHub API exactly matches the entry's claim; PR #150 merge commit `cb9a999` per the GitHub API exactly matches the log). Correction C1: re-record the 82/85 progress through the CLI (restoring header-percent, formatting, and state-sync coherence) and note the deviation in the ledger; do not rewrite history.

**F-CP-2 (VIOLATED — required correction C2): `manifest.json` audit_log is missing entries for D-007 amendments 2–6.**
- Record: `project-control/directives/D-007-codex-claude-supervisor-bridge/manifest.json` — `sources` lists 7 entries (through source-007) and `version: 7`, but `audit_log` has exactly 2 entries (original capture + amendment 1) and manifest `updated_at` is stale at `2026-08-03T04:05:27`. Rule 2 of `.claude/rules/project-control.md` requires "new amendment files plus a manifest.audit_log entry". `validate_directive_compliance.py` does not enforce this (it exits 0), so the gap slipped mechanically. Correction C2: append the missing audit_log entries as a new corrective, append-only manifest write.

**F-CP-3 (minor, informational): the grant-(b) citation table stops at Phase 4.** `project-control/reports/M0-T036-grant-b-review-citations.md` (recorded 2026-08-03T23:15 on owner demand, R551) covers pushes `2f937c1`…`fdca6ba`. Later pushes to the task branch (`eb3430a`, `1d83803`, `8b9a189`, `307b7c6`, `f68f578`, `387378f`, `43848bd`, plus merges) are not in the table. Their passing-review basis exists narratively in the progress_log (the 82% entry cites the 1d83803 fix's 3/3 hash match, scanner PASS, 1042-collected suite) and they are control-plane captures/pilot evidence rather than phase integrations, so I rank this informational, not a grant breach.

**F-CP-4 (cosmetic): residue of F-CP-1 plus stale title.** Header percent 80 vs latest entry 85; the title still carries the creation-time "AWAITING OWNER DISPATCH" suffix despite the dispatched state; the first dispatch entry recorded 5% after claim set 10%. All cosmetic; the first item resolves with C1.

## Integrity properties positively verified

1. **Lifecycle coherence (CONFIRMED):** transitions backlog→ready (G0 PASS, `project-control/gates/M0-T036-G0.json`, reviewer "orchestrator", role "administrative", reviewed_sha 8175a6a) → claimed → in_progress are all legal and CLI-written (indent-2, header-consistent). Every phase progress entry maps one-to-one to real commits on the remote branch: Phase 1 `2f937c1`/`f39192f` (139 tests), Phase 2 `d1f0f74`/`6f4208f` (383), Phase 3 `3a09aff`/`7b179ec` (735), Phase 4 `f49ae74`/`fdca6ba` (1042) — all present in `git log origin/task/M0-T036-supervisor-bridge`.
2. **Directive registry (CONFIRMED):** `python tools/validate_directive_compliance.py --check` exits 0 at 43848bd. Each of source-001…source-007 has exactly ONE commit at its registry path (append-only; the second `--follow` hit on source-001 is rename detection from the owner-tracked v4.3 file at commit 148d13b, not an edit). `locked_requirement_ids` is contiguous and monotone D-007-R001…R576 with no gap or renumbering; manifest/requirements changed only in the corresponding amendment commits (c72b32a, caa15c7, f68f578, 387378f, 43848bd); `verification.json` untouched since creation at df951b0 (consistent with independent verification still ahead).
3. **Pilot evidence (CONFIRMED):** runtime-run1/3/4/5/6 committed at `8b9a189` and `387378f`. I re-verified all five audit chains programmatically using the repo's own `compute_record_digest` from `tools/agent_supervisor/audit_log.py`: sequence contiguity, prev_digest linkage from the genesis digest, per-record digest recomputation, and head-anchor (`audit.jsonl.head.json`) match all PASS — run1: 8 records; run3: 7; run4: 7; run5: 14; run6: 12. The absent runtime-run2 is explained in PILOT-LOG.md (run 2 refused before acting — `IllegalTransitionError` out of PAUSED_RECOVERY, no cycle, so no runtime evidence exists by design).
4. **Authority boundaries (CONFIRMED):** no acceptance (no `accepted_by`/`accepted_at`; status `in_progress`); the only gate record for T036 is G0 by the orchestrator; `project-control/master_plan.json` last changed at `0fcdc68`, which predates the task entirely; no checkpoint is claimed for T036. PRs #150, #151, #152 were each merged by `martin10101` (`is_bot: false`; mergedAt 2026-08-03T23:07:15Z / 2026-08-04T05:03:46Z / 2026-08-04T00:55:17Z) per the GitHub API — the R721 merge queue was honored; main advanced only via owner-merged PRs, never a direct push from this task.
5. **Reviewer independence (CONFIRMED):** producers were unnamed backend-engineer units; every pre-push verification is recorded as an orchestrator producer≠verifier review, and the honest boundary statement in the grant-b citations file states plainly that those were not independent gate reviews. No G2/G3/G4/G5 record exists at 43848bd — this five-review wave genuinely is the first independent gate review, correctly labeled in the record.
6. **Holds and blockers (CONFIRMED):** no blocker file references T036 (grep over `project-control/blockers/` returns nothing); the six open blockers (B-001/004/010/011/012/013) and the expansion-planning hold are unimplicated by this task's scope.
7. **Grant (b) push target (CONFIRMED):** every recorded push landed on `task/M0-T036-supervisor-bridge` only; the citations file exists with its honest boundary statement, and the phase 1–4 pushes each carry a preserved producer≠verifier verification (per-file SHA-256 manifests, orchestrator-re-run suites) in the progress_log and `M0-T036-phase1-port-hashes.json`.

## Unverifiable, stated plainly

- **Grant (a) local executions:** individual `pytest tools/test_agent_supervisor_*.py` runs in the worktree leave no durable per-invocation record; the CI job and progress_log are consistent with the grant and nothing contradicts it, but I cannot enumerate the actual local invocations.
- **Pusher identity:** git records author/committer, not who executed the push; orchestrator-vs-owner push attribution rests on the record's own statements.
- **CI detail beyond merge metadata:** the "31 checks green" claim on PR #152 was not re-enumerated check-by-check; the owner's merge at the exactly-matching recorded timestamp corroborates it indirectly.
- **Phase-port hash manifests:** `M0-T036-phase1-port-hashes.json` records the Phase 1 state; re-hashing the current tree against it is not meaningful (later phases legitimately changed those files), so those verifications stand on the recorded evidence, not re-execution.

Key files: `project-control/tasks/M0-T036.json`, `project-control/directives/D-007-codex-claude-supervisor-bridge/manifest.json`, `project-control/reports/M0-T036-grant-b-review-citations.md`, `project-control/reports/M0-T036-shadow-pilot/PILOT-LOG.md`, `project-control/gates/M0-T036-G0.json`, `tools/project_control.py` (lines 217, 1007), `tools/agent_supervisor/audit_log.py`.
