# M5-T116 — G4 test review (qa-engineer "qa-t116", read-only)

> Transmission history: pinned at 7bed25c0 (material 99c8457f), delivered as 3 SendMessage parts ending with
> END-OF-REPORT; no truncation. Joined verbatim by the orchestrator (transport wrapper tags removed only). Verdict PASS with two ADVISORY
> items (the genuine-bowtie gap; a prepared-vs-raw-ring fixture sensitivity).

---

M5-T116 G4 test review (READ-ONLY) — part 1/3.

PIN: `git rev-parse HEAD` = 7bed25c07aa2 at start (unchanged through the review). Material 99c8457f (cherry-pick of wt-m5t116 74e97bb4), 3 files. Harness re-run from services/api: `ruff check .` → All checks passed; `pytest tests/cad -q` → 458 passed in 5.29s (matches harvest). All in-process mutants below ran via `python -c` in the CONSUMING namespace (es.*); no repo/scratchpad file written — Write and shell file-writes are both guard-blocked for this reviewer identity, so the qa-engineer memory write was SKIPPED per instruction (not delegated).

AS-1 concave GLB caps — PASS.
- Oracle independence: `_shoelace_area` (test:389), `_tri_signed_area`, `_point_in_ring`, `_caps_from_mesh` are all computed IN test_export_service.py; nothing imported from the code under test. Good.
- MANDATORY vertex-0-fan mutant, RE-RUN BY ME against the REAL oracle (not only the producer's dedicated test): I patched es.triangulate_polygon to a fan over the prepared ring and ran the actual assertions from `test_glb_caps_lie_inside_the_footprint_and_sum_to_its_area` (test:437) → RED for L (bottom |area| 3600 vs 2700) and U (4200 vs 3000); convex correctly stays GREEN. Load-bearing confirmed. The producer's own `test_vertex0_fan_mutation_reddens_for_a_concave_footprint` (test:478) also passes.
- WINDING IS ASSERTED, not just area: the oracle asserts `_tri_signed_area(...) < 0` for every bottom-cap triangle and `> 0` for every top (test:460+). For a horizontal cap the 2D signed-area SIGN equals the sign of the face-normal z-component, so this is a genuine outward-winding check (bottom -z / top +z), not merely area magnitude.
- MY OWN MUTANT (reversed cap winding — swap b,c in the triangulator's returned triples) → RED "bottom not -z" for BOTH L and convex. A winding regression is caught.
- Source is correct: export_service.py:403 `triangulate_polygon(footprint, field="building_ring")`; bottom cap `[a, c, b]` at :419 (reversed → -z), top `[n+a, n+b, n+c]` at :421 (kept → +z); the prepared ring is reused for the walls, so caps and walls share one boundary and one winding.

Continues in part 2.

---

M5-T116 G4 — part 2/3.

AS-2 refusals — PASS, with one prominent ADVISORY.
- Reconcile path correct: `_reconcile_triangulation` (export_service.py:319) keeps ONLY the fixed reason code + a server-built detail; `_render_glb` catches MassingModelError at :477. Proven: duplicate-vertex → `self_intersection` with payload keys == {reject_code, detail} and no caller coordinate echoed (`test_self_intersecting_footprint_is_a_reconciled_typed_refusal`, test:523); a budget/coordinate reason is reconciled with the coordinate discarded (test:536); the reconcile-neuter mutant reddens (`test_glb_triangulation_refusal_mutation_reddens`, test:552).
- ADVISORY (NOT blocking): a GENUINE crossed bowtie with DISTINCT vertices is not a refusal. I confirmed in-process: `triangulate_polygon([(0,0),(60,60),(60,0),(0,60)])` returns 2 triangles (no ear-clip stall), and `build_export(format=glb, building_ring=bowtie)` returns an ExportResult with a STRUCTURALLY VALID glTF (body starts b"glTF", 1668 B) — a valid file of a self-overlapping shape. So AS-2's "self-intersecting → typed refusal" holds only for the DUPLICATE-VERTEX subclass; the "never a malformed mesh" clause DOES hold (the glTF is valid). This is honestly disclosed by the producer (OQ-2, DISCOVERY D-a) and is a property of `triangulate_polygon` (massing_triangulation.py ear-clip). `services/api/app/scenario/` is a FORBIDDEN path for this packet (owned by M5-T112), and the route is UNMOUNTED — no live exposure. NOT a producer defect: the packet reconciles every typed refusal the triangulator actually raises, which is its stated objective.
- Required follow-up (orchestrator/PKT-H): the mount MUST add a pre-triangulation simplicity gate (or M5-T112 hardens the triangulator) before untrusted footprint geometry reaches this path — a bowtie currently produces a valid-but-wrong glTF (silent geometry corruption, not a crash). Ensure D-a/D-b land in docs/DISCOVERY_BACKLOG.md at the accept seam.

AS-3 fallback token — PASS. Shared choke point `_allowlist_token` (export_service.py:205) used by BOTH `build_filename_token` and `_sanitize_fallback_token` (:226); re-allowlist at build_export :547. Hostile fallback → _SAFE_FILENAME match, exactly 2 quotes / 2 semicolons, no CR/LF (`test_fallback_token_is_re_allowlisted...`, test:573); identity-mutant leaks a raw quote into the filename (test:588); a fallback that allowlists to nothing → _DEFAULT_TOKEN. Load-bearing.

Continues in part 3 (AS-4, AS-5, verdict).

---

M5-T116 G4 — part 3/3.

AS-4 screens + edges — PASS. Claim-word and length screens are parametrized over es._TEXT_FIELDS (4 params each; test:613 claim, test:623 length). Four-field coverage is GENUINELY pinned, not tautological: `test_text_field_set_narrowing_reddens_the_non_address_fields` (test:632) asserts bbl/generated_at/generator_version are REFUSED on the REAL path, then narrows _TEXT_FIELDS to ("address",) (test:639) and shows they slip through — so dropping any of the four reddens the real-path assertion. Floor edge: `test_floor_cap_edge_2000_accepted_2001_refused` (test:645) — 2000 accepted, 2001 → floor_cap_exceeded; mutant lowers MAX_FLOORS→1999 so the legal 2000 export refuses. The 2001-refuse direction is also pinned by that two-sided boundary test (a raise-to-2001 mutant would fail the "2001 refused" assertion). Load-bearing.

AS-5 scope — PASS. DXF/PDF writers and app/scenario/ untouched (forbidden paths). Owner-sample byte-identity is pinned by test_cad_owner_samples.py (parametrized dxf/pdf/glb, committed bytes == fresh regeneration) — GREEN within the 458; the GLB sample builder calls write_glb directly on a synthetic box, BYPASSING _build_prism_mesh, so this change cannot affect that golden, and DXF/PDF never route through _build_prism_mesh. Convex GLB fixture sha changed (cap INDEX triples only; length unchanged 1680 B) — disclosed and allowed by the packet's "convex output may change only where the triangulation differs". Docstring fix present (export_service.py:37-49: provenance['glb_massing'], explicitly NOT asset.extras). ruff clean; modularity exit 0 per report; only the 3 allowed paths changed.

ADVISORY 2 (test sensitivity, low): all three cap fixtures (L, U, convex) have prepared_ring == localized raw input (I verified n_raw==n_prep and coords equal), so a "positions built from the RAW ring instead of the PREPARED ring" regression would be INVISIBLE to the suite. The code is correct; adding one fixture where preparation actually reorders (a CW-ordered input) or collapses a collinear midpoint would harden the "share the SAME prepared ring" invariant. Not required by the AS list.

SUMMARY: No BLOCKING findings. Every named mutation obligation is consuming-namespace and load-bearing; the two I independently re-ran (vertex-0 fan, reversed winding) redden the REAL oracle. Two advisories: (1) genuine-bowtie AS-2 gap — honestly disclosed, out-of-scope (M5-T112), unmounted, must be gated at PKT-H; (2) prepared-vs-raw fixture sensitivity.

M5-T116 G4 VERDICT: PASS
END-OF-REPORT
