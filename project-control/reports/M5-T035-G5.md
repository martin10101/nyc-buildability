# M5-T035 — G5 security review return (VERBATIM; security-reviewer, read-only, pinned 97fa2eea)

Orchestrator note: returned via the agent-return channel 2026-09-18T06:31:02Z; the transport
truncated the first delivery mid-judgment 4. Part 1 below is the truncated delivery verbatim;
Part 2 is the reviewer's resent remainder, verbatim. VERDICT: PASS (3 LOW / advisory items,
none blocking).

---

## Part 1 (first delivery, verbatim)

I have completed the G5 security review. All documented suites pass (188 tests green here; ruff clean), the fail-safe chain is verified in source, and no secrets are present. Full report below.

---

# G5 Security Review — M5-T035 (DB-015 live wide-street determination provider)

**Verdict: PASS** (3 LOW / advisory items, none blocking)

**Scope / identity:** Pinned material commit `97fa2eea`. The five `allowed_paths` files are byte-identical between `97fa2eea` and live HEAD `3ce7b87d` (`git diff --stat` empty); HEAD is a control-plane-only submit side-effect commit. Review conducted against the pinned content. Documented suites re-run in-sandbox: `188 passed` (DCM connector + buffer engine + provider + rule-eval API); `ruff check` clean on all four source files.

## Judgments on the five required questions

**(1) SSRF / injection — PASS.** No request-controlled value can reach the ArcGIS host/path or alter the fetch target.
- The endpoint accepts only the `bbl` path param, validated by `normalize_bbl` before any I/O (`services/api/app/api/v1/rule_evaluation.py:227-244`); no request body or browser-supplied profile is ever accepted.
- Host/path are hardcoded constants, never interpolated from input (`dcm_street_centerline_arcgis.py:122-123`, `build_segment_query_url` at `:508-516`).
- The live path only ever emits the **envelope** predicate (numeric floats derived from *server-side* lot geometry, not request input) and the **object_id_in** predicate. The envelope is validated finite + magnitude-bounded before I/O (`_validate_envelope` at `:334-390`), formatted `:.4f`, and URL-quoted `safe=''` (`:440-446`). Object IDs are positive-int-validated, count-capped at 50, and `str`-joined (`:474-487`). Neither can inject.
- The borough (allowlist domain) / street_name (safe-text regex `:193` + SQL-literal escape `:308-313`) predicates are not reachable from the request path at all, and are still allowlisted/escaped if they were. Tests confirm rejection before network I/O (`test_unknown_borough_is_disallowed_before_any_network_io`, `test_unsafe_street_name_is_disallowed`).
- The `get_wide_street_determination_provider` dependency-override seam is a FastAPI DI point (`rule_evaluation.py:165-171`), not reachable from request body/query — production input can only drive it through the default gated provider.

**(2) DoS — PASS (with LOW-1, LOW-2 below).** The buffer-engine ceilings are enforced in the correct order — *before* the O(vertices) shapely work: lot vertex bound before `canonical_to_shapely` (`wide_street_buffer_engine.py:663-665`, `_check_lot_vertex_count` `:744-758`), per-path vertex bound and path-count bound before linework build (`:902-904`), extent bound before `.buffer()` (`:906`), and segment count before the loop (`:892`). DCM transport is single-attempt no-retry with a bounded 16 MB body read (`:562-567`) and hard paging ceilings + duplicate/zero-progress pathology guards (`HARD_MAX_PAGES=200`, `:1014-1095`).

**(3) Information disclosure — PASS.** `_fail_safe` logs only `event` + typed error **class** + `correlation_id`, never `str(exc)` (`wide_street_live_provider.py:231-239`); proven by a canary test that embeds `"canary-wide-street-detail"` in the exception message and asserts it never reaches the log line (`test_typed_connector_error_logs_payload_only_never_str_exc`, tests `:370-396`). Endpoint errors return generic bodies and log type+correlation only, no traceback/path/secret (`rule_evaluation.py:189-201`, `:269-273`). Connector error payloads that carry the full ArcGIS URL (keyless service — no secret in URL) are caught inside the provider and converted to `None`; they never reach a response or a log. The disabled-feature 404 is byte-identical to an unmounted path with no correlation id or hint (`:182-186`).

**(4) Fail-safe integrity — PASS (with LOW-3 below).** No path fabricates a determination.
- Every failure / partial / insufficiency returns `None` (→ conservative row / professional review as a normal 200)[TRANSPORT TRUNCATION POINT]

---

## Part 2 (resent remainder, verbatim; delivered 06:32:12Z, clipped again inside LOW-2 — Part 3 below)

Resending from judgment 4 onward.

---

**(4) Fail-safe integrity — PASS (with LOW-3 below).** No path fabricates a determination.
- Every failure / partial / insufficiency returns `None` (→ conservative row / professional review as a normal 200): lot error, lot-not-usable, envelope-unavailable, segment error, transfer-limited page, zero segments in envelope (D-051 honest absence, not a confident not-within claim), geometry error, geometry partial (`services/api/app/spatial/wide_street_live_provider.py:377-435`).
- A wide-disposed segment **never** yields a confident `within_100ft_of_wide_street`: the load-bearing `_UNATTESTED_EC5` gate (`wide_street_live_provider.py:138-147`) forces the B4 engine to `STATUS_PRECONDITIONS_NOT_ATTESTED`, which the wiring maps to `professional_review_required` (`services/api/app/rules/wide_street_wiring.py:462-480`). Proven by `test_wide_segment_yields_professional_review_never_fabricated_wide` (`tests/spatial/test_wide_street_live_provider.py:264-281`).
- `determine_wide_street_far` never raises (catches `WideStreetBufferEngineError` → professional review, `wide_street_wiring.py:403-419`); a hypothetical escaped shapely GEOSException would surface only as the endpoint's generic 500 (`services/api/app/api/v1/rule_evaluation.py:367-372`), never a wrong answer.
- Consistent with the orchestrator's AS-3/DB-014 ruling: the structured wide-street block is deliberately not serialized; only coverage/professional-review/reasons effects surface. Confirmed as intended, not reported as a gap.

**(5) No secrets — PASS.** No API keys/tokens/secrets in code, tests, fixtures, or the deploy checklist (grep across all five files). The ArcGIS service is keyless (`services/api/app/connectors/dcm_street_centerline_arcgis.py:233-234`). `docs/RENDER_INTERNAL_WEB_DEPLOY_CHECKLIST.md` uses placeholders only (`<owner-pastes-private-API-URL>`, `<new-web-service-origin>`) and repeatedly instructs that no real URL/key ever enters a repo file (§0 line 9, §2 line 143, D-043-R002). No real deploy URLs present.

**Not applicable to this diff:** cross-tenant isolation / service-role secrecy / private storage / upload controls / prompt-injection — these files have no auth/tenancy, no Supabase service-role use, no storage, no uploads, and no AI/LLM ingestion. The endpoint's unauthenticated internal/flag-gated posture is honestly disclosed to the owner (deploy checklist §5, and `rule_evaluation.py:1-33`). Least privilege holds: the connector is not a general HTTP client (`DisallowedRequestError` before I/O), outbound only to a hardcoded official host.

## LOW / advisory findings (non-blocking)

**LOW-1 — DB-013 lot-vertex bound not applied before the provider's own first shapely build.** The provider constructs the lot geometry in `_lot_envelope` via `canonical_to_shapely` (`wide_street_live_provider.py:282`) with no vertex-count guard; the `MAX_LOT_VERTICES` bound only runs later inside the engine's `_lot_shapely` (`services/api/app/connectors/wide_street_buffer_engine.py:664`). A pathological lot polygon (millions of vertices) from a malformed/compromised *official* upstream would be built once before any bound. Not request-controlled (only the BBL is; MapPLUTO is a trusted official source), so LOW. *Remediation (optional, defense-in-depth):* apply `_canonical_vertex_count`/`MAX_LOT_VERTICES` in `_attested_lot`/`_lot_envelope` before the first `canonical_to_shapely`.

**LOW-2 — engine `MAX_WIDE_SEGMENTS` (512) ceiling runs after the provider has fetched all wide-segment geometry.** `_attested_wide_segments` fetches geometry for every wide-disposed object id (in 50-id chunks) and builds all `AttestedWideSegment` objects (`wide_street_live_provider.py:325-359`) before `_check_segment_count` refuses (`wide_street_buffer_engine.py:892`). Bounded in practice by the lot-neighborhood envelope (lot bbox + 150 ft) and DCM paging, and only reachable with the flag on, so LOW. *Remediation (optional):* cap/refuse `wide_object_ids` length in[SECOND TRUNCATION POINT]

---

## Part 3 (final tail, verbatim; delivered 06:33:18Z — report COMPLETE)

Rest of LOW-2 remediation: *…the geometry-fetch loop.*

**LOW-3 — attestation-honesty (a G6/legal item, not a security fabrication).** `_policy_decisions` attests `exceptions_checked=True` and `frontage_match_method=coverage_established` to the D-052 width policy (`wide_street_live_provider.py:303-314`), although the provider does not implement the ZR 12-10 named-street / alternate-width exception check (DB-010, out of scope). Per `services/api/app/connectors/dcm_street_width_policy.py:359-363`, attesting `exceptions_checked=True` lets a confident WIDE/NARROW through instead of `UNRESOLVED`, so an all-narrow neighborhood resolves to a confident `not_within_100ft` (conservative standard/**lower** FAR) rather than professional review. This can **never** produce a permissive over-claim: every confident `within_100ft` is still blocked by the `_UNATTESTED_EC5` gate (LOW-3 does not weaken finding 4). The only divergence is a possible under-claim — the documented safe FAR direction (EC-2 under-claim / D-051) — and the field is not serialized (AS-3/DB-014). Surfaced for G6 legal review; not a security defect. Worth a docstring line noting the mild internal inconsistency: the same ZR 12-10 exceptions are attested "checked" at the width-policy layer while left "unattested" at the EC-5 layer.

**Verdict: PASS** — three LOW advisory items, none blocking.
