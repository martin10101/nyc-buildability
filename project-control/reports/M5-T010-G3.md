# M5-T010 — G3 QA / Test-Adequacy Review (verbatim reviewer return)

Reviewer: **qa-engineer** (independent ≠ producer scenario-optimization-engineer). Reviewed content identity `comparison.py` blob `db1043b09fcc20a31942e454d0097c53ea2a3d30` at commit `ea08f504`. Transport entity-decoding applied; content otherwise verbatim.

---

# G3 QA GATE REPORT — M5-T010 (deterministic scenario comparison/delta engine)

**Reviewer:** qa-engineer (independent; NOT the producer)
**Task:** M5-T010 — `services/api/app/scenario/comparison.py` + `services/api/tests/scenario/test_scenario_comparison.py`
**Reviewed identity (frozen):** commit **ea08f504** on `candidate/D-024-mrl-option-b`; reviewed blob `comparison.py` = `db1043b09fcc20a31942e454d0097c53ea2a3d30`

## VERDICT: PASS

Test suite is adequate and mutation-sensitive. Every acceptance scenario AS-1..AS-7 is backed by real, meaningful assertions. No test theater found. No blocking findings.

## SHA / identity verification (note, not a blocker)

The task said "verify `git rev-parse HEAD` == ea08f504 else STOP." My dispatched reviewer worktree is isolated at an **older base** (`d8b3899f`) and does not contain the work — this is a harness artifact, not a defect. I located the frozen SHA via `git worktree list`: both the shared `ctl24` checkout and `wt-m5t010` are at **ea08f504**. I confirmed the reviewed content identity directly: `git hash-object` of the on-disk `ctl24/.../comparison.py` equals `git rev-parse ea08f504:services/api/app/scenario/comparison.py` (`db1043b0…`). So I reviewed the correct frozen content. Commit ea08f504 touches exactly 4 files, all within `allowed_paths`:

```
project-control/reports/M5-T010-producer-report.md | 836 ++
services/api/app/scenario/__init__.py              |  10 +   (additive: +10/-0)
services/api/app/scenario/comparison.py            | 601 ++  (new)
services/api/tests/scenario/test_scenario_comparison.py | 841 ++ (new)
```
No forbidden file (derive/builder/models/constants/contract/ranking/sensitivity/_json_safety, other test files, packages/contracts) was modified. AS-7 additive-`__init__` and read-only-derive scope are satisfied at the commit level.

## Reproduced counts (offline, Python 3.11.9, pytest 8.4.2)

| Command (run from `services/api`) | Result |
|---|---|
| `python -m pytest tests/scenario -q` | **323 passed** in 1.37s, 0 failures/skips |
| `python -m pytest tests/scenario/test_scenario_comparison.py -q` | **71 passed** in 0.15s |
| pre-existing (323 − 71) | **252** — matches AS-6 "≥252 pre-existing", 0 regressions |
| `python tools/modularity_check.py --check` | **failures 0**; 14 warnings, all pre-existing unrelated files. `comparison.py` not flagged |

`documented_test_commands` = `python -m pytest services/api/tests/scenario` → reproduced green.

## AS → test mapping (all backed by real assertions)

| AS | Backing tests | Notable real assertions |
|---|---|---|
| **AS-1** comparison doc: base lineage + per-set derive echo + baseline-relative abs+% deltas + verbatim cap | `test_as1_comparison_document_shape_and_baseline_relative_deltas`, `test_as1_baseline_row_has_all_zero_self_deltas` | 5 lineage fields echoed; `row["derived"] == derive_practical_usable_range(...)` (verbatim echo); cap byte-equal to derive; point `absolute_delta==-3000.0`, `percent_delta≈-20.0`; cap delta `0.0/0.0/moved False`; baseline self-deltas all 0 |
| **AS-2** deterministic BYTE-IDENTICAL across input orders; baseline=smallest content key first | `test_as2_byte_identical_across_input_reorderings`, `test_as2_baseline_is_smallest_content_key_and_ordered_first`, `test_as2_identical_input_is_byte_identical`, `test_as2_duplicate_named_sets_are_interchangeable_and_stable` | `json.dumps(forward)==json.dumps(reversed_)==json.dumps(shuffled)`; baseline forced to "a_raw" from shuffled input; duplicate rows interchangeable |
| **AS-3** never invents / never Verified; not-derivable non-baseline marked not dropped | `test_as3_one_row_per_supplied_set_never_fabricates`, `test_as3_not_derivable_nonbaseline_set_is_marked_not_dropped`, `test_as3_comparison_is_never_verified_and_honestly_labelled`, `test_as3_incoming_verified_coverage_is_capped_to_conditional` (+ rework disclaimer tests) | not-derivable row kept with `comparable False`, `metrics None`, reason set, deltas `None`; no string equals `"verified"`; incoming `verified`→`conditional`; canonical disclaimer stamped |
| **AS-4** typed-error/degenerate; zero/None/absent baseline % not-computable | `test_as4_fewer_than_two_sets_is_invalid` [2], `test_as4_malformed_container_is_invalid` [6], `test_as4_no_cap_document_is_typed_empty_with_reason` [3], `test_as4_not_derivable_baseline_is_invalid`, `test_as4_degenerate_document_is_typed_no_crash`, `test_as4_percent_delta_against_zero_baseline_is_not_computable`, `test_as4_absent_or_nonfinite_baseline_metric_is_not_computable` [nan/inf/None/"x"] | <2 sets → INVALID; malformed container → INVALID; no-cap doc → EMPTY; not-derivable baseline → INVALID; zero baseline → `percent_delta None` + reason, absolute still 5.0; nan/inf/None/str baseline → not-computable |
| **AS-5** strict-JSON-safe via SHARED sanitizer | `test_as5_output_is_strict_json_safe_with_negatives_only_in_deltas`, `test_as5_malformed_assumption_value_is_typed_marker_and_json_safe` [nan/inf/-inf/-0.5/1e400/1e5000], `test_as5_unserializable_object_value_is_typed_and_json_safe`, `test_as5_uses_shared_sanitizer_and_does_not_reimplement_it` | `json.dumps(allow_nan=False)` never raises; non-delta numbers ≥0; negatives only in delta fields (`-7500.0` preserved as float, not marker); `no " at 0x"`; `comparison_module._json_safe is _json_safety._json_safe` (identity) + source bans local sanitizer defs |
| **AS-6** regression + modularity | full suite (323, incl 71 new) + `tools/modularity_check.py --check` | 0 regressions; no other scenario test file edited; modularity 0 failures |
| **AS-7** contract-free + offline; additive `__init__` | `test_as7_module_imports_only_allowed_dependencies`, `test_as7_runs_fully_offline_with_sockets_blocked`, `test_as7_output_is_strict_json_safe_on_every_path` (+ commit diff) | source bans builder/models/contract/ranking/sensitivity/packages.contracts/requests/httpx/urllib/socket/supabase/psycopg/sqlalchemy; requires derive/_json_safety/constants imports; socket-blocked run still produces COMPARED; `__init__` +10/-0 |

No AS is left without a real assertion.

## Mutation-sensitivity findings (probed in-process; no source file touched; blob hash re-verified afterward)

| Mutation | Effect | Test that fails |
|---|---|---|
| A: invert absolute-delta sign (`baseline−set`) | point delta `+3000.0` instead of `−3000.0` | `test_as1` (`absolute_delta == -3000.0`) → FAIL ✔ |
| B: percent uses `set_value` as denominator | `−25.0%` instead of `−20.0%` | `test_as1` (`percent_delta ≈ -20.0`) → FAIL ✔ |
| C: baseline by input order (content key → constant) | determinism broken: reversed input yields different bytes | `test_as2_byte_identical…` → FAIL ✔ |
| D: drop not-derivable set instead of marking | `set_count` 1, `z_bad` absent | `test_as3_not_derivable_nonbaseline…` → FAIL ✔ |
| E: zero-baseline percent returns `inf` (not typed marker) | `json.dumps(allow_nan=False)` raises | `_strict_json_safe` + `test_as4` → FAIL ✔ |

Negative-delta preservation guard is strong: `test_as5_output_is_strict_json_safe_with_negatives_only_in_deltas` asserts `absolute_delta == -7500.0` AND `isinstance(..., float)`.

## Findings

- **BLOCKING:** none.
- **LOW (optional, non-blocking):**
  1. AS-3 verifies never-Verified via property checks (no string == `"verified"`, coverage capped, canonical disclaimer, label) rather than calling the exported `assert_scenario_not_verified` helper. The packet says "assert_scenario_not_verified-**style**"; the manual checks are arguably stronger and appropriate for a contract-free doc — noted only for completeness.
  2. Local pytest is 8.4.2 / Python 3.11.9; the repo's authoritative lock pins pytest 9.0.3 / requires-python ≥3.12. The module uses no 3.12-only syntax and collected/passed cleanly here, but the binding evidence remains CI at 3.12 (consistent with thin-client policy).

## Reviewer conclusion
**VERDICT: PASS.** Test suite is adequate, mutation-sensitive (5/5 mutations caught), every AS-1..AS-7 backed by real assertions, 323/323 green (71 new + 252 pre-existing, 0 regression), modularity clean, only the 4 allowed paths changed. No blocking findings; two non-blocking LOW notes.

**Note for orchestrator:** G3 (QA/test-adequacy) pass only. Per-requirement directive verification is the separate `directive-compliance-verifier` pass. Read-only reviewer — did not run project_control.py/git-write/gh; please record the gate result.
