# M5-T088 — G4 independent test-adequacy review (qa-engineer "qa-mass2", read-only)

> Transmission history: pinned at 6c79cd88 (the two reviewed blobs byte-identical at the material 9f355fe1),
> delivered as three SendMessage parts (1/3..3/3) ending with END-OF-REPORT; no truncation. Joined verbatim
> by the orchestrator (transport wrapper tags removed only). Verdict PASS (advisories: a y-only magnitude
> fixture; scan-charge ordering).

---

M5-T088 G4 test-adequacy review (qa-mass2, independent, read-only). PART 1/3.

SETUP / IDENTITY
- PIN: HEAD at start 6c79cd88. Material 9f355fe1. The two reviewed blobs are BYTE-IDENTICAL across 9f355fe1..HEAD (git ls-tree: massing_model.py=42545728, test=c55e2cd7) and the working tree is clean for both, so I reviewed at HEAD == material.
- cwd services/api; Python 3.11.9, numpy 2.3.3, shapely 2.0.7. My environment is write-blocked, so I could not use the producer's file-based plugin; I used the in-memory equivalent that mutates the SAME consuming namespace: register the mutated whole-module copy in sys.modules["app.scenario.massing_model"] AND set the app.scenario attribute BEFORE the test module imports, one FRESH interpreter (separate process) per mutant. BASELINE reproduced 54 passed and the golden held -> faithful harness. Documented suite `pytest tests/scenario/test_massing_model.py -q` = 54 passed; `ruff check` on both files = All checks passed.

PRODUCER MUTANT TABLE — all 14 reproduced RED, reddened sets match the producer report exactly:
- M1 naive fan -> RED 11: golden, closure[ushape,comb], cap-area[ushape,comb], concave-count[ushape,comb], near-collinear, 2 budget, five-floor golden. Crucially the concave-SPECIFIC assertions fire on U/comb and the L-shape params do NOT (L is star-shaped from v0) -> the non-star fixtures do the work.
- M2 drop CW norm -> RED 3 (cw_lot,cw_footprint,cw_both).
- M3 drop collinear collapse -> RED 1 (exact_collinear_collapses).
- M4 tol 1e-6->50 -> RED 2 (half_foot,thousandth).
- M5 world-coord volume -> RED 1 (non_integer_volume).
- M6 remove magnitude bound -> RED 2 (near_overflow, magnitude_inclusive).
- M7a up-front budget -> RED 1; M7b metering -> RED 2 (M7c = their union, necessarily RED 2).
- M8 remove floor cap -> RED 2; M9 remove output ceiling -> RED 2.
- M10 floor-cap >/>= -> RED 2; M11 output >/>= -> RED 1; M12 magnitude >/>= -> RED 1.
- M13 reverse bottom cap (accepted T082 guard) -> RED 14: closure/volume/goldens still bite.

Continues 2/3 (my own weakenings + F1–F8 close-check).

---

M5-T088 G4 review (qa-mass2). PART 2/3.

MY ADDITIONAL WEAKENINGS (fresh interpreter each; all the ones the task named):
- MA CW-normalize footprint but NOT the lot -> RED 2 (cw_lot, cw_both). The LOT-ring orientation is genuinely pinned, not just the footprint.
- MB exact collinear collapse -> tolerance `<1.0` (removes a real corner) -> RED 2 (near_collinear_is_kept, budget_admits_realistic). The 0.001-ft corner is protected; collapse is exact-only.
- MC containment tolerance -> 1.0 -> RED 2 (half_foot, thousandth). The 0.5-ft case DOES catch a 1.0-ft tolerance -> the boundary is tightly pinned (any tol >= ~0.001 reddens).
- MF floor-cap reason reverted over_cap_floors->over_cap_vertices (message still says "floors") -> RED 2 (names_floors, boundary). The reason ENUM is asserted, not just that it raises — not vacuous.
- MD magnitude bound on X ONLY (drop the `or abs(y)>MAX` clause) -> SURVIVED GREEN 54/54. FINDING (below).
- ME worst-case scan units charged AFTER the scan runs instead of before (same total charge) -> SURVIVED GREEN 54/54. Advisory-low (below).

CLOSE-CHECK of the M5-T082 F1–F8 riders this packet closes — all genuinely closed with mutation-sensitive, non-vacuous tests:
- F1 concave/fan: CLOSED. U (4800) + comb (4820) non-star fixtures on cap-area/closure(_cap_faces_face_outward)/count; M1 reddens U/comb-specific, L-shape can't.
- F2 CW normalization: CLOSED. cw_lot/footprint/both; M2 reddens all 3; MA proves the lot ring is covered too.
- F3 collinear collapse: CLOSED both directions — M3 (exact must collapse) + MB (near-collinear must be kept).
- F4 containment tolerance: CLOSED. 0.5/0.001-ft refused, on-lot-line + full-lot accepted; M4 and MC redden.
- F5 volume conditioning: CLOSED. non-integer fixture, Fraction-EXACT volume/area compare, self-validating `world_misses>0`; M5 reddens.
- F6 misnamed lot-with-hole: repaired — renamed too_few_vertices (invalid_source) + new keyhole_lot_ring (self_intersection, field lot_ring).
- F7 no multi-floor golden: CLOSED. five_floor_stack_golden sha256 pinned; M1/M5/M13 redden it.
- F8 floor cap untested: CLOSED. real 2500-vs-2000 refusal, reason over_cap_floors, message "2500 floors", heavy_work==Counter(); M8/M10/MF redden. DB-054 "names floors" met (reason renamed from over_cap_vertices).

Continues 3/3 (vacuity, budget constants, scope, findings, verdict).

---

M5-T088 G4 review (qa-mass2). PART 3/3 — findings + verdict.

VACUITY (M5-T083 class) — spot-checked, none vacuous: exact_collinear asserts full mesh-equality + vertex-count 8; non_integer_volume uses Fraction-EXACT compares and self-validates the fixture (world_misses>0); floor-cap asserts the reason ENUM + "2500 floors"; both goldens pin an exact sha256. No raises-only or wrong-index assertions.

BUDGET CONSTANTS — fast AND real: patched small budgets drive the metering/up-front logic quickly; the REAL constants are exercised (`4*_min_ear_clip_work(999) <= MAX_TRIANGULATION_WORK`, a real-budget 200-gon build, real 2000-floor output ceiling, real 1e8 magnitude, real 2500 floor cap). Only the real-budget REFUSAL (998-spiral at 2M) is an out-of-suite producer probe (~4s) — reasonable; not a gap.

SCOPE/HYGIENE (independently confirmed): material commit touches exactly the 3 allowed paths, no forbidden path; massing_model has no production importer (grep app/) -> not wired; no permitted/approved/maximum-allowed wording; ruff clean on both files.

FINDINGS
- ADVISORY-1 (strong; recommend before wiring): MD survived. No test violates the coordinate-magnitude bound on the Y-axis alone — near_overflow is big on both (x checked first), inclusive uses x, the footprint probe uses x. So a regression dropping `or abs(y)>MAX_COORD_ABS` passes CI, re-opening the DB-054(h)/G5-F1 overflow via y. Code is CORRECT today; AS-4's required mutation (M6 remove-bound) reddens, so AS-4 is met — this is finer than required. Fix = one symmetric fixture (small x, large y -> coordinate_out_of_range).
- ADVISORY-2 (low): ME survived. The suite doesn't distinguish charging the m-3 scan units before vs after the inner scan (both raise; heavy_work only pins builder-level "before triangulate/prism"). Low: the tested up-front `least>MAX` check (M7a) is the real runaway guard and the budget still bounds total work; charge-after costs at most one extra candidate scan.

BLOCKING: none.

BASIS: all 5 acceptance scenarios' required assertions + named mutations hold; all 14 producer mutants reproduced RED with matching reddened sets; all 8 prior M5-T082 findings closed with non-vacuous mutation-sensitive tests; the 2 survivors are finer than the AS-4-required mutations, act on correct code, break no required behaviour, and are trivially closable — advisories, not blockers.

M5-T088 G4 VERDICT: PASS
END-OF-REPORT
