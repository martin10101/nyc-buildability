# M5-T116 — G3 code review (code-reviewer "cr-t116", read-only)

> Transmission history: pinned at 7bed25c0 (material 99c8457f), delivered as 4 SendMessage parts ending with
> END-OF-REPORT; no truncation. Joined verbatim by the orchestrator (transport wrapper tags removed only). Verdict FAIL on BLOCKING B1:
> a crossed-bowtie footprint with distinct vertices yields a GLB mesh instead of the AS-2 typed refusal -> rework.

---

M5-T116 G3 review (cr-t116): VERDICT FAIL — one BLOCKING AS-2 self-intersection gap; (a)/(c)/(f)/(g)/(h)/scope all correct. [part 1/4]

PIN: HEAD = 7bed25c07aa25fecf5e3ca06908adb3b1c639aa4. Material 99c8457f (cherry-pick of wt 74e97bb4) = exactly the 3 allowed files; `git diff --stat 99c8457f..HEAD` on both code files is EMPTY (working tree == material). Pre-change at claim seam c375457c.

CHECKS (cwd services/api unless noted):
- `python -m ruff check .` -> All checks passed!
- `python -m pytest tests/cad -q` -> 458 passed (matches the harvest).
- `python tools/modularity_check.py --check` (repo root) -> exit 0; export_service.py NOT flagged (572 lines, < 600 warn). The warnings are pre-existing unrelated files only (integration.py, breakeven.py, max_envelope.py, agent_supervisor/*).

The concave-cap core (a) and (c)/(f)/(g)/(h)/scope/zero-dep/unmounted are correct and well-tested. The FAIL is narrow and cheap to resolve. Literal verdict line is at the end of part 4.

---

M5-T116 G3 BLOCKING finding B1: a self-intersecting (bowtie) footprint yields a GLB mesh, not a refusal (AS-2 unmet). [part 2/4]

The packet's binding GLB-CORRECTNESS input and AS-2 require: "a self-intersecting footprint is a typed refusal, never a malformed mesh." UNMET for a crossed bowtie (distinct vertices, crossing edges).

Path trace — no simplicity check anywhere in scope: build_export -> _render_glb (export_service.py:468) -> _coerce_footprint (334-360: pair/numeric/>=3-distinct only) -> _build_prism_mesh (381) -> triangulate_polygon (403). _prepare_ring refuses only duplicate-vertex (massing_triangulation.py:136-139), magnitude, finiteness; _triangulate (216-246) detects a *stall*, but a crossed bowtie ear-clips to completion with no stall.

Reproduced IN-PROCESS (cwd services/api, python -c):
- building_ring=[[0,0],[60,60],[60,0],[0,60]] -> ExportResult, glTF 1668B (NO refusal)
- NYC-scale bowtie -> ExportResult 1680B; [[0,0],[40,0],[0,40],[40,40]] -> 1668B
- triangulate_polygon(small bowtie) -> tris=((3,0,1),(1,2,3)), no MassingModelError
- only the duplicate-vertex ring refuses (self_intersection)

The AS-2 test `test_self_intersecting_footprint_is_a_reconciled_typed_refusal` (test_export_service.py:523-533) exercises ONLY the duplicate-vertex ring, yet its docstring asserts "never a malformed mesh." That claim is false for the bowtie class and left unpinned — a weak/overstated test. The producer discloses the gap honestly (report OQ-2, D-a) — correct — but the acceptance EVIDENCE for AS-2 overstates coverage of a known-failing case.

---

M5-T116 G3 (cont.): why B1 blocks, two cheap resolutions, and (a) concave-caps verified correct. [part 3/4]

WHY BLOCKING (team-lead's explicit AS-2-vs-accepted-limitation question): the triangulator's bowtie-blindness is legitimately M5-T112/PKT-H's to fully close (DB-084 b/d; the mount adds auth+limits+geometry trust). BUT (1) AS-2 / the binding requirement AS WRITTEN is unmet, and (2) the AS-2 test asserts a false "never a malformed mesh" guarantee and does not pin the known-failing case. (2) is THIS packet's evidence defect regardless of where the code fix lands. Route is UNMOUNTED, so no live exposure today — this is a contract/evidence-integrity FAIL, not a security incident.

Two acceptable resolutions (orchestrator/owner picks which; either clears B1):
- MIN: fix the AS-2 test docstring to match reality + add a test that PINS today's bowtie->mesh behavior as a known, routed limitation, so the residual is machine-visible not prose-only.
- CLOSE: add an in-scope service simplicity refusal in _coerce_footprint / _build_prism_mesh (shapely is admitted) so a bowtie becomes a typed self_intersection refusal and AS-2 holds as written.
I do not dictate which.

(a) CONCAVE CAPS — CORRECT (verified): caps = triangulation.triangles; walls use the SAME prepared ring (export_service.py:404-417); bottom reversed [a,c,b] (-z), top [n+a,n+b,n+c] (+z) (418-421); MassingModelError -> _reconcile_triangulation (319-327, reason-code only, no caller text) caught at 475-478; fresh work budget applies (403 omits budget). `test_glb_caps_lie_inside_the_footprint_and_sum_to_its_area[L|U|convex]` (test:434-462) verifies area-sum + containment + winding with in-test oracles; the vertex-0-fan mutation (478-511) reddens. Strong.

---

M5-T116 G3 (cont.): advisories + (c)/(f)/(g)/(h)/scope verified, and the literal verdict line. [part 4/4]

ADVISORY:
- A1 (vertex-cap narrowing 10,000 -> 1,000): acceptable (fail-closed; NYC footprints << 1000; single source of truth). Disclosed in report OQ-1 / D-b. But NOT documented in-code: _FORMAT_RING_CAP['glb']=10_000 (export_service.py:119-123) now over-states the effective cap (1000 via _prepare_ring, massing_triangulation.py:102-105), and no test pins the 1000/1001 over_cap_vertices edge on the footprint path. Consider a one-line code note + an edge test.
- A2: the reconciled detail (export_service.py:326) embeds the reason CODE only (fixed vocab: self_intersection / over_cap_vertices / coordinate_out_of_range / non_finite / invalid_source / triangulation_budget_exceeded) — no caller data. Good.

VERIFIED-CORRECT (non-blocking):
- (c) single choke point _allowlist_token (205-209) used by build_filename_token AND _sanitize_fallback_token (226-235); fallback re-allowlisted before content_disposition (545-547); tests 573-610 + mutation redden. Every token reaching content_disposition is allowlisted.
- (f) _TEXT_FIELDS = 4 fields (254), screened in _screen_caller_text (257-275); parametrized tests 613-642 cover all four + the narrow-to-address mutation reddens.
- (g) MAX_FLOORS=2000 (126); edge test 645-664 pins 2000 accept / 2001 refuse + a 1999 mutation.
- (h) module docstring (37-53) now discloses the dedupe in ExportResult.provenance['glb_massing'] (not asset.extras) + adds the concave-caps bullet.
- SCOPE: DXF/PDF never touch _build_prism_mesh (only _render_glb does) -> byte-identical; the convex-GLB sha change is the disclosed, allowed cap-index change (test 465-475 pins positions unchanged); the owner-sample GLB is a synthetic box not via _build_prism_mesh; zero new deps (imports are in-repo M5-T112 modules); app/main.py untouched (UNMOUNTED); exactly the 3 allowed paths.

Memory note skipped (sandbox blocks writes; per rule, not delegated).

M5-T116 G3 VERDICT: FAIL
END-OF-REPORT
