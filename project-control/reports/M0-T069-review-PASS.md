# M0-T069 independent review — PASS

> Saved VERBATIM by the orchestrator from the independent read-only reviewer's
> agent-return channel (transport entity-decoding only). Reviewer: fresh
> general-purpose agent (producer = orchestrator ≠ reviewer).

**Reviewed HEAD:** `7c3215a07a1f7b9864524d71af98570c965814fe` on branch `task/M0-T069-benchmark-runbook` in worktree `C:/Users/MLFLL/Downloads/nyc-zoning/wt-m0t064` (verified via `git rev-parse --show-toplevel` / `HEAD`). Merge-base with main: `cf65b78` (Unit E merge). Working tree carries uncommitted orchestrator lifecycle records only (state.json, packet status claimed→awaiting_gate, untracked submission record `reports/M0-T069.json`) — post-submit mechanics outside the reviewed HEAD; all producer artifacts are committed and clean.

## Per-item verdicts

**1. SCOPE — PASS.** `git diff --name-only main...HEAD` = exactly 16 files: the 13 allowed_paths + `project-control/tasks/M0-T069.json` + `project-control/state.json` (adds M0-T069 to active_tasks only) + `project-control/gates/M0-T069-G0.json` — all the packet's own control-plane records. Explicit diff over every forbidden path (`tools/agent_supervisor/`, all `repo_index_*`, `context_pack_*`, `subsystem_*`, `memory_*`, `repo_views*`, `code_graph/`, `.github/ services/ apps/ .claude/`) returned **empty** (exit 0, no output). R082 satisfied.

**2. AS-1 corpus + cases (R054/R056) — PASS.** Five shapes are real hermetic git fixtures; determinism proven by independent re-run: my regenerated `corpus_manifest_digest` and `baseline_export_digest` for `single_file_bug` matched the committed values exactly (`af4cd441…`, `f0cefc23…`). The reference-authenticity claim was **empirically refuted-tested and held**: I traced the cold path (it runs the A2 assembly replica `asm.drive`, not literally `codegraph.build_graph`) and then directly compared — `_reference_full(root) == inc.clean_full_build_bytes(root)` (the REAL unmodified generator) → **True**, and `domain_hash(ref) == rib.capture_baseline(root).export_digest` → **True**, equal to the committed per-shape digest. Method statement present and valid (same snapshot, same SHA, only cache state differs). Committed report: **42 rows across 5 shapes** (9+9+8+8+8), case names exactly: cold_build, warm_no_change, one_file_change/non_eligible_change, dependency_change (cross_module), config_change (frontend), rename, delete, corrupt_cache_recovery, interrupted_write_recovery, concurrent_writer. *Observation (O3 below):* R056's "parser-version change" is not a distinct executed case.

**3. AS-2 promotion evidence (R059) — PASS.** I re-ran a full shape (`run_shape('single_file_bug', samples=2)`: 8 rows, all byte-identical, census reconciles) **and** rebuilt the full 5-shape report: its deterministic content (all 42 rows, evidence, corpus digests, thresholds) is **identical to the committed report** modulo timings. All six evidence booleans **mechanically re-derive from the committed rows** (I recomputed them independently — match). No vacuous derivation fires on the actual data: every relevant row explicitly sets its flag (`zero_reparse`, `no_full_rebuild`, `stale_nodes`), file-node ids are bare relpaths (stale-node check is real, verified in `code_graph/generate.py` lines 308/520/738), and `census` fails closed on missing telemetry. The `non_eligible_change` handling is honest: mode=full, reason "full: rebuild" disclosed as A2's documented conservative fallback for a non-source fingerprint move, byte-identity still enforced, and its exclusion from the local-change boolean is justified (not a source change). Two derivation weaknesses noted (O2, O6). Exit-2-on-failure demonstrated empirically (patched failing evidence → `main()` returned 2).

**4. AS-3 honest reporting (R057/R012/R053) — PASS.** `measured_runtime` carries real per-shape samples/median/p95 (reference_full samples=1, warm_no_change samples=3), labeled "measured wall-clock seconds; … never byte-identity content". Deterministic reuse separate. Provider token savings labeled UNMEASURED in JSON, MD, and runbook. Grep of the **entire diff** for savings/token-reduction/percentage claims: every hit is a disclaimer or absence-assertion; the only percentages are threshold definitions (100% byte-identity / recovery validity). No combined savings number exists. One statistic defect (O1: p95 formula).

**5. AS-4 decision discipline (R060) — PASS.** Three thresholds each with recorded rationale; `proposed_before_owner_decision: true`; promotion decision explicitly "PENDING owner/control-plane decision" in report JSON, MD, and runbook. The diff adds new files and control-plane records only — no behavior flag, config, or default path changes anywhere.

**6. AS-5 projection (R061/R062/R063) — PASS.** Regenerated at current state: two runs **byte-identical**; node set = M0-T063…M0-T069 derived from `verification.json` `task_verifications` (authoritative registry, never hardcoded — confirmed in code and by the registry's actual rows, including M0-T069's honest pending row with 0 ids/empty SHA). All six accepted-unit nodes are **field-for-field identical** to the committed snapshot; only M0-T069's own node differs (claimed→awaiting_gate + new reviewed_sha) — exactly the expected control-plane evolution since the recorded generating SHA `b086bba` (= the G0 gate record's reviewed_sha = each node's rollback-point convention). `check` on the committed snapshot: machine-readable `{"stale": true, "recorded": b086bba…, "current": 7c3215a…}`, **exit 3** — the expected stale-by-design disclosure. R063 fields verified per node against the requirement text (task id, exact requirement ids from independent verification, dependency ids, roles, branch honest-null + reviewed SHA, implementation files, evidence location, review-report digest with path+sha256, required gates + per-gate state, accepted/blocked reason, rollback point); R062 mapping present and tested; committed projection MD == `render_md(committed JSON)` byte-for-byte, Mermaid edges generated from the same JSON. Mapping/staleness observations O4, O5.

**7. AS-6 determinism + fail closed — PASS.** Projection byte-identity: verified myself and by test. Unreadable inputs: `ProjectionError` machine-readable codes (`task_packet_unreadable`, `gate_record_unreadable`, `submission_record_unreadable`, `verification_registry_unreadable`, `projection_unreadable`, `git_unavailable`), CLI exit 2 with error JSON, detail proven free of absolute paths (test + code: basename only — see O8). Benchmark exit-2 logic inspected (`0 if all(evidence) else 2`) and demonstrated. Runbook documents **all four R071 elements** as explicit numbered steps (disable-without-delete via `code_graph/generate.py --repo` / `context_pack.py --no-index`; restore full-build; quarantine-not-reinterpret; committed why-evidence + G0 rollback point).

**8. TESTS — PASS.** Exact results at HEAD (Python 3.11.9, ruff 0.13.0 = CI version):
- `python tools/test_context_benchmark.py` → **15 tests, OK, exit 0**
- `python tools/test_status_projection.py` → **8 tests, OK, exit 0**
- `python -m pytest … -q` → **23 passed, exit 0**
- `python tools/modularity_check.py --check` → **exit 0** (pre-existing warnings only)
- `ruff check` on the four new files → **"All checks passed!", exit 0**
- Regressions: `test_repo_views.py` **26 OK**, `test_memory_graph.py` **31 OK**, `test_subsystem_resolver.py` **21 OK**
No vacuous tests: assertions are concrete (exact case-set equality, `files_parsed == 0`, byte equality, specific exit codes 0/3, digest determinism across two independent fixture generations, absence of combined-savings keys).

**9. DOC + HONESTY — PASS.** Runbook commands spot-checked and all real: `status_projection.py generate/check` (0/0 fresh to scratchpad; 3 on the committed stale snapshot), `subsystem_resolver.py vocabulary` (works) and `version` subcommand (exists), `context_pack.py --no-index` (exists, line 103), `code_graph/generate.py --repo` (exists, line 914). Evidence map: **31 rows == the 31 mechanically applicable ids, missing []** (`evaluate_task_refs` → `31 []`; set equality of mapped vs applicable ids independently confirmed — no extras, no gaps; R055 is not in the mechanical applicable set). Committed JSON is valid UTF-8 (em-dash intact; an earlier mojibake was my own console decoding). Producer report claims match my measurements (42/42, 15+8, 23, thresholds, PENDING) with one wording nit (O9).

**10. UNIT BOUNDARY — PASS.** No accepted behavior changed (additive files only; all reused modules byte-untouched). No self-promotion: promotion PENDING everywhere; the projection shows M0-T069 itself unaccepted and the D-013 verification row for M0-T069 empty/pending; no "everything ready"/initiative-complete claim exists in the diff. `tools/agent_supervisor/` untouched.

## Findings

**BLOCKING: none.**

**Observations (non-blocking, recommend follow-up):**
- **O1 — p95 formula understates** (`tools/context_benchmark.py` `_stats`, line 322): `ordered[max(int(n*0.95)-1,0)]` yields the *median* at n=3 (committed report shows p95==median throughout) where nearest-rank p95 would be the max. No promotion threshold consumes latency, so no decision impact, but the label "p95" is inaccurate at small n. Fix: `ordered[ceil(0.95*n)-1]`.
- **O2 — shape-name-based full-rebuild excuse** (line 229-231): `is_invalidator = name in ("frontend_backend_boundary","schema_change") and mode=="full"` would excuse an *undocumented* full rebuild on the frontend shape's plain .ts change. In the committed run that row is mode=incremental (nothing masked), but the derivation should test the `rebuild_reason` prefix (`full: global_invalidator`/`full: schema-node set changed`) instead of the shape name.
- **O3 — R056 "parser-version change" not a distinct executed case.** It is the identical `config_versions` global-invalidator code path (any key mismatch → full rebuild; `repo_fingerprint.default_config_versions` includes the parser/schema/eligibility bumps), proven end-to-end by the executed `config_change` case and by accepted A1/A2 tests (`test_a_config_version_moves_the_fingerprint`, `test_global_invalidator_forces_full_rebuild`). The G0-passed contract enumerated the case list without it; however the evidence-map R056 row says "all executed" without disclosing this subsumption — a wording amendment would be more precise.
- **O4 — R062 mapping gaps** (`status_projection.py` `_STATUS_MAP`): real CLI statuses `self_check` and `canceled` are unmapped (render fail-visibly as `unknown:<status>`, never fabricated); `superseded` is mapped but is not a CLI status; the docstring's "submitted-with-gates-open → gates pending" row has no source state and is not implemented.
- **O5 — `check` compares repo_sha only**, not the declared task/directive index digests, though `stale_when` mentions digest changes (uncommitted control-plane edits move digests without moving HEAD). Packet AS-5 ("exit nonzero when HEAD has moved") is met.
- **O6 — `lock_refused`/`orphan_quarantined`** are recorded per-row and asserted by tests, but not folded into the six R059 evidence booleans (which check byte-identity only for those cases).
- **O7 — Benchmark MD ordering round-trip:** the committed MD was rendered from the in-memory dict; re-rendering from the canonical (key-sorted) JSON reorders two list sections — line multiset identical, content identical. Projection MD round-trips byte-exactly.
- **O8 — ProjectionError detail carries the basename**, not a repo-relative path (the machine-readable code disambiguates the directory; packet AS-6 requires only machine-readable codes).
- **O9 — Producer-report wording:** "cross-checked against the A1 baseline harness digest per shape" — the digest is recorded and (verified by my independent re-run) equal, but no automated assert ties the reference bytes to the baseline digest. Recommend adding a `reference_matches_baseline_digest` boolean to the report.

## Overall verdict

**PASS** — evidence at HEAD 7c3215a is genuine, reproducible, honestly labeled, in scope, and decision-disciplined; all observations are non-blocking quality improvements.
