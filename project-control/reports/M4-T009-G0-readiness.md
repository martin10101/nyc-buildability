# M4-T009 — G0 Definition-of-Ready

**Task:** M4-T009 — R1-R12 residential FAR draft rule family (flat districts)
**Reviewer:** orchestrator
**Date:** 2026-09-11

## VERDICT: PASS

## Readiness checklist

- **Governing sources committed and byte-verified.** `docs/research/zr-snapshots/v1/zr-23-21.snapshot.json` (recaptured in full, commit `17e8eb78`) and `zr-23-22.snapshot.json` (commit `23ca629d`). Both carry `raw_html_sha256`, the machine-readable `last_amended` stamp `2024-12-05T12:00:00Z`, complete tables, and correctly scoped footnotes. Both independently cross-checked against owner-supplied PDFs generated by `zr.planning.nyc.gov` on 2026-09-10 — every row, value, footnote and date matches.
- **Zero invention surface.** The producer transcribes from in-repo JSON. It does not fetch, interpret, or decide.
- **Template exists and is accepted-shape.** `r5_residential_far.rule.json` supplies the structure; the engine already provides `param_select`, `in_set` and `conditional_alternative`. No engine change is in scope.
- **File scope is narrow and uses `**` globs.** A trailing-slash directory never matches under `policy.py path_matches`, which silently defers every worker write; `**` avoids that. `allowed` wins over `forbidden`, so the packet does not pretend to forbid a path a glob admits.
- **Test commands satisfy the closed command profile** (M0-T149): one clean segment each, no `&&`, `cd`, `#`, redirection or quoting.
- **Acceptance scenarios encode the honesty bar, not just the feature.** This is a direct response to the M5-T004 gate wave, where all 8 acceptance scenarios PASSED while four of five gates FAILED — the criteria under-specified the bar. AS-1 forbids tautological tests, AS-2 requires coverage to be *computed* rather than asserted, AS-3 forbids the conditional districts from ever returning the higher value, AS-4 pins footnote scope.

## Known defect this task must not propagate, and must fix

`r5_residential_far.rule.json` carries the ZR 23-21 footnote-1 0.60 per-dwelling-unit cap as an exception on the R5 family. The recapture proved the markup is `0.75<sup>1</sup>` — the footnote binds the first table row only. AS-9 requires moving it. The R5 FAR values themselves are correct and must not change.

## Dependencies

`M4-T001` (rules-engine foundation, `awaiting_gate`). This task consumes the engine and the rule-file shape; it does not depend on M4-T001's acceptance, since it adds data-shaped rules on existing primitives.

## Not in scope

Wide-street determination (geometry — unblocks the higher R6/R7-1/R7-2/R8 values). Height, setback, yards, density. Special districts and overlays. Any engine change. Any new dependency.
