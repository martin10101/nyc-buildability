# M5-T073 — G3 independent code review (code-reviewer, read-only)

> Transmission history: returned across five transmissions — the opening span (truncated
> inside F3 at "yet `:468` only checks the `sha256:` p"), then four continuations each
> requested from the exact cut phrase ("Second, the two sides select \"first polygon\" by
> different route", "P07 0.00276989 / 0.00524803 /", "on this 8-pair sample the DB-045(a)
> dens"). Joined verbatim at each cut point (resend anchor labels are transport
> artifacts). Saved verbatim by the orchestrator (transport entity-decoding only).
> Line-number anchors reference the PRE-correction file state at pinned head 6ff14664;
> the [ORCH-CORRECTED per G3 F1-F5] cluster at 114a6805 moved some spans.

# M5-T073 — G3 independent code review

Pinned head verified: `git rev-parse HEAD` = `6ff1466437c1911fde9ec8d162fff581a00e5c70`. Working tree carries only untracked agent-memory/scratchpad; all task scope is committed. Read-only throughout; no writes anywhere, no ledger/git write verbs.

**Re-ran the documented evidence at the pinned head (cwd `services/api`):** `python -m ruff check .` → `All checks passed!` (exit 0). `python -m pytest tests/connectors/test_bridge_ring_preconditions.py -q` → `14 passed` (exit 0). `python tools/modularity_check.py --check` from repo root → `selected 476 files; failures 0; warnings 22` (harness not among them). Material file set across `28fd92b6 + 06e3e72d + 2c86b35f + a0ed5295` is exactly the three allowed paths — no production edit, no new dependency (AS-4 holds).

## Findings

**F1 — MEDIUM (provenance, AS-1). Half the sample pairs two *different MapPLUTO releases*, and nothing discloses it.**
`services/api/tests/connectors/fixtures/bridge_ring_pairs/pairs_manifest.json` records per side only `endpoint, kind, response_body_sha256, retrieved_at, source_file_sha256, source_fixture` — no `dataset_version`, although both bodies disclose one and the packet's FIXTURE CAPTURE clause asks for "dataset version if disclosed". Measured at the pinned head:

| pairs | display `Version` | authoritative `Version` | capture gap |
|---|---|---|---|
| P01–P04 | `26v2` (2026-09-12) | **`26v1`** (2026-07-20) | ~54 days, cross-release |
| P05–P08 | `26v2` | `26v2` | same second |

Reproduce: `cd services/api && python -c "import sys,json;sys.path.insert(0,'.');import tests.connectors.test_bridge_ring_preconditions as m;[print(p['pair_id'], json.loads(m._read_body(p['display']['source_fixture']))['features'][0]['properties']['Version'], json.loads(m._auth_response_body(p['authoritative']['source_fixture'],p['authoritative'].get('kind','provenance_envelope.response_body_raw')))['features'][0]['attributes']['Version']) for p in m.load_pairs()]"`.
This does not overturn the headline (the four same-release pairs P05–P08 are also `counts_equal=true`, and cross-release equality is arguably a stronger result), but the mount configuration fetches both rings live from one release, so the §5/§7 bound is currently stated over a sample it does not describe. `measure_pair` (test file `:303-322`) also drops `dataset_version` from the provenance echo even though the display `ParcelRing` carries it (`:193-198`). **Required correction:** record `dataset_version` per side in the manifest and add one disclosure sentence to §5/§7 ("P01–P04 pair a 26v2 display ring against a 26v1 authoritative ring; P05–P08 are 26v2/26v2").

**F2 — MEDIUM (test adequacy, AS-3). No test pins any real pair's verdict or refusal class, so the headline table is unguarded.**
`test_real_pair_measured_offline_with_roundtrip_provenance` (`:470-475`) asserts only `verdict in ("pass","refuse")` and non-null `refusal_class` on refusal; `test_verdict_table_builds_over_every_real_pair` (`:478-485`) is self-referential (`len(table) == len(_PAIRS)`). In-process mutant (no file writes): wrapping `m.classify_ring_pair` to silently drop the `ambiguous_correspondence` gate flips the sample from **3/8 pass to 6/8 pass** and **zero tests fail** — i.e. exactly the "weakening the bridge's bounds" the report promises not to do would land green. To be fair to the producer, two mutation classes *are* caught: `test_bridge_constants_are_the_accepted_bounds` catches constant drift, and the AS-2 pass test catches an inverted separation comparison (both verified in process). **Recommended:** carry an expected `verdict`/`refusal_class` per pair in the manifest and assert it, or pin the aggregate (3 pass, 2 `too_many_control_points`, 3 `ambiguous_correspondence`).

**F3 — LOW (AS-1). The display digest is computed and echoed but never asserted.** `measure_pair` records `computed_sha256` alongside the manifest's `source_file_sha256` (`:312-313`), yet `:468` only checks the `sha256:` prefix; only the authoritative side gets recorded==computed (`:466-467`). I verified independently that all 8 display digests and all 8 authoritative digests match on this Windows checkout, so the single-line-JSON newline-noop argument in §4 holds empirically, and the fix is a free one-line assertion. Reproduce: `cd services/api && python -c "import sys;sys.path.insert(0,'.');import tests.connectors.test_bridge_ring_preconditions as m;[print(p['pair_id'], m._sha256(m._read_body(p['display']['source_fixture']))==p['display']['source_file_sha256']) for p in m.load_pairs()]"` → `True` x8.

**F4 — LOW-MEDIUM (stale coverage floor). The AS-1 sample floor is not machine-enforced, and two artifacts still describe the pre-harvest state.** `test_real_pairs_manifest_is_present_and_covers_multiple_classes` asserts `>=4` pairs / `>=2` boroughs / `>=3` classes (`:453-455`) against a packet floor of `>=8` pairs / `>=3` boroughs; a pair silently dropped from the manifest leaves the suite green while the report's "8 pairs / 5 boroughs" claim quietly becomes false. The module docstring (`:39-46`) still says the accepted packs "overlap on 4 real single-lot pairs across 2 boroughs" with the remainder "ROUTED TO HARVEST", and `pairs_manifest.json → sample_bound` leads with the same 4-pair/2-borough sentence before its appended "EXTENDED 2026-09-23: +4 net-new… 5 boroughs" clause. Both were true when written and were not reconciled at the finishing pass, so a future reader of the harness alone concludes the sample is 4 pairs / 2 boroughs.

**F5 — LOW-MEDIUM (report hygiene). The report's header contradicts its own conclusion.** Line 9 still reads `## STATUS — BLOCKED`, §5's preamble says the measured columns "are shown as `⧗ pending-harvest` — **not guessed**" directly above a fully filled numeric table, and §9 (`:253-256`) says "status is BLOCKED pending the routed supervisor/CI capture… Do not accept, merge, or mount." All three are superseded by the appended `[ORCH-HARVEST]` section (`:260-292`), which records the capture as executed. Anyone reading top-down gets the wrong task state. Recommended fix: a single `[ORCH-CORRECTED]` pointer line under the STATUS heading directing to the `[ORCH-HARVEST]` section, leaving the producer's original text intact per the report-preservation convention.

**F6 — LOW (harness fidelity, verified sound in fact).** The authoritative reconstruction (`test_bridge_ring_preconditions.py:209-215`) substitutes a bare `len(features) != 1` check for production's envelope validation: `fetch_lot_geometry` additionally runs `_validate_query_envelope` and raises `ResultMismatchError` when the returned feature's `BBL` attribute differs from the requested lot (`app/connectors/mappluto_geometry_arcgis.py:1790`). The display side does keep that defense, because `build_lot_outline` → `parse_lot_outline` raises `LotOutlineResultMismatchError` on the same condition (`app/connectors/mappluto_lot_outline.py:451`). I confirmed all 8 authoritative bodies carry the requested BBL and all 8 spatial references are `wkid 102718 / latestWkid 2263`, so pair identity is correct today; it simply is not defended by the harness on the authoritative side, and a future mis-paired fixture would be measured silently.

**F7 — INFORMATIONAL (carry to the mount packet).** Two notes. First, `counts_equal` for P02 (320 vertices) and P04 (3093) is the harness's own `len()` comparison — the mounted bridge never reaches its count check for those lots, because `fit_correspondence` raises `too_many_control_points` (`outline_bridge.py:456`) before the `vertex_count_mismatch` check (`:462`). Measuring the counts independently is the right way to answer DB-045(a), but the "counts_equal true on all 8" headline is an independent measurement for those two pairs, not a path production would traverse. Second, the two sides select "first polygon" by different routes: the display side reaches its exterior ring through `_exterior_ring_from_geojson`, which takes `coords[0]` for a Polygon and `coords[0][0]` for a MultiPolygon, while the authoritative side takes `assessment.canonical_geometry[0][0]`. The two selections coincide across this sample — P04's display members are `[3094], [38]` against authoritative `[[3093], [37]]` (the off-by-one is the closing vertex `_open_ring` drops), and P02's display rings are `321 / 107 / 38` against authoritative `320 / 106 / 37`, so hole rings are discarded identically on both sides. Neither connector contract guarantees member ordering, though, so a future multipolygon lot could in principle pair non-corresponding members while still reporting equal vertex counts. It costs the current verdict nothing (P04 refuses on capacity before any fit runs), but the mount packet should treat first-member selection as an assumption to verify rather than an invariant.

## Assessment per duty

1. **Harness fidelity — PASS.** `classify_ring_pair` (`services/api/tests/connectors/test_bridge_ring_preconditions.py:134-159`) reproduces the bridge consumer (`app/api/v1/outline_bridge.py:729-770`) symbol-for-symbol: `fit_correspondence` → typed `_CorrespondenceError.reason` → `fit.rms_residual > BRIDGE_MAX_RMS_RESIDUAL_FT` → `separation < BRIDGE_AMBIGUITY_SEPARATION_FT` → pass, with the `None`-for-`inf` handling equivalent to the consumer's `math.isinf` guard. Ring reconstruction matches the production adapters: display through the real `build_lot_outline` with an injected transport (same parse, same strict contract validation, same `single_lot` gate, same `_exterior_ring_from_geojson` → `_open_ring`), authoritative through the same `analyze_lot_geometry(geometry, crs=dict(CRS_STAMP))` call `fetch_lot_geometry` makes internally, then `canonical_geometry[0][0]` → `_open_ring`. The only divergences are F6 (envelope validation) plus the two production gates that need caller input and cannot apply to a pair-only measurement — the `crs` equality check (a no-op, since the harness stamps the CRS itself) and `_outside_neighborhood` (no drawn vertices exist here). Constants and the fit are imported from the bridge, never re-implemented, so a bridge drift fails this harness rather than silently diverging from it.
2. **Provenance (AS-1) — PASS with F1 and F3.** Byte-basis selection in `_auth_response_body` (`:271-279`) is correct for both documented kinds — `provenance_envelope.response_body_raw` unwrapped for P01–P04, `raw_esri_body` returned verbatim for P05–P08 — and recorded==computed holds for all 8 authoritative bodies on a Windows checkout, which is the real test of the single-line/newline-noop argument in §4. The gaps are the undisclosed dataset-release split (F1) and the unasserted display digest (F3).
3. **AS-2 synthetic pins — PASS.** The passing pair asserts `rms < 1e-3`, separation `>= 2.0`, `counts_equal`, and vertex count 5; the densified pair asserts `vertex_count_mismatch` with 5 vs 6 and `counts_equal is False`; the perturbed same-count pair asserts `residual_too_high` with `rms > BRIDGE_MAX_RMS_RESIDUAL_FT`. Numerically pinned as the scenario requires. Neither observed refusal class has a synthetic of its own, but `ambiguous_correspondence` semantics are proven upstream in the accepted bridge suite (`tests/api/test_outline_bridge.py:318-325`: symmetric square parcel → 422 / `ambiguous_correspondence`), which is why F2 is a strong recommendation rather than a block.
4. **§5 transcription — PASS, spot-checked against a fresh run at the pinned head.** Every measured cell matches at 4-decimal display: P01 0.00239954 / 0.00285625 / 49.8473 / 49.8449; P03 0.00177143 / 0.00179818 / 0.87824 / 0.876469; P05 0.00131307 / 0.00131342 / 0.00133246 / **1.9384e-05** (the report's `0.0000194` is correct at full leading precision); P06 0.00217699 / 0.00236745 / 0.198672 / 0.196495; P07 0.00276989 / 0.00524803 / 7.31301 / 7.31025; P08 0.00393256 / 0.0064008 / 35.509 / 35.505; P02 and P04 correctly `—` (they refuse before the fit runs, so no residual exists). Vertex counts 6 / 320 / 4 / 3093 / 4 / 4 / 6 / 30 and `counts_equal=true` reproduce exactly, as do the verdicts: pass = P01, P07, P08; `too_many_control_points` = P02, P04; `ambiguous_correspondence` = P03, P05, P06. The report's stated coverage also checks out independently: 5 distinct borough codes {1,2,3,4,5} and 7 distinct geometry classes across 8 pairs. Every value in the §5 table is a faithful 4-decimal rendering of the run output, with P05's separation correctly shown at full leading precision (1.9384e-05 → 0.0000194).
5. **Refusals-as-findings — PASS.** All 8 pairs are individually parametrized (14 tests = 1 constants + 3 synthetic + 1 manifest + 8 per-pair + 1 table). No pair is skipped, filtered, or downgraded, and a precondition violation never fails a test — the assertion is `verdict in ("pass","refuse")` with a non-null `refusal_class` on refusal.
6. **Evidence-status convention — PASS with F5.** The [OBSERVED]/[PREDICTED]/[PENDING-HARVEST] separation in the producer-authored body is unusually disciplined: it refuses to claim runtime results it did not run, names the residual risk (a trailing-newline digest mismatch) that only the routed run could settle, and leaves every numeric cell explicitly pending rather than guessed. Every prediction it made was confirmed by my independent re-run. The only defect is that the appended harvest never reconciled the STATUS header and §9.
7. **D-084-R002 fit — PASS.** R002 requires lane 2 to run on a contracted, claimed, in-regime ledger packet under the normal gated process and the D-070 finished-seam standard, naming DB-045(a) real-parcel bridge-mount validation as a candidate with the choice left to the orchestrator. The packet is bound (`applicability.task_ids` includes `M5-T073`), the evidence map maps R002 to this delivery, and the artifact answers the named question. Launch-journal and seam evidence belong to the DCV's surface, not G3's.

## Corrections

**Required before acceptance**
- **F1** — record `dataset_version` per side in `services/api/tests/connectors/fixtures/bridge_ring_pairs/pairs_manifest.json`, and add one disclosure sentence to §5/§7 of the producer report: P01–P04 pair a **26v2** display ring against a **26v1** authoritative ring (captures ~54 days apart), while P05–P08 are 26v2/26v2. The headline survives either way — the four same-release pairs are also all `counts_equal=true` — but a provenance-bounded verdict must state the sample it actually describes, and the packet's AS-1 asked for dataset version where disclosed.

**Recommended before the mount packet consumes this evidence**
- **F2** — pin the real-pair verdicts (per-pair expected `verdict`/`refusal_class` in the manifest, or the aggregate 3 pass / 2 `too_many_control_points` / 3 `ambiguous_correspondence`). Today a dropped ambiguity gate flips the sample from 3/8 to 6/8 pass with zero test failures.
- **F4** — raise `test_real_pairs_manifest_is_present_and_covers_multiple_classes` to the packet floor (`>=8` pairs, `>=3` boroughs) and refresh the now-stale module docstring (`:39-46`) and manifest `sample_bound`, which still lead with the pre-harvest 4-pair / 2-borough description.
- **F5** — add one `[ORCH-CORRECTED]` pointer line under the report's `STATUS — BLOCKED` heading directing readers to the `[ORCH-HARVEST]` section, leaving the producer's original text intact per the report-preservation convention.
- **F3** — assert display `computed_sha256 == source_file_sha256` (all 8 already match; it is a free one-line guard).

**Bottom line.** The headline is real and I reproduced it independently at the pinned head: on this 8-pair sample the DB-045(a) densification risk does not materialize — `counts_equal` is true on all 8 pairs, and where the fit runs the RMS residuals are 0.0013–0.0039 ft against a 2.0-ft bound. 3/8 pass, and the two refusal classes that do appear — a control-point capacity cap on the 320- and 3093-vertex lots, and near-symmetric-rectangle ambiguity on the common small-residential class — are correctly routed to the mount packet rather than resolved by weakening a bound here. Scoped ruff clean, 14 passed, modularity 476 files / 0 failures, scope confined to the three allowed paths. One blocking correction (F1, the undisclosed 26v2-vs-26v1 release split on P01–P04); F2/F4/F5/F3 recommended before the mount packet consumes this evidence.

G3 VERDICT: PASS

END-OF-REPORT
