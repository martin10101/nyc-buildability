# M5-T035 — G4 QA review return (VERBATIM; qa-engineer, read-only, pinned 97fa2eea)

Orchestrator note: returned via the agent-return channel 2026-09-18T06:30:00Z; the transport
truncated the first delivery mid-item 5. Part 1 below is the truncated delivery verbatim;
Part 2 is the reviewer's resent remainder (requested and delivered same session), verbatim.
VERDICT: PASS.

---

## Part 1 (first delivery, verbatim)

I have completed the review. All source-to-test bindings check out: the lot-vertex ceiling at engine line 664 runs immediately before `canonical_to_shapely` at 665, the segment path-count check at 904 precedes `_segment_linework` at 905, and envelope validation at `build_segment_query_url` (line 991) runs before `fetch_layer_metadata` (line 1002). Here is my gate report.

---

## G4 QA Review — M5-T035 (DB-015 live wide-street determination provider)

### VERDICT: PASS

Reviewer: qa-engineer (read-only). No repository file was created, edited, or deleted. Material commit reviewed: `97fa2eea`; live HEAD `3ce7b87d`.

### Identity check
`git diff --stat 97fa2eea..3ce7b87d` restricted to `services/api`, `docs/RENDER_INTERNAL_WEB_DEPLOY_CHECKLIST.md`, and `project-control/reports/M5-T035-producer-report.md` returns **empty** — every allowed_paths file the tests exercise is byte-stable between the pinned material commit and HEAD. The reviewed test/source content is the material content.

### Evidence I ran (this checkout, HEAD 3ce7b87d, cwd repo-root, exactly the documented commands)
| Command | Observed |
|---|---|
| `python -m ruff check services/api` | All checks passed! |
| `python -m pytest services/api/tests/spatial -q` | 76 passed |
| `python -m pytest .../test_dcm_street_centerline_arcgis.py -q` | 69 passed |
| `python -m pytest .../test_wide_street_buffer_engine.py -q` | 54 passed |
| `python -m pytest services/api/tests/api/test_rule_evaluation_api.py -q` | 38 passed |
| `python tools/modularity_check.py --check` | selected 441 files; failures 0; warnings 19 |

These reproduce the producer's §2 outcomes. (Minor: modularity `selected` is 441 here vs 440 at harvest — a one-file delta from control-plane commits after the pin; `failures 0` and the same two task-module warnings are unchanged, so it is immaterial.)

### Required-coverage verification (each present and meaningful, not vacuous)

1. **Provider typed-failure classes → None** — `test_lot_connector_error_returns_none`, `test_unexpected_lot_error_returns_none`, `test_segment_connector_error_returns_none`, `test_unexpected_segment_error_returns_none`, `test_wide_geometry_connector_error_returns_none`, `test_partial_segment_page_returns_none`, `test_partial_wide_geometry_page_returns_none`, `test_multiple_feature_lot_returns_none`. Doubles inject only the network fetch; the classifier→D-052 policy→wiring→B4 engine chain runs for real via the accepted parse surfaces (`parse_segment_page`/`parse_segment_geometry_page`/`analyze_lot_geometry`), so these exercise the code under test, not a re-implementation.

2. **Zero-segment honesty** — `test_zero_segments_in_envelope_returns_none_never_confident_not_within` asserts `None`, explicitly not a not-within determination (D-051). Source line 408-413 confirms the honest-absence branch.

3. **Flag-token matrix + zero connector calls when off** — `test_enabled_only_for_explicit_true_token`/`test_disabled_for_non_true_token`/`test_disabled_when_absent` cover the token set; `test_flag_off_returns_none_with_zero_connector_calls` and endpoint `test_m5t035_flag_off_default_wide_provider_zero_calls_byte_identical` both assert `calls == {lot:0,segments:0,geometries:0}`. The load-bearing assertion is the recorded-call count, not the `is None` (a flag regression would still return None via the fail-safe except but would record a call) — correctly present.

4. **Payload-only logging** — `test_typed_connector_error_logs_payload_only_never_str_exc` asserts the canary `str(exc)` text is absent and only `event`/`error_type=<class>`/`correlation_id` appear; `test_zero_segment_fail_safe_logs_payload_only_with_correlation_id` covers the no-exception branch (`error_type=none`). The `_fail_safe` template (source 231-239) is a fixed format string that structurally cannot carry a URL/secret.

5. **Envelope validation before any I/O, incl. non-finite/absurd/huge-int** — `test_envelope_rejects_nonfinite_coordinates`, `_absurd_magnitude` (both float overfl[TRANSPORT TRUNCATION POINT]

---

## Part 2 (resent remainder, verbatim; delivered 06:31:32Z, clipped again at its very end — Part 3 below)

Resending from required-coverage item 5 through the end.

---

5. **Envelope validation before any I/O, incl. non-finite/absurd/huge-int** — `test_envelope_rejects_nonfinite_coordinates`, `_absurd_magnitude` (both float overflow and `10**400`), `_inverted_bounds` (x and y), `_boolean_component`, `_must_be_a_four_item_numeric_sequence`, plus five huge-int `_safe_repr` robustness tests (`test_envelope_wrong_length_with_huge_int_is_disallowed_not_valueerror`, `_non_numeric_component_with_huge_int_is_disallowed`, `test_predicate_conflict_with_huge_int_envelope_is_disallowed`, `_huge_int_object_id_is_disallowed`, `test_fetch_street_segments_refuses_huge_int_envelope_before_any_network_io`) and `test_fetch_street_segments_refuses_bad_envelope_before_any_network_io` — the last two assert zero fetch calls. Source confirms `build_segment_query_url`→`_validate_envelope` runs at line 991 before `fetch_layer_metadata` at 1002; `_validate_envelope` (334-390) rejects non-4-item, non-numeric, bool, overflow, non-finite, over-magnitude, and inverted-bounds cases with typed `DisallowedRequestError` and bounded `_safe_repr` detail. This fully closes RUN-40 carryover item (2).

6. **Predicate exclusivity** — `test_envelope_is_mutually_exclusive_with_attribute_predicates` (envelope×borough / object_id / object_id_in) + `test_multiple_predicate_styles_are_disallowed` + `test_no_predicate_is_disallowed`; source XOR (`sum(predicate_styles) != 1`) at lines 413-431.

7. **Metadata freshness / schema-drift / CRS gates preserved on the envelope path** — `test_envelope_fetch_runs_metadata_first_and_returns_segments` (asserts `metadata_request_url` populated and the first page URL carries the envelope spatial predicate), `_preserves_the_wrong_crs_gate`, `_preserves_the_paging_pathology_guard`, plus the base `test_fetch_layer_metadata_validates_crs_and_pins_freshness`, `test_unrecognized_feat_type_is_schema_drift_never_wide`, `test_wrong_geometry_type_is_schema_drift`.

8. **Paging pathologies** — `test_duplicate_page_is_a_typed_paging_pathology`, `test_repeated_object_ids_across_pages_is_a_typed_paging_pathology`, `test_page_budget_exhaustion_is_a_typed_pathology`, and the duplicate-page guard re-proven through the envelope predicate.

9. **DB-013 ceilings, typed fail-closed at boundary values** — `test_lot_vertex_bound_beyond/at_bound`, `test_segment_path_count_bound_beyond/at_bound`, `test_db013_ceilings_are_above_real_world_sizes`. The monkeypatch technique is valid: the checks read module-level `MAX_LOT_VERTICES`/`MAX_PATHS_PER_SEGMENT` at call time; strict `>` gives an exact at-allowed / bound-1-fails boundary; the at-bound test runs the full `compute_wide_street_buffer_intersection` to `STATUS_COMPUTED` (non-vacuous). Wiring confirmed: lot check at engine:664 precedes `canonical_to_shapely` at :665; path check at :904 precedes `_segment_linework` at :905. No engine split, no wiring added (modularity failures 0).

10. **Endpoint wide-row decision effect + professional-review escalation** — `test_m5t035_within_wide_determination_fires_wide_row_via_endpoint` and `test_m5t035_professional_review_determination_escalates_coverage_via_endpoint` drive a real typed `WideStreetDetermination` through the route. Strong, non-vacuous assertions: coverage (`conditional` / `professional_review_required`), the rule's own DSL trace output held at the conservative `2.2` while the reason names governing FAR `3.0` (proving the server-side fold does not pollute the rule computation), and an **exact reconstructed** reason string (not a substring probe). This closes the M5-T034 disclosed gap.

11. **Unchanged flag-off endpoint behavior** — `json.dumps` byte-parity + zero calls in `test_m5t035_flag_off_default_wide_provider_zero_calls_byte_identical`, and the entire pre-existing suite (AS-3..AS-14, M2-T020, M5-T033) passes unmodified within the 38.

### AS-3 / DB-014 ruling — honored
The two endpoint tests assert `wide_street_far_row`, `wide_street_governing_far`, and `wide[SECOND TRUNCATION POINT]

---

## Part 3 (final tail, verbatim; delivered 06:32:36Z — report COMPLETE)

### AS-3 / DB-014 ruling — honored
The two endpoint tests assert `wide_street_far_row`, `wide_street_governing_far`, and `wide_street_determination` are **absent** from the document. Per the binding orchestrator ruling this is correct behavior: `rule_evaluation @ 1.0.0` is strict-closed, so the structured block is deliberately not serialized and the decision effect is proven via existing fields (`coverage_status` conditional / `professional_review_required`; `reasons[]` naming governing FAR 3.0 + DRAFT marker; DSL trace held conservatively at 2.2). The tests pin the absence rather than papering over it. I verified the tests assert exactly this and did not re-adjudicate the ruling.

### Non-blocking observations (branch-coverage; no concrete risk; not defects)
- `segment_geometry_unexpected_error` branch (non-DCM exception during wide-geometry fetch, provider source 430-432) has no dedicated test; only the DCM-typed geometry error is exercised. Structurally identical to the tested `segment_unexpected_error` branch. Low risk.
- `_attested_lot` sub-branches for bad geometry status (not VALID/REPAIRED) and `canonical_geometry is None` are reached only indirectly; only outcome≠SINGLE / review_required is directly tested. All converge to the tested `lot_not_usable → None`. Low risk.
- Endpoint "returning a determination" cases use a dependency override, not the real live default seam — necessarily, since live composition never emits confident WITHIN until DB-010. Disclosed in the test docstring and report §4/§6; the real default seam is separately proven for the flag-off/None path. Acceptable.
- `ENVELOPE_MARGIN_FT = 50.0` superset adequacy is correctly routed UNVERIFIED, not tested — a geometric-correctness question, not a test-adequacy gap for this gate.

### Test isolation
Endpoint fixtures clear `app.dependency_overrides` on teardown; env/`_ACTIVE_FETCHERS` swaps use `monkeypatch` (auto-reverted); non-monkeypatched provider tests pass `fetchers=` explicitly. No shared mutable state or order dependence; full suites pass in one run.

### Verdict: PASS
Coverage for the packet's acceptance scenarios (AS-1, AS-2, AS-3-effect, AS-4, AS-5, AS-6, AS-8) is present, meaningful, and non-vacuous; assertions bind to verified source behavior; the AS-3/DB-014 ruling is respected. AS-7's CI-green-on-pushed-head half remains the orchestrator's to capture and is not part of this verdict. Reviewer role was read-only; no repository file was created, edited, or deleted.
