# M5-T066 G4 gate — independent QA/test-adequacy review (verbatim reviewer return)

Reviewer: qa-engineer (claude-opus-4-8 per D-064), dispatched by the orchestrator at the
seq-124 T066 wave, pinned to the frozen submit head 84132df5 (material abfd182b). Recorded
by the orchestrator; the reviewer is read-only and records no gate itself. Arrived complete
with the END marker. Verbatim return follows.

---

VERDICT: PASS

G4 QA / test-adequacy review — M5-T066 (D-082 map-CLICK interaction slice + T065-wave fold-ins). Frozen identity: submit head 84132df5; material abfd182b. Save verbatim as project-control/reports/M5-T066-G4.md.

## Frozen-identity verification
- Material abfd182b = exactly 11 files, ALL within allowed_paths (no forbidden path, no manifest/lockfile). AddressConfirmCard.tsx, PropertyOverview.tsx, outline-bridge-api.ts, helpers.ts — all untouched (forbidden, confirmed absent from the diff).
- Task-source byte-stable: `git diff abfd182b..HEAD` over all 10 source/test files = EMPTY. The 3 post-material commits (2578329c, 2fbaaf53, 84132df5) touch only state/task/evidence-map. Working tree carries only agent-memory + scratchpad (disjoint). Verdict binds to the frozen path-scoped content.
- CI head 2578329c: `abfd182b IS ancestor of 2578329c`; source identical abfd182b..HEAD, so CI SUCCESS validated exactly the frozen source. Web + pointer + mutation proofs ride that CI run (thin client — I judged test CONTENT by reading, execution by that run; did not run npm/node).
- Local documented check reproduced: `python tools/modularity_check.py --check` → exit 0 (LotOutlineMap.tsx not among the 22 advisory warns).

## AS → concrete spec mapping (with mutation judgment)

| AS | Where proven | Mutation teeth |
|---|---|---|
| AS-1 place + sync | proposal-outline-draw.test.tsx "AS-1: map clicks and keyboard entry append into the SAME table" (pointFeatureCount 0→3; clicked rows carry clicked coords, keyboard row typed); lot-outline-map.test.tsx "AS-1 registers a click listener…reports the 4326 position" + "AS-1 sync…setData…never a second source" (addLayer stays 4 after rerender); e2e "AS-1 pointer: click the lot map to place…" (real MapLibre clicks at 3 canvas positions → bridge→adopt→check) | STRONG. Sync test would redden if map/keyboard used separate state; setData test reddens if overlay re-adds a source; e2e reddens if clicks don't populate the shared table. |
| AS-2 select/move/delete + focus | proposal-outline-draw.test.tsx "AS-2 …selected then MOVED by a map click" (count stays 2, coords change, deselect→next click PLACES) + "deleting a SELECTED point…clears selection + keeps focus (DB-043a)"; proposal-outline-map.test.tsx place-vs-move-vs-select routing; LotOutlineMap "AS-2 …routes to onDrawnVertexClick, never a place" + "AS-2 early-click guard" | STRONG on wiring + keyboard. Early-click guard is explicitly mutation-noted (reverting the getLayer guard reddens — unguarded query returns the stub hit and mis-routes to select). Move-vs-place hinges on selection state, asserted both directions. |
| AS-3 display byte-equivalence | lot-outline-map.test.tsx "AS-3 byte-equivalence: no interaction props → NO click listener, only 2 lot-outline layers" (addLayer===2, clickListeners===0); ALL accepted display tests (S1/S2/honest-states/WebGL-unavailable/error-routing/M5-T029/runOnStyleReady) remain unchanged | STRONG. Reddens if interaction gating (`interactive = onOutlineMapClick != null`) leaks a listener or overlay layers into the display path. |
| AS-4 e2e | e2e "AS-4: draw…bridge…adopt…check" (keyboard-draw journey RETAINED) + "AS-5: keyboard-only edit→check→save" retained + new AS-1 pointer journey. Route stubs ASSERT the request contract (POST, srid 4326 ≥ triangle at bridge; adopted 2263 vertices at check) BEFORE returning canned bodies | STRONG. A wrong/missing POST body fails inside the stub; response can't paper over it. |
| AS-5 no new math/deps | Manifests/lockfiles: 0 files touched (git-confirmed). `git grep` for proj4/reproject/transform/feetPerDegree over all 5 packet source files → only doctrine COMMENTS and `srid:2263` labels + `vertices.map(v=>[v.x,v.y])` reshape (no arithmetic). Conversion is server-bridge only | STRONG / grep-provable. |
| AS-6 adoption reconciliation | proposal-draft.test.ts: EQUAL-count preserves walls/levels; SMALLER-count "drops walls that would dangle, leaving NO out-of-range reference"; LARGER-count preserves; danglingWallIds exactness incl. negative/non-integer | STRONG. Smaller-count test asserts surviving walls all in-range AND ids===["W-S","W-E"]; reverting reconciliation (keep base.exterior_walls) reddens on BOTH the loop and the id list (W-N end 3 / W-W start 3 point past the 3-vertex outline). Equal-count preservation re-pinned as AS-6 requires. |

## jsdom honesty (verified)
Unit specs assert only the WIRING/state contract (listener registered, callback fires with lngLat, hit-test routing, setData sync) — the mock's queryRenderedFeatures returns a configured hit regardless of pixel geometry, so real hit-testing is NOT claimed there. Comments state this repeatedly. Real pointer geometry is proven by the e2e PLACE journey (real WebGL canvas clicks). Division is honest.

## Coverage gaps (all NON-BLOCKING)
1. LOW — HJ-2 editor announcement untested. The lib reconciliation (drop dangling walls) is mutation-tested with teeth, and the editor test "adopts map-drawn vertices (M5-T065)" DOES run the shrinking path (5-vertex seed → 3-vertex adopt, 2 walls dangle) but asserts only the vertex table, not the ProposalEditor announcement suffix ("Removed N walls that referenced deleted vertices") nor the wall-count drop. A regression that silenced the announcement would redden no test. AS-6's mutation clause is met by the lib test (reconciliation is safe), so the untested piece is the never-silent honesty copy only.
2. LOW-MEDIUM — real-pointer SELECT/MOVE has no e2e proof. The e2e AS-1 pointer test exercises PLACE (clicks on empty parcel area, deliberately never on a vertex circle). The click-a-drawn-vertex-to-select and move-selected-by-click paths prove only in jsdom with a stubbed hit. Keyboard equivalents (table Select/Deselect, lng/lat edit, Delete) are fully proven in unit + e2e, so the binding a11y bar is met; only the real-browser MapLibre hit-test against the circle layer is inferred.
3. LOW (observation) — AS-2 "deleted via the map interaction" is delivered as select-on-map → delete via the keyboard-accessible table Delete button, not a distinct map delete gesture. Delivered behavior is fully tested; reasonable and arguably more accessible, but the literal map-delete gesture does not exist. Noted for the record.
4. INFO — an accepted T065 proposal-draft unit test WAS deleted (the old "adopts bridged 2263…leaving levels/walls intact" that adopted a 2-vertex outline and asserted all 4 walls preserved). This deletion is CORRECT and REQUIRED: it encoded the pre-HJ-2 buggy semantics (walls preserved on a shrinking adoption = dangling refs). Its legitimate equal-count content is re-pinned by the new equal-count + larger-count tests. AS-3's "0 deletions" holds for the display-surface accepted specs; the sole lot-outline-map.test.tsx deletion is a mock method-signature widening (queryRenderedFeatures one-arg → two-arg), not an assertion removal.

## Conclusion
All six acceptance scenarios map to concrete, mutation-sensitive specs; the frozen source is byte-stable and identical to the CI-SUCCESS tree; no forbidden path or dependency touched; keyboard parity and DB-043(a) focus remedy preserved and tested. The gaps above are non-blocking (honesty-announcement and real-pointer-select coverage), suitable for a follow-up note rather than rework. PASS.

--- END OF REPORT ---
