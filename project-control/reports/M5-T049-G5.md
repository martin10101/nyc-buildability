# M5-T049 G5 — independent security review (security-reviewer, saved VERBATIM from the agent return)

G5 SECURITY REVIEW — M5-T049 (named-street matcher extraction) — VERDICT: PASS

Head reviewed: f3bf4fd3d65ff8db17d91aba48e4b83c2670cff2 (= pin). Material commit d52b1402 (cherry-pick b5d3343f). Read-only git plumbing only; no project_control.py.

BASIS: AST symbol-level byte comparison of the original file at parent e04e6aad vs the two split modules at HEAD. All 26 original top-level symbols are byte-for-byte identical in their new homes — zero mismatch, zero missing, zero extra. Genuine pure move; no logic/threshold/message/ordering change.

FINDINGS:
1. ADVISORY (non-blocking) — named_street_override.py:61 — the facade INTRODUCES __all__ (17 names); the original at e04e6aad had NO __all__. Not a widened surface: every name incl. the 7 underscore helpers was already importable by explicit name pre-split (status.py:33 imports _normalize_cd; Python doesn't hide underscore names from explicit import). Only effect is on `import *`, and nothing in the tree wildcard-imports this module (verified). No security impact. Flagged only because the launch note said "same 17 names" — the original in fact had none.

FIVE REQUIRED POINTS:
1. No new I/O/network/file/env/subprocess/dependency — CONFIRMED. New imports are internal only (_table/_matching). External import `app.rules.snapshots` unchanged from original :64. Stdlib re/dataclass/Enum pre-existed. Added-line grep for os.environ/getenv/subprocess/socket/requests/urllib/httpx/open(/exec(/eval(/pickle/__import__/URLs: no hits (only the word "token" in token-grammar identifiers).
2. Security behaviors byte-identical — CONFIRMED IDENTICAL: fail-closed snapshot validation (NamedStreetOverrideMatcher + _validate_* , matching.py:117-372); DB-023c bounded-repr cap _MAX_QUERY_REPR=80 + _bounded_repr "...(truncated)" marker (table.py:142-153); DB-023d _word_bounded (table.py:187-197); fail-closed _normalize_cd (table.py:166-176).
3. Facade re-exports no widened surface — CONFIRMED (see finding 1).
4. No secrets/keys/URLs added — CONFIRMED; CI secret-scan (gitleaks) success at pushed head per M5-T049-ci-evidence.md, no leaks.
5. Supply chain — CONFIRMED clean: commit touched exactly 6 files (report + 3 modules + 2 test files); no lockfile/requirements/pyproject/*.toml.

No BLOCKING findings. G5 PASS.
