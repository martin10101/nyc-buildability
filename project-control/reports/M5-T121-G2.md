# M5-T121 - G2 producer self-check record (recorded by the orchestrator, role self_check)

Producer: backend-engineer (orchestrator-dispatched subagent) in wt-m5t121; one in-worktree commit 3fdb77b5 (parent a990de06), cherry-picked to d865d977 with every file MATCH by git blob identity (3 files: project-control/reports/M5-T121-producer-report.md ea826181, services/api/app/drawings/sheet_import.py faa95c04, services/api/tests/drawings/test_sheet_import.py ecc14b54).

- **Scope:** services/api/app/drawings/sheet_import.py (NEW, pure, unmounted): list_candidates (closed rings by stable polyline index, bounded, measured in sheet units, page disclosure, escaped scale notes) and build_draft (a user-named edge measured by the service -> scale; the assigned ring scaled with explicit closure through validate_proposed_massing; a provenance block with input_class imported_pdf, precision, label, page, scale inputs, roles and a LOCAL-frame note); typed ImportRefusal values (unreadable_pdf, page_out_of_range, coordinate_out_of_range, bad_candidate, bad_edge, invalid_scale, draft_invalid). services/api/tests/drawings/test_sheet_import.py (NEW): 31 offline tests incl. 8 named in-process mutations.
- **Producer self-checks (report, OBSERVED):** ruff clean; tests/drawings/test_sheet_import.py 31 passed and the DXF import/reader/round-trip suites 90 passed (local Python 3.11.9 - the pack imports only sheet_primitives value types, so no shim was needed); modularity exit 0 (failures 0, sheet_import.py not flagged).
- **[ORCH-HARVEST] at the harvested head d865d977 (cherry-pick d865d977; Python 3.11 local): `python -m ruff check .` (cwd services/api) -> exit 0: All checks passed!; `python -m pytest tests/drawings/test_sheet_import.py tests/drawings/test_dxf_import.py tests/drawings/test_dxf_reader.py tests/drawings/test_dxf_roundtrip.py -q -p no:cacheprovider` (cwd services/api) -> exit 0: 121 passed in 3.01s; `python tools/modularity_check.py --check` (cwd .) -> exit 0:   warn review_signal: tools/agent_supervisor/gate_wave.py - above the justification threshold; record a cohesion justification in review.**
- **Discoveries routed (producer):** DISC-A the draft is a LOCAL scaled sheet frame: a real un-aligned sheet fails the unchanged NYC-2263 bounds check as draft_invalid (same as DXF) - the sheet-to-state-plane alignment the user confirms against the mapped lot is the next C2 packet; DISC-B the mount owns the route, rate limit, pre-render allow_nan guard and body ceilings; DISC-C printed scale notes stay disclosure-only (a future assist may offer them, never auto-apply). OQ-2 resolved per packet: the contract kind stays 'proposed'; input_class carries imported_pdf.

G2 verdict: PASS (self_check; the independent reviews and the DCV follow - dispatched by the successor session per the handoff).
