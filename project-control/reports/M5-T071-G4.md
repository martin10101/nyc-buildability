# M5-T071 — G4 integration/regression review (VERBATIM, two-part transmission)

TRANSMISSION HISTORY (orchestrator note, not reviewer text): delivered complete in two
proactively split parts on 2026-09-23 (~04:39Z and ~04:46Z), no truncation. The reviewer's
read-only guard refused agent-memory writes, so the report arrived through the return
channel. The reviewer's own text follows, unedited.

---

M5-T071 G4 (integration/regression) — VERDICT: PASS, no blocking findings. Report part 1 of 2.

REVIEWER: qa-engineer (independent; producer was frontend-engineer — no roster conflict). Read-only throughout: no edits, no commits, no control-plane writes. Agent-memory writes were refused by the read-only guard, so nothing was written anywhere.

IDENTITY VERIFIED MYSELF
- Review head 94d0117d; material commit 2396a000 (6 files: the 5 allowed source/test files + the producer report — `git show --stat 2396a000`).
- `git diff 2396a000 94d0117d -- <the 5 allowed files>` is EMPTY, and `git log 2396a000..94d0117d -- <same 5>` lists NO commits, so byte-identity holds continuously (not just at the endpoints) across the frozen submission 717562a6 and the CI head c0b71938. `git diff --name-only 717562a6 94d0117d -- apps/web/` is also empty.
- Ancestry confirmed: 2396a000 → c0b71938 → 94d0117d.
- CI verified by me via gh, not from the report: run 35817816845 conclusion success, headSha c0b71938, all 18 jobs green; secret-scan 35817816896 success at the same sha. web-e2e log: "Test Files 55 passed (55) / Tests 1356 passed (1356)" plus "119 passed" Playwright. `python tools/modularity_check.py --check` run by me at the review head → exit 0.

AS-1 (copy gating, DB-047(d)) — ADEQUATE
- Drawable lead: proposal-outline-map.test.tsx:195 (click-to-place) and :204 (click-to-move).
- Each required fallback state: the `it.each(FALLBACK_STATES)` at proposal-outline-map.test.tsx:217, driven by the table at :29-41 — condo/no-polygon `lot-outline-empty`, `multiple_features` → `lot-outline-review`, `invalid_geometry` → `lot-outline-invalid`, no-WebGL → `lot-outline-webgl-unavailable`, plus render-error. Each asserts the typed surface present, `queryByLabelText(INTERACTIVE_MAP_LABEL)` null, copy NOT containing "Click the lot map to place", copy containing the keyboard lead.
- Selection mutation guard :234; MutationObserver transition test :245.
- I verified the mock's variants against the REAL leaf rather than trusting the report: LotOutlineMap.tsx:646 carries exactly `aria-label="Interactive approximate lot outline map"`, and all five fallback testids exist at :657/:672/:696/:707/:719. The report's line anchors are accurate.
- Teeth (checked by reasoning against the source, not assumed): reverting the gate to always-interactive copy makes the copy wrong at FIRST paint, so all five `it.each` rows and the selection guard red synchronously. Deleting only the MutationObserver reddens :245 specifically — the effect keys on `[bbl]`, the rerender uses the same bbl, so the one-time initial sync leaves `mapRendered` stale-true and the waitFor for the keyboard copy times out. Real teeth, not tautology.

AS-2 (finiteness gate, DB-047(e)) — ADEQUATE
- proposal-outline-draw.test.tsx:127: three rows added-but-untyped keep Convert disabled, hint contains "coordinates filled in", `pointFeatureCount()` is 0; two finite still disabled; the third finite enables and draws exactly 3, hint gone.
- Announced count: proposal-outline-map.test.tsx:287 (pure `finitePointCount`, incl. Infinity) and :297 (status says "1 point drawn" and NOT "2 points drawn" with one NaN row).
- Teeth: reverting `canConvert` to `drawnCount` enables Convert on three untyped rows → :127 reds; reverting the status to `points.length` → :297 reds on both assertions.
- Branch coverage of the hint is complete both ways: the `incompleteCount > 0` arm at :138, the accepted HJ-4 "Add N more points" arm still at :302-316 (unchanged, still green).

AS-3 (finite-flow byte-compatibility) — SATISFIED, with one honest caveat already recorded
- No prop contract of ProposalOutlineDraw/ProposalOutlineMap changed (diff adds internal state and one exported pure helper only). ProposalEditor.tsx is absent from 2396a000 and from c0b71938 — grep-provable.
- The ONE consumer break was on the NON-finite path, so AS-3's finite-flow claim survives: the M5-T065 adoption test clicked Convert on NaN/NaN rows. Closed by orchestrator peer commit c0b71938 cluster C (proposal-editor.test.tsx +11 lines that type finite display coordinates before clicking Convert). I read that diff: it STRENGTHENS the spec (it now exercises the real keyboard path) — it does not delete or relax any assertion, and the click-Convert → `outline-draw-bridged` assertion is untouched. proposal-editor.test.tsx runs 7 tests, green in the run.
- The evidence map states this miss honestly under D-066-R001 rather than claiming a clean sweep; I confirmed the claim matches the commits.

(continued in part 2: AS-4/AS-5/AS-6, regression surface, no-weakened-tests analysis, and six advisory findings)

---

M5-T071 G4 report part 2 of 2 — findings and verdict.

AS-4 (DB-048 hardening) — SATISFIED as written in the scenario
- The AS-1 sync spec (lot-outline-map.test.tsx:608) contains no exact cross-render call tally: `toHaveBeenCalledTimes(4)` is gone both before and after the rerender. Final state is asserted by source presence (:654) plus the two overlay layer IDs (:656-660), in-place update by `toHaveBeenLastCalledWith(overlay2)` (:679) read fresh from the live source, one-source-only by per-instance counts (:687-693).
- Grep of the whole file shows the only remaining `toHaveBeenCalledTimes` on that counter are in OTHER specs (see finding 3).

AS-5 (teeth retention) — HOLDS under my own analysis, not just the producer's notes
- Duplicate-source mutation (drop the `getSource(id) ? setData : addSource` guard at LotOutlineMap.tsx:586-591): the same live instance's count goes to 2, so `every(n <= 1)` reds AND `some(n === 1)` reds; the fresh source mock also never receives setData, so :679 times out first. Red three ways.
- Stale-payload mutation: `toHaveBeenLastCalledWith` reds where a plain `toHaveBeenCalledWith` would not — the producer's reasoning is correct.
- I also checked the layer dimension the old tally used to cover: `addLayer` for the overlay sits INSIDE the else-branch of the same guard (LotOutlineMap.tsx:591-610), so duplicate layers cannot occur without duplicate sources. No separate layer teeth are needed. This is verified in source, not assumed.

AS-6 (scope) — SATISFIED
Exactly the packet's allowed_paths changed; no package.json/lockfile in the diff (zero new dependencies); no e2e spec touched; no forbidden file touched; modularity exit 0 at the review head.

NO TEST WAS WEAKENED TO PASS
- Across the three test files: 4 assertions removed, 27 added. Exactly one `it(` block disappears and it is a RENAME — "gates convert until 3 points exist" became the DB-047(e) version, which keeps every original assertion (honesty text, disabled at zero, disabled at two, enabled at three) and reinterprets "two" as two FINITE points. Nothing lost.
- The four removed assertions in lot-outline-map.test.tsx are the two `addLayer` tallies plus the source-defined/setData pair, all replaced by equal-or-stronger checks except the one dimension in finding 2.

FINDINGS — BLOCKING: none. ADVISORY (6):

1. ProposalOutlineDraw.tsx:182 still says "Click the lot map to place points" UNGATED, in the section header directly above the wrapper copy this task just gated. On a condo lot / no-WebGL / invalid-geometry lot the user reads "Click the lot map to place points" immediately above "The reference map on this lot has no interactive drawing surface." This is the DB-047(d) defect class surviving one level up, in a file that IS inside allowed_paths. It is pre-existing accepted T066 copy, not a regression from this diff, and AS-1's testable claim is scoped to the wrapper's instruction element — which is why this is advisory and not a FAIL. It belongs to the human-journey lens; I recommend the orchestrator route it either as a correction here or a new DB rider.

2. The AS-1 sync rewrite tolerates a whole-map rebuild and therefore no longer proves the overlay updates WITHOUT rebuilding the map — the dimension the post-rerender `toHaveBeenCalledTimes(4)` used to cover. The instrumentation to restore it already exists and the producer's own comment says the stable fetchImpl makes the happy path build exactly one instance: `expect(mocks.state.mapInstances).toHaveLength(1)` would be deterministic today. Worth a follow-up.

3. The packet input's stronger clause ("never an exact tally of calls across renders/effects anywhere in the file") is not fully met: exact `addLayer` tallies remain at lot-outline-map.test.tsx:248, :530, :568, :721, :765. AS-4 itself scopes the requirement to the AS-1 spec, which IS clean, so this is advisory. :568 is the closest to the flake class (it tallies 4 across two different effects with the overlay installed), but all five sit in single-render specs, so the documented trigger — a rerender with a fresh fetchImpl identity rebuilding the map — is absent. Residual hazard, not a live flake.

4. The gate depends on a string literal duplicated three ways with no correspondence pin: the leaf owns it (LotOutlineMap.tsx:646), the wrapper re-declares it (ProposalOutlineMap.tsx), the test mock re-declares it again. Every unit test mocks the leaf, so the mock mirrors the wrapper's expectation and never the leaf's reality. I verified the strings match exactly today, so the feature works; drift would fail SAFE (copy degrades to the honest keyboard-only lead) but silently and with the whole suite green. lot-outline-map.test.tsx is in allowed_paths, so a one-line assertion pinning the interactive container's aria-label would close it.

5. Untested mixed branch: with 3 finite points AND an extra untyped row, `finiteCount >= MIN` so Convert is ENABLED, the hint is absent (the condition is `drawnCount > 0 && finiteCount < MIN`), and `convert()` silently drops the untyped row from the bridge payload while the status announces 3 points against 4 visible table rows. Defensible, but nothing tells the user a row was ignored and no spec covers the combination.

6. Real-browser proof for the drawable branch is absent: no Playwright spec asserts the instruction copy (grep over apps/web/e2e finds no reference to "Click the lot map" or `proposal-outline-map-instructions`), so the click-to-place lead is proven only against the mock. Separately, the MutationObserver watches `{childList: true, subtree: true}` over a container MapLibre mutates heavily; `setMapRendered` early-returns when unchanged so no render churn, but the querySelector cost per mutation batch is only observable in a real browser.

REGRESSION SURFACE — CLEAN
All 55 vitest files and 1356 tests pass in run 35817816845, including every neighbouring architect/address suite I checked by name in the log: lot-outline-map (26), proposal-outline-draw (9), proposal-outline-map (17), proposal-editor (7), address-confirm (35), address-resolution (34), condo-resolution-display (49), development-limits (198), max-envelope-panel (12), entry (38), workspace, report-view, source-links, analysis-identity-substitution, proposal-check-report, zoning-context-panel, autocomplete. Playwright 119 passed. All other CI jobs (api, control-plane, modularity, contracts, dependency-security, supervisor-bridge, etc.) green, and secret-scan green at the same sha.

VERDICT: PASS

END-OF-REPORT
