# M5-T043 — G3 code review (verbatim reviewer return; transport entity-decoding only)

Saved by the orchestrator from the code-reviewer agent return, 2026-09-19 (seq-118 wave,
pinned at submission sha 20c597c1). Nothing added or removed beyond decoding transport
HTML entities.

---

I have completed a thorough independent G3 review. All checks pass. Here is my gate report.

---

# G3 Independent Code Review — M5-T043 (wide-street wiring hardening + extraction; DB-028 a-e,h)

**Reviewer:** code-reviewer (read-only) · **Reviewed SHA:** `20c597c1` (candidate/D-024-mrl-option-b) · **Material commit:** `f6e88222` (7 files, +829/-249)
**Verdict: PASS** — 0 BLOCKING, 3 ADVISORY (all in-scope/explained). Extraction is behavior-preserving; guard/derivation changes are tighten-only; all scenarios reproduce green locally.

## Identity / scope verification
- `f6e88222` is an ancestor of HEAD `11bed5f7`. `git diff f6e88222..HEAD` restricted to the 7 M5-T043 allowed paths is EMPTY, so the reviewed content is byte-stable across the submission SHA and HEAD.
- Material `f6e88222` touches exactly 7 files, all within `allowed_paths`. No `services/api/app/connectors/**` (M5-T044 disjointness preserved — the disjoint peer `1fd07e9a`/CI head only changed `dtm_condo_soda.py` + its test).
- Contract/schema files byte-untouched: `git show f6e88222 --stat` for `integration.py`, `app/schemas/`, `api/v1/response.py`, `packages/contracts/`, `rule-evaluation-contract.ts` is empty. Contract v1.1.0 intact (AS-7).
- CI-identity: `git diff f6e88222..a380a956` (the orchestrator-captured green-CI head) touches no M5-T043 allowed path — so CI run 35412920210 (success) covers this material byte-for-byte (AS-8 holds via captured evidence).

## (1) EXTRACTION FIDELITY (load-bearing) — CORRECT, behavior-preserving
Diffed each moved body between `f6e88222^:wide_street_wiring.py` and `f6e88222:named_street_override_status.py`:
- `_fully_resolved_typed_inputs` (old :335-361 → new :99-124): **byte-identical** (docstring + body).
- `build_named_street_override_status` (old :363-489 → new :127-251): **byte-identical** — all four attesting shapes, the MATCHED_OVERRIDE reason/provenance, the INDETERMINATE and unresolved fail-closed returns, and the all-clear return match verbatim. The PRESERVATION truth table is intact.
- `MatchedNamedStreetOverride` (old :164-183 → new :47-65): **byte-identical** (docstring + 5 fields).
- `NamedStreetOverrideStatus` fields (`override_table_implemented`, `segment_may_touch_named_override`, `note`, `matched_override=None`): **byte-identical**; the class **docstring** received an in-scope (b)/(c) accuracy edit (now describes "APPLIED and fully resolved" and the override_table_implemented-keyed guard). This is documentation, not behavior drift.
- No circular import: `named_street_override_status.py` imports only from `app.rules.named_street_override` (:33), never from `wide_street_wiring` — clean DAG.

## (2) FACADE completeness — CORRECT
- `wide_street_wiring.py:94-98` re-imports the three public symbols from the submodule and keeps them in `__all__` (:112-115). `test_db028a` asserts `facade.<name> is submodule.<name>` (object identity, not copies) plus `__all__` membership on both.
- Consumer imports resolve unchanged, zero consumer edits: `integration.py` (:56,:514-520 → `WideStreetDetermination`, `COVERAGE_PROFESSIONAL_REVIEW_REQUIRED`, `FAR_ROW_WIDE_STREET`, `select_far_row_value` — none of the moved symbols), `rule_evaluation.py:74` (`WideStreetDetermination`), and `wide_street_live_provider.py:109-114` (`NamedStreetOverrideStatus`, `build_named_street_override_status` now resolving through the facade to the submodule objects). The 475-test consumer suite is green with no consumer file edited (AS-1).
- Private `_fully_resolved_typed_inputs` is correctly NOT re-exported; grep confirms no external consumer/test imports it.

## (3) Guard hardening (c) — TIGHTEN-ONLY, verified
Old `_elevated_exceptions_checked`: `if may_touch AND NOT implemented: return False`. New (`:238-254`): `if not override_table_implemented: return False`. New rejects a strict superset (adds the `NOT implemented AND NOT may_touch` case). Same for `named_pending`: old `may_touch AND NOT implemented` → new `not override_table_implemented` (`:324`). Swept every production constructor of `NamedStreetOverrideStatus` (`git grep` over `app/`): only the builder's 4 shapes + the provider fail-safe (`implemented=False/may_touch=True`) — **none** constructs the adversarial `implemented=False/may_touch=False` shape. Every real builder output is unaffected; only the hand-built inconsistent shape now fails safe. `test_db028c` forces it (exceptions_checked=False, pending=True, professional review). Attestation only got stricter.

## (4) Empty-decisions (d) — CORRECT on all branches
`return bool(decisions) and all(...)` (`:252-254`): for non-empty decisions `bool(decisions)` is True so behavior is unchanged; only the empty set flips from vacuous `all(())→True` to `False`. This affects solely the no-policy-decisions professional-review branch (`far_row=none`, no FAR granted). `test_db028d` forces it.

## (5) Accuracy (b) — TRUTHFUL
Grep of both rules modules for `not implemented`/`out-of-scope`/`follow-up` (case-insensitive) → no matches. Module docstring (`:48-61`) and the branch-4 reason (`:421-430`) now state the ZR 12-10 matcher "was consulted but could not resolve the candidate segment (missing community district or cross-street bounds, or an indeterminate locator)" — accurate to what `build_named_street_override_status` actually does.

## (6) Provider fail-safe (e) — SOUND; provider source unchanged
`git show f6e88222 --stat -- wide_street_live_provider.py` is empty (source out of scope, untouched). `test_db028e` clears the `@lru_cache` (`:159`) then monkeypatches `provider.load_default_matcher` to raise, so the pre-existing `except Exception` branch (`:196-205`) runs. Assertions (`override_table_implemented=False`, `segment_may_touch=True`, `matched_override is None`, note "could not be loaded", `event=named_override_matcher_unavailable`, `error_type=RuntimeError`) match the provider branch exactly; `cache_clear()` in `finally` avoids poisoning siblings.

## (7) Web fallback (h) — DISPLAY-ONLY, no logic change
`CalculationEvidence.tsx:44` — single line, the `??` fallback in the NON-review branch changed `"withheld — professional review required"` → `"Not calculated"`; the `wideReview` branch is unchanged. `report-view.test.tsx:169-182` forces it on a `within_100ft_of_wide_street` (non-review) state with null FAR, asserting "Not calculated" and NOT any professional-review phrase within the `wide-street-provenance` region. (Web proves in CI; captured CI success confirms.)

## (8) Modularity — independently verified
Ran `python tools/modularity_check.py --check` → failures 0, warnings 21, EXIT 0. Both `wide_street_wiring.py` and the new `named_street_override_status.py` are **absent** from the warn list; only the pre-existing, out-of-scope `named_street_override.py` matcher (a different file) remains. AS-7 satisfied.

## Independent reproduction (read-only)
- `python -m ruff check .` (services/api) → All checks passed!
- `pytest tests/rules/test_wide_street_wiring.py tests/rules/test_named_street_override.py -q` → **101 passed**
- `pytest tests/spatial/test_wide_street_live_provider.py -q` → **39 passed**
- `pytest tests/api tests/rules/test_rules_integration.py -q` → **475 passed** (consumers green, zero consumer edits — facade proven)
- Focused `-k "db028 or extracted or facade or exceptions_checked or override"` → 12 passed
- `python tools/modularity_check.py --check` → failures 0, EXIT 0

## Test adequacy
Not weak. New tests are behavioral, not tautological: `test_db028a` asserts object identity through the facade; `test_db028c`/`test_db028d` drive the full `determine_wide_street_far` path (not the helper in isolation) and assert the tri-value outcome; `test_db028e` clears the cache and forces the real load-failure branch with a raising loader. The `OVERRIDE_CLEAR` fixture change (`implemented=False`→`True`) is a legitimate repair modelling the builder's genuine all-clear shape (`build_named_street_override_status` :242-251), not masking — every existing test using it stays green because `named_pending`/`exceptions_checked` are identical for that shape with non-empty decisions.

## ADVISORY findings (non-blocking)
1. **ADVISORY (informational)** `named_street_override_status.py:68-91` — the `NamedStreetOverrideStatus` docstring is the one part of the extraction that is NOT byte-identical to its prior location; it was updated for the (b)/(c) accuracy items. In-scope and correct (documents the new guard behavior); flagged only so the extraction record is complete — fields and logic are byte-identical.
2. **ADVISORY** `test_wide_street_wiring.py:107-113` — the `OVERRIDE_CLEAR` fixture semantics changed under the same name. Behavior-equivalent for all existing callers (verified: 101 green), but future readers should note the fixture now means "matcher applied and resolved clear", not the prior "table not implemented, no touch".
3. **ADVISORY** pre-existing, out of scope — `named_street_override.py` (the matcher) remains above the warn threshold (21-warn list). Correctly untouched here; route to whichever task next grows that module (already tracked as a separate concern).

## Read-only commands run
- `git show --stat --oneline f6e88222`; `git merge-base --is-ancestor f6e88222 HEAD`; `git diff --stat f6e88222..HEAD -- <7 allowed paths>` (empty)
- `git show f6e88222^:...wide_street_wiring.py | sed -n` for old bodies of the 4 moved symbols + old `_elevated_exceptions_checked` (269-292) + old `named_pending` (509-511)
- `git show f6e88222 -- <CalculationEvidence.tsx | report-view.test.tsx | test_wide_street_live_provider.py | test_wide_street_wiring.py>`
- `git show f6e88222 --stat -- wide_street_live_provider.py` (empty = source untouched); `git grep -n "NamedStreetOverrideStatus(" f6e88222 -- services/api/app/`; `git grep -n` for consumer imports
- `git diff --stat f6e88222 a380a956 -- <7 allowed paths>` (empty); `git diff --name-only f6e88222 a380a956`
- `Read` of `named_street_override_status.py`, `wide_street_wiring.py`, `CalculationEvidence.tsx`, provider seam (159-207), integration import sites; `Grep` for stale phrasing + `_fully_resolved_typed_inputs`
- `python tools/modularity_check.py --check`; `ruff check .`; the four documented pytest invocations from `services/api`

**Recommendation to orchestrator: record G3 PASS.** All eight judge points hold; the three advisories are non-blocking and in-scope. The directive-compliance verification of D-045-R008/R009 and D-066-R001 is the directive-compliance-verifier's pass (producer ≠ verifier); on the code side I confirmed D-045-R009 DRAFT/not-verified vocabulary is preserved (the (h) edit did not touch any DRAFT label; `DRAFT_LABEL_NOTICE` unchanged).

Relevant files (absolute):
- `C:\Users\MLFLL\Downloads\nyc-zoning\ctl24\services\api\app\rules\named_street_override_status.py`
- `C:\Users\MLFLL\Downloads\nyc-zoning\ctl24\services\api\app\rules\wide_street_wiring.py`
- `C:\Users\MLFLL\Downloads\nyc-zoning\ctl24\services\api\app\spatial\wide_street_live_provider.py`
- `C:\Users\MLFLL\Downloads\nyc-zoning\ctl24\services\api\tests\rules\test_wide_street_wiring.py`
- `C:\Users\MLFLL\Downloads\nyc-zoning\ctl24\services\api\tests\spatial\test_wide_street_live_provider.py`
- `C:\Users\MLFLL\Downloads\nyc-zoning\ctl24\apps\web\src\components\architect\CalculationEvidence.tsx`
- `C:\Users\MLFLL\Downloads\nyc-zoning\ctl24\apps\web\src\components\architect\__tests__\report-view.test.tsx`
