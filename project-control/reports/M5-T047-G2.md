# M5-T047 — G2 producer self-check record (recorded by orchestrator)

Producer: backend-engineer (loop-2 runs 09/10/11 across the T046→T047 lane handoff; the
T047 build itself is run 11's).

- Producer self-checks: all four documented commands re-run from the packet-specified
  cwds in the final unit (producer report §8, all PASS); web results honestly declared
  unknown pending orchestrator CI + AS-8; the producer committed nothing and asked for
  per-file digest-bound collection (§0) after the in-run round lost evidence to an
  aggregate diff cap — the collection discipline the orchestrator then provided.
- Orchestrator reproduction at the material identity (wt-m5t047): ruff clean;
  `pytest tests/api/test_lot_geometry_api.py -q` = 33 passed; `pytest tests/api -q` =
  439 passed; `python tools/modularity_check.py --check` exit 0.
- CI: green at the corrected head d27a6a83 (run 35431045720 incl. web-e2e); the single
  first-push failure (3 consumer-count tests outside allowed_paths) was corrected by the
  orchestrator per the M5-T023 precedent, test-only, tagged.
- The run-11 breaker (DB-012 trip 19) closed on evidence-collection churn after the
  build was complete on all nine files — consistent with the after-substantive-work
  pattern, not a self-check failure.

**G2 verdict: PASS** — self-check discipline satisfied at the frozen identity.
