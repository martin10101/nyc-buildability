# M5-T075 — G4 test-adequacy review (backend-engineer, read-only)

> Transmission history: returned across six transmissions — Parts 1 and 2 (truncated inside
> F5 at "over-claim slightly; the"), an F5-completion micro-chunk, then the Part-3
> assessment arriving as per-prediction micro-chunks (prediction 1; prediction 2;
> prediction 3) with two watchdog-driven resume requests from the exact cut phrases, and a
> final message carrying predictions 4–5, the remaining assessment, and the verdict.
> Joined verbatim in order at each cut point. Saved verbatim by the orchestrator
> (transport entity-decoding only).

Memory write is blocked by the read-only guard for this named reviewer session (noted, not retried). Here is the review.

---

**PART 1 of 3 — M5-T075 G4 test-adequacy review (backend-engineer, read-only). Pinned head 9bc68dc733c17d60dfdb2add1672e0faceb47d73 confirmed by `git rev-parse HEAD`.**

Identity verified first: test blob `ed57959e4c9fb8e5d56d086f8d2a6f5d215cbf53` is byte-identical at `7d225e2f`, at `490c3413`, and at the pinned head; production client blob `b1b36871…` is identical at `7d225e2f^` and HEAD (zero production edits, AS-4); `gh run view 35829303244` returns conclusion `success`, headSha `490c3413`, workflow CI. The CI green therefore covers exactly the reviewed content.

**Findings**

**F1 (LOW — adequacy, non-blocking) test file `apps/web/src/lib/__tests__/outline-bridge-api.test.ts:368-382`, helper `:299-309`.** The mid-flight caller-abort spec asserts the same outcome (`aborted`, announcement `""`) that the pre-flight guard at `outline-bridge-api.ts:359-361` produces, and nothing in the spec proves the request actually left the client. Today the branch is genuinely exercised (the abort listener is registered synchronously inside `abortAwareFetch` before `controller.abort()` runs), but the spec has no guard against silently degenerating: insert any `await` into `fetchOutlineBridge` before the `fetchImpl` call at `:372` and the caller's abort lands on the pre-flight guard instead, the mid-flight catch guard at `:381` goes uncovered, and the suite stays green. The pre-flight spec already models the fix — it counts calls and asserts `fetchCalls === 0` (`:354`, `:362`). A counting `abortAwareFetch` with `expect(fetchCalls).toBe(1)` closes it in one line. Recommended on the next touch of this file, not a required correction.

**F2 (LOW — adequacy, non-blocking) test file `:384-402` against `outline-bridge-api.ts:399`.** The over-budget class is proven only from above the bound (`MAX_RESPONSE_BYTES + 1`). The smallest realistic regression on a size bound is the off-by-one `>` → `>=`, and that mutant survives every assertion in the packet: an at-budget response would then fail closed while the spec, which never exercises the boundary, stays green. A companion fixture with `Content-Length` exactly `MAX_RESPONSE_BYTES` asserting `bridged` would pin the operator. Mutation prediction 5 (`>` → `<`) is correct but only covers the far side.

**END OF PART 1**

---

**PART 2 of 3**

**F3 (INFORMATIONAL) test file `:6`, `:388`.** The spec imports `MAX_RESPONSE_BYTES` and derives its fixture from it, so a change to the constant's value (512_000 at `outline-bridge-api.ts:47`) is invisible to the suite. This is the right coupling — it tests the rule, not the number — but it means no test anywhere pins the budget value. Paired with F2's at-budget fixture it would matter less.

**F4 (INFORMATIONAL — producer-prediction check) producer report AS-3 item 4.** The correction is accurate and I reproduced it independently. A caller abort runs `onExternalAbort` (`:362-363`), which aborts the internal controller, so at `:381` both operands are true; dropping either alone leaves the fixture green, and only deleting the whole guard reddens it (the run then falls through to `network_error` at `:382`, since `timedOut` is false under the 30_000 ms budget, flipping both `.toBe("aborted")` and `.toBe("")`). The consequence worth recording: `externalSignal?.aborted` at `:381` is unfalsifiable through the client's public surface, because the mirror listener makes it redundant in every reachable state. That is production redundancy, not a test gap — no spec can or should pin it.

**F5 (INFORMATIONAL) test file `:295-309`, spec titles `:335`, `:368`.** The client's `catch {}` at `:379` discards the error object entirely and decides from `timedOut` / `aborted` state, so the `DOMException("Aborted", "AbortError")` the stub throws is immaterial — the specs would pass with any rejection value. The titles naming "AbortError" over-claim slightly; the behavior proven (rejection plus flag state) is exactly what production can distinguish, so the specs are faithful and no change is warranted. (Distinct from G3's F3, which concerned the "BEFORE parsing" title.)

**PART 3 of 3**

**Assessment per duty**

*Sufficiency against realistic regressions (mutation reasoning).* I checked all five producer predictions and all five hold. (1) network: retyping the `:382` return reddens `:321`; independently, editing the `:550` announcement arm reddens `:330`, and dropping `network_error` from `outlineBridgeOutcomeIsRecoverable` (`:215`) reddens `:325` — three separate mutants, the strongest spec of the five. (2) timeout: deleting the `if (timedOut)` guard at `:380` falls through to `:381`, where the timer has already aborted the controller, yielding `aborted` and reddening `:340`; `:341` additionally pins the carried budget. (3) pre-flight abort: removing the `:359-361` early return lets the counting stub run, and its `{}` body reaches `boundReport` as a `validation_failure`, reddening both `:360` and `:362`. (4) mid-flight abort: as F4 — the corrected prediction is accurate; only deleting the whole `:381` guard reddens the spec (the run then falls to `network_error`, flipping both `.toBe("aborted")` and `.toBe("")`), while either single-operand drop leaves it green. (5) over-budget: flipping `:399` `>` to `<` makes `512001 < 512000` false, so the body parses through to `bridged` and `:390` reddens — correct, but only from above the bound (F2). Coverage is adequate for the four DB-045(i) classes, with the two named soft spots.

*Over-fitting.* None. Every spec calls the real exported `fetchOutlineBridge` through its documented options seams (`fetchImpl`, `signal`, `timeoutMs`), uses real `Response` and `AbortController` objects, and asserts exported contract values — outcome `kind`, carried fields, announcement strings, recoverability. No mock internals (call arguments, header sets, stub invocation order) are asserted anywhere.

*Determinism / residual flake risk: low.* The never-settling `abortAwareFetch` leaves the internal timer as the only thing that can settle the timeout spec, so the 10 ms budget is load-independent. The pre-flight path returns before `setTimeout` at `:364`, so no timer dangles; the other paths clear theirs in the `finally` at `:508`. Every stub rejection is awaited inside the client's `try`, so no unhandled rejection can surface as cross-test noise. The `headers.set("Content-Length", …)` mechanism is empirically proven by the CI green itself, since the real (small) length would have produced `bridged` and reddened `:390`. The one residual is F1's degeneration mode, which is silent coverage loss rather than a flake.

*Adjacent cases within the packet's four classes.* F1 and F2 are the two in-scope additions I would ask for. The `response.json()` catch sub-branches at `:407-408` carry the same class names but sit on a second fault surface needing a new body-throwing fixture family; I concur with G3 F4 that they belong to DB-045(j), not here.

G4 VERDICT: PASS

END-OF-REPORT
