# M5-T072 — G3 independent code review (code-reviewer, read-only)

> Transmission history: returned as three proactively split parts (Part 1/3, 2/3, 3/3), no
> truncation; joined verbatim in order. Saved verbatim by the orchestrator (transport
> entity-decoding only).

M5-T072 G3 code review verdict: PASS (5 findings, all LOW/advisory, none blocking).

Part 1/3 — identity and method

HEAD verified `4ed4d3d737c9755750dc1969cb289eae49017c31` (git rev-parse match; primary checkout). Read: packet M5-T072.json, producer report + [ORCH-HARVEST], evidence-map, G0, G2, `git show ecb6a13f` and `abc574a8`, the test file and `store.py` at the pinned head, plus the route error path. Independently re-ran, cwd `services/api`: `python -m ruff check .` -> All checks passed (exit 0); `python -m pytest tests/site_definition tests/api/test_site_definition_api.py -q` -> 77 passed in 4.84s. Both reproduce the harvest exactly. I wrote nothing (the harness also blocks my own agent-memory write; noted, not a defect of this task).

F1 — Pin correctness: VERIFIED, no defect. Severity: none (affirming).
`services/api/tests/site_definition/test_site_definition_records.py:842` adds `assert str(missing.value) == _UNIFIED_NOT_FOUND_TEXT`. Combined with the pre-existing `:836` `assert str(missing.value) == str(foreign.value)`, both revoke branches are bound to the constant, not merely to each other. Both raise sites confirmed to use the one shared constant: `services/api/app/site_definition/store.py:428` (record is None) and `:441` (foreign scope), constant at `store.py:72-75`. I parsed both constants with AST and compared: byte-equal (`'no site-definition confirmation matching that record id exists for the property addressed by the request path'`).

F2 — Test-local byte-copy design: SOUND and consistent with the `:886` precedent. Severity: none (affirming).
`_UNIFIED_NOT_FOUND_TEXT` at `test_site_definition_records.py:855` is a literal copy and is never imported from the store. That is the correct choice: importing `_NOT_FOUND_FOR_ADDRESSED_PROPERTY` would make the assertion tautological (fork the constant and both sides of the comparison move together, leaving the test green). The copy is the independent oracle. Same structure the T069 G3 review accepted for supersede at `:886` and `:909`.

Part 2/3 — findings F3 to F5

F3 — Constant is used above its definition, under a section banner naming the other operation. Severity LOW, advisory, do not fix in this packet.
`test_site_definition_records.py:842` references `_UNIFIED_NOT_FOUND_TEXT`, defined 13 lines later at `:855` under the header "M5-T069 — DB-040(s) supersede not-found message unification". This is valid Python (module-level name resolved at call time) and the suite passes, so it is not a defect. It is now a two-operation constant sitting inside the supersede section. A future touch could hoist it to the module header; doing it here would exceed the packet's "+1 assert, +1 comment" bound, so leaving it is the right call.

F4 — Transitive binding depends on `:836` surviving. Severity LOW, advisory.
Only the missing-id branch is pinned directly; the foreign branch inherits the binding through `:836`. If `:836` were ever deleted, the foreign branch would silently lose its pin. This mirrors the accepted `:886` structure exactly, so it is consistent, not a regression. Direct mitigation existed (`assert str(missing.value) == str(foreign.value) == _UNIFIED_NOT_FOUND_TEXT`, the form used at `:909`) but would have rewritten a pre-existing assertion, which the packet forbids.

F5 — AS-2 mutant step (b) ran a narrower selection than the documented command. Severity LOW, advisory; conclusion unaffected.
The producer recipe step 3b says "re-run step 2" (`tests/site_definition tests/api/test_site_definition_api.py`, 77 tests); the [ORCH-HARVEST] transcript records `tests/site_definition` only, 43 passed. The G2 record states the 43 openly, so this is disclosed, not hidden. I closed the gap independently: the literal appears exactly twice repo-wide (`store.py:73` and the test at `:856`; `grep` for both the full sentence and the fragments "matching that record id" / "addressed by the request path" across .py/.ts/.tsx/.json), and the route-level tests assert only intra-operation equality (`tests/api/test_site_definition_api.py:819`, `:874`, with no-echo at `:830-831`, `:882-883`). So no test in the wider 77 would have caught the both-branch mutant. The "sole catcher" claim stands.

Part 3/3 — remaining finding, duty assessment, verdict

F6 — D-084-R001's named harness evidence is not repo-visible. Severity LOW, routes to the DCV, not to G3.
R001 lists "the lane-1 run journal/launch record" as required evidence. Grepping the checkout for the run id `persistent-local-69` hits only `project-control/reports/M5-T072-evidence-map.json` and `M5-T072-G2.md` — the evidence-map's own assertion, not a corroborating journal. Everything ledger-side does verify: in-regime packet (`directive_regime_version` 1.0 + `directive_refs` D-084-R001), G0 recorded PASS at contract seam `5670a916`, claim seam `f31abde4`, both commits exist, worktree field is the full path `C:\Users\MLFLL\Downloads\nyc-zoning\wt-m5t072`, and R001's applicability lists M5-T072 so applicable equals cited.

Duty assessment
- Correctness of the pin: PASS. Binds both branches via the `:836` chain; the constant is byte-identical to the store's; both raise sites share the one constant.
- Test-local byte-copy design: PASS. Sound (non-tautological oracle) and consistent with the accepted `:886` precedent. Extra support the report did not make: the route body text derives from the store constant too — `services/api/app/api/v1/site_definition.py:314` emits `_bounded_message(str(exc))` with the 404 mapping at `:168` — so there is no independent route literal to fork, and a store-level pin is the correct single place for this invariant.
- Scope discipline: PASS. `ecb6a13f` is +2/-0 in the test file plus the report, nothing else; `abc574a8` touches only the report; no pre-existing assertion was modified or deleted (the diff has zero deletions in the test); `store.py` and every route are untouched — `git log` shows the test file last changed at `463f98f9` (T069 material) before this commit; `git status --porcelain` on both allowed paths is clean at the pinned head.
- Mutation matrix demonstrates the A2 gap closed: YES. Both-branch fork green without the pin (43 passed) and FAIL at `:842` with it, with the AssertionError recorded. The choice of a both-branch fork is the right mutant — a single-branch fork reddens `:836` first and was already covered.
- D-084-R001 fit: PASS on everything repo-verifiable, with F6 left for the DCV.

G3 VERDICT: PASS

END-OF-REPORT
