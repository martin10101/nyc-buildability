# M5-T043 — G5 security review (verbatim reviewer return; transport entity-decoding only)

Saved by the orchestrator from the security-reviewer agent return, 2026-09-19 (seq-118 wave,
pinned at submission sha 20c597c1). Nothing added or removed beyond decoding transport
HTML entities.

---

Memory write is blocked by the read-only guard (shell writes disallowed); that's fine — the report is the deliverable. All verification is complete. Here is my G5 gate report.

---

# G5 SECURITY REVIEW — M5-T043 (wide-street wiring hardening + `named_street_override_status` extraction; DB-028 a-e,h)

**Reviewer:** security-reviewer (read-only). **Pinned submission:** `20c597c1`. **Material:** `f6e88222` (candidate/D-024-mrl-option-b). Prior surface review: M5-T040 G5 (`b4865d1c`).

## VERDICT: PASS
No BLOCKING findings. **Every M5-T040 fail-closed invariant still holds — and each was strengthened, not weakened, by this task.** Two ADVISORY/observation items, neither blocking.

## IDENTITY / SCOPE
- Working tree byte-identical to `20c597c1` for all nine T043 allowed_paths (`git diff 20c597c1 -- <files>` = 0 lines). `20c597c1` and `f6e88222` are both ancestors of HEAD.
- `f6e88222` touches ONLY allowed_paths: `named_street_override_status.py` (new), `wide_street_wiring.py`, `test_wide_street_wiring.py`, `test_wide_street_live_provider.py`, `CalculationEvidence.tsx`, `report-view.test.tsx`, and the producer report. **No `integration.py`, `response.py`, schema, or contract `.ts`** — contract v1.1.0 byte-untouched (item 6, confirmed). **No connector file touched.**
- Disjointness with M5-T044 (item 6): `1fd07e9a` (T044 material) touches ONLY `connectors/dtm_condo_soda.py` + its test + report — **none of those paths are in T043's allowed_paths**; T043 touches no `connectors/**`. Fully disjoint.
- No new dependency: `named_street_override_status.py` imports only `collections.abc`, `dataclasses`, and internal `app.rules.named_street_override`.

## (1) EXTRACTION DID NOT WEAKEN ANY FAIL-CLOSED PROPERTY — CONFIRMED
- **Byte-equivalence.** The removed `MatchedNamedStreetOverride`, `NamedStreetOverrideStatus`, `_fully_resolved_typed_inputs`, and `build_named_street_override_status` in the wiring diff are reproduced verbatim in `named_street_override_status.py:47-251`. The ONLY intra-extraction text change is the `NamedStreetOverrideStatus` docstring wording (`named_street_override_status.py:75-90`) — documentation reflecting the (b)/(c) accuracy, no field/logic change. Dataclass fields identical (`:93-96`).
- **`exceptions_checked=True` reachable only via the (now stricter) guard.** The only branch granting the higher FAR (`FAR_ROW_WIDE_STREET`) is branch 4b WITHIN_WIDE (`wide_street_wiring.py:462-476`); it uses the guard-computed value and is reachable only after `named_pending=False` (⇒ `override_table_implemented=True`, `:414`). `select_far_row_value` (`:535-539`) returns the wide value ONLY for `FAR_ROW_WIDE_STREET`. Branch 0 matched-override (`:339`) and branch 4 named_pending (`:419`) hardcode `exceptions_checked=False`.
- **No path converts an error into attested.** B4 typed engine failure → professional review, `far_row=NONE` (`:442-458`); a bare non-typed exception propagates (500), never a determination. Matcher-load failure (provider) → unresolved status (test AS-5 below), never cleared.
- **MATCHED_OVERRIDE / INDETERMINATE refusals intact with verbatim provenance.** `build_named_street_override_status` (byte-equivalent) still emits `matched_override` carrying all four provenance elements (`named_street_override_status.py:199-214`); branch 0 folds it into `reason` + `named_street_override_match` (`wide_street_wiring.py:334-346`). INDETERMINATE / non-typed NOT_MATCHED → `override_table_implemented=False` → `named_pending=True` → branch 4.
- **No numeric alternate-width anywhere.** `MatchedNamedStreetOverride` has no width field; the only "width" tokens in the module are docstrings/reason strings stating the value is "NOT applied numerically" (`named_street_override_status.py:52,195`). Grep-confirmed.

## (2) GUARD HARDENING (c) IS STRICTLY-TIGHTER — TRUTH TABLE ENUMERATED
`_elevated_exceptions_checked` (`wide_street_wiring.py:238-254`). Inputs: `matched_override` present?, `override_table_implemented` = I, `decisions`.
- BEFORE (M5-T040): `if matched: False`; `if may_touch and not I: False`; else `all(decisions)` (note `all(())`=True).
- AFTER (M5-T043): `if matched: False`; `if not I: False`; else `bool(decisions) and all(...)`.

| matched | I | may_touch | decisions | BEFORE | AFTER | Δ |
|---|---|---|---|---|---|---|
| yes | * | * | * | False | False | same |
| no | False | True | any | False | False | same |
| no | False | False | empty | **True** | False | tighter |
| no | False | False | all-clear | **True** | False | tighter |
| no | False | False | non-clear | False | False | same |
| no | True | * | empty | **True** | False | tighter (item d) |
| no | True | * | all-clear | True | True | same |
| no | True | * | non-clear | False | False | same |

**Every cell stays equal or moves True→False. No input refused before clears now.** Non-BLOCKING; strictly tighter. The exact old-slip shape (`may_touch=False, I=False`) is now refused by `if not I` (item c). Correspondingly `named_pending = not override_table_implemented` (`:324`) only EXPANDS the professional-review set (fail-safe). Regression `test_db028c_hand_built_not_implemented_status_never_clears_guard` reproduces the refusal — **PASSED**.

## (3) EMPTY-DECISIONS FIX (d) CLOSES MY PRIOR COSMETIC A-ITEM HONESTLY — CONFIRMED
`bool(decisions) and all(...)` (`:252`) replaces the vacuous `all(())`→True. My M5-T040 finding-4 (empty-decisions professional-review branch could read `exceptions_checked=True`) is now honestly False across every branch using the computed value (branches 1/2/3/4b/4c/engine-error). Not a relabel — the code returns False for the empty set. `test_db028d_no_policy_decisions_reports_exceptions_checked_false` — **PASSED**. Note: the test fixture `OVERRIDE_CLEAR` was correctly re-shaped to `override_table_implemented=True` (the honest builder shape), and the old inconsistent `implemented=False/may_touch=False` shape was moved to a NEW `OVERRIDE_NOT_IMPLEMENTED` fixture used only to prove the refusal — no test was softened to paper over the change.

## (4) FACADE: NO IMPORT CYCLE, NO DUPLICATE-OBJECT HAZARD — CONFIRMED
- Chain is acyclic: `wide_street_wiring` → `named_street_override_status` → `named_street_override`. `named_street_override.py` imports neither of the other two (grep-confirmed).
- Same objects re-exported (not parallel copies): wiring does `from app.rules.named_street_override_status import (MatchedNamedStreetOverride, NamedStreetOverrideStatus, build_named_street_override_status)` and lists all three in `__all__`. `test_db028a` asserts `getattr(facade, name) is getattr(submodule, name)` for all three — **PASSED** (`is`-identity, so no drift-capable copy).
- Consumers untouched and resolve through the facade: provider (`wide_street_live_provider.py:109-114`, source unchanged in this commit) imports `NamedStreetOverrideStatus, WideStreetDetermination, build_named_street_override_status, determine_wide_street_far` — all in `__all__`. No consumer imports the private `_fully_resolved_typed_inputs`, `MatchStatus`, `OverrideQuery`, or `_normalize_cd` from the wiring facade (grep-confirmed).

## (5) DISPLAY CHANGE (h) IS COPY-ONLY — CONFIRMED
`CalculationEvidence.tsx:44` — a single string-literal swap in the NON-review branch of the ternary: the defensive null-FAR fallback `governing_max_residential_far ?? "Not calculated"` (was `?? "withheld — professional review required"`). The review branch (`wideReview ? "withheld — professional review required"`) is unchanged. No logic, no new sink; plain JSX text interpolation (React auto-escapes). Security-positive: a confident (non-review) determination can no longer render a "professional review required" phrase on missing data. `report-view.test.tsx` asserts the non-review + null-FAR case shows "Not calculated" and neither professional-review phrase (web proves in CI; item verified by inspection + orchestrator CI capture).

## (6) SCOPE / LEAST PRIVILEGE / DEPS — CONFIRMED (see IDENTITY/SCOPE)
Allowed_paths only; contract byte-untouched; no connector touched; no new dependency; disjoint from M5-T044.

## FINDINGS
1. **[ADVISORY / observation]** `services/api/app/rules/named_street_override.py` (853 lines) remains above the modularity warn threshold. **Pre-existing, out of this task's scope** (not in allowed_paths; untouched by `f6e88222`); `modularity_check.py --check` exit 0. The producer's "both rules modules below warn" claim is accurate for the two modules this task owns (`wide_street_wiring.py` 723→539; `named_street_override_status.py` 251). Route to a future task touching the matcher module.
2. **[ADVISORY / latent, carried from M5-T040]** The M5-T040 residual advisories (`_collapse` no NFKC-normalize; sentence-terminator/capitalized-token snapshot trust-root heuristics) live entirely in `named_street_override.py`, which this task did not touch — so they are unchanged and remain latent-for-future-callers, not regressions. No new instance introduced here.

## M5-T040 FAIL-CLOSED INVARIANTS — EXPLICIT STATUS
**ALL STILL HOLD.** `exceptions_checked` reachable only via the (now stricter) guard: HOLDS. No error→attested path: HOLDS. MATCHED_OVERRIDE/INDETERMINATE refusals + verbatim provenance: HOLD. No numeric alternate-width: HOLDS. M5-T039 M1/L1/L2 closures are unaffected (their code in `named_street_override.py` is byte-untouched). The guard and named_pending changes make the attestation strictly tighter.

## EXACT READ-ONLY COMMANDS RUN
- `git log --oneline -5`; `git rev-parse HEAD`; `git merge-base --is-ancestor {20c597c1,f6e88222} HEAD`
- `git diff 20c597c1 -- <9 T043 allowed_paths>` (all 0)
- `git show --stat f6e88222`; `git show --name-only --format="" {f6e88222,1fd07e9a}`
- `git show f6e88222 -- {wide_street_wiring.py, named_street_override_status.py (new), test_wide_street_wiring.py, test_wide_street_live_provider.py, CalculationEvidence.tsx, report-view.test.tsx}`
- `grep -nE "^(from|import)" named_street_override.py | grep wide_street/override_status` (none — no cycle); `sed __all__` of wiring; import blocks of provider + tests
- `grep -rnE "wide_street_wiring import" services/api`; symbol-leak grep (none)
- `grep -niE "not implemented|out of scope" wide_street_wiring.py named_street_override_status.py` → GREP-CLEAN (item b, AS-4)
- `grep -niE "width" named_street_override_status.py` (only "not applied numerically" prose)
- `python tools/modularity_check.py --check` → exit 0; rules modules not flagged except pre-existing `named_street_override.py`; `wc -l` = 539 / 251 / 853
- `cd services/api && python -m ruff check app/rules/{wide_street_wiring,named_street_override_status}.py` → All checks passed!
- `cd services/api && python -m pytest tests/rules/test_wide_street_wiring.py tests/rules/test_named_street_override.py -q` → **101 passed**
- `... tests/spatial/test_wide_street_live_provider.py -q` → **39 passed**
- `... tests/api tests/rules/test_rules_integration.py -q` → **475 passed** (consumers green, untouched)
- The four new DB-028 tests by node id (a/c/d/e) → **4 passed**
- `Read`: M5-T043.json, M5-T044.json packets, M5-T040-G5.md, M5-T040-G3.md, named_street_override_status.py, wide_street_wiring.py (guard + full determine_wide_street_far)

**Relevant absolute paths:**
- `C:\Users\MLFLL\Downloads\nyc-zoning\ctl24\services\api\app\rules\named_street_override_status.py`
- `C:\Users\MLFLL\Downloads\nyc-zoning\ctl24\services\api\app\rules\wide_street_wiring.py`
- `C:\Users\MLFLL\Downloads\nyc-zoning\ctl24\services\api\tests\rules\test_wide_street_wiring.py`
- `C:\Users\MLFLL\Downloads\nyc-zoning\ctl24\services\api\tests\spatial\test_wide_street_live_provider.py`
- `C:\Users\MLFLL\Downloads\nyc-zoning\ctl24\apps\web\src\components\architect\CalculationEvidence.tsx`

**G5 verdict: PASS** — no BLOCKING findings; 2 ADVISORY (both pre-existing/out-of-scope, route to discovery). The extraction is behavior-preserving, the guard/named_pending changes are strictly tighter, the (d) fix is honest, the facade re-exports the same objects with no cycle, and (h) is copy-only. All M5-T040 fail-closed invariants hold.

Note (evidence-capture division of labor): web behavior (h) proves only in CI on the pushed head; I verified it by source inspection + the added report-view assertion and defer to the orchestrator-captured CI conclusion for the web suite. All api/python evidence above I reproduced directly.
