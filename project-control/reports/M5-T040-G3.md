# M5-T040 — G3 code review (verbatim reviewer return; transport entity-decoding only)

Saved by the orchestrator from the code-reviewer agent return, 2026-09-18 (seq-118 wave,
pinned at submission sha b4865d1c). Nothing added or removed beyond decoding transport
HTML entities.

---

I have completed my independent review. I walked the full wiring truth-table, verified the provider integration, confirmed display gating, checked scope/contract preservation, ran the modularity check, and assessed test adequacy. Below is my gate report.

---

# G3 Independent Code Review — M5-T040 (named-street override WIRING + display gating)

**Reviewer:** code-reviewer (read-only) · **Reviewed SHA:** `b4865d1c` (candidate/D-024-mrl-option-b) · **Material commits:** `987930a2` (run-45), `751a1826` (run-46)
**Verdict: PASS** (6 advisory findings, 0 blocking; cohesion ruling = acceptable-with-justification, split recommended before next growth)

## Identity / scope verification
- HEAD `7b7e7faf`; `b4865d1c` is an ancestor; `git diff b4865d1c..HEAD` across `services/api` + `apps/web` is EMPTY, so the working-tree source I reviewed is byte-identical to the pinned SHA (only `project-control/tasks/M5-T040.json` moved 3 lines after submit — task metadata, not material).
- Union of files in both material commits is entirely within `allowed_paths`. No forbidden path touched.
- `ReportView.tsx` is in allowed_paths but was NOT edited (confirmed absent from both diffs). Contract files byte-untouched: `integration.py` last touched by M5-T037 (`a6875731`), NOT in either T040 commit; no `response.py`, schema, or generated `.ts` in the diffs. **AS-7 contract-byte-untouched holds.**

## (1) Wiring truth-table correctness — CORRECT
Walked every branch of `determine_wide_street_far` (wide_street_wiring.py:490-703) and `build_named_street_override_status` (:363-487):
- Branch 0 (:522) MATCHED_OVERRIDE → PROFESSIONAL_REVIEW, `far_row=none`, `exceptions_checked=False`, carries `named_street_override_match` + provenance in `reason`. Preempts the width branches (verified `test_matched_override_preempts_all_narrow_standard_row`).
- Branch 4 (:601) `named_pending` (segment_may_touch ∧ ¬override_table_implemented) → PROFESSIONAL_REVIEW, `exceptions_checked=False`.
- Branch 4b (:644) WITHIN_WIDE fires ONLY when not matched-override, all decisions wide/narrow, not named_pending, buffer computed ∧ intersects — exactly the states where `_elevated_exceptions_checked` (:269-290) also returns True.
- No path where an exception, empty list, or None sneaks past as attested: `build_named_street_override_status` empty-list (:403) and INDETERMINATE/non-fully-resolved NOT_MATCHED (:451-463) both fail closed to `segment_may_touch=True, override_table_implemented=False`. `_fully_resolved_typed_inputs` (:335-360) re-establishes typed/normalized inputs independently of the matcher's coercing NOT_MATCHED short-circuit. No numeric alternate-width is ever applied (`select_far_row_value` returns `wide_street_far` only on FAR_ROW_WIDE_STREET, which branch 0 never emits). **AS-4 satisfied.**
- Provider fail-safe is now genuinely improved: the run-46 diff REPLACED a hand-built `NamedStreetOverrideStatus(override_table_implemented=False, segment_may_touch_named_override=False, …)` (a contradictory combo that formerly relied solely on the EC-5 gate) with `_named_street_override_status`, which feeds queries with `community_district=None` → every `match()` returns INDETERMINATE (matcher.py:700-701, cd checked before rows) → unresolved status → named_pending=True. Two independent fail-safes now route a wide segment to professional review (verified `test_wide_segment_yields_professional_review_never_fabricated_wide`).

## (2) MODULARITY / COHESION RULING — ADVISORY (acceptable-with-justification; split recommended before next growth)
`python tools/modularity_check.py --check` → **failures 0; warnings 22**; `wide_street_wiring.py` flagged "above the warning threshold" (file is 723 SLOC; warn 600 / justify 750 / hard 1000 — so it is in WARN, below justify). Ruling: the named-street attestation logic added this task (`MatchedNamedStreetOverride`, `_fully_resolved_typed_inputs`, `build_named_street_override_status`, the matched-override branch) is **cohesive here** — it produces the `NamedStreetOverrideStatus`/`MatchedNamedStreetOverride` that `determine_wide_street_far` and `_elevated_exceptions_checked` consume in the same attestation seam; they change together, and the module mixes NO unrelated concern (no storage, serialization, external I/O, CLI/API wiring, or presentation — the matcher itself remains a separate module, consumed read-only). Acceptable for this task. HOWEVER the file is now one increment (27 SLOC) from the 750 justify threshold, and `build_named_street_override_status` + `_fully_resolved_typed_inputs` + `MatchedNamedStreetOverride` form a clean extraction seam. **Recommendation (advisory, route to the next task touching this module):** extract a `named_street_override_status.py` submodule with a compatibility re-export BEFORE adding the alternate-width leg or the future CD/cross-street connector, rather than crossing 750 in place.

## (3) Provider status construction (+70) & DB-021(e) — CORRECT
`_named_street_override_status` (wide_street_live_provider.py:167-206) and `_named_street_matcher` (`@lru_cache(maxsize=1)`, :159-164) are sound; lru_cache does not cache the load exception, so a transient load failure retries fail-safe. DB-021(e) branches directly tested: `test_db021e_unexpected_wide_geometry_error_returns_none` (segment_geometry_unexpected_error), `test_db021e_attested_lot_rejects_every_unusable_subbranch` + `_accepts_usable_single_valid_lot`, `test_db021b_wide_segments_over_cap_returns_none_before_geometry_fetch`. End-to-end exercised by `test_all_narrow_neighborhood_yields_confident_not_within` and the wide-segment professional-review test.

## (4) Display gating — CORRECT (DB-025 a-c)
`WideStreetResult` (DevelopmentLimits.tsx:62-96) and CalculationEvidence.tsx:16-26,41-44 gate strictly on `determination_state` (`review`/`within`), never on a null-FAR heuristic. Not-within → heading + caption + `aria-label` = "Governing floor-area ratio" (no conditional-FAR heading over the conservative value); within → "Wide-street conditional FAR"; review → conditional-FAR heading over a withheld value + the "Professional review required" line, which appears ONLY when `wideReview`. The static panel banner at CalculationEvidence.tsx:30 is the overall DRAFT panel status (whole family is DRAFT pending G6), correctly distinct from the wide-street-specific gated label at :42. **AS-6 satisfied.**

## (5) Report-view test repair — SOUND (not masking an a11y regression)
`ReportView` nests `CalculationEvidence` inside a closed `<details>` (pre-existing; ReportView.tsx untouched this task), so its `<h3>` is outside the a11y tree until expanded — standard progressive disclosure, not a regression. The repair restores the missing `within` import (report-view.test.tsx:7 — the file was RED/uncompilable before) and asserts the heading with `getByRole("heading", {hidden:true})` (correctly includes collapsed-details content) plus value captions via `toHaveTextContent` (textContent is unaffected by the closed details). The visible case is covered separately on the screen surface via `getByRole("region", …)` without `hidden`. Sound.

## (6) Scope coherence — CONFIRMED
ReportView.tsx unchanged while its parity tests changed is coherent: the DB-025 gating lives inside the two shared child components (`DevelopmentLimits`→`WideStreetResult` and `CalculationEvidence`) that ReportView renders, so screen/report parity is structural with a single data path — the component needed no edit.

## Test adequacy
Not weak. Wiring suite (35 tests) uses real matcher oracles (`matcher.match(query).status is MatchStatus.X  # oracle`) and asserts all four provenance elements individually (test_wide_street_wiring.py:718-721) plus their presence in `reason` (:747-749). Matcher suite (46) covers DB-023a metadata-bypass + narrowed-quote + all-signals-stripped + complete-span, DB-023b construction fail-closed (parametrized), DB-023c bounded reprs. Provider suite (24) covers the DB-021 caps and unexpected-error branches.

## ADVISORY findings (non-blocking)
1. **ADVISORY** wide_street_wiring.py:48-56 — module docstring still says the named-street override table is "OUT OF SCOPE here (follow-up)"; the matcher is now consumed via `build_named_street_override_status`. Stale doc (the `NamedStreetOverrideStatus` dataclass docstring WAS updated; the module header was not).
2. **ADVISORY** wide_street_wiring.py:608-614 — the branch-4 `named_pending` reason says the override table is "not implemented here (follow-up)"; the matcher IS now consulted but could not resolve (missing CD/cross-street bounds). The user-facing reason slightly understates what happened; no safety impact (still professional review).
3. **ADVISORY** wide_street_wiring.py:285-290, 509-512 — `named_pending`/`_elevated_exceptions_checked` trust the (`segment_may_touch_named_override`, `override_table_implemented`) boolean pair. A dishonestly hand-built status with `segment_may_touch=False ∧ override_table_implemented=False` would bypass the guard. The sole production constructor (`build_named_street_override_status`) never emits that combo, so not exploitable in-tree; consider making the guard require `override_table_implemented=True` for the safe path to harden the contract.
4. **ADVISORY** wide_street_wiring.py:290 + branch 1 (:537) — `all(())` is True, so `exceptions_checked` can be reported True on the no-policy-decisions PROFESSIONAL_REVIEW outcome. `far_row=none` there, so no FAR is ever overstated; cosmetic attestation-honesty nit only.
5. **ADVISORY** wide_street_live_provider.py:193-206 — the matcher-load-failure fail-safe branch has no direct test (grep found no matcher-load/cache_clear test in the provider suite). Defensive branch; outcome equals the happy-path unresolved status.
6. **ADVISORY (cohesion ruling)** — see (2): acceptable-with-justification now; extract a `named_street_override_status.py` submodule (with facade re-export) before the next substantial growth crosses the 750 justify threshold.

## Read-only commands run
- `git log --oneline -5 b4865d1c`; `git show --stat --oneline 987930a2 751a1826`; `git show b4865d1c --stat`
- `git rev-parse HEAD`; `git merge-base --is-ancestor b4865d1c HEAD`; `git diff --stat b4865d1c HEAD -- services/api apps/web project-control/tasks/M5-T040.json`
- `git show 751a1826 -- <wide_street_wiring.py | CalculationEvidence.tsx | report-view.test.tsx | development-limits.test.tsx | wide_street_live_provider.py>`
- `git log --oneline -1 b4865d1c -- services/api/app/rules/integration.py`
- `python tools/modularity_check.py --check` (failures 0; warnings 22) and `--report`
- `Grep` over test_wide_street_wiring.py / test_named_street_override.py / test_wide_street_live_provider.py (truth-table, provenance, DB-023 a/b/c, DB-021b/e defs and counts)
- `Read` of wide_street_wiring.py, named_street_override.py, wide_street_live_provider.py, CalculationEvidence.tsx, DevelopmentLimits.tsx, integration.py:520-590, task packet, producer report, ci-evidence

Note on CI: I could not run `gh`/web suites (thin-client; web proves only in CI). Per the evidence-capture division of labor I verified the orchestrator-captured `M5-T040-ci-evidence.md` (CI run 35407153078 success @ `4d8847c6`; `git diff 751a1826..4d8847c6 -- apps/ services/` empty, so the green run speaks for the material identity) and independently reproduced modularity (0 failures) locally.

**Recommendation to orchestrator: record G3 PASS. The six advisories are non-blocking; route findings 1-2 (doc/reason staleness) and finding 6 (extraction) to the next task touching this module.**

Relevant files (absolute):
- `C:\Users\MLFLL\Downloads\nyc-zoning\ctl24\services\api\app\rules\wide_street_wiring.py`
- `C:\Users\MLFLL\Downloads\nyc-zoning\ctl24\services\api\app\rules\named_street_override.py`
- `C:\Users\MLFLL\Downloads\nyc-zoning\ctl24\services\api\app\spatial\wide_street_live_provider.py`
- `C:\Users\MLFLL\Downloads\nyc-zoning\ctl24\apps\web\src\components\architect\DevelopmentLimits.tsx`
- `C:\Users\MLFLL\Downloads\nyc-zoning\ctl24\apps\web\src\components\architect\CalculationEvidence.tsx`
- `C:\Users\MLFLL\Downloads\nyc-zoning\ctl24\apps\web\src\components\architect\__tests__\report-view.test.tsx`
- `C:\Users\MLFLL\Downloads\nyc-zoning\ctl24\services\api\tests\rules\test_wide_street_wiring.py`
