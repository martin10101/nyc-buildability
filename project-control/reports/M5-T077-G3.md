# M5-T077 — G3 independent code review (code-reviewer "cr-t077", read-only)

> Transmission history: pinned at 71ab388a (material f63cfad1 blobs byte-identical; later commits are
> disjoint orchestrator seams), delivered as ONE SendMessage ending with END-OF-REPORT; no truncation.
> Saved verbatim by the orchestrator (transport wrapper tags removed only). Identity carry to the gate
> head: `git diff f63cfad1 HEAD -- <the 5 allowed_paths>` is empty at 70c29c5a.

---

M5-T077 G3 code review (code-reviewer, read-only). PINNED HEAD = 71ab388a5c5570f2d56a29b11d4c15ab6eb5b5f0 (recorded at start). The 4 material blobs are byte-identical f63cfad1→HEAD (later commits are disjoint orchestrator seams); reviewed those blobs. Ran from services/api on local Python 3.11.9.

VERIFIED
- Scope/AS-5: f63cfad1 touches EXACTLY the 5 allowed paths, no forbidden path. max_envelope.py byte-UNTOUCHED — `git diff b56f3d5b HEAD -- .../max_envelope.py` empty. ruff clean (exit 0). Route UNMOUNTED — main.py has no max_envelope reference. No dependency/lockfile change. Scoped suite `test_lot_geometry_derivation.py + test_max_envelope_api.py -q` = 64 passed locally.
- AS-1 single-null contract (correct): `_should_derive_lot_geometry` (max_envelope_api.py:162-183) returns True ONLY for absent field or empty-list PLUS usable scalar BBL; `null` and every other non-list value fall to `return False` and ride `_build_lot_context`'s typed 422. Confirmed in source: proposal_checks_api.py:358 (`lot.get("lot_line_segments", [])` → absent defaults to [], null stays None) → :359-362 raises field `lot.lot_line_segments`; that function never reads lot.bbl (:338-415), so the non-scalar-bbl 200 gap is real. Both docstrings accurate (max_envelope_api.py:167-174; lot_geometry_derivation.py:15-19) — no text claims null==absent.
- AS-2 falsy-net: parametrize extended to `["abc",42,{"id":"L-S"},True,0,"",{},False,0.0]` (test:700-733); each 422, no provider resolved/called, byte-identical w/ and w/o BBL.
- AS-3 taxonomy: BBL_UNRESOLVABLE unit detail now pinned non-empty + named (test_lot_geometry_derivation.py:442-453); route-detail threading asserted for 5 failure classes (no_feature/multiple/invalid :571-588, connector_fault :591-610, bbl_unresolvable :613-630) — reason reaches placement.detail; append branch (max_envelope_api.py:435-447) is outcome-agnostic.
- AS-4: at-cap pinned (`>` guard, count==max derives, +1 over_cap; test:423-439); non-scalar bbl {dict}/[list] → 200 honest gap, provider never RESOLVED via spy, no derived block (test:646-665).

MUTATION (live, in-process rebind, no writes)
- (b) re-admit None → test_null_segments_refuse... = 1 failed (RED). ✓
- (a) idiomatic `if lot.get(...): return False` → test_malformed... = 5 failed (the 5 falsy) / 4 passed. ✓ Matches producer's recorded runs exactly. (c) blank-detail and (d) `>`→`>=` are trivially-correct guards by inspection on stable code + producer-recorded; not re-run.

Producer judgment calls — both SOUND: rename absent-or-null→absent-or-empty test (null now covered by the dedicated 3-state refusal) accurately reflects the dropped-None ruling; the additive lot_geometry_derivation.py module-docstring paragraph is accurate, additive, behavior-neutral (no false claim existed to "correct").

FINDINGS
- F1 (ADVISORY, non-blocking; completeness) test_max_envelope_api.py route pack. GEOMETRY_OVER_CAP is the one fail-closed outcome with no route-level placement.detail assertion; the append branch is outcome-agnostic and proven by the 5 covered classes, and over_cap's unit detail IS pinned. Matches G4's disposition; route is unmounted and over_cap has no consumer (DB-051(f)/(g), mount packet).
- F2 (ADVISORY, LOW, non-blocking) max_envelope_api.py:444-446. The detail-append reads `placement['detail']` directly (not `.get`) in the un-try/excepted response-assembly section; safe because the engine guarantees a detail on lot_geometry_unsupported placements (tested), a `.get` would be belt-and-suspenders only.

No BLOCKING findings. Directive verification (D-066/D-087) left to the DCV per gate split.

M5-T077 G3 VERDICT: PASS

END-OF-REPORT
