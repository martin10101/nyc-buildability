# M5-T037 — G1 data-contract review return (VERBATIM; data-contract-verifier, read-only, pinned a6875731)

Orchestrator note: returned complete (untruncated) via the agent-return channel 2026-09-18
~10:13Z. VERDICT: PASS (one non-blocking display-copy finding F1, routed to the HJ pass and
the discovery backlog).

---

# G1 DATA-CONTRACT REVIEW — M5-T037 (rule_evaluation v1.1.0 + DB-020 + display + report parity)

## VERDICT: PASS (one non-blocking display-copy finding; no contract defect)

Reviewed at pinned material commit `a6875731`; HEAD `fa2aecab`. The core contract change (schema both copies, serialization, DB-020 digest fix, generated TS, web validator) is correct and honest, and DB-020 closes the M5-T035 G1 F1 finding exactly. The one finding (F1) is a UI-copy imprecision in CalculationEvidence, not a contract or serialization defect; it never over-grants a FAR or overstates certainty.

## Identity / scope (verified)
- All 20 packet allowed_paths are BYTE-STABLE `a6875731..HEAD` (`git diff` empty). Reviewed at the pinned identity.
- The only later material commit `be0a7062` touches exactly one file — `services/api/tests/scenario/test_scenario_contract.py` — which is OUTSIDE the packet's allowed_paths. Legitimate `[ORCH-CORRECTED]` out-of-scope consumer sweep (contract-guard admitting the sanctioned enum); does not affect the reviewed identity.
- Independent read-only reruns: `generate_ts_types.py --check` EXIT 0 ("up to date" incl. rule_evaluation + client SUPPORTED_CONTRACT_VERSIONS); `tools/modularity_check.py --check` EXIT 0 (failures 0).

## (1) SCHEMA — PASS
- Both copies byte-identical: `packages/contracts/schemas/v1/rule_evaluation.schema.json` and `services/api/app/_contract_schemas/v1/rule_evaluation.schema.json` share git blob `0f2eebd6…` (identical content).
- `contract_version` enum = `["1.0.0", "1.1.0"]` (schema L33) — 1.0.0 RETAINED, 1.1.0 appended; enum stays closed.
- `wide_street` is OPTIONAL: `$ref` added at L146 but NOT added to the top-level `required` array (L8-29, unchanged 20 keys). Top-level `additionalProperties:false` (L7) preserved.
- `$defs/wide_street` (L450) is `additionalProperties:false` (L452) with all 15 D-052 keys in its own `required` (L454-470). The block is closed and fully-required within.
- Hunk-by-hunk diff (`a6875731~1..a6875731`) shows ONLY additions: the enum value, the `$ref`, and the new `$def`. No existing key/enum/required-set weakened anywhere.

## (2) SERIALIZATION HONESTY — PASS
- `integration.py` `PropertyRuleEvaluation.as_dict` (L160) emits `document["wide_street"]` ONLY under `if self.wide_street_determination is not None:` (L193-194); otherwise the key is absent (1.0.0-shaped body).
- `_wide_street_summary` (integration.py:557-573) emits EXACTLY the 15 keys the schema `required` array names (cross-checked one-to-one), so a real determination satisfies `additionalProperties:false` and passes validate-before-send. `governing_max_residential_far`=fold value/None matches schema number|null.
- `response.py` `RULE_EVALUATION_CONTRACT_VERSION = "1.1.0"` (L59) emitted for EVERY document (L160); the block flows through via `**payload` (L169). `validate_rule_evaluation_document` (L207) does strict Draft-2020-12 validation against the bundled schema before send; the endpoint calls both serialize + validate.
- Unit proof (`tests/rules/test_rules_integration.py`): block present on within-wide fold (R6 + determination), ABSENT with no determination (R6), ABSENT for non-conditional R5 + determination; `export()` carries the same block, JSON-serializable, no Verified.
- Endpoint proof (`tests/api/test_rule_evaluation_api.py` `test_m5t037_as3_…`): within-wide → block present + valid on BOTH schema copies; provider None → block ABSENT, contract_version 1.1.0, still valid on both copies.

## (3) DB-020 (closes M5-T035 G1 F1) — PASS
- `wide_street_live_provider.py` `_attested_wide_segments` now walks `geom_result.pages` (L347) and sets `source_raw_digest=page.raw_digest` (L362) — the geometry-PAGE raw-body sha256, NEVER `entry.segment.streetwidth_raw`. Diff confirms the old value was `source_raw_digest=entry.segment.streetwidth_raw` on the flattened `geom_result.entries`.
- Type-backed: `dcm_street_centerline_geometry.py` — `SegmentGeometryQueryResult.pages: list[SegmentGeometryPage]` (L360); `SegmentGeometryPage.raw_digest: str` (L343) = `raw_body_digest(transport.body)` (L475). Real per-page raw-body digest, not fabricated.
- Lot side UNCHANGED-correct: `source_raw_digest=getattr(lot_result, "raw_digest", None)` (L269).
- Regression test `test_db020_attested_segment_carries_geometry_page_digest_not_width_text`: asserts `re.fullmatch(r"sha256:[0-9a-f]{64}", digest)`, `digest == raw_body_digest(_dcm_page_body(features))`, `digest != "80"`, and `streetwidth_raw == "80"` (source preserved untouched).
- Cross-checked `project-control/reports/M5-T035-G1.md` F1 (L44): it named `:356 source_raw_digest=entry.segment.streetwidth_raw` receiving the width text and recommended `SegmentGeometryPage.raw_digest`. The fix implements exactly that (per-page, even more precise than the flat `raw_digests`). F1 is CLOSED.

## (4) GENERATED TS — PASS
- `generate_ts_types.py --check` clean (deterministic generation matches; EXIT 0).
- `packages/contracts/generated/rule_evaluation.ts`: `contract_version: "1.0.0" | "1.1.0"` (union); optional `wide_street?` block with all 15 keys and types mirroring the schema (determination_state/far_row enums, `number | null`, `string[]` and `(string | null)[]` arrays). `_expected_failure?` still optional.

## (5) WEB VALIDATOR — PASS
- `apps/web/src/lib/rule-evaluation-contract.ts`: `RULE_EVALUATION_CONTRACT_VERSIONS = ["1.0.0","1.1.0"]` (L103, typed to `RuleEvaluation["contract_version"]`); the old `!== "1.0.0"` check is replaced by `checkEnum(...)` so exactly those two admit (out-of-enum e.g. "2.0.0" rejected — proven in `contract-versions.test.ts`).
- `checkWideStreet` (L337): `if (value === undefined) return;` (absent = valid); every present key shape-checked (enums, number|null, non-empty strings, booleans, string / string-or-null arrays); invoked at L460 BEFORE the failure gate, so a malformed block fails TOTAL validation. Tests confirm (`contract-versions.test.ts`: bad `determination_state` + string `governing_max_residential_far` → `ok:false` with the two problem paths).
- No undocumented key is admitted/invented (all checked keys exist in the schema).
- Note (not a defect): the web validator is lenient on EXTRA unknown keys (no client-side `additionalProperties` rejection). This is defense-in-depth only — the SERVER schema enforces `additionalProperties:false` and validates before send. Extra-key rejection IS proven server-side on BOTH copies: `test_m5t037_as2_schema_rejects_undocumented_extra_key_in_block_and_at_root` rejects an extra key inside the block AND at the root via both the canonical fixture validator and the bundled runtime validator (`RuleEvaluationContractError`).

## (6) PROVENANCE DISPLAY — PASS with one non-blocking finding
Correct:
- `DevelopmentLimits.tsx` `WideStreetResult`: renders nothing when no block (no invented value); shows WHAT (conditional FAR row), UNITS ("a dimensionless ratio. Floor area = FAR × zoning-lot area (sq ft)"), CONDITIONS ("within 100 ft of a wide street" / conservative-governs otherwise), DRAFT marker (`{draft_label} · Pending qualified legal review (not verified)`), and a SOURCES link; `professional_review_required` → honest escalation with the higher FAR WITHHELD ("Professional review required — the higher … ratio is withheld…"), never presented as computed.
- `CalculationEvidence.tsx`: surfaces the D-052 quintuple through the EXISTING `CapturedRecord` affordance (`value={evaluation.wide_street}`) without re-labeling; governing FAR labeled dimensionless-ratio (or "withheld — professional review required"); fallback_direction_note and determination_state/far_row/exceptions_checked/named_street_override_pending shown; reason shown. Report parity (`report-view.test.tsx`) proves byte-equal `wide-street-result` on screen and report from one shared document and surfaces `OBJECTID=12345` in the provenance panel; 1.0.0 docs get no block on either surface.

**FINDING F1 (NON-BLOCKING — display-copy precision, `CalculationEvidence.tsx`).** The provenance panel status line renders `Draft · {draft_label} · Professional review required` UNCONDITIONALLY for every wide_street block. But only `determination_state == "professional_review_required"` is actually a professional-review state; a confident `not_within_100ft_of_wide_street` (a real production outcome for an all-narrow lot per the provider docstring L52-54) and a confident `within_100ft_of_wide_street` are NOT — they yield conditional coverage. So the panel labels 2 of 3 states as "Professional review required" when they are not. It errs over-cautious (never over-grants FAR, never overstates certainty), the accurate `determination_state` is shown in the same panel, and the DevelopmentLimits headline distinguishes states correctly, so it is not a contract or safety defect. Recommendation: make the CalculationEvidence status line conditional on `determination_state` (mirror `WideStreetResult`). Flagging for the human-journey (G3) reviewer as the copy-honesty owner. No blocking action for G1.

## Acceptance scenarios (evidence-bound)
- AS-1 (digest) PASS — provider regression test above.
- AS-2 (contract: byte-identical copies, enum, block validates, extra-key fails) PASS — schema blob equality + endpoint extra-key rejection on both copies + typegen --check.
- AS-3 (serialize present/absent) PASS — integration + endpoint tests, both validators.
- AS-4 (consumers) — producer/CI evidence: `tests/api` 426 passed at harvest; CI all 20 green at `be0a7062` per `project-control/reports/M5-T037-ci-evidence.md` (orchestrator-captured; I did not re-run the full suite).
- AS-5/AS-6 (web display + report parity) — tests present and correct by inspection; proven only in web/web-e2e CI on the pushed head (thin-client) — orchestrator-captured, green per the CI evidence.
- AS-7 (CI green) — orchestrator-captured (all 20 green at corrected head).
- AS-8 (preservation) PASS — no *.rule.json touched; DRAFT/not_verified assertions intact; endpoint tests assert "verified" absent; the higher FAR is withheld on uncertainty.

## Preservation / provenance (D-045-R009, D-051, D-073-R003) — HOLDS
DRAFT-pending-G6 marker present on both surfaces; no published/verified/compliance claim introduced; D-051 fallback direction (higher FAR withheld on uncertainty) honored and labeled; report renders from the same validated document (structural parity, no second data path).

Recommendation: ACCEPT G1. Track F1 as a UI-copy tightening for the G3/human-journey pass; it does not block the contract gate.

---

## Delta-attestation (VERBATIM; delivered ~10:21Z — rework 75daaebd → head 8240a497)

# M5-T037 G1 delta-attestation — PASS carries to 8240a497

Verified read-only (`git show 75daaebd`, range diff `a6875731..8240a497`). **My G1 PASS carries unchanged to 8240a497.**

## Evidence
- `75daaebd` is an ancestor of `8240a497` (confirmed via `git merge-base --is-ancestor`).
- Rework `75daaebd` touches exactly 3 display files: `DevelopmentLimits.tsx` (+9/-6), `ReportView.tsx` (+6/-2... net +5), `report-view.test.tsx` (+54). No other code file in the range except the already-noted sanctioned out-of-scope `test_scenario_contract.py` (`be0a7062`); the rest of the range is M5-T038 + control-plane files.
- **Contract surface BYTE-STABLE `a6875731..8240a497` (diff empty):** both schema copies, `generated/rule_evaluation.ts`, `rule-evaluation-contract.ts`, `integration.py`, `response.py`, `wide_street_live_provider.py`, all backend tests, `contract-versions.test.ts`, and `CalculationEvidence.tsx`. My items (1) schema, (2) serialization, (3) DB-020, (4) generated TS, (5) web validator are entirely unaffected.

## Item (6) provenance-display — delta reviewed, still PASS (strengthened)
- **F2** (`DevelopmentLimits.tsx`): the calm panel drops the raw `draft_label`/`reason`/`fallback_direction_note` server strings and shows a clean static "Draft — pending qualified legal review (not verified)." The DRAFT marker is preserved and the FAR row/units/conditions are unchanged. The full D-052 provenance (including `fallback_direction_note` and `reason`) still surfaces via `CalculationEvidence` (byte-stable, `CapturedRecord`), so nothing is lost — answer-first/detail-on-demand is improved, not weakened. New jargon-guard test asserts D-045-R009/D-051/needs_review/enum tokens never reach the calm panel while "3.44" and the clean draft line remain.
- **F1** (`ReportView.tsx`): now feeds `DevelopmentLimits` the inspectability-GATED `evaluation` (matching `CalculationEvidence` and every screen surface) instead of `identityEvaluation`, so the report withholds the wide-street panel for a non-inspectable document exactly as the screen does. New guard test proves screen/report agree on a non-inspectable-with-block document. This strengthens the D-073-R003 report-parity property I assessed.

## On my original G1 F1 (unchanged)
My non-blocking F1 (the `CalculationEvidence.tsx` status line hard-coding "Professional review required" for confident within/not-within states) is untouched by this delta — `CalculationEvidence.tsx` is byte-stable — and correctly remains a recorded backlog advisory. Note it is a DIFFERENT item from the HJ/G3 F1 (the report's ungated feed), which this rework fixed; do not conflate them.

## Caveat
Web behavior (the changed components + the two new report-view guard tests) proves only in web/web-e2e CI on the pushed head (thin-client) — orchestrator-captured. My attestation is that the contract surface is byte-stable and the display delta preserves/strengthens item-6 honesty and parity.

VERDICT: G1 PASS carries to 8240a497.


---

## Carry confirmation to 1c89922c (VERBATIM; delivered 10:23:26Z)

G1 PASS carries to 1c89922c. Verified read-only: `8240a497..1c89922c` is only the one test-assertion casing alignment (`development-limits.test.tsx:563`, "Pending" -> "pending qualified legal review") plus control-plane files; the entire contract surface (schemas, generated TS, web validator, integration.py, response.py, wide_street_live_provider.py, all three display components) is byte-stable (empty diff). No re-review needed.
