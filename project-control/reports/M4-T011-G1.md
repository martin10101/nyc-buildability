<!-- Reviewer return preserved VERBATIM by the orchestrator (report-preservation rule,
.claude/rules/project-control.md). Reviewer: independent code-reviewer subagent (G1),
returned 2026-09-12, reviewed HEAD e919d9ec. -->

All verification complete. Every claim in the packet and producer report reproduced against actual source. Here is my gate report.

---

# Gate Report

- Gate ID: G1
- Task ID: M4-T011
- Reviewer: code-reviewer (independent G1)
- Producer: qa-engineer/orchestrator (commit 40cd4544)
- Result: **PASS**
- Reviewed HEAD: e919d9ec68953f10a0f1d65ffb99fa868bc59bd4 (branch candidate/D-024-mrl-option-b)
- Clean environment/worktree used: read-only review of the frozen HEAD; producer material commit 40cd4544 confirmed byte-identical to HEAD for the four test modules (`git diff 40cd4544 e919d9ec -- services/api/tests/api/` is empty; only control-plane files changed after 40cd4544).

## Acceptance criteria reviewed

**S1 — red/green on the recorded failures: PASS (mechanically coherent).** The diff repairs exactly six sites, matching the six recorded CI failures. Four were `KeyError 'max_residential_floor_area_sq_ft'` (reading `[0]["outputs"][...]` when index-0 is now a not_applicable member with empty outputs): evidence AS-1, evidence AS-2, scenario_api AS-1, scenario_analysis_api AS-1. Two were `assert 5 == 1` (`len(evaluations) == 1`): rule_evaluation `test_as3...` and `test_m2t020_s2...`. Each edited site is precisely one of these. GREEN (371 = 365 + 6) is the CI authority at the pushed head per packet discipline; I did not run pytest (forbidden by the task).

**S2 — value assertions unchanged: PASS.** Read the full `git show 40cd4544`. Every value literal is preserved: `1.5`, `TRACE_CAP`, `15000.0`, `10000.0`, `"R5"`, `COVERAGE_CONDITIONAL`, the `"23-21"` exception, `verified_eligible is False`, all citation/coverage byte-equality checks. The only changes are (a) the added helpers, (b) `evaluations[0]`→`_applicable_trace(...)` / `rule_citations[0]`→`_applicable_group(...)` selection swaps, and (c) the count-assertion form `== 1`→`== len(doc["family_coverage"]["rule_ids"])` — which S2 explicitly permits. No value drift disguised as selection.

**S3 — selection by applicability, never position: PASS.** All four `_applicable_trace` copies and the evidence `_applicable_group` filter on `applicability_outcome is True`, `assert len(applicable) == 1` with the offending `sorted(rule_id...)` as the message, and return `applicable[0]` only after the exactly-one assertion — never a silent index-0. This is the same idiom M4-T009 applied to the tests/rules siblings (`test_rules_integration.py:192-196`, `:215`). The `is True` identity form is marginally stricter than the sibling's truthy form and equivalent for a schema-typed boolean.

**S4 — count survives family growth: PASS.** The count keys on `len(doc["family_coverage"]["rule_ids"])`, the document's own family list — never a hardcoded 5. Verified `family_coverage.rule_ids` exists and is populated on the confident path in `app/rules/registry.py:209-214` (the conditional branch), and `app/rules/integration.py:600-605` appends exactly one trace per `rule_ids` entry, so `len(evaluations) == len(rule_ids)` holds by construction. (Observation, not a defect: on this code path the equality is structurally guaranteed rather than an independent invariant; but it is exactly the growth-proof, in-document form S4 directs, and strictly better than the old `== 1`.)

**S5 — scope discipline: PASS.** `git show 40cd4544 --stat` = exactly the four named test modules + the producer report, all within the packet's exact-file `allowed_paths`. Nothing under `services/api/app/**` (forbidden). `git diff 40cd4544 e919d9ec` touches only `project-control/**`. Test-only change → the modularity policy (production source) is not implicated; ~12-line helpers cross no threshold.

## Directive/requirement verification

Task is in-regime (`directive_refs: D-038 ALL`). Per ADR-005 / the skill, the formal per-requirement verification of D-038 is performed by the independent `directive-compliance-verifier` into `verification.json` (producer ≠ verifier); that is a separate pass from this G1 code review and is a precondition the orchestrator enforces at `accept()`. From the code-review vantage I confirm the diff does not conflict with D-038 (product pivot): it turns the M4 rule-family api CI job green so the R1–R12 family can be accepted, which is product-delivery work.

| Requirement ID | Reviewed SHA / content identity | Verdict | Reproduced evidence |
|---|---|---|---|
| D-038 (ALL) — formal per-requirement pass | e919d9ec | DEFERRED to directive-compliance-verifier | Out of G1 code-review scope; no code-level conflict observed (test-only, product-supporting change) |

## Steps independently executed

- `git show 40cd4544` (full diff, all four modules) and `git show 40cd4544 --stat`.
- `git diff 40cd4544 e919d9ec` and `... -- services/api/tests/api/` (confirmed test modules unchanged since producer commit).
- Read `app/api/v1/evidence.py` — group construction (lines 340-352): each `rule_citations` group is `{**copy.deepcopy(trace), "claim_verification_status": ...}`, so `applicability_outcome` (a required `evaluation_trace` field) travels with each group → `_applicable_group`'s filter is valid and "provably tied to the applicable trace" per S3.
- Read `app/rules/integration.py:596-653` (evaluation loop, family_coverage composition) and `app/rules/registry.py:199-214` (family_coverage.rule_ids).
- Read `app/scenario/builder.py:98-111` (`_find_residential_far_trace` returns first applicable — production selection correct, as the packet claimed).
- Read `app/rules/evaluator.py:660-769` to validate the untouched-site rationale.
- Read the two untouched sites: `test_rule_evaluation_api.py:407` and `test_evidence_api.py:1132` + the zip loops at `test_evidence_api.py:461, 520, 1133`.

## Expected versus actual

- Evidence group carries `applicability_outcome`: expected (needed by `_applicable_group`) — actual: confirmed at `evidence.py:347-351`. Match.
- Exactly one applicable trace on confident R5: expected — actual: `test_rules_integration.py:186-190` documents disjoint per-district applicability sets, `:215 assert sum(...) == 1`. Match; the helper's `assert len==1` will not spuriously fire.
- Untouched `test_as6` (`evaluations[0]["outputs"] == {}`): expected order-independent — actual: on the missing-lot-area path the evaluator returns at `evaluator.py:689-698` with **no** `outputs=` (defaults empty) even for the applicable R5 member (its applicability predicate is district-only, so `applicability_outcome=True` but outputs empty), and non-applicable members short-circuit at `:706-714` also with empty outputs. So every member has `outputs == {}`; positional access is safe regardless of ordering. The producer's "judged, not missed" rationale is accurate.
- Untouched zip/len==len sites (`test_evidence_api.py:461, 520, 1132-1133`): whole-list, order-preserving transport-fidelity checks (rule_citations built in evaluations order), growth-proof (5↔5). Correctly left untouched; not single-member assumptions.

## Evidence paths

- C:/Users/MLFLL/Downloads/nyc-zoning/ctl24/services/api/tests/api/test_rule_evaluation_api.py
- C:/Users/MLFLL/Downloads/nyc-zoning/ctl24/services/api/tests/api/test_evidence_api.py
- C:/Users/MLFLL/Downloads/nyc-zoning/ctl24/services/api/tests/api/test_scenario_api.py
- C:/Users/MLFLL/Downloads/nyc-zoning/ctl24/services/api/tests/api/test_scenario_analysis_api.py
- C:/Users/MLFLL/Downloads/nyc-zoning/ctl24/services/api/app/api/v1/evidence.py (group construction verified)
- C:/Users/MLFLL/Downloads/nyc-zoning/ctl24/services/api/app/rules/integration.py (evaluation loop / family_coverage)
- C:/Users/MLFLL/Downloads/nyc-zoning/ctl24/services/api/app/rules/registry.py (family_coverage.rule_ids)
- C:/Users/MLFLL/Downloads/nyc-zoning/ctl24/services/api/app/rules/evaluator.py (missing-input empty-outputs path)
- C:/Users/MLFLL/Downloads/nyc-zoning/ctl24/services/api/app/scenario/builder.py (production applicable-trace selection)
- C:/Users/MLFLL/Downloads/nyc-zoning/ctl24/services/api/tests/rules/test_rules_integration.py (mirrored M4-T009 idiom + exactly-one invariant)
- C:/Users/MLFLL/Downloads/nyc-zoning/ctl24/project-control/reports/M4-T011-producer-report.md

## Human-style walkthrough findings

N/A — no UI surface; test-only adaptation.

## Regression/security/provenance findings

None. No production code changed, so no behavior, provenance, or security surface moved. The change strengthens the tests against silent index-0 selection (fails loudly on zero/multiple applicable members). No provenance or contract value altered.

## Defects

None (blocking).

Non-blocking observations (no action required):
1. The S4 count assertion is structurally guaranteed equal on the confident path (integration.py appends one trace per rule_id), so it is a growth-proof coherence check rather than an independent invariant. This is exactly the form S4 directs and is superior to the old hardcoded `== 1`; recorded only for transparency.
2. Producer report cites the untouched len==len site as `test_evidence_api.py:1108`; the actual assertion is at `:1132` (line numbers shifted by the +24-line helper block; `:1108` now falls in the `test_as8` docstring). Cosmetic citation imprecision; the substance is correctly identified and genuinely passes.

## Required rework

None.

## Reviewer conclusion

The change is a disciplined, selection-only de-brittling. Every expected value is byte-identical; selection is by `applicability_outcome is True` with a loud exactly-one assertion mirroring the accepted M4-T009 tests/rules idiom; the count follows the document's own `family_coverage.rule_ids`; the evidence `_applicable_group` is sound because the endpoint copies the whole trace (carrying `applicability_outcome`) into each group; the two untouched sites genuinely pass under the five-member family (verified from the evaluator's empty-outputs path and the whole-list transport checks) and leaving them is correct scope discipline. Scope is exactly the four allowed test modules plus the report — nothing under `services/api/app`. Red/green is mechanically consistent with the diff.

**VERDICT: PASS** (reviewed HEAD e919d9ec68953f10a0f1d65ffb99fa868bc59bd4). Note for the orchestrator: this is the G1 code-review verdict; the D-038 in-regime per-requirement verification (`verification.json`, independent directive-compliance-verifier) and G4 remain as separate required gates before acceptance.
