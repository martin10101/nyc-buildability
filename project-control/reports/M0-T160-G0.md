# M0-T160 G0 — readiness gate RE-RECORD after the DB-063 re-scope (orchestrator, administrative)

Task: **M0-T160 — B-026 remedy step 5.0: re-pin the controller install source (tools/controller_update/source_binding.json) to the recertified candidate a3f24ff3 (M0-T159 accepted 275th) - a reviewed contract commit; NO install, NO machine change**

## Why this re-record

- Round 1 (producer commit 28878be2 on task/M0-T160-source-binding-repin) returned NEEDS_SPLIT: the binding
  edit is correct, but AS-3 could not pass inside a binding-only scope. The committed contract test
  `tools/controller_update/ps_tests/test_runbook_parse.ps1` hardcodes `$pinnedSha = 3f4cee86` and the runbook
  section 4 names 3f4cee86; the test is already RED at HEAD (orchestrator-observed; DB-063).
- Re-scope: the packet becomes the 3-file lockstep (binding + runbook section-4 pin paragraph + the test's
  `$pinnedSha`); forbidden_paths now name each other ps_tests file explicitly instead of the whole folder.
  Objective, outputs and AS-1..AS-4 are amended accordingly; two binding inputs are appended (RE-SCOPE,
  HARNESS). Nothing else in the packet moved.
- Status stays `claimed` (G0 PASS only moves backlog/rework to ready); the claim, its FULL worktree path
  `C:\Users\MLFLL\Downloads\nyc-zoning\wt-m0t160` and the producer are unchanged. The orchestrator re-applies 28878be2 on the claim-seam commit in that
  worktree before the round-2 producer starts.

## Authority

- D-024-R287 (the Claude Code 2.1.281 controller admission remedy, B-026 step 5.0), D-087-R001 (capacity),
  D-066-R001 (navigation block). Cited: D-024-R287, D-066-R001, D-087-R001; `evaluate_task_refs`: **ok=true, applicable == cited**.
- Dependency security: zero packages. NO machine change: the packet forbids every install/record/verify
  run against the live controller; the offline suite uses throwaway %TEMP% fixture repositories only.
- Boundaries unchanged: every gate, Tier D, PR #241, the owner-typed commissioning (recertification
  section 5), B-026 stays open until commissioning.

## Packet readiness

- allowed_paths: 4 entries, all tracked files.
- 4 acceptance scenarios; AS-3's before/after (RED at the pre-rescope head,
  GREEN at harvest) is the load-bearing proof; documented_test_commands unchanged (one plain command).
- Producer: backend-engineer; reviewers: code-reviewer, security-reviewer, directive-compliance-verifier; gates G0,G2,G3,G5.

## Disjointness (every task in state.json active_tasks - no filter, DB-057 (p))

| Neighbor | State | Overlap |
|---|---|---|
| M0-T021 | awaiting_gate | none - EMPTY overlap |
| M0-T034 | awaiting_gate | none - EMPTY overlap |
| M0-T080 | in_progress | none - EMPTY overlap |
| M0-T109 | awaiting_gate | none - EMPTY overlap |
| M0-T133 | rework | none - EMPTY overlap |
| M0-T145 | awaiting_gate | none - EMPTY overlap |
| M0-T153 | claimed | none - EMPTY overlap |
| M0-T155 | claimed | none - EMPTY overlap |
| M4-T001 | awaiting_gate | none - EMPTY overlap |
| M4-T002 | awaiting_gate | none - EMPTY overlap |
| M4-T003 | awaiting_gate | none - EMPTY overlap |
| M4-T004 | awaiting_gate | none - EMPTY overlap |
| M4-T005 | awaiting_gate | none - EMPTY overlap |
| M4-T006 | awaiting_gate | none - EMPTY overlap |
| M5-T001 | awaiting_gate | none - EMPTY overlap |
| M5-T094 | awaiting_gate | none - EMPTY overlap |
| M5-T095 | awaiting_gate | none - EMPTY overlap |
| M5-T096 | awaiting_gate | none - EMPTY overlap |
| M5-T097 | awaiting_gate | none - EMPTY overlap |
| M5-T098 | awaiting_gate | none - EMPTY overlap |
| M5-T099 | awaiting_gate | none - EMPTY overlap |
| M5-T100 | awaiting_gate | none - EMPTY overlap |

## Seam records

- Packet amendment + this report land in ONE commit; G0 is re-recorded at that head; a progress entry records
  the re-scope; the worktree is re-based on the claim-seam commit with 28878be2 re-applied.

Verdict: **PASS** — packet is contract-ready for round 2.
