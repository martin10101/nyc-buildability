# G0 Definition-of-Ready — M0-T020 (orchestrator, administrative)

**Task:** Python dependency-policy enforcement (hash-pinned tooling lock + machine release-age gate + CI hardening)
**Reviewer:** orchestrator (administrative) · **Result:** PASS · **Date:** 2026-07-20

G0 readiness confirmed for the M0-T020 dependency-security follow-up (recorded at acceptance under the deferred-ledger pattern; the underlying work was contracted, implemented, and independently reviewed before this record):

1. **Objective unambiguous** — packet `project-control/tasks/M0-T020.json`: lock and age-gate the Python tooling/build/audit install surface so every CI job installs pinned, hash-verified, advisory-clean tools; keep the production runtime lock (`services/api/requirements.txt`) byte-identical. The one legal-interpretation-free supply-chain fact that forced a scope decision (pytest 8.x carries CVE-2025-71176 / PYSEC-2026-1845, fixed only in pytest 9.0.3) was surfaced by producer STOP #1, independently corroborated (OSV `POST /v1/query` + pyproject grep), and resolved by an owner-authorized **bounded packet amendment** (control PR #59) permitting ONLY the `pytest>=8,<9` → `pytest>=9.0.3,<10` specifier change. That amendment is a packet-scope decision, **not** a dependency-security-policy waiver and not an advisory suppression.
2. **Dependencies** — `[M0-T018]` accepted (38th, task PR #55 `7ffd542` / acceptance PR #56 `270e81b`). M0-T020 must be accepted before M0-T019 (frontend npm parity) is dispatched.
3. **File scope exclusive** — `requirements.in`; `pyproject.toml` STRICTLY the `dev` pytest specifier + adjacent comment; `requirements-tools.in`/`requirements-tools.lock`; `services/api/scripts/**` (one documented tooling-lock generator + the release-age gate and its tests); `.github/workflows/ci.yml` + `scheduled-audit.yml` Python resolution only; `.gitignore` (egg-info); own producer report. Forbidden: `services/api/app/**`, any other pyproject/[tool.*] edit, npm/Playwright resolution, M0-T018 artifacts, a non-byte-identical production runtime lock, any advisory suppression/allowlist.
4. **Inputs/outputs defined** — inputs: current pins, pyproject, ci.yml/scheduled-audit.yml, verified `uv==0.11.28`, live PyPI/OSV. Outputs: hash-pinned tooling lock (11 direct → 42 pkgs / 389 sha256) via one generator with byte-identical `--check`; every Python CI job installs tools `--require-hashes` and the app `--no-deps`; dual pip-audit (runtime + tooling) blocking zero; machine release-age gate (live PyPI UTC, newest admitted-hash artifact, ≥604800 s, fail-closed) + deterministic tests; production lock `requirements.txt` byte-identical (uv 0.11.29→0.11.28 age-clean).
5. **Acceptance scenarios exist** — enumerated in `project-control/reports/M0-T020-producer-report.md`: tooling-lock byte-identical regeneration (`lock_tools.sh --check`); age-gate suite (17 tests: 604800 pass / 604799 fail + fail-closed branches); pytest 9.0.3 collection byte-identical (538) + full suite green with no weakening; dual pip-audit zero; production-lock byte-identity proof; CI regression (new `api-tooling-lock-verify` job).
6. **Required gates assigned** — G0 (administrative), G2 (producer self-check), G3/G4 (code-reviewer), G5 (security-reviewer). Producer `cloud-architect` ≠ reviewers `code-reviewer` / `security-reviewer`.
7. **Execution & disk** — GitHub Actions (clean Python 3.12) authoritative for lock/install/audit/age-gate; bounded temp venvs cleaned; owner low-storage PC preserved (no local heavy stack; source-only checkout). Local orchestrator re-check of the age-gate suite (17 passed) used the existing local interpreter only.
8. **Cleanup/storage routing** — no durable artifacts on the owner PC; all output source in Git; durable CI evidence in GitHub Actions.

Ready-to-start criteria met. Work executed by cloud-architect in isolated worktree from the amended main; task PR #60 (`9da5449`) merged to main at `4693ca4`.
