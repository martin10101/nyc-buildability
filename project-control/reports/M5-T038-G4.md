# M5-T038 — G4 QA review return (VERBATIM; qa-engineer, read-only, pinned d94bbcb6)

Orchestrator note: returned complete (untruncated) via the agent-return channel 2026-09-18
~09:59Z. VERDICT: PASS (two non-blocking observations).

---

G4 QA REVIEW — M5-T038 (address-flow + context-panel polish; DB-006/007/009/019a-c)
VERDICT: PASS. Read-only; no files edited. Material commit d94bbcb6; verified at HEAD 33b0c161.

IDENTITY / BYTE-STABILITY
1. All 13 packet allowed_paths are byte-stable d94bbcb6..HEAD (per-file `git diff --name-only` empty for each). Later commits are disjoint: 8c089343 = [ORCH-CORRECTED] e2e copy fix to apps/web/e2e/architect-workspace.spec.ts, which is NOT in allowed_paths (correct — frozen submission identity preserved, no rework needed); a6875731/f954aa59/33b0c161 = M5-T037 material + control-plane, disjoint from this packet's files. Confirmed.
2. Material diff is additive-only for the two widely-consumed components. PropertyOverview.tsx (ce7c148b..0eb5ffb9): public props unchanged; the site-zola-link pre-existed ("Open ZoLa ↗"), the change only adds data-testid, wraps the glyph in `<span aria-hidden="true">`, and adds the null-BBL else-branch. ZoningContextPanel.tsx (98a129e6..caf78654): adds CoverageBadge import, wraps glyph, adds one guarded per-row badge. No prop/markup contract break.

TEST ADEQUACY vs AS-1..AS-8
3. AS-1 (retry gate) — STRONG. address-search.test.ts DB-006 describe asserts retry COUNTS and labels together: 404 (autocomplete) and 400 → `rejected`, calls=1, plus an explicit `.not.toEqual source_unavailable`; 422 (/search) → `rejected`, calls=1; 500 → `source_unavailable`, calls=ADDRESS_SEARCH_MAX_ATTEMPTS. Pre-existing 503-recovers (calls=2), 503-exhausts (calls=MAX), 429→rate_limited (calls=1) are preserved. The new `rejected` reason is asserted distinctly, and autocomplete.test.tsx:53 gives it distinct copy (/didn.t accept that search/i vs source_unavailable's /temporarily unavailable/i). Mutation check: reverting the `>=500` gate to the old `!response.ok → source_unavailable` makes both the label and calls=1 assertions fail. Not vacuous.
4. AS-2 (timeout affordance) — PASS. autocomplete.test.tsx:68-81 sets a timeout outcome, asserts getByTestId("full-address-search") present, not disabled, and timeout copy. getByTestId throws if the button disappears, so the affordance is guarded in the timeout state.
5. AS-3 (Not-my-property focus) — PASS with one note. address-resolution.test.tsx:1005-1026 renders `<AddressResolutionScreen architect />`, resolves via the manual form, clicks not-my-property, and `waitFor`s document.activeElement === getByLabelText("Street address") (= the autocomplete input, the documented target per AddressResolutionScreen.tsx:126 architect branch + AddressAutocomplete.tsx:132), then asserts the confirm card is gone. Verifies the observable focus-return outcome AS-3 requires. NOTE (non-blocking): in the architect path the AddressAutocomplete input is rendered unconditionally (AddressResolutionScreen.tsx:301), so it does NOT remount when result clears; a hypothetical synchronous `.focus()` regression would also pass this test on this path. The CODING_RULES remount hazard doesn't materialize here, so this is an adequacy nuance, not a defect — the required behavior is verified.
6. AS-4 (copy/affordance no-drift) — PASS. autocomplete.test.tsx:83-94 mocks malformed; asserts the button text and the status copy both contain the imported FULL_ADDRESS_SEARCH_LABEL. Implementation drives both the button (AddressAutocomplete.tsx:174) and every button-naming ERROR_MESSAGES line (25-30) from that one constant, so rendered strings cannot diverge silently. Hardcoding a differing literal on one side would fail the test.
7. AS-5 (aria-hidden glyphs) — PASS. zoning-context-panel.test.tsx:224-244 finds `span[aria-hidden='true']` with textContent "↗" inside both links and asserts exact accessible names via getByRole("link",{name:"Open in ZoLa"/"Open ZoLa"}). Verifies both decorative-glyph and unchanged-name halves.
8. AS-6 (null-BBL honest note) — PASS. zoning-context-panel.test.tsx:246-287 covers non-canonical "12345", a literal-null probe (`null as unknown as string`, exactly the null edge case), and the canonical-BBL byte-stable path. Strong guard: siteAbsent.textContent === panelAbsent.textContent (both from the same literal, copy byte-matched) plus the "needs a valid BBL" substring; canonical case asserts link href correct and no absent note.
9. AS-7 (coverage_status rows) — PASS, non-vacuous. Base fixture has landmark and histdist each carrying coverage_status:"conditional" (builder_output_m1_t005.json:266-275), and mappedFeatureView (contract.ts:257-268) derives coverageStatus from that field; test asserts exactly 2 `.status-badge` elements scoped to zoning-context-designations, both status-conditional, and 0 badges + 2 "Unknown — not supplied" rows when mapped_features=[]. The exactly-2 count also guards against duplicating the full coverage table into the panel.
10. AS-8 — relied on orchestrator CI evidence (M5-T038-ci-evidence.md): vitest 42/42 files green at material d94bbcb6; corrected head 8c089343 all 20 checks success incl. web-e2e. Consistent with additive PropertyOverview change not breaking the out-of-scope development-limits.test.tsx (its /Open ZoLa/ regex still matches the trimmed accessible name).

REPORT REPRODUCIBILITY
11. Producer digest-bound sections match committed content: §A.1 diff matches address-search.ts:128-141; §B verbatim block matches zoning-context-panel.test.tsx:213-312 exactly; §D per-file summaries match the committed AddressAutocomplete.tsx, PropertyOverview.tsx, ZoningContextPanel.tsx, autocomplete.test.tsx, and address-resolution.test.tsx AS-3 block. modularity_check --check reported 0 failures (report §F).

OBSERVATIONS (non-blocking; for orchestrator awareness)
12. Scope-wording ambiguity: task input line 11 parenthetical says "only status >= 500 (and network failure) classifies as retryable source_unavailable," but the implementation keeps a transport/offline failure as the distinct `unavailable` reason (address-search.ts:153) and does NOT retry it (retry loop retries only source_unavailable, :175). This matches the binding AS-1 (silent on network failure) and the accepted M5-T032 distinct-reasons design (folding `unavailable` into source_unavailable would contradict the "never fold these into one message" rule and break the existing `unavailable`→/check your connection/ test). Reading the parenthetical as "relabel transport errors as source_unavailable" would be a regression; the code correctly preserves the distinct reason. Flagging only so the wording is reconciled, not as a defect.
13. AS-3 mechanism-distinction limit (see finding 5) — optional future hardening if a remounting-target variant is ever desired.

No blocking findings. All eight acceptance scenarios are covered by adequate, mutation-resistant, non-vacuous tests; CI proves execution on the pushed head; identity and byte-stability hold. Verdict PASS.
