# M5-T075 — G2 producer self-check record (recorded by the orchestrator, role self_check)

Producer: qa-engineer (loop-1 worker, run persistent-local-70-m5t075). The run closed
COMPLETE/decision_complete — the clean close class — with the unit delivered uncommitted and
zero queued asks.

- **Scope self-check (report):** additions confined to the one allowed test file (+143,
  zero deletions there); one import symbol added; every existing assertion byte-unchanged;
  production outline-bridge-api.ts untouched (READ-ONLY per the packet); no finding needed
  routing.
- **Coverage (report's branch table):** the five client fault branches each mapped to a
  spec by source line (:382-387 network, :380 timeout, :359-361 pre-flight abort, :381
  mid-flight abort, :399-401 over-budget), with the two abort paths proven as DISTINCT
  branches (the pre-flight spec additionally asserts fetch was never called).
- **AS-2 (never laundered):** every spec asserts the outcome is neither bridged nor any
  refusal kind; network/timeout additionally assert recoverability (Retry meaningful).
- **AS-3 (mutation predictions):** one single-mutation prediction per spec, including the
  honest SUPERSEDE of the producer's own earlier mid-flight prediction (the single-operand
  drop is ineffective because the caller abort propagates into the internal controller; the
  whole :381 guard is the load-bearing target). Predictions only — nothing run locally;
  CI proves green.
- **Thin-client discipline:** vitest was NOT run locally; the determinism basis is argued
  statically (real timers, synchronous abort-listener attach, Content-Length read before
  parse); the suite proves on the seam-push CI, which this submit cites as its backstop.
- **Modularity:** exit 0 (producer-observed, prior increment; the revision is test-only).
- **Identity:** in-wt commit cherry-picked to 7d225e2f, MATCH x2 (LF-normalized).

G2 verdict: PASS (self_check; never independent review — G3/G4 follow).
