<!-- Orchestrator preservation header (the only non-reviewer content in this file):
     G2 wave return for M0-T036 (D-007-R589, dispatched under R599 at the live owner window).
     Reviewer identity: g2-qa-engineer-m0t036 (qa-engineer role, Fable-5-pinned, independent,
     read-only; producer backend-engineer != reviewer). Frozen SHA 98da15e04d081730c383555f7a2c6aec7c695200.
     Preserved VERBATIM on arrival 2026-08-04; transport entity-decoding only. -->

<!-- VERBATIM REVIEWER RETURN BEGINS -->
VERDICT: PASS — the M0-T036 producer self-check record is complete-enough, honest, and reproducible to permit submission. This verdict permits submission only; it does not accept the task, close the live seam-rotation gap, or touch any owner gate.

G2 SELF-CHECK VERIFICATION REPORT — M0-T036 (identity: g2-qa-engineer-m0t036, independent, read-only)
Frozen SHA confirmed: HEAD = 98da15e04d081730c383555f7a2c6aec7c695200, branch task/M0-T036-supervisor-bridge (git rev-parse HEAD + git branch --show-current, run first). Working-tree note: the only dirty tracked file outside .claude/agent-memory/ is project-control/state.json (orchestrator ledger churn); no tools/ or code file is dirty, so the reproduction below is at the frozen content.

(1) THE PRODUCER SELF-CHECK RECORD — what exists and who recorded it

a. project-control/reports/M0-T036-producer-report.md (620 lines): four producer checkpoints (Phases 1–4), each preserved VERBATIM under an explicitly separated orchestrator header that enumerates its R024 redactions (3+1, 2, 0, 1 — each header states exactly what was substituted and that nothing else changed). EVERY phase carries: exact test commands with tallies (Phase 1: `python -m unittest ...` "Ran 139 tests ... OK (skipped=1)" + per-suite standalone tallies; Phase 2: 383/2 + 8 per-suite tallies; Phase 3: 735/2 + 13 tallies; Phase 4: 1042/2 + a 19-suite table), live doctor results (12→20→30→36 checks PASS), repo-regression outputs quoted, and a §5 "Self-checks" section with the actual check commands (effort-key grep, shell=True grep, bypass-flag grep with file:line results, stdlib verification against sys.stdlib_module_names, `git status --porcelain -uall` containment). These are commands-plus-outputs in reproducible form, not narrative. Honesty markers are first-class throughout: Phase 3 §3 discloses that its one live probe predated the persistence wiring so doctor correctly still reads UNVERIFIED ("I did not re-run ... and I did not fabricate a record"); Phase 4 §0 corrects a false "recovered from corruption" narrative against itself; four producer-found defects are documented with the fix direction stated (none weakens a stop). Requested status every phase: awaiting_gate.

b. M0-T036-V1.1-producer-report.md (214 lines): producer-recorded, with a before/after baseline in actual command output form — `python -m pytest tools/test_agent_supervisor_*.py -q` = 1065 passed/2 skipped BEFORE any edit, 1091/2 after (+26, split itemized), doctor and secret-scan outputs quoted, per-finding (B-1..B-4, F-2, F-4, F-5, L-1, V-4 partial, V-6) change→test mapping, and an honest "Risks/ambiguities" section including the one pre-existing assertion it changed and why.

c. V1.2 and V1.2.1: the producer artifacts are the VERBATIM diffs M0-T036-V1.2.diff (1050 lines, 7 files) and M0-T036-V1.2.1.diff (691 lines, 3 files) — raw git diffs, no narrative. The producers' command outputs are NOT preserved as standalone report files; they survive as ORCHESTRATOR-recorded verifications in the task-packet progress log: 18:24 entry (V1.2: 7/7 file hashes match the producer worktree, 1114 passed/2 skipped, doctor + scanner PASS) and 19:19 freeze entry (V1.2.1: hashes 3/3 match, orchestrator-reproduced full sweep 1121 passed/2 skipped, +7 ModelSubstitutionTests, producer's no-live-quota-probe flag preserved). Finding 1 (LOW, non-blocking): for these two units the producer-narrative return was not preserved as its own report file the way Phases 1–4 and V1.1 were — mitigated three ways: the diffs are verbatim, the orchestrator independently reproduced the tallies, and I reproduced the final tally myself (below).

d. M0-T036-V1.2-live-exercises/README.md + ex1, ex2a–d, ex3a–c: orchestrator-recorded (the live exercises were orchestrator-run by design, D-007-R592–R594). Each run directory preserves the verbatim start --json output, audit.jsonl, audit.jsonl.head.json, and supervisor_journal.sqlite3 (spot-verified ex1/ex2a/ex3a — all four artifacts present in each).

(2) REPRODUCTION AT THE FROZEN SHA (my own runs, repo root)

- `python -m pytest tools/ -q -k "agent_supervisor"` → **1121 passed, 2 skipped, 200 deselected in 61.32s** — EXACTLY the claimed tally. The 2 skips match the recorded justifications (POSIX-only guard; symlink-privilege guard with junction twin covered).
- `python -m tools.agent_supervisor doctor` (no controller config) → **36 checks, all PASS, exit 0**, "controller 0.4.0-phase4 (phase 4)", "limited-auto: NOT IMPLEMENTED and disabled." It behaves honestly without inputs: `configuration` reports "no --config/--model-selection supplied; skipped", and `control_response_live_probe` reports UNVERIFIED on this fresh runtime (correct — the live-probe record is per-checkout and lives on the controller checkout, per M0-T036-doctor-live-probe.md). Disclosure: running doctor initializes a runtime dir under %LOCALAPPDATA%\NYCBuildabilitySupervisor\289bba68... keyed to this checkout (journal 0 transitions, audit 0 records) — a doctor side effect on the machine, not a repository write.

(3) SCENARIO → SELF-CHECK EVIDENCE MAP (packet AS-1..AS-8)

- AS-1 (Section 16 criteria + reviews + CI): five independent reviews PASS at 43848bd + five delta PASS at c193a52 (decision packet §2, review files on disk); CI at 43848bd = run 30886102559 all 13 jobs, V1.1 CI 30890198955. NOT VERIFIED BY ME: a CI run at the current frozen 98da15e — no run id is recorded for the freeze commit in anything I read, and I cannot run gh. The V1.2 delta re-gate running in parallel is the closure path.
- AS-2 (Section 15 matrix, fake executables): Phase 4 §4 19-suite table (1042) → V1.1 (1091) → V1.2 (1114) → V1.2.1 (1121); reproduced by me at the frozen SHA, 1121/2 exact.
- AS-3 (13.12 invariants 1–15): test_agent_supervisor_invariants.py (45 tests) + policy suite (98), recorded Phase 4 §4; doctor trust_zones / policy_four_tiers / hard_deny_enforced reproduced PASS by me.
- AS-4 (audit chain + anchoring): audit suite (Phase 1, 15 tests: tamper/truncation/reorder) + V1.1 V-6 corrupt-head-anchor cases; the Option A external anchor is mechanism-built, NOT ACTIVE, owner-gated (doctor audit_anchor_option_a) — the deferral carries G5 PASS twice, which is the packet's stated alternative.
- AS-5 (replay corpus): Phase 4 §4 live replay output, 8/8 with the per-case table; doctor replay_corpus 8/8 reproduced by me.
- AS-6 (shadow real lifecycle within touch budget): orchestrator-recorded, decision packet §4 (runs 1–6; run 6 full cycle 0/2 touches, ends AT the gate), G4-reproduced. No producer self-check exists for this by design — Phase 5 was orchestrator-run; the producer report ends at Phase 4.
- AS-7 (containment): per-phase §5 git-status outputs recorded; I independently verified both V1.2/V1.2.1 diffs touch ONLY tools/agent_supervisor/** and tools/test_agent_supervisor_*.py (grep of diff headers); no effort key set anywhere per per-phase greps.
- AS-8 (limited-auto disabled): per-phase self-checks + doctor loop_modes ("limited-auto refused by name") reproduced by me at the frozen SHA.

Scenarios/items with NO recorded self-check: (i) THE KNOWN OPEN ITEM — live seam-rotation ACTUATION (V1.2 scope C live leg / QA gap 4) is UNSATISFIED at this SHA, recorded first-class in the live-exercises README with the structural cause (held supervised prompt has no operator continuation command — F-2 analogue; live reviewer digests cannot be pre-approved, 6 runs / 6 distinct digests). G2 effect: this does NOT fail G2 — the self-check record is honest about it (triggers ARMED live both ways and audited; actuator unit-suite-proven; the gap named, its cure identified as new scope not built without authorization). What it defers to the owner: authorizing the follow-up continuation-command unit, and weighing the gap in the activation decision. It would only become a G2 problem if anyone claimed the live leg satisfied — no record does. (ii) V1.2.1 live quota probe — not wired, producer-flagged, consistent with the pre-existing injected-probe stance. (iii) Finding 1 above (no standalone V1.2/V1.2.1 producer report files).

(4) NO SELF-APPROVAL — CONFIRMED

gates/M0-T036-G0.json: reviewer "orchestrator", role "administrative", PASS at 8175a6a — the administrative readiness gate, which the protocol assigns to the orchestrator; not a producer act. gates/ holds ONLY G0 for M0-T036 (F-CPΔ-3 confirmed — no G2/G3/G4/G5 record yet, which this report is the basis for). Every review report header names a distinct read-only reviewer identity (g3-v11-m0t036, g4-qa-engineer-m0t036 / g4-v11, g5-security-reviewer, CPV, DCV), each stating producer ≠ reviewer and no-writes; all preserved with explicit orchestrator verbatim-preservation headers. Every producer checkpoint requests "awaiting_gate" and states no ledger/git/gh command was run. The V1.1 header confirms the delta reviewers were the same independent identities re-engaged, not the producer.

WHAT I COULD NOT VERIFY: (a) CI at frozen 98da15e (no gh; no run id recorded yet). (b) The historical intermediate tallies (139/383/735/1042/1065/1091/1114) — those are at prior SHAs; I reproduced only the final state, which subsumes them for current-content purposes. (c) The port-time hash verifications (26/26, 17/17, 21/21, 25/25, 7/7, 3/3) — producer worktrees are gone; these are orchestrator-recorded, corroborated by the final tree passing its full suite. (d) The live-exercise runs themselves — owner-plane controller execution; I verified the preserved artifacts exist and the README's claims are internally consistent with them, not the runs' execution. (e) The Phase 5 shadow pilot execution (same class — orchestrator-run, G4-reproduced at 43848bd).

Read-only discipline: no repo file written, no control CLI, no git mutation, no gh. Commands run: git rev-parse/branch/status (read), pytest, doctor (side effect disclosed above), grep/head/ls over committed reports.
<!-- VERBATIM REVIEWER RETURN ENDS -->
