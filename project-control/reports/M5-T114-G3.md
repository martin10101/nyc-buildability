# M5-T114 — G3 design review (visual-quality-reviewer "vq-t114", read-only)

> Transmission history: pinned at c074b6b5, delivered as four SendMessage parts (1/4..4/4) ending
> with END-OF-REPORT; no truncation. Joined verbatim by the orchestrator (transport wrapper tags
> removed only). Verdict PASS with three ADVISORY items (a distinct draft claim token in the
> mockup, the section-29 apostrophe, one line anchor) routed to the backlog for the P2+ slices.

---

M5-T114 G3 (D-086 P1) — visual-quality-reviewer report. PART 1/4.

PIN: git rev-parse HEAD = c074b6b5c8720d41cb07517a90ae5432e4b5a9a1 at start. Independent review — I read the spec, mockup, both P0 ledgers, P0-RECONCILIATION, assessment §14, and every cited backend source file; NOT producer screenshots.

SCOPE (AS-6): PASS. Exactly the 3 docs files (P1-VISUAL-STATE-SPEC.md, P1-mockups.html, M5-T114-producer-report.md); allowed_paths clean; no apps/ services/ ledger/ assessment edit.

AS-1 (ONE status vocabulary, mapped FROM real backend status, no remap, no UI-computed number): PASS — line-accurate. I verified every load-bearing citation against source at this head:
- coverage.ts:22-47 — six-value enum + symbols (✓◐!≠∅—) + glosses match the spec §2.2 table VERBATIM; coverage.ts:60-77 DataCompleteness headlines match §2.4 verbatim.
- rule-evaluation-contract.ts:69-75 DRAFT_COVERAGE_STATUSES excludes `verified` (spec §2.1/§2.2 claim confirmed); :26,67 comments confirm a verified body can never reach screen; :88-100 the 7 FAIL_SAFE_REASONS; :123-134 wide-street determination_state + far_row; :287-299 base_district_candidates share_min/point/max (range never collapses).
- development-limits.ts:14-27 residentialReference status set (City reference/Unknown/Conflicting records/Source value unavailable); :155-165 FAILURE_LABELS (each distinct label, "Condo base lot needs site confirmation" etc.); :168 "Property identity mismatch"; :171 "Rule details incomplete…"; :180 default "Draft assessment · envelope incomplete"; :198-209 bulkRow states (Not calculated/Not supported/Review required/Conflicting). All match §2.3.
- condo-records.ts:724-732 CondoChannelState (8 states) and :788-794 channelWithholdsAllowances = multi_lot|unresolved|resolver_error ONLY (transport `unavailable` never withholds) — match §2.6; :437-448 self-attested → refusedForCalculation forced TRUE regardless of payload — match §2.5 (C05).
- components/property/OutcomeAnnouncer.tsx:36-45 = visually-hidden role=status aria-live=polite aria-atomic=true — matches the §2.6 live-region rule exactly.
- max-envelope copy-wall present and case/hyphen/space-proof (max-envelope-panel.test.tsx:292-330) banning "maximum allowed building"/"demonstrated maximum".

No status is remapped; no number is computed in the presentation layer (all mockup figures are 0.00 / 00,000, banner-labelled fictional). Continued in Part 2.

---

M5-T114 G3 — PART 2/4.

AS-2 (coverage): PASS. §6 is a full 7×4 matrix, every cell filled (no "n/a" needed — each surface has a genuine normal/boundary/missing/failure). §5 gives desktop+mobile annotations and all four states per surface. The mockup carries all seven surfaces with desktop AND phone frames (S/C/O/K/P/E/R). Each surface names one dominant decision (five-second hierarchy holds).

AS-3 (no-loss): PASS — exceeds the 10-row bar. I pulled 15 L-marked rows across BOTH ledgers and compared each ledger's recorded progressive_destination / accessibility / print cell to where §5 places it. All 15 exist, all carry an "L" mark, and all are placed consistently:
- SH-01 (disclaimer footer landmark, prints every page), SH-02 (compact landing, not a print surface / P5-D1), AD04 (street-vs-full-address kept distinct; flagged not silently fixed), AC10 (record-address channel; late fetch cannot shift CTAs), M01 (display geometry ≠ survey accuracy), PE-08 (adopted/removed-wall announcements via editor OutcomeAnnouncer; N/A to print), A15 (identity-withhold role=alert KEPT; prints in brief), C05 (self-attested refusal always beside record; prints), C08 (Unknown as a word + "Zoning missing for N lots" + named ZTLDB gap), R01 (print toolbar identity/not-saved/appendix), LS-P16 (one persistent visually-hidden announcer, exactly-once, not printed), LS-C04 (both conflict values kept, nothing reconciled), LS-E10 (null-end corrective), F03 (data-completeness never "Feasible"), ME-03 (exact server disclosure survives; pinned @b56f3d5b).
Notably the LS-E10/LS-C10 null-end "in effect… to present" corrective is NOT silently adopted — it is correctly routed to the §10 meaning-change register (MR-2), exactly as its L→V corrective mark requires. Preservation checklist §7 is complete; §29 kept verbatim in §4; city warnings kept above Continue (§5.2); returned disclosures (report.disclosure, not_verified_disclaimer, wide_street.reason/fallback, condo provenance) locked unchanged.

AS-4 (copy + meaning): PASS. 12 before→after samples each with protected meaning. I grepped both files for permitted/approved/maximum-allowed/demonstrated-maximum: every hit is EITHER the §29 disclaimer text ("will be approved"), a prohibition naming the banned term to forbid it, or process language ("reviewer-approved visibility contract" / "Approved copy samples" heading) — NONE presents a property/building AS permitted/approved/max-allowed. D-083 vocabulary present: "Proposed — not a city record", "Generated building option" in both files. §10 register lists 8 legal-meaning items (MR-1…MR-8) all NOT adopted and routed to qualified review. Continued in Part 3.

---

M5-T114 G3 — PART 3/4.

AS-5 (mockups): PASS. P1-mockups.html is ONE static self-contained file — I confirmed 0 <script>, 0 http(s):// refs, 0 src=, 0 @import, 0 url() in CSS, 0 cdn, 0 <link>. Inline <style> only; the one background is repeating-linear-gradient (no url()). Fictional data is prominently banner-labelled ("ALL DATA ON THIS PAGE IS FICTIONAL"; address "0000 Example Avenue, Test Borough"; BBL "0-00000-0000"); no real address shown as a real record. Desktop + phone frames for all seven surfaces.

ACCESSIBILITY / human-lens walkthrough (my required lenses):
- Colour never sole carrier: every status = hue + non-colour symbol + text. Mockup chips render .sym (✓◐!≠∅—) plus a text label; a shared legend explains them. Keyboard user: BBL is a native <details>, fields are labelled, no auto-pick, and the keyboard path on the confirm map is the numeric identity, not the map. VERIFIED against sources.
- Landmarks / roles preserved as they exist today: role=contentinfo disclaimer footer (layout.tsx:35-48 — I confirmed the exact role + aria-label="Required disclaimer"); role=alert KEPT for identity-mismatch/error-boundary/refusal and downgraded only via a reviewed AT test; role=status for polite announcers; role=group for condo records. Reduced-motion user: §3 collapses all transitions under prefers-reduced-motion (and the mockup has no motion). Focus order: skip-to-workspace first; inspector focus-in / Escape / focus-return; Retry moves focus to the loading card / h1, never <body> (LS-P02).
- First-time client / 5-second hierarchy: one dominant decision per view; stable property identity header (address · borough · BBL · Draft analysis). No clutter — density from spacing/grouping, legal text never shrunk or hidden at a breakpoint; the ≤700px env-badge/nav-footnote hides are explicitly flagged as P0 gaps to CLOSE, not kept.
- Experienced zoning professional: claim classes distinct, provenance first-class, ranges stay ranges, conflicts keep both values + "unresolved", wide-street conditional vs conservative kept distinct with the withholding sentence beside the value.
- Tablet user: §8.2/§11 describe 768px single-column (map above results) → 360px stacked, inspector full-width. Mockup frames are desktop+phone; tablet is spec text only (acceptable for a spec).
- No default-template appearance: a restrained custom token system + decision-first IA + claim-class chips; §12 "do not ship a default dashboard template" respected.

AS-6 (scope): PASS (see Part 1). Continued in Part 4 (defects + verdict).

---

M5-T114 G3 — PART 4/4. DEFECTS (all ADVISORY; none BLOCKING) then verdict.

ADVISORY-1 (visual consistency / claim-class distinctness — mockup). P1-mockups.html:265 tags a "Draft rule result" value with the k-generated (Generated building option) claim chip reading "DRAFT". Per spec §2.1 a draft rule result is a DISTINCT claim class (indigo "Draft" eyebrow), not the generated-option class; and the mockup's claim-class legend (lines 144-150) shows only 4 of the spec's 5 claim classes — it OMITS "Draft rule result" and reuses k-generated for it. This contradicts the spec's own preservation checklist §7 (line 464-465: "Claim classes stay distinct: City record ≠ draft rule result ≠ generated option…"). Not blocking: the label text "DRAFT" is honest, no permitted/approved/generated claim is falsely made, and the authoritative spec text is correct. Recommend for P2+: add a distinct draft claim token + legend entry and retag line 265, so a build slice does not inherit a merged treatment.

ADVISORY-2 (transcription, not meaning). The §29 block quoted at P1-VISUAL-STATE-SPEC.md:197-200 (labelled "preserved verbatim", line 193) uses a straight ASCII apostrophe in "platform's"; the canonical source disclaimer.ts:8 uses a typographic apostrophe U+2019 ("platform's"). Byte-checked: source has 1 curly / spec block has 0 curly + 1 straight. Every word is identical and the binding preservation instruction still points at REQUIRED_DISCLAIMER (rendered via layout.tsx:35-48, verified), so meaning/prominence are intact. Recommend the doc reproduce the curly apostrophe (or note it renders REQUIRED_DISCLAIMER) so "verbatim" is byte-true.

ADVISORY-3 (citation precision, trivial). Spec §2.1 line 69 cites the max-envelope copy-wall at "max-envelope-panel.test.tsx:274-290"; the actual copy-wall (containsBannedClaim + describe) is at :292-330. Enforcement exists and does exactly what the spec claims; only the line anchor is off.

BASIS: AS-1..AS-6 all met; vocabulary is line-accurately grounded in real backend statuses with no remap and no UI-computed number; 15/15 spot-checked L-rows placed consistently with the ledgers; §29 + city-warning visibility + returned disclosures preserved; honesty vocabulary correct (banned claims only ever named to prohibit); mockup static, self-contained, clearly fictional; exactly the 3 docs files. The 3 advisories are polish items for the P2+ slices, not gate blockers.

M5-T114 G3 VERDICT: PASS

END-OF-REPORT
