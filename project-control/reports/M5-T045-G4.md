<!-- Orchestrator capture: qa-engineer G4 return for M5-T045 saved VERBATIM
     (transport entity-decoding only). Pinned at frozen head 2b851c18; reviewer proved
     blob-identity across material/CI/frozen heads and traced mutation-sensitivity. -->

I have completed the independent G4 test-adequacy review against the frozen source. All evidence is verified via git plumbing at the frozen SHA, with blob-identity confirming the green CI head covers exactly the reviewed source.

---

# GATE REPORT — M5-T045 (G4: QA / test-adequacy)

**Task:** M5-T045 — Condo billing-BBL live wiring (DB-029): registry + real null-billing fixture + base-lot resolution seams + records-vs-allowances display
**Reviewed SHA (frozen):** `2b851c18` (branch `candidate/D-024-mrl-option-b`)
**Material producer commit:** `28082288`
**CI-green head covering the surface:** `21ee7379`
**Reviewer:** qa-engineer (read-only, independent; producer ≠ reviewer)
**Verdict: PASS** — 0 blocking findings, 4 advisory.

## Verification context / reproduction

- My worktree is isolated and divergent: HEAD `d8b3899f` does not carry the M5-T045 files, and `2b851c18` is not an ancestor of it. I therefore reviewed every file via `git show/rev-parse` at the frozen SHA `2b851c18`, not the working tree.
- **Blob-identity proof that green CI covers the reviewed source:** for `condo_base_lot.py`, `test_live_provider.py`, and `condo-resolution-display.test.tsx` the git blob hash is identical across `28082288` (material), `21ee7379` (CI-green), and `2b851c18` (frozen) — e.g. `condo_base_lot.py` = `39551152…`, `test_live_provider.py` = `82631121…`, `condo-resolution-display.test.tsx` = `4101e03a…`. The CI evidence file's byte-identity claim is confirmed.
- I could not re-execute the other worktree's pytest (git-isolation guard refuses operations outside my worktree, and my HEAD lacks the files). Per the task instruction I verified the **stored** records (M5-T045-ci-evidence.md: CI run 35431367744 success incl. web-e2e at `21ee7379`; producer §7 orchestrator-reproduction: ruff clean, 81 focused + 58 seam-trio + 1494 full-regression passed, modularity exit 0) rather than returning BLOCKED for non-execution. My adequacy verdict is grounded in reading the actual frozen test + production source.

## G4 surface findings (adequacy against the AMENDED AS-1..AS-10)

### 1. Seam module `test_condo_base_lot.py` — typed vocabulary + mutation-sensitivity — ADEQUATE
Every outcome branch is covered with the real routed transport AND injected doubles: pass-through zero-lookup (`_never_resolver` asserts the resolver is never called; parametrized over land/unit/lot-0001 BBLs, plus malformed BBL, `test_condo_base_lot.py:98-114`); resolved-single exposes base lot + full provenance (`:118-144`); multi-lot exposes the full set and **no** single substrate (`:148-169`); unresolved fail-safe (`:173-191`); typed errors `rate_limited`/`schema_drift` → `OUTCOME_ERROR` (`:195-217`); frozen dataclass (`:257-262`).
- *Multi-lot-collapse mutant* (map `>=2` lots to `base_bbls[0]`/RESOLVED_SINGLE): caught — `:163-168` asserts `outcome==OUTCOME_MULTI_LOT`, `resolved_base_bbl is None`, `base_bbls==("3022640032","3022640033")`.
- *Base-lot-fabrication mutant* (populate `resolved_base_bbl` on unresolved): caught — `:190-191` asserts `resolved_base_bbl is None` and `base_bbls==()`.
- *`len(base_bbls)==1` boundary mutant* (`>=1`): caught by the 2-lot multi test.

### 2. `test_live_provider.py` condo blocks — substitution + fail-safe + regression — ADEQUATE (strong)
- AS-1 (`:602-631`): asserts the downstream fetches receive the **base** lot — `recording.ztldb_calls==[(_BASE_LOT,CID)]` and `recording.lot_calls==[(_BASE_LOT,CID)]` — so a mutant passing `canonical_bbl` instead of `substrate_bbl` to `fetch_ztldb/fetch_lot` fails. Payload-only log asserted (input_bbl/base_bbl/correlation_id present).
- Fail-safe (`:632-666`, parametrized multi-lot/unresolved/typed-error): `result is None` with **zero** ztldb/lot/layer calls and one payload-only warning per event token. A fabricated-substrate mutant produces fetches → fails.
- Non-condo pass-through via the **default** seam (no double, `:668-679`): proves classification purity end-to-end and byte-identical prior behavior on the input BBL.

### 3. `test_ztldb_crosscheck.py` condo_resolution_report mapping — ADEQUATE; `test_wave_integration.py` — regression only (not modified)
- Resolved-single = record NOTE not conflict, note in `connector_notes`, readiness stays `ready` (`:510-528`); multi-lot = conflict with field `condo_base_lot_resolution`, `resolution=="unresolved"`, **every** base lot as a record, "NO computed allowance" in reason, propagated into the schema-validated profile with both values preserved (`:531-556`) — a first-lot-collapse mutant fails `recorded==["3022640032","3022640033"]`; unresolved/error = honest notes (`:559-580`). Each asserts profile-schema validity → confirms NO contract-shape change.
- `test_wave_integration.py` and `test_rule_evaluation_api.py` were in `allowed_paths` but **unchanged** by the material commit (verified: empty diff). Their existing suites are the regression proof that existing channels are preserved; this is appropriate, not a gap.

### 4. `test_dtm_condo_soda.py` — real key-absence fixture + riders — ADEQUATE
- AS-2: real byte-faithful capture `condo_key 103343` resolves single base lot (`:428-...`), and parametrized 8-key (103343) / 7-key (100355) both assert `"condo_billing_bbl" not in record` (the load-bearing key-absence fact) and resolve; the byte-infaithful explicit-null synthetic is retired (diff deletes `"condo_billing_bbl":null`), the impossible multi-lot-null-billing case kept only as a labeled synthetic. A parser mutant requiring an explicit null fails.
- AS-7 riders all closed with mutation-sensitive tests: `_ERROR_CODE_SAFE_RE.fullmatch` (prefix-injection `"…no-such-column\n<injected>"` → repr; Unicode digits → repr) + the 400 end-to-end detail-repr test; `$limit` off-by-default byte-identical URL vs opt-in `&$limit=1000`, with non-positive/`1.5`/`True`/`"1000"` all failing closed **before** transport; response-side `condo_key` guard negative (`30131X` → SchemaDriftError, expansion never reached); documented `invalid_component` code; canonical-normalizer consistency.

### 5. Display tests — displayable-control pattern + allow-list mutant — ADEQUATE (this was the crux)
- **The withhold probes are non-vacuous.** `condo-resolution-display.test.tsx:271-277` is an explicit control: a non-condo profile with the same scenario/evaluation **renders** the computed allowance (`development-evaluated-far`="1.50", `architect-cap`="15,000 sq ft"). The withhold cases reuse the identical `displayableInputs()` and assert those same testids fall to "Not calculated" — `getByTestId` throws on absence, so the pair is genuine.
- **The allow-list → deny-list mutant fails.** Withhold is proven not just for multi-lot (`:279-292`) and unresolved (`:294-301`), but for a MISSING token (`:312-328`), an UNKNOWN token `[resolved_multi_condo_v2]` (`:330-341`), and a MALFORMED bracket `[resolved single base lot]` (`:343-354`). A deny-list mutant (withhold only on multi_lot/unresolved/error) would leave allowances rendered on the missing/unknown/malformed cases → those three tests fail. The `!==`→`===` mutant is caught by the resolved-single "STILL shows" test (`:303-310`).
- Printed brief (`report-view.test.tsx`): the same is proven on the `wide-street-result` computed allowance — resolved-single control prints it, missing/unknown-token cases withhold it, multi-lot prints records only, non-condo prints nothing. `wideStreetDoc` is a real helper (`:39`) already used by passing M5-T040 tests, so the control is real, not a stub. ReportView shares the one `condoWithholdsAllowances` guard (verified in source), so screen/brief cannot diverge.
- AnalysisIdentityNotice covers AS-8's differing/equal/absent/unrelated-mismatch cases with neutral wording and an explicit `not.toMatch(/base lot|billing|condo|resolved/i)` assertion, plus `data-identity-state` differs/absent.

### 6. Regression / scenario-coverage completeness — ADEQUATE
Every AMENDED contracted scenario has a mutation-sensitive probe (AS-1..AS-5, AS-7 code; AS-8 display; AS-6/AS-9/AS-10 are the registry doc + CI records). No contracted scenario is left without a probe. Non-condo suites are unchanged; the 1494 full-regression green is in the stored record.

## Advisory findings (non-blocking)

- **A1 — cross-module note-token contract is not pinned on the emitting side.** `_condo_note_prefix` emits `condo_resolution: [<outcome>]` and the web regex `/^condo_resolution:\s*\[([a-z_]+)\]\s*/` depends on that exact bracket shape, but the Python tests only assert `note.startswith("condo_resolution:")` (`test_ztldb_crosscheck.py:523,564`; confirmed no `[resolved…]` assertion exists). A producer-side mutation dropping/renaming the bracket would break the web parser yet pass the entire Python suite (the web fixtures are hand-authored with the bracket, so they only catch a web-side regex change). The code comment claims "the two sides can never drift," but nothing tests it. Recommend one crosscheck assertion that `_condo_note_prefix(resolution) == "condo_resolution: [resolved_single_base_lot]"`. Non-blocking: the token is `resolution.outcome` by construction and both sides agree at the frozen head (verified).

- **A2 — defensive seam branches untested.** `condo_base_lot.py:262-267` (STATUS_NOT_A_CONDO on a billing input) and `:269-270` (unknown-status fallback → OUTCOME_UNRESOLVED), plus the empty-`base_bbls`-with-STATUS_RESOLVED edge (len 0 falls into the multi-lot branch, `:239-252`), have no direct probe. All are fail-safe by construction (never map to a fabricated success), so a mutant flipping them would degrade safety without fabricating — low risk. Non-blocking.

- **A3 — AS-5 flag-off asserts zero *connector* calls, not zero *condo-resolver* calls specifically.** The condo step lives inside `build_live_substrate`, which the flag gate never invokes when off, so zero condo I/O is structurally guaranteed by the pre-existing default-off test; noted only for completeness. Non-gap.

- **A4 — `live_provider.py:269` `… or canonical_bbl` fallback is unreachable** for a well-formed RESOLVED_SINGLE (which always sets `resolved_base_bbl`); no test drives `substitutes_base_lot=True` with `resolved_base_bbl=None` (an impossible seam state). Dead-defensive; not a gap.

## Conclusion

The test surface is mutation-sensitive on every load-bearing behavior: multi-lot is never collapsed, no substrate is fabricated, the downstream fetches provably run on the base lot, the fail-safe allow-list withholds on missing/unknown/malformed tokens over a proven-displayable control, key-absence is tolerated on real 7- and 8-key bytes, and all five riders are closed with named negative tests. The green CI at `21ee7379` covers byte-identical source to the frozen `2b851c18`. The four advisory items (A1 is the most substantive — a cross-boundary token not pinned on the Python side) do not block acceptance.

**Verdict: PASS.**
