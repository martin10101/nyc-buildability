# M5-T096 — G4 test-adequacy review (qa-engineer "qa-t096", read-only)

> Transmission history: pinned at 49dcdbd2, delivered as three SendMessage parts (1/3..3/3) ending with END-OF-REPORT;
> no truncation. Joined verbatim by the orchestrator (transport wrapper tags removed only). Verdict PASS: M1-M6 reproduced;
> 11 reviewer weakenings run, all but the `__import__` allowlist bypass killed (A1, beyond AS-4); the sample test genuinely
> regenerates from the writers. Advisories A1-A4 are routed at the accept seam.

---

M5-T096 G4 test-adequacy review (qa-engineer, read-only) — part 1/3

PIN: `git rev-parse HEAD` = 49dcdbd2ab1927e8997079a03733eef3be7cc580 (== expected 49dcdbd2). Material commit d59cbfca. Worktree clean of these paths.

METHOD: baseline = documented suite `python -m pytest tests/cad/test_dxf_writer.py tests/cad/test_cad_owner_samples.py tests/cad/test_pdf_sheet_writer.py tests/cad/test_glb_writer.py -q` from services/api → 277 passed in 2.67s [OBSERVED]. Then an in-process mutation harness (Python 3.11.9, cwd services/api): each mutation patches ONLY in-memory module attributes of app.cad.dxf_writer (or the consuming test module's by-value binding, e.g. SAMPLE_LOT), restores between runs, and invokes the actual test functions; a test "reddens" iff it raises. No repo file written (my Write and shell file-writes are guard-blocked; all mutation is in-memory). All 13 target tests green at baseline.

SUPPLY-CHAIN / BLOB CHECK [OBSERVED]:
- `git check-attr -a` → binary: set, text: unset for *.dxf/*.pdf/*.glb (`.gitattributes` in force).
- raw-blob sha256 == checkout sha256 == README sha256 for all three samples (no CRLF smudge on this Windows machine): dxf 4e19c669…, pdf 8f28f12d…, glb 0bc7a37d….

PRODUCER M1–M6 REPRODUCED:
| # | Mutation | Test | Result |
|---|---|---|---|
| M1 | _write_style_table → no-op | style_defines_standard / every_text_resolves | RED / RED ✓ |
| M2 | _vport_frame → (0,0,110) | vport_active_frames_the_extents | RED ✓ |
| M3 | STYLE before VPORT | tables_in_r12_order | RED ✓ |
| M4 | STYLE font txt→arial | golden_stable / style_defines | RED / RED ✓ |
| M5 | writer drift (font) | as3 regen dxf byte-identity | RED ✓ |
| M6 | `import json` in source | (see note) | protection genuine, verified |

M6 note (ADVISORY A2, non-blocking): the producer table names `test_as4_allowlist_is_load_bearing` as going RED. That test is a POSITIVE meta-test (asserts the detector returns {"json"}) and stays GREEN. The REAL guard is `test_as4_module_import_allowlist`, which reads the actual module file and WOULD redden if json were imported — I confirmed `_imported_modules('import json\n'+realsrc) - ALLOWLIST == {'json'}` (non-empty), and neutering the detector reddens BOTH allowlist tests. So the protection is real and load-bearing; only the report's row label is imprecise.

Parts 2/3 (reviewer weakenings) and 3/3 (findings + verdict) follow.

---

M5-T096 G4 — part 2/3 — reviewer weakenings (>= 4 required; 11 run, all in-memory)

| # | Weakening | Test exercised | Result |
|---|---|---|---|
| W1 | VPORT centre off by +width/2 | vport_active_frames_the_extents | RED ✓ |
| W2 | view height = 0.5× span (no longer frames) | vport_active_frames_the_extents | RED ✓ |
| W3 | TEXT given group-7 style "FANCYSTYLE" not defined by any STYLE record | every_text_resolves_to_a_defined_style | RED ✓ |
| W4 | LTYPE emitted AFTER LAYER (violates R12 LTYPE-before-LAYER) | tables_in_r12_order | RED ✓ |
| W5 | regenerate with a DIFFERENT synthetic lot (test SAMPLE_LOT shifted) while committed file unchanged | as3 regen dxf byte-identity | RED ✓ |
| W7 | (vacuity) VPORT view direction (0,0,1)→(0,0,0) | vport_active_frames_the_extents | RED ✓ |
| W8 | (vacuity) STYLE fixed height 0→2.5 (would override per-TEXT height) | style_defines_standard / golden | RED / RED ✓ |
| W9 | (vacuity) mutate PROPOSED_LABEL constant | as4_annotation_carries_required_labels / regen dxf | RED / RED ✓ |
| W9b | (vacuity) mutate CRS_UNITS_NOTE constant | as4_annotation_carries_required_labels | RED ✓ |
| W-asp | emit group 41 aspect=0.1 while frame math uses 1.0 (breaks horizontal coverage) | vport_active_frames_the_extents | RED ✓ |
| W6 | dynamic-import bypass of the allowlist | (see A1 below) | mixed |

KEY POSITIVES:
- W5 answers "does the sample test truly regenerate, or compare the file to itself?" — it GENUINELY regenerates: shifting the test's SAMPLE_LOT (the builder's input) while leaving the committed file untouched reddens the byte-identity test. It is not a file-vs-itself compare.
- W3 = your "STYLE present but a TEXT uses an undefined group-7 style" case — caught.
- W4 = your "LTYPE after LAYER" case — caught (the exact order VPORT,LTYPE,LAYER,STYLE is pinned, so LTYPE-before-LAYER is enforced).
- W7/W8/W9/W9b/W-asp confirm the VPORT view-direction, STYLE fixed-height, honesty-label literals, and VPORT aspect assertions are all NON-VACUOUS (each reddens when the value it pins is displaced). The honesty labels are asserted as HARDCODED literals AND the constants are pinned — the correct pattern (no tautology).

README-sha staleness (in-memory README-string mutation, files read read-only):
- baseline green; corrupting one hex digit of the dxf digest in the README → RED; drifting the file's bytes while keeping the README → RED. The test is load-bearing.

Part 3/3 (specific-question answers, findings, verdict) follows.

---

M5-T096 G4 — part 3/3 — specific-question answers, findings, verdict

YOUR TARGETED QUESTIONS:
- VPORT centre off by half extents → caught (W1). View height < extents → caught (W2). STYLE present but TEXT uses undefined group-7 style → caught (W3). LTYPE after LAYER → caught (W4). Different synthetic lot with stale committed file → caught (W5) AND stale README digest → caught (W10). importlib bypass → CAUGHT (W6b: `import importlib` is flagged, importlib∉allowlist). `__import__` bypass → NOT caught (W6a, see A1).
- Sample test truly regenerates from the writers (W5), not a file-to-itself compare.
- README-sha test hashes the CHECKOUT bytes, not the raw blob (A3), but is adequately protected — see below.
- Vacuity: no vacuous assertion found; every pinned value reddens when displaced (W7/W8/W9/W9b/W-asp), and honesty labels use hardcoded literals + constant pins.

BLOCKING: none.

ADVISORY (non-blocking):
- A1 — Import allowlist AST scan is blind to the `__import__("name")` builtin (W6a survives: it produces no ast.Import/ast.ImportFrom node). `importlib.import_module` IS caught because it needs `import importlib`. This is beyond the stated AS-4 mutation ("add import json reddens", which IS caught) and is mitigated by the dependency-security lockfile/age gate (a real DWG lib still can't install) and code review. Optional hardening: also flag ast.Call to a `__import__` Name and any `importlib` reference.
- A2 — Producer mutation-table row M6 mislabels which test reddens (part 1); protection is genuine.
- A3 — README-sha test reads checkout bytes, not the raw git blob. Adequately closed: `.gitattributes` marks the three files `binary` (verified in force); blob==checkout==README on this machine; and it FAILS SAFE (a lost binary attribute smudges the DXF and reddens BOTH the readme-sha and byte-identity tests). G1 independently verified raw-blob==README. Optional: hash via `git cat-file` for belt-and-suspenders.
- A4 (minor) — `test_as4_module_import_allowlist` also asserts `ALLOWLIST - imported == {}`, forcing the module to keep importing all four allowlisted names; a legitimate future refactor dropping one (e.g. collections.abc) would redden it. Intentional non-vacuity; low brittleness.

CONSUMER SWEEP: baseline 277 includes test_pdf_sheet_writer.py and test_glb_writer.py (both green); public names + CLAIM_CLASS_WORDS byte-unchanged, so no silent-red consumer.

The test set is adequate: it kills every producer mutant and all 11 reviewer weakenings, contains no vacuous assertions, genuinely regenerates the samples from the accepted writers, and ties the README digests to the file bytes with a fail-safe binary-attribute guard.

M5-T096 G4 VERDICT: PASS

END-OF-REPORT
