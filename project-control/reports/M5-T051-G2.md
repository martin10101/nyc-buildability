# M5-T051 G2 — producer self-check record (recorded by orchestrator)

The producer's self-check is the report's validation section: the documented suites run
in-worktree with real outcomes; the report leads with the implementation map and honest
scoping (the DB-034(c) closure isolated; B0 semantics otherwise preserved).

Orchestrator reproduction at harvest (wt-m5t051, documented cwds): ruff clean; 568
scenario tests; modularity exit 0; and tests/api 439 IN THE WORKTREE. CI: run
35455214032 all jobs success at f1ec64a8, first round (M5-T051-ci-evidence.md).

## [ORCH-CORRECTED per G4 finding 1] api-count record correction

The "tests/api 439" figure in the producer report, my harvest reproduction, and the
source-sections/ci-evidence files is the STALE WORKTREE BASE count: wt-m5t051 was created
at the T051 claim seam (before the M5-T052/M5-T053 acceptances), so its tests/api tree
predates the +15 condo-records and +43 proposal-validation suites. At the frozen
submission head (f1ec64a8) and at every later head the TRUE count is 497 — G4 executed it
(497 passed, zero failures) and attributed the +58 delta commit-by-commit to the two
accepted disjoint tasks via ancestry + byte-stability. No hidden regression; the
correction is recorded HERE (the evidence files outside allowed_paths carry pointers to
this note; the frozen producer report is deliberately not edited post-submit).

Run-history note: the build survived four provider-side model-downgrade collision stops
(runs 15/17/18) and one supervisor crash (run 16) with no build failure — the work
accumulated across runs under the between-runs drills.

Verdict: PASS.
