# M5-T123 — G4 test review (qa-engineer "qa-t123", read-only)

> Transmission history: pinned at d1970b06 (material de92182d; CI run 36115519503 at d1970b06: api job green, web-e2e
> red on one unrelated Playwright focus test - the zero-delta rerun 36116139678 at b0eb29f7 was fully green),
> delivered as 3 SendMessage messages ending with END-OF-REPORT; no truncation. Joined verbatim by the orchestrator
> (transport wrapper tags removed only). Verdict PASS; independent mutants Q1-Q6 caught, Q7/Q8/Q12 survive but are
> behaviourally equivalent, Q9-Q11 survive = ADVISORY 1-2 (scene/export streamed-path and exact-ceiling tests); A3 the comment
> symbol.

---

M5-T123 G4 TEST REVIEW (independent, READ-ONLY) — part 1/3: scope, baseline, and the CI-red clarification.

PIN: HEAD at start = d1970b06 (matches "expected d1970b06 or later"). Material de92182d = cherry-pick of wt-m5t123 8d226b2f; git diff-tree confirms EXACTLY the 9 allowed paths, no forbidden path. proposal_validation.py untouched (MAX_BODY_BYTES stays 256 KiB; the 3 routes dropped the import). app/main.py untouched (routes stay UNMOUNTED). Env: local Python 3.11.9, ruff 0.13.0, anyio 4.10.0.

BASELINE (cwd services/api): documented pytest `tests/resilience tests/scenario/test_scene_api.py tests/drawings/test_dxf_import_api.py tests/cad/test_export_api.py -q` -> 199 passed (matches producer; was 190 in T117; +9). `ruff check .` -> All checks passed. `modularity_check --check` -> DIRECT_EXIT=0 (the 3 warnings are out-of-scope tools/ files; none of the 4 changed files). No test function REMOVED (comm of `def test_` names parent->material: scene 28->31, export 20->23, dxf 35->37, rate_limit 21->22 — additions only).

CI (head d1970b06): the run shows overall FAILURE, but this is NOT a T123 defect. The authoritative **api (ruff + pytest) job PASSED** (1m16s) under Python 3.12.14 — that is the T123 backend suite. The failure is isolated to **web-e2e -> Playwright human journeys (S1-S8)**, an external-network flake: `pluto_soda throttled (429)` from data.cityofnewyork.us inside e2e/a11y-announcements.spec.ts. T123 touches only UNMOUNTED backend Python (no apps/, no packages/), so it cannot affect web-e2e; and the very next push (M5-T121 accept seam, identical web code, +7 min) shows CI success — confirming the web-e2e red is transient. (I stopped further gh drilldown at the shared 5,000/hr GitHub rate limit; the api-green + web-flake conclusion is firm.)

Continued in part 2 (independent mutants: AST guard, slot-sum, principal, handshake, DXF deviation 1).

---

M5-T123 G4 — part 2/3: independent mutants (in-process; monkeypatch in `python -c`; nothing written to the repo).

Producer's 7 re-confirmed where load-bearing; my independents beyond them:

- Q1 AST guard DROP-ONLY-"Counter" from `_LIMITER_CONTAINER_CALLS` -> RED (test_ast_guard_also_catches_list_set_counter_limiters). Q2 revert to dict/deque-only (= producer M6) -> RED. Broadened guard catches list()/[]/set()/{'a'}/Counter()/collections.Counter(); leaves non-empty constant lists (__all__, _ROUTE_METHODS) and frozenset matrices alone (verified). CAUGHT.
- Q3 slot-sum: scene 8->24 so SUM==40 -> RED ("summed slots 40 must stay under the anyio pool 40") — the exact mutant you asked about. Q4 scene->17 so SUM==33, headroom 7<8 -> RED (the `pool-total>=8` assertion is load-bearing). Q5 scene->13 (>export 12) breaks heavier->fewer -> RED. The guard reads LIVE constants (imported from the 3 route modules) AND the LIVE anyio limiter (`current_default_thread_limiter().total_tokens`, observed 40) — NOT hard-coded copies. CAUGHT on all three.
- Q6 principal `and principal` (no strip) [= producer M5] -> RED (whitespace "   " keys as principal, diverging from host). Q7 `.lstrip()` / Q8 `.rstrip()` only -> SURVIVE, but behaviorally EQUIVALENT, not a gap: any all-whitespace string reduces to "" (falsy) under lstrip/rstrip/strip alike, and any string with content stays truthy under all three, so the keying decision is identical (truthiness table verified over " ", "\t", "\n", " \t\n ", "", "user-9", " u "). Real caller_key confirmed: whitespace-only -> host; " u " -> "principal: u " (raw kept).
- Q12 handshake: restore sleep(0.05) -> SURVIVE (a determinism fix, not a behavior change). The handshake IS proven sound: `_blocking_work` sets `started` before blocking; the runner blocks on `started.wait(5.0)` before the 0.05s deadline arms; resilience suite 98 passed x6, and the two post-start-timeout tests 20/20 under 4x background-CPU load — deterministic, no sleep-based race.

DXF deviation 1 (small monkeypatched ceiling + real-value arithmetic): genuinely equivalent to testing the real 20 MiB value. The enforcement compare is value-agnostic, so proving the MECHANISM at 4096 (declared 413, streamed 413, exact `>` boundary) PLUS the arithmetic test pinning the VALUE (>=20MB decimal; `_IMPORT_DXF_LIMITS.max_bytes == DXF_IMPORT_MAX_BODY_BYTES` so the reader clamp is raised WITH it; <= 64 MiB `_LIMIT_CEILINGS` hard clamp — value confirmed) covers it. Rigorous.

Continued in part 3 (scene/export streamed-path + boundary gaps; comment nit; verdict).

---

M5-T123 G4 — part 3/3: findings + verdict.

BLOCKING: NONE.

ADVISORY (file:line):
1. Scene/export prove ceiling+1 -> 413 on the DECLARED-length path only, NOT the STREAMED (no-Content-Length) path — unlike DXF (test_over_ceiling_streamed_refused_413). Via a TestClient probe I confirmed the scene/export streamed guard IS correct (force `_declared_content_length`->None + over-ceiling body -> 413), but it is the SOLE refuser of a no-CL over-ceiling body (defeat `_read_body_within_ceiling` -> 422, not 413), and the committed test sends a Content-Length body so it still 413s with the streamed guard defeated. So an "enforce only on Content-Length" mutant SURVIVES the scene/export suites (test_scene_api.py:325, test_export_api.py:259). Mitigating: pre-existing (T111/T117), UNMOUNTED, the shared helper IS tested in DXF, and the admission tests (~811KB/~452KB) do traverse the streamed path with the real constant (so a LOWERED constant is caught; only a fully-defeated guard survives). RECOMMEND (non-blocking): add the one-line no-CL streamed test to scene/export, mirroring the DXF test.
2. Scene/export have no exact-ceiling boundary test, so a `>`->`>=` off-by-one SURVIVES (probe: exactly-ceiling admitted today, status 422 not 413). DXF covers it (test_upload_exactly_at_ceiling_accepted_plus_one_refused). Minor — a `>=` safely/conservatively refuses a body of exactly the ceiling. RECOMMEND: assert exactly-ceiling admitted for scene/export.
3. dxf_import_api.py:119 comment names `dxf_reader._MAX_LIMITS`; the real symbol is `_LIMIT_CEILINGS` (dxf_reader.py:190). Comment-only inaccuracy; code + test reference `_LIMIT_CEILINGS` correctly. RECOMMEND: fix the comment symbol.

AS-5 invariance holds: the rate_limit.py diff changes only caller_key (strip) + docstrings; no change to 429 shape / per-window limits / deadlines / correlation ids / disabled-404; the full 199-suite (which exercises those) is green. Scope exact (9 allowed paths); `__all__` updated cleanly; ruff clean; modularity 0.

Net: the packet's binding SAFETY/AS-1 bar (per route: ceiling+1 refused + a maximal legitimate request admitted) is MET for all three routes; every NEW behavior (per-route ceilings incl. the raised DXF reader clamp, the summed-slots-under-40 guard, the whitespace principal, the broadened list/set/Counter AST guard, the event handshake) is covered by a reddening mutation; the residual gaps are inherited depth-of-coverage on UNMOUNTED routes, consistent with the M5-T117 G4 ADVISORY precedent.

Mutant table: Q1-Q6 CAUGHT (RED); Q7/Q8/Q12 SURVIVE-but-behaviorally-equivalent; Q9/Q10 (scene/export streamed-defeat) + Q11 (off-by-one) SURVIVE = ADVISORY 1 & 2 above.

M5-T123 G4 VERDICT: PASS
END-OF-REPORT
