# M5-T078 + M5-T079 — G3 independent code review (code-reviewer "cr-web", read-only; joint review)

> Transmission history: one joint review pinned at 33662211 (materials 5879cdfa / 006f3589 blob-identical
> through acb224b2 and the pin), delivered as six SendMessage parts (1/6..6/6) ending with END-OF-REPORT;
> no truncation. Joined verbatim in delivery order by the orchestrator (transport wrapper tags removed
> only); saved identically as M5-T078-G3.md and M5-T079-G3.md. Verdicts: M5-T078 FAIL (F1-F3 BLOCKING),
> M5-T079 FAIL (F1-F2 BLOCKING).

---

M5-T078 / M5-T079 G3 code review (code-reviewer, read-only) — part 1/6.

PIN: HEAD 33662211137a36f0ff4b7c8ac8ae779f75943f8a. Materials 5879cdfa (T078) and 006f3589 (T079) are ancestors of acb224b2 and of the pin (git merge-base --is-ancestor). All 13 listed blobs are the same at the material commit, at acb224b2 and at the pin (git rev-parse <commit>:<path>).
CI 35975846444 (gh run view, read-only): headSha acb224b27ebd, conclusion success, 18/18 jobs success, including web (lint + typecheck + build) and web-e2e.
Scope: T078 touches exactly its 6 allowed paths and T079 exactly its 7. No forbidden neighbour is edited, and nothing changes in services/api, packages, package.json or any lockfile. T078 has no e2e edit. max_envelope does not appear in services/api/app/main.py, so the route is still unmounted.
Code graph: `query.py --no-regen impact` returned "STALE (stale fingerprint): refusing to serve the cached graph". I did not regenerate it; the consumer sweep was done with grep.
Not run: npm, npx, node, vitest, playwright, test_directive_compliance.py.

M5-T078 FINDINGS
F1 BLOCKING — the new blocked-Convert path overwrites the refusal card's explanation, which weakens a disclosure. The refusal card shows the shared announcer text: ProposalOutlineDraw.tsx:420 `<p>{announcement}</p>`. T078 adds a second writer to that state, onConvertActivate (:231-237, write at :233). Before T078 the `disabled` attribute meant this path could not be reached.
Repro (jsdom):
1. fetchImpl returns 422 out_of_neighborhood (or 404, which gives feature_unavailable).
2. Fill 3 finite points and click Convert. The card reads "Outline not converted: a drawn point fell outside the shown lot…".
3. Clear one longitude (2 finite points left) and click Convert (aria-disabled). The same card now reads "Outline not converted / Convert needs at least 3 points with both coordinates filled in…".

The server's refusal reason is gone. For feature_unavailable, the card now invites the user to add points to a feature that is not available. Pressing Convert again during a new conversion also puts "Conversion is already in progress." inside the old card.
Fix: build the card text from announcementForOutlineBridge(outcome) and use `announcement` only for the announcer. Add a spec that goes refusal → blocked activation.

---

M5-T078 G3 — part 2/6.

F2 BLOCKING — the status and instruction text describe map interactions that are not there. This breaks the DB-047(d) rule stated in the file's own header (ProposalOutlineMap.tsx:26-35), the same class as the T071 HJ B1/B2 corrections.
(i) :204 adds " — it has no coordinates yet, so a map click will place it" whatever mapSurface is. On every typed-fallback lot (condo, multiple_features, invalid_geometry, no-WebGL, render error) and while the map loads, a screen reader hears that a map click will place the point. Meanwhile the instructions (:185) say "The reference map on this lot has no interactive drawing surface." Repro: lotMock.variant="lot-outline-empty", points=[NaN/NaN], selectedIndex=0 → the status contains "a map click will place it".
(ii) :206 adds " The lot map is ready to draw on — click it to place points." even when a finite point is selected. In that state a click MOVES the selected point (:167-170).
(iii) :181 (untyped point selected, map present) says "Click the point again to deselect." That point is not drawn, so there is nothing on the map to click.
No spec covers the status clause in fallback states.
Fix: gate each clause on the same mapSurface × selection × selectedIsFinite state the instructions use, and add specs for fallback states and for a selected point.

F3 BLOCKING (one-line fix) — AS-5 duplicate pair predicate. ProposalOutlineDraw.tsx:286-288 builds rowComplete = Number.isFinite(p.lng) && Number.isFinite(p.lat) outside isDrawnPointFinite, split across three lines. The report's single-line grep cannot find it. If the shared predicate is ever tightened (for example with a range check), rows that Convert omits will lose their marker and aria-invalid. That is the silent-omission defect (a) and (b) are meant to close. Fix: `const rowComplete = isDrawnPointFinite(p);`. Keep the per-ordinate calls only for naming which ordinate is missing.

---

M5-T078 G3 — part 3/6.

ADVISORY
A1 — The disabled look is gone. architect.css:45 `.architect-shell button:disabled {cursor:not-allowed;opacity:.6}` no longer matches, so a blocked Convert (ProposalOutlineDraw.tsx:355-363) looks and hovers like an active primary button. The surface mounts inside ArchitectShell (ArchitectEntry.tsx:49/:219). The CSS file is outside allowed_paths, and the report's DISCOVERIES section does not mention it. Please route it (an [aria-disabled="true"] rule).
A2 — The copy at :202-203, "fill it in or delete it to include it", says deleting a row includes it. For several half-typed rows, "without their coordinates" is also wrong. Fix this together with F1.
A3 — A second blocked activation with the same reason is silent. The state value is identical, so the text does not change, and OutcomeAnnouncer only speaks when its text changes. Clear the message first, then set it.
A4 — Convert has no aria-describedby pointing at the omission hint (:366-375), and the aria-invalid inputs are not linked to the row marker. A screen-reader user on Convert does not hear how many rows will be left out.
A5 — The AS-3 spec counts live regions inside the wrapper only (proposal-outline-map.test.tsx:358-378). AS-3 asks for the count across the whole drawing section.
A6 — The copy table is missing "Conversion is already in progress." (:206).
A7 — The map-ready sentence repeats on every status change, which is verbose for screen-reader users.

VERIFIED OK
- aria-invalid is set only on the missing ordinate (:305/:314).
- aria-disabled keeps Convert focusable. A blocked activation makes zero bridge calls; the spy check is valid because fetchImpl is called before any await (outline-bridge-api.ts:348-378).
- No live region was added.
- The (g)/(h) changes keep the duplicate-source and stale-payload checks (lot-outline-map.test.tsx:648-740). grep finds no call-count assertion on addLayer.
- The F8 comment is present (ProposalOutlineMap.tsx:154-161).
- The six disabled→aria-disabled assertion changes are equivalent.
- Consumers proposal-editor.test.tsx:165-166 and e2e :334/:415/:418 only use the convertible path. Public props are unchanged.

M5-T078 G3 VERDICT: FAIL

---

M5-T079 FINDINGS — part 4/6.

F1 BLOCKING — the XOR check lets a row with BOTH fields through when its gap_reason is present but blank or not a string. max-envelope-api.ts:344 runs gap_reason through stringOrNull, which turns "", " ", 7 or {} into null, BEFORE dimensionRowKind (:591-596) classifies the row. binding_value goes through finiteOrNull (:337), which can only fail closed at the row level, so gap_reason is the side that leaks.
Repro: completeBody() with dimensions [bindingDimension({gap_reason: ""}), max_height 60] and summary {binding 2, gap 0}.
- The row renders envelope-value-max_far_floor_area "20000 sq_ft" (data-row-kind="value").
- The aggregate reads "All 2 preliminary development limits were checked" (data-complete="true").
- gap_reason: 7 gives the same result.

The server has broken its own "exactly one is set" rule and nothing on screen shows it. This is the exact case D-083-R004 and (d) exist for. The panel itself treats "" as a reason that is present but not stated (gapReasonCopy, MaxEnvelopePanel.tsx:79), yet classification throws it away.
Fix:
- Have boundDimension record whether each raw key is present (not null or undefined) for both fields.
- Classify both-present, neither-present and present-but-malformed rows as contract_violation.
- Stop the detail at MaxEnvelopePanel.tsx:144 from saying "returned neither" when the value was malformed.
- Add specs for "" and for a non-string token.

---

M5-T079 G3 — part 5/6.

F2 BLOCKING — cluster item (i) and AS-1 were not delivered, and the report does not say so. The source finding (M5-T070-G4.md:160) asks for PANEL rendering of client_timeout, aborted, payload_too_large and invalid_request, plus "boundCandidate rejection paths other than wrong srid". AS-1 asks for "one spec each".
What was delivered:
- api classification specs (max-envelope-api.test.ts:411-445; the payload_too_large and invalid_request ones already existed before T079, at :248-255);
- one panel failure-card spec, for internal_error only (max-envelope-panel.test.tsx:454).

Missing:
1. No panel spec renders the client_timeout, payload_too_large or invalid_request card with its reason text.
2. None of the seven boundCandidate rejects other than the srid one (max-envelope-api.ts:268/271/274/280/287/291/297) has a spec. The report never mentions this item, yet it claims (i) is closed.
3. The new aborted guard (MaxEnvelopePanel.tsx:288) has no spec that fails without it. Delete the guard and every current spec stays green: the superseded spec (:429) resolves a 500, which the token check drops, and the loading spec's abort happens after unmount.

The report says the guard cannot be observed in a test. It can:
- render with an abort-honoring stub;
- rerender with request=null and flush;
- rerender with REQUEST;
- record inserted envelope-failure nodes with a MutationObserver or takeRecords() (or a render spy).

---

M5-T079 G3 — part 6/6.

ADVISORY
A1 — "Complete" trusts the server's summary.gap (max-envelope-api.ts:605-611). If the client shows a row as a gap while summary.gap is 0, the aggregate still reads "All N … were checked". This dates from T070. Also derive completeness from the row kinds.
A2 — The null-request branch (MaxEnvelopePanel.tsx:271-275) does not bump activeRef. A fetch that finishes normally after the abort is stored and announced after request→null: a fetchImpl that ignores the signal (as the panel's own test stubs do), or a body that was already fully buffered. Bumping the token in the null branch and in cleanup makes (c) rest on the token and makes it testable.
A3 — announcementForMaxEnvelope (:631-637) never mentions the contract violation and says "0 of N could not be checked". The visible aggregate has the same "0 of N" wording (report D3).
A4 — The claim wall (panel test :287-295) only collapses ASCII "-" and whitespace, so Unicode dash variants such as a non-breaking hyphen (U+2011) or a minus sign (U+2212) get through. Its line-comment strip would also drop the rest of a line after a "//" inside a string (none today).
A5 — The CRS guard (api test :516-530) does not catch MapLibre MercatorCoordinate/fromLngLat (maplibre-gl is already a dependency) or hand-written degree/feet math.
A6 — (m) only keeps the client union and the copy map in step. A new server EnvelopeGapReason value would still drift silently and render as the raw token. A spec that parses max_envelope.py:131-146 would catch it.
A7 — The e2e captures only the last request (proposal-editor.spec.ts:553-557). Collect every request and assert exactly one POST.
A8 — Adoption is still offered when a contract violation is present (candidateIsAdoptable, :616-622). Consider gating it; it cannot be reached in production today (DB-050(a)).

VERIFIED OK
- D-083 strings unchanged (MaxEnvelopePanel.tsx:302/:182/:228); no "maximum allowed" or "demonstrated maximum" claim.
- The aggregate stays incomplete for a valid both/neither row.
- The gap-reason union matches max_envelope.py:131-146 exactly.
- Record<EnvelopeGapReason,string> is exhaustive at compile time, and the SEC-F1 own-property guard is kept (:74-81).
- The dead street_width_class read is gone and the seed stays "" (maxEnvelopeRequestForProfile never set it).
- The e2e now fails with an explicit diff.
- entry.test.tsx stubs fetch and unstubs it afterwards (:32/:403).
- Scope is exact, services/api is untouched, and the route is unmounted.

M5-T079 G3 VERDICT: FAIL

END-OF-REPORT
