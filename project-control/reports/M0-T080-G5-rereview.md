# M0-T080 G5 security re-review, round 2 (verbatim reviewer return)

Saved VERBATIM by the orchestrator from the read-only reviewer agent's return channel
(report-preservation rule). Reviewer: t080-g5-rereview (independent). Verdict PASS with one
blocking required correction (model-axis stream-silence fail-open) — independently corroborates
G3 re-review R-2 and raises it to important. Round-1 must-fix M1 fully closed. Feeds a targeted
round-3 correction before acceptance.

---

# G5 adversarial security RE-REVIEW — M0-T080 round 2 (corrected)

**VERDICT: PASS — with one blocking required correction** (recorded PASS per gate-verdict semantics; the correction is blocking for the next gate and for acceptance).

Reviewed identity `de4d5505cb217c8ed7d84456bcc6ee994f39ed73`, material manifest `e43566981ebfd55d75258a5cc0782280bffff8605f7f70eec6af7c2b0713b0ed` — recomputed independently through project_control._task_git_identity over the task's allowed_paths, matching before and after. Tree clean; wrote nothing.

## Direct answers
**Is round-1 must-fix M1 closed? YES, completely.** Re-attacked adversarially.
**Is the fail-open family fully closed? NO — four of five axes.** The MODEL axis still returns ok=True on stream silence. Raising it from minor to IMPORTANT; the behaviour finding is unchanged, only severity calibration moved.

## M1: re-attacked and closed
Round-1 script + wider sweep. Armed a reorientation gate (task M0-T080 / branch task/x / worktree C:/wt / model approved-model-A), fed a checkpoint passing ClaudeCheckpoint.validate() with all five identity fields blank. Round 1: READY gate PASSED / mismatches []. Now: require_ready raises ready_without_session_id, verify_post_launch ok=False with four "successor reported NOTHING" mismatches. Also closed: blank-session archived smuggle (refused before the archive is consulted, even when the archive literally contains ""); resume-gate blank-id (blank → ready_without_session_id; populated session vs a RESUME gate recording no provider session → resume_gate_without_session). Sweep: each axis blanked individually (all caught, one mismatch each), whitespace-only (caught), None/0/False/[] type confusion (caught), duck-typed checkpoint missing attributes (caught). Only an object whose __str__ equals the commanded value passes — that IS the expected value. Structural: test_an_axis_the_supervisor_never_commanded_is_not_invented preserves a skip when no expectation was written, but that is unreachable — the expectation is built in execute from the same SeamFacts that build_handoff puts through rotation.validate_handoff, and handoff.py:57 exempts only changed_files/open_blockers/reviews_and_findings/owner_gates/evidence_digests/tests_and_ci; branch, worktree, task/stage, next action, HEAD must be non-empty or the build raises handoff_incomplete first. All four axes always commanded in production.

## THE BLOCKING RESIDUAL — model axis fails open on stream silence
Severity IMPORTANT. Not a new hole (unchanged from round 1, where I carved this axis out), but the written justification is false and the package contradicts itself. turnover_seam.py:583 guards `if expected_model and observed_models:`. Docstring :549-555 claims "A stream that reported no model at all is caught there [R739], not here." FALSE: claude_runner.inspect_stream sets model_mismatch=True only when an event reports a model DIFFERING from expected_model (claude_runner.py:692); a stream naming NO model and an empty event list both return observed_models=() with model_mismatch=False; nothing requires a non-empty observed set.

Exact reproduction (scratchpad/attack_model_axis.py, real code at frozen HEAD): arm a gate with model_id=approved-model-A; the successor reports all four hardened axes CORRECTLY but is on a different model whose stream carries no model identifier. require_ready PASSES; verify_post_launch(observed_models=()) → ok=True, mismatches=[]; verify_post_launch(run_result=None) → ok=True, []; returned reason "the successor reported the commanded task, branch, HEAD, and model" — the success string asserts a model report that never happened.

Decisive point: the package already implements the correct rule one layer down — claude_runner.py:1660-1664, a launch probe whose process exits without reporting a model id is PROBE_MODEL_NOT_REPORTED → unavailable, fail closed. So the probe layer treats stream silence as "proves nothing" while the post-launch identity gate treats the identical signal as "commanded model confirmed." Reachability is wider than an impostor: any cycle whose stream parsing yields no model events (truncated stream, provider CLI format change, RunResult default observed_models=() on a degraded path) silently disables the axis. Provider-drift fragility, not only a forged stream. Mitigating: SHADOW-ONLY, unactivated.

Fix (three lines, matching the other four axes): when run_result is supplied and expected_model is set, an empty observed_models is a mismatch. Keep the run_result is None skip so a caller that supplied no run result does not fail a cycle it cannot judge. Correct the false docstring sentence either way.

## Other corrections — all verified closed, no new hole
U5 — string attack dead ("sure, it launched", True, {"ok":True}, 1, raising probe all → model_probe_failed; only a real ProbeOutcome(ok=True) passes). U6 — turnover_no_approved_successor row before the safe stop; _safe_append degrades an unwritable chain to "(unaudited: …)" not a crash. U7 — honest both branches (no seam → "NOTHING was probed", phrase "tried by an actual launch probe" absent; real failing probe → all candidates tried). U8 — does not loosen: _claude_cli_identity(exe) and runner.executable_identity()["digest"] are byte-identical on a real file so a start-recorded probe satisfies the watchdog; a watchdog with no --claude-executable gets "" and matches no start-recorded probe (still fails closed); start cannot produce "" (executable_identity raises on empty path); watchdog wires no probe seam (model_probe defaults None); ProbeLedger still checks config identity AND CLI version. U11 — ConfigError from _run_loop → refusals.UNSAFE with the exception's code, nonzero exit; converts a crash-stop to a refusal-stop. U12 — confirmed by source order (arm before complete) + a real crash test (patched complete_rotation raises → armed gate, no last_rotation, still-pending, rotation_ready_required next). U2 — all unknown combinations incl. both-unknown → reorientation cross_model; matched pair resumes. U3 — FactSource has ZERO references outside turnover_seam.py (not production-wired); labels distinct; scope block names 6/8; report §3.3 states the overstatement, names arm (b) with reason, says verified_by_model can no longer imply an independent check.

## Regression and held properties
pytest -k agent_supervisor → 1854 passed, 2 skipped, 555 deselected, 0 failed (392.90s). modularity_check → 280 files, 0 failures, 5 pre-existing warnings. No silent/unlisted substitution route (removed constants only in docstrings/history/negative tests + a sweep test asserting they cannot return; only new default is --claude-executable default=None, fail-closed; _CONTROLLER_ONLY_KEYS still carries approved_models/model_chain). Activation surface unchanged (RUNNABLE_MODES still (shadow,supervised); actuation needs the explicit flag + job-object containment). Containment untouched (process.py/policy.py/github_flow.py/run_budget.py absent). Supply chain clean (new imports contextlib/io/typing + intra-package; no manifest changed). No test weakened: 4 removed (3 dead-gate + 1 renamed/strengthened), 25 added, no skip/xfail; producer's "+24/-3" is imprecise (actually +25/-4, one a rename) — report-accuracy nit, no live coverage lost.

## Residual minors (non-blocking)
1. Round-1 N3 unchanged: probe_record returns a freshly recorded ProbeRecord without re-checking matches(), so a probe reporting a different CLI version authorizes that one selection and writes a record that can never authorize again. Latent — no probe seam wired.
2. _safe_append keeps # pragma: no cover on its catch though U6 added a test covering it. Stale pragma.
3. READY gate key rotation_ready_gate/<run_id> and SeamTurnover.run_id defaults "", so two default-constructed seams share a key. Unreachable in production (run_id derives from checkout key).
4. verify_post_launch checks no session/continuity axis itself; the pairing with require_ready in post_rotation_gates covers it (deliberate read of the durable gate before require_ready clears it). Any future second caller must preserve that ordering.

## Verdict framing
Round-1 M1 closed; nothing introduced opens a hole. If the bar is "fail-open family fully closed," this is a FAIL by that bar — one axis still fails open with a false stated backstop. Returning PASS because the residual is pre-existing, was carved out in my round-1 fix direction, and needs stream silence to reach; but I would not accept with the false justification in the code. Simplest path: fold the three-line hardening + docstring correction into a targeted fix, not a full rework round.

## Commands run
git rev-parse/status/log; git show --stat 31e6b87; git diff ee6e688 31e6b87 per-file across turnover_seam/session_continuity/approved_models/turnover_controller/turnover_adapters/rotation/loop/loop_turnover/turnover_wiring/worker_turnover/cli/config.example.toml + test files; git diff --name-only for containment + manifests; git grep removed constants/model literals/RUNNABLE_MODES/activation/observed_models/fact_source/_CONTROLLER_ONLY_KEYS/new imports; full reads of turnover_seam, post_rotation_gates, _no_launch/_safe_append, probe_record/_attempts_sentence/ProbeLedger, decide_continuity, validate_handoff, gate placement loop.py:2037, U8/U11 CLI paths; python -c frozen-identity recomputation (twice); four read-only scratchpad probes (attack_u1.py eight families, attack_rest.py U2/U5/U7/U8/U11/U12, probe_r739.py, attack_model_axis.py); pytest -k agent_supervisor; modularity_check --check. No repo writes, no git mutations, no project_control.py.
