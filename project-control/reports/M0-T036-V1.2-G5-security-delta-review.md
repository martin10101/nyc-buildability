<!-- Orchestrator preservation header (the only non-reviewer content in this file):
     G5 SECURITY leg of the M0-T036 V1.2 delta re-gate (D-007-R599). Reviewer identity:
     g5-security-reviewer-m0t036-v12 (security-reviewer role, Fable-5-pinned, independent,
     read-only; producer backend-engineer != reviewer). Frozen SHA
     98da15e04d081730c383555f7a2c6aec7c695200. Preserved VERBATIM on arrival 2026-08-04;
     transport entity-decoding only. -->

<!-- VERBATIM REVIEWER RETURN BEGINS -->
PASS — G5 SECURITY delta re-gate, M0-T036 V1.2 + V1.2.1, frozen SHA 98da15e04d081730c383555f7a2c6aec7c695200 (HEAD confirmed identical), branch task/M0-T036-supervisor-bridge. Reviewer: g5-security-reviewer-m0t036-v12 (independent, read-only; producer backend-engineer/v121-producer). No BLOCKING findings. 272 supervisor tests pass at the frozen SHA. Six findings: 2 non-blocking (Medium/Low), 1 redaction advisory (Low), 3 informational.

== FINDINGS ==

F-1 (non-blocking, MEDIUM — latent honesty gap, fail direction safe): a substitution "relaunch" never changes the model the worker is actually launched with. `_substitute_at_seam` sets only loop bookkeeping (`self._current_model = SUBSTITUTE_MODEL`, tools/agent_supervisor/loop.py:946); the ClaudeRunner's RunnerConfig.model is fixed once at assembly (tools/agent_supervisor/cli.py:1966-1969) and `build_argv` always emits `--model <pinned>` (tools/agent_supervisor/claude_runner.py:176-177). Nothing in loop.py ever touches runner.config (verified by grep: no `runner.config` / `config.model` writes). Scenario: if a live availability probe were wired and returned (False, "quota_exhausted") to an orchestrator-role session, the audit chain would record a first-class `model_substitution` event claiming the session "relaunches EXPLICITLY on claude-opus-4-8", while the next `run_unit` still launches `--model claude-fable-5` (the exhausted pin) — the substitute never actually runs. Fail direction is SAFE (never weaker than the pin; the unit would fail on the exhausted pin and per-event verification stays bound to the pin), but the first-class audit record asserts behavior the assembled system does not perform — an audit-integrity defect. Currently unreachable from the CLI (see F-6/disposition b: the probe is injectable only programmatically and defaults always-available). The unit tests (ModelSubstitutionTests, tools/test_agent_supervisor_loop.py:1431-1571) use FakeRunner and cannot catch this. Remediation before any live probe is wired: at the seam, rebuild the runner (or dataclasses.replace its config) with model=_current_model and define expected_model semantics under substitution; or amend the audit note to state the relaunch model is bookkeeping until runner propagation is built.

F-2 (non-blocking, LOW-MEDIUM): the assembled wired path passes EMPTY repo-state and executable-identity into every approval binding. `_run_loop` constructs SupervisedLoop without head_sha, origin_main_sha, or executable_identity (tools/agent_supervisor/cli.py:1995-2012 — none of the three parameters passed; defaults "" / {} at loop.py:571-573), so the S13.5 binding digest (broker.py:121-146) binds empty strings for head_sha/origin_main_sha and {} for executable_identity. Live-confirmed: ex1 audit seq 4 shows `"executable_identity": {}`. Impact today is limited: AUTO approvals are decided and honored in-line (no window), and deferred approvals have no live continuation path (the recorded FINDING). But any future owner-continuation/resume command would inherit approvals that do NOT pin repository state or the executable — exactly the S13.5 components meant to invalidate an approval when the repo or binary changes underneath it. Remediation: pass live HEAD/origin-main SHAs and runner.executable_identity() into SupervisedLoop at assembly; make this a precondition of the held-prompt continuation unit (see e).

F-3 (non-blocking, LOW — redaction advisory): the preserved live-exercise artifacts embed local absolute paths and the owner's Windows username. `C:\Users\MLFLL\AppData\Local\Temp\claude\c--Users-MLFLL-...\scratchpad\synth\worktree\docs\*.md` appears in every ex*/audit.jsonl and ex*-start-output.json and inside all eight binary supervisor_journal.sqlite3 files; `C:\Users\MLFLL\.local\bin\claude.exe` appears at project-control/reports/M0-T036-doctor-live-probe.md:9. NO API keys, tokens, bearer strings, emails, or account identifiers were found (string-scanned all eight SQLite journals plus every JSON/JSONL/MD artifact for sk-/ghp_/github_pat/gho_/xoxb/AKIA/api-key/bearer/password/secret/oauth/email patterns). Username + session-GUID scratchpad paths are acceptable in a private repo; nothing MUST be redacted before this branch merges to the private main. If the repository ever becomes public, redact these paths and prefer regenerating (or dropping) the binary SQLite journals — they are also unreviewable as binaries; future evidence packs should preserve a dumped text form alongside.

F-4 (informational): `verify_before_execute` (TOCTOU revalidation + single-use consumption, broker.py:557-606) is NOT on the wired allow path — `broker_permission_handler.handle` answers allow directly from `evaluate_request` (claude_runner.py:1078-1087). For AUTO one-shot decisions this is acceptable (the CLI executes immediately on the control response; evaluation and permission are effectively atomic). The consumed/revoked/digest-changed machinery exists for an owner-resume path that does not yet exist. Any future continuation command MUST route through verify_before_execute; flagged so it is not forgotten.

F-5 (informational): model-identity verification reads only structural stream fields — `event.model`, `message.model`, `modelUsage` keys (claude_runner.py:374-390, `inspect_stream` 428-460). Worker-authored text lives inside JSON string content the CLI serializes; it cannot materialize top-level fields without breaking the CLI's own encoding. Even a hypothetical spoof only triggers rotation/pause (safe direction); a worker cannot SUPPRESS a real mismatch (real events still carry real ids; detection is any-event, first-hit) and cannot lower peak context usage (max over events).

F-6 (informational): the probe knobs `--context-rotation-threshold` and `--expected-worker-model` are operator-only CLI arguments; no worker/reviewer content path reaches them. Note that `--expected-worker-model`, if set to a model OTHER than the pin by an operator, could mask a real downgrade to that model (verification checks expected, launch pins primary — cli.py:1964-1969). This is operator-trust-level (same person controls the config), documented "never in production"; no code change required, but the runbook should say the flag is probe-only.

== DISPOSITIONS (a)-(e) ==

(a) Broker wiring fails closed end-to-end: VERIFIED. Shadow or missing broker → `_permission_handler` returns None (loop.py:694-703) → runner uses `deny_everything` (claude_runner.py:642-650, 757); shadow additionally ends after one observation before any seam (loop.py:1570-1575). Four-tier order enforced in `evaluate_request` (broker.py:403-468): HARD-DENY first and final; AUTO approve-once; advisory tier INERT in the wired path (cli.py builds the broker without `advisory=`, and NEVER_ADVISORY_KINDS/classification guards remain); everything else defers — DEFER_TO_OWNER is answered "deny" to the worker while the ask queues (claude_runner.py:1079). Unknown tools map to kind "unknown" → queued, not allowed. Handler exception → deny "handler_error"; malformed/unsupported control subtypes get an error response; unhandled requests DENY via `handle_unhandled` — nothing hangs, nothing silently allows (claude_runner.py:964-992; broker.py:542-553). "Always allow" suggestions (setMode/addRules/alwaysAllow/acceptEdits/updatePermissions/bypassPermissions) are recorded-and-refused, never written anywhere (broker.py:295-300, 386-399) — live-confirmed: ex1 audit seq 4 `rejected_suggestions: ["setMode:acceptEdits"]` alongside the AUTO in-scope Write that actually executed. Approvals are digest-bound (binding excludes stated_reason AND permission_suggestions — broker.py:121-146) and single-use (approve_once exact-digest echo 610-640; consumption 557-606, subject to F-4). No new worker→approval/audit/forwarded-prompt influence path found in the delta: the new RunResult fields (observed_models, mismatch_detail, context_tokens) flow only into rotation flags and audit detail, never into policy decisions or the forwarded prompt; `_current_session_id` comes from the CLI's structural init event and is outside the binding digest.

(b) V1.2.1 substitution: VERIFIED fail-closed. The availability probe is a constructor-only parameter (loop.py:571, default always-available at 597); `_run_loop` never passes one (cli.py:1995-2012), so the substitution branch is UNREACHABLE from the CLI — operator/test-harness injection only. Role gate: argparse `choices=["orchestrator"]` + LoopConfig rejection of unknown roles (loop.py:205-210, test :1567); worker-default quota exhaustion PAUSES (test :1467). Non-quota reasons, bare-False probes, and probe exceptions all pause (loop.py:695-712 normalization — a bare bool carries no reason and is never quota; tests :1484, :1499). Shadow never flags or substitutes (loop.py:809-811 forwards-guard; run() exits pre-seam; tests :1395, :1551). Never silent: first-class `model_substitution` / `model_substitution_ended` audit events plus a durable journal record that is marked ended, not deleted (loop.py:948-993, 1006-1032; tests :1431, :1513). `SUBSTITUTE_MODEL = "claude-opus-4-8"` is a module constant (loop.py:134) referenced only within loop.py — no config/env/CLI override path exists. Reviewer model-selection path untouched: `git diff c193a52..98da15e -- codex_reviewer.py model_selection.py policy.py broker.py` is EMPTY (D-004-R748 upheld). Caveat: F-1 — the "explicit relaunch on the substitute" is bookkeeping-only in the assembled path.

(c) Model-identity discipline: VERIFIED. Per-event verification over structural fields only (F-5); a detected mismatch sets `rotation_pending` reason `model_downgrade` (loop.py:772-798) and the seam either rotates on the PIN or pauses for the owner — `_rotate_at_seam` probes the pinned model and PAUSES+queues a security-classified ask when unavailable (loop.py:845-890); no path continues silently on the observed substitute. Live evidence (ex3a-c): worker launched on the REAL model (never the fake id), `model_mismatch=True` flagged live, rotation armed and audited; ex1 confirms clean-path no-mismatch with `observed_models: ["claude-fable-5"]` and live usage reading (usage_known).

(d) Evidence pack: no secret leakage found; local-path/username disclosure only (F-3). Nothing must be redacted before merging to the private repository; redaction becomes mandatory only if visibility ever changes.

(e) Held-prompt FINDING posture: fail-closed CONFIRMED. All six held runs stopped `operator_declined`; `forwarded_message_ids` empty in every preserved run; nothing forwarded, WAIT_FOR_OWNER parks. The recommended future cure (a CLI command firing `owner_approved_pending_prompt`) would NOT weaken the approval binding PROVIDED it: (1) requires the owner to echo the stored digest over the reviewer decision's instruction fields including `next_claude_prompt` verbatim (the approve-once displayed-digest pattern, broker.py:610-640); (2) operates single-use on the SAME journal's pending-prompt record; (3) routes through verify_before_execute-style revalidation before the forward; and (4) is built only after F-2 is fixed so the binding actually pins repo state. A bare state-flip (WAIT_FOR_OWNER → continue) without digest echo WOULD weaken it. Advisory only, per the packet.

== COMMANDS RUN ==
git rev-parse HEAD; git log --oneline; git diff c193a52..98da15e (--stat, per-file for tools/agent_supervisor/, .claude/settings.json, and emptiness checks for codex_reviewer/model_selection/policy/broker); Read/Grep over tools/agent_supervisor/{loop,cli,claude_runner,rotation,broker}.py and tools/test_agent_supervisor_loop.py at HEAD; python string-extraction scans over all 8 supervisor_journal.sqlite3 + grep scans over all JSON/JSONL/MD evidence artifacts (secret patterns, emails, paths, argv/env fields); python -m pytest tools/test_agent_supervisor_loop.py tools/test_agent_supervisor_rotation.py tools/test_agent_supervisor_runner.py tools/test_agent_supervisor_broker.py -q → 272 passed in 18.31s.

== NOT VERIFIED ==
(1) Live seam actuation (archive→mint→relaunch) and live substitution — structurally unreachable live (the recorded FINDING + always-True probe); verified through the unit suite only. (2) The real-world shape of a quota-exhaustion signal — no live probe exists; ModelAvailability reasons are harness-injected. (3) The SQLite journals were reviewed by string extraction, not full row-level parsing of every table. (4) Byte-verbatim equality of project-control/reports/M0-T036-V1.2.diff / M0-T036-V1.2.1.diff against the producer's originals (G3's remit; I reviewed the actual code at the frozen SHA). Full evidence paths: C:\Users\MLFLL\Downloads\nyc-zoning\nyc-development-feasibility-claude-pack\project-control\reports\M0-T036-V1.2-live-exercises\ and tools\agent_supervisor\ at 98da15e.
<!-- VERBATIM REVIEWER RETURN ENDS -->
