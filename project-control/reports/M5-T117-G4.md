# M5-T117 — G4 test review (qa-engineer "qa-t117", read-only)

> Transmission history: pinned at 675dc7a1 (material cf6526e3), delivered as 2 SendMessage parts ending with
> END-OF-REPORT; no truncation. Joined verbatim by the orchestrator (transport wrapper tags removed only). Verdict PASS; all five named mutants + two own
> mutants RED; two ADVISORY items (positive-guard contention tests; list/set/Counter AST blind spot).

---

M5-T117 G4 TEST REVIEW (independent, READ-ONLY) — part 1/2

PIN: HEAD at start = 675dc7a1 (matches expected). Baseline documented command from services/api: `pytest tests/resilience tests/scenario/test_scene_api.py tests/drawings/test_dxf_import_api.py tests/cad/test_export_api.py -q` -> 190 passed. `ruff check .` -> All checks passed. `tools/modularity_check.py --check` -> DIRECT_EXIT=0. Python 3.11.9 (the four files do not import the PEP-695 chain; no shim needed — matches producer).

MUTATION RE-RUN (in-process, consuming namespace: loaded the committed test_rate_limit.py by path, patched the binding the tests consume, ran the exact target test, restored). 11 baselines incl. all pre-existing behaviour tests -> GREEN. All FIVE named mutants reproduce RED:
- [1] never-release-on-pre-start-cancel (rate_limit.py:337-345 cancel path removed) -> test_pre_start_cancel_releases_the_slot_exactly_once_no_leak RED (test_rate_limit.py:283-306).
- [2] release-on-every-cancel (drop the `if not worker_started` guard) -> RED on BOTH test_post_start_cancel_...no_double (309-350) AND test_slot_held_until_thread_ends_not_on_await_cancel (209-253) — matches the producer's claim.
- [3] sweep-on-every-refused-key (rate_limit.py:166-180 _maybe_sweep -> unconditional) -> test_full_table_sweep_runs_at_most_once_per_window RED (356-388).
- [4] empty-principal-accepted (`if principal is not None`) -> test_empty_or_non_string_principal_falls_back_to_host RED (394-406).
- [5] trust-XFF (caller_key reads x-forwarded-for) -> test_x_forwarded_for_header_never_changes_the_key RED (409-420).

MY OWN two mutants (both RED, with a GREEN control):
- (B) raw double-release on the post-start cancel path (`slots.release()` bypassing the `released` idempotency flag AND the started-guard) -> test_post_start_cancel_...no_double RED. This proves the `released` flag (rate_limit.py:319-324) is load-bearing against a double-decrement — a property NO named mutant proves directly (mutant [2] via the idempotent _release_once reddens only by early-release timing, never by a double COUNT; the test's `release_calls == 1` at :348 is the guard).
- (A) non-string-principal-accepted (`if principal is not None and principal != ""` — drops ONLY the isinstance-str half) -> test_empty_or_non_string... RED, while the control test_caller_key_prefers_principal_over_host stays GREEN. Isolates the `isinstance(principal, str)` check (rate_limit.py:220) independently of the non-empty check.

Continued in part 2 (determinism, contention, HEAD/OPTIONS + AST guard, nothing-weakened, scope, verdict).

---

M5-T117 G4 TEST REVIEW — part 2/2

CANCELLATION DETERMINISM (no flaky sleeps): the three slot tests use injected runners (_never_starts_runner never calls the worker; _abandoning_runner starts a real thread then returns a coroutine sleeping 3600s) + threading.Events. The only timing elements are a short deadline GUARANTEED to fire (runner sleeps 3600s so wait_for always times out first) and bounded event-waits with generous margins (started.wait(1.0), may_finish.wait(5.0), a 500x0.01s poll) that return immediately on the happy path. Low CI-flake risk; the producer's "deterministic, no flaky sleeps" is honest.

CONTENTION (test_rate_limit.py:426-474): the producer discloses these are POSITIVE guards (admitted==cap under 64 barrier-synced callers) and a bare drop-lock does not redden on CPython (GIL). I independently confirmed: real locked JobSlots and limiter admit EXACTLY 10; a drop-lock with a widened check->mutate window over-admits (64/cap10) deterministically -> the threading.Lock IS load-bearing. Position is HONEST and ADEQUATE. ADVISORY (non-blocking): a deterministic drop-lock mutation is feasible but needs a production test-seam or a logic-duplicating subclass; the positive guards + the report's widened-window demonstration are acceptable for this gate.

HEAD/OPTIONS + AST GUARD "fail for the right reason": routes register all 7 methods via @router.api_route(methods=_ROUTE_METHODS) where _ROUTE_METHODS=["GET","HEAD","POST","PUT","PATCH","DELETE","OPTIONS"] (scene_api.py:98,279; same in dxf/export), so disabled->404 for every method is the route's OWN flag-gate (enabled non-POST is a genuine 405 — test at scene:210). The disabled loops assert 404 + no X-Correlation-ID + HEAD empty-body (scene:194-207, dxf:153-167, export:116-129). For the AST guard: I ran the real _module_level_limiter_containers over the live route sources -> [] each; it catches every documented form (plain dict literal, dict(), defaultdict(), collections.OrderedDict() attr-call, deque(), annotated assign); injecting a plain-dict `_limiter_table={}` (a name the substring scan misses) into scene_api source and running test_route_uses_the_shared_limiter_and_slots_only reddens it via the AST assertion specifically (control unpatched=GREEN). ADVISORY (non-blocking): blind spot — a module-level list/set/Counter limiter is caught by neither the substring scan nor the AST guard; low risk since the positive behavioural tests (get_rate_limiter is the shared SlidingWindowRateLimiter + the 429 tests) are the real enforcement and the documented reintroduction shapes are covered.

NOTHING WEAKENED: scene_api.py is docstring-only (app.resilience.rate_limit.* path fix, G3 A3 — the module is not re-exported from app/resilience/__init__, so the old path was wrong). The only existing-helper change is additive (_FakeRequest gains headers=; test:56-64). All rate_limit.py code removals are docstrings or the 3 intended Finding fixes; no existing test function removed; every pre-existing behaviour test passes against the new code.

SCOPE (AS-5): material commit cf6526e3 touches EXACTLY the 7 allowed paths; app/main.py untouched (routes unmounted); no forbidden path.

BLOCKING findings: NONE. ADVISORY: (1) contention positive-guard-only; (2) AST guard list/set/Counter blind spot.

M5-T117 G4 VERDICT: PASS
END-OF-REPORT
