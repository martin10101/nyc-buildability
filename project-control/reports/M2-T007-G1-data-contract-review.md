# M2-T007 - G1 Data-Contract Review

- **Task:** M2-T007 — GIS Zoning Features connector (ArcGIS, six canonical DCP layers)
- **Gate:** G1 (source and data-contract)
- **Reviewer:** data-contract-verifier (independent; did not produce this work)
- **Review target:** worktree `.claude/worktrees/M2-T007`, branch `task/M2-T007-zoning-features-connector`, commit `4468954`
- **Review date:** 2026-07-19 (UTC session spanning into 2026-07-20 source-time frame)

*(Orchestrator note: saved verbatim from the reviewer's agent-return channel per the report-preservation rule; transport entity-decoding only.)*

## 1. Scope and method

Read in order: `project-control/tasks/M2-T007.json` (packet, safeguards 1–6, ZF-S1..S13), `docs/research/zoning-features-ztldb-2026-07-16.md` (G1-corrected research, sections 2.3/4.2/4.3/5.2/5.3/6), G1 checklist in `docs/GATES_AND_CHECKPOINTS.md`; then the implementation: `services/api/app/connectors/zoning_features_arcgis.py` (1,904 lines, full read), `services/api/tests/connectors/test_zoning_features_arcgis.py` (1,042 lines, full read), all 32 fixtures + `MANIFEST.json` + `build_fixture_pack.py`, `docs/research/source-registry-drafts/zoning-features.json`, and last `project-control/reports/M2-T007-producer-report.md`.

Executed independently (read-only; no writes to the worktree):

1. `git diff --name-status e27b9d1..4468954` — file-scope audit.
2. Python sweep over all 32 fixtures: sha256 of `response_body_raw` recomputed vs every MANIFEST digest; file-set equality manifest↔disk; `_synthetic` filename ↔ `classification` field consistency; secret-pattern scan; pack size.
3. Python cross-check of all six ZF01 metadata fixtures against `LAYER_SPECS` (field name→type maps), objectIdField, SR, geometry type, maxRecordCount, paging capabilities; all six ZF02 counts vs packet baselines; independent epoch-ms→UTC conversion of all six `dataLastEditDate` values plus the ZF95 synthetic value.
4. Content inspection of ZF03/ZF04a-c/ZF05/ZF06/ZF07 (paging semantics, empty-result shape, error-with-200, nyzma null EFFECTIVE).
5. **Live cross-check (3 bounded keyless GETs to the official services):** nylh and nysp `returnCountOnly` counts; full nyzd `/0?f=json` metadata re-fetch with sha256.
6. Independent offline test run: new suite (80 tests) and full api suite (356 tests), with `PYTHONDONTWRITEBYTECODE=1 -p no:cacheprovider` (no cache writes).
7. Independent recomputation of both cross-platform normalized-digest anchors from the PROSE of `ZF_CANONICALIZATION_SPEC` (no connector import), directly from the raw fixture bodies.

## 2. Checklist findings

**2.1 Official source identity — PASS.** `SERVICE_ROOT` is pinned to `https://services5.arcgis.com/GfwWNkhOj9bNBqoJ/arcgis/rest/services` (connector line 135), the DCP_GIS org root verified in M1-T001/M1-T003 research (Z7/Z8). `_require_layer` (lines 413–423) is an exact-match allowlist over exactly the six canonical keys `nyzd, nyco, nysp, nysp_sd, nylh, nyzma`; it is the only path from caller input to a URL segment, and every builder routes through it. Test `test_s11_every_built_url_targets_the_pinned_official_root` plus 8 refusal negatives (including the non-canonical `v_Zoning_Districts_NYZD` view service, case variants, traversal, absolute URLs) prove no other host/service is reachable, with zero transport calls on refusal.

**2.2 Endpoint/dataset identifiers current and correctly transcribed — PASS.** All six `LAYER_SPECS` field inventories (connector lines 180–247) compare EQUAL (name→esri-type dict equality) to the committed ZF01a–f fixture bodies, which in turn match research section 4.2 exactly: nyzd ZONEDIST; nyco OVERLAY; nysp SDNAME/SDLBL; nysp_sd SPNAME/SPLBL/SUBDIST/SUB_AREA_NM/SUBDIST_LBL/SUBAREA_LBL/SUBAREA_OTR; nylh LHNAME/LHLBL; nyzma EFFECTIVE(esriFieldTypeDate)/STATUS/ULURPNO/LUCATS/PROJECT_NAME; plus OBJECTID/Shape__Area/Shape__Length on each. No invented fields, no dropped fields. The research 5.4 hazard (service description mentions a nonexistent SUBSUB attribute) is correctly resolved by mapping from the live schema, and `test_s1_metadata_snapshot_validates_all_six_layers` asserts `meta.fields == LAYER_SPECS[layer].fields`, so transcription drift fails the build. **Live currency confirmed by me:** re-fetched nyzd `/0?f=json` on review day — the body is sha256-identical to the committed ZF01a fixture (prefix `7b126c00046a0eee` matches the manifest digest), and live nylh/nysp counts (14/95) match ZF02e/ZF02c.

**2.3 Authentication and rate limits — PASS.** Services are keyless; the only request header is `Accept: application/json` (test-asserted). No token handling exists anywhere; my secret-pattern scan over all fixtures + manifest + capture script found only the explicit "no tokens exist" statements. 429 handling: bounded retry with `Retry-After` honored EXACTLY via the shared M1-T009 `parse_retry_after` (test: sleeps == [7.0]), over-cap Retry-After aborts typed without blocking (test: zero sleeps, one call), persistent 429 → typed `rate_limited`. The 429 body fixture (ZF101) is honestly labeled synthetic with the disclosed reason (cannot politely trigger a live 429); transport handling is body-independent — acceptable.

**2.4 Pagination/update behavior — PASS.** The committed ZF04a-c fixtures are real captured pages proving the exact live semantics: `exceededTransferLimit: true` on both partial pages (offsets 0 and 6), key ABSENT on the final 2-record page; OIDs 1–6/7–12/13–14 with no gap or overlap; all pages carry SR 102718/2263 and `objectIdFieldName: OBJECTID`. `advancedQueryCapabilities.supportsPagination/supportsOrderBy` are true in all six metadata fixtures (and in my live nyzd re-fetch), resolving research OQ-11 for resultOffset paging with committed evidence. The extraction algorithm (lines 1531–1639) pages by `orderByFields=<validated objectIdField> ASC` + `resultOffset`, respects exceededTransferLimit, and types every pathology (repeated_object_id, duplicate_page, zero_progress, page_budget_exhausted, count_mismatch) with a hard 200-page ceiling — the research C3 cap-exceedance hazard (95>92, 336>317, 1414>1292, 14=14) is encoded both in the module docstring and in `test_s2_cap_exceedance_hazard_is_real_in_fixtures`. Update cadence (monthly, last Monday) and the `dataLastEditDate` freshness signal are carried into the registry draft.

**2.5 Actual response fixtures — PASS.** All 32 manifest digests recomputed by me match the committed fixture bytes exactly; file sets are equal; classification labels are consistent (19 `raw` live captures, 13 `synthetic`, every synthetic has `derived_from` lineage and a "SYNTHETIC ... NOT official data" capture_method). The strongest authenticity evidence is my live nyzd metadata re-fetch hashing byte-identical to ZF01a. Structures are genuine AGOL shapes; SR is 102718/2263 everywhere expected; values plausible (ZF03 OBJECTID 86 ZONEDIST R3-2 matches the research Z10 live capture of 2026-07-16 exactly). Embedded timestamps are internally consistent: capture 03:07:55–03:08:01Z, manifest generated 03:08:47Z, commit 03:25:53Z on 2026-07-20 — one coherent session, no timestamp contradiction.

**2.6 Field mapping and units — PASS.** Attributes and esri geometry are preserved VERBATIM in normalized records (`_normalize_features`, lines 861–888): no value coercion, no unit conversion, no reprojection; EPSG:2263 (US survey feet) preserved as authoritative with the correct CRS stamp. esriFieldTypeDate handling: nyzma `EFFECTIVE` is kept verbatim (epoch-ms or null); ZF07 records the live finding that EFFECTIVE can be null even on STATUS "Adopted" (`test_s3_nyzma_date_field_preserved_verbatim` asserts `attrs["EFFECTIVE"] is None`), and the registry draft honestly narrows OQ-7 to "partial evidence" rather than claiming domains.

**2.7 Null/unknown semantics — PASS.** Well-formed empty `features` array is a valid empty result with an explicit `empty_result` note (live ZF05); missing `features` key, truncated JSON, non-object feature, and missing integer OID are each typed `malformed_response` — never coerced to empty (ZF99/ZF100 + synthetic in-test variants). Empty responses omitting `spatialReference` (live-observed on ZF05) are handled: SR is required-when-features-present, absent-with-features degrades visibly (`page_missing_spatial_reference`). Geometry null is preserved as null. Missing `editingInfo` degrades typed (`missing_editing_info` drift signal), never guessed.

**2.8 Retrieval/version timestamps — PASS.** Every fixture and manifest entry carries `retrieval_timestamp_utc` plus `source_data_last_edit_ms` where the body exposes it; every connector result carries `retrieved_at` (RFC3339) plus `source_data_last_edited_ms` AND its RFC3339 form. I independently re-verified ALL of the epoch conversions with python (not connector code): 1782912288115→2026-07-01T13:24:48Z (nyzd), 1782912829636→13:33:49Z, 1782912605345→13:30:05Z, 1782912514806→13:28:34Z, 1782912700015→13:31:40Z, 1782912214592→13:23:34Z, and ZF95's 1577836800000→2020-01-01T00:00:00Z — all correct to the second and matching the research C6-verified table.

**2.9 Provenance persistence — PASS.** `LayerExtractResult` carries service URL, metadata/count/page request URLs, layer, retrieved_at, source edit ms+RFC3339, CRS stamp, maxRecordCount/page_size/page_count/expected_count, and SEPARATE metadata/count/per-page raw digests plus the canonical-normalized digest, with the self-describing `ZF_CANONICALIZATION_SPEC` attached to every result. I recomputed both hardcoded cross-platform anchors from the spec's PROSE (no connector import) directly from the raw fixture bodies: `sha256:aa48af94...` (nylh 3-page extract) and `sha256:a7570d13...` (ZF03 query) — both reproduce exactly, proving the spec text is sufficient to independently re-derive the digests. Raw and normalized digests are kept separate (test-asserted) and the shuffled-fixture test proves normalized-order independence with raw-digest sensitivity.

**2.10 Schema-drift handling — PASS.** Renamed/removed field, retyped field, wrong CRS (metadata AND page level), missing objectIdField, missing maxRecordCount, lost pagination capability, layer-name mismatch, and response objectIdFieldName mismatch each raise typed `schema_drift` (fixtures ZF90–ZF93 + in-test variants); ADDED fields and missing editingInfo degrade with visible drift signals that propagate into query/extract results (ZF94 + propagation test). Nothing is silently guessed.

**2.11 Owner two-staleness rule — PASS, both directions proven.** Direction 1: ZF95 (source dataLastEditDate forced to 2020-01-01) with a fresh retrieval → `staleness is None` through both the plain function and the resilient client (`test_s12_old_source_with_fresh_retrieval_is_not_stale`, `..._via_resilient_client`). Direction 2: cache-hit serve (`stale: False`) and LKG serve (`stale: True`, with `upstream_error_type`) both copy `source_data_last_edited(_ms)` and `retrieved_at` verbatim from the original result (`test_s12_cache_hit_serve_does_not_alter_source_timestamps`, `test_s12_lkg_serve_preserves_source_timestamps_and_flags_transport` — the latter runs the LKG path with the OLD-source fixture, proving the timeout, not source age, caused the stale flag). `staleness` is stamped only in `ResilientZoningFeaturesClient`; the plain functions structurally cannot set it.

**2.12 Cross-check against a second official presentation — PASS.** My sandbox had network: 3 bounded keyless GETs re-verified counts (nylh 14, nysp 95) and the full nyzd metadata (byte-identical). The CRS is additionally corroborated by the official nyzd metadata PDF (research Z11, independently direct-read at the M1-T003 G1), and the registry draft records the mm69-vrje blob channel as the companion presentation. Identical counts vs 2026-07-16 are EXPECTED corroboration, not staleness: dataLastEditDate is unchanged at 2026-07-01 and no last-Monday release boundary was crossed before capture or review.

**2.13 Scope, secrets, low-storage, synthetic labeling — PASS.** Git file list for the task commit equals the allowed paths exactly (connector, test module, fixture dir, one registry draft, own producer report); no forbidden path touched; `pluto_soda.py` reused import-only, unmodified. No secrets anywhere. Fixture pack totals 782,954 bytes (~765 KB) — KB-scale, low-storage compliant. All 13 synthetic fixtures are labeled in filename, `classification`, `capture_method`, and manifest `derived_from`; none is presented as official data.

**2.14 Independent test execution — PASS.** 80/80 new tests and 356/356 full api suite pass in my own run (0.86s / 13.50s), matching the producer's G2 claim.

## 3. Defects

- **D1 (LOW)** — `query_features` with a caller-supplied `out_fields` list that omits the object-id field (e.g. `out_fields=["ZONEDIST"]`, permitted by `build_query_url` at services/api/app/connectors/zoning_features_arcgis.py:512–523) would make ArcGIS return attributes without OBJECTID, which `_validate_page_envelope` (lines 843–850) then classifies as `malformed_response` — blaming upstream for a client-side parameter mistake. A pre-flight `disallowed_request` (require object-id field in any explicit out_fields list) would type it correctly. Not fixture-reachable, default `*` unaffected, no official-data misinterpretation possible; record for a future hardening pass, non-blocking.

## 4. Observations (non-blocking)

- **O1** — Synthetic manifest entries inherit `retrieval_timestamp_utc` and `official_endpoint` from their source raw fixture. The `classification: synthetic` + `derived_from` labels make this unambiguous, but a reader scanning only the timestamp/endpoint columns could misread a synthetic row as a live capture. A `derived_at` field or null retrieval timestamp for synthetics would be marginally clearer.
- **O2** — `test_s9_added_field_signal_propagates_into_extraction_result` contains a dead `page = ...` load followed by `del page` and an unused first transport — cosmetic, G3 territory.
- **O3** — The resilient client's cache key includes `page_size` but not `max_pages`; two calls differing only in `max_pages` share a cache entry. Harmless (a successful cached result is identical regardless of budget), noted for the future import-worker task.
- **O4** — The producer report section 7 discloses "2 metadata/page probes and 1 connectivity probe" beyond the 19 manifested captures — properly disclosed, bounded, keyless; no concern.
- **O5** — Registry draft honestly keeps `f=geojson` open (OQ-11 residue) and OQ-7 partial; the follow-up PostGIS/Storage worker plan is correctly labeled PLAN, not implementation.

## 5. Verdict

**PASS** — the connector pins exactly the six official DCP_GIS services with correctly transcribed live schemas (fixture- and live-verified, including a byte-identical independent re-fetch), genuine digest-verified raw fixtures, correct paging/null/error/drift semantics, arithmetically correct timestamps, separate raw/normalized provenance digests independently reproducible from the prose spec, and the owner two-staleness rule proven in both directions; the single LOW defect (D1 error-type imprecision on a caller footgun) is non-blocking.
