# M0-T068 independent review — FAIL (rework: two blocking findings, both narrowly fixable)

> Saved VERBATIM by the orchestrator from the independent read-only reviewer's
> agent-return channel (transport entity-decoding only). Reviewer: fresh
> general-purpose agent (producer = orchestrator ≠ reviewer). Round 1 at
> reviewed HEAD 319078b.

Reviewed HEAD: `319078b5dc5375628cb4277d4dfd2a5f846f5919` (worktree `C:/Users/MLFLL/Downloads/nyc-zoning/wt-m0t064`, confirmed via `git rev-parse`). Pre-existing uncommitted control-plane entries (`state.json`, `tasks/M0-T068.json`, untracked `reports/M0-T068.json`) were present before any review command and are orchestrator submit activity, not part of HEAD; they were unchanged by my runs.

## Per-item verdicts

**1. SCOPE — PASS.** `git diff --name-only main...HEAD` = 11 files: the 4 allowed code/doc files, the 4 allowed report files, plus the packet's own control-plane records (`project-control/gates/M0-T068-G0.json`, `state.json`, `tasks/M0-T068.json`). The forbidden-path diff (agent_supervisor, subsystem_*, memory_*, repo_index_*, context_pack*, code_graph) is **empty** (0 lines). R082 satisfied.

**2. AS-1 coverage modes (R023) — PASS.** All four modes are real and labeled from exactly {census, changed, neighborhood, deep} (`card` labels itself `coverage_mode: "neighborhood"`, correct). Census surfaces the accepted A1 census (eligible/indexed/excluded-by-reason/failed-by-reason/stale/`reconciles`) — live probe: `{'eligible': 3, 'indexed': 3, 'excluded': {}, 'failed': {}, 'stale': 0, 'reconciles': True}`; groups are genuine per-reason dicts (`Census.add_excluded/add_failed` in `tools/repo_fingerprint.py`). Changed view refuses any stated base other than the current snapshot (`unsupported_base_fingerprint`, verified live) — it never guesses. Deep excerpt is exact with line provenance and a CRLF-normalized whole-file sha256, independently recomputed in the test. Observation: an out-of-range deep request (start 99 on a 3-line file) returns an empty excerpt with an honest marker (`returned:0, truncated:false`) rather than refusing; `end_line` echoes `start` when empty.

**3. AS-2 coverage record (R024) — FAIL (Blocking finding 1).** Field-by-field against the R024 text: repository identity (64-hex path-sha, no absolute path) / snapshot fingerprint / HEAD / branch / dirty-state digest / eligible / indexed / excluded-by-reason / failed-by-reason / stale / indexer-schema-config versions (`codegraph_config_inputs`, `codegraph_schema`, `eligibility`, `fingerprint` + `generator_identity`) / ontology stamp / source-manifest digest / export digest / exact `query_params` + `limits` — all present, **no None values** in a live record. Cache hit/miss lives in the labeled `cache_state_non_identity` section (`cache_result` flag plus per-file counts `files_parsed`/`files_reused`) — acceptable. **But R024 also names "files reparsed/rebound/removed/invalidated": "removed" is carried NOWHERE** — neither section. Live probe: telemetry contains `change_set` counts including `deleted` (`tools/repo_index_incremental.py:448`), but `tools/repo_views.py` copies only `_DETERMINISTIC_KEYS` (lines 50–54) and `_CACHE_STATE_KEYS` (lines 59–60), both of which omit `change_set`; the code comment at lines 56–58 itself narrows R024 to "cache hit/miss + reparse counts". "Rebound" survives only via an arguable `files_reused` mapping; "removed" has no carrier at all (the changed view's content shows deleted lists, but R024 says *every* coverage record). Minimal fix: add `change_set` (counts) to `_CACHE_STATE_KEYS`. The evidence-map R024 row makes the same silent narrowing, so this is an unclaimed gap, not a disclosed deviation.

**4. AS-3 truncation (R003/R039/R042) — PASS.** Every list output flows through `_truncate` (census file list, in/out edges, memory digests, allowed_paths, citing tasks, deep excerpt) with the `{limit, returned, omitted, truncated}` marker; limits are echoed in the coverage record. AS-3 tests assert exact `omitted` counts (1, 2, 2). No prompt text, no token estimation, no budget anywhere in either module — structured dicts only.

**5. AS-4 honesty (R009/R018/R045/R051) — PASS.** No LLM/network anywhere; kinds come straight from the graph export (`gi.nodes[nid]["kind"]`, test pins `py_module`); subsystem exclusively via `resolve_path` on the versioned Unit C map (`_subsystem_of`, honest miss carried); unknown seed → `seed_not_in_graph`, unknown requirement → `unknown_requirement_id`; memory reads carry `advisory: True` with `store_unavailable`/`store_empty`/`ok` labels and one test surfaces a digest through a **real** Unit D promotion. No fabricated values found.

**6. AS-5 fail closed (R013) — PASS on the named cases; see Blocking finding 2 for the unnamed one.** Run live by me, all exit 2 with the exact codes: traversal deep path → `non_canonical_path`; absolute `C:/Windows/win.ini` → `non_canonical_path`; unknown task → `task_packet_unreadable`; missing ask value → `missing_question_value`; non-git repo census → `index_unavailable`. Bonus honesty: a corrupt task packet makes `about_requirement` fail closed entirely (exit 2, `EntityIndexError: task_packet_unreadable` via the Unit C index loader) rather than silently skipping — the `continue` at `repo_views_query.py:132` is reachable only for read races.

**7. AS-6 determinism — PASS.** The cold-vs-warm test is real: it asserts `runtimes == ["miss", "hit"]` (`test_repo_views.py:322`) while the deterministic sections byte-match, so the non-identity separation is proven, not assumed. CLI `check` → exit 0, `{"check": "PASS", "bytes": 6684}` on my fixture. The changed view's exclusion is disclosed in both `docs/REPO_VIEWS.md` (lines 33–35) and a code comment (`repo_views_query.py:206–208`). No wall clock/run_id/absolute path in deterministic sections (`elapsed_seconds`, `run_id` are dropped keys; `repo_identity` is a sha); all orderings sorted (`file_ids` sorted — verified; edges sorted; canon JSON sorts keys). Observation: CLI `check` samples census/card/neighborhood only (no deep); deep's byte-identity is covered by the test, so the doc's "(tested)" claim is accurate.

**8. TESTS — PASS.** `python tools/test_repo_views.py` → **Ran 23 tests, OK, exit 0**. `python -m pytest tools/test_repo_views.py -q` → **23 passed, exit 0**. `python tools/modularity_check.py --check` → **selected 259 files; failures 0; warnings 4 (all pre-existing), exit 0**. `ruff check` (0.13.0, CI-matching) on the three new files → **All checks passed, exit 0**. Regressions: `test_subsystem_resolver.py` → **21 OK**; `test_memory_graph.py` → **31 OK**. Tests are not vacuous — concrete digests, counts, codes, and a real promotion pipeline. (One softness: AS-2 asserts key *presence*, not non-None; my live probe closed that hole.)

**9. DOC + HONESTY — PASS with observations.** Doc modes table, CLI, coverage-field list, and advisory-memory labels match the code; no token-savings or whole-repo-understanding claims anywhere. Evidence map: **27 rows exactly equal the mechanically applicable set** — `evaluate_task_refs` → `27 []`, IDs match one-for-one. Observations: doc's fail-closed list omits emitted codes `view_failed`, `missing_question_value`, `nondeterministic_views`; producer report's "257 files" modularity count is now 259 (immaterial, failures 0 both).

**10. UNIT BOUNDARY — PASS.** No benchmarks/runbook/status projection. `build_index` passes `persist_telemetry=False` (`repo_views.py:84`); index-cache writes go to the accepted external per-checkout dir (XDG state home, `repo_index_cache.py:89–98`). `git status` after all my runs shows exactly the three pre-existing control-plane entries — the views layer wrote nothing.

## Findings

**BLOCKING**
1. **R024 field gap — "files removed" (change_set counts) carried in neither coverage section** (`tools/repo_views.py:50–60`). Directive text names it as a minimum field; telemetry has it one key away. Fix: carry `change_set` counts in the labeled cache-state section (it is cache-relative, so that is the right section).
2. **`about_task` task-id path traversal escapes the repository.** `repo_views_query.py:92–93` builds `project-control/tasks/f"{task_id}.json"` with no canonical-ID validation. Verified live: `ask about_task "../../../probe_outside_secret"` read a JSON file **outside the fixture repo** and returned it as a task packet with **exit 0** (title leaked). That is a false-provenance answer presented as complete — contrary to R013's fail-closed bar for ambiguous input and inconsistent with the unit's own deep-view discipline. Fix: validate the task id (pattern or `is_canonical_repo_path`-style single-segment check) and refuse otherwise.

**Observations (non-blocking)**
- Error `detail` strings can carry absolute paths (e.g. the packet path in `task_packet_unreadable`); nothing is persisted, but sanitizing details would match the R050 posture.
- `deep_view` decodes with `errors="replace"` — a non-UTF-8 file's excerpt silently contains replacement chars while the digest covers raw bytes; and it follows in-tree symlinks (Windows-rare; defense-in-depth).
- Out-of-range deep requests return an empty excerpt instead of a refusal (honest marker, mildly odd `end_line`).
- Doc's fail-closed list is missing three emitted codes; modularity file count drifted 257→259 in the producer report.
- On Windows pipes, non-cp1252 output can trip `_emit` into a fail-closed `view_failed` exit 2 — honest but surprising cross-platform behavior.

## Overall verdict: FAIL

Both blocking items are small, surgical fixes (one tuple entry + one input-validation guard, plus matching tests/doc/evidence-map rows); everything else — scope, all six scenario packs as tested, determinism proof, boundedness, honesty labels, evidence-map completeness, and unit boundary — verified sound at the reviewed HEAD.
