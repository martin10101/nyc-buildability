G5 FAIL — M5-T031 V2

- Frozen SHA: `5b832c8420e28d19bc4da45bfa4e92fbd234e18a`
- Task material identity: `634be631d1665c53ecae1a4b0f34a8e87283d835780d2cc5264b17462b51fedf`
- Independently matched 20-file application digest: `703ea0f8c745ec8e63a5a0426979f8edaa403679a48b92547dc0bec730ba2ed5`
- Independent read-only reviewer; no files, git, control state or reports changed.

The five V1 findings are closed. One medium-severity citation traceability defect remains.

| V1 finding | V2 result |
|---|---|
| G5-01: Conflicting duplicate bulk traces | PASS: no numeric bulk promotion. |
| G5-02: Missing association fingerprint | PASS: documents differ; results withheld. |
| G5-03: FAR relabelled as height | PASS: bulk row remains “Not calculated.” |
| G5-04: Missing nested trace field | PASS: selector returns unavailable without throwing. |
| G5-05: Conflict alongside promoted cap | PASS: current function signature and rendered summary both withhold the cap. |

G5-V2-01 — Empty citations still substantiate numerical promotion.

In `apps/web/src/lib/architect/development-limits.ts`, `readableCitation` checks rendering shape, while `uniqueResidentialTrace` and `scenarioCap` also use that shape as sufficient citation evidence.

Replace both the trace and cap citation arrays with:

```json
[{"snapshot_id":"","section":"","quote":"","provenance":{}}]
```

Both existing runtime validators accept these documents. `evaluationIsInspectable` returns true, and the actual summary renders:

```json
{"scenarioValid":true,"evaluationValid":true,"inspectable":true,"far":"1.50","cap":"15,000 sq ft","status":"Draft assessment · envelope incomplete"}
```

There is no source identifier, section, quotation or provenance metadata substantiating these promoted values. This is a missing-evidence case, not a question requiring legal interpretation.

Required repair: distinguish safe inspection from sufficient evidence for numerical promotion. Preserve empty citation records for inspection, but do not count them as substantiating citations. Add regression coverage for empty citations and whitespace-only trace citations, including the evaluation-only path. No backend or contract change is required.

Other independently executed checks passed: 70 assertions covering valid/zero outputs, cap rule/version/status/output mismatches, malformed nested fields, rendered conflict suppression, escaped source/label/citation text and safe URLs.

The bounded delta remains within allowed application paths. The additional map-test change waits for the documented rendered state before firing the same error; production map code and fallback assertions are unchanged. No calculation, backend, contract, dependency, configuration, credential or network behavior was added. Raw unusable evaluations and scenario records remain available; existing source links and report print handling are preserved.

Directive assessment is scoped to G5 at the SHA above:

| Requirement | Assessment |
|---|---|
| D-063-R001 | PASS for security/content retention; visual acceptance remains G3. |
| D-063-R002 | **FAIL:** empty citation records still permit promoted results. |
| D-063-R003 | Internal testing evidenced; independent official-source correctness remains G4/DCV. |
| D-063-R004 | PASS for presentation scope: no district/property/borough hardcoding. Legal coverage unverified. |
| D-063-R005 | PASS for application boundaries; protected refs are recorded in the root identity evidence. |
| D-063-R006 | PASS for claims discipline: limitations and incomplete bulk coverage remain explicit. |
| D-063-R007 | PASS: no architect-validation dependency introduced. |
| D-063-R008 | PASS within this read-only review; root evidence records main unchanged and PR 241 open/unmerged. Remote refs were not independently queried. |
| D-063-R009 | PASS: G6 and production approval remain separate. |
| D-063-R010 | PASS: no replacement calculation, schema/database change or credential access. |

Exact V1 replay command, executed from `/workspace/scratch/cfa2464c5c7f/nyc-buildability`:

```bash
python -B <<'PY'
from pathlib import Path
import re,subprocess
report=Path('project-control/reports/M5-T031-G5-v1.md').read_text()
command=re.search(r'```bash\n(.*?)\n```',report,re.S).group(1)
result=subprocess.run(['bash'],input=command,text=True,capture_output=True)
print(result.stdout,end='');print(result.stderr,end='');print('exit_code',result.returncode)
PY
```

Exit 0. All three bulk reproductions returned `value:null`; the missing fingerprint returned `recordsDiffer:true`; missing nested input validation produced no exception; the conflict case returned `cap:null`. The new three-argument `scenarioCap` call was independently checked in the 70-assertion probe.

Exact citation reproduction command, using the already committed V1 in-memory loader:

```bash
node <<'NODE'
const fs=require('node:fs');
const code=fs.readFileSync('project-control/reports/M5-T031-G5-v1.md','utf8').match(/node <<'NODE'\n([\s\S]*?)\nNODE/)[1].split('\n{\n')[0].replace('esModuleInterop:true','esModuleInterop:true,jsx:ts.JsxEmit.ReactJSX');
new Function('require',code+String.raw`
const React=ext('react'),render=ext('react-dom/server').renderToStaticMarkup,{JSDOM}=ext('jsdom');
const View=get('apps/web/src/components/architect/DevelopmentLimits.tsx').DevelopmentLimits;
for(const text of ['', '   ']){
 const {s,e}=inputs();const p=get('apps/web/src/test-support/fixtures.ts').baseProfile();
 s.evaluated_input.bbl=p.identity.bbl;e.evaluated_input.bbl=p.identity.bbl;
 const citation={snapshot_id:text,section:text,quote:text,provenance:{}};
 e.evaluations[0].citations=[citation];s.cap_provenance.citations=[structuredClone(citation)];
 const d=new JSDOM(render(React.createElement(View,{profile:p,evaluation:e,scenario:s}))).window.document;
 console.log(JSON.stringify({case:text?'whitespace_citations':'empty_citations',scenarioValid:VS(s).ok,evaluationValid:VE(e).ok,inspectable:L.evaluationIsInspectable(e),far:d.querySelector('[data-testid="development-evaluated-far"]').textContent,cap:d.querySelector('[data-testid="architect-cap"] .architect-metric').textContent,status:d.querySelector('.architect-development-status').textContent}));
}
`)(require);
NODE
```

Exit 0. The empty-citation case is accepted by both validators and reproduces the finding. The whitespace control is rejected by the scenario validator, so its direct-component cap rendering is not counted as an additional reachable defect; the evaluation validator accepts it.

Evidence: `bounded-reviews/M5-T031-v2-delta.md`, `project-control/reports/D-063-review-identity.json`, saved V1 report, updated task/directive requirements, producer V2 addendum and actual scoped source.

The producer’s 861-test/typecheck/lint/build results were reviewed as supplied evidence; this reviewer did not rerun the full suite or claim completed CI/browser acceptance. Unverified legal coverage is outside G5 and is not the reason for this failure.
