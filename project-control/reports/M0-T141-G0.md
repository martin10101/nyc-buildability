# M0-T141 G0 scoping gate (orchestrator, 2026-09-02)

Task: M0-T141 — D-024 Amendment 44 Claude Code JSON-Schema Draft-7 provider-schema
compatibility hotfix at the CLI boundary.

- **Authority:** owner directive 2026-09-02 captured verbatim as D-024 Amendment 44
  (`project-control/directives/D-024-fable-codex-loop/source-044-amendment.md`),
  requirements D-024-R664..R683; registry validator exit 0 at capture. R675 explicitly
  authorizes creating the next available bounded hotfix task (next unused ID = M0-T141).
- **Qualifying evidence (supervisor-freeze AD-093):** a reproduced defect AND provider
  CLI drift AND explicit D-024 requirements — Claude Code 2.1.252 rejects the Draft
  2020-12 `$schema` declaration on `--json-schema` (preserved live evidence:
  `mrl\canary-b5-02\one_shot_unit.json`, child exit 1, stderr
  `no schema with key or ref "https://json-schema.org/draft/2020-12/schema"`, zero
  provider contact; controller audit records 22–26). Cited requirement IDs:
  D-024-R664..R683 (packet + commit message per supervisor-freeze §3).
- **Scope:** production change limited to the schema-at-CLI-boundary path (one new
  focused module `mrl_provider_schema.py` + the single `--json-schema` call site in
  `mrl_one_shot.py`) and necessary focused tests (R675). The canonical
  `schemas/worker_result.schema.json` and `mrl_worker_result.py` are forbidden paths —
  the internal contract and controller-side validation stay untouched (R666). The
  freeze rebind (R678) touches only the binding contract and its two checked
  consistency teeth (runbook §4 SHA, ps_test `$pinnedSha`).
- **Dependencies:** M0-T136, M0-T138, M0-T139 (all accepted).
- **Gates:** G0 scoping, G2 producer self-check, G3 code review, G4 QA — G3/G4 plus the
  directive-compliance verification run as separate foreground read-only reviewers,
  producer ≠ verifier (R677). No push, merge, or provider call anywhere in the task
  (R679); the owner-run canary script is generated only after acceptance (R680) and is
  never executed by the agent (R683).
- **Modularity:** new logic goes in a new focused module with focused tests; the
  existing call site changes by one expression. No oversized-file growth.

Verdict: PASS (task is atomic, authorized, scoped, dependencies accepted).
