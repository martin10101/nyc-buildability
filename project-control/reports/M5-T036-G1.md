# M5-T036 G1 review — data-contract-verifier return (VERBATIM)

Reviewer agent: data-contract-verifier (spawn rev-g1-m5t036), read-only, pinned HEAD
657b238dce8af8df1bfff2e22e454caab1749413. Saved verbatim from the agent-return channel
(transport entity-decoding only).

---

G1 (data-contract/provenance) review of M5-T036 at pinned HEAD 657b238dce8af8df1bfff2e22e454caab1749413: VERDICT PASS. HEAD verified equal to the pin.

Findings (all evidence-bound):
F1 Faithful consumption, nothing computed/inferred/defaulted/dropped — districts/overlays/special via the exact shared ZoningValueList (ZoningContextPanel.tsx:71-91) with identical emptyText/arrayName; landmark/histdist via mappedFeatureView (contract.ts:257-269) with the same source-column keys+labels as AdditionalZoningFlags.tsx:7-9; split-lot multiples both render (AS-1). PASS.
F2 Provenance resolves through the real path (resolveDistrictProvenance / provenanceById+resolveFactProvenance) — verified in the committed base fixture: 1.0.0 no-map so the D5 fallback is exercised (zonedist1->R3-2, zonedist2->C4-1, spdist1->GI all resolve; landmark/histdist provenance_refs resolve). The AS-1 populated-overlay fixture uses the exact production D5 shape (original_field_name overlay1, value match), not invented. PASS.
F3 Honest absence — empty arrays -> empty text; absent mapped_features -> "Unknown — not supplied" rows, labels visible, no disclosure invented (ZoningContextPanel.tsx:110-120, AS-2). PASS.
F4 DB-005 — git grep "zola.planning.nyc.gov" over the 3 touched PRODUCTION files = zero matches (reproduced the producer claim). All links via zolaLotUrl; null -> no link. AddressConfirmCard migration provably behavior-neutral (validateBblInput canonical = 10 ASCII digits, borough 1-5 = strict match for zolaLotUrl's /^[1-5][0-9]{9}$/, encodeURIComponent of digits is a no-op -> render-condition and href byte-identical). PropertyOverview site link now honestly absent on invalid BBL (was unconditional raw template) = intended hardening. Test-file ZoLa literals are assertion constants only. PASS.
F5 Print-exclusion genuine — ReportView.tsx:11 imports only PropertyIssuesSummary; panel mounts only in PropertyOverview.tsx:63; ReportView renders its own ZoningSection(:79)+AdditionalZoningFlags(:80) in #brief-zoning(:77-81); AS-4 asserts both directions. PASS.
F6 Scope clean — 7 changed files all within allowed_paths; no services/api/** or apps/web/src/lib/** edited (lib imported, not modified). PASS.

Non-blocking observation O1: coverage_status ("conditional" in fixture) is not surfaced on the panel's landmark/historic rows (mirrors AdditionalZoningFlags per the objective; full coverage-badged table remains on the Zoning tab + printed brief). Not a G1 defect — suggest a discovery-backlog note for possible parity follow-up. Producer's D1/D2 discoveries are legitimate and correctly surfaced-not-fixed (D-069).

Evidence-capture required (do NOT block G1 on it): per the evidence-capture rule + AS-6, no web test ran locally (thin client). Web CI vitest on the pushed head is orchestrator-captured evidence required before acceptance; panel+DB-005 suite runtime PASS stays UNVERIFIED until then. My G1 verdict covers contract/provenance fidelity (fully judgeable from source), not CI-runtime behavior.

Honesty bar: observations separated from conclusions; no exhaustiveness claimed (targeted the G1 mandate + adjacent same-class surfaces); runtime-dependent items marked UNVERIFIED. I am read-only — did not run any write/git/control command beyond read-only git show/grep/rev-parse and file reads.
