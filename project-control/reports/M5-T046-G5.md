<!-- Orchestrator capture: security-reviewer G5 return for M5-T046 saved VERBATIM
     (transport entity-decoding only: &lt;/&gt;/&amp; decoded). Review pinned at frozen head
     c9113e09; reviewer verified content identity via git plumbing. The reviewer's
     preamble and closing note are preserved as part of the verbatim return. -->

The read-only guard blocked the shell write (memory note is optional anyway). I have everything needed. Here is my G5 report.

---

# G5 Security/Privacy Gate Report — M5-T046

**Task:** M5-T046 — Address-to-lot identity honesty (DB-026): GeoSearch equality gate + typed no-match + confirm-arc address-of-record display
**Gate:** G5 (security/privacy), independent read-only review
**Reviewer:** security-reviewer
**Frozen submission head:** `c9113e09` (content-identical to material corrected head `fb7af67d` for the security surface)
**Live HEAD at review:** `665d3d0b` — one control-plane-only commit later (stages the frozen-submission record); predicted by the task prompt
**Content identity:** VERIFIED via git plumbing. Surface files clean (no working-tree drift); `apps/web/src/lib/address-search.ts` LF-sha256 `0096e659a6…` and `geosearch.json` LF-sha256 `137e4e3adf…` match the frozen `M5-T046-source-sections.md` index exactly.

## Verdict: PASS

No critical, high, or medium findings. Three advisory (non-blocking) notes recorded below. All five G5 surface items and the authority/forbidden-path invariants reproduce clean at the frozen content.

---

## Scope confirmation (forbidden paths / authority surface — item 3)

Per-commit `git show --stat` across the full M5-T046 material series (`3182f0a2` build, `c3c690f7` adjudication, `fb7af67d` AS-5 correction): the nine changed material files are **all** inside `allowed_paths`. **No** forbidden path was touched — `services/api/`, `packages/contracts/`, `PropertyOverview.tsx`, `AnalysisIdentityNotice.tsx`, `ReportView.tsx`, and their named tests are untouched. The later commits `c3c690f7`/`c9113e09`/`665d3d0b` only modify `project-control/**` (ledger/reports/state) — outside the security surface.

Independent confirmation of the adjudication's core safety claim: repo-wide grep for `resolveLotFromGeoSearch` (and its exported types `GeoSearchLotResolution`/`ResolvedGeoSearchLot`/`GeoSearchParsedInput`/`normalizeStreetForMatch`) returns **exactly two files** — its definition and its unit test. **No production importer exists.** No new client-side lot-promotion/authority path was introduced; the server Geoclient route (`resolveAddress` in `AddressResolutionScreen.runResolve`, out of scope) remains the sole live lot authority. This diff cannot change lot identity in the live product.

Because this is a client-only web change touching no server/DB/storage/auth surface, the broad checklist items **cross-tenant isolation, service-role secrecy, private storage** are **N/A for this diff** (no such surface present, forbidden paths verified untouched).

---

## Item 1 — Untrusted returned text rendering (GeoSearch bodies as external input)

**PASS.** Verified in `AddressConfirmCard.tsx`, `AddressResolutionScreen.tsx`, `AddressAutocomplete.tsx`.

- Every rendered field is an **escaped React text node**. No `dangerouslySetInnerHTML`/`innerHTML`/`document.write`/`eval` anywhere in the diff (grep clean).
- The **only two hrefs** in the surface are built exclusively from `canonicalBbl`, never from a returned or user-typed string:
  - `AddressConfirmCard.tsx:184` `href={zolaUrl}` where `zolaUrl = zolaLotUrl(canonicalBbl)` (`:69`); `canonicalBbl` is the client re-validated 10-digit BBL (`validateBblInput`, `:63`), and `zolaLotUrl` returns `null` (→ no link) for anything non-canonical.
  - `AddressConfirmCard.tsx:282` `href={`/property/confirm?bbl=${encodeURIComponent(canonicalBbl)}${architect ? "&ruleeval=on" : ""}`}` — `canonicalBbl` only, `encodeURIComponent`-wrapped; the `&ruleeval=on` suffix is a static literal.
- The confirm card's **entered-input line** (`:96`–`:107`, rendered `:132` as `<strong>{enteredInput}</strong>`) is the raw user-typed text (`typedInput`, capped at `maxLength={200}` on the input, `AddressAutocomplete.tsx:170`) or a fallback of escaped `view.inputEcho` fields. It renders as inert escaped text and reaches **no** href. It cannot become an injection vector.
- GeoSearch feature text reaches the DOM only through the autocomplete suggestion `<li>` (`AddressAutocomplete.tsx:196`–`205`): `item.query.houseNumber/street`, `item.borough`, `item.query.zip` — all escaped text nodes, and each was pre-validated by `parseAddressSuggestions` (`boundedString` rejects control chars `\u0000-\u001f\u007f`, bounds length, enforces the `BOROUGH_NAMES` enum and `/^\d{5}$/` postalcode). The confirm card itself renders the **server Geoclient document** (`outcome.view`), not GeoSearch feature text.
- The committed hostile-fixture tests reproduce this: `address-confirm.test.tsx` S2 (`:284`), S6 (`:420`) inject `<script>`/`<img onerror>`/`javascript:` payloads into `canonical.bbl`, normalized street/borough, param values, and fact values and assert zero `script/img/iframe/style` elements, no `pwned`/`javascript:` href, and `window.__pwned` undefined.

## Item 2 — Equality gate input handling of hostile shapes (`address-search.ts`)

**PASS.** `resolveLotFromGeoSearch` (`:299`–`:342`) and helpers:

- **Non-object / drifted bodies:** `:300` rejects non-objects to `no_match`; `:303` rejects non-`FeatureCollection`/non-array features; `:309`–`:315` skip features whose `properties`/`housenumber`/`street` are absent or non-string. `readParsedInput` (`:264`) and `readPad` (`:277`) type-guard every level.
- **Prototype-pollution-shaped keys:** the code only **reads** own-property values off parsed JSON and constructs a fresh object literal — there is no `Object.assign`/merge/dynamic-key-assignment sink, so a `__proto__`/`constructor`-shaped body cannot pollute. `readPad` tolerates non-object `addendum`/`pad` and returns `null`.
- **Canonical BBL as the only identity accepted:** `:325` `if (pad === null || !/^\d{10}$/.test(pad.bbl)) continue;` — identity binds to `addendum.pad.bbl` (canonical 10-digit) only, never the address string or `bin` (`bin` is recorded, `:334`). AS-3 (`test :256`) proves the GARAGE sibling (same bbl, different bin) and reverse frontage bind to the same bbl.
- **Never gate on match_type/confidence:** the gate (`:320`–`321`) compares only exact housenumber + normalized street; `match_type`/`confidence` are read onto the result (`:336`–`337`) but never compared. AS-4 never-consult (`test :284`) proves a `confidence:1/match_type:"exact"` feature on a wrong street is still refused.
- **ReDoS:** `ORDINAL_SUFFIX = /\b(\d+)(?:ST|ND|RD|TH)\b/g` (`:249`) and `/\s+/g` are linear — a single quantifier plus a bounded alternation and zero-width anchors, no nested/overlapping quantifiers. Absurdly long strings cost O(n), not exponential. No catastrophic backtracking.
- **/autocomplete tolerance:** absent `confidence`/`match_type`/`pad.bin`/`version` are tolerated and recorded `null` (AS-5b, `test :318`).

## Item 4 — Secrets/keys and keyless posture

**PASS.** `address-search.ts` uses hardcoded public endpoint constants (`GEOSEARCH_AUTOCOMPLETE`/`GEOSEARCH_SEARCH`); the `fetch` (`:127`) sets `credentials: "omit"`, `referrerPolicy: "no-referrer"`, and only an `Accept: application/json` header — **no** `Authorization`, API key, or token. Grep across the diff for `console.`/`process.env`/`Authorization`/`apiKey`/`Bearer`/`SUPABASE`/`service_role`/`localStorage`/`sessionStorage` = **no matches**. No user-typed address is logged (no `console.*` introduced). The `geosearch.json` `authentication.required: false` / "keyless GET" and the DO-LB "ephemeral load-balancer cookie, not a credential" claims match the code (with `credentials:"omit"` the browser neither sends nor persists it). **No SSRF:** the host/path are compile-time constants; user `text` is only the `encodeURIComponent`-escaped `?text=` value and cannot alter the target. **Prompt-injection / upload controls: N/A** — no LLM surface and no file upload in this diff.

## Item 5 — Corpus fixtures / PII

**PASS.** The committed test files (`address-search.test.ts`, `address-confirm.test.tsx`, `autocomplete.test.tsx`) embed only GeoSearch response bodies — addresses, BBLs, BINs, coordinates, PAD version — all public-record fields; no owner names, emails, phones, or natural-person identifiers. The only owner-name value anywhere is `ownername:"3622 THIRTEENTH LLC"` in the **read-only basis** `db026-address-to-lot-fixture-capture.md` (§6 MapPLUTO row) — a public PLUTO column and an LLC, not natural-person PII. The owner email does not appear. Grep for secret/PII patterns returned only the benign fixture note "…is not a secret" and a pre-existing display-label mapping `ownername: "Owner name"` in `format.ts` (not in this task's scope).

---

## Advisory findings (non-blocking)

- **A-1 (informational, forward-looking):** `resolveLotFromGeoSearch` applies no explicit length bound to `parsedInput.street/houseNumber` before normalization, unlike `parseAddressSuggestions`' `boundedString`. Not exploitable today — normalization is linear (no ReDoS), fetched bodies are byte-capped upstream (`MAX_BYTES = 128000`, `:16`/`:92`), and the function has no production caller. If a future packet wires the gate to a body not bounded by this module's fetch path, add an input-length bound at the gate entry.
- **A-2 (privacy, forward-looking):** MapPLUTO `ownername` can, on other lots, contain a natural person's name. This lot's value is an LLC and lives only in the read-only fixture basis, so it is acceptable now; any future fixture that captures a lot with a natural-person `ownername` should redact it before it enters the repo (public repo).
- **A-3 (tracking):** The equality gate ships as a **dormant** library (no production importer). The adjudication and `geosearch.json` (`match_discipline_verbatim_basis`, `connector_implementation`, OQ-5) correctly bind it as the mandatory guard for any future GeoSearch→lot promotion. That binding must be honored — and re-reviewed — the moment a promotion path is introduced; it is not exercised on any live authority path at this head.

---

## Reproduction commands (read-only, orchestrator-verifiable)

- `git show --stat 3182f0a2 c3c690f7 fb7af67d` → changed files all in `allowed_paths`; forbidden paths untouched.
- `rg -n "resolveLotFromGeoSearch" --glob '*.{ts,tsx}'` → 2 files (def + test); no production importer.
- `rg -n "dangerouslySetInnerHTML|console\.|process\.env|Authorization|localStorage" apps/web/src/lib/address-search.ts` and the three changed components → no matches (only the two `href={…}` from `canonicalBbl`).
- `git show fb7af67d:apps/web/src/lib/address-search.ts | tr -d '\r' | sha256sum` → `0096e659a64b4bd68d4e5ea107a2d2e622698f2b8decd374e36fe55c14f55e3f` (matches source-sections index).
- CI at the corrected head `fb7af67d` (per stored `M5-T046-ci-evidence.md`): CI/web-e2e run `35425516645` success; secret-scan `35425516655` success — verified as stored evidence, consistent with the reviewed content.

**Files reviewed (all absolute):**
`C:\Users\MLFLL\Downloads\nyc-zoning\ctl24\apps\web\src\lib\address-search.ts`,
`…\apps\web\src\components\address\AddressConfirmCard.tsx`,
`…\apps\web\src\components\address\AddressResolutionScreen.tsx`,
`…\apps\web\src\components\architect\AddressAutocomplete.tsx`,
`…\apps\web\src\lib\__tests__\address-search.test.ts`,
`…\apps\web\src\components\address\__tests__\address-confirm.test.tsx`,
`…\apps\web\src\components\architect\__tests__\autocomplete.test.tsx`,
`…\docs\research\source-registry-drafts\geosearch.json`,
`…\docs\research\db026-address-to-lot-fixture-capture.md` (read-only basis),
`…\project-control\reports\M5-T046-producer-report.md` / `-source-sections.md` / `-ci-evidence.md`.

**Verdict: PASS** (advisories A-1/A-2/A-3 are non-blocking; none require rework for acceptance).

Note for the orchestrator: I could not write my optional agent-memory note — the read-only guard blocks shell file-writes for this agent. No action needed; the finding content is fully contained in this report.
