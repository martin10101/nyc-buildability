# M5-T055 HJ — independent human-journey review (original return + delta-attestation, both saved VERBATIM; the headers are the orchestrator's)

## Original return (at frozen head db0cf0f8)

# G3 Human-Journey Review — M5-T055 (DB-035 confirm-arc rider cluster)

**Reviewer:** human-journey-reviewer (read-only, ADR-005)
**Task:** M5-T055 — DB-035 four-rider cluster on the address confirm arc
**Frozen identity verified:** reviewed at head `db0cf0f8`; material `db445634` (card + its unit tests + `responsive-a11y.spec.ts` append + report). Confirmed the working-tree `AddressConfirmCard.tsx` is byte-identical to `db0cf0f8` (`git diff db0cf0f8 -- …` empty). The frozen→live delta (`db0cf0f8`→`59862f2c`) touches only the disjoint M5-T056 condo/architect files — the M5-T055 material is stable at the reviewed head.
**Directive refs in packet:** D-066-R001, D-073-R006, D-077-R002/R003 (formal per-requirement re-derivation is the DCV's pass; this is the journey gate).

## VERDICT: PASS (no blocking findings; 3 advisories)

All four riders are implemented honestly and match the exact refinements I asked for on the M5-T050 HJ wave (I originated rider classes (b) and (c) there; DISCOVERY_BACKLOG row 139). Web behavior proves only in CI per thin-client rules — I judged from the frozen code, the tests, the fixture harness, and my accumulated confirm-arc knowledge. The pixel-proof e2e is well-constructed and the fixture supports it end-to-end.

---

## Rulings on the four required questions

**1. Rider (b) — reading order: CONFIRMED, it is what I meant.**
The record note now renders after both interactive actions (Continue `:377-385`, Not-my-property `:392-399`) and **before** the `<Meta>` reference-id footer (`:420-431`). For a screen-reader user the linear order is genuinely content-before-footer: heading → matched → entered note → BBL → ZoLa → lot-outline → provenance → Continue → Not-my-property → **record note** → reference-id footer. The record note is card *content*; only the non-interactive reference-id footer (the sole element below it) shifts down on the late insert, so no interactive element moves — CLS is preserved by construction, not by a height guess. Unit tests bind the DOM order three ways (`compareDocumentPosition` following both actions `:898-904`; note precedes `correlation-id` `:908-910`; note is the new second-to-last child, Meta still last `:982-988`), and the e2e proves it visually (`noteBox.y ≥ ctaBox bottom`, `metaBox.y ≥ noteBox bottom`, `:284-289`). This is exactly my T050 ask ("after Not-my-property but BEFORE `<Meta>`; only the non-interactive footer shifts"). It does read as belonging to the confirm decision — it is content, reachable, above the footer — not orphaned below it. See HJ-1 for the residual tradeoff.

**2. Rider (c) — accessible name at any length: announcing full text is the honest choice; the long announcement is acceptable.**
`enteredTitle = enteredInput` (the 512-char cap `ENTERED_TITLE_ATTR_MAX_LEN` is deleted; no dangling refs remain — grep clean). For a 600-char paste the `<strong role="img" title aria-label>` announces the full visible text: announced === seen, with no "…" the eye never shows. The prior 513-char truncation *was* the defect (eye saw 600, SR heard a truncation). "Truncate both" would be strictly worse UX and guards nothing — React escapes attribute and text alike and the visible `<strong>` text was already unbounded (`{enteredInput.trim()}`), so the cap protected no exposure. The over-long announcement is acceptable: it is a rare abusive-paste edge, it is the user's own typed value, and it is strictly more honest than before (the long-announcement downside pre-existed identically at 513 chars, so this is a net improvement, not a regression). Unit binds equality directly (`title`/`aria-label`/`toHaveAccessibleName` all === the 600-char value, no "…", `:1197-1203`).

**3. Rider (d) — no-street copy trim: clear for a non-expert.**
Both rendered sentences read cleanly:
- Matched present: "You searched for **X**. We show it alongside the city-matched address so you can compare them; the identity we carry forward is the tax lot (BBL)."
- No-street: "You searched for **X**. The identity we carry forward is the tax lot (BBL)."

The removed clause ("We show it as the address you searched for") was genuinely redundant with "You searched for X". The comparison-purpose clause is correctly dropped only in the no-street branch (there is no matched line to compare against). No clarity loss. Copy sweep verified: the old sentence now survives only in a source comment (`:229`) and a `.not.toContain` assertion (`:1141`); no rendered branch emits it.

**4. Journey coherence / identity honesty: intact (my m5t046/m5t047 standards hold).**
This change touched only the entered-note copy, the accessible-name cap, and the record-note placement — none of the identity derivations. Entered value (`enteredInput`, verbatim or input_echo fallback), city-matched frontage (`addressLine`), and PLUTO record address (`recordAddress`, shown only when it differs, labeled "City record address") remain distinct and honest. No route/api/contract change; no "Verified" badge; the not-verified provenance posture (`:369-373`) is untouched. D-073-R006 record-class (no computed value) is preserved — the why-note asserts `not.toMatch(/\d/)`.

---

## Findings

**HJ-1 (ADVISORY) — record note sits below the decision controls (inherent CLS-vs-proximity tradeoff).** `AddressConfirmCard.tsx:420-431`. In the differ case the record-address disclosure is decision-relevant ("is this the right lot?"), yet it is reached only after the Continue / Not-my-property actions. A user who acts before scrolling past the buttons could miss it. This is inherent to the accepted CLS-safe design (a late async insert must sit below every interactive element) and the (b) move is a strict improvement over the accepted bottom-of-card placement. The top-of-card matched line + entered-vs-matched note already carry the primary identity check, and the record note only appears for corner/vanity lots. Not blocking; this was my own T050 refinement.

**HJ-2 (ADVISORY) — long accessible-name announcement via `role="img"`.** `AddressConfirmCard.tsx:220`. A 600-char entry is announced as a single atomic image name, which is less navigable for a screen reader than plain inline text would be. This is honest and correct per AS-3, the abusive-paste case is rare, and it is a net improvement over the prior dishonest 513-char cap. The `role="img"` wrapper itself is the pre-existing T047 mechanism (needed because `<strong>` is name-prohibited) and is out of this packet's scope.

**HJ-3 (ADVISORY, cosmetic) — stale describe-block title.** `address-confirm.test.tsx:806` still reads `S11 — DB-033 confirm-arc polish riders (a-d, i)` although rider i is now superseded by DB-035 rider c. Harmless label staleness; individual `it(...)` titles are correct.

## Positive verification (not findings)
- **AS-1 e2e exercises the real "shown" state end-to-end.** `resolveToConfirmCard` fills `100 OUTLINE AVENUE` → fixture `fixture_api.py:322` maps it to bbl `1008350041`, which matches the intercepted `record-address` body's bbl, so `showRecordAddress` is genuinely true and `data-record-address-status="shown"` is asserted before the "after" measurement. The spec waits for the lot-outline surface to settle *before* the "before" CTA measurement, so the only layout change between measurements is the note inserting below the CTA — the exact-equality assertion (`toBe(ctaTopBefore)`, `:277`) is the correct honest expectation, and the `.or()` chain tolerates the headless-CI webgl-unavailable fallback.
- **No out-of-scope consumer left red.** `address-resolution.test.tsx:74` references `/record-address` only as a stubbed 404 with no position/last-child assertion; the lib tests (`record-address.test.ts`, `condo-records.test.ts`) test the lib, not the card DOM. The (b) move breaks no sibling test. `Page` type is imported in the spec; `correlation-id` testid exists in `Meta` (`AddressOutcomeCards.tsx:41`).
- Pre-existing S6 tests and S1–S10 unit tests are byte-preserved (the spec change is a pure append after line 141; all unit hunks fall inside the S11 describe at 806).

## Residual for the record (not blocking this gate)
- Web suites (vitest `address-confirm` + Playwright `responsive-a11y`) prove ONLY in CI at the pushed head; this gate did not and cannot execute them locally (thin client). The producer correctly marks web behavior PENDING CI. Recommend the orchestrator confirm the green CI run at the pushed head (all four files landed together) before acceptance — consistent with the standard evidence-capture division; do not treat this as BLOCKED.

Memory updated: `.claude/agent-memory/human-journey-reviewer/m5t055-db035-confirm-riders.md`

---

## Delta-attestation (after the [ORCH-CORRECTED per G4-1 + CI 35493851185] spec fix)

Delta-attestation: **PASS carries to 05f1aea2.**

I verified the delta independently. `git diff db0cf0f8 05f1aea2 -- apps/web/` shows a single changed file: `responsive-a11y.spec.ts` (+47/-1). `AddressConfirmCard.tsx` and `address-confirm.test.tsx` are byte-unchanged — so every journey behavior I judged (rider b placement/reading order, rider c accessible-name honesty, rider d copy) is identical to what I passed.

The spec delta is exactly the settle-wait you described, and it is sound:
- A terminal-map-state `expect.poll` (a fallback surface present, OR `.architect-map-status` free of "Preparing"/"loading") before the "before" measurement — this correctly targets the root cause: the LotOutlineMap status line sits ABOVE the CTA, so its late "Preparing→loaded" settle moved the CTA 27px between the two reads. That is an environmental measurement-window artifact, not a product CLS defect — the record note still inserts strictly below the CTA and never moves it.
- A 500ms double-read CTA quiescence check as belt-and-braces.
- `releaseRecord()` still fires AFTER both settle-waits, and the exact-equality assertion `toBe(ctaTopBefore)` is unchanged — so the fix isolates the measurement to above-CTA stability without weakening or masking the assertion on the note insert itself.

No new findings on the delta. My three advisories (HJ-1 note-below-actions tradeoff, HJ-2 long role="img" announcement, HJ-3 stale S11 describe title) are all in unchanged files and stand as-is. Task re-frozen at 05f1aea2 carries my PASS. [Orchestrator note: CI satisfied — run 35494571792 success at bd408ab9; see M5-T055-ci-evidence.md.]
