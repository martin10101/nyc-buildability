# M5-T046 — G0 contract gate (orchestrator)

- **Packet:** address→lot identity honesty (DB-026) — the equality gate on GeoSearch
  match promotion in `apps/web/src/lib/address-search.ts` (the byte-faithful corpus
  proves `match_type`/`confidence` are non-discriminating: `fallback`/`0.8` on the true
  hit AND on nonsense that returns a real WRONG lot; the only honest success test is
  returned housenumber+street equality against the parsed input; lot identity binds to
  `addendum.pad.bbl`); the typed no-match instead of silently wrong lots; autocomplete
  shape tolerance (those two keys absent on /autocomplete); the confirm-arc records
  display (entered vs matched vs record address, D-073-R006 class, existing channels
  only — a missing record-address channel is a discovery, never a new endpoint); the
  case-1 corner-lot regression pinned offline; the GeoSearch source-registry draft.
  The server Geoclient path (M2-T021, integrated) is OUT of scope. Cites D-066:R001 +
  D-073:R006. Depends on M5-T032 (address-search reliability outcomes preserved).
- **Evidence files named in-packet:** docs/research/db026-address-to-lot-source-recon.md
  (incl. the orchestrator correction: Geoclient already integrated; the researcher's
  stale-worktree flag #10 confirmed material), docs/research/
  db026-address-to-lot-fixture-capture.md (8 byte-faithful captures, all keyless),
  docs/DISCOVERY_BACKLOG.md row DB-026 + the 2026-09-19 recon/correction notes, the
  committed source-registry-drafts/ztldb.json shape example.
- **Pairwise disjointness (D-072-R003):** mechanical check at this seam — recorded
  output `PAIRWISE DISJOINT T045 vs T046: True | intersection: []` (M5-T045 live on
  loop-1, 17 allowed paths; M5-T046 10 allowed paths). forbidden_paths additionally
  pin the T045-owned architect components and ALL of services/api. No third lane.
- **Directive binding:** D-066-R001 and D-073-R006 task_ids appended (same-commit
  digest resyncs + audit entries); `evaluate_task_refs` ok=True, applicable == cited.
  `affected_tasks` untouched (c14: verification-time only).
- **Nav block (D-066-R001):** graph regenerated at this seam (743 files / 15651 nodes /
  6934 edges); embedded in inputs — address-search consumers, the confirm-arc address/
  components, the depth-2 T045-owned exclusions, the integrated-Geoclient boundary.
- **Placeholders (c17):** geosearch.json registry-draft placeholder + producer-report
  placeholder seeded; every other allowed path already tracked with content (web test
  files all carry passing suites — no empty-vitest hazard introduced).
- **Scope sanity:** web-only (thin client: no npm locally, CI is the proof authority);
  M5-T032 outcomes byte-identical for existing cases; suggestions stay permissive,
  only single-feature PROMOTION is gated; normalization corpus-anchored, unclear cases
  fail closed to no-match; no new dependency; no contract change.

**G0 verdict: PASS** — packet is contractable and claimable.
