# M5-T095 — G4 test-adequacy review (qa-engineer "qa-t095", read-only)

> Transmission history: pinned at a612652b, delivered as two SendMessage parts (1/2, 2/2) ending with END-OF-REPORT;
> no truncation. Joined verbatim by the orchestrator (transport wrapper tags removed only). Verdict PASS: 8 of 10
> mutants caught; the 2 survivors are proven EQUIVALENT (the spike check is redundant behind GEOS validity; is_valid vs
> is_simple is interchangeable behind the O(n) checks). Advisories (a vacuous `0 <=` assert; a mutation-table label) are
> routed at the accept seam.

---

M5-T095 G4 test-adequacy review (read-only) — PART 1/2

PIN: HEAD a612652b1e777218dd7d0d6de82ab5cf9648dee5 (candidate/D-024-mrl-option-b) at start; unchanged at finish.
METHOD: in-memory mutation of the live app.scenario.proposal namespace (Write + shell file-writes are blocked for me, so harness scripts were piped to `python -` from services/api, never written to disk), re-running all 18 committed tests per mutant. Equivalent-mutant claims confirmed by independent divergence searches (120k–320k rings, fresh seeds). Env: Py 3.11.9 / shapely 2.0.7.

BASELINE: 18/18 pass in-memory. Documented full suite independently reconfirmed: `ruff check` = All checks passed; `pytest` (7 files) = 312 passed (48s). AS-3 consumers-unchanged holds.

MUTATION TABLE (2 producer + 8 mine; ≥4 required):
- M1 drop total bound → CAUGHT (test_over_budget_request_is_refused_typed: field flips to a vertex-bounds field; + boundary + sums).
- M2 restore all-pairs loop → CAUGHT (5 tests; the DETERMINISTIC kill is test_worst_case_simplicity_does_no_pairwise_python_work asserting Polygon call_count==1; plus wall-time and both AS-4 tests, since all-pairs stops using Polygon).
- W1 bound `>=` not `>` → CAUGHT (test_total_bound_boundary_is_pinned; empirically real fn does NOT raise at total==MAX=20000 and DOES at MAX+1, so `>=` reddens line 245).
- W2 footprint-only (levels ignored) → CAUGHT (test_total_bound_sums_footprint_and_all_level_outlines: footprint-only=10001<MAX vs full=20002>MAX; also both AS-1 tests, over_budget footprint=1000<MAX).
- W3 drop cond1 zero-length-edge → CAUGHT (test_fuzz_equivalence_small_and_targeted; corpus's _gen_repeat_consecutive makes zero-length edges that shapely tolerates but the old predicate rejects — cond1 is load-bearing and pinned).
- W4 drop cond2 reversal-spike → SURVIVED — EQUIVALENT mutant (see part 2).
- W5 GEOSException→True (fail-open) → CAUGHT (both AS-4 tests).
- W6a Polygon.is_valid→Polygon.is_simple → SURVIVED — EQUIVALENT mutant (see part 2).
- W6b Polygon.is_valid→LinearRing.is_valid → CAUGHT (Polygon call_count==1 + both AS-4; the Polygon construction is pinned).
- W7 bound checked AFTER outline validation → CAUGHT (test_total_bound_refuses_before_any_simplicity_check: spy call_count>0).

Result: 8 CAUGHT, 2 SURVIVED (both proven behavior-preserving). Continues in part 2.

---

M5-T095 G4 review — PART 2/2

SURVIVORS ARE EQUIVALENT MUTANTS (cannot be killed; not test defects):
- W4 (drop cond2 spike): 0 divergence vs the shipped predicate across 200k random small-grid rings + 120k corpus-generator rings (seed 999) + 5 targeted distinct/repeated reversal spikes. A 180° reversal always overlaps a segment, which shapely Polygon.is_valid already rejects, so cond2 never changes the decision. ADVISORY (code-quality, not blocking): cond2 is effectively dead for decision-identity; the producer's design note ("keep O(n) conditions 1+2") is only half true — cond1 is load-bearing (W3), cond2 is redundant with shapely. Route as a DISCOVERY, do not fix in-packet.
- W6a (is_valid→is_simple): with cond1+cond2 in front, 0 divergence vs the old all-pairs oracle across 120k rings (seed 4242). The 10k raw is_valid≠is_simple cases are all pre-caught by cond1 (duplicate points), so the predicate choice is interchangeable behind cond1/cond2. Benign.

JUDGE-QUESTION ANSWERS:
- Drop-cond1 killed by corpus? YES (W3). Drop-cond2 killed? NO — but equivalent, so correct that it survives.
- Work-count guard alone sufficient / timing flake on CI 3.12? The real M2 kill is DETERMINISTIC (Polygon call_count==1), not the timing asserts — so M2 coverage is flake-proof. Timing guards (0.5s vs ~10ms = ~50×; 2.0s vs ~53ms = ~38×) are generous secondary defense. Low flake risk.
- 24k-corpus runtime in CI: acceptable. small_and_targeted 1.8s; medium_and_extreme 8.5s (dominated by the O(n²) ORACLE on 3×999-gon, not production); full budget file ~12s.

ADVISORY (non-blocking) vacuity notes:
1. test_worst_case_simplicity_does_no_pairwise_python_work line 287 `assert 0 <= WORK_BUDGET_PAIRWISE_TESTS` is vacuous (always true); the meaningful guard is line 286 call_count==1 (verified as the real M2/W6b kill).
2. test_restoring_all_pairs_loop_reddens_work_count_guard mutates NOTHING in production — it asserts the test-module ORACLE's pairwise count. It documents the budget's meaning but does not itself kill a production regression (that's line 286). The producer mutation-table row for M2 cites this test; the actual kill is elsewhere. Label precision only.
3. GOOD: MAX_TOTAL_OUTLINE_POSITIONS==20000 asserted BY VALUE (line 544); both fuzz outcomes exercised (old_true>0 and old_false>0); refusal fields/decisions asserted (the displaced value), not tautologies.

No BLOCKING findings. Every behavior-changing mutant is caught; both survivors are provably equivalent; consumers + ruff green independently.

M5-T095 G4 VERDICT: PASS
END-OF-REPORT
