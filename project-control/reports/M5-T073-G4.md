# M5-T073 — G4 independent test-adequacy review (qa-engineer, read-only)

> Transmission history: returned complete in ONE message (END-OF-REPORT present). Saved
> verbatim by the orchestrator (transport entity-decoding only).

M5-T073 G4 (qa-engineer, read-only) — VERDICT: PASS. G3's dropped-ambiguity mutant now reddens; 5 of 6 mutants caught; one survivor and three silent-drift channels named, none blocking.

**Identity.** Pinned head `6de7f7a56e730e9b90bd2c42b908fc3e655295c8` verified at start (`git rev-parse HEAD`). HEAD moved to `ebceecfe738f72c520903aa7560d51d13574b53f` during the review — two disjoint peer commits (`2d87a99a` G3 report saved verbatim, `ebceecfe` G3 delta re-attestation + gate record). `git diff 6de7f7a5..ebceecfe` over my whole reviewed surface (harness, `bridge_ring_pairs/**`, producer report, `outline_bridge.py`) is EMPTY; the only task-packet delta is `updated_at` (05:29:32 → 05:33:13) from the gate record. **This verdict holds byte-identically at both shas.** I was producer of nothing here; I wrote no files anywhere (the Write tool is blocked for this identity, so I could not save agent memory either — the reusable lessons are in Duty 1 and F1 below). `git status --porcelain -- services/api project-control` is clean after all probes.

**Scoped suite at the pinned head (cwd `services/api`):** `python -m pytest tests/connectors/test_bridge_ring_preconditions.py -q` → `14 passed in 2.54s` (exit 0). Confirmed 4x (see Duty 4).

## Findings

**F1 — MEDIUM (test adequacy, AS-3). `counts_equal` — the exact metric DB-045(a) asks about, and the report's headline — is not asserted for any real pair.**
`services/api/tests/connectors/test_bridge_ring_preconditions.py:470-493` asserts the verdict, the refusal class, `vertex_count >= 3` on both sides, and the bound echo, but never `verdict.counts_equal` and never the per-pair counts. The F2 pins guard the *derived* verdict, not the *headline* measurement. Demonstrated in-process: substituting P05's authoritative body into P02's pair yields `counts 320 vs 4 → counts_equal = False`, and every assertion in `test_real_pair_measured_offline_with_roundtrip_provenance` still passes (digest True, verdict pin True, class pin True, `vtx>=3` True, bound True) — because P02 refuses on the *display* ring's own size before lot identity matters. So "counts_equal = true on ALL 8 pairs", the one claim the mount packet inherits, can go false with the suite green. Production-side drift is largely covered (a `_open_ring`/`_exterior_ring_from_geojson` change reddens 6 of 8 via the class pins), so the unguarded route is a manifest/fixture-pairing edit. **Recommended (strongly, before the mount packet consumes this evidence):** carry `expected_counts_equal` — or the two expected vertex counts — per pair in `pairs_manifest.json` and assert them, exactly as G3-F2 did for the verdict. Not blocking: the claim is verified true *today* by two independent reproductions (G3's and mine, below).

**F2 — LOW-MEDIUM (fixture integrity). The authoritative digest guard is conditional and silently self-disables.**
`:476-477` reads `if auth["recorded_response_body_sha256"]:` before comparing. Probe: deleting `response_body_sha256` from a manifest pair makes the recorded value `None`, the guard is skipped, and nothing fails. The display side at `:481` reads `pair["display"]["source_file_sha256"]` directly and raises `KeyError` when absent — fails loudly. Make the two sides symmetric (assert the key is present, or drop the `if`).

**F3 — LOW-MEDIUM (fixture integrity; G3-F6 assessed as requested). The authoritative-side BBL-envelope gap is now PARTIALLY closed, and should stay a recommended correction, not a required one.**
I enumerated all 56 cross-pair authoritative-body swaps through `measure_pair`. **17 of 56 survive every assertion silently** (digest still matches because the manifest digest travels with the swapped path): every swap targeting P02 or P04 (they refuse on the display ring's size, so the authoritative body is irrelevant), plus P03←P05, P05←P03, P06←P05 inside the 4-vertex ambiguous class. Crucially **none of the three passing pairs (P01, P07, P08) survives a swap** — e.g. P05←P06 flips `ambiguous_correspondence` → `residual_too_high` (3.77 ft RMS) and reddens. So the F2 verdict pins closed the channel for the load-bearing rows. The one-line fix — assert the authoritative body's `features[0].attributes.BBL` equals the pair's BBL, mirroring the display side's `parse_lot_outline` check at `app/connectors/mappluto_lot_outline.py:451` — closes all 17 and is cheap enough to take here rather than as a mount-packet rider, but the residual risk requires a deliberate coordinated manifest edit and does not warrant blocking G4.

**F4 — LOW (determinism/clarity). `build_verdict_table()` re-reads the manifest at call time while the parametrized tests bind `_PAIRS` at import.**
`:335-344` calls `load_pairs()` fresh; `:451` snapshots `_PAIRS` at import. So `test_verdict_table_builds_over_every_real_pair`'s `len(table) == len(_PAIRS)` is not the tautology it looks like — it compares a fresh disk read against the import-time snapshot. Surfaced incidentally: my dropped-pair mutant made that test fail for this reason rather than the intended one. No flake in practice; noted so a future reader does not mistake it for a coverage assertion.

**F5 — LOW (test adequacy, numeric columns). The real-pair measured numerics are unpinned to any tolerance — this is my one surviving mutant.**
Mutant: halve `rms_residual_ft` / `max_residual_ft` / `runner_up_rms_residual_ft` / `alignment_separation_ft` for real pairs only (verdict classes unchanged, since residuals stay far below 2.0 ft and passing separations stay above it) → **14 passed, exit 0**. The §5 quantified claims the mount packet will cite ("RMS residuals are 0.0013–0.0039 ft against a 2.0-ft bound", "P05's separation 1.9e-05 ft") therefore have no machine guard. One `assert verdict.rms_residual_ft < 0.01` for fitted pairs would pin the order of magnitude without pinning fragile digits. AS-2 pins the synthetics numerically, so this is a real-pair-only gap.

**F6 — LOW (coverage floor below claimed coverage).** The floor test at `:455-465` enforces the PACKET floor correctly (`>=8` pairs, `>=3` boroughs, `>=3` classes) and catches a silent drop — verified, see Duty 1. But the report and manifest claim **5 boroughs / 7 classes**; a reduction to 3 boroughs would leave the suite green while those stronger claims go stale. Risk is low (it would require removing committed, digest-bound fixtures), noting only.

**F7 — INFORMATIONAL (provenance transcription, verified sound).** Nothing asserts that the manifest's `endpoint` / `retrieved_at` agree with the P01–P04 fixture envelopes, but I checked all four: `request_url` matches the manifest `endpoint` exactly, and `retrieval_timestamp_utc` matches `retrieved_at` exactly (`05:30:57/58/58/59Z`), `http_status` 200 on all four. Faithful today, unasserted.

## Assessment per duty

**1. Mutation adequacy NOW — PASS (5 of 6 mutants caught).** All mutants applied in-process by attribute rebinding through a `pytest_collection_modifyitems` / `pytest_configure` plugin passed to `pytest.main`; no file was edited.

| mutant | result |
|---|---|
| G3's dropped ambiguity gate (classifier keeps counts + residual gates) | **CAUGHT** — 3 failed / 11 passed, at `:489 assert verdict.verdict == pair["expected_verdict"]`, on exactly P03, P05, P06 (`assert 'pass' == 'refuse'`) |
| Bridge drift: `BRIDGE_MAX_CONTROL_POINTS` 64 → 4096 (my choice; the constants test does NOT pin this one) | **CAUGHT** — 2 failed / 12 passed, P02 and P04 flip `refuse` → `pass` |
| Residual gate neutered inside the classifier (bound 2.0 → 1e6, constant echo preserved so the constants test still sees 2.0) | **CAUGHT** — 1 failed, AS-2 `test_synthetic_noncorresponding_same_count_pair_is_residual_too_high` (`'ambiguous_correspondence' == 'residual_too_high'`) |
| Count check removed (`counts_equal` forced True) | **CAUGHT** — 1 failed, AS-2 `test_synthetic_densification_mismatch_is_a_vertex_count_finding` (`assert True is False`) |
| Silent sample drift: P08 dropped from the manifest before import | **CAUGHT** — 2 failed, the floor test (`7 >= 8`) plus the table test (per F4) |
| Real-pair measured numerics halved, classes unchanged | **SURVIVED** — 14 passed → F5 |

So G3's named mutation class is closed, and the pins reach further than G3 required: they also catch a bridge-side capacity-cap change that no constants assertion covers. **Technique note worth carrying forward:** the harness imports the bridge constants BY VALUE (`from app.api.v1.outline_bridge import BRIDGE_MAX_RMS_RESIDUAL_FT`), so rebinding `outline_bridge.BRIDGE_MAX_RMS_RESIDUAL_FT` is a **no-op** on the harness — my first residual mutant reported a false "survivor" (14 passed) for exactly this reason and I re-ran it against the test module's own binding. Anyone re-running these must mutate the consuming namespace, not the defining one. (The by-value import does not weaken the real guard: a genuine bridge-side constant change is re-read at import and caught by `test_bridge_constants_are_the_accepted_bounds`.)

**2. The synthetic AS-2 triad — ADEQUATE; no missing synthetic is required in this harness.** The triad numerically pins pass (`rms < 1e-3`, separation `>= 2.0`, count 5), `vertex_count_mismatch` (5 vs 6, `counts_equal is False`), and `residual_too_high` (`rms > 2.0`) — and mutants 3 and 4 above prove each one actually bites. On G3's open question: **both observed refusal classes have their own known-outcome synthetics upstream in the accepted bridge suite** — `too_many_control_points` at `tests/api/test_outline_bridge.py:202-220` (fit level, `BRIDGE_MAX_CONTROL_POINTS + 1` circle) and `ambiguous_correspondence` at `:318-325` (HTTP level, symmetric square → 422 + no coordinates); I ran that suite at the pinned head: `42 passed`. So THIS harness does not need duplicates. The one gap worth naming: the harness's *own* ambiguity branch (`:165-166`) is exercised only by real fixtures — a ~6-line symmetric-square synthetic would make the classifier's numeric ambiguity behavior fixture-independent and give that class the same known-outcome pin the other three have. Recommended, not required.

**3. Fixture-integrity guards — MOSTLY. Silent drift now fails loudly for the load-bearing cases; four channels remain, all named above.** The digest + floor + expected-verdict assertions do work: an edited body breaks the digest on both sides (F3 applied at `:481`; both sides match on this Windows checkout, which is what actually validates §4's single-line/newline-noop argument), a dropped pair reddens the floor test, and a swapped classification reddens the verdict pin. Remaining channels, in descending order of concern: **F1** (`counts_equal` unpinned — the headline metric), **F2** (conditional authoritative digest guard self-disables), **F3** (17 of 56 authoritative swaps survive; passing pairs protected), **F6** (borough/class claims sit above the machine floor). **On G3-F6 specifically: it stays a recommended correction / mount-packet rider, not a required correction now** — the verdict pins already protect every pair whose result the mount seam relies on, and the surviving swaps all land inside refusal classes the mount packet is explicitly being told to handle.

**4. Determinism / flake — PASS.** Four consecutive scoped runs: `14 passed` at 1.50s / 2.19s / 1.85s / 2.03s. Whole `tests/connectors` suite at the pinned head: **921 passed in 6.25s** — no cross-test interference, no ordering dependence. Paths are `Path(__file__).resolve()`-anchored, so the suite is cwd-independent; `read_text(encoding="utf-8")` does universal-newline translation, making a CRLF checkout smudge a content no-op on the single-line bodies, and the two-sided digest assertions now prove that empirically rather than by argument; the arithmetic is pure-Python float (no numpy, no RNG, no clock, no network, no temp files), and `candidates.sort` is stable so alignment ties resolve deterministically. Float display in the report is transcription only. **Perf note, not a flake:** the cap-drift mutant took 64s versus 2s baseline, because the correspondence search is O(2N) affine solves at N=3093 — the production cap is what keeps this suite fast, worth knowing if the mount packet ever proposes raising it.

**5. Coverage within DB-045(a)'s question — ADEQUATE and honestly bounded.** I reproduced the full verdict table independently at the pinned head; it matches the report's §5 cell for cell: counts 6/320/4/3093/4/4/6/30 with `counts_equal=true` on all 8; RMS 0.0024 / — / 0.0018 / — / 0.0013 / 0.0022 / 0.0028 / 0.0039; separations 49.844929 / 0.876469 / 1.9384e-05 / 0.196495 / 7.310245 / 35.505023; pass = P01, P07, P08; `too_many_control_points` = P02, P04; `ambiguous_correspondence` = P03, P05, P06; borough codes {1,2,3,4,5}, 7 distinct classes. The DB-045(a) question is answered negatively on this sample and the report does not generalize. Limitations, none blocking and most already disclosed: (i) no pair in the sample actually exhibits a count mismatch, so the real-world frequency of the risk is bounded only as 0/8, never measured; (ii) P02's and P04's counts come from the harness's own `len()` comparison, a path production never traverses (G3-F7, correctly disclosed); (iii) the cross-release split on P01–P04 is now disclosed per G3-F1; (iv) vertex counts cluster at 4,4,4,6,6,30,320,3093 with nothing in the 7–29 band and nothing near the 64-point cap boundary — **a lot just under and just over the cap would be the single most informative missing sample for the mount packet**, since the capacity class is 2 of the 5 refusals.

## Corrections

**Required before acceptance:** none.

**Recommended before the mount packet consumes this evidence**, in priority order:
- **F1** — pin `counts_equal` (or the two expected vertex counts) per pair in `pairs_manifest.json`; it is the DB-045(a) headline and the only load-bearing claim with no machine guard.
- **F3** — one-line authoritative BBL check mirroring `mappluto_lot_outline.py:451`; closes all 17 surviving swaps.
- **F2** — make the authoritative digest assertion unconditional so it cannot self-disable.
- **F5** — an order-of-magnitude bound on fitted real-pair RMS (`< 0.01`).
- **F6 / F4** — optional: raise the floor to the achieved 5 boroughs / 7 classes, and note in `build_verdict_table`'s docstring that it re-reads the manifest.

**Bottom line.** The corrections cluster did its job: the mutation class G3 named now reddens on exactly the three ambiguous pairs, and the pins additionally catch a bridge capacity-cap drift that no constants assertion covers. Test adequacy is sufficient for what this task claims — the classifier is bound to production symbol-for-symbol, both observed refusal classes carry known-outcome synthetics upstream, the sample cannot silently shrink, and I reproduced every §5 number independently. The residual gaps are drift protection for evidence the mount packet will consume later, not defects in the measurement, and the strongest of them (F1) guards the very sentence the mount packet inherits — I would take it there if not here.

G4 VERDICT: PASS

END-OF-REPORT
