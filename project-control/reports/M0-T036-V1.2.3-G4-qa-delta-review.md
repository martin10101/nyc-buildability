# M0-T036 V1.2.3 — G4 QA delta review (R207 + resume-pending-prompt)

- Gate: G4 (qa-engineer) | Task: M0-T036 | Reviewed SHA: **4ff4d88 (HEAD)** | Delta: b95ebf7..HEAD
- Reviewer model: claude-opus-4-8 / xhigh | Producer: backend-engineer
- **VERDICT: PASS** (no defects). Recorded by the orchestrator; reviewer read-only (frozen-SHA export via git archive), wrote nothing to the repo.
- (Condensed faithful record; full verbatim return is in the agent transcript.)

## Verdict basis
- **R207 [R615]** — PASS. 9 tests cover: configurable via TOML; bounded/positive fail-closed defaults; malformed→`ConfigError("bad_limit")` for all 4 new fields (=0, =-5, ="lots"); per-day warn-then-trip; per-day trip-at-bound; per-day reset on UTC-day roll; missing/empty-day fail-closed (`BreakerError`); `record_daily` refuses a non-per-day counter; cpu/memory gauges trip at ceiling with warn band. Validation is non-vacuous (derives keys from dataclass fields + positive-int enforcement).
- **resume-pending-prompt [R616]** — PASS. 6 tests, integration-level (real StateMachine/DurableJournal + real CLI via `cli.main`): happy-path (parked WAIT_FOR_OWNER + matching digest → FORWARD_PROMPT, durable advance, redacted `operator_resume_pending_prompt` audit event), + 5 fail-closed (wrong digest / no pending record / wrong state PAUSED_RECOVERY / malformed journal / durable emergency stop) each asserting non-zero exit + specific stderr + no state mutation.

## Independently executed
- Full supervisor suite at frozen 4ff4d88 (frozen export): **1163 passed / 2 skipped, 0 genuine failures** (5 transient failures were a `tools/`-only-export artifact reading `project-control/` files; re-ran green once the full tree was exported — none touch the delta).
- **Mutation probe (proves tests genuinely gate):** (a) neuter per-day reset → per-day-reset test FAILED; (b) `raw<=0`→`raw<0` → malformed-limit test FAILED; (c) disable digest-mismatch guard → wrong-digest test FAILED. 3/3 red on breakage.

## Findings: none (no defects, no rework).
Informational: the R207 per-day counters + cpu/memory gauges are mechanism-complete + fail-closed at the breaker/config layer but not yet invoked in the live loop (no production `record_daily`/`gauge("cpu_percent"|"memory_bytes")` call site) — consistent with the existing per-task counters/process/disk gauges (also not cycle-wired); live-cycle wiring belongs to supervisor activation, outside this delta's scope. Flagged so the orchestrator does not assume these caps already bound a running supervisor.

## Conclusion: PASS. 15 new tests collected, green, mutation-sensitive; adequately gate both new behaviors; full suite 1163/2, no regressions. Wrote nothing (worktree `git status` empty; all writes to isolated Temp).
