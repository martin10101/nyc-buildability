# M5-T096 — G5 security review (security-reviewer "sec-t096", read-only)

> Transmission history: pinned at 49dcdbd2, delivered as two SendMessage parts (1/2, 2/2) ending with END-OF-REPORT;
> no truncation. Joined verbatim by the orchestrator (transport wrapper tags removed only). Verdict PASS: the PDF has no
> active content, the GLB is self-contained, the DXF has no external references or xdata; synthetic content only.
> Advisory: the README's optional third-party web glTF viewer wording must not be reused for real geometry.

---

M5-T096 G5 security review (read-only, sec-t096) — part 1/2. PIN: HEAD = 49dcdbd2ab1927e8997079a03733eef3be7cc580 at start (== expected). Material commit d59cbfca. Binaries inspected by reading bytes only (Python from repo root); never opened in any application. Recorded nothing to the ledger.

SCOPE (exact): the 9 files in d59cbfca are precisely the packet allowed_paths — dxf_writer.py, its 2 tests, docs/samples/cad/** (3 samples + README + .gitattributes), producer report. No forbidden path, no route/main.py/web/requirements change, native DWG untouched, zero new dependencies. All 3 sample sha256 match README + G1.

PDF (docs/samples/cad/example-site-plan.pdf, 1839 B): standard %PDF-1.4, 5 objects. NONE of /JavaScript /JS /OpenAction /AA /Launch /URI /EmbeddedFile /RichMedia /XFA /AcroForm /Action /GoToR /SubmitForm /Encrypt present. Only benign names (Catalog/Pages/Page/Font/Helvetica/MediaBox/Contents/Resources). No external/http/file/UNC refs. xref consistent: startxref 1656 -> "xref"; all 5 object offsets resolve to "N 0 obj"; trailer /Size 6 /Root 1 0 R. Content stream = pure vector ops + text; nothing fires on open.

GLB (example-massing.glb, 2460 B): valid glTF 2.0, declared length == actual, 2 chunks (JSON+BIN), no trailing bytes. Single EMBEDDED buffer (no uri); 0 images/textures/samplers; extensionsUsed/Required = none; no uri/http/data:/file:/.. token anywhere in JSON; bufferViews in-bounds (max end 312 == buffer 312).

DXF (example-site-plan.dxf, 7936 B): pure ASCII, LF-only, 0 control bytes. NO XREF/external block ref/OLE/IMAGE/HYPERLINK/RTEXT/FIELD/PROXY/WIPEOUT/VBA/LISP/SCRIPT. ZERO xdata group codes 1000-1071 (no group-1001 URL xdata). ZERO INSERT/block refs. Only 3DFACE/LINE/POLYLINE/VERTEX/SEQEND/TEXT; sections HEADER/TABLES/ENTITIES/EOF; tables VPORT/LTYPE/LAYER/STYLE. Text values safe (no CR/LF/backslash escapes).

Part 2/2 follows: content/PII, module, README, findings, verdict.

---

M5-T096 G5 part 2/2 — content/PII, module, README, findings, verdict.

CONTENT / PII: synthetic 100x80 ft lot at a round NYC-range EPSG:2263 anchor ($EXTMIN 988000/209987, $EXTMAX 988100/210080). NO real BBL — the PDF shows "BBL: EXAMPLE-0000000000" (borough digit 0 = invalid, clearly a placeholder). No address, owner, email, or other PII in any file. Triple-labelled "Example lot - not a real property" (DXF TEXT, PDF title block, GLB node names) + "PROPOSED - NOT A CITY RECORD" everywhere. JUDGMENT: the label is sufficient — any valid 2263 coordinate lies within the mapped city, the geometry is a generic rectangle (not a real parcel boundary), and no identifier ties it to a real property; it is not presented as the subject.

MODULE (dxf_writer.py): import allowlist is load-bearing — AST-based, exactly {__future__, math, collections.abc, dataclasses}, mutation-proven (`import json` reddens it, test_dxf_writer.py:573). No logging/network/fs/dynamic-exec calls; pure in-memory string build. Single-choke-point sanitizer refuses CR/LF/NUL/ESC/non-ASCII (mutation-proven necessary, test_dxf_writer.py:419); entity + floor caps refuse before allocation (DoS guard). No secret-like patterns in the diff.

.gitattributes: exactly *.dxf/*.pdf/*.glb binary, scoped to the docs/samples/cad/ subtree only; nothing broader.

N/A dimensions (no attack surface in this packet): cross-tenant isolation, service-role secrecy, private storage/RLS, SSRF, upload controls, prompt-injection — this ships a stdlib in-memory writer + static synthetic files; no DB/Supabase/storage/auth/route/AI. Least privilege = enforced by the import allowlist; log redaction N/A (no logging).

FINDINGS — no critical/high/medium; nothing BLOCKING.
- LOW/ADVISORY: README (docs/samples/cad/README.md:60-62) offers an external third-party web glTF viewer (client-side, no upload implied). Harmless for this synthetic sample; if the wording is reused for a real building's massing, dragging real geometry onto a third-party site is a data-handling consideration. Primary path (Windows 3D Viewer) is local. README asks the owner to lower NO security setting, enable no macros, install nothing beyond a first-party MS app.
- INFO: the allowlist AST helper skips relative imports (level>0); none exist and such an import cannot add network/FS capability — negligible.

Absolute paths: C:\Users\MLFLL\Downloads\nyc-zoning\ctl24\docs\samples\cad\example-site-plan.dxf | ...\example-site-plan.pdf | ...\example-massing.glb | ...\README.md | ...\.gitattributes ; C:\Users\MLFLL\Downloads\nyc-zoning\ctl24\services\api\app\cad\dxf_writer.py ; C:\Users\MLFLL\Downloads\nyc-zoning\ctl24\services\api\tests\cad\test_dxf_writer.py ; ...\test_cad_owner_samples.py

M5-T096 G5 VERDICT: PASS
END-OF-REPORT
