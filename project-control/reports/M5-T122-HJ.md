# M5-T122 — human-journey review (human-journey-reviewer "hj-t122", read-only)

> Transmission history: pinned at 9d5311a8 (material b2e833fd; CI run 36116891984 green at 9d5311a8), delivered as
> 3 SendMessage parts ending with END-OF-REPORT; no truncation (part 3 was queued twice, byte-identical; the first
> copy is joined). Joined verbatim by the orchestrator (transport wrapper tags removed only). Verdict PASS with four
> ADVISORY items (1 the doubled 'Unresolved data conflicts' heading when both kinds of conflict exist, 2 a pre-existing
> grammar nit in the moved 'Parcels differ' sentence, 3 the C06 comparison always prints, 4 the polish shows only
> when the condo-records channel returns data).

---

M5-T122 HJ review (READ-ONLY) part 1/3 — CI green, condo overview is clearer + honest, no blocking findings.

HEAD pinned 9d5311a8 at start (matches expected). CI run 36116891984 CONCLUDED: status=completed, conclusion=success. web-e2e (vitest + Playwright) ✓ 5m14s; web lint/typecheck/build ✓; modularity, control-plane, supervisor-bridge, all jobs ✓. Walked the loaded /property overview + printed brief from source, the unit tests, and the green e2e specs (thin client, no local browser). Compared against the claim-seam 55276f2d.

WHAT AN OWNER SEES DIFFERENTLY (all honest, all clearer than 55276f2d):
1. The "Active issues" box at the top is now a real heading you can jump to, and a screen reader says its name once, not twice (OverviewExceptionStrip.tsx:66,69).
2. The stale-source warning now has a clickable "Retrieval status in Evidence →" link, like the conflict and missing warnings already had (:115).
3. For a condo split across several lots, the top warning now says plainly "the condo's base lot is not resolved to a single lot; see 'City records for this condo' below" instead of the misleading "official sources disagree" (:90).
4. The most serious "wrong property" withhold is now a visibly distinct rose-tinted box, no longer the same amber as the issues strip.
5. The condo records section carries small "Reference only" / "City record" / "Human record" claim labels, a plain "Zoning missing for N lots" count, and a "Parcels differ" note with a click-to-compare list — all making clearer these are city records, not computed allowances.

HONESTY (verified): the condo surface MOVE into CondoRecordsSection.tsx is verbatim — I grepped the before-state (55276f2d) and every condo h2 name and core sentence ("city record of the mapping", "shown for reference under the development limits above", the self-attested refusal, revoked-vs-never, the base-lot list, the provenance footers) is BYTE-IDENTICAL. The only changes are ADDITIONS (three claim tags placed as h2 siblings so the locked report-view.test.tsx names are unchanged, the missing-lot count, and the discrepancy comparison). Nothing weakened; records never read as allowance/permitted/approved/maximum-allowed (D-073-R006 grep gate + explicit test assertions); the withhold stays monotonic; role=alert kept for identity, role=status for channel disagreement. Scope is exactly the allowed paths; the facade compiles (web typecheck green). No BLOCKING findings. Riders + advisories follow.

(part 1/3 continues)

---

M5-T122 HJ review part 2/3 — my four M5-T119 riders all landed; the condo walk is honest per state.

MY T119 ADVISORIES — how they landed:
- A2 (doubled name / no heading): FIXED. Single `<h2 id="overview-exception-strip-heading">Active issues</h2>` + region `aria-labelledby` that id, and NO aria-label (OverviewExceptionStrip.tsx:66,69). Name announced once, strip now in the heading outline. Unit test asserts the single h2 + aria-labelledby + `not aria-label`; desktop AND 360px e2e assert it too.
- A4 (stale row no link): FIXED. `<Link href=evidence>Retrieval status in Evidence →</Link>` (:115), matching the actionable path the conflict/missing rows set.
- A5 (condo conflict wore "official sources disagree"): FIXED. condo_base_lot_resolution partitioned out (:53-54) into its own row with a TRUE clause (:90) pointing at "City records for this condo"; genuine source conflicts keep the accurate clause. The both-present test (overview-exception-strip.test.tsx:138-151) proves each clause distinct and true.
- A7 (identity alert shared the strip's warm box): FIXED — and I checked this is NOT dead CSS. The new rule architect.css:71 `.architect-alert[data-identity-state]` (rose) matches a `data-identity-state="differs"/"absent"` attribute ALREADY emitted by the FORBIDDEN/read-only AnalysisIdentityNotice.tsx:72, so the rose treatment is live. The selector correctly EXCLUDES the `.architect-note` substitution (line 64, data-identity-state="substituted"), so a legitimate substitution stays neutral per spec A14. Rose is layered on top of role=alert + the withhold text — colour is never the sole signal (satisfies the "not colour alone" rule).

CONDO WALK — every situation keeps its exact meaning and role (CondoRecordsSection.tsx):
- Single resolved: "Recorded base lot for this condo" + "City record" tag + "a city record of the mapping, stated as entered versus analyzed" (:233-237). Not an allowance.
- Multi-lot, mixed-known zoning: "City records for this condo" + "Reference only" + per-lot "recorded zoning: R7-2" / "not recorded (unknown)" + "Zoning missing for N lots" (singular/plural correct) + the named ZTLDB gap note gated on anyZoningUnknown, divergent notice gated on anyZoningRecorded (:247-302).
- Self-attested: "Human record" + "Self-attested … refused for any calculation use" beside the record (:192,195).
- Revoked/superseded vs never-recorded: two distinct honest texts on confirmationCount (:210).
- Discrepant parcels: bold "Parcels differ." + comparison `<details>` listing recorded-vs-current + "does not change the confirmation, which changes only by a human act" (:196-201). Surfaced for review, never a silent revoke.
- Unavailable / route_absent: renders null — honest absence, no withhold on its own (:316). Non-multi-lot channel/profile disagreement: role=status "no city records are shown here. The development limits above govern." (:309-312).

(part 2/3 continues)

---

M5-T122 HJ review part 3/3 — print, a11y, advisories, verdict.

PRINT (OQ-3): the condo section reaches the brief — ReportView renders the SAME deriveCondoSurface decision + CondoRecordsChannelSection (ReportView.tsx:45-46,97). The new C06 comparison `<details>` (class architect-condo-discrepancy-detail, NOT architect-raw) is opened by ReportView's generic beforeprint handler that opens every non-raw details (ReportView.tsx:57-58), so recorded/current parcels print. Acceptable per C06 ("prints in brief"). report-view.test.tsx is untouched and green.

ACCESSIBILITY: one role=status strip per event (LS-P16); identity mismatch stays role=alert and incomplete-assessment stays role=status — neither folded (overview-exception-strip.test.tsx:178-209). The strip h2 joins the heading outline that already has "Site context"/"Development limits". Keyboard: every row's review link is a real anchor. Colour never sole carrier — the rose identity box adds to role=alert + text; the claim tags are text. No horizontal overflow at 360px with all riders present (responsive-a11y e2e). The integration test asserts zero legacy `.architect-alert` blocks remain and "Unresolved data conflicts" appears once on a normal (single-conflict) overview.

ADVISORIES (none blocking):
1. D1 doubled heading (producer OQ-1): a profile carrying BOTH a genuine source conflict AND a condo base-lot conflict shows two rows each bold-headed "Unresolved data conflicts" with different clauses. Each clause is true and the condo row disambiguates via its "City records" pointer, but an accurate heading like "Condo base lot not resolved" would read clearer beside a real source conflict. The clause faithfully honours my A5 ("keeps its heading") — worth an owner call on whether accuracy or literal-rider wins. Cosmetic.
2. Pre-existing grammar nit carried in the verbatim move: "The base lots the city records now differ from the lots in this confirmation" (CondoRecordsSection.tsx:197) reads garbled — clearer as "the city now records". NOT introduced here (byte-identical 55276f2d:240); flagging only because it now sits inside an eye-catching "Parcels differ" block.
3. C06 comparison always prints (not gated to the audit-appendix tier). Acceptable per C06; a future slice could mark it raw-tier if print bulk on large condos matters (producer OQ-3).
4. Reachability caveat (context, not a defect): the §5.4 polish is seen only when the per-BBL condo-records channel (useCondoRecords, PropertyOverview.tsx:84) returns single/multi_lot; an `unavailable` channel shows the honest-absence null path and none of the new tags. I verified UI wiring, not that the backend condo-records route returns data in prod. Correct fail-safe; it just bounds what an owner will actually SEE.

VERDICT BASIS: the condo overview is clearer than 55276f2d and still honest — the four riders I raised all landed (A7 is live CSS, not dead), the §5.4 changes are purely additive over a byte-identical move, records never read as allowances, withholding stays authoritative, and the strip is now one heading-navigable status region. Scope is exactly the allowed paths and CI is fully green. The four advisories are clarity refinements, none a user-safety or honesty defect.

M5-T122 HJ VERDICT: PASS

END-OF-REPORT
