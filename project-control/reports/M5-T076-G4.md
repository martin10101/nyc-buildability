# M5-T076 — G4 test-adequacy review (qa-engineer, read-only)

> Transmission history: returned complete in ONE message (END-OF-REPORT present). Saved
> verbatim by the orchestrator (transport entity-decoding only).

M5-T076 G4 (test adequacy) verdict: PASS — with two non-blocking coverage riders; the producer's mutation claims are honest but the F2 net covers only half its failure surface.

Head verified: `git rev-parse HEAD` = 8ea1c8cc89124beeaf80a7bd0f0f4f4216f71e3d. Working tree clean under services/api and project-control. I did not re-litigate the G3 findings. All mutation work was in-process attribute rebinding (`python -c` / stdin + `pytest.main`); no tree edits, no writes.

Independently re-run at the pinned head, from `services/api`:
- scoped suite → **55 passed** (2.89s); also 55 in reversed file order, 37 route-file-alone, 18 scenario-file-alone, 55 on a repeat run
- `python -m ruff check .` → All checks passed
- wider `tests/api tests/scenario` → **1320 passed**
- `python tools/modularity_check.py --check` (repo root) → selected 476 files; failures 0; warnings 22
- AS-4 bound re-verified by blob, not diff-absence: `HEAD:services/api/app/scenario/max_envelope.py` = f0abf88479d078d8ec7e88d4c2b1942fd69c040d, identical to the pre-T076 parent ca06a0fc

FINDINGS

**F1 (MODERATE, mutation-adequacy gap on the F2 fix) — services/api/tests/api/test_max_envelope_api.py:668-694.** The malformed-segments parametrization is `["abc", 42, {"id": "L-S"}, True]` — all TRUTHY. Every truthy value takes the same branch under both the strict guard and the idiomatic weakening `if lot.get("lot_line_segments"): return False`, so that naive guard ships GREEN across the whole route pack (37 passed under the mutant) while five falsy malformed values are silently replaced by derived geometry. Verified live, both directions, at `max_envelope_api.py:162-180`:
  shipped → `0`, `""`, `{}`, `False`, `0.0` all give 422 validation_error (correct)
  naive mutant → all five give 200 with `placement=fitted`, `derived=derived`
That is precisely the F2 defect class ("non-list silently replaced instead of typed-refused, reproduced 200-fitted"). To be fair to the producer: the mutation it actually claimed — restoring the exact pre-fix guard from 672c5743 — does redden, 4 failed with `assert (200, None) == (422, 'validation_error')` at :686. The claim is honest; the net is just half-width. Fix is one line: extend the existing `@pytest.mark.parametrize` with `0, "", {}, False`.

**F2 (MINOR, taxonomy detail coverage) — tests/scenario/test_lot_geometry_derivation.py:423-428.** `BBL_UNRESOLVABLE` is the only one of the seven outcomes whose honest `detail` is asserted nowhere, at either level. A mutant blanking the details of `BBL_UNRESOLVABLE` and `CONNECTOR_FAULT` reddens only `test_connector_fault_is_fail_closed` (1 failed, 54 passed) — so emptying the bbl_unresolvable reason ships green. Related, smaller: the route tests at :591 and :608 assert only `derived_lot_geometry.outcome`, not that the reason reaches `placement["detail"]`; the append branch itself is covered by the 3-case parametrized test at :571, so this is completeness, not a live hole.

**F3 (MINOR, boundary) — services/api/app/scenario/lot_geometry_derivation.py:177.** The at-cap case (segment count == `max_segments`) is untested; the cutter test at :412-420 uses caps of 800 (far above) and 2 (N-1) only. Rebinding the cutter with `max_segments - 1`, which is exactly the `>` → `>=` off-by-one, gives **55 passed**. Real-world effect of that mutant: a valid lot with exactly 800 exterior segments would be wrongly refused as `geometry_over_cap`. Fail-closed direction, so low risk.

**F4 (MINOR, untested taxonomy hole) — max_envelope_api.py:177-180.** A non-scalar `lot.bbl` (dict or list) returns False from `_should_derive_lot_geometry`, so the request gets 200 with the honest gap but NO `derived_lot_geometry` block at all — no outcome, no signal, no typed refusal. Verified live for `{"x":1}` and `["1008350041"]`. A malformed *string* BBL by contrast yields `bbl_unresolvable`. No test covers it. Fail-closed, and consistent with "no BBL supplied", so a rider.

DUTY-BY-DUTY

1. **Mutation adequacy.** I spot-verified three of the producer's claimed mutations plus two of my own. F1 guard removed (in-memory source variant, `except Exception` → a never-raised type, router exec'd into a fresh namespace): shipped gives `(500, 'internal_error')` + `X-Correlation-ID`; unguarded gives `(500, None)`, no header, bare `Internal Server Error` body — the test at :643 is a true red/green witness. F2 old guard restored: 4 failed as claimed. Exterior-only → all-rings (holes promoted to exteriors, so the original cutter walks every ring): `assert 463 == 320` at test_lot_geometry_derivation.py:273, exactly the number the report predicted. The per-fix matrix in the producer report is honest. Adequacy is nonetheless incomplete per F1 and F3.

2. **Seven-outcome taxonomy.** All seven are reachable and asserted at unit level, and all seven at route level too. Honest detail asserted for 6 of 7 (F2 above). The three-way separation the G3 F4 asked for is genuinely pinned: `GEOMETRY_OVER_CAP` vs `INVALID_GEOMETRY` has a dedicated non-conflation test asserting the outcomes AND the detail strings differ, and the recorded MPG07 multipolygon is exercised at both cap=800 (over_cap) and cap=4000 (derives all 3130) — that second half is what actually proves the official data is not at fault. The degenerate branch is honestly disclosed as private-helper-level only, with a stated reason (no assessment-valid degenerate ring is producible through the real analyzer); I agree with that call.

3. **Derived-but-unfittable (MPG02).** Yes, and this is the strongest test in the pack. `test_recorded_single_lot_derives_but_is_honestly_unfittable` (:751) asserts `placement["detail"] ==` the full engine string by equality, not containment. I diffed it against the engine literal at `max_envelope.py:663-666` — byte-for-byte identical. It additionally asserts the derivation suffix is *absent* (no derivation failure occurred), which is the harder half. Zero drift tolerance.

4. **Byte-identity (the F7 fix).** Non-derivation is proven at both levels — the getter is a spy asserting `resolutions == []` and the provider asserts `calls == []` (:554-555), so a mutant that resolved-then-discarded still reddens. The test name overstates slightly: it performs no byte comparison. I closed that myself by exec'ing the pre-T076 route (ca06a0fc) in memory and diffing full JSON bodies modulo `correlation_id` across five request classes — segments present, segments+bbl, empty+no-bbl, malformed, malformed+bbl — **all five byte-identical**. AS-3 byte-identity is true; the suite proves it by proxy, which I judge adequate.

5. **The G3-delta's null → 422 concern: factually accurate, and it stays an accept-sweep rider.** Reproduced: with a BBL and a failing provider, absent → 200 honest gap (`no_feature`), `[]` → 200 honest gap, `null` → **422 validation_error on `lot.lot_line_segments`**. One point sharper than the delta made: on the null path the derivation outcome is discarded entirely, so the caller is told their own field is invalid rather than receiving the `no_feature` gap. Still not a required correction now — it is fail-closed, it is not a regression (`null` without a BBL is 422 today), the route is UNMOUNTED so there is no consumer, and G3 already recorded it. Note the cheapest fix is a docstring qualification, not a behavior change: `_should_derive_lot_geometry`'s docstring (max_envelope_api.py:167-171) and the test docstring at :698-700 both assert an absent/None/[] equivalence that holds only on the success path.

6. **Flake risk: LOW.** 55 passed across four invocations, both file orders and each file alone; no ordering or module-state leakage (the one test touching module globals uses `monkeypatch.setattr` on `_PRODUCTION_CLIENT` and `ResilientMapPlutoGeometryClient`, which restores both, and no test constructs the real client). No external network: I re-ran the pack with `socket.socket.connect` blocked for every non-loopback address → 55 passed, exit 0 (the only connects are the TestClient portal's 127.0.0.1 self-pipe). Fixture I/O is deterministic, and both loaders assert `classification == "raw"` plus a `live ` capture_method, so a synthetic substitution cannot masquerade as recorded evidence — good discipline. Float assertions are exact equality on decimal-string round-trips, which is deterministic. One bounded environmental dependency, pre-existing and not introduced here: the canonical rings come from shapely/GEOS via `_canonicalize_shapely`, so the exact counts (6 / 320 / 143 / 3130) and the first-vertex tuple ride the connector's existing shapely pin.

DISPOSITION. F1 and F3 are test-only additions; shipped behavior is correct in every case I probed, so I am not making them blocking corrections — that would cost a rework + delta cycle for what is a one-line parametrize extension and one extra assertion, against an unmounted route. I recommend both as accept-sweep riders alongside the G3-delta's null-path item, with F1 carrying the most weight because it leaves the exact defect class that caused the G3 FAIL only half-netted.

One process note: I was unable to write my agent-memory entry — Write is blocked for this identity ("'t076-g4' is operationally read-only and may not use Write"), including under `.claude/agent-memory/qa-engineer/`, which the dispatch prompt permitted. The lesson worth persisting, if you want to capture it on my behalf: when a guard fix is the headline correction, run two mutants — the literal pre-fix code from git AND the idiomatic simplification a future maintainer would plausibly write — because a truthy-only malformed-value parametrization cannot discriminate against `if container.get(key):`.

G4 VERDICT: PASS

END-OF-REPORT
