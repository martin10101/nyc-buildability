# M5-T033 producer report (placeholder)

Seeded at contract time so the packet's allowed_paths carry a tracked file (gate
fail-closed rule). The loop producer replaces this content with its verbatim return.
