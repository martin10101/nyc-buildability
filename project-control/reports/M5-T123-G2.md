# M5-T123 - G2 producer self-check record (recorded by the orchestrator, role self_check)

Producer: backend-engineer (orchestrator-dispatched subagent) in wt-m5t123; one in-worktree commit 8d226b2f (parent 8acf9c86), cherry-picked to de92182d with every file MATCH by git blob identity (9 files: project-control/reports/M5-T123-producer-report.md ac510dcc, services/api/app/api/v1/dxf_import_api.py 1329bc90, services/api/app/api/v1/export_api.py 0d5f2f07, services/api/app/api/v1/scene_api.py 1f03ff7c, services/api/app/resilience/rate_limit.py 0ff04d27, services/api/tests/cad/test_export_api.py d6407d7d, services/api/tests/drawings/test_dxf_import_api.py 0e789d64, services/api/tests/resilience/test_rate_limit.py 4ee8baa2, services/api/tests/scenario/test_scene_api.py 027116e2).

- **Scope:** services/api/app/api/v1/scene_api.py, export_api.py, dxf_import_api.py: each route's OWN request-body ceiling (scene 2 MiB, export 1 MiB, DXF import 20 MiB with the route DxfLimits max_bytes raised) replacing the borrowed proposal_validation 256 KiB, enforced before buffering past it with the unchanged 413 shape; job slots scene 8 / export 12 / dxf 4 (sum 24 < anyio's 40). services/api/app/resilience/rate_limit.py: a whitespace-only principal treated as absent; the pool relation documented. Tests: ceiling edge cases per route, the slot-sum guard, principal hygiene, the AST no-local-limiter guard extended to list/set/Counter, event handshakes replacing the 50 ms thread-start window.
- **Producer self-checks (report, OBSERVED):** ruff clean; the four target suites 199 passed (was 190 at M5-T117); modularity exit 0 (no changed file flagged); all run directly under local Python 3.11 (no PEP 695 chain). Seven in-process source mutations each RED: three ceilings reverted to 256 KiB, slot sum to 40, principal not stripped, the old AST guard, release-on-every-cancel.
- **[ORCH-HARVEST] at the harvested head de92182d (cherry-pick de92182d; Python 3.11 local): `python -m ruff check .` (cwd services/api) -> exit 0: All checks passed!; `python -m pytest tests/resilience tests/scenario/test_scene_api.py tests/drawings/test_dxf_import_api.py tests/cad/test_export_api.py -q -p no:cacheprovider` (cwd services/api) -> exit 0: 199 passed in 13.34s; `python tools/modularity_check.py --check` (cwd .) -> exit 0:   warn review_signal: tools/agent_supervisor/gate_wave.py - above the justification threshold; record a cohesion justification in review.**
- **Discoveries routed (producer):** DB-D4 a vertex-dense DXF averages ~7.45 bytes/line, so the reader's default max_lines 2,000,000 binds at ~14.2 MiB - a dense 20 MiB file is refused TOO_MANY_LINES before max_bytes (raise max_lines with max_bytes at the next dxf_reader touch or PKT-H; both under the reader's ceilings); DB-D5 the architect corpus records no DXF byte sizes (the 20 MiB ceiling rests on the documented range); worst-case transient memory ~312 MiB (108 MiB raw) across all 24 slots - PKT-H re-tunes against the real instance; the auth principal (DB-086 e) stays a PKT-H item.

G2 verdict: PASS (self_check; the independent reviews and the DCV follow - dispatched by the successor session per the handoff).
