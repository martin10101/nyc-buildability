# M5-T112 — G4 test review (qa-engineer "qa-t112", read-only)

> Transmission history: pinned at 1461966b, delivered as 3 SendMessage part(s) ending with END-OF-REPORT;
> no truncation. Joined verbatim by the orchestrator (transport wrapper tags removed only). Verdict FAIL on the same vacuous
> charge-order guard (mutant ME caught by no test) -> rework; equivalence, triangulator and DB-079 b proven real.

---

M5-T112 G4 review Part 1/3 (setup, scope, equivalence AS-2, modularity AS-1) — verdict in Part 3.

PIN: HEAD `1461966b` at start (matches expected). All verification in-process: the sandbox blocked file writes, so instead of an on-disk `git archive` copy I loaded the CLAIM-SEAM `massing_model.py` (275ef85f) in-process. `proposal.py` is byte-identical claim-seam↔HEAD (verified by `git diff`), so the claim-seam monolith's relative imports resolve correctly.

SCOPE (clean): vs claim seam 275ef85f only the 6 code/test files + the report changed. `tests/scenario/test_massing_model.py` and `test_scene_assembler.py` are BYTE-IDENTICAL to the claim seam (`git diff` empty); `scene_assembler.py`, `proposal.py`, `test_scene_api.py` UNTOUCHED. The facade re-exports all six names scene_assembler.py:63 imports (SOURCE_PROPOSED, VERTICAL_UNIT, MassingModel, MassingModelError, build_from_generated_option, build_massing_model); 140 scene+model tests pass.

AS-2 EQUIVALENCE — PROVEN REAL. I recomputed the corpus against the CLAIM-SEAM code and compared to the goldens pinned in test_massing_split_equivalence.py:
- All 7 success `content_hash`es MATCH claim-seam (rect/lshape/ushape/comb/five_floor/multi_level_shared_outline/generated_option).
- 10/11 refusals MATCH claim-seam EXACTLY. The ONLY difference is `level_outline_overflow_bigint`: claim-seam field = `proposed_massing.outline` → post-split golden = `proposed_massing.levels[0].outline`. `footprint_overflow_bigint` stays `proposed_massing.outline`.
So the goldens are genuine captures (not fabricated to match post-split output), and the sole behaviour change is exactly DB-079(b) — not a blanket rename. `python -m ruff check .` clean; `pytest tests/scenario` = 813 passed.

AS-1 MODULARITY — PASS. `python tools/modularity_check.py --check` exit 0; none of the four modules appears in the warning list (all under 600). Physical lines: facade 565, guards 213, triangulation 290, mesh 109. Import DAG proposal→guards→triangulation→mesh→facade, no cycle (813 tests import cleanly).

AS-5 SCOPE — PASS. Forbidden paths untouched; test_t098_as5 import allow-list passes; zero new dependencies.

---

M5-T112 G4 review Part 2/3 (public triangulator AS-3, DB-079b AS-4, mutation battery).

AS-3 PUBLIC TRIANGULATOR — PASS; tests adequate and NOT tautological. test_massing_triangulation.py covers convex (square/octagon), concave L & U, collinear collapse, work-budget refusal, self-touching, too-few, and closing-duplicate indexing. The area oracle is independent: `_tri_signed_area` is defined in the test (not imported from the module under test), cross-checked against shapely `poly.area`, and `poly.covers(tri)` catches an ear spanning a concave notch. 30 passed.

AS-4 DB-079(b) — PASS. test_db079b_level_outline_overflow_names_the_level_field (test_massing_split_equivalence.py:222) reddens under the OLD label, proven two ways: (1) claim-seam yields field=`proposed_massing.outline` ≠ the asserted `proposed_massing.levels[0].outline`; (2) mutant M10 (locator → const `proposed_massing.outline`) makes the assertion fail. The footprint-overflow case is unchanged, so the fix is specific.

MUTATION BATTERY — re-run on the CONSUMING namespaces in-process; all detected (RED):
- M1 `massing_triangulation._point_in_triangle`→False: concave-L triangle-area gap 1500, `poly.covers` False → area/cover assertions fail. RED.
- M2 `massing_triangulation._cross3`→const 1.0 (collinear collapse never fires): triangulate_polygon raises on the 5-vertex non-simple ring, so `len(ring)==4` cannot hold. RED.
- M3 `massing_triangulation._WorkBudget.charge`→no-op: no refusal raised, pytest.raises unsatisfied. RED.
- M10 `massing_model._locate_overflow_field`→const (facade namespace, where the OverflowError arm reads it): field reverts to `proposed_massing.outline`. RED.
- MY OWN, on a MOVED guard `massing_guards._require_raw_vertices_in_nyc_bounds`→no-op (patched in the consuming triangulation namespace): the 4326-lot refusal reason flips `lot_ring_out_of_nyc_bounds`→`footprint_outside_lot`, reddening the pinned equivalence refusal. RED.

Every moved path has a pinned case: ring prep, collinear collapse, concave ear-clip, work budget, NYC-bounds, finiteness/magnitude, bounded echoes + geometry_engine_error (via the accepted file: unrepresentable×3, MAX_ECHO_CHARS×4, geometry_engine_error×6), mesh build, multi-level plates, the new DB-079b locator.

---

M5-T112 G4 review Part 3/3 — one BLOCKING finding + verdict.

BLOCKING (test adequacy): `tests/scenario/test_massing_model.py::test_t098_as4_scan_units_charged_before_the_inner_scan` (lines 1021-1044) is now VACUOUS. It does `monkeypatch.setattr(mm, "_point_in_triangle", counting_pit)` and asserts `calls["n"] == 0` to guard mutant ME ("charge the m-3 scan units AFTER the inner scan"). After the split, `mm._triangulate` IS `massing_triangulation._triangulate`, whose inner containment scan resolves `_point_in_triangle` from the massing_triangulation namespace, NOT the facade. I measured it directly: patching `mm._point_in_triangle` → 0 calls during a full concave triangulation; patching `massing_triangulation._point_in_triangle` → 9. So the counter wrapper is dead, `calls["n"] == 0` holds regardless of charge order, and mutant ME is now caught by NO test (the budget=1 triangulation test overspends at the first `charge(1)`, so it never discriminates the m-3 charge order).

Scoping (important): production behaviour is CORRECT and byte-identical — the code still charges before the scan (massing_triangulation.py:228 precedes the scan at :229; equivalence proven). This is LOST TEST POWER, not a behaviour bug. But the producer report (lines 56-63) affirmatively claims every monkeypatch-on-facade "takes effect" — true for `_triangulate`/`_build_prism`/`MAX_*`/`Polygon` (facade code calls those; I confirmed `_triangulate`/`_build_prism` bite, 1 call each), FALSE for `_point_in_triangle`. G4 is the test-adequacy gate and my mandate was to confirm the monkeypatch tests still bite; this one does not.

REQUIRED CORRECTION (small, owned scope): add a charge-order discriminating test to `tests/scenario/test_massing_triangulation.py` that patches `massing_triangulation._point_in_triangle` (the consuming namespace) and asserts the worst-case m-3 units are charged before the inner scan — restoring the mutant-ME guard without touching the byte-identical accepted file.

ADVISORY (non-blocking): (1) OPEN-Q1 — a huge-int `floor_to_floor_ft` overflow is still labelled `proposed_massing.outline`; unchanged from claim seam, fails closed as non_finite; fine as a scoped discovery. (2) `triangulate_polygon` intentionally not re-exported from the facade — fine.

Everything else (AS-1/AS-2/AS-3/AS-4/AS-5) is met and independently verified. (Note: agent-memory write was sandbox-blocked, so I did not persist the lesson.)

M5-T112 G4 VERDICT: FAIL

END-OF-REPORT
