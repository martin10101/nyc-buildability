<!-- Reviewer return preserved VERBATIM by the orchestrator (report-preservation rule,
.claude/rules/project-control.md). Reviewer: independent code-reviewer subagent (G1),
returned 2026-09-12, reviewed HEAD 032dffe7. -->

All checks complete. Here is my gate report.

---

VERDICT: PASS

# G1 Code-Review Gate Report — M4-T009 (R1–R12 residential FAR draft rule family)

- **Reviewed HEAD:** `032dffe7da8fbf1ef9af35bfda55cef184275b49` (branch `candidate/D-024-mrl-option-b`)
- **Gate:** G1 (code-quality dimension), first full wave for this task
- **Reviewer:** code-reviewer (independent; did not author). Read-only discipline observed — no writes, no `project_control.py`, no write-git; all probes were offline `PYTHONDONTWRITEBYTECODE=1 PYTHONPATH=.` in-process evaluations against committed files.
- **Producer commits reviewed:** `ae117874` (family, 11 files) + `197156b9` (E501 wraps) + `317df044` (AS-5 unit). Disclosed orchestrator commits: base-repair `3c46f923` (snapshot/bundle sync only), integration merge `3433c3bb`, record commits.
- **Result:** No findings at any severity. Every acceptance clause AS-1…AS-9 independently reproduced or verified against source.

## What I verified myself (executed, not taken on record)

The sandbox is Python 3.11.9. Contrary to the producer's collection note, `PYTHONPATH=.` in-process import of `app.rules` **works** here (pytest's own `app` import failure is a rootdir path issue, not an engine incompatibility), so I executed the engine directly.

- **AS-1 (source fidelity):** Re-derived every FAR value from the two committed snapshots and compared to the rule parameters — **95 value comparisons** (45 standard via live evaluation + 50 qualifying/wide-street/footnote-2 via parameter maps). **Zero mismatches.** Every district returns its snapshot governing (conservative for conditionals) standard value.
- **AS-2 (coverage):** Enumerated distinct districts: zr-23-21 = **18**, zr-23-22 = **27**, total **45**. All covered, applicability sets pairwise disjoint, empty documented-exclusion list. Counts asserted in-test (18/27/45). The subtle split is correct: R6A/R6-1/R7B/R7A/R8A/R8X are flat (single row) and live in `r6_r12`; the bare R6/R7-1/R7-2/R8 (which appear twice) live in the conditional rule.
- **AS-3 (conservative):** Live — R6→2.20, R7-1/R7-2→3.44, R8→6.02; higher values (3.00/4.00/7.20) never returned; `wide_street_far_alternative` surfaced as `conditional_alternative`; coverage `conditional`. Computation references only `standard_far_by_district` (the conservative map).
- **AS-4 (footnote scope):** `single_dwelling_unit_equivalent_far_cap` present on `r1-r2-r3` only; absent from `r2x-r4` and `r5` (confirmed live and in source). Snapshot confirms the superscript is on the first row's standard value only.
- **AS-5 (digest binding):** Verified three-way agreement: recorded digest == on-disk snapshot stored digest == `sha256(verbatim_excerpt)` for both snapshots (`b52771e6…` / `943b65f9…`). Reproduced the fail-closed loader (`dsl._check_refs`, from M4-T010): a mismatched recorded digest raises `DSLError` carrying the exact phrase the drift test's `match=` regex checks (`"records content_digest_sha256 … but the snapshot on disk stores"`) and naming `zr-23-22`. Confirmed absence of the field is still legal (additive/optional, backward-compatible). `export()` raises `ProvenanceError` when provenance is stripped.
- **AS-5 strengthening (reliability §3.5):** The rewritten `test_as5_silent_recapture_drift_fails_closed_at_load` is a **genuine strengthening, not weakening**. `registry.load()` → `load_rule_file` → `build_rule_definition` → `_check_refs` runs per rule, so a drifted (internally-consistent) store now makes the whole registry **refuse at load**. The old "detectable post-hoc by comparing resolved digest" behavior is genuinely **unreachable** because the registry can no longer load over drifted content — I confirmed `build_rule_definition` raises before any post-hoc comparison is possible. The retained `drifted != committed` assertion plus the mismatch-specific `match=` (which a schema-shape rejection cannot satisfy) guard against passing for the wrong reason.
- **AS-6 (second column surfaced):** Live across **all 45 districts** — qualifying alternative surfaced by exception ID (`conditional_alternative`), the formatted value present in the exception description, `max_residential_far` stays the standard/conservative value, higher value never applied. The R8 discriminator is real: `qualifying` (7.20) and `wide_street` (7.20) coincide, so by-id selection (not text scan) is required; the metamorphic negatives are load-bearing as written.
- **AS-7 (status/honesty):** All 5 rules `needs_review`, `independent_review=pending`, `qualified_human_approval=pending`, `effective_from=2024-12-05`, exactly one citation each. Before-effective (`as_of=2020-01-01`) → `not_applicable`, no output. No rule published/verified/approved.
- **AS-8 (no engine change):** Producer commits touch **no** `app/rules/*.py` (only `rulesets/*.rule.json`, `tests/rules/*.py`, and the report). Family uses only existing primitives (`in_set`, `param_select`/`identity`/`multiply`, `conditional_alternative`/`documented_limitation`). Both packs under the 600-SLOC warn threshold (455 / 560 raw lines), split by concern, no dumping ground; no vacuous asserts found.
- **AS-9 (R5 pilot correction):** Live — R5 no longer carries the cap; `r1-r2-r3` does. R5 FAR values unchanged (1.5/1.5/1.5/2.0) and byte-match the snapshot.
- **Scope:** All three producer commits are inside `allowed_paths`. Base-repair `3c46f923` touched **only** the four snapshot files (docs digest conformance + bundle sync of zr-23-21/zr-23-22) — a legitimate, disclosed orchestrator repair, not a producer scope breach. `dsl.py` was changed by M4-T010 (separate accepted task), never by this packet.
- **Regression safety (critical):** Sibling tests use the DEFAULT registry (`RuleRegistry().load()`), which resolves the packaged bundle. Because rules now record digests enforced at load, I verified the bundle at HEAD is fully synced (bundle `zr-23-21`=`b52771e6…`, `zr-23-22`=`943b65f9…`, matching docs and the rules). The default registry loads cleanly — the producer report's stale-bundle §3 concern is **resolved at the reviewed SHA**. The three sibling adaptations are correct: the AS-9 assertion flip matches corrected behavior, and the `_applicable_trace` selection (asserting exactly one applicable trace) is a necessary de-brittling with value assertions unchanged.

## Taken on record (execution authority = CI + recorded self-checks per DISCIPLINE; not re-run)

- The producer/orchestrator red/green record: `pytest services/api/tests/rules` → **368 passed**; `ruff check` clean; `modularity_check.py --check` exit 0 (377 files, 0 failures, 16 pre-existing unrelated warnings). I did not run pytest/ruff/modularity per the gate discipline; I independently reproduced the behavioral substance in-process, which corroborates the recorded green.
- M4-T010 (accepted, 179th) supplied the schema property + fail-closed loader; I confirmed both are present at HEAD (`rule_definition.schema.json` citations item admits optional `content_digest_sha256` with `^[0-9a-f]{64}$`; `dsl._check_refs` enforces the match).

## Informational notes (no action required; not defects)

1. `forbidden_paths` names `services/api/app/rules/engine.py`, which does not exist — the engine is split across `dsl/evaluator/operations/registry/models/…`. The intent ("no engine change") is nonetheless satisfied: no `app/rules/*.py` is touched by any producer commit. Packet-authoring nit only.
2. The producer report is layered — its body honestly marks AS-5 UNRESOLVED and behavioral claims pending; the out-of-loop **AS-5 completion addendum** closes AS-5 and records the green run. A reader must reach the addendum to see AS-5 closed. Internally consistent and honest.
3. Directive `D-038-R003`: the packet is in-regime; the substantive requirement (author the R1–R12 flat FAR family from the committed snapshots) is delivered. The formal `verification.json` at content identity is the independent `directive-compliance-verifier`'s pass (producer ≠ verifier), separate from this code-quality gate.

## Reproduction (key commands run from `services/api`, `PYTHONPATH=.`, `PYTHONDONTWRITEBYTECODE=1`)

- Digest three-way check: `sha256(verbatim_excerpt)` == stored == recorded for both snapshots → MATCH.
- Family load + full-family value cross-check (45 districts standard + 50 qualifying/wide-street/footnote-2) → zero mismatches.
- AS-3/6/7/9 in-process evaluation → all conform.
- AS-5 fail-closed: tamper recorded digest in-memory → `build_rule_definition` raises `DSLError` with mismatch phrase; strip provenance → `export()` raises `ProvenanceError`; absent field → loads.
- Default registry (`RuleRegistry().load()`) loads with synced bundle; `git show --stat` on `ae117874`/`197156b9`/`317df044` confirms allowed-paths-only; `3c46f923` = snapshots only.

## Verdict

**PASS.** All packet clauses AS-1…AS-9 are satisfied and independently verified; the source transcription is byte-faithful across the entire family; the AS-5 unit closes the digest-binding gap with a genuine test-strengthening (not weakening); scope, modularity, and contract-compatibility are clean; and the change is regression-safe at the reviewed SHA. No MEDIUM+ defects; no unmet clauses.

Relevant paths (all absolute):
- `C:\Users\MLFLL\Downloads\nyc-zoning\ctl24\services\api\app\rules\rulesets\r1_r2_r3_residential_far.rule.json`
- `C:\Users\MLFLL\Downloads\nyc-zoning\ctl24\services\api\app\rules\rulesets\r2x_r4_residential_far.rule.json`
- `C:\Users\MLFLL\Downloads\nyc-zoning\ctl24\services\api\app\rules\rulesets\r5_residential_far.rule.json`
- `C:\Users\MLFLL\Downloads\nyc-zoning\ctl24\services\api\app\rules\rulesets\r6_r12_residential_far.rule.json`
- `C:\Users\MLFLL\Downloads\nyc-zoning\ctl24\services\api\app\rules\rulesets\r6_r7_r8_wide_street_conditional_far.rule.json`
- `C:\Users\MLFLL\Downloads\nyc-zoning\ctl24\services\api\tests\rules\test_r1_r12_residential_far.py`
- `C:\Users\MLFLL\Downloads\nyc-zoning\ctl24\services\api\tests\rules\test_r1_r12_residential_far_provenance.py`
- `C:\Users\MLFLL\Downloads\nyc-zoning\ctl24\services\api\app\rules\dsl.py` (M4-T010 loader; verified, not in scope)
- `C:\Users\MLFLL\Downloads\nyc-zoning\ctl24\docs\research\zr-snapshots\v1\zr-23-21.snapshot.json`
- `C:\Users\MLFLL\Downloads\nyc-zoning\ctl24\docs\research\zr-snapshots\v1\zr-23-22.snapshot.json`
