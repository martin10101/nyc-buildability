# M5-T095 - G2 producer self-check record (recorded by the orchestrator, role self_check)

Producer: backend-engineer (orchestrator-dispatched subagent) in wt-m5t095; one in-worktree commit 3be80a1d (parent a749d995), cherry-picked to a2dcc60f with every file MATCH by git blob identity (3 files: project-control/reports/M5-T095-producer-report.md eeb81b3a, services/api/app/scenario/proposal.py d00b1728, services/api/tests/scenario/test_proposal_validation_budget.py e25d19bb).

- **Scope:** proposal.py (total-positions bound + GEOS-backed condition-3 simplicity; three unused private helpers removed; derivation.py keeps its own copies, untouched), the new budget test (18 tests) and the producer report; no consumer, route, requirements or web change.
- **Producer self-checks (report, OBSERVED):** ruff clean; 18 passed (new budget suite); the documented 7-suite consumer command 312 passed; modularity exit 0 with no warning on proposal.py; mutations: a no-op bound reddens the zero-call guard, the all-pairs reference exceeds the 50,000 pairwise-work budget and the 0.5 s wall guard; measurements old one-ring 1377 ms vs new 10.3 ms, worst request ~597 s -> 53-87 ms.
- **[ORCH-HARVEST] at the cherry-picked head a2dcc60f (Python 3.11 local): `python -m ruff check .` (cwd services/api) -> exit 0: All checks passed!; `python -m pytest tests/scenario/test_proposal_validation_budget.py tests/scenario/test_scenario_proposal.py -q -p no:cacheprovider` (cwd services/api) -> exit 0: 74 passed in 13.49s.**
- **Discoveries routed (producer):** derivation.py:314-363 carries its own copy of the all-pairs simplicity logic (bounded today by re-validated input; adopt the shared check in a derivation packet); DB-034(a) is now addressed for proposal.py but the route-level cancellable job + wall-clock timeout + rate limit (M5-T088 G5 fix b) is still required at the wiring/public-exposure packet.

G2 verdict: PASS (self_check; the independent reviews and the DCV follow - dispatched by the successor session per the handoff).
