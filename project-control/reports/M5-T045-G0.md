# M5-T045 — G0 contract gate (orchestrator)

- **Packet:** condo billing-BBL live wiring — the DB-029-bound packet: (b) source-registry
  draft record for BOTH DTM datasets (p8u6-a6it + eguu-7ie3) mirroring the committed
  ztldb.json shape, landed before any live-traffic path; (c) the REAL captured null-billing
  fixture (condo_key 103343) pinned byte-faithfully with KEY-ABSENCE tolerance asserted
  (SODA omits null columns — the explicit-null synthetic is byte-infaithful and retires
  where covered); a new seam-policy module `app/connectors/condo_base_lot.py` (typed
  fail-closed outcomes: single / multi-lot / unresolved / not-condo-billing, with
  provenance); wiring BEFORE the zoning-lot lookup at both recon-named consumer seams
  (`spatial/live_provider.py` — fail-safe None on multi-lot/unresolved, never fabricate,
  never collapse divergent zoning; profile `zoning_crosscheck` seam — existing
  contract-1.3.0 channels only, NO contract-shape change); the D-073-R006
  records-vs-allowances display increment (screen + printed brief); and the T044-wave
  riders on the connector. Cites D-066:R001 + D-073:R006. Depends on M5-T042 (225th) +
  M5-T044 (227th), both accepted.
- **Pairwise disjointness (D-072-R003):** M5-T045 is the ONLY packet contracted at this
  seam — no other task is claimed or in flight in the M5 lanes (ledger: 227 accepted,
  nothing claimed since the seq-118 turnover; the optional DB-026 second lane is
  deliberately deferred pending its own source recon). Pairwise check against a peer lane:
  vacuously disjoint (no peer). If a second lane is contracted before this packet closes,
  its G0 must record the mechanical intersection against these 17 allowed_paths.
- **Directive binding:** D-066-R001 and D-073-R006 task_ids appended (same-commit digest
  resyncs + audit entries in both manifests); `evaluate_task_refs` ok=True, applicable ==
  cited == [D-066-R001, D-073-R006]. `affected_tasks` deliberately untouched (c14:
  verification-time only).
- **Nav block (D-066-R001):** graph regenerated at this seam (740 files / 15648 nodes /
  6932 edges); embedded in inputs — resolver leaf status, live_provider consumers
  (rule_evaluation route :66 + two test files), zoning_crosscheck's test-only consumers,
  the single live ztldb call site (:110), bbl.py identity primitives, and the web
  PropertyOverview consumer set. Advisory clause included.
- **Placeholders (c17):** five new allowed_paths seeded and tracked — condo_base_lot.py
  (module docstring, ruff clean), test_condo_base_lot.py (empty-py ok), the web
  condo-resolution-display.test.tsx (trivial PASSING test — vitest no-suite trap),
  dtm-condo.json registry-draft placeholder, producer-report placeholder. All other
  allowed_paths already tracked.
- **Evidence files named in-packet:** docs/DISCOVERY_BACKLOG.md (DB-029/DB-030 rows +
  2026-09-19 recon/capture sweep lines), docs/research/condo-base-lot-resolution-sources.md,
  docs/research/condo-null-billing-fixture-capture.md, the committed
  source-registry-drafts/ztldb.json shape example.
- **Scope sanity:** fail-closed preservation written as binding text (non-condo and
  flag-off behavior byte-identical; resolver contract consumed as-is; no new dependency;
  no network in tests; no contract-schema change — unavoidable change = STOP + discovery);
  forbidden_paths pin builder.py, adapter.py, contract schemas, rules/**, and
  packages/contracts. Modularity boundary answers required in the producer plan for the
  new module. HJ reviewer on the roster for the display walkthrough (AS-8).

**G0 verdict: PASS** — packet is contractable and claimable.
