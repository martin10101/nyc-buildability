# M5-T103 — G1 data-contract verification (data-contract-verifier "dc-t103", read-only)

> Transmission history: pinned at 68bc726e, delivered as three SendMessage parts (1/3..3/3) ending with END-OF-REPORT;
> no truncation. Joined verbatim by the orchestrator (transport wrapper tags removed only). The report's "3fab26f6" is a
> transcription slip for the material commit 3fab28f6 (git log). Verdict PASS: every load-bearing ISO 32000-1 rule verified;
> all six corpus files fetched in memory, sha256 MATCH, the producer's results reproduced exactly. Advisories F1-F4 are
> carried into the M5-T103 rework inventory.

---

M5-T103 G1 verification (data-contract-verifier, read-only) — PART 1/3. Verdict is PASS; spec table + first-hand corpus in parts 2-3.

PIN: HEAD 68bc726e (candidate/D-024-mrl-option-b) at start; material commit 3fab26f6 in history. Repo ctl24.

METHOD: read pdf_object_streams.py + sheet_objects/sheet_reader/sheet_interpreter + the strict reader (pdf_xref/pdf_objects) it composes; verified each constant against ISO 32000-1, web-corroborated on the load-bearing rules (the ~13MB spec PDF exceeds WebFetch); ran the 3 sheet suites via the 3.11 shim (84 passed); fetched all 6 corpus files IN MEMORY (no file written — shell writes are blocked for me), sha256-checked, ran read_sheet twice each.

VERIFICATION TABLE (claim / spec / result):
1. Cross-ref stream /Type /XRef required — §7.5.8.2 — PASS.
2. /Size present, direct non-neg int — §7.5.8.2 — PASS.
3. /W = 3 ints, 2nd element ≥1, zero-width type field → default 1, zero-width field3 → 0 — §7.5.8.2 Table 17 — PASS. Spec text confirmed: "zero shall not be used for the second element" (code refuses out[1]<1) and "if the first element is zero…default to Type 1" (code sets field1=1 when w0=0).
4. /Index default [0 Size]; even-length pairs; subsection counts must equal decoded rows — §7.5.8.2 Table 17 — PASS (exact default; count cross-check present).
5. Entry types 0=free / 1=byte-offset+generation / 2=objstm-number+index — §7.5.8.3 — PASS (type 0 skipped; a newer free entry deletes an older in-use one via the first-seen `seen` set).
6. /Prev chain followed newest-first, cycle guard (visited set) + depth bound — §7.5.8.4 — PASS.
7. Trailer keys carried on the stream dict: /Root indirect required, /Encrypt refused — §7.5.8.2 — PASS.
(continued in 2/3)

---

M5-T103 G1 — PART 2/3 (spec table cont. + test run).

8. Object stream /Type /ObjStm, /N, /First; header = N (objnum, offset-relative-to-First) pairs; object parsed at First+offset; generation forced 0 — §7.5.7 — PASS. Also present: header identity check (slot index must hold the requested object number), header-overruns-/First guard, and offset-range guard.
9. Objstm prohibited objects (streams / gen≠0 / Encrypt / Length) — §7.5.7 — MOSTLY PASS: generation forced 0 ✓, /Encrypt refused at the trailer ✓; the "no stream object inside an objstm" prohibition is NOT explicitly enforced (advisory F2, non-blocking).
10. Hybrid /XRefStm: classic table marks compressed objects free, the stream completes it, stream takes precedence — §7.5.8.4 — PASS (stream entries win; classic type-1 entries fill the gaps).
11. Predictors: 1/absent=none, 2=TIFF honest refusal, 10-15=PNG per-row tag; /Columns=1, /Colors=1, /BitsPerComponent=8 defaults; row_len=ceil(cols*colors*bpc/8); bpp=max(1,ceil(colors*bpc/8)) — §7.4.4.4 Tables 8-10 — PASS.
12. PNG None/Sub/Up/Average/Paeth incl. Paeth tie-break (a, then b) — §7.4.4.4 / RFC 2083 — PASS, byte-exact; the "Up" filter (Predictor 12) that xref streams use is correct.
13. TIFF predictor 2 — implemented-or-honestly-refused — PASS (typed refusal).
14. Mandatory guard: bounded INCREMENTAL inflate (never an unbounded zlib.decompress), absolute inflated-bytes cap + inflate-ratio checked BEFORE each chunk is retained, shared document decoded-bytes budget, plus /Prev-depth + objstm-count + /N bounds — PASS; 6 reddening in-process mutation tests + a zip-bomb refusing with tracemalloc peak <5MB.
15. Integration: XREF_STREAM_FEATURES == the strict reader's three refusal strings EXACTLY ("cross-reference stream" / "hybrid-reference file" / "incremental update chain", pdf_xref.py:143/185/191) so the fallback fires only for those; a classic file never calls the resolver (proven by the explode-patch test); the shared reader is not widened; the 62-case split-equivalence golden stays byte-identical — PASS.

TESTS (shim, local 3.11): 84 passed = existing sheet_reader suite + the 62-case split-equivalence golden + 29 new resolver/guard/mutation tests. AS-4 no-regression holds.
(continued in 3/3)

---

M5-T103 G1 — PART 3/3 (corpus, deviation, findings, verdict).

CORPUS (AS-5) — all 6 fetched IN MEMORY, sha256 MATCH the note, read_sheet run twice (run1==run2 each), reproduces the producer report EXACTLY:
- item1/2 (VA HVAC, 6.49/6.80 MB): REFUSAL origin=sheet_profile feature='decode parameters' (content /DecodeParms).
- item3 (VA, /Prev + multi-/Index): REFUSAL 'malformed pdf', found '<<' (content marked-content dict).
- item4/5/6 (HABS/HAER scanned): REFUSAL 'malformed pdf', found '<<' (content).
NONE returns 'cross-reference stream' (the old M5-T093 baseline) — the xref/object-stream blocker is removed on all 6; each now advances to a correctly-typed CONTENT-level limit. NONE produces linework (no SheetDocument), so the report's honesty caveat ("no real architect PDF fully reads yet") is confirmed — no over-claim. /Prev + multi-/Index are exercised on REAL files (items 3-4), not only synthetically.

DEVIATION (/Extends ignored) — SAFE: random-access resolution uses each type-2 entry's direct (objstm, index) pair, verified against that objstm's own header (header_number == number); /Extends only records collection numbering-order and never changes which objstm holds an object. Correct to ignore.

FINDINGS — no BLOCKING. Advisory only:
- F1 (cosmetic): _MAX_FIELD_WIDTH=8 cites "§7.5.8.2 max bytes" but the spec sets no 8-byte /W cap; the bound is safe and fail-closed.
- F2 (spec completeness): a stream object embedded in an object stream is materialized as a PdfStream, not refused (§7.5.7 prohibits it); bounded within the decoded buffer + downstream refuses; low impact.
- F3 (defense-in-depth): MAX_PDF_OBJECTS is enforced only at materialize; the merged entry map can transiently reach ~MAX_PREV_DEPTH×MAX_XREF_ENTRIES entries before the cap; bounded by the 134MB inflate budget — security-reviewer's lane.
- F4 (scope): classic-table /Prev chains stay refused; a hybrid that also carries a classic /Prev follows only the /XRefStm stream chain — a uncompressed object unique to an older classic revision would surface as a typed "unresolvable reference", never wrong data. Within packet scope.

All load-bearing spec behavior is correct; every refusal is a typed VALUE; no guessing; fail-closed throughout.

M5-T103 G1 VERDICT: PASS

END-OF-REPORT
