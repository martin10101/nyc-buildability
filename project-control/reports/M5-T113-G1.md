# M5-T113 — G1 format-authority review (data-contract-verifier "g1-t113", read-only)

> Transmission history: pinned at 7b3686f0, delivered as 3 SendMessage part(s) ending with END-OF-REPORT;
> no truncation. Joined verbatim by the orchestrator (transport wrapper tags removed only). Verdict PASS; ISO 32000-1
> citations web-verified; five ADVISORY items (EMC balance untracked, a corpus-prose nit, the corpus-note citation, a section-8.2 cite, a form-stream fixture).

---

M5-T113 G1 data-contract/format review — PART 1/3 (PIN 7b3686f0; verdict PASS, in part 3). Read-only; ISO 32000-1 citations web-verified.

SCOPE/PROVENANCE: Material fcd0033e = exactly 7 files, all within allowed_paths; the forbidden read-only test_pdf_object_streams.py is UNTOUCHED (confirmed by the commit name-only diff). apply_predictor + the PNG unfilter are REUSED unchanged from M5-T103 (whose G1 already verified them byte-exact vs §7.4.4.4/RFC 2083, incl. the Paeth a-then-b tie-break) — so the predictor MATH is independently confirmed; this task only adds the content/form-stream PATH to it.

PREDICTORS (§7.4.4.4, Table 8 — verified: /Predictor default 1, /Colors 1, /BitsPerComponent 8, /Columns 1; BPC ∈ {1,2,4,8,16}):
- _content_decode_parms (sheet_objects.py:193) resolves /DecodeParms correctly: single-element array unwrapped (mirrors the object-stream resolver), multi-element or non-dict → typed refusal; /DecodeParms without /FlateDecode → refuse (never a raw read; test_as1_decode_parms_without_filter_refuses).
- _decode_stream (:136) charges the INFLATED bytes against the document budget BEFORE apply_predictor runs, and apply_predictor bounds row_len against absolute_cap (:418) and early-returns on empty data (:408) — the M5-T103 G5 "geometry bounded before any buffer is sized" guard is preserved. Proven by test_as1_predictor_stream_shares_the_document_decoded_budget.
- TIFF(2), out-of-range (<10/>15), non-int, and boolean /Predictor all → typed "predictor" refusal (_parm_int uses `type is int`, correctly excluding bool).
- CONTENT and FORM streams share the identical _decode_stream→apply_predictor path (decode_form→decode_stream→_decode_stream), so forms get predictor support too.

(continued 2/3)

---

M5-T113 G1 — PART 2/3: marked content + scan-only (both PASS).

MARKED CONTENT (§14.6 Table 352; property list = a dict, inline OR a /Properties name — web-confirmed):
- read_inline_dict (sheet_marked_content.py:77) consumes a balanced << >> operand, depth- and byte-bounded (16 / 65536), returning a DISCARDED sentinel — it never interprets the contents as drawing. Correct: a marked-content sequence paints nothing.
- ONE primitive lexer: << >> [ ] are handled structurally; every scalar goes to the strict lex_primitive, which REFUSES << by design (pdf_lexer.py:137-140, confirmed) — so the "exactly one primitive lexer" claim holds. A single '<' hex string is left to lex_primitive (test_as2_hex_string_operand_still_lexes).
- BDC/DP take tag + (name | inline dict); BMC/EMC/MP handled via _IGNORED; unbalanced / too-deep / too-long / wrong-position (dangling operands) are all typed refusals. Inline images BI/ID/EI (§8.9.7) stay refused (DB-055 d) — a stream cannot appear inline, and an indirect ref inside a content-stream dict would refuse, both correct.
- Caps are reasonable: real MCID/OC property lists are tiny; the decoded content stream is itself pre-bounded to 8 MB, so the lexer's scan is bounded even before the 64 KB cap bites.

SCAN-ONLY (DB-076 c,e):
- _scan_only_refusal (sheet_reader.py:363) fires only when used_resolver AND zero polylines across ALL fully-parsed pages — i.e. no vector linework (a page refusal propagates first, so the label only lands on a fully-parsed, zero-linework document). Reachable now that predictors + inline dicts let a real scan parse to completion. Load-bearing (mutation → empty-page "success"; test_as3_scan_only_gate_is_load_bearing).
- Both DB-076(e) inconsistencies are HONESTLY disclosed AND I verified the rationale: test_pdf_object_streams.py::test_scan_only_page_is_typed_refusal (:443) genuinely pins feature=="scan only" for a text-only resolver page, so relabeling would break a must-pass-unchanged read-only test — the producer correctly enriches the detail ("...text-only page set...") instead. The image-only classic-succeeds/resolver-refuses asymmetry is real, disclosed, and immaterial (all six corpus files take the resolver path).

(continued 3/3)

---

M5-T113 G1 — PART 3/3: citations, real-corpus honesty, advisories, VERDICT.

CITATIONS — all verified against ISO 32000-1 (web-corroborated): §7.4.4.4 predictors + Table 8 defaults ✓; §14.6 marked content (Table 352) ✓; §8.9.7 inline images ✓; §7.2.3 Comments ✓; §7.3.7 Dictionary Objects ✓; §8.2 Graphics Objects ✓. Every [recalled-verify] marker in THIS task's files (§7.4.4.4 / RFC 2083 on the unfilter + Paeth) is CORRECT.

GOLDEN REVISION — deliberate and documented: the single refuse_decode_parms case → decode_parms_png_predictor (now draws; digest equals the plain `line` case) + refuse_decode_parms_tiff (still refuses); the other 61 per-case digests UNCHANGED; _OVERALL updated (767766eb→887a0a68); 63-case corpus. Matches AS-1.

REAL CORPUS — honest: "0 of 6 read fully" is accurate; the six per-file outcomes are PLAUSIBLE and consistent with the independently-verified M5-T103 G1 baseline and the M5-T093 byte-scan facts (all six PDF 1.5+/resolver; items 1-2 = 88/94pp vector; item 3 has /Shading+sh; item 4 has an inline image; items 5-6 = 1pp XObject scans → scan-only). (a) unblocks 1-2 past decode; (b) unblocks 3-6 past '<<'; items 5-6 reach scan-only = DB-076(c) on real scans.

ADVISORY (non-blocking):
- A1: BMC/BDC…EMC SEQUENCE balance is not tracked (all ignored). Immaterial to geometry/safety; the inline << >> balance IS enforced. Lenient by design.
- A2: the report DISCOVERIES prose says inline images in "items 2, 4" are "now REACHED", but the binding corpus TABLE shows item 2 stops at the 200k operator budget BEFORE its inline image — only item 4 actually reaches BI. The table is honest; the prose is loose. Worth a note when recording the discovery.
- A3: the packet cites the trial note for the six sha256s, but the full 64-hex digests live in architect-drawing-corpus-2026-09.md §2; the producer verified against that (correct, more precise). Both files exist.
- A4 (cosmetic): §8.2 is a categorization cite for "content-stream operators"; the exhaustive list is Annex A/Table A.1 and the marked-content ops are §14.6/Table 352 (cited). No correction needed.
- A5 (QA lane): explicit predictor tests cover CONTENT streams; FORM streams share the identical decode path but lack a dedicated fixture.

No BLOCKING findings. Format authority, safety bounds, provenance, and disclosure are all sound.

M5-T113 G1 VERDICT: PASS

END-OF-REPORT
