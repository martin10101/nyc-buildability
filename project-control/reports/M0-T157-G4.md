# M0-T157 — G4 QA Gate Report (verbatim)

Orchestrator preservation note: report returned by the independent qa-engineer agent
("G4 QA review M0-T157") on 2026-09-12 and preserved verbatim below (transport
entity-decoding only). Reviewer was read-only. Verdict: PASS (advisories 1–3 non-blocking).

---

I have everything needed. Returning the G4 gate report.

---

# G4 QA Gate Report — M0-T157 (resolver-test fixture repair)

**Reviewer:** qa-engineer (independent, read-only)
**Task:** M0-T157 — replace hardcoded `D-042` in `test_s12_wrong_directive_reference_fails_closed` with a registry-derived nonexistent id
**Frozen material:** commit `14bdcc73` (test-file blob `265f93e2`)
**Branch:** candidate/D-024-mrl-option-b · branch submit HEAD `b405d350`

## Material identity / stability

- Test-file blob is **byte-identical** at `14bdcc73` and at branch HEAD `b405d350` (both `265f93e20a3eae7705c76a0adde51659d272310b`). No commit in `14bdcc73..b405d350` touched `tools/test_directive_compliance.py` or the producer report.
- The only later commit touching `project-control/tasks/M0-T157.json` is `b405d350` itself ("M0-T157 submitted: awaiting_gate") — a control-plane status transition, expected and not part of the reviewed material.
- The shared/primary checkout (ctl24) working file hashes to `265f93e2` = the frozen blob; all tests were run there against the real registry. (Note: my dispatched worktree was parked at an older ancestor commit `d8b3899f` whose working copy predates the fix — that working-tree state is not the review material and was excluded.)
- Commit `14bdcc73` changes exactly the one test function (test file +9/−1) plus control-plane artifacts (G0 gate/report, producer report, state.json, task file). No `services/**`, `apps/**`, `packages/**`, or other `tools/**` production/test source changed.

## S1 — derived-id fails closed against the current registry — **PASS**

- Real registry now contains **40 directives incl. the real `D-042`** (max numeric id = 42), confirming the original defect's trigger (the owner's D-042 capture made the hardcoded `D-042` real).
- Derivation `max(numbers)+500` → **`D-542`**, `assertNotIn` confirms it is absent from the live registry.
- `evaluate_task_refs` returns `ok=False` with a `"does not exist"` invalid_ref; the test passes against the current registry. No hardcoded future directive id remains in the function.
- Diff inspection: **no assertion weakened.** `assertFalse(ev["ok"])` and `assertTrue(any("does not exist"...))` are unchanged; a new `assertNotIn` guard was **added** (strengthening).

## S2 — regression: local + CI green, nothing else changed — **PASS**

- `python tools/test_directive_compliance.py ResolverTests -v` → **Ran 5 tests, OK** (reproduced).
- `python -m ruff check tools/test_directive_compliance.py` → **All checks passed** (reproduced).
- Control-plane CI corroborated at the fix head: **run 34726577993, conclusion `success`, headSha `18ee6b79`.** All 18 jobs succeeded, including `control-plane (workflow regression test, ADR-005)`. `14bdcc73` is an ancestor of `18ee6b79`, and the test blob at `18ee6b79` = `265f93e2` (fix present in the CI run).
- `.github/workflows/ci.yml:444` runs `python3 tools/test_directive_compliance.py` (whole file, no test selection) inside the `control-plane` job — so CI-green genuinely exercises the repaired `test_s12`.

## Mutation / tautology verdict — **NOT tautological (PASS)**

Verified against the real registry (scratch harness run outside the repo, read-only):
- **Real resolver:** test passes (baseline).
- **Mutation 1** — resolver returns `ok=True` for the derived id (i.e., stops reporting nonexistent directives): test **correctly FAILS** at `assertFalse(ev["ok"])`. The fix is not tautological.
- **Mutation 2** — resolver fails but with a different reason (no `"does not exist"`): test **correctly FAILS** at the invalid_ref check. The test requires the specific fail-closed reason, not merely any failure.
- **Collision guard reasoning (verified from code):** `max+500` is by construction strictly greater than every existing numeric id, so the derived id is **provably absent** from the current registry — `assertNotIn` can never fire on a real capture. If the derivation formula ever broke and produced a present id, `assertNotIn` **fails loudly** before the resolver call (demonstrated: forcing a present id raises AssertionError) — it can never silently green-pass. The derivation is also self-adjusting: if the registry ever grew to include the derived number, `max` would rise and the derived id would move above it.

## Collision-class sweep — 2 advisory latent instances (non-blocking)

The defect class = a hardcoded directive id **assumed absent** against the real registry (or a `Fixture` copy of it) that a future owner capture could make real. Synthetic from-scratch tmp-registry ids (`D-700`/`D-701` in `_make_*` helpers, `D-900-R001` defaults in `_req_row`/`_ver_row`) are out of scope per rubric and were excluded.

- **ADVISORY-1 — `D-900` in `MultipleDirectivesTest._add_second_directive` (line 349).** A hardcoded `D-900` directory is injected into a `Fixture` (a copy of the *real* registry) and asserted active. This is the **same collision class**: if the owner ever captures a real `D-900`, `d2dir.mkdir()` raises `FileExistsError` and the run goes red. Severity **low/advisory**: 900 is far above the current max (42), it fails **loudly** (not the silent-until-red flip the original `D-042` caused), and closing it is outside this task's one-function scope.
- **ADVISORY-2 — `D-001-R999` in `test_c14_invented_id_digest_mismatch` (line 124).** Renumbering `requirements[0]` to `D-001-R999` assumes R999 is not a real D-001 requirement. Severity **negligible**: D-001 amendments are append-only and reaching R999 (currently ~154 reqs) is remote; even then the deletion/locked-id check would still likely trip.
- **Minor note (advisory):** the task objective states the derivation "closes the whole hardcoded-future-id collision class." That is slightly overstated — `D-900`/`D-001-R999` remain — but the delivered scope (the `test_s12` function + the `max+500` pattern) is correct. Recommend a follow-up task to apply the same derivation to `D-900` if desired; not required for this gate.

Non-collision-class references confirmed safe: `D-001`/`D-001-Rxxx` presence checks (append-only/locked, stable by policy); `D-004-R322..R501` are `assertNotIn` against *source-code text* (not registry state).

## Modularity

Change is a +8-line edit to one function in an existing test file; no new responsibility mixing or module growth. The `modularity` CI job passed at the fix head.

## Findings summary

| # | Finding | Severity | Blocking |
|---|---|---|---|
| 1 | `D-900` in MultipleDirectivesTest is the same collision class (fails loud, far-future) | Low | No |
| 2 | `D-001-R999` in test_c14 assumes R999 never becomes real | Negligible | No |
| 3 | Objective's "closes the whole class" claim slightly overstated | Info | No |

No blocking defects. S1 and S2 fully satisfied; mutation/tautology proof clean; CI SUCCESS corroborated at the fix head via stored evidence and gh.

## VERDICT: **PASS**

(Two advisory follow-ups noted above; neither blocks acceptance of M0-T157.)

---

**Files relevant to this review (absolute paths):**
- Material under review: `C:\Users\MLFLL\Downloads\nyc-zoning\ctl24\tools\test_directive_compliance.py` (blob 265f93e2 at 14bdcc73/b405d350)
- Task packet: `C:\Users\MLFLL\Downloads\nyc-zoning\ctl24\project-control\tasks\M0-T157.json`
- CI evidence: `C:\Users\MLFLL\Downloads\nyc-zoning\ctl24\project-control\reports\M0-T157-ci-evidence.txt`
- CI workflow step: `C:\Users\MLFLL\Downloads\nyc-zoning\ctl24\.github\workflows\ci.yml:444` (control-plane job)
- Mutation harness (scratch, outside repo): `C:\Users\MLFLL\AppData\Local\Temp\claude\C--Users-MLFLL-Downloads-nyc-zoning-ctl24\3afbe240-cc9c-47d1-9a18-63cd1d2ff5dc\scratchpad\mutation_check_m0t157.py`

**Requested status:** PASS — return to orchestrator to record the G4 gate. I did not run `project_control.py`, git write ops, or `gh` write ops (read-only `gh run view` only).
