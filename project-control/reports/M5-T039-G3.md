# M5-T039 — G3 code review return (VERBATIM; code-reviewer, read-only, pinned 1564e509)

Orchestrator note: returned via the agent-return channel 2026-09-18T09:16:00Z; the transport
truncated the first delivery inside section 4. Part 1 below is the truncated delivery
verbatim; Part 2 is the reviewer's resent remainder, verbatim. VERDICT: PASS (5 non-blocking
advisories).

---

## Part 1 (first delivery, verbatim)

Review complete. All six judging areas verified, plus the three binding orchestrator rulings and the identity check. Returning the G3 gate report.

---

# G3 Code Review — M5-T039 (DB-010 named-street override matcher + phase-0 snapshot repair)

**VERDICT: PASS** (with 5 non-blocking advisories, none blocking acceptance)

Reviewer: read-only. All conclusions verified in source; four documented commands reproduced green.

## Identity check (as instructed)
- Pinned material `1564e509` carries exactly the 5 contract files (`git show --name-only`): `named_street_override.py`, `test_named_street_override.py`, both `zr-12-10.snapshot.json` copies, `M5-T039-producer-report.md`. Commit stat +1609/-36.
- Live HEAD `44649797` is submit side-effects only; `git diff 1564e509..HEAD -- services/ docs/research/` is **empty**. Material identity is stable — code/snapshot review at `1564e509` == HEAD.
- Evidence map `material_commit` = `1564e5094c8...` (the material commit, correctly, not a seam).

## 1. Tri-state fail-closed logic + construction guard — SOUND
- Tri-state enum `named_street_override.py:59-64`. `match()` fails closed to INDETERMINATE on missing/unparseable borough/street/CD (`:368-373`), missing bounds (`:399-405`), degenerate segment (`:406-411`); returns NOT_MATCHED for unknown street (`:376-380`), wrong borough/CD (`:387-394`), disjoint bounds (`:419-426`); a segment sharing exactly one designated boundary → INDETERMINATE with G6-Q3 (`:417-418`, `:492-506`).
- **MATCHED_OVERRIDE is reachable only** when `disposition_when_located == "matched_override"` (`:473-483`). The two real rows carry `"indeterminate"` (`snapshot.json:54`), so both return INDETERMINATE with full provenance + G6-Q1 — confirmed by `test_as1...:304-316`.
- **Query matching is EXACT normalized equality** (`:375`, `:385`, `:413-415`) — no fuzzy match, no geometry, no abbreviation expansion (`test_normalization_no_abbreviation...:421-426`). No divergent snapshot row can produce a false caller-visible match: `_validate_against_source` (`:269-317`) rejects unrecognized dispositions (`:285-290`), unanchored row fields (`:301-308`), and alt districts absent from their own quote (`:311-317`). Rejection paths proven: divergent borough (`test:181-224`), absent alt district (`test:227-254`), bad disposition (`test:257-284`).

## 2. Determinism / typing / exception discipline — SOUND
Pure functions over frozen dataclasses and a tuple of rows; `open_legal_questions` derived from deterministic JSON order; `test_determinism...:494-499`. Construction errors are typed (`NamedStreetOverrideError`, `:54-56`); malformed **query** input never raises (returns INDETERMINATE); `_normalize_cd` returns `None` rather than raising and rejects `bool` (`:162`). `from __future__ import annotations` + full hints throughout; no silent coercion.

## 3. Snapshot repair quality — VERIFIED (independently reproduced)
- **Digest:** recomputed `sha256(verbatim_excerpt)` == stored `23a9ccad31f1...` (independent hashlib recompute). This is exactly what `load_snapshot_file` validates (`snapshots.py:119-139`).
- **Both copies byte-identical** (10434 bytes each; `read_bytes()` equal).
- **Transcription byte-exact:** all three excerpt paragraphs are char-for-char equal to the sanctioned M4-T018 §1.3 (street, wide) and §1.2 (street, narrow) quotes — programmatic paragraph compare returned True/True/True against the source record.
- Both structured-block `verbatim_source_quote`s are substrings of the excerpt (digest-covered).
- `section_last_amended` corrected `2024-12-05 → 2026-03-26` (`snapshot.json:17`), matching M4-T018 §2.3 (`/node/21683`, 3/26/2026).
- `provenance_chain` honest (`snapshot.json:23-38`): `transcribed_from` M4-T018 report, `content_sha256 4a75e22f...`, `byte_count 1316658`, `http_status 200`, print/PDF 504 fallback disclosed — all consistent with the source record. `raw_html_verified: false`, `extraction_status: extracted_draft` correctly preserved.

## 4. Modularity — CLEAN
`modularit[TRANSPORT TRUNCATION POINT]

---

## Part 2 (resent remainder, verbatim; delivered ~09:19Z — report COMPLETE)

G3 M5-T039 — remainder (sections 4-6, advisories, closing). VERDICT: PASS.

## 4. Modularity — CLEAN
modularity_check --check → failures 0; module 512/513 SLOC, single responsibility, not in warn list. Leaf confirmed: the ONLY import of app.rules.named_street_override is the test (test_named_street_override.py:29). The many named_street_override_* grep hits in wide_street_buffer_engine.py / wide_street_wiring.py / wide_street_live_provider.py / integration.py are pre-existing precondition-flag names (named_street_override_checked / _pending / NamedStreetOverrideStatus), NOT imports of this module — M5-T037 lane disjointness preserved. No consumer touched.

## 5. Test quality — STRONG (40 passed, 0.20s)
Anti-tautology real: digest recomputed with hashlib not restated (test:77-85); source-tracing uses independent _norm not the module helper (test:56-61,136-178); real snapshot via production loader (test:63-65,612-614). MATCHED_OVERRIDE proven via a synthetic unconditional source-shaped row (test:523-567) — only way to reach it since every real row is indeterminate. Boundary inclusive/exclusive/disjoint, wrong borough/CD, unknown street, malformed inputs, normalization (incl. abbreviation limit), alt-width applicable/non-applicable, exact provenance, determinism all covered.

## 6. D-045-R009 — PRESERVED
DRAFT docstring (:34-35), "implements the capture; not legal advice"; no verified/published/approved/compliant legal claims. C-districts → professional_review_required. Three G6 questions recorded as conservative refusals only (report:288-293), deciding none.

## Binding rulings — verified within
(1) Phase-0 byte-exact transcription from M4-T018 (channel sha 4a75e22f) — fidelity CONFIRMED (all 3 excerpt paragraphs char-for-char equal to §1.3/§1.2). (2) G6-Q1→INDETERMINATE, G6-Q2→professional_review_required, G6-Q3→INDETERMINATE, none decided. CONFIRMED. (3) Leaf, zero consumers, wiring deferred. CONFIRMED.

## Advisories (all NON-BLOCKING)
A1 — _anchored_in (:180-201) is substring-based; as a construction tamper guard it is permissive (a field that is a substring of a source token could false-anchor, e.g. "Broad"/"way" → "broadway"). Cannot produce a wrong caller-visible result: query matching is exact equality and the real rows' disposition is "indeterminate". Consider word-boundary anchoring later.
A2 — result builders read block["provision_id"]/["section_anchor"]/["disposition_reason"] with no default (:340-344,404,471,480,488,503) but _validate_against_source doesn't assert their presence; a future snapshot missing one passes construction then KeyErrors on first locating match() instead of failing closed. Current snapshot has all fields; tests green. Consider validating at construction.
A3 — _section_provenance hardcodes section_anchor "ZR 12-10, definition 'street, wide'" (:327) rather than deriving; accurate here. snapshot_sha256/section_number stay snapshot-derived, so provenance integrity holds.
A4 — classify_alternate_width hardcodes coverage_class="professional_review_required" (:437) not block["disposition"]; consistent with the fixed-refusal contract. Trivial.
A5 — report/commit say "513 lines"; file is 513 content lines / 512 newlines. Immaterial.

## Reproduced (read-only)
ruff module+tests → All checks passed!; pytest → 40 passed; modularity --check → failures 0. Digest/byte-identity verified directly in Python (did NOT run write-producing sync_zr_snapshots.py): both copies 10434 bytes identical; sha256(verbatim_excerpt) == stored 23a9ccad31f1...

## Paths
C:\Users\MLFLL\Downloads\nyc-zoning\ctl24\services\api\app\rules\named_street_override.py
C:\Users\MLFLL\Downloads\nyc-zoning\ctl24\services\api\tests\rules\test_named_street_override.py
C:\Users\MLFLL\Downloads\nyc-zoning\ctl24\docs\research\zr-snapshots\v1\zr-12-10.snapshot.json
C:\Users\MLFLL\Downloads\nyc-zoning\ctl24\services\api\app\_zr_snapshots\v1\zr-12-10.snapshot.json
C:\Users\MLFLL\Downloads\nyc-zoning\ctl24\project-control\reports\M4-T018-zr1210-wide-street-reconciliation.md (sanctioned source)
C:\Users\MLFLL\Downloads\nyc-zoning\ctl24\services\api\app\rules\snapshots.py (loader contract)

G3 verdict: PASS. A1-A5 are hardening suggestions over an already-fail-closed, correctly-transcribed leaf module; none reaches a caller-visible defect.
