# M5-T089 — G1 official-source verification (data-contract-verifier "dc-t089", read-only)

> Transmission history: pinned at fa7e5fae, delivered as four SendMessage parts (1/4..4/4) ending with
> END-OF-REPORT; no truncation. Joined verbatim by the orchestrator (transport wrapper tags removed only).
> Verdict PASS: live layer metadata and all 8 recorded query URLs re-fetched byte-identical to the
> fixtures (dataset unedited since capture). Advisories A1-A4 (none blocking) are routed to DB-058.

---

M5-T089 G1 official-source verification (dc-t089, READ-ONLY) — part 1/4

HEAD pinned fa7e5fae8c9d (matches expected). Material fe254eee verified against the LIVE OTI BUILDING_view service + City data dictionary + service FGDC metadata, 2026-09-24. Live read-only fetches only; no writes.

STRONGEST RESULT: the dataset is unedited since capture (live dataLastEditDate still 2026-09-20T02:16:01Z), so the recorded bodies reproduce live BYTE-FOR-BYTE.

(6) FIXTURE INTEGRITY: 11/11 fixtures match MANIFEST sha256 AND byte count EXACTLY; all LF-stored (raw==LF hash), each ≤16 KB. PASS.

(1) LAYER METADATA: live FeatureServer/0?f=json is BYTE-IDENTICAL to layer_metadata.json (sha256 bba7e9f9…0374, 10897 B; HTTP Last-Modified = dataLastEditDate). Live schema vs connector:
- name=BUILDING, geometryType=esriGeometryPolygon, objectIdField=OBJECTID — all match.
- maxRecordCount=2000 (=MAX_PAGE_SIZE); advancedQueryCapabilities.supportsPagination=true, supportsOrderBy=true — both present (validator requires both).
- extent.spatialReference={wkid:102100,latestWkid:3857} = connector PUBLICATION_SPATIAL_REFERENCE.
- currentVersion=12, capabilities=Query,Extract.
- All 12 REQUIRED_FIELDS present with EXACT esri types (table in part 2).
- NAME, Shape__Area, Shape__Length exist live but are deliberately NOT requested (dict: NAME "not maintained", Shape__* "Do not use"). Correct.

(2) QUERY RE-FETCH: all 8 recorded query URLs re-fetched byte-for-byte IDENTICAL to fixtures — subject p1/p2, lot_polygon, condo, courtyard_hole, placeholder, zero_height_condo, empty_result. The fixtures are genuine, unmodified official responses.

→ part 2/4 (fact table).

---

M5-T089 G1 — part 2/4 (FIELD/FACT TABLE: fact | fixture/code | official source | verdict)

Long field names + esri types (live-exact, all 12):
OBJECTID/OID, DOITT_ID/Integer, BIN/Integer, BASE_BBL/String, MAPPLUTO_BBL/String, HEIGHT_ROOF/Double, GROUND_ELEVATION/Integer, CONSTRUCTION_YEAR/Integer, FEATURE_CODE/SmallInteger, GEOM_SOURCE/String, LAST_EDITED_DATE/Date, LAST_STATUS_TYPE/String | REQUIRED_FIELDS connector:113 | live layer/0?f=json | MATCH.

CRS (item 5): outSR=2263 → response body spatialReference {wkid:102718, latestWkid:2263} | EXPECTED_WKID 102718 / EXPECTED_LATEST_WKID 2263 connector:96 | live query body + FGDC peXml (WKID 102718 / LatestWKID 2263, UNIT Foot_US, EPSG 2263) | MATCH.

Key field values (fixtures == live):
- subject oid229537: BIN 2019299, DOITT 353927, BASE=MPLUTO 2033800084 (non-condo, equal), H 33.49, GE 197, YR 1910, FC 2100, GEOM_SOURCE Photogrammetric | test:290-297 | MATCH.
- neighbours 29471 (H 28.2, GE 197) / 794314 (H 21.28, GE 195) | test:307-312 | MATCH — relative_base −2.0, relative_roof 19.28 arithmetic correct.
- placeholder: FC 1003, H 0, GE null, YR null, GS null, BBL 1201359999 | test:521-529 | MATCH.
- condo: 3 buildings BASE 4068150020 → MPLUTO 4068157501 (billing lot 7501) | test:535-537 | MATCH.
- zero-height condo oid47319: BASE 2059440110 → MPLUTO 2059447501, H 0, GE 201 | test:551 | MATCH.
- courtyard BIN 1028637: ring0 CW exterior (signed area −40953.0), ring1 CCW hole (+5962.6) | test:359-363 | MATCH — esri CW=exterior / CCW=hole convention correct.
- empty result: features [], no spatialReference key | test:478 | MATCH — connector CRS gate fires only when features or SR present.

Condo billing-lot constants CONDO_BILLING_LOT_MIN=7501 / MAX=7599 (mappluto_geometry_arcgis:222-223, imported by connector) | recon empirical + fixtures (lot 7501) | MATCH.

→ part 3/4 (field meanings + discoveries).

---

M5-T089 G1 — part 3/4 (FIELD MEANINGS vs City dictionary + service FGDC metadata; item 3)

- HEIGHT_ROOF "the height of the roof above the ground elevation, not height above sea level"; zero/NULL = "this information was not available" | HEIGHT_REFERENCE:153, _height_attr | City dict AND FGDC both verbatim | CONFIRMED.
- HEIGHT unit = us_survey_foot INFERENCE (RQ-1): NO per-field unit tag exists in the dict or FGDC for HEIGHT_ROOF/GROUND_ELEVATION (FGDC's Foot_US is the horizontal-CRS unit only) | HEIGHT_UNIT_BASIS:148 | CONFIRMED — correctly labeled INFERENCE.
- GROUND_ELEVATION conflict, BOTH definitions verified verbatim: City dict = "Lowest Elevation at the building ground level. Calculated from LiDAR or photogrammetrically"; FGDC = "an interpolated elevation value at the centroid… bare-earth surface… from the 2010 LiDAR… Digital Terrain Model" | GROUND_DATUM_BASIS:158 keeps BOTH, chooses neither | CONFIRMED.
- NAVD88 stated only by City dict ("when collected photogrammetrically or from modern sources"); ABSENT from FGDC | GROUND_DATUM_BASIS says exactly this | CONFIRMED.
- FEATURE_CODE domain 1000–5110 (1000 Parking, 1001 Gas Station Canopy, 1002 Storage Tank, 1003 Placeholder, 1004 Auxiliary, 1005 Temporary, 1006 Cantilevered, 2100 Building, 2110 Skybridge, 5100 Under Construction, 5110 Garage) | FEATURE_CODE_LABELS:130 + PLACEHOLDER_FEATURE_CODE=1003 | dict verbatim | ALL 11 MATCH.
- million-BIN 1000000/2000000/… = unassigned, "not unique" | MILLION_BINS {1e6..5e6}:145 | dict + FGDC | CONFIRMED.
- MAPPLUTO_BBL = DOF billing BBL (condo aggregation); BASE_BBL = physically-within (+off-lot caveat) | join logic:897-904 | dict + FGDC verbatim | CONFIRMED.

PRODUCER DISCOVERIES (item 4) — all reproduced LIVE:
- Short field names → HTTP/1.1 200 OK carrying {"error":{"code":400,…"'outFields' parameter is invalid"}} (NOT an HTTP 400 status) | connector refuses via UpstreamError _parse_json_object:606 | CONFIRMED.
- Missing heights published as 0, not NULL: live counts HEIGHT_ROOF IS NULL = 0; HEIGHT_ROOF = 0 = 723; GROUND_ELEVATION IS NULL = 569 | connector types both 0 and NULL | CONFIRMED.
- Request-units headers: x-esri-query-request-units: 3; x-esri-org-request-units-per-min: usage=3198;max=28800 | not read by connector (informational) | CONFIRMED.

→ part 4/4 (findings + verdict).

---

M5-T089 G1 — part 4/4 (FINDINGS + VERDICT)

No BLOCKING findings. Advisory only (all already disclosed by the producer; none blocks G1):

A1 (ADVISORY): the packet / M5-T087-G1 wording "short names return HTTP 400" is inaccurate — actual is HTTP 200 + ArcGIS error object. Producer disclosed (Discovery #1 + Deviation); the connector correctly refuses BOTH shapes. No action.

A2 (ADVISORY — carry to the WIRING packet's G1): GROUND_ELEVATION has two conflicting official definitions (dict lowest-ground-level vs FGDC centroid-interpolated bare-earth DTM), and NAVD88 is asserted only by the dict. The connector correctly surfaces both and chooses neither; the consuming/massing packet must pick the ground datum knowingly (ties to G3 F2 relative-frame reconciliation, which this connector already handles via relative_base_z).

A3 (ADVISORY): RQ-1 height unit remains an inference (no official per-field unit tag anywhere). Correctly labeled; resolve only if an OTI planimetrics spec ever tags it. US-survey vs international foot is ~2 ppm, immaterial at building scale.

A4 (ADVISORY, immaterial): a capture-threshold wording conflict (>400 vs >500 sq ft) exists inside the FGDC; not encoded in the connector.

CONCLUSION: the connector's encoded field names, esri types, CRS gate (outSR=2263 → 102718/2263), publication-CRS drift signal, feature-code domain, condo billing-lot join (7501–7599), million-BIN, gap taxonomy (0/NULL height, null ground, zero-ground-unverified, placeholder 1003), and datum/unit caveats are all faithful to the LIVE official OTI source, City dictionary, and FGDC metadata. Fixtures are genuine, unmodified, byte-exact official responses; provenance quintuple complete. Live probes: metadata + 8 query URLs byte-identical, short-name error object, request-units headers, NULL/zero height counts, FGDC ground-elevation definition — all reproduced.

M5-T089 G1 VERDICT: PASS

END-OF-REPORT
