# M5-T114 — human-journey review (human-journey-reviewer "hj-t114", read-only)

> Transmission history: pinned at c074b6b5, delivered as three SendMessage parts (1/3..3/3) ending with
> END-OF-REPORT; no truncation. Joined verbatim by the orchestrator (transport wrapper tags removed only).
> Verdict PASS with eight ADVISORY items (ADV-1..ADV-8) routed to the backlog for the P2+ slices; the five
> M5-T080 journey findings are honoured.

---

M5-T114 human-journey review (human-journey-reviewer, read-only) — part 1 of 3.

PIN: HEAD c074b6b5 at start (matches expected). Read-only: no git write, no project_control.py, no npm/node; mockup read as text.

VERDICT UP FRONT: PASS. No BLOCKING finding. Eight advisories, none a disclosure loss, status remap, or legal-determination risk.

Method: read the spec, the mockup (as text), and the M5-T080 HJ + rework reports, then verified the load-bearing honesty claims against LIVE source at HEAD (not just the doc): the coverage enum/symbols/glosses, the §29 disclaimer, draft-excludes-verified, the fail-safe labels, wide-street states, the condo withhold set, the development-limits selectors, and the city-warning placement.

Backend grounding is faithful and accurate — the strongest part of this deliverable:
- coverage.ts:22-47 — the six-value enum, symbols (✓ ◐ ! ≠ ∅ —) and glosses match spec §2.2 verbatim.
- disclaimer.ts:5-12 — the §29 text matches the spec §4 blockquote (one cosmetic apostrophe nit, ADV-7); layout.tsx confirms the `<footer role="contentinfo" aria-label="Required disclaimer">` landmark.
- rule-evaluation-contract.ts — DRAFT_COVERAGE_STATUSES excludes `verified`; 7 FAIL_SAFE_REASONS; the three wide-street determination states + far_row — all match §2.1/§2.3/§2.6.
- condo-records.ts:788-794 — channelWithholdsAllowances = multi_lot | unresolved | resolver_error only; `unavailable` never withholds on its own — matches §2.6.
- development-limits.ts — residentialReference statuses ("City reference"/"Unknown"/"Conflicting records"/"Source value unavailable"), FAILURE_LABELS (7 distinct, never a generic "Warning"), calculationStatus + bulkRow labels — all match §2.3.
- AddressConfirmCard.tsx:236-256 — both city warnings (grcMessage, grc2Message) render verbatim inside the warnings banner, above Continue (:384). Preservation intact.

Nothing is remapped; no number is computed in the presentation layer; no claim-class promotion. The mockup uses "permitted/approved/maximum allowed" only to prohibit them (lines 152, 394) and is fully self-contained (grep: no script / http / src= / @import / url() / cdn / img).

Continued in part 2.

---

M5-T114 HJ review — part 2 of 3. Journey walk (search → confirm → overview → condo → proposal/drawing → evidence → report, desktop + phone) and the substantive advisories.

The journey reads clearly: each surface names one dominant decision, distinguishes official vs draft vs proposed vs generated vs human, names what is missing and why, and (except the carried condo gap, ADV-6) gives a next step. No stuck state.

ADV-1 (mockup never renders §29) — the companion mockup, the "picture" P2–P7 build against, never renders the verbatim §29 disclaimer in any frame; it only notes "Required disclaimer footer (§29) renders below" (P1-mockups.html:175) and "canonical reliance statement on paper" (:467). §29 is the one always-present, full-prominence landmark. Spec §4/§7 preserve it authoritatively (so no AS-3 loss), but for an owner ask that is literally "more visually explanatory", the single most important preserved legal element is unillustrated. Render the footer landmark (verbatim text) in at least one desktop + one phone frame.

ADV-2 (claim-class token conflation) — P1-mockups.html:265 tags the DRAFT rule-evaluation FAR row with `class="claim k-generated"` (the *Generated building option* token, dashed indigo) + label "DRAFT". Spec §2.1 makes these two distinct claim classes and §7 requires "draft rule result ≠ generated option". The rendered text ("DRAFT" + "◐conditional") is honest and distinct from the city-record row above, so no analyst is misled — but the mockup reuses the generated-option token for a draft result and defines no dedicated draft token. P2 must not copy it.

ADV-3 (cap cell omits per-cap status in mockup) — frame O-D's "Zoning floor-area cap" row (:266) shows the number + "FAR only · envelope not assessed" but NO readable per-cap coverage status, whereas spec §5.3 + copy-sample #3 require "+ readable coverage status (A06)" — the exact number/flag separation M5-T080 HJ-4 corrected. The spec is right; the mockup under-renders. Add the status beside the cap so a builder replicating the frame doesn't drop it.

ADV-4 (shell/env badge shown inconsistently) — the persistent "Internal build" badge + "professional review required" line, kept visible at every width (closing the architect.css:138 ≤700px hide), is a load-bearing honesty element (§4/§8.2). The mockup renders the shell only on Overview and the env badge only on Search + Overview phone frames; Confirmation/Condo/Proposal/Evidence/Report omit it. Show it consistently (at least once per surface on phone) so builders do not reintroduce the hide.

Continued in part 3.

---

M5-T114 HJ review — part 3 of 3.

ADV-5 (Report "normal" frame shows a failure alert) — frame R-D is labelled "desktop · normal" but leads with the identity-mismatch `≠` "Results are withheld" alert (:459), a failure state, mixed into the clean brief. Muddles normal vs failure; relabel the frame or move the alert to a failure frame.

ADV-6 (condo refusal has no in-app next step — carried gap) — on unresolved/resolver_error/multi_lot the limits carry the withhold and a self-attested confirmation is honestly "not usable for calculations", but no in-app control reaches an *authenticated* site confirmation; the withhold is honestly terminal while B-001 (no-auth) is open. Correct carry of a known gap, not a regression — flag so P4/P5 either gives the refusal a real next step or states plainly that none exists yet.

ADV-7 (§4 apostrophe) — the spec §4 quote renders "platform's" with an ASCII `'` where disclaimer.ts:5 uses a curly `’` (U+2019). Rendered app text comes from the constant (unaffected), and §7 tells builders to reuse exact returned disclosures — so cosmetic, but P2 must import REQUIRED_DISCLAIMER, never retype the §4 quote.

ADV-8 (search recovery copy not pictured) — the search failure state names a concrete recovery only for source_unavailable ("retry once"); rejected/rate_limited/timeout/unavailable defer to the ledger (AD01–AD28). The recovery *principle* (§8.3: state-specific status + the actually-available control, never suggest an unavailable path) is present, so no user is stuck; flag so P2 pulls the exact per-reason recovery copy from the ledger.

M5-T080 CARRY: the five prior BLOCKING findings are honoured in P1, not weakened — HJ-1 (LS-P caveats print in the brief by default, §5.3/§5.7), HJ-3 (wide-street reason + fallback note in the readable/printing tier, §5.6), HJ-4 (cap keeps its review flag beside the value, §2.3/§5.5/copy #3 — modulo the mockup under-render in ADV-3), HJ-5 (one persistent announcer, existing alerts kept, §2.6/§8.6), HJ-2 (proposal/limits/drawing "not printed today", §5.5).

COVERAGE: AS-1 vocabulary maps every state FROM a named backend status with non-colour text (verified at source); AS-2 7×4 matrix all cells filled; AS-3 L-rows placed with ledger ids + a11y/print paths, SR/DB correctly deferred to P6; AS-4 copy samples before→after + 8-item meaning-change register (not adopted); AS-5 static self-contained fictional mockup; AS-6 docs-only, exactly the three allowed paths.

M5-T114 HJ VERDICT: PASS

END-OF-REPORT
