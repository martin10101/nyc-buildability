# M5-T107 — G4 test-adequacy review (qa-engineer "qa-t107", read-only)

> Transmission history: pinned at 7f5c82fa, delivered as three SendMessage parts (1/3..3/3) ending with END-OF-REPORT;
> no truncation. Joined verbatim by the orchestrator (transport wrapper tags removed only). Verdict PASS: 15/15 reddening
> mutations caught plus M18a/b. Advisories 1 (the AST not-wired test can pass vacuously - add a traversal floor), 2 (no
> route caplog test), 3 (include_in_schema on a mounted app) and 4 (the 500 matrix pairs) are carried into the rework
> inventory (recorded after the round-1 G5 FAIL, same identity).

---

M5-T107 G4 test-adequacy review — Part 1/3 (qa-engineer, READ-ONLY, in-memory mutation).

PIN: HEAD 7f5c82fa at start (material d3c47a85). Baseline: from services/api, `python -m pytest tests/scenario/test_scene_assembler.py tests/scenario/test_scene_api.py tests/connectors/test_building_footprints_arcgis.py -q` → 180 passed in 9.15s.

Method: mutated IN MEMORY in the consuming namespace (setattr on the scene_assembler / scene_api / connector modules, restored in finally), exercised the exact value each producer test pins, confirmed the assertion reddens. Reproduced the producer's mutation table and added my own across every AS. (Write is blocked for this reviewer identity → no memory file and no scratch file written; mutations run as python test-execution only.)

Scope integrity (AS-6):
- Connector existing tests intact: the material commit removes EXACTLY one test — the grep-based `test_as5_connector_is_not_wired_to_any_route_or_module` — replaced by the AST/import-graph check. One existing test is legitimately updated: `test_pktc_wall_clock_deadline_stops_paging_typed` now asserts `transport.calls == []` (was `[metadata]`) because DB-073(c) adds a deadline check BEFORE the metadata fetch — a correct contract change, not a weakening. All other connector tests unchanged and green within the 180.
- Determinism / no network: the assembler suite uses synthetic ContextBuilding/Result objects; the route suite uses an injected offline fake fetch + TestClient on a fresh FastAPI(); the connector suite replays recorded fixtures through an injected transport. No socket anywhere. The only real-clock test is `test_504_deadline...` (time.sleep(0.3) vs SCENE_MAX_SECONDS 0.05 then 30.0 — 6x / 100x margins; no realistic flake).
- `test_as1_golden_context_building_shape_is_pinned` is a full-dict equality (the strongest displaced-value guard).

Continued in Part 2 (mutation table).

---

M5-T107 G4 review — Part 2/3. Mutation table (all in-memory, consuming namespace; CAUGHT = a producer assertion reddens).

MINE (added), by AS:
AS-1 — M1 base_z uses raw GROUND_ELEVATION (190.0) not the relative offset → golden pins base_z_ft=-10.0 → CAUGHT. M3 change sa.VERTICAL_UNIT → payload pins "us_survey_foot" → CAUGHT. M11 _coerce_coordinate coerces "abc"→0.0 → producer pins reason="unparseable_coordinate", field="lot_ring[0]"; mutation drops that reason (downstream massing error) → CAUGHT.
AS-2 — M2 _ground_status→"grounded" for a zero/unverified ground → parametrized test pins "ground_zero_unverified" → CAUGHT. M5 drop each part's holes → golden + DB-053(g) pin holes kept (len 5) → CAUGHT. M6 _CONTEXT_RECORD_DISCLOSURE→"maximum allowed building envelope" → claim-word screen flags "MAXIMUM ALLOWED" → CAUGHT.
AS-3 — M4 _escape_untrusted→identity → geom_source "<script>" survives; test pins entity-encoded value + raw absent from whole payload → CAUGHT.
AS-4 — M7 _not_found adds a body key + X-Correlation-ID header → test pins exact {"detail":"Not Found"} and no cid header → CAUGHT.
AS-5 — M18a make app.main "import" the connector → unreachability assertion reddens → CAUGHT. M18b assembler no longer imports the connector → positive-wiring assertion reddens → CAUGHT.

PRODUCER mutations reproduced:
- Untrusted strip (empty _RENDER_UNSAFE_DELETE) — covered by M4 → CAUGHT.
- Rate limit (M10 _rate_limit_allows→True) → [200,200,200] vs pinned 429 → CAUGHT.
- Per-request deadline (M9 SCENE_MAX_SECONDS 0.05→30.0, slow fetch) → 504 vs 200 → CAUGHT.
- U+2028/U+2029 strip (M12 drop from _CONTROL_CHAR_DELETE) → separators survive the sanitiser → CAUGHT.
- Deadline-before-metadata (M14 _check_deadline→noop) → past deadline still makes 3 calls; test pins calls==[] → CAUGHT.
- Deadline-in-retry-sleep (M13 plain self.sleep) → upstream_error vs deadline_exceeded → CAUGHT.
- tz-naive (M15 drop _validate_deadline) → internal_error vs disallowed_request → CAUGHT.
- Deadline per-page-not-once (M16 only first check runs) → page 2 fetched, status ok; test pins refused + p2 never fetched → CAUGHT.
- Cumulative-bytes (M17 huge ceiling) → ok; producer sets ceiling between the one/two-page sums → refused cumulative_bytes_ceiling (baseline reconfirmed, 3 calls) → CAUGHT.

Total: 15/15 reddening mutations CAUGHT, plus M18a/b CAUGHT. Continued in Part 3 (findings + verdict).

---

M5-T107 G4 review — Part 3/3. Findings and verdict.

BLOCKING: none.

ADVISORY (non-blocking; suitable as DISCOVERIES, not rework):
1. The not-wired AST test can pass VACUOUSLY (probe M18c). If `_module_path` ever fails to resolve modules (a source-tree move/refactor), the BFS from app.main traverses nothing (seen={app.main}); both unreachability assertions then pass with no work, and the positive-wiring assertion still passes on its own — so the guard goes green while verifying nothing about reachability. The two active halves (M18a/b) do redden correctly today. Harden it with a traversal floor: assert a KNOWN-reachable module IS in `seen` (or `len(seen) > 1`).
2. No route-side caplog regression test for "no caller/upstream text reaches a log line" (AS-3/AS-4). The assembler's no-logger property IS AST-tested; the route logs only a server correlation id + `_bounded_field(exc.field)`, and exc.field is always server-derived vocabulary ("lot_ring[0]", "proposed_massing", …) and length-bounded, so the property holds by construction today — but a future change routing str(exc)/a caller value into a log line would go uncaught. Low risk.
3. include_in_schema=False on the mounted app is not pinned by a test (M8). AS-4 "absent from OpenAPI" is satisfied and TESTED via the unmounted real-app assertion; the mounted-app schema exclusion (belt-and-suspenders for PKT-H) is only verified indirectly (I confirmed it works: mounted route present in routes, absent from OpenAPI). Informational.
4. The (500, "internal_error") matrix pairs are not exercised (the defensive assemble-stage and serialization-unsafe branches). `test_every_emitted_pair_is_in_the_matrix` covers 200/413/422 only. Low risk.

Assessment: every safety/honesty/provenance guard the packet introduces is backed by a test that asserts the DISPLACED value and reddens under an in-memory mutation in the consuming namespace; the tests are deterministic and offline; the connector's existing tests are intact apart from the one legitimately replaced not-wired test (and the one contract-driven assertion update). The advisories are test-robustness/coverage refinements, not defects in the shipped behavior or in the reddening guards.

M5-T107 G4 VERDICT: PASS

END-OF-REPORT
