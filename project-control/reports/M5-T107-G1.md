# M5-T107 — G1 data-contract verification (data-contract-verifier "dc-t107", read-only)

> Transmission history: pinned at 7f5c82fa, delivered as two SendMessage parts (1/2, 2/2) ending with END-OF-REPORT;
> no truncation. Joined verbatim by the orchestrator (transport wrapper tags removed only). Verdict PASS: the datum,
> NAVD88 attribution, units, zero-ground disclosure, HEIGHT_ROOF semantics and the connector riders verified against the
> live official sources. ADV-1/ADV-2 are carried into the rework inventory.

---

M5-T107 G1 data-contract verification (data-contract-verifier, READ-ONLY) - part 1/2

PIN: HEAD 7f5c82fa (>= expected 52594a03). Material d3c47a85 = exactly the 7 packet files; `git diff --stat d3c47a85..HEAD` on the 6 source/test files is EMPTY (no drift; content read == submitted). Official facts INDEPENDENTLY re-fetched live 2026-09-25: City dictionary raw MD + ArcGIS FeatureServer/0 FGDC metadata XML.

VERIFICATION TABLE (claim | source | result)

1. DATUM = local z=0 frame, base_z = GROUND_ELEVATION - site_ground (scene_assembler.py:100-133). Both official GROUND_ELEVATION definitions disclosed, neither reconciled | live City dict "Lowest Elevation at the building ground level. Calculated from LiDAR or photogrammetrically"; live FGDC "interpolated elevation value at the centroid... bare-earth surface generated from 2010 LiDAR... DTM" | MATCH - both disclosures faithful (:113-122).

2. NAVD88 attributed ONLY to the dictionary (:123-127) | live FGDC contains NO "NAVD88"/"1988"/"North American Vertical Datum" anywhere; dictionary states NAVD88 "when from modern sources" | MATCH.

3. Units = feet / us_survey_foot (:129,:389) | EPSG:2263 = US survey feet (coordinate-frame unit); DB-054(k) accepted decision to declare the vertical unit = the horizontal CRS unit | MATCH as the frame unit. See ADV-1: the per-field feet-is-INFERENCE basis (RQ-1) is not echoed.

4. Zero/missing/unverified grounds disclosed, never sea level (:356-364 _ground_status; :128-132 unverified_ground_policy) | connector _ground_attr keeps a literal 0.0 flagged ground_elevation_zero_unverified; missing ground / missing site ground -> base_z null | MATCH - test_as2_zero_and_unverified_grounds parametrizes grounded / zero_unverified / missing / site_not_supplied; base_z never fabricated to 0.

5. Choice stated with source in provenance | scene.provenance.scene.ground_datum AND context_buildings.ground_datum = GROUND_DATUM_DECISION; source names "NYC OTI BUILDING_view/FeatureServer/0; 5zhs-2jue GROUND_ELEVATION, as published" | MATCH (source registry + connector agree).

6. HEIGHT_ROOF = roof height ABOVE GROUND, not sea level; assembler top = roof_z_ft = (ground - site_ground) + height_roof | live dict "height of the roof above the ground elevation, not height above sea level"; live FGDC "difference from the building elevation... and the interpolated TIN" | MATCH - unit/datum mix correct (all feet, all local frame; HEIGHT_ROOF never added to an absolute elevation). Golden: 190-200=-10 base, -10+45=35 roof.

-> part 2/2

---

M5-T107 G1 - part 2/2 (rows 7-11, findings, verdict)

7. DB-054(o) string coords -> float or typed refusal, no silent coercion | scene_assembler.py:207-246 _coerce_coordinate/parse_ring; "nan"/"inf"/non-numeric -> SceneAssemblyError reason=unparseable_coordinate | MATCH - test_as1_non_numeric_or_non_finite_coordinate_is_refused_typed covers nan/inf/abc; bool rejected.

8. DB-058(a) untrusted text declared + escaped for every renderer; raw not emitted | :332-410 _escape_untrusted (control + U+2028/29 strip, html.escape quote=True, MAX_UNTRUSTED_LEN bound) over geom_source/last_status_type/every attributes string value; untrusted_text_fields declared | MATCH - test asserts <script> absent from json.dumps(scene); a<b>&"' -> a&lt;b&gt;&amp;&quot;&#x27;.

9. DB-053(g) MultiPolygon courtyard holes disclosed, never dropped | :155-166 disclosure + :392-399 parts carry every exterior+holes+area, has_holes/multipart flags | MATCH - test keeps the 5-pt hole ring; layer discloses the single-ring-consumer-must-refuse policy.

10. Connector riders DB-073(a)-(d) correct vs the service paging behaviour | (a) _CONTROL_CHAR_DELETE incl 0x2028/0x2029 + _safe_correlation_id bound to _LOG_FIELD_MAX (:438-468); (b) same bound; (c) _check_deadline before the metadata fetch (:1080) AND in _sleep_within_deadline during retry backoff (:641-649); scene route passes interactive=True + wall-clock deadline (scene_api.py:292,304-305); (d) _validate_deadline rejects tz-naive as disallowed_request before I/O (:966-979) | MATCH - each has a reddening-mutation test (separator/length cid, deadline-before-metadata, deadline-in-retry-sleep, tz-naive, per-page-not-once, cumulative-bytes ceiling).

11. No overclaim (D-083) | context record_class official_city_footprint, disclosure "display and massing grade, not survey grade; not a zoning determination and not a statement of what may be built"; massing keeps its own labels | MATCH - test_as2_..._no_barred_claim_word screens every emitted label + all datum/definition strings; no legal conclusion.

FINDINGS
- No BLOCKING findings.
- ADV-1 (safe, non-blocking): the scene payload omits the connector's height_unit_basis (RQ-1: feet is an INFERENCE, no official per-field unit tag) and height_reference ("HEIGHT_ROOF above ground, not sea level"). vertical_unit=us_survey_foot is correct (the coordinate-frame unit; roof_z_ft correctly bakes in the above-ground semantic), so this is a completeness nit, not a false claim - a raw height_roof_ft/ground_elevation_ft renderer would not see the RQ-1 residual. Consider threading them into the context provenance. The ground-datum two-definition + NAVD88 disclosure IS carried (more completely) via GROUND_DATUM_DECISION.
- ADV-2 (non-material): connector docstring item 3 says FGDC "says the same" on HEIGHT_ROOF; the live FGDC wording differs though semantically equivalent (a height above ground, not sea level). Pre-existing accepted connector text, unchanged this packet. Registry-draft wording is DB-073(h), out of this packet's scope.

Provenance: every checked claim traces to an official publisher (OTI City dictionary MD, ArcGIS FeatureServer/0 FGDC XML, source-registry draft) re-fetched live 2026-09-25, or to the byte-stable material at d3c47a85==HEAD. Inferences (height unit) and residuals (RQ-1/RQ-2) are labelled; nothing asserted beyond the sources.

M5-T107 G1 VERDICT: PASS

END-OF-REPORT
