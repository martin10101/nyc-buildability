# M0-T161 - G2 producer self-check record (recorded by the orchestrator, role self_check)

Producer: backend-engineer (orchestrator-dispatched subagent) in wt-m0t161; one in-worktree commit 49dfbd79 (parent 11089429), cherry-picked to b27fc89b with every file MATCH by git blob identity (4 files: project-control/reports/M0-T161-owner-guide.md f7276bee, project-control/reports/M0-T161-producer-report.md bad3a5a1, tools/controller_update/commission_lanes.ps1 24b2a155, tools/controller_update/ps_tests/test_commission_lanes.ps1 958e0aa2).

- **Scope:** tools/controller_update/commission_lanes.ps1 (918 lines: five phases, PowerShell 5.1, StrictMode, named constants, one Invoke-Ext helper, every section-5 STOP), tools/controller_update/ps_tests/test_commission_lanes.ps1 (offline: dot-sourced with -LoadOnly, Invoke-Ext and the tree/launch helpers stubbed, sandbox in %TEMP%), and the plain-English owner guide.
- **Producer self-checks (report, OBSERVED):** The producer sandbox refused powershell.exe in every form ([BLOCKED], recipe recorded); the orchestrator reviewed the test harness before running it (every machine-changing call is stubbed or unreachable: the update case STOPs at 5.4 before propagation, the lane case stubs Start-Detached, the plan cases use a %TEMP% sandbox) and ran the parser + the full offline suite at harvest.
- **[ORCH-HARVEST] at the cherry-picked head b27fc89b (Python 3.11 local): `powershell -NoProfile -Command $e=$null;$t=$null;foreach($f in @('tools/controller_update/commission_lanes.ps1','tools/controller_update/ps_tests/test_commission_lanes.ps1')){[void][System.Management.Automation.Language.Parser]::ParseFile((Resolve-Path $f),[ref]$t,[ref]$e); if($e.Count -gt 0){Write-Output ($f+' PARSE ERRORS '+$e.Count); exit 1}}; Write-Output 'parse ok: 0 errors in both files'` (cwd .) -> exit 0: parse ok: 0 errors in both files; `powershell -NoProfile -ExecutionPolicy Bypass -File tools/controller_update/ps_tests/run_ps_tests.ps1` (cwd .) -> exit 0: run_ps_tests(controller_update): 8 test file(s) passed.**
- **Discoveries routed (producer):** D-1 the producer sandbox refuses powershell.exe (orchestrator-run harness, as for M0-T160); D-2 a cosmetic lane-wrapper header note; D-3 lane checkout keys are computed at run time, never hardcoded.

G2 verdict: PASS (self_check; the independent reviews and the DCV follow - dispatched by the successor session per the handoff).
