# M5-T081 - G2 producer self-check record (recorded by the orchestrator, role self_check)

Producer: backend-engineer (orchestrator-dispatched subagent, D-087 capacity wave) in wt-m5t081; one in-worktree commit a8e6c8ed, cherry-picked to 4fd58a98 with every file MATCH by git blob identity.

- **Scope:** the commit touches exactly the packet's allowed_paths; shared package files untouched; no dependency, route, main.py or web change.
- **Producer self-checks (report):** 17 passed (producer, Python 3.11.9) / part of the 71 harvest; ruff clean; modularity exit 0.
- **Mutation record (report):** sanitizer bypass mutant -> test_as3_newline_injection_refused RED ('DID NOT RAISE'), reverted GREEN.
- **[ORCH-HARVEST] primary re-run at the cherry-picked head (Python 3.11.9, cwd services/api): `python -m ruff check .` -> All checks passed!; `python -m pytest tests/cad/test_dxf_writer.py tests/scenario/test_massing_model.py tests/drawings/test_dxf_reader.py -q` -> 71 passed; `python -m pytest tests/cad/test_pdf_sheet_writer.py -q` -> 19 passed; repo root `python tools/modularity_check.py --check` -> exit 0 (selected 484 files; failures 0; warnings 22, none on the new modules).**
- **Environment note:** local Python is 3.11.9 while CI targets 3.12; the seam push's CI api job is the 3.12 proof.

G2 verdict: PASS (self_check; never independent review - the packet's G1/G3/G4/G5 reviewers and the DCV follow).

## Rework re-check (after G3 FAIL + G4 FAIL; re-recorded at the rework head)

- **Rework producer:** backend-engineer (orchestrator-dispatched, same worktree wt-m5t081); one commit 778bf8ca (parent a8e6c8ed), cherry-picked to 0ffe581c with every file MATCH by git blob identity (dxf_writer.py 152f1f2e, test_dxf_writer.py 2aa45291, producer report 9a4d06ef). Touches exactly the 3 allowed paths.
- **Cluster closed (producer report "## Rework" section):** G3 F1 / G1 $INSUNITS 2 -> 21 (US survey feet) with the false comment replaced; G4 F1 honesty labels pinned as hardcoded literals; G4 F2 sanitizer rejection tests (\x00, \x1b, e-acute); G4 F3 claim-word guard load-bearing test + literal 11-word set; G5 F1 builder refuses over-cap floors/ring/entities BEFORE allocating; G5 F2 coordinate magnitude bound 1e8 at the single numeric choke point. Golden sha256 re-anchored (only the header value changed).
- **Producer mutant table (report):** five guard mutants (INSUNITS 21->2, label drop, sanitizer CR/LF-only, claim guard no-op, floor pre-check removed) each RED on its named test and GREEN on revert.
- **[ORCH-HARVEST] primary re-run at 0ffe581c (Python 3.11.9, cwd services/api):** `python -m ruff check .` -> All checks passed!; `python -m pytest tests/cad/test_dxf_writer.py -q` -> 24 passed.
- **Evidence map:** D-087-R004 corrected [ORCH-CORRECTED per G3-F1/G1] ($INSUNITS=21), material_commit names both harvests.

G2 verdict (re-recorded): PASS (self_check; the G1/G3/G4/G5 delta re-rulings by the same reviewers and the DCV follow).
