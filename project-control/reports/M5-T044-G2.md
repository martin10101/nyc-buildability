# M5-T044 — G2 producer self-check record (recorded by the orchestrator)

G2 is the producer self-check gate; the CLI records it under the orchestrator's name (it
rejects the producer's own label). Basis, 2026-09-19 seq-118:

1. **Producer self-checks (loop-2 run persistent2-local-08, worker claude-opus-4-8):** the
   producer report documents all four documented commands recaptured with exact cwd
   discipline (ruff clean; 54 passed; 880 connectors-wide; modularity failures 0 with the
   justification-tier cohesion note recorded), the worker-vs-supervisor evidence split, and
   the disclosed (j) $limit deferral.
2. **Orchestrator reproduction at harvest (in wt-m5t044):** the same four commands re-run
   independently green before the material commit.
3. **Correction cycle:** G3 returned PASS-with-required-correction F1; the orchestrator
   applied the tagged correction `a471e376` (post-response stamp on resolve_by_condo_key +
   mutation-sensitive timing test) and re-ran: ruff clean; **55 passed**; **881
   connectors-wide**; the resubmission freezes this corrected identity.
4. **CI:** run 35412920210 success at `a380a956` for the original material (M5-T044 files
   byte-identical from material to that head); the corrected head's CI is captured on the
   next pushed run before acceptance.
5. **Scope check:** material + correction touch exactly the packet's two code files (+ the
   report placeholder path); module remains a leaf.

**G2 verdict: PASS.**
