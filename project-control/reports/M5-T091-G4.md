# M5-T091 + M5-T092 — G4 independent test-adequacy review (qa-engineer "qa-w2", read-only; joint review)

> Transmission history: one joint review pinned at 4c999e15 (HEAD advanced to 6c79cd88 during the review via
> disjoint commits; the four target blobs byte-unchanged), delivered as four SendMessage parts (1/4..4/4)
> ending with END-OF-REPORT; no truncation. Joined verbatim by the orchestrator (transport wrapper tags
> removed only); saved identically as M5-T091-G4.md and M5-T092-G4.md. The part-4 request to persist
> reviewer memory on the reviewer's behalf is not acted on (the orchestrator does not write another agent's
> memory for it). Verdicts: both PASS (advisories: T091 y-coordinate finite case; T092 claim-word pin).

---

G4 test-adequacy review (independent, read-only), Part 1/4. Repo ctl24.

IDENTITY: reviewed at pinned blobs — pdf_sheet_writer.py 86b129c9, glb_writer.py 64568629, test_pdf_sheet_writer.py 52f37aa9, test_glb_writer.py 9166d160. HEAD advanced 4c999e15→6c79cd88 during review via DISJOINT peer commits; all four target blobs byte-unchanged, working tree clean for them, so the review holds. Local Py3.11.9 (CI 3.12); baseline 180/58 pass. Mutations applied IN MEMORY: the re-exec'd module is installed in sys.modules and the test module is purged each run, so by-value imports (render_site_plan_pdf / write_glb) bind to the mutant (per the consuming-namespace lesson); repo bytes never touched (disk_unchanged asserted every run).

M5-T091 mutation table — all 13 producer-named mutants reproduce RED (counts corroborated):
- M1a num-type guard → RED 14
- M1b vertex-sequence guard → RED 18
- M1c arity check → RED 6
- M1d ring-container guard → RED 8
- M2 _num finite guard → RED 9
- M3a claim-screen off → RED 66
- M3b raw-form only (drop printed form) → RED 1 (the ſ/long-s case)
- M3c no separator-normalisation → RED 5
- M3d address-field only → RED 45
- M4a no paren-escaping → RED 19
- M4b escaper skips lone "(" → RED 28
- M4c escaper skips lone ")" → RED 28
- M4d no backslash-escaping → RED 8
Every producer mutant reproduces; counts align with the producer report.

---

G4 review Part 2/4 — M5-T091 reviewer weakenings + honesty + scope.

Reviewer weakenings (≥3 added; 4 caught, 1 survived):
- EX1 drop bool-exclusion (accept True) → RED 2
- EX3 _TEXT_LIKE=() (str/bytes become sequences) → RED 4
- EX4 _num guard isnan-only (misses inf) → RED 6
- EX5 idiomatic DXF-style screen (word in value.upper()) → RED 5
- SURVIVOR — EX2 finite guard on x only (isfinite(x) and isfinite(y) → isfinite(x)): GREEN, 180 pass.
  Probed both modules: real code returns non_finite_coordinate for (0,nan) AND (0,inf). Mutant still returns TYPED refusals — (0,nan)→non_finite_value (via _num), (0,inf)→oversize_input (via magnitude bound) — never bytes, never raises. The y finite-guard is REDUNDANT defense-in-depth; AS-1 "typed refusal, never raises" holds. This is a test COVERAGE gap (no y-position non-finite case), NOT a fail-closed regression → ADVISORY.
  Fix: add ((0.0,nan),"non_finite_coordinate") and ((0.0,inf),"non_finite_coordinate") to the malformed-vertex parametrization (pass on real code, redden the mutant).

Tautology / honesty (M5-T081 lesson): PASS. The stamp is a hardcoded literal — weakening "PROPOSED - NOT A CITY RECORD"→"PROPOSED" reddens test_proposed_..._stamps. The claim set uses an identity assert PLUS a hardcoded _PINNED_CLAIM_WORDS equality; simulated DXF drift (drop APPROVED, identity preserved) reddens the pin. No by-value-constant tautology found.

AS-5 (independently confirmed, read-only): the 4 blobs are byte-pinned, ruff clean on all four files, golden c38360f9 held, not wired (only app/cad/__init__.py line-3 docstring; no import statement).

---

G4 review Part 3/4 — M5-T092 mutation table + oracle independence.

All 13 producer-named mutants reproduce RED; counts EXACT-match the producer report:
- M1 JSON pad 0x20→0x00 → RED 15
- M2 BIN pad 0x00→0x20 → RED 1
- M3 max[Y] computed with min → RED 3
- M4 min − 1e-6 → RED 3
- M5 skip unit conversion → RED 6
- M6 international foot 0.3048 → RED 7
- M7 axis-map made identity → RED 7
- M8 header length omits the 12-byte header → RED 16
- M9 index bound "<=" count → RED 1
- M10 degenerate check disabled → RED 3
- M11 vertex-cap off → RED 2
- M12 _real finite check off → RED 6
- M13 claim-name check off → RED 2

Reviewer weakenings (4 added, ALL caught): EX uint16 index (5125→5123) → RED 4; EX +y axis (drop the −y negation) → RED 7; EX pad modulo-8 → RED 8; EX min/max over the whole flat buffer → RED 3. Honesty label PROPOSED_LABEL is pinned to a hardcoded literal (test:302) — mutating the source label → RED 3.

IN-TEST PARSER = INDEPENDENT ORACLE (NOT circular): magic 0x46546C67, chunk types, FT_TO_M = 1200/3937, the axis map (x, z, −y) and f32 are all RE-DECLARED as hardcoded literals in the test, and it decodes with raw struct.unpack — no writer format-constants or helpers on the decode path; the golden is a hardcoded hex string. Confirmed live: M5/M6/M7/EX_plus_y all redden the independent expected_gltf_point/min-max expectations. Note: M2 (BIN pad) only reddens the DIRECT _bin_chunk helper test because the public path never emits BIN padding (float32/uint32 payloads are always 4-byte multiples) — disclosed by the producer and acceptable; the parser still asserts any BIN tail is zero.

---

G4 review Part 4/4 — M5-T092 advisory, coverage, verdicts.

ADVISORY (non-blocking), M5-T092: the claim-class screen on mesh names DUPLICATES dxf_writer's list (producer deviation #4) but is UNDER-pinned vs T091. Only 2 of 11 words are exercised (claim_word "Maximum allowed", claim_word_2 "Approved"); g.CLAIM_CLASS_WORDS is never pinned by value and there is no drift guard. Evidence (live): dropping CERTIFIED, LAWFUL, ENTITLEMENT, GUARANTEED, or AS-OF-RIGHT each SURVIVES (58 pass); dropping APPROVED reddens. Fix: pin the full tuple as a hardcoded literal (mirror T091's _PINNED_CLAIM_WORDS) and/or parametrize all words in test_as4. The honesty LABEL itself is fully pinned, so this is secondary-defense hardening, not a broken guard.

AS coverage: T091 AS-1..AS-4 each have a reddening mutant plus the honesty pins; T092 AS-1..AS-4 each reddened; AS-5 scope (stdlib-only AST test + not-wired test) present and green on both. No BLOCKING findings on either task.

MEMORY-WORTHY (please persist under .claude/agent-memory/qa-engineer/ — my own writes are guard-blocked): (1) Redundant-guard survivors: when a coordinate/value guard is backstopped downstream (here _num finite + magnitude bound), an x-only / one-field mutant can survive while fail-closed still holds — classify by PROBING whether output stays a typed refusal (never bytes/raise) and rate ADVISORY, not BLOCKING; recommend a symmetric test case to close it. (2) Duplicated honesty vocabularies must be pinned PER writer: T091 pins the full tuple, T092 does not, so a word dropped from the duplicate ships green.

M5-T091 G4 VERDICT: PASS
M5-T092 G4 VERDICT: PASS
END-OF-REPORT
