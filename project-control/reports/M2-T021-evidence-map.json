{
  "requirements": {
    "D-038-R003": [
      "PRODUCT ENGINEERING, NOT SELF-INFRASTRUCTURE: the deliverable is the Geoclient address-resolution connector - the foundation of address entry, the owner working list's single biggest client-demo improvement (docs/MVP_AGENDA.md B3 / A2 item 4). The entire diff is one commit (dc227c0a on task/M2-T021-geoclient-connector): connector module, 36-test acceptance pack, two recorded fixtures, one source-registry record, one producer report. ZERO files under tools/, .claude/, the supervisor, project-control machinery, or any loop/self-infrastructure path.",
      "HONEST SCOPE NOTE for the verifier: R003's enumerated examples are user-facing screens, deterministic rule families, and the scenario/optimization engine. A source connector is none of those three literally; it is product engineering in the head-term sense - it exists solely so the address-entry screen (a named MVP deliverable) can be built, and its packet objective explicitly excludes endpoint and UI as follow-up product packets. Whether the enumeration is exhaustive or illustrative is the verifier's ruling to make, not this map's.",
      "Contracted as a normal G0 packet with EXECUTABLE acceptance scenarios: gates/M2-T021-G0.json PASS at contract SHA 5937b1f4; 8 scenarios S1..S8 each an {input, expected} pair naming the fixture or seam it runs against; required gates G0/G1/G3/G4/G5 with a four-reviewer independent roster (code-reviewer, qa-engineer, security-reviewer, data-contract-verifier), producer distinct from every reviewer. Gate results beyond G0 are PENDING at submit time - none is claimed here."
    ],
    "D-038-R004": [
      "VERIFICATION IS FULLY OFFLINE: all 36 tests drive the connector through an injected transport seam against three recorded fixtures; no test performs live Geoclient resolution, no test uses a real key (S6 uses a sentinel and asserts leak absence across success, ambiguity, rejection, 401, timeout and malformed paths, including captured log records); a missing key yields a typed KeyMissingError with zero network calls (tested), and the default network transport is proven unreachable before the key check (tested). No Supabase, no durable storage, no cloud credential is touched by any test. Fixture-based testing is exactly the mode R004's own text permits.",
      "CHANGED-FACT DISCLOSURE, stated plainly for the verifier: R004 records the owner's decision AT THE D-038 CAPTURE SESSION - 'owner ... did not provide the NYC Geoclient key (B-004)' and its text scopes itself to 'the product task selected for the loop this session'. That factual premise has since been superseded by the owner's own acts, recorded in docs/MVP_AGENDA.md section I: the owner obtained the key (2026-09-10), placed it on Render and on the owner machine (2026-09-11), personally executed the J1 first-capture step, and directed this task ('go ahead with the address entry connector'). M2-T021 is not a loop-selected task of that session.",
      "LIVE-CALL SURFACE, disclosed exactly: three live Geoclient captures total were made during production - G01 (happy path, captured before contract), G02 (ambiguity EE) and G03 (rejection 42) - each a single KB-scale GET from the owner machine holding the owner-provided key, each recorded with key-free request URL, http status, retrieval timestamp and response sha256, each machine-verified key-absent before commit. These captures are evidence recording (the packet path note authorizes them), not live resolution inside build or verify: no build step and no test depends on network access. If the verifier rules R004 strictly applicable to this non-loop task despite its session scoping, the superseding owner decision should be recorded as a D-038 amendment rather than this map claiming the constraint away."
    ]
  }
}
