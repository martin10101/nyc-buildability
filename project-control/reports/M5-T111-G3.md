# M5-T111 — G3 code review (code-reviewer "cr-t111", read-only)

> Transmission history: pinned at fb613812 (material c3e3df15), delivered as 2 SendMessage part(s) ending with END-OF-REPORT;
> no truncation. Joined verbatim by the orchestrator (transport wrapper tags removed only). Verdict PASS; five ADVISORY items
> (HEAD/OPTIONS unasserted, AS-1 scan breadth, a docstring path, the 256 KiB ceiling at PKT-H, the real-runner deadline path).

---

M5-T111 G3 CODE REVIEW (independent, read-only) — PART 1/2

IDENTITY: Reviewed at HEAD fb613812; material c3e3df15 (cherry-pick of wt d3e303eb). Verified no commit after c3e3df15 touches any of the 9 M5-T111 files (git log c3e3df15..HEAD -- <8 code/test files> is EMPTY); working tree clean for them. Disjoint peer commits (0c24a8b5→fb613812) touch other tasks' files only, per PIN. Review identity = c3e3df15, byte-stable.

EVIDENCE RUN (all green, my sandbox 3.11):
- `ruff check .` (services/api): All checks passed.
- pytest tests/resilience + scenario/test_scene_api.py + cad/test_export_api.py: 148 passed; drawings/test_dxf_import_api.py: 35 passed. Total 183 (matches report).
- `modularity_check.py --check`: DIRECT_EXIT=0. rate_limit.py 252 lines; every warn line is a PRE-EXISTING untouched file, none of this packet's.
- Exactly the 9 allowed_paths; zero forbidden paths; no requirements/lockfile/pyproject touched → zero new deps (stdlib + starlette, already admitted). app/main.py untouched → routes stay UNMOUNTED (test_route_is_unmounted_in_the_real_app in all three).

BLOCKING FINDINGS: NONE.

CONFIRMED CORRECT (the load-bearing surface):
- Limiter (rate_limit.py:90-145): sliding window; ALL check+mutate under self._lock (now=clock() read pre-lock is a benign timestamp, no check-then-act race); per-key deque bounded by max_requests (append only when len<max_requests, :106-108); key ceiling enforced (:113-117); _sweep_expired (:137-144) deletes ONLY empty-window keys → active key's history never evicted; new key at a full-of-active ceiling REFUSED fail-closed (:116-117); empty window dropped on access (:99-103); clock injected.
- caller_key (:147-172): principal-else-host, namespaced principal:/host: (no cross-collision), reads request.state.principal defensively (getattr chain, no crash pre-auth); X-Forwarded-For deliberately untrusted; proxy-collapse disclosed. IMPROVEMENT over old scene _caller_key (host-only, unnamespaced).
- JobSlots/run_in_job_slot (:180-251): slot released ONLY in _guarded finally (bound to THREAD end), never on await cancel; over-cap raises SlotsExhausted synchronously before work; release() guarded >0 (no negative). DB-082(b) satisfied.
- All 3 routes: rate-limit runs BEFORE any body/param parse — scene:295, export:239, dxf _gate_and_limit:387 called before _parse_assignment (the DB-081 b fix moved it ahead of param parse). include_in_schema=False on all (:279/:395/:444/:223). Per-route deadline STATUS unchanged vs claim seam (scene 504, export/dxf 503 — diff adds only (405,None) and (503,capacity_exhausted)); 429 shape unchanged; export filename-safety + nosniff + redacted refusal preserved; connector interactive=True+deadline preserved (scene:355-368).

Part 2 (test adequacy + 5 advisories + verdict) follows.

---

M5-T111 G3 CODE REVIEW — PART 2/2

TEST ADEQUACY (behavioural, load-bearing):
- Limiter-before-parse proven BOTH ways with a body-read tripwire: refuse→429 & body NOT read, admit→same malformed body→422 & read (scene test:346, export:266). dxf DB-081(b) canonical: max_req=0→429, max_req=1000→same bad param→422 (test:376). Strong.
- every-method-404 iterates GET/POST/PUT/PATCH/DELETE→404+{"detail":"Not Found"}+no X-Correlation-ID; enabled non-POST→405 proves it's the flag not a missing route (all 3). OpenAPI-absence includes the router on a throwaway app then asserts absence (scene:222, dxf:184, export:135) — meaningful.
- 503 capacity + 504/503 deadline each have a restore→200 reddening mutation. rate_limit.py unit pack (14 tests): active-no-evict, idle-evict, empty-window-drop, principal-over-host, two-principals-one-host, slot-held-past-deadline-then-freed. DB-081(e) surrogate/NaN guard proven (dxf test:499).

ADVISORY (non-blocking):
1. every-method-404 tests omit HEAD and OPTIONS though both are in _ROUTE_METHODS (scene_api.py:98). Path is flag-first so behaviour is identical/correct, but 2 registered methods are unasserted (test_scene_api.py:198 et al).
2. AS-1 guard breadth: dxf/export test_route_uses_the_shared_limiter_and_slots_only source-scans class _RateLimiter/deque/OrderedDict but NOT the plain-dict form; scene additionally scans "_rate_state" and is stronger. A dict-based local limiter reintroduced in dxf/export could evade the AST scan (behavioural 429 tests still enforce the limit). (test_dxf:200-202, test_export:151-153 vs test_scene:248.)
3. Doc cross-ref: scene_api.py docstrings reference app.resilience.run_in_job_slot / .SlidingWindowRateLimiter (short form), but by design these are NOT re-exported from resilience/__init__ — importable path is app.resilience.rate_limit.*. export_api.py uses the correct :mod: form. Cosmetic (scene_api.py:37-40,121).
4. Deferred body ceiling — MUST honor at PKT-H: 256 KiB MAX_BODY_BYTES correctly KEPT fail-closed + routed (report DB-D1/DB-D2, refining DB-081 c / DB-082 e) since it lives in forbidden proposal_validation.py. Consequence: once MOUNTED, real DXF files (1-20 MB) and two-max-ring exports (~452 KB) get a 413. _IMPORT_DXF_LIMITS is tied to MAX_BODY_BYTES (dxf_import_api.py:117) so PKT-H must raise both. Sound deferral (routes unmounted, fail-closed), but a mount blocker to track.
5. Deadline real-path (pre-existing, accepted M5-T107/8/9): slot-held-under-abandonment proven with an INJECTED runner (test_rate_limit.py:204); the real run_in_threadpool path is exercised only for the 504/503 status (scene:402, export:294), which passes whether the await abandons early or blocks on the thread. JobSlots logic is correct and the concurrency cap holds either way; a 3.12 integration test asserting in_flight while the real runner is past deadline would fully close DB-082(b).

In-scope riders DB-080(a)(b)(c), DB-081(a)-(e), DB-082(b)(d)(e) all addressed; out-of-scope items (DB-080 d GLB, DB-082 a/c) correctly untouched.

M5-T111 G3 VERDICT: PASS

END-OF-REPORT
