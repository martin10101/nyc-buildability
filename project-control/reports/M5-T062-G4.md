# M5-T062 G4 gate — independent QA/test-adequacy review (verbatim reviewer return)

Reviewer: qa-engineer (opus-4-8 xhigh per D-064), dispatched by the orchestrator at the
e085eba5 submit seam, pinned to material 0539cc84 + the [ORCH-CORRECTED] correction 099c32a7
(submit head 0366210c). Recorded by the orchestrator; reviewer is read-only and records no
gate itself.

---

PASS

# G4 Gate Report — M5-T062 (site-definition mount hardening)

**Gate:** G4 (QA / test-adequacy), independent read-only reviewer (qa-engineer). **Verdict: PASS** (no required corrections).

## Pinned identity (verified via plumbing)
- Frozen submitted head reviewed: `0366210c` = material `0539cc84` + `[ORCH-CORRECTED]` `099c32a7`. Confirmed `0539cc84` and `099c32a7` are both ancestors of `0366210c`; `0366210c` is an ancestor of seam `e085eba5`.
- ctl24 live checkout head at review: branch `candidate/D-024-mrl-option-b` = `e085eba5` (the seam); `0366210c` is a direct ancestor — no disjoint peer touched the 8 files.
- `0539cc84` touched exactly the 8 packet files; `099c32a7` touched **only** `tests/api/test_site_definition_api.py` (test-only, per `git diff-tree`).
- The 7 source/test files on disk in ctl24 are **byte-identical (LF-normalized)** to the `0366210c` frozen blobs (7/7 MATCH via `diff --strip-trailing-cr` against `git cat-file blob` extracts). Tests below therefore ran against the frozen material.

## Commands run (cwd + outcome)
- `cd services/api && python -m ruff check .` → `All checks passed!`
- `cd services/api && python -m pytest tests/site_definition tests/api/test_site_definition_api.py -q` → **68 passed** (expected 68).
- `cd services/api && python -m pytest tests/api -q` → **611 passed** (expected 611).
- `cd services/api && python -m pytest tests/api/test_condo_records_api.py -q` → **23 passed** (preservation).
- `cd . (repo root) && python tools/modularity_check.py --check` → **exit 0**; failures 0; warnings all under `tools/**`, none in the packet's scope files.
- Interpreter: **Python 3.11.9 / fastapi 0.128.0** locally (NOT the pinned 3.12/fastapi-0.139 — see AS-5 note).
- Mutation probe (scratch, `PYTHONPATH=. python`) — see AS-1/AS-3 below.
- `git diff 0539cc84~1 0539cc84 -- app/main.py` and `git diff 099c32a7~1 099c32a7 -- tests/api/test_site_definition_api.py` for scope/diff review.

## AS → test mapping and mutation-sensitivity

**AS-1 no-oracle (binding-before-status).** PASS, ordering genuinely pinned.
- Discriminating test: `test_revoke_binds_scope_before_status_so_a_foreign_non_active_record_is_404` (records test:453) — revokes at the own property so the record becomes REVOKED, then a FOREIGN-condo probe of the now-non-active record asserts `ConfirmationNotFoundError` (404), and an OWN-property probe of the same non-active record asserts `ConfirmationNotActiveError` (409). This is exactly the "wrong-condo probe against a non-active record asserting 404 not 409" the gate requires — not only the happy 404.
- Mutation proof: I built a `StatusFirstStore` regression (status-checked before scope) and ran the same scenario. Current (scope-first) store raises `ConfirmationNotFoundError`; regression raises `ConfirmationNotActiveError`. Since `records.py:300/306` makes `ConfirmationNotFoundError` and `ConfirmationNotActiveError` **sibling** subclasses of `SiteDefinitionError` (not parent/child), `pytest.raises(ConfirmationNotFoundError)` would NOT catch the regression → the test fails red. Store code implements scope-before-status correctly at `store.py:390-418`.
- Route delegates ordering entirely to `store.revoke` (`site_definition.py:679`); it performs no independent status check, so no route-level ordering exists to regress. (See note I-1.)

**AS-2 degraded-safe revoke (D-051 fail-direction).** PASS, each class separate.
- `test_revoke_survives_each_degraded_resolver_class_over_the_route` (api:537) parametrizes 5 degraded classes: `single`, `unresolved`, `multi_lot_null_condo_key`, `error`, `raises` — each asserts revoke → 200 revoked.
- Store-layer: `test_revoke_survives_a_degraded_resolver_via_the_addressed_bbl_fallback` (records:496), `test_degraded_revoke_still_rejects_a_foreign_property_probe` (records:513), `test_degraded_revoke_matches_either_the_billing_or_the_entered_bbl` (records:531, entered≠billing both-directions + neither-matches 404).
- A regression that consulted the live resolver on revoke (e.g. used `_resolve_multi_lot`) would 422/500 the degraded classes → the parametrized test flips red. Route uses `_resolve_condo_key_best_effort` (`site_definition.py:451-486`, never raises → None on degrade); store falls back to `addressed_bbl in (billing_bbl, entered_bbl)` (`store.py:351-372`).

**AS-3 caps/guards + atomicity + index.** PASS.
- Over-cap note `test_over_length_note_is_a_typed_field_too_long_refusal` (records:580); name `test_over_length_confirmer_name_is_a_typed_confirmer_refusal` (records:590); both AT-cap accepted. (These raise in the pure `create_confirmation` before any store insert, so no store state can be touched.)
- Chain-depth `test_chain_depth_cap_refuses_typed_without_touching_store_state` (records:605) — **re-reads store state after refusal** (`after == before`, second still ACTIVE). Collision `test_record_id_collision_is_a_typed_refusal_without_overwriting` (records:649) — re-reads (original intact, condo-y list empty). List `test_over_cap_list_is_a_typed_refusal_never_a_silent_truncation` (records:633).
- Atomicity mutation proof: over-cap supersede raised `SupersessionChainTooDeepError` and `before_ids == after_ids` with second ACTIVE (probe output). Guard raises before mutation at the single insertion point `store.py:219-252`.
- condo_key index (`_by_condo_key`): `test_list_for_condo_key_isolates_by_condo_key` (records:662) proves keyed lookup isolates. Lookup-after-mutation coverage: `test_supersede_chains_a_new_active...` (records:218) lists after supersede (index maintained via `_record_insert`); `test_after_revoke_a_fresh_create_is_allowed_again` (records:294) + `test_block_with_only_revoked_history...` (records:351) list after revoke. An index not maintained on supersede would drop the new record from these lists → red.

**AS-4 shape class + matrix + condo_key None.** PASS.
- Parcel-shape under NEW class: `test_normalize_parcels_rejects_malformed_sets_with_a_parcel_shape_error` (records:178) asserts `ParcelShapeError` AND `not issubclass(ParcelShapeError, InvalidConfirmerError)` AND `reject_code == "parcel_set_shape"`; route-level `test_malformed_parcels_refuse_under_the_parcel_shape_code_not_invalid_confirmer` (api:587). Class defined distinct at `records.py:193`.
- Matrix enforcement (drives off-matrix → 500): `test_emission_matrix_is_enforced_not_decorative` (api:629) monkeypatches the matrix to drop the success pair; a real create is forced to 500 — proving the `_json` assert (`site_definition.py:214-250`) is live, not decorative. `test_status_state_matrix_contains_the_documented_pairs` (api:614) pins the pair set.
- condo_key None both directions: write refuses (`test_a_multi_lot_resolution_with_no_condo_key_refuses_create`, api:601 → 422; enforced `site_definition.py:439-447`); revoke survives null condo_key (degraded label `multi_lot_null_condo_key`, api:537).

**AS-5 mount.** PASS (with reliance note I-2 on the 0.139 environment).
- Flag-off generic 404 on ALL routes: `test_flag_off_all_routes_are_generic_404_with_no_correlation_leak` (api:651, list/supersede/revoke) + `test_flag_off_is_generic_404_with_no_correlation_leak` (api:140, create) — assert `{"detail":"Not Found"}` and no `X-Correlation-ID`.
- Flag-on registration+serve: `test_flag_on_the_app_mounts_the_site_definition_routes_and_they_serve` (api:714) — asserts route present AND POST→201.
- Both-flags: `test_mount_flag_on_but_handler_flag_off_is_still_a_generic_404` (api:732).
- Production default off from config: `test_production_default_leaves_the_write_mount_flag_off` (api:688) — `site_definition_write_enabled({})` False, empty/"maybe" False, true tokens True. Fail-safe implemented `site_definition.py:121-135`.
- Corrected `_registered_paths` introspection (api:328-345): the `[ORCH-CORRECTED per CI 35546337617]` fix (diff `099c32a7`) adds the helper and swaps 3 inline `{getattr(route,"path","")...}` comprehensions to it in `test_router_is_not_mounted_in_main_app_by_default`, `test_flag_off_the_app_registers_no_site_definition_route`, `test_flag_on_...serve`. It expands `original_router.routes` (the `_IncludedRouter` layout on fastapi>=0.139) with an `isinstance(path, str)` filter, no-op on the old layout (`inner is None` → `[route]`). **Meaningfulness restored:** the old form returned `""` for `_IncludedRouter` entries on 0.139, making the absence asserts vacuous and the presence assert fail (the CI red). The flag-ON positive assert (api:721) is the structural linchpin — same helper — so if it were still blind that assert would fail; CI-green at `0366210c` on pinned 3.12/fastapi-0.139 confirms it passes there, which makes the flag-OFF absence asserts non-vacuous. Own correctness: non-str filtered; no-op-on-old-layout confirmed by my 0.128 local run (68 passed includes all three). Pattern mirrors the accepted `tests/api/test_evidence_api.py::_flattened_route_list:590-601`.

**AS-6 preservation.** PASS.
- `tests/api` **611 passed** incl. condo_records (23 passed). Wallabout named fixture exercised end-to-end: `test_named_wallabout_condo_confirmation_end_to_end` (api:755) — billing `3022647515`, base lots `3022640032`/`3022640033`, create→list→revoke. `[ORCH-CORRECTED]` diff is test-only (verified: `099c32a7` touches only the api test file; strengthens, never weakens, asserts). ruff clean; modularity exit 0. main.py diff is exactly the two import lines + one flag-gated `include_router` block (`main.py:41-42, 217-218`) — nothing else (verified vs parent), satisfying the D-066-R001 main.py constraint.

## Findings (informational; NOT required corrections)
- **I-1 (info):** AS-1 no-oracle ordering is discriminated at the STORE layer only; the route is a pure pass-through to `store.revoke` with no independent status check, and the API-level cross-property tests (`test_cross_condo_revoke_...` api:417, `test_degraded_revoke_..._foreign_property_probe` api:559) use ACTIVE records — those alone do not distinguish binding-first from status-first. Acceptable because the ordering authority is the store and the store test (records:453) is mutation-proven; not a gap.
- **I-2 (reliance):** AS-5's 0.139 introspection-meaningfulness is corroborated by orchestrator-captured CI-green at `0366210c` (my sandbox is fastapi 0.128, which only exercises the no-op-on-old-layout path). The code logic is correct for both layouts and the flag-ON/flag-OFF same-helper linkage closes the argument; per read-only reviewer protocol I rely on the committed CI evidence for the 0.139 run rather than executing it. No independent defect found.

## Modularity
Seven boundary answers hold against the actual diff: new caps/exceptions/index live in `records.py`/`store.py` (their owning responsibilities), the mount is one line in `main.py`, façade re-exports in `__init__.py`; no responsibility mixing, no dumping ground; `modularity_check.py --check` exit 0 with no scope-file warnings.

**Conclusion: PASS.** No required corrections (no C-n). Test pack is mutation-sensitive across AS-1..AS-6; the two items above are informational.

---

## Orchestrator identity note (appended at CLI-record time, seq 124)

The opus-4-8 session limit killed the recording seam after this verbatim PASS report was
committed (c0d69934); the CLI gate record is made in the successor session. Disjoint peer
commits landed between the reviewed submit head 0366210c and the record head: 8800e7b8
(seq-123 handoff doc) and a3173987 ([ORCH-CORRECTED] web-e2e tabUntil helper fix for
M5-T065 - apps/web/e2e/helpers.ts, outside this task's surface). M5-T062 task identity is
byte-stable across them: git diff 0366210c..HEAD over every allowed_path is empty
(verified at record time). Per the recorded disjoint-peer precedent the review remains
valid at the frozen identity 0366210c.
