# M5-T097 - G2 producer self-check record (recorded by the orchestrator, role self_check)

Producer: backend-engineer (orchestrator-dispatched subagent) in wt-m5t097; one in-worktree commit 45e34106 (parent 2e0b2351), cherry-picked to 9a281517 with every file MATCH by git blob identity (4 files: project-control/reports/M5-T097-producer-report.md 021f0569, services/api/app/drawings/dxf_reader.py 2ecbdf12, services/api/tests/drawings/test_dxf_reader.py d7916592, services/api/tests/drawings/test_dxf_roundtrip.py f301eed4).

- **Scope:** dxf_reader.py (streaming CR/LF split, in-scan line caps, clamped limits, checked str input, INSUNITS 22-24), test_dxf_reader.py (28 unchanged + 21 new), test_dxf_roundtrip.py (7 cases) and the producer report; app/cad and the sheet_* modules untouched.
- **Producer self-checks (report, OBSERVED):** ruff clean; 56 passed (reader + round trip); modularity exit 0 (dxf_reader.py 603 SLOC, 3 over the 600 warning threshold - disclosed, cohesive, below the 750 justification band); each new guard and each G4 survivor (M3/M4a/M4b/M5) killed by a mutation; declared choices: control characters kept verbatim, over-ceiling limits clamped, str input checked.
- **[ORCH-HARVEST] at the cherry-picked head 9a281517 (Python 3.11 local): `python -m ruff check .` (cwd services/api) -> exit 0: All checks passed!; `python -m pytest tests/drawings/test_dxf_reader.py tests/drawings/test_dxf_roundtrip.py tests/cad/test_dxf_writer.py tests/cad/test_cad_owner_samples.py -q -p no:cacheprovider` (cwd services/api) -> exit 0: 95 passed in 0.94s.**
- **Discoveries routed (producer):** DISC-1 a later reader-wiring packet should grant a second module path so the lexer can split behind a facade (the file sits just above the warning threshold); DISC-2 the known local 3.11 collection artifact for tests/drawings (PEP 695 behind app/documents) - informational.

G2 verdict: PASS (self_check; the independent reviews and the DCV follow - dispatched by the successor session per the handoff).

## Rework round 2 re-record (e04c2b5b)

- Round 1 (9a281517) was reviewed: G3 (cr-t097) FAIL B1 and G5 (sec-t097) FAIL Finding 1 on ONE root cause (the per-line double find() made pure-LF/pure-CR input O(n^2)); G4 (qa-t097) PASS with ADVISORY-1 (exact-edge probes). The full failure surface was inventoried before the fix (CLAUDE.md principle 17).
- Round 2 (one bounded change, same producer role): the single-pass regex splitter, two scaling-ratio time guards that redden under the old double-find, the exact-edge probes, and the docstring wording fix. Out of scope and NOT done: G5 A3 digit cap, the import-route advisories, the lexer/model split.

- Harvest: 218ff7ce (parent 481e51ff) cherry-picked to e04c2b5b, every file MATCH by blob (project-control/reports/M5-T097-producer-report.md e6816ad0, services/api/app/drawings/dxf_reader.py 94895927, services/api/tests/drawings/test_dxf_reader.py 78b4a278).
- **[ORCH-HARVEST rework] at the cherry-picked head e04c2b5b (Python 3.11 local): `python -m ruff check .` (cwd services/api) -> exit 0: All checks passed!; `python -m pytest tests/drawings/test_dxf_reader.py tests/drawings/test_dxf_roundtrip.py -q` (cwd services/api) -> exit 0: 61 passed in 7.08s; `python tools/modularity_check.py --check` (cwd .) -> exit 0:   warn review_signal: tools/agent_supervisor/gate_wave.py - above the justification threshold; record a cohesion justification in review.**

G2 verdict (re-recorded): PASS (self_check; delta re-reviews by the SAME reviewers follow).
