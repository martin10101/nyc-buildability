# M5-T060 G4 gate — independent QA/test-adequacy review (verbatim reviewer return)

Reviewer: qa-engineer (opus-4-8 xhigh per D-064), dispatched by the orchestrator at the
c314a29a submit seam, pinned to material d7d45e84. Recorded by the orchestrator; reviewer is
read-only and records no gate itself.

---

I have completed the independent G4 review. Here is the verdict and complete gate report.

---

**VERDICT: PASS** (no required corrections; five advisory findings, all non-blocking)

# G4 Gate Report — M5-T060 (Phase B3 slice 2, proposal editor UI)

## 1. Pinned identity (verified via read-only plumbing)
- **Reviewed material commit:** `d7d45e84…` (full sha matches the prompt's stated frozen identity), 18 files, `+3097 / −35`.
- **Branch tip / submit seam:** `c314a29a` (`candidate/D-024-mrl-option-b`, = ctl24's checked-out HEAD). `git diff --stat d7d45e84 c314a29a` shows the seam touched **only 4 ledger/report files** (evidence-map, M5-T060.json report copy, task json, state.json) — every one of the 18 material files is byte-stable between the material commit and the tip, so the working-tree copies I read equal the frozen identity.
- **Scope integrity:** all 18 changed files ∈ `allowed_paths`; **zero** `forbidden_paths` touched. The T058-HELD `test-support/rule-evaluation-fixtures.ts` is **not in the diff** (confirmed via `git show --stat` + `git diff`) — untouched, though entry/workspace specs legitimately *import* from it. `ScenarioWorkspace.tsx`, `LotOutlineMap.tsx`, `scenario-api.ts`, `packages/contracts/**`, `services/api/**` all absent from the diff.
- **Mount + CSS diffs (via `git show`):** strictly additive — one import + `case "proposal"` arm + inspector-exclusion entry in `ArchitectEntry.tsx`; `"proposal"` appended to `WORKSPACE_VIEWS`/`VIEW_LABELS`/`PRIMARY`; new `.proposal-*` CSS after line 186. **`.architect-map-status` is untouched** (DB-038(e) preserved).

## 2. AS → named-spec mapping (every AS covered)
- **AS-1 (arithmetic-pinned):** `proposal-checks-api.test.ts` L40-57 + L186-220 (unchanged-rectangle request/response pair, captured POST body asserted before decode); `proposal-check-report.test.tsx` L26-38 (**byte-pinned** `"0.625 ratio provided; 0.5 ratio required; 0.125 ratio short"`, height Pass 30/60), L40-64 (both CNC first-class, each with its own **closed** `<details>`), L147-188 (separate arithmetic: composed non-fixture `18 ft/20 ft/2 ft short` + fixture self-consistency `provided−required===shortfall`); backed by `ProposalCheckReport.tsx` `shortfallPhrase` L44-48 and hand-transcribed `proposal-check-fixtures.ts`.
- **AS-2 (honesty):** `proposal-editor.test.tsx` L25/L37/L41; `proposal-check-report.test.tsx` L29; announcement L243 asserts *not* `/verified/`. Icon+text via `OUTCOME_META`/`StatusBadge` (asserted "Pass"/"Could not check" text, never colour-only).
- **AS-3 (typed refusals + bounded echoes):** `proposal-checks-api.test.ts` L27-168 (full matrix incl. negatives, 422 `field`, 413, 500, flag-off 404→feature_unavailable, over-budget Content-Length, non-JSON, timeout, abort, network, bounded-markup-as-text); `proposal-check-report.test.tsx` L66-72 (markup renders as TEXT, `container.querySelector("img"/"script")` null); `proposal-editor.test.tsx` L82-88 (mirror blocks bad charset naming `_LABEL_CHARSET`, nothing sent) + L104-118 (grep: no `dangerouslySetInnerHTML`); `proposal-draft.test.ts` L32-110.
- **AS-4 (variations):** `proposal-editor.test.tsx` L23-42 (save + ephemeral copy), L90-102 (compare two), L104-118 (grep: no `contract_version` in the 6 production sources).
- **AS-5 (a11y/keyboard):** `e2e/proposal-editor.spec.ts` — genuine keyboard-only journey (real `Tab`/`Enter`, `toBeFocused()` asserted before every action, `Control+a`+`pressSequentially` coordinate edit; route-interception stub asserts the serialized POST body of the *edited* draft — srid 2263, 6 vertices, `[1000005,200000]`, 2 levels, 5 walls, area 8000 — **before** fulfilling), plus `proposal-editor.test.tsx` add/edit/delete interactions.
- **AS-6 (mount + gating):** `entry.test.tsx` L183-189 (proposal view renders inside the gated tree) + `workspace.test.tsx` L15-19 (`readWorkspaceView("proposal")` and href, with `"made-up"→overview` fallback preserved). No other spec enumerates the view lists (grep of `WORKSPACE_VIEWS`/`VIEW_LABELS`/`PRIMARY`/nav links) — so the additive view breaks **no silent consumer**; the "two-file-only disturbance" claim holds.
- **AS-7 (proof):** CI-only per thin client (deferred to CI at the pushed head, per the gate instruction to judge specs by reading). Modularity: not re-run in my isolated worktree (different HEAD; would validate the wrong tree), but confirmed by inspection — largest new file `proposal-checks-api.ts` ≈476 lines, all others below it, well under the 600 warn threshold; six focused single-responsibility modules (draft model / transport client / editor container / report renderer / variations / fixtures), no dumping ground. Orchestrator captured `modularity_check --check` exit 0.

## 3. Mutation-sensitivity assessment (per reviewer question)
- **Draft-model mirror:** each cap has a test naming the route constant via the asserted `routeConstant` field (`MAX_LABEL_LEN`, `_LABEL_CHARSET` incl. trailing-newline fullmatch, the four `ROUTE_MAX_*`, finiteness). **However**, a *numeric* drift (mirror 501 vs route 500) would **not** fail any test — the over-cap probes build `MIRROR_*+1`, deriving the boundary from the mirror itself; no test hard-pins the value. See F1.
- **Client decode matrix:** each status decodes to a **distinct** kind, each distinctly asserted; a decoder collapsing 413 into a generic error **would fail** `proposal-checks-api.test.ts` L66-71. The `(status,state)` pair is enforced on the **raw** state before sanitizing (L384-395), and Content-Length is bounded **before** parse (L363-372) — both proven.
- **Editor keyboard flow:** real interaction specs (state mutates, DOM re-asserted), not presence checks; add/edit/delete of vertex+level+wall proven at unit level and keyboard-only in e2e.
- **Report rendering:** shortfall phrasing is **byte-pinned** (exact `toHaveTextContent`), not loosely matched; the bounded-echo spec uses markup-shaped ids and proves TEXT rendering (no injected element). A default-open disclosure would fail (`open === false` asserted).
- **Variations / e2e / entry+workspace / fixtures:** save-compare + ephemerality copy pinned; e2e asserts the real POST contract; entry/workspace enumerate the new view correctly; fixtures are hand-transcribed with clean hygiene (the `outline_digest` is an all-zero 64-hex placeholder, not a secret). CODING_RULES honored: typed DOM queries (`querySelector<HTMLDetailsElement>`, `<HTMLSelectElement>`), invalid-shape probes as `as unknown as T`, no empty specs, grep-proven no `dangerouslySetInnerHTML`.

## 4. Findings (all advisory — none blocks acceptance)
- **F1 (low):** Mirror numeric-value drift is unguarded — tests catch `validateDraft` *ceasing to use* a constant but not an accidental edit to the constant's value. The producer report §6 claim that "drift tests fail if any client constant diverges from its named route source" is **narrower than stated**. Mitigated: the server refusal is the authority (over-permissive mirror → server still refuses; over-strict → degraded UX only), and a cross-language pin to the Python route value is impossible in a TS test. The packet requirement ("a test naming the route constant") is satisfied. Recommend cheap value-pin assertions (`expect(MIRROR_ROUTE_MAX_EXTERIOR_WALLS).toBe(500)`).
- **F2 (low):** `e2e/proposal-editor.spec.ts` walks only the happy path; unhappy decode paths are unit-only. Adequate distribution (each unhappy branch distinctly unit-asserted) and consistent with AS-5, but no browser-level unhappy-rendering coverage.
- **F3 (trivial):** `unattestedReportBody` is exported from the fixtures but unused by the test pack — dead fixture.
- **F4 (low):** `proposalCheckOutcomeIsRecoverable` test asserts only `network_error`/`internal_error` true and `feature/validation/payload` false; `client_timeout`/`validation_failure`/`unexpected_response` (true) and `aborted` are not directly asserted, and the retry-note render test exercises only `network_error`/`validation_error`. A regression flipping `client_timeout` to non-recoverable would pass.
- **F5 (low):** The AS-6 gating spec proves the view renders inside the mounted tree but not that it is unreachable when the rule-eval surface is off. Gating is structurally inherited (case arm inside the already-gated `LoadedWorkspace`, no new flag), so this is sound; a flag-off negative test would be stronger.

## 5. Conclusion
The spec pack pins every claimed behavior with genuine mutation sensitivity; the packet's SCOPE 4/4 test surface is fully delivered; identity, scope, and preservation (additive mount, protected class untouched, no silent red consumers) are verified at the frozen SHA. Findings F1–F5 are advisories, not required corrections. **G4 = PASS.**

Note on remaining proof: web behavior (lint/typecheck/vitest/e2e) is CI-only under thin-client policy and is not asserted verified here — its green CI at the pushed head remains a prerequisite for acceptance (AS-7), outside this reading-based G4 verdict.

Reviewer report authored read-only; no ledger/git/gh commands run. Recording of this gate is the orchestrator's action.

---

Orchestrator note on AS-7: CI at the pushed material head d7d45e84 completed all green (CI /
secret-scan / context-budget) before submit — the prerequisite the reviewer names is satisfied
and recorded in the submit seam c314a29a.
