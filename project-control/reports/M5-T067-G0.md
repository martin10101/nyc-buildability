# M5-T067 G0 — readiness gate (orchestrator, administrative)

Task: **M5-T067 — D-078 site-definition store hardening residuals (DB-040 q+r)**: the two
LOW security residuals the accepted M5-T062's G5 review specified and routed onward —
(q) supersede scope-before-status reorder (mirroring the accepted revoke discipline) and
(r) revoke not-found message unification — both mutation-tested.

## Authority

- D-078 (condo site-definition flow) governs the record substrate; the closures are the
  G5-specified remediations on the just-accepted slice-2a surface (M5-T062-G5.md LOW-1 /
  LOW-2, routed "to the follow-up auth/hardening packet"). This packet takes ONLY the
  non-auth halves — auth/exposure stays B-001-bound and untouched.
- Cited + bound at this seam with same-commit digest resyncs: D-078-R001/R002,
  D-066-R001, D-077-R002/R003. `evaluate_task_refs`: **ok=true, applicable == cited (5 ids)**.
- Sequencing: depends on M5-T062 — ACCEPTED (246th) at this session's prior seam.

## Packet readiness

- allowed_paths: 4 entries (store.py + the two scoped test suites + the producer report);
  every entry is a tracked file. Forbidden paths pin the routes, records module, __init__,
  condo_records read path, main.py, and every neighboring surface.
- 5 acceptance scenarios: oracle closure across all three foreign-record statuses,
  mutation sensitivity both ways, byte-identical not-found bodies, full preservation.
- Documented commands: api ruff first, the scoped pytest, modularity — the closed profile.
- Spec source is the committed G5 report (verbatim finding text + exact line anchors) —
  no rediscovery needed.

## Disjointness (live/held neighbors)

| Neighbor | State | Overlap |
|---|---|---|
| M5-T064 (loop-1 LIVE run-66) | in_progress | none — T064 is `services/api/app/scenario/**` + its tests |
| M5-T065 | awaiting_gate (frozen 65faee54; wave running) | none — T065 is apps/web + outline_bridge.py; store.py not in its 12 paths |
| M5-T066 (loop-3 HELD) | claimed | none — T066 is apps/web only; all of services/api forbidden there |
| M5-T062 | ACCEPTED | successor packet on the released surface |

Pairwise overlap of allowed_paths: EMPTY against every live lane.

## Seam records

- Code graph REGENERATED at this seam: 793 files / 16728 nodes / 7304 edges. store.py
  consumers verified (routes + condo_records read path both FORBIDDEN; consumer-sweep duty
  for out-of-scope message-text assertions written into the packet inputs).
- Validator: EXIT 0 at the prior accept seam this session; control-plane CI green at the
  pushed head 3b36c7c4 lineage.
- Reviewers: code-reviewer, qa-engineer, security-reviewer, directive-compliance-verifier;
  gates G0,G2,G3,G4,G5 (G5 required — a security-finding remediation packet).

**G0 verdict: PASS** — backlog → ready; claim follows at this seam for the loop-2 re-feed
(launcher retarget + fresh run-id + stagger ≥60s).
