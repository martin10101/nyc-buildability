# M5-T058 G4 gate — independent QA/test-adequacy review (verbatim reviewer return)

Reviewer: qa-engineer (opus-4-8 xhigh per D-064), dispatched by the orchestrator at the
5f7b032e submit seam, pinned to reviewed sha 817a6546 (material 7d02eddc + the [ORCH-CORRECTED]
consumer fix; the reviewer extracted the exact frozen tree via git archive, so the review is
pinned by construction). Recorded by the orchestrator; reviewer is read-only and records no
gate itself.

---

I have everything needed. All checks complete. Extracted and verified at the frozen object, ran the required suites, read all web specs and the component, and confirmed scope/compatibility.

---

**VERDICT: PASS**

# G4 Gate Report — M5-T058 (D-078 substitution stamp; rule_evaluation contract 1.2.0)

## Pinned identity (reviewed head)
- Material: `7d02eddc` — "M5-T058 material (substitution stamp; loop-2 runs 21-25)"
- Reviewed sha: `817a6546…059da9` — `[ORCH-CORRECTED]` frozen-contract guard admits rule_evaluation 1.2.0
- Submit seam referenced: `5f7b032e` (verified reachable)
- Review method: pinned the exact frozen object. My isolated worktree HEAD is `d8b3899f` (an *ancestor* of the reviewed sha), so I extracted the reviewed tree via `git archive 817a6546 services/api packages/contracts` into the system temp dir and read production/web files via `git show 817a6546:<path>`. No disjoint peer commit could perturb the review because every artifact was read at the exact sha. Reviewed content is the frozen identity by construction.
- Scope confirmed at both commits: material `7d02eddc` touches exactly the 19 packet `allowed_paths` files and **no** forbidden consumer (evidence.py / scenario.py / scenario_analysis.py / condo_records.py / main.py / connectors / profile / scenario / rulesets / proposal_* untouched); `817a6546` touches only `tests/scenario/test_scenario_contract.py` (sanctioned guard fix, correctly routed outside producer scope).

## Commands run (read-only; cwd + outcomes)
All from the extracted reviewed tree `…/scratchpad/rev-817a6546/services/api`:
- `python -m ruff check .` → **All checks passed! (exit 0)**
- `python -m pytest tests/api/test_rule_evaluation_api.py tests/contracts/test_rule_evaluation_contract.py tests/rules/test_rules_integration.py tests/spatial/test_live_provider.py -q` → **168 passed in 10.26s** (matches evidence)
- `python -m pytest tests/scenario/test_scenario_contract.py -q` → **60 passed** (cross-suite guard confirmed green)
- Schema byte-identity (LF-normalized sha256, both copies): `0f7aafba…0ded19` == `0f7aafba…0ded19` — **identical**. Canonical schema carries closed enum `["1.0.0","1.1.0","1.2.0"]`, `$defs/substrate_substitution`, `condo_base_lot_unresolved` in the fail_safe_reason enum, `additionalProperties:false` throughout.
- Python 3.11.9. Full-suite root NOT run (per instruction; the documents-tree PEP-695/3.11 collection artifact is characterized). Web specs judged by reading only (thin client; they prove in CI at the reviewed head).

## AS → executable test mapping (each AS has ≥1 test)
- **AS-1** (headline end-to-end stamped result): `test_live_provider.py::test_m5t058_as1_resolved_single_carries_stamp_and_provenance`; `::test_m5t058_default_resolved_flag_on_carries_stamp_single_call`; `test_rule_evaluation_api.py::test_m5t058_route_stamps_substrate_substitution`; `test_rule_evaluation_contract.py::test_m5t058_stamped_1_2_0_document_round_trips`; `test_rules_integration.py::test_m5t058_confident_result_carries_substitution_stamp_verbatim`; web `contract-versions.test.ts` "accepts a 1.2.0 document carrying the optional substrate_substitution block".
- **AS-2** (honest refusal, no auto-pick, discriminator): `test_live_provider.py::test_m5t058_as2_condo_fail_safe_names_cause_no_auto_pick[multi-lot|unresolved|typed-error]` + `::test_m5t058_as2_genuine_absent_non_condo_keeps_no_condo_cause`; `test_rules_integration.py::test_m5t058_absent_substrate_condo_unresolved_names_honest_reason` + `::…_non_condo_keeps_generic_reason`; `test_rule_evaluation_api.py::test_m5t058_route_condo_unresolved_names_reason_no_stamp` + `::test_m5t058_route_non_condo_absent_keeps_generic_reason`.
- **AS-3** (compatibility, call-count, old-doc validity): `test_live_provider.py::test_m5t058_as3_build_live_substrate_delegates_substrate_only` (legacy seam stays object|None, single condo call) + `::test_m5t058_default_resolved_flag_off_empty_result_zero_calls`; `test_rules_integration.py::test_m5t058_absent_substrate_flag_default_is_pre_m5t058_byte_identical`; `test_rule_evaluation_contract.py::test_runtime_bundle_copy_is_byte_identical_to_canonical` + `::test_m5t058_optional_block_absent_still_validates_at_1_2_0` + `::test_m5t058_old_documents_stay_valid_without_the_block`. Forbidden-consumer byte-invariance corroborated by the scope check above + `test_scenario_contract.py` green (60).
- **AS-4** (web honesty): `analysis-identity-substitution.test.tsx` (both directions + non-correspondence + degenerate stamp); `contract-versions.test.ts` (1.2.0 decode, absence-tolerance, reason-enum admit/reject, block-shape enforcement); `development-limits.test.tsx` (condo label + non-condo discriminator).
- **AS-5** (provenance byte-match): `test_live_provider.py::test_m5t058_as1_…` asserts `stamp == build_substrate_substitution_stamp(...)` field-by-field (condo_key, resolution_path, source_id, dataset_ids, retrieved_at, mixed_substrate); `test_rule_evaluation_api.py::test_m5t058_route_stamps_substrate_substitution` asserts `block == stamp` verbatim; `test_rule_evaluation_contract.py::test_m5t058_stamped_1_2_0_document_round_trips`.
- **AS-6** (proof): reproduced here — ruff 0, targeted 168 green, scenario contract 60 green. Modularity 0 and CI-green-at-head are orchestrator-captured evidence (thin client; not re-run).

## Mutation-sensitivity per key behavior
- **Stamp on the wrong lot**: caught at two layers. `test_m5t058_as1_…` asserts the pipeline's `ztldb_calls`/`lot_calls` are on `_BASE_LOT` (not billing) AND `stamp["analyzed_bbl"] == _BASE_LOT`; the route test asserts `block["analyzed_bbl"] == "1000010050"` and `evaluated_input.bbl == BBL`. A builder that used input_bbl for analyzed, or a serializer that swapped entered/analyzed, or a pipeline that ran on billing, each fails.
- **Stamp omitted when required**: `doc["substrate_substitution"]` is asserted present (KeyError on omission); `result.substitution_stamp is not None`. Fails on omission.
- **Stamp emitted when not applicable**: AS-2 tests assert `substitution_stamp is None` / `"substrate_substitution" not in doc` on every fail-safe path; `test_m5t058_resolved_base_lot_absent_substrate_no_stamp` covers resolved-but-genuinely-absent. Fails on spurious emission.
- **Version selection / decode**: serializer emits 1.2.0 for every body (M5-T037 precedent — see `test_as3_confident_supported_family_is_200_draft` asserting `1.2.0` with no block), block is optional. Old-payload decode is pinned by `test_m5t058_old_documents_stay_valid_without_the_block` and web absence-tolerance tests; out-of-enum version rejected (`test_m5t058_contract_version_outside_enum_rejected`, web "rejects a contract_version outside the published enum"). See Observation 2.
- **Honest-refusal vocabulary**: `FAILSAFE_SPATIAL_ABSENT="spatial_intersection_absent"` vs `FAILSAFE_CONDO_BASE_LOT_UNRESOLVED="condo_base_lot_unresolved"` are distinct constants; tests assert *both the constant and the literal string* (a rename mutation is caught), and the non-condo discriminator proves the condo cause never leaks. Unknown reasons rejected by both contract and web validators (distinct pinning).
- **D-078-R002 no-auto-pick**: `test_m5t058_as2_…_no_auto_pick` asserts zero geometry/lot/layer lookups on multi-lot/unresolved/typed-error; route/integration assert `zoning_district is None`, `evaluations == []`, and reason prose "no base lot is auto-selected" / "site-definition confirmation".
- **AnalysisIdentityNotice guard (DB-036(d))**: the component's `stampLegitimate` requires `entered_bbl === requestedBbl === evaluated_input.bbl` AND `analyzed_bbl` a non-empty lot ≠ entered. The spec's non-corresponding cases (stamp for a different property; entered_bbl ≠ evaluated_input.bbl; degenerate analyzed==entered) each assert the substitution notice does NOT render and the withhold alert governs — a defect that trusts an uncorroborated stamp (weakening the safety guard) fails these.
- **live_provider degraded paths**: connector-failure/no-record/partial-page absent-substrate paths and the three condo fail-safe outcomes are all bound; single-SODA-call (no double resolution) asserted on both seams.

## Findings
None blocking. Two non-blocking observations:
- **Obs-1 (layering, not a gap)**: the route-level AS-1 test injects a pre-built `LiveSubstrateResult` rather than driving the real condo resolver through the endpoint. This is adequate — the seam-carry-with-correct-base-lot is proven at the `live_provider` layer and the route→resolved-provider wiring is proven by the injected stamp surfacing verbatim in the document. No end-to-end coverage gap results.
- **Obs-2 (prompt-premise clarification)**: the packet/mutation prompt phrasing "1.2.0 emitted only when the substitution block is present" does not match the actual (correct, M5-T037-precedented) design — the serializer emits `1.2.0` unconditionally and the block is optional. This is not a defect; decode/absence-tolerance for older payloads is well covered.

## Required corrections
None.

Test adequacy is comprehensive and mutation-sensitive across all six acceptance scenarios; the two schema copies are byte-identical; the honest-refusal vocabulary and the AnalysisIdentityNotice safety guard are both pinned in both directions; the forbidden consumers are byte-unchanged. G4 PASS.
