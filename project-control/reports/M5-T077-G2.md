# M5-T077 - G2 producer self-check record (recorded by the orchestrator, role self_check)

Producer: backend-engineer (orchestrator-dispatched subagent after the B-026 lane refusal) in wt-m5t077;
one in-worktree commit af332042, cherry-picked to f63cfad1 with every file MATCH by git blob identity.

- **Scope:** exactly the five allowed paths; services/api/app/scenario/max_envelope.py byte-untouched
  (git diff b56f3d5b..HEAD empty; blob f0abf884); route still unmounted; no dependency change.
- **Producer self-checks (report):** ruff clean; the documented pytest 64 passed (baseline 55 + 9 new);
  the wider tests/api + tests/scenario sweep 1329 passed (baseline 1320); modularity exit 0.
- **Mutation record (report):** (a) idiomatic `if lot.get(...)` weakening -> 5 falsy cases RED; (b)
  re-admitting None -> the null-refusal test RED; (c) blank BBL_UNRESOLVABLE detail -> RED; (d) `>` ->
  `>=` at the cutter -> the at-cap test RED; all reverted GREEN.
- **[ORCH-HARVEST] primary re-run at f63cfad1** (Python 3.11.9, cwd services/api): `python -m ruff check .`
  -> All checks passed!; `python -m pytest tests/scenario/test_lot_geometry_derivation.py
  tests/api/test_max_envelope_api.py -q` -> 64 passed.
- **Judgment calls disclosed by the producer:** the old absent-or-null derive test was renamed and its null
  case moved to the dedicated refusal test (per the orchestrator ruling); lot_geometry_derivation.py had no
  false null==absent claim, so its docstring output was satisfied additively.

G2 verdict: PASS (self_check; never independent review - G3 code-reviewer and G4 qa-engineer follow).
