# M5-T048 G1 — independent data-contract review (data-contract-verifier, saved VERBATIM from the agent return)

G1 DATA-CONTRACT REVIEW — M5-T048 — VERDICT: PASS (read-only; reviewed at pinned HEAD 15c1b3a4d1d592a0b62c4483bedd7a08aec16b50, confirmed).

EXECUTED REPRODUCTIONS (all green, at HEAD):
- Independent raw + LF byte-equality of the two schema copies: RAW sha256 A==B==271653ab...5def8 (identical, 33486 bytes, 543 CR each); LF-norm A==B==dc41be0e...ce4b9. Matches recorded digests.
- python .github/scripts/validate_contracts.py -> "Checked 11 schema file(s); 0 failure(s)." exit 0. It walks only valid/ and invalid/ (line 585), so semantically_invalid/ is deliberately outside the schema-must-reject convention.
- python packages/contracts/scripts/generate_ts_types.py --check -> OK, scenario TS byte-identical + client SUPPORTED_CONTRACT_VERSIONS matches enum. exit 0.
- pytest tests/scenario/test_scenario_contract.py test_scenario_proposal.py -> 112 passed; ruff on the 4 changed source files -> clean.
- Direct jsonschema probe: both semantically_invalid fixtures -> 0 schema errors (schema-valid confirmed).

FINDINGS (seven contract-gate concerns):
1. ADDITIVITY PASS. Enum ["1.0.0","1.1.0"] (closed-enum append). proposed_massing optional (in properties, NOT required) under preserved top-level additionalProperties:false. Generated-TS diff purely additive (contract_version union-widened + optional proposed_massing?; no field removed/retyped). All recorded 1.0.0 fixtures still valid. Block-present=>1.1.0 enforced in contract.py:147.
2. BYTE-EQUALITY PASS (reproduced raw, not just LF).
3. TS GENERATION PASS (--check byte-identical; diff is generator output, no hand edit).
4. THIRD INPUT CLASS (D-076-R002) PASS. kind schema-literal ["proposed"] + re-checked proposal.py:421; provenance stamp present; validate_proposed_massing returns None, derives no allowance (test_validator_returns_none_never_a_value, test_input_block_is_not_mutated); docstrings/schema descriptions state records/rules/proposals distinction; nothing presents a proposal as a city record or rule.
5. FIXTURE TAXONOMY PASS. valid/ (2 real docs, schema+semantic valid); invalid/negative_height schema-REJECTED via exclusiveMinimum:0 (CI-confirmed); semantically_invalid/ open_ring + self_intersecting schema-VALID (verified: _expected_failure is a sanctioned optional schema property; a random extra key IS still rejected) yet proposal.py-refused at proposed_massing.outline. Third dir justified by the CI walker scope.
6. VALIDATOR/CONTRACT COHERENCE PASS. proposal.py covers ring closure, non-self-intersection (O(n^2), adjacent-edge exclusion correct at line 178), distinct non-closing vertices, level-index contiguity {0..N-1}, wall index range+distinctness, NYC-2263 unit sanity, positive-finite heights, DB-013 ceilings — each a typed ProposedMassingError naming the exact dotted field. contract.py threads the block after schema+verified+strict-JSON guards WITHOUT weakening them (wraps to ScenarioContractError location=field).
7. SCHEMA-DRIFT DISCIPLINE PASS. M5-T048's material commits (0c7b17f1, 93f194c0) touched ONLY scenario.schema.json in both copies (+ its validator/tests/fixtures/report); no other schema file, no forbidden path. NOTE to preempt a false alarm: git diff main...HEAD also shows lot_geometry + rule_evaluation schema changes, but those are sibling accepted tasks M5-T047/M5-T037, NOT this task — absent from both M5-T048 commit stats.

ADVISORY (non-blocking):
- AS-3 wording residual (already disclosed by producer, routed to you). AS-3 literally asks each invalid shape to "fail schema validation," but ring closure / non-self-intersection are provably inexpressible in JSON Schema, so those two fixtures are schema-valid and refused only by the semantic gate — the correct data-contract outcome. Relocation to semantically_invalid/ + whole-gate refusal tests is the sound resolution; the open item is pure acceptance-criteria wording (Tier A/owner), not an implementation defect. The contract gate refuses all three defect fixtures.
- proposal.py is 457 lines, single-responsibility, stdlib-only — within the modularity warn threshold.

BASIS: The 1.1.0 bump is strictly additive and byte-identical across both schema copies; the optional proposed_massing block is schema-shaped with the D-076-R002 kind='proposed' literal and no derived value; proposal.py supplies exactly the cross-value/geometry invariants the schema cannot express with typed field-named refusals, threaded without weakening the existing gate; fixtures are correctly partitioned and the CI contracts job passes at 0 failures. No BLOCKING defect. VERDICT: PASS.
