# M5-T065 G4 gate — independent QA/test-adequacy review (verbatim reviewer return)

Reviewer: qa-engineer (claude-opus-4-8 per D-064), dispatched by the orchestrator at the
seq-124 T065 wave, pinned to the frozen submit head 65faee54 (material d3793c2e). Recorded
by the orchestrator; the reviewer is read-only and records no gate itself. The return
arrived in two parts (the first transmission truncated at the AS-5 mapping row; the
reviewer re-sent the remainder from that exact point) — joined verbatim below.

---

VERDICT: PASS

# G4 QA / Test-Adequacy Gate — M5-T065 (D-082 map-drawing input slice)

**Reviewer:** qa-engineer (independent; read-only). **Verdict: PASS** — no blocking gaps. Acceptance scenarios map to concrete, mutation-sensitive tests; the load-bearing correctness property (fit residual is *necessary but not sufficient*) is proven; preservation of accepted T060 behavior is verified. Non-blocking coverage gaps are recorded below as recommended follow-ups.

## Identity verification
- Frozen submit head `65faee54`; material `d3793c2e` over base `f98924e5`. Live HEAD is `9a3d4883` (control-plane T063/T062 seam commits, disjoint from the 12 task paths).
- `git diff 65faee54..HEAD` over all 12 allowed_paths is **empty** — working tree equals the frozen surface. **My verdict binds to the frozen path-scoped content.**
- Material diff touches exactly 12 files (the packet's allowed_paths); no forbidden/held/live path present. Confirmed via `git show --stat d3793c2e`.
- Local evidence I re-ran at this tree: `python -m pytest tests/api/test_outline_bridge.py -q` → **38 passed**; `python tools/modularity_check.py --check` → **exit 0**. Web behavior proves in CI only (thin client); the AS-4 e2e journey was CI-green at `a3173987` / `93b5943a` per the orchestrator record, after an [ORCH-CORRECTED] shared-helper fix in `e2e/helpers.ts` (outside the 12 task paths; the task's own e2e spec content is byte-stable).

## Acceptance scenario → test mapping

| Scenario | Tests | Assessment |
|---|---|---|
| **AS-1** drawing: place/adjust/delete by keyboard; focus-managed on delete (DB-043(a)); proposed labeling | `proposal-outline-draw.test.tsx` (keyboard input + edit + adopt; focus-on-delete lands on a delete control, falls back to Add when list empties; "not a city record" label) | **COVERED for the delivered surface.** Mouse *click-to-place on the map* is NOT delivered — governed by the recorded [ORCH-SCOPE-DISPOSITION] (map-CLICK → M5-T066). Per the binding ruling I do not fail on the disposition; residual noted below. |
| **AS-2** bridge honesty + typed refusals; ambiguous emits no coordinates | `test_outline_bridge.py`: unit `fit_correspondence` (unique/cyclic-shift/reversed recovery, symmetric-ambiguous, triangle too-few, over-cap, count-mismatch, collinear) + route (provenance surfaced; residual-too-high; out-of-neighborhood; source-unavailable; symmetric refuses with `"vertices" not in body`) | **STRONG, mutation-sensitive.** The central "low residual ≠ correspondence" proof (symmetric square: RMS ≈ 0 yet `separation < bound` → refused, no coordinates) is explicitly exercised. Deleting either the residual gate or the ambiguity gate fails a test. |
| **AS-3** no-reprojection doctrine: zero new deps, no CRS math | Independently grep-verified: `pyproj\|Transformer\|Lambert\|to_crs\|proj4` over the 4 code paths hits only doc comments asserting *absence*; no requirements/lockfile/pyproject/package.json in the material diff | **VERIFIED.** This is a negative-doctrine constraint that a green functional test cannot prove — the grep + manifest-untouched check is the adequate evidence, and it holds. |
| **AS-4** adoption + manual; T060 specs stay green unchanged | Unit `adoptOutlineVertices` (replaces outline, preserves levels/walls, immutable); `proposal-editor.test.tsx` integration (5 seed vertices → draw 3 → convert via stubbed bridge → numeric table now the 3 bridged 2263 vertices); e2e AS-4 (bridge POST asserts srid 4326 + ≥3 before responding; adopted table shows Vertex 0 X=1000020 / Vertex 2 Y=200030; re-check POSTs exactly the adopted 2263 vertices) | **STRONG.** T060 spec files are purely additive — `git diff --numstat` shows **0 deletions** in `proposal-editor.spec.ts` (+133), `proposal-editor.test.tsx` (+60), `proposal-draft.test.ts` (+21), `proposal-draft.ts` (+14). |
| **AS-5** decode matrix, distinct refusals | `outline-bridge-api.test.ts`: full documented (status,state) matrix; residual/neighborhood/ambiguous/invalid decoded distinctly; residual verbatim (0.0004 passes through un-rounded); "never route by state alone" (418+`residual_too_high` → `unexpected_response`); malformed 200 (missing correspondence / non-finite vertex / wrong srid) → `validation_failure` | **STRONG, mutation-sensitive.** `isDocumentedBridgePair` positive+negative asserted; `crs` bounded-as-text (keeps `:`) proven distinct from tokenization. |
| **AS-6** preservation; local gates green | forbidden paths absent from diff; ruff/pytest/modularity green (re-run below); held T063 + live T062 surfaces untouched | **VERIFIED.** |

## Coverage gaps (non-blocking; recommended follow-ups)

1. **MODERATE — client browser-fault branches untested.** `fetchOutlineBridge`'s `network_error` (fetch throws), `client_timeout` (timer fires), `aborted` (external signal), and the over-budget `Content-Length > MAX_RESPONSE_BYTES` rejection are never produced by a vitest. Only `outlineBridgeOutcomeIsRecoverable` is tested against a *hand-built* `network_error` object, and the module comment claims the timeout is "provable in CI" without a test that triggers it. Recommend unit tests: a throwing `fetchImpl` → `network_error`; a `timeoutMs: 0` + slow `fetchImpl` → `client_timeout`; a pre-aborted external signal → `aborted`.

2. **LOW-MODERATE — server (500, internal_error) path unproven.** The three 500 branches (`ring_crs_mismatch`, the generic `except Exception` at the fetch stage, `serialization_unsafe`) have no route test, and `test_documented_status_state_matrix` does not assert `(500, "internal_error")` is in the matrix. A mismatched-CRS `ParcelRing` seam (e.g. auth ring tagged `EPSG:4326`) would exercise `ring_crs_mismatch` cheaply and distinguish 500 from the 502 source-fault path.

3. **LOW — smaller request/UI branches.** `not_object` (empty/array/scalar body) and `non_finite` request reasons, `srid: "4326"` string acceptance, a neighborhood *boundary-adjacent* vertex (only a far-away vertex is tested), the convert button's disabled/"Converting…" state, and the `max_residual_ft` value are each unexercised.

## Notes (not gaps)
- `outline_bridge.py` is 825 lines and shows as a modularity **`warn review_signal`** (above the 600 warn threshold, below the 750 justify / 1000 hard thresholds — exit 0). The file has a clearly documented three-responsibility boundary (connector I/O adapters / pure correspondence arithmetic / HTTP handling) with the pure math exported for isolated unit testing; cohesive but approaching the boundary — a future addition should split rather than grow it.
- AS-1 residual: the delivered "drawing" surface is an Add-point button plus keyboard lng/lat entry (labeled proposed input), not click-to-place on the map. This matches the recorded scope disposition; the map-click interaction layer is M5-T066. Flagged for the human-journey reviewer as a copy nuance ("Place points over the lot below" implies map interaction that is actually numeric entry), which is a G3 concern, not a G4 test-adequacy failure.

**Commands run:** `git rev-parse` / `git diff 65faee54..HEAD -- <12 paths>` (empty) · `git show --stat d3793c2e` (12 files) · `git diff --numstat f98924e5 d3793c2e -- <T060 specs>` (all 0 deletions) · `cd services/api && python -m pytest tests/api/test_outline_bridge.py -q` (38 passed) · `python tools/modularity_check.py --check` (exit 0) · `git grep` for `pyproj|Transformer|Lambert|to_crs|proj4` (doc-comment hits only) and for dep manifests (none).

VERDICT: PASS — no blocking gaps; the three coverage gaps above are recommended non-blocking follow-ups.
