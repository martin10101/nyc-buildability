# M4-T011 — G0 administrative contract check (orchestrator)

Packet completeness verified against the /start-controlled-task checklist before claim:

- Requirement identifiers: the six named CI failures (run 34664497740 api job, duplicated in
  exact-production-install), M4-T009's applicable-trace adaptation idiom (its wave-passed
  tests/rules siblings), and the family-evaluation contract in app/rules/integration.py.
- Directive refs: D-038:ALL (in-regime; empty-applicable row expected at accept per the
  M2-T021/M4-T010 precedent).
- Evidence files: named exactly in inputs[] (CI run id, the two production selection sites
  verified correct before contracting, the sibling-idiom source).
- Scope: EXACT-FILE allowed_paths (the four broken consumer test modules + this packet's
  report); services/api/app/** forbidden (production verified correct; a discovered production
  defect is a stop-and-report); disjoint from M4-T009 (rules/rulesets + tests/rules) and every
  other open packet.
- Acceptance scenarios S1-S5: red already recorded (CI + local repro), value-identity diff rule,
  applicability-keyed selection with loud zero/multi failure, growth-proof count, whole-suite
  green - executable, anti-tautology forms required.
- Gates: G0/G1/G4 with code-reviewer + qa-engineer, both distinct from the producer. G3 not
  required: no contract changes (the endpoint documents/contracts are untouched; the change is
  test-side selection); G5 not required: no security surface (test-only diff).
- Modularity boundary answers: owning responsibility = the api acceptance packs' trace
  selection; owning modules = the four named test files; all under thresholds; no extraction;
  no public interface involved; modularity check runs before submission.

G0 verdict: PASS (administrative; recorded by the orchestrator per gate-class rules).
