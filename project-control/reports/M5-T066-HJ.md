# M5-T066 — Human-journey review record (human-journey-reviewer return, VERBATIM)

[Orchestrator record header — not part of the reviewer return: FRESH full re-walk dispatched
2026-09-21 ~06:14 UTC at frozen 84132df56047bae7097b4172575ef75099f8a43e per the seq-124
handoff disposition (the pre-handoff HJ delivered VERDICT PASS but its verbatim report
transmission was lost mid-parts; that partial is NOT preserved as evidence — this fresh
complete walk supersedes it entirely). Return received COMPLETE in one part with the required
END-OF-REPORT marker present; transport entity-decoding only (&gt;/&amp; sequences decoded).
Verdict PASS; six advisories (HJ-1..HJ-6), zero blocking findings.]

---

VERDICT: PASS (advisory findings only; none blocking)

# M5-T066 — G3 Human-Journey Review (fresh, complete re-walk)

Task: D-082 map-CLICK interaction slice — click to place/select/move/delete outline vertices on the lot map, feeding the accepted drawing→bridge→adoption path, plus fold-ins HJ-1/HJ-2/HJ-4. Frozen submit head `84132df5`; material `abfd182b`; CI head `2578329c`. This walk does not reference any prior (lost) HJ report for T066.

## Identity / evidence honesty (verified via git plumbing)
- Material `abfd182b` is an ancestor of BOTH the CI head `2578329c` and the frozen head `84132df5`; `git diff 84132df5..2578329c` over the task source files is EMPTY, so the recorded CI SUCCESS validated exactly the frozen source. Order: `abfd182b`(material)→`2578329c`(CI)→…→`84132df5`(frozen); post-material commits touch only state/task/evidence.
- Thin client: I did not run npm/node; I read the frozen source and tests and rely on the orchestrator-validated CI SUCCESS at `2578329c` for runtime/mutation behavior. Cross-referenced G3 (code-review) and G4 (QA) — both PASS, advisory-only.

## The journey (as an architect drawing an outline)
- AS-1 place / one model — clicked positions travel raw `{lng,lat}` (display 4326) from `clickEvent.lngLat` into the SAME `points` state the keyboard `addPoint` fills (`placePoint`, ProposalOutlineDraw.tsx:101). Overlay stays in sync via `setData`. Drawn shape labeled "Proposed — your sketch, not a city record" (ProposalOutlineDraw.tsx honesty header). PASS.
- AS-2 select/adjust/delete + keyboard-equivalent + focus — vertex-hit routes to select/deselect (aria-pressed toggle); empty-map click moves the selected point else places; delete keeps focus on a delete control via the DB-043(a) nonce+useEffect remedy (ProposalOutlineDraw.tsx:82,131) AND reconciles `selectedIndex`. Notably this NEW drawing surface applies the delete-focus remedy that my T060 note flagged as missing in the numeric table. PASS.
- AS-3 display-surface safety — interaction gated purely by `interactive = onOutlineMapClick != null` (LotOutlineMap.tsx:376); the real `AddressConfirmCard` consumer passes only `bbl`+`context`, so no listener/overlay is added — byte-equivalent. PASS.
- AS-4 e2e — the pointer journey (proposal-editor.spec.ts "AS-1 pointer") places 3 real canvas clicks → bridge (stub asserts srid 4326, ≥3) → adopt → check (stub asserts adopted 2263 vertices); the keyboard AS-4/AS-5 journeys are retained unchanged. PASS.
- AS-6 adoption reconciliation — `adoptOutlineVertices` (proposal-draft.ts:313) drops walls that would dangle on a smaller-count adoption; equal/larger keeps all; mutation-pinned; `adoptDrawnOutline` announces exactly which walls dropped. PASS.
- Honesty bar — clicked positions stay display-4326, no client measurement/CRS math anywhere; "Nothing is measured from it; your drawing is converted to official-grid feet on the server." Server bridge is the only conversion path. HOLDS.

## Advisory findings (none blocking)
- HJ-1 (LOW-MED, clarity/recovery): ProposalOutlineMap's instruction "Click the lot map to place a proposed outline point…" always renders even when no interactive map exists. The click listener lives inside the map-build effect gated on drawable geometry (single_lot + WebGL). For a condo unit lot (no polygon — common in NYC), `multiple_features`, `invalid_geometry`, or a no-WebGL browser, LotOutlineMap renders a typed fallback and no clickable map, yet the copy still tells the user to click one. The same sentence offers the keyboard table so it is recoverable, but the lead is stale in the no-map case. Anchor: ProposalOutlineMap.tsx `proposal-outline-map-instructions` paragraph (not gated on map render state).
- HJ-2 (LOW): Convert enables by point COUNT only — `canConvert = drawnCount >= MIN_DRAWN_VERTICES && !converting` (ProposalOutlineDraw.tsx:148). Three keyboard rows Added but not yet typed (NaN) enable Convert → a doomed bridge round-trip (JSON `null` coords, server refuses). Also the wrapper status announces "3 points drawn" while the overlay filters non-finite points and shows none — a count/overlay divergence. Recoverable via the honest refusal card; a finiteness gate + clearer hint would spare it. Same family as my T060 HJ-3 (draftIsRunnable not wired). Pointer path is immune (clicks are finite).
- HJ-3 (LOW, a11y verbosity): the drawing subtree stacks several `role="status"` live regions — wrapper count/selection (`proposal-outline-map-status`), `lot-outline-summary`, context-mode `architect-map-status` layer-loading, the OutcomeAnnouncer, and the min-hint — so a single place/select can fire competing SR announcements. Consider consolidating.
- HJ-4 (LOW, clarity): the dropped-wall announcement names internal ids ("Removed 2 walls that referenced deleted vertices (W-N, W-W)…", ProposalEditor.tsx:124) — opaque to a non-developer. G4 also noted this announcement is untested (its coverage gap #1).
- HJ-5 (NIT): to MOVE a selected point you click empty map, but a click landing on the selected point's own circle routes to deselect (vertex-hit → onSelect toggle), so a small nudge onto the point deselects instead of moving; the copy says "click the map to move it."
- HJ-6 (NIT): 0-based point numbering in the selection status/table ("Point 0 is selected") reads oddly to humans but is internally consistent with the accepted numeric vertex table — leave as-is.

## Carryover (not T066 defects)
- My T060 HJ-2 (MEDIUM): delete-focus is still unmanaged in ProposalEditor's numeric vertex-table row delete — untouched here and out of scope; the new drawing surface correctly applies the remedy.
- Bridge refusal cards (residual_too_high etc.) state the reason + "No coordinates were produced" but give no explicit "redraw more accurately and convert again" next step (T060 HJ-3 family). Code-review CR-1 (street_lines not reconciled) is latent-unreachable (no street-line editor/seed).

## Bottom line
The slice delivers the map-click interaction honestly: one draft model shared by pointer and keyboard, display-4326 positions with all measurement server-side, additive-optional prop keeping the accepted address-confirm display byte-equivalent, keyboard parity with the DB-043(a) delete-focus remedy now present in the drawing surface, and mutation-pinned adoption reconciliation. CI SUCCESS validated the exact frozen source. No blocking human-journey defect. Six advisories are clarity/verbosity/round-trip-sparing improvements for a future B-phase increment.

END-OF-REPORT M5-T066-HJ
