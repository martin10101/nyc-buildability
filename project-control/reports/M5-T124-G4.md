# M5-T124 — G4 test review (qa-engineer "qa-t124", read-only)

> Transmission history: pinned at f2986fac (material c65a0724; CI green at f2986fac), delivered as 2 SendMessage parts
> ending with END-OF-REPORT; no truncation. Joined verbatim by the orchestrator (transport wrapper tags removed only).
> Verdict PASS; property round-trip 60/60 at 2263 magnitudes, 6 of 6 independent mutants caught; three ADVISORY items
> (A1 an unreachable non_finite_result path, A2 fixture notes, A3 a loose residual assertion).

---

M5-T124 G4 independent test/regression review (qa-engineer, READ-ONLY) — PART 1/2.

PIN: HEAD f2986fac26948af907cb0a3c2cd118e74279470f (candidate/D-024-mrl-option-b). Material c65a0724 = only the 3 allowed paths (drawing_alignment.py NEW, test_drawing_alignment.py NEW, producer report); no forbidden path touched.

RAN (cwd services/api, local py3.11.9):
- Producer suite: 39 passed (`pytest tests/drawings/test_drawing_alignment.py -q -p no:cacheprovider`).
- Regression siblings: test_sheet_import.py + test_dxf_import.py = 60 passed; unchanged by this commit (last touched by M5-T121/M5-T108).
- ruff on both new files: All checks passed. modularity --check EXIT=0 (drawing_alignment not flagged; SLOC<600).
- CI at f2986fac (authoritative py3.12): `api (ruff+pytest)` completed success; all check-runs success.

AS COVERAGE — each asserts the property, not just runs (cite test:line):
- AS-1 fit: pure translation lands EXACT 2263 vertices (L110-123); 90deg + 31.7deg land on true-transform vertices abs 1e-6 (L136-176); proper-motion orientation preserved (L196-203). Formula cited drawing_alignment.py L24-31, _rigid_fit L248-254.
- AS-2 disclosed-never-reconciled: per-pair residuals + max vs tol (L209-224); 2-pair unchecked (L238-241); implied-scale=2 disclosed AND edge length preserved = not applied (L265-274); better mirror disclosed AND orientation preserved = not flipped (L293-301); outside-lot 300 sqft disclosed AND full 5-vertex extent kept = not clipped (L331-345).
- AS-3 contract: re-validated, kind stays proposed (L367-372); still-out-of-bounds -> typed aligned_invalid naming proposed_massing.outline (L375-386); alignment block complete 18 keys (L401-413); per-level outline transformed (L416-424).
- AS-4 honesty: precision kept exactly + echoed (L430-433); no permit/approved/maximum-allowed anywhere in the JSON blob (L443-452); every number finite (L455-463).
- AS-5 typed refusals: too_few/too_many/non_finite/coincident/indeterminate/bad_control_pair/bad_draft_block/bad_lot_ring/over-cap each assert reason== (L469-537); purity: idempotent, input not mutated, no io/network (L543-563).

FIXTURE REALISM — VERIFIED against source:
- _block() == real build_draft output shape (sheet_import.py L576-590, dxf_import.py L518-528): outline{srid,vertices}, levels[{level_index,floor_count,floor_to_floor_ft}], exterior_walls[], provenance{author,editor_version,kind:proposed}.
- srid 2263 == _REQUIRED_SRID (both importers). Precision fixture string == DRAFT_INPUT_PRECISION (dxf_import.py L76) EXACTLY.
- Closed ring (first==last), >=4 positions, in-bounds after transform — matches validate_proposed_massing (proposal.py L233, L254-272).
- Lot rings at real 2263 magnitudes (BASE 1e6, 2e5).

PART 2 (my property harness + independent mutants + verdict) follows.

---

M5-T124 G4 review (qa-engineer) — PART 2/2.

INDEPENDENT PROPERTY CHECKS (my harness, in-process, piped to python via stdin — nothing written to the repo):
- P1 round-trip: a random rigid transform (rotation -2.9..2.9 rad + in-bounds translation) of a random simple polygon is recovered EXACTLY from exact pairs, 12 seeds x {ok, vertices maxerr<1e-3, rotation<1e-6, max_residual<1e-3, implied_scale~1} = 60/60 pass. Confirms centring/numerical stability at 1e6-ft magnitudes. A collinear-but-distinct source set also recovers exactly.
- P2 noisy: a single injected d-ft target error yields max_residual of the injected order (0.3d<=max<=d), per-pair count preserved, residual_exceeds_tolerance fires — 3 seeds pass.

INDEPENDENT MUTANTS (6, distinct seams/assertions from the producer's 10) — ALL CAUGHT:
- M-A skip centring (dot/cross on raw ~1e6 coords) [_cross_dot_sumsq] -> CAUGHT: 90deg fit corrupted.
- M-B measure residuals before applying translation [_residuals] -> CAUGHT: a perfect fit reports a huge residual.
- M-C let a better mirror win silently [_choose_transform] -> CAUGHT: applied orientation flips.
- M-D drop the outside-lot disclosure [_outside_lot_area->None] -> CAUGHT: 300 sqft overhang hidden.
- M-E accept 1 pair [MIN_CONTROL_PAIRS=1] -> CAUGHT: too_few_pairs no longer raised (defense-in-depth: coincident_source still refuses the 1-pair fit).
- M-F negate the rotation angle [_compose sin] -> CAUGHT: 31.7deg fit wrong.

The applied transform is provably always a proper rigid motion: _choose_transform L300-309 returns rigid unconditionally, _applied_scale L225-229 = 1.0, _final_outline_vertices L312-316 = identity — scale/mirror/clip are computed only to DISCLOSE (_build_alignment_block L519-595). Fit degeneracy is typed-refused before use: saq==0 -> coincident_source, hypot(dot,cross)==0 -> indeterminate_rotation (L646-659).

FINDINGS:
BLOCKING: none.
ADVISORY (non-blocking; no change required for PASS):
- A1: the non_finite_result path (L690-695) is unreachable-by-construction (non-finite inputs, saq==0, hypot==0 all refused earlier) and has no red-path test — acceptable defense-in-depth.
- A2: the fixture frame_note is a shortened form of the real FRAME_NOTE, and the per-level-outline fixture (L416-424) adds geometry build_draft does not emit today — both harmless (the aligner reads only provenance.precision; the per-level transform is forward-defensive and routes footprint=False).
- A3: the producer residual test asserts max_residual>1.0 (loose); my P2 tightens it to the injected order. Not a defect.

Coverage is thorough, fixtures faithful to the real contract, the math property-verified at real 2263 magnitudes, every discrepancy disclosed-never-applied (mutation-proven), typed refusals complete, honesty invariants held, sibling suites unaffected.

M5-T124 G4 VERDICT: PASS

END-OF-REPORT
