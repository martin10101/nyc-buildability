<!-- Saved VERBATIM by the orchestrator from the qa-engineer agent-return channel
     (report-preservation rule, 2026-07-16). Transport entity-decoding only: the
     harness neutralized ">" as "&gt;" and "<" as "&lt;" in three spots; decoded back. -->

My independent review is complete. Here is the full G4 gate report.

---

# G4 Independent QA Gate Report — M0-T094 (D-024 unit G: operator channel)

- **Task:** M0-T094 — "D-024 F: operator channel (one-command start, status, zero-context /loop-* commands, ask-Codex)"
- **Reviewer role:** qa-engineer (independent; producer = fable-orchestrator-session). Producer ≠ reviewer confirmed.
- **Reviewed at frozen SHA:** `3c4b888fea6a117e7517612aa1250a50c7c57759` (branch HEAD). Code deliverable `f6edf11d74d5e126439ce804750bca4c1ccd7fa5`; the only diff `f6edf11 → 3c4b888` is `project-control/tasks/M0-T094.json` (+10/-3, control-plane submit event) — confirmed via `git diff --stat`. Code identity under review is `f6edf11`, parent `8ae669b`.
- **Environment:** strictly read-only on the repo. Extracted the frozen tree to a scratch dir via `git archive 3c4b888 | tar -x` (no repository modification); ran pytest there. Python 3.11.9. All mutation spot-checks were applied to the scratch copy only and restored.
- **Scope of this gate:** whether the test evidence actually proves the S1–S14 scenario pack (the 6 assigned checks). Directive requirement-by-requirement verification is the DCV's pass, recorded separately.

## Commands run and outputs (reproducible)

| Command (from the extracted frozen tree) | Result |
|---|---|
| `python -m pytest tools/test_agent_supervisor_operator_channel.py -q` | **52 passed in 19.14s** |
| `python -m pytest tools/test_agent_supervisor_command_authority.py tools/test_agent_supervisor_controller_succession.py tools/test_agent_supervisor_start_reentry.py -q` | **120 passed** |
| `python -m pytest tools/test_agent_supervisor_reviewer.py tools/test_agent_supervisor_phase1.py -q` | **161 passed** |
| Spot-mutant M8 (`operator_status.unknown()` → `fact(0, …)`), run `S2StatusSection14` | **1 failed, 4 passed** — killed by `test_absent_facts_are_unknown_never_zero` (`AssertionError: 0 != 'unknown'`) |
| Spot-mutant M6b (drop trailing `$` in the hook `_COMMAND`), run `S10ExactMatchOnly` | **1 failed, 1 passed** — killed by `test_similar_text_passes_through_untouched` (`/loop-statuses` now emits a block) |
| Re-run full matrix after restoring both mutants | **52 passed in 18.53s** |

Independently reproduced green: **333 tests** across the unit + adjacent regression surfaces at the frozen tree, plus 2 confirmed mutant kills.

## Check 1 — Re-run the matrix and affected suites

PASS. The matrix produces **52 passed** (matching the packet `progress_log` and G2 self-check "52/52"). The moved CLI helpers (`_open_runtime`/`_emit` relocated into `operator_channel_cli.py`) do not regress `command_authority`/`controller_succession`/`start_reentry` (120 passed) or `reviewer`/`phase1` (161 passed). `cli.py` wiring is exercised behaviorally via the matrix's `cli_run` helper (status/graceful-stop/ask CLI paths).

## Check 2 — Scenario-to-test mapping (S1–S14)

Every pack row maps to concrete methods in `tools/test_agent_supervisor_operator_channel.py`:

| Row | Methods | Layer | Assessment |
|---|---|---|---|
| S1 start/alias | `S1StartSurface` (3) L203-220 | parser default + doc + source-grep | Behavioral for "no duration"; idempotency is an R018 **reuse citation** (`inspect.getsource` "SingleInstanceLock" L220) — real idempotency lives in `start_reentry` (ran green). Acceptable, see ADVISORY-2. |
| S2 section-14 status | `S2StatusSection14` (5) L236-298 | behavioral vs `compose_status` | Strong. Asserts all 16 facts present+labeled, unknown≠0, persisted labels preserved, campaign summarized, CLI `--json`+concise. |
| S3 durable controls | `S3DurableControls` (5) L307-358 | behavioral + source-order | Durable-record + clear round-trip behavioral; **ordering is a source-index proxy** (L328-338) — see ADVISORY-2. Emergency>graceful precedence behavioral (L346-349). |
| S4 ask synchronous | `S4AskSynchronous` (3) L367-416 | behavioral (fake runner) | Strong. Verifies `--sandbox read-only`, `--ephemeral/--ignore-user-config/--strict-config`, question on **stdin not argv** (L398), audit digests not raw text. |
| S5 ask fallback | `S5AskDurableFallback` (4) L425-499 | behavioral | Strong. One durable row, resubmit same-row, second-timeout single row, `already_answered` guard, CLI `--show`. |
| S6 bridge security | `S6BridgeSecurity` (12) L508-618 | behavioral | Strong (see Check 3). |
| S7 skills thin | `S7Skills` (3) L637-659 | file/frontmatter | Real: `disable-model-invocation:true`, thin (<3000 chars), names CLI, no `/loop` collision, no `/btw`, documents interceptor + second-terminal. Confirmed by reading `loop-status/SKILL.md`. |
| S8 feature detection | `S8FeatureDetection` (4) L671-697 | fixture + hook source + settings | Real. Fixture selects measured path; hook consults fixture; unproven expansion passes through (subprocess); settings registration present. |
| S9/S11 honest measurement | `S9S11HonestMeasurementState` (2) L706-721 | fixture assertion | See Check 6 — asserts honest `pending-owner-C1`. |
| S10 exact-match only | `S10ExactMatchOnly` (2) L741-754 | behavioral subprocess | Strong; 8 near-miss variants incl. `/Loop-Status`, `/loop-statuses`, `/loop`. |
| S12 hook fail-closed | `S12HookFailClosed` (4) L786-834 | behavioral subprocess | Strong: malformed payloads no-op, failing supervisor blocks with "exit 7", ask-no-env fail-closed, hung supervisor killed. |
| S13 no worker pollution | `S13NoWorkerPollution` (2) L843-865 | behavioral + source-grep | `assert_worker_text_clean` on packet; hook source has no `additionalContext`/`hookSpecificOutput`; interception emits only `{decision,reason}`. |
| S14 identity/Gate-0 | `S14Identity` (3) L874-909 | behavioral | Module/CLI/hook identity refusals with typed exits 11/13. |

No row is only nominally covered. The four source-grep/source-order proxies (S1, S3, S6.ask-reuses, S13) are each paired with a behavioral test of the load-bearing effect; noted as ADVISORY, not a defect.

## Check 3 — R111 named cases each have a real failing-mode test

PASS — all present and behavioral:
- UserPromptSubmit exact-match / block / display: `S10.test_the_exact_command_is_intercepted` (L748), `S12`/`S14` assert `decision=block` + reason content via real subprocess.
- **erase**: mechanism (`decision:block`, no context-injection keys) asserted at `S13.test_the_hook_never_injects_context_into_the_model` (L853-865); the *live* zero-context measurement is honestly deferred (Check 6 / ADVISORY-3).
- similar-text non-interception: `S10.test_similar_text_passes_through_untouched` (L741) — independently proven load-bearing by my M6b mutant kill.
- `/loop-status` without model call: `S7` `disable-model-invocation:true` + hook runs CLI directly.
- `/loop-ask` read-only reach: `S4.test_the_invocation_is_the_read_only_reviewer_contract` (`--sandbox read-only`, L393-394) + `S12` hook ask fail-closed.
- empty/large/Unicode/quoted/multiline/metacharacter safety: `S6` L508-522 (`sanitize_question` bounds + data-preservation) with the question travelling on stdin (L398), so metacharacters are data not shell.
- timeout single-request: `S5` (one row on timeout, `outcome.tree_terminated`, resubmit same row).
- hook fail-closed: `S12` (malformed/failing/hung).

## Check 4 — Mutation-pass adequacy and the M6 equivalence

PASS. The 10 killed mutants target the load-bearing invariants (size bound, control-strip, redaction, durable-queue-on-timeout, resubmit identity, token boundary M6b, hook identity, unknown≠zero, clear authority, hook-ask fail-closed) — each has a corresponding real test, verified by reading the implementations (`operator_ask.py`, `operator_status.py`, the hook).

**M6 equivalence is correct.** The pattern (`loop_command_interceptor.py:53-55`) is `^/(loop-(?:…))(?:\s+([\s\S]*))?$` used with `.match` (L139), and there is **no `re.MULTILINE`**. Without MULTILINE, `^` matches only at offset 0, so `.search` cannot start anywhere but position 0 — making `.match` and `.search` behaviorally identical here. The boundary that actually rejects `/loop-statuses` is the `$` end-anchor + `\s+` gate, which is the property M6b targets. I independently confirmed both halves: removing `$` makes `/loop-statuses` intercept and **kills** `S10`; the `.match→.search` swap is genuinely equivalent. No obviously untested *critical* branch found. Minor untested non-critical branches (ADVISORY-4): `resolution.usable==False` in `run_ask` (L296-300), the `provider_failure_reason` guard in `_read_answer_file` (L246), and `validate_answer` refs coercion (L207-209) — none security- or durability-critical.

## Check 5 — Composed-baseline validity

PASS (as the freeze-rule baseline, with CI as the confirming run). `project-control/reports/M0-T094-full-suite-T1.txt` records a chunked same-tree composition — 4 chunks + a dedicated `test_directive_compliance.py` run + 2 probes = **2971 passed / 3 skipped / 0 failed** across 80 files, arithmetic verified (844+900+787+173+124+120+15+8=2971; 3 skips = chunk2's 2 + chunk4b's 1). The tree note "HEAD 8ae669b + unit-G working set" is content-consistent: `f6edf11`'s parent is `8ae669b` and the working set is exactly the 22-file unit-G diff. The G2 honesty note (`M0-T094-G2-self-check.md`, "Full-suite baseline") discloses that a first background run was **discarded** because the in-flight mutation pass had briefly mutated on-disk production files that the hook subprocess tests read at runtime; the recorded run started only after all mutant sites were verified restored (matrix 52/52 on the restored tree). Single-run kills/timeouts are disclosed. This is a legitimate, honest composed baseline meeting the M0-T039 duty (≥1165 tests / 0 failures). Caveat (ADVISORY-5): the composed log is a *producer* artifact and the independent single-run confirmation is the CI supervisor-bridge job on the pushed SHA, which I cannot observe from the read-only sandbox; I corroborated greenness by independently reproducing 333 tests over the unit + adjacent surfaces at the frozen tree. The orchestrator should confirm the CI supervisor-bridge job is green on `3c4b888` before/at acceptance.

## Check 6 — Owner-gated C1 rows (S9/S11) assert the honest pending state

PASS. `fixtures/loop_interception_detection_2_1_248.json` records `zero_context_proof.status = "pending-owner-C1"` (L21) and `queued_input_behavior.status = "pending-owner-C1"` (L26), each with the second-terminal fallback and (for zero-context) a recorded `measurement_method`. `S9S11HonestMeasurementState` (L706-721) asserts the status is one of `("measured-live","pending-owner-C1")`, and when pending, requires the `second-terminal` note + a measurement method / "NOT advertised as real-time" text. No measurement is faked. The fixture also correctly distinguishes the *measured-live* payload from the *documented-but-unmeasured* response/erasure contract (L10-12 vs L20-24), and `S8.test_the_installed_version_fixture_selects_the_measured_path` (L671) asserts `"measured-live"` on the payload only.

## Output-deliverable verification (task.outputs — prior gate lesson)

All four named outputs exist at the frozen SHA and match their claims:
1. Canonical start/status/control/ask CLI — `cli.py` (+57/-30) + `operator_channel_cli.py` (244 ln) + `operator_ask.py` (400 ln) + `operator_status.py` (279 ln) + `durable_state.ask_by_id` (+18); exercised by S1–S5.
2. Feature-detected `/loop-*` interception (+ honest fallback) — `.claude/hooks/loop_command_interceptor.py` (232 ln) + fixture; zero-context proof honestly deferred.
3. `tools/test_agent_supervisor_operator_channel.py` — 913 ln, 52 tests, includes metacharacter/Unicode/timeout-single-request/hook-fail-closed.
4. `project-control/reports/M0-T094-operator-channel.md` — present, §4.4 lists exact owner commands.

Scope is clean: `git diff --stat 8ae669b f6edf11` shows all 22 changed files inside `allowed_paths`; the untouchable guard hooks (`agent_dispatch_guard.py`, `readonly_agent_guard.py`) are **not** in the diff; `.claude/settings.json` is a purely additive `UserPromptSubmit` registration (+5 lines).

## Findings

- **BLOCKING:** none.
- **MAJOR:** none.
- **MINOR-1 (report provenance):** `project-control/reports/M0-T094-operator-channel.md` states the matrix is **"51/51 PASS"** (intro line and §4.3) but the actual count is **52** (confirmed by my run, the G2 self-check, and the packet `progress_log`). The primary deliverable report was not refreshed after the 52nd test was added. No test-validity impact; the report understates its own evidence. Recommend the producer correct the two "51/51" occurrences to "52/52" (report-path edit only; identity-neutral).
- **ADVISORY-2:** Four proofs are source-grep / source-order proxies — `S1` idempotency (`getsource` "SingleInstanceLock", L220), `S3` durable-before-ack (`source.index(write) < source.index(ack)`, L328-338), `S6` build_argv reuse (L612-613), `S13` no-context-injection (L858-859). Each is paired with a behavioral test of the effect, so risk is low, but a refactor preserving the grepped token while breaking behavior would slip past these specific assertions. The durable-before-ack ordering in particular has no crash-between-write-and-ack fault-injection test (inherent difficulty; unit-F `stop_intent` durability covers the primitive).
- **ADVISORY-3:** The feature's core value — pre-model zero-context interception — is **unproven on the installed version**. Context-erasure relies on Claude Code treating `{decision:block,reason}` as a non-context block (documented, not measured here); it is honestly deferred to the owner-gated C1 canary with the second-terminal advertised as the real-time path (R088 fallback). Correctly represented by S9/S11 + the fixture; the acceptance record should carry this caveat explicitly.
- **ADVISORY-4:** Non-critical untested branches: `run_ask` model-unusable path, `_read_answer_file` provider-failure guard, `validate_answer` refs coercion. Not security/durability-critical.
- **ADVISORY-5:** The 2971-test whole-suite total is a producer-composed baseline; independent single-run confirmation is the CI supervisor-bridge job on `3c4b888`, unobservable from this read-only sandbox — orchestrator should confirm it is green at acceptance.

## Conclusion

The test evidence genuinely proves the S1–S14 scenario pack. The matrix reproduces at 52/52; the implementation modules are real, substantive logic (not stubs); all R111-named cases have real failing-mode tests; the M6 equivalence argument is correct and I independently confirmed two hard mutants (M8, M6b) are actually killed; the composed whole-suite baseline is honest and methodologically valid; and the owner-gated C1 rows assert the truthful `pending-owner-C1` state rather than faking measurements. The only substantive nit is a stale test-count in the deliverable report (MINOR-1). Nothing blocks the QA gate.

VERDICT: PASS
