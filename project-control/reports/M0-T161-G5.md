# M0-T161 — G5 security review (security-reviewer "sec-m0t161", read-only, static)

> Transmission history: pinned at 7f5c82fa, delivered as three SendMessage parts (1/3..3/3) ending with END-OF-REPORT;
> no truncation. Joined verbatim by the orchestrator (transport wrapper tags removed only). Verdict PASS: certified-chain
> integrity, owner touchpoints, no command injection and no-write-on-check all hold. The hardening advisories (plan-file
> validator, lanes-phase mode, robocopy source precondition, rollback pointers, test coverage) are taken into the
> required-corrections round.

---

M0-T161 G5 security review (READ-ONLY) — Part 1/3

PIN: HEAD 7f5c82fa (>= 52594a03). Material b27fc89b confirmed (commission_lanes.ps1, test_commission_lanes.ps1, owner-guide, producer-report). grep shows NO Invoke-Expression / Add-Type / scriptblock / Start-Job / .Invoke() in the script (only a comment matches). Static review only — sandbox powershell is blocked (matches the producer's [BLOCKED] AS-4); memory write also blocked (skipped). Bottom line: NO BLOCKING defects; advisories below.

CERTIFIED-CHAIN INTEGRITY — solid:
- Install source is source_binding.json (M0-T160 re-pin). `check` STOPs unless the binding pins the candidate a3f24ff3 (commission_lanes.ps1:269-273). The install-phase guard requires the candidate commit AND subtree AND explicitly refuses the cfc3d22c subtree 11d43515 (commission_lanes.ps1:402-410). Verified against live output: update_controller_from_candidate.ps1:697-700 prints commit/commit-tree/subtree/files, so these are real matches, not false STOPs.
- 5.4 manifest STOP: reads the manifest back and STOPs unless exactly "147 55dc7135..." (commission_lanes.ps1:433-440); its message names rollback section 10.
- config/model immutability: update snapshots config.toml + model_selection.toml raw SHA before/after and STOPs on any change (commission_lanes.ps1:336-337, 349-356). record-manifest writes only the LOCALAPPDATA manifest (--out), never config; C:\Program Files\SupervisorConfig\config.toml is read-only throughout (only Get-FileHash + passed as --config). A tampered config would also fail the 5.4 digest STOP and verify-controller. It cannot re-record on a wrong tree or changed config.

OWNER TOUCHPOINTS — preserved:
- `approve` requires an owner-supplied -PromptDigest, STOPs if absent (commission_lanes.ps1:746-748), and STOPs unless that digest is actually among pending-approvals (749-751). No auto-fetch, no auto-approve; it only calls resume-pending-prompt with the owner's exact digest (754-758) and never bypasses WAIT_FOR_OWNER. Matches runbook §12(1).
- repin is first-start-only: lane phase -Repin $true (654), approve -Repin $false (766); --repin-cli-identity added only when Repin (714).
- lane 1 forced supervised (Get-LaneMode 624-629); --owner-enable-bounded-auto added only for 'limited-auto' (712), so the canary never gets bounded-auto; the plan phase cannot start lane 1 (Assert-ValidLanePlan restricts to 2-5, line 810).

Continued in Part 2/3.

---

M0-T161 G5 — Part 2/3

INJECTION / PATH SAFETY:
- No caller text is interpolated into a command string. Every external call routes through Invoke-Ext, which uses `& $FilePath @ExtArgs` (array splat, commission_lanes.ps1:131) — args pass individually, no shell parse, so -PromptDigest / -Worktree / -PacketId / plan fields cannot inject a command. $FilePath is always a constant ('python'/'git'/'robocopy'/'powershell.exe').
- The python `-c` strings embed only script constants ($Checkout from the LaneCheckout constant map, $ManifestPath, $manifestOut derived from a constant checkout), never caller text (185-186, 199-200, 433-434, 701). Safe.

ROBOCOPY /MIR — no wipe path:
- Source $certifiedTree = C:\SupervisorController\tools\agent_supervisor and each $dst is a constant lane folder (456, 472-473). Both are literal constants — never caller-controlled, never empty. /MIR runs only after install (5.3) + verify-manifest (5.5) succeed, so the source is populated by ordering; a wiped dest would be caught post-hoc by the empty-tree STOP in Assert-SameTree (218-220).
- ADVISORY (low): add an explicit "source has N files" precondition immediately before the /MIR loop, so a missing/empty certified tree STOPs BEFORE any destructive mirror rather than being detected after. recert §5.6 has the same gap.

DETACHED STARTS:
- Logs under %LOCALAPPDATA%\NYCBuildabilitySupervisor\autostart-logs (72, 247-248). Start args carry no secrets — only -m/start/--mode/--checkout/--launch-manifest/--max-cycles/flags (711-714); every value is a space-free constant/validated string, so the Start-Process -ArgumentList array (249) is safe here (no quoting split). Nothing launches until the per-lane chain STOP passes — Start-Detached is the last call in Start-LaneFirstLaunch, after the 704 chain check.
- cwd for the start is the lane folder $Checkout (not wt-controller-src as the wrappers use); benign because 5.6 propagation + 5.7 verify-controller prove each lane folder byte-identical to the certified source. Not a finding.

ROLLBACK:
- A mid-update STOP is restorable via `-Phase rollback` (runbook §10 bound-backup restore; ROLLBACK VERIFIED). Only the 5.4 STOP names rollback (439).
- ADVISORY (low): the 5.5-5.9 STOP messages (447-448, 497-499, 513-514, 529-530) omit the "roll back per runbook section 10" pointer that recert §5.6-5.9 specify; add it so a non-technical owner relaying a STOP has the recovery hint. (The owner guide already routes every STOP back to the assistant, so impact is small.)

Continued in Part 3/3.

---

M0-T161 G5 — Part 3/3

PLAN VALIDATOR (lanes phase) refuses overlapping allowed_paths, duplicate lanes, shared worktrees, unclaimed packets, and out-of-range lanes (Assert-ValidLanePlan 788-859; covered by test section 5). Gaps are ADVISORY (medium) — the plan is orchestrator-authored and downstream-validated by mrl_launch_draft/the supervisor, so none is exploitable/blocking:
- The "is a git worktree" check uses `git rev-parse --show-toplevel` (825), which SUCCEEDS for the PRIMARY ctl24 checkout and for any subdirectory of any repo. It does not reject a lane pointed at the primary checkout or a non-worktree-root, weakening the D-088-R001/R005 isolation intent. Recommend proving a LINKED worktree (e.g. --git-common-dir != --git-dir, or `git worktree list` membership).
- Worktree dedup keys on the raw entry.worktree fullpath, not its toplevel (818), so two entries under different subdirs of one worktree pass as "distinct".
- entry.mode is NOT validated in the lanes path — Invoke-LanesPhase passes it straight to `start --mode` (892-893) without Get-LaneMode, unlike the single-lane path. No privilege gain (bounded-auto only on the literal 'limited-auto', 712; unknown mode fails closed at the supervisor start gate), but recommend routing lanes-phase mode through Get-LaneMode for helper-level fail-closed parity.
- packet_id/worktree are not charset-constrained; a crafted packet_id joins into a tasks path (682, 831). Gated by JSON+status checks and only reachable via a malicious plan; recommend a `^M\d+-T\d+$`-style guard.

TEST (test_commission_lanes.ps1) — offline, cannot touch the machine: Invoke-Ext + Get-FileSha256Raw + Assert-SameTree + Ensure-ActivationDir + Start-Detached all stubbed; real FS ops confined to $env:TEMP. It proves parse-clean; the 5.4 STOP halts everything after it (verify-manifest/verify-controller/robocopy/doctor never run, 71-74); the 5.11 chain STOP prevents the detached start (92); the check config-hash STOP; check performs NO write (before/after snapshot, 138); the wrapper diff (only lane-specific values + not-yet-fed block); and the plan-validator refusals. Solid, and it matches AS-1..AS-5.
- ADVISORY (low) coverage gaps: no test exercises the install WrongSubtree/cfc3d22c refusal (408), lane-1 mode enforcement / no-bounded-auto, the approve "digest not held" STOP (749), or the config/model immutability guard (351-356). These guards are correct by inspection; add offline stubs to lock them against regression.

SUMMARY: certified-chain integrity, owner-touchpoint preservation, no command injection, secret-free detached starts, and no-write-on-check all hold. All findings are defense-in-depth hardening; none is exploitable given orchestrator-authored input plus the downstream supervisor STOPs. No BLOCKING items.

M0-T161 G5 VERDICT: PASS
END-OF-REPORT
