# M5-T122 — G4 test review (qa-engineer "qa-t122", read-only)

> Transmission history: pinned at 9d5311a8 (material b2e833fd; CI run 36116891984 green at 9d5311a8), delivered as
> 2 SendMessage parts ending with END-OF-REPORT; no truncation (each part was queued twice, byte-identical; the first copy is joined). Joined verbatim by the orchestrator (transport
> wrapper tags removed only). Verdict PASS; every condo state and strip rider
> proven green in CI; two ADVISORY items (A1 the C08 count predicate only tested on two statuses, A2 the A7 visual change
> is CSS-only and not assertable in jsdom).

---

M5-T122 G4 independent test/regression review (READ-ONLY). PIN: HEAD `git rev-parse` = 9d5311a89fee5e2e6ca450324798773446bdb384 at start. Material b2e833fd = 9 files, exactly the allowed paths; report-view.test.tsx (forbidden) untouched; diff b2e833fd..9d5311a8 is project-control only, so CI at HEAD proves the material.

CI run 36116891984 @ 9d5311a8: COMPLETED / conclusion=success. All 18 jobs green, 0 failures (verified via `--json jobs`). web-e2e (vitest+Playwright) ✓ 5m14s. Per-test log evidence:
- vitest: condo-records-section.test.tsx 10 (NEW) ✓, overview-exception-strip.test.tsx 13 ✓, condo-resolution-display.test.tsx 49 (UNCHANGED, via facade) ✓, report-view.test.tsx 20 (UNCHANGED, via facade) ✓; Test Files 57 passed (57).
- Playwright: architect-workspace:269 "stale Evidence link + true condo base-lot clause" ✓(#29); architect-workspace:239 folded-strip +HJ-A2 heading ✓(#28); responsive-a11y:424 "M5-T122 HJ A2/A4/A5 riders no overflow at 360px" ✓(#116); 134 passed. Known flake a11y-announcements:152 PASSED this run (#6) — no recurrence.

EXIT-GATE SCENARIO MAP — every condo state has a test asserting property (text+role+present/absent) that ran green:
- mixed-known zoning: condo-records-section.test.tsx:188 "Zoning missing for 1 lot" (+absent :200) + condo-resolution-display :766 (divergent+partial gap). ✓
- self-attested: condo-resolution-display :897/:904 (refusal loud, fail-safe even if payload says false). ✓
- revoked/superseded vs not-confirmed: :919 (no history) / :927 (revoked history) — distinct texts. ✓
- discrepant parcels: condo-records-section :226 "Parcels differ"+recorded/current disclosure (+absent :247) + condo-resolution-display :946. ✓
- unavailable/route_absent: condo-resolution-display :382/:583/:692 (no records, never withholds on its own). ✓
- single/multi_lot/unresolved/resolver-error + missing/unknown/malformed outcome token: :410/:389/:564/:574/:616/:632/:646 (allow-list fail-safe). ✓
- strip riders: overview-exception-strip :53 (A2)/:99 (A4)/:115 (A5) + both e2e. ✓

FACADE (AS-2): condo-records-section.test.tsx:135-141 asserts REFERENCE IDENTITY (`facade.X === CondoRecordsSection.X`) for all 4 moved names — a forked copy would redden. PropertyOverview.tsx:34-40 genuinely re-exports; all facade importers (ArchitectEntry/ProfileViews/PropertyFacts/ReportView/ScenarioWorkspace + tests) resolve — every imported name re-exported. Transparency proven by the 49+20 UNCHANGED consumer tests green in CI (git diff claim-seam..material for both = empty). Part 2 next.

---

M5-T122 G4 part 2.

TESTS FAIL FOR THE RIGHT REASON (mutants reasoned, no tautologies):
- Honesty labels asserted as HARDCODED literals, never imported constants: "Reference only", "City record", "Human record", "Parcels differ", "Zoning missing for 1 lot", locked h2 names "City records for this condo"/"Recorded base lot for this condo". No by-value-constant tautology.
- Doubled-name mutant (keep aria-label + h2): overview-exception-strip.test.tsx:64-65 asserts aria-labelledby==heading.id AND NOT aria-label → reddens. e2e :424 too.
- Stale link elsewhere: :111-112 asserts name "Retrieval status in Evidence →" AND href=…view=evidence → a wrong target reddens.
- Condo clause reverted to "official sources disagree": :133 not.toMatch + the both-conflicts discrimination :138-151 (generic keeps it, condo gets true clause) → reddens.
- "Reference only" dropped on multi_lot: condo-records-section.test.tsx:152 getByTestId throws if dropped.
- C06 recorded list dropped: :240-242 asserts both "Recorded in the confirmation: …" and "Current city records: …".
- C08 count: MIXED=1, ALL_UNKNOWN=2, ALL_RECORDED=absent kill "count all"/"count recorded"/always-plural mutants; singular guarded by :192 not.toMatch(/1 lots/).

NO EXISTING TEST WEAKENED: only removed assertion is the old stale-row `queryByRole("link").toBeNull()` — mandated obsolete by HJ A4 and REPLACED by a stronger positive href assertion. architect-workspace.spec.ts: additions only. condo-resolution-display + report-view byte-unchanged.

E2E STABILITY: no fixed sleeps (auto-waiting expect + explicit profile-view timeout); stable selectors (getByTestId/getByRole name); route-inject mutates the REAL fixture body (route.fetch→fulfill). 360px test uses expectNoHorizontalOverflow (responsive-a11y.spec.ts:21-33 = real doc.scrollWidth ≤ clientWidth+1), a genuine layout measurement.

SCOPE/BACKEND: exactly 9 allowed-path files; no forbidden path; no contract/backend change; modularity_check green (PropertyOverview 337→129 lines, CondoRecordsSection new). HJ A7 CSS selector `.architect-alert[data-identity-state]` verified NON-vacuous — AnalysisIdentityNotice.tsx:72 renders that class+attr (differs/absent). Grid-comment "280px ≤1200px" claim matches the real @media rule architect.css:177.

ADVISORY (non-blocking):
- A1: C08 count predicate `!== "recorded"` (CondoRecordsSection.tsx:253) is fail-safe-correct, but fixtures only use recorded/unknown, so an `=== "unknown"` mutant survives (differs only on a 3rd status). Low risk.
- A2: HJ A7 visual distinctness is CSS-only — jsdom can't assert computed style; only role/text-unchanged is tested. Selector is real (see above); defer the visual pass to human-journey. Producer D4 notes print bg-color may drop (more prominent, never weaker).

No BLOCKING findings.

M5-T122 G4 VERDICT: PASS

END-OF-REPORT
