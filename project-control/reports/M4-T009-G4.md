<!-- Reviewer return preserved VERBATIM by the orchestrator (report-preservation rule,
.claude/rules/project-control.md; transport entity-decoding applied: &lt; -> <, &gt; -> >, &amp; -> &).
Reviewer: independent qa-engineer subagent (G4), returned 2026-09-12, reviewed HEAD 032dffe7. -->

All six verification tracks are complete. Here is my gate report.

---

VERDICT: PASS

# G4 Quality Gate Report — M4-T009 (R1–R12 residential FAR draft rule family)

- **Task:** M4-T009 — R1–R12 residential FAR draft rule family (flat districts) from committed ZR 23-21 / ZR 23-22 snapshots
- **Gate:** G4 (QA — first full wave: coverage + executability of the acceptance packs vs AS-1..AS-9)
- **Reviewer:** qa-engineer (independent; did not author this work)
- **Reviewed HEAD:** `032dffe7` on `candidate/D-024-mrl-option-b` (verified reachable via shared object DB; producer commits `ae117874` / `197156b9` / `317df044`; AS-5 dependency `M4-T010` accepted at `12a6ed61`)
- **Method:** ADR-005 read-only — static analysis, read-only `git show <sha>:` extraction of frozen bytes, offline `python -c` AST/digest probes (`PYTHONDONTWRITEBYTECODE=1`). No pytest/ruff run; CI remains the execution authority.

## Recomputed collected-case counts (AST, parametrize expanded)
- **`test_r1_r12_residential_far.py` (AS-1/2/3/4/7/8/9): 15 cases** — 15 test functions, no parametrization.
- **`test_r1_r12_residential_far_provenance.py` (AS-5/6): 17 cases** — 12 functions; three parametrized: `test_as6_flat_rules_...` ×4 (`list(_FLAT_RULES)`), `test_as6_negative_deleting_district_entry_...` ×2, `test_as6_negative_misassigning_district_entry_...` ×2.
- **Authored total: 32.** The provenance-pack 17 reconciles the addendum's RED record exactly (1 selected by `-k records_its_snapshot_digest` + 16 deselected = 17).
- Sibling adaptations recount cleanly: `test_rules_engine.py` 31, `test_rules_integration.py` 33, `test_rules_fh_safeguards.py` 48 (all parse at 3.11 → no PEP 695 / 3.12-only syntax present).

## 1. Scenario map — every AS-1..AS-9 clause has an executable assertion

| AS clause | Implementing test(s) | Result |
|---|---|---|
| AS-1 byte-exact, LOADED not restated | `test_as1_flat_rule_values_match_snapshot`, `test_as1_conditional_rule_values_match_snapshot` | PASS — expected side is `Decimal(snapshot_row[...])` loaded from the cited snapshot; both columns + full key coverage compared; conditional maps + R8 footnote-2 8.64 anchored to the footnote-carrying row |
| AS-2 computed coverage, count asserted, silent-absent = FAIL | `test_as2_every_snapshot_district_is_covered_or_excluded`, `test_as2_district_counts_are_asserted` | PASS — counts asserted 18/27/45; `invented` and `silently_absent` both empty; applicability sets asserted pairwise-disjoint |
| AS-3 conservative on conditionals, higher never returned/surfaced | `test_as3_conditional_districts_return_conservative_value`, `test_as3_higher_value_is_the_wide_street_footnote_row` | PASS — returns `low`, asserts `!= high`; `wide_street_far_alternative` surfaced as `conditional_alternative`; higher row anchored to snapshot footnote-1 |
| AS-4 footnote scope | `test_as4_footnote_1_cap_only_on_first_row_rule`, `test_as4_snapshot_marks_footnote_1_on_first_row_only` | PASS — cap present on r1-r2-r3 only; absent from r2x-r4 and r5; snapshot marks footnote only on row 0 |
| AS-5 citation + digest match what the rule records | `test_as5_citations_resolve_on_disk_with_expected_dates`, `test_as5_every_family_rule_records_its_snapshot_digest`, `test_as5_matching_...`, three mismatched tests | PASS — see §3 |
| AS-6 second column surfaced per district, never applied | `test_as6_flat_...` (×4), `test_as6_conditional_...`, four negatives | PASS — by-id selection, effect + citation + byte-association, higher value never applied; exceeds "≥1 per source" (every applicable district) |
| AS-7 status/honesty + not-effective | `test_as7_all_rules_are_draft_and_unapproved`, `test_as7_no_family_result_is_ever_verified`, `test_as7_before_effective_from_is_not_effective` | PASS — engine confirms base coverage caps at `conditional` for draft; before-effective → `not_applicable`, `applicability_outcome False`, outputs `{}` |
| AS-8 no engine change / existing primitives | `test_as8_family_uses_only_existing_primitives`, `test_as8_registry_loads_whole_family` | PASS — ops `identity`/`multiply` ∈ COMPUTE_OPS; all effects ∈ {conditional_alternative, documented_limitation, professional_review_required}; engine `.py`/schemas untouched by the diff (pytest/modularity = CI/tool authority) |
| AS-9 correct R5 pilot | `test_as9_r5_no_longer_carries_footnote_cap_r1r2r3_does`, `test_as9_r5_far_values_unchanged_and_match_snapshot`, adapted `test_exception_applies_and_does_not` | PASS — cap moved R5→r1-r2-r3 (doc + evaluation output); R5 FAR 1.50/1.50/1.50/2.00 unchanged & snapshot-matched |

No AS clause lacks an executable assertion.

## 2. Anti-tautology (spot-checks)
- **FAR values are never restated literals.** Every AS-1/AS-6/AS-9 value assertion derives the expected side from the loaded snapshot row (`_district_rows` → `_snapshot_raw` → the cited `*.snapshot.json`) and compares to the rule parameter/evaluation output. No FAR literal is hardcoded in the packs.
- **R8 6.02/7.20 discriminator:** rule returns `standard_far_by_district[R8]=6.02`; AS-3 asserts `== low` and `!= high`, both from `sorted(snapshot standards)`. AS-6's R8 case is genuinely discriminating — I confirmed R8 `qualifying=7.20` equals `wide_street=7.20`, so by-id selection (not a "7.20" text scan) is required; the metamorphic test `..._on_r8_is_not_masked_by_wide_street_720` proves it.
- **AS-9 negative pair:** cap absent in `r5_residential_far.rule.json` (only `qualifying_residential_site`), present in `r1_r2_r3_residential_far.rule.json`; asserted at both rule-doc and evaluation-output level.
- **Coverage count:** 18/27/45 are the asserted tie-points; 18/27 derive from snapshot rows, 45 from rule applicability — a dropped district on either side fails. Correct per AS-2 ("the count is asserted").
- **AS-6 negatives genuinely fail under their mutations:** delete qualifying exception → `_applied_by_id == []`; delete district entry → `KeyError`; misassign → `AssertionError`; R8 discriminator → by-id empty while text still finds 7.20. All trace cleanly against the extracted rule/engine bytes.
- **Digest chain is non-tautological:** independently recomputed `sha256(verbatim_excerpt)` for both snapshots and confirmed it equals BOTH the stored snapshot digest AND the digest each rule records — `zr-23-21 = b52771e6…a48ad8a`, `zr-23-22 = 943b65f9…df1a38e` (all five family rules record the correct one).

## 3. AS-5 unit (presence/equality + rewritten drift test)
- **Presence+equality kills the drop-the-digest mutant.** `test_as5_every_family_rule_records_its_snapshot_digest` requires the field on every family rule and three-way equality (recorded == committed stored == `sha256(excerpt)`). RED (rulesets reverted to `12a6ed61` pre-digest) → 1 failure on the presence assertion; I confirmed via `git show 12a6ed61:` that the family citations carried no `content_digest_sha256`.
- **Rewritten drift test (`…_fails_closed_at_load`) is strengthening, not weakening.** With rules now recording the authored-against digest, `dsl._check_refs` (M4-T010) raises `DSLError` when a rule's recorded digest ≠ the on-disk snapshot digest, so `RuleRegistry(...).load()` REFUSES the drifted store — the old post-hoc "detectably-different resolved digest" path is unreachable (evaluation never begins). Confirmed in source: `build_rule_definition` runs `_schema_validate` → `assert_agent_authorable` → `_check_refs`; in the drift scenario the rule files are unchanged/valid so schema passes and only `_check_refs` fires.
- **`match=` cannot be satisfied by a schema-shape rejection.** The regex `records content_digest_sha256 .* but the snapshot on disk stores` matches only the `_check_refs` mismatch message; a schema violation emits `rule schema violation at …` (no match). The drifted store is internally self-consistent (digest recomputed), so `SnapshotStore.load()` does not raise first. `"zr-23-22" in str(excinfo.value)` holds.

## 4. Red/green coherence
- **RED:** 1 failed (presence, provenance module) + 16 deselected = 17 total — matches my independent AST count of 17. Pre-digest rules genuinely lack the field at `12a6ed61` (verified by `git show`). (The addendum's "line 192" vs the current presence-assert at ~193 is a benign narrative offset; the mechanism is verified.)
- **GREEN:** `368 = 367 + 1 new`, drift test rewritten in place (count unchanged) — internally consistent. The earlier "359 passed" figure is a *different tree state* (task branch before M4-T010 added `test_rule_citation_digest.py` and before the presence test), so no contradiction. Full 368 is a suite-wide figure CI will confirm.

## 5. Executability statics
- **Parse:** all five changed modules `ast.parse` cleanly (3.11; `feature_version=(3,12)` unavailable on the 3.11 sandbox — successful 3.11 parse confirms no 3.12-only syntax is present).
- **Line length ≤ 100:** max line lengths 100 / 99 / 100 / 98 / 98; zero lines over 100.
- **Deterministic parametrize ids** (string/tuple literals; `list(_FLAT_RULES)` uses insertion-ordered dict keys).
- **Module-scoped `registry` fixture is sound:** `evaluate()` rebuilds `citations` (fresh dicts) per call, so `test_as5_export_fails_closed`'s local mutation of `result.trace.citations[0]` cannot contaminate other tests; mutation tests build separate registries.
- **Packs read only committed files** (`_RULESET_DIR`, `_DOCS_SNAPSHOT_DIR`); `_write_snapshot_dir`/`_write_ruleset_dir` and all in-memory `_rule_doc` edits write only under `tmp_path` or local dicts — no repo writes. Path anchors (`parents[2]`/`parents[4]`) resolve correctly.
- **Dependency check for AS-5 executability:** the M4-T010 schema at this HEAD admits `content_digest_sha256` (`^[0-9a-f]{64}$`) on citation items, so the family rules pass `_schema_validate`.

## 6. Regression surface
- **Scope:** the three producer commits touch only `services/api/app/rules/rulesets/**` (5 rule files), `services/api/tests/rules/**` (5 test files) and `project-control/reports/M4-T009-producer-report.md`. Nothing under `services/api` outside `rulesets` + `tests/rules` changed — engine `.py`, `schemas/**`, `api/**`, `scenario/**` untouched (schema/loader are M4-T010, separately accepted).
- **Sibling adaptations behavior-neutral / correct:**
  - `test_rules_integration.py` and `test_rules_fh_safeguards.py` — `evaluations[0]` → select-by-`applicability_outcome` (`_applicable_trace`, asserts exactly one; safe because family applicability is disjoint). Asserted values unchanged (1.5, 15000.0, 2.0/10000.0, 12000.0, needs_review, `{}`+MISSING_CRITICAL).
  - `test_rules_engine.py` — the R5 cap assertion flips to "absent from R5, present on R1" — the required AS-9 behavior change (strengthening, not masking).
- **Out-of-scope base remediation confirmed at this HEAD (enables the full-suite GREEN):** the packaged bundle `services/api/app/_zr_snapshots/v1/` now contains `zr-23-21` and `zr-23-22` byte-identical to `docs/` (digests `b52771e6…` / `943b65f9…`, self-consistent), so the default-store sibling tests can resolve `zr-23-22` — the drift the producer flagged (§3 of the report) is closed.

## Non-blocking observations (not gate failures)
1. `test_as5_citations_resolve_on_disk_with_expected_dates` line 176 (`snap.content_digest_sha256 == _sha256(snap.verbatim_excerpt)`) is tautological by construction (the store computes the digest that way) — harmless belt-and-suspenders; the load-bearing non-tautological binding lives in `test_as5_every_family_rule_records_its_snapshot_digest`.
2. Producer-narrative "line 192" for the RED presence failure is off by ~1 from the current source; substance verified.

## Conclusion
Coverage is complete (every AS-1..AS-9 clause maps to at least one executable, non-tautological assertion), the acceptance packs are statically executable and self-contained, the AS-5 rewrite is a genuine strengthening, the red/green record is arithmetically and mechanically consistent, and the regression surface is clean with the base bundle remediated so the documented commands are runnable at `032dffe7`. **G4 = PASS.**

Recommendation to the orchestrator: record G4 PASS at `032dffe7`; final pytest/modularity pass (`python -m pytest services/api/tests/rules`, `python tools/modularity_check.py --check`) is CI-authoritative and should be green before acceptance given the synced bundle at this HEAD.

---

Files I created (scratchpad + agent memory, all outside the reviewed source):
- Extracted frozen-SHA copies and probes under `C:\Users\MLFLL\AppData\Local\Temp\claude\C--Users-MLFLL-Downloads-nyc-zoning-ctl24\5967607e-525a-430f-b258-e908e6db62e6\scratchpad\`
- `C:\Users\MLFLL\Downloads\nyc-zoning\nyc-development-feasibility-claude-pack\.claude\worktrees\agent-a31fdde8e77b7de4a\.claude\agent-memory\qa-engineer\{MEMORY.md, reference_collected_case_counting.md, reference_git_show_frozen_review.md}`

No implementation files were edited; no ledger/git/gh commands were run (ADR-005 read-only).
