# M5-T048 G3 — independent code review (code-reviewer, saved VERBATIM from the agent return)

G3 CODE review M5-T048 — VERDICT: PASS (2 advisories, 0 blocking). Reviewed at HEAD 15c1b3a4 (verified). Material cherry-picks 0c7b17f1 + 93f194c0; both touch only allowed_paths (name-status confirmed: no builder/derive/api/rules/spatial/profile/connectors/web change; the correction is a pure R100 rename to semantically_invalid/scenario/ + test/report edits). proposal.py pre-image was a 15-line placeholder seed (M status expected).

FINDINGS
1. ADVISORY (proposal.py:364-407 _validate_walls) — wall start/end indices are bounds-checked against len(outline_vertices) which INCLUDES the repeated closing vertex, and the distinctness check compares INDICES not points. A wall (start=0, end=n) referencing the closing duplicate is a degenerate zero-length wall that passes B0. No effect at B0 (no derivation); flag for the B1 reviewer when walls get consumed.
2. ADVISORY (contract.py:145-153) — a 1.1.0 document WITHOUT a proposed_massing block is accepted (block optional). The "emits 1.1.0 only when block present" invariant is builder-side (B1); read-side leniency is acceptable for an additive optional block.

BASIS (all PASS): (1) proposal.py correctness — closure convention is explicit first==last (documented); _ring_is_simple is a sound dependency-free predicate (zero-length edges, 180° reversal spikes via cross==0/dot<0, non-adjacent edge intersections; distinct-vertex check runs first so adjacency handling is correct; O(n^2) bounded by MAX_OUTLINE_VERTICES=1000); NaN/inf refused for both coords (_is_real_number, bool excluded) and heights (math.isfinite, bool excluded); level_index contiguity {0..N-1}; per-level outlines validated recursively; NYC 2263 bounds are defensible generous fail-closed unit-sanity guards (documented as not a city boundary); every failure a typed ProposedMassingError naming the exact dotted field; no silent pass-through. (2) contract.py threading is additive, appended AFTER the existing schema gate (1.0.0 gate intact), version-gated, strict-JSON NaN/Inf guard fires first (non-finite refuses at "<root>"). (3) No evaluation/derivation/builder change anywhere; forbidden paths untouched. (4) proposal.py 456 lines (below 600 warn), stdlib-only (math), single responsibility; the 7 boundary answers match the diff. (5) Scope clean incl. [ORCH-PACKET-FIX 2] semantically_invalid glob. (6) Tests genuinely pin invariants (mutate-one-defect-from-valid, assert exact field); DB-013 ceiling tests at/over boundary; whole-gate test_invalid_fixture_rejected parametrizes INVALID_FIXTURES + SEMANTICALLY_INVALID_FIXTURES covering all three new fixtures across both dirs; 5 fixtures confirmed real 1.1.0 docs with honest _expected_failure annotations. (7) Producer report accurate — scrupulously flags the three api-cwd checks were NOT run by the loop-worker (routed to orchestrator capture); orchestrator addendum accurately describes the tagged relocation; no unexecuted command claimed as executed.

NOTE: code_graph cache is STALE ("refusing to serve cached graph") — did not regenerate (reviewer read-only); consumer set verified directly in source (only contract.py imports proposal). Could not write the code-reviewer memory note (Write disabled this session).
