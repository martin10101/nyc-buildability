# Startup capsule — M2-T017 (producer tab)

**You are a producer, not the controller.** Do not merge, accept, replan the ledger, change another
task, operate another worktree, read/import an unmerged sibling branch, or modify a shared contract.
Commit and push only your assigned branch; open one PR against main when the complete packet is ready.

- **Task / title:** M2-T017 — Canonical `source_fact` and `analysis_state_transition` contract hardening (DF-4/DF-5).
- **Branch:** `task/M2-T017-contract-hardening` · **Expected worktree:** the sibling folder the controller created for you (path in the owner return).
- **Frozen base SHA:** your worktree's initial `HEAD` (run `git rev-parse HEAD`) — the post-D-002-consolidation `main` SHA in the controller's owner return. Start from it.
- **Read before starting:** (1) root `CLAUDE.md`; (2) your packet `project-control/tasks/M2-T017.json` (source of truth); (3) `project-control/reports/FIRST-WAVE-INTEGRATION-CONTRACT.md`; (4) your applicable D-002 requirements (the 14 PROD ids); (5) `project-control/reports/WHOLE-SYSTEM-TRUST-REPLAN-2026-07-23.md` (Area G / DF-4/DF-5), the current `packages/contracts/schemas/v1/source_fact.schema.json` + `analysis_state_transition.schema.json`, and the drift scripts `packages/contracts/scripts/generate_ts_types.py` + `services/api/scripts/sync_contract_schemas.py`; (6) your latest `M2-T017-*` resume report if any.
- **Directive refs:** D-002 (regime v1.0; cite `D-002:ALL`).
- **Owned interfaces / schema versions:** `source_fact.schema.json` (closed; version/`$id` bumped), `analysis_state_transition.schema.json` (closed), `services/api/app/contracts/serializers.py` (NEW allowlist serializer; **frozen interface, NOT wired** into the production route/builder). **You are the sole owner this wave of** `packages/contracts/generated/property_profile.ts` (regenerate ONLY if the source_fact change alters it) and the runtime bundle copy `services/api/app/_contract_schemas/v1/source_fact.schema.json`. **Consumed read-only from main:** the drift scripts (run, don't edit); `services/api/app/profile/builder.py` (reference only — do NOT edit).
- **Allowed paths:** the two schemas; `packages/contracts/generated/property_profile.ts`; `packages/contracts/fixtures/{invalid,valid}/{source_fact,analysis_state_transition}/`; `services/api/app/_contract_schemas/v1/source_fact.schema.json`; `services/api/app/contracts/`; `services/api/tests/contracts/`; `project-control/reports/M2-T017-producer-report.md`.
- **Forbidden paths:** all other canonical schemas + their generated TS (rule_evaluation.ts/scenario.ts); `legal_source_manifest.schema.json` (M3-T001); `services/api/app/profile/builder.py` + `.../api/**` (production wiring is a LATER integration task); `services/api/app/rules/**` (M4-T007); `packages/contracts/scripts/**` (shared generators frozen); every reserved hotspot.
- **Dependencies proven accepted:** M2-T003 (accepted; defined these contracts). No first-wave sibling dependency.
- **Required tests / harnesses:** negative typo/undocumented-field/diagnostic-leak fixtures FAIL validation while positives PASS; serializer unit tests; `sync_contract_schemas.py --check` and `generate_ts_types.py --check` byte-identical.
- **Permitted report path:** `project-control/reports/M2-T017-producer-report.md`.
- **Completion condition:** both contracts closed (additionalProperties:false or a single versioned `extensions`); allowlist serializer added as a frozen interface (NOT wired into the route); negative fixtures + tests green; bundle/typegen byte-identical for owned schemas only; G0/G2/G3/G4/G5 evidence in your report; one PR against main opened.
- **Stop conditions (return a ≤200-word blocker):** if closing a contract forces a change to a shared production entrypoint/builder — submit an interface-change request, do NOT wire it; credential/payment/security-exception/destructive-op/impossible-acceptance.
- **Final return (≤500 words, ≤8 bullets):** task+outcome; PR link+head; what now works; contract/interface impact (source_fact/analysis_state_transition vNext; property_profile.ts regen y/n); tests+review result; blockers/limitations; exact next dependency unlocked (later integration task wires the serializer into builder.py); whether you are now idle.
