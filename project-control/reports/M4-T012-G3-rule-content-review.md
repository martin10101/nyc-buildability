# G3 GATE REPORT — M4-T012 (R1/R2-series height/setback draft rule families, D-049 definitive scope)

> Orchestrator note: reviewer return saved VERBATIM from the agent-return channel
> (transport entity-decoding only). Reviewer: code-reviewer. Recorded by the
> orchestrator per ADR-005.

I have completed my independent review. I ran all available read-only checks and re-derived every constraint value against the captured snapshots and the directive scope. Here is my full G3 report.

---

**Gate:** G3 (rule-content / legal-sourcing correctness), independent
**Reviewer:** code-reviewer (independent; not the producer rules-engineer)
**Task:** M4-T012 — `residential_height_setback_r1_r2` draft family
**Frozen review identity:** primary checkout `C:/Users/MLFLL/Downloads/nyc-zoning/ctl24`, HEAD `48aed9c093b0c33ae1f26438a09211466a982ecd` (material head `dd7c8b74` + control-plane stamps). I ran NO git state-changing command (anchor confirmed I am on the primary checkout).
**Posture:** read-only. No files edited, no control CLI, no git/gh writes.

## What I ran (reproducible)

- `python services/api/scripts/sync_zr_snapshots.py --check` → `OK: runtime-bundled ZR snapshots are byte-identical to the canonical source (14 file(s)).`
- `cd services/api && PYTHONPATH="." python -m pytest tests/rules/test_r1_r2_height_setback.py -q` → **100 passed**.
- `cd services/api && PYTHONPATH="." python -m pytest tests/rules -q` → **558 passed** (reproduces the producer/orchestrator regression claim).
- `python tools/modularity_check.py --check` → **EXIT 0** (16 warnings, all in pre-existing untouched files: `scenario_analysis.py`, `mappluto_geometry_arcgis.py`, `breakeven.py`, `surveyReview/types.ts`, `tools/agent_supervisor/*`; none in the four new JSON rulesets).
- `git diff --stat dbd07817 dd7c8b74` (read-only) → confirmed the producer's material file set and that **no engine/evaluator/schema file was touched** (only `rulesets/`, `_zr_snapshots/v1/`, `docs/research/zr-snapshots/v1/`, `tests/rules/`, the two report files, and `.claude/agent-memory/rules-engineer/`).
- Sandbox note: I am Python 3.11.9 vs the repo's 3.12; the suite nonetheless collects and passes under `PYTHONPATH="."` from `services/api`, matching the documented M2-T015 pattern. I did not need orchestrator-captured CI evidence for the local checks; the `api` CI job on the pushed head remains the deploy authority for installed-wheel behavior (AS-6 also proves packaged-registry load locally).

## Per-mandate rulings

### Mandate 1 — constraint traceability + SNAPSHOT HONESTY RULE — PASS
Every emitted constraint value traces to a hash-guarded snapshot whose captured text supports it:
- 25 ft perimeter wall / 35 ft ridge → `zr-23-421-r1-r2` verbatim ("maximum height above the base plane of 25 feet"; "meet at a ridge line of 35 feet above the base plane").
- 5 ft reference-plane offset → `zr-23-421-g` verbatim ("may be located up to five feet above the base plane"); 9,500-present check PASS; word-for-word MATCH vs the owner-verified verbatim.
- 35/35 QRS base/building height → `zr-23-424-r1-r2` table row (R1-1, R1-2, R1-2A, R2, R2A, R2X → 35/35), independently and correctly separated from the accepted M4-T006 R5 row.
- Suffix inheritance basis → `zr-11-25` verbatim, carrying explicit owner-decision provenance, never an express per-variant citation.

The digest guard is real, not merely asserted: `test_as2_*` recompute `sha256(verbatim_excerpt)` and assert `recorded == snapshot == recomputed`, and the tamper test fails closed — both pass in my rerun. `sync --check` confirms canonical↔bundle byte-identity. **SNAPSHOT HONESTY RULE satisfied**: no snapshot note or `verbatim_excerpt` asserts content its capture channel (the full-text PDF) could not render; the raw-HTML corroboration is explicitly attributed to a named channel (direct HTTPS GET, sha256 `fc51ec09…`). No hard-coded/guessed values, no schema guess (schemas untouched), and no hidden default-to-False (`test_nc3_omitted_modifier_flag_is_indeterminate_not_confident` proves an omitted flag is UNKNOWN→PRR).

### Mandate 2 — D-049 fidelity — PASS (all rows)
- **D-049-R001** (suffix inheritance, owner-decision provenance, never express): PASS. `r1_r2_suffix_variants_pitched_height.rule.json` scopes exactly {R1-1, R1-2, R1-2A, R2A, R2X}, cites `zr-11-25`+`zr-23-421-r1-r2`, carries exception `owner_decision_suffix_inheritance_not_express` and DRAFT/needs_review; `test_as1_bare_and_suffix_rules_cite_distinct_provenance` proves the bare rule does NOT cite 11-25 and the suffix rule does.
- **D-049-R002** (R2X FAR floor-area-only, no height effect): PASS on the merits — R2X height is identical (25/35) to the other variants, and exception `r2x_far_row_is_floor_area_only` documents the distinction; `test_as1_r2x_far_row_documented_as_floor_area_only` binds it and proves non-R2X variants don't carry the note. (See ADVISORY A2 on the citation-quote wording.)
- **D-049-R003** ((g) explicit conditional): PASS. Applicability = in_set{R1-1, R1-2, R2} AND pitched building type AND `((area≥9500 AND width≥100) OR slope≥5)`. Letter-suffix exclusion proven (`test_nc2_letter_suffix_and_bare_r1_excluded_from_reference_plane` — R1-2A/R2A/R2X/R1 → not_applicable even with fully-satisfying geometry); missing geometry input → PRR, never silent (`test_nc5_reference_plane_missing_any_single_geometry_input_fails_closed`); trigger boundaries bound at 9500/9499, 100/99, 5/4.99 (`test_nc8_trigger_boundary_values`). Sloping-plane geometry stays a typed A2 gap (`test_nc8_setback_geometry_never_a_numeric_output`).
- **D-049-R004** (modifiers never silent): PASS. 23-424 → dedicated `r1_r2_qrs_height` rule (fail-closed on `qualifying_residential_site`, transit-route exclusion recorded, same-family conflict surfaced not silently resolved — `test_nc7_*`); 23-425 → `large_site` flag; 23-426(a) → `historic_district` flag; 23-443(b) → new `transportation_infrastructure_adjacent` flag; 119-212 + 113-523 → `special_district_present` flag with both sections named in its description/exception text; 23-413(a) noted so the pitched envelope is never rendered as a requirement.
- **D-049-R005** (provenance + open ask): PASS. All four rules `needs_review`, `qualified_human_approval: pending`; `docs/ARCHITECT_REVIEW_QUESTIONS.md` A1/A2 show "OWNER DECISION D-049 ISSUED 2026-09-13" with the professional-confirmation ask explicitly OPEN.
- **D-049-R006** (supersession): PASS. The five lettered variants moved from D-048 not-assessed to suffix-inheritance encoding; the packet objective and evidence map record the chain; the binding capture-completeness proof (PDF, 9,500-present) is satisfied.
- **D-048-R001** (express half — bare R1/R2 intact): PASS. `r1_r2_bare_pitched_height` scopes exactly {R1, R2} from the express enumeration, no lettered variant swept in by assumption (`test_nc1_bare_rule_scoped_to_bare_labels_only`). D-048-R002's literal not-assessed text is correctly treated as SUPERSEDED for the five variants by D-049-R006 (the evidence map and packet cite both).

### Mandate 3 — routed hazards
- **(a) The disclosed HTML/(g) discrepancy — HONEST AND ADEQUATE; capture proof VALID; no snapshot note misleading.** The binding requirement (prove 23-421 completeness from a full-text artifact with the 9,500-present check) is met via the PDF channel of record (sha256 `b5777618…`, 9,500 present, verbatim MATCH). The producer additionally captured the raw HTML, found (g) present in the raw bytes, and DISCLOSED that this contradicts the packet's "HTML provably loses (g)" premise, giving a best-effort unconfirmed explanation (a prior readability/text-extraction tool truncated after an embedded base64 PNG) rather than silently reconciling. The `zr-23-421-g` `capture_channel_note` records both channels transparently and attributes the HTML finding to its named source. Two independent channels agreeing STRENGTHENS, not weakens, the proof. **The needed correction to the packet premise and to the `docs/ARCHITECT_REVIEW_QUESTIONS.md` A1 TOOLING NOTE (lines 52-58) is an ORCHESTRATOR follow-up item, NOT a producer defect** — that doc is outside the producer's allowed_paths and the producer correctly disclosed instead of editing it. See ADVISORY A1.
- **(b) All-three-geometry-inputs-required on the (g) rule — ACCEPTABLE fail-closed conservatism, not a content defect.** The OR trigger is correctly encoded at applicability; the conservatism only bites when an input is missing, in which case the outcome is `professional_review_required` — which is exactly the visible review flag D-049-R003 mandates for the unavailable-input case ("never silently applied, never silently denied"). It never emits a wrong value; it is disclosed in the rule `limitations`, producer decision 3, and `test_nc5_*`/NC-8. Acceptable.
- **(c) 119-212 + 113-523 folded into `special_district_present` — SATISFIES D-049-R004.** R004 requires each modifier "surface as conditions/flags/typed gaps," never silent; it does not mandate a dedicated flag per section. The flag downgrades to `professional_review_required` and NAMES both sections in its input description and its exception description. Requirement met. See ADVISORY (none blocking) — a future refinement could add dedicated flags, but this is not required.
- **(d) Bare-R1 (g)-ineligibility — FAITHFUL to D-049-R003.** D-049-R003's owner-verified enumeration names exactly R1-1, R1-2, R2 as (g)-eligible; the producer encoded exactly that set and explicitly excluded bare "R1", disclosing the choice in the rule input description, `limitations`, and the producer report. The captured verbatim's phrase "R1 and R2 Districts without a letter suffix" could on its face read onto a hypothetical bare "R1", but the binding scope is the owner enumeration, and per permanent principle 3 the producer did not extend eligibility beyond it. This is correct and is already an open G6 professional question (A1/A3). See ADVISORY A4 (observation only).
- **(e) Canonical-snapshot writes outside the original allowed_paths — correction COVERS what was written.** The corrected packet adds `docs/research/zr-snapshots` to allowed_paths; the producer wrote exactly the four canonical snapshots under `docs/research/zr-snapshots/v1/`, which that prefix covers. The `git diff` confirms the producer's writes fall entirely within the corrected allowed set (plus its own agent-memory), and the sync script was run, not edited. The forbidden `project-control/directives/**`, `tasks/**`, engine/evaluator/schema, and `docs/MVP_ARCHITECT_REVIEW_QA.md` paths were not touched by the producer (the D-045/D-046/M4-T015/task-packet changes in the range are orchestrator control-plane commits, not producer writes). Confirmed.

### Mandate 4 — deliverables completeness vs the packet outputs list — PASS
All four packet outputs present: 4 per-variant rulesets (needs_review, full citation/effective-date/snapshot provenance); 4 new snapshots in canonical + byte-identical bundle (hash-guarded, sync-verified); `test_r1_r2_height_setback.py` (100 cases: min/max separation, fail-closed gaps, wrong-district + cross-variant isolation negative controls, effective-date discipline, tamper/mutation-style checks, installed-wheel load); both report files. Acceptance scenarios S1–S6 are each backed by executing tests I reproduced, not just claimed.

## Corrections

No BLOCKING corrections arise from the G3 (rule-content/legal-sourcing) dimension. The following are ADVISORY (none block acceptance):

- **A1 (ADVISORY — ORCHESTRATOR follow-up).** Correct/annotate the packet premise and `docs/ARCHITECT_REVIEW_QUESTIONS.md` A1 TOOLING NOTE (lines 52-58): the raw HTML bytes DO contain 23-421(g); the loss was a text-extraction-tool artifact, not a raw-response limitation. Leaving it uncorrected risks misleading future capture tasks and the G6 professional. Not a producer defect (doc is out of producer scope); the PDF capture proof stands regardless.
- **A2 (ADVISORY — content annotation).** In `r1_r2_suffix_variants_pitched_height.rule.json`, the `zr-23-21` citation quote parenthetical "(R2X grouped with R4A/R4B/R4/R4-1, its own table row…)" is internally contradictory and imprecise versus the cited `zr-23-21` snapshot, which places R2X in its OWN row (standard FAR 1.00) separate from the R4 row (standard 1.00 / qualifying 1.50). No emitted value depends on it and D-049-R002 is satisfied on the merits; recommend rewording to "R2X occupies its own 23-21 row at standard FAR 1.00, distinct from the R2/R2A row (0.75) and from the R4 row." (This is a rule-file citation gloss, not a snapshot note; the SNAPSHOT HONESTY RULE is not violated.)
- **A3 (ADVISORY — language normalization).** The pitched-rule parameter notes read "Candidate value pending raw-HTML verification and G6 approval," a template carryover that sits awkwardly beside the disclosed finding that raw HTML contains the text. In context (`raw_html_verified:false` means "not yet re-verified by a second reviewer") it is not misleading, but the wording could be normalized.
- **A4 (ADVISORY — observation, no action required).** The bare-R1-vs-verbatim (g) nuance (hazard d) is already an open G6 item; the orchestrator may wish to note it explicitly on the G6 agenda.

## Conclusion

Every constraint value traces to a hash-guarded snapshot whose captured text supports it; the snapshot-honesty discipline, D-049/D-048/D-045 fidelity, fail-closed posture, per-variant isolation, effective-date discipline, engine-untouched constraint, and DRAFT-until-G6 labeling all hold; the binding capture-completeness proof is valid and the disclosed HTML/(g) discrepancy is honestly and adequately handled. Local rules suite (558), the new pack (100), modularity (EXIT 0), and snapshot sync all pass in my independent rerun. No gate-fatal content defect. The four advisories above are non-blocking; A1 and A2 are recommended orchestrator/rework touch-ups but do not affect any encoded value.

VERDICT: PASS
