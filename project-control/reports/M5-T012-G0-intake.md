# M5-T012 — G0 Intake (orchestrator)

- **Gate:** G0 (intake / readiness) — **Verdict:** PASS — **Reviewer:** orchestrator
- **Task:** M5-T012 — expose the accepted offline optimization toolkit (sensitivity / ranking / comparison / threshold) via the internal, feature-flag-gated scenario API (new `services/api/app/api/v1/scenario_analysis.py`).

## Readiness
- **Bounded scope:** new `scenario_analysis.py` + an ADDITIVE-ONLY edit to `services/api/app/main.py` (one import + one `include_router` + a mirroring comment) + `services/api/tests/api/test_scenario_analysis_api.py` + own report = 4 allowed_paths. forbidden_paths lock the entire engine package `services/api/app/scenario/**` (imported READ-ONLY through the public facade, never edited), the three accepted sibling routes (`scenario.py`, `rule_evaluation.py`, `properties.py`), **`app/config.py`** (the existing `internal_scenario_enabled` flag is REUSED — no new flag), profile/rules/spatial/connectors, every pre-existing test file, `packages/contracts/**`, `apps/web/**`, `supabase/**`, `tools/**`.
- `documented_test_commands` = two single clean segments (closed command profile, M0-T149): `python -m pytest services/api/tests/api` and `python -m pytest services/api/tests/scenario`.
- **8 executable AS:** (1) four POST endpoints, facts rebuilt SERVER-SIDE over the accepted M5-T003 seams, engine called READ-ONLY via the `app.scenario` facade; (2) the untrusted-input boundary — assumptions yes, FACTS never (an injected cap cannot change the echoed cap); (3) flag-gated generic-404 fail-safe + `include_in_schema=False`, config.py untouched; (4) documented `STATUS_STATE_MATRIX`, honest 200 non-errors, `X-Correlation-ID`, no leaky error bodies; (5) bounded body dimensions fail closed — deep/oversized/malformed bodies are a typed 422, never a raise/RecursionError/hang; (6) strict-JSON-safe, never Verified, cap verbatim; (7) additive `main.py` registration + facade-only imports + fully offline; (8) full regression on both suites (0 regression, existing test files unedited) + `modularity_check --check`.
- **Rationale (D-038-R003):** five gated engine modules are accepted but reachable only from Python — no client, and specifically not the parked Compare (Step-3) UI M5-T004, can call them. This is the bridge that turns the toolkit into a product surface. It is also the landing pad for the owner's incoming Supabase / NYC API credentials: the seams behind these endpoints go live without re-shaping the surface.
- **Design guard (the key correctness/security boundary reviewers must probe):** M5-T003 accepts no body precisely so a caller can never inject the FACTS a scenario rests on. These endpoints must accept caller input **without** weakening that: only illustrative analysis parameters are readable from the body; the scenario document — and therefore the canonical cap and every coverage/verification status — is always rebuilt server-side from the `bbl` path parameter. Any body key that would supply or override a fact is a typed 422, never a merge. Second guard: the body is the first genuinely UNTRUSTED input this codebase's scenario surface has taken, so every dimension is explicitly bounded and fails closed (this carries the M5-T009 / M5-T011 RecursionError lessons out to the edge).
- **Category note:** first D-038 product task touching the API layer rather than the contract-free engine package. Scope is deliberately one route module + an additive registration; no engine change, no contract change, no config change.
- Not G6-blocked (no new legal rule; the cap is transported verbatim and nothing is ever Verified); fully offline (no Supabase/Geoclient/network); no deferred credentials required (D-038-R004 by construction).
- directive_refs `D-038:ALL`; appended to R003/R004 applicability; requirements digest resynced (`37035a7d` → `32f8d252`) + manifest audit note added; `validate_directive_compliance.py --check` **EXIT 0**.
- Gates G0/G1/G3/G4/G5; reviewers code-reviewer / qa-engineer / security-reviewer (all ≠ producer `backend-engineer`).
- Dependencies M5-T003 and M5-T011 are both **accepted**.

Ready to claim and dispatch to the build loop.
