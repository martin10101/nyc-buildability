# M5-T106 — G3 geometry review (geospatial-engineer "geo-t106", read-only)

> Transmission history: pinned at 436e0e34, delivered as two SendMessage parts (1/2, 2/2) ending with END-OF-REPORT;
> no truncation. Joined verbatim by the orchestrator (transport wrapper tags removed only). Verdict PASS: DB-069 (a)-(d)
> closed on the scoped paths, mutant ME killed, public API stable. ADVISORY-1 (two remaining unbounded echoes at :812 and
> :926 and the module docstring's over-claim) is taken into a required-corrections round before acceptance.

---

M5-T106 G3 geometry review (geospatial-engineer, READ-ONLY). PART 1/2 — setup + verified.

SETUP: PIN HEAD 436e0e34 at start (unchanged). Material commit 966cca75 = EXACTLY the 3 allowed paths (git show --name-only: massing_model.py, test_massing_model.py, M5-T106-producer-report.md). [OBSERVED] from services/api: `ruff check .` = All checks passed; `pytest tests/scenario/test_massing_model.py -q` = 75 passed (65 prior + 10 added).

VERIFIED (all PASS, independent in-process probes):
- Raw-vertex NYC check (DB-069 a): `_require_raw_vertices_in_nyc_bounds` (massing_model.py:272-290) iterates EVERY raw vertex on BOTH axes, called at :344-345 BEFORE the collinear collapse (:362-371). [OBSERVED] a collinear x=2e6 spike (cross3=0.0, i.e. would collapse) → lot_ring_out_of_nyc_bounds/lot_ring; a LATER-position y-collinear spike likewise. Bounds identical to B0 (imports proposal.py NYC_2263_*; inclusive `<= x <=` both axes == proposal.py:255-256).
- Ordering preserved: magnitude check per-vertex in the parse loop (:331), NYC check after the whole loop (:344). [OBSERVED] 1e154 → coordinate_out_of_range/lot_ring[0]; 1e8+0.5 at a later vertex → coordinate_out_of_range/lot_ring[1]. The accepted test_t088_as4 meaning is intact (in the 75 pass).
- Edge-of-city lots inside (arithmetic; pyproj not needed): SI Tottenville, Bronx city line, Far Rockaway, Riverdale, Coney Is all within [9e5,1.1e6]x[1e5,3e5].
- ShapelyError wrap (DB-069 b): `_wrap_geos_errors` (:175-203) on build_massing_model (:786); build_from_generated_option (:903) delegates → covered. [OBSERVED] Topological/GeometryType/Dimension/GEOS all → geometry_engine_error; a typed MassingModelError (footprint_outside_lot) passes through (ValueError disjoint from ShapelyError). The WRAP docstring is now exact.
- DB-061 b (mutant ME): [OBSERVED] in-process — baseline test_t098_as4 pit_calls=0 GREEN; mutant (charge m-3 AFTER the inner scan) pit_calls=5 → RED. Killed. No new test needed; producer claim honest.
- Scope: no public name/signature changed (only private _preview + _require_raw_vertices_in_nyc_bounds added, _prepare_ring gained keyword-only nyc_range_check=False; __all__ unchanged) → M5-T107 public API stable. Both goldens byte-identical (recomputed b7fa9862 + e23b5cbc with exact fixtures; suite asserts pass). Zero new deps. Unwired (only tree-wide importer: test_massing_model.py). Modularity 958 SLOC = justification-band WARN, `modularity_check --check` exit 0; cohesion note honest.

PART 2 follows (advisories + verdict).

---

M5-T106 G3 review. PART 2/2 — advisories, verdict.

ADVISORY-1 (bounded-echo completeness + docstring exactness; NON-BLOCKING). The DB-069(d)-scoped LOT-vertex path IS fixed and AS-4 passes ([OBSERVED] huge lot vertex → non_finite, len 184, value elided, "chars>" marker; huge source → invalid_source, len 198). BUT two OTHER caller-input echoes in massing_model.py remain UNBOUNDED — both counterexamples to the module docstring (:66-67) "every refusal echo of caller input is length-bounded... a huge pasted vertex cannot amplify a message":
 (A) :926 `{detail or ''}` in build_from_generated_option no-candidate path. [OBSERVED] a 200,000-char candidate_placement.detail → 200,113-char message (value present). In-scope, UNDISCLOSED, trivially bound-able with _preview.
 (B) :812 `{exc}` re-echoes the B0 ProposedMassingError, which itself echoes `{vertex!r}` unbounded (proposal.py:250). [OBSERVED] a huge FOOTPRINT vertex → 200,144-char message. Footprint is primary user geometry, so "a huge pasted vertex cannot amplify" is materially false for it. Root cause is in FORBIDDEN proposal.py and the producer DID disclose it as D-OBS-2 (routed to wiring) — but the massing-layer re-echo at :812 could be bounded in-scope (_preview(exc)), and the docstring over-claims regardless.
Recommendation: narrow the docstring to the exact truth (the same DB-069(b) standard this packet applied to the wrap docstring) and/or wrap :812 and :926 in _preview; keep proposal.py:250 as D-OBS-2 for the wiring packet; flag echo-amplification to G5/security.
Why NON-BLOCKING: geometry is correct; the DB-069(d) lot-path target and every concrete AS-1..AS-6 pass; the footprint root is forbidden here + disclosed; M5-T098 accepted the identical echo-amplification class as a forward rider. Flagged for the orchestrator's decision if it wants docstring exactness held uniformly (trivial [ORCH-CORRECTED] doc/preview edit closes it).

ADVISORY-2 (favorable/stale). Producer report (:107-111) records `ruff check .` = "Found 6 errors" in forbidden placeholder files; at HEAD 436e0e34 those are resolved — [OBSERVED] `ruff check .` = All checks passed. The two T106 files are clean. The report line is time-stamped, not a defect.

BLOCKING: none.

BASIS: AS-1..AS-6 each verified with independent in-process probes; DB-069 (a)-(d) closed on the scoped paths; DB-061 (b) mutant ME killed; public API/goldens/deps/scope/unwired/modularity all clean. The two residual echo sites are message-amplification/docstring nits on a secondary or forbidden-root path — advisories, not blockers.

M5-T106 G3 VERDICT: PASS
END-OF-REPORT
