# M5-T061 G4 gate — independent QA/test-adequacy review (verbatim reviewer return)

Reviewer: qa-engineer (opus-4-8 xhigh per D-064), dispatched by the orchestrator at the
caddbf59 submit head, pinned to material 1f1db078 (content identity verified by LF digests —
the reviewer's sandbox worktree-isolation guard blocked ctl24 git plumbing, so it substituted
the stronger content-identity check; recorded here as the identity basis). Recorded by the
orchestrator; reviewer is read-only and records no gate itself.

---

All independent checks are green. ruff exit 0 corroborates the evidence map and resolves the producer report's conflicting exit-1 note (an explicit worker mis-cwd artifact). Here is my G4 gate report.

---

**VERDICT: PASS**

# G4 Gate Report — M5-T061 (DB-039 proposal-checks route hardening)

**Gate:** G4 (QA / test-adequacy) · **Reviewer:** qa-engineer (read-only) · **Date:** 2026-09-20

## 1. Pinned identity and content verification

- Material commit (short): `1f1db078` · submit head (short): `caddbf59` · frozen reviewed sha: `1f1db078…` (full 64-hex not quoted per gitleaks hook).
- **Git plumbing against the ctl24 primary checkout is blocked** by this session's worktree-isolation guard (I run inside a separate repo, `nyc-development-feasibility-claude-pack`; commondir differs from ctl24). I could not run `git cat-file`/`rev-parse` against ctl24. Per the read-only-reviewer evidence rule, I substituted a stronger content-identity check that needs no git.
- **Content identity verified by LF-normalized sha256** of the four reviewed files in ctl24 vs `M5-T061-evidence-map.json` — ALL FOUR MATCH (8-char prefixes): producer-report `bc0a47dc…`, `_proposal_fact_domains.py` `fd9fc4fe…`, `proposal_checks_api.py` `8c219a07…`, `test_proposal_checks_api.py` `7cbd9398…`. The ctl24 working tree I reviewed is byte-identical to the frozen reviewed content at `1f1db078`. Any disjoint M5-T058 peer commits do not touch this surface; I reviewed the frozen content, not a live HEAD snapshot.

## 2. Commands run (cwd + outcome; reproduced firsthand)

| Command | cwd | Outcome |
|---|---|---|
| `python -m pytest tests/api/test_proposal_checks_api.py -q` | `C:\…\ctl24\services\api` | **67 passed** in 8.20s (exit 0) — matches packet expectation |
| `python -m pytest tests/api -q` | `C:\…\ctl24\services\api` | **591 passed** in 75.78s (exit 0) — matches packet expectation |
| `python -m ruff check .` | `C:\…\ctl24\services\api` | **All checks passed!** (exit 0) — corroborates evidence-map ruff 0 |
| in-process probe (`_proposal_fact_domains` derivation/cache/charset) | ctl24/services/api on sys.path | all 20+ assertions true (see §4) |

(Runs invoked via a Python runner that `os.chdir`'s into ctl24/services/api, because the Bash guard refuses `cd` into the shared checkout; pytest requires that cwd for `app` imports.) I did not run the full services/api suite root or the documents tree (3.11-vs-3.12 interpreter artifact, per instructions); modularity is not a G4 concern (evidence-map records exit 0; route now 623 SLOC, sibling `_proposal_fact_domains.py` 381 SLOC after the permitted extraction — both within tier).

## 3. Packet acceptance-scenario → executable test mapping (every AS ≥ 1 test)

- **AS-1 (registry-derived numeric bounds, no invented limit):** `test_derive_input_domains_numeric_bounds_are_registry_derived`, `test_derive_input_domains_exclusive_bounds_reject_the_endpoint`, `test_numeric_bound_only_when_every_declaring_rule_bounds_the_direction`, `test_numeric_input_left_unbounded_has_no_window`, `test_route_numeric_fact_out_of_bounds_refused_before_engine` (422, field `lot_rule_facts.lot_depth_ft`, value never echoed). ✓
- **AS-2 (domain check before the O(n²) gate):** `test_domain_check_runs_before_the_input_gate` (gate spy `calls == []`), `test_registry_unavailable_500_is_recorded_before_the_gate` (500 + gate spy `calls == []`). ✓
- **AS-3 (per-registry memoization):** `test_domain_derivation_memoized_once_per_registry` (derive spy == 1 across 3 requests), `test_test_injected_registry_derives_its_own_domains`. ✓
- **AS-4 (wall-id charset alignment):** `test_wall_id_bad_charset_refused`, `test_wall_id_over_length_refused_length_only`, `test_wall_id_non_string_left_to_the_validator` (ordering-spy discriminator), `test_wall_id_conforming_round_trips_the_accepted_arithmetic` (coverage 0.625 / shortfall 0.125 byte-identical). ✓
- **AS-5 (exact-cap 200 vs above-cap refusal + shape branches):** at-cap 200 → `test_at_cap_scenario_label_accepted` (label 200), `test_body_at_exact_ceiling_valid_payload_returns_200` (body == MAX_BODY_BYTES), `test_at_wall_cap_exact_returns_200_for_a_valid_solid` (walls 500), `test_at_street_line_cap_exact_returns_200` (street 400); above-cap → `test_bp2_over_length_scenario_label_refused` (201→422), `test_oversized_body_is_413_before_parse` (+1→413), `test_bp4_over_wall_cap_refused` (501→422), `test_bp4_over_street_line_cap_refused` (401→422); shape branches → `test_build_lot_context_shape_refusals_each_bound` (7 branches, exact field per branch), `test_lot_rule_facts_not_object_is_422`. ✓
- **AS-6 (preservation):** 591/591 tests green (reproduced), `(status,state)` matrix pinned by `test_status_state_matrix_is_the_documented_set`, ruff 0 (reproduced), modularity 0 (evidence map). ✓
- Ancillary: flag-off 404 (`test_flag_off_is_generic_404_no_leak`, `test_route_absent_from_openapi`), non-JSON/empty/non-object/NaN/lone-surrogate 422s, generic bounded 500 (`test_forced_500_is_generic_and_bounded`, exact key set). ✓

## 4. Mutation-sensitivity per key behavior

- **Exact-cap boundaries (walls/street/label/body):** each is bracketed at-cap-200 ↔ above-cap-refusal. `>` → `>=` mutation on any cap flips the corresponding at-cap-200 test red; cap `-1`/`+1` mutations break one side of the bracket. **Strong.**
- **Numeric domain refusals:** `_NumericBounds.refusal_reason` inclusive/exclusive endpoints are pinned (`refusal_reason(10.0) is None` vs `refusal_reason(9.0) is not None`; exclusive endpoint refused). Probe confirmed inclusive floor accepted, below/above refused, exclusive endpoint refused, `describe()` = `>= 10.0 and <= 500.0` never echoes the value. "No invented limit" pinned by the unbounded-input and every-declaring-rule tests (a rule leaving a direction open ⇒ 1e9 accepted). **Strong.**
- **Domain-check-before-gate ordering:** `test_domain_check_runs_before_the_input_gate` spies `validate_proposed_massing_input`; a regression to gate-first records a call ⇒ `calls == []` fails. Registry-unavailable-500-before-gate likewise spy-pinned. **Strong** — a genuine order-regression-detecting test exists.
- **Per-registry memoization + cache keying:** probe confirmed two distinct fake registries derive distinct domains (`regA` min/max 10/500 vs `regB` 100/200), same registry returns the same memoized object, 4 lookups ⇒ 1 derive, `clear_input_domain_cache` empties the dict. A single-slot/mis-keyed cache mutation is ALSO caught end-to-end by the suite: interleaved production-registry tests (e.g. `test_bp3_…` line 322) would poison a leaking slot before the fixture-registry domain tests (`test_bp5_out_of_domain_street_width_class_refused` L467, `…_value_derived_from_the_registry` L486), which rely on the fixture's narrower `{R5}`/`{wide,narrow}` vocabulary to refuse `R6`/`"medium"`. **Adequate** (see gap G-2).
- **Wall-id charset:** removing `_validate_exterior_wall_ids` lets a short markup id (`<b>south</b>`, under the gate's 512 ceiling) ride through to a 200/other-field error ⇒ `test_wall_id_bad_charset_refused` (expects 422, field `…exterior_walls[0].id`) flips red. Non-string-id path pinned by ordering spy. **Strong.**
- **`_build_lot_context` shape refusals:** each of 7 branches directly asserted with its exact field; removing any branch flips one assertion. **Strong.**

## 5. Findings (advisory; none blocking)

- **G-1 (LOW, advisory):** `clear_input_domain_cache()` has no mutation-pinning test. Every test that calls it uses a function-scoped (freshly-constructed) registry, so a no-op `clear` would not flip any test red (a fresh object is a new WeakKeyDictionary key regardless of prior state). It is a test-only helper (production is load-once and never invalidates); my probe independently confirms it correctly empties the cache, so there is no latent defect. Not blocking.
- **G-2 (LOW, advisory):** Numeric out-of-bounds refusal ordering (before the gate) is not independently spy-asserted (`test_route_numeric_fact_out_of_bounds_refused_before_engine` asserts status/field/no-echo only). It is covered transitively: enum and numeric refusals share the exact call site (`_validate_lot_rule_fact_domains`, route line 559, ahead of the gate at line 571), and `test_domain_check_runs_before_the_input_gate` spy-proves that site fires before the gate. Not blocking.

## 6. Notes

- Producer report and evidence map disagree on ruff/pytest exits (report: ruff exit 1 / pytest exit 4 "authoritative"; evidence map: ruff 0 / 67 / 591 / modularity 0). The report is explicit that its exit-1/exit-4 outcomes are the loop worker's **mis-cwd invocation artifacts**, superseded by the orchestrator's fresh explicit-cwd harvest. I **reproduced the superseding results firsthand** (ruff 0, 67 passed, 591 passed), so the conflict resolves in favor of the evidence map. This is a provenance-honesty artifact, not a test-adequacy defect (a DCV/G3 concern, not G4).
- The B2 rectangle arithmetic (coverage 0.625 / shortfall 0.125, height 30≤60) round-trips byte-identically through the hardened route (AS-1 tests), confirming the reorder/extraction did not disturb accepted behavior.

## 7. Required corrections

None. No C-n corrections. The test pack pins every claimed behavior with mutation-sensitive assertions; the two findings above are LOW-severity advisories for a future packet, not acceptance blockers.

---

Files reviewed (all absolute, ctl24 primary checkout):
- `C:\Users\MLFLL\Downloads\nyc-zoning\ctl24\services\api\app\api\v1\proposal_checks_api.py`
- `C:\Users\MLFLL\Downloads\nyc-zoning\ctl24\services\api\app\api\v1\_proposal_fact_domains.py`
- `C:\Users\MLFLL\Downloads\nyc-zoning\ctl24\services\api\tests\api\test_proposal_checks_api.py`
- `C:\Users\MLFLL\Downloads\nyc-zoning\ctl24\project-control\tasks\M5-T061.json`
- `C:\Users\MLFLL\Downloads\nyc-zoning\ctl24\project-control\reports\M5-T061-producer-report.md`
- `C:\Users\MLFLL\Downloads\nyc-zoning\ctl24\project-control\reports\M5-T061-evidence-map.json`

**Verdict: PASS** — record G4 as PASS for M5-T061 at material commit `1f1db078` (content verified byte-identical via LF-digests). Two LOW-severity advisory findings (G-1 `clear_input_domain_cache` not mutation-pinned; G-2 numeric-ordering covered transitively) may be routed to the discovery backlog; neither blocks acceptance.
