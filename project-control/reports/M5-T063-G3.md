# M5-T063 G3 gate — independent code review (verbatim reviewer return) — FAIL

Reviewer: code-reviewer (opus-4-8 xhigh per D-064), dispatched by the orchestrator at the
7839035c submit seam, pinned to material f33b752c (submitted at f98924e5). Recorded by the
orchestrator; reviewer is read-only and records no gate itself. VERDICT: FAIL with ONE
required correction (F1) — handled by the recorded [ORCH-CORRECTED] cluster + delta
re-attestation remedy.

---

I have completed my independent verification. Let me compile the findings. First, let me confirm the bounded.ts diff is strictly additive (no byte changes to pre-existing helpers) via the hunk headers already seen, and confirm the 11 material files are all within allowed_paths.

The material commit `f33b752c` diff showed only additions in bounded.ts (no `-` lines touching `boundedText`/`boundedToken`), and AnalysisIdentityNotice.tsx changed exactly one `<p>` line. All verifications are complete.

---

**VERDICT: FAIL**

# G3 Code-Review Gate Report — M5-T063 (web condo rider cluster)

## Identity (read-only plumbing, verified)
- Reviewed HEAD at review time: `7839035c` (submit seam; disjoint peers may advance HEAD — material identity is byte-stable).
- Material commit: `f33b752c` (11 files changed; 472 ins/ 14 del). Confirmed via `git show --stat`.
- Submit HEAD: `f98924e5`; producer commit (worktree): `50e8978e`; claim seam: `ae8be2ed`.
- Material zero-drift: `git diff --stat f33b752c f98924e5` over all 11 material paths returns empty — no drift across the intervening T064/T065 seams.
- PropertyOverview.tsx: NOT in `f33b752c` (`git diff --stat f33b752c^ f33b752c -- ...PropertyOverview.tsx` empty) — the 12th allowed_path is byte-unchanged as declared.
- All 11 changed files are inside `allowed_paths`; zero forbidden/T060/T062-lane files touched.
- No new dependency (only intra-lib import `boundedZoningDistrict` added to condo-records.ts).

## Verdict rationale
Ten of eleven criteria pass on independent read. One blocking defect (F1) in the reworked announcer: its stamp gate is a pure *shape* check that does NOT mirror the visible `AnalysisIdentityNotice` correspondence discipline, so it will announce a legitimizing "analyzed on base lot" message for a stamp the visible guard withholds/ignores. This is exactly the drift the packet flagged as CRITICAL and the AS-3 / DB-042(c)/HJ-3 requirement ("screen-reader text must not trail / must match the visible labels").

---

## Findings (severity-ranked)

### F1 — REQUIRED CORRECTION (blocking): announcer legitimizes a stamp the visible guard would withhold
`apps/web/src/lib/rule-evaluation.ts:529-543` (the reworked `substrate_substitution` branch).

The announcer emits the positive substitution message when ONLY these hold: `entered_bbl` non-empty string, `analyzed_bbl` non-empty string, `analyzed_bbl !== entered_bbl`. It does **not** check `entered_bbl === document.evaluated_input.bbl`.

The visible guard `AnalysisIdentityNotice.tsx:54-61` `stampLegitimate` additionally requires `substitution.entered_bbl === analyzedBbl` (= `document.evaluated_input.bbl`) **and** `=== requestedBbl`. Its JSDoc (`:14-23`) states the discipline explicitly: "A stamp that does NOT correspond is ignored, so the fail-safe withhold guard still governs." The visible spec pins this in four dedicated non-corresponding cases (`analysis-identity-substitution.test.tsx:126-168`), including `:142-155` "ignores a stamp whose entered_bbl disagrees with evaluated_input.bbl (still withholds)".

The producer report and the in-code comment (`rule-evaluation.ts:527-528`, "the same shape the visible record requires") claim parity. That claim is inaccurate: the visible record requires *correspondence*, not merely a well-formed shape.

Reproducible drift (contract-valid document; each substitution field is a non-empty string, so `validateRuleEvaluationDocument` / `checkSubstrateSubstitution` — shape-only, no cross-check — admits it):
- `document.evaluated_input.bbl = "X"`, `substrate_substitution = { entered_bbl: "Q", analyzed_bbl: "Z" }`, with `Q !== "X"`, `Z !== "Q"`.
- At the `ArchitectEntry.tsx:73-78` call site the announcer only runs when `evaluated_input.bbl === profile.identity.bbl` (= `requestedBbl`), i.e. `X === requestedBbl`. So this document reaches the announcer.
- Announcer output: `"…analyzed on the recorded base tax lot Z for the condo billing lot Q you entered — a city record of the resolution, not a computed allowance…"`.
- Same-screen visible `AnalysisIdentityNotice` (`ArchitectEntry.tsx:174`, same `requestedBbl` + same document): `stampLegitimate` false (`entered_bbl "Q" !== requestedBbl "X"`); then `analyzedBbl ("X") === requestedBbl ("X")` → returns `null` → renders NOTHING.
- Result: the screen-reader announces a substitution naming BBLs `Q`/`Z` that appear nowhere on screen; the sighted surface shows no substitution record. A false a11y statement on a fail-safe honesty surface.

The omitted check is fully within the announcer's information reach — `evaluated_input.bbl` is a required contract field (`rule-evaluation-contract.ts:229-233`) and is on the same `document`. The visible guard's `requestedBbl` correspondence remains structurally out of the announcer's reach and is guarded upstream at `ArchitectEntry.tsx:73-76` (the inline mismatch announcement), which is acceptable and should be noted.

Required correction:
1. Gate the announcer's substitution branch on the in-document correspondence it can enforce, e.g. add `typeof document.evaluated_input.bbl === "string" && substitution.entered_bbl === document.evaluated_input.bbl` so a non-corresponding stamp falls through to the `condo_base_lot_unresolved` / generic classifier instead of announcing a legitimizing substitution.
2. Add a mutation-sensitive test in `rule-evaluation.test.ts` feeding a stamp with `entered_bbl !== evaluated_input.bbl` and asserting the announcer does NOT emit the substitution string (falls back to the generic/condo announcement). The current announcer suite (`:527-542`) only exercises `substitutionStampDoc()`, whose fixture sets `evaluated_input.bbl === entered_bbl` (`rule-evaluation-fixtures.ts:57-63`) — the corresponding case only — so the drift is presently untested (weak-test gap folded into F1).
3. Correct the `:527-528` comment so it no longer claims parity with the visible record's requirements.

---

## Passing verifications (evidence-bound)

**AS-1 sanitizer — PASS.** `bounded.ts` diff is strictly additive (only `MAX_ZONING_DISTRICT_LENGTH` :22, `NON_ZONING_DISTRICT_CHARS` :32-35, `boundedZoningDistrict` :75-99 added; no `-` hunks on `boundedText`/`boundedToken`). Charset `[^A-Za-z0-9/-]` — trailing `-` literal, `/` literal, no unintended range. Admits `M1-5`, `R7-2`, `R6`, `R10H`, `C6-4` and slash class `M1-5/R7-2` byte-exact (`bounded.test.ts:70-81`); length-capped to 32 (`:95-97`); non-string/empty/fully-hostile → `null` (`:99-105`). boundedToken slash-strip pin intact (`bounded.test.ts:64-66` asserts `boundedToken("M1-5/R7-2") === "M1-5R7-2"`), proving the two vocabularies stay distinct.

**"Rework correction" claim — VERIFIED TRUE.** `boundedZoningDistrict("<script>M1-5/R7-2</script>")`: strip `<`/`>` → `script` + `M1-5/R7-2` + `/script` = `scriptM1-5/R7-2/script`; the closing-tag `/` legitimately survives the district charset. Matches the corrected expectation `bounded.test.ts:90`. Residue is inert plain text (no `<`/`>`; React escapes reflected text regardless). Corrected assertion is TEST-ONLY; `bounded.ts` unchanged for it.

**AS-2 honest gating — PASS (code path).** `condo-records.ts:349` routes `recorded_zoning` through `boundedZoningDistrict` (not `boundedToken`); status derivation `:352-354` (`recorded_zoning_status` → fallback `recorded`/`unknown`); `recorded_zoning_dependency` parsed `:535-538`. `divergentZoningNotice` interface doc reworded permanent→dormant-unless-recorded (`:210-216`) and matches the actual PropertyOverview gate (byte-unchanged). Specs: `condo-records.test.ts:439-461` (status/dependency parse + fallback), `:463-479` slash-survives revert-killer; `condo-resolution-display.test.tsx:754-764` (no zoning → notice absent + total gap), `:766-779` (mixed → notice present + partial gap, recorded districts protected).

**AS-3 announcer specificity — PARTIAL / see F1.** The condo_base_lot_unresolved branch (`:544-545`, `:517-518`) and the substitution message strings are correctly more specific than the generic classifier and carry no "verified"/"best" wording (`rule-evaluation.test.ts:512-542`). Specificity requirement met; correspondence discipline not met (F1).

**AS-4 wording/definitions + withhold guard — PASS.** `AnalysisIdentityNotice.tsx` diff is exactly one `<p>` line (`:66`): inline "condo billing lot (the single tax lot a condo is billed under)" and "the land parcel the city records as this condo's base", consistent "a city record of the documented resolution, not a computed allowance" framing. `stampLegitimate` gate (`:54-61`) and the `role="alert"` withhold branch (`:72-79`) are behaviorally unchanged (diff-verified copy-only). Both-directions withhold specs (`analysis-identity-substitution.test.tsx:94-179`) untouched and green in direction.

**AS-5 hygiene — PASS.** S11 describe label + comment now name the true scope (rider-i cap superseded by rider-c a11y honesty; `address-confirm.test.tsx:800-809`, DB-038(b)). No empty spec files; DOM queries typed (`container.querySelector<HTMLElement>` at `analysis-identity-substitution.test.tsx:176`; testid/role queries elsewhere).

**AS-6 preservation — PASS.** PropertyOverview.tsx byte-unchanged (plumbing-verified); export surface/props intact; T060/T062-lane files untouched; modularity exit 0 per orchestrator-captured evidence (`M5-T063-evidence-map.json`; thin-client — not re-run here).

## Modularity / dependency / directive spot-check
- Modularity: additive helper in the existing sanitizer lib; announcer branch grew within `rule-evaluation.ts`; no dumping-ground or responsibility mixing. `python tools/modularity_check.py --check` recorded exit 0 (467 files / 0 failures / 20 pre-existing out-of-scope warnings) — orchestrator/producer captured; not independently re-run (thin client).
- No new dependency. D-073-R006 (recorded_zoning display-only) holds: `boundedZoningDistrict` output feeds rendering only; no allowance-input wiring introduced.
- D-066-R001 (bounded.ts additive-only; PropertyOverview byte-compatible): satisfied. Full per-requirement directive verification (D-066/D-073/D-077) is the `directive-compliance-verifier`'s pass, not this G3.

## CI evidence
Web behavior proves only in CI (thin client). Orchestrator-captured evidence (`M5-T063-evidence-map.json`) reports CI at `f33b752c` (web vitest/typecheck/lint/e2e + secret-scan + context-budget) all success. Material paths are zero-drift from `f33b752c` to reviewed head, so that CI identity covers the reviewed material. Not independently re-run here; verified as stored evidence per the evidence-division rule. NOTE: adding the F1 test will require a fresh CI run at the corrected head.

## Required corrections summary
- **F1 (blocking):** `rule-evaluation.ts:529-543` — add the in-document `entered_bbl === evaluated_input.bbl` correspondence gate to the announcer's substitution branch to mirror `AnalysisIdentityNotice.stampLegitimate`; add a non-corresponding-stamp regression test in `rule-evaluation.test.ts`; correct the misleading "same shape the visible record requires" comment. Route as rework to the packet's frontend producer (in scope: `rule-evaluation.ts` + `rule-evaluation.test.ts`, both already in `allowed_paths`).

All other G3 criteria PASS.
