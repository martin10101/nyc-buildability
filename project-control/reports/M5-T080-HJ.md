# M5-T080 — human-journey meaning review (human-journey-reviewer "hj-p0", read-only)

> Transmission history: pinned at 33662211, delivered as nine SendMessage parts (1/9..9/9) ending with
> END-OF-REPORT; no truncation. Joined verbatim in delivery order by the orchestrator (transport wrapper
> tags removed only). Verdict: FAIL (HJ-1..HJ-5 BLOCKING). HJ is not a required gate for this packet; its
> blocking findings are folded into the M5-T080 rework with the G3 corrections.

---

M5-T080 HJ meaning review (D-086 P0 disclosure ledger) — part 1 of 9.

Pinned HEAD 33662211137a36f0ff4b7c8ac8ae779f75943f8a. The ledger's own pin is b56f3d5b; apps/web differs from it only by the in-flight M5-T078/T079 units (see HJ-10). Read-only: no npm/node/dev server, no project_control.py, no directive test suite.

Method: read P0-RECONCILIATION.md, both ledgers, D-086 R001–R004, D-083 R001–R013 and assessment §§2–14. Read all 155 Part A rows and about 140 of the 226 Part B rows in full (every A/C/F/E/R/LS-P/LS-C/LS-E row; SR and DB sampled), plus every consolidate row. Checked claims against source: ReportView, ProfileViews, PropertyFacts, ScenarioCard, ScenarioResult, ScenarioConstraints, CalculationEvidence, MissingInputsSection, ArchitectShell, ArchitectEntry, the three /property route pages, the architect.css print and ≤700px rules, and every role="alert" / role="status" / aria-live site.

Completeness holds: re-running the §6 enumeration gives 381 ids, each exactly once across the two files; the 4 replacement_ref values (LS-C24, LS-C22, LS-C06, SR12) resolve; there are no retire rows.

What holds up: the primary-state and "more detail" columns are faithful and specific for most rows I sampled, e.g. SH-05, SH-09, SH-14, PE-01, PE-10, PC-02, PC-03, ME-01 to ME-04, DR-05 to DR-08, M01, ZC-01, ZC-02, AD11, AD12, AC03 (city warnings stay above Continue), AC10, A04 to A07, A09, A12, A15, C05, C08, F03, F08, LS-C13 (scope note stays directly under the number), LS-C16, LS-E10, SR24, SR48. Consolidations LS-C08→LS-C24 and SR27→SR12 are faithful.

What fails: the print and accessibility columns are mostly one repeated sentence (Part A: 144/155 rows share one print line and 145/155 share one accessibility line; Part B: 188/226 share one accessibility line), and the print column is factually wrong in both directions. Two planned moves bury a caveat or split it from its number.

Blocking: HJ-1 to HJ-5. Advisory: HJ-6 to HJ-11.

---

M5-T080 HJ review — part 2 of 9.

HJ-1 BLOCKING — print: caveats that print today are marked "no print regression to preserve".

All 72 LS-P/LS-C/LS-E/LS-F rows in disclosure-ledger-b.json say: "P0-proposed: not part of the architect printed brief; this legacy route has no dedicated print/export surface - no print regression to preserve".

That is false for 16 rows. The architect property brief prints these shared components, and its print handler opens every non-raw disclosure (ReportView.tsx:58):
- ReportView.tsx:104 PropertyFacts → FactsTable → CoverageBadge + ProvenanceDisclosure
- :108 ZoningSection
- :113 OpenIssues → ConflictsSection, MissingInputsSection, UnsupportedSection, ProfessionalReviewPanel, CoverageLegend (ProfileViews.tsx:60-71)
- :115-116 ScenarioConstraints, ScenarioAssumptions (when a matching scenario is shown)

Rows: LS-P03, LS-P04, LS-P05, LS-P06, LS-P07, LS-P08, LS-P09, LS-P10, LS-P11, LS-P12, LS-P13, LS-P14, LS-P15, LS-C17, LS-C18, LS-C19.

Caveats that travel with printed numbers and would lose their print protection, e.g.:
- LS-P11 "Official records disagree on the values below. All values are shown with their source. Nothing has been resolved automatically."
- LS-C18 "A constraint recorded as missing is a gap: it may never be inferred, defaulted, or estimated, and no number on this screen accounts for it." (ScenarioConstraints.tsx:83-88)
- LS-C17 "Nothing has been assumed, factored, or discounted"
- LS-P07 "Multiple districts on one lot (a split zoning lot) are all shown."

The column also misses real print behaviour it should record: LS-P10's extra missing fields sit behind a button (MissingInputsSection.tsx:76) and print hides report buttons (architect.css:140), so paper shows "Missing official inputs ({total})" without the administrative fields or the "Show N more" cue. LS-P15's full captured JSON sits in non-raw disclosures, so it prints by default.

"Legacy route" is wrong on screen too: these are shared primitives in the architect facts, zoning, issues and scenarios views (assessment §8 intro: "Shared primitives also occur in the architect workspace").

Fix: for these 16 rows, record "prints in the property brief today; must stay in default print", route them to the P5 print gate, and label them "shared primitive (architect + legacy)".

---

M5-T080 HJ review — part 3 of 9.

HJ-2 BLOCKING — print: "prints" is claimed where nothing prints, which hides real gaps.

144 of 155 Part A rows share one line: "P0-proposed: the material state/value/scope prints beside the number; full record in the report appendix (assessment sec 12.7)." The proposal, limits, drawing, check, variation, address and map surfaces are not in the brief at all (MaxEnvelopePanel and ProposalEditor mount only in ArchitectEntry; ReportView has no map). The same line is applied to rows that have no number (SH-04 BBL format errors, DR-08 click instructions, ME-10 "the request was too large to send").

Three real print gaps are recorded as printed:
- A01 says "Prints in the property brief...". The "Internal development build" badge and its banner (ArchitectShell) sit in .architect-topbar, which architect.css:140 hides in print. LS-P01 says the opposite about the same banner. Result: the "must not be shared outside the engineering team" restriction never reaches paper, and the ledger certifies that it does.
- A03: the nav footnote "Preliminary analysis / Professional review required" is hidden in print (.architect-nav) and at ≤700px (.architect-nav-footnote).
- SH-08: "Searched address retained · PLUTO representative address: …" (ArchitectEntry.tsx:182) is inside the hidden .architect-property-header. The brief's title is the searched label (ArchitectEntry.tsx:106), so a printout can show the searched address with no note that the city record's address differs.

Fix: give each row its true print status ("not printed today" or "printed via <component>"), and open explicit P5 decisions for the environment restriction and the identity alias on paper.

---

M5-T080 HJ review — part 4 of 9.

HJ-3 BLOCKING — E12 sends a legal caveat to the raw tier, which drops it from default print.

E12 "more detail" destination: "Verbatim reason, fallback_direction_note and D-052 source provenance in the raw/technical tier." E12 print: "the exact record prints only under the selected audit appendix (raw tier)".

Today CalculationEvidence.tsx:43 renders evaluation.wide_street.reason and :45 renders fallback_direction_note as plain paragraphs inside the brief's non-raw Calculations section, so both print by default. The note is the real server constant (fixture report-view.test.tsx:122-129) and states the limit of the conservative fallback: "validated for the ZR 23-22 conditional-FAR rows only ... This is not a universal 'narrow is always conservative' claim". That is a legal-scope caveat on the governing FAR.

The assessment's E12 triage sent only the "internal D-052 ID" to the technical tier; the ledger goes further. Its proof owner (development-limits.test.tsx:589-682) tests the DevelopmentLimits panel, not CalculationEvidence or print, so it would not catch the loss.

Fix: the reason and fallback note stay in the readable detail tier (which prints by default); only IDs and the captured record go to raw.

---

M5-T080 HJ review — part 5 of 9.

HJ-4 BLOCKING — LS-C06 + LS-C27 separate the cap number from its professional-review flag.

LS-C06 "more detail" destination: "document.cap_label and the DRAFT/professional-review flag reachable." Today the flag sits right after the number: ScenarioCard.tsx:67-71 "(DRAFT — needs professional review, not Verified)" when needs_review is set.

LS-C27 (consolidate → LS-C06) retires the card that says "This scenario is flagged for professional review before any reliance." (ScenarioResult.tsx:213-217, when professional_review_required is set) into LS-C06, whose own flag is planned behind a click. After both moves, a flagged cap shows no review flag until the user opens a detail. The other half of that card is the disclaimer, which belongs to LS-C05, but replacement_ref names only LS-C06.

This contradicts .claude/rules/frontend-web.md ("Keep conflicts, unsupported checks, and professional-review flags visible in results") and assessment §12 rule 1 ("Always visible at the relevant decision: ... draft/review scope"). The proof owner (compare-screen.test.tsx:52-87) queries the DOM, so a flag inside a closed disclosure still passes — the "passing a string assertion while hiding a critical warning" trap named in assessment §8.6.

Fix: LS-C06's primary state keeps both conditional flags beside the value; LS-C27's replacement_ref becomes LS-C06 + LS-C05, with professional_review_required mapped to a visible state.

Related (advisory): A06's primary state ("One large cap cell with attached 'FAR only' and 'Envelope incomplete' text labels") omits the readable per-cap status (Conditional / Professional review required / Data conflict / Unsupported); only its proof owner (development-limits.test.tsx:417-426) holds it.

---

M5-T080 HJ review — part 6 of 9.

HJ-5 BLOCKING — the accessibility column is one repeated sentence, not a destination.

Part A: 145 of 155 rows read "P0-proposed: keyboard- and touch-reachable control with a meaningful accessible name; one polite announcement on state change; focus returns on close (assessment sec 12.5/12.8)." Part B: 188 of 226 read "P0-proposed: label+symbol, not color-only; expansion reachable by keyboard and touch with an accessible name; no tooltip-only meaning". Assessment §14 asks, per row, for role/name, keyboard/touch path, announcement and focus behaviour.

Harms if a designer follows it:
(a) Silent downgrade from alert to polite. These use role="alert" today: SH-10 (ArchitectEntry.tsx:222, :234); SH-14 (app/property/error.tsx:37 — its own proof owner property-error-boundary.test.tsx:80-163 pins the alert, per LS-T15); PE-07 (ProposalEditor.tsx:413); PC-06 (ProposalCheckReport.tsx:215); ME-06 (MaxEnvelopePanel.tsx:285 at the ledger pin); DR-07 and DR-11 to DR-22 (ProposalOutlineDraw.tsx:306, refusal outcomes). Survey errors with role="alert" (CorrectionForm.tsx:90 stale notice, :146, :151; ReasonForm.tsx:67, :72; FocusedItem.tsx:166; ConfirmDocumentPanel.tsx:180) get only Part B's visual sentence, with no announcement rule.
(b) Duplicated live regions. 145 rows each promise "one polite announcement" and none names the region that speaks (the shared OutcomeAnnouncer, the single drawing status region M5-T078 adds, LotOutlineMap's summary). Per-element announcements are how competing live regions arise (DB-049; assessment §12 rule 8). A reviewer cannot check "not duplicated" from this column.
(c) Part A's column never says "not colour-only"; that appears only in PC-01's detail column and in M13.
(d) SH-01, the required-disclaimer footer (a labelled contentinfo landmark), is told "control ... focus returns on close". It is not a control.

What is real: the 10 Part A keep-rows for announcements (SH-05, PE-04, PE-08, DR-04, DR-09, DR-23, ME-07, ME-08, ME-17, M13), LS-P16, and the 17 LS-F rows ("One arrival announcement via the persistent OutcomeAnnouncer; focus moves to the outcome heading...").

Fix: for every L row with a changing state, name the speaking region, alert vs status, and the focus rule; keep existing alerts unless a reviewed change says otherwise.

---

M5-T080 HJ review — part 7 of 9.

HJ-6 ADVISORY — D-083 claim classes survive; three tightenings.

Survives: ME-04 "Separate 'Generated building option' card with class badge and estimate qualifier"; ME-03 "Persistent Incomplete/unchecked count and conflict state ... no approval/maximum-building headline"; ME-02 conflict "surfaced, not resolved"; A06 "never labelled 'Buildable area'"; LS-C16 "Buildable envelope: Not established"; A12 "'Unknown/unassessed' never renders as Pass". The limits panel's aggregate stays incomplete while anything is unchecked.

Tighten:
(a) ME-05's primary state ("Answer-first compact states/actions...") does not pin the heading "Preliminary development limits" or the visible qualifier "a rules-derived estimate that requires professional review, not a maximum permitted building", which is the only on-screen "not a maximum permitted building" for the tiles. Only ME-03's proof owner (max-envelope-panel.test.tsx:119-140) holds the title.
(b) D-083 is cited only on ME-02 and ME-04; D-083-R004 is cited nowhere.
(c) PC-05 "Fail / Unchecked / Pass count tabs" lets a user land on Pass with unchecked rows out of view; assessment §5.9 says "Failed and unchecked rows lead". Add an explicit "Incomplete — N could not be checked" state and a failed/unchecked default. Same for PV-01.

HJ-7 ADVISORY — the route/state map (P0-RECONCILIATION §2) cannot be followed as written.
- "/property/confirm → AC*, M*, LC*": with the flag on, that route renders the architect overview (confirm/page.tsx:24). The address search and confirm card mount from ArchitectEntry.tsx:67, the /property no-BBL state.
- "/property/compare → SW*, ZC*, ME*, §7 A/C": ME* mounts only in the proposal view; ZC-01 to ZC-03 in the overview (PropertyOverview.tsx:386); A05 to A12 and C* in the overview and the report.
- AD* (28 rows) appear nowhere, and the no-BBL search state has no row.
- No per-family split between the architect and legacy flag variants (assessment §2 point 4), and no marker for the one view that prints (view=report).
Fix: one row per route + flag + view, with family lists taken from the mount sites.

---

M5-T080 HJ review — part 8 of 9.

HJ-8 ADVISORY — freshness and sample pointers.
P0-RECONCILIATION §5 routes the §13 High "report freshness" finding to "Ledger PE-08/PE-10/PV-02"; none of those carries a stale state. The rows that need "Changed since check" are PC-04, PC-05 and PV-01 (assessment §5.9: "Not checked / Changed since check").
PE-02's primary state gives inputs "User supplied" provenance; until PE-10's fix lands, that badge would sit on the example seed (R5 / wide / 8,000 sq ft). Cross-reference PE-10 (the assessment says "User supplied or actual provenance").

HJ-9 ADVISORY — removal bookkeeping.
52 rows carry an R mark; only the 4 consolidate rows have a replacement_ref. Cross-row duplicates have no named survivor: A03 (its own question: "Which occurrence is canonical...?"), A15, E03. A03's current element is hidden at ≤700px and in print, so it cannot be the survivor.
LS-C23 → LS-C22: the surviving row lacks the "missing (n)" count, the named blockers, and the empty state "No coverage-matrix family is recorded as missing for this scenario."

HJ-10 ADVISORY — drift at the current head.
HEAD contains the M5-T078/T079 units (5879cdfa, 006f3589; 11 apps/web files). New honesty text has no row: MaxEnvelopePanel "Could not check — this development limit's response broke the binding-or-gap data contract and was withheld." and "...so the value is withheld and this preliminary picture stays incomplete."; ProposalOutlineDraw's omitted-row notice ("Convert will include N point(s)... not filled in and will not be included"), "...converted to numeric coordinates; N row(s) without both coordinates were not included.", and "Conversion is already in progress." The in-flight rider must add rows, not only re-check spans. M01/M02/M05/M11 proof spans in lot-outline-map.test.tsx shifted, but those rows are not flagged in-flight.

HJ-11 ADVISORY — proof column.
Part A: 106 rows name a test file with no lines; 14 name only a phase (PV-01 to PV-03 "P4 (proposal variations spec)" — no such spec exists; variations are tested in proposal-editor.test.tsx). Most proofs are text assertions; D-086-R003 also requires a visibility proof (for example "not inside a closed disclosure") for each primary-state L row. SH-01 says the disclaimer prints "in the report appendix (PRD 29; ReportView)"; it actually prints as the root layout footer (app/layout.tsx).

---

M5-T080 HJ review — part 9 of 9.

Answers to the five questions:
(1) Destinations are faithful for most rows. Two planned moves bury or split a caveat: E12 (HJ-3) and LS-C06/LS-C27 (HJ-4).
(2) Yes for the limits panel and the generated option. "Aggregate stays incomplete" is not bound on the proposal check summary or the variation comparison (HJ-6).
(3) No. The print column is wrong in both directions (HJ-1, HJ-2), and E12 drops a caveat from default print (HJ-3).
(4) No. It is boilerplate except the announcement keep-rows and the LS-F rows; it would downgrade existing alerts and cannot show "not duplicated" (HJ-5).
(5) No. Families are placed on the wrong routes, AD* is missing, and flag variants are not split (HJ-7).

To reach PASS: correct HJ-1 to HJ-5 row by row in the two ledgers. HJ-6 to HJ-11 are recommended in the same pass.

M5-T080 HJ VERDICT: FAIL

END-OF-REPORT
