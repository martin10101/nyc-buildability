# M5-T035 — G3 code review return (VERBATIM; code-reviewer, read-only, pinned 97fa2eea)

Orchestrator note: returned via the agent-return channel 2026-09-18T06:30:53Z; the transport
truncated the first delivery mid-criterion 4. Part 1 below is the truncated delivery verbatim;
Part 2 is the reviewer's resent remainder, verbatim. VERDICT: PASS (two non-blocking
observations + one recorded cohesion justification).

---

## Part 1 (first delivery, verbatim)

I have everything I need. All ten files reviewed against the diff, all six documented checks re-run green locally, out-of-scope consumer swept, and the binding rulings verified. Here is my gate report.

---

## G3 CODE REVIEW — M5-T035 (DB-015 live wide-street determination provider)

**VERDICT: PASS** (with two non-blocking observations and one recorded cohesion justification the modularity policy requires)

### Identity / scope check
- Material commit reviewed: `97fa2eea`. Live HEAD `3ce7b87d`. The delta between them is **only** four project-control files (`M5-T035-evidence-map.json`, `reports/M5-T035.json`, `state.json`, `tasks/M5-T035.json`); **zero** files under `services/api/**` or `docs/` differ (`git diff --name-only 97fa2eea 3ce7b87d -- services/api docs/…` returns empty). The reviewed allowed-paths content is byte-stable between the material commit and HEAD — the identity check holds.
- Diff footprint matches the packet's ten allowed paths exactly; no out-of-scope files touched.

### Verification I ran (read-only, this checkout)
- `python -m ruff check services/api` → clean (exit 0).
- `python tools/modularity_check.py --check` → 0 failures (warnings only; see cohesion note).
- Suites, run from `services/api` cwd (per CODING_RULES): `tests/spatial` **76 passed**, `tests/connectors/test_dcm_street_centerline_arcgis.py`+`test_wide_street_buffer_engine.py` **123 passed** (69+54), `tests/api/test_rule_evaluation_api.py` **38 passed**. Matches the harvest tally exactly.
- Consumer sweep: `build_segment_query_url` has no out-of-scope app consumers; `fetch_street_segments` has one out-of-scope consumer (`dcm_street_centerline_geometry.py:511`) whose suite is not in the documented commands. I ran the **entire** `tests/connectors` dir → **826 passed**, so the connector's metadata-ordering change leaks no regression into the geometry sibling.

### Judgment against the six required criteria

1. **Orchestration-only (PASS).** `wide_street_live_provider.py` fetches lot geometry, derives the envelope, gathers segments, wraps accepted typed objects, and calls the accepted `classify_street_width_policy` and `determine_wide_street_far`. No width classification, no policy value decision, and no buffer geometry is reimplemented. `_policy_decisions` (`:236-274`) only builds `AttestedPreconditions` and delegates. The hardcoded `_UNATTESTED_EC5` / `_NAMED_OVERRIDE_STATUS` (`:130-165`) are honest fail-safe attestations (all-False), not policy decisions.

2. **Every early-exit returns None + typed log, no partial state (PASS).** Each branch in `build_live_wide_street_determination` (`:335-…`) returns `None` via `_fail_safe(event, correlation_id, exc)` which logs event + `type(exc).__name__` + correlation id and **never** `str(exc)` (`:222-232`). The `caplog` tests (`test_typed_connector_error_logs_payload_only_never_str_exc`, `test_zero_segment_fail_safe_logs_payload_only_with_correlation_id`) prove the canary message string never reaches the log line, and that the honest-absence branch logs `error_type=none`.

3. **No consumer/contract leak; response byte-stable (PASS).** The route change (`rule_evaluation.py:150-171`) only makes the default seam delegate to the settings-gated provider; `serialize_rule_evaluation`/`integration.py` are untouched. `test_m5t035_within_wide_determination_fires_wide_row_via_endpoint` pins the structured block **absent** (`wide_street_far_row`/`wide_street_governing_far`/`wide_street_determination` not in doc) and asserts the DSL trace output stays the conservative `max_residential_far == 2.2`; `test_m5t035_flag_off_default_wide_provider_zero_calls_byte_identical` proves flag-off is `json.dumps(...sort_keys=True)`-identical to the no-determination path with zero connector calls. This is exactly the AS-3/DB-014 orchestrator ruling (decision effect via `coverage_status`/`professional_review_required`/`reasons[]`, structured block deferred to DB-014) — compliant.

4. **Exception discipline (PASS).** The two `except[TRANSPORT TRUNCATION POINT]

---

## Part 2 (resent remainder, verbatim; delivered 06:32:10Z, clipped again inside O1 — Part 3 below)

Resending from criterion 4 onward.

### Judgment against the six required criteria (continued)

**4. Exception discipline (PASS).** The two `except Exception` catch-alls in `wide_street_live_provider.py` (segment fetch and geometry fetch boundaries) carry `# noqa: BLE001 - fail-safe boundary, typed log only` and return `None` — the conservative direction — never a fabricated determination, so no re-classification can flip the fail-safe toward a granted FAR bonus. Typed connector errors (`MapPlutoGeometryConnectorError`, `DCMConnectorError`) are caught separately before the catch-all, each with its own `_fail_safe` event label. The connector's `OverflowError → DisallowedRequestError` mapping in `_validate_envelope` (`dcm_street_centerline_arcgis.py`) is a correct fail-closed refusal, and `_safe_repr` (`dcm_…arcgis.py:~316`) prevents an oversized-int `repr()` from turning a clean typed refusal into an uncaught `ValueError` — directly proven by the four `_HUGE_INT` tests (`test_envelope_wrong_length_with_huge_int_is_disallowed_not_valueerror` and siblings). No bare `except:` anywhere in the diff.

**5. Modularity (PASS, cohesion justification recorded).** `python tools/modularity_check.py --check` = **0 failures**; no engine split; no wiring imported into `wide_street_buffer_engine.py` (the diff adds only the two ceiling helpers `_check_lot_vertex_count`/`_check_segment_path_count` + the `MAX_LOT_VERTICES`/`MAX_PATHS_PER_SEGMENT` constants + two call sites in `compute_wide_street_buffer_intersection`) — matches the binding M5-T034 G3 ruling and the packet's DB-013 "no split, no wiring" instruction. The checker flags `dcm_street_centerline_arcgis.py` and `wide_street_buffer_engine.py` at `review_signal: above the justification threshold`. **Cohesion judgment (the record the policy asks for):** both files remain single-responsibility. The connector is one ArcGIS source's URL-build / validate / page / parse / freshness-and-schema-drift-guard surface, and the new envelope-intersects predicate is simply another predicate style for the *same* layer (it reuses the same paging, CRS, freshness, and byte-ceiling machinery). The engine is one geometric computation (100-ft buffer ∩ lot), and the DB-013 bounds are input guards for the *same* computation, run before the same shapely operations. Growth is within-responsibility and contract-directed; splitting either now would fragment a single source/computation across modules and add a facade with no cohesion gain. Justified to stay as-is.

**6. Test quality (PASS).** Tests build real typed objects through the accepted parse surfaces (`parse_segment_page`, `parse_segment_geometry_page`, `analyze_lot_geometry`) rather than mocking them, so they exercise the genuine typed contract. Reason strings are asserted as exact reconstructed strings (`expected_reason in doc["reasons"]`), not broad substrings. The ceiling tests probe at *and* beyond each bound by monkeypatching the bound to the fixture's actual count (avoiding hundred-thousand-vertex fixtures) plus a `test_db013_ceilings_are_above_real_world_sizes` guard on the shipped constants. Zero-connector-call proofs are asserted *after* the request returns, with a comment noting the fail-safe `except` would swallow an in-call `AssertionError` but cannot hide a recorded call — a correct, deliberate design. Not over-mocked.

### Non-blocking observations (for the orchestrator; not defects)

- **O1 — Fail-safe boundary scope.** The provider wraps the three network fetches (and `_attested_wide_segments`) in try/except→None, but the pure-assembly steps `_attested_lot` / `_lot_envelope` / `_policy_decisions` and the final `determine_wide_street_far` call are **not** wrapped, and the route (`rule_evaluation.py:335`) does not wrap the provider call either (consistent with the accepted `live_provider` / substrate precedent). These steps operate on already-validated typed data, and the accepted policy/wiring are contractually non-raising, so in practice they [SECOND TRUNCATION POINT]

---

## Part 3 (final tail, verbatim; delivered 06:32:33Z — report COMPLETE)

Rest of O1 onward:

**O1 (continued)** — …so in practice they can't fail; and if one ever did, it fails toward an error surface (a 500), **never** toward a fabricated permissive determination — the load-bearing safety property (never grant an unearned wide-street FAR bonus) stays intact. Optional future hardening only, not required for this gate.

**O2 — AS-3 end-to-end seam coverage.** The WITHIN / professional-review endpoint tests supply the determination through a `dependency_overrides` override (the standard substrate pattern) rather than driving the live default seam's fixture-based composition all the way through the endpoint in one test. The composition is proven in `test_wide_street_live_provider.py`, and the default seam's flag-off→None + zero-connector-calls path is proven at the endpoint, so every link is covered; only the flag-*on* default seam composing from fixtures *through* the endpoint is not exercised in a single test. This matches the binding AS-3/DB-014 ruling and is acceptable.

**Closing.** G3 PASS — no blocking corrections; O1/O2 are optional (discovery-backlog candidates if you want them tracked). Identity note: reviewed at material `97fa2eea`; live HEAD `3ce7b87d` differs only in four project-control files, so reviewed source/doc content is byte-stable and the gate may be recorded against HEAD.
