{
  "requirements": {
    "D-038-R003": [
      "apps/web/src/app/property/compare/page.tsx + apps/web/src/components/compare/**: the Compare (Step 3) USER-FACING SCREEN named explicitly in the D-038 pivot plan as product engineering (Compare UI -> Evidence view -> rule families). It consumes the accepted M5-T003 endpoint GET /api/v1/properties/{bbl}/scenario and renders the draft residential zoning-floor-area cap, the ranked scenario, the coverage matrix, the constraint breakdown, the integrity check, the assumptions, and the full provenance trail. Before this task the route did not exist and the Confirm screen dead-ended.",
      "apps/web/src/lib/scenario-api.ts + scenario-contract.ts + scenario-contract-checks.ts + scenario-bounds.ts + scenario-display.ts: the client transport, runtime contract validation against scenario@1.0.0 BEFORE any render (AS-4), the response bounding (arrays reject at 64, free text truncates visibly at 600, identifiers reject rather than being silently sanitized), and the display selection layer. No legal or numeric value is computed, derived, rounded or defaulted anywhere in the client.",
      "PRODUCT, NOT SELF-INFRASTRUCTURE (D-038-R002 prohibition satisfied): nothing in this task touches tools/, .claude/, the supervisor, or any control-plane machinery. The diff is apps/web plus one producer report. This is the second user-facing screen of the pivot after M5-T003's endpoint.",
      "EVIDENCE OF INDEPENDENT VERIFICATION: project-control/reports/M5-T004-rereview.md records a five-gate re-review (G1, G3, G4, G5, DCV) at reviewed SHA 9b417875, manifest 9f57ea9e886ef1909fd6f6260877c5e560e3b8af4740d7a9a0ef16cf97692c67, verdict PASS 5-0. The first-round wave at 84815a76 returned FAIL 4-1 on a silent projection; project-control/reports/M5-T004-G1.md, -G3.md, -G4.md, -G5.md and -DCV.md record that arc. Measured outcome: leaf transport 191/515 -> 656/665 with 0 authored legal/numeric values; constraint leaves 0/96 -> 96/96 on the conflict fixture; 45/45 schema leaves rendering.",
      "OFFLINE / NO CREDENTIALS (D-038-R004 constraint satisfied): AS-7 requires AS-1..AS-6 to run entirely under vitest with committed fixtures and/or Playwright against apps/web/e2e/harness/fixture_api.py, with NO network, NO Supabase and NO Geoclient. G5 audited every render and fetchScenario site and found zero reaching global fetch; G4 independently audited every test for a real network call and found none. No credential, token or real endpoint appears in any fixture. The only network-shaped constant is the build-time apiBaseUrl() default."
    ],
    "D-038-R004": [
      "The entire task was built and verified with no cloud credentials. The endpoint it consumes is flag-gated internal-only (INTERNAL_SCENARIO_ENABLED) and the e2e harness supplies it via fixture_api.py rather than any live service.",
      "apps/web/e2e/harness/fixture_api.py:265 enables INTERNAL_SCENARIO_ENABLED so a browser can reach /property/compare at all; apps/web/e2e/compare-journey.spec.ts adds the first Playwright journeys anywhere in the repository that navigate to that route. Both run against the fixture harness, never a live backend.",
      "No node_modules is installed anywhere under the thin-client policy (docs/LOW_STORAGE_CLOUD_DEVELOPMENT_POLICY.md), so neither the producer nor any of the five reviewers executed the documented test commands. NO GREEN IS CLAIMED at acceptance; CI is the authority. G4's CI prediction is static, derived from reading the configs, hand-checking every cross-module signature, and transcribing validateScenarioDocument into Python to run all four committed fixtures through it (4/4 PASS)."
    ]
  }
}
