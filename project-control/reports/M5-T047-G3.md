<!-- Orchestrator capture: code-reviewer G3 return for M5-T047 saved VERBATIM
     (transport entity-decoding only). Pinned at frozen head ca94b2c3; reviewer
     reproduced the three-way surface identity and four digest rechecks. -->

I have completed independent verification of the full G3 surface. All identity, digest, backend-contract, client, display, rider, correction, and scope checks reproduce. Below is my gate report.

---

# G3 (code) gate report — M5-T047 (record-address display channel, DB-032)

Reviewer: code-reviewer (independent, read-only). Frozen reviewed head: `ca94b2c3`. Live HEAD at review: `21ee7379` (one control-plane commit later). CI-proven identity: `d27a6a83`.

## Verdict: PASS

No blocking findings. Three advisory findings (A1–A3) recorded for the orchestrator/backlog; none block acceptance.

## Identity and evidence verification (reproduced)

- `git rev-parse HEAD` = `21ee7379`; `git log` shows `21ee7379` = "M5-T047 SUBMITTED awaiting_gate" over `ca94b2c3` — the single control-plane commit noted in the task prompt. Confirmed.
- Surface byte-identity across the three heads (`git diff --name-only`):
  - `d27a6a83..ca94b2c3` touches only M5-T045 material (architect/, condo connectors, spatial, profile) + control-plane reports — zero T047 allowed-path files, zero `address-resolution.test.tsx`.
  - `ca94b2c3..21ee7379` touches only `project-control/{reports/M5-T047.json,state.json,tasks/M5-T047.json}`.
  - `d27a6a83..21ee7379` restricted to all 9 allowed paths + `address-resolution.test.tsx` = **empty**. The entire T047 surface is byte-identical from the CI-green head `d27a6a83` through the frozen head and live HEAD. The `M5-T047-ci-evidence.md` byte-identity claim is independently confirmed.
- Digest recheck (LF-normalized sha256 via `git show ca94b2c3:<path> | tr -d '\r' | sha256sum`) for `lot_geometry.py`, `record-address.ts`, `address-search.ts`, `AddressConfirmCard.tsx` — all four match `M5-T047-source-sections.md` exactly.
- Web behavior: thin-client posture; I did not execute vitest/Playwright/gh. Per the evidence-capture division I verified the stored CI evidence tied to a surface I proved byte-identical to `d27a6a83` (full CI incl. web-e2e run 35431045720 = success). This is sufficient; not a BLOCKED condition.

## Surface 1 — backend route `services/api/app/api/v1/lot_geometry.py`

Route correctness fully verified against the read-only PLUTO connector contract (`app/connectors/pluto_soda.py`):
- normalize_bbl BEFORE fetch: `:380` validates; fetch at `:401`. 422 emitted with zero network I/O; RA-5 landmine test proves the seam is never invoked on the 422 path. Correct.
- Flag-off 404 BEFORE correlation id: `:373-374` returns generic `{"detail":"Not Found"}` with no `X-Correlation-ID`, checked before the id is minted at `:376`. Byte-indistinguishable from an unmounted path (RA-5 asserts no leak). Correct.
- Injected read-only PLUTO seam: `get_pluto_record_fetch` (`:331`) / `Depends` (`:366`); `pluto_soda.py` consumed read-only, untouched. Correct.
- Typed error taxonomy is complete and matrix-consistent: the connector's only `error_type` values are `source_unavailable / rate_limited / schema_drift / timeout` (`pluto_soda.py:286,308,315,321,328`), and the base class defaults to `source_unavailable`. `_PLUTO_ERROR_STATUS` (`:309`) maps all four; every resulting (status,state) pair — (429,rate_limited),(504,timeout),(502,source_unavailable),(502,schema_drift) — is in `RECORD_ADDRESS_STATUS_STATE_MATRIX` (`:294`). No orphan error_type can escape the matrix. RA-6 confirms empirically (subset assertion).
- Three honest 200 outcomes: `no_record` (`:432`), `no_address_of_record` for SODA key-absence (`:444`), `address_of_record` (`:456`), all `address=null` except the last. Absence is a typed outcome, never a fabricated/empty line — `_record_address_from_result` (`:338`) returns None on missing column AND on blank/whitespace value (`value.strip() != ""`), so drift never becomes an empty line (RA-2/RA-2b). Correct.
- Provenance quintuple travels on every 200 (`_record_source` `:351`: source_id, dataset_id, dataset_version, retrieved_at, request_url), including the two absence outcomes (the retrieval that proved absence is evidence). Satisfies permanent principle 2 and D-073-R006 (record, not computed value).
- JSON-safety guard `_assert_json_safe` (`:470`) before send, failing closed to a typed 500 — same discipline as the lot-geometry endpoint. Correct.

Module cohesion: the file is a single responsibility — per-BBL, flag-gated, display-only, read-only transport endpoints for the confirm card. The record-address sibling shares `_json`, `_not_found`, `_internal_error_500`, `_assert_json_safe`, and the normalize-first/inject-seam/typed-error posture. ~363 non-blank/non-comment SLOC, well under the 600 warn threshold; `modularity_check.py --check` reported exit 0 (orchestrator-reproduced, CI-green). A sibling endpoint in this module was explicitly packet-authorized. Boundary judged acceptable.

## Surface 2 — `apps/web/src/lib/record-address.ts`

- Typed outcome union (`:129-136`) is comprehensive: document / route_absent / error / network_error / client_timeout / aborted / unexpected_response.
- `DOCUMENTED_PAIRS` (`:50-59`) mirrors the server matrix pair-for-pair; routing is by (status,state), never `state` alone; a 200 must carry `document_kind==="record_address"` and a known `outcome` or it becomes `unexpected_response` (`:256-272`). Generic 404 is a first-class `route_absent` (`:276`).
- Bounded display view: `boundedText` on the reflected address (blank → null, never an empty line), `boundedToken` on provenance and correlation id (`:157-173`).
- Abort/timeout discipline: pre-aborted external signal short-circuits (`:192`); a timer-driven abort yields `client_timeout`, an external abort yields `aborted`, transport failure yields `network_error` (`:211-222`); timer cleared and listener removed in `finally`. Correct.
- Hook `useRecordAddress` (`:336`): one fetch per BBL, supersession aborts the prior request, returns null while loading and when bbl is null (no fetch). Effect deps `[bbl, fetchImpl]` are stable in production (fetchImpl undefined) and in tests (global stub). Correct React hooks usage (called unconditionally at card top level).

## Surface 3 — `AddressConfirmCard.tsx`

- Record-line render conditions correct: `showRecordAddress` (`:137-139`) is true only when a resolved `address_of_record` differs from `matchedForCompare`. Equal → false → no line (AS-2); absent/error/loading → `recordAddress` null → no line (AS-3). `matchedForCompare` (`:131-136`) is built from the SAME components as the matched display line (`:76`, house# + normalized street), so an equal record produces no line. Correct.
- HJ A2: layout-neutral copy at `:191-194` ("alongside the city-matched address … the identity we carry forward is the tax lot (BBL)") — no above/below. Correct.
- HJ A3a: `<strong title={enteredInput} aria-label={enteredInput}>{enteredInput.trim()}</strong>` (`:188-190`) — display-trim with the true raw value in title AND aria-label. Correct.
- `data-record-address-status` (`:147-160`) is an observability-only signal (no visual effect) enabling terminal-state test waits. Sound.
- AddressResolutionScreen unchanged: confirmed — it is absent from the material commit's changed-file list; `git diff 7ecdcbf4~1 d27a6a83 -- AddressResolutionScreen.tsx` is empty.

## Surface 4 — riders in `address-search.ts` (+ tests)

- G5-A1 length bound: `GEOSEARCH_RESOLVE_INPUT_MAX_LEN = 512` (`:220`), enforced at gate entry of `resolveLotFromGeoSearch` (`:313-318`) AFTER the FeatureCollection/parsedInput-null guard but BEFORE any normalization/compare, returning the existing typed `no_match`. Diff is +16/-0 in the library; match semantics unchanged. Correct.
- G4-A1 abbreviation fixture: a `/search`-shaped body with parsed street `"37 st"` against a `"37 STREET"` feature asserts `no_match` (`address-search.test.ts` G4-A1) — a strong, discriminating fail-closed test.

## Surface 5 — tagged `[ORCH-CORRECTED]` change in `address-resolution.test.tsx`

Correct and M5-T023-precedent-consistent. The file's `installFetch` side stub (`:80-91`) intercepts `/lot-geometry` (and now `/record-address`) URLs, returning a benign 404 so they never reach — or count against — the resolution spy's queued `mockResolvedValueOnce` responses. The card's new `useRecordAddress` fetch would otherwise consume a queued resolution response and skew call counts (the exact 3/1064 `address-resolution.test.tsx` failure at prior head `7ecdcbf4`). The one-line predicate extension (`|| url.includes("/record-address")`) mirrors the existing lot-geometry handling exactly. It hides no real defect: record-address is served the same benign 404 that the client legitimately treats as `route_absent` (→ no line), and record-address behavior is covered directly in `record-address.test.ts` and `address-confirm.test.tsx` S10 with proper routed stubs. Authored by the orchestrator, tagged, and routed out-of-scope per CODING_RULES. The `/record-address` substring is specific enough to not collide with resolution URLs.

## Surface 6 — scope

Exactly the material change-set required. `7ecdcbf4` (material build) = the 8 allowed source/test files + the producer report (9 files, all in `allowed_paths`). `d27a6a83` = only `address-resolution.test.tsx` (the tagged consumer fix). Forbidden-path probe over `LotOutlineMap.tsx`, `architect/`, `connectors/`, `profile/`, `spatial/`, `packages/contracts/`, `_contract_schemas/` = empty. (The interleaved `proposal.py` / D-076 / M5-T048 files in the branch range belong to the disjoint M5-T048 lane, not T047, and touch no T047 forbidden path.) Consumer sweep complete: only `address-confirm.test.tsx` (in-scope) and `address-resolution.test.tsx` (corrected) render these components and stub fetch; no other consumer test is left red, consistent with CI green at `d27a6a83`.

## Directive posture (advisory to directive-compliance-verifier)

- D-073-R006 (records-display class): the record-address line and the entered-input line both read as records implying no computed value (card copy `:200-202`, `:191-194`). Honored in code.
- D-066-R001 (code-graph navigation discipline): the substantive outcome — a consumer sweep beyond the file list — was performed and the one affected consumer (`address-resolution.test.tsx`) was caught and corrected. Honored in code.
Formal per-requirement verification remains the directive-compliance-verifier's gate.

## Advisory findings (non-blocking)

- **A1 — G5-A1 length-bound tests are mutation-weak** (`apps/web/src/lib/__tests__/address-search.test.ts`, the two over-length cases). Both use `features: []`, so `resolveLotFromGeoSearch` returns `no_match` via the empty-features path whether or not the length bound exists — deleting `:313-318` would not fail them. The fourth test ("a normal-length input AT the bound is NOT refused") uses `"37 STREET"` (9 chars), not a 512-char string, despite its "boundary-length" comment, so the actual off-by-one boundary (512 allowed vs 513 refused) is untested. The bound code itself is correct and the G4-A1 abbreviation test is strong; this is a test-adequacy nit, not a defect. Suggest a follow-up: assert the guard fires with a non-empty matching feature present, and cover length exactly 512 vs 513.
- **A2 — stale module docstring** (`services/api/app/api/v1/lot_geometry.py:1-36`). The top-of-file docstring still describes only the lot-geometry endpoint and `STATUS_STATE_MATRIX`; it was not updated to mention the additive record-address sibling now co-hosted in the module. The record-address section has its own thorough header (`:265-284`), so this is a documentation-cohesion nit.
- **A3 — RA-3 reason assertion weaker than it appears** (`services/api/tests/api/test_lot_geometry_api.py`, `test_ra3_no_record_is_honest_absence`): `assert "no PLUTO record" in body["reason"].lower() or body["reason"]` — the trailing `or body["reason"]` makes the substring check non-binding (passes on any truthy reason). Immaterial (outcome `"no_record"` is strongly asserted and the fixture explanation does contain the substring), but the assertion should drop the `or` clause to mean what it reads.

**G3 verdict: PASS** (advisories A1–A3 for follow-up; not acceptance-blocking).
