# M5-T072 — G2 producer self-check record (recorded by the orchestrator, role self_check)

Producer: qa-engineer (loop-1 worker, run persistent-local-69-m5t072). The unit closed
unsafe_condition with the work DELIVERED uncommitted plus an exact harvest recipe — the
recorded [BROKER-LIMIT→harvest] class (T067/T069 precedent).

- **Scope self-check (report, [OBSERVED]):** test-only diff +2/-0 inside the one revoke test
  (the :841 comment + the :842 pin); every existing assertion byte-unchanged; working tree
  confined to the two allowed files; store.py and every route untouched.
- **Design note (report):** the pin uses a test-local byte-copy of the store constant
  (never imported from the store) and the pre-existing :836 intra-revoke equality makes the
  binding transitive to BOTH branches — the identical structure the T069 G3 review accepted
  for supersede's pin at :886.
- **Harvest execution of the recipe (orchestrator, cwd services/api, wt-m5t072):** ruff
  clean; scoped suite 77 passed (baseline +0 as predicted); the AS-2 both-branch revoke-only
  mutant GREEN without the pin (43 passed — the A2 gap made visible) and FAILED exactly at
  the :842 pin with it (the predicted AssertionError observed verbatim); store.py restored
  via plain checkout; restored candidate 77 passed. Every routed row elevated to OBSERVED
  ([ORCH-HARVEST] section of the report).
- **Modularity:** exit 0 (producer-observed, 475 files / 0 failures; no site_definition file
  among the 22 pre-existing warnings).
- **Identity:** in-wt commit cherry-picked to ecb6a13f, MATCH x2 (LF-normalized); the
  harvest transcript at abc574a8; CI on the pushed head is the backstop.

G2 verdict: PASS (self_check; never independent review — G3/G4 follow).
