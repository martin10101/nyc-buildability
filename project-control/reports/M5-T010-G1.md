# M5-T010 — G1 Independent Code Review (verbatim reviewer return)

Reviewer: **code-reviewer** (independent ≠ producer scenario-optimization-engineer). Reviewed SHA `ea08f504ea4b52cc93062a74a9c125e5133c4b7b`. Transport entity-decoding applied (`<` `>` `&`); content otherwise verbatim.

---

# Gate Report

- **Gate ID:** G1 (independent code review)
- **Task ID:** M5-T010 — Deterministic scenario comparison/delta engine (contract-free, offline)
- **Reviewer:** code-reviewer (independent; not the producer)
- **Producer:** scenario-optimization-engineer (build loop persistent-local-26)
- **Result: PASS** (no BLOCKING findings; 4 LOW / informational items below are non-blocking)
- **Clean environment / frozen SHA used:** yes — `git rev-parse HEAD` = `ea08f504ea4b52cc93062a74a9c125e5133c4b7b` (matches the reviewed SHA). All source read from the committed tree at that SHA.

## Acceptance criteria reviewed

Reproduced AS-1..AS-7 by reading the committed source and running the documented command. All pass.

- **AS-1 comparison document / verbatim cap** — PASS. `compare_scenario_assumption_sets` returns base lineage (`base_lineage_identity`: bbl/scenario_kind/contract_version/coverage_status/data_completeness), a per-set `derived` echo obtained by calling `derive_practical_usable_range` READ-ONLY, and per-metric absolute+percent deltas with a breakdown. The canonical cap is transported verbatim: `_extract_metrics` reads `derived["canonical_cap_sq_ft"]` directly (comparison.py:352); no independent legal recompute — the only arithmetic is delta of already-surfaced numbers (`_metric_delta`). Test `test_as1_*` asserts `row["derived"] == derive_practical_usable_range(...)` and `canonical_cap == expected cap == 15000.0`.
- **AS-2 deterministic total stable order** — PASS. Sets ordered by `_content_key` = insertion-order-preserving `json.dumps({"name","assumption_set"})` (comparison.py:423-438); baseline = smallest key; ties are byte-identical rows (same name + same sanitized `assumption_set` ⇒ same `candidate_document` ⇒ same deterministic `derive` output), so `sorted` stability makes the order total with no reliance on dict-insertion or float equality. `test_as2_byte_identical_across_input_reorderings` passes (forward == reversed == shuffled).
- **AS-3 never invents / never Verified / typed not-comparable** — PASS. One row per supplied set; a not-derivable non-baseline set is kept in order as a typed not-comparable row (never dropped/raised). `NOT_VERIFIED_DISCLAIMER` is stamped unconditionally on every outcome via `_base_lineage` (comparison.py:204-208, applied by both `_degenerate_result` and the COMPARED assembler). Incoming `coverage_status="verified"` capped to `conditional`; `test_as3` asserts `"verified"` appears nowhere in the whole output tree.
- **AS-4 typed-error + degenerate handling** — PASS. `<2` sets / non-list container / not-derivable baseline → typed INVALID with reason; no positive cap → typed EMPTY; zero/None/absent baseline metric → typed not-computable (no ZeroDivisionError/NaN/Inf). Independently probed adversarially (see Steps).
- **AS-5 strict-JSON-safe via SHARED sanitizer** — PASS. `from ._json_safety import _json_safe` (comparison.py:65); Grep confirms comparison.py defines none of `_json_safe/_safe_scalar/_unsafe_marker/_safe_key/_bounded_repr/_json_safe_mapping`. `test_as5_uses_shared_sanitizer_and_does_not_reimplement_it` asserts `comparison_module._json_safe is _json_safety._json_safe`. Delta fields are the only place a negative appears and are finite-by-construction (finiteness re-checked before emission).
- **AS-6 full regression + modularity** — PASS. `python -m pytest services/api/tests/scenario` → **323 passed, 0 failed** (252 pre-existing + 71 new comparison items). `python tools/modularity_check.py --check` → exit 0, failures 0; comparison.py is NOT among the 14 pre-existing unrelated warnings. Single cohesive responsibility (parse → derive echo → extract → delta → order → assemble).
- **AS-7 contract-free + offline** — PASS. Imports only stdlib (`json`, `math`, `typing`) + `.derive` + `.constants` + `._json_safety` (comparison.py:59-67). Forbidden-path diff empty (see Steps). `test_as7_runs_fully_offline_with_sockets_blocked` passes with `socket.socket` hard-blocked.

## Directive/requirement verification

In-regime task, `directive_refs = [{D-038: ALL}]`. Full atomic independent verification (producer ≠ verifier) is the **directive-compliance-verifier**'s pass recorded in `project-control/directives/D-038-build-product-not-self/verification.json`; the verdicts below are my code-level (G1) assessment at the frozen SHA and do not substitute for that pass.

| Requirement ID | Reviewed SHA / content identity | Verdict | Reproduced evidence |
|---|---|---|---|
| D-038-R001 (loop liveness) | ea08f504 | UNVERIFIABLE (out of code scope) | Orchestration/loop-operations requirement; not a property of this file diff. |
| D-038-R002 (no self-infrastructure build target) | ea08f504 | PASS | Deliverable is `services/api/app/scenario/comparison.py` product code, not supervisor/control-plane self-infra. |
| D-038-R003 (positive product deliverable — Compare) | ea08f504 | PASS | Deterministic comparison/delta engine backing the Compare (Step 3) UI; user-facing product engineering. |
| D-038-R004 (no cloud creds — offline) | ea08f504 | PASS | No Supabase/Geoclient/network/persistence imports; `test_as7` offline-with-sockets-blocked passes; import allow-list test passes. |
| D-038-R005 (Supabase/Geoclient carry-forward) | ea08f504 | UNVERIFIABLE (out of code scope) | Owner-decision carry-forward; no code obligation in this diff. |
| D-038-R006 (authority/no-bypass; gates & holds unchanged) | ea08f504 | PASS | Producer did not self-accept or run control CLI; reviewer read-only; gate recorded by orchestrator. |
| D-038-R007 (return report) | ea08f504 | UNVERIFIABLE (out of code scope) | Orchestrator return-report obligation, not this file diff. |

## Steps independently executed

1. `git rev-parse HEAD` → `ea08f504…c4b7b` (matches reviewed SHA).
2. `git show --stat ea08f504` → exactly 4 files: producer report, `__init__.py (+10)`, `comparison.py (+601)`, `test_scenario_comparison.py (+841)`.
3. `git diff ea08f504^ ea08f504 -- derive.py builder.py models.py constants.py contract.py ranking.py sensitivity.py _json_safety.py packages/contracts` → **empty** (no forbidden path touched).
4. `git diff ea08f504^ ea08f504 -- __init__.py` → additive only: one isort-ordered `from .comparison import (...)` block placed between `.builder` and `.constants`, and four names appended to `__all__`. No existing export or its position changed.
5. `python -m pytest services/api/tests/scenario -q` → **323 passed in 1.20s**.
6. `python -m pytest tests/scenario/test_scenario_comparison.py -q` → **71 passed** (252 pre-existing + 71 = 323).
7. Grep comparison.py for sanitizer defs → none (shared import only).
8. `python tools/modularity_check.py --check` → 364 files, **failures 0**, 14 warnings all pre-existing/unrelated; comparison.py not flagged.
9. Adversarial `_metric_delta` / `compare_scenario_assumption_sets` probes (direct Python):
   - baseline `-1e308`, set `1e308` → absolute overflows to inf → typed `absolute_delta=None`, not-computable reason. No crash.
   - baseline `5e-324`, set `1e308` → percent overflows → `percent_delta=None` with reason; absolute still reported.
   - baseline `True` (bool) → rejected as non-numeric → not-computable (no silent `True==1`).
   - NaN `utilization_factor` → derive fails closed → not-comparable row; whole result COMPARED and `json.dumps(result, allow_nan=False)` succeeds.

## Expected versus actual

| Check | Expected | Actual |
|---|---|---|
| HEAD | ea08f504…c4b7b | ea08f504…c4b7b ✓ |
| Files changed | 4 (report, `__init__`, comparison, test) | 4 ✓ |
| Forbidden paths | untouched | untouched ✓ |
| Full suite | 323 passed, 0 fail | 323 passed, 0 fail ✓ |
| Comparison items | new file green | 71 passed ✓ |
| Modularity | passes | exit 0, failures 0 ✓ |
| Shared sanitizer | imported, not re-defined | confirmed ✓ |
| Derive schema | read verbatim, not guessed | `derived_kind` / `practical_usable_range.{min,point,max}` / `canonical_cap_sq_ft` / `not_derivable_reason` all match derive.py's emitted keys ✓ |

## Regression / security / provenance findings

- No regressions: 252 pre-existing scenario tests all pass; the four forbidden test files (`test_scenario_ranking/sensitivity/derive/json_safety.py`) are unmodified.
- Security/offline: no network/persistence/subprocess/file I/O; socket-blocked test green; no address leaks (`_json_safety` markers carry type-name only).
- Provenance/honesty: canonical cap transported verbatim; no independent legal recompute; every outcome carries `NOT_VERIFIED_DISCLAIMER` + `needs_review=True` + coverage capped to `conditional`; `"verified"` never appears anywhere in the output tree.

## Defects

None BLOCKING.

**LOW / non-blocking:**

1. **`__init__.py` `__all__` ordering (cosmetic).** The four new names (`COMPARISON_LABEL`, `COMPARISON_METRIC_KEYS`, `ComparisonKind`, `compare_scenario_assumption_sets`) are appended at the end of `__all__` (`__init__.py:89-92`), breaking the list's otherwise alphabetical grouping. The *import block* is correctly isort-ordered. Cosmetic only; existing exports and their order are unchanged. Non-blocking.

2. **Content-key-smallest baseline can poison an otherwise-valid comparison (design observation).** If the set with the smallest content key is not-derivable, the whole comparison returns typed INVALID even when other supplied sets are derivable (`compare_scenario_assumption_sets`:534-544; `test_rework_missing_assumptions_baseline_is_typed_invalid`). This is exactly what AS-4 specifies ("not-derivable BASELINE → invalid") and fails closed with a machine-readable reason, so it is compliant — flagged only so product/UX is aware of the sharp edge. Non-blocking.

3. **Nested `row["derived"]` echoes derive's disclaimer field verbatim (informational).** The top-level rework-2 fix stamps the canonical disclaimer unconditionally, but `derive.py:_base_document` (a forbidden, out-of-scope file) still forwards an input document's empty/arbitrary `not_verified_disclaimer`, so a tampered document's `""` can appear inside `row["derived"]["not_verified_disclaimer"]`. This is not a comparison defect: the comparison document as a whole is unambiguously never-Verified (top-level canonical disclaimer, `needs_review=True`, coverage capped, no `"verified"` leak per `test_as3`), and the nested echo is derive's accepted contract behavior outside M5-T010's scope. Informational only.

4. **Producer-report digest-binding instruction is technically incoherent (documentation nit).** Report A.0 asks the orchestrator to confirm `git hash-object <file> == <sha256>`; `git hash-object` yields a SHA-1 object id over a `blob <len>\0`-prefixed payload, which cannot equal a raw-bytes SHA-256. The authoritative reviewed content is the committed tree at ea08f504, which I read directly — so this does not affect the verdict; the orchestrator should bind via a raw-bytes SHA-256 tool (e.g. `Get-FileHash`/`certutil`) if it wants to match the reported digests, not `git hash-object`. Non-blocking.

## Required rework

None required for a PASS. The four LOW items above are optional polish (item 1 is the only code touch, and it is cosmetic).

## Reviewer conclusion

**PASS.** At frozen SHA `ea08f504`, M5-T010 delivers exactly the four in-scope files with no forbidden-path changes. `compare_scenario_assumption_sets` correctly: consumes `derive_practical_usable_range` READ-ONLY, transports the canonical cap verbatim (no independent legal recompute), computes finite-by-construction baseline-relative absolute+percent deltas with typed not-computable markers for zero/None/absent/overflowing values (adversarially reproduced), orders sets by a documented total stable content key with the baseline first, stamps `NOT_VERIFIED_DISCLAIMER` on every outcome and caps coverage to `conditional`, uses the shared `_json_safety._json_safe` with zero re-duplication, and stays contract-free and fully offline. Reproduced: `python -m pytest services/api/tests/scenario` = **323 passed, 0 failed** (252 pre-existing + 71 new); `python tools/modularity_check.py --check` exit 0. No BLOCKING defects; four LOW/informational notes recorded.
