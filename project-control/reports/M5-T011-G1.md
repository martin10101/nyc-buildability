# M5-T011 — G1 Independent Code Review (verbatim reviewer return)

Reviewer: **code-reviewer** (independent ≠ producer scenario-optimization-engineer). Reviewed SHA `bbf3267f8be2444c5460742e22792698bdc1307f`. Transport entity-decoding applied (`<` `>`); content otherwise verbatim.

---

# Gate Report

- **Gate ID:** G1 (independent code review)
- **Task ID:** M5-T011 (deterministic scenario break-even / threshold finder)
- **Reviewer:** code-reviewer (independent; not the producer)
- **Producer:** scenario-optimization-engineer (build loop persistent-local-27)
- **Result: PASS**
- **Clean environment/worktree used:** Yes. Reviewed at frozen SHA `bbf3267f8be2444c5460742e22792698bdc1307f` (verified `git rev-parse HEAD` in the `ctl24` checkout == reviewed SHA; the dedicated `wt-m5t011` worktree is at the same SHA). Working tree carries only two untracked `.claude/agent-memory/qa-engineer/` files, outside deliverable scope.

## Acceptance criteria reviewed

AS-1..AS-7 all reviewed against the actual diff and reproduced. `git show --numstat HEAD` confirms EXACTLY 4 files: `services/api/app/scenario/breakeven.py` (+776, new), `services/api/app/scenario/__init__.py` (+12, additive), `services/api/tests/scenario/test_scenario_breakeven.py` (+609, new), `project-control/reports/M5-T011-producer-report.md`. No forbidden path touched.

## Directive/requirement verification

In-regime task (`directive_refs: D-038:ALL`). Full requirement-by-requirement compliance is the independent `directive-compliance-verifier`'s pass; below is my code-level assessment at content identity `bbf3267f`.

| Requirement ID | Reviewed content identity | Verdict | Reproduced evidence |
|---|---|---|---|
| D-038-R001 (keep loop running nonstop) | bbf3267f | UNVERIFIABLE (orchestration-scope) | No code artifact; supervisor/loop liveness is orchestrator domain. |
| D-038-R002 (prohibition: no self-infra as build target) | bbf3267f | PASS | Deliverable is product optimization code, not supervisor/control-plane self-infra. |
| D-038-R003 (positive product deliverable) | bbf3267f | PASS | Break-even finder completes the scenario optimization toolkit (derive→sensitivity→ranking→comparison→threshold); not self-tooling. |
| D-038-R004 (no cloud credentials; offline) | bbf3267f | PASS | Imports only stdlib + `.derive`+`.constants`+`._json_safety`; offline socket-blocked test passes; no Supabase/Geoclient/network. |
| D-038-R005 (Supabase/Geoclient deferred) | bbf3267f | PASS | Fully offline, no credential-gated I/O introduced. |
| D-038-R006 (no-bypass: never Verified; contract-free; no independent legal calc) | bbf3267f | PASS | Cap verbatim from derive; coverage capped verified→conditional; NOT_VERIFIED_DISCLAIMER stamped unconditionally; no independent legal recomputation; contract-free. |
| D-038-R007 (return report) | bbf3267f | UNVERIFIABLE (orchestration-scope) | Orchestrator domain, not this code deliverable. |

## Steps independently executed

1. `git rev-parse HEAD` → `bbf3267f…`. `git show --numstat HEAD` → 4 files, additive only.
2. Read the full working-tree `breakeven.py`, `__init__.py`, and test file; confirmed byte-consistency with the diff.
3. Verified derive.py contract consumption: `DerivedRangeKind.DERIVED = "derived_practical_usable_range"`, `practical_usable_range` has `min/point/max`, `canonical_cap_sq_ft`/`factor_product`/`not_derivable_reason` keys exist, `RECOGNIZED_FACTOR_TYPES = {"utilization_factor","efficiency_ratio"}` matches the `ThresholdVariable` enum values exactly.
4. `python -m pytest tests/scenario -q` → **384 passed in 1.35s** (323 pre-existing + 61 new; 0 regression). `--collect-only` on the new file → **61 tests**.
5. `python tools/modularity_check.py --check` → **EXIT 0, failures 0**; `breakeven.py` receives a non-blocking `warn review_signal`.
6. No sanitizer re-duplication: grep for `def _json_safe|_unsafe_marker|_safe_scalar|_safe_key` in breakeven.py → NONE.
7. Adversarial probe on `[0.8, NaN, Inf, -0.5, 10**5000, <object>, 0.5, 0.9, 1.0]`: `json.dumps(result, allow_nan=False)` did not raise; no `verified:true`; no ` at 0x` leak; the only interpolated field is `illustrative_bracket_midpoint` (`is_estimate:True, verified:False, value 0.85` between candidates 0.8/0.9); all emitted numbers finite and ≥ 0.
8. Monotonicity probe with a controlled derive double forcing a 4-crossing zig-zag on shuffled input → `crossings_count=4`, `non_monotonic=True`, FIRST crossing reported (0.1→0.2 ascending), NON-MONOTONIC reason surfaced.

## Regression/security/provenance findings
- Regression: none — 384/384 pass; five forbidden sibling test files byte-unchanged; no forbidden path touched.
- Provenance/honesty: canonical cap transported verbatim from derive (no independent legal calc); NOT_VERIFIED_DISCLAIMER stamped from the module's own constant (an incoming tampered disclaimer cannot suppress it); incoming `coverage_status:"verified"` capped to `conditional`; a literal `"verified"` variable never echoed. No invented-precision crossing: the crossing is the explicit grid bracket; the only sub-grid value is an arithmetic midpoint of the CANDIDATE VALUES (not an interpolated metric crossing), explicitly `is_estimate:True, verified:False`.
- Security: offline (socket-blocked test passes); no network/persistence/subprocess/file I/O; strict-JSON-safe via the shared sanitizer; no object-address leak.

## Defects
None blocking.

Non-blocking observations (LOW, informational):
- **L1 — commit-message imprecision.** Commit says "modularity_check --check EXIT 0 (not flagged)", but breakeven.py is flagged with a non-blocking `warn review_signal`. The check still EXITs 0; the producer report §5 honestly defers the SLOC measurement to the orchestrator. Narrative imprecision only.
- **L2 — populated `crossing` on a non-monotonic ALREADY_MET result.** If a metric started at-or-above target then dipped, the `crossing` field carries the first `meets_target` change while `threshold_kind` is `ALREADY_MET`. Only reachable with a genuinely non-monotonic metric (never with the real monotonic derive); explicitly labelled by `direction` + outcome kind, so honest. Informational.
- **L3 — file size.** breakeven.py is 776 wc-l, driven by the honesty-label/docstring convention shared with sibling modules; logical SLOC below the justify threshold (checker flagged only the warning threshold). Consider the module boundary before further growth.

## Reviewer conclusion
**PASS.** Deterministic bounded scan over an explicit ordered candidate domain, calls derive READ-ONLY, reports the FIRST crossing as an honest grid bracket with no invented-precision crossing (the sole sub-grid value is a clearly-labelled illustrative midpoint, never Verified). Does not assume monotonicity — counts all crossings and sets a typed `non_monotonic` flag on ≥2 crossings (independently verified). Contract-free (stdlib + .derive + .constants + ._json_safety), shared sanitizer no re-duplication, cap verbatim, never Verified, all degenerate/error cases typed markers (no raise/ZeroDivision/NaN/Inf), strict-JSON-safe under adversarial input, additive `__init__.py`. 384 passed / 0 fail. Modularity EXIT 0 (non-blocking warn). Cohesion: single responsibility, no responsibility mixing, split not warranted. No blocking defects; three LOW advisory items.
