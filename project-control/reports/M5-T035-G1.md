# M5-T035 — G1 data-contract review return (VERBATIM; data-contract-verifier, read-only, pinned 97fa2eea)

Orchestrator note: returned via the agent-return channel 2026-09-18T06:33:30Z; the transport
truncated the first delivery inside section 2. Part 1 below is the truncated delivery
verbatim; Part 2 is the reviewer's resent remainder, verbatim. VERDICT: PASS (two
non-blocking findings).

---

## Part 1 (first delivery, verbatim)

All corroborating suites pass locally (ruff clean; 123 connector+engine, 65 provider+endpoint) on Python 3.11.9 — CI at 3.12 on the pushed head remains the AS-7 authority. My review is complete.

---

# G1 DATA-CONTRACT REVIEW — M5-T035 (DB-015 live wide-street determination provider)

## VERDICT: PASS
Two non-blocking findings (one provenance mislabel to fix before DB-014 serializes the block; one bounded named-override observation refining DB-010). Neither affects the currently-serialized contract, the fail-safe direction, or any granted FAR.

**Identity check.** `git diff 97fa2eea..HEAD -- services/ docs/RENDER_INTERNAL_WEB_DEPLOY_CHECKLIST.md` is empty; the reviewed allowed_paths content is byte-stable between the pinned material commit `97fa2eea` and live HEAD `3ce7b87d` (control-plane-only follow-up). Reviewed at the pinned identity.

## 1. DCM ArcGIS request/response vs the official source — CORRECT
- **Official source confirmed.** `services5.arcgis.com/GfwWNkhOj9bNBqoJ` is the DCP_GIS org; `DCM_Street_Center_Line/FeatureServer/0` is a live DCP-owned layer (ArcGIS Hub `DCP::dcm-street-center-line`, data.gov, NYC geo-metadata). Matches `dcm_street_centerline_arcgis.py:122-123`.
- **Envelope query params correct per the ArcGIS REST query spec** (`build_segment_query_url` :435-446): `geometryType=esriGeometryEnvelope`, `inSR=2263`, `spatialRel=esriSpatialRelIntersects`, `geometry=xmin,ymin,xmax,ymax` (4-decimal, comma-joined, URL-quoted). The metadata fixture's `supportedSpatialRelationships` includes `esriSpatialRelIntersects`, and `spatialReference` is exactly `wkid 102718 / latestWkid 2263`, matching `EXPECTED_WKID`/`EXPECTED_LATEST_WKID`. The default `intersects` is the correct superset relation for a ZR "within 100 ft" candidate gather.
- **EPSG:2263 units consistent end-to-end.** The envelope is derived from MapPLUTO lot bounds (`wide_street_live_provider._lot_envelope` :273-285), and MapPLUTO's geometry connector guarantees 2263 (raises `wrong_crs` otherwise); `inSR=2263` matches; there is no reprojection path. No unit mismatch.
- **Metadata-first + request-before-I/O ordering verified.** In `fetch_street_segments` the first-page URL (with full envelope validation) is built at :991-1000 **before** `fetch_layer_metadata` at :1002, so a bad envelope is refused with `disallowed_request` without a round-trip, while the freshness pin + schema-drift + CRS gate still run before any page is trusted (tests `..._refuses_bad_envelope_before_any_network_io`, `..._preserves_the_wrong_crs_gate`).
- **Schema-drift / CRS / paging guards preserved on the envelope path.** `SchemaDriftError` (name/geometryType/objectIdField/maxRecordCount), `WrongCRSError` (non-102718), and paging pathologies (duplicate page, repeated OBJECTIDs, zero-progress, `HARD_MAX_PAGES`) all fire on the envelope path (`..._preserves_the_paging_pathology_guard`; deterministic `resultOffset=len(segments)` + `orderByFields=OBJECTID ASC`).
- **Predicate exclusivity + injection safety.** `predicate_styles` enforces exactly-one XOR (:413-431); envelope is a constant-true `1=1` WHERE plus formatted-float geometry (no SQL surface); `inSR`/`spatialRel` are hardcoded constants. Non-finite/absurd/inverted/boolean/oversized-int envelopes fail closed with bounded diagnostics (`_safe_repr` avoids the int→str cap ValueError).
- **Observation (not a defect).** `build_segment_query_url` omits `returnGeometry`, relying on the ArcGIS default TRUE (satisfies the packet's "returnGeometry TRUE"); this attribute-only connector then discards the returned geometry — minor wasted bytes, functionally correct, well under the 16 MB read ceiling.

## 2. No invented fields/units; provenance preserved — HOLDS (one mislabel)
- `OUT_FIELDS` (:151-169) is the documented field allowlist and matches the metadata fixture's `fields[]` (minus geometry); `FEAT_TYPE_DOMAIN` and `BOROUGH_DOMAIN` match the metadata; no `*`.
- CRS threaded correctly: both the geometry connector (`CRS_STAMP`) and MapPLUTO [TRANSPORT TRUNCATION POINT]

---

## Part 2 (resent remainder, verbatim; delivered ~06:35Z — report COMPLETE)

Resending the G1 report from the truncated CRS bullet through the end (verdict recap at bottom):

- CRS threaded correctly: both the geometry connector (`CRS_STAMP`) and MapPLUTO (`CRS_STAMP` `{"wkid","latest_wkid"}`) expose the keys the provider reads (`crs.get("wkid")`, `crs.get("latest_wkid", crs.get("latestWkid"))` at `wide_street_live_provider.py:264-266` and `:349-350`), so `wkid=102718 / latest_wkid=2263` populate rather than silently defaulting to None.
- D-052 quintuple carried: `source_version` (`source_data_last_edited or retrieved_at`), `matched_geometry_ref` (`OBJECTID=…`), interpreted bounds, classification reasons, and per-decision `decision_state` aggregated in `wide_street_wiring._provenance` (:210-223).
- FINDING F1 (non-blocking — provenance mislabel). `wide_street_live_provider.py:356` passes `source_raw_digest=entry.segment.streetwidth_raw` for each `AttestedWideSegment`. That field is documented as "raw-body-digest provenance passthrough" (`wide_street_buffer_engine.py:426-428`), but it receives the street-WIDTH text (e.g. `"80"`), not a digest. A real digest is available (`SegmentGeometryPage.raw_digest` / `geom_result.raw_digests`). The lot side is correct (`source_raw_digest=lot_result.raw_digest`, :270). Currently latent — this provenance flows only into the structured `wide_street_determination` block, which is deliberately NOT serialized (DB-014) — so no user-facing impact today. Fix before DB-014 serializes the provenance block, or it will surface a mislabeled digest.

## 3. Fail-safe direction (D-051) — CORRECT
- Every failure/insufficiency class returns `None` + a payload-only typed log (class + correlation id, never `str(exc)`): lot connector/unexpected error, lot-not-usable, envelope-unavailable, segment connector/unexpected error, transfer-limited segment page, zero segments (honest absence), wide-geometry connector/unexpected error, transfer-limited geometry page (`build_live_wide_street_determination` :377-435; `_fail_safe` :231-239). All covered by named passing provider tests.
- Never fabricates `within_100ft`. EC-5 preconditions left unattested (`_UNATTESTED_EC5` :138-147), so any wide-disposed segment routes through the wiring to `professional_review_required` (`wide_street_wiring.py` :455-471; tests `..._yields_professional_review_never_fabricated_wide`, endpoint professional-review test). `within_100ft` is producible only by a caller-supplied fully-attested determination (the AS-3 override tests), exactly as documented.
- All-narrow → confident `not_within` grants the conservative LOWER FAR (`FALLBACK_DIRECTION_NOTICE`, validated for the ZR 23-22 rows only). The two-layer exceptions model is honest: policy-layer `exceptions_checked=True` attests the WIDTH-READING exceptions (accepted M4-T019 semantics), while the ZR 12-10 named-street/alternate-width provision is handled by `NamedStreetOverrideStatus` + `_elevated_exceptions_checked` (:226-245) and the EC-5 gate. Setting `exceptions_checked=True` is not an overclaim under this accepted design — the EC-5 gate, not the policy attestation, is the load-bearing block on a fabricated within.
- OBSERVATION F2 (bounded; within DB-010). The provider hard-codes `NamedStreetOverrideStatus.segment_may_touch_named_override=False` for all lots without screening (:153-161). For a lot near the two named-override streets (Broadway W94–97 CD7; Allen St Rivington–Delancey CD3) that is ALSO narrow-by-width, the all-narrow path emits a confident `not_within` rather than escalating. FAR-conservative (higher wide-street FAR withheld, never over-granted) and DRAFT pending G6, so no unsafe result; refines the DB-010 discovery the producer already surfaced (report §6). No action required in this packet.

## 4. Settings gate — PROVEN
`live_wide_street_provider_enabled` reads the env var on every call; only explicit true tokens `{1,true,yes,on}` enable (:164-172). Flag off ⇒ `None` with zero connector calls, endpoint byte-identical: `test_m5t035_flag_off_default_wide_provider_zero_calls_byte_identical` asserts `recording.calls == {"lot":0,"segments":0,"geometries":0}` AND `json.dumps(live_default, sort_keys=True) == json.dumps(baseline, sort_keys=True)`. `rule_evaluation.py` wires the default seam exactly as the spatial-substrate default (:150-171).

## 5. Deploy-checklist honesty — HOLDS
§6c documents `LIVE_WIDE_STREET_PROVIDER_ENABLED` with the fail-safe default, the "flag reading + typed logs decide, never the response shape alone" discipline (correlation-id-matched `_fail_safe` line), and DRAFT/D-045-R009. §6d pins `PYTHON_VERSION=3.12.11`; its cited constants `PINNED_SHAPELY_VERSION=2.0.7` / `PINNED_GEOS_VERSION_STRING=3.11.4` are real (import-time asserts at `wide_street_buffer_engine.py:959-966`), and it cites CI `python-version: "3.12"` / `requires-python >=3.12`. No real deploy URLs (service name `nycdf-api` + bracketed placeholders only).

## 6. AS-3 / DB-014 (verified within the orchestrator ruling)
The frozen `rule_evaluation @ 1.0.0` schema root is `additionalProperties:false` with a closed `contract_version` enum (`app/_contract_schemas/v1/rule_evaluation.schema.json`); the structured wide-street block is deliberately not serialized. The decision effect surfaces through `coverage_status`, `professional_review_required`, and a `reasons[]` entry naming the governing FAR + DRAFT marker, with the DSL trace staying the conservative `max_residential_far == 2.2`. The two endpoint tests assert the effect and pin the structured block's absence (`assert absent not in doc`). Per the binding ruling, the missing serialization is NOT reported as a gap.

## 7. Corroboration (read-only)
Local Python 3.11.9 (repo targets 3.12; CI on the pushed head is the AS-7 authority): `python -m ruff check services/api` → clean; `test_dcm_street_centerline_arcgis.py` + `test_wide_street_buffer_engine.py` → 123 passed; `test_wide_street_live_provider.py` + `test_rule_evaluation_api.py` → 65 passed. Suites collected and passed here despite the 3.11/3.12 gap, corroborating the harvest evidence.

## Recommendation
Accept G1. Track F1 (source_raw_digest mislabel) so it is fixed in the DB-014 serialization follow-up before the structured provenance block reaches any response; F2 is already covered by DB-010. No blocking data-contract defect.

VERDICT: PASS (two non-blocking findings above; neither affects the currently-serialized contract, the fail-safe direction, or any granted FAR).

Sources (official-source verification):
- ArcGIS REST query spec: https://developers.arcgis.com/rest/services-reference/enterprise/query-feature-service-layer/
- DCM Street Center Line (ArcGIS Hub, DCP): https://hub.arcgis.com/datasets/DCP::dcm-street-center-line
- DCP_GIS org services folder: https://services5.arcgis.com/GfwWNkhOj9bNBqoJ/ArcGIS/rest/services
- data.gov DCM_StreetCenterLine: https://catalog.data.gov/dataset/dcm_streetcenterline
- nyc-geo-metadata StreetCenterline: https://github.com/CityOfNewYork/nyc-geo-metadata/blob/main/Metadata/Metadata_StreetCenterline.md
