# M5-T089 G0 — readiness gate (orchestrator, administrative)

Task: **M5-T089 — D-087 3D-2: official NYC building-footprint + height connector (OTI BUILDING_view ArcGIS, outSR=2263) - footprints on one BBL and its neighbours as typed, provenance-bearing context buildings with a relative ground datum; recorded fixtures; fail-closed; unwired (connector + tests)**

## Authority

- Owner directive D-087 (capacity + released feature families): an orchestrator-dispatched subagent
  producer packet; D-087-R001 capacity, R002 non-interference.
- Directive refs: D-087-R001, D-087-R002, D-087-R003, D-087-R009, D-066-R001 - bound via applicability.task_ids with same-commit digest
  resyncs + audit_log entries; `evaluate_task_refs`: **ok=true, applicable == cited**.
- Scope source: the packet's objective and SPEC SOURCE input (the D-087 released feature families,
  the accepted-task review riders in docs/DISCOVERY_BACKLOG.md, or DB-052 for the harness packet).
- Dependency security: zero new packages here (stdlib + already-admitted only).
- Boundaries unchanged (D-087-R009): every gate, Tier D, PR #241, native-DWG licensing (R007
  hold), the D-086 sequencing; the max-envelope route stays UNMOUNTED; no route or web wiring
  unless the packet says so (none of this wave does).

## Packet readiness

- allowed_paths: 4 entries (existing files are tracked; each NEW path is
  seeded with a committed placeholder at this seam).
- 5 acceptance scenarios, each with a named mutation or
  grep/CI proof obligation; documented_test_commands are plain single commands.
- Producer: geospatial-engineer; reviewers: data-contract-verifier,geospatial-engineer,qa-engineer,security-reviewer,directive-compliance-verifier; gates G0,G1,G2,G3,G4,G5.
- Code graph regenerated at this seam (817 files/17386 nodes/7525 edges); the navigation block names consumers,
  forbidden neighbors, and the `query.py --no-regen impact` instruction (D-066-R001).

## Disjointness (live/frozen neighbors)

| Neighbor | State | Overlap |
|---|---|---|
| M0-T021 | awaiting_gate | none - EMPTY overlap |
| M0-T034 | awaiting_gate | none - EMPTY overlap |
| M0-T080 | in_progress | none - EMPTY overlap |
| M0-T109 | awaiting_gate | none - EMPTY overlap |
| M0-T133 | rework | none - EMPTY overlap |
| M0-T145 | awaiting_gate | none - EMPTY overlap |
| M0-T153 | claimed | none - EMPTY overlap |
| M0-T155 | claimed | none - EMPTY overlap |
| M0-T159 | awaiting_gate | none - EMPTY overlap |
| M5-T001 | awaiting_gate | none - EMPTY overlap |
| M5-T077 | awaiting_gate | none - EMPTY overlap |
| M5-T078 | awaiting_gate | none - EMPTY overlap |
| M5-T079 | awaiting_gate | none - EMPTY overlap |
| M5-T080 | awaiting_gate | none - EMPTY overlap |
| M5-T081 | awaiting_gate | none - EMPTY overlap |
| M5-T083 | awaiting_gate | none - EMPTY overlap |
| M5-T086 | awaiting_gate | none - EMPTY overlap |
| M5-T088 | contracting | none - EMPTY overlap |
| M5-T090 | contracting | none - EMPTY overlap |
| M5-T091 | contracting | none - EMPTY overlap |
| M5-T092 | contracting | none - EMPTY overlap |

## Seam records

- Packet + binds + digest resyncs + placeholders + this report land in ONE contract-seam
  commit; G0 is recorded at that head; claim with the FULL worktree path; the worktree is
  created AT the claim-seam commit.

Verdict: **PASS** — packet is contract-ready.

## Reviewer-roster amendment (G0 re-record)

- The packet's producer is geospatial-engineer, which is also the only G3-class reviewer on its roster; a producer may
  not review its own work. Per the standing rule (fix a missing independent reviewer by ADDING one, never by dropping
  the gate), code-reviewer is added to reviewer_agents as the independent G3 reviewer. No scope, path or gate change.
- G0 is re-recorded at the amendment head; the task is re-claimed with the same FULL worktree path.

Verdict (re-recorded): **PASS**.
