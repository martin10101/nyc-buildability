# M5-T037 — G4 QA review return (VERBATIM; qa-engineer, read-only, pinned a6875731)

Orchestrator note: returned via the agent-return channel 2026-09-18T10:11:42Z; the transport
truncated the first delivery inside AS-5. Part 1 below is the truncated delivery verbatim;
Part 2 is the resent remainder, verbatim. VERDICT: PASS (2 non-blocking advisory findings
routed as discoveries). (The reviewer's opening line notes its agent-memory write was blocked
by the read-only guard — expected for named reviewer spawns; no repository write occurred.)

---

## Part 1 (first delivery, verbatim)

The memory write is blocked because I'm dispatched as a read-only reviewer (`m5t037-g4`) — the guard forbids all writes, including agent-memory in this session. That's fine; I'll proceed to the report. My review is complete.

---

# G4 QA Gate Report — M5-T037

**Verdict: PASS** (with 2 non-blocking advisory findings routed as discoveries)

Task: DB-014 screen wiring — rule_evaluation v1.1.0 additive contract bump + DB-020 digest fix + web display + report parity.
Reviewer: qa-engineer (read-only). No repository file was edited, created, or committed.

## Identity / scope verification
- **allowed_paths byte-stable a6875731..HEAD: CONFIRMED.** `git diff --name-only a6875731 HEAD` over all 21 packet allowed_paths returned **empty**. Material commit `a6875731`; submission identity stamped `f954aa59`. HEAD is `fa2aecab`.
- The only material-code change in `a6875731..HEAD` outside allowed_paths is `services/api/tests/scenario/test_scenario_contract.py` — the sanctioned `[ORCH-CORRECTED]` version-pin guard update (commit `be0a7062`), which is an out-of-scope consumer of the contract enum change, not in the packet. Legitimate per the consumer-sweep rule; frozen submission identity preserved.
- **Schema copies byte-identical: independently CONFIRMED** (LF-normalized diff of canonical `packages/contracts/schemas/v1/rule_evaluation.schema.json` vs bundled `services/api/app/_contract_schemas/v1/rule_evaluation.schema.json` = empty).

## Acceptance-scenario adequacy

**AS-1 (DB-020 digest) — PASS, single-page only (see F1).** `test_db020_attested_segment_carries_geometry_page_digest_not_width_text` asserts the sha256 shape AND the **exact** value `digest == raw_body_digest(_dcm_page_body(features))` (not just 64-hex), plus `digest != "80"` and that `streetwidth_raw` is preserved but distinct. The old width-text value would fail this. This is a genuine, non-vacuous regression of the exact defect. Production code (`_attested_wide_segments`, `wide_street_live_provider.py:347-363`) correctly binds each segment to its own `page.raw_digest` while walking `geom_result.pages`.

**AS-2 (contract) — PASS.** typegen `--check` clean (CI + harvest); schemas byte-identical (verified); enum exactly `["1.0.0","1.1.0"]`. A 1.1.0 doc with the block validates at both api (endpoint `iter_errors` against both copies) and web layers. Extra-key rejection is proven strongly at the api schema layer: `test_m5t037_as2_schema_rejects_undocumented_extra_key_in_block_and_at_root` rejects an undocumented key at **root AND inside the block** on **both** schema copies (canonical fixture validator + bundled `validate_rule_evaluation_document`). Web validator genuinely rejects an unknown version (`contract_version = "2.0.0"` → `ok:false`). Web extra-key behavior is a divergence — see F2.

**AS-3 (serialization) — PASS.** Both branches asserted at the endpoint (`test_m5t037_as3_within_wide_serializes_block_and_none_omits_block_via_endpoint`): within-wide serializes the block, validated against both schema copies; provider `None` omits the block, document stays valid 1.1.0. Also at the integration layer (`test_m5t037_as_dict_emits_wide_street_block_on_within_wide_fold` / omit tests). Values are exact and non-vacuous (governing FAR 3.0, `matched_geometry_refs`, `policy_decision_states`, DRAFT marker; `export()` parity; no verified label).

**AS-4 (consumers) — PASS.** Full `tests/api` = 426 passed (CI + harvest). The evidence/scenario/scenario_analysis suites reference their **own** contracts (`EVIDENCE_CONTRACT_VERSION`, `profile_contract_version`), not rule_evaluation's `contract_version`, which is why the additive bump left them green — confirming the change is non-breaking (AS-4's literal requirement). The strong shape-catching guarantee (schema `iter_errors` on every produced document) lives in `test_rule_evaluation_api.py`, which is the correct place for it.

**AS-5 (web display) — PASS.** `WideStreetResult` is genuinely data-driven: the within branch renders `farValue([TRANSPORT TRUNCATION POINT]

---

## Part 2 (resent remainder, verbatim; delivered 10:12:24Z — report COMPLETE in substance; the
final sentence clipped mid-word after the explicit "Verdict: PASS", noted below)

Resending from where AS-5 truncated onward.

**AS-5 (web display) — PASS.** `WideStreetResult` is genuinely data-driven: the within branch renders `farValue(governing_max_residential_far)` + "dimensionless ratio" units + "within 100 ft of a wide street" conditions; the review branch **withholds the FAR** (it is never rendered in that branch), so the professional-review test's `queryByText(/3\.44/)` null is a real guarantee, not vacuous; `if (!wide) return null` gives the genuine documentless-1.0.0 case. All three cases (within-wide / professional-review / no-block) are asserted, including the DRAFT marker ("Pending qualified legal review") and the sources link href to the evidence view.

**AS-6 (report parity) — PASS.** `report-view.test.tsx` uses **one shared fixture** (`wideStreetDoc`), renders it to `DevelopmentLimits` and to `ReportView`, and asserts `reportValue === screenValue` plus the report's `wide-street-provenance` surface (`OBJECTID=12345`) and the no-invented-block negative on both surfaces. I validated the fixture's `wide_street` block mentally against the real `$defs/wide_street`: all 15 required keys present, correct types, no extra keys — **no fixture drift**.

**AS-7 (CI) — PASS.** Corrected head `be0a7062`: all 20 check-runs green incl. web + web-e2e (recorded-1.0.0-fixture compatibility proven). The single first-run red at `f954aa59` was the out-of-scope contract-guard (twice-run, one root cause), corrected out-of-scope in `test_scenario_contract.py` (not in allowed_paths). `contract-versions.test.ts` confirms a 1.0.0 doc stays valid, protecting the web-e2e fixtures.

**AS-8 (preservation) — PASS.** No verified/compliance claim introduced; DRAFT/`not_verified_disclaimer` assertions present at both the integration layer (`_iter_coverage_values` excludes verified; `"DRAFT" in draft_label`) and the web layer; the schema narrows coverage to exclude `verified`; the block carries the DRAFT label and the D-051 fallback-direction note.

No vacuous assertions, shared-state, or order issues found (web tests use `afterEach(cleanup)` plus an explicit `cleanup()` between the two parity renders).

## Non-blocking findings (route as D-069 discoveries; do not block acceptance)

- **F1 (AS-1, test hardening):** The DB-020 regression exercises only a **single** geometry page — `_geometries_double` always builds `pages=[page]`. The multi-page "each segment carries its OWN page's digest" case AS-1 names is not regression-guarded. The production code binds per-page correctly (`for page in geom_result.pages: … source_raw_digest=page.raw_digest`, `wide_street_live_provider.py:347-363`), and the original text-vs-digest defect IS guarded, so AS-1's literal criteria are met. Recommend a follow-up multi-page fixture asserting a segment parsed from page 2 gets page 2's digest.

- **F2 (AS-2, discovery + doc accuracy):** The web runtime validator `rule-evaluation-contract.ts` is a **positive-shape checker** — it verifies documented keys are present and well-typed but never enumerates/rejects unknown keys (root or in-block; `checkWideStreet` included). It therefore does **not** enforce `additionalProperties:false`, so the AS-2 extra-key negative exists only at the api schema layer and cannot be written to pass at the web layer under current behavior. This diverges from the packet nav-block's claim that the web validator is a "documented-key-set check: an undocumented key FAILS validation" (pre-existing behavior; defense-in-depth, since the trusted API already rejects extra keys before serving). Recommend either tightening the web validator to reject unknown keys + adding the web-layer negative, or correcting the packet/docstring wording.

Producer report: `project-control/reports/M5-T037-producer-report.md`. CI evidence: `project-control/reports/M5-T037-ci-evidence.md`.

**Verdict: PASS** — 2 non-blocking advisories (F1, F2) to route as discoveries; no b[final half-sentence clipped by the transport; the reviewer's verdict line above is complete and explicit]

---

## Delta-attestation round 1 (summary, 10:22:36Z at head 8240a497): does-NOT-carry — the F2 casing regression (capital-P assertion at development-limits.test.tsx:563 vs the new lowercase panel line; case-sensitive matcher; deterministic vitest red predicted) with the same one-line fix the HJ recommended; both new report-view guard tests judged sound and non-vacuous. Full text crossed with the fix landing at 1c89922c; superseded by round 2 below.

## Delta-attestation round 2 (VERBATIM; delivered 10:24:23Z at head 1c89922c — CARRIES)

Verified. The casing regression is fixed and consistent across both suites, no web source file changed after the rework, and no api/contract file changed. Here is my attestation.

---

# M5-T037 delta-attestation — head 1c89922c (vs reviewed a6875731)

**Answer: YES — my G4 PASS carries to 1c89922c.** The one blocking regression I flagged is resolved; the two new guard tests are sound and non-vacuous; the casing is now consistent across both suites and the component. One orchestrator action remains: a fresh CI run (below).

## What I verified read-only at 1c89922c
- **Casing regression FIXED.** `development-limits.test.tsx:563` now asserts `toHaveTextContent("pending qualified legal review")` (lowercase), matching the component's `Draft — pending qualified legal review (not verified).` (`DevelopmentLimits.tsx:81`). `report-view.test.tsx:136` asserts the full lowercase line `"Draft — pending qualified legal review (not verified)."`. All three agree; the case-sensitive substring match now succeeds. **Casing consistent across both suites.**
- **The fix is exactly one line.** `git diff a6875731..1c89922c -- development-limits.test.tsx` is only the `Pending`→`pending` change plus a 2-line `[ORCH-CORRECTED]` comment. Nothing else in that file moved, so the rest of my AS-5 analysis is untouched.
- **No web source changed after the rework.** `git diff 8240a497..1c89922c -- apps/web` shows only `development-limits.test.tsx`; `DevelopmentLimits.tsx` and `ReportView.tsx` are byte-stable `8240a497..1c89922c` (empty stat) — i.e. identical to the F1/F2 forms I already reviewed in 75daaebd.
- **No api/contract change.** The only api/contracts file in `a6875731..1c89922c` is the sanctioned out-of-scope guard `services/api/tests/scenario/test_scenario_contract.py`. No schema, serializer, or api source moved — so AS-1, AS-2, AS-3, AS-4, AS-7, AS-8 (backend/schema/contract) are unaffected and carry unchanged.

## The two new guard tests (sound and non-vacuous)
- **Non-inspectable parity guard** (`report-view.test.tsx`): makes the doc non-inspectable (`evaluations[0].rule_id = 123`), renders `ReportView`, asserts `development-wide-street` and `wide-street-result` are absent, and confirms the screen equivalent passes `null`. Genuinely exercises the F1 fix — it would fail if `ReportView` still fed the ungated `identityEvaluation`. Non-vacuous.
- **Real-constants jargon guard** (`report-view.test.tsx`): injects the actual `DRAFT_LABEL_NOTICE`/`FALLBACK_DIRECTION_NOTICE`/fold-reason wording, renders `DevelopmentLimits`, and asserts the panel textContent excludes `D-045-R009`, `D-051`, `needs_review`, `within_100ft_of_wide_street`, `ZR 23-22`, while still showing the clean draft line and "3.44". Would fail if the F2 raw-string removal were incomplete. Non-vacuous.

## F1 gating carries
`ReportView.tsx:68` feeds `DevelopmentLimits` the inspectability-gated `evaluation` (the same form `CalculationEvidence` receives) rather than the ungated `identityEvaluation` — a strict tightening that closes the screen/report divergence the HJ and G3 reviewers converged on. Consistent with my AS-6 parity judgment; it cannot make a previously-withheld panel appear.

## AS carry-over at 1c89922c
- AS-1/2/3/4/7/8 — carry unchanged (no api/schema/serializer change).
- **AS-5 — now carries** (within-wide, professional-review, no-block cases all intact; the DRAFT-marker assertion is casing-aligned).
- AS-6 — carries, strengthened by F1 and the new non-inspectable parity guard.

## One remaining orchestrator action (not a blocker to the attestation, but required before acceptance)
The committed CI evidence (`M5-T037-ci-evidence.md`) is for the **pre-rework** head `be0a7062`. The rework commits (`75daaebd` F1/F2, the casing commit) are **not** covered by that run. Because web proves only in CI on the pushed head, the orchestrator should capture a **fresh all-20-green CI run on `1c89922c`** (especially `web` + `web-e2e`) to confirm AS-7 for the corrected head.

## Round-2 tail (VERBATIM; delivered 10:24:53Z — completes the record)

Remainder of the jargon-guard assessment (from where round-1 truncated), then my attestation at 1c89922c.

## Jargon-guard test (remainder) — sound and non-vacuous
It injects the actual `DRAFT_LABEL_NOTICE` / `FALLBACK_DIRECTION_NOTICE` / a real fold-reason string into `wide_street.draft_label` / `fallback_direction_note` / `reason`, renders `DevelopmentLimits`, and asserts the panel's textContent does **not** contain the internal tokens `D-045-R009`, `D-051`, `needs_review`, `within_100ft_of_wide_street`, `ZR 23-22`, while still asserting the clean line (`wide-street-draft` = "Draft — pending qualified legal review (not verified).") and the value (`wide-street-result` = "3.44"). Because the F2 change dropped `{wide.reason}` / `{wide.fallback_direction_note}` / `{wide.draft_label}` from the panel, those tokens cannot appear — and the test would fail if any raw string leaked back in, so it genuinely locks the F2 correction. It uses the real wiring constants as the fixture, so future drift in the server strings can't silently re-introduce jargon. Non-vacuous and correctly scoped to the calm panel (`getByTestId("development-wide-street")`).

**Verdict: my G4 PASS carries to 1c89922c** — existing assertions intact, casing consistent across both suites, two new guard tests sound. One orchestrator action remains before acceptance (not a blocker to this attestation): the committed CI evidence is for the pre-rework head `be0a7062`, so capture a fresh all-20-green run on `1c89922c` (especially `web` + `web-e2e`) to confirm AS-7 at the corrected head. My source analysis predicts green — the casing mismatch was the only defect and it is now aligned — but web behavior must be confirmed in CI, not from local reasoning.
