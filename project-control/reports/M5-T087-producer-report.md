# M5-T087 producer report - PLACEHOLDER

Seeded at the contract seam so the packet's allowed_paths carry a tracked file (gates fail
closed on zero tracked files). The producer replaces this content with the evidence pass.
