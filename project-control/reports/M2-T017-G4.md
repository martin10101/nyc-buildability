# M2-T017 — G4 contract-integrity review (independent, read-only) — PR #99
Reviewer: data-contract-verifier (!= producer). Reviewed head fead3f38 vs main cc14208.
**VERDICT: PASS.** Both contracts CLOSED (additionalProperties:false); AST adds only the close (actor enum
unchanged). The 4 added source_fact keys correctly typed via common $refs; input_vintages inner map intentionally
open while the record stays closed; descriptions cite real emitters. Read every source_fact producer at the frozen
SHA: every emitted key is a subset of the documented set (no field dropped/rejected); validate_contracts.py 0
failures; 406 producer/contract tests pass. $id kept at v1 (in-place additive hardening); canonical vs bundled
byte-identical; typegen no drift. Negatives fail / positives pass causally (37 tests). Non-blocking: stale inline
comments in pluto_soda.py (~L837) and ztldb_soda.py (~L1424) still say "source_fact v1 permits additional keys"
(now inaccurate; no functional impact) — optional follow-up.
