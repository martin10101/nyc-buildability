# M5-T010 — G5 Security/Privacy Review (verbatim reviewer return)

Reviewer: **security-reviewer** (independent ≠ producer scenario-optimization-engineer). Reviewed SHA `ea08f504ea4b52cc93062a74a9c125e5133c4b7b`. Transport entity-decoding applied; content otherwise verbatim.

---

# Gate Report

- **Gate ID:** G5 (security/privacy)
- **Task ID:** M5-T010 (deterministic scenario comparison/delta engine)
- **Reviewer:** security-reviewer (independent; NOT the producer)
- **Producer:** scenario-optimization-engineer (build loop)
- **Result:** **PASS**
- **Clean environment/worktree used:** Reviewed at frozen HEAD `ea08f504ea4b52cc93062a74a9c125e5133c4b7b` (verified == packet SHA). Primary checkout, Python 3.11.9. Read-only; no repo/control-plane writes.

## Acceptance criteria reviewed (security-relevant subset)
AS-1..AS-7 reproduced via the committed suite and independent adversarial probes. Full 71-item comparison suite green; full scenario suite green at 323 passed (252 pre-existing + 71 new — matches the AS-6 target exactly, 0 regressions).

## Directive/requirement verification
Task is in-regime (`directive_refs: D-038:ALL`). Full per-requirement directive verification is the `directive-compliance-verifier`'s pass (producer != verifier) recorded in `verification.json` — out of this G5 security scope. The two security-load-bearing AI-boundary requirements are independently verified below.

| Requirement | Reviewed identity | Verdict | Reproduced evidence |
|---|---|---|---|
| Never-Verified (contract-free, no AI legal calc) | HEAD ea08f504 | PASS | `_bounded_coverage_status` caps incoming `verified`->`conditional` (comparison.py:175-185); `_base_lineage` stamps `needs_review=True` + canonical `NOT_VERIFIED_DISCLAIMER` unconditionally (:193-208). Probe: input `coverage_status="verified"` never surfaced as a `verified` coverage on any of ~20 probe inputs; `"verified"` absent from all coverage values. Test `test_as3_*` green. |
| Consumes derive READ-ONLY, no independent legal recompute; cap transported verbatim | HEAD ea08f504 | PASS | Metrics/cap taken verbatim from `derive_practical_usable_range` (comparison.py:339-356); only arithmetic is baseline-relative diff/ratio of already-surfaced numbers. Input byte-unchanged and not aliased (probe I: doc/sets JSON identical after mutating result; `test_as6_*` green). |

## Steps independently executed
1. `git rev-parse HEAD` -> `ea08f504…` matches packet. `git show --name-only HEAD` -> only the 4 allowed paths changed (`comparison.py`, `__init__.py`, `test_scenario_comparison.py`, producer report); **no forbidden scenario file** (`derive/builder/models/constants/contract/ranking/sensitivity/_json_safety`, `packages/contracts`) touched -> AS-7 contract-free confirmed.
2. `python -m pytest tests/scenario/test_scenario_comparison.py -q` -> **71 passed**.
3. `python -m pytest tests/scenario -q` -> **323 passed** (no regression).
4. `python tools/modularity_check.py --check` -> failures 0 (exit 0); no warning for any `scenario` file.
5. Grep of `comparison.py` for `socket|requests|httpx|urllib|http.client|subprocess|os.|sys.|open|pathlib|supabase|psycopg|sqlalchemy|boto3|getenv|eval|exec|__import__` -> **no matches** (offline confirmed; imports are only `json`, `math`, `typing`, `._json_safety`, `.constants`, `.derive`).
6. Adversarial probes via the public API (`python -c`, no file writes) — see below.

## Expected versus actual (adversarial probe domain — the JSON/public-API input surface)
For every input below, expected = `json.dumps(result, allow_nan=False)` never raises, no NaN/Inf, negatives only in delta fields, no `at 0x` address leak, no `verified` coverage, deterministic, fail-closed typed marker; actual = all held:
- Factor values NaN / +Inf / -Inf / -0.5 / `10**400` / `10**5000` / `2**2000` / negative-huge -> COMPARED, offending set flagged not-comparable via a typed marker, JSON-safe, no crash.
- Cap = `10**5000` (overflows float) / scenario_document is a `list` or arbitrary object -> typed **EMPTY** (fail-closed), no crash.
- Object (`Weird` with an address-bearing `__repr__`) as factor value, as set name, as whole entry, as `scenario_kind`, as `bbl`; huge int as name/bbl -> sanitized to type-name markers / `None`; **no `at 0x` leak**; byte-identical run-to-run (probe H).
- Cyclic assumption dict; 600-deep nested value (beyond `_MAX_JSON_SAFE_DEPTH=500`); non-string dict keys `{1:…,(2,3):…}`; tuple assumptions container -> all handled via shared `_json_safe` (cycle / max_depth / typed markers); JSON-safe.
- **Subnormal-baseline percent overflow** (factors `1e-160 * 1e-160` -> subnormal endpoint, other set full cap): `percent_delta = None` + typed `not_computable_reason`, `absolute_delta` still finite. **No Inf emitted** — the `math.isfinite(percent)` re-check (comparison.py:410) is load-bearing and correct.
- `_metric_delta` direct edges: zero baseline -> abs computed, pct None; None/NaN baseline -> both None; tiny baseline (1e-320) + huge set (1e300) -> abs finite, pct None (guarded); set=Inf -> both None. No `ZeroDivisionError`, no NaN/Inf on any branch.

## Regression/security/provenance findings
1. **Strict-JSON-safety (AS-5) — PASS.** Every untrusted echoed value (`assumption_set`, `derived`) flows through the shared `_json_safe` (comparison.py:306,315,331). Values emitted verbatim (metrics, `baseline_metrics`, delta `baseline_value`/`set_value`) are gated finite by `_extract_metrics`/`_finite_float` (:339-356) before a row is comparable; delta `absolute`/`percent` are finite-re-checked (:393,410). I could not construct any public-API input producing NaN/Inf, a negative-where-invalid, a non-serializable value, or an object-address leak.
2. **Numeric fail-closed (AS-4) — PASS.** Division site (:409) is guarded by an explicit `baseline_float == 0.0` check (:402) plus finiteness re-checks; percent overflow from a subnormal baseline yields a typed marker, never Inf. Verified live.
3. **Injection / untrusted-instruction surface — none.** No `eval/exec/subprocess/network/file/env`. Reason strings interpolate only `type(x).__name__`, `len()`, and already-bounded, address-free sanitized reprs (derive's `_bounded_echo` over `_json_safe`'d values); they are ordinary JSON string values (no structural injection).
4. **Cross-tenant isolation / service-role secrecy / private storage / SSRF / upload controls / log redaction — N/A.** This is a pure offline in-process function: no tenants, no secrets, no storage, no network, no uploads, no logging. Offline confirmed by grep + socket-blocked test (`test_as7_runs_fully_offline_with_sockets_blocked`).
5. **Prompt-injection defense — PASS by construction.** No AI/LLM invocation; deterministic code only. Never emits a `Verified` result (coverage capped, disclaimer stamped unconditionally, `comparison_kind` is always one of three non-`verified` constants).
6. **Least privilege / read-only consume — PASS.** Input document and assumption-sets are never mutated or aliased into the output (fresh sanitized structures; probe I + `test_as6_*`).

## Defects
None (no Critical / High / Medium).

## Low / defense-in-depth (non-blocking, currently unreachable)
- **L-1 (informational):** `metrics`, `baseline_metrics`, and delta `baseline_value`/`set_value` are emitted verbatim rather than through `_json_safe`, so strict-JSON-safety of those fields relies on the `_finite_float` gate in `_extract_metrics` (and derive's non-negative construction) rather than on the sanitizer. This is currently airtight — a non-number/non-finite metric forces the row not-comparable, and I found no reachable input that violates it — so no change is required. It is a coupling to derive's numeric contract worth a one-line note for any future derive change; not a finding against M5-T010.

## Required rework
None.

## Reviewer conclusion
**VERDICT: PASS.** M5-T010's comparison/delta engine is fail-closed and strict-JSON-safe across the entire adversarial JSON/public-API input domain I exercised: `json.dumps(result, allow_nan=False)` never raised, no NaN/Inf/negative-where-invalid/object-address ever surfaced, division and percent sites are guarded (no ZeroDivisionError/Inf), the shared `_json_safety._json_safe` (not a re-implementation) sanitizes all echoed/untrusted values, the module is fully offline (no network/storage/env/file/subprocess) and consumes `derive` read-only without mutating inputs, and no outcome is ever `Verified`. 323/323 scenario tests green, modularity clean, only the 4 allowed paths changed. No blocking security or privacy findings; one non-blocking defense-in-depth note (L-1).
