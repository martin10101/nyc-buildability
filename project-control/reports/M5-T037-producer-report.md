# M5-T037 producer report — DB-014 screen wiring (rule_evaluation v1.1.0 + DB-020 + display + report parity)

Task: M5-T037 · branch `task/M5-T037-screen-wiring` · worktree `wt-m5t037`
Contract-seam base: `c92154e7` (three-loop seam; G0 recorded at `8d095495`).
Producer: backend-engineer (persistent-local worker, M5-T037 lane).

This revision is an **evidence-handoff pass**. It does not change implementation identity; it
re-runs every documented command with recorded working directories and real outcomes, specifies
the exact bounded excerpts the supervisor must collect (digest-bound), quotes the exact task-file
delta with independent controller attribution, and states honestly that the backend acceptance
claims are **worker-tested but independently UNVERIFIED** until corroborating evidence arrives.
Web behavior (AS-5/AS-6/AS-7) stays EXPLICITLY PENDING orchestrator-collected pushed-head CI.

## What the packet asked (ORDER OF WORK) and what landed

1. **DB-020 first (M5-T035 G1 F1).** `services/api/app/spatial/wide_street_live_provider.py`
   `_attested_wide_segments` (def L325) now sets `AttestedWideSegment.source_raw_digest =
   page.raw_digest` (L362) walking the typed `geom_result.pages`, never the flattened `entries`.
   The provenance digest is the geometry-PAGE raw-body sha256, never the segment's free-text
   `Streetwidth` attribute.

2. **Contract bump to 1.1.0 (additive).** Both schema copies
   (`packages/contracts/schemas/v1/rule_evaluation.schema.json` and the byte-identical bundled
   `services/api/app/_contract_schemas/v1/rule_evaluation.schema.json`) append `"1.1.0"` to the
   CLOSED `contract_version` enum (L33) and add the OPTIONAL `wide_street` object
   (`$ref` at L146, `$defs/wide_street` at L450; `additionalProperties: false`, all D-052 summary
   keys required within the block). A 1.0.0 body stays valid because the block is optional and
   1.0.0 remains admitted.

3. **Serialization.** `services/api/app/rules/response.py`
   `RULE_EVALUATION_CONTRACT_VERSION = "1.1.0"` (L59, emitted for every document).
   `services/api/app/rules/integration.py` `PropertyRuleEvaluation.as_dict` (L160) emits
   `document["wide_street"] = dict(self.wide_street_determination)` (L193–194) **only when a
   determination folded in** (`wide_street_determination is not None`); otherwise the key is absent
   and the body is 1.0.0-shaped.

4. **TS regenerated byte-identically.** `packages/contracts/generated/rule_evaluation.ts` carries
   `contract_version: "1.0.0" | "1.1.0"` (L131) and the optional `wide_street?` block (L151).
   `generate_ts_types.py --check` is clean and the client SUPPORTED_CONTRACT_VERSIONS block matches.

5. **Web strict validator.** `apps/web/src/lib/rule-evaluation-contract.ts` admits
   `contract_version` from `RULE_EVALUATION_CONTRACT_VERSIONS = ["1.0.0","1.1.0"]` (L103) and adds
   `checkWideStreet` (L337, invoked L460): absent is valid; when present every documented key is
   shape-checked, so a malformed block fails TOTAL validation and never renders.

6. **Display + report parity (D-073-R003).**
   - `DevelopmentLimits.tsx` `WideStreetResult`: renders WHAT (wide-street conditional FAR row), its
     UNITS (dimensionless ratio; floor area = FAR × zoning-lot area sq ft), the CONDITIONS (within
     100 ft of a wide street), the DRAFT marker, and a link to SOURCES. `professional_review_required`
     shows the honest escalation and the higher FAR is withheld.
   - `CalculationEvidence.tsx`: renders the D-052 provenance summary via the existing `CapturedRecord`
     evidence affordance.
   - Report parity is structural: `ReportView.tsx` (UNMODIFIED) already embeds the SAME
     `DevelopmentLimits` (L68, fed `identityEvaluation`) and `CalculationEvidence` (L92) components
     fed the identity-matched document, so screen and report render the same values by construction
     (no second data path; no gap to record).

## Verification status — worker-tested, independently UNVERIFIED

Per permanent principle 7, a worker cannot mark its own task complete; it submits evidence for an
independent gate. Accordingly, the backend acceptance items below are described as **worker-tested**
(the producer executed the documented commands this unit and observed the real outcomes recorded in
the command table) **but independently UNVERIFIED** — no independent gate reviewer has confirmed
them and no orchestrator-collected pushed-head CI corroborates them yet. They are submitted
evidence, not accepted facts, and stay unverified until the independent gate review and CI arrive.

**Worker-tested (this unit, exit 0) — awaiting independent corroboration:**

- **AS-1 digest (DB-020):** worker-tested — `test_wide_street_live_provider.py` 28 passed incl. the
  exact geometry-page-digest regression.
- **AS-2 schema (both copies, extra-key rejection):** worker-tested — `generate_ts_types.py --check`
  OK; endpoint extra-key rejection test inside the 426-pass `tests/api` run.
- **AS-3 serialize/omit (endpoint + integration):** worker-tested — covered inside the 426-pass
  `tests/api` and 49-pass `tests/rules/test_rules_integration.py` runs (both branches: block present
  on within-wide fold, absent on provider None / non-conditional district).
- **AS-4 consumers (full `tests/api`):** worker-tested — 426 passed, all four api consumer surfaces
  (evidence, scenario, scenario_analysis, rule_evaluation).
- **AS-8 preservation:** worker-tested — DRAFT / `not_verified_disclaimer` assertions unchanged; no
  published/verified/compliance claim introduced.
- **Contract/type/modularity gates:** worker-tested — `generate_ts_types.py --check` OK;
  `packages/contracts` scripts tests 29 passed; `modularity_check --check` failures 0.

**Remaining — PENDING external evidence (NOT a producer claim of pass):**

- **AS-5 web display / AS-6 report parity:** NOT verified. Thin-client policy — npm/npx/node are not
  run locally; web proves ONLY in CI on the pushed head. The web validator, DevelopmentLimits /
  CalculationEvidence display, and ReportView parity are implemented and reasoned but stay PENDING
  the web + web-e2e CI jobs on the pushed head, supplied by the orchestrator against the submitted
  change.
- **AS-7 CI green on the pushed head:** PENDING orchestrator-collected pushed-head CI.
- **Supervisor-collected digest-bound excerpts:** PENDING — the manifest below tells the supervisor
  exactly which bounded excerpts to collect.
- **`project-control/tasks/M5-T037.json` provenance:** controller-owned; established independently
  and excluded from the material submission (see the task-file delta section). NOT a producer claim.

I am NOT claiming acceptance and NOT claiming all acceptance scenarios are verified. The backend
items are worker-tested and independently unverified; AS-5/AS-6/AS-7 and the transcript/task-file
items remain open pending orchestrator- and supervisor-supplied evidence tied to the submitted
change.

## Reproduced this unit (documented commands re-executed 2026-09-18, real cwd + real outcome)

Every documented command was RE-EXECUTED in this unit, verbatim, one per invocation, never chained.
First four from `services/api`, last three from the worktree root
`C:\Users\MLFLL\Downloads\nyc-zoning\wt-m5t037` (per the packet COMMAND CWD rule). All exit 0.

| # | cwd | command | actual outcome (exit 0) |
|---|-----|---------|-------------------------|
| 1 | `services/api` | `python -m ruff check .` | **All checks passed!** |
| 2 | `services/api` | `python -m pytest tests/api -q` | **426 passed in 12.79s** (all four api consumer suites green) |
| 3 | `services/api` | `python -m pytest tests/spatial/test_wide_street_live_provider.py -q` | **28 passed in 0.31s** (DB-020 exact-digest regression included) |
| 4 | `services/api` | `python -m pytest tests/rules/test_rules_integration.py -q` | **49 passed in 1.88s** (incl. the three `as_dict()` block serialize/omit tests) |
| 5 | worktree root | `python packages/contracts/scripts/generate_ts_types.py --check` | **OK: generated TypeScript types are up to date** (rule_evaluation incl.; client SUPPORTED_CONTRACT_VERSIONS block matches the enum) |
| 6 | worktree root | `python -m pytest packages/contracts/scripts/tests -q` | **29 passed in 0.30s** |
| 7 | worktree root | `python tools/modularity_check.py --check` | **selected 442 files; failures 0; warnings 19** (`integration.py` stays a warning-level `review_signal`, not a failure — see boundary justification) |

**Prior nonzero invocations — preserve SEPARATELY (supervisor duty).** Any earlier execution of the
first four `services/api` commands that exited nonzero must be preserved by the supervisor as a
SEPARATE, cwd-stamped execution in the collected transcript. The green re-runs above do NOT replace
or overwrite that earlier record; both stand as distinct executions, each with its own cwd stamp
(four `services/api`, three worktree-root).

**Root lint left untouched (out of scope).** Ruff was run from the `services/api` cwd (api CI job
step 1). A repo-root ruff pass is OUT OF SCOPE for this packet — `allowed_paths` does not include
root files, and no unrelated root lint finding was repaired in this unit.

## Digest-bound excerpt manifest (supervisor collects; bounded, LF-normalized, digest-bound)

The supervisor assembles bounded excerpts and binds each to its LF-normalized sha256 (CRLF smudge
changes raw digests; strip `\r` from CLI-piped digests on Windows). The producer does NOT compute
the digests in this unit — computing a file/excerpt sha256 requires a non-documented command that
the thin-client / native-tool discipline forbids here; the supervisor collects the digests at
handoff. Targets (exact bounded regions), covering every item the handoff requires:

| # | Coverage | File | Bounded anchor | The excerpt must show |
|---|----------|------|----------------|-----------------------|
| E1 | Digest fix (DB-020) | `services/api/app/spatial/wide_street_live_provider.py` | `_attested_wide_segments` def L325; fix L362 `source_raw_digest=page.raw_digest` | `source_raw_digest` carries the geometry-PAGE raw sha256, never the `Streetwidth` text |
| E2 | Digest regression | `services/api/tests/spatial/test_wide_street_live_provider.py` | `test_db020_attested_segment_carries_geometry_page_digest_not_width_text` | asserts sha256-shaped digest `== raw_body_digest(_dcm_page_body(features))`, `!= "80"`; `Streetwidth` preserved |
| E3 | Schema (canonical) | `packages/contracts/schemas/v1/rule_evaluation.schema.json` | enum L33; `wide_street` `$ref` L146; `$defs/wide_street` L450 | enum `["1.0.0","1.1.0"]`; optional `wide_street`; `additionalProperties:false` |
| E4 | Schema (bundled) | `services/api/app/_contract_schemas/v1/rule_evaluation.schema.json` | same anchors L33 / L146 / L450 | byte-identical to the canonical copy |
| E5 | Generated types | `packages/contracts/generated/rule_evaluation.ts` | `contract_version` union L131; `wide_street?` block L151 | regenerated byte-identically; `generate_ts_types.py --check` clean |
| E6 | Serializer | `services/api/app/rules/integration.py` (as_dict L160, emit L193–194); `services/api/app/rules/response.py` (L59) | additive branch + version const | block emitted ONLY when `wide_street_determination is not None`; `RULE_EVALUATION_CONTRACT_VERSION = "1.1.0"` |
| E7 | Endpoint tests | `services/api/tests/api/test_rule_evaluation_api.py` | AS-3 both-branch test, AS-2 extra-key test, updated M5-T035 within-wide test | serialized block present on within-wide, absent on provider None; extra-key rejected by BOTH validators |
| E8 | Integration (serializer) tests | `services/api/tests/rules/test_rules_integration.py` | the three new `as_dict()` block serialize/omit tests | `as_dict()` emits the block on a within-wide fold, omits it without a determination and for a non-conditional district |
| E9 | Strict web validator | `apps/web/src/lib/rule-evaluation-contract.ts` | `RULE_EVALUATION_CONTRACT_VERSIONS` L103; `checkWideStreet` L337; call L460 | admits 1.0.0 and 1.1.0; absent block valid; a malformed block fails TOTAL validation |
| E10 | Existing ReportView composition | `apps/web/src/components/architect/ReportView.tsx` (UNMODIFIED) | imports L12 / L16; `<DevelopmentLimits … evaluation={identityEvaluation}/>` L68; `<CalculationEvidence evaluation={evaluation} …/>` L92 | report embeds the SAME components fed the identity-matched validated document — no second data path |
| E11 | integration.py boundary justification | this report's "module-boundary justification" section + `python tools/modularity_check.py --check` output | +11-line additive branch; `failures 0`; `integration.py` a warning-level `review_signal` | the additive branch did NOT push `integration.py` across the failure threshold |

## Exact task-file delta — controller-owned, NOT a producer edit

`project-control/tasks/M5-T037.json` shows as modified in the working tree. I did NOT edit or revert
it this unit. `git diff HEAD -- project-control/tasks/M5-T037.json` (captured this unit) is precisely
two controller dispatch-envelope edits, attributable to the loop controller at dispatch (S4.5
checkpoint-envelope population + worktree binding), NOT to this producer:

```diff
@@ -6,6 +6,7 @@
   "business_reason": "Closes the second street-width gap named by the external review: the computed street answer must reach the screen and report, not sit inside an internal component",
   "inputs": [
+    "CHECKPOINT ENVELOPE (binding; two S4.5 stops proved this): in every checkpoint, OMIT the four envelope fields (branch, worktree, starting_sha, current_sha) entirely - the controller fills the authoritative values itself. If you do supply one, it must match EXACTLY full-length: 40-hex SHAs from `git rev-parse HEAD` (never --short), the FULL worktree path. Any abbreviated value stops the run.",
     "COMMAND CWD (binding; the rules suite does NOT collect from repo root - known invocation artifact): run the first four documented commands from the services/api directory ...",
@@ -117,7 +118,7 @@
     "python -m pytest packages/contracts/scripts/tests -q",
     "python tools/modularity_check.py --check"
   ],
-  "worktree": "wt-m5t037",
+  "worktree": "C:\\Users\\MLFLL\\Downloads\\nyc-zoning\\wt-m5t037",
   "progress_log": [
```

Independent controller attribution: (1) one ADDED `inputs[0]` entry — the "CHECKPOINT ENVELOPE
(binding; two S4.5 stops proved this)…" note; and (2) the `worktree` field rewritten from the
relative `"wt-m5t037"` to the absolute `"C:\\Users\\MLFLL\\Downloads\\nyc-zoning\\wt-m5t037"`. Both
are supervisor/controller dispatch-envelope population, not implementation identity. The controller
establishes its provenance INDEPENDENTLY and EXCLUDES this file from the worker material
submission / cherry-pick, mirroring the M5-T035 carryover pattern.

## integration.py module-boundary justification (packet-required)

`services/api/app/rules/integration.py` is in the material change set (the v1.1.0 serialization is
its `PropertyRuleEvaluation.as_dict()` branch), so its module boundary is justified here:

- The edit is a single ADDITIVE serialization branch inside the EXISTING `as_dict()` method
  (net +11 lines, L193–194): when `wide_street_determination is not None`, map the already-assembled
  DRAFT D-052 provenance summary onto one optional `wide_street` key; on every other path the key is
  absent and the body stays valid under both the 1.0.0 and 1.1.0 schema.
- No new responsibility crosses the boundary: `as_dict()` is already this module's serialization
  seam; the provenance summary itself is assembled upstream by `_wide_street_summary` (the branch
  consumes a ready dict, it does not compute domain logic, do storage/external I/O, or render
  presentation). No unrelated domain concern was co-located.
- `python tools/modularity_check.py --check` returns **failures 0**; `integration.py` remains the
  same warning-level `review_signal` ("above the warning threshold; consider the module boundary
  before growing it further") it was before this unit — the additive branch did NOT push it across
  the failure threshold. A +11-line additive branch on an established serialization method does not
  warrant a split; no compatibility facade is required because no public import moved.

## Handoff to orchestrator / supervisor (not producer actions)

- **Push + full CI (web pending):** please push the branch and run full CI, then collect the web +
  web-e2e conclusions on the pushed head; route any web fixups back. AS-5/AS-6/AS-7 stay explicitly
  pending until that evidence exists.
- **Supervisor digest-bound excerpts:** collect the bounded excerpts E1–E11 above, each bound to its
  LF-normalized sha256, reproducing the recorded cwd settings (four from `services/api`, three from
  the worktree root). Preserve any prior nonzero command execution as a SEPARATE cwd-stamped
  invocation outcome; the green re-runs recorded above do not overwrite it.
- **Task-file provenance (controller/supervisor only):** `project-control/tasks/M5-T037.json` is
  controller-owned; the exact delta is quoted above. I did NOT edit it. The controller establishes
  its provenance INDEPENDENTLY and EXCLUDES this file from the worker material submission /
  cherry-pick.
- **Material submission (orchestrator integrates; ADR-005):** the bounded evidence revision left in
  the worktree covers the backend implementation, both schemas, the runtime validator, the added
  backend tests, and the ReportView composition — EXCLUDING `project-control/tasks/M5-T037.json`
  (controller-owned, above). Material files:
  - Backend: `services/api/app/rules/integration.py`, `services/api/app/rules/response.py`,
    `services/api/app/spatial/wide_street_live_provider.py`.
  - Both schemas (byte-identical): `packages/contracts/schemas/v1/rule_evaluation.schema.json`,
    `services/api/app/_contract_schemas/v1/rule_evaluation.schema.json`.
  - Generated TS (regenerated, never hand-edited): `packages/contracts/generated/rule_evaluation.ts`.
  - Runtime validator: `apps/web/src/lib/rule-evaluation-contract.ts`.
  - Display: `apps/web/src/components/architect/DevelopmentLimits.tsx`,
    `apps/web/src/components/architect/CalculationEvidence.tsx`.
  - ReportView composition: `ReportView.tsx` is UNMODIFIED — parity is structural (it already embeds
    the SAME `DevelopmentLimits`/`CalculationEvidence` fed the identity-matched validated document,
    so screen and report cannot fork a second data path). Composition is proven by the
    `report-view.test.tsx` parity assertion (AS-6 stays PENDING web CI).
  - Added/updated tests: `services/api/tests/api/test_rule_evaluation_api.py`,
    `services/api/tests/rules/test_rules_integration.py`,
    `services/api/tests/spatial/test_wide_street_live_provider.py`,
    `apps/web/src/lib/__tests__/contract-versions.test.ts`,
    `apps/web/src/components/architect/__tests__/development-limits.test.tsx`,
    `apps/web/src/components/architect/__tests__/report-view.test.tsx`.
  - This report: `project-control/reports/M5-T037-producer-report.md`.

## Discovery routing (D-069)

None new. The DB-020 fix closes the M5-T035 G1 F1 finding at its source; the M5-T034 contract-
limitation discovery is resolved by this 1.1.0 bump.
