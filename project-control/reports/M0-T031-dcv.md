<!-- Orchestrator note: directive-compliance-verifier return saved VERBATIM from the agent-return
     channel. Reviewed head 1891ef3f886c7ba65413ddd9727b5a596c8e705a; all 21 applicable
     amendment-2 rows (D-005-R090..R110) PASS. Basis for the M0-T031 verification.json rows. -->

R090-R110: PASS 21, FAIL 0, NA 0

Independent directive-compliance verification, D-005 amendment 2 (rows R090-R110) at frozen head 1891ef3f886c7ba65413ddd9727b5a596c8e705a (branch `task/M0-T031-codegraph-hardening`, single commit off main base cc273b5, worktree clean). All evidence below personally reproduced at that head; producer report treated as claims only.

- **D-005-R090: PASS** — Owner GO WITH CONDITIONS captured verbatim in `project-control/directives/D-005-codebase-knowledge-graph-pilot/source-003-amendment.md` (lines 13-15); manifest audit_log 2026-07-30T02:12:21 records "Amendment 2 recorded append-only... rows D-005-R090.."; `python tools/validate_directive_compliance.py --check` exit 0 at head (digests match).
- **D-005-R091: PASS** — README addition states the honest interpretation: "The proven benefit is better correctness, better completeness, and fewer false dependency/impact claims... not demonstrated by the M0-T030 benchmark" (`tools/code_graph/README.md`, selective-routing section); no reduced-work claim anywhere in the diff.
- **D-005-R092: PASS** — README: "approved the graph as advisory navigation infrastructure with *selective* use — not universal graph-first"; SKILL.md: "Graph use is never required on every task; graph output is advisory only".
- **D-005-R093: PASS** — `project-control/tasks/M0-T031.json` contracted under the normal control plane: `directive_refs` = [{"directive_id":"D-005","requirement_ids":"ALL"}], `directive_regime_version` 1.0, gates G0/G3/G4/G5, isolated worktree, forbidden_paths encoding R105/R107/R109/R092/R096.
- **D-005-R094: PASS** (all four checklist items reproduced) — c1: `generate_into` hashes the exact written graph bytes into `meta["graph_sha256"]` (generate.py diff) and `_read_cache_attempt` verifies it on every load (query.py:54-95 at head); I flipped a mid-file byte myself in a scratchpad cache: query printed `regenerated (cache integrity)` exit 0 and restored byte-identical original; with `--no-regen` it printed `STALE (cache integrity): refusing to serve the cached graph` exit 3. c2: cache key = `sha256(realpath(repo_root))[:12]+"-"+basename`; observed `ef435abbcd93-M0-T031-codegraph-hardening` and independently recomputed the hash prefix (match=True). c3: truncated `graph.meta.json` myself → exit 3 / regeneration with zero tracebacks (traceback=False on all four runs). c4: imports in generate.py/query.py are argparse/ast/hashlib/json/os/re/shutil/sys/tempfile + sibling module only (grep); no network, no hooks, no product paths in diff.
- **D-005-R095: PASS** — `git diff --name-only cc273b5..HEAD` = 7 files (2 existing tool files, tests, tool README, CLAUDE.md, SKILL.md, producer report); no new modules, services, or platform surfaces; the `--limit`-position fix traces to M0-T030 G3/G4 review INFO named in the packet inputs.
- **D-005-R096: PASS** — grep of the full diff for universal graph-first wording (`always use/query the graph`, `required on every`, `must use the graph`, ...) returned zero hits; only explicit negations exist.
- **D-005-R097: PASS** — SKILL.md paragraph and README section direct ordinary navigation for simple/local cases and graph use "when relationship/dependency discovery is materially useful".
- **D-005-R098: PASS** — README names all 9 graph-preferred cases (c1-c9) verbatim to amendment lines 46-54; SKILL.md carries a condensed 7-of-9 list and points to the README for the canonical list.
- **D-005-R099: PASS** — all 5 not-mandatory cases (c1-c5) present verbatim in README and, condensed, in the SKILL.md paragraph.
- **D-005-R100: PASS** — README encodes the exact decision model block (TASK → materially useful? → YES: graph → narrow → source verification / NO: direct navigation), matching amendment lines 64-71.
- **D-005-R101: PASS** — diff-wide grep for token/time-savings claims: only hits are the required negations (README "**No token or time savings are claimed.**"; producer report negation at its line 90-92).
- **D-005-R102: PASS** — guidance preserves the accuracy pattern (source verification for material conclusions) while exempting easy tasks ("small localized edits where ordinary search is clearly cheaper"; "use ordinary direct navigation otherwise").
- **D-005-R103: PASS** — advisory/source-verification language on all three surfaces: README ("remains advisory... never graph → assumption"), SKILL.md ("advisory only, and material conclusions must be verified in the actual source"), CLAUDE.md row labeled "selective, advisory" routing to the README.
- **D-005-R104: PASS** — the complete generate.py diff has exactly three hunks (version 1.0.1→1.1.0, `default_out_dir`, `generate_into`); `compute_source_fingerprint`/`CONFIG_INPUTS` byte-untouched; `generate.py --repo . --check` = "determinism check PASS ... 235 input files, fingerprint 21aa77fb90376adc" (expected value); cold-cache query printed `regenerated (stale fingerprint)`; `tools/test_code_graph.py` = 36/36 OK.
- **D-005-R105: PASS** — no watcher/hook/scheduler code or files anywhere in the diff (no `.claude/hooks`, no `.git/hooks`, no new processes); grep hits only the negation line in the producer report.
- **D-005-R106: PASS** — cache remains per-checkout (realpath-keyed; `test_same_basename_checkouts_get_distinct_cache_dirs` in suite); grep of query.py+generate.py for branch/merge logic: zero hits.
- **D-005-R107: PASS** — `git diff --name-only cc273b5..HEAD -- .github/` = 0 files; no protected-main/PR/CI/secret-scan/dependency-policy/directive-compliance/security-review file in the 7-file diff.
- **D-005-R108: PASS** — integration surfaces are exactly the two contemplated: CLAUDE.md numstat = 1 added line (routing-table row), SKILL.md numstat = 3 added lines (one paragraph + blanks), both purely additive; README is the tool's own documentation and the producer report is lifecycle evidence, not integration surfaces; `tools/context_budget_check.py` = PASS at head.
- **D-005-R109: PASS** — case-insensitive grep of the full diff for Mission Control / layer-B / six-PRD / evidence knowledge graph / Graphify / Agent Teams injection: zero occurrences added.
- **D-005-R110: PASS** — `project-control/tasks/M0-T031.json` progress_log (2026-07-30T02:22:05) records "Owner dispatch GO recorded 2026-07-30: dispatch M0-T031 exactly under the contracted packet, no scope expansion (D-005 amendment 2 execution trigger)" — the packet was returned and the owner's GO is the recorded dispatch trigger.

Harness reproduction at the frozen head: `tools/test_code_graph.py` 36/36 OK; `tools/test_directive_compliance.py` 55 tests OK; `tools/test_project_control.py` all 14 groups OK (incl. S9 accept-requires-verification proofs); `tools/test_directive_reminder.py` 12 tests OK; `tools/validate_directive_compliance.py --check` exit 0; `tools/context_budget_check.py` PASS. Prohibited-action evidence: head 1891ef3 is NOT an ancestor of main (nothing merged/accepted), task status is `in_progress` at 5%, nothing dispatched/installed/deployed beyond the contracted producer run, and no reserved surface was touched.

**Verdict: PASS.** Acceptance at head 1891ef3f886c7ba65413ddd9727b5a596c8e705a is D-005-compliant for all 21 applicable amendment-2 rows: every hardening, routing, prohibition, and lifecycle requirement reproduces from primary evidence at this content identity, and no prohibited action has occurred. Compliance is conditional only on the normal remaining lifecycle: the orchestrator must record the other contracted gates (G3/G4/G5) and write `verification.json` marking R090-R110 verified at this exact content identity before `accept()` (the CLI refuses otherwise), and merge must go through the normal protected-main PR process. Owner-gated afterwards and untouched by this verdict: any future token/time-savings claim requires new measured evidence (R101); each reserved surface — Mission Control Engineering Map, layer-B project/control graph, six-PRD expansion, NYC Evidence Knowledge Graph, Graphify adoption, and any automatic Agent Teams graph injection beyond contract — remains a separate owner decision (R109).

Key evidence paths: `C:\Users\MLFLL\Downloads\nyc-zoning\nyc-development-feasibility-claude-pack\.claude\worktrees\M0-T031-codegraph-hardening\tools\code_graph\{generate.py,query.py,README.md}`, `...\tools\test_code_graph.py`, `...\CLAUDE.md`, `...\.claude\skills\start-controlled-task\SKILL.md`; `C:\Users\MLFLL\Downloads\nyc-zoning\nyc-development-feasibility-claude-pack\project-control\directives\D-005-codebase-knowledge-graph-pilot\{source-003-amendment.md,requirements.json,manifest.json}`; `C:\Users\MLFLL\Downloads\nyc-zoning\nyc-development-feasibility-claude-pack\project-control\tasks\M0-T031.json`.
