# M5-T077 — G4 independent test-adequacy review (qa-engineer "qa-t077", read-only)

> Transmission history: pinned at 71ab388a (HEAD moved to 70c29c5a mid-review through disjoint
> ledger-only peer commits; the reviewer proved the 4 reviewed files byte-identical at both heads),
> delivered as two SendMessage parts (1/2, 2/2) ending with END-OF-REPORT; no truncation. Joined
> verbatim in delivery order by the orchestrator (transport wrapper tags removed only).

---

M5-T077 G4 (test-adequacy + mutation) — VERDICT PASS, 1 advisory; all 4 producer-named mutants killed. PART 1/2.

PINNED HEAD at start: 71ab388a. HEAD moved to 70c29c5a mid-review (disjoint peer commits: only project-control/ ledger for M5-T080/T082/T083/T086). The 4 reviewed files are BYTE-IDENTICAL 71ab388a↔70c29c5a; material f63cfad1 is an ancestor of both. Verdict holds at both heads. `git status --short services/` = clean (in-memory attribute-rebind mutation only; no repo/ledger/memory write).

Suite: `cd services/api && pytest tests/scenario/test_lot_geometry_derivation.py tests/api/test_max_envelope_api.py -q` → 64 passed (5.62s).

MUTATION TABLE (in-process module-attr rebind + pytest.main; RED = killed):
- M1 idiomatic `if lot.get("lot_line_segments"): return False` → test_malformed_segments_refuse_identically_with_and_without_a_bbl → RED: exactly the 5 FALSY params [0],[""],[{}],[False],[0.0] failed, 4 truthy passed. The falsy extension is the load-bearing discriminator.
- M2 re-admit None to the derivable set → test_null_segments_refuse_in_every_state_with_no_provider_call → RED at state "BBL + passing provider" ((200,None)≠(422,validation_error)).
- M3 blank BBL_UNRESOLVABLE detail (wrap _fail) → test_unresolvable_bbl_is_fail_closed_without_calling_provider → RED at `assert derived.detail` ('').
- M4 cutter `>`→`>=` (rebind _exterior_ring_segments) → test_ring_cutter_at_cap_boundary_derives_exactly_max_segments → RED at `assert reason is None` (got 'over_cap') — the EXACT-MAX assertion.
- MA (own) drop `and not isinstance(bbl, bool)` → full 64 → GREEN / SURVIVOR.
- MD (own) bbl branch `return lot.get("bbl") is not None` (dict/list derivable) → test_non_scalar_bbl_does_not_derive_and_never_calls_the_provider → RED (both params; resolutions==[] fails).
- ME (own) drop the strip check → accept empty/whitespace str bbl → full 64 → GREEN / SURVIVOR.
- MC (own, direct TestClient probe) M2 + failing provider, the 3rd null state → status stays 422 but provider RESOLVED+CALLED (resolutions=['resolved'], calls=['1008350041']) → only the spy catches M2 there.

All 4 producer-named mutants KILLED. Part 2 (AS verdicts + findings + verdict) follows.

---

M5-T077 G4 — PART 2/2. AS verdicts, findings, VERDICT PASS.

AS verdicts:
- AS-1 one-null contract: PASS. absent + [] derive (test_absent_or_empty_segments_with_a_bbl_still_derive); null → 422 in all 3 BBL/provider states with no provider call; M2 reddens. Both docstrings name only "absent or empty list" (verified by inspection; not test-enforced — acceptable).
- AS-2 falsy net: PASS. 9-param truthy+falsy set; M1 reddens the 5 falsy; provider never resolved/called (asserted).
- AS-3 taxonomy: PASS. BBL_UNRESOLVABLE detail pinned at unit level; route asserts each failure reason reaches placement.detail (connector_fault + bbl_unresolvable); M3 reddens.
- AS-4 boundaries: PASS. exact-max derives / max+1 = geometry_over_cap, M4 reddens the exact-max assertion; non-scalar bbl (dict/list) → 200 honest gap, no derived block, no provider call, MD reddens the guard.
- AS-5 scope/modularity: PASS (corroborated). f63cfad1 diff = exactly the 5 allowed_paths; max_envelope.py NOT in the diff (byte-untouched); route-unmounted pinned by test_route_is_unmounted_in_the_real_app. (ruff/modularity are G2/G3 evidence, not re-run in G4.)

Duty-6 CONFIRMED: the null test covers no-BBL / BBL+passing / BBL+failing, each pinning NO provider call. M2 is caught at state 2 via status; MC empirically proves state 3 (failing provider) is caught SOLELY by the resolutions==[]/calls==[] spy — status stays 422 there, so the "no provider call" pin is load-bearing.
MB CONFIRMED: the at-cap exact-max assertion is the sole discriminator for `>`→`>=`; a max+1-only test would let it survive. Producer pinned the boundary correctly.
Consuming-namespace: no false survivors — M4's target re-imports _exterior_ring_segments at call time (reads the rebind); all other mutants hit module-global call sites (route→_should_derive_lot_geometry; derive_lot_line_segments→_fail/_exterior_ring_segments).

F1 (ADVISORY, non-blocking): MA + ME survivors — the deliberate scalar-bbl edge guards (bool-exclusion `and not isinstance(bbl, bool)`; empty/whitespace-string rejection via `bbl.strip() != ""`) are unpinned; a maintainer dropping either ships green. NON-blocking because (a) no realistic client sends a bool or empty-string bbl (web client sends a valid string or omits the field), and (b) downstream is fail-closed — a regressed guard still hits normalize_bbl → BBL_UNRESOLVABLE honest gap, never fabricated geometry; the only observable change is a spurious derived_lot_geometry(bbl_unresolvable) block + a provider resolution on nonsense input. Optional 1-line fix: extend the non-scalar/no-derive test with bbl ∈ {True, False, "", "  "}.

No BLOCKING gap. All producer-named mutants killed; boundary and 3-state pins are load-bearing.

M5-T077 G4 VERDICT: PASS

END-OF-REPORT
