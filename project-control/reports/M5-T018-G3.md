# M5-T018 — G3 Human-Journey Gate Report (verbatim)

Orchestrator preservation note: report returned by the independent human-journey-reviewer
agent ("G3 journey review M5-T018") on 2026-09-12 and preserved verbatim below (transport
entity-decoding only). Reviewer was read-only; frozen review identity: producer commits
1b2495da + ee05ad17. Verdict: PASS (advisories A1–A4, none blocking; A5 positive note).

---

Here is my G3 gate report.

---

# G3 Human-Journey Gate Report — M5-T018 (D-041 C1 web half: unused-draft-zoning-floor-area line on Compare)

**Reviewer:** human-journey-reviewer (read-only, ADR-005)
**Task:** M5-T018 — C1 unused draft zoning floor area: web contract mirror + Compare rendering
**Directive in regime:** D-041, applicable requirement **D-041-R001 only** (R002 sequencing and R003 scope-limit bind `D-041-BOOTSTRAP`, not M5-T018 — verified from `requirements.json` applicability lists)
**Frozen SHAs reviewed:** producer material `1b2495da`, report `ee05ad17` (base `6919dab2`)
**Method:** source + tests + fixtures walkthrough (thin client cannot run the web app; vitest/Playwright are CI-only per the packet's `documented_test_commands`)

## Freeze verification
`git log` on both target paths confirms the **latest commit touching `apps/web/src/lib/scenario-contract.ts` and `apps/web/src/components/compare/**` is `1b2495da`**. The three later commits on the branch (`ee05ad17`, `44048007`, `574298e8`, `f4f01352`) do **not** touch either path; `git diff HEAD` on both paths is empty. The frozen content equals the working tree — walkthrough is on the reviewed identity.

## Component under review
`apps/web/src/components/compare/UnusedFloorAreaSection.tsx` (new, 205 lines, single responsibility), mounted once in `ScenarioResult.tsx` **directly beneath the cap card, on every branch** (line 267, after the `ScenarioCard`/`NoScenarioBlock` branch). Contract mirror additions in `scenario-contract.ts` (`UNUSED_FLOOR_AREA_KEYS`, `checkUnusedFloorArea`, enums). User-visible `label`/`scope_note`/`over_built_statement` are the server document's own strings (M5-T017 content), bounded via `boundedText` at render.

---

## Journey narrative by state

### State 1 — computed (positive remainder)
A professional who resolved a lot lands on Step 3, reads the cap card ("Draft maximum residential zoning floor-area cap: 15,000 square feet (DRAFT…)"), and immediately below sees its **own labeled card**: heading "Unused draft zoning floor area", the document's precise-noun label verbatim ("Unused draft zoning floor area (FAR-derived): the DRAFT residential zoning-floor-area cap (ZR 23-21) minus the existing built floor area. A zoning floor-area difference only…"), then the value line "Unused draft zoning floor area (cap minus existing built floor area): **5,000** square feet (DRAFT — needs professional review, not Verified)", then the scope note as a plain, always-rendered `<p>` (not hover-hidden): geometry/height/yards/setbacks/layout/coverage "has NOT been assessed… does not establish achievable floor area or a buildable envelope, and the tax lot is treated as the zoning lot."
- **WHAT it is** (precise noun, cap-minus-existing): clear. **What it is NOT** (geometry not assessed): clear and visible. **Zoning-lot assumption**: stated. **DRAFT**: unmistakable. Sits as its own card; no contamination of the cap line. **PASS.**
- Honest exact-zero: `formatValue(0)` → "0" with "square feet" unit shown (`value !== null` guard) — a real zero renders honestly, not blanked.

### State 2 — over_built (negative remainder)
Value renders **"-5,000" square feet** — `formatValue` emits the minus sign; the S3 test asserts the DOM string is `-5,000` and explicitly `not "5,000"` and `not "0"` (never clamped, never restyled positive, never hidden). The document's `over_built_statement` renders verbatim as its own sentence ("The existing built floor area exceeds the draft residential zoning floor-area cap; the remainder is negative."), followed by a needs-professional-review notice carried as **text in a `role="status"` region** (announced by assistive tech; escalation is not colour-only). `professional_review_required` is also set at the document root, so `OpportunityRiskBlock` independently shows the review flag. WHY it needs review (exceeds the draft cap) is explicit. **PASS.**

### State 3 — not_computable (all three reasons)
Renders the standard **"No supported estimate"** treatment: no number, no invented zero, no unit, and a plain-language reason derived from the typed enum:
- `missing_existing_building_area`: "No existing building floor-area record was available for this lot, so no difference can be stated. **An absent value is not zero.**" (verbatim to research §3.2 ethos)
- `existing_building_area_unusable`: "…cannot be used for this calculation… **An unusable value is not zero.**"
- `no_draft_far_cap`: "No draft residential zoning floor-area cap was surfaced for this lot, so no difference can be stated."
All three are honest, jargon-free, and cannot be misread as a guarantee. No alarm-page effect — the rest of the Compare screen renders normally (S4 asserts `scenario-result` still present). **PASS.** The shared fixtures exercise `missing_existing_building_area` (preliminary) and `no_draft_far_cap` (all three no-scenario/unsupported); `existing_building_area_unusable` is exercised via a local variant.

### State 4 — recovery / coherence
`UnusedFloorAreaSection` mounts once for **every** branch (preliminary shows cap card + C1; no_scenario/unsupported show `NoScenarioBlock` + C1). The C1 card and the cap card are separate `.card` sections with distinct labels and separate `data-testid`s — no confusion or shared state between "Draft maximum…cap" and "Unused draft zoning floor area". No user-facing overclaim: "buildable" and "envelope" appear **only as negations** in the scope note; "capacity"/"development rights" never appear in C1 copy; "verified" appears only as "not Verified". **PASS.**

---

## Design-standard alignment (astra-presentation-research.md §3)
- **§3.1** precise noun + material assumption directly beneath the number, DRAFT discipline: **met** (label + scope note always visible, not hover-gated).
- **§3.2** status language, no "verified"/"compliant"/approval seal: **met** (DRAFT/needs-review text; "not Verified").
- **§3.3** honest result states — "No supported estimate" for unresolved inputs; **an unassessed downstream issue must NOT suppress a bounded FAR calculation**: **met, and precisely.** Geometry-not-assessed is shown as a scope note *without* suppressing the computed difference; a *missing required input* (existing area / cap) suppresses the number → "No supported estimate". The component implements exactly the §3.3 distinction — the strongest single point of this build.

## Requirement verdict — D-041-R001 (re-derived, not taken from producer map)
1. "own clearly labeled line, existing built floor area vs draft FAR cap" — **PASS** (own card, "cap minus existing built floor area").
2. "standard draft-label + per-fact provenance discipline" — **PASS with advisory** (draft-label present; per-number source affordance not on the C1 line — see A1).
3. "over-built renders HONESTLY as its own explicit statement + routes to professional-review, never clamped/hidden" — **PASS**.
4. "missing inputs = standard missing/unsupported treatment, never estimated" — **PASS** (all three reasons).

---

## Findings

**No blocking findings.**

**A1 (advisory) — no per-number evidence affordance on the C1 value.** §3.2 ideal is that every quantity opens evidence. The C1 line shows a value with no source/evidence control, and the section renders none of the required-and-validated input provenance (`inputs.draft_zoning_floor_area_cap.provenance` with ZR 23-21 citations, `inputs.existing_building_floor_area.provenance`). Mitigations that make this *consistent, not a C1 regression*: the whole Compare screen defers per-number evidence to Step 4 (`NextActionBlock`: "Evidence and provenance for every value shown here (Step 4) arrive with a later milestone"), the adjacent cap card already surfaces the cap's rule provenance, and provenance is required+validated at the contract level. Recommend the eventual Step-4 evidence-drawer work include the C1 inputs.

**A2 (advisory/cosmetic) — scope note text on not_computable states.** The verbatim scope note ("…the tax lot is treated as the zoning lot (see the zoning_lot_extent assumption)") is shown even when no difference was computed and `assumptions: []`. Not misleading (it scopes the intended calculation) and the string is M5-T017 server content outside this task's editable scope. No action required in M5-T018.

**A3 (scope boundary, not a defect) — computed/over_built proven only against synthetic documents.** No committed shared fixture carries `computed` or `over_built`; those states are built locally in `scenario-fixtures.ts` (derived from the fixture's own 15,000 cap, no invented official value — the correct choice given the forbidden shared fixtures). Consequence: the positive-remainder and over-built render is verified only against locally-constructed documents. The true end-to-end computed/over_built path depends on M5-T017's server actually emitting those sections; flag this for the C1 end-to-end / M5-T017 gate.

**A4 (cosmetic) — "surfaced"** in the `no_draft_far_cap` string is mild product-jargon; a professional will understand it. Server-owned wording.

**A5 (positive note)** — `boundedText` fallbacks "(label not stated)"/"(scope note not stated)" are unreachable because the validator requires non-empty `label`/`scope_note` before render; harmless defensive code.

## Accessibility
- Over-built review notice in `role="status"`, carried as text — announced, not colour-only. **PASS.**
- Negative value conveyed by the "-" character plus explicit `over_built_statement` prose — meaning is textual, not colour. **PASS.**
- DRAFT label is text. Heading hierarchy consistent (h1 Step 3 → h2 per card, C1 is an h2 sibling). Scope note is a persistent `<p>`, not hover-gated (satisfies §3.1 "do not require hovering to find evidence"). **PASS.**

## Wording verdict
No user-facing overclaim. Forbidden nouns absent or negated; "unused" is explicitly scoped as a FAR-derived floor-area *difference*, not unused rights/capacity. Plain-language reason strings are honest and would not be read as a guarantee. **PASS.**

## Execution-evidence note (not a block)
Per project policy, I do not return BLOCKED for inability to run commands on the thin client. The S1–S4 unit assertions were read and independently confirm all four states; the mirror validator's negative tests (missing section, unknown inner key, bad state/reason enum, NaN value, and the preserved top-level-unknown rejection) re-derive R001's structural guarantees. **S5 (CI web + web-e2e SUCCESS at combined head `44048007`) is orchestrator-captured evidence** (commit `574298e8`); I request the orchestrator confirm the stored CI run before recording acceptance.

---

## VERDICT: **PASS** (with advisories A1–A4; none blocking)

The C1 unused-draft-zoning-floor-area line satisfies D-041-R001 and research §3.1/§3.2/§3.3 across all four states. A real-estate professional gets an honest, precisely-scoped answer to "how much unused draft floor area does this lot have?" — a bounded number when supported, an explicit honest negative routed to professional review when over-built, and a "No supported estimate" with a plain-language reason when a required input is missing — without any "buildable/capacity/rights/verified" overclaim and without an unassessed-geometry caveat suppressing a valid calculation.

**Relevant files:**
- `C:\Users\MLFLL\Downloads\nyc-zoning\ctl24\apps\web\src\components\compare\UnusedFloorAreaSection.tsx`
- `C:\Users\MLFLL\Downloads\nyc-zoning\ctl24\apps\web\src\components\compare\ScenarioResult.tsx`
- `C:\Users\MLFLL\Downloads\nyc-zoning\ctl24\apps\web\src\components\compare\__tests__\scenario-fixtures.ts`
- `C:\Users\MLFLL\Downloads\nyc-zoning\ctl24\apps\web\src\components\compare\__tests__\unused-floor-area.test.tsx`
- `C:\Users\MLFLL\Downloads\nyc-zoning\ctl24\apps\web\src\lib\scenario-contract.ts`
- `C:\Users\MLFLL\Downloads\nyc-zoning\ctl24\project-control\tasks\M5-T018.json`
