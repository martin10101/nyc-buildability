# M5-T051 G3 — independent code review (code-reviewer, saved VERBATIM from the two agent returns)

# G3 CODE GATE — M5-T051 (D-076 phase B1)

**Verdict: PASS** (no blocking findings; 3 advisory)
**Reviewed at HEAD** `ce11d09fea402cbfd5b0b0a64b4cc717790e6e09` (confirmed = pin). Material cherry-pick `f1ec64a8` (task-branch `8d879a95`). Read-only; no control CLI run.

## Verification against the gate checklist

**1. Math correctness — CORRECT.**
- Shoelace (`derivation.py:302`) iterates consecutive pairs over the closed ring, `abs(total)/2`; sign-independent, so winding- and rotation-invariant. Closing-vertex convention matches B0 (`vertices[0]==vertices[-1]`); `_closed_ring` (`:378`) requires ≥4 positions and keeps the closing duplicate, so all polygon edges (incl. the closing edge) are summed. Verified exact for the fixtures: rectangle absolute-coord shoelace = 8000, L-shape 2A=12800→6400 (concavity handled).
- Coverage (`:435`) guards non-finite/non-positive **and bool** (`:438`) before dividing; provenance carried through.
- Per-level own-vs-base outline recorded explicitly on each record (`outline_source`, `:471–501`).
- Wall setback is genuine **segment-segment** distance (`_segment_segment_distance` `:360`: intersection→0 else min of four point-segment distances), not point-point. Verified against `wall_setback.json` (front lot-line 18, rear 48, street 25) and rectangle uniform 15-ft frame.
- Heights = `floor_to_floor_ft × floor_count`, cumulative summed (`:532`).

**2. Facts-not-allowances (D-076-R002) — CLEAN.** Grep of the module: `permitted/required/allowed/compliant/permit` appear ONLY in disclaiming docstrings (`:22–24`, `:77`) and `not an allowance / not a maximum` disclaimers in `detail` strings — never as a value, field, method, unit, enum, or input_id. Gross treatment is a declared enum `PER_LEVEL_GROSS_NO_DEDUCTIONS` with explicit inclusions/exclusions that state "NOT a zoning floor area" (`:85–109`). No rule import, no requirement lookup anywhere.

**3. Purity — CORRECT.** Imports are stdlib only (`hashlib, json, math, collections.abc, dataclasses, enum`) plus the same-package B0 validator (`:54`). No spatial/connector/api import; forbidden paths untouched (diff touches only `derivation.py` + `proposal.py` under `app/scenario/`). Every input is a parameter; `LotContext`/`AttestedStreetLine` are caller-supplied. Defense-in-depth re-validation on entry documented and enforced (`derive_proposal` calls `validate_proposed_massing`, `:704`).

**4. DB-034(c) — CORRECT, byte-safe.** `ring_length = vertex_count-1` (distinct-point count; ≥3 because outline is validated first at `validate_proposed_massing:467` before walls at `:471`). The modular check `start % ring_length == end % ring_length` (`proposal.py:416`) has exactly one collision pair `{0, closing_index}` given the validator's index bound `0 ≤ iv < vertex_count` (`:396`) and duplicate-vertex rejection (`:268`): **no false positives** (a legitimate closing-edge wall `4→0` or `4→1` passes — confirmed by `test_wall_between_first_and_closing_neighbour_ok`) and **no false negatives**. No div-by-zero (ring_length ≥ 3). Diff on `proposal.py` is the (c) block **only** (+20 lines); B0 semantics otherwise byte-preserved.

**5. Honest absence — CORRECT.** No attested street line → `EvidenceRecord(value=None, ...)` + `StreetSetbackResolution.ABSENT_NO_ATTESTATION` + method `street_line_setback_absent` (`:623–637`); lot-line absence handled the same way (`:584–594`). `value is None` (not `0.0`) plus the resolution enum and method name make absence unmistakable for a caller. D-051 discipline satisfied.

**6. EvidenceRecord — CORRECT.** Frozen dataclass (`:138`) carrying value/unit/method/input_ids/source_class/detail/provenance; `input_ids` genuinely tie to inputs (outline digest via `_points_digest`, `wall:<id>`, `lot_line:<id>`, `lot_area_provenance:<digest>`, `street_attestation:<digest>`) — never a rule id. `source_class` literal `"proposed_derivation"`.

**7. Scope — CLEAN.** Diff = 9 files, all within `allowed_paths` (+ fixtures glob); `--stat` shows only `app/scenario/derivation.py` and `app/scenario/proposal.py` under app — no `contract.py`/`scenario.py`/`api/`/`spatial/`. `git log -- proposal.py` shows its last change is this commit `f1ec64a8`; created by M5-T048 (`0c7b17f1`) — **M5-T053 did not touch proposal.py**, so no overlap conflict at this head. `proposal.py` diff is the DB-034(c) block only (+20 lines).

**8. Modularity — OK.** 734 physical lines; SLOC well below the 600 warn tier (large module + class docstrings dominate). Single, cohesive responsibility (derive proposal facts from a validated block + LotContext). Producer's modularity exit 0 is consistent with the CI-green head.

**9. Producer-report accuracy — ACCURATE.** Spot-checked anchors resolve exactly: `derive_proposal:685`, `EvidenceRecord:138`, `SOURCE_CLASS:79`, `_derive_footprint:421`, `_derive_gross:506`, `_derive_wall_setbacks:661`, `_street_line_setback:617`, `GrossFloorAreaTreatment:85`, `proposal.py:408–427`. Fixture arithmetic in `README.md` matches the committed `expected` blocks and is independently hand-verifiable — not self-proven (`_derive_fixture` loads `expected` from JSON, never from module output).

## Advisory findings

- **ADVISORY-1** (`derivation.py:314–343` vs `proposal.py:105–140`): geometry primitives `_orientation` / `_on_segment` / `_segments_intersect` are duplicated across the two modules. Intentional to keep B1 derivation independent of the B0 validator (sharing would couple them); modularity policy tolerates small dependency-free primitives. Future-refactor DRY candidate only. Not blocking.
- **ADVISORY-2** (`derivation.py:573–577`): the `_wall_segment` `start == end` degenerate check is unreachable via the public `derive_proposal` entry because `validate_proposed_massing` (DB-034(c)) refuses that wall first. Genuine defense-in-depth for a validation-bypassing caller, but dead under normal flow and not marked `# pragma: no cover` (cf. `:705`); will read as uncovered if line-coverage is ever gated. Not blocking.
- **ADVISORY-3** (`test_scenario_derivation.py`): tests assert exact float equality. Correct here because every fixture is axis-aligned with integer coordinates and products < 2^53 (documented in `README.md`; I verified the `hypot`/projection paths collapse to exact values). The pattern won't extend to rotated/real geometry — future fixtures must keep the axis-aligned construction or switch to tolerances. Test-design note, not a module defect.
