<!-- Saved VERBATIM by the orchestrator from the qa-engineer agent-return
     channel (report-preservation rule). Transport entity-decoding only
     ("&amp;" -> "&", "&lt;" -> "<", "&gt;" -> ">"). -->

I have completed a thorough independent verification. Here is my gate report.

---

# Gate Report

- **Gate ID:** M0-T093-G4 (independent QA / test-evidence gate)
- **Task ID:** M0-T093 (D-024 unit H1 — guardrail-refusal classification + bounded 4.8 bridge, Phase E)
- **Reviewer:** qa-engineer (read-only, independent; producer was fable-orchestrator-session)
- **Producer:** fable-orchestrator-session (seq-20 successor implementation)
- **Result:** PASS
- **Clean environment/worktree used:** Reviewed at frozen content identity `b58772027f2e9fb0d631f204dc249f29f1489404`. My dispatched worktree sits on a divergent branch (`d8b3899`, NOT an ancestor of the frozen SHA), so I did NOT review my worktree's working tree. Instead I extracted the frozen `tools/` and `project-control/` trees via `git archive b587720` into an isolated scratchpad and reproduced all tests there on Python 3.11.9. Mutation testing was performed on scratchpad copies only; the repository was never modified (read-only discipline preserved).

## Frozen-identity verification

1. `git diff --stat b587720..654a083 -- tools/` is **EMPTY** — the one post-frozen commit (`654a083`, the in-regime submit + G2 self-check) touches only control-plane files (`project-control/gates`, `reports`, `tasks`, `state.json`). The frozen review target is stable; later commits are control-plane only, as stated.
2. Unit diff `84f658e..b587720` is scoped exactly as claimed: 2 new modules (`guardrail_refusal.py` +479, `refusal_bridge.py` +970), additive `state_machine.py` (+60: +2 states/+11 transitions), a thin loop seam, a facade-preserving `loop.py`→`pending_prompt.py` split, `model_turnover.py` (+23 additive aliases), 1 fixture, and 3 test files (guardrail_bridge new; controller_succession + phase1 count-assertion edits). No stray production files.

## Acceptance criteria reviewed

The section-16.4 (D-024-R110) matrix S1–S16 in `project-control/reports/M0-T093-guardrail-bridge.md §1`, plus the two additive Phase-E states and the real-`SupervisedLoop` seam behavior. Every R110-named case maps to a concrete, non-vacuous test (table below).

## R110 case → covering test (independently mapped in `tools/test_agent_supervisor_guardrail_bridge.py`)

| R110 case | Covering test | Verdict |
|---|---|---|
| exact recognized refusal | `S1RecognizedRefusalTests.test_recognized_shape_classifies_as_guardrail_refusal` | PASS |
| quota cannot enter bridge — BOTH directions (R075) | `S2.test_quota_evidence_never_classifies_as_refusal` (quota→refusal) + `S2.test_refusal_evidence_never_classifies_as_exhaustion` (refusal→quota, via real `classify_exhaustion`) | PASS |
| unrecognized-similar no actuation | `S3.test_refusal_looking_text_is_held_not_actuated` (+ empty/malformed corpus fail-closed) | PASS |
| security-test failure not a refusal | `S4.test_failing_tests_are_defects_not_routing_events` | PASS |
| unknown approval never auto-approved | `S5.test_unknown_approval_prompt_is_never_a_refusal` + `test_non_continuation_options_are_never_selected` | PASS |
| exact allowlisted continue-with-4.8 selectable | `S6.test_exact_allowlisted_option_is_the_single_selectable_choice` (+ wrong-model, unlisted, ambiguous refuse) | PASS |
| bridge cannot start new task/investigation/subagent | `S7.test_forbidden_operations_refuse_with_typed_error` + `test_bridge_never_spawns_children` | PASS |
| existing bounded subagents finish & reconcile | `S8.test_existing_children_finish_and_reconcile` | PASS |
| bridge lands at first valid seam | `S9.test_retirement_lands_once_and_never_continues` | PASS |
| fresh Fable 5 receives complete bounded handoff | `S9.test_fresh_fable_receives_a_complete_bounded_handoff` (incomplete handoff → `RotationError`) | PASS |
| good bridge output still reviewed | `S10.test_completion_is_never_acceptance` + `test_only_an_independent_pass_accepts` | PASS |
| defective bridge output rejected | `S10.test_defective_output_is_rejected` (FAIL/BLOCKED) | PASS |
| re-present preserves purpose/authority/prohibitions/criteria | `S11.test_representation_preserves_every_field_verbatim` | PASS |
| first re-entry success | `S12.test_first_success_clears_and_records` | PASS |
| two refusals → lower-tier/blocked | `S13.test_configured_lower_tier_continues_the_same_task` + `test_higher_precedence_policy_conflict_blocks_and_cites` | PASS |
| attempt count survives restart | `S14.test_counter_survives_a_controller_restart` (real reopened journal) | PASS |
| no ping-pong / duplicate primary | `S13.test_third_attempt_is_refused_no_ping_pong` | PASS |
| subsequent task returns to Fable 5 at next seam | `S13.test_subsequent_work_returns_to_fable_at_next_seam` | PASS |

**No R110 case is left without a covering test.** Bonus coverage present: R165 native-fallback boundary (S15), hygiene + disjoint typed codes + namespaced journal keys + unknown-never-zero status facts (S16), the double-gate `assert_actuation_permitted` (S6), and the full activated-path state walk end-to-end (PhaseE state machine tests).

## Steps independently executed (reproduced outputs)

1. `python -m tools.test_agent_supervisor_guardrail_bridge` → **Ran 71 tests … OK** (matches report §4.3 "71 tests, 0 failures"; ~1.5s).
2. `python -m tools.test_agent_supervisor_controller_succession` → **Ran 75 tests … OK**.
3. `python -m tools.test_agent_supervisor_phase1` → **Ran 80 tests … OK**.
4. State-count assertion updated 27→29 **with citations** in BOTH `test_agent_supervisor_phase1.py` (line 435 comment cites M0-T093, R070/R071/R103) and `test_agent_supervisor_controller_succession.py` (line 146 comment). `state_machine.py` adds exactly `GUARDRAIL_BRIDGE` + `REPRESENT_FABLE` and 11 documented transitions (each with a non-empty `doc`); `test_every_new_edge_is_walkable_and_documented` asserts ≥11 walkable edges.
5. `python -m tools.test_agent_supervisor_loop` → 107 tests; **OK** once `project-control/` was present (see finding 8).
6. `python -m tools.test_agent_supervisor_pending_prompt` → **Ran 19 tests … OK** (facade split intact).

## Mutation evidence — independently reproduced (report §4.3 claims 10/10 killed)

I applied four targeted mutations to scratchpad copies and confirmed each is genuinely caught (tests are non-vacuous, not fakes-testing-fakes):

- **M1** (skip the quota-direction delegate in `classify_guardrail_refusal`): → **3 failures**, incl. `S2.test_quota_evidence_never_classifies_as_refusal` (`'ordinary_result' != 'quota_policy_owns_signal'`). KILLED.
- **M5** (`MAX_REENTRY_ATTEMPTS = 2` → `3`): → **4 failures + 7 errors**, incl. `S14` restart cap and `S13` third-attempt. KILLED.
- **M7** (`spawn-subagent` moved into `PERMITTED_BRIDGE_OPERATIONS`): → **2 failures** (the hardcoded forbidden-op list + the PERMITTED-tuple pin). KILLED — confirms the deliberate "hardcoded op names, never iterated from the production constant" design closing the original M7 survivor.
- **M10** (disable the `constraint_deleted` raise): → **1 failure** at exactly the `constraint_deleted` subcase of `test_prohibited_transforms_each_refuse_with_typed_code`. KILLED — confirms negatives are one-per-prohibited-transform (each with its own typed code), not merged.

**Mutant-set adequacy:** the 10 mutants target the load-bearing invariants of R068/R069/R070/R071/R072/R073/R075 (quota delegate, authorization proof, recognition boundary, attribution, cap value, cap-boundary success semantics, forbidden-op closure, retire latching, exact continuation match, constraint preservation). Behavioral tests additionally cover contradictory co-occurrence, redaction/bounded excerpt, and digest isolation even without dedicated mutants — consistent with the unit-F/G targeted (not exhaustive) method. **Equivalence argument is sound:** the loop-seam-order swap is observationally equivalent because the classifier's own quota-direction delegate enforces R075 separation regardless of consultation order, and the contradictory-signal guard prevents any single input from being both signals. I independently confirmed the premise — killing M1 (the delegate) breaks S2 — so the "defense-in-depth, analyzed equivalent, not run" characterization is honest.

## Test-quality assessment

- **Restart survival (S14):** genuine — records attempt 1, `journal.close()`, reopens a NEW `DurableJournal` from the same sqlite path, asserts the digest-bound counter continues at 2 and a different request has its own untouched counter. Real durable state, not a fake.
- **Loop seam tests (LoopSeamTests):** genuine — a REAL `SupervisedLoop` with real `StateMachine`, real `DurableJournal`, real `TaskAuthority`; only the subprocess runner/reviewer (the correct external boundary) are faked. Confirms record-intent-only divergence (`REASON_REFUSAL_RECORDED` + `PAUSED_RECOVERY`, `attempts_recorded == 0`), the ordinary/refusal-looking failure paths stay `no_valid_checkpoint`, absent-integration unchanged, and R075 loop ordering (`test_quota_seam_evaluates_first_and_wins_its_signal` + `test_refusal_signal_with_both_seams_takes_the_refusal_path`). Loop code at `loop.py:1580–1620` places the guardrail seam AFTER the quota seam, both fail-closed, both keeping the PAUSE.
- **Documentation-confidence honesty (ASSERTED):** `S1.test_corpus_is_loaded_and_honestly_unverified` iterates every shape asserting `verified_live is False` and `REFUSAL_SHAPE_VERIFIED is False`; the fixture `guardrail_refusal_shapes_2_1_248.json` carries `verified_live:false` everywhere with `live_capture.status: pending-owner-C1`; `test_live_actuation_is_double_gated` proves `assert_actuation_permitted` refuses each half-gate. Honest and mechanically enforced.

## Expected versus actual

Production modules are genuine fail-closed policy logic, not stubs: `guardrail_refusal.classify_guardrail_refusal` delegates quota first, scans a real corpus, detects contradictory co-occurrence, requires Fable attribution + proven authorization, and defaults to AMBIGUOUS/NOT_A_REFUSAL; `refusal_bridge` implements the closed permitted-op set, retire-latching, digest-bound CAS counter, structured-field semantic-preservation with per-transform typed codes, R072 conservative lower-tier decision, and record-intent-only loop integration. All observed behavior matches the packet's stated intent.

## Evidence paths

- `tools/test_agent_supervisor_guardrail_bridge.py` (71 tests), `tools/agent_supervisor/guardrail_refusal.py`, `tools/agent_supervisor/refusal_bridge.py`, `tools/agent_supervisor/state_machine.py`, `tools/agent_supervisor/loop.py` (seam @1596–1620; facade @532–541), `tools/agent_supervisor/pending_prompt.py`, `tools/agent_supervisor/fixtures/guardrail_refusal_shapes_2_1_248.json`.
- `project-control/reports/M0-T093-guardrail-bridge.md`, `project-control/reports/M0-T093-evidence-map.json` (49 requirements; all packet-named Phase-E core R068–R075/R103/R110/R165/R177 present).

## Regression / security / provenance findings

- No regression: `loop.py` facade-split preserves public imports (`from .pending_prompt import …` with `# noqa: F401`); the extracted pending-prompt block was moved verbatim; its 19-test pack and the full 107-test loop suite pass. Full loop suite is clean.
- New reason/condition codes and journal-key prefixes (`guardrail_refusal/`, `guardrail_reentry/`, `guardrail_refusal_last_digest`) are namespaced and disjoint from quota-side codes (`S16` asserts this end-to-end) — R075/R184 satisfied.
- Redaction: `build_refusal_journal_record` bounds the excerpt (≤1200 chars) and pattern-redacts every field; `test_journal_record_is_bounded_and_redacted` proves `sk-ant-` never survives.
- Security posture: SHADOW-ONLY / record-intent-only is enforced mechanically, not narratively — `GuardrailBridgeDecision.actuated` is always False and `assert_actuation_permitted` double-gates any live actuation behind measured-live shape AND R595 owner authorization (both absent on this build). C1 live canary correctly flagged owner-gated and non-blocking.

## Defects

None blocking. One transient observation, fully explained (not a defect):

- Running the unmodified pre-existing test `CliReplayTests.test_replay_through_the_cli_reports_zero_provider_calls` initially showed 1 failure at `assertTrue(payload["provenance_checked"])`. Root cause: my first scratchpad extract contained only `tools/`; `replay.provenance_checkable` returns False when `<repo_root>/project-control` is absent (`replay.py:430`). After extracting `project-control/` too, the test PASSES. This is an artifact of my partial extraction, not a regression — the test file is untouched by this unit and the code path is unrelated to the guardrail bridge.

## Items verified via stored evidence / deferred to CI (not BLOCKED)

- `modularity_check.py --check` requires `git ls-files` (my extract has no `.git`), so I could not execute it here. Verified structurally instead: the facade re-export is present and the split is verbatim; sizes are as reported. The report's "0 failures" and "ruff 0.13.0 0 findings" claims, and the composed 2,669-test §4.4 suite, are confirming runs for CI on the pushed SHA (per the standing workstation constraint that long background runs are externally killed). I directly reproduced 352 tests across the four most load-bearing modules (guardrail 71, controller_succession 75, phase1 80, loop 107, pending_prompt 19) with zero real failures, making §4.4's plausibility strong.
- Full per-requirement directive verification over all 49 applicable requirements is the independent `directive-compliance-verifier`'s pass (producer ≠ verifier), recorded by the orchestrator in the directive `verification.json`. My gate confirms the R110 test-evidence obligations directly and that the 49-row evidence map exists and covers every packet-named Phase-E core requirement.

## Required rework

None.

## Reviewer conclusion

The D-024-R110 section-16.4 matrix is fully and honestly implemented: every named case has a concrete, independently-reproduced, non-vacuous test; the restart-survival and loop-seam tests exercise real durable state and a real `SupervisedLoop`; negatives are one-per-prohibited-transform with distinct typed codes; documentation-confidence is asserted; the mutation set covers the load-bearing invariants and its one equivalence argument is sound (premise independently confirmed by killing M1). Frozen identity is clean (no `tools/` change post-`b587720`). The single observed test failure was conclusively an artifact of my own partial extraction and clears once `project-control/` is present. No blocking defects; no required rework.

VERDICT: PASS

Numbered findings:
1. Frozen identity verified: `git diff b587720..654a083 -- tools/` empty; post-frozen commit is control-plane only.
2. All 18 R110/section-16.4 cases map to concrete covering tests; none missing (including R075 in BOTH directions via the real `classify_exhaustion`).
3. Reproduced: guardrail_bridge 71/0, controller_succession 75/0, phase1 80/0; state-count 27→29 cited in both files; +2 states/+11 documented transitions all walkable.
4. Mutation claims independently reproduced non-vacuous: M1, M5, M7, M10 each KILLED at the expected assertions; equivalence argument for the loop seam-order swap is sound (M1 confirmed load-bearing).
5. Test quality genuine: real reopened `DurableJournal` (S14), real `SupervisedLoop` seam tests, one-per-transform negatives with distinct typed codes, documentation-confidence (`verified_live=false`) asserted + double-gated actuation.
6. Production modules are real fail-closed logic, not stubs; SHADOW-ONLY/record-intent-only enforced mechanically; redaction + bounded excerpt proven; codes/keys disjoint from quota (R075/R184).
7. R075 loop ordering confirmed at `loop.py:1580–1620` (quota seam first, refusal seam second, both fail-closed, both keep PAUSE).
8. `loop.py`→`pending_prompt.py` split is facade-preserving (public re-exports intact); pending_prompt 19/0 and full loop 107/0 pass (once control-plane present).
9. Evidence map `M0-T093-evidence-map.json` has 49 requirements covering all packet-named Phase-E core (R068–R075, R103, R110, R165, R177).
10. Only-observed loop-test failure (`provenance_checked`) is an extraction artifact (needs `project-control/`); resolved to PASS on full extract — not a regression.
11. `modularity_check --check`, ruff 0.13.0, and the composed 2,669-test suite are CI-confirmed on the pushed SHA (not runnable in my `.git`-less extract); I reproduced 352 tests directly with zero real failures. This does not warrant BLOCKED per the evidence-capture division of labor.
