<!-- Verbatim reviewer return (agent-return channel; agentId a527774b7da86aa15, code-reviewer, 2026-07-17). Saved by the orchestrator per the report-preservation rule. Contains BOTH the G3 verdict (FAIL) and the G4 verdict (FAIL) on the single defect D1; delta re-review required after the orchestrator-authorized correction. -->

# GATE REPORT — M2-T004 (G3 independent walkthrough + G4 integration/regression)

- **Task:** M2-T004 — Data-semantics separation and snapshot lineage (owner code-audit P1)
- **Producer:** backend-engineer | **Reviewer:** code-reviewer (independent; did not produce this work or the M0-T014 sibling content)
- **Review target:** branch `task/M2-T004-data-semantics` @ `be55a3a8c479d78bb3403b260560144f679b70a8` (task commit `66b6f40`, merge of hardened main `7abfac5`), worktree `.claude/worktrees/M2-T004` (clean, verified `git status --short` empty at head)
- **Method:** started from packet scenarios S1–S6 (`project-control/tasks/M2-T004.json`), not the producer report. All commands re-run first-hand in the worktree. Producer report read only afterward for disclosure cross-check.
- **Date:** 2026-07-17

## 1. Scenario walkthrough table

| Scenario | Expected | Actual | Evidence | Verdict |
|---|---|---|---|---|
| S1 — 108-column denominator gone; 19-column basis explicit, documented, drives completeness | Old builder counted every `missing_inputs` entry; new counts only `feasibility_relevant` members of a documented basis | CONFIRMED. Old logic (`git show d61c9b6:services/api/app/profile/builder.py`, `_missing_inputs` → `if any(...critical...)` over ALL entries) derived `data_completeness` from every absent column. New: `services/api/app/profile/builder.py:564-570` filters `feasibility_relevant`; basis `FEASIBILITY_COLUMNS` (builder.py:169-177) = 2 critical (`lotarea`, `zonedist1`) + 17 noncritical = 19, each column cited against the official "PLUTO DATA DICTIONARY — May 2026 (26v1)" with page numbers and F08 inventory grounding (builder.py:100-167); documented exclusions (conditional-presence, geometry, identity/admin) with SODA null-omission rationale. Test pins `FEASIBILITY_COLUMNS <= PLUTO_COLUMNS` and `len == 19` (`tests/profile/test_data_semantics.py:138-146`). Real-capture proof: F01 gaps = exactly `{numfloors, yearbuilt}` → `partial`/`missing_noncritical` (test_data_semantics.py:164-169); synthetic all-basis-usable record with ~38 non-basis absences → `complete` while absences stay visible (test_data_semantics.py:149-161) | **PASS** |
| S2 — dimension independence | Complete source record + missing geometry simultaneously; no dimension computed from another dimension's value | CONFIRMED. `test_s2_complete_record_with_missing_geometry_reports_both_truths` (test_data_semantics.py:197-210): `source_record_completeness == "complete"` AND `geometry_validity == "missing"` in one profile; inverse case (geometry present, critical missing) and conflict case also pass. Committed schema-validated fixture exhibits the same pair (`packages/contracts/fixtures/valid/property_profile/status_dimensions_lineage_m2_t004.json`: complete + geometry missing, no `identity.geometry`). Code inspection (builder.py:403-459): each dimension derives from its own raw inputs (`missing_inputs`+drift → completeness; conflicts + critical subset → readiness; `identity.geometry` presence → geometry; constants → rule/financial). Shared raw inputs, never another dimension's label. `GEOMETRY_COLUMNS` disjoint from basis by design and by test (test_data_semantics.py:146). `not_computed` dimensions declared, never inferred (test_data_semantics.py:242-253) | **PASS** |
| S3 — fact_key stable, observation_id unique per retrieval; updated volatile sets sound | Constructions correct; prior idempotency guarantees not weakened | CONFIRMED. `build_fact_key` = `fact:<source_id>:<dataset_id>:<bbl>:<column>` (pluto_soda.py:242-251) — excludes version/time/event by construction; `_build_observation_id` = `obs:<event_id>:<bbl>:<column>` with `event_id` minted uuid4 per fetch (pluto_soda.py:254-259, 665). Stability across dataset versions proven with provenance_id divergence (test_data_semantics.py:273-290). **Volatile-set change reviewed critically and found SOUND and NARROW:** all three updated S6 tests strip ONLY `observation_id`, while `fact_key`, `value_digest`, `response_digest` now sit INSIDE the exact comparison (content determinism is stricter than pre-task), and new assertions add observation-id disjointness across retrievals (`tests/api/test_properties_v1.py:566-582`; `tests/connectors/test_pluto_soda.py:447-497`). The retry test additionally asserts one retried fetch = one event (same digests). No prior assertion was deleted | **PASS** |
| S4 — canonical-json-1 digest determinism | Sorted keys, canonical separators, UTF-8; deterministic cross-platform; any value change flips | CONFIRMED by independent hand-recomputation (not via the implementation): `sha256:` + SHA-256 of `json.dumps(value, sort_keys=True, separators=(",",":"), ensure_ascii=False).encode("utf-8")` matches a manually-constructed canonical string for a non-ASCII payload, and Unicode-code-point key order (`Z` < `a`) verified (pluto_soda.py:222-234). Windows/Linux: CPython `sort_keys` sorts by code point, float shortest-repr is platform-independent in CPython, dict insertion order is irrelevant under `sort_keys`, and encoding is explicit UTF-8 — no CRLF exposure (parsed values, not raw bytes). Ran the digest tests first-hand: key-order independence, byte-different-but-identical bodies digest equal, single value change flips exactly the response digest and that fact's value_digest (test_data_semantics.py:308-358). Digest recorded per response (`reproducibility.response_digest` + verbatim spec) and per fact, each independently recomputable | **PASS** |
| S5 — gap-free lineage; builder fail-loud on digest-less result | Every fact: observation → dataset_version → request_url/retrieved_at; loud failure path exists | CONFIRMED. Every provenance record carries all 9 lineage keys, asserted non-empty per fact (test_data_semantics.py:365-381; emission at pluto_soda.py:870-885); `no_match` also digests the empty response (pluto_soda.py:732-735, test:384-397). Fail-loud found at builder.py:540-547 (`if not result.response_digest: raise ValueError`), test at test_data_semantics.py:400-404. Traced the failure surface: the route wraps the builder in the generic-500 guard (`services/api/app/api/v1/properties.py:243-269`) → documented `internal_error` 500, never a misclassified 4xx | **PASS** |
| S6 — additive evolution; M2-T001 fixtures and journeys remain green | Full suites green; regenerated fixture diff = additive keys + intended completeness flip only | **PARTIAL — FAILS on the journeys clause.** API side green first-hand: `pytest tests -q` in `services/api` → **164 passed, exit 0**; `.github/scripts/tests` → 24 passed; `validate_contracts.py` → 0 failures incl. the 3 new invalid fixtures rejected for the intended reasons and the advanced `contract_version_unknown.json` exemplar (1.3.0). Pre-M2-T004 stripped shape still validates; builder still declares 1.0.0 (deliberate, M2-T003 owns declaration). **Fixture regeneration claim verified by full real-code-path regeneration** (F05 capture, fixed clock 2026-07-16T12:00:00Z, `correlation_id=m1t006-s6-ground-truth`, `observation_event_id=m2t004-s6-ground-truth-event`) → EXACT match with the committed file; the `missing_noncritical`→`complete` flip is CORRECT per the documented basis (24 missing inputs, zero feasibility-relevant — the defect fix demonstrating, not a regression). **BUT the M2-T001 web journey harness is RED — see D1** | **FAIL (D1)** |
| S7 — G1 re-verification of column set | data-contract-verifier gate | Not mine; outstanding. Noted only that the packet requires it before acceptance | N/A (separate gate) |

## 2. Defects

### D1 — HIGH, BLOCKING (G3 S6 + G4): M2-T001 web suite fails at review head; web-e2e job red on PR #5

- **Claim under test:** the charge stated "PR #5 CI green on the updated branch." **Reality: `gh pr checks 5` at head `be55a3a` shows `web-e2e (vitest + Playwright...)` FAIL on BOTH runs** (jobs 87941929246 and 87941940159); the five other jobs (api ruff+pytest, contracts, control-plane, web build, credential scan) pass.
- **Root cause (fully diagnosed):** `apps/web/src/test-support/fixtures.ts:30` imports the SHARED contract fixture `packages/contracts/fixtures/valid/property_profile/builder_output_m1_t005.json` across the package boundary. M2-T004 regenerated that fixture (correctly). The M2-T001 component test `apps/web/src/components/property/__tests__/property-lookup.test.tsx:65-66` hard-asserts the completeness banner shows `"missing_noncritical"` — the OLD value that encoded the retired 108-column defect. The banner now renders "All expected official inputs were retrieved (complete)…" (job log, 2026-07-17T16:50:14Z). One test fails; the job exits 1 before Playwright runs.
- **Severity reasoning:** the packet's own risk line names "existing web-e2e job is the regression harness" for S6, and the producer report (line 61/120) explicitly delegated the web-side proof to that CI job. The delegated proof came back negative. A red acceptance-scenario harness at the review head is a failed scenario, not a correction rider — the in-repo "PASS with required corrections" precedent covers in-scope fixable items, and this fix is OUT of M2-T004's scope (`apps/web/**` is a forbidden path), so it cannot be corrected by this producer under the current packet.
- **Not an architectural or contract-breaking defect:** the flip itself is the intended, verified defect fix; the stale web expectation is asserting the old defect. Contract evolution is genuinely additive (proven by stripped-shape validation and untouched `full_example*.json`/`geosearch_bbl_fact.json`).
- **Reproduction:** `gh pr checks 5`; `gh run view --job 87941940159 --log-failed`; locally: regenerate check above, then read `apps/web/src/components/property/__tests__/property-lookup.test.tsx:65-66` vs `packages/contracts/fixtures/valid/property_profile/builder_output_m1_t005.json` `data_completeness: "complete"`.
- **Remediation (orchestrator decision required):** either (a) a scoped amendment/companion task updating the M2-T001 expectation to `"complete"` (and any banner-wording assertion), or (b) rework packet extension. Then rerun CI and re-review the delta only. The producer's disclosure was honest but the "apps/web untouched ⇒ green" assumption was wrong; the shared-fixture import is exactly the cross-package coupling the assumption missed.

### Non-blocking findings

- **D2 (Low):** `_status_dimensions` declares an unused `result: PlutoFetchResult` parameter (`services/api/app/profile/builder.py:403-409`). Dead parameter; drop it or use it at rework.
- **D3 (Info):** `observation_event_id` is injectable into `fetch_by_bbl`; nothing enforces per-fetch uniqueness if a future production caller reuses one, which would violate the "never reused" observation_id contract. Today's only production path (`properties.py:_default_fetcher`) does not pass it → uuid4 per fetch, safe. Enforce/guard at M2 persistence.
- **D4 (Info, disclosed in spec text):** canonical-json-1 is Python-semantics-bound ("numbers serialized by Python json.dumps defaults"); cross-language recomputation (JS worker) would diverge on float edge cases and int-vs-float typing; `json.loads` also accepts `NaN`/`Infinity` literals (digest stays deterministic but non-RFC-8259 — intersects M1-T002 carry-forward D2). And because the digest covers the PARSED body, it cannot byte-verify a raw stored snapshot (duplicate keys collapse to last occurrence). Acceptable now — all digesting is Python-side — but at M2 Supabase snapshot persistence, store raw bytes and consider a raw-bytes digest alongside.
- **D5 (Info):** builder still declares `contract_version` 1.0.0 while the schema publishes 1.2.0 — deliberate and test-pinned (test_data_semantics.py:434-440); declaration semantics explicitly reserved for M2-T003 in the enum description. Consistent with the M1-T006 carry-forward; must be resolved there.
- **Review technique note:** running ruff from the repo root with `--config services/api/pyproject.toml` reports a phantom I001 in `test_data_semantics.py` (misclassifies `app` as third-party). From `services/api`, `ruff check .` (ruff 0.9.9) = "All checks passed!" — matching CI, which runs with `working-directory: services/api`. Not a defect.

## 3. Packet discipline

- Task commit `66b6f40` (sole parent `d61c9b6`) touches ONLY: `packages/contracts/**` (schemas, fixtures, README), `services/api/**` (connector, builder, tests), `project-control/reports/M2-T004-producer-report.md`. `git diff d61c9b6 66b6f40 --name-only` over `services/api/requirements.txt`, `services/api/app/main.py`, `.github/workflows/ci.yml`, `apps/web`, `render.yaml`, `docs`, `.claude` → EMPTY. Wave-1 exclusivity respected.
- Merge `be55a3a` (`66b6f40` + `7abfac5`) brings ONLY M0-T014 files (tools/, project-control M0-T014 gates/reports/task, state.json) — a legitimate merge from hardened main, not task-scope writes.
- No large/persistent artifacts written locally (API suite 1.7 s offline on fixtures; worktree clean at head). Low-storage policy respected.

## 4. Code-quality assessment

Strong. The basis documentation (builder.py:100-167) is the best-grounded platform-policy comment in the repo: per-column official citations, explicit exclusions with reasons, a disclosed vacant-lot limitation, and an explicit "platform completeness policy, not a legal interpretation" boundary — no hard-coded legal values, no guessed schemas, no silent defaults (the `feasibility_relevant` flag keeps every absence visible while changing only the derivation). The self-describing `policy`/`digest_canonicalization` verbatim-spec pattern makes historical reports self-explaining. Closed enums with single-value `not_computed` dimensions are the correct anti-invention posture pre-M4/pre-financial-engine. The M1-T002 F7 carry-forward (confidence never → coverage) remains enforced (builder.py:258-272).

## 5. Verdicts

Both verdicts fail on the single defect D1; everything else in the task independently verified sound. Expected rework is small and precisely scoped (one web test expectation + CI rerun), but it requires an orchestrator scope decision because the fix lives in a path forbidden to this task.

**G3 verdict: FAIL** (S1–S5 PASS; S6 FAIL on the "M2-T001 journeys remain green" clause — defect D1, blocking; D2–D5 non-blocking)

**G4 verdict: FAIL** (local API suite 164/164 + validator 24/24 + contracts validation green, but PR #5 CI at head `be55a3a` is NOT green: web-e2e failed on both runs — defect D1)
