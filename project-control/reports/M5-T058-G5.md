# M5-T058 G5 gate — independent security review (verbatim reviewer return)

Reviewer: security-reviewer (opus-4-8 xhigh per D-064), dispatched by the orchestrator at the
5f7b032e submit seam, pinned to reviewed sha 817a6546 (material 7d02eddc + the [ORCH-CORRECTED]
consumer fix). Recorded by the orchestrator; reviewer is read-only and records no gate itself.

---

I have completed my independent G5 review. All required checks pass; the material at HEAD is byte-identical to the reviewed sha; source, schema, tests, and TestClient-backed suite all corroborate the security claims.

---

VERDICT: PASS

# G5 Security Gate Report — M5-T058 (D-078 substitution stamp; rule_evaluation contract 1.2.0)

## Pinned identity (verified via read-only git plumbing)
- Reviewed sha: `817a6546` (short) — reviewed head, confirmed a `commit` object.
- Material commit: `7d02eddc` — 19 allowed_paths files (confirmed via `git diff-tree`).
- [ORCH-CORRECTED] consumer fix: `817a6546` — touches ONLY `services/api/tests/scenario/test_scenario_contract.py` (the frozen-contract guard admitting 1.2.0; a forbidden-consumer-adjacent test, routed to the orchestrator per the sweep rule, M5-T037 precedent). Legitimate: this is the sanctioning contract task.
- Submit seam / current HEAD: `5f7b032e`.
- Byte-stability: `git diff 817a6546..HEAD` touches only 4 ledger/report files (`state.json`, `tasks/M5-T058.json`, `reports/M5-T058.json`, `reports/M5-T058-evidence-map.json`). All 19 material files are byte-identical between the reviewed sha and HEAD (empty `git diff --stat` on the material paths). I reviewed the working tree at HEAD as a faithful proxy for the reviewed head.
- Schema mirror integrity: both schema copies carry git blob `764dd4c1…` and `diff` reports IDENTICAL — no divergent-mirror validation-bypass vector.

## Reproduced evidence (cwd services/api, Python 3.11.9)
- `python -m ruff check .` → `All checks passed!` (exit 0).
- Targeted 4-file pytest (`tests/api/test_rule_evaluation_api.py tests/contracts/test_rule_evaluation_contract.py tests/rules/test_rules_integration.py tests/spatial/test_live_provider.py`) → `168 passed` (matches the evidence-map claim exactly).
- Security-relevant named tests run verbose and green: route stamp threading, condo-unresolved honest refusal, non-condo generic reason, and the parametrized `no_auto_pick[multi-lot|unresolved|typed-error]`.
- An independent inline `python -c` TestClient probe was blocked by the read-only guard (only `pytest` execution is whitelisted); I substituted source-level verification plus the passing route tests, which assert the same properties (see F-checks below).

## Threat-surface walkthrough

1. Identity integrity — can a caller forge / spoof / strip the stamp?
   - The endpoint `get_rule_evaluation(bbl: str, …)` (`services/api/app/api/v1/rule_evaluation.py:240-253`) accepts ONLY the `bbl` path param plus `Depends`-injected server-side providers. No request body, no Pydantic body model, no `Request`. A caller has no channel to supply a stamp. GET-only route ignores any body.
   - The stamp is built server-side in `build_substrate_substitution_stamp` (`live_provider.py:309-333`): `entered_bbl = input_bbl` where `input_bbl` is `normalized.canonical` (from `normalize_bbl(bbl)` — server-normalized), and `analyzed_bbl = resolution.resolved_base_bbl` (from the resolver). Every provenance field is read via `getattr` off the `CondoResolution`. Nothing is echoed from caller input.
   - Route test `test_m5t058_route_stamps_substrate_substitution` asserts `block == stamp` (verbatim, nothing re-derived), `evaluated_input.bbl == BBL` (entered, not moved to the base lot), `analyzed_bbl == "1000010050"` (resolver-derived). Confirmed.
   - Strip/forge-of-shape is fail-closed: `validate_rule_evaluation_document` runs against the closed schema before send (`rule_evaluation.py:417`); a malformed stamp → typed 500, never an invalid 200.

2. Echo boundedness / injection.
   - `entered_bbl` / `analyzed_bbl` are `common.schema.json#/$defs/bbl` = `^[1-5][0-9]{5}[0-9]{4}$` (strict 10-digit). The one caller-influenced value (the entered BBL) is validated by `normalize_bbl` before any connector call.
   - The new refusal vocabulary `condo_base_lot_unresolved` is a fixed constant; the refusal `reason` (`integration.py`) is fixed prose with NO source/user data interpolated — no response or log injection via the new vocabulary.
   - Log redaction: this task introduces ZERO new logger calls in `live_provider.py` (verified against the material diff). The pre-existing `_fail_safe` logs event + typed error CLASS + correlation id (never `str(exc)`), and the new condo fail-safe branch logs `condo_{condo.outcome}` where `outcome` is a fixed OUTCOME_* constant, not source data.
   - Web rendering: provenance fields surface only inside `CapturedRecord`, which renders `<pre>{JSON.stringify(value, …)}</pre>` — React-escaped text, no `dangerouslySetInnerHTML` anywhere in `components/architect/`. The visible notice text renders only the (bounded) BBLs. No markup passthrough / XSS.

3. Schema safety — strictly additive, no new surface.
   - `substrate_substitution` is OPTIONAL (not in root `required`); `contract_version` enum grows to add `1.2.0`; `fail_safe_reason` enum adds one member. Pre-1.2.0 documents stay valid (contract test proves it).
   - `additionalProperties: false` at the root, in `substrate_substitution`, and in nested `mixed_substrate`. All 9 fields required inside the block.
   - Both schema copies byte-identical (blob `764dd4c1…`). Generated TS (`packages/contracts/generated/rule_evaluation.ts`) matches the schema exactly, and the web `FAIL_SAFE_REASONS`/`RULE_EVALUATION_CONTRACT_VERSIONS` arrays are proven mutually-equal to the generated unions via `MutuallyEqual` (tsc fails on drift) — the TS is not loose enough to mask a server refusal; `condo_base_lot_unresolved` is in the closed union and validated by `checkSubstrateSubstitution` (positive-shape; server owns the closed schema).
   - No new route, HTTP method, or public parameter; the route stays `include_in_schema=False` and gated by `internal_rule_eval_enabled()` (404 when off). No new unauthenticated surface.

4. live_provider degraded paths — fail-direction (D-051 lesson).
   - Multi-lot / unresolved / typed-error condo outcome (`is_fail_safe`): returns `substrate=None, fail_safe_cause=CONDO_BASE_LOT_UNRESOLVED_CAUSE`, NO base-lot auto-pick, NO stamp (`live_provider.py:359-368`) → route emits `fail_safe_reason=condo_base_lot_unresolved`, `zoning_district=None`, no `substrate_substitution` key. Verified by `test_m5t058_route_condo_unresolved_names_reason_no_stamp` and `..._as2_condo_fail_safe_names_cause_no_auto_pick` (zero geometry/lot lookups).
   - Resolved-single but composition fails (no candidate districts / partial page / connector error): returns absent substrate with NO stamp and NO condo cause → generic `spatial_intersection_absent`. Still a refusal.
   - Non-condo absent, and any unexpected exception in the outer boundary: `substrate=None`, generic reason. Verified by `test_m5t058_route_non_condo_absent_keeps_generic_reason`.
   - There is NO path where a base-lot substrate composes but the stamp is dropped while results are presented as the entered lot: on `substitutes_base_lot` + successful composition, the stamp is always built and carried. Fail-direction is correct — a degraded resolver fails toward honest refusal, never toward a silent unstamped analysis presented as the entered lot.

5. SSRF / upload / prompt-injection / least privilege / secrets.
   - No new outbound request; the condo resolver call is the accepted M5-T045 seam, reused at most once per evaluation (asserted). No new dependency, no new flag. No file upload surface. No AI/prompt surface in this change. No new secret; browser receives only the response document; fixtures contain no secrets (scanned — the fixture values are public condo/dataset identifiers and public BBLs).

## DB-036(d) coupling ruling — RESOLVED (correct)
`AnalysisIdentityNotice.tsx` resolves the potential contradiction with mutually-exclusive, ordered branches: a stamp legitimizes a mismatch ONLY when it CORRESPONDS to the on-screen identities — `stampLegitimate` requires `entered_bbl === requestedBbl === evaluated_input.bbl` AND `analyzed_bbl` is a non-empty, different string (`AnalysisIdentityNotice.tsx:54-61`). A corresponding stamp renders the "analyzed on base lot" record (no `role="alert"`); an unstamped OR non-corresponding mismatch still fires the withhold alert (`role="alert"`). A forged/mismatched stamp cannot bypass the withhold guard (it fails the correspondence test and the withhold branch fires). The guard is closed by ADDING the stamped branch, not by relaxing the unstamped path. Both directions bound in `analysis-identity-substitution.test.tsx` (web proof CI-only, thin client). No contradiction; the accepted safety behavior is preserved.

## Fail-direction question — ANSWERED
The degraded resolver fails toward honest refusal (`condo_base_lot_unresolved` when the cause is a condo outcome; generic `spatial_intersection_absent` otherwise), with no base-lot auto-selection (D-078-R002) and no silent unstamped analysis of the entered lot. Correct per the D-051 lesson.

## Findings

- F-1 (LOW / informational, non-blocking, defense-in-depth): The source-derived provenance strings `condo_key`, `resolution_path`, `retrieved_at`, and `dataset_ids[]` are schema-typed `string`/`string|null` with no `maxLength` or charset bound; `note`/`source_id`/`mixed_substrate.*` use `non_empty_string` (minLength 1, no maxLength). These originate from the official DOF Digital Tax Map condo channel via the accepted (frozen) connector — not caller-controlled — are rendered as React-escaped text, and are not written by any new log line. The absence of length/charset bounds mirrors the ACCEPTED M5-T037 `wide_street` D-052 provenance arrays and the repo-wide `non_empty_string` convention, so it is consistent with precedent. Optional future hardening (not required for this gate): add `maxLength` bounds to source-derived provenance strings across the rule_evaluation contract in a dedicated additive contract task. No exploit path exists in this change.

## Required corrections
None. (F-1 is informational only.)

## Modularity note
`integration.py` is a pre-existing `review_signal` warn-band file; this task added only additive threading (one constant, one optional field, one conditional serialize line, two default-valued params, one honest-reason branch) through the existing fail-closed boundary — no new responsibility, I/O, or presentation. `live_provider.py` is not among the modularity warns; the carry is cohesive with its fetch→compose responsibility and preserves the `object|None` seam via the `build_live_substrate` facade for the three forbidden consumers. Consistent with the modularity policy.

Verdict: PASS. No critical, high, or medium findings; one low/informational defense-in-depth observation (F-1). Identity integrity, echo boundedness, additive schema safety, byte-identical mirrors, consistent generated TS, correct degraded-path fail-direction, no new unauthenticated surface, no new dependency/secret, and log redaction are all satisfied. The DB-036(d) coupling and the fail-direction question are resolved correctly.
