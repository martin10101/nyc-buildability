# M5-T073 — Data-contract review, fixture capture provenance P05–P08 (data-contract-verifier, read-only)

> Transmission history: returned across three transmissions — the opening span (truncated
> inside F5 at "the stored body's `Shape__Area` is 18,"), a continuation from that exact
> cut phrase (truncated in duty 3 at "The harness re-checks the authoritative digests
> itself — `python"), and a final span from that cut. Joined verbatim at each cut point
> (resend anchor labels are transport artifacts). Saved verbatim by the orchestrator
> (transport entity-decoding only).

## M5-T073 — Data-contract review (fixture capture provenance, P05–P08)

Verified at pinned HEAD `06db8ab91d325efa4535c1be0b5a890159eddc3b` (`git rev-parse HEAD` matches). All fixture files are tracked and the working tree is byte-clean against HEAD for `services/api/tests/connectors/fixtures/bridge_ring_pairs/` (`git diff HEAD` empty, `git status --porcelain` empty for that path). I wrote nothing.

### Findings

**F1 — Medium, required correction. `PROVENANCE.md` is entirely pre-harvest and now asserts something false for half the pairs.**
File: `C:\Users\MLFLL\Downloads\nyc-zoning\ctl24\services\api\tests\connectors\fixtures\bridge_ring_pairs\PROVENANCE.md`, lines 12–16 and 31–48. The "Byte source (single source of truth, no fabrication, no drift)" section states the verbatim bodies "are **already stored** ... in two accepted fixture packs, and are referenced here by path + recorded sha256 rather than duplicated". That is true only for P01–P04. P05–P08 are stored directly in this directory as raw bodies and come from a net-new orchestrator egress capture. The bound section still says the sample is "4 real single-lot pairs across 2 boroughs" and that the ≥8-pair remainder "is routed to harvest" — the harvest was executed 2026-09-23 (commit `06e3e72d`). The file never mentions P05–P08, the capture channel used for them, or the 26v1/26v2 cross-release split.

This is a documentation-accuracy defect, not a byte-provenance defect: the channel and the cross-release fact *are* recorded accurately in `pairs_manifest.json` (`sample_bound` carries the `[ORCH-CORRECTED per M5-T073-G3 F1/F4]` disclosure; each P05–P08 entry's `notes` says "net-new orchestrator egress capture per HARVEST_SPEC.md") and in the producer report's `[ORCH-HARVEST]` section. But the file named `PROVENANCE.md`, which the manifest itself points at, gives a reader a materially wrong picture of where half the bytes came from. It should gain the four harvested pairs, their capture channel, and the per-side release split, and the "already stored in two accepted packs" sentence should be scoped to P01–P04.

**F2 — Low. `HARVEST_SPEC.md` reads as an un-executed to-do, and one instruction in it is already done.**
`HARVEST_SPEC.md` lines 84–89 tell the reader to "extend `_auth_response_body` to return the body verbatim for `kind == "raw_esri_body"` (a one-branch change routed to the orchestrator...)". That branch exists at HEAD in `services/api/tests/connectors/test_bridge_ring_preconditions.py:285-286`. The spec would benefit from an EXECUTED-on marker pointing at the manifest's P05–P08 entries; as a recipe its content is otherwise correct (see duty 4).

**F3 — Informational. Timestamp format splits across the manifest.** P01–P04 use `2026-07-20T05:30:57Z`; P05–P08 use `2026-09-23T04:58:15+00:00`. Both are valid RFC 3339 and the harness does not parse the field, so there is no functional effect. It is a useful tell rather than a problem: the connectors' own `_rfc3339` emits the `Z` form, so the `+00:00` form confirms the harvest script stamped these itself rather than copying a connector-produced value.

**F4 — Informational. P05–P08 carry thinner provenance metadata than the accepted packs.** The M2-T009 envelopes record `http_status`, `capture_method`, and `classification` alongside the body; the directly-stored raw bodies record only endpoint, `retrieved_at`, digest, `kind`, and `dataset_version`, with the channel in free-text `notes`. HTTP 200 is inferable from the body being a well-formed feature set. This matches `HARVEST_SPEC.md`'s own registration template, so it is not drift from the spec — just a lower ceiling than the pattern it sits beside.

**F5 — Informational, clarification worth making.** The per-pair `notes` area figures are PLUTO **LotArea**, not the geometry's `Shape__Area`, and the notes do not say so. On P08 the two diverge 5.7×: the note says "3300 sq ft" while the stored body's `Shape__Area` is 18,692.83 sq ft. I confirmed live that `LotArea` for BBL 5000040010 is exactly 3300, so the note is accurate — but a reader cross-checking the note against the stored bytes finds no 3300 anywhere in them. Naming the field would close that.

**F6 — No fabrication indicators. I found NONE, and I looked specifically.** Coordinate precision across all eight harvested rings is uniform full-service float precision (7–9 decimals on the 2263 side, 12–13 on the 4326 side) with zero low-precision or hand-rounded values and no duplicate vertices beyond ring closure. `OBJECTID`, `BBL`, `Shape__Area`, and `Shape__Length` are byte-identical across the display and authoritative sides of every pair, which is what a genuine same-second capture of one feature looks like and what a hand-built pair would have to get right to 16 significant digits. Capture timestamps (`04:58:15–17Z` = 00:58 EDT) fall 82 seconds before the harvest commit's author date (2026-09-23 00:59:37 -0400) — plausible and not conveniently round.

### Per-duty assessment

**Duty 1 — endpoint family and query shape: PASS.** I imported the accepted builders and compared their output to every recorded endpoint. All 16 (8 pairs × 2 sides) match byte-for-byte: `build_outline_query_url` for the display side and `build_lot_query_url` for the authoritative side, both rooted at the official NYC DCP ArcGIS service `services5.arcgis.com/GfwWNkhOj9bNBqoJ/.../MAPPLUTO/FeatureServer/0`. Same bounded `outFields` set (the 10-field allowlist, never `*`), same `orderByFields=OBJECTID ASC`, same `resultRecordCount=10&resultOffset=0`, display side adding `f=geojson&outSR=4326` and authoritative side `f=json`. Neither builder sets `returnGeometry`, so the service default (true) applies and both stored bodies carry geometry — consistent. I also confirmed the connectors *issue* exactly these URLs and add no extra parameters: `mappluto_lot_outline.default_fetch` (line 247) and the geometry connector's fetch path (line 1722) each pass the builder's output straight to `urllib` with only an `Accept: application/json` header.

**Duty 2 — plausible verbatim captures: PASS.** Each authoritative body carries the full esri envelope (`objectIdFieldName`, `uniqueIdField`, `globalIdFieldName`, `geometryProperties` with `units: esriFeet`, `geometryType: esriGeometryPolygon`, `spatialReference {wkid: 102718, latestWkid: 2263}`, the complete 10-entry `fields` array with per-field `sqlType`/`domain`/`defaultValue`), exactly one feature, the pair's BBL, `Version: "26v2"`, and a closed single exterior ring. Each display body is GeoJSON with the esri `crs` member, one Polygon feature, matching BBL and Version. All eight are single-line minified with no trailing newline and no CRLF, as documented. The structure is indistinguishable from the already-accepted M5-T020 capture `LOT01_single_1008350041.geojson` down to key ordering. Ring vertex counts are 5/5/7/31, matching the manifest notes exactly.

**Duty 3 — digests: PASS, all 16 recomputed and matched.** For `raw_esri_body` / `raw_geojson_body` the basis is the stored file bytes; for the P01–P04 envelope fixtures it is `provenance_envelope.response_body_raw`. Every one reproduces. Beyond that I verified P01–P04 are referenced faithfully: all four `source_fixture` paths resolve, and each recorded endpoint and `retrieved_at` equals the source pack's own `request_url` / `retrieval_timestamp_utc`, with the packs' `classification: raw` and `http_status: 200`. I also body-verified every `dataset_version`: the `Version` attribute inside the bytes matches the recorded per-side value on all 16 sides, so the G3-F1 cross-release correction (P01–P04 = 26v2 display against 26v1 authoritative; P05–P08 = 26v2/26v2) is accurate against the bytes rather than asserted. The harness re-checks the authoritative digests itself — `python -m pytest tests/connectors/test_bridge_ring_preconditions.py -q` run from `services/api` gives 14 passed.

**Duty 4 — re-run recipe and channel honesty: recipe PASS, channel statement FAIL (F1).** The recipe reproduces captures of exactly this shape: it calls the same two builders (so the URLs are identical by construction, which I proved rather than assumed), uses the same `Accept: application/json` keyless GET the connectors use, writes the raw body bytes, and defines the digest as SHA-256 over the exact UTF-8 body bytes — the basis I successfully recomputed against. Only the output filename differs from the stored layout (`{bbl}_{label}.json` versus `<pair_id>/{label}.json`), and the spec's own registration section covers that. `PROVENANCE.md` does not state the P05–P08 capture channel at all and mis-states the byte source for them; see F1.

**Duty 5 — live spot-check: feature still matches.** Two requests, both against the official endpoint. The first was the exact recorded P08 authoritative query (BBL 5000040010). It returned one feature, `spatialReference {wkid: 102718, latestWkid: 2263}`, `esriGeometryPolygon`, `OBJECTID: 617805`, `Version: "26v2"` (unmoved — no release drift to report), `Shape__Area: 18692.827423095703` and `Shape__Length: 989.6074506768515` — identical to the fixture at full float precision. One caveat I want to state rather than paper over: the fetch summarizer reported 25 coordinate pairs in the first ring where the fixture has 31. I read that as a counting error by the summarizer, not geometry drift, because `Shape__Length` came back identical to 16 significant digits (a changed ring could not do that) and the reported first and last coordinates are exactly the fixture's ring-closure point `[959500.986835003, 174955.06648922]`. I did not independently recount, having stayed inside the two-request bound. The second request resolved F5 by confirming `LotArea: 3300` for the same BBL.

**Duty 6 — fabrication indicators: NONE found.** Coordinate precision across all eight harvested rings is uniform full-service float precision (7–9 decimals on the 2263 side, 12–13 on the 4326 side) with zero low-precision or hand-rounded values and no duplicate vertices beyond ring closure. `OBJECTID`, `BBL`, `Shape__Area`, and `Shape__Length` are byte-identical across the display and authoritative sides of every pair — what a genuine same-second capture of one feature looks like, and what a hand-built pair would have to get right to 16 significant digits. Capture timestamps (`04:58:15–17Z` = 00:58 EDT) fall 82 seconds before the harvest commit's author date (2026-09-23 00:59:37 -0400): plausible, and not conveniently round. No impossible timestamps, no digest mismatches, no hand-editing signal anywhere.

### Verdict rationale

The byte provenance of the four orchestrator-captured pairs is sound on every measurable axis: correct official source family, exact accepted-connector query shape, verbatim capture shape, reproducing digests, body-verified versions, live-confirmed feature identity, and no fabrication signal. The one defect is that the prose provenance document beside those bytes still describes the world before the harvest and contains a sentence that is now false for half the pairs. That is a required correction — a reader of `PROVENANCE.md` alone would be misinformed about where P05–P08 came from — but it does not put the fixture bytes or the measurements built on them in question, and the accurate account already exists in `pairs_manifest.json` and in the producer report's `[ORCH-HARVEST]` section.

**F1 should be applied before acceptance.**

DATA-CONTRACT VERDICT: PASS

END-OF-REPORT
