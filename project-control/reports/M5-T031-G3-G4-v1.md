**G3 FAIL · G4 FAIL — M5-T031**

Reviewed SHA: `b4514d9a338a7ced8bd268b85afd55b12c729fd3`. Independently reproduced the 19-file content digest `6a805688f01797e34604a34f32f36266ddade407edf5680ed22fcb37c9857ac4` in `m5t031-ui`. No files changed.

The defect cluster is in `apps/web/src/lib/architect/development-limits.ts` and its composition in `DevelopmentLimits.tsx`.

| Finding | Exact reproduction | Actual result |
|---|---|---|
| Malformed traces crash the new summary | Clone `draftApplicableDoc()`. Individually delete its first trace’s `input_validation`, `effective_window`, `outputs`, or `citations`. Run `validateRuleEvaluationDocument`, then `evaluatedResidentialFar`. | Validator accepts every case; selector throws `TypeError` for every case. Overview now consumes these unchecked fields. |
| Conflicting analysis still presents a cap | Pair `ruleConflictDoc()` with `preliminary_r5_cap.json`; align BBL and fingerprint. Call `analysisRecordsDiffer`, `calculationStatus`, `evaluatedResidentialFar`, and `scenarioCap`. | `false`, **Conflicting rule results**, `null`, **15000**. The conflicting evaluation suppresses FAR but leaves the numeric cap promoted. |
| Missing fingerprint permits association | Set the preliminary scenario’s fingerprint to `null`; retain the applicable evaluation. | Scenario validator accepts it; `analysisRecordsDiffer=false`; cap remains **15000**. Missing association evidence is treated as agreement. |
| FAR can masquerade as height | Set `height_limit` to draft value `1.5`, unit `feet`, referencing the residential FAR trace’s rule/version and `max_residential_far` output. | Scenario validator accepts it; `bulkRow` returns **1.5 feet · Draft**. Matching a number and identifiers does not establish the governing bulk family or units. |
| Value filtering conceals competing traces | Add a second trace with the same rule/version/output identity but value `2.5` to the previous case. | `bulkRow` still returns **1.5 feet · Draft** because it removes the conflicting trace before checking uniqueness. |

I independently reproduced all five with `node` heredocs loading the actual TypeScript modules through in-memory `typescript.transpileModule`; no copied selector implementation or file edits.

Executed regression commands from `m5t031-ui/apps/web`:

- `npm test -- --run src/components/architect/__tests__/development-limits.test.tsx src/components/architect/__tests__/entry.test.tsx src/components/architect/__tests__/workspace.test.tsx src/components/architect/__tests__/source-links.test.tsx src/components/compare/__tests__/unused-floor-area.test.tsx` — **113 passed**.
- `npm test` — **40 files, 769 passed**, with existing jsdom canvas warnings.

These passing tests omit the failing associations above. Root supplied exact-head CI evidence for lint, typecheck, build, dependency security, and modularity. Browser screenshots were not yet inspected; no browser acceptance claim is made.

| D063 requirement | Review result within frontend scope |
|---|---|
| R001 | **FAIL:** shared hierarchy and existing-fact disclosure work, but unsupported bulk values can be promoted. |
| R002 | **FAIL:** malformed, conflicting, unassociated, and semantically mismatched results escape safeguards. |
| R003 | **PASS, frontend contribution:** internal executable checks reproduced; independent official-source audit remains separately owned. |
| R004 | **PASS, presentation only:** 17 district/context cases and five borough prefixes pass; no district lookup or example-specific production hardcoding. |
| R005 | **PASS, reviewed diff:** presentation changes preserve backend, contracts, dependencies, and deployment files. Remote actions remain orchestrator-owned. |
| R006 | **FAIL:** producer guard claims exceed observed behavior; revise evidence after closing this cluster. Source/display/calculation limitations otherwise remain explicit. |
| R007 | **PASS, reviewed workflow:** no architect benchmark or repeated client check required. |
| R008 | **UNVERIFIED remotely:** no reviewer mutations; main/PR241 hold enforcement remains root evidence. |
| R009 | **PASS:** no legal publication or universal-buildability conclusion; missing envelope coverage remains visible. |
| R010 | **PASS:** no frontend arithmetic, API/schema changes, database work, or credential access in the reviewed changes. |

Modularity is acceptable: the focused adapter/component own presentation selection, views compose them, public contracts remain unchanged, and `DraftHeadline` retains its compatibility export.

**Required rework:** guard nested traces, require proven document association, suppress promoted values when analysis conflicts, establish bulk family/output/unit compatibility from supported contracts, and check trace identity ambiguity before value agreement. Add negative tests for the complete cluster, update the producer report, and rerun G3/G4 on the newly frozen candidate. This review makes no legal or citywide calculation-completeness determination.

