# M5-T103 — G3 code review (code-reviewer "cr-t103", read-only)

> Transmission history: pinned at 68bc726e, delivered as three SendMessage parts (1/3..3/3) ending with END-OF-REPORT;
> no truncation. Joined verbatim by the orchestrator (transport wrapper tags removed only). Verdict PASS with advisories:
> A1 (HIGH, the object-count cap applied only after the whole /Prev chain is collected - the same defect as G5 F2) and
> A2-A7. They are carried into the M5-T103 rework inventory.

---

M5-T103 G3 code review (read-only) — PDF 1.5+ xref/object-stream resolver. PART 1/3.

PIN: HEAD 68bc726ec064c28d66761dbfb9b504a693565f64 at start. Material commit 3fab28f6 (6 files, all in allowed_paths).

INDEPENDENTLY VERIFIED GREEN (this checkout, HEAD 68bc726e):
- Tests via the 3.11 shim (run_sheet_all_tests.py): 84 passed, exit 0 — the new file's 29 resolver/guard/mutation tests + the 62-case split-equivalence golden + the existing test_sheet_reader.py suite, all green. (Producer reported 75; the +9 delta is benign parallel M5-T104 additions to test_sheet_reader.py, not a regression.)
- `cd services/api && python -m ruff check .` → All checks passed! (exit 0).
- `python tools/modularity_check.py --check` → exit 0; NONE of the four touched modules (pdf_object_streams/sheet_reader/sheet_objects/sheet_interpreter) appear in the warn list. SLOC all <600.
- app/documents & app/extraction diff EMPTY — shared read_object_table byte-untouched, not widened (DB-055 k / AS-4).

BLOCKING: none.

ADVISORY A1 (HIGH — memory amplification beyond the enumerated guards). `_collect_stream_entries` (pdf_object_streams.py:253-287) merges the /Prev-chain entry map with NO bound against MAX_PDF_OBJECTS (=4096, pdf_xref.py:72) until the POST-collection check in `_materialize` (pdf_object_streams.py:497). A crafted small PDF — 33-hop /Prev chain (MAX_PREV_DEPTH=32), each xref stream declaring MAX_XREF_ENTRIES=131072 type-1 entries at distinct /Index object numbers, entry width 1 byte (w=[0 1 0], type defaults to 1) — decodes to only ~4.3 MB total (well under the 8 MB per-stream cap, the 512 ratio, and the 134 MB doc budget; the zeroed entry bytes compress to tens of KB) yet builds an entries_map of ~4.3M dict entries (~hundreds of MB transient) before the 4096 check fires and refuses. It is BOUNDED and transient (freed on refusal), and every ENUMERATED mandatory guard (inflated-bytes cap, inflate ratio, /Prev depth, objstm count, /N) is present + mutation-proven and the inflation peak is bounded to <5 MB as tested — so this is a SEPARATE un-guarded vector, not a failure of the M5-T099 G5 F2(a) criterion. Fix: an incremental `if len(entries_map) > MAX_PDF_OBJECTS` (or bound len(seen)) inside the collection loop. Recommend routing to the security-reviewer's memory-DoS lane. Not blocking G3.

Part 2 (A2-A7) next.

---

M5-T103 G3 review. PART 2/3 — remaining ADVISORY findings (none blocking).

A2 (MEDIUM — scan-only label imprecise). The scan-only gate (sheet_reader.py:345-351) refuses any resolver-path document with zero polylines as feature "scan only", but SheetPage carries `text_runs` as a SEPARATE vector channel (sheet_primitives.py:199). A legitimate all-vector-TEXT PDF 1.5+ document (e.g. a general-notes/spec sheet with no linework) is refused as "scan only" — a false label; it is vector text, not a scan. It only bites when NO page in the whole document has a polyline, so a normal multi-sheet drawing set passes. Faithful to DB-055(l)'s literal "zero vector geometry" wording, and test_scan_only_page_is_typed_refusal enshrines text-only→scan-only, so this is intended; but consider distinguishing text-only from raster-only, or a truer "no vector geometry" feature name. (Report DISCOVERY D3 already notes the real-file interaction.)

A3 (LOW — deliberate, scoped inconsistency). The same image-only page is a SUCCESS (image_counted) on the classic-xref path but a "scan only" refusal on the resolver path, because the gate is gated on `used_resolver`. Deliberate, to keep the READ-ONLY 62-case golden byte-identical (AS-4); documented in the producer report. Acceptable as scoped; note for convergence when the classic golden can be revised.

A4 (LOW — weak test). test_broken_type1_offset_is_typed_refusal (test file:335-339) asserts only `ref.origin in (STRICT_READER, SHEET_PROFILE)` — accepts either origin, pins no `feature`. It proves "a typed refusal happened" but not which; consider asserting the feature (`object identity`, pdf_object_streams.py:525-530).

A5 (LOW — dead code in test). test_unsupported_predictor_tiff_is_typed_refusal (test file:322-332) builds `pdf = _xref_pdf(...)` then immediately overwrites it with `_xref_pdf_with_predictor_value(2)`; the first assignment is dead. Cosmetic.

A6 (LOW — coverage gap). The "incremental update chain" trigger path (a classic xref table with trailer /Prev but no /XRefStm) correctly DEFERS — `_resolve_hybrid` returns None (pdf_object_streams.py:404-405) → facade re-wraps the strict refusal (sheet_reader.py:271) — but is untested. Recommend a test that such a file still returns the strict "incremental update chain" refusal unchanged.

A7 (LOW — implicit, safe). Objects "forbidden inside object streams" (nested streams) are handled implicitly: compressed objects are parsed with `parse_object` (never assembled into a PdfStream) and always assigned generation 0 per §7.5.7 (pdf_object_streams.py:570); a nested stream degrades to a dict with trailing bytes ignored rather than an explicit typed refusal. Safe, no crash. Acceptable.

Part 3 (verified positives + verdict) next.

---

M5-T103 G3 review. PART 3/3 — VERIFIED POSITIVES + verdict.

INTEGRATION TRIGGER IS PRECISE. XREF_STREAM_FEATURES = {"cross-reference stream","hybrid-reference file","incremental update chain"} (pdf_object_streams.py:105-107) EXACTLY matches the strict emitter (pdf_xref.py:143, :185, :191). "encryption" (:198) and "object count bound" (:205) are NOT in the set → they stand as strict refusals; a PdfSyntaxError (not UnsupportedPdfFeature) also falls through untouched (sheet_reader.py:277-278). No refusal class misrouted. test_classic_xref_still_takes_old_path proves the classic path never calls the resolver (patched to explode). Classic read is byte-for-byte: same read_object_table(bytes(data)) first, decoded_seed=0.

RESOLVER CORRECTNESS (verified in source): /W (3 ints, w1≥1, bounded byte width, degenerate refused, :330-343); /Index (even-length, default [0,size], subsection-count==rows, :346-374); entry types 0/1/2 (free deletes via `seen`; zero-width type→1 per §7.5.8.2, :380); /Prev newest-wins + visited-offset cycle guard + depth bound (:265-284); hybrid /XRefStm precedence — stream first, classic fills (:409-418); objstm header integer-pair parse with /First-overrun, offset-range, and header-number==requested-number identity checks (:600-616, :561-566); gen 0 for compressed objects (:570); lying offset caught by (number,generation) identity check (:525-530). /Extends intentionally ignored — documented, safe (random access via the type-2 (objstm,index) pair + per-stream header identity).

MANDATORY GUARD: inflate_guarded uses bounded incremental decompressobj().decompress(src,65536) (never unbounded), checks absolute cap + ratio on the cumulative projection BEFORE retaining each chunk, then charges the SHARED doc budget before appending (DB-064 d, no post-increment overshoot; sheet_objects.py:307-341). Zip-bomb test proves peak <5 MB; all six guards mutation-reddened. Budget seeded into _StreamDecoder(decoded_bytes=) so xref/objstm + content phases share one budget.

REFUSAL HYGIENE: all five XObject /name echoes (sheet_interpreter.py:553,561,567,573,608) and the unsupported /Filter name (pdf_object_streams.py:656; sheet_objects.py:164) are _preview-truncated (64 chars + "...(truncated)"); every other refusal detail echoes only fixed strings or bounded integers — no raw attacker text.

NO REGRESSION / SCOPE: classic content _decode_stream unchanged except the /Filter name is now _preview-wrapped (no-op ≤64 chars) and decoded_bytes defaults 0 → 62-case golden byte-identical (passed). Imports acyclic (facade→resolver→objects; sheet_objects imports neither back — grep-confirmed). Exactly the 6 allowed paths; zero new deps (stdlib zlib/dataclasses); unwired (no route/main.py). Resolver never raises (zlib.error→typed refusal; parse errors→_wrap_strict).

M5-T103 G3 VERDICT: PASS

END-OF-REPORT
