# M5-T037 — G5 security review return (VERBATIM; security-reviewer, read-only, pinned a6875731)

Orchestrator note: returned via the agent-return channel 2026-09-18T10:10:56Z; the transport
truncated the first delivery inside check (4). Part 1 below is the truncated delivery
verbatim; Part 2 is the resent remainder, verbatim. VERDICT: PASS (two INFO observations).

---

## Part 1 (first delivery, verbatim)

G5 security review complete. All documented suites pass (ruff clean; 77 + 40 tests green including the DB-020 digest regression and the additionalProperties rejection tests). Returning my full report.

---

# G5 Security Review — M5-T037 (rule_evaluation v1.1.0 wide-street screen wiring)

**VERDICT: PASS** — no critical/high/medium/low security defects. Two INFO observations, neither blocking.

## Scope & integrity verification
- **Pinned material commit `a6875731`** (16 files) reviewed via `git show`.
- **Allowed-paths byte-stability `a6875731..HEAD` CONFIRMED**: `git diff --name-only a6875731 HEAD` over all 16 material files returns empty. The only changes after the material commit are control-plane (gates/reports/tasks/state) and the single sanctioned out-of-scope guard `services/api/tests/scenario/test_scenario_contract.py` (enum admission, `be0a7062`). No material source changed.
- **Both schema copies byte-identical at `a6875731`** (sha256 `ed9931fc…1970ea` for both canonical `packages/contracts/schemas/v1/` and bundled `services/api/app/_contract_schemas/v1/`).
- Independent re-run (documented read-only commands, `services/api` cwd): `ruff` clean on the three server modules; `tests/spatial/test_wide_street_live_provider.py` + `tests/rules/test_rules_integration.py` = 77 passed; `tests/api/test_rule_evaluation_api.py` = 40 passed.

## The five focus checks

**(1) Information disclosure via the new client-bound block — CLEAN.**
The serialized `wide_street` block is exactly `dict(self.wide_street_determination)`, built by `_wide_street_summary` (`services/api/app/rules/integration.py:552-573`). Its keys are only the D-052 provenance summary: `determination_state`, `far_row`, `governing_max_residential_far`, `coverage_hint`, `exceptions_checked`, `named_street_override_pending`, `policy_decision_states`, `original_labels`, `source_versions`, `matched_geometry_refs`, `interpreted_bounds_summaries`, `classification_reasons`, `draft_label`, `fallback_direction_note`, `reason`. No raw upstream bodies, no internal URLs, **no `correlation_id`** (it stays a server-side fetcher argument, `wide_street_live_provider.py:339`), no secrets. `matched_geometry_refs` (OBJECTID=…) and `source_versions` (source last-edited stamps) are public open-data identifiers — within the sanctioned provenance-disclosure surface.

**(2) Validation boundary — CLEAN.**
Server validate-before-send is unchanged and still enforced: `validate_rule_evaluation_document` (`response.py:207`) is called by all four consuming routes before send (`evidence.py:518`, `rule_evaluation.py:346`, `scenario.py:277`, `scenario_analysis.py:535`) — none of those route files are in the diff. The new block therefore passes the same strict `Draft202012Validator` against the bundled schema. Error surface keeps details server-side and returns a generic typed error (`response.py:207-224`) — good log redaction. Web validator `checkWideStreet` (`rule-evaluation-contract.ts`) shape-checks every documented key and, on any problem, returns `{ ok: false }` — **TOTAL failure, no partial typed document** (`validateRuleEvaluationDocument` early-returns on `problems.list.length > 0`). No `dangerouslySetInnerHTML`/`innerHTML`/`eval` in any display component; all block fields render as auto-escaped React text or via `CapturedRecord` (`EvidenceRecord.tsx:9-16`, `JSON.stringify` inside `<pre>`, no hrefs).

**(3) Contract integrity — CLEAN (hunk-by-hunk).**
Root `additionalProperties:false` intact; new `$defs/wide_street` has `additionalProperties:false`; version enum grew only `["1.0.0"] → ["1.0.0","1.1.0"]`. Nothing else in the schema weakened. `test_rule_evaluation_api.py` explicitly asserts undocumented keys are rejected both **inside the block and at the root, on both schema copies**.

**(4) DB-020 digest swap — CLEAN.**
`source_raw_digest` now carries `page.raw_digest` (geometry-page raw body sha256) instead of the free-text `streetwidth_raw` (`wide_street_live_provider.py:339-361`). `raw_bod[TRANSPORT TRUNCATION POINT]

---

## Part 2 (resent remainder, verbatim; delivered 10:11:34Z — report COMPLETE)

Resending from the truncation point.

---

**(4) DB-020 digest swap — CLEAN (remainder).**
`source_raw_digest` now carries `page.raw_digest` (geometry-page raw body sha256) instead of the free-text `streetwidth_raw` (`wide_street_live_provider.py:339-361`). `raw_body_digest` returns opaque `"sha256:"+hexdigest` (`dcm_street_centerline_arcgis.py:213-215`). Critically, `source_raw_digest` is **not surfaced into the client block at all** — no reference to it exists in `app/rules/` or `wide_street_wiring.py`. It is an internal provenance-correctness field on `AttestedWideSegment`; only its routing changed, so nothing new can leak to clients. The regression test asserts the value is sha256-shaped and `!= "80"`.

**(5) Secrets / outbound / storage / report parity — CLEAN.**
Full-diff scan: no keys, service-role material, tokens, `.env`, or internal/localhost/Supabase/Render URLs. No new outbound calls (connector fetchers are unchanged in behavior). No `localStorage`/`sessionStorage`/cookie/IndexedDB anywhere in the web diff. Report parity is achieved by the **same shared components** — `ReportView.tsx` is unchanged and already embeds `DevelopmentLimits` and `CalculationEvidence` from the same `evaluation` document (`ReportView.tsx:68,92`); there is no forked data path, so the print/report surface leaks nothing beyond the on-screen document.

## Broader checklist
Cross-tenant isolation: no auth/RLS/DB-query/tenant-scoping surface touched; serialization is pure per-request. Service-role secrecy, private storage, upload controls: not touched. SSRF: no new URL is fetched from block/user data; the only `href`s are internal (`propertyHref(bbl,"evidence")`) or pre-existing. Prompt-injection: the block is deterministic policy output rendered as escaped text — never fed to an LLM; even attacker-influenced upstream label text (`original_labels`, `source_versions`) is inert in the DOM (React escaping / `<pre>` JSON). Least privilege / draft preservation: tests confirm no `verified` value leaks into the block or coverage (D-045-R009 intact). Log redaction: the schema-validation error path keeps the detailed message server-side and returns a generic typed error (`response.py:207-224`).

## INFO (non-blocking, no action required for this task)
- **INFO-1** — The web validator (`checkWideStreet` / `validateRuleEvaluationDocument`, `rule-evaluation-contract.ts`) is a positive documented-key shape check and does not itself reject *undocumented extra* top-level/block keys client-side. This matches the pre-existing 1.0.0 validator design and is not a regression; the server schema enforces `additionalProperties:false` before send and the components only read documented keys. Defense-in-depth only.
- **INFO-2** — Pre-existing `CalculationEvidence.tsx:113` renders `officialZoningTextUrl((citation.provenance).request_url)` as a `target="_blank"` link. This was neither introduced nor altered by this task, and no `wide_street` block field feeds any href. Flagged only for visibility; out of scope.

**VERDICT: PASS** — no critical/high/medium/low security defects; allowed-paths byte-stable `a6875731..HEAD`; the two INFO items are advisory and do not block acceptance.

---

## Delta-attestation (VERBATIM; delivered 10:20:22Z — rework 75daaebd → head 8240a497)

Confirmed. `evaluation` (line 30) is the strictly-gated form — `evaluationIsInspectable(identityEvaluation) ? identityEvaluation : null` — so F1 is a tightening.

## Delta-attestation — M5-T037 rework 75daaebd → head 8240a497

**My G5 PASS carries to 8240a497.** These are display-only reductions; no new security concern.

Verified read-only:
- **Scope**: `git diff a6875731..8240a497` over `apps/web/**`, `packages/**`, `services/**` touches only `DevelopmentLimits.tsx` (9), `ReportView.tsx` (6), `report-view.test.tsx` (54 test-only), plus the already-reviewed sanctioned `test_scenario_contract.py`. **No api/schema/serialization change** — the server contract, `_wide_street_summary`, validate-before-send, and both schema copies are byte-identical to what I passed. 75daaebd is an ancestor of 8240a497.
- **F1 (`ReportView.tsx:72`)** — swaps `identityEvaluation` for `evaluation`, where `evaluation = evaluationIsInspectable(identityEvaluation) ? identityEvaluation : null` (line 30). This is strictly more restrictive: a non-inspectable document now passes `null` to `DevelopmentLimits`, so the report withholds the wide-street panel instead of showing it (and now matches both the screen and the `CalculationEvidence` feed at line 96). **Reduces** the report-rendered surface.
- **F2 (`DevelopmentLimits.tsx:73`)** — removes three raw server-string renders (`wide.draft_label`, `wide.reason`, `wide.fallback_direction_note`) from the calm answer-first panel, replacing them with one static, hardcoded line. Those fields remain only behind the evidence disclosure via `CalculationEvidence` (the escaped-text / `CapturedRecord`-`<pre>` path already reviewed as safe). **Reduces** the primary-surface exposure of server-controlled strings (which, though already inert under React escaping, no longer appear on the calm surface at all).

No new HTML sink, href, outbound call, or storage is introduced; the remaining dynamic render is the numeric `farValue(...)` and static condition copy. Both edits reduce, not expand, the client-rendered surface. **Verdict unchanged: PASS at 8240a497.**


---

## Carry confirmation to 1c89922c (VERBATIM; delivered 10:23:10Z)

Verified: the only source-tree change in `8240a497..1c89922c` is `development-limits.test.tsx:563` (test-assertion casing aligned to the F2 clean line); no production source, schema, serialization, or security surface changed.

**G5 PASS carries to 1c89922c** — test-only casing alignment, no security-surface change.
