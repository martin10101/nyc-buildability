# M5-T118 — G3 code review (code-reviewer "cr-t118", read-only)

> Transmission history: pinned at a120220e (material 992c47b1), delivered as 2 SendMessage parts ending with
> END-OF-REPORT; no truncation. Joined verbatim by the orchestrator (transport wrapper tags removed only). Verdict PASS; A1 sheet_interpreter.py at 748/750
> (split before the next feature), A2 the dict-byte cap checked one token late (bounded).

---

PART 1/2 — M5-T118 G3 code review (READ-ONLY, code-reviewer)

PIN: HEAD = a120220e (matches "a120220e or later"; that commit touches only project-control report/evidence/state — no drawings source). Material = 992c47b1 (cherry-pick of wt-m5t118 9378890f); diff base d895f3c8 ≡ claim seam f2ada974 for all 9 files.

SCOPE (PASS): material commit 992c47b1 touches EXACTLY the 9 allowed paths; NO forbidden path. scene_api.py / export_service.py that changed in the wider range are peer commits M5-T117 / M5-T116, not M5-T118. pdf_object_streams.py, sheet_marked_content.py, app/documents/, main.py, api/, cad/ untouched by M5-T118.

AS-1 budgets (PASS): per-page counters reset in begin_page() (sheet_reader.py, called at each page in _build_page BEFORE decode_contents); document-wide op_count/point_count/decoded_bytes never reset. Both checked with DISTINCT typed refusals — operators run() sheet_interpreter.py:176-184 ("operator count"/"operator ceiling"); points _curveto 329-346 + _charge_points 360-374 ("path points"/"path point ceiling"); bytes sheet_objects.charge_decoded ("page decoded bytes"/"decoded bytes budget"); page checked FIRST each time. Doc ceilings = 100x per-page (MAX_DOCUMENT_OPERATORS 20M, MAX_DOCUMENT_PATH_POINTS 50M, sheet_reader.py); page decoded 32 MiB < doc 128 MiB (sheet_objects.py:259). No-bypass VERIFIED: the cubic flatten is per-call bounded by min(page_remaining,doc_remaining) AND charges both counters via _charge_points(len(out)) (352); lineto charges _charge_points(1) (306). No path where a page counter is skipped or a ceiling bypassed.

AS-2 sh (PASS): _execute 429-433 skips sh via a rebindable _SKIP_SHADING=frozenset({"sh"}) (line 72), increments page+doc counts, clears operands, never refuses; dispatched after Do, before _IGNORED; unsupported ops still hit _refuse("unsupported operator",...) (439). Only "sh" is captured — no accidental skip.

AS-3 inline images (PASS): _op_bi (~638) delegates to sheet_inline_image.skip_inline_image and resyncs self._pos = span.end_offset (past EI) so linework after the image still draws; counts; no sample byte decoded. Module: bounded dict parse (_parse_dictionary, max_dict_bytes), determinable length ceil(W*comp*bpc/8)*H (_determinable_length) vs bounded EI scan (_scan_for_ei, max_data_scan_bytes), whitespace/delimiter-anchored _ei_at (no "EIGHT" false match), typed refusals for no-ID / over-cap / length-mismatch / non-positive geometry / invalid BPC / nested array. EI-scan false positive is documented AND proven to fail into a downstream refusal, never a wrong drawing.

END PART 1/2

---

PART 2/2 — M5-T118 G3 code review (code-reviewer)

GOLDEN REVISIONS (exactly scoped, PASS): test_sheet_reader.py — two `sh`→`zz` probes (sh is now skipped, so it can no longer stand for "unsupported operator"; same assertion kept). test_sheet_reader_split_equivalence.py — only the refuse_unsupported_op content `sh`→`zz`, its per-case digest, and _OVERALL recaptured; the _GOLDEN diff changes NO other case. The report's recompute script confirms CHANGED=['refuse_unsupported_op']; the 272-passing suite proves the other 62 goldens byte-identical → the per-page/document budget split changed no other behavior.

sheet_primitives.py (PASS): SheetPage gains shading_skips/inline_image_skips (208-209) as defaulted-0 TRAILING fields (additive, positional-safe, byte-stable — a page with neither is byte-identical to pre-P3); SheetDocument adds read-only aggregate properties (224-231). This is the disclosed deviation; it is additive and within allowed paths.

TESTS (adequate, PASS): test_sheet_p3_features.py tests BEHAVIOR (polyline points, skip counts, refusal feature+detail), not structure. Every new guard has a red/green mutation in the CONSUMING namespace: begin_page→no-op (per-page operator AND decoded-byte resets load-bearing), _SKIP_SHADING=frozenset() (sh→unsupported), skip_inline_image refuse-all (whole doc refuses = old DB-055 d), and an under-consume desync that MUST become a SheetRefusal (asserts the real line = [0,0,10,10] that a desync would displace — satisfies the "assert the field the bug displaces" rule). Determinable, image-mask, scanned, array-filter, length-mismatch, over-cap, no-ID all covered.

CHECKS (all green, re-run by me): `python -m ruff check .` → All checks passed!; `tools/modularity_check.py --check` → failures 0; drawings suite → 272 passed via the 3.11 shim (incl. equivalence + forbidden test_pdf_object_streams.py). sheet_interpreter.py 748/750; sheet_inline_image.py 340. Zero new deps (stdlib + pdf_lexer + sheet_objects/primitives); unwired (no route/main/api change).

AS-4 honesty (report spot-check): 3/6 read fully (items 1,3,4); item 2 honestly refuses at a NEW deeper blocker ('l' with no current point); items 5,6 scan-only terminal; run1==run2 MATCH all six; copies deleted, nothing corpus committed (material commit carries no corpus bytes). No overclaim.

BLOCKING: none.

ADVISORY (non-blocking):
A1. sheet_interpreter.py is at 748/750 and flagged by modularity as a WARNING-level review signal ("consider the module boundary before growing it further"). It PASSES now, but the next feature landing here will fail CI — plan a split (e.g. extract the path/text operator handlers) before then. Team-lead/producer already noted "a split soon?".
A2. Minor/bounded: _parse_dictionary checks max_dict_bytes at the loop TOP (before lexing), so one pathological token (e.g. a long string value) is lexed in a single lex_primitive call and caught only on the next iteration — effective bound is "cap + one token". Fully bounded because `data` is an already-decoded content stream capped at 8 MiB/32 MiB; not unbounded, not blocking. Real inline-image dict values are small scalars.

M5-T118 G3 VERDICT: PASS

END-OF-REPORT
