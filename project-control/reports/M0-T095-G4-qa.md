# M0-T095 — G4 independent QA review (verbatim reviewer return)

Saved VERBATIM by the orchestrator from the read-only `qa-engineer` agent's return
channel (transport entity-decoding only). Review conducted at frozen identity
`5974f42b468f4565460914ce3c9413834cc42000`; delta attestation at corrected identity
`a3030ba6c907475436cebcf16936375d4e004fb4` appended below.

---VERBATIM-RETURN-BEGIN---
VERDICT: PASS

# G4 QA Gate Report — M0-T095 "D-024 G: root-cause repair gate + GitHub effect integration"

- **Task:** M0-T095 (governance; D-024 in-regime, `directive_refs: D-024:ALL`, 46 applicable reqs)
- **Reviewer role:** qa-engineer, read-only (ADR-005). No files modified; no git-write/gh/project_control.py run.
- **Reviewed identity:** HEAD `5974f42` on `control/D-024-fable-codex-loop` (deliverables `1e86670`, report `cef5ded`, evidence map `068bdbd`).
- **Verification target:** the `ctl24` integration checkout (confirmed at `5974f42` via `git worktree list`). My own dispatched worktree is stale (`d8b3899`); all commands below were run against `ctl24`.
- **Scope of this gate:** QA — matrix completeness (16.6 T1–T9, 16.8 E1–E14), prove-first register integrity, test quality, packet-output delivery, and reproducibility of producer evidence claims. (Directive-compliance per-requirement PASS is the separate `directive-compliance-verifier` pass; not duplicated here.)

## Commands run and observed output

| Command (cwd = ctl24) | Observed |
|---|---|
| `python -m pytest tools/test_agent_supervisor_repair_gate.py -q` | **78 passed in 0.25s** |
| `python -m pytest tools/test_agent_supervisor_github_flow.py -q` | **76 passed in 1.12s** |
| `python tools/modularity_check.py --check` | **selected 321 files; failures 0; warnings 8** (one is `review_signal: tools/agent_supervisor/repair_gate.py — above the warning threshold`) |
| `ruff check tools/agent_supervisor/repair_gate.py` | **All checks passed!** |
| `ruff check tools/test_agent_supervisor_repair_gate.py` | **All checks passed!** |
| `python tools/modularity_check.py --report` (filtered) | repair_gate.py flagged `review_signal — above the warning threshold` (warn=600, below justify=750) |
| line count (`python -c ...`) | repair_gate.py physical=826, non-comment/non-blank=**625**; test file physical=878, non-comment=653 |

Not run (explicitly out of scope / externally-killed-background risk): the full 56-file supervisor suite (2563/0), the mutation pass (12/12), and any `gh` call for CI status. These are relied on from CI-green-at-`068bdbd` per the task packet and cross-checked structurally where possible.

## 1. 16.6 matrix T1–T9 (D-024-R112) — FULLY IMPLEMENTED, matches frozen pack §1

Every row has a dedicated test class with **both directions per predicate** (reject + accept), and the Given/When/Then maps to pack §1:

| Row | Test class | Reject direction | Accept direction | Pack match |
|---|---|---|---|---|
| T1 patch-stacking | `T1PatchStackingTests` | `..._layer_around_a_known_bad_path_is_rejected` (loops all 6 `LAYER_KINDS`, asserts `patch_stacking`) | `a_justified_layer_...`, `a_layer_not_around_a_known_bad_path_needs_no_justification` | OK |
| T2 direct repair | `T2DirectRepairTests` | reproduce-first, missing root/boundary, uncharacterized preserved-behavior | `..._accepted_without_a_forced_rewrite` (asserts `direct_repair_mode`, "no broad rewrite"); falsifiable-condition path | OK |
| T3 bounded replacement | `T3BoundedReplacementTests` | each removal field False (loop) + missing unreachability evidence → `replacement_unproven` | `a_fully_proven_replacement_is_accepted` | OK |
| T4 stale callers | `T4StaleCallerTests` | `reachable_stale_callers_refuse...` (`stale_callers_reachable`, surfaces names); unconfirmed path | `resolved_callers_restore...` | OK |
| T5 regression binding | `T5RegressionBindingTests` | 4 sub-fields blank/False (loop) → `regression_test_unbound` | `a_bound_regression_test_passes` | OK |
| T6 compat fields | `T6CompatibilityFieldTests` | **one typed refusal per missing field** (loops `COMPATIBILITY_REQUIRED_FIELDS`, asserts exactly `[code]`); multi-missing | `a_complete_exception_yields_a_single_ok` | OK |
| T7 expiry blocks acceptance | `T7ExpiryTests` | expired ISO / undecidable / per-id milestone → `blocked` | current exception does not block; injected `now_utc`/`milestone_reached` only | OK |
| T8 unrelated deletion | `T8UnrelatedDeletionTests` | `deleting_unrelated_working_code_is_refused` | `a_fix_with_no_unrelated_deletion_passes` | OK |
| T9 checkpoint + disposition | `T9CheckpointTests` | each missing/blank/unknown question refuses; incomplete+PASS still rejects | closed 6-key set asserted; `complete_answers_never_auto_accept`; PASS→accept, FAIL/BLOCKED→reject, unrecognized→review_required (fail closed) | OK |

The R078 `CHECKPOINT_QUESTIONS` tuple is asserted equal to the directive's 6 questions (root_cause, old_logic_removed_or_covered, one_authoritative_path, failing_if_removed_test, wrapper_justification, retained_behavior_removal_plan) — matches pack T9. `Section166RegisterTests` additionally asserts all 9 cases map to a named test here.

## 2. 16.8 matrix E1–E14 (D-024-R114) — prove-first register verified

`Section168RegisterTests` lists all 14 cases and `test_every_16_8_case_maps_to_a_real_named_test` mechanically re-derives each citation against the cited file's actual source (breaks the build on a renamed/deleted proof) — this passed inside the 78/78 run. I additionally **spot-read the cited tests to confirm they genuinely prove the named case**, not merely that a name exists:

**Existing-proof citations — confirmed real and semantically correct:**
- **E1** FLOW `test_a_push_to_the_wrong_task_branch_is_denied` (`unauthorized_branch`), `test_a_remote_identity_mismatch_is_denied` (`remote_identity_mismatch`), `test_pr_creation_works_and_is_journaled` (identity + CONFIRMED journal). OK
- **E2** FLOW `test_direct_main_push_is_hard_denied` (`push_to_main`/HARD_DENY), `test_force_push_is_hard_denied` (`force_push`/HARD_DENY); POLICY citation file present. OK
- **E5** FLOW `test_crash_mid_push_leaves_a_pending_effect_reconciled_not_retried`, `test_reconciling_a_proven_merge_confirms_it_without_duplication` (RECONCILED_OCCURRED → CONFIRMED, no duplicate), `test_a_second_merge_attempt_on_a_pending_effect_does_not_re_fire` (`pending_effect_reconcile_first`, `merges == []`). Genuine crash/reconcile-not-retry proofs. OK
- **E6** existing halves FLOW `test_a_confirmed_push_is_not_pushed_again` (`already_pushed`), `test_a_repeated_begin_after_a_crash_reuses_the_same_action_id`. OK
- **E10** existing half FLOW `test_merge_refuses_when_not_authorized` (`not_authorized_or_dependency_invalid`). OK
- **E12** FLOW `test_merge_refuses_on_a_failing_required_check` (`required_check_failing`), `test_merge_refuses_when_no_required_check_ran` (`no_required_checks`). OK
- **E3/E4/E8/E9** cited files exist (`test_agent_supervisor_bounded_contracts.py`, `runtime_supervision.py`, `reviewer.py`, `policy.py`) and the exact named tests are present (grep-confirmed); register test enforces them.

**Gap cases — real new tests confirmed:**
- **E6** (PR-create idempotence): `E6DuplicatePrCreateTests` builds a real `gf.GitHubFlow` over `shadow_effects_journal(temp journal)` + an injected `_PrRunner`, calls `create_pull_request` twice → second `performed=False`, `reason_code=already_created`, `runner.prs==1`. Exercises the EXISTING `GitHubFlow` CONFIRMED guard (github_flow.py L843–846) through fakes — no new effect machinery. SHADOW-ONLY. OK
- **E7** (identity invalidation): `E7ReviewValidityTests` — change→`identity_changed_re_review_required`, unchanged→valid, blank→`identity_unknown` (fail closed). OK
- **E10/E11** (PR classification): `E10PreExistingPrTests` + `E11ClassificationTests` — closed 4-class vocabulary asserted; owner-hold precedence; current-task PR routes only `evaluate_via_github_flow`; stale flagged/never actioned; unknown age never stale; **every classification effect-free**; malformed snapshot refused; plus a cross-proof against the existing S5.5 gate. OK
- **E13** (freeze citation): `E13FreezeCitationTests` — uncited→both `missing_freeze_citation_packet/_commit`; packet-only still fails commit side; fully cited passes; non-supervisor path owes no citation; backslash path recognition. OK
- **E14** (consolidated round): `E14ConsolidatedRoundTests` — single-round valid; drip-feeding `drip_feeding`; no-move `correction_did_not_move_identity`; unaddressed `findings_unaddressed`; no-findings ok; blank identity fail closed. OK

`test_the_16_8_register_lists_all_fourteen_cases` asserts count = 14.

## 3. Test quality — PASS

- **Lean B6 one-invariant-per-test:** materially different invariants live in separate methods; `subTest` loops iterate over a *homogeneous* set of the same invariant (e.g., all `LAYER_KINDS`; all removal fields; all missing questions; per-missing-field refusal). No merged materially-different cases observed.
- **Deterministic fixtures:** fixed identities (`"a"*12`/`"b"*12`), fixed ISO deadlines, factory helpers with overrides. No randomness.
- **Injected clock/milestone/age; no wall-clock:** `repair_gate.py` imports only `dataclasses, re, typing, redaction, models.digest_of, policy` — no `datetime`/`time`. Expiry consumes `now_utc`/`milestone_reached`; staleness consumes `days_since_update`; undecidable → fail closed (raises / blocks). OK
- **E6 SHADOW-ONLY:** injected `_PrRunner` fake + `tempfile.TemporaryDirectory` journal via `shadow_effects_journal` (resolves from SHADOW specs, never the production registry). No subprocess/network. R595 untouched. OK
- Redaction wiring proven: `test_answers_are_routed_through_redaction` inserts a `ghp_…` token and asserts it is absent from the canonical-JSON packet and `[REDACTED…]` appears.

## 4. Packet outputs (M0-T095.json `.outputs`) — delivered

1. **Repair-gate review evidence + compatibility-exception tracking:** `checkpoint_section` builds a bounded, redacted, digest-bound section carried by `evidence.build_packet(extra_sections=…)`; `CheckpointSectionWiringTests` proves guard-admittance and packet carriage. Compatibility-exception tracking (all 7 fields + expiry-blocks-acceptance) delivered as `CompatibilityException` / `evaluate_compatibility_exception` / `evaluate_acceptance` and tested by T6/T7. OK *(See INFO-1 on where compat enforcement lives.)*
2. **GitHub effect-journal compliance verification:** the 16.8 prove-first register + E6 journal idempotence + citations into github_flow/external_effects. OK
3. **`tools/test_agent_supervisor_repair_gate.py` (16.6 + 16.8 incl. supervisor-freeze fixture):** delivered; E13 freeze-citation fixture present. OK
4. **`project-control/reports/M0-T095-repair-gate.md`:** delivered. OK

Modularity boundary answers (code-architecture item 6): one NEW focused module; single responsibility (Phase-G acceptance/review record protocols), pure policy mirroring `push_policy` (no I/O/persistence/effects). I concur it is not a dumping ground. It crosses the **warn** threshold (see LOW-1) and the required cohesion justification is recorded in report §4.3, satisfying item 6; `failures 0`.

## 5. Producer evidence claims (report §4.3) vs reproduction

| Claim | Result |
|---|---|
| New matrix 78/78 | **Reproduced: 78 passed (0.25s).** OK |
| modularity failures 0 | **Reproduced: failures 0 / 321 files.** OK |
| ruff clean on both new files | **Reproduced: All checks passed (both).** OK |
| "below the 600-SLOC warn threshold" / "no warning on either new file" | **Contradicted** — repair_gate.py (~625 non-comment SLOC) crosses the 600 warn threshold; checker emits `review_signal`. See **LOW-1**. |
| Full suite 2563/0 (3 chunks) | Not reproduced (out of scope). Relies on CI-green-at-`068bdbd`. See INFO-2. |
| Mutation 12/12 killed | Not reproduced (mutation out of scope). Described mutants map to real covered branches (e.g. M8 `>`→`>=` boundary is killed by `test_an_iso_deadline_compares_against_the_injected_now` at `now==deadline`; M7 unknown-verdict fail-closed killed by `test_an_unrecognized_verdict_fails_closed`). Plausible. See INFO-2. |
| CI all 20 checks green at `068bdbd` incl. supervisor-bridge | Accepted as orchestrator-provided; I cannot run `gh` (read-only). |

## Findings

- **LOW-1 (evidence accuracy; required correction, blocking for acceptance/G5):** Report §4.2 ("below the 600-SLOC modularity warn threshold") and §4.3 ("no warning on either new file") are inaccurate for `repair_gate.py`, which the checker flags with a `review_signal` warning (~625 non-comment SLOC; warn=600, justify=750). This does **not** change the gate outcome: `failures 0`, CI-relevant status is green, and the modularity policy's warn-threshold obligation (record WHY it stays cohesive) IS met by the §4.3 cohesion judgment. Correction: amend §4.2/§4.3 to state repair_gate.py crosses the warn (not justify/hard) threshold with a recorded cohesion justification. Provenance/accuracy is a permanent principle, hence flagged.
- **INFO-1 (design note, not a defect):** `checkpoint_section` embeds the repair-gate findings + R078 answers but does NOT embed the compatibility-exception register; compat completeness (T6) and expiry (T7) are enforced at the **acceptance seam** (`evaluate_acceptance`), which is the correct point for "expiry blocks acceptance." The objective's substance is met and tested; a reviewer expecting compat data literally inside the review packet should note enforcement is acceptance-time.
- **INFO-2 (verification limitation):** Full-suite 2563/0 and mutation 12/12 were not independently reproduced (whole-suite/mutation runs are out of the read-only sandbox scope and background python is externally killed on this box). I relied on CI-green-at-`068bdbd` and structural cross-checks. I also could not run `git status`/`git diff` inside `ctl24` (worktree-isolation guard blocks git into the shared checkout), so I verified the checked-out file content and assume it equals the reviewed SHA `5974f42` per `git worktree list`.

## Verdict

**PASS.** Both matrices are fully and correctly implemented (T1–T9 with both directions; E1–E14 via a build-enforced prove-first register whose citations I spot-read and confirmed genuinely prove their cases; gap cases E6/E7/E10/E11/E13/E14 have real, SHADOW-ONLY, injected-fact tests). Test quality (Lean B6, deterministic fixtures, no wall-clock, injected runner + temp journal) holds. All four packet outputs are delivered. Reproducible evidence (78/78, ruff clean, modularity failures 0) matches. The only accuracy defect (LOW-1, the report's modularity "no warning" wording) is a report correction that is blocking for acceptance but is not a code defect and does not affect the gate outcome; INFO-1/INFO-2 are non-blocking notes for the orchestrator.
---VERBATIM-RETURN-END---

## Delta attestation (corrected identity `a3030ba`) — VERBATIM

---VERBATIM-DELTA-BEGIN---
DELTA-VERDICT: PASS

**G4 delta re-attestation — M0-T095 at a3030ba (verified in ctl24 integration checkout, read-only)**

Verified against the shared object store (`git -C <own-worktree> diff 7c3af52 a3030ba`) and by re-running the suite in ctl24 (confirmed `git worktree list` → ctl24 at `a3030ba`).

- **Delta scope confirmed exactly 3 files** (report, G2 self-check, repair_gate.py); intervening `26db34b`/`7c3af52` touch no deliverable content, consistent with the coordinator note.
- **LOW-1 RESOLVED.** Report §4.2 now reads "625 SLOC — above the 600 warn signal, below the 750 justify and 1,000 hard thresholds; cohesion judgment recorded in §4.3"; §4.3 now states "failures 0; repair_gate.py emits a `warn review_signal` (625 SLOC > 600)" with a CORRECTION note explaining the prior "no warning" run predated `git add` (untracked file excluded from selection). This matches my reproduced result byte-for-byte (failures 0; review_signal on repair_gate.py; 625 non-comment SLOC). The inaccurate wording is gone.
- **Code change is behavior-neutral.** The only repair_gate.py diff is a 6-line docstring caveat on `checkpoint_section` (redaction-contract note; G5 INFO-1) — no executable line changed. The §4.4 Bootstrap Gate-0/MCP attestation is added report evidence, not code.
- **Regression re-run:** `python -m pytest tools/test_agent_supervisor_repair_gate.py -q` → **78 passed** at a3030ba. Matrices (T1–T9, E1–E14), prove-first register, test quality (Lean B6, injected facts, SHADOW-ONLY) are unchanged since no test or logic was modified.

**My G4 PASS verdict carries to identity a3030ba.** LOW-1 is closed; INFO-1/INFO-2 remain non-blocking notes. No new findings.
---VERBATIM-DELTA-END---
