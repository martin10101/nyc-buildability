# M5-T050 G5 — independent security review (security-reviewer, saved VERBATIM from the agent return)

G5 SECURITY GATE — M5-T050 (DB-033 confirm-arc polish + hardening) — VERDICT: PASS. No blocking findings; 2 non-blocking advisories. Pin verified HEAD=ee1b361c. Material bcfcc538 ancestor of HEAD; CI head 499ae642 ancestor of HEAD with all 8 material files byte-identical to HEAD (CI run 35440808156 all-success covers the reviewed surface). Production digests match source-sections; address-search.ts byte-identical to accepted T047 (2d3b6ecf…, no semantic drift). No new imports, no console/log of user values, no dangerous sinks, no secrets in the 8 files.

Originating T047-G5 riders CLOSED:
- A1 (length-bound test de-vacuation, rider e): CLOSED — over-length probes now carry a would-match NON-EMPTY feature so no_match is attributable to the length guard, plus exact-boundary 512 accepted / 513 refused; address-search.ts guard untouched.
- A2 (credentials omit + 422 json-safety parity, rider i): CLOSED — credentials:"omit" on the single fetch path (record-address.ts:211); malformed 422 body caught → typed unexpected_response (no throw); valid 422 message bounded via boundedText at byte parity with 502 (proven by four22===five02 test). Server-side lot_geometry.py 422 unchanged and remains ASCII-safe repr (original A2 was observation-only; not regressed).
- A3 (title/aria length bound, rider i): CLOSED — ENTERED_TITLE_ATTR_MAX_LEN=512, enteredTitle capped at 513 with truncation marker, applied to BOTH title and aria-label (AddressConfirmCard.tsx:134-137,222). Rider b aria raw-value now bounded; React attribute-escaping sound, no injection surface.

api riders f/g: lot_geometry.py docstring-only (no behavior diff); test_lot_geometry_api.py test-only. secret-scan green per ci-evidence at 499ae642, corroborated by grep.

Advisories (non-blocking): (1) visible bold text {enteredInput.trim()} is unbounded but is React-escaped self-input, no security surface; (2) server 422 still not routed through _assert_json_safe — unchanged, non-exploitable (repr ASCII-safe), client-side parity closes the practical concern.

Full gate report returned in my prior assistant message for you to save verbatim and record the gate.

[Orchestrator identity note at recording time: between the review pin ee1b361c and the gate-record head, the only commits are the M5-T051 contract-seam records (control-plane files + the new scenario derivation seeds) — disjoint from every M5-T050 allowed_path; the task's material identity is byte-stable, per the reproduced empty diffs.]
