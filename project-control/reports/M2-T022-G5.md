<!-- Reviewer return preserved VERBATIM by the orchestrator (report-preservation rule,
.claude/rules/project-control.md; transport entity-decoding applied: &lt; -> <, &gt; -> >).
Reviewer: independent security-reviewer subagent (G5), returned 2026-09-12, reviewed HEAD ec4b75dc. -->

All verification complete. Every probe passed; scope is clean; the route imports no network libraries and never reads the key. Here is my G5 gate report.

---

VERDICT: PASS

# G5 Security Gate Report — M2-T022 (address-resolution API endpoint)

- **Task:** M2-T022 — `GET /api/v1/address-resolution` over the internal flag-gated API in front of the M2-T021 Geoclient connector
- **Reviewed HEAD:** `ec4b75dce9f507c84e9db8504587b963d04866ae` (branch `candidate/D-024-mrl-option-b`)
- **Producer commit:** `e245b40b` (+ control-plane commits `8da312ff` evidence-map, `ec4b75dc` submit — verified below to touch only `project-control/**`)
- **Reviewer:** independent security-reviewer (did not author). Read-only (ADR-005). No pytest/ruff, no writes, no `project_control.py`.
- **Method:** static trace of every route→response/log path + in-process `TestClient` probes driving the REAL connector over injected transports with a words-only sentinel key, no network (egress seams blocked), nothing written to the repo. `PYTHONDONTWRITEBYTECODE=1`, `PYTHONPATH=services/api`.

## Scope integrity (packet item 8) — PASS
- Producer commit changed exactly the 4 allowed files: `services/api/app/api/v1/address_resolution.py` (new), `services/api/app/main.py` (router registration only), `services/api/tests/api/test_address_resolution_api.py` (new), `project-control/reports/M2-T022-producer-report.md`.
- `git diff --name-only e245b40b..HEAD -- app/connectors app/resilience app/config.py` → **empty**. Nothing under `app/connectors`, `app/resilience`, or `app/config.py` changed. The two later commits touch only `project-control/**`.
- Working tree is byte-identical to HEAD for the reviewed source files.
- `main.py` change is a single `include_router(address_resolution_v1_router)` plus a posture comment, matching the sibling internal routes exactly.

## Probes executed and observed results

### 1. Key containment across the new surface — PASS
- Route module contains **no** reference to `GEOCLIENT_SUBSCRIPTION_KEY` / `KEY_ENV_VAR` / `Ocp-Apim` / `os.environ` / `os.getenv` (grep clean; the only occurrence is the word "subscription key" in a docstring). The key exists solely inside the connector's call-time env read; the route never touches it.
- Drove success (G01 200), auth (real upstream 401 with body `{"detail":"gateway says no"}`), rate-limit (real 429 with hostile `Retry-After: "7\r\nSet-Cookie: x=1"`), malformed (200 non-address body containing `secretish`/`gateway says no`), and timeout through the REAL connector with sentinel key `TEST-SENTINEL-KEY-OBVIOUSLY-FAKE`.
  - Status mapping observed: 200 / 502 / 503 / 502 / 504 — matches `STATUS_STATE_MATRIX`.
  - **Sentinel key absent** from every response body, every header set, and captured caplog (positive control: transport observed the sentinel in its request header on every real-connector call; logs non-empty, 3330 chars).
  - **Upstream body text `"gateway says no"` absent** from all bodies/headers/logs — the connector-authored `message` (status only) is what travels, never the upstream body.
  - **No `Set-Cookie`** materialized from the hostile CRLF `Retry-After`.
- Error handler transports a **selective** field set only: observed error payload keys ⊆ `{error_type, message, connector_correlation_id, source_id, endpoint, retry_after}`. The connector `detail` dict is **NOT** transported wholesale: a planted `detail["leaky"]="WHOLESALE-MARKER"` and `detail["url"]=...street=SECRETPARAM` were both **absent** from the response. Only `retry_after` is extracted from `detail`, via the local guard.

### 2. Local Retry-After guard `_bounded_retry_after` — PASS
- Dropped (returned `None`) for all 14 hostile inputs: trailing `\n`, `\r\n`, 200 KB string, non-string `int`, `None`, nested dict, Arabic-digit confusables (`١٠`), `"7; DROP"`, RTL-override `‮7`, HTTP-date + newline, 33 digits, 33 alphas, tab, embedded NUL. Non-dict `detail` → `None`; missing key → `None`.
- Passed **verbatim** (never truncated) for valid `"7"`, a valid HTTP-date, and the 32-char boundary. Confirmed **drop-never-truncate**: a 33-char value returns `None`, not a 32-char slice.
- Nothing else from upstream headers reaches the response (only `retry_after` is ever considered, and only when it survives fullmatch + 32-char cap).

### 3. Injection surfaces — PASS
- Reflected text (`grc_message`/`grc2_message` carrying `<script>alert(1)</script>\r\nX-Injected: pwned`) arrives **byte-exact (JSON-encoded) in the body only**; it does **not** appear in any response header value, no `X-Injected` header materialized, and it appears in **no** log line from this module.
- `X-Correlation-ID` is server-minted `uuid4().hex` — matched `^[0-9a-f]{32}$`; body `correlation_id` equals the header. No request-derived value reaches it.
- Query params: oversized 10 KB street → **422 with zero transport calls** (connector length cap fires pre-network) and the value is not echoed; repeated params, array-style repeats, and CRLF/NUL in params produce no crash and no header injection; control chars are percent-encoded in the connector-composed URL (no raw `\r`/`\n`/`\x00`).
- SSRF: the route composes **no URL/host** — no network libs imported (`requests`/`httpx`/`urllib`/`http.client`/`socket` grep clean); the connector owns the static `ENDPOINT_URL`. No request-derived value reaches any URL/host in the route.

### 4. Flag gate — PASS
- Flag off → generic `404 {"detail":"Not Found"}` with **no `X-Correlation-ID`** header (byte-indistinguishable from an unmounted path).
- Gate runs **before** input handling: invalid input (`house_number=""`) with the flag off still returns the generic 404, not a 422.
- `app.openapi()` never lists `/api/v1/address-resolution` (`include_in_schema=False`); no `address` path present in either state.
- `internal_rule_eval_enabled` is strict-true-token: `{1,true,yes,on}` (case-insensitive, stripped) enable; `0/false/off/no/maybe/""/2/tru/true1` and absent all disable.

### 5. DoS / amplification — PASS
- Response is bounded on every path: the connector's ~180-field `raw_fields` blob is **not** transported (only 7 canonical fields, ≤7 source_facts, and the key-free provenance) — G01 success body observed at 7714 bytes; suggestions capped at 32 by the connector.
- `_assert_json_safe` runs both `json.dumps` forms with `allow_nan=False`; it raises on a lone surrogate and on NaN. A lone-surrogate success body is caught fail-closed → **500 static `internal_error`** with no surrogate/reflected leak (and an `X-Correlation-ID` present). An unexpected `RuntimeError("BOOM-SECRET-EXC-TEXT")` → 500 with the exc text **not** leaked. No cache/state: two identical requests produce two connector calls with distinct HTTP and connector correlation ids (S7).

### 6. Logging discipline — PASS
- All four `logger` calls in the module carry only status/state (fixed enum), correlation ids, and counts — never reflected text, never exception text. Empirically confirmed: the generic guard emitted only `address_resolution_v1 internal_error correlation_id=<hex>` with no `<script>`, no surrogate, and no `BOOM-SECRET-EXC-TEXT`.
- The generic guard deliberately logs **no `exc_info`/`format_exc`** (grep confirms neither in the module). Judgement: acceptable — losing the traceback is the correct posture for a surface handling untrusted reflected/upstream content; the HTTP correlation id ties the record to the connector's own log lines, and the assembly path adds `stage=assembly` to disambiguate. The M2-T021 row-15 frame-locals concern does not recur here.

### 7. Secret scan — PASS
- High-entropy/secret-shaped scan of both new files returns only docstring path text (false positives); the sole credential-like literal is the words-only sentinel. The one `# gitleaks:allow` (test line 353, `KeyMissingError("no key available", ...)`) suppresses a false positive on the word "key" in a taxonomy message string — **legitimate**, no credential present.

## Observations (informational — not blocking, no rework required)
- **O1 (observability trade-off, accepted):** the connector-call generic guard logs only a correlation id (no `stage` marker, no traceback), so a genuine internal defect on that path is diagnosable only via correlation with the connector's own logs. This is the correct security-over-observability choice given reflected/upstream content; noted for future debugging expectations.
- **O2 (trust-coupling, accepted):** on success the route transports the connector's `provenance` dict wholesale, and on failure the connector's `message` field. Both are within the M2-T021-G5-accepted key-free/body-free connector contract and were empirically clean here (sentinel and upstream body absent from the success body which includes provenance). This is a coupling point: a future connector change that put upstream body or the key into `provenance`/`message` would be transported. The `retry_after` field is correctly the one value re-bounded locally. No action now; flag for any future connector-contract change.

## Verdict
No critical, high, medium, or low security defects. Key value and upstream body text cannot reach any response, header, or log across the full success/ambiguous/rejected/failure matrix; injection, SSRF, amplification, and flag-bypass surfaces are absent; logging is disciplined; scope is clean.

**VERDICT: PASS**
