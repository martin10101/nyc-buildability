# M5-T069 — G3 code review record (code-reviewer return, VERBATIM)

[Orchestrator record header — not part of the reviewer return: dispatched 2026-09-22 ~20:52 UTC
at frozen 141007c37c58c1a0e3d1297c582b015ca0f20bb1 (a second parallel code-reviewer-identity
instance, dispatched via a general-purpose agent because the named code-reviewer instance was
reviewing M5-T068 concurrently). The agent's stream stalled mid-review in the ~21:0x-21:29Z
provider stream incident (four of the wave's five reviewer streams died); it was RESUMED with
its context intact and finished cleanly. Return received COMPLETE in one part with the required
END-OF-REPORT marker. Verdict PASS; zero blocking; advisories A1-A4.]

---

VERDICT: **PASS** (G3 code review, M5-T069, frozen identity 141007c37c58c1a0e3d1297c582b015ca0f20bb1)

All file:line anchors are at the frozen identity. Content-identity chain verified first: material 463f98f9 touches exactly the 4 allowed_paths files; the 3 code files are byte-identical across material → frozen 141007c3 → CI head a66cf818 → live HEAD/working tree (all empty diffs, clean status under services/api), and 463f98f9 is an ancestor of the CI head — so my local runs exercised the frozen content.

## Blocking findings

None.

## Verified (numbered per review charge)

1. **Frozen order byte-preserved.** Only two commits touch `services/api/app/site_definition/store.py` in frozen history (4e1491f8 = T067 accepted, then 463f98f9), so `463f98f9^` IS the accepted T067 state; the diff against it contains only the new constant (store.py:72), replaced raise arguments, and comments. Order in frozen source: existence lookup :322→:328 (unified 404) → supersedes_id mismatch :329→:334 (unified 404) → reason guard :335 (position byte-unchanged) → foreign condo_key :345→:350 (unified 404) → status read :353 (409). No check reordered; `_record_insert`/`_current_status` untouched; 409 reachable only after the own-binding scope pass (proven by test at records-test :782).

2. **Unification.** Exactly five raise sites reference `_NOT_FOUND_FOR_ADDRESSED_PROPERTY` — supersede :328/:334/:350, revoke :428/:441 — all `ConfirmationNotFoundError`, no id echo in the constant (:72-75). Revoke's **raised text is byte-identical** to the T067 literal (the constant is that exact string); note the *source* lines were refactored from inline literals to the constant reference — see Advisory A1.

3. **No behavior change beyond messages.** Exception class (records.py:300) and `reject_code` untouched (records.py is a forbidden path, not in the commit); route mapping `ConfirmationNotFoundError: (404, "not_found")` at api/v1/site_definition.py:168 untouched; no new exception type; ordering unchanged → no new timing/type channel. The mismatch branch (fires only when the record exists) now responds identically to the missing branch — AS-2 option (a), proven at records-test :907-:908.

4. **Test quality: mutation-sensitive, no tautology.** `_UNIFIED_NOT_FOUND_TEXT` (records-test :853) is a test-local literal, NOT imported from the store. Single-branch divergent-literal mutants redden the pairwise equality (:878 or :907; api :874); constant-wide drift reddens the literal pins (:884, :907); id-echo mutants redden :878 (probe ids differ) plus :882/:883/:910/:911 and api :882/:883; api test also pins status/state/reject_code and full-body-minus-correlation_id equality (:867-:880) and refusal-mutates-nothing (:885). Consistent with the harvest's six-mutation matrix. Locally observed: `ruff check .` exit 0 and **77 passed** from services/api cwd, matching the [ORCH-HARVEST] counts.

5. **Consumer sweep clean.** git grep at the frozen rev for all three old supersede literals: the only remaining code hit is store.py:282 — `get()`'s pre-existing, different message (G5-INFO-1, out of scope). No test or client pins any old supersede text; `get()` is not called from any route (grep over app/api empty).

## Advisory (non-blocking)

- **A1** — The dispatch claim "revoke's branches byte-unchanged" is true of the *raised bytes*, not the source: revoke :422-:428/:437-:441 were refactored to the shared constant with comment updates. Within packet scope ("only unify message text"); T067's revoke tests pass unchanged; the refactor strengthens the invariant structurally.
- **A2** — No assertion pins *revoke's* raised text to `_UNIFIED_NOT_FOUND_TEXT` (T067's test :811 asserts only intra-revoke equality). Cross-op identity rests on the shared constant plus supersede's pin at :884. A future revoke-only literal fork would not redden any test. Suggest a one-line pin in a later test task.
- **A3** — The reason guard at store.py:335 sits between the mismatch and scope checks (`TransitionReasonRequiredError` for an existing+matching record vs 404 for a missing one, when reason is None). Unreachable via routes — `create_confirmation` requires the reason exactly when `supersedes_id` is set (records.py:602-604) — and the position is T067-accepted frozen order this packet forbids moving. Store-direct callers only; note for the G5 ledger.
- **A4** — `get()` (store.py:278-:284) still echoes the record id in divergent text; already tracked as G5-INFO-1 and not route-exposed at this identity.

END-OF-REPORT M5-T069-G3
