# M5-T015 — G0 administrative contract check (orchestrator)

Packet completeness verified against the /start-controlled-task checklist before claim:

- Requirement identifiers: docs/design/address-entry-confirm-design-spec.md sections 1-3, 5 and
  6-Packet-1 (the preserved product-design-director spec); the endpoint contract in
  services/api/app/api/v1/address_resolution.py (statuses, error matrix, escape checklist);
  MVP_AGENDA B3 / A2 row 4.
- Directive refs: D-038:ALL (in-regime; empty-applicable row expected at accept per precedent).
- Evidence files: named exactly in inputs[] (spec, endpoint, PropertyLookup/FailureState,
  bounded/announce, the compare test idioms).
- Scope: EXACT-FILE allowed_paths (four new address components/libs + announce.ts additive
  extension + PropertyLookup mount point + one test module + report); app router pages, shared
  api.ts/bounded.ts, package manifests, compare/confirm surfaces, and e2e all FORBIDDEN with a
  stop-and-rescope rule; dependency M2-T022 (accepted, 182nd) is the true prerequisite (the
  endpoint this UI consumes).
- Acceptance scenarios S1-S8: flag posture (byte-equivalent off-state, no fetch), stub handoff
  with warnings-beside-results, literal caller_selects (no preselection + re-resolve contract
  pinned from the connector suggestion shape), visible honest non-success with both GRCs,
  the COMPLETE error matrix one-scenario-per-pair, hostile-reflected-text render safety with a
  source-scan, state-machine integrity (supersession + D5 + focus + single announcement), and
  the no-new-dependency/no-new-token guard - executable via the CI web/web-e2e jobs, the only
  authority on the thin client.
- Gates: G0/G1/G3/G4/G5 with code-reviewer, qa-engineer, security-reviewer, and
  visual-quality-reviewer (a user-facing surface warrants the visual/interaction reviewer; the
  packet's reviewer_agents roster names all four; the human-journey walkthrough belongs to
  Packet 2 when the full flow closes). All reviewers distinct from the producer.
- Modularity boundary answers: each new component owns one responsibility (form / chooser /
  state-machine host / fetch mapping); the state machine clones the proven PropertyLookup shape
  rather than forking a new idiom; announce.ts grows additively; no file approaches thresholds;
  focused tests in the one new module; the CI web job is the lint/typecheck/build check.

G0 verdict: PASS (administrative; recorded by the orchestrator per gate-class rules).
