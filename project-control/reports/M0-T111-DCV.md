VERDICT: PASS

# M0-T111 Directive-Compliance Verification (DCV) — D-024 Amendment 8, unit L (one-way Telegram sink)

**Reviewed head:** `4ce8131d0f88fa0e2009a2a7049b4df2d6fcafb8` (branch `control/D-024-fable-codex-loop`; confirmed via `git rev-parse HEAD`)
**Deliverable content identity:** `c9b3b9a`; commits `c9b3b9a..HEAD` touch control-plane only (gates/reports/state.json/task file — verified with `git diff --name-only c9b3b9a..HEAD`), so the production-source identity under review is stable.
**Applicable set:** exactly 10 rows — each row's `applicability.task_ids` independently confirmed to include `M0-T111` (verified by parsing `requirements.json`).
**Verifier ≠ producer:** producer is `fable-orchestrator-session`; this is an independent read-only pass. No file/git/gh/CLI mutation performed. No live network send performed.

**Test pack reproduced:** `python -m pytest tools/test_agent_supervisor_telegram_sink.py -q` → **31 passed in 1.17s**. `python tools/validate_directive_compliance.py --check` → **EXIT=0**. `python tools/modularity_check.py --check` → **EXIT=0** (only pre-existing advisory warnings; neither new telegram module flagged).

## Requirement verdicts (grounded in primary evidence I personally reproduced)

| Req ID | One-line obligation | Verdict | Primary evidence I verified |
|---|---|---|---|
| D-024-R231 | Capture direction verbatim as amendment; reconcile before scope change | SATISFIED | `source-008-amendment.md` carries `---VERBATIM-BEGIN---`/`---VERBATIM-END---` (lines 41–105) + a reconciliation section (lines 11–29) + forward trace; owner report present; `validate_directive_compliance.py --check` EXIT=0 confirms source-digest/amendment integrity. |
| D-024-R232 | Two capabilities required before continuous-mode activation (hold) | HOLD-IN-FORCE-AND-HONORED | This unit delivers capability 2 (Telegram sink); no continuous-mode activation occurs anywhere in the diff. Campaign record `project-control/campaigns/D-024-fable-codex-loop.json` restriction: "Amendment 8 (D-024-R232/R247): … M0-T110 … + Telegram sink (M0-T111) must be ACCEPTED and M0-T112 must re-run the golden certification … BEFORE the R187/R595 activation package is presented; the M0-T096 activation-package document is NOT yet presentable." |
| D-024-R241 | Bounded sink for EXACTLY the eight listed conditions | SATISFIED | `telegram_sink.py` `CONDITIONS` (8-tuple) maps 1:1 onto the amendment's eight verbatim bullets (source lines 79–86): STOP_FOR_OWNER→`stop_for_owner`, approval waiting→`approval_waiting`, circuit-breaker/open stuck state→`breaker_open_stuck`, repeated CI failure→`repeated_ci_failure`, unrecovered controller/session failure→`unrecovered_controller_failure`, quota/refusal hold→`quota_refusal_hold`, golden-run completion→`golden_run_complete`, campaign completion→`campaign_complete`. `CONDITION_RISK` keyset == `CONDITIONS`; `notify_condition` raises typed `unknown_condition` for anything else. Tests `L1Conditions::test_all_eight_conditions_accepted...` (all 8 deliver) + `test_an_unknown_condition_is_a_typed_refusal` + `L8::test_R241...` (verbatim-bullet map) — reproduced green. |
| D-024-R242 | One-way only; no approvals/merges/execution/config surface | SATISFIED | Independent grep of both modules for `getupdates\|get_updates\|webhook\|long_poll\|offset\|receive\|inbound\|poll\|recv\|getme`: only match is the docstring on line 13 (honest doc, not functional). Transport POSTs `sendMessage` only (`method="POST"`, single fixed URL). `L4OneWay::test_no_receive_or_command_surface_exists` AST functional-source scan (docstrings excluded) — reproduced green. |
| D-024-R243 | Bot token + chat id only in approved local secret mechanism; never Git/packets/logs/telemetry/reports/messages | SATISFIED | `resolve_credentials` reads env-only (`SUPERVISOR_TELEGRAM_BOT_TOKEN`/`CHAT_ID`); `Credentials.__repr__/__str__` redacted; failure details carry status buckets/exception classes, never the token-bearing URL. Full-diff scan `git diff 6662b88..HEAD | grep -E '[0-9]{6,}:[A-Za-z0-9_-]{20,}'` yields ONLY the fake sentinel `0000000000:FAKE-sentinel-…` carrying `# gitleaks:allow secretscan:allow` pragma; chat sentinel `FAKE-chat-id-sentinel-887766`. `L3Secrets` 4 tests assert absence across outcome dict, queue/delivered/dedup rows, audit.jsonl, error messages, and CLI output — reproduced green. |
| D-024-R244 | Redaction, bounded retries, deduplication, failure isolation so downtime cannot stop the loop | SATISFIED | Composition only via S13.10 `build_notification` (redaction + leak-shape refusal + summary bound); `MAX_DELIVERY_ATTEMPTS=3` bounded; FIFO `DEDUP_KEY` register (`DEDUP_MAX_ENTRIES=64`); `deliver` never raises (`except Exception … never raise`); `notify_condition`/CLI ride `queue.deliver(..., unit_can_proceed=True)`. `L5Isolation` 6 tests (bounded-then-queued, raising-contained, dedup, FIFO-bound, `run_must_pause` structurally False via audit `notification_delivery_failed.run_must_pause is False`) + `L2ViewOnly` 2 tests — reproduced green. |
| D-024-R245 | Real Telegram send remains owner-gated exact-command canary (hold) | HOLD-IN-FORCE-AND-HONORED | `build_real_transport` raises typed `live_send_owner_gated` (naming D-024-R245 + exact command) unless `live_send_authorized=True`, supplied only by owner CLI flag `--live-canary-authorized-by-owner`. Grep of test file: the flag is never passed; the two `build_real_transport(live_send_authorized=True,…)` calls both inject a fake opener (`fake_opener`/`lambda`); no `socket`/`urlopen`/real network anywhere. CLI canary refuses without the flag (`test_cli_canary_refuses_without_the_owner_flag`). Live send NOT fired by this unit. Exact command documented in `LIVE_CANARY_COMMAND`. |
| D-024-R246 | Do not broaden M0-T096; determine + durably capture safest bounded sequence | SATISFIED | Durable packets `project-control/tasks/M0-T110.json`, `M0-T111.json`, `M0-T112.json` all exist; owner report `## 2. Proposed bounded task sequence (captured durably as ledger packets)`. M0-T096 packet untouched by this diff. `L8::test_R246...` — reproduced green. |
| D-024-R248 | No continuous-mode/4.8-bridge/PR-241/Agent-SDK/MCP/global-settings; no other owner boundary | SATISFIED | `git diff --name-status 6662b88..HEAD`: touches only `tools/agent_supervisor/{cli.py,telegram_sink.py,telegram_sink_cli.py}`, `tools/test_...telegram_sink.py`, and control-plane files. No `.claude/hooks`, no `.claude/settings.json`, no dependency manifest/lockfile, no forbidden path. Imports are stdlib-only (`dataclasses,os,urllib.parse,urllib.request,typing,argparse`) + relative package imports; no `requests/aiohttp/httpx`. `L8::test_R248_no_prohibited_surface` — reproduced green. |
| D-024-R249 | First report answers the five numbered items, then proceed | SATISFIED | `project-control/reports/D-024-amendment-8-owner-report.md` present with headings `## 1.`–`## 5.` (already-authorized-vs-amendment; bounded sequence; components reused; exact Claude Code limitation; certification to rerun). `L8::test_R249...` — reproduced green. |

## Special-attention items (explicitly re-derived)

- **R241 one-to-one mapping** — confirmed directly against the verbatim block (source lines 79–86 = 8 bullets) → `CONDITIONS` closed 8-tuple with matching `CONDITION_RISK` keyset; anything outside is a typed refusal. No missing, extra, or defaulted condition.
- **R242 no functional receive surface** — confirmed by independent grep + AST scan; transport is outbound `sendMessage` POST only.
- **R243 no committed secret** — the only token-shaped string in `git diff 6662b88..HEAD` is the obviously-fake, pragma-marked sentinel; leak-absence tests genuinely assert absence across result objects, journal-state rows, audit log, error messages, and CLI output.
- **R245 live send never fired** — no socket in any test; owner flag never passed; real transport construction refuses without owner authorization; canary path unexecuted.
- **R248 no prohibited surface** — `.claude/hooks` verified untouched; no dependency manifest; stdlib-only.

## Discrepancies between producer's evidence map and my findings

No material discrepancies. Every primary obligation the map claims is reproduced by the source, git objects, and tests I re-ran. Notes:

1. **Supplementary, non-reproduced claims (not load-bearing).** The map asserts "CI 20/20 green at c9b3b9a", "Both secret scanners green (gitleaks + CI credential scan)", and specific mutation-kill IDs (M1–M11). I did not re-execute CI, the external scanners, or the mutation run in this read-only pass. Each underlying obligation is independently proven by evidence I did reproduce (diff-level secret absence + pragmas; the 31-test behavioral pack), so these remain strengthening claims, not the basis of any verdict.
2. **Observation, not a defect.** The R245 owner-gated canary (`cmd_telegram` canary path) composes a fixed diagnostic notification with `reason="owner_live_canary"`, which is outside the eight-condition vocabulary. This is the R245 canary mechanism itself — owner-gated and never fired — and does not weaken R241's closed operational vocabulary, which is enforced at the `notify_condition` entry point. No action required.

**Conclusion:** All 10 applicable requirements are SATISFIED or HOLD-IN-FORCE-AND-HONORED on reproduced primary evidence. No VIOLATED and no UNVERIFIABLE result. **VERDICT: PASS.**

*(Saved verbatim from the reviewer's return by the orchestrator.)*
