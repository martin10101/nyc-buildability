# M0-T024 — G4 control-plane verification (independent, read-only)

Reviewer: control-plane-verifier (!= producer orchestrator). Round 1 head: `5d7c0fe` (PR #98).
Verbatim verdict + findings as returned by the independent reviewer.

**VERDICT (round 1): FAIL** — one mechanical violation; all other checks CONFIRMED.

1. CONFIRMED — Lifecycle. G0 role=administrative/orchestrator PASS; G2 role=self_check/orchestrator PASS (legal classes). Task awaiting_gate, producer_agent=orchestrator, required_gates G0/G2/G3/G4/G5, reviewer roster excludes the producer.
2. CONFIRMED — Separation. D-002 verification.json producer=orchestrator, task verifier rows empty/pending (independent gates fill them).
3. CONFIRMED — Ledger. accepted=43; test_project_control.py 14 groups + hardening pass; validate_directive_compliance.py --check exits 0; D-001 + D-002 both active, structurally valid.
4. CONFIRMED — Packets. evaluate_task_refs ok for all 9 (M0-T024=40 CTRL; M3-T001/M4-T007/M2-T017=14 PROD each; ported/backlog=0 applicable, correctly scoped). All cite D-002, regime v1.0. No mojibake.
5. **VIOLATED (round 1)** — Blocker wiring. `_blocker_references('M3-T001', B-011)` returned True: B-011 `detail` embedded bare word-bounded tokens `M3-T001` ("drafted by M3-T001") and `M3-T004` ("Renumbered from M3-T004..."). Since accept() scans open blockers over affects AND detail, open B-011 would REFUSE acceptance of first-wave task M3-T001, and spuriously couple M3-T004.
6. CONFIRMED — Master plan (M3 chain; M4-T007=Decimal; footprint superseded); exactly one active SESSION_HANDOFF.
7. CONFIRMED — Authority. Diff = project-control/ + docs/SESSION_HANDOFF.md only; no product code; two-lane provenance; acceptance via CLI.

**Remediation (applied by orchestrator; re-gate at corrected head):** reword B-011 affects/detail so no bare `M3-T001`/`M3-T004` token remains and only `M3-T005` is matchable, or amend the invariant. → Applied: B-011 reworded; re-verified `_blocker_references` = False for M3-T001/M3-T004, True for M3-T005 only.
