# M5-T093 - G2 producer self-check record (recorded by the orchestrator, role self_check)

Producer: backend-engineer (orchestrator-dispatched subagent) in wt-m5t093; one in-worktree commit b12a0d0d (parent 6cc93092), cherry-picked to ab2a6730 with both files MATCH by git blob identity.

- **Scope:** exactly the research note and the producer report; no app/**, reader, script, fixture or corpus file committed; the six downloads were deleted from the producer's scratch subfolder.
- **Producer evidence (report, OBSERVED):** six fetches HTTP 200 with sha256 MATCH against the corpus note; an independent stdlib byte walk (+ zlib inflation, corroborated by a second parser) for version, producer, pages, xref table vs stream, object streams, shading, inline images, encryption; read_sheet run twice per file through the bare-package 3.11 shim pointed at the worktree - identical refusals.
- **Result:** the reader reads NONE of the six real files (cross-reference stream refusal, 6/6); object streams are a co-requisite; shading and inline images are latent next blockers; prioritised C1 scope recorded.

G2 verdict: PASS (self_check; G1 data-contract-verifier, G3 code-reviewer and the DCV follow).
