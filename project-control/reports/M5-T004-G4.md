# M5-T004 — G4 Integration and Regression Gate

**Task:** M5-T004 — Compare (Step 3) UI
**Reviewed SHA:** `84815a7616a2519f499777cb52d283f1c26f1d4b`
**Reviewer role:** independent reviewer (read-only; did not author the code)
**Reviewed at:** 2026-09-11

## VERDICT: FAIL

Two HIGH findings. Scope discipline and regression safety are both clean; the failure is on the two items G4 exists to catch — duplicate implementation, and a validator hole behind the AS-4 gate.

---

## Summary

Verified by diff that **no forbidden path was touched**: `apps/web/src/lib/api.ts`, `contract.ts`, `contract-matrix.ts`, `validate-profile.ts`, `bounded.ts`, `components/property/**`, `components/confirm/__tests__/**`, `services/**`, `packages/contracts/**` are all unmodified. The `ConfirmScreen` rewire does **not** break any existing confirm test or e2e spec. Both CI jobs would almost certainly go green.

It fails on: (1) `scenario-api.ts` being a mechanical clone of the existing `rule-evaluation.ts` client, and (2) the client contract validator letting an invalid `cap_provenance` through the AS-4 "validate before render" gate.

---

## Findings

**1. HIGH — `scenario-api.ts` is a rename-clone of `rule-evaluation.ts`; a matrix fix to one will silently not reach the other.**
`apps/web/src/lib/scenario-api.ts:53-359` vs `apps/web/src/lib/rule-evaluation.ts:102-403`. Not "similar code" — line-for-line identical with identifiers renamed:
- `scenario-api.ts:175-186` `DOCUMENTED_SCENARIO_PAIRS` and `rule-evaluation.ts:222-233` `DOCUMENTED_RULE_PAIRS` are the **same ten pairs in the same order**
- `:192-194` `isDocumentedScenarioPair` ≡ `:239-241` `isDocumentedRulePair`
- `:202-206` `asRecord` ≡ `:249-253` `asRecord` (also ≡ `api.ts:156-160`)
- `:213-359` `fetchScenario` ≡ `:260-403` `fetchRuleEvaluation` — same AbortController/timeout wiring, same `timedOut` flag, same inner try/catch, same eight classification branches, same `finally`
- the twelve outcome interfaces and `scenarioOutcomeIsRecoverable` (`:155-165`) ≡ `ruleOutcomeIsRecoverable` (`:202-212`)

`docs/GATES_AND_CHECKPOINTS.md:83` lists "No duplicate or contradictory implementations" as a G4 item.

*Concrete drift scenario:* `apps/web/src/lib/contract-matrix.ts:48` **already** carries a pair the other two clients do not — `(500, "unsupported_contract_version")`. The day `services/api/app/api/v1/scenario.py:96-108` adds a pair (or `rule_evaluation.py` does), whoever fixes one client has no mechanical reason to find the other; the un-fixed client classifies a documented, recoverable server response as `unexpected_response` and shows the analyst "Unexpected response from the platform API" with a Retry that can never succeed. This is now the **third** copy of the pattern (`api.ts` → `rule-evaluation.ts` → `scenario-api.ts`).

*Fix:* extract the transport shell — `asRecord`, the pair-key set builder, the AbortController/timeout/`aborted`/`client_timeout`/`network_error` envelope, and the state→outcome dispatch — into one parameterized module (e.g. `apps/web/src/lib/envelope-client.ts`) taking `{ pathSuffix, documentedPairs, validate, copy }`, and have all three clients call it.

**ORCHESTRATOR NOTE — not a rework item for this packet.** The packet's `allowed_paths` did **not** cover `rule-evaluation.ts`, so the producer could not have done this in scope. Recorded as a separate orchestrator-scoped follow-up task. M5-T004's rework must not be held on it.

**2. HIGH — the AS-4 contract gate does not validate `cap_provenance`, so a maximum value can render with an empty optimized-objective name.**
`apps/web/src/lib/scenario-contract.ts:339-341` and `:350`; consumed at `apps/web/src/components/compare/ScenarioCard.tsx:70-99`.

**What the schema requires** (parsed, not eyeballed): `scenario.schema.json` `$defs.cap_provenance` declares `required = ["rule_id","rule_version","rule_status","output_name","citations","note"]` with `additionalProperties: false`. The top-level document likewise declares `additionalProperties: false` with 16 required keys. The generated type (`packages/contracts/generated/scenario.ts:34-41`) makes all six non-optional; `rule_status` is a 4-value enum.

**What the validator actually accepts**, in its entirety:
```ts
if (!(body.cap_provenance === null || isRecord(body.cap_provenance))) {
  problems.add("cap_provenance", "must be an object or null");
}
```
Not one of the six required fields is examined. Then `:350` returns `{ ok: true, document: body as unknown as Scenario }` — an unchecked double cast handing the caller a value typed `CapProvenance` that was never verified to be one. **The type lies.**

**Concrete failure.** A 200 body carrying `draft_zoning_floor_area_cap_sq_ft: 15000`, a valid `cap_label`, and `cap_provenance: {"note":"tbd"}` passes `validateScenarioDocument`, so `scenario-api.ts:288-298` returns `{kind:"scenario"}` and the screen renders:
- `ScenarioCard.tsx:50-52` — "Draft maximum residential zoning floor-area cap: **15,000 square feet**"
- `:75-77` — `<code data-testid="scenario-objective-name">` **EMPTY** (React renders nothing for `undefined`)
- `:83-86` — "Rule: *(empty)* (version *(empty)*, status *(empty)*)"

The analyst sees a 15,000 sq ft maximum with **no objective naming what was maximized and no rule identifying what produced it**.

**The rule it violates:** `.claude/rules/frontend-web.md:13` — *"Never show 'best' without naming the optimized objective."* `ScenarioCard`'s own header comment at `:16-17` asserts the opposite is guaranteed — "the optimized objective NAMED from `cap_provenance.output_name` (the UI rule forbids showing a 'best'/max value without naming the objective)" — so the intent was right and only the enforcement is missing. Also note `{}` is truthy, so `ScenarioCard.tsx:70` takes the **populated** branch rather than the honest "No cap provenance is attached" fallback: silent degradation, the worse failure mode.

**Why this is an AS-4 defect specifically.** AS-4 promises every 200 body is runtime-validated against scenario@1.0.0 before any render, precisely so a malformed-but-plausible payload cannot reach the screen. **The ordering is right; the depth is not.** This is defence-in-depth against a server bug — and the server has `internal_contract_error` for the same reason, so "the server validates too" is not a reason to omit it.

**Structural, not a one-off.** The same file also (a) never enforces the schema's top-level `additionalProperties: false`, and (b) omits `assumptions[].value` and `assumptions[].unit` at `:232-253`, both schema-required. Contrast `checkConstraints` at `:196-230`, which *does* check all seven required constraint keys — so this is an omission in an otherwise careful validator, not a deliberate tolerance.

*Interaction:* G5 independently found the same class (LOW) — the validator never rejects unknown keys despite documenting a "documented key set" check. G1 reached it from field presence, G4 from validator depth, G5 from key discipline: **three methods, one root cause — the validator claims guarantees it does not implement.**

*Complements DCV rather than duplicating it:* DCV CRITICAL-1 is that `cap_provenance.citations` are never **rendered**; this is that `cap_provenance` is never **validated**. Different layers, same field, and they compound — a cap can presently reach the screen with neither a verified provenance nor a displayed citation.

*Fix:* add `checkCapProvenance(problems, body.cap_provenance)` — when non-null, require non-empty `rule_id` / `rule_version` / `output_name` / `note`, `rule_status` within its 4-value enum, and `citations` as an array; and assert `cap_provenance !== null` whenever `draft_zoning_floor_area_cap_sq_ft !== null`. Add the missing `assumptions` checks in the same pass.

---

**3. HIGH — the data-conflict path states something false, on a path the suite actually renders.**
For `no_scenario_conflict.json` the screen prints *"Official sources disagree; both values are shown, nothing was resolved"* **twice** — `ScenarioResult.tsx:170` (result header) and `CoverageMatrixSection.tsx:68` (legend), both from `coverage.ts:35-38` — while rendering **neither** competing value. `ScenarioResult.tsx:178-182` routes non-preliminary to `NoScenarioBlock`, which renders only `reasons` (`:50`) and share ranges (`:30`); that fixture's `zoning_district` provenance is null, so the share block is skipped too. **The values are in the document and dropped:** `constraints[0]` `residential_far_cap` state `"conflicting"`; `constraints[2]` `zoning_district` state `"conflicting"`, value `"R5"`. Root cause is the dead `ruleConflict()` at `scenario-display.ts:126-153` — zero consumers repo-wide, and because it is exported, `no-unused-vars` never fires. The test rendering this fixture (`compare-screen.test.tsx:93-98`) asserts only that a testid exists, so it cannot catch it. Confirms DCV-6, traced end to end.
*Fix:* wire `ruleConflict()` into `NoScenarioBlock`, or stop printing a gloss promising a display the screen does not provide.

**4. MEDIUM — the two render branches are disjoint; neither ever shows the whole document.**
`document.reasons` renders **only** at `ScenarioResult.tsx:50` (inside `NoScenarioBlock`, non-preliminary only). `document.constraints` renders **only** at `ScenarioCard.tsx:103`, and `document.integrity_check` **only** at `:121-133` (preliminary only). So `no_scenario`/`unsupported` loses all 11 constraints — including the eight whose notes read *"MUST NOT be inferred, defaulted, or estimated"* — plus the integrity note *"Not performed: a conflict stopped the build before any cap candidate."* And **preliminary loses `reasons`**, i.e. the sentence stating the cap came from the ZR 23-21 trace verbatim and is NOT a buildable envelope. Confirms DCV-5's 0/96 and 0/5 counts and adds the **mirror-image loss DCV did not flag**.
*Fix:* hoist `constraints` + `integrity_check` into `ScenarioResult`; render `reasons` on both branches.

**5. MEDIUM — `CompareEntry` has zero test coverage.** Every test renders `CompareScreen` directly with an injected `fetchImpl`; nothing renders `CompareEntry`. The bad-param branch (`CompareScreen.tsx:161-175`) is unexercised — both copy variants, its `h1`, and critically **the guarantee that no fetch is issued for an invalid parameter**. Confirm has exactly this test (`confirm-entry.test.tsx`, with `expect(fetchSpy).not.toHaveBeenCalled()`) plus an e2e. Compare has neither. *Fix:* mirror `confirm-entry.test.tsx`.

**6. MEDIUM — no browser-level coverage for the new route; the third declared test command covers nothing new.** `apps/web/e2e/harness/fixture_api.py:250` sets only `INTERNAL_RULE_EVAL_ENABLED_ENV_VAR`; no spec navigates to `/property/compare`. AS-6's forward navigation is never followed end to end — the vitest test asserts only the `href` attribute (`compare-screen.test.tsx:204-206`). The producer disclosed this accurately in report §3 and AS-7 says "and/or", so **not a scope violation** — it is a coverage fact, and it is why both honesty defects above survive a green suite.

**7. MEDIUM — test quality is uneven.** Genuinely strong in two places: `:45` asserts `capNode.textContent === formatValue(bodyCap)`, and `:120-128` asserts an exact gap count of 8 plus a named family. Elsewhere assertions are existence checks. No axe scan anywhere — correctly, since `jest-axe` is not a dependency and the 7-day age gate would block adding it — so AS-8 rests on one `aria-live` attribute check and two focus checks. **The owner-directed BLOCKING regression `(500, state:"no_match")` — recorded fixture at `packages/contracts/fixtures/client_regression/http500_state_no_match.json`, named blocking at `contract-matrix.ts:12-17` — is never driven through `fetchScenario`**, even though `scenario-api.ts` reimplements the very pair check that defends against it.

**Eight LOW findings:** dead unreachable `"aborted"` branch (`CompareScreen.tsx:129`); prop named `document` shadowing the global in four components; heading level h3→h2→h3 inside `ScenarioResult`; redundant `cleanup()` at test `:180`; the "documented key set" docstring claim no key-set check backs (inherited template wording, also in `rule-evaluation-contract.ts:15`); no `.catch` on the fetch promise (`CompareScreen.tsx:58-67`, inherited verbatim from `ConfirmScreen`); possible non-unique React keys on constraints/problems; `assumptions` and `citations` validated but never rendered.

---

## Modularity — PASS (measured, not estimated)

Ran `tools/modularity_check.py --check`: **377 files, 0 failures, 16 warnings, exit 0**, and neither new file appears among the warnings. `ScenarioFailureStates.tsx` (355 SLOC) and `scenario-api.ts` (424) are well under the 600 warning threshold (`CODE_MODULARITY_POLICY.md:47-53`). On cohesion by reading: `ScenarioFailureStates` does one job and is cohesive; `scenario-api.ts` carries three responsibilities (transport, classification, announcement copy) but that is exactly `rule-evaluation.ts`'s shape — a consistency choice, not a new violation. **Finding 1 is the real boundary problem, not file size.**

---

## Untested branches (16)

1. `(404,"no_match")` → `ScenarioNoMatchState` (`ScenarioFailureStates.tsx:80-93`), incl. the `outcome.bbl === null` sub-branch at `:85`
2. `(422,"validation_error")` → `ScenarioValidationErrorState` (`:95-111`), incl. `boundedToken(detail?.code,48) ?? "unknown"` (`scenario-api.ts:322`)
3. `(500,"internal_error")` → `ScenarioInternalErrorState` (`:165-184`)
4. `(500,"internal_contract_error")` → `ScenarioServerContractErrorState` (`:186-209`)
5. `network_error` at component level (`:251-265`) — a throwing `fetchImpl` is never driven
6. `client_timeout` at **component** level (`:267-286`) — proved only as a client unit at test `:248-261`; the card and its `Math.round(timeoutMs/1000)` are never asserted
7. The owner-directed adversarial `(500, state:"no_match")`, plus `(200, state:"x")` and a non-JSON 200 body (`scenario-api.ts:256-268`)
8. `scenario_kind === "unsupported"` — `unsupported_family.json` is never imported, so `SCENARIO_KIND_LABELS.unsupported` and that branch of `NoScenarioBlock` never render
9. `NoScenarioBlock`'s `professional_review_required === false` path — exercised implicitly, never asserted
10. `ScenarioValidationFailureState`'s `problems.length === 0` branch (`:231-244`) and the `MAX_REPORTED_PROBLEMS` overflow marker (`scenario-contract.ts:128-130`)
11. `ScenarioCard`'s `cap_provenance === null` fallback (`:95-99`) and the `integrity_check` performed=false / agreed=false / agreed=null arms (`:121-129`)
12. `CoverageMatrixSection`'s no-gaps branch (`:77-80`) — all three imported fixtures have 8 gaps
13. `CompareEntry` entirely, both bad-param branches and the no-fetch guarantee
14. **Request supersession — the MECHANISM IS CORRECT** and this is worth stating plainly: a monotonic `useRef` sequence incremented per effect run plus `AbortController` cleanup (`CompareScreen.tsx:54-69`), mirroring `ConfirmScreen.tsx:409-423` exactly, so a slow earlier response **cannot** overwrite a newer one. It is simply asserted nowhere.
15. `bounded.ts` truncation on a scenario body — no test proves the `… [truncated]` marker reaches a Compare card
16. `document.assumptions` and `cap_provenance.citations` — validated, never rendered, never asserted

---

## Scope and regression verification

- **Forbidden paths:** all unmodified, verified by `git diff --name-only 02379432 HEAD`. No violation.
- **`ConfirmScreen.tsx` (15 lines changed):** does **not** break the existing confirm tests in `components/confirm/__tests__/**` (which sit in `forbidden_paths` and could not have been updated by the producer) or any e2e spec. This was the specific CRITICAL risk flagged at dispatch; it did not materialize.
- **CI prediction:** `web (lint + typecheck + build)` and `web-e2e (vitest + Playwright)` would both almost certainly go green.
- **Known-red, not attributable to this packet:** the `secret-scan` job is already red on this tree from two pre-existing fake `postgres://u:p@h/db` canaries in untouched files, confirmed present at base `02379432` (G5). <!-- secretscan:allow fake canary postgres://u:p@h/db quoted in gate-report evidence; single-character placeholders, reviewer method note, not a credential -->

---

## Notes

No reviewer in this wave could execute the suite — `apps/web/node_modules` is absent everywhere under the thin-client policy (`docs/LOW_STORAGE_CLOUD_DEVELOPMENT_POLICY.md`), as recorded in the packet's own `test_evidence_capture_note`. CI is the only execution path. This static CI prediction is the sole pre-merge signal; no green is claimed.
