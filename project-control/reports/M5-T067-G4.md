# M5-T067 G4 gate — independent QA/test-adequacy review (verbatim reviewer return)

Reviewer: qa-engineer (claude-opus-4-8 per D-064), dispatched by the orchestrator at the
seq-124 T067 wave, pinned to the frozen submit head 4ed36c5e (material 4e1491f8). Recorded
by the orchestrator; the reviewer is read-only and records no gate itself. Arrived complete
with the END marker. Verbatim return follows.

---

VERDICT: PASS

# M5-T067 G4 QA / test-adequacy review — VERDICT: PASS

Reviewer: qa-engineer (independent; read-only). Task: M5-T067 — DB-040(q)+(r) site-definition
store hardening (supersede scope-before-status reorder; revoke not-found message unification),
mutation-tested. Producer: backend-engineer.

## Frozen identity (verified)
- Submit head 4ed36c5e; material 4e1491f8. `git diff-tree` of the material commit = exactly the
  four allowed paths (store.py, tests/site_definition/test_site_definition_records.py,
  tests/api/test_site_definition_api.py, project-control/reports/M5-T067-producer-report.md).
  Zero forbidden-path touches (no main.py, no routes, no contracts).
- Byte-stability: `git diff --stat 4e1491f8 4ed36c5e` on the four paths is EMPTY — the two
  post-material seams (cd808a2e harvest, feb4acac evidence-map) do not touch task content. Working
  tree matches HEAD for the three code/test files. My verdict binds to this frozen path-scoped
  content; disjoint control-plane peer commits do not affect it.

## Evidence I executed this session (OBSERVED, cwd services/api)
- `python -m pytest tests/site_definition tests/api/test_site_definition_api.py -q` -> **74 passed**
  (matches the harvest count).
- `python -m ruff check` on the three in-scope files -> **All checks passed**.
- Exception hierarchy (grep records.py): `ConfirmationNotFoundError` and `ConfirmationNotActiveError`
  are BOTH direct subclasses of `SiteDefinitionError` — SIBLINGS, neither subclasses the other
  (`issubclass(NotActive, NotFound) == False`, confirmed at runtime). This is the linchpin of AS-2.
- AS-2 mutant harness (status-first `supersede` subclass over the REAL store internals, run via
  python stdin — no tree edit): REAL=NotFound for all three statuses; MUTANT: [active]=NotFound
  (GREEN), [superseded]=NotActive (RED), [revoked]=NotActive (RED). Because the classes are siblings,
  `pytest.raises(ConfirmationNotFoundError)` does NOT absorb the NotActive the mutant raises ->
  the parametrized test genuinely goes red. **A status-first mutant actually fails: YES.**
- AS-4 mutant harness: diverging the missing-branch literal makes the two revoke not-found texts
  unequal -> store-level `str(missing)==str(foreign)` assertion RED. Real code: texts byte-identical
  (`"no site-definition confirmation matching that record id exists for the property addressed by
  the request path"`) -> GREEN.
- Consumer sweep (grep tests/): NO test anywhere pins the OLD revoke not-found literals
  ("confirmation being revoked must belong...", "...exists for record id {id}"). Message unification
  reddens no existing suite. `test_condo_records_api.py` uses site_definition additively only.
- Both test diffs are PURELY ADDITIVE: records test = single `@@ -717,3 +717,124 @@` append, ZERO
  deletions; api test = 55 lines appended after L780, ZERO pre-existing assertions modified.

## AS -> test mapping (packet defines AS-1..AS-5)
| AS | Test(s) | Mutation judgment |
|---|---|---|
| AS-1 supersede oracle closed (404 for active/superseded/revoked foreign probe; 409 preserved on own non-active) | records.py:745-779 parametrized `test_supersede_foreign_probe_is_404_whatever_the_record_status[active|superseded|revoked]` (also asserts refused probe mutated nothing: `after==before`, foreign condo empty) + :782-804 `test_supersede_of_a_non_active_own_record_is_still_the_409_conflict` | Concrete + mutation-sensitive (see AS-2). Same-property 409 preserved and tested. |
| AS-2 mutation sensitivity (q) | same parametrized test | CONFIRMED red under status-first mutant on [superseded]+[revoked]; [active] stays green. [active] is insensitive to the reorder but load-bearing against scope-check REMOVAL (would NO-RAISE) — each parametrization pulls weight against a different mutant. |
| AS-3 message unification | store: records.py:811-841 `test_revoke_not_found_message_is_identical_for_missing_and_foreign_ids` (`str(missing)==str(foreign)` + reject_code eq + neither id echoed); route: api:788-834 `test_revoke_not_found_response_is_identical_for_missing_and_foreign_ids` (404/state/reject_code/message eq + FULL-body eq after stripping only the per-request uuid4 correlation_id, asserting the field exists before stripping) | Direct equality both levels. Route body-equality catches any oracle field, not just message. |
| AS-4 mutation sensitivity (r) | the two equality tests above | CONFIRMED red on literal divergence. |
| AS-5 preservation | 74 scoped green (I ran it) + purely-additive diffs + no forbidden path | No legitimate same-property path changes status/state/refusal code; report's "no pre-existing assertion adjusted" claim is VERIFIED TRUE (zero deletions in both test files). |

## Observations (non-blocking)
- OBS-1 (INFO): AS-1 is proven at STORE level only; there is no route-level supersede-oracle test.
  Acceptable — the packet scopes AS-1 to "tests covering all three statuses" without mandating a
  route test, the supersede route is forbidden/unmounted, and the shared _refuse mapping
  ConfirmationNotFoundError->404 is independently proven by the revoke route test.
- OBS-2 (LOW/INFO): the reorder moved STATUS past supersedes-ref AND reason (not only past scope),
  so a non-active record combined with a mismatched supersedes-ref or missing reason now yields
  404/reason-required instead of 409. All remain refusals, all reduce disclosure, none are
  legitimate paths, and no test pinned the old ordering. Not a defect; noted for completeness.
- OBS-3 (INFO): supersede's OWN two not-found branches remain non-unified (missing echoes the id vs
  static foreign text) — a real existence oracle one class lower, correctly OUT of this packet's
  (q)+(r) scope and HONESTLY disclosed by the producer as an orchestrator follow-up.

## Report honesty
The producer report scrupulously labels the mutations [PREDICTED] and the scoped suite/ruff
[BLOCKED for producer] due to a cwd-broker defect (routed to harvest), making no false "verified"
claim. My independent run CONFIRMS the harvest suite/ruff results and ELEVATES AS-2/AS-4 from
PREDICTED to OBSERVED. AS-6-class hygiene: tests are deterministic (fixed constants, no
time/random/network/fs), fully isolated (fresh store/TestClient per test), and the correlation_id
normalization is sound. No flake or order-dependence found.

Gate is met on all five acceptance scenarios with confirmed mutation sensitivity and verified
preservation. VERDICT: PASS.

--- END OF REPORT ---
