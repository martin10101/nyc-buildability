# M2-T008 - G1 Data-Contract Review

**Task:** M2-T008 — ZTLDB lot-level zoning connector (SODA fdkv-4t4z)
**Reviewer:** data-contract-verifier (independent; did not produce this work; read-only)
**Review target:** worktree `C:\Users\MLFLL\Downloads\nyc zoning\nyc-development-feasibility-claude-pack\.claude\worktrees\M2-T008`, branch `task/M2-T008-ztldb-connector`, HEAD afacff2 (7e40e81 producer commit + disclosed orchestrator two-comment secretscan-pragma fix)
**Date:** 2026-07-20

*(Orchestrator note: saved verbatim from the reviewer's agent-return channel per the report-preservation rule; transport entity-decoding only.)*

## 1. Scope and method

Started from the acceptance criteria, not the producer's conclusions: read `project-control/tasks/M2-T008.json` (safeguards 1-6, ZT-S1..S17, allowed/forbidden paths), the G1-corrected research `docs/research/zoning-features-ztldb-2026-07-16.md` (sections 2.1, 3, 4.1, 5.1, 5.4, 6, 8), and the G1 checklist in `docs/GATES_AND_CHECKPOINTS.md`. Then independently reviewed `services/api/app/connectors/ztldb_soda.py` (1,947 lines), `services/api/app/profile/zoning_crosscheck.py`, the `builder.py` diff vs main, both test modules, all 24 fixtures + MANIFEST + `build_fixture_pack.py`, and the registry draft diff. Read the producer report LAST and used it only for cross-confirmation.

Independent executable evidence gathered in my sandbox:
- Recomputed all 24 fixture sha256 digests against MANIFEST bytes (script, not trust): all match.
- Made 5 bounded tokenless live GETs to the official source `data.cityofnewyork.us`: api/views metadata, the ZT01 record URL, the no-record URL, the offset-5 page URL, and the deliberate no-such-column 400.
- Re-ran the full suite in the worktree: `cd services/api; python -m pytest tests -q` → **442 passed in 4.31s** (matches the expected 442 = 356 baseline + 86 new). CI attestation deferred to the orchestrator per blocker B-009; local re-execution is the evidence here, per the dispatch instruction.

## 2. Checklist findings

**Official source identity — PASS.** Origin and dataset pinned as module constants (`ztldb_soda.py:160-162`): `data.cityofnewyork.us` + `fdkv-4t4z` only; every URL is built in `build_record_url`/`build_page_url` from those constants; callers can inject only a validated canonical BBL or bounds-checked ints (`ztldb_soda.py:563-598`). Live api/views fetch returned id `fdkv-4t4z`, name "NYC Zoning Tax Lot Database". Test `test_s16_every_built_url_targets_the_pinned_official_dataset` asserts the pinning.

**Schema authority — PASS.** `ZTLDB_COLUMN_TYPES` (`ztldb_soda.py:178-195`) is the 16-column inventory; I compared it against (a) the committed ZT08 columns array, (b) research section 4.1's table, and (c) my LIVE api/views fetch — all three match exactly in names and Socrata dataTypeName (4 number + 12 text). The transcription-drift test (`test_s1_column_type_snapshot_matches_the_committed_metadata_fixture`, test file lines 256-267) locks the constant to the fixture. Record keys are never used to infer schema: unknown keys produce `unknown_column:*` drift signals and NO fact (`ztldb_soda.py:1317-1321`, test line 822); `check_columns_for_drift` + `fetch_source_freshness` raise typed `schema_drift` on removed/re-typed columns and degrade visibly on added columns (`ztldb_soda.py:862-880`).

**Omitted-key semantics — PASS.** Four presence states kept distinct: present value (fact), observed explicit null (fact with normalized None + `observed_null:<column>` observation, `ztldb_soda.py:1030-1032`), `not_applicable_per_source_semantics` with the documented dictionary text (`NOT_APPLICABLE_WHEN_ABSENT` + `_ABSENCE_SEMANTICS`, lines 221-260), and `absent_undocumented` = unknown-never-fabricated (lines 1398-1409). No conversion to confirmed null/zero/false/empty anywhere; `test_s4_omitted_keys_map_to_documented_absence_classes` proves every one of the 16 columns is either a fact or a typed absence, never both. The ZD1 case is handled exactly right: `zoning_district_1` is deliberately NOT in the not-applicable set; its absence is classified `absent_undocumented` plus a `zoning_district_1_absent` observation (lines 221-228 comment, 1379-1384). I verified LIVE that BBLs 1000010201, 1000010301, 1000010401 genuinely lack the ZD1 key on the official endpoint (page-2 fetch) — the fixture claim is real, and the connector preserves the absence as absence rather than fabricating a post-2019 value.

**Domain semantics vs official dictionary — PASS.** Split-lot ZD1..ZD4 ordering: positions come from the official column index, values never resorted (`_build_zoning_assignment`, lines 1090-1100; `test_s2_ordering_is_never_resorted` proves a lexicographic-swap synthetic keeps official order); ordering-semantics text quotes the greatest-percentage rule with the 2019-12-31 change. CO1/CO2 Appendix C set is exactly C1-1..C1-5, C2-1..C2-5 (lines 266-268, matching research 4.1 verbatim). SD slash-tie parses to components with `tie: true` and the verbatim value preserved (lines 1104-1122; `test_s6`). PARK carries the official caveat verbatim (`PARK_CAVEAT`, lines 285-289, byte-matching the research quote). LH Appendix D = LH-1/LH-1A/LH-2/LH-3 advisory. `zoning_map_code` Y border semantics quoted verbatim (lines 312-317). BBL identity uses the shared algorithmic `normalize_bbl` (1+5+4); the PDF example typo 5158287501 appears nowhere in the implementation — the registry records the typo correctly and tests use the corrected 3158287501 form. Borough-code is handled 1-digit; Marble Hill/Rikers legal-borough note is carried in the registry record, matching the dictionary.

**Open ZD1 vocabulary (research C4) — PASS.** District vocabulary checks are shape-advisory only (`_APPENDIX_B_SHAPE_RE` + PARK/BPC, lines 281-282); a ZR-section-number-shaped value ("107-42", fixture ZT91) is accepted as a fact with an `open_set_vocabulary:*` observation, never rejected, coerced, or drift-flagged (`test_s8`). No closed-set check for special districts, correctly justified: Appendix A is not transcribed in the accepted research and is never guessed (`_advisory_vocabulary_observations` docstring).

**Number-as-string serialization — PASS.** Live and fixture records serialize borough_code/tax_block/tax_lot/bbl as JSON strings; normalization preserves `original_value` verbatim and `normalized_value` separately per fact with full provenance (`test_s1_single_lot_maps_the_16_column_contract` asserts "1" → 1 with original preserved). The decimal-tail tolerance from M1-T001 C6 is retained defensively (`_parse_source_integer`).

**Freshness guard (owner two-staleness rule) — PASS.** `SourceFreshness` (lines 770-922) computes `rowsUpdatedAt` age against the injected clock, reported as SOURCE freshness with a self-describing policy string; `staleness` (transport) is stamped exclusively by `ResilientZtldbFetcher` (lines 1828-1833, 1933-1939). All four combinations proven in `test_s10_regression_the_two_staleness_dimensions_vary_independently`; fresh retrieval of the old dataset carries `staleness=None` and `source_stale_suspected=True`; cache/LKG serves copy `source_freshness` and `retrieved_at` verbatim. Missing `rowsUpdatedAt` is typed `schema_drift` — the guard cannot be silently skipped (lines 882-890). The staleness honesty claim is TRUE: my live fetch on 2026-07-20 returned `rowsUpdatedAt` 1775414816 = 2026-04-05T18:46:56Z — identical to fixture ZT08 and honestly recorded in the registry draft (update_frequency, health_status `degraded_suspected`, OQ-3 "second monthly boundary has passed").

**Cross-check — PASS.** `zoning_crosscheck.py` compares 12 field pairs; every disagreement becomes a contract-1.3.0 conflict entry with BOTH (or all three) observations plus provenance-bearing derivation text, `resolution` always `unresolved`, no winner, no adjudication (`_values_disagree`, `crosscheck_lot_zoning`). Present-vs-documented-blank is a conflict with the absence semantics stated (`test_s13_present_vs_documented_blank...`). Cross-property and no-record inputs are refused (would fabricate conflicts). No contract or schema file changed (`packages/contracts/**` and `_contract_schemas/**` absent from the git diff); profiles built with the integration validate against `property_profile.schema.json` in-test.

**builder.py additivity — PASS.** The diff vs main is exactly three optional kwargs defaulting to None plus docstring/comments; `provenance`, `conflicts`, and `connector_notes` are extended only when provided. I verified the docstring's gating claim in code: `_status_dimensions` is called with `profile["conflicts"]` (builder.py:677-683), the combined list — so an unresolved cross-source conflict on critical `zonedist1` genuinely produces `blocked_data_conflict` through the EXISTING M2-T004 machinery (`test_integration_critical_conflict_gates_analysis_readiness`). The default-path regression test (`test_integration_defaults_leave_existing_builder_behavior_unchanged`) plus all 356 pre-existing tests passing unmodified evidence the additive claim.

**dataset_version label deviation — PASS (honest and documented).** The dataset has no version column (research 5.3, confirmed live). The label `socrata-rows-<rowsUpdatedAt RFC3339>` is self-describing, derived from the only official version signal, documented in the connector docstring, fact comments, registry record, and producer report section 8.1, and cannot be mistaken for an official YYYYMM version string. Provenance ids become idempotent per rows-state, which is a sound property.

**Pagination/ordering, rate limits, drift signatures, timestamps, digests — PASS.** Deterministic `$order=bbl` with bounded `$limit`/`$offset`, hard page cap 50, typed pathologies (duplicate_page, no_progress, repeated_record, unordered_page, page_overflow, page_budget_exhausted) — each test-asserted; budget consumed pre-I/O. The 400 taxonomy is correct and live-verified: my deliberate live GET returned HTTP 400 errorCode `query.soql.no-such-column` (drift signature, fixture ZT10 raw); `query.soql.type-mismatch` (ZT100, labeled synthetic derived from ZT10) maps to `upstream_error`, not drift. Retry-After honored exactly with a cap that fails typed instead of blocking. `retrieved_at` stamped after the successful response; raw/response/normalized digests kept separately per the self-describing `ZT_CANONICALIZATION_SPEC`; cross-platform anchor `ZT01_NORMALIZED_DIGEST` asserted; shuffled-fixture test proves raw flips while response/normalized hold.

**Fixtures — PASS.** 24 fixtures, MANIFEST digests all recomputed and matching; 11 raw + 13 synthetic, every synthetic labeled with mandatory `derived_from` lineage (test-enforced in `test_s15_manifest_digests_match_committed_fixture_bytes`); pack ~104 KB (low-storage compliant). Wider secret-scan needles (M2-T007 G5 O4 carry-forward) enforced in `test_s16_fixture_pack_contains_no_credential_material`; my independent needle scan across all fixture files found zero hits; the app token is header-only with negative tests for logs, URLs, payloads, and error payloads. **Strongest authenticity evidence:** because the Socrata rows are stalled, my live tokenless re-fetches reproduced committed raw fixtures byte-for-byte — ZT01 live body digest `sha256:37d1e4a4f4df9368f413dcb9175b9c8553830ba571cffccc65ca5304c3e7deac` and the offset-5 page digest `sha256:2ac4f3cd5f1bebfd42d0e0de94bf0b986dae7ba43d945515d9ff8c42a37299fd` both match MANIFEST exactly; the live no-record query returned `[]`.

**Scope — PASS.** The git diff main...HEAD contains exactly 34 files, all inside allowed_paths (new connector module, new crosscheck module, builder.py cross-check integration, new test modules, `tests/fixtures/ztldb/**`, the one registry draft, own producer report). Forbidden paths untouched: `pluto_soda.py`, `bbl.py`, `zoning_features_arcgis.py`, `packages/contracts/**`, `_contract_schemas/**`, `app/resilience/**`, `apps/web/**`, `.github/workflows/**` — none appear in the diff. No existing test file modified. The disclosed orchestrator commit afacff2 is verified comment-only (2 insertions, 2 deletions, both `secretscan:allow` pragmas on the fake-token lines, matching the repo convention at test_pluto_soda.py).

**Case-only difference classification — PASS (semantics judged sound).** PLUTO `zonemap` '16a' vs ZTLDB `zoning_map_number` '16A' is a real live formatting divergence between two official presentations of the same quartersection map identifier; the letter designates the sub-map and case does not distinguish different maps. Classifying pure casefold-equality as typed `case_only_difference` UNCERTAINTY — with both verbatim values preserved, surfaced in conflict-adjacent notes, and never adjudicated — is conservative and correct; content differences remain conflicts.

## 3. Defects

**D1 (low, documentation-only, non-blocking):** The MANIFEST purpose text for ZT07a (`MANIFEST.json` line 140) and the corresponding `build_fixture_pack.py` CAPTURE_PLAN entry (lines 99-102) claim page 1 "includes real records with absent zoning_district_1" — the absent-ZD1 rows (1000010201/301/401) are actually in **ZT07b** (all five ZT07a rows carry ZD1; verified against fixture bytes and live). The connector comment (ztldb_soda.py:218-219), registry record, and producer report all attribute the finding to ZT07b correctly; only the ZT07a purpose string is misattributed. Correct the purpose text (move the clause to the ZT07b entry) at the next convenient commit; no data, digest, or behavior impact.

## 4. Observations

**O1:** The absence-semantics strings for `special_district_2`/`special_district_3` (`ztldb_soda.py:254-255`) lack the parenthetical dictionary citation the other seven entries carry. Cosmetic consistency only.
**O2:** A fresh `fetch_by_bbl` without injected freshness consumes two budget units (metadata + record). Documented via the injectable `freshness` parameter; API-route callers should amortize the metadata fetch. Fine as designed; worth remembering at route wiring (recommended next task 1).
**O3:** `zmcode` is always compared (excluded from the skip-if-all-blank branch) so both-blank lots produce a "consistent" agreement entry with both values None — harmless positive evidence, noted for G3 readers.
**O4:** OQ-3 escalation (Socrata stall now across a second monthly boundary) is a producer-recommended human action; the orchestrator should track it as a follow-up, since live per-BBL answers can be one or more rezonings behind despite the guard making it visible.
**O5:** CI attestation for this branch remains an orchestrator duty after B-009 (GitHub Actions billing) is resolved; my local 442-pass run is the gate evidence per the dispatch instruction.

## 5. Verdict

**PASS** — the connector's 16-column schema authority, omitted-key/observed-null separation, split-lot/slash-tie/PARK/open-ZD1 semantics, two-staleness freshness guard, no-adjudication cross-check, and additive builder integration all verified independently against the official source (five live spot-checks reproduced committed fixtures byte-for-byte, including the still-stalled rowsUpdatedAt honestly recorded), with the full suite green at 442 passed locally; single defect D1 is a documentation-only fixture-purpose misattribution, non-blocking.
