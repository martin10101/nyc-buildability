# G3 CODE-REVIEW GATE REPORT (ROUND 2) — M0-T023 (D-001 amendment 3, rework)

- Task: M0-T023 — Owner Directive Compliance System, directive D-001, amendment 3 (rework of the F1/F2 blocking findings + N1/N2/N4)
- Reviewer: code-reviewer (independent; producer = orchestrator; producer != verifier)
- Prior reviewed head: `bd8e1011` (round-1 verdict: FAIL — F1 blocking bypass)
- New reviewed head: `ed8a6776f89c9f2b17ecad35e528a0180fe6bfd1`
- Content-manifest identity: `9ab6c9601b1a60f903d1a399d57192f662d96e912fdc50304ba931894b3004be`
- Rework delta: `git diff bd8e1011..ed8a6776` = 3 files (tools/directive_registry.py, tools/project_control.py, tools/test_project_control.py), +98/-13

## VERDICT: PASS

The round-1 blocking bypass (F1) is genuinely and completely closed, the new proofs (F2) are non-tautological, and N1/N2/N4 are correct without breaking any legitimate flow. All required suites pass. No residual blocking findings.

## Reproduction of required suites (all pass at HEAD `ed8a6776`)

```
python tools/test_project_control.py         -> OK: all 14 project-control test groups passed
python tools/test_directive_compliance.py    -> Ran 55 tests ... OK
python tools/validate_directive_compliance.py --check -> exit 0
```

## (1) F1 — is there ANY remaining path to grandfather-accept a not-in-regime task without regime entry? NO.

Re-enumerated every status-mutating command; no path lets a not-in-regime task under an enabled regime reach a continuation status (in_progress/self_check/awaiting_gate) from a non-continuation status (backlog/ready/rework/blocked) without regime entry:
- `claim` (:610-621): mandates regime entry — fails closed for a not-in-regime task under an enabled regime.
- `progress` new guard (marker "Regime-entry guard (D-001 amendment 3, Section 1; G3 F1)"): fails closed exactly when `enabled and not _task_in_regime(t) and target != "canceled" and cur not in _CONTINUATION_STATUSES and target in _CONTINUATION_STATUSES`. This is the precise F1 launder path (blocked->awaiting_gate/in_progress, rework->in_progress), now blocked. Placement correct: after transition-legality + roster checks, before `t["status"]=target`, only inside the `target and target != cur` branch.
- `gate` (:851-869): FAIL->rework, BLOCKED->blocked, PASS/G0->ready, PASS/awaiting_gate->progress% only. NEVER sets a continuation status.
- `submit`: for a not-in-regime task `_legacy_grandfather_check` requires cur in continuation, so a rework submit fails closed.
- `accept` (:869-936): grandfather path requires cur==awaiting_gate AND manifest membership AND material-unchanged AND cur in continuation.

Net: the only not-in-regime tasks that can occupy a continuation status are genuine pre-regime in-flight tasks (the owner-sanctioned "may finish without deadlock" set) or their within-continuation descendants. A blocked/rework/backlog/ready legacy task now has exactly one forward path — reach ready/backlog via progress (guard inert) then `claim --directive-refs` (regime entry). No deadlock.

Guard does not break legitimate continuation flows (cur in continuation -> guard inert): claimed->in_progress, in_progress->self_check, awaiting_gate->rework all unaffected. S7/S8 groups still pass.

## (2) PROOF 7b / 7c prove it (non-tautological) — CONFIRMED

- PROOF 7b (test:1526-1541): builds a genuine pre-regime `blocked` task (regime OFF: claim->in_progress->blocked), enables regime, registers it in the migration manifest with its current material_digest (manifest-member + material-unchanged — the live M0-T007/M0-T008 shape), attempts `progress blocked->awaiting_gate`, asserts rc!=0 + "regime", and that a subsequent accept fails. With the guard removed the transition would succeed and accept would grandfather it — the assertion would then fail. The "regime" substring isolates the regime guard from the roster precondition (valid roster present).
- PROOF 7c (test:1543-1562): genuine pre-regime `rework` task (claim->in_progress->submit->awaiting_gate->rework), attempts `progress rework->in_progress`, asserts fail-closed "regime". rework->in_progress has no roster gate, so the failure is unambiguously the regime guard.

## (3) N1 / N2 / N4 correctness and no legitimate-flow breakage — CONFIRMED

- N1 (:483-490): accept now intersects the applicable set with each cited directive's own requirement_ids per iteration. Single-directive (D-001) is identity-preserving (no regression); multi-directive false-block eliminated; absent directive -> empty (still caught upstream by evaluate_task_refs). No false-accept.
- N2 (directive_registry.py:778-788): frozen_git_identity(require_clean) resolves HEAD and fails closed if commit!=HEAD (cleanliness is HEAD-relative). submit/gate pass HEAD, accept passes None->HEAD, so no legitimate flow blocked. Test helper `_git_identity` correctly repointed to git_tree_manifest for cross-commit comparisons. GitContentIdentityTests all pass.
- N4 (:749): rename/copy skip checks both status columns. Inert for porcelain v1; defensive. No regression.
- G5 hardening: GIT_LITERAL_PATHSPECS=1 (dir recursion under ls-tree -r still works), rev-parse --end-of-options, timeout=60 fail-closed. All suites pass.

## (4) No regression in previously-correct code — CONFIRMED

Surgical delta; git-identity core, migration/grandfather, v2 per-task resolution, and validator otherwise untouched. Full round-1 property set holds: 55 directive-compliance tests + 14 project-control groups + validate --check all pass at ed8a6776.

## Residual non-blocking observations (unchanged; not gating)

- N3 (fail-closed direction): relevant_working_tree_dirty with empty `paths` scans the whole tree; an in-regime task with empty allowed_paths would fail closed on unrelated dirt. Degenerate; safe direction; not required for this bounded amendment.
- Coverage note: PROOF 7b tests blocked->awaiting_gate; blocked->in_progress is the same guard condition, not separately asserted.

## Scope confirmations

Read-only review (test suites + read-only git only; no project_control.py/git-write/gh). Nothing merged, accepted, deployed, purchased, product-dispatched, or PR-closed by me. Per-requirement D-001 re-derivation is the separate directive-compliance-verifier (G4) pass.

Recommendation: PASS. The blocking bypass is closed with a correct, minimal, well-tested guard; supporting fixes sound. Freeze the reviewed identity at ed8a6776 and record this G3 result.
