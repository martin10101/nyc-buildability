# M5-T059 G2 — producer self-check record (recorded by orchestrator, 2026-09-20, seq 122)

Verdict: **PASS**.

Producer self-check basis (report §10 producer-observed rows) plus the orchestrator's INDEPENDENT
re-capture (report §10.1) at the harvested snapshot (worktree 48fe290b; material cherry-pick
2fbd661e; frozen submission f58f4d89):

- T1 `python -m ruff check .` from `wt-m5t059/services/api` → "All checks passed!", exit 0.
- T2 `python -m pytest tests/site_definition tests/api -q` from `wt-m5t059/services/api` →
  **578 passed** in 34.33s, exit 0.
- T3 `python tools/modularity_check.py --check` from the worktree root → exit 0 (pre-existing
  `tools/*` warns only; neither the new `site_definition` package nor `condo_records.py` flagged).
- Regression beyond the documented commands: `python -m pytest tests/rules -q` → **726 passed**.
- The producer's retained failed run R1 (same ruff argv at repo-root cwd; pre-existing
  reports/tools lint, out of scope) is preserved as a separate outcome, not repaired.
- CI run 35502941971 **success** at `fc2910af` (contains the material) — the web proof for the
  PropertyOverview / condo-records surfacing (thin client: web proves only in CI).
- Scope check: material = exactly 13 files, all inside allowed_paths; `main.py` untouched (the
  new router deliberately UNMOUNTED); `documents/**` and `connectors/**` consumed read-only.

G2 is the producer-side gate; the independent G3/G4/G5/HJ/DCV wave is dispatched at the frozen
submission identity and rules separately.
