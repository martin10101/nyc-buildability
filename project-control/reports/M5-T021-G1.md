# M5-T021 — G1 Code Review Gate Report (verbatim)

Orchestrator preservation note: report returned by the independent code-reviewer agent
("G1 code review M5-T021") on 2026-09-12 and preserved verbatim below (transport
entity-decoding only). Reviewer was read-only; frozen review identity: d62a779e +
3cd3b93c + bot lock d83bcfe5 + 18ee6b79. Verdict: PASS (advisories A1–A4 non-blocking).

---

I have completed all independent verification. Every material fact reproduces from primary sources. Here is the gate report.

---

# GATE REPORT — G1 Independent Code Review

- **Gate ID:** G1 (independent code review)
- **Task:** M5-T021 — D-042 Next.js 15.5.24 upgrade + whole-tree advisory sweep
- **Directive regime:** in-regime; `directive_refs` = `D-042:D-042-R001`
- **Branch:** candidate/D-024-mrl-option-b — **HEAD b405d350**
- **Frozen material identity reviewed:** d62a779e (pins, 5 values + research) + 3cd3b93c (browserslist override) + d83bcfe5 (bot lock, `github-actions[bot]`) + 18ee6b79 (vitest-4 test correction + ratification)
- **Reviewer:** code-reviewer (read-only, ADR-005). Live workflow/gh/npm not runnable in-sandbox; S2/S3 machine-proofs verified from orchestrator-captured evidence per the evidence-capture division of labor; lock content and all registry timestamps independently reproduced.
- **Result:** **PASS** (4 non-blocking advisories)

## Scope & provenance verification

Path containment across the full task range (9cf2890b..b405d350), reproduced with `git log --oneline <range> -- <path>`:

- `apps/web/package.json` — touched **only** by d62a779e + 3cd3b93c (both material). Diff = next 15.5.21→15.5.24, vitest 3.2.7→4.1.11, sharp override 0.35.3→0.35.4, js-yaml override 4.3.1→4.3.2, +baseline-browser-mapping 2.11.21 override, +browserslist 4.28.9 override. `react`/`react-dom` = 19.1.2 unchanged; postcss/brace-expansion/nanoid unchanged. Exactly 5 values (d62a779e) + 1 override (3cd3b93c) — matches the frozen shape.
- `apps/web/package-lock.json` — touched **only** by d83bcfe5, authored AND committed by `github-actions[bot] <41898282+github-actions[bot]@...>`. Bot-only lock commit confirmed.
- test file — touched **only** by 18ee6b79.
- `git diff --stat 18ee6b79 b405d350 -- <3 code paths>` = **empty** → no code drift after the last material commit. Later commits (60095356 docs; 14bdcc73 = separate task M0-T157; a53e7244 CI-evidence reports; b405d350 submit bookkeeping) touch **none** of the three code allowed_paths.
- Prohibitions: `git log <range> -- render.yaml .github/** apps/web/.npmrc apps/web/next.config.* services/ packages/` = **empty**; `apps/web/src/**` touched **only** by the ratified test. Deploy config byte-unchanged.

## Per-scenario verdicts

**S1 exact_pin_and_sweep — PASS.**
`dependencies.next == "15.5.24"` exactly at HEAD (D-042-R004); no 16.x in package.json or lock (`grep -nE 'next.*16\.' package-lock.json` empty; registry `latest` is 16.3.5, deliberately not taken). Every other change advisory-driven. I spot-verified publish timestamps against the live npm registry `.time` and recomputed ages vs the 604800s gate at the admission/CI instants:

| pkg | registry `.time` | research report | age | gate |
|---|---|---|---|---|
| next 15.5.24 | 2026-08-25T16:14:06.715Z | 2026-08-25T16:14:06Z | 18.3d | PASS |
| sharp 0.35.4 | 2026-08-26T09:42:27.903Z | 2026-08-26T09:42:27Z | 17.6d | PASS |
| js-yaml 4.3.2 | 2026-08-26T20:42:48.747Z | 2026-08-26T20:42:48Z | 17.1d | PASS |
| vitest 4.1.11 | 2026-08-18T14:27:07.240Z | 2026-08-18 (~25d) | 26.0d | PASS |
| browserslist 4.28.9 | 2026-09-04T13:39:02.361Z | 2026-09-04T13:39:02Z | 8.4d | PASS |
| baseline-browser-mapping 2.11.21 | 2026-09-03T17:37:12.010Z | 2026-09-03T17:37:12Z | 9.25d | PASS |

Registry timestamps are internally consistent and match the research verbatim. I also confirmed the too-young siblings that justify the `baseline-browser-mapping` override pin: 2.11.22 @ 2026-09-10 (2.25d, would FAIL) and 2.11.23 @ 2026-09-12 (~0.3d, would FAIL) — a free regen would have tripped the age gate, so the override to the aged 2.11.21 is correct. browserslist 4.28.9 is the boundary case but clears with ~1.4d margin.

**S2 workflow_proven_lock — PASS.** Lock (`lockfileVersion: 3`) resolves, verified directly from `apps/web/package-lock.json`: next 15.5.24, sharp 0.35.4, js-yaml 4.3.2, vitest 4.1.11, @vitest/mocker 4.1.11, browserslist 4.28.9, baseline-browser-mapping 2.11.21 — each a **single** node (no nested/duplicate vulnerable copies; `grep -nE '"node_modules/([^"]*/)?<pkg>"'` returns one key each). lock↔package.json consistent. The four-check proof chain (npm ci integrity, blocking audit total==0 all severities, committed-lock age gate, CLI advisory) and the fail-closed-first-run (34726060266) → success (34726113984 → bot commit d83bcfe5) are orchestrator-captured in M5-T021-ci-evidence.txt/producer-report.md and are internally consistent. The machine "total==0" proof is not sandbox-reproducible (thin client); I independently confirmed the resulting lock content and every pinned version's age.

**S3 ci_green_last_red_retired — PASS (via orchestrator-captured evidence).** ci-evidence.txt + evidence-map.json record run **34726577993** at head **18ee6b79**: CONCLUSION SUCCESS, every job green — web-dependency-security (last standing red retired), web (build on 15.5.24), web-e2e (448/448 on vitest 4.1.11), api, control-plane, contracts, typegen, bundle, modularity, supervisor-bridge. Consistent with the intermediate run 34726200121 (web-dependency-security first-green; web-e2e 1/448 + control-plane the two inventoried convergence failures, both addressed). I did not run `gh` (read-only guard + explicit prohibition); this rests on committed evidence, which is internally consistent.

**S4 prohibitions_preserved — PASS.** No deploy performed/configured (render.yaml + .github byte-unchanged). No 16.x. No app-code change beyond the ratified test. react/react-dom, Supabase, G6 untouched. Test correction inspected: `waitFor` is imported (line 1), the `it` is `async` (line 276), and only line 287's assertion is wrapped — `await waitFor(() => expect(document.activeElement).toBe(streetInput))`. Behavior-preserving: still throws if focus never lands. All sibling S4 assertions (announcer non-empty/empty, card removed, retained "BROADWAY"/"120", fetch called once) unchanged. No assertion weakened.

## Directive requirements (independently re-derived at frozen identity)

- **D-042-R001 (bound to M5-T021) — SATISFIED.** Exact 15.5.24 pin ✓; lock regenerated + committed ✓; whole-tree advisory sweep with exact pins, registry-integrity resolutions, and 7-day age gate ✓ (all six timestamps reproduced live); web-dependency-security GREEN (last red retired) ✓. G5 provenance review remains a required gate; the independent `directive-compliance-verifier` writes verification.json (producer ≠ verifier).
- **D-042-R003 (no deploy) — RESPECTED.** No deploy config touched.
- **D-042-R004 (exactly 15.5.24, no 16.x) — RESPECTED.** Pin == 15.5.24; no 16.x in manifest or lock.
- **D-042-R002 (B-022 discharge / MapLibre admission) — not bound to M5-T021** (applicability = D-042-BOOTSTRAP only); it is the post-acceptance follow-up. B-022 correctly remains `open` pending acceptance.

## Findings / advisories (all NON-BLOCKING)

- **A1 (governance, disclosed).** Material commit 18ee6b79 bundles the ratified production test change with control-plane self-ratification of forbidden paths — `project-control/tasks/M5-T021.json` (adds the test to its own allowed_paths + path_notes + progress) and `project-control/state.json`. Defensible: producer IS the orchestrator (per path_notes/G0), which holds sole ADR-005 authority over control files, and the expansion is disclosed in path_notes + progress_log. This G1 gate is the first independent check of that expansion; my independent finding is that it is justified (vitest 4 is the security-required major bump; it shifted effect-driven focus timing; exactly 1/448 tests affected; the fix is behavior-preserving; CI 448/448 green). Recorded for owner/verifier visibility, not a required correction.
- **A2 (skew, acceptable).** `eslint-config-next` stays 15.5.21 while `next` is 15.5.24. Not advisory-named (research confirms it is in neither next advisory), OSV-clean, and the web lint job is GREEN at the fix head. Minimal-diff discipline; optional future alignment. Not a required correction.
- **A3 (evidence, disclosed).** The literal "7th advisory" identity was inferred (sharp 0.35.3 / GHSA-rgj7-g3m4-5g8c) rather than read from raw CI audit JSON of job 103547594072 (artifact not in repo). The binding fact — workflow audit total==0 across every severity at the regenerated lock — subsumes the count. Honestly disclosed by the researcher.
- **A4 (reviewer-sandbox limitation).** generate-lockfile/gh/npm are not runnable by a read-only reviewer on a thin client; S2/S3 machine-proofs rest on orchestrator-captured evidence. Independently reproduced instead: full lock content, single-copy resolutions, no 16.x, and all six publish timestamps/ages against the live registry.

## Reproduction commands (read-only)
- `git show --stat --format='%an <%ae>' d62a779e 3cd3b93c d83bcfe5 18ee6b79`
- `git log --oneline 9cf2890b..b405d350 -- apps/web/package.json apps/web/package-lock.json apps/web/src/components/address/__tests__/address-confirm.test.tsx`
- `git diff --stat 18ee6b79 b405d350 -- apps/web/package.json apps/web/package-lock.json <test>`
- `grep -n '"lockfileVersion"' / grep -nE '"node_modules/([^"]*/)?<pkg>"'` on `apps/web/package-lock.json`
- `curl -s https://registry.npmjs.org/<pkg>` → `.time[<version>]`, ages recomputed vs 604800s

**VERDICT: PASS** — M5-T021 material identity (d62a779e + 3cd3b93c + d83bcfe5 + 18ee6b79) is scope-contained, advisory-driven, exact-pinned to 15.5.24 with no 16.x, every bumped version registry-verified and age-gate-passing (>= 604800s), the lock independently confirmed clean and single-copy, the vitest-4 test correction behavior-preserving, and prohibitions (no deploy, no app-code drift) preserved. Four non-blocking advisories (A1 self-ratification disclosure, A2 eslint-config-next skew, A3 inferred 7th-advisory, A4 sandbox evidence limitation) carry forward for G5 / directive-compliance-verifier awareness. No FAIL-level defects.

Relevant paths (absolute): C:\Users\MLFLL\Downloads\nyc-zoning\ctl24\apps\web\package.json; C:\Users\MLFLL\Downloads\nyc-zoning\ctl24\apps\web\package-lock.json; C:\Users\MLFLL\Downloads\nyc-zoning\ctl24\apps\web\src\components\address\__tests__\address-confirm.test.tsx; C:\Users\MLFLL\Downloads\nyc-zoning\ctl24\project-control\tasks\M5-T021.json; C:\Users\MLFLL\Downloads\nyc-zoning\ctl24\project-control\reports\M5-T021-registry-research.md; C:\Users\MLFLL\Downloads\nyc-zoning\ctl24\project-control\reports\M5-T021-producer-report.md; C:\Users\MLFLL\Downloads\nyc-zoning\ctl24\project-control\reports\M5-T021-ci-evidence.txt; C:\Users\MLFLL\Downloads\nyc-zoning\ctl24\project-control\reports\M5-T021-evidence-map.json; C:\Users\MLFLL\Downloads\nyc-zoning\ctl24\project-control\directives\D-042-nextjs-15-5-24-upgrade\requirements.json.
