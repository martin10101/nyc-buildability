# M5-T048 — G0 contract gate (orchestrator)

- **Packet:** phase B0 of the owner-authorized proposal-editor plan (D-076 "Plan it"):
  the `proposed_massing` scenario INPUT CLASS — an additive scenario-contract 1.1.0 bump
  per the proven M5-T037 recipe (closed-enum append in BOTH schema copies byte-identically;
  optional block under additionalProperties:false; regenerated TS via typegen --check;
  1.0.0 documents stay valid with recorded fixtures green) + a NEW focused validation
  module `app/scenario/proposal.py` (closed simple outline in 2263 feet, level
  consistency, unit sanity, bounded DB-013-pattern ceilings, typed refusals) + valid and
  invalid fixtures. NO evaluation/derivation/builder/api change (B1/B2 territory).
  Proposals are a third input class (D-076-R002 binding text in the packet): never a
  record, never a rule, no allowance computed or implied. Cites D-066:R001 +
  D-076:R001/R002/R003. Depends on M5-T046 (228th).
- **Evidence files named in-packet:** docs/PROPOSAL_EDITOR_PHASED_PLAN.md (phase B + B0
  row), the M5-T037 bump commit a6875731 as the recipe anchor, the scenario schema's
  closed-enum text (1.0.0), the DB-013 ceiling precedent.
- **Pairwise disjointness (D-072-R003):** mechanical checks at this seam — recorded
  outputs `PAIRWISE DISJOINT M5-T045 vs M5-T048: True | intersection: []` and
  `PAIRWISE DISJOINT M5-T047 vs M5-T048: True | intersection: []` (T045 live on loop-1;
  T047 submitted, review wave orchestrator-side). Two loop lanes total (D-072 ≤3).
  forbidden_paths pin builder/derive, all api routes, rules/spatial/profile/connectors,
  and apps/web.
- **Directive binding:** D-066-R001 + D-076-R001/R002/R003 task_ids appended (same-commit
  digest resyncs + audit entries); `evaluate_task_refs` ok=True, applicable == cited.
  This is the FIRST packet in the D-076-R003-released phase-B slice; per R003 it stays a
  flat contract/validation packet — no 3D/visual-scene work.
- **Nav block (D-066-R001):** graph regenerated at the seam (743 files / 15656 nodes);
  embedded in inputs — the scenario package map with the not-in-scope consumer modules
  named, both schema copies, the typegen and fixtures layout, the T037 recipe anchor.
- **Placeholders (c17):** proposal.py (module docstring carrying the third-input-class
  statement, ruff clean), test_scenario_proposal.py (empty-py ok), producer-report
  placeholder. Every other allowed path already tracked.
- **Scope sanity:** byte-identical schema copies fail closed in CI; typegen deterministic
  via the repo script only; validation must not grow into derivation; api/scenario suites
  + contracts-script tests documented; modularity boundary answers required in the plan.

**G0 verdict: PASS** — packet is contractable and claimable.
