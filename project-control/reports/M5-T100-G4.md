# M5-T100 — G4 test-adequacy review (qa-engineer "qa-t100", read-only)

> Transmission history: pinned at 83f86ce0, delivered as two SendMessage parts (1/2, 2/2) ending with END-OF-REPORT;
> no truncation. Joined verbatim by the orchestrator (transport wrapper tags removed only). Verdict PASS: all 7 producer
> mutants killed incl. the three old G4 survivors (MY1a, MY1b, MY7); 6 of the reviewer's 8 weakenings killed; the 2
> survivors are test gaps over correct code (advisories A1/A2, routed to the DB-058 wiring packet at the accept seam).

---

M5-T100 G4 test-adequacy review (qa-engineer "qa-t100", read-only) — PART 1/2.

PIN HEAD 83f86ce0 (recorded at start; matches expected). Material commit 2624adec.

SCOPE VERIFIED (git): production diff is EXACTLY the DB-058(e) internal-error log — `import sanitize_retry_after`, constants `_INTERNAL_ERROR_LOG_MESSAGE`/`_LOG_FIELD_MAX`, the `_sanitized_bounded` helper, and one `logger.error(...)` in the UNEXPECTED-exception branch of the catch-all (35 add / 3 del). Test file 132 add / 0 del (no existing test changed); test functions 51→57 (+7 items → 110→117); fixtures dir untouched; unwired; zero new deps. Baseline `pytest tests/connectors/test_building_footprints_arcgis.py -q` = 117 passed (Python 3.11 local; CI 3.12; nothing version-sensitive here).

METHOD (Write + shell file-writes are guard-blocked for me): each mutant ran in a FRESH subprocess that reads the real connector source, applies ONE in-memory text mutation (strict "exactly one occurrence" no-op guard), registers the mutant under the real dotted name in sys.modules BEFORE the test module imports (so its `import as bf` and by-value `from ... import` bind to the mutant — consuming namespace), then runs the full suite. Baseline REAL (unmodified, same injection path) = GREEN(117) → harness faithful.

MUTATION TABLE — producer's 7, ALL KILLED (re-run):
| MU-A1a log str(exc) not the sanitized fixed msg | RED — findable test |
| MU-A1b drop sanitizer on class name | RED — hostile-class test |
| MU-A1c logger.error→debug | RED — findable+hostile |
| MU-H remove `if esri is None` null_geometry guard | RED — policy[None]+end-to-end |
| MY1a CRS gate checks only latestWkid | RED — crs[sr1]{3857,2263} (old G4 survivor NOW KILLED) |
| MY1b CRS gate checks only wkid | RED — crs[sr0]{102718,9999} (old G4 survivor NOW KILLED) |
| MY7 drop `year=None` | RED — zero-year test (old G4 survivor NOW KILLED) |

MY OWN 8 weakenings (exceeds ≥4) — 6 killed, 2 survivors:
| QY1 logger.error→warning | RED — findable+hostile (EXACT LEVEL pinned) |
| QY2 drop correlation_id from log line | RED — findable (cid pinned) |
| QY3 `_LOG_FIELD_MAX` 200→10000 | RED — hostile+helper unit (length bound pinned) |
| QY8 log to logger "app.other" | RED — findable+hostile (EXACT LOGGER name pinned) |
| QY5 drop the null-geometry record from results | RED — end-to-end (record-kept/len==3 pinned) |
| QY6 null case raises instead of typed-return | RED — policy[None]+end-to-end (never-untyped/status ok pinned) |
| QY4 ALSO log typed connector errors at ERROR (double log) | GREEN — SURVIVOR |
| QY7 log str(exc) only when message is alphanumeric | GREEN — SURVIVOR |

Continued in part 2 (findings + verdict).

---

M5-T100 G4 — PART 2/2. FINDINGS.

LOG EXACTNESS (the asked question — answered YES): the suite pins EXACT level (`_error_records` filters `levelno==ERROR`; QY1 warning reddens), EXACT logger (`name==LOGGER_NAME`; QY8 wrong-logger reddens), EXACT count (`len(records)==1` — a second stray log on the unexpected path reddens), and the correlation id (QY2 reddens). A wrong logger or a stray second line on the tested path is caught.

VACUITY: none found. Every target test reddens under its own guard mutation, so no synthetic page fails to reach the code under test, and no assertion is invariant under its bug. Value-displaced assertions are present where the T089 advisories demanded them: `construction_year is None` (not only the gap; MY7 reddens), `geometry_findings==["null_geometry"]` + `len(buildings)==3` (MU-H/QY5/QY6 redden), both CRS halves (MY1a/MY1b redden). Honesty labels remain hardcoded-literal (unchanged from T089).

ADVISORY A1 (QY7) — no probe pins "str(exc) is never logged even when the message is a short ALPHANUMERIC token". The findable test's message carries braces/quotes/newline, so a maintainer weakening to "log the message only when it looks safe/alphanumeric" ships GREEN yet leaks a short alphanumeric secret — the exact class the producer report reasons about (`sanitize_retry_after('AdminSecret123')` returns it verbatim). Production is CORRECT (never renders str(exc)) and AS-1's literal requirement is met by MU-A1a, so this is defense-in-depth, not shipped risk. Recommend: inject `RuntimeError("AdminSecret123")` and assert `"AdminSecret123" not in line` AND the fixed message present.

ADVISORY A2 (QY4) — no caplog assertion on the TYPED-refusal path: re-logging a typed BuildingFootprintConnectorError at ERROR (a double log for expected refusals) survives. No leak (only sanitized class + fixed msg + cid), but ERROR-level noise / false-alerting for expected refusals like wrong_crs/upstream_error. Recommend: assert `_error_records(caplog)==[]` on a typed refusal.

NO BLOCKING FINDINGS. Production behavior is correct in both survivor cases; both are defense-in-depth test gaps over correct code, the same disposition T089 gave MY1a/MY1b/MY7 (advisory, routed forward). AS-1..AS-5 each have a killing mutant; the three prior G4 survivors are now killed; scope is exactly the three allowed paths; no existing test changed; fixtures byte-identical; unwired; zero new deps.

Suggest routing A1 (and optionally A2) to the DB-058 footprint-wiring packet, mirroring how T089's (l)/(m) advisories became this task.

M5-T100 G4 VERDICT: PASS

END-OF-REPORT
