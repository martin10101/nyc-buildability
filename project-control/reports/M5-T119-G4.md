# M5-T119 — G4 test review (qa-engineer "qa-t119", read-only)

> Transmission history: pinned at 25de64ae (material 4a631bf3; CI run 36110406806 green at 25de64ae; HEAD moved to
> a990de06 through disjoint peer contract seams, no T119 file touched), delivered as 2 SendMessage parts ending with
> END-OF-REPORT; no truncation. Joined verbatim by the orchestrator (transport wrapper tags removed only). Verdict PASS;
> every exit-gate scenario proven green in CI; two ADVISORY items (the D2 exclusion half is near-tautological; the cap
> status chip now also sits beside the value in the printed brief - meaning preserved).

---

M5-T119 G4 test/regression review (READ-ONLY). Part 1 of 2.

IDENTITY: Reviewed at submission head 25de64ae (start PIN, as expected). CI run 36110406806 ran at 25de64ae and concluded completed/success, 0 failed jobs. Disjoint peer commits advanced HEAD to a990de06 (M5-T120/T121 contract seams + services/api/app/drawings) during review — `git diff --name-only 25de64ae a990de06` shows NONE touch any M5-T119 material file, so the reviewed identity is byte-stable.

CI EVIDENCE (web-e2e job 107992384521 log):
- vitest: overview-exception-strip.test.tsx (10) ✓; development-limits.test.tsx (203, incl. M5-T119 A06/A09) ✓; workspace.test.tsx (8, incl. shell-env) ✓. Suite: 56 files / 1486 tests passed, 0 failed.
- Playwright ✓: architect-workspace:219 two-column canvas; :239 fold into ONE strip (.architect-alert count 0 on the real overview route); development-limits:74 cap-status beside value; responsive-a11y:382 no-overflow @360/768/1280; :391 shell env+review visible @360 loaded; :409 shell strip hidden @1280.

EXIT-GATE SCENARIO MAP (each → cited test exists, asserts the property not just render, green in CI):
1. Compare city/reference/evaluated/existing FAR → development-limits.test.tsx:717 (asserts reference element ≠ evaluated element; 3.00 vs 1.50) + existing entry-points + existing-building disclosure. PASS.
2. Unsupported district → :371 "uses no district-specific lookup" (incl. "unknown"). PASS.
3. Critical gaps → overview-exception-strip.test.tsx:65 (field + blocked effect + link) & :42 absent when none. PASS.
4. Mismatched identity → :118 role=alert AnalysisIdentityNotice preserved. PASS (see A1).
5. Missing citation → development-limits.test.tsx:332 "missing citation" withholds evaluated FAR. PASS.
6. Stale source → overview-exception-strip.test.tsx:79 (exact stale sentence + review clause) & :42 absent when fresh. PASS.
7. Split zones → out of scope (ZoningContextPanel untouched); existing district tests cited, not rebuilt. Acceptable per packet scope.
8. Incomplete assessment → :136 role=status IncompleteEvaluationNotice preserved. PASS (see A1).

Base fixture verified genuinely clean (builder_output_m1_t005.json: 0 unresolved conflicts, 0 critical missing, staleness null; missing_inputs[0]=noncritical) — so the "no strip"/flip-state tests are non-vacuous and withCriticalMissing flips a real entry.

(continued in Part 2)

---

M5-T119 G4 — Part 2 of 2.

MUTANT ANALYSIS (new tests fail for the right reason):
- Empty strip when nothing active → caught: :42 queryByTestId null AND queryByRole status "Active issues" null on the verified-clean base.
- Dropped preserved link → caught: :61/:73 getByRole link by exact name + href to propertyHref(bbl,"issues").
- Cap status moved back after the note → caught: development-limits.test.tsx:692 asserts architect-cap-status is WITHIN .architect-cap-line alongside "15,000 sq ft". Impl (DevelopmentLimits.tsx:42-44) places the chip inside .architect-cap-line beside .architect-metric; reverting to pre-fix placement (below the FAR-only note, outside cap-line) makes within(line).getByTestId throw → reddens. Real placement proof, not tautology.
- No invented status without scenario → caught: :710 chip null when scenario null (guard at line 44).
- Phone strip shown@desktop / hidden@360 → caught by real Playwright visibility: :409 toBeHidden@1280, :391 visible@360. CSS verified: .architect-shell-environment default display:none, ≤700px display:flex (architect.css:282-287).
- Canvas not stacking → bracketed by two-column desktop geometry (architect-workspace:219: map wider + left, vertical overlap>0) + no-overflow@360/768 (a non-stacked pair would overflow).
No tautologies in meaning-carrying assertions: status glosses asserted as LITERALS ("Conditional", "Professional review required") ≠ enum "conditional" (STATUS_LABELS DevelopmentLimits.tsx:15-21); blocked-effect sentences literal; field names via shared fieldLabel (acceptable formatter use).

FOLD IS OVERVIEW-ONLY (no double-render; A04 preserved): ArchitectEntry case "overview" (:136) renders ONLY PropertyOverview→strip; PropertyIssuesSummary (.architect-alert) is retained on scenarios(:146)/zoning/facts/report(ReportView:98). The brief keeps A04 unchanged.

BRIEF: development-limits.test.tsx:729 pins the ReportView cap region (value + FAR-only note + status "Conditional"). ReportView.tsx untouched (forbidden path). Broader brief pinned by workspace.test.tsx:57 + e2e report tests (green).

E2E STABILITY: no fixed sleeps in the new specs; stable selectors; 360px check measures real documentElement.scrollWidth vs clientWidth (+1 tolerance). All green.

REGRESSION: additions only (M5-T119 describe blocks appended; strip test new; e2e appended); no existing test weakened/removed. Canvas map-left reorder did not break map/settledMap or tab tests (explicit .focus() used, not tab traversal). modularity job green.

ADVISORY (non-blocking):
A1. D2 boundary tests (overview-exception-strip.test.tsx:118,:136): part (a) proves the dedicated region keeps role=alert / role=status + text (solid — the actual protected meaning). Part (b) renders a CLEAN profile and asserts strip / incomplete-row absence — near-tautological: the strip's only input is `profile`, which structurally cannot carry identity/incomplete state (nulled upstream in LoadedWorkspace), so exclusion is guaranteed by type. Acceptable, but the exclusion half proves little on its own.
A2. The A06 status chip now also renders in the printed brief beside the value (DraftHeadline is shared) — a real brief rendering change; meaning preserved, ReportView.tsx untouched, pinned by :729.

No BLOCKING findings. Whole web suite green at the reviewed head.

M5-T119 G4 VERDICT: PASS
END-OF-REPORT
