# M5-T050 G3 — independent code review (code-reviewer, saved VERBATIM from the agent return)

**G3 CODE REVIEW — M5-T050 (DB-033 riders a–i)**
**Verdict: PASS** (2 ADVISORY, 0 BLOCKING). Reviewed at HEAD `ee1b361c`, material cherry-pick `bcfcc538`.

**Findings**

1. ADVISORY — `AddressConfirmCard.tsx:420-430`: the record note now renders as the card's LAST child, below the Continue/Not-my-property actions AND the `<Meta>` correlation-id footer — i.e. moved out of the three identity-line cluster (matched/entered/city-record) that the T047 HJ walkthrough praised. The CLS mechanism is sound (a pure append below every settled element cannot shift the CTA), but the reading-order/IA relocation is a genuine UX tradeoff. The producer flagged it honestly and routed it to the HJ/visual reviewers; recording here so it is not lost. Not a code-correctness defect.

2. ADVISORY — `AddressConfirmCard.tsx:134-137,222`: with `role="img"` the accessible name derives from `aria-label=enteredTitle`, which for a pathological >512-char entry is the 513-char truncated value while the visible `<strong>` text is the full (e.g. 600-char) string — the announced name is shorter than the visible text. Edge case only (real addresses are far shorter); the length bound is otherwise correct.

**Basis.** Every rider verified in source: (a) reservation logic fully removed — no `RECORD_ADDRESS_RESERVE_FALLBACK`/`recordReserveSample`/slot remain (grep confirms only test null-checks); the last-child append is correct-by-construction and the S11 structural tests genuinely bind it (same-node byte-identical CTA `outerHTML`, append-only `childIds`, non-shown `it.each` collapse-free) — not vacuous. (b) `role="img"` is a correct name-permitting override of the name-prohibited `strong` role; tests assert accessible name + attributes. (c) conditional copy correct in both branches; the matched-present branch is byte-identical to the accepted wording (`address-confirm.test.tsx:1116-1121`). (d) why-note renders only in the differ case, digit-free RECORD wording (D-073-R006). (e) `address-search.ts` is byte-untouched (not in the commit; last touched at T047 `7ecdcbf4`); the guard at `address-search.ts:316-320` runs before the feature loop, so the non-empty would-match features make the length guard the sole refusal cause; `GEOSEARCH_RESOLVE_INPUT_MAX_LEN=512` is inclusive (512 resolves, 513 refuses) — de-vacuation is real. (f) `lot_geometry.py:33-42` is docstring-only. (g) RA-3 assertion binds the exact fixture-driven reason: `_pluto_no_match().no_match_explanation` (`test_lot_geometry_api.py:450-452`) flows through the route's `or`-passthrough (`lot_geometry.py:447`) — fixture-driven, not a guessed legal value; the non-load-bearing or-tail is gone. (h) all six `RECORD_ADDRESS_ERROR_STATES` map to distinct documented (status,state) pairs and are now each covered. (i) `credentials:"omit"` on the sole fetch path (`record-address.ts:211`); 422 json-safety parity is real via the unified `boundedText` error branch (`record-address.ts:294-303`) — the test proves `422.message === 502.message` (600-cap + marker + control-strip via `bounded.ts`); the title/aria bound is applied on the only attribute-writing path. PRESERVATION holds: test diff is 379/56/97 insertions with **zero deletions**, and no out-of-scope consumer test (`address-resolution.test.tsx` only stubs the fetch; no snapshots; no external `getByRole("img")`) asserts the changed output — the S9 test was augmented consistently, S10 uses position-independent `toContain`/`queryByTestId`. Scope clean: all 8 files in `allowed_paths`; `AddressResolutionScreen.tsx` and all forbidden paths untouched. The outstanding pixel-CLS proof (CI Playwright, out of allowed_paths) is honestly flagged for the orchestrator — no overclaim in the producer report.

[Orchestrator identity note at recording time: between the review pin ee1b361c and the gate-record head, the only commits are the M5-T051 contract-seam records — disjoint from every M5-T050 allowed_path; the task's material identity is byte-stable, per the reproduced empty diffs.]
