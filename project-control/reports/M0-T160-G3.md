# M0-T160 — G3 code review (code-reviewer "cr-m0t160", read-only)

> Transmission history: pinned at 481e51ff, delivered as two SendMessage parts (1/2, 2/2) ending with END-OF-REPORT;
> no truncation. Joined verbatim by the orchestrator (transport wrapper tags removed only). Verdict PASS: every binding
> value re-derived by git plumbing; only the intended hunks moved; provenance accurate. The reviewer's sandbox refused
> PowerShell, so AS-3 rests on the orchestrator-captured suite run (M0-T160-G2.md). Advisories 1-2 (read the SHA from the
> binding; add the ps_tests to CI) are routed at the accept seam.

---

M0-T160 G3 independent code review (read-only). PIN: review ran at HEAD 481e51ff3e16d4ec7a864dbb75aaaba3a4b2d5cb (>= expected b4fa0b53). Material commits confirmed: 8df2bec5 (round 1 binding) + 83fdb5bd (round 2 lockstep); base = 83f86ce0 (G0 re-scope seam). Reviewed-content identity matches the G2 submission: blob SHAs at HEAD = binding 4f6fdfdb, runbook 4ee5a21b, test 2288cb62 (M0-T160-G2.md:3) — no post-submit drift on material files. PART 1 of 2.

AS-1 (identity) — PASS. Verified every binding value myself with git plumbing:
- git rev-parse a3f24ff3^{commit} = a3f24ff3825c126c038f59b1ea6d2352f29d724a = source_binding.json:6 commit_sha.
- a3f24ff3^{tree} = 82432361540c3c2a11c55ffa3d6d485426cd45f7 = source_binding.json:7 commit_tree_sha.
- a3f24ff3:tools/agent_supervisor = 9c0b14eaa56ce32d241c77f789266035b584380c = source_binding.json:9 subtree_tree_sha.
- On origin: git branch -r --contains a3f24ff3 -> origin/task/M0-T159-cli-2-1-281-admission.
- Subtree file count = 204 (matches spec).
- All 12 required_modules (source_binding.json:11-22) exist at a3f24ff3: git cat-file -e returned 0 for every one.
- Same full 40-hex SHA in all three files: binding:6, runbook CONTROLLER_UPDATE_RUNBOOK.md:84, test test_runbook_parse.ps1:27. grep confirms the new SHA appears exactly once per file.
- Correctly pins the ACCEPTED unit commit a3f24ff3, NOT the integrated ledger commit cfc3d22c (which recertification §5.0, M0-T159-recertification.md:82-89, warns carries uncertified M0-T149/T152 changes). git diff --stat a5886dab a3f24ff3 = only M0-T159 evidence/fixture/test/event_drift paths.

AS-2 (nothing else moved) — PASS. git diff 83f86ce0 HEAD on the three files shows ONLY the intended hunks:
- source_binding.json: three identity values + one appended authority sentence (binding:3). Schema stays controller_update_source_binding/v2 (binding:2); every transaction key (source_repo, expected_origin_url, subtree_path, required_modules, source_worktree, destination, controller_root, backup_root, a1_runtime_dir, backup_evidence, rollback_evidence, controller_manifest, evidence_out) byte-identical (absent from diff).
- CONTROLLER_UPDATE_RUNBOOK.md: only the section-4 pin paragraph (lines 83-88). All 14 fenced ```powershell blocks byte-identical (none in diff); the -Phase install block at 112-114 unchanged. No angle bracket in the new prose (grep clean).
- test_runbook_parse.ps1: only the $pinnedSha line + one adjacent comment (lines 26-27).
Old full SHA 3f4cee86...dbc3 is gone from all three files (grep = 0); it survives only as the short "3f4cee86 (M0-T143)" superseded reference in runbook:87.

---

M0-T160 G3 review — PART 2 of 2.

PROVENANCE ACCURACY — PASS.
- Appended authority sentence (source_binding.json:3): "D-024-R287 (Claude Code 2.1.281 admission; task M0-T160)... recertified candidate a3f24ff3 (M0-T159 accepted 275th... records 3662 passed / 2 skipped / 0 failed on the certified tree), superseding a5886dab as the install source." All accurate: recert numbers match M0-T159-recertification.md:25 (3,662 passed, 2 skipped, 0 failed); D-024-R287 = the 2.1.281 admission (recert §1); "accepted 275th" verified against the accept-seam commit 9c44b68d "M5-T089 + M5-T093 + M0-T159 ACCEPTED (273rd-275th)". (The accepted_tasks list in state.json is task-id-sorted, so M0-T159 sits at list-position 139; the chronological ordinal is separately and correctly 275th — not a defect.)
- Runbook provenance (CONTROLLER_UPDATE_RUNBOOK.md:83-88): superseded chain a5886dab (M0-T148), e0dd4a3a (M0-T146), 3f4cee86 (M0-T143) — every SHA-to-task mapping matches the binding's own authority history (binding:3). Listing the full chain correctly clears the stale 3f4cee86 the runbook used to name as current.

LOCKSTEP / AS-3 — PASS (design correct; run is orchestrator-captured).
- test_runbook_parse.ps1:87 (binding.commit_sha -eq $pinnedSha) and :88 (runbookText.Contains($pinnedSha)) both now resolve to a3f24ff3 -> GREEN; :82 angle-bracket check satisfied.
- Load-bearing RED->GREEN confirmed via orchestrator-captured evidence (M0-T160-G2.md:7): run_ps_tests.ps1 EXIT 0, 7/7 files PASS at harvested head 777e6b81; RED at pre-rescope head a612652b on "binding contract pins the immutable accepted SHA". NOTE: this session's read-only guard blocked me from running the offline test myself (PowerShell execution denied), so per the evidence-capture division (project-control.md) I verified the stored G2 evidence + the static test logic + blob identities rather than returning BLOCKED.

AS-4 (scope + no machine change) — PASS. Exactly the 4 allowed paths changed across both material commits (8df2bec5: binding + report; 83fdb5bd: runbook + test + report). The other 6 ps_tests files and update_controller_from_candidate.ps1 are untouched (git diff --stat scoped to the folder). This review used only read-only git plumbing + file reads; no supervisor/controller verb, no machine path touched.

FINDINGS.
- BLOCKING: none.
- ADVISORY 1 (endorsing producer discovery #1 / DB-063): test_runbook_parse.ps1:27 hardcodes $pinnedSha, forcing this manual 3-file lockstep on every re-pin — the third stale-pin drift. A later task should have the test READ commit_sha from source_binding.json (single source of truth) and assert the runbook contains it. Out of scope for M0-T160.
- ADVISORY 2 (producer discovery #2): the controller_update ps_tests suite is not in CI (why the drift stayed latent for two re-pins). Consider wiring it into CI. Out of scope.

M0-T160 G3 VERDICT: PASS

END-OF-REPORT
