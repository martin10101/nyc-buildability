<!-- Orchestrator capture: code-reviewer G3 return for M5-T046 saved VERBATIM
     (transport entity-decoding only). Review pinned at frozen head c9113e09; reviewer
     recomputed all six changed-file digests and reproduced the modularity check. -->

The reviewer read-only guard blocks shell file-writes (memory is optional anyway). I have everything needed. Returning the gate report.

---

# G3 Code Review — M5-T046 (address→lot identity honesty, DB-026)

**Reviewer:** code-reviewer (independent, read-only)
**Task:** M5-T046 · **Frozen reviewed SHA:** `c9113e09` · **Branch:** `candidate/D-024-mrl-option-b`
**Gate:** G3 (code) · **Date:** 2026-09-19

## Verdict: PASS

No blocking defects. Three advisory items (A1–A3) are latent gaps that the ORCH-SCOPE-ADJUDICATION correctly defers to the future GeoSearch-promotion packet; one process note (A4).

## Identity / evidence verification (independently reproduced)
- **Frozen-head stability:** live HEAD `665d3d0b` is one commit past `c9113e09` and touches only control-plane files (`project-control/reports/M5-T046.json`, `state.json`, `tasks/M5-T046.json`). The G3 web surface is byte-identical at both.
- **CI validity:** CI ran at `fb7af67d`; `git diff fb7af67d c9113e09 -- apps/web docs/research/source-registry-drafts/geosearch.json` is empty, so CI run 35425516645 (web-e2e success) is valid for the frozen identity.
- **Digests:** all 6 changed-file LF-normalized sha256 recomputed and matched `M5-T046-source-sections.md` exactly (address-search.ts `0096e659…`, test `973ec3c5…`, geosearch.json `137e4e3a…`, AddressAutocomplete `018aa354…`, AddressConfirmCard `dd4f02f5…`, AddressResolutionScreen `70cc76df…`).
- **Modularity:** `python tools/modularity_check.py --check` → exit 0 reproduced; no warning touches M5-T046 files (`address-search.ts` = 342 lines, under the 600 warn threshold).
- **Scope:** M5-T046 material (`3182f0a2` + AS-5 correction `fb7af67d`) is confined to the 9 allowed files; no `services/api`, `packages/contracts`, PropertyOverview/AnalysisIdentityNotice/ReportView, or their tests. (The broad file list in a `d94bbcb6..c9113e09` diff spans prior accepted tasks M5-T038/M5-T044 on the shared branch, not this task.) `address-search.test.ts` material is **+332/-0** — the M5-T032 reliability suite is byte-preserved (AS-7).

## Surface 1 — equality gate (`apps/web/src/lib/address-search.ts`)
Correct and faithful to the corpus. I hand-traced all six fixtures:
- **AS-1** `SEARCH_1279_37_STREET_SIZE3` → resolves to bbl `3052960043` on feature 1 (GARAGE feature `1279 GARAGE` fails housenumber equality; correctly skipped).
- **AS-2** `37th street` → `normalizeStreetForMatch` folds `37TH`→`37` via `ORDINAL_SUFFIX` (`:249`), equals returned `37 STREET`.
- **AS-3** reverse frontage `3622 13 avenue` → same bbl `3052960043`; identity binds to `pad.bbl` (`:322-325`), never bin/address (test also asserts garage bin `3123204` ≠ true-hit bin `3340270`).
- **AS-4a** nonexistent street → refused by street inequality (`ZZQQXX STREET` ≠ `53 STREET`); **AS-4b** out-of-range house → refused by housenumber inequality (`99999` ≠ `207`). Both `no_match`, no lot surfaced.
- **match_type/confidence** are read onto the result (`:336-337`) but never appear in the gate condition (`:320-321`); the synthetic "confidence:1/exact on wrong street" probe (test `:284-296`) proves the gate still refuses.
- **Key-absence tolerance:** `readPad` (`:277`) and `readParsedInput` (`:264`) tolerate the reduced `/autocomplete` shape; the optional `input?` reference-parse parameter (`:299`) is honored.
- **Fail-closed correctness verified:** the canonical returned form `37 STREET` is *not* altered by the ordinal regex (the space breaks the `ST` suffix), and the abbreviation `37 st` normalizes to `37 ST` ≠ `37 STREET` → honest `no_match`. The normalization is deliberately narrow as contracted.
- **Cohesion:** the DB-026 block is appended below the untouched M5-T032 suggestion exports; same module responsibility (GeoSearch client). Appropriate, not responsibility-mixing.

## Surface 2 — confirm-arc thread
- `AddressAutocomplete.choose` (`:110-120`) captures the raw one-box `text` **before** `setText` (`:117`) rewrites the field, passing it as the second `onPick` arg (`:116`); signature widened to `(query, typedText)` (`:68`) — backward-compatible (CI green confirms no consumer breaks).
- `AddressResolutionScreen` threads `typedInput` through `runResolve` (`:146/:159`) to `AddressConfirmCard` (`:251`, `:308-311`).
- `AddressConfirmCard.enteredInput` (`:96-107`): prefers `typedInput`, trim decides **blank-only**, original string rendered through an escaped React `<strong>` text node (`:132`); no href is built from returned/user text (the only hrefs use `canonicalBbl`). The records-class comment (`:80-95`) and the reported PLUTO address-of-record discovery are honest (D-073-R006). Journey tests S8/S9 (`address-confirm.test.tsx`) prove verbatim preservation including surrounding whitespace (`:629`), distinct from the matched line and from the picked label, with the BBL as the carried identity. Hostile-fixture tests (S6) confirm inert rendering.

## Surface 3 — AS-5 tagged [ORCH-CORRECTED] change
Sound and honest. The diff (`c3c690f7^..c9113e09`) flips AS-5 from asserting `resolved` to asserting `no_match` for the abbreviated `1279 37 st` autocomplete body — the correct fail-closed outcome under the contracted normalization (abbreviation expansion `ST→STREET` was never contracted). AS-5b is **added** and proves shape-tolerance + null-recording of the absent `confidence`/`match_type` by supplying the unabbreviated reference parse through the explicit `input` parameter. AS-5b does **not** dodge the defect: AS-5 still asserts the fail-closed refusal; the explicit parameter is a documented, legitimate API feature, used honestly to exercise the resolved path.

## Surface 4 — ORCH-SCOPE-ADJUDICATION (option A) consistency
Delivered code is consistent. Independent grep over `apps/web/src` confirms `resolveLotFromGeoSearch` / `normalizeStreetForMatch` / the gate types have **no production importer** — only the definition and its test. No new client-side authority path is introduced; the confirm card's BBL comes from the server Geoclient outcome (`view.canonical.bbl`, re-validated). The gate ships as a library-only primitive whose match discipline is recorded in `geosearch.json` to bind future promotion packets. Registry draft mirrors the committed `ztldb.json` key set with the body-only `addendum.pad.version="26c"` freshness stamp and the no-Last-Modified finding (AS-9).

## Advisory findings (non-blocking; for the future promotion packet the gate binds)
- **A1 — borough not disambiguated.** `GeoSearchParsedInput` is `{houseNumber, street}` only; a same housenumber+normalized-street collision across boroughs would bind the first feature. No corpus case exercises it and no live consumer exists. When a live promotion path is wired, add locality/borough to the equality. (Partially anticipated by `geosearch.json` OQ-1, which does not name borough specifically.) `apps/web/src/lib/address-search.ts:218,305-321`.
- **A2 — first-match resolution of genuine ambiguity.** The gate returns the first equality match; a real multi-bbl ambiguity is silently resolved rather than surfaced (library vocab is `resolved|no_match` only, unlike the server path's `ambiguous`). Consider an ambiguous distinction before the library becomes a live authority. `address-search.ts:308-341`.
- **A3 — promoted-feature strings unbounded/unsanitized.** `matchedName/matchedStreet/matchedHouseNumber` are copied to the result without the `boundedString` discipline `parseAddressSuggestions` applies (`:55-73`). Harmless today (never rendered), but a future consumer must render via text nodes and bound length. `address-search.ts:333-335`.
- **A4 — process note (not a code defect).** The original AS-5 asserted `resolved` for an input the gate correctly refuses — the producer's own test carried an incorrect expectation, surfaced by CI (1/1038) and orchestrator-corrected. Expected under the thin-client rule (web behavior proves only in CI); the corrected suite is right.

## Files (all absolute)
- `C:\Users\MLFLL\Downloads\nyc-zoning\ctl24\apps\web\src\lib\address-search.ts`
- `C:\Users\MLFLL\Downloads\nyc-zoning\ctl24\apps\web\src\lib\__tests__\address-search.test.ts`
- `C:\Users\MLFLL\Downloads\nyc-zoning\ctl24\apps\web\src\components\architect\AddressAutocomplete.tsx`
- `C:\Users\MLFLL\Downloads\nyc-zoning\ctl24\apps\web\src\components\architect\__tests__\autocomplete.test.tsx`
- `C:\Users\MLFLL\Downloads\nyc-zoning\ctl24\apps\web\src\components\address\AddressResolutionScreen.tsx`
- `C:\Users\MLFLL\Downloads\nyc-zoning\ctl24\apps\web\src\components\address\AddressConfirmCard.tsx`
- `C:\Users\MLFLL\Downloads\nyc-zoning\ctl24\apps\web\src\components\address\__tests__\address-confirm.test.tsx`
- `C:\Users\MLFLL\Downloads\nyc-zoning\ctl24\docs\research\source-registry-drafts\geosearch.json`

**Note to orchestrator:** I could not write the optional reviewer memory note (`.claude/agent-memory/code-reviewer/`) — the read-only guard blocks shell file-writes. This is expected and not a defect. The three advisory items (A1–A3) are worth carrying forward as binding checks on whatever future task introduces a live GeoSearch→lot promotion.
