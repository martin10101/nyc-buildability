# M5-T019 — G0 Contract/Packet Gate

Gate: G0 (packet completeness). Result: PASS. Recorded by: orchestrator (contract gate;
implementation producer will be a dispatched frontend-engineer agent ≠ gate reviewers).

## Checklist

1. **Exact requirement identifiers** — D-040-R002 verbatim (inputs[0]); the normative design
   intent `docs/design/address-entry-confirm-design-spec.md` §6 (~:118); the originating
   finding `project-control/reports/M5-T015-G5.md:52-54` (F-1).
2. **Exact directive references** — `D-040:D-040-R002`, bound via append-only applicability
   (digest 27d6d1b8 → a3fc9d01, audit-logged); `evaluate_task_refs` ok=true, applicable ==
   cited == ["D-040-R002"]; validator EXIT 0.
3. **Exact source/evidence files** — eight pinned inputs with line refs, including the
   verified-absent evidence (render.yaml carries neither flag and the web service block is
   withheld; no workflow or .env.example sets either name) that scopes the owner return item.
4. **Exact allowed/forbidden paths** — 7 allowed (the lib, the Playwright config, one unit-test
   file, three comment-only files, own report); forbidden fences services/api (the canonical
   flag name changes NOTHING there), render.yaml, workflows, the e2e harness (already on the
   canonical name), components, dependency files, control plane. No overlap with any in-flight
   task (none in flight).
5. **Acceptance scenarios** — S1 exclusive rename + old-name-inert guard test, S2 byte-preserved
   semantics (full token matrix + two-factor gate + non-NEXT_PUBLIC posture), S3 e2e one-name
   through both processes, S4 the explicit owner return note (the D-040-R002 deploy obligation).
6. **Gates & reviewers** — G0/G1/G4/G5; code-reviewer/qa-engineer/security-reviewer (≠ producer).
   No UI behavior change → no G3.
7. **Modularity answers** — in path_notes (pure rename inside the existing focused module; no
   threshold concern; unification direction and deployment posture stated).

## Notes

- The owner return item is precisely scoped by repo evidence: the only deployment action is a
  Render-dashboard rename IF the owner ever set the old name (the web service does not exist in
  the Blueprint yet); the API service's env is untouched.
- Thin client: web suites execute in CI only (documented_test_commands names the CI jobs).

VERDICT: PASS — packet is claimable.
