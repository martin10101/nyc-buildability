# M5-T057 G0 — contract-readiness record (phase B3 slice 1: proposal-check route)

**Task:** M5-T057 — the flag-gated internal check endpoint carrying BP-1..BP-7 +
T053-wave residual closures + the G4-advisory fold-in tests.
**Contracted at:** 2026-09-20 (session seq 121, the M5-T054 acceptance seam — B2
accepted as the 237th, unlocking the B3 route slice per the plan's order; the UI slice
B3-2 contracts at a later seam).
**Reviewer of record:** orchestrator.

## Packet completeness (start-controlled-task checklist)

- **Requirement identifiers:** the plan's B3 row (route/recalculation seam only — the
  editor UI is the follow-on slice); M5-T054-G5.md BP-1..BP-7 verbatim as the binding
  security preconditions; M5-T054-G4.md G4-1..G4-4 as fold-in test obligations; the
  T053 acceptance-seam residual list (400-cap truncation, 500-path, surrogate guard,
  shared-flag coupling); DB-034(d)/BP-6 no-emission disposition in-packet.
- **Directive refs:** D-066:R001 + D-076:R001,R002 + D-077:R002,R003 — bound this seam
  (task_ids appends + digest resyncs cbb34d6e→8a30c29f / e282bf5d→fe4275ed /
  95080f6e→da58b2e8 + audit entries, same commit); `evaluate_task_refs` → ok,
  applicable == cited (5 ids). D-077-R004 NOT cited: it was satisfied and verified at
  M5-T054 (the first derive_proposal caller); this route consumes check_proposal per
  BP-1. No contract-schema touch → no G1 (T053 precedent: flag-gated internal,
  include_in_schema=False).
- **allowed_paths / forbidden_paths:** seven allowed (new route + new api test + the
  T053 route/test pair for residuals + the T054 rules-test file for fold-ins + main.py
  mount + report); forbidden pins the accepted read-only seams (rules/, scenario/
  incl. proposal_input_gate.py, contracts, web).
- **Acceptance scenarios:** AS-1..AS-8 in-packet (end-to-end rectangle through HTTP,
  BP-2/3 boundary + capped errors, BP-4 documented compute caps, BP-5 value
  validation, BP-1/BP-6 single-entry + no-emission proof, residuals, G4 fold-ins,
  proof).
- **Gates:** G0,G2,G3,G4,G5; reviewers code-reviewer, qa-engineer, security-reviewer,
  directive-compliance-verifier (G2 recorded by orchestrator). G5's duty here is the
  explicit BP-1..BP-7 CLOSED/NOT-CLOSED ruling at the route.
- **Code graph (D-066-R001):** graph current at this seam (regenerated at the D-077
  contract seam and unchanged since for these paths); NAV BLOCK embedded naming the
  read-only seams, the T053 route consumers, and the main.py mount precedent.
- **Modularity boundary answers:** proposal_checks_api.py is a NEW single-responsibility
  route module (boundary enforcement + serialization only; engine logic stays in the
  accepted B2 module); checker in the documented commands.
- **Seeds:** the three new allowed_paths entries seeded this commit (placeholder route
  module - NOT mounted, placeholder py test, report placeholder); the other four paths
  are tracked.

## Pairwise disjointness (mechanical, D-072 + the frozen-identity rule)

Live/awaiting lanes at contract time: M5-T055 + M5-T056 (both awaiting_gate, waves
running, path identities FROZEN under their DCV conditions).
- T057 ∩ T055 = ∅; T057 ∩ T056 = ∅ (set intersection over allowed_paths: EMPTY /
  EMPTY — T057 is api routes/tests + one rules-test file; T055 is address web files +
  e2e; T056 is architect web files + condo api files; condo_records.py ≠
  proposal_checks_api.py; main.py is held by NEITHER frozen lane).
- tests/rules/test_proposal_checks.py belongs to ACCEPTED M5-T054 (free); the fold-in
  edits are additive tests binding accepted behavior, the T051→T054 pattern.

## Ledger/scope hygiene

- New allowed_paths files seeded this commit — no zero-tracked-file refusal.
- No open blocker `affects`/`detail` references M5-T057.

**Verdict: PASS — packet is contract-complete; claim may proceed at this head.**

## [ORCH-CORRECTED per DCV §3.1] Code-graph line correction (2026-09-20, seq 122)

The line above stating the graph was "current at this seam (regenerated at the D-077 contract
seam and unchanged since for these paths)" relied on a currency claim that was FALSE: 13 graph
inputs had changed since that regen (including this packet's own central seams), the cached
fingerprint was stale, and `query.py --no-regen` refused it. No regeneration was performed at
this contract seam — the D-066-R001 regeneration conjunct was NOT satisfied here. Remedy: the
graph was regenerated at the seq-122 tree (772 files / 16254 nodes / 7149 edges) and the T057
consumer/impact set re-derived and CONFIRMED in source (route consumed by main.py:36 + its test
file only; engine by the route + two test files) — the packet's navigation block was materially
accurate, so no packet change follows. Recorded per the DCV's minimal-remedy instruction; the
original text above is preserved unedited.
