# M5-T014 — G0 administrative contract check (orchestrator)

Packet completeness verified against the /start-controlled-task checklist before claim:

- Requirement identifiers: the failing web-e2e assertion (compare-screen.test.tsx ~:404,
  red on every CI run since the M5-T004 acceptance - the last non-owner-gated CI red), the
  accepted component design it collided with (NoScenarioBlock both-absent collapse), and the
  post-acceptance-discovery rule (follow-up task, never reopen accepted work).
- Directive refs: D-038:ALL (in-regime; empty-applicable row expected at accept per precedent).
- Evidence files: named exactly in inputs[] (CI run id with the full vitest DOM diff, the test,
  the two read-only context sources).
- Scope: TWO exact files (the test module + this packet's report); every component/lib/e2e path
  forbidden; dependencies=[] (M5-T004 is accepted; the initial M2-T022 entry was a contracting
  reflex corrected during packet authoring, before G0 - recorded here for transparency).
- Acceptance scenarios S1-S4: externally-recorded red, intent-preserving fixture-derived repair,
  strict no-other-change, CI-green as the only executable proof (thin-client policy: no local
  vitest exists).
- Gates: G0/G1/G4 with code-reviewer + qa-engineer, both distinct from the producer. G3/G5 not
  required: no contract change, no security surface (a one-test repair).
- Modularity boundary answers: test-only edit inside one existing test function; no production
  source touched; no thresholds implicated.

G0 verdict: PASS (administrative; recorded by the orchestrator per gate-class rules).
