# M5-T111 — G4 test review (qa-engineer "qa-t111", read-only)

> Transmission history: pinned at 0c24a8b5, delivered as 2 SendMessage part(s) ending with END-OF-REPORT;
> no truncation. Joined verbatim by the orchestrator (transport wrapper tags removed only). Verdict PASS; five ADVISORY coverage gaps
> (lock contention untested, XFF-not-trusted untested, HEAD/OPTIONS unasserted, substring AS-1 scan, route-level slot hold).

---

M5-T111 G4 independent TEST review (READ-ONLY) — part 1/2

PIN: HEAD at start = 0c24a8b5 (matches expected). Material c3e3df15 = 9 files (git show --stat confirms; MATCHES the report). Producer report claim d3e303eb→c3e3df15 corroborated.

RAN (cwd services/api, Python 3.11.9):
- `ruff check .` → All checks passed.
- `pytest tests/resilience tests/scenario/test_scene_api.py tests/drawings/test_dxf_import_api.py tests/cad/test_export_api.py -q` → 183 passed (report said 183; match).

IN-PROCESS MUTANTS I re-ran myself (CONSUMING-namespace; loaded the producer test module by path and patched its bindings — nothing written to repo):
- M2 evict-active (SlidingWindowRateLimiter._sweep_expired drops every key, rate_limit.py:137-144) → RED at test_bounded_keys_fail_closed_and_never_evict_an_active_key (test_rate_limit.py:102-123). OK.
- M4 host-first (caller_key reads host before principal, rate_limit.py:147-172) → CONSUMING patch RED at test_two_principals_from_one_host_are_distinct_keys (test_rate_limit.py:156-165). OK. (I also ran the DEFINING-module patch; it reddened only because my harness re-imports the test module AFTER patching, so the fresh `from … import caller_key` binds the mutant — a harness artifact, not a SUT property; the consuming patch is the authoritative RED.)
- M5 release-on-cancel (run_in_job_slot releases the slot on TimeoutError/CancelledError instead of only in the thread's finally, rate_limit.py:240-251) → RED at test_slot_held_until_thread_ends_not_on_await_cancel (test_rate_limit.py:204-248), in_flight==1 assertion fails. OK.
- M9 parse-before-limit on /dxf-import/draft (reordered _parse_assignment ahead of _gate_and_limit, dxf_import_api.py:456-464) via an in-memory mutated module + TestClient → control route 429, mutant 422 → reddens test_draft_rate_limit_precedes_parameter_parse (test_dxf_import_api.py:376-391). OK.
- MY OWN mutant: drop BOTH caller_key namespacing prefixes (host==principal collision) → RED at test_caller_key_falls_back_to_host_then_unknown (test_rate_limit.py:168-172). Confirms the principal:/host: namespacing is load-bearing (an anti-impersonation property).

All 4 named mutants + my own reddened for the right reason; unmutated baselines GREEN.

(part 2/2 follows: 404 byte-parity, no-weakening, coverage gaps, verdict)

---

M5-T111 G4 — part 2/2

404 BYTE-PARITY (I drove a throwaway app that includes the disabled scene router, comparing each method against a genuinely unmounted path): status 404, content-type application/json, body {"detail":"Not Found"} (empty for HEAD), NO X-Correlation-ID, NO Allow header — parity=True for GET/POST/PUT/PATCH/DELETE **and HEAD/OPTIONS**. The disabled route is byte-indistinguishable from an unmounted path; no existence leak. Confirms scene_api.py:286-289 (flag-then-method), matched in dxf_import_api.py:399-402/450-453 and export_api.py:230-233. OpenAPI-absence tests use a throwaway app that really include_router()s it, yet no path appears (test_*: no_path_in_the_throwaway_app_openapi) — a real include_in_schema=False guard, not just "unmounted".

NOTHING WEAKENED: only two tests were removed — test_rate_limit_evicts_expired_keys_and_bounds_the_key_count and test_rate_limit_evicts_a_single_key_whose_window_emptied (test_scene_api.py). Both were route-local-limiter internals; their properties MOVED to the shared pack (test_rate_limit.py: test_idle_keys_are_evicted_so_the_ceiling_readmits, test_single_key_with_an_empty_window_is_dropped_not_retained, test_bounded_keys_...). No other test def or assertion deleted in the three files (git diff 275ef85f..c3e3df15). Deadline (504 scene / 503 dxf+export), correlation-id, bounded body read (413 at/over ceiling, test_dxf:252-264), 429 shape, media/clamp — all still asserted; 183 pass. Zero new deps (rate_limit.py imports only asyncio/threading/time/collections + starlette.concurrency; requirements untouched).

COVERAGE GAPS — all ADVISORY (each AS still has a reddening test that fails for the right reason):
1. Thread-safety UNTESTED: the threading.Lock in SlidingWindowRateLimiter and JobSlots (rate_limit.py:87/95/193/204) has no concurrent test; removing it reddens nothing (single-threaded suite). Correct by inspection (tiny read-modify-write), but the "thread-safe" claim is unproven under contention.
2. X-Forwarded-For "deliberately NOT trusted" (rate_limit.py:159-165) is prose-only — no test sends an XFF header and asserts the key stays host-based; a mutant that starts trusting XFF would survive. Worth a test at PKT-H when auth lands.
3. HEAD/OPTIONS are registered (_ROUTE_METHODS) but the every-method-404 loops cover only GET/POST/PUT/PATCH/DELETE (matches AS-6 wording); I verified HEAD/OPTIONS 404 correctly, so behavior is right, just unasserted.
4. AS-1 "no local limiter remains" guard is a substring scan (test_scene_api.py:246-248: "deque"/"OrderedDict"/"_RateLimiter" not in src) — brittle to a novel-named reintroduction; the primary "route uses the shared limiter" is solidly covered by isinstance (:242-243).
5. Route-level "deadline-abandoned request holds its slot" is proven at the primitive+wiring level (unit test + 503 mapping), not end-to-end at the route (would be timing-flaky).

Concurrency determinism: test_slot_held_… uses threading.Event + injected runner + generous waits (1s/5s) and a bounded poll; the only wall-clock dependency is the 0.05s deadline, which slowness makes MORE likely to fire. Low CI flake risk.

None of the above blocks acceptance.

M5-T111 G4 VERDICT: PASS
END-OF-REPORT
