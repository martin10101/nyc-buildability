# M5-T036 G3 review — code-reviewer return (VERBATIM)

Reviewer agent: code-reviewer (spawn rev-g3-m5t036), read-only, pinned HEAD
657b238dce8af8df1bfff2e22e454caab1749413. Saved verbatim from the agent-return channel
(transport entity-decoding only).

---

G3 (code quality/correctness/modularity) verdict for M5-T036: PASS. No blocking findings; all findings advisory/minor. HEAD verified == 657b238dce8af8df1bfff2e22e454caab1749413. Full report below.

# G3 code-quality review — M5-T036 (ZoningContextPanel / DB-016 + DB-005 ZoLa unification)

VERDICT: PASS
Pinned head: 657b238dce8af8df1bfff2e22e454caab1749413 (git rev-parse HEAD matches — proceeded).
Reviewer posture: read-only. I ran only read-only inspection + the packet's one documented command (python tools/modularity_check.py --check) and JSON fixture reads; no writes, no git/gh/control-CLI writes.
Deviations accepted per your brief: (a) cycles 3-4 were evidence-transport only (no implementation change) — I judged the code as it stands; (b) thin client, no local web test — I did NOT fail for missing CI; web runtime behavior is marked UNVERIFIED throughout.

## Observations (what I read/ran)

O1. ZoningContextPanel.tsx is 128 SLOC (wc -l; 129 with the file tool's trailing count). Renders four groups from profile.zoning: districts / commercial_overlays / special_districts via the shared ZoningValueList (imported from components/property/ZoningSection), and a landmark/histdist <dl> built from mapped_features via mappedFeatureView + resolveFactProvenance + ProvenanceDisclosure. One ZoLa link via zolaLotUrl(profile.identity.bbl): link when non-null, honest absent note when null.
O2. The panel consumes the shared contracts faithfully, matching the reference idiom: the three arrays use ZoningValueList with the SAME emptyText strings as ZoningSection.tsx; the landmark/histdist rows mirror ZoningSection's mapped-features table pattern (view.provenanceRef → resolveFactProvenance → ProvenanceDisclosure records=[record]|[]). No competing schema, no legal text/logic, no computation/inference/defaulting — every value read straight from the profile (label-display-only boundary honored).
O3. Mount placement (PropertyOverview.tsx:63) is inside PropertyOverview's returned JSX only. Grep confirms <PropertyOverview> is rendered in exactly one place: ArchitectEntry.tsx:95, case "overview" (the default landing view). The other four importers named in the nav block (ProfileViews, PropertyFacts, ReportView, ScenarioWorkspace) import only the PropertyIssuesSummary / DraftHeadline re-exports, NOT the PropertyOverview component — so the panel does not leak to any of them. <ZoningContextPanel> appears only in its own file + PropertyOverview.tsx:12/:63 + tests.
O4. Print-exclusion verified in source: ReportView.tsx:11 imports only PropertyIssuesSummary from ./PropertyOverview; ReportView renders its own zoning surface (ZoningSection at :79, AdditionalZoningFlags at :80) and never renders <PropertyOverview> or <ZoningContextPanel>. AS-4 tests assert both directions (present via PropertyOverview, absent via ReportView).
O5. DB-005 unification: PropertyOverview.tsx:47 and AddressConfirmCard.tsx:58 both build the link from zolaLotUrl(...); AddressConfirmCard's local ZOLA_BBL_URL_PREFIX + encodeURIComponent template is removed. Independent grep: zero matches for `zola.planning.nyc.gov` or `ZOLA_BBL_URL_PREFIX` across all three touched components — no raw template URL remains (AS-3).
O6. modularity_check.py --check: "selected 441 files; failures 0; warnings 19" (exit 0). All 19 warnings name pre-existing tools/* files (agent_supervisor/*, context_benchmark.py); NONE of the touched files (ZoningContextPanel, PropertyOverview, AddressConfirmCard) is flagged (AS-7).
O7. Fixture backing for the test assertions (builder_output_m1_t005.json, BBL 1000010010): districts ['R3-2','C4-1'], commercial_overlays [], special_districts ['GI']. landmark="INDIVIDUAL LANDMARK" and histdist="Governors Island Historic District" both carry provenance_ref values that resolve in the provenance array. District columns zonedist1=R3-2 and zonedist2=C4-1 exist for the D5 fallback join.
O8. Typed DOM queries: panel test uses querySelectorAll<HTMLElement> at line 168; LotOutlineMap stubbed via vi.mock (same seam the dev-limits suite uses); assertions use within(panel)/getByText/queryByTestId. Test imports/helpers (ZOLA_PREFIX, renderResolved, resolvedDoc, within, baseProfile, PropertyOverview) all exist in their files.

## Conclusions (evidence-bound)

C1 (mandate 1 — correctness/clarity): PASS. The component is clear, idiom-matched to the reference components, consumes contracts rather than reinventing them, and carries no legal text/logic. Landmark/histdist use the same hasValue-based presence check as AdditionalZoningFlags, so absent-but-keyed values render "Unknown — not supplied" consistently with the reference. Basis: O1, O2, ZoningContextPanel.tsx:35-125 vs ZoningSection.tsx:84-190 / AdditionalZoningFlags.tsx:4-16.
C2 (mandate 2 — modularity/mount): PASS. Focused single-responsibility component well under thresholds; no responsibility mixing; mount reaches exactly the overview surface and nothing unintended; print-exclusion is correct and tested. Basis: O3, O4, O6.
C3 (mandate 3 — DB-005 no-behavior-change): PASS, and I independently verified the load-bearing claim. validateBblInput (lib/bbl.ts:36-72) returns canonical == the trimmed input only when it is exactly 10 digits with borough 1-5 — precisely zolaLotUrl's accepted pattern (/^[1-5][0-9]{9}$/, length 10). So zolaUrl is non-null exactly when canonicalBbl is non-null (identical render gate), and encodeURIComponent over pure digits is a no-op, so the href is byte-identical to the prior template. The producer's "strict superset" claim is accurate; this is a genuine internal refactor. Basis: O5, bbl.ts, provenance-link.ts:53-56, AddressConfirmCard diff.
C4 (mandate 4 — test quality): PASS. Assertions are real and non-tautological. The AS-1 provenance assertions ("Source for R3-2/C4-1/Landmark/Historic district") are meaningful because ProvenanceDisclosure renders its <summary> label ONLY when records is non-empty (ProvenanceDisclosure.tsx:26-33); I confirmed the fixture carries resolvable refs for all four (O7), so these assertions test the real join, not a constant. The populated-overlay test injects an overlay1 provenance record to exercise the same D5 fallback the production path uses. The .architect-map-card scoping in development-limits.test.tsx keeps the two ZoLa affordances independently asserted. Results remain UNVERIFIED until CI. Basis: O7, O8, zoning-context-panel.test.tsx, the two diffs.
C5 (mandate 5 — accessibility): PASS. section aria-labelledby + matching h2 id; proper dl/dt/dd; link target=_blank rel="noopener noreferrer"; honest absent note in a labeled span. Basis: ZoningContextPanel.tsx:41-63,95-125.
C6 (allowed-but-untouched): Correct call. Leaving ProfileViews.tsx / AdditionalZoningFlags.tsx / workspace.test.tsx untouched was right — mounting via PropertyOverview alone satisfies the packet's "PropertyOverview.tsx and/or ProfileViews.tsx"; ProfileViews.ZoningView is the zoning tab, which already renders ZoningSection, so mounting the panel there would duplicate that surface. Basis: O3, ProfileViews.tsx:20-30.

## Advisory findings (non-blocking)

F1 (minor a11y, advisory). ZoningContextPanel.tsx:57 "Open in ZoLa ↗" and PropertyOverview.tsx:58 "Open ZoLa ↗" place the ↗ glyph directly inside the link text, whereas the sibling PropertyOverview.tsx:53 wraps its arrow in <span aria-hidden="true">. A screen reader may announce the arrow character. The panel matches the nearest sibling (the Site-context link, which is pre-existing), so this is cosmetic consistency, not a regression. Route as a discovery / future polish; do not block.
F2 (minor test overlap, advisory). address-confirm.test.tsx adds a non-canonical "12345" case (added lines ~261-266) that partly overlaps the existing "a BBL that fails client re-validation" test just below it. It is still justified — it specifically proves the zolaLotUrl null contract at the surface post-migration — so this is redundancy, not a defect.

## UNVERIFIED (bounded)

U1. All vitest/web runtime behavior for the six suites — no node_modules on this thin client; I did not execute any web test. Code logic + fixtures support every assertion I traced, but CI on the pushed head is the executable authority (AS-6).
U2. TypeScript strict-mode compilation of the new/edited .tsx — not run locally; CI decides. I saw no obvious type violation on read (typed props, PropertyProfile consumed, typed DOM query present).

## Discoveries surfaced by the producer (I confirmed they are correctly routed, not fixed)
D1 (flood/transit/split-zone/zonemap mapped features not on this overview panel) and D2 (two ZoLa affordances on the overview) are surfaced in the report for backlog routing, consistent with D-069. No in-packet fix — correct.

Relevant files (absolute):
- C:\Users\MLFLL\Downloads\nyc-zoning\ctl24\apps\web\src\components\architect\ZoningContextPanel.tsx
- C:\Users\MLFLL\Downloads\nyc-zoning\ctl24\apps\web\src\components\architect\PropertyOverview.tsx
- C:\Users\MLFLL\Downloads\nyc-zoning\ctl24\apps\web\src\components\address\AddressConfirmCard.tsx
- C:\Users\MLFLL\Downloads\nyc-zoning\ctl24\apps\web\src\components\architect\ReportView.tsx
- C:\Users\MLFLL\Downloads\nyc-zoning\ctl24\apps\web\src\components\architect\__tests__\zoning-context-panel.test.tsx
- C:\Users\MLFLL\Downloads\nyc-zoning\ctl24\apps\web\src\components\address\__tests__\address-confirm.test.tsx
- C:\Users\MLFLL\Downloads\nyc-zoning\ctl24\apps\web\src\components\architect\__tests__\development-limits.test.tsx
- C:\Users\MLFLL\Downloads\nyc-zoning\ctl24\apps\web\src\lib\bbl.ts
- C:\Users\MLFLL\Downloads\nyc-zoning\ctl24\apps\web\src\lib\provenance-link.ts

Nav block accuracy (requested): the code-graph nav block was accurate and useful — PropertyOverview's five importers and the ReportView print reach matched source exactly, and the "PropertyOverview.tsx:52 inline link" DB-005 pointer landed on the right defect (now at :47/:58 post-edit). No correction needed.
