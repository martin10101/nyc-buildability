# M5-T063 G4 gate — independent QA/test-adequacy review (verbatim reviewer return) — FAIL

Reviewer: qa-engineer (opus-4-8 xhigh per D-064), dispatched by the orchestrator at the
7839035c submit seam, pinned to material f33b752c. Recorded by the orchestrator; reviewer is
read-only and records no gate itself. VERDICT: FAIL with ONE required correction (C-1 —
independently the SAME defect as G3's F1, completing the one-cluster inventory).

---

Verdict below.

---

**VERDICT: FAIL** (one required correction, C-1)

# G4 Gate Report — M5-T063 (web condo rider cluster)

## Pinned identity
- Material commit reviewed: **f33b752c** (11 files changed; `PropertyOverview.tsx` deliberately byte-unchanged and not in the changed set). Verified via git plumbing (`git show f33b752c:<path>`), independent of my worktree HEAD.
- Submitted at f98924e5; submit seam 7839035c (per packet); my worktree HEAD d8b3899f is an **ancestor** of f33b752c (merge-base = my HEAD), so f33b752c is a clean descendant — no disjoint-peer contamination of the reviewed content. Reviewed head recorded: **f33b752c**.
- 11 changed files confirmed by `git diff-tree` = the packet's allowed_paths minus PropertyOverview.tsx. No forbidden/T060-lane file touched (diff shows only the 11 allowed files). Modularity corroborated: bounded.ts 99 SLOC, rule-evaluation.ts 572 SLOC (under warn 600); my own `modularity_check.py --check` reproduces the same 20 pre-existing out-of-scope warnings (surveyReview/types.ts, mappluto_geometry_arcgis.py, agent_supervisor/*, context_benchmark.py) — none is a packet file. AS-6 modularity clean.

## AS → spec mapping and mutation-sensitivity

**AS-1 (sanitizer) — PASS, strong.** `bounded.test.ts` `describe("boundedZoningDistrict …")`:
- Suffixed/numbered/plain pinned byte-exact: "admits plain, numbered, and suffixed districts byte-exact" (M1-5, R7-2, R6, R10H, C6-4).
- Slash class pinned: "PRESERVES the slash of a special mixed-use district" (M1-5/R7-2, M1-6/R10).
- Hostile markup residue: "strips the markup delimiters (< and >) …" pins `<script>M1-5/R7-2</script>` → `scriptM1-5/R7-2/script`. **I independently confirmed this is the TRUE residue**: charset `[A-Za-z0-9/-]` drops only `<`/`>`, so the `/` in `</script>` survives → `script` + `M1-5/R7-2` + `/script`. The corrected expectation (Rework section) is correct; control-char (`\x07`) and whitespace cases also pinned to `M1-5/R7-2`.
- Over-length → `MAX_ZONING_DISTRICT_LENGTH` (32); non-strings (null/undefined/42) → null; `<<<>>>`/`""` → null.
- Two-vocabularies proof: `boundedToken("M1-5/R7-2")` → `"M1-5R7-2"` (slash still stripped). Existing helpers byte-unchanged is diff-provable (bounded.ts is purely additive) and behavior-pinned. A boundedToken revert, a charset dropping `/`, or a missing cap all fail here.

**AS-2 (honest gating) — PASS, strong.** Gating logic lives in `PropertyOverview.tsx` (byte-unchanged); the (e) tests are pre-existing (M5-T056) and exercise the real render:
- `condo-resolution-display.test.tsx:754` "(e) NO recorded zoning": `condo-divergent-notice` absent, `condo-zoning-dependency` present, names ZTLDB in plain language, no "out of scope" jargon leak.
- `:766` "(e) MIXED zoning": divergent notice present + partial gap ("not yet shown for every base lot", "recorded districts shown above are unaffected").
- `:800` new "(a) slash renders byte-exact" pins `recorded zoning: M1-5/R7-2` and `.not.toContain("M1-5R7-2")` through the real component.
- `condo-records.test.ts:463` new (a) pins `recordedZoning === "M1-5/R7-2"`, `.not.toBe("M1-5R7-2")`; pre-existing `:439` per-lot status + dependency parse and `:448` fallback-derivation (both directions) cover the "status defaulting on absent fields" gap. This packet's AS-2 contribution (sanitizer wiring) is well-tested; the notice-gating it references was already tested.

**AS-3 (announcer) — FAIL (positive branches PASS; negative branch missing → C-1).** `rule-evaluation.test.ts` `describe("… condo specifics match the visible label")`:
- condo_base_lot_unresolved: `toContain("condo billing lot")` + `toContain("site-definition confirmation")` + `not.toBe(GENERIC_MISSING_EVIDENCE)` + no `/verified/` `/best/`.
- substitution: `toContain("base tax lot")` + both BBLs + `toContain("not a computed allowance")` + `not.toBe(GENERIC_APPLICABLE_DRAFT)` + no `/verified/` `/best/`. Both positive branches are mutation-sensitive (a revert to the generic classifier fails `not.toBe(...)`; BBL substitution failure fails `toContain(BBL)`). Fallback/generic path covered by the pre-existing announcer describe block. **Gap: no test for a non-corresponding stamp, and the branch guard is genuinely weaker than the visible guard — see C-1.**

**AS-4 (wording/defs) — PASS, strong.** `analysis-identity-substitution.test.tsx` new `describe("DB-042(d)/(e) …")` (:75): pins inline "condo billing lot (the single tax lot a condo is billed under)", "the land parcel the city records as this condo", "a city record of the documented resolution", "not a computed allowance", and `queryByRole("alert")` === null (record branch stays non-alarming). The pre-existing both-directions withhold specs (:94–180 — unstamped mismatch withholds, missing-identity, non-corresponding-stamp-ignored :126–155, degenerate stamp, matching-identity-nothing) are **untouched and present** — the behavioral-unchanged proof for the withhold guard holds; the AnalysisIdentityNotice.tsx diff is copy-only inside the already-legitimate branch (`stampLegitimate` logic byte-identical).

**AS-5 (hygiene) — PASS.** `address-confirm.test.tsx:809` describe label now "S11 — DB-033 confirm-arc polish riders (a-d; rider-i cap superseded by rider-c a11y honesty)" and the block comment is corrected to name the true scope. Change is label/comment only (no test-logic delta), no empty spec files, DOM queries typed.

**AS-6 (preservation) — PASS.** PropertyOverview.tsx byte-unchanged (export/props compatible); bounded.ts additive-only; no forbidden/T060-lane file in the diff; modularity exit 0 corroborated; web CI green at f33b752c per orchestrator evidence (thin client — CI is the authority, not local reasoning).

## Findings

**C-1 (REQUIRED CORRECTION — test-adequacy + reachable behavior defect, AS-3 / DB-042(c)/HJ-3).**
The announcer substitution branch this packet introduced (`rule-evaluation.ts:530–543`) guards only on `entered_bbl` present + `analyzed_bbl` present + `analyzed_bbl !== entered_bbl`. The visible guard it is meant to mirror (`AnalysisIdentityNotice.tsx:54–61` `stampLegitimate`) additionally requires `entered_bbl === evaluated_input.bbl`. The announcer has `document.evaluated_input.bbl` in hand but does not apply that check.

Reachability (confirmed in source): the contract validator (`rule-evaluation-contract.ts:409` `checkSubstrateSubstitution`) is positive-shape only ("no legal or substitution meaning is judged here, only shape"), so `entered_bbl` may be any non-empty string. In `ArchitectEntry.tsx`, the announcer (line 78) is reached only when `identityEvaluation` is set, i.e. `evaluated_input.bbl === profile.identity.bbl`, and `AnalysisIdentityNotice` (line 174) renders the same document. So a shape-valid document with `evaluated_input.bbl = <opened BBL>` but `substrate_substitution.entered_bbl = <a different BBL>`, `analyzed_bbl = <another BBL>` produces:
- Visible: `stampLegitimate` false (entered_bbl ≠ requestedBbl) and `analyzedBbl === requestedBbl` → **nothing rendered**.
- Announcer: fires the substitution string → aria-live announces "analyzed on the recorded base tax lot {analyzed} for the condo billing lot {entered} **you entered** …".

A screen-reader user hears a substitution naming two BBLs shown nowhere and a **false "you entered" claim**, while the sighted user sees a normal evaluation with no substitution notice — a screen-reader/visible divergence, which is the exact HJ-3 anti-pattern this rider exists to close. The visible component has an explicit test for this document shape (`analysis-identity-substitution.test.tsx:142` "ignores a stamp whose entered_bbl disagrees with evaluated_input.bbl"); the announcer has no analog, so the divergence is both unimplemented and untested (the pack passes it).

Fix: in the announcer's substitution branch, also require `substitution.entered_bbl === document.evaluated_input.bbl` (mirroring the reproducible portion of `stampLegitimate`), so a non-corresponding stamp falls through to the generic classifier; add a mutation-sensitive test asserting that a stamp with `entered_bbl !== evaluated_input.bbl` announces the generic classifier string (not the substitution string). Route as this packet's rework (rule-evaluation.ts + rule-evaluation.test.ts are both in allowed_paths).

**N-1 (non-blocking note).** `boundedZoningDistrict` admits a lone/leading/trailing slash: `boundedZoningDistrict("/")` → `"/"`, `"/R7-2"` → `"/R7-2"`. The null-case test pins `<<<>>>`→null and `""`→null but not `"/"`. This is acceptable — the helper is display-only, byte-faithful provenance (D-073-R006) and never validates district semantics (that is the API's job), and a lone slash is inert; but the behavior is unpinned in either direction. Optional: add a `"/"` case to lock the intended contract. Not required for acceptance.

## Reproducible commands
- `git show f33b752c:<path>` for all 11 files + `rule-evaluation-contract.ts`, `rule-evaluation-fixtures.ts`, `ArchitectEntry.tsx` (co-render confirmation).
- `git diff-tree --no-commit-id --name-status -r f33b752c` → the 11 files.
- `python tools/modularity_check.py --check` (my worktree) → 20 pre-existing warnings, none a packet file.

Net: AS-1, AS-2, AS-4, AS-5, AS-6 pass with strong mutation sensitivity; AS-3 has thoroughly-tested positive branches but a reachable, untested divergence between the new announcer guard and the visible guard that undermines the rider's own accessibility objective. **FAIL** pending C-1.
