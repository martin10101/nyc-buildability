# M5-T069 — G2 producer self-check record (recorded by the orchestrator, role self_check)

Producer: backend-engineer (loop-2 worker, run persistent2-local-29). The producer's broker
could not run the services/api-scoped commands (the broker-cwd [BLOCKED]-evidence class,
T067 precedent) and routed them to the orchestrator harvest with an explicit recipe — the
report's [WORKER-OBSERVED] vs [BROKER-LIMIT→harvest] legend keeps the provenance honest.

- **Scope self-check:** material 463f98f9 = exactly the packet's four allowed_paths; the
  FROZEN scope-before-status transition order is preserved (the producer's report walks the
  order; the foreign-probe 404 lands before the status read).
- **Consumer sweep (producer, in-run):** no out-of-scope suite pins any old supersede
  not-found literal; revoke text byte-preserved.
- **Harvest execution of the recipe (orchestrator, cwd services/api, wt-m5t069):** ruff clean;
  scoped suite **77 passed** (the expected 74+3); the SIX-mutation matrix all RED at exactly
  the predicted first-failing assertions (A1/A2/C1/C2 → records :878; B1/B2 → :907) with
  hash-verified restores (D_cand 89f998f5…) and a final green; modularity exit 0. Every
  [PREDICTED] row elevated to OBSERVED (recorded in the report's [ORCH-HARVEST] section).
- **CI (authoritative):** run 35769445255 at a66cf818 — api job green on BOTH attempts.
- **Identity:** in-worktree c96e5387 cherry-picked to 463f98f9, ALL-MATCH x4 (LF-normalized);
  frozen submit identity at 141007c3.

G2 verdict: PASS (self_check; never independent review — G3/G4/G5/DCV follow).
