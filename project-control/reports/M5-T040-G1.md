# M5-T040 — G1 data-contract review (verbatim reviewer return; transport entity-decoding only)

Saved by the orchestrator from the data-contract-verifier agent return, 2026-09-18 (seq-118
wave, pinned at submission sha b4865d1c). Nothing added or removed beyond decoding transport
HTML entities.

---

I have completed the full data-contract review. All six criteria trace cleanly through the actual source at the pinned submission. Here is the gate report to record.

---

# G1 Data-Contract Gate Report — M5-T040 (named-street override WIRING)

**Task:** M5-T040 · **Reviewer:** data-contract-verifier · **Reviewed identity:** `b4865d1c` (submission), material commits `987930a2` (run-45) + `751a1826` (run-46) on `candidate/D-024-mrl-option-b` · **Verdict: PASS** (no BLOCKING findings; 3 ADVISORY)

## Scope / identity checks
- HEAD `7b7e7faf` is one bookkeeping-only commit past the pinned `b4865d1c`; `b4865d1c` is an ancestor of HEAD; the three key source files are clean in the working tree, so on-disk state equals the pinned submission. `named_street_override.py` is not touched in `751a1826`, so its final state = `987930a2`.
- **File-union ⊆ allowed_paths: PASS.** All 11 files touched across both material commits are inside `M5-T040.json` allowed_paths; no forbidden/out-of-scope path. Contract files (schema/generated-TS/`integration.py`/`response.py`) appear in neither commit.

## Findings (numbered)

1. **(criterion 1 — tri-state consumed exactly) PASS.** `build_named_street_override_status` (`services/api/app/rules/wide_street_wiring.py:363-487`) maps `MATCHED_OVERRIDE` → decisive match status (`:418-450`), `INDETERMINATE` → unresolved/refusal (`:451-453`), `NOT_MATCHED` → clears *only* when `_fully_resolved_typed_inputs` passes else unresolved (`:454-464`). The extra input re-gate on `NOT_MATCHED` is a fail-closed hardening, not a reinterpretation of the matcher's meanings. No field-semantics guessing.

2. **(criterion 2 — exceptions_checked only on all-NOT_MATCHED fully-resolved) PASS.** `_elevated_exceptions_checked` (`wide_street_wiring.py:269-290`) returns True only when `matched_override is None`, NOT `(segment_may_touch and not implemented)`, and all decisions wide/narrow. The sole production status with `segment_may_touch=False` is the clean all-NOT_MATCHED return at `:478`, reachable only after every segment passes `_fully_resolved_typed_inputs` (`:335-360`, independent of the matcher's coercing short-circuit). Sweep of every `NamedStreetOverrideStatus(` construction in `app/` confirms no other site attests (`:404`,`:435`,`:466` all fail-closed; provider `wide_street_live_provider.py:197` fail-closed). The live provider supplies `community_district=None` and both cross-streets `None` (`wide_street_live_provider.py:183-192`), so the live path can never attest `exceptions_checked=True` via this leg. **No path attests with a missing input.**

3. **(criterion 3 — refusal provenance verbatim) PASS.** MATCHED branch (`wide_street_wiring.py:419-449`) reads `prov.snapshot_sha256`, `prov.section_anchor`, `prov.matched_row_verbatim`, and `result.provision_id` straight from the matcher's `OverrideProvenance`/`MatchResult` (built at `named_street_override.py:662-673` from the snapshot's digest-covered `verbatim_source_quote` and `content_digest_sha256`). Values pass through verbatim; only `reason` is a composed string, and it is a separate field, not a provenance element. Note: the packet/prompt says "quintuple" but names four provenance elements — all four are carried verbatim.

4. **(criterion 4 — two-layer attestation distinction) PASS.** Provider `_policy_decisions` DB-021c docstring (`wide_street_live_provider.py:346-362`) plus `_UNATTESTED_EC5` (`:148-157`) keep the D-052 width-policy layer (`AttestedPreconditions.exceptions_checked=True`) distinct from the ZR 12-10 EC-5 layer (`named_street_override_checked=alternate_width_clause_checked=False`). Wiring `_elevated_exceptions_checked` docstring (`:273-282`) matches its code. The wiring change flips the ZR 12-10 named-street leg (layer 2, now driven by the real matcher); the B4 engine's EC-5 preconditions remain an independent, still-unattested gate. Docstring contract matches code.

5. **(criterion 5 — no numeric alternate-width) PASS.** `classify_alternate_width_district`/`AlternateWidthResult` are defined in `named_street_override.py:762-787` but never called by the wiring or provider (grep of `app/`). A matched override yields `far_row=FAR_ROW_NONE`/professional review (`wide_street_wiring.py:522-534`); `select_far_row_value` returns `None` for any non-firing row (`:719-723`). No alternate-width value is applied numerically anywhere.

6. **(criterion 6 — contract v1.1.0 byte-untouched) PASS.** Git confirms no schema/generated-TS/`integration.py`/`response.py` in either material commit. The new dataclass field `named_street_override_match` is referenced only in `wide_street_wiring.py` (+ tests); `_wide_street_summary` (`integration.py:552-573`) is field-selective and does not read it, so the serialized document vocabulary is unchanged. `named_street_override_pending` is a pre-existing contract field (`_contract_schemas/v1/rule_evaluation.schema.json:460,501`, untouched).

## Advisory (non-blocking)

- **A1 (ADVISORY, modularity):** `wide_street_wiring.py` is 723 SLOC — above the 600 warn threshold, below justify(750)/hard(1000). `modularity_check --check` = 0 failures (producer-disclosed cohesion signal). Route to code-reviewer before further growth; not a G1 concern.
- **A2 (ADVISORY, out of G1 scope):** `ReportView.tsx` is in allowed_paths and DB-025(b) requires ReportView to gate uniformly on `determination_state`, but `ReportView.tsx` itself is unmodified in both material commits (only `report-view.test.tsx` changed). Whether the DB-025(b) gating actually landed for ReportView is a UI/display matter for code-reviewer/human-journey-reviewer, not the data-contract gate — flagged for routing.
- **A3 (ADVISORY, provenance clarity):** With the pinned snapshot the MATCHED_OVERRIDE branch is currently unreachable in production — `disposition_when_located="indeterminate"` makes a located Broadway/Allen row return INDETERMINATE (`named_street_override.py:806-831`), and the provider never supplies CD/cross-street bounds. The MATCHED_OVERRIDE wiring is correct-by-construction but exercised only by tests until a future snapshot flips the disposition (passing the DB-023a unconditional-source binding) and a connector supplies CD + cross-street bounds. This is the intended conservative-refusal design, not a defect.

## Evidence basis
CI is orchestrator-captured (read-only reviewer, thin client — not re-run): `M5-T040-ci-evidence.md` records CI run `35407153078` success at `4d8847c6`, with `git diff 751a1826..4d8847c6 -- apps/ services/ packages/` empty (bookkeeping only), so the green CI speaks for the material identity. Local suite counts (98 / 38 / 475 passed; ruff clean; modularity 0 failures) are orchestrator-reproduced in `wt-m5t040`.

## Exact read-only commands run
- `git show --stat --oneline 987930a2` ; `git show --stat --oneline 751a1826` ; `git show --stat --oneline b4865d1c`
- `git rev-parse HEAD` ; `git log --oneline b4865d1c..HEAD` ; `git merge-base --is-ancestor b4865d1c HEAD` ; `git status --porcelain <3 source files>`
- `git show --name-only --pretty=format: 987930a2` + `... 751a1826` piped to `sort -u` (file union); filtered for `integration.py|response.py|schema|generated|contract|.ts`
- `git log --oneline 987930a2^..b4865d1c -- services/api/app/rules/integration.py services/api/app/rules/response.py services/api/app/_contract_schemas/`
- `wc -l services/api/app/rules/wide_street_wiring.py`
- Read: `M5-T040.json`, `M5-T040-producer-report.md`, `M5-T040-ci-evidence.md`, `docs/research/zr-snapshots/v1/zr-12-10.snapshot.json`, `named_street_override.py`, `wide_street_wiring.py`, `wide_street_live_provider.py`, `integration.py:552-591`
- Grep (`app/`): `named_street_override_match|_wide_street_summary|named_street_override_pending`; `classify_alternate_width_district|AlternateWidthResult|alternate_width`; `NamedStreetOverrideStatus\(|build_named_street_override_status|determine_wide_street_far`

**VERDICT: PASS** — the wiring consumes the accepted matcher's typed tri-state without reinterpretation, attests `exceptions_checked` only on genuinely-resolved all-NOT_MATCHED inputs (never on a missing input on any traced path), carries override provenance verbatim on refusals, preserves the two-layer attestation distinction with docstrings matching code, applies no alternate-width value numerically, and leaves the rule_evaluation v1.1.0 contract and serialized vocabulary byte-untouched. Advisories A1–A3 are non-blocking and routed to the appropriate reviewers/orchestrator.

Relevant files (absolute):
- `C:\Users\MLFLL\Downloads\nyc-zoning\ctl24\services\api\app\rules\wide_street_wiring.py`
- `C:\Users\MLFLL\Downloads\nyc-zoning\ctl24\services\api\app\spatial\wide_street_live_provider.py`
- `C:\Users\MLFLL\Downloads\nyc-zoning\ctl24\services\api\app\rules\named_street_override.py`
- `C:\Users\MLFLL\Downloads\nyc-zoning\ctl24\services\api\app\rules\integration.py`
- `C:\Users\MLFLL\Downloads\nyc-zoning\ctl24\docs\research\zr-snapshots\v1\zr-12-10.snapshot.json`
- `C:\Users\MLFLL\Downloads\nyc-zoning\ctl24\project-control\tasks\M5-T040.json`
