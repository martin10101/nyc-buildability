# M5-T058 G2 gate — producer self-check (recorded by the orchestrator)

G2 is the producer self-check gate; the CLI records it under --reviewer orchestrator (it
rejects the producer's own name). Recorded at the 5f7b032e submit seam for material 7d02eddc
at reviewed head 817a6546.

## Producer self-check record (loop-2 worker, runs persistent2-local-21..25)

The loop worker built the 19-file cross-layer material over runs 21-25 (12/19 files by run 23;
completed by run 25) and, under the in-loop reviewer's REVISE rulings (final checkpoint
M5-T058-rev7-evidence-handoff at the S13.8 breaker), converged its report to evidence-handoff
honesty: its own in-run command outcomes are recorded as that unit's outcomes with the failed
transcripts preserved, the preexisting-failure claim qualified, supervisor-owned digest-bound
evidence collection requested (explicit cwd + interpreter version), and CI/commit/push
retained as orchestrator-owned steps. The producer report (part of the material) is the
self-check artifact.

## Orchestrator corroboration at harvest (explicit cwd, wt-m5t058 at the reviewed tree; Python 3.11.9)

- `python -m ruff check .` (cwd services/api) — exit 0, "All checks passed!"
- Targeted 4-file pytest (rule_evaluation api/contract, rules integration, live_provider)
  (cwd services/api) — 168 passed, exit 0
- `python -m pytest -q --ignore=tests/documents` (cwd services/api) — 3086 passed / 2 failed,
  BOTH characterized: (1) the recorded local-3.11 ast.parse artifact on documents/units.py
  PEP-695 syntax (proves on CI 3.12); (2) the frozen-contract guard pinning the pre-1.2.0
  enum — the legitimate consumer of this contract change, fixed as the routed
  [ORCH-CORRECTED] commit 817a6546 (M5-T037 precedent) and green after the update (60 passed)
- `python tools/modularity_check.py --check` (repo root) — exit 0, failures 0
- LF-normalized digests of all 19 files verified byte-equal after cherry-pick into ctl24 at
  7d02eddc (ALL-MATCH)
- CI at 817a6546: CI (11m32s, incl. the web jobs — the material's only web proof, thin
  client) / secret-scan / context-budget all completed success

G2 verdict: PASS — the self-check obligation is satisfied by the producer's converged honest
evidence-handoff report plus the orchestrator's corroborating explicit-cwd evidence; the
independent review is the G3/G4/G5 wave + HJ + DCV (recorded separately).
