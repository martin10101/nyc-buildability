# Batch resume note — 2026-08-05 (D-009 + M0-T019 + M2-T014)

For a fresh session to resume this batch. Authoritative state is the ledger on branch
`control/D-009-depsec-and-m0t019-dispatch` (pushed) + git + these worktrees.

## Where things are
- **Batch branch:** `control/D-009-depsec-and-m0t019-dispatch` (pushed to origin), based on origin/main `d5d9b50` (post-D-008). Carries: D-009 directive, M0-T019 + M2-T014 ledger, and BOTH producers' deliverables (merged in). Head ~`194f63f`. **Not merged** — accumulates to one owner-accept batch (owner batching rule 2026-08-04).
- **Worktrees:** `C:/Users/MLFLL/Downloads/nyc-zoning/ctl` (batch/control), `.../t19` (M0-T019 code @ `2e31711`), `.../t14` (M2-T014 research @ `ed12721`).
- **D-009** — fresh owner-activated governance directive (2026-08-04) authorizing M0-T019's `.github/workflows/` + `CLAUDE.md` edits. Registry validates clean.

## Task states
- **M2-T014** (survey Packet A): `awaiting_gate` — **G0 ✓ G2 ✓ G3 ✓** (data-contract-verifier PASS, independently live-verified). Gate-complete. 3 non-blocking cosmetic corrections noted in its G3 report. Rostered security-reviewer leg covered within the G3 review; separate agent deferred (Fable 5). **Accept blocked only by dependency M0-T019 + owner batch accept.**
- **M0-T019** (frontend security, full scope): `claimed`. **G0 ✓**. Producer done (`2e31711`): package pins (next 15.5.21 / react 19.1.2 / **postcss 8.5.23** — deviation from packet's 8.5.10, which is advisory-hit; producer chose minimum advisory-free ≥7-day version — a re-verification, NOT a waiver), age gate + 32 tests, 3 CI workflows, policy doc, CLAUDE.md rule. G2/G3/G4/G5 NOT yet recorded.

## Blockers / next actions
1. **Reviewer model:** owner rule 2026-08-05 — gate reviewers fall back Fable 5 → `claude-opus-4-8` + `effort: xhigh` while Fable is unavailable; revert to Fable when owner says "Fable is back". The 5 reviewer agent files are already flipped to opus-4-8 xhigh, but **pins are cached at session start** — this only takes effect on a FRESH session. Revert files to `claude-fable-5` (no effort key) when Fable returns. See memory `reviewer-model-fallback`.
2. **M0-T019 gates:** dispatch G3 (code-reviewer) + G5 (security-reviewer) + G4 (qa-engineer) on opus-4-8 xhigh. Brief them: the committed `apps/web/package-lock.json` is still the pre-patch 15.3.4 tree (thin-client — no local npm); it's regenerated by `.github/workflows/generate-lockfile.yml`. Treat the package.json↔lock mismatch as an orchestrator-pending CI step, not a defect.
3. **Lockfile CI wrinkle:** `generate-lockfile.yml` is `workflow_dispatch` but **not on `main`**, so it can't be triggered via workflow_dispatch until it reaches main. Resolve as part of the merge/verify sequence (get the workflow onto main, dispatch on the branch, regenerate + validate the lock, then run ci.yml on the regenerated lock). Do this BEFORE recording M0-T019's gates so their content-identity isn't stale.
4. **Order for M0-T019:** lockfile CI (final lock + audit-zero) → submit (content-identity at HEAD) → G2 self-check → G3/G4/G5 → then the batch is accept-ready.

## Standing rules in force
Owner escalation boundary (D-008): stop-ask only for credentials/payment or legal sign-offs, else drive the chain. Batching: accumulate at submit, surface the whole batch once with accept lines. Nothing merges/accepts without the owner's typed line (D-004-R721).
