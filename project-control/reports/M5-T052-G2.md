# M5-T052 G2 — producer self-check record (recorded by orchestrator)

The producer's self-check is the report's §6 (verification with explicit cwds), notable for
its HONEST reconciliation of the supervisor's wrong-cwd suite failures as invocation
artifacts distinct from the worker's real passes, and for §1 documenting the
guard-coherence ruling with its timing evidence (the ruling entered the CONTRACT at
caed4644, before any code).

Orchestrator reproduction at harvest (wt-m5t052, documented cwds): ruff clean; 15 route +
454 api + 697 rules tests; modularity exit 0. CI: run 35451055372 all green at a6f35a0f
(M5-T052-ci-evidence.md) after one root-caused round (the 2/1151 boundedToken timestamp
corruption, fixed tagged).

The G3 "PASS with required corrections" cycle: the report's "byte-unchanged" guard claim
(three sites) and "SHRANK" claim were FALSE (the guard was a semantics-preserving refactor,
test-proven; the component grew within limits) — corrected as tagged [ORCH-CORRECTED per
G3] edits, counts harmonized (15/454), digest row resynced, task re-frozen at e9abeaaf via
the rework walk; ALL FIVE reviewers delta-attested their PASS CARRIES to e9abeaaf with the
corrections satisfied (attestations saved verbatim in each gate report).

Ledger-note correction (G1 ADVISORY-B): the accepted condo resolver lives at
services/api/app/connectors/condo_base_lot.py — NOT app/rules/ as the packet nav text and
some prior notes said. The route imports the correct path; the stale pointer is corrected
here rather than by editing the frozen packet.

Verdict: PASS.
