# M5-T053 G0 — contract-readiness record (B3-scaffold slice 1)

**Task:** M5-T053 — proposal validation route + the DB-034(a)/(b) input hardening.
**Contracted at:** 2026-09-19, session seq 119, the M5-T052 harvest seam (D-075 window;
B0 accepted, so the plan's "B1 ∥ B3-scaffold" parallel slot opens; DB-034 binds (a)+(b)
to exactly this first editor-input wiring packet).
**Reviewer of record:** orchestrator.

## Packet completeness (start-controlled-task checklist)

- **Requirement identifiers:** docs/DISCOVERY_BACKLOG.md row DB-034 items (a)+(b)
  (verbatim from the T048 G5 gate return, M5-T048-G5.md); the plan's B3 row
  (docs/PROPOSAL_EDITOR_PHASED_PLAN.md) for the scaffold slice's place; DB-023c
  bounded-repr precedent; the D-076-R002 no-derived-value echo discipline.
- **Directive refs:** D-066:D-066-R001 + D-076:D-076-R001,D-076-R002 — bound this seam
  (task_ids appends + digest resyncs + audit entries); `evaluate_task_refs` → ok,
  applicable == cited (3 ids). D-076-R003 not cited (no contract-schema scope).
- **Evidence files named in SCOPE:** DB-034 row; the plan's B3 row; proposal.py
  (READ-ONLY); M5-T048-G5.md (the originating worst-case arithmetic).
- **allowed_paths / forbidden_paths:** five allowed (two new modules + main.py mount +
  the new test file + report); forbidden pins proposal.py/contract.py/derivation.py and
  tests/scenario/ — the LIVE M5-T051 lane owns them — plus schemas/rules/spatial/web.
- **The disjointness-driven design decision is IN the packet** (the wrapper input-gate
  module): recorded explicitly so reviewers judge it as a deliberate boundary, with the
  fold-inward option noted for a post-T051 follow-up.
- **Acceptance scenarios:** AS-1..AS-8 (budget boundary incl. the many-levels probe the
  per-outline ceilings would admit; string ceilings + bounded-repr; route discipline;
  the zero-derived-value echo; B0 fixture passthrough; the monotone-gate property;
  purity/disjointness; proof).
- **Gates:** default fuller set G0,G2,G3,G4,G5; reviewers code-reviewer, qa-engineer,
  security-reviewer, directive-compliance-verifier (G2 by orchestrator). Security is
  the natural lead reviewer — the packet exists to close its own T048 findings.
- **Code graph (D-066-R001):** regenerated at this seam; NAV BLOCK in inputs[3] names
  the validator's consumers and flags the LIVE-lane ownership explicitly.
- **Seeds:** all four new files seeded this commit (py placeholders; empty-py-test rule).

## Pairwise disjointness (mechanical, D-072)

Live lanes at contract time: M5-T051 (loop-2 run 16: derivation.py, proposal.py,
tests/scenario/**, fixtures) and M5-T052 (harvested, awaiting submit: condo route/client
+ architect components).
- T053 ∩ T051 = ∅ — enforced STRUCTURALLY: T051's files are in T053's forbidden_paths;
  the input gate is a new module; the only shared file class is none.
- T053 ∩ T052 = ∅ except **services/api/app/main.py** — both add one mount line. T052 is
  HARVESTED (its main.py change is committed at 46bf1105 and its lane is closed to new
  producer work; the T052 worktree will not move again), so no two LIVE producers share
  the file; the T053 producer works on a worktree based AT/past the T052 cherry-pick and
  appends its own mount line. Recorded as the one deliberate sequential-overlap with a
  closed lane (the D-072 rule targets CONCURRENT writers; none exist here).

## Ledger/scope hygiene

- Seeds committed this commit — no zero-tracked-file refusal.
- No blocker `affects`/`detail` references M5-T053.

**Verdict: PASS — packet is contract-complete; claim may proceed at this head.**
