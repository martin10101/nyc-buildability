# M5-T096 — G3 code review (code-reviewer "cr-t096", read-only)

> Transmission history: pinned at 49dcdbd2, delivered as two SendMessage parts (1/2, 2/2) ending with END-OF-REPORT;
> no truncation. Joined verbatim by the orchestrator (transport wrapper tags removed only). Verdict PASS; advisories
> (Linux byte-identity is CI's to prove - the a612652b api job was green; the unreachable span floor; a future
> dxf_writer split behind a facade) are routed at the accept seam.

---

M5-T096 G3 code review (read-only) part 1/2 — bottom line PASS, no blocking findings.

PIN: HEAD = 49dcdbd2 at start (== expected). Material commit d59cbfca. Method: read the full dxf_writer.py + both test files + README + .gitattributes; diffed dxf_writer.py and both tests against d59cbfca^; recomputed the three raw blob digests; ran the allowed ruff + pytest. Independent evidence [OBSERVED] on Windows/py3.11: `ruff check .` = All checks passed; `pytest tests/cad/{test_dxf_writer,test_cad_owner_samples,test_pdf_sheet_writer,test_glb_writer}.py -q` = 277 passed. Raw blobs match README/report exactly: dxf 7936B/4e19c669…, pdf 1839B/8f28f12d…, glb 2460B/0bc7a37d…; `git check-attr` shows binary:set, text:unset on all three.

STYLE/VPORT code (dxf_writer.py:529-652): the diff is PURELY additive in serialization — `_vport_frame` + `_write_vport_table` prepended, `_write_style_table` appended, and the old inline LTYPE/LAYER bodies extracted to `_write_ltype_table`/`_write_layer_table` with byte-identical content; `_write_header`/`_write_entities` untouched (grep for CLAIM_CLASS_WORDS/PROPOSED_LABEL/INSUNITS/_write_text/etc. finds no changed lines). `_vport_frame` math (dxf_writer.py:529-544) is correct: centre = extents midpoint; `span = max(width/aspect, height, 1.0)`, view_height = span*1.1 ⇒ view_height ≥ height and view_height*aspect ≥ width (both coverage assertions in test_as1_vport_active_frames_the_extents hold with a 10% margin). Degenerate zero-size extents floor at span=1.0 → height 1.1 > 0; aspect is a constant 1.0 so no div-by-zero. Deterministic: every real goes through `_format_real` (%.6f, −0.0 normalized). For the golden fixture this yields centre (1050.0, 2035.0), height 110.0 — matches the report.

Golden re-pin (test_dxf_writer.py:53) 2d8988d6→6a8dbd94: reason recorded inline + in the report and honest — only the VPORT (before LTYPE) and STYLE (after LAYER) tables are inserted; LTYPE/LAYER/HEADER/ENTITIES bytes do not change. `test_as1_golden_digest_is_byte_stable` passes at the new digest, so the new bytes are pinned. AS-2 backward-compat is empirically confirmed: test_pdf_sheet_writer.py passes unchanged inside the 277, and no existing public name changed (pdf_sheet_writer imports CLAIM_CLASS_WORDS — byte-unchanged).

(part 2/2 follows: samples/D3, allowlist, README, D1, modularity, scope, verdict.)

---

M5-T096 G3 code review part 2/2 — findings continued and verdict.

Sample builder D3 (test_cad_owner_samples.py:83-101): LEGITIMATE public-API use, not mutable-internals reliance. `DxfDocument` is a non-frozen dataclass; `texts` (a list) and `extents_min/max` are public fields; appending a public `TextLabel` and reassigning extents mirrors what production `build_site_plan_document` itself does at dxf_writer.py:915. `serialize_document` re-validates the appended label. The GLB/PDF/DXF all encode the SAME synthetic lot (100×80) + building (60×40, 42ft) — I checked the GLB's hand-built local coords (20,20,80,60,0,42 / 0,0,100,80) against SAMPLE_BUILDING/SAMPLE_LOT and they match, so the README's "same made-up example" is accurate.

Import allowlist (test_dxf_writer.py:549-578): AST-based, non-vacuous (asserts each allowlisted module IS imported), load-bearing (prepended `import json` is detected as outside). It REPLACES a weak six-name regex denylist that would have passed on `ezdxf`/`json`/`os` — strictly stronger, not a weakening. Old test_cad_owner_samples.py was a placeholder; no coverage lost.

README: plain English, accurate (dimensions/units/4 layers/stamps all match the parsed file), honest that AutoCAD-open is a pending owner check ("Did each file open?", "Opening the files only proves the export works… does not mean any building is permitted or legal"). sha256 table matches the committed bytes.

D1 (casing): ACCEPTABLE. DXF/PDF carry upper-case "PROPOSED - NOT A CITY RECORD"; the GLB carries title-case (its writer is a forbidden accepted path). Semantic honesty holds per file; asserted in test_as3_every_file_carries_a_proposed_not_a_city_record_stamp; disclosed as DSC-1 for later normalization.

Modularity D5: 671 SLOC WARN band, CI exit 0, cohesion justification recorded. A split was IMPOSSIBLE here (allowed_paths permit only dxf_writer.py). Acceptable; ADVISORY: the next non-scope-locked touch should split model/serialization/builder behind a facade preserving the public names pdf_sheet_writer imports.

Scope: exactly the allowed paths; zero new deps; no route/main.py/web change; DWG untouched; no forbidden path modified (pdf/glb writers only imported read-only for samples). .gitattributes is a dotfile under docs/samples/cad/ — required for byte-identity; confirm the allowed_paths glob accepts dotfiles (D2), non-blocking.

ADVISORY (non-blocking): (a) byte-identity was generated on Windows/py3.11 — CI on Linux/py3.12 is the authoritative proof; low risk (all formatting is platform-stable, files binary-marked). (b) `_vport_frame`'s degenerate span=1.0 floor is unreachable via the public builder and has no direct unit test — defensive only. (c) format-authority (VPORT R12 codes/table order) is G1's remit and already PASSED.

M5-T096 G3 VERDICT: PASS

END-OF-REPORT
