# M5-T115 — human-journey review (human-journey-reviewer "hj-t115", read-only)

> Transmission history: pinned at ba48acad (material 75c03f25; CI run 36102902944 green at bfefd934, byte-identical
> sources), delivered as 3 SendMessage parts ending with END-OF-REPORT; no truncation. Joined verbatim by the
> orchestrator (transport wrapper tags removed only). Verdict PASS with four ADVISORY items (HJ-1 label
> proximity, HJ-2 'map CSS' wording, HJ-3 two role=note, HJ-4 scope calibration).

---

M5-T115 HUMAN-JOURNEY REVIEW (G3, READ-ONLY) — part 1/3

PIN: HEAD ba48acad at start. Material 75c03f25 present; claim seam 01856916 present. All 7 code/test P2 files are BYTE-IDENTICAL between material 75c03f25 and the green CI head bfefd934 (git hash-object verified), so CI run 36102902944 (`web-e2e (vitest + Playwright)` = success; web build/lint/typecheck = success; modularity = success) proves exactly the code I walked. Walked from source + the P1 spec §5.1/§5.2 + the four e2e/unit specs + the CI log (no local browser — thin client).

WHAT AN OWNER ACTUALLY SEES DIFFERENTLY (plain answer to the packet's question):
P2 is a MODEST, additive, meaning-preserving slice. Four real visible changes, everything else is re-cited/re-proved prior behaviour (the producer says this openly in report §0 — honest, not misrepresented):
1. An always-visible "Internal build" badge + one-line environment note, and a "Preliminary analysis — professional review required before any reliance" line, on the search screen at EVERY width (ArchitectEntry.tsx:76-79, :100). This is the most valuable win and closes a REAL gap: the shell's internal-build disclosure is a COLLAPSED `<details>` (ArchitectShell.tsx:24-27) that is `display:none` ≤700px (architect.css:138), and the professional-review footnote lives inside the hamburger nav — so on a phone a first-time analyst previously saw NEITHER the internal-build warning NOR the review requirement on the search surface. Now they always do.
2. A static "City-matched address" label above the big matched line on the confirm card (AddressConfirmCard.tsx:195-199) — sharpens the entered-vs-matched distinction (AS-2).
3. The BBL path reframed as "an alternative to the address search above, not a separate step" (ArchitectEntry.tsx:83), plus distinct red styling + `aria-invalid` for the four verbatim lib/bbl.ts errors (ArchitectEntry.tsx:87, :96; CSS :248).
4. Search/confirm/autocomplete CSS polish (architect.css:243-252) — including making the two autocomplete recovery buttons a wrapping flex row that stacks full-width ≤700px.

AS verdicts: AS-1 PASS (one area; suggestions keyboard+touch, no auto-pick AddressAutocomplete.tsx:170-206; each typed error names a distinct, NON-CIRCULAR next step via two message tables ERROR_MESSAGES/FULL_SEARCH_ERROR_MESSAGES :39-60; manual fallback keeps typed text AddressResolutionScreen.tsx:225-229). AS-2 PASS (label + entered/matched/PLUTO distinct; absent when no normalized street). AS-3 PASS (label paints synchronously; record line appends after BOTH actions AddressConfirmCard.tsx:432-442; CI pixel-CLS e2e green). AS-4 PASS-unchanged (map re-cited, not even touched). AS-5/AS-6 PASS. Continues below.

---

M5-T115 HJ — part 2/3

HONESTY (all hold):
- Nothing reads as a legal determination / permitted / approved / maximum-allowed. The new strings are "Internal build", "No sign-in or access control yet, the official data shown is unreviewed, and nothing here is a legal determination — do not share…", "professional review required before any reliance", "…an alternative to the address search above…", "City-matched address". None claim permission/approval.
- §29 disclaimer is verbatim and prominent, UNTOUCHED: rendered from REQUIRED_DISCLAIMER (lib/disclaimer.ts:5-12 — full "not a legal opinion… DOB determination… permit approval… reviewed by qualified New York professionals") in the global `<footer role="contentinfo" aria-label="Required disclaimer">` (layout.tsx:35-48) on every page. The env note is a DIFFERENT disclosure and never retypes §29 (unit-asserted, entry.test.tsx:246-248).
- The env note is an honest paraphrase of InternalBanner (InternalBanner.tsx:8-15): it preserves internal-build / no-access-control / not-a-legal-determination / do-not-share, and ADDS the accurate "official data shown is unreviewed". No meaning lost, no overclaim (producer OQ-3 — acceptable).
- "City-matched address" is accurate for what the line is (view.canonical Geoclient match, AddressConfirmCard.tsx:84-91), consistent with the existing not-verified posture "official address match from the city's Geoclient service… has not been through the platform's rule review" (:381-385). Not "Verified".
- Both city warnings stay verbatim ABOVE Continue (AddressConfirmCard.tsx:248-269 warnings block; Continue :389) — unchanged; unit S1 asserts DOM order.

RECOVERY (nothing strands the user):
- Each typed error category names a real next step and is non-circular (DB-024a): suggestion-path failures point at the full-address button; when THAT search itself fails, FULL_SEARCH_ERROR_MESSAGES points instead at manual entry / BBL (AddressAutocomplete.tsx:39-60). `rejected` is an honest non-retry line (DB-006). no_match → "No matching address found… Search the full address, or use manual entry or BBL" (:164).
- The confirm-card dead ends still each name recovery: no BBL → "use the BBL lookup on this page instead" (:399-402); no ZoLa link → shared ABSENT_BBL_MAP_LINK_NOTE (:302-306). "Not my property" returns to entry with form values retained (AddressResolutionScreen.tsx:215-218).
- The BBL alternative is findable (native `<details>`, now labelled as an alternative). Manual fallback preserves the typed text verbatim and focuses it (:225-229).

ACCESSIBILITY:
- `aria-invalid` on the BBL input is NEW and correct (toggles true only while errored, removed on valid — entry.test.tsx:293-294); the error `<p role="status">` announces via the polite live region and is the `aria-describedby` target. Error is colour + verbatim text (never colour alone).
- Env badge is `role="note"` with a text label ("Internal build"), not colour alone. Reading order on the search view is sensible: eyebrow → env note → search → BBL details → review line.
- Keyboard path intact (arrow/Enter/Escape combobox AddressAutocomplete.tsx:170-195; heading focus-move on outcome). Verdict + advisories in part 3.

---

M5-T115 HJ — part 3/3

BLOCKING: none.

ADVISORY (none block acceptance):
- HJ-1 (label proximity, confusing-vocabulary): the NEW "City-matched address" label (AddressConfirmCard.tsx:196-198) sits on the same card as the pre-existing "City record address:" PLUTO line (:433). Both lead with "City-…" for two DISTINCT identities — mild lexical collision that slightly softens the very distinction AS-2 sharpens. Mitigated by prose ("We show it alongside the city-matched address so you can compare them" :243; "the address the city's official tax record (PLUTO) carries… can differ from the matched frontage" :24). The PLUTO label is out of P2 scope to change (meaning-change register stays closed), so this is advisory only; a future slice could widen the wording gap (e.g. "matched frontage" vs "record address").
- HJ-2 (report/objective vs diff mismatch): report §1/§2 and the packet objective say P2 applied "map CSS polish", but the CSS diff (architect.css:243-252) contains NO map selector — the LotOutlineMap and all map CSS are byte-unchanged. Code is fine (AS-4 is purely re-cited, previously gated); only the wording overstates. Worth a one-line correction so the ledger isn't read as "map was touched".
- HJ-3 (two role=note at desktop, producer OQ-2): the search surface is wrapped by ArchitectShell, so at ≥768px both the shell InternalBanner (role=note, inside the collapsed details, ArchitectShell.tsx:24-27) and the new always-visible search-environment (role=note) are in the DOM. role=note is not a landmark, so this is ARIA-legal and honest, just mildly redundant. Acceptable as-is.
- HJ-4 (scope calibration, not a defect): the packet PROMISED the full P2 behaviour matrix, but ~80% of it (all error categories, identity comparison, stable action area, map states, source disclosure) was ALREADY built in M5-T016/T023/T046/T047/T050/T055/T080/T114 and is re-proved here, not newly built. The producer states this plainly (report §0). Meaning-preservation was the mandate (D-086-R003), so this is legitimate — but an owner expecting a big visual overhaul from "P2" should know the net-new visible surface is items 1-4 in part 1. DISC-P2-1 (shell env disclosure still hidden ≤700px on the LOADED workspace surfaces, only the SEARCH surface was fixed) and DISC-P2-2 (synthetic e2e resolver emits only `resolved`, so the address error/warning matrix on the architect arc is unit-only) are correctly routed as discoveries, not fixed in-packet.

Every AD/AC/M/SH row the producer touched keeps its protected meaning; no disclosure, honest gap, provenance note, or professional-review meaning was deleted or weakened; the P1 meaning-change register (MR-1..MR-8) stays NOT adopted. Scope clean (exactly 8 files, no forbidden path, no backend, no deps). CI green at a byte-identical head.

The slice is honest, correct, accessible, and does deliver genuine "more visually explanatory" wins (chiefly the always-visible phone-width environment + review disclosure). The advisories are polish/expectation notes, not correctness or honesty failures.

M5-T115 HJ VERDICT: PASS

END-OF-REPORT
