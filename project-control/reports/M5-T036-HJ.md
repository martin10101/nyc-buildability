# M5-T036 human-journey review — human-journey-reviewer return (VERBATIM)

Reviewer agent: human-journey-reviewer (spawn rev-hj-m5t036), read-only source-walkthrough,
pinned HEAD 657b238dce8af8df1bfff2e22e454caab1749413. Saved verbatim from the agent-return
channel (transport entity-decoding only).

---

# G3 human-journey review — M5-T036 (DB-016 context panel + DB-005 ZoLa unification)

VERDICT: PASS (source-walkthrough; all web runtime execution UNVERIFIED, deferred to CI on the pushed head — no browser/npm on this thin client, per packet).

HEAD verified: `git rev-parse HEAD` == 657b238dce8af8df1bfff2e22e454caab1749413. Material change is `git show 657b238d` (7 files: ZoningContextPanel.tsx new; PropertyOverview/AddressConfirmCard modified; 3 test suites; producer report).

## What I did
Read the new panel, both modified surfaces, the reference components it consumes (ZoningSection/ZoningValueList, AdditionalZoningFlags, ProvenanceDisclosure, provenance-link.ts, provenance.ts, contract.ts mappedFeatureView, bbl.ts), ReportView, and every consumer test of the touched components. Independently ran the one documented check and two verification greps. Traced the fixture data behind the panel test assertions.

## Findings (evidence-bound)

**F1 — Journey reads clearly; computed answer stays distinct (PASS).** In the overview view PropertyOverview renders, in order: issue alerts → `architect-overview-grid` [DevelopmentLimits (computed FAR) + "View zoning details" link | Site-context map card] → `ZoningContextPanel` (full-width) → existing-building details (PropertyOverview.tsx:48-69). The panel sits BELOW (not literally beside) the computed answer — a coherent "answer first, then the official designations behind it" order. The panel's section-note explicitly states "Nothing here is computed — the calculated result stays in Development limits." (ZoningContextPanel.tsx:65-70), and AS-1's test asserts none of the development-limits result testids appear in the panel (zoning-context-panel.test.tsx:92-97). Good separation of the label-display panel from the differentiator engine. Exact visual spacing/proportions UNVERIFIED (no browser).

**F2 — Honest absence throughout (PASS).** districts/overlays/special_districts empty → explicit per-array empty text via the shared `ZoningValueList` (ZoningContextPanel.tsx:71-91; ZoningSection.tsx:40-41), tested AS-2 (test:100-131). Absent landmark/histdist → "Unknown — not supplied" row with the label still visible and no disclosure offered (ZoningContextPanel.tsx:99-124), mirroring AdditionalZoningFlags.tsx:13, tested AS-2 (test:133-145). Non-canonical BBL → NO ZoLa link + honest absent span (ZoningContextPanel.tsx:59-63), tested AS-3 (test:158-171). Nothing absent is hidden. Note (observation, not a defect): "Unknown — not supplied" for landmark is deliberately a DATA-absence statement, not "this lot is not a landmark" — the conservative bounded-negative posture, consistent with the existing flags component.

**F3 — Provenance affordance consistent (PASS).** districts/overlays/special use the same `ProvenanceDisclosure` (native `<details>/<summary>`, keyboard-accessible) via `ZoningValueList`; landmark/historic use the same component with `label="Source for {label}"` (ZoningContextPanel.tsx:114-120). Identical disclosure pattern to the zoning tab and the rest of the product. When a present value's provenance doesn't resolve, ProvenanceDisclosure renders "Provenance not linkable for this value" (ProvenanceDisclosure.tsx:26-33) — honest. Fixture verification: the M1-T005 fixture's landmark/histdist/zonedist1/zonedist2/spdist1 provenance_refs all resolve to real records, so AS-1's "Source for R3-2/C4-1/Landmark/Historic district" assertions have real backing (confirmed by reading builder_output_m1_t005.json).

**F4 — DB-005 duplication (DB-018): ADVISORY, not blocking.** The overview now shows two ZoLa affordances to the same lot page: the Site-context card link "Open ZoLa ↗" (PropertyOverview.tsx:58) and the panel heading "Open in ZoLa ↗" (ZoningContextPanel.tsx:50-58), both via zolaLotUrl(bbl) → identical URL. Severity for the journey: mild redundancy only — both are honest, both go to the same place, worst case is a momentary "why two?". Acceptable for now; correctly routed to backlog DB-018 as a design-director call. Not confusing enough to block.

**F5 — Print exclusion is the right reading experience (PASS).** ReportView imports only `PropertyIssuesSummary` from ./PropertyOverview (ReportView.tsx:11), never the panel-mounting PropertyOverview, and renders its own richer zoning surface — `ZoningSection` (full mapped-feature table with coverage) + `AdditionalZoningFlags` — inside #brief-zoning (ReportView.tsx:77-81). Mounting the panel there would duplicate that surface with a strictly smaller subset. Excluding it is correct; tested both directions (AS-4, test:174-201). Producer's decision is accurate and well-evidenced.

**F6 — Accessibility of new markup (PASS with minor advisory).** Heading hierarchy is clean: `<section aria-labelledby="zoning-context-title">` → `<h2 id="zoning-context-title">` → `<h3>` per group, no skipped levels (ZoningContextPanel.tsx:41-94). `<dl>` for landmark/historic uses valid dt/dd-in-div structure mirroring AdditionalZoningFlags. ZoLa link is a native `<a target=_blank rel="noopener noreferrer">` — keyboard-reachable; absent state is plain text, not a dead control. Minor advisory: the panel's "Open in ZoLa ↗" decorative arrow is NOT aria-hidden, unlike the adjacent "View zoning details <span aria-hidden>→</span>" (PropertyOverview.tsx:53); and target=_blank carries no explicit "opens in new tab" announcement (though this matches the existing product convention on the ProvenanceDisclosure ZoLa link, so it's not a regression). Non-blocking.

## Independently verified (not just from the report)
- **Consumer sweep — no known-red consumer.** The code-graph lists 5 importers of PropertyOverview, but only ArchitectEntry (view==="overview", ArchitectEntry.tsx:94-95) renders the panel-mounting `PropertyOverview`. ReportView/PropertyFacts import only `PropertyIssuesSummary`; ScenarioWorkspace imports only `DraftHeadline`. The one out-of-scope consumer test, entry.test.tsx, exercises view=overview (line 71 it.each, line 112) but its assertions target rule-eval/architect-cap and a scoped "Development limits" region — no collision with the panel's `zoning-context-*` testids or its "Source for R3-2/C4-1/…" names. entry.test.tsx also already mocks LotOutlineMap (line 16). No consumer breakage.
- **DB-005 genuinely closed.** grep for `zola.planning.nyc.gov` / `ZOLA_BBL_URL_PREFIX` across apps/web/src/components/architect + address shows ZERO production hits — only test assertions and the single lib constant remain. AS-3 holds at source level.
- **Unification is behavior-neutral.** validateBblInput ok-canonical (10-digit, borough 1-5) is a strict superset of zolaLotUrl's check (bbl.ts:36-71 vs provenance-link.ts:53-56), so AddressConfirmCard's link renders in exactly the prior cases.
- **AS-7 modularity:** ran `python tools/modularity_check.py --check` → "selected 441 files; failures 0; warnings 19", exit 0. None of the 19 warnings names a file this task touched.
- **AS-5 scope:** git show stat confirms edits confined to apps/web/** (6 files) + the report; no services/api/** and no apps/web/src/lib/** edited.
- **Test/fixture consistency:** the panel suite's data claims (districts R3-2+C4-1, empty overlays, special GI, landmark "INDIVIDUAL LANDMARK", histdist "Governors Island Historic District") match builder_output_m1_t005.json exactly.

## Discoveries surfaced (route to DISCOVERY_BACKLOG at the seam, don't fix in-packet)
- DB-018 (already noted by producer): two ZoLa affordances on the overview — consolidation is a design-director call.
- D1 (producer): FIRM flood flags / transitzone / zonemap are in the client profile but not on this overview panel (they surface via AdditionalZoningFlags + the zoning-tab ZoningSection + the printed brief). Fuller overview parity is a follow-up increment; honestly disclosed, boundary held.

## Honesty bar
All "PASS" dimensions above are source-level conclusions; every claim is file:line- or command-bound. What I could NOT verify without a browser/vitest: that the suites actually pass and that the rendered layout reads well visually — both marked UNVERIFIED and correctly deferred to web CI on the pushed head (AS-6). Per the packet, I do not FAIL solely for inability to run a browser. The suites are well-formed, internally consistent with the fixtures, and express exactly the intended journey; I have high confidence, but green rests on CI.

VERDICT: PASS. No blocking defects. Two non-blocking advisories (DB-018 duplication; ↗-glyph aria-hidden consistency) and one scoping discovery (D1), all suitable for backlog. Acceptance of AS-6 runtime execution should be gated on the CI web job at the pushed head.
