# M5-T070 — G0 readiness (orchestrator, seq 125, 2026-09-22)

Task: D-082-R003 + D-083 max-first UI surfacing — the "Preliminary development limits" panel
renders the computed envelope FIRST on the architect surface (claim-class vocabulary, per-
dimension binding-rule provenance, honest gaps, no unrestricted green pass) with one-action
adoption into the editor; stubbed unmounted route, web-only.

## Readiness checks

1. **Dependencies accepted:** M5-T064 (max-envelope engine, 248th) and M5-T066 (map-CLICK
   slice, 250th) are both `accepted` in the ledger — the engine contract and the editor
   surface this packet composes over are frozen acceptance evidence.
2. **Directive regime:** packet carries `directive_refs` for D-082 (R001/R003), D-083
   (R001/R002/R003/R004 — the claim-class vocabulary, answer-first flow, and no-green rules
   captured 2026-09-21 from the owner-relayed architect-lens assessment), D-066 (R001),
   D-076 (R001/R002), D-077 (R002/R003). Applicability bound at this seam with same-commit
   digest resyncs; `reg.evaluate_task_refs(task)` → ok, applicable == cited (11 ids).
3. **Code-graph seam duty (D-066-R001):** graph REGENERATED at this contract seam
   (795 files / 16876 nodes / 7377 edges); the packet's navigation block names the
   ProposalEditor/ArchitectEntry consumer chain and the three ACCEPTED property pages as
   forbidden display surfaces; the frozen T065/T066 drawing spine is walled.
4. **Contract fidelity:** the packet's MAX-ENVELOPE CONTRACT block transcribes the exact
   `MaxEnvelope.as_dict()` / `EnvelopeDimensionResult.as_dict()` /
   `CandidatePlacement.as_dict()` field names and the POST /api/v1/max-envelope route from
   the accepted T064 source at HEAD (never guessed); the route ships UNMOUNTED and
   services/api is a forbidden path — mounting stays a later release seam.
5. **Placeholders seeded (fail-closed rule):** all four NEW allowed_paths files exist
   tracked at this commit — MaxEnvelopePanel.tsx, max-envelope-panel.test.tsx (trivial
   PASSING suite — an empty spec fails vitest), max-envelope-api.ts,
   max-envelope-api.test.ts (trivial passing suite).
6. **Pairwise overlap vs live lanes:** T068 (services/api/tests/{scenario,api} max-envelope
   suites) ∩ T070 = ∅; T069 (services/api site_definition store + suites) ∩ T070 = ∅.
   T070 is the only lane touching apps/web this wave. Empty pairwise overlap holds.
7. **Thin client:** documented local check = `python tools/modularity_check.py --check`
   only; web proofs ride CI on the pushed head (CODING_RULES; no npm/npx/node anywhere).
8. **Vocabulary wall is testable:** AS-2/AS-3 make the D-083 claim-class and
   incomplete-aggregate rules grep-provable and mutation-tested — the G3/G4/HJ reviewers
   can gate on them mechanically.

## Verdict

G0 PASS — the packet is contractable: scope walls are explicit, every input names its exact
source contract, all six acceptance scenarios are executable, and both live-lane overlaps
are empty. Ready to claim for the loop-3 lane (worktree wt-m5t070 at/past the claim-seam
commit).
