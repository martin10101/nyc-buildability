# M5-T051 G0 — contract-readiness record (phase B1 derivation packet)

**Task:** M5-T051 — deterministic proposal-derivation module + DB-034(c)/(e) closures.
**Contracted at:** 2026-09-19, session seq 119, the M5-T048 acceptance seam (D-075 window;
B0 accepted as the 232nd, making B1 contractable per the plan's dependency order).
**Reviewer of record:** orchestrator.

## Packet completeness (start-controlled-task checklist)

- **Requirement identifiers:** docs/PROPOSAL_EDITOR_PHASED_PLAN.md phase-B B1 row (the
  packet's SCOPE enumerates its deliverables verbatim-faithfully); DB-034 rows (c)+(e)
  closed in-packet; DB-013 ceiling pattern inherited; D-051 fallback discipline cited for
  the street-setback absence case.
- **Directive refs:** D-066:D-066-R001 + D-076:D-076-R001,D-076-R002 — bound this seam
  (task_ids appends + digest resyncs + audit entries, same commit); `evaluate_task_refs`
  → ok, applicable == cited (3 ids). D-076-R003 deliberately NOT cited: this packet
  touches no contract schema (contract.py and both schema copies are FORBIDDEN paths).
- **Evidence files named in SCOPE:** the plan's B1 row; DB-034/DB-013 rows; proposal.py.
- **allowed_paths / forbidden_paths:** six allowed (2 new source/test files + the shared
  proposal.py/test pair + the new fixtures glob + report); forbidden list pins the
  facts-not-allowances boundary structurally (no contract/api/rules/spatial/profile/
  connectors/web edit possible).
- **Acceptance scenarios:** AS-1..AS-8 in-packet (hand-computed primary, concavity,
  setbacks with honest street absence, the D-076-R002 grep gate, invariance properties,
  fail-closed degeneracies incl. DB-034(c)/(e), purity+bounds, proof).
- **Gates:** G0,G2,G3,G4,G5 per the plan row; reviewers code-reviewer, qa-engineer,
  security-reviewer, directive-compliance-verifier (G2 recorded by orchestrator).
- **Code graph (D-066-R001):** regenerated at this seam (stale fingerprint → regenerated);
  NAV BLOCK embedded in inputs[3] (proposal.py/contract.py consumer sets with line
  anchors, verified in source; the read-only spatial seams named as parameters-only).
- **Modularity boundary answers:** derivation.py is a NEW single-responsibility module
  (pure computation, stdlib-only); its boundary is pinned by the forbidden paths and the
  caller-supplied-parameters rule; the packet requires the producer's seven answers in
  the plan/report and the checker in the documented commands.
- **Seeds:** all four new allowed_paths entries seeded this commit (placeholder module,
  placeholder py test, fixtures README, report placeholder).

## Pairwise disjointness (mechanical, D-072)

Live/awaiting lanes at contract time: M5-T050 (awaiting_gate, wave running at ee1b361c).
- T051 ∩ T050 allowed_paths = ∅ (T050 = address confirm surface + lot_geometry route/test;
  T051 = scenario derivation/proposal + its tests/fixtures). Zero overlap.
- M5-T048/M5-T049 are ACCEPTED (no live scope); T051's proposal.py touch post-dates
  T048's acceptance and is the DB-034(c)/(e) closure the T048 wave itself routed here.

## Ledger/scope hygiene

- New allowed_paths files seeded this commit — no zero-tracked-file refusal.
- No blocker `affects`/`detail` references M5-T051.

**Verdict: PASS — packet is contract-complete; claim may proceed at this head.**
