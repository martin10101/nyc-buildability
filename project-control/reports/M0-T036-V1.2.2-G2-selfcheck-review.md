# M0-T036 V1.2.2 — G2 held QA wave (verbatim reviewer return)

- Gate: G2 (self-check / readiness QA) | Task: M0-T036 | Reviewed SHA: **b95ebf7** (V1.2.2) | Delta baseline: 98da15e
- Reviewer model: **claude-opus-4-8 / effort xhigh** (owner-approved Fable-out fallback, D-007-R609 lifting R607 for M0-T036 only)
- Recorded verbatim by the orchestrator; reviewer was read-only and did not record the gate.

---

1150 tests collected cleanly, no collection errors (1148 pass + 2 skip). I have everything for the verdict.

---

# G2 QUALITY GATE REPORT — M0-T036 (Codex-Claude Supervisor Bridge)

## Header
- **gate:** G2 (self-check / readiness QA at the frozen head)
- **task:** M0-T036 — D-007 Supervisor Bridge build (V1.2.2 model-chain rework re-run of the HELD G2 wave)
- **reviewed_sha:** `b95ebf7` (b95ebf7b96c3f151a83df3140f91affc1ae45676)
- **delta baseline:** `98da15e` (V1.2.1)
- **reviewer model:** claude-opus-4-8, effort **xhigh** (owner-approved Fable-5 fallback while Fable is out)
- **role:** independent, READ-ONLY

## VERDICT: **PASS**

The frozen head is build-clean, the supervisor suite is green at the exact claimed counts, the delta is scope-confined, and readiness-level acceptance-scenario coverage of the V1.2.2 detect-and-switch change is present. Nothing observed makes the deeper G3/G4/G5 wave unsafe to run.

## Method note (transparency — important for the record)
My isolated worktree HEAD is `d5d9b50`, which has **diverged** from the frozen `b95ebf7` (neither is an ancestor of the other), so the V1.2.2 code is **not** on disk in my worktree. To review at the true frozen head without mutating the repo/worktree or switching branches, I extracted the `b95ebf7` tree read-only via `git archive b95ebf7 | tar -x` into the session scratchpad and ran everything there. I proved the extraction is a faithful frozen head by comparing git blob hashes to on-disk hashes for all six changed source/test files — **6/6 identical**:

| file | blob @ b95ebf7 | extracted |
|---|---|---|
| tools/agent_supervisor/claude_runner.py | 76ab3c0 | 76ab3c0 ✓ |
| tools/agent_supervisor/cli.py | 3793a30 | 3793a30 ✓ |
| tools/agent_supervisor/config.py | a398bd6 | a398bd6 ✓ |
| tools/agent_supervisor/loop.py | af76f2c | af76f2c ✓ |
| tools/test_agent_supervisor_loop.py | 4159d1c | 4159d1c ✓ |
| tools/test_agent_supervisor_model_chain.py | 60b3f22 | 60b3f22 ✓ |

(An initial `tools/`-only extraction produced 5 spurious FileNotFound failures because 5 tests read provenance/packet files under `project-control/`; a full-tree extraction resolved them — 1143+5 = the 1148 that pass with the full tree present. This was a harness-extraction artifact, not a code defect.)

## G2 focus results

### (1) Build / import sanity — PASS
All changed modules and their dependency modules import cleanly at the frozen head: `claude_runner, cli, config, loop` (changed) plus `policy, rotation, state_machine, models, codex_reviewer, audit_log, durable_state` (unchanged deps). Output: `ALL IMPORTS OK`.

### (2) Supervisor test suite — VERIFIED, matches claim exactly
Command (from the extracted frozen tree):
`python -m pytest tools/test_agent_supervisor_*.py -q -p no:cacheprovider`

**Observed counts: 1148 passed, 2 skipped, 0 failed, 0 errors** (111.31s; a second run 91.58s). Collection: **1150 tests collected, no collection errors**. This matches the producer/orchestrator claim of ~1148 passed / 2 skipped precisely (across all 20 `test_agent_supervisor_*.py` files).

The **2 skips are legitimate environment guards, not defects:**
- `test_agent_supervisor_policy.py:449` — SKIPPED: cannot create a symlink (Windows WinError 1314, privilege not held).
- `test_agent_supervisor_process.py:423` — SKIPPED: POSIX-only guard.

### (3) Scope-identity — PASS
`git diff --stat 98da15e b95ebf7` (whole tree) shows the only **code** changes are the 8 declared supervisor files (`tools/agent_supervisor/{README.md, claude_runner.py[new], cli.py, config.example.toml, config.py[new], loop.py}` + `tools/test_agent_supervisor_{loop,model_chain[new]}.py`). Everything else in the delta is control-plane/docs (gates, reports, directives registry, task file, state.json). No `services/`, `apps/`, `packages/`, or shared-`tools/` runtime code changed; the supervisor's unchanged dependency modules are byte-identical (absent from the diff). The verbatim producer diff `project-control/reports/M0-T036-V1.2.2.diff` covers exactly those 8 files and matches the git diff file set. Conclusion: `b95ebf7` is what the deeper gates review, and nothing outside the declared V1.2.2 delta changed supervisor behavior.

### (4) Acceptance-scenario coverage of the V1.2.2 change — PASS (readiness level)
The new `test_agent_supervisor_model_chain.py` (807 lines, +27 tests) exercises the detect-and-switch actuation, chain, and triggers at a real-process level:
- **Detect-and-switch actuation:** `test_a_real_process_comes_up_on_opus_4_8_after_a_fable_5_exhaustion`.
- **The exact V1.2 defect fixed:** `test_the_switch_is_a_launch_not_a_record_when_actuation_is_impossible` (asserts a launch, not a record-only substitution — the blocking condition the V1.2.2 rework was required to close).
- **Chain walk / triggers / config:** default chain = owner order, reorder in immutable config, empty/malformed refused, walk never leaves the chain, exact-string ids (no aliasing).
- **Return-to-pin & exhaustion:** `test_the_return_to_the_pin_launches_a_real_process_on_the_pin`; `test_no_chain_entry_launches_so_the_run_stops_and_notifies`.
- **Guards:** spoofed reported id not made available; unlisted id never selectable even if it launches; non-orchestrator role launches nothing new.
- **SHADOW-ONLY safety enforced:** `test_shadow_observes_and_never_switches` (directly guards the accepted shadow-only posture).
- **Crash/resume durability:** effective model read from durable record; resumed run launches a real process on the effective model; runner start configured on the effective model.

### (5) Anything making the deeper wave unsafe — NONE
Suite is fully green and runs offline; `test_replay_through_the_cli_reports_zero_provider_calls` passes (no provider/network). Scope is confined and provenance-clean. The deeper G3/G4 actuation-correctness judgment (e.g., mutation-proof that the test fails when `_actuate_model` is removed) is theirs to make — the frozen head is safe for them to run.

## Findings

**Acceptance-blocking (readiness): NONE.**

**Activation-only (do NOT block acceptance — M0-T036 is accepted SHADOW-ONLY; limited-auto never enabled by this task per AS-8):** these are producer-disclosed and consistent with the shadow-only posture; carrying them forward for the deeper gates / activation decision, not as G2 blockers:
- **A1** `QUOTA_EXHAUSTION_SIGNAL_VERIFIED=False` — the live CLI's account-quota stderr/exit signal has never been captured, so no classifier is wired in production and the chain step will not fire live until it is (fail-closed pause holds). Tests inject the classifier via a fake CLI; the live contract is unproven by design.
- **A2** the switch fires at a rotation **seam**; a quota exhaustion arriving as a mid-unit launch failure still ends at `no_valid_checkpoint`.
- **A3** `expected_model` now moves with the switched model, changing `--expected-worker-model` probe-knob behavior after a switch.

**Environment note (informational):** the 2 skips are Windows/host-driven; on a POSIX/privileged host they would run. The suite's target platform is Windows (prior runner-death finding), so the Windows skip profile is expected.

## Read-only / scope confirmation
- I **wrote, created, and edited NO repository file.** All extraction went to the session scratchpad (`.../scratchpad/frozen-full-b95ebf7`), never the repo or worktree.
- I did **not** run `tools/project_control.py`, `git push`, `gh`, or any git-write; git usage was read-only (`log, show, ls-tree, diff, cat-file, hash-object, merge-base, rev-parse, archive`). I did **not** switch my worktree branch or merge/accept anything.
- I stayed within the declared V1.2.2 delta (`98da15e..b95ebf7`, 8 supervisor files) plus the control-plane artifacts needed to interpret it.
- The orchestrator should record this verbatim; the reviewed content is `b95ebf7` as proven by the byte-identity table above.
