# M5-T068 — G2 producer self-check record (recorded by the orchestrator, role self_check)

Producer: qa-engineer (loop-1 worker, run persistent-local-67). The self-check facts below are
the producer's committed report claims corroborated by the orchestrator harvest and CI — the
broker-cwd [BLOCKED]-evidence class does not apply here (the loop-1 broker ran the documented
suites in-run; the orchestrator re-ran them fresh at harvest).

- **Scope self-check:** TEST-ONLY wall honored — material 5aad9007 = exactly the two test
  allowed_paths + the (new) producer report; the forbidden engine/route/main byte-untouched
  (all-insertions diff vs the claim-seam head).
- **Suites (fresh at harvest, explicit cwd services/api, wt-m5t068):** `python -m ruff check .`
  clean; `python -m pytest tests/scenario/test_max_envelope.py tests/api/test_max_envelope_api.py -q`
  → **63 passed**; `python tools/modularity_check.py --check` exit 0.
- **CI (thin-client authoritative channel):** run 35769445255 at a66cf818 — the api job
  (ruff + pytest) green on BOTH attempts; the sole attempt-1 red was the DB-048 web flake
  (unrelated, zero path overlap; zero-delta rerun green).
- **Mutation teeth:** each of the seven added tests carries the producer's tooth note (the
  asserted mutant behavior that would turn it red); the G4 independent review verifies them.
- **Identity:** in-worktree 4e49d8e7 cherry-picked to 5aad9007, ALL-MATCH x3 (LF-normalized);
  frozen submit identity at 141007c3.

G2 verdict: PASS (self_check; never independent review — G3/G4/DCV follow).
