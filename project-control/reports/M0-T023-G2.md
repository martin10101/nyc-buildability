# G2 Producer Self-Check — M0-T023 (D-001 amendment 3, re-stamped at ed8a6776)

- Gate ID: G2 (producer self-check; recorded by the orchestrator, role self_check; never satisfies an independent gate)
- Task: M0-T023 — Owner Directive Compliance System, directive D-001, amendment 3
- Reviewed head: `ed8a6776f89c9f2b17ecad35e528a0180fe6bfd1`
- Content-manifest identity: `9ab6c9601b1a60f903d1a399d57192f662d96e912fdc50304ba931894b3004be`

## Producer self-check result: PASS (evidence submitted for independent review)

Amendment 3 (final bounded hardening of PR #97) implements the owner's three engineering sections plus reconciliation, decomposed into D-001-R119..R165 (47 new requirements, append-only; source-001/002 untouched):

- Section 1 — immutable migration_manifest.json bound to baseline 1acb9b5 (57 pre-regime task ids + material-field digests, append-only content-locked in the D-001 manifest); new-task fails closed without --directive-refs under an enabled regime; claim/reclaim forces regime entry; progress() fails closed on a blocked/rework/backlog/ready -> continuation launder (G3 F1 fix); grandfathering requires manifest membership + unchanged material digest + already-active status; material amendment invalidates it; corrupt/missing registry or manifest fails closed. No bypass flag.
- Section 2 — directive_verification/v2 with task_verifications[]; acceptance evaluates only the requirements applicable to the task at its own identity; per-task producer/verifier separation; missing/duplicate/extra/stale/cross-task rows fail closed; D-001 migrated v1->v2 preserving evidence history.
- Section 3 — frozen_git_identity(): identity from canonical tracked git content (path + blob/object id + file mode) at the reviewed commit; LF/CRLF-invariant, binary-exact, mode-sensitive, stable across merge/rebase/squash, fails closed on dirty/untracked and on a non-HEAD clean stamp. Submit, gates, and accept share it.

## Self-check battery (local, all green at ed8a6776)

- python tools/test_project_control.py -> 14 groups OK (incl. 9 adversarial migration proofs + PROOF 7b/7c blocked/rework launder-closed)
- python tools/test_directive_compliance.py -> 55 tests OK (git-identity + two-task v2 isolation)
- python tools/test_directive_reminder.py -> 12 OK
- python tools/test_readonly_agent_guard.py -> PASS
- python tools/test_agent_dispatch_guard.py -> PASS
- python tools/validate_directive_compliance.py --check -> exit 0
- python tools/context_budget_check.py -> PASS

Evidence map (all 136 applicable requirements) submitted at project-control/reports/M0-T023-evidence-map.json. This self-check does not satisfy the independent gates G3/G4/G5, which are recorded separately by independent reviewers != producer.
