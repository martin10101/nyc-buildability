# M5-T050 G2 — producer self-check record (recorded by orchestrator)

The producer's self-check is the report's Validation-outcomes section
(project-control/reports/M5-T050-producer-report.md): the documented api-cwd commands run
in-run where the runner permitted them, web behavior honestly deferred to CI (thin client),
and — notably — a scrupulous "Honest limitations" section that (1) refuses to claim pixel
stability from jsdom (the structural pure-append assertions are the jsdom-provable
guarantee; the real-browser measurement is out of the packet's allowed_paths and routed to
the orchestrator), and (2) surfaces the rider-a bottom-placement reading-order tradeoff for
the journey reviewer instead of deciding it silently. Riders are explicitly NOT claimed
"all verified" pre-CI.

Orchestrator reproduction at harvest (wt-m5t050): ruff clean; 33 lot-geometry + 439 api
tests; modularity exit 0. CI at the covering head 499ae642: run 35440808156, all 19 jobs
success (M5-T050-ci-evidence.md) — the authoritative web proof.

Wave outcome context recorded here: all four independent reviewers PASS zero blocking; the
HJ explicitly ACCEPTED the bottom placement (its report carries a concrete optional
before-<Meta> alternative); the outstanding pixel-CLS e2e measurement goes to the backlog
as an orchestrator-side rider at the acceptance seam.

Verdict: PASS.
