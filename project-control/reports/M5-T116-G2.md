# M5-T116 - G2 producer self-check record (recorded by the orchestrator, role self_check)

Producer: backend-engineer (orchestrator-dispatched subagent) in wt-m5t116; one in-worktree commit 74e97bb4 (parent c375457c), cherry-picked to 99c8457f with every file MATCH by git blob identity (3 files: project-control/reports/M5-T116-producer-report.md b7e93b90, services/api/app/cad/export_service.py a2f53c41, services/api/tests/cad/test_export_service.py 91a36f3e).

- **Scope:** services/api/app/cad/export_service.py: GLB caps from triangulate_polygon (DB-082 a) with its MassingModelError mapped to the one redacted ExportRefusal; the fallback filename token re-allowlisted at a shared choke point (c); the GLB dedupe docstring fixed (h); tests/cad/test_export_service.py: concave L/U caps (area-sum + containment oracle in the test), a convex case, refusals, the four-field screens (f) and the 2000/2001 floor edge (g).
- **Producer self-checks (report, OBSERVED):** ruff clean; tests/cad 458 passed (was 435; owner samples unchanged); modularity exit 0. GLB disclosure: the convex fixture's GLB digest changed 2bcfd4c8 -> 146797d2 (same length 1680) - only the cap triangulation differs; DXF/PDF/owner samples byte-identical. Mutants: restoring the vertex-0 fan fails the L/U area oracle; five in-process mutants (fan, reconcile, fallback allowlist, four-field narrowing, floor edge) each redden.
- **[ORCH-HARVEST] at the harvested head 99c8457f (cherry-pick 99c8457f; Python 3.11 local): `python -m ruff check .` (cwd services/api) -> exit 0: All checks passed!; `python -m pytest tests/cad -q` (cwd services/api) -> exit 0: 458 passed in 2.97s; `python tools/modularity_check.py --check` (cwd .) -> exit 0:   warn review_signal: tools/agent_supervisor/gate_wave.py - above the justification threshold; record a cohesion justification in review.**
- **Discoveries routed (producer):** OQ-1 the GLB footprint vertex cap narrows from 10,000 to the triangulator's 1,000 (fail-closed; recommend accept). OQ-2 the ear-clipping triangulator does not detect a crossed 'bowtie' (self-intersecting) footprint - owned by M5-T112 / DB-084; the route is UNMOUNTED so there is no live exposure.

G2 verdict: PASS (self_check; the independent reviews and the DCV follow - dispatched by the successor session per the handoff).
