# M0-T102 G4 QA review (VERBATIM save)

Reviewer: independent read-only `qa-engineer` (unnamed spawn); reviewed the exact tree at frozen
SHA `b69600f45d967887b16eaa68d9d19d5c2265e211` via `git archive` into an isolated scratch dir;
returned through the agent-return channel 2026-08-26 UTC and saved verbatim below (transport
entity-decoding only).

---

All 8 checks are complete. Every machine-checkable claim reproduced exactly against the frozen tree at b69600f. Here is my gate report.

---

# G4 QA Gate Report — M0-T102 (D-024 Amendment 3 unit A: capability re-baseline)

**G4: PASS**

- Reviewer: qa-engineer (independent; read-only)
- Task: M0-T102 (governance/evidence; discharges D-030)
- Frozen review SHA: `b69600f45d967887b16eaa68d9d19d5c2265e211` ("M0-T102 submit snapshot record (CLI-written)")
- Review method: extracted the exact tree at b69600f via `git archive` into an isolated scratch dir (clean checkout at frozen SHA; independent of the shared checkout's current state) and re-executed every check there. Wrote nothing to the repo.
- Deliverable content confirmed present at b69600f via `git ls-tree` (docs-snapshot tree `1a9981f…`, fixtures tree `098ce96…`, matrix blob `5d66018…`, evidence-map blob `aad850a…`).

## Per-check results

**Check 1 — Coverage union vs requirement_index: PASS**
- Union of `applicable_requirement_ids` over M0-T092(65), T093(49), T094(54), T095(46), T096(72), T102(45), T103(29) plus D-024-R141/R142/R189 (all three were NOT already in the task-row union; added explicitly) = **147**.
- `requirement_index` keys in native-reuse-matrix.json = **147**.
- MISSING (in union, not index) = **[]**; EXTRA (in index, not union) = **[]**.
- Each ID mapped **exactly once** via `areas[].affected`: zero IDs appear >1×; every union ID appears exactly once; the 29 areas' affected-union == the 147 index keys with no orphans on either side.

**Check 2 — Evidence map vs M0-T102 applicable set: PASS**
- `M0-T102-evidence-map.json` → `requirements` has **45** keys == the 45 `applicable_requirement_ids` of the M0-T102 row. MISSING = []; EXTRA = []. Every value is a non-empty list of evidence strings (0 empty).

**Check 3 — Fresh capability probe: PASS**
- Ran `python -m tools.agent_supervisor.capability_probe` fresh at frozen SHA.
- `claude_version.first_line` = `"2.1.220 (Claude Code)"` (matches fixture; output_sha256 `74fd5c25…` identical).
- `codex_version.first_line` = `"codex-cli 0.146.0"` (matches fixture; output_sha256 `446dce03…` identical).
- Fresh body byte-identical to `capability_probe_live_2026-08-26.json` (only `probe_meta.generated_at` differs).
- Fixture grep `MLFLL` = **0 hits**; binary paths redacted to `[HOME]`.

**Check 4 — Docs-snapshot completeness: PASS**
- All **16** expected files present (changelog, agent-view, goal, hooks, cli-reference, skills, worktrees, workflows, sub-agents, sessions, cross-session-messaging, plugins-reference, statusline, commands, checkpointing, remote-control) and substantive (118–1365 lines each; 9044 total — no stubs).
- Each begins with a `Source URL: https://code.claude.com/docs/en/<slug>` matching its filename and a fetch date **2026-08-26**.
- grep `MLFLL` across the directory = **0 hits**.
- `changelog.md` version **headings**: 24 headings covering 2.1.220..2.1.246 with **2.1.230 / 2.1.242 / 2.1.244 correctly absent as headings** (they appear only in prose, e.g. as regression references); no missing headings, no extras. Names latest explicitly: "Latest released version: **2.1.246** (August 25, 2026)."

**Check 5 — Directive-compliance registry: PASS**
- `python tools/validate_directive_compliance.py --check` → **EXIT=0**.
- Without flag (to confirm non-vacuous): "directive registry OK: 28 directive(s), 28 active; source hashes, ID append-only, and producer/verifier separation verified." EXIT=0.

**Check 6 — Version-gate spot-checks (all 6 examples traced to snapshot source): PASS**
- `/autocompact` 2.1.221+ → commands.md:49 "(v2.1.221+)"; remote-control.md:298 "from v2.1.221". ✓
- `promptCacheTtl` 2.1.243+ → changelog.md:84 under the `## 2.1.243` heading. ✓
- `/goal` background check-ins 2.1.234+ → changelog.md:339 under `## 2.1.234` ("when background tasks keep a goal waiting for 30+ minutes, Claude now checks in"). ✓
- `/goal` idle check-ins 2.1.236+ → changelog.md:265 under `## 2.1.236` ("an idle session whose goal is parked… now checks in automatically after 30 minutes"). ✓
- dynamic workflows 2.1.154+ → workflows.md:14 "Dynamic workflows require Claude Code v2.1.154 or later". ✓
- `--forward-subagent-text` 2.1.211+ (nested 2.1.219+) → cli-reference.md:73 verbatim: env var "Requires Claude Code v2.1.211 or later" and nested-subagent forwarding "requires Claude Code v2.1.219 or later". ✓

**Check 7 — Mutation-style falsifiability tooth: PASS**
- (a) D-024-R141 and D-024-R002 are genuinely in union AND index; removing either from the index would leave it MISSING → coverage claim is non-vacuous/falsifiable.
- (b) D-024-R005 absent from union and index and appears in no task row. D-024-R129 absent from union and index; it appears only in M0-T099 (which is deliberately outside the remaining-union target set) → correctly excluded. Both out-of-scope IDs correctly absent.

**Check 8 — Suite guard: PASS (with note)**
- The exact file `tools/test_agent_supervisor_capability.py` **does not exist**.
- The closely-named guard for the module this task re-baselines, `tools/test_agent_supervisor_capability_probe.py`, exists; ran it alone (not the full suite): **16 passed in 5.03s**.

## Cross-checks (beyond the 8)
- Matrix self-reported `coverage.requirement_count` = 147, matching my independent recomputation. `installed` (claude 2.1.220 / codex 0.146.0) matches the fresh probe; `target` 2.1.246 (released 2026-08-25) matches changelog.
- Decision distribution over 147 index entries: CUSTOM_REQUIRED 110, NATIVE_WRAPPED 31, OPTIONAL_ENHANCEMENT 3, REJECTED_OR_DEFERRED 3.
- Modularity: N/A — no handwritten production source changed (deliverables are reports, JSON data, and a sanitized data fixture; the probe module itself was not modified and is outside `allowed_paths`).

## Findings

BLOCKING: none.

ADVISORY (non-blocking, informational):
- A1. Review-instruction naming mismatch: check 8's named file `tools/test_agent_supervisor_capability.py` does not exist; the actual regression guard is `tools/test_agent_supervisor_capability_probe.py` (16/16 pass). Producer defect: none — this is a wording gap in the gate instruction, not a missing test.
- A2. Snapshot header label inconsistency: files split between "Fetched: 2026-08-26" and "Fetch date: 2026-08-26". Cosmetic only; all 16 carry the correct 2026-08-26 fetch date and correct source URL.
- A3. Cosmetic: some snapshot title lines render an en-dash as `�` (UTF-8/console artifact in the upstream doc titles). Body content, source URLs, and version text are intact; does not affect any provenance or version-gate claim.
- A4. `decision_vocabulary` defines `NATIVE_REPLACEMENT` but 0 of 147 index entries use it. Not a defect — the vocabulary is a complete enum; usage need not exercise every value.

## Verdict
Every machine-checkable producer claim reproduced exactly against the frozen tree at b69600f. Coverage is complete (147 = 147, exactly-once mapping) and falsifiable; the evidence map is complete and non-empty; the live probe reproduces the recorded versions; the 16-page docs snapshot is complete, correctly sourced, MLFLL-free, and version-heading-correct; the registry validator is EXIT=0; all six version gates trace to snapshot source text. No blocking defects.

**G4: PASS**

(Orchestrator records the gate result; I did not run `tools/project_control.py`, git write ops, or `gh`. Read-only git used was limited to `cat-file`/`ls-tree`/`log`/`show`/`archive`/`merge-base` against my own worktree's object database.)
