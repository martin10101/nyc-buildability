<!-- Orchestrator capture: data-contract-verifier G1 return for M5-T046 saved VERBATIM
     (transport entity-decoding only). Review pinned at frozen head c9113e09; reviewer
     verified all 8 surface digests against the source-sections index. -->

The agent-memory shell-write is blocked by the read-only guard (write path is fine per policy, but shell mutation is blocked in my sandbox); the note was optional, so I'll return findings directly. My review is complete.

---

# G1 Data-Contract Review — M5-T046 (address-to-lot identity honesty, DB-026)

**Reviewer:** data-contract-verifier (read-only) · **Frozen reviewed SHA:** `c9113e09` · **Branch:** `candidate/D-024-mrl-option-b`

## Verdict: **PASS** (no blocking findings; 4 advisory items)

## Identity / provenance verification
- Live HEAD is `665d3d0b`, one commit above the frozen `c9113e09`. `git diff c9113e09..665d3d0b` touches **only** `project-control/{state.json, tasks/M5-T046.json, reports/M5-T046.json}` — zero G1-surface files changed. Working tree is clean for all surface files, so Read == frozen-head content.
- `c9113e09`'s parent is `fb7af67d` (the ORCH-CORRECTED AS-5 head where CI is green, run 35425516645); `c9113e09` adds only submission evidence (docs/reports), no code change. CI-green identity therefore covers the reviewed code. Coherent.
- **Digests I verified** (LF-normalized sha256 of frozen-head blobs vs `M5-T046-source-sections.md`): all 8 match exactly — `address-search.ts` `0096e659…`, `address-search.test.ts` `973ec3c5…`, `geosearch.json` `137e4e3a…`, `ztldb.json` `b6580dcf…`, `db026-…-fixture-capture.md` `15e98b63…`, `AddressConfirmCard.tsx` `dd4f02f5…`, `AddressResolutionScreen.tsx` `70cc76df…`, `AddressAutocomplete.tsx` `018aa354…`.

## Surface 1 — Registry draft parity + provenance (`geosearch.json`) — PASS
- **Top-level key parity vs committed `ztldb.json`: EXACT** — 19 shared keys, **0 invented, 0 dropped** (parsed both as valid single-object JSON arrays). `authentication` and `rate_limits` sub-keys are byte-parallel. `fields_available` uses documented per-source parallels: `columns→properties`, `assignment_rules_verbatim_basis→match_discipline_verbatim_basis`, `null_semantics` kept, domain-specific `park_caveat` dropped. No invented shape. Passes the "invented shapes fail closed" bar.
- **Every material registry claim traces** to the corpus (`db026-…-fixture-capture.md`) or recon (`db026-…-source-recon.md`):
  - `match_discipline_verbatim_basis` (`geosearch.json:35`) reproduces the exact hazard from corpus §4a/§4b/assertions 1–2 (zzqqxx→`1279 53 STREET`/bbl `3056627501`; `99999`→`207 37 STREET`/hn 207; both HTTP 200 fallback/0.8).
  - `geographic_coverage` multi-frontage aliasing (`:21`) ↔ corpus §1/§3 + recon §3.
  - `properties[]` field-by-field (`:24-33`) ↔ corpus §1/§5 (non-numeric `"1279 GARAGE"`; UPPER-case street; source `nycpad`; confidence/match_type present on /search, omitted on /autocomplete; pad.{bbl,bin,version}).
  - `known_limitations`/`open_questions` (`:43-61`) ↔ recon OQ-set + corpus (multiple BINs, text-search-only, EPSG:4326 display-only, PLUTO address-of-record differs, OQ-5 record-address gap).
  - `connector_implementation`/`fallback_source` (`:39,:54`) ↔ recon recommendation + the adjudication (server Geoclient = sole live authority; GeoSearch = keyless cross-check; no server endpoint/contract field added).

## Surface 2 — Fixture fidelity — PASS
- **All six corpus constants in `address-search.test.ts:220,222,224,226,228,230` are BYTE-IDENTICAL** to the capture doc's verbatim bodies §1/§2/§3/§4a/§4b/§5 (programmatic char-for-char compare, no diffs). Lengths equal the recorded HTTP `content-length` values (3545 / 1566 / 1560 / 1578 / 1561 / 3361). No reindent, reorder, or field mutation.

## Surface 3 — Match discipline (`address-search.ts`) — PASS
- **Equality gate** (`:320-321`): housenumber exact (`house.trim() !== wantHouse`) + normalized street; **match_type/confidence read only** (`:336-337`), never compared — proven by AS-4 (`test:284-296`) where a synthetic `confidence:1/match_type:"exact"` feature on a wrong street is still refused.
- **Identity binds to `addendum.pad.bbl`** with canonical `/^\d{10}$/` (`:322-325`); a matched frontage lacking a canonical bbl is an honest no-match, never a partial lot. Aliasing verified: reverse frontage resolves through the gate to the same bbl `3052960043`; the GARAGE sibling (hn `"1279 GARAGE"` ≠ `"1279"`) is correctly NOT gate-resolvable and its same-bbl/different-bin identity is shown by direct feature inspection (`test:269-272`) — correct behavior, honestly framed.
- **Key-absence tolerance** (`readParsedInput:264`, `readPad:277`, typeof guards `:336-337`): /autocomplete bodies with no confidence/match_type parse and gate, recording nulls (AS-5b `test:318-331`).
- **AS-5 fail-closed ruling on street-TYPE abbreviations is CONTRACT-HONEST.** Simulated the rule: `ORDINAL_SUFFIX = /\b(\d+)(?:ST|ND|RD|TH)\b/` folds only a digit-run immediately suffixed. Autocomplete parsed street `"37 st"` (space-separated) → `"37 ST"` ≠ `"37 STREET"` → `no_match`. The packet contracted **only** a numeric-ordinal fold (`'37th'→'37 STREET'`) and explicitly requires unclear cases to fail closed; `ST→STREET` expansion was never contracted. Fail-closed direction here (refuse-to-promote) is the safe direction for an identity gate (the hazard is surfacing a WRONG lot), consistent with D-051's per-rule fallback-direction check. Amended AS-5 (`packet:55`) asks only for shape tolerance + gated promotion, which the test asserts (suggestions permissive `test:310`; promotion `no_match` `test:315`); AS-5b proves the resolved path. Honest. Correction is test-file-only (`ci-evidence.md`), production code unchanged.
- Also confirmed the fold is safe: `"ST NICHOLAS AVENUE"` untouched (needs leading digits); `"1ST AVENUE"→"1 AVENUE"` is correct (PAD stores numbered avenues without ordinal suffix).

## Surface 4 — Freshness semantics — PASS
- `update_frequency`/`latest_source_version`/`known_limitations` (`geosearch.json:20,41,47`) document: currency signal is **body-only `addendum.pad.version="26c"`**; **no Last-Modified and no dataset/PAD-version HTTP header**, only a weak `W/"…"` etag — matches corpus §8 verbatim headers. The **frozen 2015 Open Data mirror `bc8t-ecyu`** (rowsUpdatedAt 2015-06-08) is flagged MUST-NOT-use — traces to recon §2/§3. `last_successful_ingestion: null` mirrors the committed shape.

## Advisory items (non-blocking)
- **A-1 (report nit):** producer report §4 comparison table (`M5-T046-producer-report.md:110`) labels `connector_implementation` as "additive (— )", but `ztldb.json:47` already has that key — it is a **shared** key, so parity is actually better than the table states. The registry FILE is correct; only the report's table is inaccurate.
- **A-2 (registry):** `rate_limits.throttle_signal = "HTTP 429"` (`geosearch.json:17`) is **not observed** in the corpus (no 429 occurred across 8 captures; the field's own `evidence` says so) — it is inferred from the client's 429 handling. Consider labeling it inferred/unverified rather than a source-observed fact. `without_token: "No published rate limit"` is honest.
- **A-3 (registry phrasing):** `connector_implementation`/`fallback_source` describe GeoSearch as "the keyless cross-check / degraded-mode resolver," which reflects the recon's *recommended* architecture; no degraded-mode promotion path is actually wired at HEAD (`resolveLotFromGeoSearch` has no production importer, producer report §5.4). The text does correctly state the primary resolver is server Geoclient and no endpoint/contract field was added, so it is honest, but the "resolver" phrasing is slightly ahead of the wired reality.
- **A-4 (recon citation):** `geosearch.json:20` cites the 2015-mirror warning as "source-recon §3"; the 2015 timestamp fact sits in recon §2 and the file-not-queryable fact in §3. Cosmetic.

## Scope-authority note (deferred, not a G1 defect)
AS-1/AS-4 (library level) and AS-5 (fail-closed) are orchestrator-amended via the tagged ORCH-SCOPE-ADJUDICATION (`packet.inputs[17]`) and the G0 addenda; the amendments are properly recorded and the data-contract deliverables are honest under them. Whether the live-objective adjudication (option A) is the right scope decision is the orchestrator's / directive-compliance-verifier's call, not G1's.

## Relevant absolute paths
- `C:\Users\MLFLL\Downloads\nyc-zoning\ctl24\docs\research\source-registry-drafts\geosearch.json`
- `C:\Users\MLFLL\Downloads\nyc-zoning\ctl24\docs\research\source-registry-drafts\ztldb.json`
- `C:\Users\MLFLL\Downloads\nyc-zoning\ctl24\docs\research\db026-address-to-lot-fixture-capture.md`
- `C:\Users\MLFLL\Downloads\nyc-zoning\ctl24\docs\research\db026-address-to-lot-source-recon.md`
- `C:\Users\MLFLL\Downloads\nyc-zoning\ctl24\apps\web\src\lib\address-search.ts`
- `C:\Users\MLFLL\Downloads\nyc-zoning\ctl24\apps\web\src\lib\__tests__\address-search.test.ts`
- `C:\Users\MLFLL\Downloads\nyc-zoning\ctl24\project-control\reports\M5-T046-source-sections.md`
- `C:\Users\MLFLL\Downloads\nyc-zoning\ctl24\project-control\reports\M5-T046-ci-evidence.md`

**G1 verdict: PASS.** Data contracts are faithful to the official corpus and recon; the equality gate honors the proven non-discriminating match_type/confidence, binds identity to pad.bbl, tolerates /autocomplete key-absence, and fails closed honestly; the registry draft mirrors the committed shape with exact key parity and body-only freshness semantics. The 4 advisory items are documentation/labeling nits with no data-contract impact.
