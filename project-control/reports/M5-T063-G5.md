# M5-T063 G5 gate — independent security review (verbatim reviewer return)

Reviewer: security-reviewer (opus-4-8 xhigh per D-064), dispatched by the orchestrator at the
7839035c submit seam, pinned to material f33b752c (submitted at f98924e5). Recorded by the
orchestrator; reviewer is read-only and records no gate itself.

---

I have completed the independent G5 security review. All verification is done against the frozen material identity via git plumbing; the 12 paths are byte-stable at the current HEAD.

---

**VERDICT: PASS**

# G5 Security Gate Report — M5-T063 (web condo rider cluster)

## Pinned identity
- Material commit reviewed: `f33b752c` (parent `989a647e`); 11 files changed (PropertyOverview.tsx byte-unchanged by design — not in the diff, confirmed).
- Submitted-at: `f98924e5`; seam: `7839035c`.
- Reviewed head: `7839035c` (current HEAD). Verified via `git diff` that all 12 packet paths are **byte-identical** between `f33b752c` and `7839035c` (disjoint peers T064/T065 landed between, but did not touch this packet's surface). Content identity intact.
- Thin client: no npm/node run locally. Web behavior proof is orchestrator-captured CI at `f33b752c` (evidence map records CI/web-vitest/typecheck/lint/e2e + secret-scan + context-budget all success); verified as stored evidence per ADR-005 reviewer discipline.

## Walkthrough (security surface)

**1. New sanitizer `boundedZoningDistrict` (bounded.ts) — charset & residue class.**
Charset `[A-Za-z0-9/-]` via `new RegExp("[^A-Za-z0-9/-]", "g")`. The `-` is class-final (literal hyphen) and `/` is a plain char — no accidental range, no regex-delimiter ambiguity. Admitted set is exactly `A-Z a-z 0-9 / -`; it strips `<`, `>`, `"`, `'`, `&`, `=`, `:`, `.`, `_`, space, and all control chars. Output is length-capped (`MAX_ZONING_DISTRICT_LENGTH = 32`) and empty→`null` (never a coerced value). Residue class (e.g. `scriptM1-5/R7-2/script`) contains no markup delimiter and cannot form a tag.

**2. Sink analysis — is the residue inert everywhere it can land?** Traced every consumer:
- `boundedZoningDistrict` output → `condo-records.ts:349` → `CondoBaseLotRecord.recordedZoning` → **single render site** `PropertyOverview.tsx:316`, as a React **text-node child** inside `<span>`. React escapes it; the admitted `/`/`-` are inert in a text node.
- No attribute sink: the `data-testid` there is the literal `"condo-base-lot-zoning"`; no `aria-label`, `href`, `src`, or URL consumes `recordedZoning`. The one nearby link (`PropertyOverview.tsx:366`) is `propertyHref(bbl, "zoning")` — validated BBL + literal string, never the district.
- App-wide: no `dangerouslySetInnerHTML` (bounded.ts header asserts it; two test files grep-enforce its absence). The admitted slash cannot reach any URL/path/href sink in the codebase — none consumes this value.

**3. Additivity of pre-existing helpers.** `git diff --stat` shows bounded.ts as `32 +++, 0 -` (zero deletions) — conclusive that no existing line was removed or modified. `boundedText` and `boundedToken` are byte-unchanged; the AS-1 spec-pin (`boundedToken("M1-5/R7-2") === "M1-5R7-2"`, bounded.test.ts:65) proves the token helper still strips the slash. No cross-cutting widening of the 15+-consumer shared lib.

**4. Announcer strings (rule-evaluation.ts).** Fixed prose plus two interpolations: `substitution.analyzed_bbl` and `substitution.entered_bbl`, gated on well-formed non-empty distinct strings. DOM sink for both consumers (ArchitectEntry:78, RuleEvaluationPanel:132) is `OutcomeAnnouncer` → a `role="status" aria-live="polite"` div rendering `{message}` as an **escaped text-node child**. No attribute/markup execution possible. `condo_base_lot_unresolved` branch is a fully static constant. See F-1 for the client-validation nuance (non-blocking).

**5. Status parsing fail-safe (condo-records.ts:352-355).** `status = boundedToken(record.recorded_zoning_status,16) ?? (zoning!==null?"recorded":"unknown")`. Non-string/absent/malformed status → `boundedToken` yields null → derived honestly from whether the district survived sanitization. No crash; no false "recorded" when the district is null (→ "unknown"). Divergence notice gates on `recordedZoningStatus==="recorded"` (PropertyOverview:319) and the ZTLDB gap note on `!=="recorded"` (:320); both notice strings (`divergentZoningNotice`, `recordedZoningDependency`) are `boundedText`-bounded (condo-records.ts:531-538) and render as text nodes.

**6. Least privilege / lane isolation / secrets.** Zero forbidden paths in the diff (T060-lane ArchitectEntry/Shell/nav/css/proposal-editor, T062 surface, `services/api/**`, `packages/contracts/**`, `test-support/` all untouched — grep-verified). No `package.json`/lockfile/requirements change → no new dependency. No secrets/keys in added source or test lines (scan clean); fixtures referenced (`SUBSTITUTION_*_BBL`) are pre-existing read-only 10-digit BBLs. AnalysisIdentityNotice change is copy-only (1 line); `stampLegitimate` correspondence gate and `role="alert"` withhold branch behaviorally unchanged. `recorded_zoning` stays display-only (D-073-R006) — grep confirms no path wires it into an allowance computation.

## Findings

**F-1 — LOW / informational (no correction required; route as discovery).**
The announcer interpolates `substrate_substitution.entered_bbl` / `analyzed_bbl` into the live region. These are typed `Bbl` in the contract (strict pattern `^[1-5][0-9]{5}[0-9]{4}$`), but the **client-side** validator `checkSubstrateSubstitution` (in `apps/web/src/lib/rule-evaluation-contract.ts` — a pre-existing file **outside this packet's allowed_paths**, and `packages/contracts/**` is read-only) enforces only "non-empty string", not the digit pattern. Impact is not a security exposure: the sink is an escaped aria-live text node (no XSS/injection), the values are server-produced CondoResolution provenance, and `entered_bbl` equals the already-validated `evaluated_input.bbl`. The only theoretical residue is a non-canonical BBL string reaching a screen-reader announcement (an integrity/quality nit). Tightening the client validator to pattern-check these fields is defense-in-depth and belongs to the contract-validator scope, not this frontend packet. Recommend logging to the discovery backlog; **does not block G5**.

No Critical, High, or Medium findings.

## Checklist result
- Cross-tenant isolation: n/a to this web-display packet; lane isolation verified.
- Service-role secrecy / private storage: no secrets, no keys, no storage access introduced.
- SSRF/injection defenses: sanitizer residue and announcer strings are inert in escaped text-node sinks; no URL/href/attribute sink consumes them — PASS.
- Upload controls: n/a.
- Prompt-injection defenses: `recorded_zoning` is display-only provenance from accepted connectors, never fed to an allowance/AI computation — PASS.
- Least privilege / log redaction: bounded helpers cap/escape all rendered values; no new logging of sensitive data — PASS.

**Required corrections: none.** F-1 is advisory (route to discovery).

Relevant files (absolute):
- `C:\Users\MLFLL\Downloads\nyc-zoning\ctl24\apps\web\src\lib\bounded.ts`
- `C:\Users\MLFLL\Downloads\nyc-zoning\ctl24\apps\web\src\lib\condo-records.ts`
- `C:\Users\MLFLL\Downloads\nyc-zoning\ctl24\apps\web\src\lib\rule-evaluation.ts`
- `C:\Users\MLFLL\Downloads\nyc-zoning\ctl24\apps\web\src\components\architect\PropertyOverview.tsx`
- `C:\Users\MLFLL\Downloads\nyc-zoning\ctl24\apps\web\src\components\architect\AnalysisIdentityNotice.tsx`
- `C:\Users\MLFLL\Downloads\nyc-zoning\ctl24\apps\web\src\components\property\OutcomeAnnouncer.tsx`
- `C:\Users\MLFLL\Downloads\nyc-zoning\ctl24\apps\web\src\lib\__tests__\bounded.test.ts`
- `C:\Users\MLFLL\Downloads\nyc-zoning\ctl24\apps\web\src\lib\rule-evaluation-contract.ts` (F-1 context; out of scope)

**VERDICT: PASS** — orchestrator to record G5 at head `7839035c` for material `f33b752c`; F-1 routed to discovery, non-blocking.

---

<!-- DELTA ATTESTATION saved VERBATIM from the resumed security-reviewer agent, orchestrator seq 123, 2026-09-20. Corrected identity: 853a1d25. -->

Diff confirms the correction is a pure tightening. Delta attestation follows.

---

**VERDICT: PASS (carry) at `853a1d25`**

## G5 delta identity-carry attestation — M5-T063

**Corrected identity reviewed:** `853a1d25` (`[ORCH-CORRECTED per M5-T063 G3-F1/G4-C-1]`). Verified via plumbing:
- Scope is EXACTLY `apps/web/src/lib/rule-evaluation.ts` (+11/-2) and `apps/web/src/lib/__tests__/rule-evaluation.test.ts` (+23) — the announcer file inside my reviewed surface (walked in section 4).
- The other 10 reviewed paths are **byte-identical** between `f33b752c` and `853a1d25` (`git diff --stat` empty) — every ruling on bounded.ts, condo-records.ts, PropertyOverview.tsx, AnalysisIdentityNotice.tsx, OutcomeAnnouncer sink, and the four other test files carries unchanged.

**Security review of the delta:** the change adds two conjuncts to the substitution-announcement gate — `typeof document.evaluated_input.bbl === "string"` and `substitution.entered_bbl === document.evaluated_input.bbl`. This is a strict **narrowing** (AND-only): a non-corresponding stamp now falls through to the generic classifier. Confirmed:
- No new interpolation — still only `analyzed_bbl`/`entered_bbl` into the same string.
- No new sink — still `OutcomeAnnouncer` (`role="status"` aria-live, escaped text-node child).
- No charset/length/regex change; no new dependency; no forbidden path; no secrets. The test addition is a mutation-sensitive non-corresponding-stamp regression case (correctly asserting the fall-through).

**Rulings carry:**
- Sanitizer inertness (F none), additivity of pre-existing bounded.ts helpers, single text-node sink, status-parsing fail-safe, notice-string bounding, lane isolation, no-dependency/no-secret — all unaffected (surface byte-identical). CARRY.
- Section 4 (announcer): sink and interpolation kind unchanged; announcements strictly fewer. CARRY.
- **F-1 (LOW/advisory) posture unchanged, marginally improved:** `entered_bbl` is now additionally gated to equal the validated `evaluated_input.bbl` before it can be announced. `analyzed_bbl` remains client-validated as non-empty-string only (contract pattern enforced in the out-of-scope, read-only `rule-evaluation-contract.ts`); inert in the escaped aria-live sink. Still advisory, still non-blocking, still routed to discovery.

CI at `853a1d25` all green (orchestrator-captured; thin client — web proof only in CI). Current head `f31f69f4` adds only control-plane records, outside my surface.

**No new findings. No required corrections.** G5 PASS stands at corrected identity `853a1d25`.
