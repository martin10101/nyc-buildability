# M5-T115 — G3 code review (code-reviewer "cr-t115", read-only)

> Transmission history: pinned at ba48acad (material 75c03f25; CI run 36102902944 green at bfefd934, byte-identical
> sources), delivered as 3 SendMessage parts ending with END-OF-REPORT; no truncation. Joined verbatim by the
> orchestrator (transport wrapper tags removed only). Verdict PASS with four ADVISORY items (A1 the
> paraphrased environment note, A2 a new rule on a pre-existing element, A3 no 360px overflow assertion, A4 the e2e resolver).

---

M5-T115 G3 review — code-reviewer (cr-t115). READ-ONLY. Part 1/3.

HEAD pinned ba48acad (= expected). Material 75c03f25 (cherry-pick of f94081ac) verified ANCESTOR of CI head bfefd934, and the source files are byte-IDENTICAL at 75c03f25 / bfefd934 / HEAD — so green CI exercised the reviewed identity. CI run 36102902944 GREEN incl. web (lint+typecheck+build), web-e2e (vitest+Playwright), modularity. `tools/modularity_check.py --check` exit 0 locally; none of the 8 files flagged.

SCOPE DISCIPLINE — clean, no BLOCKING:
- ArchitectEntry.tsx: `git show` = a SINGLE hunk, PropertySearch only. Purely additive — env `<div role=note data-testid=search-environment>` before AddressResolutionScreen; `<p class=section-note>` BBL-alternative framing inside the existing details; `aria-invalid={error?true:undefined}` on the input; the error `<p>` gains className + `data-testid=architect-bbl-error` but KEEPS `id=architect-bbl-error`, `role=status`, `{error}`; a review line appended. The existing details gains an extra class (`architect-search-bbl`) with `card architect-disclosure` retained. No existing string/testid/role removed; pre-existing node order (eyebrow → resolution → details) preserved (insertions only).
- AddressConfirmCard.tsx:188-199: the label block only, gated on the SAME `addressLine` as the existing matched line. Nothing else touched.
- architect.css: appended block 231-252 only; each NEW class (`architect-search-environment/-env-badge/-env-note/-review/-bbl-error/-confirm-label`) appears ONLY in that block (grep count) — no shared/pre-existing selector edited.
- No lib/ change (bbl.ts, disclaimer.ts untouched). No forbidden path; no package.json/lockfile.

PRESERVATION — verified:
- §29 only via REQUIRED_DISCLAIMER; the env note is a DIFFERENT string and entry.test asserts it does NOT contain the §29 opening ("This platform provides preliminary…", confirmed at lib/disclaimer.ts:6). Not a retype.
- No permitted/approved/maximum-allowed wording in any of the 4 new strings.
- No number computed in the UI; no backend status remapped (`data-record-address-status` is pre-existing observability, untouched).
- Four `lib/bbl.ts` messages render verbatim; test assertions match bbl.ts exactly (empty / non_numeric / wrong_length "you entered 5." / invalid_borough "1–5" en-dash).

END PART 1/3.

---

M5-T115 G3 — Part 2/3.

STABLE-CTA (AS-3) — sound. `addressLine` (AddressConfirmCard:84) is derived SYNCHRONOUSLY from `view` props (inputEcho.houseNumber + canonical.streetNameNormalized + boroughName), with NO dependency on the async `useRecordAddress` channel. The new `confirm-matched-label` is gated on that same `addressLine`, so it paints in the first render alongside the matched line and sits ABOVE Continue; it cannot shift Continue when the async record-address line (rendered below the actions) settles. The pre-existing DB-035 pixel-CLS test (360/768/1280) + structural childIds invariant cover the CTA position; the label's synchronous gating adds no new CLS surface. Claim verified true.

ACCESSIBILITY — clean:
- `aria-invalid={error?true:undefined}` — present only while errored, ABSENT (not "false") when valid; entry.test asserts both directions. Correct pattern.
- `role=note` on search-environment; `role=status` PRESERVED on the BBL error `<p>` (aria-describedby announcer contract intact).
- Non-colour cues: badge carries text "Internal build"; error box uses border + text, not colour alone.
- Focus order undisturbed (static note inserted before the resolution screen; review line appended).
- OQ-2 (two `role=note` at ≥768px: shell InternalBanner in a collapsed details + this always-visible one): role=note is NOT a landmark, so no uniqueness violation — honest duplication. ADVISORY, acceptable.

TESTS — behaviour-focused, strict-TS-clean (CI web typecheck GREEN):
- entry.test: env meaning + review line + §29-negative assertion; four DISTINCT bbl errors verbatim; a valid input clears the error, routes (`state.push`), and REMOVES aria-invalid. RTL typed helpers; no casts; not empty.
- address-confirm.test: label present + positioned before `confirm-address` via compareDocumentPosition & Node.DOCUMENT_POSITION_FOLLOWING; absent when no normalized street.
- confirm-journey e2e: typed `Page` param; stable testids/copy, no pixel asserts; the `data-record-address-status` regex covers all 6 enum values (loading|shown|equal|absent|route-absent|error); 15s visibility wait — no timing flake. Helper testids (address-form:65, address-submit:152) are real.
- responsive-a11y e2e: loops 360/768/1280 incl. the phone width where the shell disclosure hides; asserts env + review visible with meaning text. Stable.

END PART 2/3.

---

M5-T115 G3 — Part 3/3.

"BEHAVIOUR ALREADY EXISTED" claim — supported. The material diff touches only 4 test files (entry, address-confirm, confirm-journey, responsive-a11y). Every behavioural exit-gate scenario cited as pre-existing (keyboard/touch, no-auto-pick, manual fallback keeps typed text, per-error recovery, delayed-PLUTO-no-CTA-shift, no-WebGL, multiple/invalid parcel, absent source) is proven by tests UNTOUCHED by this diff (autocomplete.test, address-resolution.test, lot-outline-map.test, lot-outline.spec, responsive-a11y DB-035) that remain green in CI. The genuinely NEW behaviour here is (a) env-badge/review-line visible at every width, (b) the four-bbl-error verbatim styled rendering, (c) the City-matched-address label — each with a new test. Additive-only diff + green CI on the untouched behavioural suites make the claim credible; those out-of-diff behaviours aren't load-bearing for this packet's change.

ADVISORY (non-blocking):
- A1 (OQ-3, ArchitectEntry env note): PARAPHRASES InternalBanner instead of importing it — two independently-maintained sources of the internal-build meaning could drift. Meaning preserved and asserted at every width; acceptable for P2, consider consolidating when the shell is in scope (matches DISC-P2-1).
- A2 (architect.css:249 `.architect-search-actions`): this NEW rule styles a PRE-EXISTING element (AddressAutocomplete.tsx:209, previously unstyled) — additive polish within the allowed autocomplete CSS scope, no behaviour change; noted only because it affects an element outside the two edited components.
- A3 (OQ-1): no 360px horizontal-overflow assertion for the search surface; the env note adds flex-wrap + flex-basis:100% ≤700px (overflow-defensive). G4 can confirm in CI; a pre-existing overflow would predate P2.
- A4 (DISC-P2-2): the synthetic e2e resolver emits only `resolved`, so "warnings above Continue" stays unit-only (address-confirm S1) — honest limitation, not a defect.

No BLOCKING findings. Scope, preservation, stable-CTA, a11y, tests and CI-on-identical-material all verified. (Note: agent-memory write was blocked — Write disabled this session — so no reviewer memory saved; not requesting one.)

M5-T115 G3 VERDICT: PASS
END-OF-REPORT
