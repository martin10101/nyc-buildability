# M4-T007 — G3 code review (independent, read-only) — PR #103
Reviewer: code-reviewer (!= producer rules-engineer). Reviewed head 0817ba1 vs main e9ae2d8.
**VERDICT: PASS.** No binary-float decision path: all COMPUTE_OPS return Fraction via units.to_exact; _divide exact
rational; comparisons via _as_comparable->to_exact; determination compares exact left/right. Residual float() sites
are off the decision path (JSON render, input-domain admissibility fail-closed, geometry canonicalized by to_exact
before any legal arithmetic; covered by test_geometry_float_is_canonicalized_exactly). Fraction built from canonical
decimal strings (to_exact float branch = Fraction(Decimal(str(value))); test asserts to_exact(0.1)==Fraction(1,10)
!= Fraction(0.1)). Per-rule rounding mode/scale/order explicit (ROUND_HALF_UP default), unit enforcement fail-closed
(unknown units -> professional_review, no value), non-finite/malformed/div-zero raise (never fabricate 0/False).
Behavior preserved: 291 rules tests green at frozen SHA. Minor non-blocking (no action): int operands render as int.
