# M5-T124 - G2 producer self-check record (recorded by the orchestrator, role self_check)

Producer: backend-engineer (orchestrator-dispatched subagent) in wt-m5t124; one in-worktree commit ede877bb (parent 4c7437dd), cherry-picked to c65a0724 with every file MATCH by git blob identity (3 files: project-control/reports/M5-T124-producer-report.md 286dc10b, services/api/app/drawings/drawing_alignment.py eede1e29, services/api/tests/drawings/test_drawing_alignment.py c1de0c40).

- **Scope:** services/api/app/drawings/drawing_alignment.py (NEW, pure, unmounted): align_draft_to_lot(draft block, provenance, lot ring in 2263, control pairs) - a 2D rigid least-squares fit (proper rotation + translation), per-pair residuals and a declared tolerance, the implied scale, a mirror-fit check, the outside-lot area (shapely), the outline transformed into 2263 and re-validated by validate_proposed_massing, an alignment provenance block; typed AlignmentRefusal values. services/api/tests/drawings/test_drawing_alignment.py (NEW): 39 offline tests incl. 10 named in-process mutations.
- **Producer self-checks (report, OBSERVED):** ruff clean; test_drawing_alignment.py 39 passed and the sibling import suites (sheet_import + dxf_import) unchanged - 99 passed together; modularity exit 0 (drawing_alignment.py not flagged); all on local Python 3.11 (no shim needed).
- **[ORCH-HARVEST] at the harvested head c65a0724 (cherry-pick c65a0724; Python 3.11 local): `python -m ruff check .` (cwd services/api) -> exit 0: All checks passed!; `python -m pytest tests/drawings/test_drawing_alignment.py tests/drawings/test_sheet_import.py tests/drawings/test_dxf_import.py -q -p no:cacheprovider` (cwd services/api) -> exit 0: 99 passed in 0.63s; `python tools/modularity_check.py --check` (cwd .) -> exit 0:   warn review_signal: tools/agent_supervisor/next_task.py - above the warning threshold; consider the module boundary before growing it further.**
- **Discoveries routed (producer):** DISC-A the importers' build_draft refuses a local-frame draft at the NYC-2263 bounds, so the C2 mount / wiring must assemble the pre-alignment block from the candidate ring + the measured scale without that premature check and validate only the aligned block (closes DB-092 end-to-end; same for non-georeferenced DXF); DISC-B the mount owns the route, rate limit, pre-render allow_nan guard and ceilings; DISC-C control-point entry (click the drawing, click the map) + the discrepancy display is a UI packet - never auto-match. OQ-2: DEFAULT_TOLERANCE_FT = 2.0 is a disclosure-only default (MapPLUTO accuracy is about +/- 20 ft).

G2 verdict: PASS (self_check; the independent reviews and the DCV follow - dispatched by the successor session per the handoff).
