# M5-T074 G0 — readiness gate (orchestrator, administrative)

Task: **M5-T074 — DB-045(c) outline-bridge server-branch proof (test-only)**: the documented
(500, internal_error) rows driven LIVE through the mounted route (internal raise,
ring_crs_mismatch seam, tail guard) with leak-absence and correlation asserts, mirroring the
accepted T068 DB-046(d)/(e) pattern.

## Authority

- The gap is specified verbatim in docs/DISCOVERY_BACKLOG.md DB-045 item (c) (M5-T065 wave
  G4-gap2), routed to the mount-seam cluster; proving it now de-risks the mount packet that
  M5-T073's validation verdict will shape. Lane-3 occupancy authorized by owner directive
  D-084 ("run 3 codex loops"), R003 bound to this packet with a same-commit digest resync.
- `evaluate_task_refs`: **ok=true, applicable == cited (D-084-R003)**.
- Sequencing: depends on M5-T065 — ACCEPTED (247th).

## Packet readiness

- allowed_paths: 2 entries (the one existing test file + the report; report placeholder
  seeded this commit). Zero production paths; outline_bridge.py and both connectors
  FORBIDDEN in the inputs.
- 4 acceptance scenarios: live 500 rows per branch anchor, secret-sentinel leak absence
  (with the ruff-safe `# secretscan:allow` pragma discipline learned at the c0b71938
  convergence), correlation identity, and an additions-only scope bound.
- The known broker-cwd limit is pre-routed in the inputs ([BLOCKED]-to-harvest standard).

## Disjointness (live/frozen neighbors)

| Neighbor | State | Overlap |
|---|---|---|
| M5-T071 (frozen; delta attestations in flight) | awaiting_gate | none — apps/web files |
| M5-T070 (harvested; submit next) | in_progress | none — apps/web + its report |
| M5-T072 (lane-1 LIVE) | claimed | none — tests/site_definition |
| M5-T073 (lane-2 LIVE) | claimed | none — tests/connectors |

Pairwise overlap of allowed_paths vs every live lane: EMPTY.

## Seam records

- Graph regenerated at the seq-126 seam; the navigation block pins the branch anchors
  (:605/:713/:716-717/:824) and the test-only boundary.
- Validator: EXIT 0 recorded at the D-084 capture chain; this packet + the R003 bind + this
  report land in ONE contract-seam commit, G0 recorded at that head.

Verdict: **PASS** — packet is contract-ready.
