# M0-T160 - G2 producer self-check record (recorded by the orchestrator, role self_check)

Producer: backend-engineer (orchestrator-dispatched subagent) in wt-m0t160, two rounds: 4e40eefa (the round-1 binding re-pin 28878be2, re-applied by the orchestrator on the claim seam 83f86ce0) and 777e6b81 (round 2: the runbook section-4 pin paragraph + the test's $pinnedSha). Both cherry-picked into ctl24 (head 83fdb5bd), every file MATCH by git blob identity (tools/controller_update/source_binding.json 4f6fdfdb, docs/CONTROLLER_UPDATE_RUNBOOK.md 4ee5a21b, tools/controller_update/ps_tests/test_runbook_parse.ps1 2288cb62, project-control/reports/M0-T160-producer-report.md 94f62afc).

- **Scope:** the 3-file lockstep of the DB-063 re-scope + the producer report; source_binding.json changes only the three identity values and one authority sentence; the runbook changes only the section-4 pin paragraph (all 14 fenced powershell blocks byte-identical); the test changes only the $pinnedSha line plus one comment.
- **Producer self-checks (report, OBSERVED):** git-plumbing re-verification of a3f24ff3 (commit/tree/subtree), 12/12 required_modules, the 3-way static SHA equality, no '<' in section 4; AS-3 [BLOCKED] in the producer sandbox (routed to harvest by design).
- **[ORCH-HARVEST] the orchestrator ran `powershell -NoProfile -ExecutionPolicy Bypass -File tools/controller_update/ps_tests/run_ps_tests.ps1` from the wt-m0t160 root at 777e6b81 -> EXIT 0 in 477 s: 7/7 test files PASS (test_backup_phase.ps1, test_install_rollback_binding.ps1, test_junction_containment.ps1, test_mutants_detected.ps1, test_null_exit.ps1, test_runbook_parse.ps1, test_source_binding.ps1), 'run_ps_tests(controller_update): 7 test file(s) passed', zero ASSERT-FAIL; at the pre-rescope head a612652b the same suite was RED in test_runbook_parse.ps1 (1 assertion failure, 'binding contract pins the immutable accepted SHA') with the other 6 files green - the lockstep is load-bearing. Static lockstep at the cherry-picked head 83fdb5bd: binding commit_sha == test $pinnedSha == the runbook section-4 SHA == a3f24ff3; the old full 3f4cee86 SHA is gone from the runbook and the test (section 4 names it only as a superseded candidate).**
- **Discoveries routed (producer):** (1) the test could read the SHA from the binding instead of hardcoding it; (2) the controller_update ps_tests are not in CI (the DB-063 root cause) - both recorded for the backlog at the accept seam.

G2 verdict: PASS (self_check; G3 code-reviewer + G5 security-reviewer and the DCV follow).
