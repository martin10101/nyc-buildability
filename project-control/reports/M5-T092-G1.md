# M5-T092 — G1 official-format verification vs the Khronos glTF 2.0 specification (data-contract-verifier "dc-glb", read-only)

> Transmission history: pinned at 4c999e15 (a disjoint peer commit advanced HEAD to 6c79cd88 mid-review;
> the verifier proved the task blobs byte-stable), delivered as four SendMessage parts (1/4..4/4) ending
> with END-OF-REPORT; no truncation. Joined verbatim by the orchestrator (transport wrapper tags removed
> only). The official Khronos glTF-Validator (npm/browser) was not run; the verifier's own spec-derived
> parser reproduced the golden and the report lists what the Validator would add.

---

M5-T092 G1 format-contract verification (data-contract-verifier) — Part 1/4: method + GLB-container constants.

Material 706ae62a: services/api/app/cad/glb_writer.py, tests/cad/test_glb_writer.py, project-control/reports/M5-T092-producer-report.md.

IDENTITY NOTE: HEAD was 4c999e15 at start; a disjoint peer commit advanced it to 6c79cd88 mid-review. Task material is byte-stable — glb_writer.py LF sha256=77404465…ca37ed and test LF sha256=9b7820d6…f999ae0c both match the producer report; 706ae62a touched exactly the 3 allowed paths.

METHOD: I fetched the OFFICIAL Khronos machine-readable JSON schemas (accessor / mesh.primitive / bufferView / material.pbrMetallicRoughness from KhronosGroup/glTF), GLB_FORMAT.md and Specification.adoc myself — NOT the producer's citations. I wrote my own GLB parser from the spec (independent of the writer's and the test's helpers), generated the golden fixture in-memory, and validated the bytes; I also ran the writer's 58 tests (58 passed). Caveat: a small-model summary of the giant HTML spec was unreliable (it hallucinated "5135=UNSIGNED_INT" and misstated buffer.byteLength), so I relied only on the schemas/adoc/GLB_FORMAT.

CONSTANT TABLE — GLB container (value | code | spec | verdict):
- 0x46546C67 magic | :88 | Header/§4.4.2 | CORRECT (decodes 1179937895)
- version 2 | :90 | §4.4.2 | CORRECT
- length = total incl header+chunks | :594-597 | §4.4.2 | CORRECT
- JSON 0x4E4F534A (1, first) | :92 | §4.4.3.1 Table1 | CORRECT
- BIN 0x004E4942 (0/1, second) | :94 | §4.4.3.1 Table1 | CORRECT
- chunk order JSON→BIN | :593 | §4.4.3.1 | CORRECT
- 4-byte chunk start/end align | :101 | §4.4.3.1 | CORRECT
- JSON pad 0x20 | :96 | §4.4.3.2 | CORRECT
- BIN pad 0x00 | :98 | §4.4.3.3 | CORRECT
- byteLength=logical; BIN len ≤ byteLength+3 | :560/:566 | "BIN MAY be up to 3 bytes bigger than buffer.byteLength" | CORRECT
- little-endian | :120 | §4.4.1 / §3.6.1.1 | CORRECT
- single GLB buffer, no uri | :560 | §3.6.1.2 | CORRECT

Continues in Part 2.

---

M5-T092 G1 — Part 2/4: accessor / bufferView / material / asset constants (verified against Khronos JSON schemas + Specification.adoc).

- componentType 5126 FLOAT | :103 | accessor.schema | CORRECT
- componentType 5125 UNSIGNED_INT | :105 | accessor.schema; "UNSIGNED_INT MUST NOT be used for any accessor not referenced by mesh.primitive.indices" | CORRECT (used only on the index accessor)
- type VEC3 / SCALAR | :523/:527 | accessor.schema enum | CORRECT
- POSITION min/max present, len 3, float32-exact | :522 | §3.7.2.1 "POSITION…MUST have…min and max"; §3.6.2.5 bounds | CORRECT
- target 34962 ARRAY_BUFFER | :107 | bufferView.schema | CORRECT
- target 34963 ELEMENT_ARRAY_BUFFER | :109 | bufferView.schema | CORRECT
- byteStride omitted | assembly | schema: required only when ≥2 accessors share a view (none do here) | CORRECT
- mode 4 TRIANGLES | :111 | mesh.primitive.schema (default 4) | CORRECT
- indices accessor SCALAR + unsigned-int | :525 | mesh.primitive.schema indices | CORRECT
- baseColorFactor RGBA ∈ [0,1] | :539 | pbr schema (4 items, 0..1, default [1,1,1,1]) | CORRECT
- metallic 0.0 / roughness 1.0 | :540 | pbr range [0,1]; defaults 1/1 — writer overrides to matte (valid, disclosed) | CORRECT
- asset.version "2.0" | :113 | required, pattern | CORRECT
- asset.generator / extras | :477 | asset.schema | CORRECT
- no NORMAL → flat normals | design | §3.7.2.1 "MUST calculate flat normals" | CORRECT
- NaN/Inf refused | :279,:591 | "MUST NOT contain infinite or NaN values" | CORRECT
- +Y up, metres | doc :26 | §3.4/3.5 | CORRECT
- media type model/gltf-binary | :115 | registered | CORRECT (unused constant)

SOURCES (fetched myself): registry.khronos.org glTF-2.0.html; KhronosGroup/glTF schemas (accessor, mesh.primitive, bufferView, material.pbrMetallicRoughness); GLB_FORMAT.md; Specification.adoc; epsg.io/2263.wkt; nist.gov/pml/us-surveyfoot.

Continues in Part 3.

---

M5-T092 G1 — Part 3/4: rulings (a)/(b)/(c) + independent empirical parse.

(a) AXIS MAP (x,y,z)→(x,z,−y): I computed determinant = +1 (proper rotation = Rx(−90°)); preserves right-handedness and winding. Empirical: an up-facing (+z source) triangle decodes to glTF normal (−0.0, +229.04, 0.0) — +Y, x=z=0, winding preserved. Node transforms are identity (det +1), so §3.7.4 → CCW front faces hold. RULING: CORRECT.

(b) 1200/3937 m per US survey foot: epsg.io/2263.wkt = "NAD83 / New York Long Island (ftUS)", UNIT["US survey foot",0.304800609601219], AXIS Easting EAST / Northing NORTH — so EPSG:2263's unit IS the US survey foot. NIST: "1 foot = 1200/3937 meter exactly"; international foot = 0.3048 exactly (different). Writer uses 1200/3937 = 0.3048006096012192, not 0.3048. Empirical: 3937 ft → exactly 1200.0 m. IMPORTANT: NIST deprecated the US survey foot for NEW measurements on 2023-01-01, but EPSG:2263 stays DEFINED in ftUS (WKT still current), so 1200/3937 is the correct factor here; 0.3048 would be wrong. RULING: CORRECT.

(c) float32 at ±100,000 ft cap: 100000 ft = 30,480.06 m < 2^15, so float32 ULP = 2^(14−23) = 2^−9 = 1.9531 mm ≤ 2 mm (computed empirically). The cap also refuses un-localized EPSG:2263 world coords (~3 cm float32 steps). RULING: CORRECT.

INDEPENDENT PARSER (my own, from the spec; golden fixture generated in-memory): SHA = 30d79d80…63c5 — reproduces the pinned golden EXACTLY. All pass: header magic/version/length; 2 chunks JSON→BIN, 4-aligned; JSON pad 0x20 (residues {0,1,2,3} all occur), no BOM; BIN pad 0x00, len ≤ byteLength+3; single buffer, no uri; POSITION 5126/VEC3 target 34962, indices 5125/SCALAR target 34963, no byteStride; decoded positions == my independent axis-map/foot computation; accessor min/max == per-axis extrema AND float32-exact; mode 4; baseColorFactor [0.8,0.35,0.1,1.0], metallic 0. Refusal smoke: un-localized world coords → coordinate_out_of_range. Writer's own suite: 58 passed.

Continues in Part 4.

---

M5-T092 G1 — Part 4/4: findings, Validator gap, verdict.

BLOCKING: none.

ADVISORY (no rework required):
1. Cosmetic: code comment :87 has a transcription typo "MUST be equal to equal 0x46546C67" (doubled "equal"); the VALUE 0x46546C67 is correct.
2. Citation nuance: §3.6.2.5's exact-match MUST is literally scoped to non-normalized INTEGER accessors; POSITION is FLOAT (bounds must be correct, with a rounding rule). The writer computes min/max FROM the stored float32 values, so they match exactly — compliant and stronger than the float requirement. No change needed.
3. Informational: UNSIGNED_INT (5125) indices are core-legal in glTF 2.0 with NO extension requirement (the WebGL1 OES_element_index_uint concern does not apply); the writer confines 5125 to index accessors, honoring the accessor-schema "indices-only" note.

WHAT THE OFFICIAL KHRONOS glTF-VALIDATOR WOULD ADD (not run — it is npm/Dart; no local npm on this thin client):
- Exhaustive JSON-Schema conformance of EVERY emitted property (I checked those this writer emits plus all constants, not every schema constraint).
- Its own accessor-bounds tolerance algorithm (I verified exact float32 match instead).
- Semantic warnings: degenerate-triangle (the writer REFUSES these — stricter than a warning), unused-object, empty node/mesh, index-vs-vertex-count OOB (the writer validates this).
- Extension validation (none used).
None of these implies a defect given my checks passed. The one machine check not run is the producer's recipe (write golden.glb → github.khronos.org/glTF-Validator, expect 0 errors) — it needs a browser/npm and is correctly routed to harvest.

Every format constant matches the authoritative Khronos schemas/spec; the emitted bytes validate against my independent parser; the golden sha reproduces; determinism holds; rulings (a)/(b)/(c) all confirm; provenance blob-shas match the producer report.

M5-T092 G1 VERDICT: PASS

END-OF-REPORT
