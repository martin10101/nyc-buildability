# M5-T081 + M5-T085 — G4 independent test-adequacy review (qa-engineer "qa-cad", read-only; joint review)

> Transmission history: one joint review pinned at 1414e1a7 (HEAD moved to 16abc891 mid-review; the
> reviewer proved the in-scope files byte-identical across both), delivered as two SendMessage parts
> ending with END-OF-REPORT; no truncation. Joined verbatim in delivery order by the orchestrator; saved
> as M5-T081-G4.md and M5-T085-G4.md (each task's gate cites it). The reviewer's reusable lesson (it
> could not persist agent memory) is relayed by the orchestrator at the seam.

---

G4 test-adequacy review (qa-engineer, independent). Env: services/api, Python 3.11.9. Pinned HEAD 1414e1a7; HEAD moved to 16abc891 mid-review but `git diff 1414e1a7..16abc891 -- <4 in-scope files>` is EMPTY (peer commit touched only registry/gates/other modules), so this verdict holds at both SHAs. No repo/services write left (git status -- services/ clean; all mutation in-memory). Suites: T081 17 passed, T085 19 passed; both goldens match.

=== M5-T081 (DXF writer) — VERDICT: FAIL (test-only correction) ===
Mutation table (mutant | target test | result):
- S0 sanitizer identity-bypass (producer-named literal) | test_as3_newline_injection_refused | RED caught
- S1 sanitizer scrub-and-continue (idiomatic) | same | RED caught  -> two-mutant rule PASSES for the sanitizer
- S2 sanitizer CR/LF-only (idiomatic) | (none) | GREEN SURVIVOR; verified it also leaks a non-ASCII byte the pristine refuses
- H0 PROPOSED_LABEL drop "NOT A CITY RECORD" | test_as4_annotation_carries_required_labels | GREEN SURVIVOR (label truly absent from output, test still green)
- H1 drop "MAXIMUM ALLOWED" from CLAIM_CLASS_WORDS | test_as4_no_claim_class_words_anywhere | GREEN SURVIVOR
- H2 disable _assert_no_claim_words | (none) | GREEN SURVIVOR
- B0 ring min-vertex `<3`->`<2` | test_as3_degenerate_ring_refused | GREEN but refusal code still "degenerate_ring" (masked by area check; not a finding)

F1 (BLOCKING, test-only): AS-4 honesty label is not pinned. test_as4_annotation_carries_required_labels imports PROPOSED_LABEL/CRS_UNITS_NOTE BY VALUE then asserts membership, so weakening the constant ships green — verified: dropping "NOT A CITY RECORD" leaves the test green while the literal is absent from output. This is the exact scenario AS-4 exists to guard and honesty is a binding directive (D-073-R006/D-076-R002/D-083). The CRS note is partly saved by hardcoded "EPSG:2263"/"US SURVEY FEET" backups; the PROPOSED label has NO hardcoded backup. Fix = one line, no source change: add `assert "PROPOSED - NOT A CITY RECORD" in text` (hardcoded literal), exactly as T085 already does. The module code is correct; the test is inadequate for AS-4.

F2 (ADVISORY): sanitizer's non-CR/LF clause untested. S2 (CR/LF-only) survives and leaks non-ASCII; the injection-critical CR/LF path is well covered (S0+S1 redden). Recommend a param test asserting DxfSanitizationError for "\x00", "\x1b", and one non-ASCII char.

F3 (ADVISORY): claim-word guard not proven load-bearing. H2 (disable guard) and H1 (shrink list) both survive; only 3 of 11 CLAIM_CLASS_WORDS have a hardcoded grep backup (CERTIFIED/LAWFUL/LEGAL/ENTITLEMENT/GUARANTEED/AS-OF-RIGHT have none). Recommend one test that feeds a claim word through build/_assert_no_claim_words and asserts refusal, plus assert the full expected word set.

Determinism: golden stable — fixed 6-dp real format, -0.0 normalized, LF EOL, in-memory ascii hash (no CRLF smudge). No concern.

M5-T081 G4 VERDICT: FAIL

(part 2/2 next: M5-T085)

---

=== M5-T085 (PDF site-plan writer) — VERDICT: PASS ===
Strict-reader round-trips ran through the test's documented 3.11 sideload fallback (exercised, worked).
Mutation table (mutant | target test | result):
- E0 escaper full ASCII-only bypass (producer-named) | test_escaper_bypass_mutant_reddens_roundtrip / round-trip | RED caught (content refused)
- EA escaper drop backslash-escape (idiomatic) | test_special_characters_round_trip_through_escaper | RED caught (backslash lost -> recovered != input)
- EB escaper drop paren-escape, keep backslash (idiomatic) | same | GREEN SURVIVOR on balanced "(REAR)"; an unbalanced "12 MAIN ST (REAR" is REFUSED_CONTENT (verified) -> the balanced-only fixture cannot see it
- X0 xref offset off-by-one (producer-named) | test_golden_sha256 + round-trip | RED caught (golden mismatch AND reader refuses)
- D0 _DIM_DECIMALS 2->1 (idiomatic) | test_dimension_strings_from_2263_feet | RED caught (asserts hardcoded "30.00"/"100.00"/"20.00"/"60.00")
- T0 title "PROPOSED - NOT A CITY RECORD" -> "PROPOSED" | test_proposed_and_professional_review_stamps_always_present | RED caught (hardcoded literal)

F1 (ADVISORY): the escaper round-trip fixture uses only BALANCED parens "(REAR)". EB shows an idiomatic weakening that forgets paren-escaping (keeps backslash) ships green, yet an unbalanced-paren address corrupts/refuses the sheet (verified REFUSED_CONTENT). The producer's named full-bypass mutant DOES redden and the escaper is present and correct, so this is a coverage completeness gap, not a defect. Recommend adding an unbalanced-paren address (e.g. a lone "(") to test_special_characters_round_trip_through_escaper. Non-blocking.

Strengths confirmed (worth recording): honesty literals are HARDCODED in the tests (T0 caught) — this is exactly the gap T081 F1 has, so T085 is the correct pattern to mirror; xref offsets are independently reparsed and the off-by-one reddens both golden and reader (X0); dimension strings hardcoded (D0 caught); measurement uses EPSG:2263 feet; grid-north/true-north pinned. Determinism: golden is a pure function of inputs (generated_at/version are inputs, not a clock), round-to-3dp + -0.0 collapse + latin-1 over ASCII-sanitized text, stdlib-only float ops (IEEE-deterministic) — same bytes on 3.11 here and 3.12 CI. Low residual risk.

Minor (note, not a finding): test_roundtrip_reader_accepts_and_counts derives expected segment/text counts from writer constants (writer._SCALE_BAR_SEGMENTS etc.), so it verifies reader/writer AGREEMENT rather than an independent spec count; a consistent change to _SCALE_BAR_DIVISIONS would survive. Acceptable for a structural round-trip.

M5-T085 G4 VERDICT: PASS

Reusable lesson for the ledger (I could not persist agent memory — Write and shell file-writes are guard-blocked for qa-cad this session): "honesty/label acceptance tests must assert the HARDCODED literal string, never `assert IMPORTED_CONST in output`, because the by-value import makes the assertion tautological — T085 does this right, T081 F1 does not." Mirrors the seq127 two-mutant-guard lesson.

END-OF-REPORT
