# M5-T120 — G3 code review (code-reviewer "cr-t120", read-only)

> Transmission history: round 1 pinned at b0eb29f7 (material 5ae5700e, identity before the correction), delivered as
> 2 SendMessage parts ending with END-OF-REPORT (part 2 was queued twice, byte-identical; the first copy is joined); then a DELTA attestation after the [ORCH-CORRECTED per M5-T120 G1]
> doc-only commit 793fed27 (re-frozen at seam 858989c1, identity 80c33f6d) - the verdict carries to the corrected identity.
> No truncation. Joined verbatim by the orchestrator (transport wrapper tags removed only). Verdict PASS; the split a pure move, the monkeypatch audit clean;
> three ADVISORY items (A1 the citation routed to G1, A2 /DP synthetic-only, A3 documented mixin coupling).

---

M5-T120 G3 code review (READ-ONLY) — PART 1 of 2

PIN: HEAD `b0eb29f7` at start (matches expected). Material `5ae5700e` (cherry-pick of wt-m5t120 `3d08a559`). Confirmed the 9 drawings/tests files are BYTE-STABLE from `5ae5700e` to HEAD (`git diff --stat 5ae5700e b0eb29f7 -- services/api/app/drawings services/api/tests/drawings` = empty), so I reviewed HEAD safely.

INDEPENDENT EVIDENCE (all green):
- `python -m ruff check .` (cwd services/api) -> All checks passed.
- Drawings suite via the orchestrator shim -> `322 passed` (includes the split-equivalence goldens + the 17 new p4 tests). NB the producer report says 289; my full-directory run is 322/0-fail — a shim-scope difference, not a defect (stronger, all green).
- `python tools/modularity_check.py --check` -> exit 0. sheet_interpreter (577), sheet_path_state (248), sheet_inline_image (444) are ABSENT from the warn list (all under the 600 warn line).
- CI on the T120-inclusive head `b0eb29f7`: the **`api (ruff + pytest)` job COMPLETED SUCCESS under Python 3.12** (the authority). Only `supervisor-bridge` (orchestration tooling, unrelated to drawings) still runs. The earlier `failure` on `d1970b06` is M5-T123's submit head (route/rate-limit work), not T120.

FINDING 1 — THE SPLIT (pure move + facade): PASS.
- No duplication: grep for every moved path helper (`_flush_open/_open_after_close/_moveto/_lineto/_orphan_lineto/_curveto/_close/_charge_points/_paint/_op_*`) in sheet_interpreter.py returns NONE — the group lives only in sheet_path_state.py. `SheetPolyline` import correctly removed from the interpreter (moved to the mixin).
- Facade intact: sheet_reader.py:81 `from app.drawings.sheet_interpreter import _IDENTITY, _StreamRun` — both still defined there (_IDENTITY:65, class _StreamRun:106). `_PATH_HANDLERS` re-imported into sheet_interpreter (line 51) and read by `_execute` as this module's global (line 282), so a `sheet_interpreter._PATH_HANDLERS` patch still bites.
- Mixin coupling on host `_StreamRun` attrs (_map/_take/_interp/_current/_cur_points/_subpaths/_subpath_start/_operands) is DOCUMENTED (sheet_path_state.py:8-14, 38-46). Acceptable, idiomatic mixin split.

Continued in part 2.

---

M5-T120 G3 code review — PART 2 of 2

FINDING 1 (cont.) — MONKEYPATCH AUDIT (seq-130 split-facade trap): PASS. Grepped every setattr/monkeypatch on sheet_interpreter in tests/drawings and traced each consumer:
- read_inline_dict / _SKIP_SHADING / skip_inline_image / _StreamRun._place_form — all STAY in sheet_interpreter and their consumers stay there; each bites.
- The at-risk one: `_is_finite_point` is now imported into BOTH modules (interpreter uses it in `_map`/`_map_via`; the moved `_curveto` uses sheet_path_state's copy). The ONLY committed patch of `sheet_interpreter._is_finite_point` (split_equivalence:708) drives `1 1 m 2 2 l S` through `_map` (the MOVETO gate, which STAYS in the interpreter) — I read it; it still bites. Not vacuous.
- Moved constants (`_PAINT_STROKE/_FILL/_CLOSE_FIRST`, `_PATH_HANDLERS`): NO committed test patches them on sheet_interpreter, and the producer added COMMITTED positive controls that patch the NEW namespace (test_split_paint_flag_sets_bite_at_new_namespace patches sheet_path_state; test_split_path_handlers_bite_at_new_namespace patches sheet_interpreter._PATH_HANDLERS -> "unsupported operator"). Both pass in the 322.

FINDING 2 — ORPHAN-LINETO (DB-090 i): PASS. `_orphan_lineto` (sheet_path_state:83) fires ONLY when `self._current is None`; opens the subpath at its OWN mapped point (`_cur_points=[point]`), draws NO segment from a stale point, increments page+doc counts. `_paint` nulls both `_current` AND `_subpath_start`, so post-paint the next `l` routes to the orphan path (not `_open_after_close`, which is the legitimate post-close Table-59 case where `_current` is the subpath start). The reopen-at-last-point mutant asserts the DISPLACED field MOVES ([10,10,20,20] -> [5,5,10,10,20,20]) — satisfies the M5-T083 G4 "assert the field the bug displaces" rule. Curves (c/v/y) with no current point stay typed refusals (feature "path"/"v"). Count threading through sheet_reader (init/begin_page reset/_build_page stamp) + sheet_primitives (SheetPage.orphan_subpaths=0 default, SheetDocument aggregate) mirrors shading/inline exactly — byte-safe.

FINDING 3 — /DP DICTIONARIES (DB-090 a): PASS. `_nested_dict_allowed` gates dicts to /DP//DecodeParms only; `_skip_dict` caps at ONE level (`_MAX_INLINE_DP_DICT_DEPTH=1`), under the same 4096-byte cap; value stored as the `_DICT_VALUE` sentinel (never a geometry key), no sample decoded. Mutations (disallow / allow-any / raise-cap) all committed and green.

FINDING 4 — GOLDEN SUBSTITUTION: PASS, machine-verified. Exactly one swap: `refuse_l_no_current` (`5 5 l S`, now a READ) -> `refuse_curve_no_current` (`5 5 10 10 20 20 c S`, still refuses); _OVERALL recaptured. `test_equivalence_every_case_matches_pre_split_golden` asserts the case SET matches AND every retained per-case digest == golden — its passing in my 322 run PROVES all 61 other digests byte-identical (not just the producer's claim).

FINDING 5 — MODULARITY / DEAD CODE: PASS. Clean move, no orphaned code, exit 0.

ADVISORY (non-blocking):
A1. Orphan-lineto reader-of-record cite (Canvas-2D/pdf.js/MuPDF) is honestly `[recalled - verify]` and routed to G1 — G1 owns that, not G3. ISO §8.5.2/8.5.3 basis is verified.
A2. /DP-dict path is exercised by synthetic tests only (real corpus item 2's 8,154 inline images carry no /DP dict) — honestly disclosed (OPEN QUESTION 1); bounded + mutation-covered; not a gap.
A3. Mixin relies on host attrs via documented coupling + a `# type: ignore`; CI has no mypy gate (ruff only), so no static-type risk.

M5-T120 G3 VERDICT: PASS

END-OF-REPORT

---

M5-T120 G3 DELTA attestation (identity carry after the doc-only G1 correction).

Inspected `git show 793fed27` (read-only):
(a) DOC-ONLY confirmed. Exactly 3 files: sheet_path_state.py, sheet_inline_image.py, and the producer report. Both code-file edits are strictly INSIDE docstrings — the `_orphan_lineto` `"""..."""` (reader-of-record citation: now WHATWG Canvas-2D lineTo / pdf.js verified, MuPDF's `fz_lineto` warns-and-ignores so removed as a match, tagged [ORCH-CORRECTED per M5-T120 G1]) and the `_components` `"""..."""` (Table 92 = /CS key, Table 93 = its value abbreviations). Every executable statement below each docstring is UNCHANGED; no test file touched; no behaviour change. The correction is factually right — the implementation still opens a subpath at the point's own end (the Canvas-2D/pdf.js leniency); MuPDF is stricter, correctly no longer claimed as a match.
(b) Independently re-verified at the new head: `ruff check` on both changed files -> All checks passed (longest changed docstring line = 100 chars, under the E501 >100 threshold). The only commit after the re-freeze `858989c1` is `e392a7cb` (an M5-T122 review-record seam) — DISJOINT from every T120 path (`git diff --stat 858989c1 HEAD` over all T120 files = empty), and `793fed27` is an ancestor of HEAD, so T120 content identity 80c33f6d is byte-stable at HEAD. The 322-pass drawings suite / modularity exit 0 carry unchanged (docstring/report text cannot alter them).

My G3 PASS carries to content identity 80c33f6d (re-froze at seam 858989c1).

M5-T120 G3 DELTA: PASS

END-OF-REPORT
