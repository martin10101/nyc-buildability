# M5-T051 G4 — independent QA review (qa-engineer, saved VERBATIM from the agent return)

G4 QA verdict for M5-T051: **PASS** (zero BLOCKING; one ADVISORY). HEAD pin verified = ce11d09fea402cbfd5b0b0a64b4cc717790e6e09.

## Executed suites (from services/api; modularity from root)
- `ruff check .` → All checks passed
- `pytest tests/scenario -q` → **568 passed** (matches producer exactly — T051's home suite)
- `pytest tests/api -q` → **497 passed** (NOT the 439 the prompt/producer stated — see finding 1)
- `modularity_check.py --check` → **exit 0**, 0 failures (20 warnings, none on T051 files)

## Findings
1. **[ADVISORY] api = 497, not 439.** Not a T051 defect. T051 adds ZERO api tests — its allowed_paths and the material diff (`git show --stat f1ec64a8`) touch only `derivation.py`, `proposal.py` (+20), `test_scenario_derivation.py` (new), `test_scenario_proposal.py` (+70), fixtures, report — nothing under `tests/api/`. The +58 (439→497) is M5-T052 (+15→454) and M5-T053 (+43 route→497), both accepted and confirmed ancestors of the material (`git merge-base --is-ancestor 25617bbd f1ec64a8` = YES). Producer's 439 is its stale worktree base (the T050 seam, which reported 439); at the frozen head f1ec64a8 and at HEAD the true count is 497. All 497 pass — no hidden regression.
2. **[PASS] Byte-stability.** `git diff f1ec64a8..HEAD` empty on all 6 allowed_paths + fixtures — T051 material intact at HEAD.
3. **[PASS] Fixture discipline — hand-recomputed BY ME, independent of the module:**
   - rectangle_100x80: shoelace 2A=16000 → 8000 sf; 8000/16000=0.5; gross 8000·1+8000·2=24000; cum 12+20=32; every-wall lot-line 15.0; south street 200000−199960=**40**. All match fixture `expected` + README.
   - l_shape: shoelace 2A = 0+4000+1600+2400+4800+0 = 12800 → **6400 sf** (concavity exact); 6400/12800=0.5; upper own 40·40=1600; gross 6400+4800=11200; cum 12+33=45; south lot 200000−199990=10. All match.
   - wall_setback (spot): front lot 18, rear lot 200030−199982=**48**, street 200000−199975=25. All match.
   Expectations live in each fixture's static JSON `expected` block, loaded by `_derive_fixture` (test_scenario_derivation.py:81) — never fed from module output. README shows the arithmetic. **Not self-proving.**
4. **[PASS] AS-6 mutation — DB-034(e) genuinely binds the branch.** `test_collinear_degenerate_outline_refused` uses A-B-C-A (3 distinct collinear vertices). In `_ring_is_simple` (proposal.py:143) at k=0: v1=(−50,0), v2=(100,0) → cross=0, dot=−5000<0 → the line-170 `cross==0 and dot<0` branch returns False. With n=3 all non-adjacent edge pairs are skipped by the adjacency guards (178) and no zero-length edge exists, so ONLY that branch can refuse. Remove lines 170-171 → `_ring_is_simple` returns True → validation passes → `pytest.raises(ProposedMassingError)` fails. Confirmed the material did NOT modify `_ring_is_simple` (pre-existing, previously-unbound branch, now bound). DB-034(c): wall 0→4 refused via `0%4==4%4==0` (proposal.py:416); reversed 4→0 refused; control 4→1 stays valid; removing the block → 0→4 passes → test fails. Both mutation-bound.
5. **[PASS] AS-3 honest absence is TYPED, not zero/null ambiguity.** rear street setback: value=None + resolution `ABSENT_NO_ATTESTATION` + method `street_line_setback_absent` + provenance None — distinguishable from a derived 0.0 (asserted in test_street_setback_absent_is_typed_honest_absence:184). Lot-line absence likewise typed (method `lot_line_setback_absent`).
6. **[PASS] AS-4 facts-not-allowances.** Module output (method/unit/detail/source_class/input_ids + gross treatment vocab) carries no permitted/required/allowed/compliant terms; all `allowance` references are disclaiming ("not an allowance"). test_no_allowance_language_in_output enforces the regex over every record + the vocab.
7. **[PASS] AS-5 invariance actually transforms inputs.** Rotation cyclically re-orders the distinct vertices (shift 2) + re-closes, asserts area equal (test:271). Translation shifts block AND lot by (3000,−4000), asserts area/coverage/setback distances equal (test:296). Both genuine transform-then-assert-equality, not tautologies.
8. **[PASS] Negative space.** Every ProposalDerivation numeric value is bound to a VALUE assertion: footprint, coverage, per-level areas, gross, per-level + cumulative heights, lot-line + street setbacks — all asserted exactly against hand values. Gross-treatment vocabulary asserted (treatment enum `PER_LEVEL_GROSS_NO_DEDUCTIONS` + "not a zoning floor area" in the exclusions).
9. **[PASS] B0 regression.** Material is purely additive: proposal.py +20 (the DB-034(c) block only; B0 semantics byte-preserved), test_scenario_proposal.py +70 (4 additive tests, no deletions). 527 pre-existing + 41 new (37 derivation + 4 proposal) = 568, all green.
10. **[PASS] CI.** Orchestrator-captured evidence (M5-T051-ci-evidence.md) records run 35455214032 success / 0 non-success at f1ec64a8, first round; verified the stored evidence (reviewer read-only, no gh) and my local ruff/scenario/modularity reproduction is consistent.

## Basis
Every hand-computed fixture value reproduces from my own arithmetic off the stated vertices — footprint/coverage/gross/heights/setbacks — with expectations stored as static JSON, so the pack is genuinely hand-computed, not self-proven. The two mutation-sensitive refusals (DB-034(c) closing-vertex wall, DB-034(e) collinear reversal spike) are each bound by a test that fails if the guarding branch is removed, and the (e) branch was pre-existing/unbound and untouched by this task. Facts-vs-allowances, typed honest absence, and the invariance properties are directly asserted. T051 material is byte-stable at HEAD, its scenario suite reproduces exactly (568), and the only count divergence (api 497 vs a reported 439) is a stale-worktree-base artifact fully attributable to disjoint accepted tasks T052/T053 — advisory, not a regression.

Suggest the orchestrator correct the "439 api" figure in the producer/source-sections records to 497 (the true count at the frozen head) for ledger accuracy — non-blocking. Note: my agent-memory write for this lesson was blocked by read-only guard, so it's captured here only.
