# M5-T016 G0 contract review (administrative; orchestrator)

Date: 2026-09-12 (UTC). Packet: project-control/tasks/M5-T016.json (address-entry Packet 2:
Address Confirm card + ZoLa deep-link + handoff).

G0 checklist per /start-controlled-task:

1. **Exact requirement identifiers** — named: design spec sections 1 / 2-resolved / 3-ZoLa rule /
   4 / 5 / 6-Packet-2 (`docs/design/address-entry-confirm-design-spec.md`) plus the confirmed ZoLa
   URL research (`docs/design/zola-deeplink-url-confirmation.md`, preserved verbatim 2026-09-12).
2. **Directive refs** — D-038:ALL, regime 1.0 (same posture as M5-T014/T015; machine-applicable
   set expected empty, verified at accept by independent DCV).
3. **Exact source/evidence files** — nine named inputs (spec, ZoLa research, the accepted Packet-1
   surface, address-api.ts, read-only bbl.ts/ConfirmScreen/endpoint, the M5-T015 carried-findings
   record). No "read the repo" scope.
4. **allowed_paths / forbidden_paths** — nine exact files; shared libs (bbl/bounded/announce/api),
   all property/confirm/compare/app trees, packages, and control-plane trees forbidden. No overlap
   with any concurrent task (none in flight).
5. **Acceptance scenarios** — S1–S8 recorded in the packet: confirm-card routing, adversarial
   ZoLa/Continue href discipline (validateBblInput-gated, reflected text can never reach an href),
   not-my-property return, provenance disclosure + not-verified posture, extended hostile-resolved
   render safety (discharges M5-T015 G5 F-2), behavior-neutral card extraction (discharges G3 F4,
   adds the G4 finding-3 body-copy assertions), no-new-deps/tokens CI-green.
6. **Gates + reviewers** — G0/G1/G3/G4/G5; roster code-reviewer, qa-engineer, security-reviewer,
   human-journey-reviewer (G3 is the human-journey walkthrough this packet was slated to carry;
   producer frontend-engineer/orchestrator differs from every reviewer).
7. **Modularity boundary answers** — (1) responsibility: the address confirm/outcome presentation;
   (2) owning modules: new AddressConfirmCard + extracted AddressOutcomeCards, host stays
   AddressResolutionScreen; (3) the screen file is currently ABOVE the 600-SLOC warn threshold —
   this packet's S7 extraction brings it back under; (4) extraction-before-growth is explicitly in
   scope; (5) stable interface: the screen's public component signature and every Packet-1
   data-testid/behavior (S7 pins neutrality); (6) protecting tests: the Packet-1 module plus the
   new address-confirm pack; (7) modularity check must pass before submit.
8. **Dependency** — M5-T015 (ACCEPTED, 184th). No hold touches this packet; Packet 3 remains on
   the owner-review expansion hold and is out of scope by forbidden paths + path note.

Registry validator run after packet enrichment: EXIT 0.

G0 verdict: **PASS** (administrative contract review; orchestrator). Production may claim.
