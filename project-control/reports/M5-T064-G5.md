# M5-T064 G5 gate — independent security review (verbatim reviewer return)

Reviewer: security-reviewer (claude-opus-4-8 per D-064), dispatched by the orchestrator at
the seq-124 T064 wave, pinned to the frozen submit head 812a5860 (material 409a8a7d).
Recorded by the orchestrator; the reviewer is read-only and records no gate itself. The
return arrived in five parts (the first transmission truncated in finding 4; the resend's
first segment was initially lost in transit and re-sent on request) — joined verbatim at
the exact break points below.

---

VERDICT: PASS

# G5 Security Gate Report — M5-T064

**Task:** M5-T064 — D-082-R002 max-envelope engine slice 1 (deterministic maximum-envelope derivation + flag-gated UNMOUNTED route)
**Reviewer:** security-reviewer (G5, read-only)
**Verdict:** PASS
**Date:** 2026-09-21

## Frozen identity (verified)
- `git diff 812a5860..HEAD` over the 5 allowed_paths is **empty** — the submit-head content is byte-identical to current HEAD (6c181a6c). Verdict binds to that frozen path-scoped content.
- `git show 409a8a7d --numstat` = **exactly 5 files**, all inside `project-control/tasks/M5-T064.json` allowed_paths (engine 996+, route 340+, two test files, producer report). No manifest (`requirements.txt` / `pyproject` / lockfile / `package.json`) touched — the **no-new-dependency** claim holds.
- Disjoint-peer note acknowledged: control-plane commits between submit and review do not touch the 5 task paths; this verdict is path-scoped.

## Evidence run (in wt scope, from `services/api`)
- `python -m ruff check` on both source files → **All checks passed** (exit 0).
- `python -m pytest tests/scenario/test_max_envelope.py tests/api/test_max_envelope_api.py -q` → **56 passed** (exit 0), matching the expected count.

## Findings by scope area

**1. Exposure posture — PASS.** The route is flag-gated on `INTERNAL_RULE_EVAL_ENABLED` (`app/config.py`: fail-safe, default-off, only the closed set `{1,true,yes,on}` enables; absent/empty/unknown → disabled). Flag-off returns a generic 404 byte-indistinguishable from an unmounted path (no correlation id, no body hint — `_not_found`). Genuinely **UNMOUNTED**: `grep max_envelope services/api/app/main.py` → no matches. The mount-absence test (`test_route_is_unmounted_in_the_real_app`) is **non-vacuous** — it imports the real app, enumerates `real_app.routes` and `real_app.openapi()["paths"]`, and asserts the path is absent from both. `include_in_schema=False`.

**2. Input validation at the route — PASS.** Ordered fail-closed hardening, all reusing the accepted sibling's primitives (not forks): raw-body ceiling (256 KiB) via bounded streaming *before* parse (catches chunked / under-reporting Content-Length); strict-JSON + NaN/Infinity + unpaired-surrogate guard with renderer-parity encoder settings; BP-4 route caps on `lot_line_segments` (800) / `street_lines` (400) before any engine work; BP-2 label charset+length cap; BP-5 `lot_rule_facts` key + value-type + **registry-derived** enum/numeric-domain checks before anything is fed. Every refusal is typed `(422, "validation_error")` with a field name and a bounded message.

**3. Information-leak posture — PASS (G4's untested-500 concern judged directly on the code).** Every 500 emitter (`_internal_error_500`, registry-unavailable, generator-checker-inconsistency, serialization-unsafe, bare `except`) returns a fixed generic body and logs **correlation_id only** — no `str(exc)`, no traceback, no path. The generator-checker inconsistency path correctly maps the internal-invariant `candidate.*` field to 500 (not 422). Response `message` bounded to 400 chars (`_bounded_message`); `field` bounded to 200 chars (`_bounded_field`). Correlation id minted per-request after the flag check and emitted on every non-disabled response.
- *Informational (not a finding):* the engine's `area_sq_ft` precondition error embeds `{value!r}` and reaches the 422 body. It is bounded (≤400 chars), `repr`-escaped, JSON-encoded (not HTML), and only reachable behind the internal flag — identical to the accepted checker's echo discipline. No action required for this slice.

**4. Compute-abuse surface — PASS.** All iteration is bounded independent of caller input: `_shrink_depth_to_cap` (≤128 steps, O(1)/step), `_footprint_area` (≤8), `_height_levels` (≤8). The only per-size loop is `_lot_rectangle`'s two O(n) passes over `lot_line_segments`, with n capped at 800 by the route. The generator-checker consistency proof runs `check_proposal` on an **internally-generated 4-vertex** candidate, so no caller-controlled outline reaches the O(n²) simplicity surface; the lot it re-feeds is bounded by the same caps the already-accepted sibling uses (no new surface). Threadpool posture correct: the engine runs off the event loop via `run_in_threadpool`, and `_effective_registry()` resolves **on** the event loop first (line 290, before the threadpool hop) so no double-load race. Global threadpool sizing / per-request timeout is a public-exposure-packet concern, explicitly deferred in the route's own disposition — out of scope for this internal slice.

**5. Cross-tenant isolation — PASS (no surface introduced).** Stateless compute; no DB, no persistence, no tenancy, no BBL lookup — the lot is caller-supplied. Authn/tenancy/ownership are documented as a deferred disposition arriving with the public-exposure packet; the route is unreachable without the internal flag.

**6. Service-role secrecy / secrets — PASS.** No secrets, credentials, tokens, or URLs in either file; no env access beyond the feature flag.

**7. Private storage / SSRF — PASS.** No outbound network calls, no caller-controlled path or URL, no file writes. `RuleRegistry().load()` reads local rulesets only.

**8. Upload controls — N/A** (no file upload).

**9. Prompt-injection — PASS/N/A.** Deterministic engine; no AI/LLM in-path. Caller input is treated strictly as untrusted data, never as instructions.

**10. Least privilege — PASS.** Read-only imports of accepted modules; the diff is exactly the 5 allowed_paths (no forbidden-path edit). The **authoritative lot area cannot be overridden** by a caller fact: `_LOT_AREA_INPUT="lot_area_sq_ft"` is set first from `LotContext.area_sq_ft`, and `lot_area_sq_ft` is absent from `CALLER_RULE_INPUT_NAMES` (verified in `proposal_checks.py`), so a caller-supplied `lot_area_sq_ft` lands in `unmapped_lot_facts` and is never fed — test-pinned by `test_unmapped_lot_facts_are_surfaced_never_fed`.

**11. Log redaction — PASS.** Every log call emits correlation_id plus at most a bounded, charset-constrained field name; never a raw value, `str(exc)`, or traceback. No CRLF log-injection path: field/key charset `[A-Za-z0-9 ._:-]` excludes newlines, and caller-influenced keys are charset-validated (`_validate_lot_rule_fact_keys`) before any domain refusal can place a key into a logged field.

**12. Provenance / fail-closed (can it emit an envelope it shouldn't?) — PASS.** Every value is registry-derived through the existing evaluator; unbounded directions become honest `NO_APPLICABLE_RULE`/`FAMILY_UNSUPPORTED` gaps rather than fabricated ceilings; non-commensurable dimensions (residential FAR, rear yard) are forced to honest gaps by a semantic gate that runs *before* the registry; the fixed disclosure marks the output a rules-derived ESTIMATE; and generator-checker consistency fails closed — a candidate the checker would refuse is never emitted (raises `MaxEnvelopeError` → 500), test-pinned by the AS-4 epsilon-mutation tests.

## Conclusion
No critical, high, medium, or low security defects. One informational note (bounded reflected `area_sq_ft` repr in the 422 body — ≤400 chars, `repr`-escaped, JSON-encoded not HTML, only reachable behind the internal flag, matches the accepted checker's echo discipline) requires no change and matches accepted precedent. The route's fail-safe disable, unmounted posture, reuse of hardened boundary primitives, bounded compute, leak-safe error/logging discipline, and fail-closed provenance are all sound.

**VERDICT: PASS** — recommend recording G5 PASS for M5-T064.

--- END OF REPORT ---
