{
 "requirements": {
  "D-038-R003": [
   "services/api/app/api/v1/scenario_analysis.py: new M5 backend PRODUCT surface (NOT M0 self-infra) exposing the already-accepted offline optimization toolkit as four internal POST endpoints /api/v1/properties/{bbl}/scenario/sensitivity|ranking|comparison|threshold; each rebuilds the property profile, its rule_evaluation and the scenario document SERVER-SIDE over the same injected seams the accepted M5-T003 route uses, then calls the accepted engine function READ-ONLY through the public app.scenario facade (analyze_scenario_sensitivity M5-T008, rank_scenario_assumption_sets M5-T007, compare_scenario_assumption_sets M5-T010, find_scenario_threshold M5-T011). services/api/app/api/v1/scenario_analysis.py is the only new module; services/api/app/main.py is additive-only (one import + one include_router + a mirroring comment, 8 insertions / 0 deletions, verified by identical blob OID 071f7755 across the rework).",
   "Contracted as a normal G0 packet: project-control/gates/M5-T012-G0.json result=PASS (intake report project-control/reports/M5-T012-G0-intake.md). required_gates G0/G1/G3/G4/G5 ALL PASS at reviewed_sha 12fda82f3f237f2432efc99614c903496a8b2e02 - G1 code-reviewer (M5-T012-G1-rereview.md), G3 qa-engineer (M5-T012-G3-rereview.md), G4 integration/regression evidence via ci-evidence-verifier recorded under the rostered label code-reviewer (M5-T012-G4-rereview.md), G5 security-reviewer (M5-T012-G5-rereview.md). Every reviewer independent and != producer backend-engineer; the DCV (directive-compliance-verifier, M5-T012-DCV-rereview.md) is independent of both the producer and the four gate reviewers.",
   "Executable acceptance scenarios AS-1..AS-8 map to real passing tests in services/api/tests/api/test_scenario_analysis_api.py (171 items). Reproduced by the orchestrator and independently by four reviewers: python -m pytest services/api/tests/api -> 312 passed (141 pre-existing + 171 new, 0 regression; the six pre-existing api files keep 4/41/34/3/32/27 = 141); python -m pytest services/api/tests/scenario -> 388 passed (unchanged, no engine module touched); python tools/modularity_check.py --check -> failures 0, EXIT 0. G3 re-ran the full mutation matrix independently: 21 mutations applied, 21 CAUGHT, 0 survived, each reverted byte-exact, confirming every acceptance scenario is load-bearing rather than merely executed.",
   "The untrusted-input boundary is the substance of the deliverable and is enforced, not merely claimed: the request body may carry ONLY illustrative analysis parameters and is merged into ENGINE ARGUMENTS only, never into the server-rebuilt document, so the canonical cap and coverage/verification status cannot be forged. A body key in FORBIDDEN_FACT_KEYS is rejected at ANY depth with a typed (422,'validation_error') carrying detail.rejected_keys. Verified independently: G1 iterated the live frozenset and got 23/23 keys rejected when buried inside an assumption dict, one level deeper, in comparison's named-dict shape and inside values[]/domain[], with no over-rejection (valid rich nested assumption sets still 200 with cap=15000.0); DCV reproduced its own earlier attack body as 422 with rejected_keys ['coverage_status','draft_zoning_floor_area_cap_sq_ft','rule_evaluation','verified'] and the forged value 987654321 absent from the response; G5 found every echo-planting body rejected at depths 3-7.",
   "Route posture mirrors the accepted M5-T003 endpoint exactly and introduces no new public surface: reachable only when INTERNAL_SCENARIO_ENABLED is an explicit true token, else a generic 404 byte-identical to an unmounted path in body and headers (no X-Correlation-ID on that path); all four routes include_in_schema=False, and G4 verified the OpenAPI document is byte-identical (same SHA-256) with the flag absent, empty, an unknown token and true - 2 paths, no toolkit path in any state. STATUS_STATE_MATRIX is programmatically set-equal to the accepted scenario route's matrix (B-A == []) and byte-identical across the rework, so zero new (status, state) pairs were introduced. services/api/app/config.py is byte-unchanged: the existing internal_scenario_enabled flag helper is REUSED and no new flag exists.",
   "No independent legal calculation and no engine maths live in the route: it rebuilds, validates, adapts the request into the engine's documented arguments, calls the engine READ-ONLY and returns its typed result verbatim in a thin envelope. The canonical cap is transported VERBATIM from the server-rebuilt scenario document; NOT_VERIFIED_DISCLAIMER is present and nothing server-authored is marked Verified. Engine access is via the public app.scenario facade, confirmed by object identity (G1 verified all five identities still hold after the rework, the guard lambda deferring without wrapping), and git diff c83206c2..12fda82f -- services/api/app/scenario/ is EMPTY.",
   "GATE-FAIL -> REWORK -> RE-GATE arc, recorded in full as evidence that the acceptance scenarios were enforced rather than assumed. At aafc75fd the wave returned G3/G4/DCV PASS but G1 FAIL and G5 FAIL (reports M5-T012-G1.md and M5-T012-G5.md retained). Three BLOCKING defects were fixed in one pass at 12fda82f: (1) G5 BLOCKING-1 - an unpaired surrogate such as {'variable':'\\ud800'}, 22 ASCII bytes and under every documented cap, raised an unhandled UnicodeEncodeError on all four endpoints yielding an untyped text/plain 500 with no correlation id, an off-matrix pair and a logged traceback, because _finish validated with json.dumps(allow_nan=False) (ensure_ascii=True, which escapes surrogates and never raises) while Starlette renders with ensure_ascii=False + .encode('utf-8') (which does) - the validator and the renderer disagreed about what is encodable, so the strict-JSON-safe-before-send assurance was false; fixed by _string_boundary_error in the existing iterative walk plus aligning _finish's encoder. (2) G1 BLOCKING-1 / G5 MEDIUM-1 / DCV - FORBIDDEN_FACT_KEYS was top-level only. (3) G1 HIGH-1 / G5 HIGH-1 - the engine call and _finish sat outside any exception guard while the trusted rebuild stage was guarded, which is what let (1) escape untyped. All five reviewers re-attested PASS at 12fda82f, each reproducing the fixes from the code rather than from the producer's report; G5 additionally neutralized each fix in memory via a read-only pytest plugin and measured 33 / 26 / 5 test failures respectively, proving the new tests are load-bearing.",
   "Non-blocking residuals recorded to backlog by explicit reviewer judgement, none of them an unmet acceptance scenario: full request-body buffering before the 64 KiB cap (needs middleware or streaming, which would bust allowed_paths and the additive-main.py constraint); caller-influenced output size and CPU (G5 MEDIUM-3); the 405-vs-404 wrong-method existence oracle and a ~1.6x flag-off timing delta, both verified byte-for-byte identical on the accepted M5-T003 and M4-T005 routes and therefore inherited; unbounded raw-BBL reflection in the 422 body (inherited from bbl.py); unenforced Content-Type; G1's case-variant verification-key residual and G3's R-1..R-5 test-specificity gaps (the never-Verified invariant should be narrowed to server-authored content, since a caller can legitimately name an assumption set 'verified'); a new non-blocking modularity review_signal (scenario_analysis.py 572 -> 666 SLOC, crossing WARN_SLOC 600, still failures 0 / EXIT 0 and well under JUSTIFY_SLOC 750) which G1, G3 and G5 each independently ruled should NOT be split now, G5 noting that extracting the validator away from _finish would reopen the exact defect class it found; and two stale wording lines in the producer report. The pre-existing branch-wide ruff failure (27 errors in already-accepted M5 files under forbidden_paths, count unmoved by this task, the three files this task owns all clean) is not this task's and is carried as a separate follow-up."
  ],
  "D-038-R004": [
   "The whole deliverable is OFFLINE and requires NONE of the deferred owner-only credentials: no Supabase (B-001), no NYC Geoclient (B-004), no network, no persistence, no file or subprocess I/O. The four endpoints consume the property profile, rule_evaluation and scenario document rebuilt over the SAME injected dependency seams the accepted routes use (get_pluto_fetcher, get_spatial_substrate_provider), which tests override with recorded-official PLUTO fixtures under services/api/tests/fixtures/pluto and faithful M2-T013 substrate dicts.",
   "Proven by execution, not assertion. AS-7 asserts the offline guarantee at the EGRESS seams rather than by blocking socket construction: landmines on app.connectors.pluto_soda._OPENER, app.resilience.transport.DEFAULT_OPENER, http.client.HTTPConnection.connect, http.client.HTTPSConnection.connect and socket.create_connection, each RECORDING the attempt as well as raising, with the load-bearing assertion egress == []. The recording is essential rather than belt-and-braces: the route converts a raise into a typed 500, so a raise-only landmine would be swallowed and a naive test would still pass. The egress test is parametrized over all four endpoints (G5 LOW-4 closed).",
   "G5 independently verified the four egress seams are COMPLETE, not merely plausible: all application egress goes through urllib to DEFAULT_OPENER resolved at call time, and there is no requests / httpx / urllib3 / psycopg / subprocess / raw-socket use anywhere in services/api/app. DCV independently re-armed its own loopback-only egress block at 12fda82f: the new test file ran 171 passed with egress [] and the whole api suite ran 312 passed with egress [], and its RED control confirmed the assertion is load-bearing (with the seams cleared, a real HTTPSConnection.connect to data.cityofnewyork.us appears and is caught).",
   "No credential is read and none can leak: the test deletes the Socrata app-token environment variable and asserts it is absent, and asserts no x-app-token header reaches any outbound request. G5 confirmed no body content and no credential appears in any log line, and that a client-supplied X-Correlation-ID is ignored. G1 injected a secret-shaped string (a filesystem path plus a token) into a forced engine failure and confirmed no leak in the typed 500 response.",
   "No new configuration or credential surface was created: services/api/app/config.py is byte-unchanged and the existing INTERNAL_SCENARIO_ENABLED flag is reused, so the deliverable adds no environment variable, no secret, and no deployment requirement. The flag is not declared in render.yaml or any workflow, so the endpoints are unreachable in every current environment. G5's no-auth containment finding is carried forward explicitly: post-rework the flag is adequate INTERIM containment since the reachable unhandled-exception path is gone, but these endpoints still have no rate limiting, no size limiting and no per-caller attribution, so when authentication lands (M0-T007 / M0-T008, themselves blocked on the deferred Supabase credential) they require limits and not only an identity check.",
   "python tools/validate_directive_compliance.py --check -> EXIT 0 at 12fda82f (run to completion by both the orchestrator and the DCV). Directive-file integrity re-verified by the DCV at all three commits c83206c2 / aafc75fd / 12fda82f: the CRLF on-disk byte digest of requirements.json equals the manifest's recorded requirements_content_digest_sha256 32f8d252, the source digest a237dd50 is unchanged, locked_requirement_ids is [R001..R007] matching requirement_count 7 with no id added, removed or renumbered, and the audit-log note's 37035a7d -> 32f8d252 resync claim was verified against the actual bytes at both ends."
  ]
 }
}
