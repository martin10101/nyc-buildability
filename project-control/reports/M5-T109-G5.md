# M5-T109 — G5 security review (security-reviewer "sec-t109", read-only)

> Transmission history: pinned at 3cd8bdb0, delivered as three SendMessage parts (1/3..3/3) ending with END-OF-REPORT;
> no truncation. Joined verbatim by the orchestrator (transport wrapper tags removed only). Verdict PASS: every mandatory
> criterion holds (filename safety, one reconciled redacted refusal, id-only logs, caps before writers, job safety,
> exposure, DB-075 (a), zero new dependencies). Advisories A1-A4 are routed at the accept seam (PKT-H items).

---

M5-T109 G5 SECURITY REVIEW (read-only) — part 1/3

PIN: HEAD = 3cd8bdb0a98ff33171443badaca99ce15fc13705 at start (matches expected 3cd8bdb0).
Scope: services/api/app/cad/export_service.py, app/api/v1/export_api.py, the DB-075(a) fix in app/cad/pdf_sheet_writer.py, the [ORCH-CORRECTED] tests/cad/test_glb_writer.py AS-5, packet M5-T109.json. I also read the trusted seams: claim_words.py, proposal_validation.py (MAX_BODY_BYTES/_bounded_message/bounded-streaming), dxf_writer/glb_writer error codes, config.py flag, main.py.

Evidence executed (cwd services/api):
- `python -m ruff check .` -> All checks passed.
- `python -m pytest tests/cad -q` -> 429 passed in 7.57s (includes test_export_service.py 36, test_export_api.py 20, test_pdf_sheet_writer.py incl. the DB-075 __float__ test, and the READ-ONLY test_cad_owner_samples.py golden + owner samples — all green, so byte-identity holds).
- Static hostile analysis of every header/refusal/log/cap/job/exposure path. My intended in-memory TestClient probes were blocked by the session's shell-write guard; the producer's own tests assert the identical hostile scenarios and I ran them green, which together with the static read is conclusive. No BLOCKING finding depends on the un-run live probe.

BLOCKING findings: NONE.

Verdict preview: PASS with 4 ADVISORY (low/informational) notes. Mandatory criteria (filename safety, one reconciled redacted refusal, caps before writers, job safety, exposure, DB-075(a), zero new deps) all PASS. Detail in parts 2 and 3.

(continued)

---

M5-T109 G5 — part 2/3: MANDATORY CRITERIA (all PASS, file:line)

1. FILENAME SAFETY (PASS). build_filename_token (export_service.py:190-202) keeps ONLY [A-Za-z0-9._-] from bbl+generated_at (quote, ';', CR/LF, NUL, U+2028/2029, '%', '/' all DROPPED not escaped), caps at 80, returns None when it reduces to nothing/only ./-/_. content_disposition (:205-214) builds site-plan-<token>.<ext> from the allowlisted token + a fixed ext, so the ASCII filename carries no quote/';'/CRLF; filename*=UTF-8'' is urllib percent-encoded. token = builder OR fallback (:480), and the route's fallback is the uuid4 correlation_id (export_api.py:282) — no caller byte reaches any header. Proven by test_hostile_caller_value_cannot_break_the_header (test_export_service.py:216-236, incl. '1"; DROP TABLE' and "1-0045; filename=evil"), test_token_is_the_allowlist_only (:232-236, input '1"; a\r\nbé'), test_token_length_is_capped (:239-241), and the MANDATORY red/green test_filename_token_mutation_reddens (:261-271). RFC 5987 filename* form present (:254-258).

2. ONE RECONCILED, REDACTED REFUSAL (PASS). _reconcile (export_service.py:277-283) DISCARDS the writer's own message (which interpolates caller name/coord) and emits a server-built detail = "the <fmt> export was refused by its writer (reject_code=<code>)". DXF .code is a fixed literal (dxf_writer.py:231-247); GLB enforces code in REFUSAL_CODES (glb_writer.py:208). Service refusals name only field + canonical claim word (contains_claim_word returns the fixed CLAIM_CLASS_WORDS entry, claim_words.py:76-93) + integer counts — never caller text. Route _refusal_response (export_api.py:172-185) bounds detail via _bounded_message (proposal_validation.py:120-129). Proven: test_claim_word...(:144-150) asserts "APPROVED" present, "HOSTILE_MARKER" absent; test_reconciled_refusal_never_echoes_a_hostile_coordinate (:177-184).

3. LOGS (PASS). Every log line carries only a server uuid correlation id + fixed tokens (export_api.py:234,240,247,288,292,299); the 500 handler logs NO exc/str (:291-296). Proven: a writer raising RuntimeError("SECRET_INTERNAL_STRING") -> "SECRET_INTERNAL_STRING" not in response (test_export_api.py:228-233).

4. CAPS BEFORE WRITERS (PASS). build_export order (export_service.py:440-476): format->source->text screen(type/len<=120/claim)-> RAW ring len(:245-256, len() before any normalization, DB-057 d)-> floors<=2000(:259-270)-> base_elevation finite -> THEN dispatch. GLB _coerce_footprint/_roof_height only ever see already-capped input. Proven + mutations: ring_cap/floor_cap/text_too_long/claim tests (test_export_service.py:85-160).

(continued)

---

M5-T109 G5 — part 3/3: remaining mandatory + ADVISORY + VERDICT

5. JOB SAFETY (PASS). Writer runs off-loop via run_in_threadpool under asyncio.wait_for(timeout=15s) (export_api.py:279-286); the 200 is built only after result returns, so a timeout -> typed 503, never a partial file (test_export_api.py:210-220). Rate-limit KEY = request.client.host only (export_api.py:146-148) — X-Forwarded-For is never read (confirmed: no request.headers use for keying), so the key is NOT caller-spoofable. Limiter memory is hard-bounded: <=8192 LRU-evicted keys x <=30 timestamps/key (export_api.py:103-138). 429 proven (:199-205). Body read via accepted bounded-streaming with a 256 KiB pre-parse ceiling (proposal_validation.py:203-222).

6. EXPOSURE (PASS). main.py imports/mounts NEITHER the service nor the route (12 routers, none is export). Route is include_in_schema=False (export_api.py:221); flag guard is first, before any correlation id/body read (:227-228); flag-off returns the generic _not_found() with NO correlation header (:155-158), byte-identical to FastAPI's default. Flag is fail-safe default-off (config.py:41-49). The corrected AS-5 test HAS TEETH: asserts glb_writer importers == exactly ["app/cad/export_service.py"] and export_service/export_api absent from main.py (test_glb_writer.py:584-594) — fails if main.py mounts it or if any other module imports the writer. Proven: OpenAPI-absent + flag-off-404 (test_export_api.py:89-102).

7. DB-075(a) (PASS). _coerce_vertex broadens the float() catch to (ValueError, TypeError) -> typed non_numeric_coordinate refusal (pdf_sheet_writer.py:347-360); a numbers.Real whose __float__ raises now yields a refusal not an escape (test_export_service.py:345-351). Golden + owner samples unchanged (test_cad_owner_samples.py green in the 429 run).

8. ZERO NEW DEPENDENCIES (PASS). export_service imports stdlib (math, urllib.parse, collections.abc, dataclasses) + app.cad only; export_api adds only fastapi/starlette (already admitted) + app internals.

ADVISORY (non-blocking):
- A1 (low, defense-in-depth): content_disposition trusts its token arg; build_export uses `builder() or fallback_token` (export_service.py:480) without re-allowlisting the fallback. Unreachable today (route fallback = uuid correlation id; default "export"), but re-asserting the allowlist at that seam would enforce header safety at the choke point rather than trusting every caller's fallback.
- A2 (low): wait_for cancels the AWAIT, not the threadpool thread — an over-deadline build keeps running to completion (bounded/pure, so finite; per-caller rate-limited). Revisit thread-budget under load at the PKT-H mount.
- A3 (info): rate key = client.host is correct/non-spoofable, but behind Render's proxy it collapses to one bucket; PKT-H (authn/tenancy) must supply trusted principal identity before relying on it for fairness. Correctly deferred by the packet.
- A4 (info): the reused 256 KiB body ceiling is tighter than the DXF/GLB 10_000-vertex ring cap (~273 KiB), so those large rings 413 before the ring cap — fail-closed, not a defect.

M5-T109 G5 VERDICT: PASS

END-OF-REPORT
