# M5-T039 — G4 QA review return (VERBATIM; qa-engineer, read-only, pinned 1564e509)

Orchestrator note: returned via the agent-return channel 2026-09-18T09:16:21Z; the transport
truncated the first delivery inside the source-anchoring section. Part 1 below is the
truncated delivery verbatim; Part 2 is the resent remainder, verbatim. VERDICT: PASS.
(The reviewer's opening line notes its agent-memory write was blocked by the read-only guard
— expected for named reviewer spawns; no repository write occurred.)

---

## Part 1 (first delivery, verbatim)

The memory write is blocked by my read-only reviewer guard, so I'll skip it and deliver the verdict directly. My review is complete.

---

# G4 QA Review — M5-T039 (DB-010 named-street override matcher + phase-0 snapshot repair)

## VERDICT: PASS

Reviewer: qa-engineer (read-only). Material commit reviewed: `1564e509`. Live HEAD: `44649797`. All four documented commands independently reproduced green in the primary checkout.

## Byte-stability of the reviewed material (verified)
The diff `1564e509..44649797` touches only control-plane files (`M5-T039-evidence-map.json`, `M5-T039.json` reports/tasks, `state.json`). `git diff 1564e509 44649797 -- services/ docs/research/` is **empty** — the material surface is byte-stable between the pinned commit and live HEAD, so gates recorded against `1564e509` remain valid at HEAD.

## Independent reproduction (my own runs, not restated from the report)
- `python -m pytest tests/rules/test_named_street_override.py -q` (services/api cwd) → **40 passed** in 0.27s.
- `python -m ruff check .` (services/api cwd, the CI step-1 full-tree run) → **All checks passed!**
- `python tools/modularity_check.py --check` (repo root) → **failures 0**; `named_street_override.py` is **not** among the 19 pre-existing warnings.
- `content_digest_sha256` recomputed with hashlib = `23a9ccad…` = stored value (**self-consistent**); both block quotes are substrings of `verbatim_excerpt`.
- Two snapshot copies **byte-identical** (file sha256 `334f8ed1…` for both canonical + packaged).

## Binding rulings — all satisfied
1. **Phase-0 source is the accepted M4-T018 capture.** I compared the snapshot's `verbatim_excerpt` against M4-T018 §1.3 (both wide-street paragraphs) and §1.2 (`"A 'narrow street' is any street less than 75 feet wide."`) — transcription is **byte-exact**, no paraphrase. Provenance chain records the capture task, channel sha `4a75e22f…`, and the 504 print/PDF fallback disclosure.
2. **The three G6 questions resolve only as conservative refusals** (confirmed at runtime): located named row → INDETERMINATE + `G6-Q1`; boundary share → INDETERMINATE + `G6-Q3`; C5-3/C6-4/C6-6 → `professional_review_required` + `G6-Q2`. None of the three questions is decided by code.
3. **Zero-consumer leaf by design** — confirmed: no module imports `app.rules.named_street_override` outside its own test. (The many `named_street_override_*` grep hits in `wide_street_wiring.py` / `wide_street_buffer_engine.py` / `wide_street_live_provider.py` are pre-existing *precondition attribute names* in the parallel M5-T037 lane, not imports of this module — disjointness holds.)

## Acceptance-scenario adequacy
- **AS-1** — both rows parametrized; located, correct `provision_id` (`zr-12-10-named-street-wide`), `matched_row_verbatim` == block quote, `G6-Q1` recorded. Status is INDETERMINATE per the park-qualifier refusal (correct); MATCHED_OVERRIDE reachability proven separately (see below). PASS.
- **AS-2** — exact designated pair located order-independently; single shared boundary → INDETERMINATE with `G6-Q3`; disjoint → NOT_MATCHED with no provenance. PASS.
- **AS-3** — unknown street / wrong CD / wrong borough → NOT_MATCHED; blank borough, blank street, bad CD, missing bound, degenerate → INDETERMINATE; abbreviation ("Allen St") → NOT_MATCHED (no fuzzy expansion). No path raises from `.match()`; no false match. PASS.
- **AS-4** — provenance carries `snapshot_sha256` + section anchor + `section_number=="12-10"` on located, boundary, and section-level (blank-borough) results; NOT_MATCHED carries none. PASS.
- **AS-5** — ruff clean, 40 green, modularity clean, material commit touched only the 5 allowed paths. PASS.
- **AS-6** — CI-on-pushed-head is the orchestrator's capture (thin-client; not verifiable locally). Deferred, as designed.

## The new source-anchoring layer — verified genuine, not vacuous
- **Independence is real.** `test_structured_row_fields_trace_to_source_quote` normalizes with a local `_norm` (strip/collapse/casefold) pl[TRANSPORT TRUNCATION POINT]

---

## Part 2 (resent remainder, verbatim; delivered 09:17:01Z)

Resending from the truncation point:

---

...normalizes with a local `_norm` (strip/collapse/casefold) plus `removesuffix(" street")` — deliberately NOT the module's `_collapse`/`_anchored_in` — and additionally asserts an **order-faithful reconstruction** (`"{street} between {from-core} and {to-core} streets"`) is a literal substring of the source. That reconstruction is what actually pins the from/to decomposition to the pinned text (source "…Broadway between West 94th and West 97th Streets…" → `frontage_from`/`frontage_to`), catching a swapped or mis-paired endpoint that pure token-presence would miss.
- **All three fail-closed tests reach the guard for the intended reason** (I traced each through `__init__`): divergent borough ("Brooklyn" vs source "Manhattan") — the quote-substring check and `_build_rows` both pass, so only `_validate_against_source` raises, and it raises specifically on the `borough` field; out-of-source alt district ("C9-9" absent from the alt block's quote) — raises in the alt-district loop; unrecognized disposition ("auto_match") — raises on the disposition-vocabulary check before touching any row. None short-circuit on an earlier check, so each genuinely exercises the new guard.
- **MATCHED_OVERRIDE is proven reachable** via `test_matched_override_for_unconditional_row`, which builds a source-shaped synthetic quote so every row field anchors while `disposition_when_located == "matched_override"`, then asserts the located result is MATCHED_OVERRIDE with `matched_row_verbatim` == the quote. Without this the success status would be dead code, since both real rows are conditional refusals (INDETERMINATE).
- Anti-tautology holds throughout: digests are recomputed with hashlib (never restated as literals), the real snapshot is loaded through the production `load_snapshot_file`/`SnapshotStore`, and the deployable bundle is exercised via `load_default_matcher()` plus the byte-equality guard.
- **Isolation / order-independence is safe.** The module-scoped `snapshot` and `matcher` fixtures are shared read-only: the matcher is immutable after construction (frozen dataclasses, tuple of rows; `.match`/`.classify_alternate_width_district` are pure with no mutation), so no test can perturb another. Every negative/integrity test builds its own `tmp_path` snapshot, so there is no shared mutable state and no ordering dependence.

## Non-blocking observations (no action required for this gate)
1. `_anchored_in` is a loose per-field **substring** check, so on its own it cannot detect a swapped `frontage_from`/`frontage_to` (both endpoints remain substrings). This is not a defect: `.match()` compares a **set** of the two bounds, so a swap is functionally irrelevant, and the order-faithful reconstruction test catches swaps for the real rows anyway. The producer report §3.2/§11 discloses the limitation and flags `_STREET_TYPE_WORDS` as a conscious fail-closed extension point for future street types (Place/Road/Boulevard).
2. `test_as3_never_raises_and_never_false_matches` only asserts `is not MATCHED_OVERRIDE` and relies on pytest treating an unexpected exception as a failure rather than an explicit no-raise assertion; the false-NOT_MATCHED / false-INDETERMINATE risk is fully covered by the specific wrong-borough / wrong-CD / unknown-street tests, so coverage is intact.
3. Two defensive `_normalize_cd` branches are untested — a value with multiple integer runs (e.g. "CD 7 and 8" → None → INDETERMINATE) and a bool CD → None. Low concrete risk (callers pass an int or a single-integer string), and both fail closed to INDETERMINATE.
4. Boundary/disjoint AS-2 cases exercise Broadway only, not Allen Street; the logic is a symmetric set-intersection, so the risk of the Allen path diverging is low.

None of the observations rise to a defect. The implementation is a well-bounded, fail-closed, zero-consumer leaf faithful to the pinned source, and the 40-test suite is substantive with real anti-tautology and fail-closed discipline.[SECOND TRUNCATION POINT — closing lines requested]

---

## Part 3 (final tail — reviewer confirmation, verbatim)

END — nothing followed. That was the end of the report; "…substantive with real anti-tautology and fail-closed discipline." was the last line of the observations section, immediately followed by the closing "**VERDICT: PASS.**" which I sent in the same resend. Nothing came after it.

**VERDICT: PASS.** (Report COMPLETE.)
