# M5-T052 G4 — independent QA review (qa-engineer, saved VERBATIM from the agent returns)

M5-T052 G4 QA gate — VERDICT: PASS (2 ADVISORY, 0 BLOCKING). HEAD pinned = 6e3e1a400450091dcdf0c7f3429e9dee1fdd5e6e (confirmed).

EXECUTED COUNTS (services/api cwd; modularity from repo root) — all match expected:
- ruff check . → All checks passed!
- pytest tests/api/test_condo_records_api.py → 15 passed
- pytest tests/api → 454 passed
- pytest tests/rules → 697 passed
- tools/modularity_check.py --check → failures 0, warnings 20 (exit 0); all 20 warnings are tools/agent_supervisor/* — none are this task's files (condo_records.py, condo-records.ts, PropertyOverview.tsx, ReportView.tsx absent from the list).

BYTE-IDENTITY (item 5): `git diff a6f35a0f 6e3e1a40` over all 10 allowed_paths + report-view.test.tsx = EMPTY. The only commit between the frozen head a6f35a0f and pinned HEAD is 6e3e1a40 (submit-record: source-sections/ci-evidence/evidence-map — control-plane, non-material). So CI run 35451055372 (green at a6f35a0f) validly covers the reviewed content.

AS-BY-AS BINDING (each names a test that FAILS on revert):
- AS-1 (one derived state + token pin BOTH sides): deriveCondoSurface (PropertyOverview.tsx:197) is the single source; screen (:283) and brief (ReportView.tsx:46) both call it + render the same CondoRecordsChannelSection. Token pin: api test_condo_records_api.py:540-549 pins the 5 OUTCOME_* literals; web condo-records.test.ts:135-143 pins the identical set. Reverting either literal → FAIL. Ruling documented in contract caed4644 BEFORE code 365f492f (git log verified).
- AS-2 (multi-lot primary, records UNDER unchanged fail-safe, no allowance value): api test_multi_lot_records_every_base_lot_no_substitution (test_condo_records_api.py:309); web display test PropertyOverview:491 + brief:359. Structural order DevelopmentLimits-then-CondoRecordsChannelSection (PropertyOverview.tsx:291/311; ReportView.tsx:91/97). Digit-free pin condo-resolution-display.test.tsx:377,511; api `"far" not in body` :339.
- AS-3 (single = allow path, substrate record, DB-029c key-absence): api :264 + :438 + :494 (condo_key=None tolerated); web :380,:514. Substitution names entered vs analyzed, distinct (:296-297,:459-462).
- AS-4 (unit-BBL route): api :438/:467/:511/:523 + billing/unit landmines proving the correct seam is used per lot class. Strong.
- AS-5 (absence/error honesty, no fabricated records): api :342/:365/:385/:494; web outage/unresolved/error → honest absence (display :390-402,:530-556).
- AS-6 (heading semantics HJ-A3 + D-073-R006 grep + guard byte-unchanged): display :363-368 (RECORD heading, not.toMatch /analys|zoning result|computed result/ AND not.toMatch /allowance/i), :386,:417; api no-FAR :306/:339. Guard semantics preserved — profile-only fail-safe tests (route-absent stub) still withhold on unresolved/missing/unknown/malformed tokens (display :564-624).
- AS-7 (compat): AnalysisIdentityNotice.tsx byte-UNCHANGED (git diff since claim seam = empty; NOT in material commit 365f492f file list). deriveCondoDisplay/condoWithholdsAllowances semantics unchanged (test-proven). main.py = import + one include_router + comment, no existing route touched (diff reviewed). Removed testid `condo-resolution-multilot` and export `CondoResolutionRecords`/`conflictValueRecordedZoning` fully orphan-free (git grep: the only CondoResolutionRecords hit is a code comment; conflictValueDerivation/ConflictValue hits are the unrelated general contract.ts helpers).
- AS-8 (proof): reproduced above.

ITEM 4 (rewritten consumer suite 46bf1105): git diff 365f492f..46bf1105 shows every change is deliberate per the ruling. Nothing lost without replacement — the OLD suite's positive "not a computed development allowance" assertion inside the records section is INTENTIONALLY dropped (that allowance-class vocabulary is the D-073-R006 target; now negatively asserted in condo-resolution-display.test.tsx). Old single-base-lot=records case → new single=allow-path/substitution (ruling change). Old missing/unknown-token=records-present → new honest-absence (records only from a channel-confirmed multi-lot). Binding: tests 1 (single→substitution appears, channel-only) and 4 (multi-lot→records+withheld allowance, channel-only) FAIL if ReportView stops reading useCondoRecords/deriveCondoSurface. Tests 2/3/5 bind the profile guard (route-absent stub) — weaker on the channel but the profile fail-safe they assert is preserved.

FINDINGS:
1. ADVISORY (test-coverage) — apps/web/src/lib/__tests__/condo-records.test.ts. The web client's `retrievedAt` round-trip is NOT value-pinned at either level (top-level provenance.retrievedAt condo-records.ts:294; per-query queries[].retrievedAt :267). Only datasetVersion + per-query rowsUpdatedAt are pinned to the colon-containing value (test :164/:166/:188). Since all three sites share the boundedTimestamp helper, a WHOLESALE revert to boundedToken is caught; but a SELECTIVE revert of only the retrievedAt sites would escape the web suite. This is exactly the 2/1151 regression class the a6f35a0f fix addressed. Current behavior is correct and CI is green — narrow residual mutation gap only. Suggest adding `expect(view.provenance.retrievedAt).toBe(RETRIEVED_AT)` + one query-level assertion.
2. ADVISORY (report accuracy) — project-control/reports/M5-T052-producer-report.md §6 states "14 passed" (route) and "453 passed" (api); the frozen submission index, CI evidence, and my reproduction all agree at 15/454. Stale-by-one self-report; the authoritative counts match expected. Non-material.

BASIS: All five permitted suites reproduce green at the pinned head with counts matching the contract's expected values; the reviewed material is byte-identical to the CI-green frozen head; every AS-1..AS-8 is bound to at least one test that fails on revert (with strong red/green binding for the load-bearing token pin, multi-lot records, substitution substrate-record, and unit-BBL increment); the guard-coherence ruling is a single derived state (deriveCondoSurface) read by both surfaces with monotonic withholding and a byte-unchanged accepted profile guard; the timestamp regression class is fixed and CI-bound (with one narrow residual coverage gap noted); and the consumer-suite rewrite loses nothing that should survive the ruling. No BLOCKING defect found.

## Delta-attestation (verbatim)

CARRIES. My G4 PASS carries to e9abeaaf15ce9cff2c6405c96da8bbe8e2e31ef0. Independently verified: git diff 6e3e1a40..e9abeaaf touches only 5 control-plane/report files (producer-report.md, source-sections.md, M5-T052.json report+task, state.json); all 10 allowed_paths + report-view.test.tsx are byte-identical (material diff empty, exit 0). My executed counts, AS-by-AS bindings, and byte-identity to the CI-green frozen head are unaffected. The producer-report §6 edit resolves my ADVISORY 2 (15/454). ADVISORY 1 (retrievedAt not value-pinned in condo-records.test.ts) is a test-coverage note untouched by a report edit — still non-blocking.
