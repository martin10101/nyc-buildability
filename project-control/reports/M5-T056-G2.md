# M5-T056 G2 — producer self-check record (recorded by the orchestrator; the CLI rejects the producer's own name)

**Task:** M5-T056 DB-036 condo-surface rider cluster. **Producer:** frontend-engineer
(loop-3 runs 04-05; the lane's audit chain was archive-repaired at relaunch — the fork
predated this packet; run 05 completed the unit at the circuit-breaker stop after 6
cycles). **Frozen head:** `db0cf0f8`; **material:** `5b7b4b30` (8 files; per-file git
blob digest bindings in the producer report §1-§7 + the orchestrator digest appendix).

## Self-check evidence

- Producer report §10 (correct-cwd runs) + preserved §11 wrong-cwd artifacts (honestly
  disclosed as invocation artifacts, the known rules-suite-cwd class).
- Orchestrator in-worktree at harvest: `python -m ruff check .` clean; `python -m pytest
  tests/api -q` **501 passed**; modularity exit 0.
- G4 independently re-ran: ruff clean; test_condo_records_api 19 passed; tests/api 501
  passed; modularity exit 0 (456 files, failures 0).
- CI on `bd408ab9` (byte-identical material): run **35494571792 SUCCESS** — web-e2e,
  web, api, modularity all green.

## Advisory routing (none blocking; recorded at this seam)

- **G5-1 (MEDIUM, pre-existing):** the 422 `message` field echoes caller input uncapped
  (root in app/connectors/bbl.py f-strings — a forbidden path for this packet); web
  consumer re-bounds at 600; route flag-gated. Routed to the discovery backlog as a
  PRECONDITION-grade fold-in for the next packet allowed to touch the condo error body
  or connectors/bbl.py.
- G3-1 (substitution allow-path entered fallback uses boundedBbl not validateBblInput),
  G3-2 (mixed-zoning divergent-notice over-inclusive — fail-safe direction), HJ-A1/A2
  (recorded-zoning branch dormant until zoning propagation; divergent notice suppressed
  live — the api docstring still says "permanent/stays visible", reword at the zoning
  packet), HJ-A4 (billing-class label redundancy): fold into the next condo-surface or
  zoning-propagation touch — discovery backlog.

**Verdict: PASS** — all six contracted riders delivered with the guard-coherence ruling
preserved; (a)/(d) honestly left for their bound packets; suites green in-worktree, by
G4 re-run, and in CI at the material identity.
