# M5-T048 G2 — producer self-check record (recorded by orchestrator)

The producer's self-check is the report's verification-pass section (project-control/reports/
M5-T048-producer-report.md): all six documented commands executed in-run with real outcomes
(ruff clean; 527 scenario; 439 api; typegen --check OK; 29 packages tests; modularity 0
failures), plus an independent in-run raw-byte schema-equality check (ci-evidence-verifier
unit). Honest limitations recorded: web behavior deferred to CI; AS-3 wording reconciliation
routed to the orchestrator rather than silently reinterpreted.

Orchestrator reproduction: all six suites re-run green at harvest AND after the tagged
correction (the exact CI contracts script at 0 failures); CI run 35439235554 all jobs green
at 149fab76 (M5-T048-ci-evidence.md).

The one review round lost: the contracts CI job failed 2/11 at the harvested head because
the packet's own SCOPE wording placed the two geometry fixtures under fixtures/invalid/.
Root cause was the schema-vs-validator layering fact; corrected as tagged 607aab6b
(semantic-fixture relocation + test updates + [ORCH-PACKET-FIX 2] + G0 re-record).

## [ORCH-SCOPE-DISPOSITION] AS-3 wording adjudication (Tier A, recorded here — the frozen
packet is deliberately not edited post-submit)

AS-3's literal clause ("the matching invalid fixtures fail schema validation") is
UNSATISFIABLE for two of its three named defects: ring closure and outline
non-self-intersection are geometry invariants provably inexpressible in JSON Schema (both
G1 and G4 verified this independently; the producer disclosed it first). The adjudicated
reading — which the delivered work implements — is the LAYERED gate: a defect the schema can
express (non-finite/negative height via exclusiveMinimum) fails schema validation under
fixtures/invalid/; a defect the schema cannot express fails the production contract gate
(validate_scenario_document -> proposal.py) with the fixture under
fixtures/semantically_invalid/, schema-validity itself asserted as part of the test. All
three named defect fixtures are refused by the production gate; the CI contracts job passes
at 0 failures. This disposition records the acceptance-criteria reading; no packet text
changed post-submit (frozen identity preserved). G1 advisory, G4 A2, and the producer
report's routed item are all closed by this record.

Verdict: PASS.
