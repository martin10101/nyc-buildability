# M5-T123 — G3 code review (code-reviewer "cr-t123", read-only)

> Transmission history: pinned at d1970b06 (material de92182d; CI run 36115519503 at d1970b06: api job green, web-e2e
> red on one unrelated Playwright focus test - the zero-delta rerun 36116139678 at b0eb29f7 was fully green),
> delivered as 3 SendMessage messages ending with END-OF-REPORT (2 report parts + a post-verdict CI acknowledgement); no truncation. Joined verbatim by the orchestrator
> (transport wrapper tags removed only). Verdict PASS; two ADVISORY items (A1 a wrong symbol
> name in a dxf_import_api.py comment; A2 the unrelated web-e2e red).

---

M5-T123 G3 code review (READ-ONLY). HEAD PIN at start: d1970b06089418d8c75af4c28650cf612aa8030f. Material: de92182d (cherry-pick of 8d226b2f), exactly the 9 allowed paths (--stat matches). Verdict at end of part 2.

SCOPE / MOUNT — clean.
- 9 files == allowed_paths; no forbidden path; app/main.py has NO reference to scene_api/export_api/dxf_import (routes stay UNMOUNTED). proposal_validation.py NOT in the commit; MAX_BODY_BYTES still 262144 (proposal_validation.py:76). Zero new deps (stdlib + already-admitted anyio).

CEILINGS (AS-1) — sound, enforced on both paths, 413 shape unchanged.
- scene_api.py: SCENE_MAX_BODY_BYTES = 2 MiB; enforced on the declared-Content-Length path (scene_api.py:316) AND the streamed path (:319); the 413 body is the same shape via _payload_too_large (:231) — only the byte number changed. Measured max 811,225 B is CREDIBLE: I verified the caps in proposal.py (MAX_TOTAL_OUTLINE_POSITIONS=20000:89, MAX_LEVELS=500:75, MAX_EXTERIOR_WALLS=4000:77, MAX_OUTLINE_VERTICES=1000:74) and the maximal-body test hits all of them (500 levels x38 +1000 footprint = 20000). Context buildings are SERVER-fetched (scene_api.py:371), not client-supplied, so the client body is dominated by the capped geometry. 2 MiB ~= 2.6x headroom.
- export_api.py: EXPORT_MAX_BODY_BYTES = 1 MiB; both paths (:257 declared, :263 streamed). Measured 452,336 B credible: two rings at _FORMAT_RING_CAP['dxf']=10000 (export_service.py:131) + MAX_FLOORS=2000 (:136), both confirmed real. ~2.3x headroom.
- dxf_import_api.py: DXF_IMPORT_MAX_BODY_BYTES = 20 MiB; _IMPORT_DXF_LIMITS.max_bytes RAISED with it (:129); both paths (:299/:304). Under the reader's 64 MiB hard clamp (_LIMIT_CEILINGS['max_bytes'], dxf_reader.py:190-191). DB-D4 (max_lines=2M binds first ~14 MiB on a vertex-dense DXF) is HONESTLY disclosed, not silently shipped; TOO_MANY_LINES is a real typed refusal (dxf_reader.py:171,579-580) — so the route's refusal for such a file is a typed, honest refusal. dxf_reader.py is read-only per the packet, so raising only max_bytes is correct. Memory arithmetic (~312 MiB worst case) declared and plausible.

SLOTS (AS-2) — correct.
- Sum 8+12+4 = 24 < 40, 16 headroom. The guard test imports the REAL constants from the three modules (not copies) and asserts total<pool, pool-total>=8, and dxf<=scene<=export. anyio citation is EXACT — I probed the installed anyio: version 4.10.0, current_default_thread_limiter builds CapacityLimiter(40), total_tokens==40 [OBSERVED].

PRINCIPAL (AS-3) — correct.
- caller_key uses isinstance(principal,str) and principal.strip() (rate_limit.py:225): whitespace-only -> host key; valid principals keyed on the RAW value unchanged (incl. " u " -> "principal: u "). X-Forwarded-For still never trusted (unchanged).

(part 2 follows)

---

M5-T123 G3 review (part 2/2).

TESTS (AS-4) — behaviour, not implementation.
- AST guard broadened to list/set/Counter + EMPTY-list literal (test_*_api.py: is_container adds ast.Set and empty ast.List; _LIMITER_CONTAINER_CALLS gains Counter/set/list). No false positives: non-empty list constants (__all__, _ROUTE_METHODS) and frozenset matrices (_TRUE_TOKENS, *_STATUS_STATE_MATRIX) are correctly NOT flagged — proven by test_ast_guard_also_catches_list_set_counter_limiters, and the existing test_route_uses_the_shared_limiter_and_slots_only still returns [] for all three real modules (passes in my run).
- Event handshake is deterministic: started.wait(5.0) replaces the 50 ms window (test_rate_limit.py:232, :334). The internal started flag is set inside _guarded BEFORE work() runs (rate_limit.py:291,319-322); the test event is set inside work(), so "test event set" implies "internal flag already True" — no wall-clock race. M7 mutation in the report confirms the post-start slot semantics still redden.

DEVIATIONS — both acceptable and disclosed.
- DXF boundary proven at a small monkeypatched ceiling (_SMALL_CEILING=4096) + a separate real-value arithmetic test asserting DXF_IMPORT_MAX_BODY_BYTES>=20MB decimal, ==_IMPORT_DXF_LIMITS.max_bytes, and <=reader hard clamp. Sound: enforcement is value-agnostic; a real readable 20 MiB DXF is impractical to synthesize.
- Scene/export "maximal legitimate request accepted" proven as "admitted PAST the 413 size gate" (status!=413) + the measurement bound; degenerate max-caps geometry then 422s. Honest — the ceiling's contract IS the size gate. NON-VACUOUS: the client fixture enables the route (test_scene_api.py:90-91,125-127), so a too-low ceiling would 413 (matches the report's M1 mutation).

EVIDENCE [OBSERVED] (local Python 3.11, services/api):
- `python -m ruff check .` -> All checks passed!
- documented pytest (4 files) -> 199 passed (matches the report's count).
- anyio probe -> 4.10.0, total_tokens==40, CapacityLimiter(40) in source.
- CI at d1970b06: the `api (ruff + pytest)` job PASSED under 3.12 (the authority for every M5-T123 file).

FINDINGS
- ADVISORY A1 (comment nit, dxf_import_api.py:119): the docstring says "under the reader's own 64 MiB hard clamp (dxf_reader._MAX_LIMITS)"; the real symbol is `_LIMIT_CEILINGS` (dxf_reader.py:190) — `_MAX_LIMITS` does not exist. Comment-only; the code and the test use the correct symbol, and the producer report is correct. No functional impact.
- ADVISORY A2 (informational, not this task): the CI `web-e2e` job is RED at d1970b06, but M5-T123 touches NO web files (forbidden_paths bars apps/, packages/) and the covering `api` job is GREEN under 3.12. The web-e2e red is a disjoint failure from other concurrent D-089 wave tasks — flag for orchestrator awareness, NOT blocking for M5-T123.

No BLOCKING findings. Schemas/caps verified against source (not guessed); no hard-coded legal values; enforcement fail-closes; DB-D4/DB-D5 discoveries disclosed honestly; tests assert behaviour and redden on the stated mutations.

M5-T123 G3 VERDICT: PASS

END-OF-REPORT

---

Ack — your CI note matches my A2 exactly (web-e2e a11y `<body>` focus false-match, empty apps/ diff, api job green). M5-T123 G3 PASS is unchanged. Watching the zero-delta rerun 36116139678 @ b0eb29f7 in the background; I'll report the web-e2e job result when it concludes. Not re-sending the report.
