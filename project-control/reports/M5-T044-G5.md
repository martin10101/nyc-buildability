# M5-T044 — G5 security review (verbatim reviewer return; transport entity-decoding only)

Saved by the orchestrator from the security-reviewer agent return, 2026-09-19 (seq-118 wave,
pinned at submission sha 20c597c1). Nothing added or removed beyond decoding transport
HTML entities.

---

Writing is disabled entirely (memory note skipped — it is optional). I have all the evidence needed. Here is my verbatim G5 gate report.

---

# G5 Security Gate Report — M5-T044

**Task:** M5-T044 — Condo resolver pre-wiring hardening (DB-029 a, d–i): ASCII-fullmatch guards + canonical-value URL interpolation + error-branch/unit-path test coverage
**Reviewed identity:** pinned submission sha `20c597c1`; material commit `1fd07e9a` on `candidate/D-024-mrl-option-b`. Verified working tree == material: `git diff 1fd07e9a -- <module> <test>` → empty (both allowed-path source files byte-identical to the frozen material). `git show --stat 1fd07e9a` → **exactly 3 files** (module, test, producer-report), +583/−37.
**Reviewer:** security-reviewer (read-only)
**Gate:** G5 (security)
**Verdict: PASS** — my prior **F1, F2, and F3 are all CLOSED**, independently reproduced. No reachable BLOCKING security defect. Three ADVISORY items ride to the wiring packet.

## Closure of my prior M5-T042 findings

**F1 — CLOSED (was Medium, MUST-FIX-BEFORE-WIRING).** Anchor-leaky guards and raw-input URL interpolation are both fixed.
- Guards are now `_STRICT_BBL_RE = re.compile(r"\d{10}", re.ASCII)` (`dtm_condo_soda.py:193`) and `_CONDO_KEY_RE = re.compile(r"\d{6}", re.ASCII)` (`:196`), matched with `.fullmatch` at all four sites: `classify_lot` (`:357`), `resolve_by_condo_key` input (`:785`), response-side base-BBL guard `_collect_base_lots` (`:514`), and response-side condo_key guard in the unit-expansion path (`:708`).
- URL construction interpolates the CANONICAL value, never raw input: `canonical_bbl = normalize_bbl(bbl).canonical` (`:619`) at billing (`:633`) and unit (`:674`); the two condo_key URL sites (`:717` expansion, `:795` direct) interpolate a value already fullmatch-guarded to exactly `[0-9]{6}`.
- Reproduced my own probes (see outputs below): raw regex `fullmatch` rejects `'3022647515\n'`, trailing space, leading space, and 10 Arabic-Indic digits for BBL, and the same classes for the 6-digit key; `classify_lot` rejects all four leaky BBL classes with code `non_numeric`; the accepted valid input still resolves and the emitted URL is exactly `…/p8u6-a6it.json?condo_billing_bbl=3022647515` (canonical, no leak).

**F2 — CLOSED (was Low).** The response-side `condo_base_bbl` guard (`_collect_base_lots:514`) now uses `re.ASCII` + `fullmatch`; a Unicode-digit, trailing-newline, trailing-space, or PLUTO-decimal `condo_base_bbl` in a response raises `SchemaDriftError` (reproduced via injected transport; `test_a_response_base_bbl_leaky_shape_rejected` at test:569 also covers it). The sibling response-side condo_key guard (`:708`) is new and correct: I drove a unit response carrying `condo_key="301313\n"` and confirmed it raises `SchemaDriftError` and the expansion URL is NEVER issued (the leaky value never reaches a URL).

**F3 — CLOSED (was Low, test-adequacy).** The negative-test class I flagged now exists and passes: `test_a_bbl_guard_rejects_anchor_leak_no_transport` (parametrized over trailing-newline / trailing-space / Arabic-Indic BBL, asserting `BBLValidationError` via the `_never_called` transport = zero transport calls), `test_a_condo_key_guard_rejects_anchor_leak_no_transport` (same three classes for condo_key), `test_a_url_carries_canonical_value`, and `test_a_unit_and_expansion_urls_carry_canonical_value`. Suite grew 26 → 54, all green.

## Judge-criteria results

1. **Guard rejection at all four sites — PASS.** Empirically reproduced (regex + `classify_lot` + injected-transport module drives + 54-test suite). All leaky classes rejected; zero transport calls on input rejection (guard runs before any I/O — `classify_lot` at `resolve:595` and the `:785` guard before `:795` fetch).
2. **URL always canonical — PASS.** All four f-string sites emit validated/canonical values; a leaky response condo_key is refused before interpolation. No raw-input path to a URL remains.
3. **New surfaces (input_kind / per-query stamps / documented codes) — PASS, no regression.** `input_kind` is a static vocabulary (`INPUT_KIND_BBL`/`INPUT_KIND_CONDO_KEY`), never derived from untrusted input — no injection surface. Per-query `retrieved_at` comes from `clock()`, RFC3339-formatted. Documented codes `non_numeric`/`invalid_component` are from the `bbl.py` vocabulary (`bbl.py:44-46`); the retired ad-hoc `wrong_shape` is gone. Token is header-only (`_build_headers:320-326`) and, reproduced, absent from the URL, from `provenance`/`notes`/`input_value`, and from the error `to_payload()`; `X-App-Token` still carries it. Error `detail`/`BBLValidationError` messages embed only `repr()`-bounded public identifiers (BBL/condo_key) and public URLs — no secret, no PII, no injection.
4. **Fail-closed integrity — PASS.** No transport/parse error becomes a resolved result: 400-drift → `SchemaDriftError`, 400-other → `SourceUnavailableError`, non-JSON/non-array/non-object → `SchemaDriftError`, 429/timeout/network → typed terminal (tests g_* green, `transport.calls >= 2` proves the retry budget was spent). Previously-valid canonical input `3022647515` resolves identically (`resolved`, base `["3022640032","3022640033"]`, path `billing`).
5. **DoS posture — unchanged/better.** Bounded retry (`max_attempts=3`), `timeout=10.0`, and the 10 MB body cap in the untouched `transport.py` still bound the response; single O(n) pass in `_collect_base_lots`. The deliberate `$limit` deferral (item j) is acceptable defense-in-depth: disclosed, not silently dropped, and already bounded by the body cap.
6. **Leaf / 3 files / no new dependency — PASS.** Grep for `dtm_condo_soda` across `services/**/*.py` returns only the module and its own test (zero production consumers). Material commit touches exactly the 3 allowed paths. Imports are stdlib (`json, logging, os, re, time, uuid, collections.abc, dataclasses, datetime, typing`) + existing `app.connectors.bbl` and `app.resilience.transport` — no third-party, no new dependency.

## Findings (all ADVISORY; none block G5)

1. **ADVISORY (Low, test-adequacy) — `dtm_condo_soda.py:708` response-side condo_key guard has no dedicated test.** The symmetric base-BBL guard is covered (test:569) but no test exercises a leaky condo_key arriving in a *unit response* pre-expansion. The guard is present and I verified it fires (SchemaDriftError, expansion URL suppressed), so this is coverage-only. Recommend adding a negative test mirroring `test_a_response_base_bbl_leaky_shape_rejected` for a unit-response condo_key. Route to the wiring packet.
2. **ADVISORY (Info) — `$limit` still deferred** (former F4). Fine for this leaf; bind as defense-in-depth on the wiring packet that first gives the module a live consumer.
3. **ADVISORY (Info) — `_ERROR_CODE_SAFE_RE` (`:201`) still uses `^…$` + `match`, not `fullmatch`.** Same anchor-leak class the packet otherwise eliminated, but on a source-controlled internal field (the SODA 400 `errorCode`) that is only stored in an error `detail` dict — never interpolated into a URL, never a secret/log-injection path. Cosmetic consistency nit; could adopt `fullmatch` for uniformity. Not part of F1/F2/F3.

## Non-security observation (for the G3/orchestrator record, not a G5 defect)
`dtm_condo_soda.py` is now 910 lines — above the 750 *justification* threshold, under the 1,000 hard threshold; `modularity_check --check` reports **failures 0** (warning tier), and the producer recorded a cohesion justification (producer report §"Cohesion / modularity note"). No responsibility mixing or dumping-ground concern from a security standpoint. Minor: producer report cites "warnings 22"; my run shows warnings 21 (trivial env drift from other uncommitted working-tree files; 880-pass and failures-0 both reproduce).

## Read-only commands run (with outputs)
- `git log --oneline -5`; `git rev-parse HEAD` (11bed5f7); `git show --stat 1fd07e9a` → 3 files, +583/−37; `git diff 1fd07e9a -- <module> <test>` → empty (frozen == working tree).
- Raw regex probe (`python - <<`): NEW `fullmatch` guards → all False for BBL/KEY trailing-newline, trailing-space, leading-space, Arabic-Indic; True for `'3022647515'`/`'301313'`; OLD `^\d{10}$` → True on newline and Arabic (contrast).
- Module drive (`python -`): `classify_lot` rejects newline/space/leading-space (`non_numeric`) and Arabic (BBLValidationError); billing URL emitted = `…/p8u6-a6it.json?condo_billing_bbl=3022647515`, token NOT in URL, `X-App-Token` carries token, token NOT in provenance/result/error payload; unit-response leaky `condo_key` → SchemaDriftError, expansion URL never issued (`urls=['…/eguu-7ie3.json?unit_bbl=3022642601']`).
- `python -m pytest tests/connectors/test_dtm_condo_soda.py -q` → **54 passed**.
- `python -m pytest tests/connectors -q` → **880 passed** (no regression).
- `python tools/modularity_check.py --check` → `selected 444 files; failures 0; warnings 21`; `dtm_condo_soda.py` at justification tier (warn), exit 0.
- `Grep dtm_condo_soda` over `services/**/*.py` → 2 files (module + its test) = leaf confirmed.

**VERDICT: PASS.** F1, F2, and F3 are CLOSED and independently reproduced. Security-critical invariants all hold: ASCII-fullmatch guards at input and response sides, canonical-only URL interpolation, header-only token secrecy, fail-closed no-fabrication, bounded DoS, leaf/no-consumer, stdlib+app-only deps, exactly 3 files. Three ADVISORY items (dedicated condo_key-response test, `$limit`, `_ERROR_CODE_SAFE_RE` fullmatch) are non-blocking and should be bound onto the wiring packet.

---

Note: the reviewer could not persist a continuity note (read-only session; agent-memory write skipped — optional). Relevant absolute paths: `C:\Users\MLFLL\Downloads\nyc-zoning\ctl24\services\api\app\connectors\dtm_condo_soda.py`, `C:\Users\MLFLL\Downloads\nyc-zoning\ctl24\services\api\tests\connectors\test_dtm_condo_soda.py`, `C:\Users\MLFLL\Downloads\nyc-zoning\ctl24\project-control\reports\M5-T044-producer-report.md`.
