# GATE REPORT — G4 (QA / test-adequacy) — M5-T003 (Scenario endpoint)

**Reviewer:** qa-engineer (independent, read-only). **Reviewed SHA:** `30d6e3b4`. **Result: PASS.**
(Verbatim reviewer return preserved below.)

---

**Task:** M5-T003 — `GET /api/v1/properties/{bbl}/scenario`. **Interpreter:** Python 3.11.9 (only resolvable; CI targets 3.12).

## Acceptance criteria (all 27 tests pass; each maps to an AS)
- **AS-1** verbatim cap: reads cap from the SIBLING rule-evaluation route over identical inputs and asserts `doc["draft_zoning_floor_area_cap_sq_ft"] == trace_cap == 15000.0`; `residential_far_cap` state=="draft", needs_review True, no `verified` coverage, disclaimer, 8 envelope families `missing`. Surfaced==trace, not a recompute. PASS.
- **AS-2** split-lot no_scenario: professional_review_required, cap/cap_label None, reasons contain "professional"+"review", share ranges preserved verbatim (R5 0.55–0.65, R6 0.35–0.45), exact substrate review reason propagated, `district.value is None` (no collapse). PASS.
- **AS-3** no-match 404 with x-correlation-id, state no_match, source_id, correlation_id, no scenario_kind/constraints (distinguishable from flag-off 404). PASS.
- **AS-4** (7 tests): parametrized malformed BBL → 422 typed detail.code + proven no-connector-call; timeout→504; unavailable→503; schema_drift→502; internal defect→500 internal_error; contract failure→500 internal_contract_error. Last two assert no partial body and no internals leak (hostile/secret-internal-path/Traceback/`File "` absent). PASS.
- **AS-5** (4 tests): `test_as5_matrix_equals_rule_evaluation_route_emitted_set` DRIVES the sibling route across all 9 branches and asserts emitted == STATUS_STATE_MATRIX (equality); scenario route own-set + every-200-validated; property-route constant cross-check accounting for the collapsed (500, unsupported_contract_version) pair. Verified in source; arithmetic holds. PASS.
- **AS-6** (3 tests): flag-off/unknown over [None,"","0","false","off","maybe","2","  "] all return exactly `{"detail":"Not Found"}`, no x-correlation-id, no scenario/flag leak; landmine seams prove seam I/O not invoked; OpenAPI never lists route while existing route stays. PASS.
- **AS-7** offline: TestClient + dependency_overrides + FakeTransport over committed fixtures; explicit test unsets SOCRATA_APP_TOKEN. No network/Supabase/Geoclient. PASS.

## Steps independently executed
`python3.11 -m pytest tests/api/test_scenario_api.py -q` → 27 passed in 2.29s; `-v` → 27 PASSED; all 4 referenced fixtures present. Read source to confirm assertions are non-tautological (monkeypatch targets are the exact module-level names the route calls; flag default False makes AS-6 404 real; landmine seams raise on invocation).

## Directive
In-regime (D-038:ALL). Full per-requirement directive verification is the directive-compliance-verifier's pass (producer ≠ verifier), recorded in the directive verification.json — outside this QA gate. Within test scope, **D-038-R004 (offline/no credentials)** is structurally satisfied by the AS-7 harness — proven, not narrated.

## Advisory (non-blocking, do not gate PASS)
1. AS-7 offline proven structurally (dependency overrides + FakeTransport), not by a hard socket block — same accepted pattern as rule-evaluation; low risk. Noted for the UI/integration follow-up.
2. AS-1 "verbatim" is exactly the specified assertion (surfaced == trace, pinned 15000.0); does not by itself discriminate "surfaced" from "coincidentally-equal recompute" — that is the builder's own unit test (M5-T001, forbidden path here). Adequate as written.
3. Scenario-kind coverage exercises preliminary + no_scenario 200 variants but not unsupported/not_applicable (builder-domain passthroughs, contract-validated; not in the AS list). Minor.

## Conclusion
**PASS.** Pack thoroughly covers AS-1..AS-7; assertions strong and non-vacuous; failure modes typed/enumerated; 200 bodies contract-validated; flag-off byte-indistinguishable with landmine-seam proof; cap cross-referenced against sibling trace with a pinned oracle; the matrix-equivalence test is meaningful (drives the actual sibling route, asserts set equality). Three advisory items are residual hardening notes, out of this endpoint task's scope. No defects. Matches captured evidence (27/27).
