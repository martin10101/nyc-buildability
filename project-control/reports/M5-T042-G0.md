# M5-T042 — G0 contract gate (orchestrator)

- **Packet:** DTM condo billing-BBL → base-lot-set resolver, MODULE ONLY (no pipeline wiring,
  no ArcGIS failover, no zoning logic). Sole source = the verified DB-002 research record
  `docs/research/condo-base-lot-resolution-sources.md` (raw official responses embedded;
  offline fixtures only, no network in tests). Cites D-066:R001. Depends on M5-T033
  (accepted — the honest-no-data lineage this capability resolves). D-073-R008 sequencing:
  the condo-to-land-lot connection is the named next live-case priority after the delivered
  street-calculation milestone.
- **Loop-lane disjointness (D-072-R003), pairwise across ALL live loop packets:** allowed_paths
  of M5-T042 ∩ M5-T040 = ∅ (checked mechanically this seam; recorded output
  "PAIRWISE DISJOINT vs M5-T040: True"). New leaf connector + its own test file + report;
  the live wiring lane owns none of them. M5-T041 is ACCEPTED (223rd) — loop-2 is being refed
  per D-072-R002.
- **Directive binding:** D-066-R001 applicability appended (same-commit digest resync +
  audit_log); `evaluate_task_refs` ok=True, applicable == cited.
- **Nav block (D-066-R001):** embedded in inputs; names the leaf status, the SODA-connector
  models to mirror (pluto_soda.py / ztldb_soda.py), the query.py instruction, and the
  advisory clause.
- **Placeholders:** ruff-clean module stub + one-passing-test suite + producer report seeded
  (c17 satisfied; python placeholder discipline per the M5-T039 precedent).
- **Scope sanity:** fail-closed contract (typed NOT_A_CONDO / UNRESOLVED results, never a
  fabricated lot), full base-lot SET always (multi-lot proven on the worked case), no
  zoning determination, divergent-zoning deferred to the qualified-human surface, PLUTO
  appbbl banned as a source — all written as binding packet text. Report-discipline block
  binding (the proven breaker cause is report churn).

**G0 verdict: PASS** — packet is contractable and claimable.
