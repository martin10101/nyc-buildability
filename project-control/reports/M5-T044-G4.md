# M5-T044 — G4 QA review (verbatim reviewer return; transport entity-decoding only)

Saved by the orchestrator from the qa-engineer agent return, 2026-09-19 (seq-118 wave,
pinned at submission sha 20c597c1). Nothing added or removed beyond decoding transport
HTML entities.

---

My independent G4 review is complete. Here is the verbatim gate report to record.

---

# G4 QA Gate Report — M5-T044 (Condo resolver pre-wiring hardening, DB-029 a, d–i)

**Reviewer:** qa-engineer (independent, read-only). **Frozen submission:** sha `20c597c1`; material `1fd07e9a`; combined pushed head `a380a956` on `candidate/D-024-mrl-option-b`. **Directive regime:** D-066-R001.

## VERDICT: PASS

The 54-test suite (26 → 54, +28) adequately covers AS-1..AS-8. Every DB-029 letter in scope (a, d–i) has ≥1 test; no letter has zero. The three M5-T042 G4 gaps (#2/#3/#4), the G5 F1/F2/F3 items, and G3 #1/#2/#3 are closed; the (j) $limit deferral is recorded, not silent. I reproduced 54 passed / 880 passed offline at the frozen tree and ran a mutation probe confirming the load-bearing negatives fail on the old leaky guard. Two ADVISORY findings (neither blocks); no BLOCKING defects.

## Content identity (independently verified)
- The 3 M5-T044 files are **byte-identical material→head**: `git diff --stat 1fd07e9a a380a956 -- <3 files>` empty; `git diff --stat 1fd07e9a a380a956 -- apps/ services/ packages/` empty (head adds only bookkeeping).
- **M5-T043 disjoint:** material `f6e88222` touches CalculationEvidence.tsx, report-view.test.tsx, named_street_override_status.py, wide_street_wiring.py, test_wide_street_wiring.py, test_wide_street_live_provider.py, its report — **zero overlap** with M5-T044's allowed_paths.
- Extracted module sha256 (LF-normalized) == git blob: `d2c289df…9e02`.
- **Leaf confirmed at `1fd07e9a`:** `git grep dtm_condo_soda` under `services/api/app/**` (minus the module) = empty; none under `apps/**`.

## Scenario adequacy table — letter → test map

| AS | DB-029 | Proven by (test:line) | Adequacy |
|---|---|---|---|
| AS-1 | (a) F1 | `test_a_bbl_guard_rejects_anchor_leak_no_transport` (:508, 3 params) + `test_a_condo_key_guard_rejects_anchor_leak_no_transport` (:520, 3 params), both via `_never_called` (catch of `BBLValidationError` also proves zero transport); `test_a_url_carries_canonical_value` (:525, exact billing URL); `test_a_unit_and_expansion_urls_carry_canonical_value` (:537, exact unit+expansion URLs); direct condo_key URL exactness held implicitly by `RoutedTransport` exact-key routing (`test_c6` :388, `test_f` :609) | STRONG |
| AS-2 | (a) F2 | `test_a_response_base_bbl_leaky_shape_rejected` (:569, params Unicode/newline/space base BBL → `SchemaDriftError`) | STRONG (base-BBL); see F-2 for the condo_key response guard |
| AS-3 | (d) G3#1 | `test_d_two_query_resolve_has_distinct_post_response_timestamps` (:576) — `_advancing_clock([12:00:00,12:00:05])` genuinely advances; asserts `stamps==[...00,...05]`, `stamps[0]!=stamps[1]`, result-level = final; a single/pre-request stamp yields equal stamps (fail) and a 3rd clock call raises StopIteration | STRONG |
| AS-4 | (e/f) G3#2/#3 | `test_e_classify_lot_uses_documented_validation_code` (:600, `code=="non_numeric"`, in `DOCUMENTED_BBL_CODES`, `!="wrong_shape"`); `test_f_*` (:609/:621/:634) resolved/unresolved condo_key carry `input_kind=condo_key`,`lot_class=None`; BBL carries `input_kind=bbl`. Docstrings+`__all__`+field rename `input_bbl→input_value` moved together; no `input_bbl` field ref remains in module or tests | STRONG |
| AS-5 | (g) G3#4/G4#2 | `_AlwaysTransport` forces: 400-drift→`SchemaDrift` (:645), 400-other→`SourceUnavailable` (:653); **3 JSON guards** non-JSON/non-array/non-object (:661/:667/:673); **3 terminals** 429→`RateLimited`+`calls>=2` (:679), timeout→`SourceTimeout` (:688), network→`SourceUnavailable` (:696) | STRONG |
| AS-6 | (h/i) G4#3/#4 | (h) `test_h_unit_empty_is_unresolved_single_query` (:705); `test_h_unit_without_condo_key_returns_direct_lot_no_expansion` (:715, no expansion query enforced by RoutedTransport, incompleteness note). (i) `test_i_unknown_columns_note_is_recorded` (:740); `test_i_multiple_condo_keys_and_numbers_notes` (:756, condo_key/number left `None`) | STRONG |
| AS-7 | leaf/counts | Reproduced: `pytest tests/connectors/test_dtm_condo_soda.py` → **54 passed**; `pytest tests/connectors` → **880 passed**; leaf verified (git grep). Modularity exit-0 (910 lines, justification tier) orchestrator-captured + CI-backed | ADEQUATE (54/880 re-run; modularity captured) |
| AS-8 | CI | `M5-T044-ci-evidence.md`: CI 35412920210 / secret-scan 35412920196 / context-budget 35412920224 all **success** at `a380a956`; material→head byte-identity (verified) means CI at head covers the material | ADEQUATE (orchestrator-captured; gh read-only) |

**Letter coverage:** a ✓ (11 cases), d ✓, e ✓, f ✓ (3), g ✓ (8), h ✓ (2), i ✓ (2) = **28 new**. b/c OUT of scope (wiring-era); (j) deferral **recorded** in producer report §items + §deferred (D-069) — not silent.

**M5-T042 gap closure:** G4 #2 (error taxonomy/400-drift/JSON guards/terminals) → CLOSED by (g); G4 #3 (unit-empty, unit-no-condo_key) → CLOSED by (h); G4 #4 (runtime unknown-columns + multiple-key notes) → CLOSED by (i). G5 F3 negatives → CLOSED by (a). G3 #2 wrong_shape → CLOSED (e). G3 #3 field overloading → CLOSED (f).

**Mutation probe (throwaway scratchpad copy; repo untouched):** reverted guards to old `^\d{10}$`/`^\d{6}$`+`.match` and `wrong_shape`. Result: **6 of the new negatives FAIL on the mutant** — `test_a_bbl_guard…[3022647515\n]`, `test_a_condo_key_guard…[301313\n]`, `…[Unicode condo_key]`, `test_a_response_base_bbl_leaky_shape_rejected[Unicode]` and `[…\n]`, `test_e…`. Confirms the fix is genuinely proven. (4 params pass on the mutant — the 3 trailing-space cases, already rejected by the old `$` anchor, and the Unicode-digit BBL *input* case, backstopped by `normalize_bbl` — benign redundancy, see F-3.)

## Findings

**F-1 — ADVISORY.** `resolve_by_condo_key` stamps `retrieved_at` **before** its fetch: `dtm_condo_soda.py:792` (`retrieved_at = _rfc3339(clock())`) precedes `_fetch_rows` at `:796`. The `resolve()` paths were correctly moved to post-response (`:638` billing, `:676` unit, `:721` expansion), but origin G3 #1 named **both** `:569` and `:739` (the old resolve_by_condo_key stamp). This single-query path has no cross-query drift, but a retry-delayed fetch makes the stamp precede actual retrieval — the exact pluto-precedent concern (d)/G3#1 targeted. No test asserts resolve_by_condo_key's stamp is post-response. Non-blocking (originally advisory; single query), but G3 #1 is not 100% closed.

**F-2 — ADVISORY (coverage gap).** The response-side condo_key guard (`dtm_condo_soda.py:708-714`, added under the F1 "apply the same to condo_key" remediation) has **no negative test**. It is reachable: `_collect_base_lots` collects `condo_key` without shape-validating it (`:526-528`), so a unit row with a valid `condo_base_bbl` but a Unicode/newline `condo_key` reaches `:708` and raises `SchemaDriftError`. The parallel base-BBL guard IS tested (`test_a_response_base_bbl_leaky_shape_rejected`); this twin branch is unexercised, so a mutation there would go undetected. Correct by inspection (byte-mirror of the base-BBL guard); security impact contained (F2 was Low, source-compromise scenario). Recommend one negative test in the wiring packet.

**F-3 — INFORMATIONAL (not a defect).** Mutation-sensitivity is uneven across the (a) parametrizations: the trailing-space params and the Unicode-digit *BBL input* param pass on the old leaky regex (old `$` anchor already rejected trailing space; `normalize_bbl` backstops the Unicode BBL input). The trailing-newline params (BBL, condo_key, response base) and the condo_key/response-side Unicode params are the genuinely mutation-detecting ones. The parametrize breadth is defensible; no change required.

**F-4 — INFORMATIONAL.** resolve_by_condo_key's documented code `"invalid_component"` (`:787`) is exercised by `test_condo_key_invalid_shape_fails_closed` (:441) but that test asserts only that `BBLValidationError` is raised, not that the code is in `DOCUMENTED_BBL_CODES` (unlike `test_e` for classify_lot). The code IS documented (bbl.py:46) and no ad-hoc code remains; minor asymmetry only.

## Read-only commands run
- `git cat-file -t 20c597c1|1fd07e9a|a380a956` (all commit); `git branch -a`
- `git show 20c597c1:project-control/tasks/M5-T044.json` and `…/reports/M5-T044-{producer-report.md,ci-evidence.md,evidence-map.json}`, `…/reports/M5-T042-{G5,G4,G3}.md`
- `git diff --stat 1fd07e9a a380a956 -- <3 files>` (empty) and `-- apps/ services/ packages/` (empty); `git show --stat f6e88222` (disjointness)
- `git show 1fd07e9a:<module|test|bbl.py>` extracted to scratchpad; `git archive 1fd07e9a services/api packages | tar -x` → frozen tree; sha256 (LF-normalized) blob==disk match
- `git grep -n dtm_condo_soda 1fd07e9a -- services/api/app/**/*.py` / `apps/**` (leaf)
- `cd services/api && python -m pytest tests/connectors/test_dtm_condo_soda.py -q` → **54 passed**; `python -m pytest tests/connectors -q` → **880 passed**
- Mutation probe on the throwaway copy: reverted regex/`.match`/`wrong_shape`, `pytest -k` → 6 target negatives FAIL, confirming sensitivity

**Not independently run (relied on as orchestrator-captured, not a BLOCKED cause):** AS-8 CI conclusions (gh read-only) — validity carried by the verified material→head byte-identity; `modularity_check.py --check` (tools/ not in the extraction) — orchestrator-captured exit 0 + CI success.

---

**Summary for the orchestrator:** VERDICT **PASS**. No blocking defects. Two ADVISORY items for the wiring packet — F-1 (resolve_by_condo_key retrieved_at still pre-fetch at `dtm_condo_soda.py:792`, leaving G3#1's `:739` sub-point partially open) and F-2 (untested response-side condo_key guard at `:708-714`) — plus two informational notes. Independently reproduced 54/880 green at the frozen tree and confirmed mutation-sensitivity of the load-bearing negatives. Content identity, disjointness from M5-T043, and leaf status all verified.
