# M5-T054 G3 — independent code review (saved VERBATIM from the reviewer return; this header line is the orchestrator's)

I have completed my independent verification. Let me compile the G3 gate report.

All review surfaces have been examined against source. Verdict below.

---

# G3 Gate Report — M5-T054 (Phase B2: proposal-conditioned rule checks)

**Reviewer:** code-reviewer (independent, read-only)
**Task:** M5-T054 — wire B1 `derive_proposal` facts into the existing rule engine as a new focused module
**Verdict: PASS** (no blocking findings; 3 advisory findings below)

## Identity verification (done before judging)

- Repo `C:\Users\MLFLL\Downloads\nyc-zoning\ctl24`, branch `candidate/D-024-mrl-option-b`. Current HEAD `f5dbdee0`.
- Confirmed `f5dbdee0` changes ONLY `project-control/state.json` + `project-control/tasks/M5-T054.json` (disjoint ledger commit). `git diff 622757b1 f5dbdee0 -- services/api` = empty, so all task-scope files are byte-identical to the frozen submission head `622757b1`.
- Material commit `ba0cf6b0`: exactly 9 files, all inside `allowed_paths`; no forbidden path touched (verified via `git show --stat --name-only`).

## Item-by-item rulings (as required by the packet)

### Item 1 — Check-engine correctness: CORRECT
- Fact-mapping table `PROPOSAL_CHECKS` (proposal_checks.py:216-255) is declared frozen dataclass data; import-time guard `_assert_checks_valid()` (740-751) rejects any check naming an unknown provided-fact key. No silent dict merge.
- Direction semantics `_compare` (602-611): MAXIMUM → PASS iff provided ≤ required, shortfall = provided−required; MINIMUM → PASS iff provided ≥ required, shortfall = required−provided. Both correct; shortfall is always the positive miss magnitude.
- Grouping/counting (717-726): pass/fail/could_not_check counts recomputed from results; summary total = len(results).
- Typed COULD_NOT_CHECK reason paths (`CouldNotCheckReason`, 108-130) are all reachable: PROVIDED_FACT_NOT_COMMENSURATE (semantic gate 651), PROVIDED_FACT_ABSENT (653), FAMILY_UNSUPPORTED (658), plus `_select_allowance` (565-599) yielding NO_APPLICABLE_RULE / ALLOWANCE_UNRESOLVED / AMBIGUOUS_RULE. Fail-closed on both 0-usable and >1-usable; never picks a winner (mirrors evaluator FH-2).
- Hand-computed arithmetic in `rectangle_case.json` independently re-derived and confirmed: footprint 100×50=5000; coverage 5000/8000=0.625 > 0.5 → FAIL shortfall 0.125; gross 5000×3=15000; cumulative height 10×3=30 ≤ 60 → PASS; min wall-to-lot-line setback |1000000−999990|=10 (west wall and both adjacent endpoints all resolve to 10; east wall 110). All match the fixture and test assertions.
- AS-2 mechanism verified end-to-end against `evaluator.py:654-698`: a missing REQUIRED non-applicability input (`street_width_class`) keeps `applicability_outcome=True` (applicability keys on `zoning_district` only) and sets `coverage_status=professional_review_required` with no output → `_select_allowance` classifies it ALLOWANCE_UNRESOLVED carrying that coverage status. This is a genuine, not accidental, first-class COULD_NOT_CHECK.

### Item 2 — Wiring preconditions (T051-G5 / DB-034(b)): AIRTIGHT (for caller inputs)
- Order in `check_proposal` (699-708): scenario_label validation → `_enforce_wiring_preconditions(lot)` → `derive_proposal(block, lot)`. Preconditions strictly precede derivation.
- `_enforce_wiring_preconditions` (425-462): type-guards LotContext; enforces MAX_LOT_LINE_SEGMENTS=1000 and MAX_STREET_LINES=500 (typed refusal naming field + count); enforces `_is_finite_number(area_sq_ft)`; and finiteness of every lot-line and street-line endpoint via `_require_finite_point` (409-422). `_is_finite_number` (398-407) correctly excludes bool, non-numeric, NaN/inf, and OverflowError-on-`float()` for oversized ints (e.g. 10**400).
- No `ProposalCheckError` embeds an uncapped value: every offending value goes through `_capped_repr` (75-81, cap 120) at 412, 419, 431, 453; the list-ceiling messages (436-449) embed only bounded integer counts. No refusal can be bloated by an oversized input. Confirmed by `test_as3_db034b_offending_value_is_length_capped`.
- No path lets a non-finite LOT-derived value reach an `EvidenceRecord`: area and all lot/street coordinates are guarded pre-derivation; `derive_proposal` never runs on a refused input.
- **Advisory G3-2:** the module's preconditions do not themselves guard the finiteness of the *block outline* vertices — that is delegated to the B0 validator (`validate_proposed_massing`, re-run inside `derive_proposal`) plus derivation's own `math.isfinite` guards (`derivation.py:407`, `_area_or_refuse`). This is consistent with the packet scope (preconditions bind the caller LotContext) and is defense-in-depth, not a gap: a non-finite block value fails closed with a typed `ProposalDerivationError` rather than reaching an EvidenceRecord. Noted for completeness, not blocking.

### Item 3 — Facts-vs-allowances honesty (D-076-R002): PASS
- Provided vs required are distinct fields on `CheckResult` (301-322) with distinct provenance; `as_dict` (324-351) never merges them.
- Derived facts are NEVER fed as rule inputs: `_build_rule_inputs` (525-542) assembles the evaluator input dict from `LotContext.area_sq_ft` + only the whitelisted `CALLER_RULE_INPUT_NAMES` (270-279); the `ProposalDerivation` object is not passed into `registry.evaluate`. The derived provided value is used only in `_compare` against the rule OUTPUT. A redundant caller `lot_area_sq_ft` copy is dropped as unmapped (verified by `test_as5`), so it cannot override the single source of truth.
- No allowance-vocabulary leakage onto derived facts: "required_value"/"allowance" refer exclusively to rule OUTPUTS; the provided-fact detail strings explicitly state "a PROVIDED value; not an allowance".
- Summary always carries `could_not_check_count` (355-390); a passing subset can never present as whole-building approval (`test_as4`).
- **Advisory G3-1:** producer report §5 (AS-4 line) claims "no permitted/required/allowed conflation (grep-tested in-suite)"; I found no explicit vocabulary-grep test. The separation is instead enforced *structurally* by distinct dataclass fields (stronger than a grep), so the substance of AS-4 holds — but the report overstates the test *form*. Non-blocking.

### Item 4 — AS-1 disposition (`[ORCH-SCOPE-DISPOSITION]`): implemented consistently; the engineering is SOUND
- The commensurability refusal is airtight: `_run_check` (638-674) applies the semantic gate FIRST (651-652), returning COULD_NOT_CHECK(PROVIDED_FACT_NOT_COMMENSURATE) before any `_family_traces`/`_select_allowance`/`_compare` for any check carrying a `semantic_gap` (rear_yard_depth, residential_far_floor_area). No path silently compares them. Proven by `test_rear_yard_not_commensurate_even_with_a_rule_present` and `test_residential_far_not_commensurate_even_with_a_rule_present` (rule computes 5 ft / 12000 sq ft respectively; `required_value` stays None), and against the REAL registry (`test_real_registry_residential_far_is_not_commensurate`).
- I explicitly concur the disposition is sound engineering: a literal "yard-class PASS" would require the module to designate which lot line is the rear — a legal interpretation barred by permanent principle 1, D-076-R002, and D-051. AS-1's substance (a commensurable PASS + a commensurable FAIL with exact hand-computed shortfall + full label/provenance propagation) is delivered via `building_height` PASS and `lot_coverage_ratio` FAIL. The deviation was escalated (test-file header 95-106; report §4) and is orchestrator-adjudicated, not self-approved.
- **Advisory G3-4 (informational):** this is a real deviation from the contracted AS-1 wording; PASS here is contingent on the orchestrator accepting the recorded `[ORCH-SCOPE-DISPOSITION]` (which it has). The follow-on (caller-designated rear-lot-line input to unlock a true yard check) is correctly routed to discovery, not fixed in-packet.

### Item 5 — Preservation + fixture isolation: PASS
- Byte-untouched: material commit `ba0cf6b0` touched only the 9 allowed files; `evaluator.py`, `integration.py`, `registry.py`, `response.py`, `rulesets/**`, and `scenario/**` are not in the diff. Current head is a disjoint ledger-only commit (verified above).
- Fixture-rule isolation is airtight: production `RuleRegistry` defaults to `_RULESET_DIR = app/rules/rulesets/` (`registry.py:23,117-118`); `_default_registry()` (proposal_checks.py:732-737) uses that default. The four synthetic rules live under `tests/rules/fixtures/proposal_checks/rulesets/` and are loaded ONLY via explicit `RuleRegistry(_RULESETS, snapshots=SnapshotStore(_SNAPSHOTS))` injection in the test file. Confirmed no `pc-*-demo` and no `demo/proposal` ruleset exists in `app/rules/rulesets/`, and the synthetic snapshot is not in any production snapshot store. No leak path.
- DB-034(d): module imports only `app.scenario.derivation` symbols + `coverage`/`registry`; builds no scenario document and emits no contract version (`test_as6` greps the source and the report dict for emitting tokens/keys). Verified against imports (42-50) and `__all__` (52-57).
- Modularity: `python tools/modularity_check.py --check` → exit 0, 0 failures; `proposal_checks.py` does NOT appear in the warn/justify list (below the warn tier despite the +754 diff-line count, which is added-lines, not SLOC). Single cohesive responsibility, clear sections, no dumping ground, preserves all existing public interfaces (touches none).

## Additional advisory
- **G3-3 (informational):** the shared synthetic snapshot `pc-demo-synthetic.snapshot.json` `verbatim_excerpt` states "30 feet" (reused verbatim from `zr-demo-rear-yard` for offline digest stability, documented in its `notes`), while `pc-rear-yard-demo` parameter and the fixture README/case say 5 ft. Harmless — the module never presents the rear-yard allowance (semantic gate), and the digest resolves — but the excerpt/param mismatch could momentarily confuse a future reader. No change required.

## Suites / evidence relied upon
- Recorded green (not re-run per packet guidance; logic verified against source): `tests/rules` 722 passed, `tests/scenario` 568 passed, CI run 35491182686 success on `ba0cf6b0`. Test assertions were cross-checked against the implementation paths above and against `evaluator.py`/`registry.py`/`coverage.py`/`derivation.py` contracts (all consistent).
- I independently ran only read-only analysis: `git show/diff/log`, `tools/modularity_check.py --report/--check` (exit 0), and targeted `grep`.

## Verdict

**PASS.** All eight acceptance scenarios are satisfied (AS-1 under the sound, orchestrator-adjudicated `[ORCH-SCOPE-DISPOSITION]`). Items 2, 3, 4, and 5 each rule favorably as detailed above. The three advisories (G3-1 report overclaim of a grep test; G3-2 block-vertex finiteness delegated to B0/derivation; G3-3 snapshot excerpt/param cosmetic mismatch) are non-blocking and require no code change.
