# M5-T041 — G5 security review (verbatim reviewer return; transport entity-decoding only)

Saved by the orchestrator from the security-reviewer agent return, 2026-09-18 (seq-118 wave,
pinned at submission sha 843e23c4). Nothing added or removed beyond decoding transport
HTML entities.

---

All verification is complete. Every security-relevant claim reproduces at the pinned SHA. Here is my G5 security gate report.

---

# G5 SECURITY GATE REPORT — M5-T041

**Task:** Address-flow + validator polish cluster (DB-024 copy/a11y quartet + DB-025(e) web-validator wording correction)
**Reviewed identity:** submission sha `843e23c4`; MATERIAL commit `740b6f2f` on `candidate/D-024-mrl-option-b`
**Reviewer:** security-reviewer (read-only, ADR-005)
**Gate:** G5 (security)
**Verdict: PASS**

## Scope of review
Web-only copy/a11y/wording change: 11 files, +818/-32 (of which the producer report is +625). Production source touched: `rule-evaluation-contract.ts`, `AddressAutocomplete.tsx`, `AddressConfirmCard.tsx`, `PropertyOverview.tsx`, `ZoningContextPanel.tsx`, `ProfileViews.tsx`, plus five test files. No `services/api`, no schema, no generated TS, no `address-search.ts`.

## Findings (all ADVISORY; zero BLOCKING)

**1. DB-025(e) validator wording now matches actual behavior — PASS (security-doc claim verified).**
`apps/web/src/lib/rule-evaluation-contract.ts` — the diff (`git show 740b6f2f`) at the module doc (lines 12-22) and function doc (lines 391-398) is **comment-only**: filtering the diff for any changed non-comment line returns empty. The new wording states a "POSITIVE-SHAPE check, not a closed-schema one; an unknown or extra top-level key is NOT rejected (no client-side additionalProperties enforcement), so the server stays authoritative." I confirmed this is exactly what the code does at `rule-evaluation-contract.ts:399-472`:
- The only `additionalProperties`/`Object.keys`/`Object.entries` occurrence in the entire file is inside the new comment (line 18); there is no unknown-key rejection logic anywhere.
- The `for (const key of [...] as const)` loop at lines 425-433 iterates a **hardcoded list of documented keys** (`needs_review`, `professional_review_required`, `fail_safe`), never `Object.keys(body)`.
- The function only calls `checkEnum`/`problems.add` on named documented keys and returns `ok:true` whenever `problems.list` is empty (lines 468-471), so an extra top-level key cannot fail validation.
This is a correction that *removes* an overclaim on a validation boundary (prior wording "checked against the documented key set... FAILURE IS TOTAL" could read as closed-schema). It is now honest. **Zero behavior change** — comment-only diff confirmed. The client positive-shape check is defense-in-depth against rendering a malformed/partial payload; the server remains the authoritative closed-schema enforcer, which the wording now states truthfully. Not a defect — a security-documentation improvement.

**2. No new external calls / no new URL construction — PASS.**
Grep of `AddressAutocomplete.tsx` for `fetch(`/`new URL(`/`XMLHttpRequest`/`innerHTML`/`dangerouslySetInnerHTML`: none. All three ZoLa links keep the existing validated helper: `AddressConfirmCard.tsx:62` (`zolaLotUrl(canonicalBbl)` → `href={zolaUrl}`), `PropertyOverview.tsx:48` (`zolaLotUrl(bbl)` → `href={siteZolaUrl}`), `ZoningContextPanel.tsx:39` (`zolaLotUrl(profile.identity.bbl)` → `href={zolaUrl}`). Helper call sites unchanged — only link *text* changed to `{ZOLA_LOT_LINK_LABEL}`. The internal confirm href at `AddressConfirmCard.tsx:238` still uses `encodeURIComponent(canonicalBbl)` (pre-existing, untouched). The external hint link `https://geosearch.planninglabs.nyc/docs/` href is unchanged.

**3. No target/rel regression — PASS.**
Every touched external link retains `target="_blank" rel="noopener noreferrer"` (AddressConfirmCard:141-142, PropertyOverview:60, ZoningContextPanel:55-56, address-hint link). `noopener` preserved — no reverse-tabnabbing introduced.

**4. Shared constants introduce no injection surface — PASS.**
`ABSENT_BBL_MAP_LINK_NOTE` and `ZOLA_LOT_LINK_LABEL` are static string literals. `FULL_SEARCH_ERROR_MESSAGES` entries are plain strings with **no `${}` interpolation at all** — no user input flows into the new copy. (`ERROR_MESSAGES` interpolates only `${FULL_ADDRESS_SEARCH_LABEL}`, a static constant.) All copy renders as React text content (auto-escaped); no `dangerouslySetInnerHTML`. The `role="status"` message selection at `AddressAutocomplete.tsx:151` picks between two static tables by a boolean flag — no injection path.

**5. aria-hidden changes hide only decorative glyphs — PASS.**
`AddressAutocomplete.tsx` wraps the `↗` glyph in `<span aria-hidden="true">` while preserving the accessible name "NYC Planning address suggestions"; `ProfileViews.tsx` wraps the `→` glyph while preserving "Inspect calculation evidence". No meaningful content is hidden from assistive tech; matches the accepted DB-019a pattern.

**6. Scope / least-privilege / fail-safe posture preserved — PASS.**
Diff stat confirms all 11 changed files are within `allowed_paths`; nothing outside. `apps/web/src/lib/address-search.ts` is **not** in the diff — retry/outcome classification and DB-006 `rejected` semantics are byte-untouched (fail-safe posture preserved). The DB-024(a) change is a presentation-only selection between two copy tables keyed by `fullSearchActive`; no retry/outcome logic changed.

## Non-applicable security surfaces (verified no regression introduced)
Cross-tenant isolation, service-role secrecy, private storage, SSRF, upload controls, prompt-injection, log redaction: **N/A** for this web copy/a11y/wording packet — no auth, storage, upload, network, logging, or secret code paths are touched. Confirmed no secrets, no `console.log` of user input, and no new logging added in the diff.

## Evidence relied upon (orchestrator-captured, verified)
`M5-T041-ci-evidence.md`: CI run `35402081543` success (web + web-e2e vitest suites), secret-scan `35402081496` success, context-budget `35402081495` success, at material head `740b6f2f`. Per ADR-005 I verify the stored CI evidence rather than re-run (thin-client; no local npm/node). Modularity reproduced exit 0 in-worktree.

## Exact read-only commands run
- `Read` project-control/tasks/M5-T041.json, project-control/reports/M5-T041-producer-report.md, project-control/reports/M5-T041-ci-evidence.md
- `git log --oneline -5 740b6f2f` ; `git show --stat 740b6f2f`
- `git show 740b6f2f -- apps/web/src/lib/rule-evaluation-contract.ts apps/web/src/components/architect/AddressAutocomplete.tsx apps/web/src/components/address/AddressConfirmCard.tsx apps/web/src/components/architect/PropertyOverview.tsx apps/web/src/components/architect/ZoningContextPanel.tsx apps/web/src/components/architect/ProfileViews.tsx`
- `git show 740b6f2f -- apps/web/src/lib/rule-evaluation-contract.ts | grep -E "^[-+]" | grep -v <comment filters>` (confirmed comment-only diff)
- `Grep` `Object\.keys|Object\.entries|additionalProperties|...` in rule-evaluation-contract.ts (only the new comment matched)
- `Read` rule-evaluation-contract.ts:399-472 (validator body)
- `Grep` `dangerouslySetInnerHTML|new URL\(|fetch\(|XMLHttpRequest|innerHTML|\$\{` in AddressAutocomplete.tsx
- `git show 740b6f2f:<AddressConfirmCard|PropertyOverview|ZoningContextPanel>.tsx | grep href/target/rel/zolaLotUrl`

## Verdict
**PASS.** The DB-025(e) validator wording is now an accurate, non-overclaiming description of a positive-shape documented-key check with the server authoritative, and the validation logic is byte-unchanged (comment-only diff). No new external calls, URL construction, or target/rel regression. The new shared constants are static strings with no user-input interpolation and no injection surface. aria-hidden hides only decorative glyphs. Scope is clean and the fail-safe retry/outcome posture in `address-search.ts` is untouched. No BLOCKING findings; no ADVISORY defects requiring remediation.

Relevant absolute paths:
- `C:\Users\MLFLL\Downloads\nyc-zoning\ctl24\apps\web\src\lib\rule-evaluation-contract.ts`
- `C:\Users\MLFLL\Downloads\nyc-zoning\ctl24\apps\web\src\components\architect\AddressAutocomplete.tsx`
- `C:\Users\MLFLL\Downloads\nyc-zoning\ctl24\apps\web\src\components\address\AddressConfirmCard.tsx`
- `C:\Users\MLFLL\Downloads\nyc-zoning\ctl24\apps\web\src\components\architect\PropertyOverview.tsx`
- `C:\Users\MLFLL\Downloads\nyc-zoning\ctl24\apps\web\src\components\architect\ZoningContextPanel.tsx`
- `C:\Users\MLFLL\Downloads\nyc-zoning\ctl24\apps\web\src\components\architect\ProfileViews.tsx`
- `C:\Users\MLFLL\Downloads\nyc-zoning\ctl24\project-control\reports\M5-T041-ci-evidence.md`
