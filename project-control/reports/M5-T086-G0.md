# M5-T086 G0 — readiness gate (orchestrator, administrative)

Task: **M5-T086 — D-087 CAD-3: strict-subset DXF reader - ASCII DXF LINE / LWPOLYLINE / POLYLINE-VERTEX / 3DFACE / TEXT on named layers into typed primitives with declared units; bounded, fail-closed refusal values, never an exception (module + tests; not wired)**

## Authority

- Owner directive D-087 (capacity + released feature families): an orchestrator-dispatched subagent producer packet; D-087-R001 capacity, R002 non-interference.
- Directive refs: D-087-R001, D-087-R002, D-087-R006, D-087-R007, D-087-R009, D-066-R001 - bound via applicability.task_ids with same-commit digest
  resyncs + audit_log entries; `evaluate_task_refs`: **ok=true, applicable == cited**.
- Scope source: the D-087 released feature family named in the packet's objective (owner
  directive 2026-09-24; section 2.3 of the hold notice records the scoped release).
- Boundaries unchanged (D-087-R009): every gate, dependency security (zero new packages here),
  Tier D, PR #241, native-DWG licensing (R007 hold), the D-086 sequencing; the max-envelope route
  stays UNMOUNTED and this packet ships a module only (no route, no web).

## Packet readiness

- allowed_paths: 3 entries (every production/test file exists and is
  tracked; the producer report placeholder is seeded at this seam).
- 5 acceptance scenarios, each with a named mutation or
  grep/CI proof obligation; documented_test_commands are plain single commands.
- Producer: backend-engineer; reviewers: data-contract-verifier,code-reviewer,qa-engineer,security-reviewer,directive-compliance-verifier; gates G0,G1,G2,G3,G4,G5.
- Code graph regenerated at this seam (815 files/17025 nodes/7441 edges); the navigation block names consumers,
  forbidden neighbors, and the `query.py --no-regen impact` instruction (D-066-R001).

## Disjointness (live/frozen neighbors)

| Neighbor | State | Overlap |
|---|---|---|
| M5-T001 | awaiting_gate | none - EMPTY overlap |
| M5-T073 | awaiting_gate | none - EMPTY overlap |
| M5-T075 | awaiting_gate | none - EMPTY overlap |
| M5-T081 | claimed | none - EMPTY overlap |
| M5-T082 | claimed | none - EMPTY overlap |
| M5-T083 | claimed | none - EMPTY overlap |
| M5-T084 | claimed | none - EMPTY overlap |
| M5-T085 | claimed | none - EMPTY overlap |
| M5-T087 | contracting | none - EMPTY overlap |

## Seam records

- Packet + binds + digest resyncs + placeholder + this report land in ONE contract-seam
  commit; G0 is recorded at that head; claim with the FULL worktree path; the worktree is
  created AT the claim-seam commit.

Verdict: **PASS** — packet is contract-ready.
