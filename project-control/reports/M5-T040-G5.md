# M5-T040 — G5 security review (verbatim reviewer return; transport entity-decoding only)

Saved by the orchestrator from the security-reviewer agent return, 2026-09-18 (seq-118 wave,
pinned at submission sha b4865d1c). Nothing added or removed beyond decoding transport
HTML entities.

---

All checks pass. I have everything needed. Here is my G5 report.

---

# G5 SECURITY REVIEW — M5-T040 (named-street override WIRING; DB-023 closure + attestation-seam integration)

**Reviewer:** security-reviewer (read-only). **Pinned submission:** `b4865d1c`. **Material:** `987930a2` (DB-023 hardening) + `751a1826` (wiring + provider + web gating).

## VERDICT: PASS
No BLOCKING findings. My three prior M5-T039 rulings are all independently confirmed CLOSED. Six ADVISORY/observation items, none blocking (defense-in-depth residues, latent-for-future-callers, or non-security ownership).

## IDENTITY / SCOPE
- Working tree byte-identical to `b4865d1c` for all nine reviewed source/test/web files (`git diff b4865d1c -- <file>` = 0 lines each).
- Both material commits touch ONLY `allowed_paths` files. `987930a2` = named_street_override.py, wide_street_live_provider.py + their tests + report. `751a1826` = wide_street_wiring.py, wide_street_live_provider.py, CalculationEvidence.tsx, DevelopmentLimits.tsx + their tests + report. Neither touches `integration.py`, `response.py`, any schema, or `rule-evaluation-contract.ts` — **contract preserved, confirmed** (grep of each commit's file list = "none"). Bookkeeping commits `4d8847c6`/`b4865d1c` = 2 control-plane files each.
- No new third-party dependency (imports: `re`, `dataclasses`, `enum`, `logging`, `os`, `functools`, `collections.abc`, and internal `app.*` only). No flag/settings semantics changed (`LIVE_WIDE_STREET_PROVIDER_ENABLED` unchanged; default DISABLED, zero connector calls when off).

## PRIOR M5-T039 RULINGS — CLOSURE VERIFICATION

**M1 (MEDIUM, disposition metadata-bypass) → CLOSED.** The load-bearing refusal is now bound to the AUTHENTICATED COMPLETE source span, not to free-floating metadata (`_validate_unconditional_source_binding`, named_street_override.py:491-569; invoked before rows are built at :463). I re-derived the exact M1 reproduction against the shipped snapshot: flipping `disposition_when_located` to `matched_override` while leaving the authentic excerpt byte-identical now fails CLOSED at the no-qualifier residual check (:551-560) — the authentic Broadway/Allen sentence carries lowercase tokens `separated/by/mapped/public/park` that are not in `_UNCONDITIONAL_DESIGNATION_WORDS` (:187-209), not numeric (:214), and not capitalized, so `residual` is non-empty and construction raises. The three sub-attacks in the task brief are all handled for the shipped artifact: (i) disposition flip → caught by residual check; (ii) full-metadata strip (clause=None, empty open_questions) with the intact conditional quote → caught by residual check; (iii) quote narrowing → caught by the complete-span boundary check (:532-540), because the authentic designation paragraph has NO internal sentence terminator separating a locator-bearing prefix from the qualifier. Confirmed the two real rows carry `disposition_when_located: "indeterminate"` (snapshot :54) and the tamper-evidence quote-substring gate (:335-341) still holds.

**L1 / G3-A2 (match()-time KeyError) → CLOSED.** `_REQUIRED_BLOCK_FIELDS = ("provision_id","section_anchor")` (:225) is validated at construction for BOTH blocks (`_validate_block_provenance` :374-392, called :349-350), requiring non-empty `str`. I enumerated every result-time hard dict access: `match()` reads `_named_block["provision_id"]` (:736,:742); `_row_provenance`/`_alt_provenance` read `["section_anchor"]`/`["provision_id"]`/`["verbatim_source_quote"]` (:662-686); `_located`/`_boundary_indeterminate`/`classify_alternate_width_district` read `["provision_id"]`. All are covered — `provision_id`/`section_anchor` by `_REQUIRED_BLOCK_FIELDS`, and `verbatim_source_quote` is independently guaranteed non-empty by the tamper-evidence loop (:335-341). match() cannot KeyError after successful construction. **Coverage is complete for every field match()/classify reads.**

**L2 (unbounded query repr in reason) → CLOSED.** `_bounded_repr` (cap 80, :233-241) is applied to EVERY user-reachable query string that lands in `MatchResult.reason`: `query.street_name` (:708,:722), `query.borough` (:724), `query.cross_street_from/to` (:755-756), and `classify_alternate_width_district`'s `district` (:784). All other reasons are fixed literals or embed snapshot-derived (repo-controlled, bounded) values only. No user-reachable string is embedded unbounded.

**Advisory (d) word-boundary anchoring → CLOSED.** `_word_bounded` (:275-285) uses `(?<!\w)…(?!\w)` on an `re.escape`'d normalized needle; prevents partial-word anchoring (`est 94th` inside `west 94th`). Used for both full and core forms in `_anchored_in` (:288-311). No regression.

## WIRING FAIL-SAFETY (751a1826) — CONFIRMED FAIL-CLOSED
- `exceptions_checked=True` is reachable ONLY via `_elevated_exceptions_checked` (wide_street_wiring.py:269-290): returns False on any `matched_override`, False on any unresolved/unimplemented named-street status, else requires ALL decisions ∈ {wide,narrow}. No error path defaults it True — professional-review branches hardcode `False` (:528,:607) and every other branch passes the computed value that already gates on a cleared status.
- The provider's queries carry `community_district=None`, `cross_street_from/to=None` (:183-192), so `_fully_resolved_typed_inputs` (:335-360) fails for every live candidate → status is always unresolved → professional review. The live path can NEVER attest `exceptions_checked=True` today (double fail-safe with `_UNATTESTED_EC5` at the B4 gate). Confirmed.
- Adversarial inputs: MATCHED_OVERRIDE with `provenance=None` is guarded (:419-424, no crash). Matcher load failure → typed-log + unresolved status (:193-205), never "cleared". A raised match() cannot occur post-construction (see L1); were one to occur it would PROPAGATE (500), not become a confident determination. B4 engine failures map to professional review via typed `except WideStreetBufferEngineError` (:622-642); a bare engine exception propagates (fail-closed), never collapses to a determination. **No code path converts an error into `exceptions_checked=True`.**
- DB-021 ceilings enforced BEFORE expensive work: lot-vertex cap (`_canonical_vertex_count > MAX_LOT_VERTICES`, provider:467-470) runs before this module's first `canonical_to_shapely` (:472); wide-segment cap (`len(wide_object_ids) > MAX_WIDE_SEGMENTS`, :509-511) runs before the per-chunk geometry-fetch loop (:512-515). Confirmed.

## OTHER JUDGED ITEMS
- **(3) Provenance strings → bounded & safe.** Logs are payload-only (`event`, `type(exc).__name__`, `correlation_id`; never `str(exc)`, never snapshot text or street names — `_fail_safe` :276-284). Response reasons embed snapshot-controlled `matched_row_verbatim`/`provision_id`/`section_anchor`/`snapshot_sha256` (wide_street_wiring.py:425-434) — repo-controlled, bounded (~250 chars), plain f-string interpolation into a message (not a logging format template, not eval'd). No format-string/injection surface, no unbounded amplification.
- **(4) No numeric use of alternate-width values.** `classify_alternate_width_district` performs no numeric test (coverage_class only; `AlternateWidthResult` has no width field). A matched override returns `FAR_ROW_NONE` → professional review, `buffer_result=None`; `select_far_row_value` returns `None` for `FAR_ROW_NONE`. `MatchedNamedStreetOverride` carries no width. Confirmed nowhere.
- **(5) DoS posture.** Per-request work bounded; no new network calls. Connector caps total segments at `HARD_MAX_PAGES(200) × MAX_RESULT_RECORD_COUNT(2000)` and the provider returns None on any transfer-limited page; the O(1) matcher/classify loops are thereby bounded, and the expensive geometry-fetch/shapely path is capped earlier by DB-021 a/b.
- **(6) Scope/least privilege.** Confirmed above — allowed_paths only, contract byte-untouched, no new dep, no flag change.
- **Web (DB-025 a-c).** CalculationEvidence/DevelopmentLimits diffs gate purely on `determination_state`; no `dangerouslySetInnerHTML`/`eval`/injection sink introduced. The one `<a href>` (CalculationEvidence.tsx:128) is pre-existing (not in this diff) and its `officialZoningTextUrl` helper (source-links.ts, untouched) enforces scheme+host+path allow-listing (its tests reject `javascript:`, foreign hosts, userinfo, query strings, suffix-spoofed hosts).

## FINDINGS
1. **[ADVISORY] Sentence-terminator + capitalized-token heuristics are the self-certifying-snapshot trust-root residual.** named_street_override.py:537 treats any `.` as a sentence terminator and :557 accepts ANY capitalized token as a "proper-noun locator." Sound for the shipped excerpt (designation paragraph has no abbreviation periods; qualifier words are lowercase). A FUTURE snapshot whose authentic designation sentence contains an abbreviation period (`St.`, `Ave.`) or Title-cased qualifier words could let a narrowed complete-span pass. Because `content_digest_sha256` is computed FROM the excerpt, a fully rewritten source text is always outside code-level defense and relies on snapshot-commit review — the same acceptability basis recorded for M5-T039 M1. Recommend a guard/test for abbreviation-period and capitalized-qualifier snapshots if the corpus grows. Not blocking.
2. **[ADVISORY] Row-level construction accesses raise bare `KeyError`.** `_build_rows` reads `row["borough"]/["street_name"]/["frontage_from"]/["frontage_to"]` directly (named_street_override.py:583-587); a malformed row raises `KeyError`, not `NamedStreetOverrideError`. Construction-time only (not match()-time — L1's actual defect is closed) and caught by the provider's `except Exception` fail-safe boundary (wide_street_live_provider.py:195), so it fails safe. Minor consistency nit.
3. **[ADVISORY] community_district source-trace uses plain substring** (named_street_override.py:622-624): `"community district 7"` would substring-match `"community district 70"`. Construction-time, snapshot-controlled, pre-existing from M5-T039. No runtime/user surface. Minor.
4. **[ADVISORY / latent] `_collapse` casefolds but does not NFC/NFKC-normalize** (named_street_override.py:244-251). No current exploit path — the sole production caller supplies DCM-derived names with `community_district=None`, so every candidate is unresolved → professional review regardless of Unicode. If a FUTURE caller ever feeds user-controlled street names WITH fully-resolved CD + cross-streets, a homoglyph could evade a named-override match (false-negative → potential overstatement when numeric width independently classifies wide). Recommend NFKC-before-casefold before any user-supplied name reaches the matcher resolved.
5. **[OBSERVATION, non-security] Modularity review-signal.** `wide_street_wiring.py` and `named_street_override.py` are above the warning threshold; `python tools/modularity_check.py --check` = `failures 0` (review-signals only; producer already flagged the cohesion signal). Cohesion verdict is owned by G3 code-review, not G5.
6. **[OBSERVATION, DoS] Theoretical ≤400k in-memory segment loop** per request (connector page ceiling). O(1) per iteration, sub-second, bounded, per-request, no amplification, no new network calls. Acceptable.

## M5-T039 RULING STATUS (explicit): **M1 CLOSED. L1 CLOSED. L2 CLOSED.** (advisory (d) also CLOSED.)

## EXACT READ-ONLY COMMANDS RUN
- `git log --oneline -8 b4865d1c`; `git merge-base --is-ancestor b4865d1c HEAD`; `git rev-parse HEAD`
- `git show --stat 987930a2`; `git show --stat 751a1826`; `git show --name-only --format="" {987930a2,751a1826}`
- `git diff b4865d1c -- <each of the 9 reviewed files>` (all 0)
- `git show 751a1826 -- apps/web/src/components/architect/{CalculationEvidence,DevelopmentLimits}.tsx`
- `grep -nE "maxRecordCount|MAX_OBJECT_ID_LIST|HARD_MAX_PAGES|exceeded_transfer_limit" services/api/app/connectors/dcm_street_centerline_arcgis.py`
- `grep -nE "dangerouslySetInnerHTML|innerHTML|eval\(|new Function" apps/web/src/components/architect/{CalculationEvidence,DevelopmentLimits,ReportView}.tsx`; `grep -rn "officialZoningTextUrl" apps/web/src/lib/`
- `cd services/api && python -m pytest tests/rules/test_named_street_override.py -q` → **63 passed**
- `cd services/api && python -m pytest tests/rules/test_wide_street_wiring.py tests/spatial/test_wide_street_live_provider.py -q` → **73 passed**
- `python tools/modularity_check.py --check` → **failures 0; warnings 22** (review-signals)
- `cd services/api && python -m ruff check app/rules/named_street_override.py app/rules/wide_street_wiring.py app/spatial/wide_street_live_provider.py` → **All checks passed!**
- Read: named_street_override.py, wide_street_wiring.py, wide_street_live_provider.py, snapshots.py, zr-12-10.snapshot.json, M5-T040.json packet, M5-T039-G5.md, DISCOVERY_BACKLOG.md (DB-023).

**Relevant absolute paths:**
- `C:\Users\MLFLL\Downloads\nyc-zoning\ctl24\services\api\app\rules\named_street_override.py`
- `C:\Users\MLFLL\Downloads\nyc-zoning\ctl24\services\api\app\rules\wide_street_wiring.py`
- `C:\Users\MLFLL\Downloads\nyc-zoning\ctl24\services\api\app\spatial\wide_street_live_provider.py`
- `C:\Users\MLFLL\Downloads\nyc-zoning\ctl24\services\api\app\_zr_snapshots\v1\zr-12-10.snapshot.json`

**G5 verdict: PASS** — record with the six advisory items routed to discovery (A1/A4 are the security-relevant follow-ups for future corpus growth / future callers); M5-T039 M1/L1/L2 confirmed CLOSED.
