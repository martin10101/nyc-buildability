# G3 Code Review — M5-T026 (D-057 default-on gate mode)

> Saved VERBATIM by the orchestrator from the code-reviewer agent return (2026-09-14;
> transport entity-decoding only, per the report-preservation rule). Reviewer ≠ producer.

**Reviewer:** code-reviewer (independent; producer was frontend-engineer) — READ-ONLY
**Review head (pin):** `1087aff6451862433dd41aa83792fd93efe921d0` (verified `git rev-parse HEAD` in primary checkout `C:/Users/MLFLL/Downloads/nyc-zoning/ctl24`)
**Material commit:** `0b112357` — exactly the 5 allowed files (`git show 0b112357 --name-only`); all 5 byte-stable from `0b112357` → CI head `2e2f7a1f` → pin `1087aff6` (per-file `git rev-parse <sha>:<path>` blob comparison, all STABLE; ancestry confirmed both hops with `git merge-base --is-ancestor`).
**Executable authority:** CI run 34814460843, `headSha 2e2f7a1fd126f76fe5f19eebd7ea1af926d90ab6`, conclusion `success` — all 18 jobs green including `web` (lint+typecheck+build), `web-e2e` (vitest+Playwright), and `modularity` (verified read-only via `gh run view 34814460843 --json headSha,conclusion`).
**Spec basis:** `project-control/directives/D-057-ruleeval-default-on/requirements.json` + `source-001.md`; packet `project-control/tasks/M5-T026.json` (S1–S4). D-046-R001/R002 texts read for context (dispatch-shape requirements; the diff staying inside the 5 allowed paths is the observable producer-side evidence of scope disjointness).

## What I personally inspected vs took from CI

- **Inspected:** full current source of `apps/web/src/lib/rule-evaluation.ts`, full pre-change source (`git show 0b112357^:...`), the complete diff of all 5 files, full test file, full `page.tsx`, checklist diff, producer report, D-057 requirements/source, packet. I independently re-derived the decision table and every matrix expected value from D-057-R001. Repo-wide grep for `INTERNAL_RULE_EVAL_DEFAULT_ON` and for the surface function's consumers.
- **From CI (not locally executable — thin client, no node_modules):** the actual execution of the 71 vitest cases, tsc/lint/build, Playwright e2e, and the modularity job — all green at a head whose material files are byte-identical to the pin.

## Findings

**Blocking (F):** none.

**Advisory (A):**

- **A1 — stale "two-factor" wording in the test file's own header docstring.** `apps/web/src/lib/__tests__/rule-evaluation.test.ts:26-27` still describes "the two-factor frontend flag (env + per-request opt-in)". After D-057, factor 2 is a composite (opt-in OR default-on-when-param-absent). Non-blocking: R003 names only the `rule-evaluation.ts` comment block and the `page.tsx` docstring (both updated and accurate), and the packet required pre-existing test blocks unmodified — leaving the header was the conservative choice. The new D-057 describe blocks carry their own accurate comments. Suggest fixing the header line in a future touch of this file.
- **A2 — packet-authoring note:** S4's parenthetical "no NEXT_PUBLIC_ anywhere in the diff" is literally unsatisfiable when the producer report (an allowed file) must disclose the term, and when the checklist legitimately contrasts against the pre-existing build-inlined var. Future packets should scope such string assertions to *code files' added/removed lines*. (Ruling on the substantive point in item 4 below.)
- **A3 — minor doc wording:** checklist §2a opens "optional and independent of steps 1-3" while the effect requires step 1's `INTERNAL_RULE_EVAL_ENABLED`; the "What it does" bullet does state the joint gating explicitly ("as long as INTERNAL_RULE_EVAL_ENABLED (step 1) is also on"), so this is not dishonest — just slightly loose. No correction required.

## Rulings on items 1–5

**1. D-057-R001 semantics — EXACT. PASS.**
`rule-evaluation.ts:131-139`: (a) `if (!ruleEvaluationFlagEnabled()) return false;` — main flag off → off unconditionally, checked first, short-circuits before either the new var or the param is read (tests at test.ts:209-253 prove this with default-on=1 and param on/off/absent). (b) `if (raw === undefined) return ruleEvaluationDefaultOnEnabled();` — param absent → on iff new var holds a TRUE_TOKENS token; `ruleEvaluationDefaultOnEnabled` (lines 114-118) is a byte-mirror of `ruleEvaluationFlagEnabled`: `typeof rawValue === "string" && TRUE_TOKENS.has(rawValue.trim().toLowerCase())` — absent/empty/unknown → false. (c) Param PRESENT falls through to the byte-identical pre-change kill-switch path: array-first, trim, lowercase, TRUE_TOKENS membership — `"off"`, `"0"`, `""`, `"banana"`, `["off"]`, and `[]` (raw[0]=undefined → not a string) all → false regardless of both env vars; explicit true token (`"on"`, `"ON "`, `["on","off"]`) → true regardless of the new var. (d) Server-read: the name is not NEXT_PUBLIC-prefixed and is read via computed bracket access as a call-time default parameter — Next inlines only literal `process.env.NEXT_PUBLIC_*` member expressions, so neither the name nor the value reaches the client bundle; in a client context the read would yield undefined → false (fail-safe). Read per request: the sole caller is the Server Component `page.tsx:37`, which awaits `searchParams` (a Next 15 dynamic API), forcing per-request dynamic rendering — the env is not baked at build; the checklist's "no rebuild needed" claim is therefore true. Only the enabled *boolean* crosses to the client tree (`page.tsx:41`), unchanged from before.

**2. D-057-R002 additivity — PASS.**
I diffed pre vs post function myself: for `raw !== undefined` the code path is character-identical to the pre-change body; for `raw === undefined` the pre-change code returned false (`typeof undefined !== "string"`), and the post-change code returns `ruleEvaluationDefaultOnEnabled()`, which is false whenever the var is unset — extensional equivalence for every input with the var unset. `git show 0b112357 --name-only` = exactly the 5 allowed files: no e2e file, no `playwright.config.ts`, no component, no `rule-evaluation-api.ts`, no package/lockfile. The test-file diff contains **zero removed lines** (3 hunks, pure additions) — every pre-existing block is byte-unmodified. Repo-wide grep confirms nothing in `apps/web/e2e/**` or any harness/config sets `INTERNAL_RULE_EVAL_DEFAULT_ON`, so the module-comment claim that param-absent harness requests stay OFF is verified, and CI `web-e2e` green at a byte-identical head is the executable proof (including the read-only input `rule-evaluation-flag-off.spec.ts`). The module-scope beforeEach/afterEach safety net (test.ts:40-51) clears/restores the var around every test without touching pre-existing test bodies — a clean way to guarantee the var-absent precondition.

**3. D-057-R003 docs honest — PASS.**
Checklist §2a (added lines, `docs/RENDER_INTERNAL_WEB_DEPLOY_CHECKLIST.md`): exact var name + value (`INTERNAL_RULE_EVAL_DEFAULT_ON = 1`); **WEB service only** with the accurate "no effect on nycdf-api" note (verified: `services/api` has zero references to the var); what it does incl. joint gating with step 1; the exposure-consequence sentence ("anyone holding the web URL sees the full internal flow immediately", framed against the owner-accepted §5 posture, citing D-057 source); `?ruleeval=off` remains the kill switch, never weakened; server-read → env save + automatic restart suffices, no rebuild (correct — see item 1d). §9 pointer added; provenance-table row added. `rule-evaluation.ts` comment block (lines 50-87) accurately describes the composite factor 2, the array-first rule, the kill-switch invariant, the harness-unaffected claim (verified), and production inertness under a closed factor 1. `page.tsx` docstring (lines 17-29) replaces the stale "acts only as a fail-safe kill switch" line with both modes and the kill-switch-in-both-modes invariant — matches the implementation exactly. No stale two-factor text remains in either R003-named location (test-file header is A1, out of R003's scope).

**4. NEXT_PUBLIC judgment call — producer's reading ACCEPTED; no correction required.**
Verified with `git show 0b112357 | grep -n NEXT_PUBLIC`: in the two code files and the test file, added/removed lines contain zero `NEXT_PUBLIC` (the one hit in `rule-evaluation.ts` is an untouched context line). The single checklist *added* line mentions the pre-existing `NEXT_PUBLIC_API_BASE_URL` solely as contrast to explain why the new var needs no rebuild — it introduces no NEXT_PUBLIC var and actively reinforces the server-read property R001 demands. The remaining hits are the producer report's own mandatory disclosure text; a literal reading of S4 would condemn the disclosure itself, which is absurd. S4's substantive requirement — "both env vars stay server-read" — is fully met. Ruling: S4 satisfied as intended (see A2 for the packet-wording advisory).

**5. General code quality — PASS.**
Edge cases: whitespace/case (`"ON "` → true), empty string present → off, garbage → off, array-first (`["on","off"]` → on, `["off"]` → off), empty array classified as *present* → off (correct and explicitly reasoned in the producer report — only a truly missing key consults the new var; matches R001's "param PRESENT without true token → off"). Every ambiguous input resolves to false (fail-safe); the kill switch is checked on the identical pre-change path and cannot be weakened by either env var; the main flag short-circuits first. Module boundaries: the change lives entirely in the flag-helper section of the module that already owns this responsibility, exports a focused named constant + function mirror-symmetric with factor 1; no new dependency, no dumping-ground growth (~540 SLOC, under the 600 warn threshold; CI modularity job green). Security: no client-bundle leak path (item 1d); only the boolean prop crosses to the client, unchanged. Test quality: the 50-row matrix's expected values are hand-labeled per bucket (not derived from the implementation), the 71-case count reproduces (9 + 50 + 7 + 5), and I independently re-derived every row's expected boolean from D-057-R001 — all agree.

## Scenario table (my own verification per row)

| Scenario | My verification | Result |
|---|---|---|
| S1 gate_matrix_complete | Re-derived all 50 matrix rows + 9 token rows + 7 flag-off rows + 5 R002 rows against D-057-R001 by reading the code; every combination in the packet's list is present (main-flag-off × {param, var} combos; flag-on + param on/ON␣/array-true → on regardless of var; flag-on + present-non-true incl. `""`, `[]`, `["off"]` → off regardless of var; param absent → var-dependent; var absent/empty/garbage → false; trim/case mirrors TRUE_TOKENS). Execution: CI web-e2e (vitest) green at byte-identical head. | PASS |
| S2 additive_zero_regression | `git show 0b112357 --name-only` = 5 allowed files only; zero deleted lines in test file (pre-existing blocks byte-unmodified); pre/post function equivalence re-derived by me (identical path for present param; unset-var absent-param → false both before and after); explicit R002 equivalence block present (test.ts:255-302). | PASS |
| S3 docs_honest | All six checklist elements verified present and true (var name, WEB-only + nycdf-api-inert claim checked against services/api grep, effect, exposure sentence, kill-switch survival, no-rebuild — dynamic-render reasoning in item 1d); module comment + page docstring match implementation; no stale text in the R003-named locations. | PASS |
| S4 scope_and_safety | Exactly 5 files; both vars server-read (non-NEXT_PUBLIC, computed access, per-request default param, Server-Component-only caller); NEXT_PUBLIC ruling per item 4; zero new dependencies (no package/lockfile in diff); CI 34814460843 success at `2e2f7a1f` with material files byte-identical to pin. | PASS (with item-4 ruling recorded) |

## Verdict

**PASS** — no blocking findings; advisories A1–A3 are non-blocking. Identity note: review pinned at `1087aff6`; material identity established at `0b112357` and byte-stable through CI head `2e2f7a1f` to the pin; intervening commits are disjoint control-plane/walkthrough commits that do not touch the task's allowed paths.

Key files:
- `C:/Users/MLFLL/Downloads/nyc-zoning/ctl24/apps/web/src/lib/rule-evaluation.ts` (gate at lines 104-139)
- `C:/Users/MLFLL/Downloads/nyc-zoning/ctl24/apps/web/src/lib/__tests__/rule-evaluation.test.ts` (D-057 blocks at lines 35-51, 124-302)
- `C:/Users/MLFLL/Downloads/nyc-zoning/ctl24/apps/web/src/app/property/page.tsx`
- `C:/Users/MLFLL/Downloads/nyc-zoning/ctl24/docs/RENDER_INTERNAL_WEB_DEPLOY_CHECKLIST.md` (§2a)
- `C:/Users/MLFLL/Downloads/nyc-zoning/ctl24/project-control/reports/M5-T026-producer-report.md`
