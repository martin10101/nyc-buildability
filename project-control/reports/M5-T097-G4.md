# M5-T097 — G4 test-adequacy review, FIRST ROUND (qa-engineer "qa-t097", read-only)

> Transmission history: pinned at 83f86ce0, delivered as three SendMessage parts (1/3..3/3) ending with END-OF-REPORT;
> no truncation. Joined verbatim by the orchestrator (transport wrapper tags removed only). Verdict PASS on the first-round
> material (recorded while the task is in rework after the G3/G5 FAIL on the O(n^2) splitter); ADVISORY-1 (exact-edge
> max_lines / max_line_chars probes) is folded into the rework; this reviewer re-attests at the rework identity.

---

M5-T097 G4 independent test-adequacy review (qa-engineer, read-only). Part 1/3.

PIN: started at HEAD 83f86ce03607921b51f4bc1836088726239fc6cb (== expected 83f86ce0). Read-only; in-memory mutation via python stdin only (Write + shell file-writes blocked for this reviewer); no repo/ledger/memory writes.

BASELINE: `python -m pytest tests/drawings/test_dxf_reader.py tests/drawings/test_dxf_roundtrip.py -q` -> 56 passed (28 pre-existing reader + 21 new reader + 7 round trip). AS-5 compatibility: the 28 pre-existing tests still pass.

SCOPE (git, read-only): material commit 9a281517 touches EXACTLY the 4 allowed paths (dxf_reader.py, test_dxf_reader.py, test_dxf_roundtrip.py, producer report). No forbidden path touched; app/cad/dxf_writer.py is UNMODIFIED (used read-only by the round trip); zero dependency/requirements change.

METHOD: identity-preserving in-process mutation — inspect.getsource -> targeted edit -> exec into the module's OWN __dict__ so the mutant sees real module globals; committed behaviour tests run as detectors (a detector that FAILS = mutation KILLED). Every mutation also carries a "must-stay-PASS" detector so an over-broad probe would be exposed.

=== PART A: producer committed mutations + the four M5-T086 G4 survivors ===
All KILLED, and every must-stay-PASS invariant held:
- A1 restore str.splitlines() -> control-char-verbatim test FAILS (KILLED).
- A2 M3 `not isfinite`->`isinf` -> nan-probe FAILS; inf-probe stays PASS (M3 survivor now KILLED, precisely).
- A3 M4a LWPOLYLINE bulge `!=0`->`>0` -> neg-bulge FAILS; positive-bulge stays PASS (M4a KILLED).
- A4 M4b VERTEX bulge `!=0`->`>0` -> neg-vertex-bulge FAILS (M4b KILLED; that path had no test before).
- A5 M5 closed `&1`->`bool(flags)` -> flag-128 FAILS; flag-129 stays PASS (M5 KILLED; the 128/129 pair discriminates).
- A6 clamp ceiling defeated -> clamp test FAILS (KILLED).
- A7 lenient str `_decode` -> str non-ASCII AND str sentinel both FAIL (KILLED).
- A8 pop INSUNITS 22/23/24 -> named-unit test FAILS (KILLED).
- AS-4 perturbed-ring + AS-1 splitlines-restore are committed self-mutation tests; re-run green.

All four M5-T086 survivors (M3/M4a/M4b/M5) are independently confirmed KILLED. (continued 2/3)

---

M5-T097 G4 review. Part 2/3 — my own idiomatic weakenings (>=4 required; 10 run).

KILLED by the committed suite:
- B1 CRLF treated as two terminators (drop LF absorption) -> CR/LF/CRLF-identity test FAILS. KILLED.
- B2 lone CR not a terminator (split on LF only) -> identity test FAILS. KILLED.
- B3 clamp applied to only SOME DxfLimits fields (skip max_bytes) -> clamp test FAILS. KILLED (per-field teeth).
- B4 str path checks non-ASCII but NOT the sentinel -> str-sentinel test FAILS while str-non-ASCII stays PASS. KILLED, and proves the two str guards are independently pinned.
- B5 INSUNITS 22 and 23 swapped -> named-unit test FAILS. KILLED (each specific mapping pinned).
- C1 max_vertices off-by-one (`>max`->`>max+1`) -> too-many-vertices test FAILS. KILLED (committed test feeds exactly max+1).
- C2 max_entities off-by-one -> entity-count test FAILS. KILLED (committed test feeds exactly max+1).
- C3 str-path max_bytes check dropped -> file-too-large test FAILS. KILLED (that test drives str input).

SURVIVED the committed suite (both are coverage gaps, NOT defects — production verified correct):
- B6 max_lines off-by-one (`count>max`->`count>max+1`) -> SURVIVED. The committed test_t097_as2_max_lines_refused uses 50 lines vs max 10 (way-over), so a +1 shift isn't caught. My exact-edge probe (8-line valid DXF, max_lines=7) DOES catch it and confirms the real code refuses at the exact edge (line max_lines+1). So: coverage gap only.
- B7 max_line_chars off-by-one -> SURVIVED, same reason (test uses a 50-char line vs max 10). Coverage gap only.

ORDERED-vs-SET (reviewer's "round trip comparing sets") — read-only check: the committed round trip asserts `lot.vertices == _LOT` (ordered tuple). Demonstrated: for the correct read-back ordered `==` is True; under a hypothetical reorder, ordered `==` is False (caught) while `set()==set()` is True (blind). So the committed assertion is correctly order-sensitive; a set-based one would be strictly weaker. Vertex order is ALSO independently pinned by test_as1_full_drawing_yields_typed_primitives. Not a gap.

Note the asymmetry: max_vertices/max_entities ARE pinned at the exact edge (C1/C2 killed); max_lines/max_line_chars are not (B6/B7). That is the sole coverage inconsistency. (continued 3/3)

---

M5-T097 G4 review. Part 3/3 — flake, vacuity, findings, verdict.

FLAKE (tracemalloc allocation probe): LOW risk, quantified.
- Committed probe ran 8/8 deterministic PASS.
- Streaming real_peak measured over 5 runs: max 32,632 bytes (~32 KB; steady-state ~5.8 KB) vs the 2,000,000-byte cap = ~61x headroom.
- Materialising mutant's list-pointer array alone is 1e6*8 = 8,000,000 bytes (structural, version-independent; "0" is interned so only the list array counts) vs required real*3 (~96 KB) = ~245x margin.
- No version-dependent ballooning on the streaming path (only a ~500-entry pairs list before refusal). Deterministic enough for CI 3.12.

VACUITY: clean.
- The clamp/ceiling tests assert BY-VALUE literals (e.g. `== 64*1024*1024`), not the imported `_LIMIT_CEILINGS` constant, so they are not tautologies; B3 proved per-field teeth.
- nan/neg-bulge/flag-128/str-sentinel/insunits/round-trip probes each shown to redden under their bug (Parts A/B). flag-128 + flag-129 discriminate only as a pair (129 is closed under both correct code and the bool(flags) mutant — 128 is the discriminator; both are present).
- Round-trip ordered `==` is genuinely order-sensitive (shown above); it asserts the value the perturbation displaces.

FINDINGS:
BLOCKING: none.
ADVISORY-1 (coverage): add an exact-edge (max_lines+1 / max_line_chars+1) refusal probe for max_lines and max_line_chars to match the edge pinning already present for max_vertices and max_entities. A +1 off-by-one currently survives (B6/B7); production is correct (my edge probe confirms), and these are safety caps where ±1 is immaterial and the way-over tests do fire — hence advisory, not blocking.
ADVISORY-2 (informational, G1 scope): INSUNITS 22/23/24 names are marked [recalled-verify] in source; the tests correctly pin the map (B5 has teeth) but the factual unit-code correctness is a data-contract (G1) matter — already corroborated by M5-T081-G1 (0-24 range, "22-24 = US Survey Inch/Yard/Mile"). No G4 action.

Every packet-required/named mutation and all four M5-T086 survivors are killed; scope/deps/compatibility verified; the surviving mutants are on CORRECT code and are coverage adds only.

M5-T097 G4 VERDICT: PASS

END-OF-REPORT
