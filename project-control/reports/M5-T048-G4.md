# M5-T048 G4 — independent QA review (qa-engineer, saved VERBATIM from the agent return)

M5-T048 G4 QA GATE — VERDICT: PASS (one ADVISORY; zero BLOCKING). Reviewed at pinned HEAD 15c1b3a4.

BASIS: All six permitted suites reproduce green at HEAD, matching the CI run 35439235554 all-green claim; the material (schema, proposal.py, contract.py, tests, fixtures, generated TS) is byte-identical between the CI-green head 149fab76 and current HEAD (git diff shows only project-control reports/ledger differ). Negative-space coverage binds every acceptance invariant, fixtures are correctly classed by defect layer, and the test changes are net-additive in rigor. One advisory: a correct defense-in-depth sub-branch of the ring-simplicity predicate has no mutation-binding test.

1. EXECUTED SUITES (services/api cwd for ruff+scenario; repo root for the rest):
 - ruff check . -> All checks passed
 - pytest tests/scenario -> 527 passed
 - generate_ts_types.py --check -> OK (scenario TS byte-identical)
 - pytest packages/contracts/scripts/tests -> 29 passed
 - modularity_check --check -> exit 0; proposal.py NOT on the warn list
 - validate_contracts.py -> "Checked 11 schema file(s); 0 failure(s)" (exit 0)
 All match the expected counts.

2. NEGATIVE-SPACE COVERAGE — every ENUMERATED invariant has a binding (mutation-failing) test:
 - ring closure: test_open_ring_refused (asserts "not closed") + contract test_open_ring_block...
 - self-intersection incl. bowtie: test_self_intersecting_outline_refused (real bowtie, asserts "self-intersecting") + contract test
 - non-finite/negative heights: test_negative_height/_zero_height/_non_finite_height/_nan_height_refused + test_vertex_non_finite_refused
 - wrong srid: test_wrong_srid_refused + schema-layer test_schema_rejects_outline_srid_not_2263
 - vertex/level ceilings: at-ceiling AND over-ceiling tests for all four (MAX_OUTLINE_VERTICES/MAX_LEVELS/MAX_FLOOR_COUNT/MAX_EXTERIOR_WALLS)
 - level-count mismatch: test_level_index_gap/_duplicate/_single_level_index_must_be_zero + contract test_level_count_mismatch...
 - coordinate bounds: test_vertex_out_of_nyc_bounds_refused (feeds a 4326 lon/lat pair)
 Plus duplicate-non-closing-vertex, wall range/distinct/unique/empty-id, provenance kind/author/parent-type, input-not-mutated, returns-none.

 ADVISORY A1 (proposal.py:170-171, the `cross == 0 and dot < 0` collinear-reversal branch of _ring_is_simple): NO binding test. Proven by scratchpad mutation — a degenerate zero-area collinear "triangle" (n=3) is refused ONLY by this branch; with it removed, the non-adjacent-segment branch (part 3) cannot fire (a triangle has no non-adjacent edge pairs) and the shape validates. Every geometry test uses a bowtie (binds part 3) or an open ring, so this branch is unbound. The code is correct and this is defense-in-depth on nonsensical geometry, and it is outside the task's enumerated invariant list — hence ADVISORY, not BLOCKING. Recommend one added test: a collinear/spike outline asserting refusal at proposed_massing.outline.

3. FIXTURE-CLASS INTEGRITY — clean:
 - invalid/proposed_massing_negative_height.json: schema-rejected for its STATED defect — CI/local both report "$.proposed_massing.levels[0].floor_to_floor_ft: -10.0 is not greater than exclusiveMinimum 0"; carries _expected_failure.
 - semantically_invalid/proposed_massing_open_ring.json: schema-VALID (test asserts zero iter_errors), refused by validate_scenario_document at proposed_massing.outline; only defect is first!=last; _expected_failure present.
 - semantically_invalid/proposed_massing_self_intersecting.json: schema-VALID, closed ring (first==last), refused at proposed_massing.outline for the bowtie only; _expected_failure present.
 - exact-2 count guard on the semantic class (test_there_are_the_required_fixtures); both valid 1.1.0 fixtures satisfy every valid-fixture invariant (never-verified, input-by-reference, unused_floor_area section) via the parametrized suites.

4. 1.0.0 REGRESSION — nothing weakened:
 - All 4 pre-existing valid + 3 pre-existing invalid scenario fixtures BYTE-UNTOUCHED (git diff vs baseline be0a7062 empty).
 - Schema change is purely additive: the only "-" lines are the contract_version enum ["1.0.0"] and its description, re-added as ["1.0.0","1.1.0"] with expanded text; proposed_massing is OPTIONAL (test_proposed_massing_is_optional_not_required); legacy 1.0.0 doc without block validates unchanged; a block on a 1.0.0 stamp is refused at contract_version. contract.py threading is inert when the block is absent.

5. TEST ADDITIONS vs baseline be0a7062:
 - test_scenario_proposal.py: entirely NEW file — all additive.
 - test_scenario_contract.py: the ONLY removed lines are the old generic test_invalid_fixture_rejected's schema-only assertion (iter_errors), REPLACED by a whole-gate assertion (validate_scenario_document) over invalid/ + semantically_invalid/ plus an _expected_failure-presence guard. NOT a weakening: per-fixture schema-layer rejection is still pinned by the dedicated tests (test_invalid_verified_fixture_fails_on_coverage_enum, ..._embedded_profile..., ..._missing_field..., test_proposed_massing_negative_height_fixture_fails_schema_validation) AND independently by the CI contracts script. Net rigor increased.

6. CI CONSISTENCY: run 35439235554 (all-green at 149fab76) validly covers HEAD — material byte-identical between the two heads; my local reproductions of the exact CI steps match one-for-one.

ADVISORY A2 (orchestrator decision, not a QA defect): the producer report's own note that AS-3's literal clause "the matching invalid fixtures fail schema validation" is unsatisfiable for the two geometry defects (JSON Schema cannot express ring-closure/non-self-intersection). The production contract gate refuses all three fixtures; the residual is only acceptance-wording. Flagging per the packet's open AS-3 reconciliation item; it does not affect this PASS.

Note: my agent-memory Write was blocked (read-only reviewer) — does not affect the verdict.
