# M5-T036 G5 review — security-reviewer return (VERBATIM; two-part transport)

Reviewer agent: security-reviewer (spawn rev-g5-m5t036), read-only, pinned HEAD
657b238dce8af8df1bfff2e22e454caab1749413. Transport note: the agent-return channel truncated
the first delivery mid-finding (5); the orchestrator requested the remainder from the same
resumable agent, which resent from the exact truncation point. Part 1 ends and Part 2 begins at
the marked splice; the two parts concatenated (dropping the duplicated splice words "replaces a
previously **raw, unvalidated** in") are the complete verbatim return. No content edited.

## Part 1 (verbatim, first delivery 2026-09-17T23:42:15Z)

G5 review complete. HEAD matches the pinned SHA, forbidden paths are clean, and every outbound link on the touched surfaces routes through the validated helper. Here is my report.

---

## G5 Security/Privacy Review — M5-T036

**Verdict: PASS**

**Scope reviewed:** cherry-pick `657b238d` (verified `git rev-parse HEAD` == `657b238dce8af8df1bfff2e22e454caab1749413`). Read-only; no writes performed. Material production files: `apps/web/src/components/architect/ZoningContextPanel.tsx` (new), `apps/web/src/components/architect/PropertyOverview.tsx`, `apps/web/src/components/address/AddressConfirmCard.tsx`. Reference (unmodified, consumed by import): `apps/web/src/lib/provenance-link.ts`, `apps/web/src/lib/bbl.ts`, `apps/web/src/components/property/ProvenanceDisclosure.tsx`, `apps/web/src/components/property/ZoningSection.tsx`.

### Mandate findings (all PASS, evidence-bound)

**(1) Outbound-link discipline — PASS.** Every ZoLa link on the touched surfaces is built only through `zolaLotUrl` (`apps/web/src/lib/provenance-link.ts:53-56`): constant prefix `ZOLA_LOT_PREFIX = "https://zola.planning.nyc.gov/bbl/"` concatenated with a token gated by `BBL_PATTERN = /^[1-5][0-9]{9}$/` (anchored; the interpolated token is provably exactly 10 digits, so no path/query/fragment/scheme injection is reachable), returning `null` → no link. Confirmed at `ZoningContextPanel.tsx:52`, `PropertyOverview.tsx:58`, `AddressConfirmCard.tsx:136`. `git grep "zola\.planning\.nyc\.gov"` over `apps/web/src` returns matches only in test files and the lib helper — **zero raw template-string ZoLa URLs remain in any touched production file**. No server- or profile-derived string reaches an href except the validated BBL token. The G5 F-1 constant-prefix discipline from M5-T032 is preserved.

**(2) `target="_blank"` / `rel` pairing — PASS.** All three touched components pair `target="_blank"` with `rel="noopener noreferrer"` (`ZoningContextPanel.tsx:50-58`, `PropertyOverview.tsx:58`, `AddressConfirmCard.tsx:136-138`). The reference `ProvenanceDisclosure.tsx` (newly reached via the panel) pairs it on every anchor (lines 47, 48, 82-86).

**(3) No new network path / api change / secret exposure — PASS.** `git grep` for `fetch(|XMLHttpRequest|axios|.ajax` in the three components: NONE. `git grep` for `process.env|NEXT_PUBLIC|secret|service.?role|api.?key|password|bearer`: NONE. No contract/type change (`apps/web/src/lib/**` is in `forbidden_paths` and `git show --name-only` confirms no forbidden path is in the commit — the `zolaLotUrl` helper is consumed by import only). No service-role key or env value enters the web bundle.

**(4) Rendered values are escaped text — PASS.** `git grep "dangerouslySetInnerHTML"` in `apps/web/src`: only test assertions and a comment; **none in production**. The panel renders profile designation values via `formatValue(view.value)` into React text nodes (`ZoningContextPanel.tsx`), and `ProvenanceDisclosure` renders all captured metadata as escaped text (`request_url` is reduced to a host string via `urlHost()` at `ProvenanceDisclosure.tsx:98`, never an href). No URL is constructed from any display value.

**(5) AddressConfirmCard prefix/encodeURIComponent removal is not weaker — PASS.** Old path rendered `href={` + backtick + `${ZOLA_BBL_URL_PREFIX}${encodeURIComponent(canonicalBbl)}` + backtick + `}` guarded by `canonicalBbl` (from `validateBblInput().ok`, i.e. exactly `/^[1-5][0-9]{9}$/`). New path renders `href={zolaUrl}` where `zolaUrl = zolaLotUrl(canonicalBbl)`, which applies the identical pattern. For every input class the output is identical: a valid canonical BBL yields the same string (encodeURIComponent on pure digits is a no-op), and a `null`/non-canonical BBL rejects in both (old: falsy guard; new: `zolaLotUrl` returns `null`, honest absent text at `AddressConfirmCard.tsx:145-149`). The replacement is equivalent-or-stronger (adds a redundant validation layer), never weaker. Separately, `PropertyOverview.tsx:58` [SPLICE →]

## Part 2 (verbatim, remainder resent 2026-09-17T23:42:50Z)

replaces a previously **raw, unvalidated** interpolation (`` `https://zola.planning.nyc.gov/bbl/${bbl}` ``) with the validated helper — a genuine defense-in-depth improvement, the core DB-005 fix.

### Other G5 domains
Cross-tenant isolation, RLS, service-role secrecy, private storage, upload controls, SSRF, and log redaction are **not applicable** to this change surface: it is a pure client-side display component plus a link-helper unification, with no server, database, storage, upload, logging, or LLM-input path introduced or modified. Prompt-injection is not in scope here — designation values are displayed, not fed to a model.

### Observations (informational, no action required for PASS)
- `PropertyOverview.tsx:58` renders nothing on the `null` BBL path (`: null`), whereas `ZoningContextPanel` and `AddressConfirmCard` render an honest "absent" note. This is a UX-consistency nuance for the design director, not a security matter.
- The redundant `bbl.length === 10` check alongside the anchored `BBL_PATTERN` in `zolaLotUrl` is harmless belt-and-suspenders.
- Internal navigation hrefs (`propertyHref(...)` Links; `/property/confirm?bbl=...` with `encodeURIComponent`) are relative-route, validated/encoded, and unchanged by this packet — no SSRF/open-redirect surface.

**Honesty note:** web runtime behavior (test pass/fail) is UNVERIFIED — thin client, no local vitest; CI on the pushed head is the executable authority per the packet. My review is a static read of the pinned diff and its imported contracts; all conclusions above are bound to the cited file:line evidence and grep output.

**Verdict: PASS** (no findings above informational).
