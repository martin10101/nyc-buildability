# M5-T118 — G1 format-authority review (data-contract-verifier "g1-t118", read-only)

> Transmission history: pinned at a120220e (material 992c47b1), delivered as 3 SendMessage parts ending with
> END-OF-REPORT; no truncation. Joined verbatim by the orchestrator (transport wrapper tags removed only). Verdict PASS; ISO 32000-1
> web-verified; ADVISORY A (an inline image with a /DP dictionary is refused, not skipped), B (three citation nits), C (a prose nit).

---

M5-T118 G1 data-contract/format-authority review — PART 1/3 (g1-t118, READ-ONLY). PIN: HEAD a120220e; material 992c47b1 (cherry-pick of wt-m5t118 9378890f). Verdict in Part 3.

SCOPE/PROVENANCE: 9 files, all within M5-T118.json allowed_paths; the forbidden read-only test_pdf_object_streams.py is UNTOUCHED (44 tests green). HARNESS (orchestrator's ctl24-pointed shim, cwd services/api): the 5 M5-T118 suites = 147 passed; test_sheet_p3_features 20/20. Full tests/drawings = 271 passed / 1 FAILED, the one failure being test_dxf_reader.py::test_t097_...double_find_reddens_time_guard (measured 7.9x vs an 8.0x TIMING threshold) — an unrelated flaky perf-mutation test in the DXF reader, not M5-T118 code. CI (3.12) is the authority.

CITATIONS VERIFIED CORRECT (web-corroborated vs ISO 32000-1):
- §8.9.7 inline images (BI/ID/EI): ID is followed by a single whitespace, the next byte is the first data byte, data ends at EI. The code consumes EXACTLY one ws (sheet_inline_image.py:117-121, data_start=id_end+1). I empirically confirmed a leading-whitespace first sample byte (0x20) is counted as data, NOT dropped — the common pdf_oxide-#1003 bug is absent. ✓
- §8.7.4.2 `sh` = paint a shading, filling the current clip (colour, not linework): skipping it for geometry extraction is legitimate (sheet_interpreter.py:~429, _SKIP_SHADING). ✓
- §8.9.6.2 "Stencil Masking": an image mask is 1 component @ 1 bit — CORRECT citation (sheet_inline_image.py:196). ✓
- Unfiltered length ceil(W*components*BPC/8)*H, each row byte-aligned: the MATH is web-verified correct (sheet_inline_image.py:208-209). ✓
- §8.5.2 path construction: item 2's "l with no current point" after a paint op is a spec-correct refusal (no current point survives a painting operator). ✓

(continued 2/3)

---

M5-T118 G1 — PART 2/3: citation corrections + the one substantive finding.

CITATION NITS (ADVISORY — every value/formula/set is correct; only the pointer is off; two sit inside [recalled-verify] markers, which is exactly my job to resolve). Authoritative ISO 32000-1:2008 §8.9 map (verified against the ISO DIS 32000 contents): 8.9.3 Sample Representation; 8.9.5.1 General (Table 89 = image-dict entries incl. permitted BPC); 8.9.5.2 Decode Arrays; 8.9.6.2 Stencil Masking; 8.9.7 Inline Images (Table 92 = KEY abbreviations; Table 93 = additional/VALUE abbreviations = colour spaces AND filters).
- sheet_inline_image.py:17 and :186 cite "§8.9.5.2" for the row-byte-alignment rule — WRONG (8.9.5.2 is Decode Arrays). Correct = §8.9.3 "Sample Representation". (Both [recalled-verify].)
- :67 cites "§8.9.5.2" for permitted /BPC {1,2,4,8,16} — WRONG. Correct = §8.9.5.1 / Table 89 (inline-image key alias: Table 92). (NOT marked.)
- :84 attributes the colour-space abbreviations (G/RGB/CMYK/I) to "Table 92" — WRONG; they live in Table 93, the SAME "additional abbreviations" table the code already calls Table 93 for filters (docstring :6-7). Internally inconsistent. (NOT marked.)
The abbreviation SETS, component counts (G1/RGB3/CMYK4/I1 + Cal*/Lab full names), and BPC set are all correct, and a wrong CS still fails safe via the EI-position check.

FINDING A (ADVISORY, not blocking, but UNDISCLOSED): an inline image whose dictionary carries a DecodeParms DICTIONARY value (/DP <<...>>) — the standard shape for CCITTFax (/F /CCF /DP <</K -1 /Columns.../>>) or Flate-predictor inline images, both common in SCANNED architect sheets — is REFUSED, not skipped. _parse_dictionary (:130-182) has no `<<>>` value handler (the strict lexer rejects `<<`) → "malformed inline-image dictionary before ID"; _skip_array (:289) likewise refuses a dict inside an array-of-dicts /DP. Empirically confirmed:
  /F /CCF /DP<<>> → REFUSE inline image; /F /Fl /DP<<predictor>> → REFUSE; array-of-dicts /DP → REFUSE; baseline /F /Fl no DP → SKIPS.
FAIL-SAFE (typed refusal, never wrong geometry) and NO regression (all inline images refused pre-M5-T118); corpus item 4's inline images skipped fine. But it partially misses DB-055(d) "skip inline images" for a realistic class, and the report's refusal enumeration ("length mismatch / over-cap / no-ID") omits it. RECOMMEND disclose as a DISCOVERY + additive follow-up: when /F is present the DP structure is irrelevant to the skip, so the parser need only CONSUME a `<<>>` / array-of-dicts value for balance, not interpret it.

(continued 3/3)

---

M5-T118 G1 — PART 3/3: budgets, honesty, VERDICT.

BUDGETS (AS-1) — coherent and bounded. Per-page (reset by begin_page) MAX_CONTENT_OPERATORS 200k / MAX_PATH_POINTS 500k / MAX_PAGE_DECODED_BYTES 32 MiB; per-document ceilings MAX_DOCUMENT_OPERATORS 20M / MAX_DOCUMENT_PATH_POINTS 50M / MAX_TOTAL_DECODED_BYTES 128 MiB — code (sheet_reader.py:146-153; sheet_objects.py:~290-311) matches the report's table exactly. Page-vs-document refusals are DISTINCT typed features ("operator count / path points / page decoded bytes" vs "operator ceiling / path point ceiling / decoded bytes budget"), asserted separately in tests. The tighter of the page/doc point budgets is threaded into flatten_cubic (sheet_interpreter.py:326-347) so a degenerate curve stops at the budget, never 2^depth. Worst-case arithmetic is bounded (ops O(1) each; points capped; decoded bytes capped; the inline-EI scan runs over already-decoded content, ≤8 MiB/image, non-overlapping → linear). Minor prose nit: the "100x the per-page cap" line is exact for ops/points only (decoded is 32→128 MiB = 4x). Non-blocking.

SAFETY: refusal is a VALUE throughout; no sample byte decoded; dict ≤4096 B, data scan ≤8 MiB; a determinable length is validated against a required EI at the computed offset (strong fail-safe); the desync-guard mutation (an under-consuming skip → refusal, never wrong geometry) is committed and green. Skip counts surfaced on SheetPage/SheetDocument = honest non-geometry disclosure. Golden revisions (sh→zz in test_sheet_reader.py + the split_equivalence _OVERALL / refuse_unsupported_op recapture) are deliberate and correctly scoped (only that one case changed; verified in the diff).

REAL CORPUS (AS-4) honesty: "3 of 6 read fully" is appropriately bounded; item 2 (l with no current point, §8.5.2), item 3 (sh), item 4 (inline skips), items 5-6 (scan-only) are internally consistent and the § classifications are correct. Files sha256-verified, run twice, deleted; nothing committed. I did not re-fetch the corpus (out of scope/network); every format claim I CAN check holds.

BLOCKING: none. The format interpretation is correct, no field/unit/formula is mis-read, every ambiguity is a typed fail-safe refusal, budgets are bounded, provenance is disclosed.
ADVISORY: (A) undisclosed DP-dict / array-of-dicts inline-image refusal — disclose + additive follow-up; (B) three citation nits (§8.9.5.2→§8.9.3 for row alignment and →§8.9.5.1/Table 89 for BPC; Table 92→Table 93 for colour-space abbreviations); (C) the "100x" prose covers ops/points only.

M5-T118 G1 VERDICT: PASS

END-OF-REPORT
