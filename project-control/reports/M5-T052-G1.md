# M5-T052 G1 — independent data-contract review (data-contract-verifier, saved VERBATIM from the agent returns)

G1 DATA-CONTRACT REVIEW — M5-T052 (DB-031) — VERDICT: PASS

Reviewed read-only at HEAD 6e3e1a400450091dcdf0c7f3429e9dee1fdd5e6e (matches pin). No writes, no CLI, no network.

BASIS (one paragraph): The route is an honest EXISTING-SHAPE proof — it changes no contract schema and builds its own `condo_records` document verbatim from the ACCEPTED `CondoResolution`, reading only fields that exist on that dataclass. The five outcome literals are byte-identical on both sides of the boundary and pinned by a dedicated test on each side. Provenance is complete and honest-nullable, the substitution substrate-record and unit-BBL increment resolve through the accepted resolver's public seams, the timestamp fix is correct and complete for every field the client reads, records discipline holds (labelled unknowns, no computation, RECORD-class wording), key-absence is consumed faithfully, and the one shared `deriveCondoSurface` decision is monotone (channel only ADDS withholding; accepted profile guard byte-unchanged and authoritative). Two non-blocking advisories below.

FINDINGS

1. EXISTING-SHAPE PROOF — PASS (BLOCKING-clear). No contract-schema file touched: the three M5-T052 commits (365f492f material; 46bf1105 tagged consumer-sweep of report-view.test.tsx; a6f35a0f tagged timestamp fix) touch zero files under packages/contracts/ or services/api/app/_contract_schemas/, and no forbidden path (connectors/profile/spatial/scenario/address). (The wider 365f492f^..a6f35a0f name-only range lists proposal_validation.py etc. — those are DISJOINT interleaved M5-T053 peer commits, already disclosed in the source-sections/ci-evidence reports; not M5-T052's.) The document is the route's own report shape (condo_records.py:508-541), document_kind "condo_records". Provenance quintuple complete: source_id, dataset_ids, retrieved_at, dataset_version (SODA rows_updated_at), per-query {dataset_id, query_kind, request_url, retrieved_at, record_count, rows_updated_at} — condo_records.py:423-439, keys confirmed against dtm_condo_soda.py:577-586. Honest None when the source omits (:343-351), never fabricated.

2. TOKEN PIN — PASS (BLOCKING-clear). api literals condo_base_lot.py:75-79 == web literals condo-records.ts:43-47, identical five strings (not_condo_billing / resolved_single_base_lot / multi_lot_set / unresolved / error). Pinned on BOTH sides: api test_condo_records_api.py:540-549 (test_outcome_token_vocabulary_is_the_pinned_literal_set); web condo-records.test.ts:136-141 ("the web outcome literals EQUAL the api resolver OUTCOME_* strings"). Route emits resolution.outcome verbatim (condo_records.py:515), so all five cross intact.

3. SUBSTITUTION SUBSTRATE-RECORD + UNIT-BBL — PASS (BLOCKING-clear). _substitution_record (condo_records.py:442-460) records entered_bbl(input) vs analyzed_bbl(resolved_base_bbl) ONLY on resolved_single; None on multi/unresolved/error so a consumer physically cannot present a single analyzed lot for a non-single outcome. Unit path maps dtm_condo_soda.resolve onto the SAME vocabulary (_resolution_from_unit_result, :254-296), input_bbl stays the entered unit BBL so entered-vs-analyzed is honest for units too. Tests: :264 (billing single asserts analyzed!=entered), :438/:467/:494/:511/:523 (unit single/multi/key-absence/unresolved/error). Web field names match (entered_bbl/analyzed_bbl/note, condo-records.ts:248-256).

4. TIMESTAMP FIX (a6f35a0f) — PASS (BLOCKING-clear), one ADVISORY. boundedTimestamp (condo-records.ts:207-213) preserves ISO charset incl. ':' and '+'; applied at all three sites the client reads — query retrieved_at + rows_updated_at (:267-268) and provenance-root retrieved_at (:294); datasetVersion derives from rows_updated_at (:289-290) so it rides boundedTimestamp too. I swept every boundedToken/boundedText use: all remaining boundedToken targets are token-vocabulary (BBLs, dataset_id, source_id, condo_key/number, error_type, correlation id, state/outcome) whose [A-Za-z0-9._-] charset is correct; boundedText targets are prose. No other ISO-timestamp field routes through boundedToken. (Note: the api document also carries per-base-lot provenance.retrieved_at, but the web baseLotRecords reads only bbl + recorded_zoning, so no corruption surface there.)
   → ADVISORY-4a (forward-compat, NOT blocking today): baseLotRecords routes record.recorded_zoning through boundedToken(...,32) (condo-records.ts:242). NYC special mixed-use district codes carry a '/' (e.g. "M1-5/R7-2"), which boundedToken drops → silent corruption — the SAME sanitizer-boundary class as the timestamp bug. Harmless NOW because _recorded_zoning_for always returns None (recorded_zoning is universally null; the RECORDED branch is unreachable in production), but it will corrupt slash-bearing districts the moment the named out-of-scope zoning-propagation dependency lands "with no shape change." Route it to the recorded-zoning propagation backlog item so it isn't missed then.

5. RECORDS DISCIPLINE (D-073-R006) — PASS (BLOCKING-clear). recorded_zoning is a RECORD with an explicit status label; null → status "unknown" (labelled, condo_records.py:391-393), rendered "not recorded (unknown)" (honest absence, never fabricated). The RECORDED path is data-driven via _recorded_zoning_for (:368-377, returns None today; dependency named in RECORDED_ZONING_DEPENDENCY :141-150; forward-compat proven by monkeypatched test :414). No aggregation/computation in the channel — `assert "far" not in json.dumps(body)` on single (:306) and multi (:339); CondoRecordsChannelSection uses RECORD-class headings/wording only ("City records for this condo", "Recorded base lot for this condo"), zero allowance vocabulary (PropertyOverview.tsx:219-261).

6. KEY-ABSENCE (DB-029c) — PASS (BLOCKING-clear). The route reads resolver output via dataclass attributes and provenance .get() — no explicit-null assumption reintroduced; _dataset_version uses entry.get("rows_updated_at") so an absent key is honest None (:347-351). Web reads record.rows_updated_at/retrieved_at → boundedTimestamp(undefined) → null. Explicit route-level key-absence test: test_unit_bbl_key_absence_db029c_shape_tolerated (:494) → resolved_single tolerated.

7. GUARD COHERENCE / MONOTONE-WITHHOLD — PASS (BLOCKING-clear). ONE derived state deriveCondoSurface (PropertyOverview.tsx:197-214) consumed by BOTH the screen (:283) and the brief (ReportView.tsx:46), each rendering the SAME CondoRecordsChannelSection UNDER DevelopmentLimits (the fail-safe). withholdAllowances = profileWithholds || channelWithholds (monotone; channel only ADDS; :206). Accepted profile guard condoWithholdsAllowances/deriveCondoDisplay byte-unchanged and authoritative (:143-167). Transport failure / route-absent → unavailable/non_condo, which NEVER withhold on their own (channelWithholdsAllowances, condo-records.ts:560-566) — an outage cannot blank every property's allowances. Disagreement → conflict, fails safe (withhold via the OR) and surfaces the honest condo-records-conflict notice (:253-257). AnalysisIdentityNotice byte-unchanged (confirmed: not in the material commit's file list).

ADVISORY-B (informational, not blocking): the task text + PROGRAM_KNOWLEDGE + your dispatch point to services/api/app/rules/condo_base_lot.py, but the accepted resolver actually lives at services/api/app/connectors/condo_base_lot.py (which the route imports correctly, and which forbidden_paths correctly lists under connectors/). Stale pointer only; no code impact. Worth correcting in the ledger notes so future consumers don't chase a missing path.

No BLOCKING findings. Contract is an honest existing-shape proof with a symmetric token pin and complete, honest provenance. PASS.

## Delta-attestation (verbatim)

YES — my G1 PASS CARRIES to e9abeaaf15ce9cff2c6405c96da8bbe8e2e31ef0. Independently verified: git diff --name-only 6e3e1a40..e9abeaaf = only the 5 report/control files (producer-report.md, source-sections.md, reports/M5-T052.json, state.json, tasks/M5-T052.json); zero .py/.ts/.tsx/contract-schema changes, so my entire data-contract review surface (condo_records.py, condo-records.ts, PropertyOverview.tsx, ReportView.tsx, both test suites, the resolver) is byte-identical at both heads. On finding 7: agreed with G3's precision — the guard is a semantics-preserving refactor (condoWithholdsAllowances delegating to the new deriveCondoDisplay), which I read in source at :165-167; "byte-unchanged" was loose wording on my part. It changes no G1 finding — my contract conclusions rest on verified behavior (profile guard authoritative; monotone fold, channel only ADDS withholding), not on literal byte-identity. All seven checks and the two advisories stand at e9abeaaf.
