# M5-T119 — G3 code review (code-reviewer "cr-t119", read-only)

> Transmission history: pinned at 25de64ae (material 4a631bf3; CI run 36110406806 green at 25de64ae), delivered as
> 2 SendMessage parts ending with END-OF-REPORT; no truncation (each part was queued twice, byte-identical; the first
> copy is joined). Joined verbatim by the orchestrator (transport wrapper tags removed only). Verdict PASS; D2 ruled
> acceptable and disclosed; two ADVISORY items (an inaccurate CSS comment on the grid floor, the D1 map-left order).

---

M5-T119 G3 REVIEW (part 1/2) — READ-ONLY

PIN: HEAD at start = 25de64ae2d413c1dcbccc0212c0e50a2c51fac97. CI run 36110406806 CONCLUDED = SUCCESS (web ✓ 1m17s, web-e2e ✓ 5m18s, all 18 jobs green; the maplibre-gl-shared.mjs + Node-20 annotations are pre-existing vendored-file/infra warnings, not failures). Material 4a631bf3 == cherry-pick 7ca2ad26 byte-for-byte, 12 files. `python tools/modularity_check.py --check` exit 0.

SCOPE DISCIPLINE — PASS. `git show 4a631bf3 --stat` = exactly the 12 T119 files. No lib/, ArchitectEntry.tsx, ReportView.tsx, MaxEnvelopePanel.tsx or layout.tsx touched. architect.css hunks touch only overview-grid / exception-strip / cap-line / shell-environment selectors; the ≤700px `.architect-environment{display:none}` rule was already present, unmodified.

D2 (key call) — ACCEPTABLE AND DISCLOSED; both facts verified in code:
1. Folding identity mismatch into the role=status strip WOULD downgrade it: AnalysisIdentityNotice.tsx:72 is `role="alert"`; the strip (OverviewExceptionStrip.tsx:43) is `role="status"`. Spec §5.3's own A-row requires "role=alert kept for identity withhold (A15)" AND "role=status for the issue strip (A04)" — the two cannot both hold if identity is folded, so the producer honored A15.
2. LoadedWorkspace nulls a mismatched/non-inspectable evaluation+scenario BEFORE PropertyOverview: ArchitectEntry.tsx:112-114 null on BBL mismatch / non-inspectable, then pass the nulled values at :136. The strip reads only `profile.conflicts/missing_inputs/reproducibility` — it structurally cannot see identity/incomplete. Both stay in dedicated regions rendered above the strip (ArchitectEntry.tsx:213-215; IncompleteEvaluationNotice is role=status at DevelopmentLimits.tsx:25). All five §5.3 categories are covered = strip(3) + A15/A05(2). Not a blocking gap.

PRESERVATION — PASS. Strip keeps the conflict/missing headings, `fieldLabel(item.field)` join and link labels "Review conflicting source values →" / "Review missing inputs →" with href `propertyHref(bbl,"issues")` verbatim vs the old PropertyIssuesSummary (PropertyOverview.tsx:34-47); additions only name the blocked effect. Stale keeps the exact sentence + a review clause, no link. PropertyIssuesSummary body is outside the diff (byte-unchanged) and still rendered by ReportView.tsx:98 (brief) and the scenarios view (ArchitectEntry.tsx:146). DraftHeadline keeps every text node — cap value, "FAR only" note, STATUS_LABELS gloss + raw `<code>{coverage_status}</code>` (DevelopmentLimits.tsx:42-52). No number computed in UI; STATUS_LABELS unchanged; no permitted/approved/maximum wording; §29/REQUIRED_DISCLAIMER not in diff. Contract predicates are real types (contract.ts:72,76,134,139) — no guessed schema.

---

M5-T119 G3 REVIEW (part 2/2)

ONE LIVE REGION PER EVENT — PASS. The strip is the ONLY new role=status and it CONSOLIDATES the former two `.architect-alert` role=status blocks into one. On the overview PropertyIssuesSummary is no longer rendered (PropertyOverview.tsx:363 renders the strip instead); no surface renders both the strip and PropertyIssuesSummary. Proven by unit test (`.architect-alert` count 0, "Unresolved data conflicts" appears once, one role=status "Active issues") and by e2e (`.architect-alert` count 0 on the loaded overview). Identity=role=alert and incomplete=role=status are distinct events, not duplicated. Shell phone strip is role=note (InternalBanner.tsx:8 is also role=note) — not a live region; e2e proves it `toBeHidden()` at 1280px and visible at 360px, so no desktop double with the topbar InternalBanner; its text is the M5-T115 paraphrase verbatim (ArchitectEntry.tsx:78 / ArchitectShell.tsx:47).

TESTS — ADEQUATE, behaviour-focused. Unit: typed DOM queries (`querySelector<HTMLElement>(".architect-cap-line")`), `as unknown as T` for the stale/delete probes, assertions on role/text/href/counts incl. the "one status region" and "no legacy alert" invariants and a ReportView-brief-unchanged case. e2e: real boundingBox geometry proves map-left + wider + side-by-side; `expectNoHorizontalOverflow` across viewports empirically backs the no-360px-overflow claim; the fold spec uses an EXACT-pathname route matcher and mutates the REAL profile response (no invented fixture/enum — clears the T070 trap). No fixed sleeps; no activeElement.textContent focus matching.

ADVISORY (non-blocking):
- architect.css comment on `.architect-overview-grid` says "Both minmax tracks keep a 0 floor so nothing forces horizontal overflow", but the LEFT track is `minmax(320px,1.25fr)` (280px ≤1200px) — a non-zero floor. Actual overflow-safety comes from the ≤950px single-column collapse, which the e2e proves. Comment accuracy only; no functional impact.
- D1 canvas order (map-LEFT) is a disclosed reading of spec §5.3/frame O-D (prose lists map first at ~55–60% but doesn't literally say LEFT); producer flagged OQ-2 for the owner. DOM/reading order now surfaces site-context before the limit matrix. Spec-conformant per the frame; disclosed. Not blocking.

CI: run 36110406806 completed success (all jobs). No failing tests to report.

M5-T119 G3 VERDICT: PASS

END-OF-REPORT
