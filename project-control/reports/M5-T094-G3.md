# M5-T094 — G3 code review (code-reviewer "cr-t094", read-only)

> Transmission history: pinned at a612652b, delivered as two SendMessage parts (1/2, 2/2) ending with END-OF-REPORT;
> no truncation. Joined verbatim by the orchestrator (transport wrapper tags removed only). Verdict PASS: the reviewer
> independently reproduced all 62 pinned golden digests from the PRE-split module (89128c62^), so post-split ==
> pre-split byte-for-byte. Advisories A1-A3 are non-blocking (routed at the accept seam).

---

M5-T094 G3 code review (read-only) — PART 1/2. PIN: HEAD a612652b at start; material commit 89128c62 is an ancestor and no later commit touches the 3 modules or the equivalence test (git log 89128c62..HEAD on those paths is empty), so disk == material commit.

METHOD (evidence run here): shim suite run_sheet_split_tests.py -> 46 passed (37 frozen + 3 equivalence/shape + 6 guard mutations), 0 failed. ruff on the 3 modules -> All checks passed, exit 0. modularity_check --check -> failures 0, exit 0. CI at a612652b (run 36025909989, api job) reported green in the packet.

GOLDEN NOT VACUOUS (independently verified — the central AS-2 risk): I reconstructed the PRE-split module from git (89128c62^:.../sheet_reader.py), loaded it under the bare-package shim, and ran the equivalence corpus's own run_corpus/digests against it. Result: all 62 per-case digests AND the overall digest (767766eb...3106) equal the pinned _GOLDEN/_OVERALL exactly — 0 mismatches, 0 missing. So the golden is genuinely pre-split behaviour; the suite asserts post-split == golden (46 passed) => post-split == pre-split byte-for-byte across curves, CTM, q/Q, forms (cycle/depth/memo), images, text, every refusal class, every budget, non-finite, multipage. AS-2 PASS.

AS-1 (API preserved): __all__ byte-identical pre vs post (diff empty). Every name the frozen suite rebinds resolves from the facade — read_sheet, sheet_refusal, _read_sheet, _flatten_cubic (sheet_reader.py:105), _concat_matrix (:104), _decode_stream (:87), _catalog_pages_root (:86), all MAX_* (:84-85,121-124), DEFAULT_FLATTEN_TOLERANCE (:127). 37 frozen tests pass unchanged. PASS.

PATCH-SURFACE THREADING (correct): in _read_sheet (sheet_reader.py:260-277) every patched name is read from the facade module globals AT CALL TIME and threaded into the _SheetInterpreter/_StreamDecoder instances, so a monkeypatch on the facade bites. _decode_stream's 3rd param changed _SheetInterpreter -> _StreamDecoder, but the frozen wrapper (test_sheet_reader.py:471) forwards it positionally with no attribute access, so it stays green — confirmed by the passing run.

(part 2 next)

---

M5-T094 G3 code review — PART 2/2.

AS-3 (modularity): source_lines (authoritative checker) sheet_objects 245, sheet_interpreter 584, sheet_reader 314 — each < 600. Runtime import DAG acyclic: facade -> {sheet_interpreter, sheet_objects, sheet_primitives}; sheet_interpreter -> {sheet_objects, sheet_primitives}; sheet_objects -> sheet_primitives. Only back-edge is TYPE_CHECKING-only (sheet_interpreter.py:57-58). modularity_check: no sheet_* warning (the pre-split 962-line JUSTIFY warning is cleared). PASS.

AS-4 (guards load-bearing): 6 mutation tests, each REAL (guard fires) + MUTANT (guard defeated -> behaviour changes): flatten-point budget, decoded-bytes budget, form depth, form cycle (asserts feature != 'xobject cycle'), top-level backstop (pytest.raises without it), finiteness gate. Each mutant targets the exact consuming namespace (sheet_interpreter._is_finite_point; _StreamRun._place_form; sheet_objects._StreamDecoder.charge_decoded) resolved at call time. All pass. PASS.

AS-5 (scope): git diff 89128c62^..89128c62 = exactly the 5 allowed paths. sheet_primitives.py and app/documents/ byte-untouched (numstat empty). Zero new deps (only math, zlib + app.*). Not wired (no importer of the 3 modules outside app/drawings + tests). PASS.

SPLIT SEAM (DB-055 b): correct — sheet_objects owns object-graph + stream decode + budgets + form memo; sheet_interpreter owns the _StreamRun operator interpreter. Disclosed deviation (coordinator _SheetInterpreter in the facade, sheet_reader.py:144-204) is ACCEPTABLE: it is document-orchestration that pairs coherently with the page-tree driver; responsibility split is clean, DAG acyclic, behaviour identical.

C1 SEAM (PDF 1.5+ xref/object streams): clean. The facade's single read_object_table() call (sheet_reader.py:249) is the integration point; object-graph resolution lives in sheet_objects (headroom at 245 SLOC), the natural home to delegate an object-stream resolver as a NEW module; forbidden pdf_xref.read_object_table stays untouched.

ADVISORY (non-blocking):
- A1: report rationale for the facade-resident coordinator ("so the runtime DAG stays acyclic", producer report Deviations #1) is inaccurate — moving it into sheet_interpreter would NOT cause a runtime cycle; it is what forces the TYPE_CHECKING back-ref. The real constraint is the <600 SLOC AS-3 sub-cap (interpreter would be ~657). Placement still fine; only the stated reason is off.
- A2: some previously module-level PRIVATE names no longer import from sheet_reader (_decode_contents, _resolved_int, _preview, various _*_KEY). None in __all__, none rebound by tests, no importer — AS-1 holds; noted only for future consumers.
- A3: report says modularity "warnings 25"; HEAD shows 27 (2 unrelated later tasks). Immaterial.

No BLOCKING findings.

M5-T094 G3 VERDICT: PASS
END-OF-REPORT
