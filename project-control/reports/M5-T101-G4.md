# M5-T101 — G4 test-adequacy review (qa-engineer "qa-t101", read-only)

> Transmission history: pinned at c2c4f523, delivered as two SendMessage parts (1/2, 2/2) ending with END-OF-REPORT;
> no truncation. Joined verbatim by the orchestrator (transport wrapper tags removed only). Verdict PASS: all 7 producer
> mutants and both DB-066 probes redden; broken re-exports redden. Advisories A1 (a multi-page advancing-clock deadline
> test) and A2 (a ceiling between one-page and two-page cumulative bytes) are routed to the wiring packet.

---

M5-T101 G4 test-adequacy review (read-only) — PART 1/2: mutation table. VERDICT in part 2.

PIN: HEAD = c2c4f523 at start (expected c2c4f523+; material commit 6a0f7f53). Baseline: `cd services/api && python -m pytest tests/connectors/test_building_footprints_arcgis.py -q` = 131 passed. Harness: in-process, scratch-only (never committed/written to repo — my Write/Bash-write are guard-blocked; fed via python stdin heredoc). It takes byte-exact `inspect.getsource` for `_collect_pages`/`fetch_context_buildings`, applies a minimal diff, re-execs in bf.__dict__ so free names resolve; guards mutated in the CONSUMING namespace (bf.*). CONTROL: rebinding the DEFINING namespace (geo.parse_footprint_geometry=None) is a FALSE survivor — confirms the connector uses its own bf.* binding and I mutated the right side.

PRODUCER'S 7 (all reproduced, all DETECTED = guard test reddened):
1 vertex-cap defeated (_geometry_vertex_count→0) — DETECTED
2 bytes-ceiling raise removed (_collect_pages copy) — DETECTED
3 _check_deadline → no-op — DETECTED
4 INTERACTIVE_MAX_ATTEMPTS=3 — DETECTED
5 _safe_correlation_id → identity — DETECTED
6 _CONTROL_CHAR_DELETE={} — DETECTED
7 _ground_attr → None — DETECTED

MY IDIOMATIC WEAKENINGS (6):
W1 corr-id strips LF only (keeps CR/NUL) — DETECTED
W4 UNTRUSTED_TEXT_FIELDS drops last_status_type — DETECTED
W6b zero-ground value 0.0 kept but the unverified flag dropped — DETECTED
W2 deadline checked ONCE before the loop (not before each page) — SURVIVED (advisory, part 2)
W7 cumulative-bytes ceiling reset per page (`=` not `+=`) — SURVIVED (advisory, part 2)

DB-066 PROBES:
(c) internal_error logs str(exc) → probe `..._secret_in_message_never_logged` DETECTED
(d) typed-refusal path logs ERROR → probe `..._typed_refusal_emits_no_error_record` DETECTED

SPLIT RE-EXPORT:
bf.classify_query_relation=None — DETECTED; bf.parse_footprint_geometry=None — DETECTED. AS-2 tests exercise the MOVED helpers through the connector's re-exports; a broken re-export reddens loudly (fetch-path + the identity test), and the acyclic check AST-greps the geometry module for the connector name.

Part 2 (survivor demos + vacuity checks + verdict) follows.

---

M5-T101 G4 review — PART 2/2: two ADVISORY survivors, vacuity checks, verdict.

ADVISORY (non-blocking) — two plausible idiomatic weakenings survive on the MULTI-PAGE dimension the requirements name. Production is CORRECT in both; only the tests are blind. Proven with LIVE mutants (behavior differs, so not a harness artifact):

A1 (DB-058 c, "checked before each page"): `test_pktc_wall_clock_deadline_stops_paging_typed` only covers a deadline already past before page 1 (plus a future deadline). Hoisting `_check_deadline` out of the loop (checked once) passes it. Demo — 2-page query, deadline 13:00 elapses between pages via an advancing clock: production → refused/deadline_exceeded after 1 page; once-check mutant → ok, 2 pages fetched. Recommend a multi-page advancing-clock deadline test.

A2 (DB-058 b, "cumulative … across pages"): `test_pktc_cumulative_bytes_ceiling_refuses_before_retention` sets the ceiling (10) BELOW one page, so it only proves "one oversized page is refused." A per-page mutant (`decoded_bytes =` vs `+=`) passes it. Demo — b1=4291, b2=2680, ceiling=5631 (between one page and the two-page sum): production → refused/cumulative_bytes_ceiling; per-page mutant → ok. Recommend a ceiling between one-page and two-page cumulative bytes.

Why ADVISORY not BLOCKING: the shipped code is correct (deadline inside the loop; bytes with `+=`); AS-2's explicitly-named mutants (cap removed, ceiling removed, deadline ignored) all redden; the connector is UNWIRED and these are defense-in-depth bounds. Natural home for both tests is the wiring packet (PKT-E) or a small follow-up.

VACUITY / HONESTY (all clean): honesty labels are HARDCODED literals, not imported-constant tautologies — the datum test asserts "NAVD88"/"lowest elevation…"/"centroid"/"neither is chosen" and the untrusted tuple as a literal (W4 caught constant drift); the zero-ground test asserts the DISPLACED value 0.0 plus the flag (P7 and W6b both caught); refusal() asserts buildings==[] so the vertex/bytes refusals also pin non-retention of records; interactive + deadline tests assert BOTH directions. DB-066(a) premise verified real: `_RETRY_AFTER_SAFE_RE` uses `.match()` with `$`, which matches before a trailing "\n", so "Foo\n" would pass the allowlist verbatim — the outright CR/LF strip closes it.

No BLOCKING findings: producer's 7 all redden, both DB-066 probes redden under faithful mutants, split re-exports redden, no vacuous/tautological guard test, no shipping defect.

M5-T101 G4 VERDICT: PASS

END-OF-REPORT
