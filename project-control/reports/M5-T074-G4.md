# M5-T074 — G4 test-adequacy review (backend-engineer, read-only)

> Transmission history: returned across four transmissions — Parts 1–2 (truncated inside F4
> at "`unexpected_error stage=fetch`, `ring_cr"), the F4-completion micro-chunk, the Part-3
> duty-(1) chunk, and a final message carrying duties (2)–(5), Part 4, and the verdict.
> Joined verbatim in order at each cut point. Saved verbatim by the orchestrator (transport
> entity-decoding only).

Review complete. All evidence gathered and independently reproduced.

**M5-T074 — G4 test-adequacy review (backend-engineer), PART 1/4: identity + evidence**

Pinned head `7c6ea4f0` verified at start. HEAD moved to `dc5a763e` mid-review (one disjoint peer commit, B-025/D-085 ledger work). All five review-surface blobs are byte-stable across the move, so this verdict holds at both heads:

- `services/api/tests/api/test_outline_bridge.py` → `55d2b365` (same blob the material commit `ea2cdb65` produced)
- `services/api/app/api/v1/outline_bridge.py` → `dfff5dc0`
- reports `M5-T074-producer-report.md` `747f201b`, `M5-T074-G4-mutant-capture.md` `b806248b`, task `M5-T074.json` `6d45ab69`

Commands I ran (read-only, cwd `services/api`, primary checkout, tree clean before and after):

- `python -m pytest tests/api/test_outline_bridge.py -q` → **42 passed in 4.81s**
- `python -m ruff check .` → **All checks passed!**
- `python .github/scripts/secret_scan.py …` → **secret-scan: PASS -- no findings** (the four `# secretscan:allow <justification>` pragmas match `PRAGMA` at `.github/scripts/secret_scan.py:45`; the sentinels are plain kebab strings that match no secret pattern anyway)
- `git status --porcelain -- services/` → empty after every probe (no production file was edited by me)

Instead of editing production, I applied four mutants **in-process only**, via `python -c` that rebinds `app.api.v1.outline_bridge._internal_error_500` and then calls `pytest.main(...)`. The route resolves that name from module globals at call time, so the rebind is equivalent to the orchestrator's source mutant while leaving the tree untouched.

**PART 2/4: numbered findings**

**F1 — CONFIRMED + STRENGTHENED (informational).** `project-control/reports/M5-T074-G4-mutant-capture.md` claims all four tests redden on a widened body. Reproduced with a *different* field name (`"debug_context": "MUTANT-A-novel-field"`, not the capture's `"detail"`): **4 failed, 38 deselected**. Restore-equivalent baseline: **42 passed**. A second mutant that leaked the exception type and sentinel text *into the `message` field only* (key set untouched) also gave **4 failed**. The capture's claims are accurate and its conclusion generalizes: widening is caught by key set, and message tampering by string equality.

**F2 — MINOR, unprobed leak channel — `test_outline_bridge.py:709-806` (oracle at :715-727).** Every leak assert reads `resp.text`, which is the **body only**. Mutant C added `X-Error-Detail: "RuntimeError: fetch-stage-leak-sentinel; internal-only-src; nan"` to the response headers, leaving the body byte-identical → **4 passed, 38 deselected**. A header-borne leak of the exception type, the sentinel, the internal source id and `nan` is invisible to all four tests. Real exposure today is low (`_json`, `outline_bridge.py:555-558`, sets exactly one header), so this is a named gap, not a defect.

**F3 — MINOR, unprobed leak channel — oracle at `:725-727`.** `correlation_id` is only checked for body/header *agreement*, never for shape. Mutant D returned `correlation_id = uuid4().hex + "|bbl=1000160001|file=C:/srv/app/api/v1/outline_bridge.py|user=ops"` in both body and header → **4 passed**. An internal datum other than the injected sentinel can ride that field undetected. One line closes it: `re.fullmatch(r"[0-9a-f]{32}", body["correlation_id"])`.

**F4 — MINOR, branch-identity drift risk — `:733-806`.** All three `_internal_error_500` call sites return an identical body, so the tests contain no discriminator; the two `ring_crs_mismatch` tests cannot distinguish `:717` from the fetch-stage `:713`. I confirmed the tests are correct **today** with a line-number spy on `_internal_error_500`: `713 / 717 / 717 / 824`, one hit each, matching each test's stated intent exactly. The risk is future drift (e.g. if ring construction ever raised first, the crs tests would silently slide onto `:713` and stay green). Each branch logs a distinct line (`unexpected_error stage=fetch`, `ring_crs_mismatch`, `serialization_unsafe`), so a `caplog` assert would pin it.

**PART 3/4: assessment per duty (1-3)**

**(1) Mutant decisiveness for AS-2 — YES, and the shared-oracle design generalizes.** All four tests route through `_assert_bounded_internal_error`, whose `assert set(body) == _GENERIC_500_KEYS` (`:724`) compares the whole key set, not a probed field. All three production branches build their response from the single `_internal_error_500` helper (`outline_bridge.py:605`; call sites `:713/:717/:824`, and grep confirms there are exactly three). Any widening at that helper therefore reddens all four regardless of field name — which F1's novel-name mutant demonstrates rather than assumes. Message tampering is caught separately by `:723`. The AS-2 claim is mutation-backed, not assertion-only.

**(2) Leak-absence sufficiency per branch — sufficient for the body, with two named channels outside it.** Fetch stage (`:733-743`): the injected `RuntimeError(secret)` message, the type name and `Traceback` are the right probes for the only thing that branch can leak. The two crs branches raise nothing, so the producer correctly moved the probe onto what the 200 path *would* echo — `source_detail` contents and `source_id`, which `outline_bridge.py:783` spreads into `source_display_ring`. The tail guard probes the sentinel, the non-finite value and the `json` exception type, which is the right three. Within the body, exact-shape plus message-equality already make any body leak impossible, so the per-test sentinels are correct belt-and-braces. The channels left unprobed *within* these four branches are response headers (F2) and the `correlation_id` value (F3).

**(3) Determinism — low flake risk.** Fully offline: both ring seams are replaced through `app.dependency_overrides` (`:121-141`), so no connector or network is touched; the 42-test suite runs in 4.81s. Each test builds its own `FastAPI` app and `monkeypatch.setenv` is function-scoped, so there is no cross-test state or env bleed. Nothing time-, order- or randomness-dependent is asserted against a constant: `correlation_id` is compared only to itself, `set()` comparison is order-free, `float("nan")` is deterministic. The tail-guard test depends on the correspondence fit succeeding, but it reuses `_DISPLAY_PTS`/`_AUTH_PTS`, whose fit is already pinned green by the pre-existing happy-path tests; if that ever drifted the test would fail loudly with a 422 body, not flake.

**(4) Oracle robustness — no false-positive path found.** Covered in F5: the sentinels are unique kebab strings, the `nan` check cannot collide with `uuid4().hex` (the hex alphabet has no `n`) or with the fixed message, and the `RuntimeError`/`ValueError`/`Traceback` checks are case-sensitive but redundant given the exact-shape and message-equality asserts. The one brittleness is `nan` as a substring, and it errs toward a loud failure.

**(5) Coverage within DB-045(c) — complete.** The backlog row (`docs/DISCOVERY_BACKLOG.md:167`, item (c)) asks for the server `(500, internal_error)` branches to be proven, "ring_crs_mismatch seam test recommended". All three `_internal_error_500` call sites are now driven live, with the recommended seam exercised on **both** sides, and the spy in F4 confirms each test truly lands on its intended line (`713 / 717 / 717 / 824`, one hit each). The tail-guard test also substantiates the "typed 500, not an untyped ASGI 500" claim, since an untyped ASGI 500 returns plain text and `resp.json()` would raise. AS-1/AS-2/AS-3 are satisfied; AS-4 holds (additions-only `+111`, two allowed paths, zero production edits, ruff clean, scoped suite green).

**PART 4/4: routing and verdict**

Worth recording: this oracle is **stronger** than the T068 precedent it mirrors (`services/api/tests/api/test_max_envelope_api.py:363-384`), which asserts the message and sentinel but not the exact body shape. F2 and F3 are pattern-level gaps **inherited** from that precedent, not T074 regressions — which is why I route rather than require them.

**Routing (not required for this gate).** F2 and F3 (the response-header and `correlation_id` leak channels) plus F4 (a `caplog` branch pin) belong with DB-045 items (i)/(j), already routed to the "next bridge/editor test touch". They apply equally to the T068 max-envelope tests, so one follow-up should close both files. Nothing here blocks acceptance.

**Note for the orchestrator:** my Write tool is blocked by the read-only guard, so I could not save the in-process-mutant technique to `.claude/agent-memory/backend-engineer/`. The reusable part: a read-only reviewer can verify a production mutant without editing the tree by rebinding the module attribute inside `python -c` and calling `pytest.main(...)`.

G4 VERDICT: PASS

END-OF-REPORT
