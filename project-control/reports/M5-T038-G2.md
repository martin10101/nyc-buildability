# M5-T038 — G2 producer self-check record (recorded by orchestrator)

G2 is the producer self-check gate; the CLI rejects the producer's own name, so the
orchestrator records it over the producer's documented self-check evidence.

- **Producer self-checks (bounded-evidence report, runs persistent2-local-03..05 in
  wt-m5t038):** modularity check clean (report §F, the packet's one locally-runnable
  documented command); the report keeps executed validation strictly apart from source
  inspection and states plainly that web behavior proves ONLY in CI on the pushed head; the
  digest-bound sections (A/B/C/D) were built specifically so reviewers read committed content,
  never worktree state — G4 verified every section matches the committed bytes.
- **CI (the executable authority):** vitest 42/42 files green at material d94bbcb6; one
  Playwright journey red only on the pre-change rate-limited sentence, corrected out-of-scope
  by the orchestrator (8c089343, [ORCH-CORRECTED], precedent in the same spec); ALL 20 checks
  green at the corrected head (project-control/reports/M5-T038-ci-evidence.md).
- **Identity:** self-checked tree == material commit d94bbcb6 content (clean cherry-pick; all
  four independent reviews verified the 13 allowed_paths byte-stable d94bbcb6..HEAD).
- **Orchestrator rulings recorded with this gate:** (a) G4 observation 12 — the packet
  parenthetical "(and network failure)" was imprecise; the implementation correctly preserves
  the distinct, unretried `unavailable` transport reason per the accepted M5-T032 design and
  the binding AS-1; the code governs, the wording is superseded. (b) The G3 advisory + HJ
  F1-F4 copy/a11y polish items are routed to the discovery backlog, none blocking.

**G2 verdict: PASS.**
