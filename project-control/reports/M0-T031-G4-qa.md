<!-- Orchestrator note: qa-engineer return saved VERBATIM from the agent-return channel.
     Reviewed head 1891ef3f886c7ba65413ddd9727b5a596c8e705a; recorded for gate G4 of M0-T031.
     Verdict PASS, zero required rework. -->

All verification complete. Here is the gate report content for the orchestrator to record verbatim.

---

# Gate Report

- Gate ID: G4 (QA)
- Task ID: M0-T031
- Reviewer: qa-engineer (independent; did not produce any reviewed artifact)
- Producer: (per ledger; reviewer worked from acceptance criteria, not the producer report)
- Result: **PASS**
- Clean environment/worktree used: yes — frozen tree `1891ef3f886c7ba65413ddd9727b5a596c8e705a` extracted via `git archive` into a clean scratchpad dir (`.../scratchpad/frozen-1891ef3`); all executions ran there with reviewer-owned temp cache dirs. `git worktree list` confirmed `.claude/worktrees/M0-T031-codegraph-hardening` is checked out at `1891ef3`. Diff base `cc273b5` (current main).

## Acceptance criteria reviewed

AS-1 through AS-9 from `project-control/tasks/M0-T031.json`, each re-executed/re-derived live — not taken from the producer report.

| Scenario | Verdict | Evidence summary |
|---|---|---|
| AS-1 tamper | **PASS** | Live: generated into own cache; JSON-valid same-length tamper (53 occurrences `code_graph/query.py`→`code_graph/EVIL9.py`); query with unchanged sources printed `regenerated (cache integrity)`, output contained zero tampered strings, post-query `graph.json` sha256 restored to original `a530f5d4…`. Literal one-byte flip + `--no-regen`: exit 3, exactly 1 stdout line `STALE (cache integrity): refusing to serve the cached graph`, stderr empty. Fixture tests `test_tampered_graph_regenerated_altered_bytes_never_served` / `test_tampered_graph_with_no_regen_exits_3` present and substantive (byte-flip, hash-restore, no-serve assertions). |
| AS-2 corrupt meta | **PASS** | Live: `graph.meta.json` truncated to 57 bytes → `regenerated (cache integrity)`, exit 0, stderr 0 bytes, no `Traceback`. `--no-regen` leg → exit 3 one-liner. OSError simulation (meta replaced by a directory) → exit 3 one-liner, stderr 0 bytes. Fixture tests `test_corrupt_meta_treated_as_stale_no_traceback`, `test_unreadable_graph_oserror_handled_no_traceback` (also covers regeneration-failure path: `cache regeneration failed (cache integrity)`, no traceback). |
| AS-3 cache identity | **PASS** | Fixture test `test_same_basename_checkouts_get_distinct_cache_dirs` run individually: OK. Live spot-check: `default_out_dir(…/siteA/myrepo)` → `…\nyc-codegraph\cccda0135080-myrepo`; `…/siteB/myrepo` → `…\940e287bca0a-myrepo` — distinct leaf keys (`sha256(realpath)[:12]-basename`). |
| AS-4 --limit both positions | **PASS** | Live on broad query `find e` (2921 candidates): `--limit 5 find e` and `find e --limit 5` byte-identical (5 results + truncation line); default → 40+1 lines; `--limit 999` → 200+1 lines (hard cap unchanged); `--limit 200` → exactly 200+1; `--limit 0` clamps to 1; both positions given (`--limit 3 find e --limit 7`) → subcommand value wins (7+1), matching documented precedence. |
| AS-5 test suite | **PASS** | `python tools/test_code_graph.py` → `Ran 36 tests … OK`, exit 0 (Python 3.11.9). Name-diff base vs frozen: all 29 prior test names preserved verbatim (0 removals/renames), 7 additive. |
| AS-6 check + budget | **PASS** | `generate.py --repo . --check` → `determinism check PASS: 2 generations byte-identical (235 input files, fingerprint 21aa77fb90376adc)`, exit 0. `python tools/context_budget_check.py` → `PASS`, eager total 9293B/~2282 tok vs 6000 budget, exit 0 — with the new CLAUDE.md row present at the frozen SHA. |
| AS-7 guidance diffs | **PASS** | `git diff cc273b5..1891ef3 -- CLAUDE.md`: exactly **+1/-0** line — one routing-table row, "selective, advisory" wording. `-- .claude/skills/start-controlled-task/SKILL.md`: exactly **+3/-0** lines (blank + one paragraph + blank). Both encode the owner decision model (materially useful? YES → graph → narrow → source verification; NO → direct navigation), advisory-only, mandatory source verification, and "never required on every task". README carries all **9** SHOULD-prefer items and all **5** NOT-mandatory items **verbatim** against `source-003-amendment.md` clarification 2 (item-by-item comparison done). No mandatory-use language in either surface. |
| AS-8 wording greps | **PASS** | Grepped all added diff lines for `graph-first`, `always use`, `on/for every task`, `must use/query the graph`, `mandatory`, `token`, `savings`, `faster`, `efficien`, `reduce work/exploration`, `start with the graph`, etc. Every hit is an explicit owner-required negation ("not universal graph-first", "NEVER required on every task", "No token or time savings are claimed"), an owner-verbatim list item ("search is clearly cheaper"), a lexical false positive ("STALE token", "mandatory freshness"), or the producer report quoting its own grep evidence. Zero universal-graph-first assertions; zero token/time-savings claims. |
| AS-9 path scope | **PASS** | `git diff --name-only cc273b5..1891ef3` → exactly 7 files: `SKILL.md`, `CLAUDE.md`, `project-control/reports/M0-T031-producer-report.md`, `tools/code_graph/README.md`, `generate.py`, `query.py`, `test_code_graph.py`. All within `allowed_paths`; no workflows/CI, no hooks, no `.claude` config beyond the single SKILL.md paragraph, no product or dependency surfaces. |

## Directive/requirement verification

The exhaustive D-005 registry pass (producer ≠ verifier) belongs to the `directive-compliance-verifier` gate. The rows below are the amendment-2 rows my QA evidence **directly and independently** establishes at the frozen SHA:

| Requirement ID | Reviewed SHA / content identity | Verdict | Reproduced evidence |
|---|---|---|---|
| D-005-R094 (4 hardening items) | 1891ef3 | PASS | AS-1/AS-2/AS-3 live + fixtures; stdlib-only enforced by `test_generator_query_tests_import_stdlib_only` (ast-walk vs `sys.stdlib_module_names`), no network/hook surfaces in diff (AS-9) |
| D-005-R092/R096 (no universal graph-first) | 1891ef3 | PASS | AS-8 greps: only negations present |
| D-005-R097/R100 (selective decision model) | 1891ef3 | PASS | AS-7: decision model verbatim in README + SKILL.md paragraph |
| D-005-R098/R099 (named case lists) | 1891ef3 | PASS | README: 9/9 SHOULD-prefer, 5/5 NOT-mandatory, verbatim item-by-item |
| D-005-R101 (no token/time claims) | 1891ef3 | PASS | AS-8: only the explicit anti-claim exists |
| D-005-R103 (source remains final) | 1891ef3 | PASS | README + SKILL.md: "graph → likely locations → actual source verification, never graph → assumption" |
| D-005-R104 (fingerprint stale detection preserved) | 1891ef3 | PASS | `test_stale_fingerprint_auto_regenerates` / `test_stale_with_no_regen_exits_3` green; fingerprint recomputed first on every invocation (query.py:105) |
| D-005-R105/R107/R109 (no watchers/hooks/CI/ reserved surfaces) | 1891ef3 | PASS | AS-9 name-only diff: none touched |
| D-005-R108 (only the two contemplated guidance surfaces) | 1891ef3 | PASS | AS-7 diffs: exactly one row + one paragraph |

## Steps independently executed (exact commands, abbreviated outputs)

1. `git worktree list` → task worktree at `1891ef3`. `git archive 1891ef3 | tar -x -C <scratchpad>/frozen-1891ef3` (clean extraction).
2. `git diff --stat cc273b5..1891ef3` → 7 files, +701/−52. `git diff --name-only` → listed above.
3. `python tools/test_code_graph.py` → `Ran 36 tests in 6.230s / OK`, `EXIT=0`.
4. Test-name diff base↔frozen → 29 preserved + 7 added, 0 removed.
5. AS-1: `generate.py --repo . --out <own-cache>` → 235 files/2921 nodes/1494 edges; semantic tamper → query → `regenerated (cache integrity)`, hash restored `a530f5d4…`; byte-flip → `--no-regen` → `EXIT=3`, 1-line `STALE (cache integrity): refusing to serve the cached graph`.
6. AS-2: truncate meta → regen leg `EXIT=0` stderr 0B; `--no-regen` `EXIT=3`; meta-as-directory (OSError) `EXIT=3`; zero `Traceback` matches.
7. AS-3: `python -m unittest tools.test_code_graph.CodeGraphTests.test_same_basename_checkouts_get_distinct_cache_dirs -v` → OK; live `default_out_dir` probe → distinct keys.
8. AS-4: five live `--limit` probes (before/after/default/999/200/0/both-positions) — outputs as tabled above; before/after outputs `cmp` IDENTICAL.
9. AS-6: `generate.py --repo . --check` → PASS `EXIT=0`; `context_budget_check.py` → PASS `EXIT=0`.
10. AS-7/AS-8: per-file diffs; verbatim list comparison against `source-003-amendment.md` clarification 2; three grep sweeps over added lines of the full 936-line diff.
11. Cross-checks: base `GENERATOR_VERSION = "1.0.1"` → frozen `"1.1.0"` (required bump present); meta `graph_sha256` equals independently computed sha256 of `graph.json`.

## Expected versus actual

No divergences. Every expected behavior in AS-1..AS-9 reproduced exactly.

## Evidence paths

Scratchpad (reviewer-local, ephemeral): `…\scratchpad\frozen-1891ef3\` (clean tree), `…\scratchpad\cache-as1\`, `as1-stderr.txt`, `as1b-stdout.txt`, `as2*-std*.txt`, `as4-a..e.txt`, `full-diff.txt`, `names_base.txt`/`names_frozen.txt`. Repo evidence: `git diff cc273b5..1891ef3` (reproducible by orchestrator).

## Human-style walkthrough findings

CLI messages are one-line, human-readable, and truthful about what happened (`regenerated (cache integrity)` vs `regenerated (stale fingerprint)` distinguish cause). Refusal paths never dump partial results.

## Regression/security/provenance findings

No regressions: all 29 prior invariants green; determinism byte-identical; hard cap/default limits unchanged; `_assert_outside_repo` still refuses in-repo artifact writes; artifacts remain uncommitted and outside the repo; no new dependencies (stdlib-only test enforces).

## Defects

None (no FAIL-level findings). Informational observations, no rework required:

1. **INFO** — `CLAUDE.md:71`: the new routing-row path `tools/code_graph/README.md` is not backticked, unlike every other row in that table. Cosmetic inconsistency only; not an acceptance criterion; budget check PASS.
2. **INFO** — `.claude/skills/start-controlled-task/SKILL.md:14` (added paragraph): inlines 7 of the 9 SHOULD-prefer examples (omits "contract-change impact" and "unfamiliar subsystem orientation"). Compliant — AS-7 places the full 9/5 lists in README (verified verbatim there) and the SKILL.md subset invents nothing; noted for transparency.
3. **INFO** — the regeneration status line prints to stdout ahead of result lines; identical to pre-existing base behavior for stale-fingerprint regeneration, and the prior output-invariant test still passes. Not a regression.

## Required rework

None.

## Reviewer conclusion

**PASS.** All nine acceptance scenarios independently reproduced at frozen SHA `1891ef3` from a clean extraction; hardening behaviors (tamper hash-bind, corrupt/unreadable meta, cache identity, `--limit` positions) verified live and by substantive fixtures; guidance surfaces are exactly the two contemplated ones, faithful to the owner's SELECTIVE decision model with no universal-graph-first or savings wording anywhere in the diff; diff scope is exactly the allowed paths. Per read-only discipline I ran no control CLI and no git write commands; the orchestrator should record this gate with `python tools/project_control.py gate`.
