<!-- Reviewer return preserved VERBATIM by the orchestrator (report-preservation rule,
.claude/rules/project-control.md; transport entity-decoding applied: &lt; -> <, &gt; -> >, &amp; -> &).
Reviewer: independent qa-engineer subagent (separate from the producer label
frontend-engineer/orchestrator), returned 2026-09-12 (UTC). The reviewer's opening note about its
agent-memory write guard is preserved at the end; it does not affect the verdict. -->

# GATE REPORT — M5-T015 (G4 integration/regression QA)

- **Gate ID:** G4
- **Task ID:** M5-T015 (address entry + resolution outcomes + error matrix, design-spec Packet 1)
- **Reviewer:** qa-engineer (independent; ADR-005 read-only)
- **Producer:** frontend-engineer/orchestrator
- **Result: PASS**
- **Clean environment / frozen SHA:** Reviewed at branch tip `candidate/D-024-mrl-option-b` = `9f679ff7de62658189dacb4db9ba1ea824b30ab1` (matches the assigned reviewed head; confirmed `git rev-parse candidate/D-024-mrl-option-b` = 9f679ff7). My isolated worktree HEAD (d8b3899f) is divergent, so every file was read at the frozen SHA via `git show 9f679ff7:<path>`, not from a working tree.

## Summary

The 31-test module `apps/web/src/components/address/__tests__/address-resolution.test.tsx` is a strong, adversarially-sound acceptance pack that satisfies all eight packet scenarios S1–S8. Every test drives the real component through a stubbed global `fetch` over recorded-response-**shaped** fixtures whose key set was verified against the endpoint truth (`_success_document`, `STATUS_STATE_MATRIX`, `_extract_suggestions`); no network, Geoclient, or Supabase is reachable. Expected values are fixture-derived (traversing the actual fetch → `address-api` bounding → render path) or are spec-mandated copy phrases — I found **no harmful tautology**. I statically verified mutant-killing power for eight named mutants (flag guard, disabled stub, default-checked radio, verbatim re-query, unknown-status coercion, retry-on-invalid_input, `dangerouslySetInnerHTML`/attribute injection, seq-guard/D5/focus/announce) and each named test genuinely fails under its mutant. Regression posture is clean: the existing PropertyLookup test suite is byte-untouched (`git diff` empty over `property/__tests__`), the flag-off placeholder block is byte-identical (only wrapped in `{ruleEvalEnabled ? null : …}`), and the M4-T009 cross-suite hazard is checked — the rule-evaluation suite's flag-on render now also mounts the address surface, but there is no label/role/testid collision and the address surface fires no fetch on mount, and CI confirms 27/27 web suites green. The CI evidence at head `6787445e` applies to the reviewed content because `git diff 6787445e..9f679ff7` is **control-plane-only** (ci-evidence.txt, evidence-map.json, state.json, task JSON); production + test bytes are identical. The producer's history claims reproduce exactly. Findings are all LOW/informational and non-blocking.

## History / provenance verification (reproduced)

| Claim | Command | Result |
|---|---|---|
| Repair `6787445e` touched ONLY the test module | `git diff 6097c8cb..6787445e --stat` | 1 file (`address-resolution.test.tsx`), +19/−9 — confirmed |
| Repair is the 2 named mechanics fixes, zero behavior change | `git diff 6097c8cb..6787445e -- <test>` | (a) `retry_after` single-source const; (b) `resolve(process.cwd(), …)` replacing `new URL(…, import.meta.url)` to avoid jsdom `ERR_INVALID_URL_SCHEME`. Both test-only |
| Production apps/web byte-identical `352aa9de`↔`6787445e` | `git diff 352aa9de..6787445e --stat -- <6 prod files>` | empty — confirmed |
| Reviewed head vs CI-tested head | `git diff 6787445e..9f679ff7 --stat` | control-plane files only (4) — CI green at 6787445e applies to reviewed 9f679ff7 |
| Producer material within allowed_paths only | `git diff b646dc0d..352aa9de --stat` | exactly the 8 allowed paths; no package.json/lockfile |
| PropertyLookup tests untouched | `git diff b646dc0d..9f679ff7 --stat -- property/__tests__/` | empty — confirmed |

CI run 34675912878 (push of 6787445e): `web` SUCCESS, `web-e2e` SUCCESS, `address-resolution.test.tsx (31 tests) PASSED`, Test Files 27 passed (27). The only failing jobs (`web-dependency-security`, `control-plane`) are pre-existing and owner-gated, unrelated to this task. RED-half evidence (first push 6097c8cb, run 34675738338: 420/421 passed; the single test failure + one typecheck error are exactly the two test-mechanics defects the repair fixed) is consistent with the repair diff.

## Scenario-by-scenario coverage (S1–S8)

| Scenario | Satisfying test(s) | Verdict |
|---|---|---|
| **S1** flag posture (off: no surface/no fetch; on: form mounts, placeholder gone) | S1 test 1 (`address-disabled-copy` present, `address-resolution-screen`/`address-form` null, `fetchSpy` not called); S1 test 2 (surface+form present, placeholder null, BBL form still present, no fetch on mount) | PASS (see Finding 1 re: byte-equivalence semantics) |
| **S2** resolved → Packet-2 stub; resolved_with_warnings beside result | S2 test 1 (`resolved-bbl`===fixture bbl, address contains normalized street+borough, `stub-continue` disabled, `address-resolved-stub` present, no warnings, "resolved to a single lot"); S2 test 2 (`address-warnings` role=status, both messages fixture-derived, result+stub still render — warnings don't gate) | PASS |
| **S3** ambiguous caller_selects literally (no default, disabled-until-choice, verbatim slot order incl. codeless slot, re-resolve verbatim, keyboard) | S3 test 1 (radios===slots, slot-order verbatim, slot0 has code / slot1 codeless, no radio checked, shared `name`, use-suggestion disabled); S3 test 2 (pick enables Use, second fetch-seam URL `street`===`suggestions[1].street_name` verbatim, house/borough===first) | PASS (Finding 4 re: gap/keyboard) |
| **S4** visible honest non-success (both GRCs, recovery, unrecognized not coerced) | S4 not_found (both grc/grc2 code+message fixture-derived, input-echo, `bbl-instead` href=`#bbl-input`, edit-address focuses Street, correlation-id); S4 rejected (own framing, both GRCs, both affordances); S4 unrecognized (`address-unrecognized-status`, not_found+resolved null, raw status token, correlation-id, Retry) | PASS |
| **S5** complete error matrix, one scenario per pair | `it.each` over all 8 emitted pairs (HTTP status + retry posture verified against `STATUS_STATE_MATRIX`); invalid_input connector-message; key_missing/auth_failed no-blame; rate_limited **with and without** retry_after; request_budget_exceeded typed card; +undocumented-pair guard +drifted-200 guard | PASS (Findings 2, 3) |
| **S6** hostile fixtures in input_echo.street, grc_message AND suggestion name + source-scan | S6 test 1 (hostile street+message inert text, 0 script/img/iframe/style, only anchor href `#bbl-input`, `window.__pwned` undefined); S6 test 2 (hostile suggestion inert, raw name verbatim to fetch seam, 0 iframes); S6 test 3 (no `dangerouslySetInnerHTML` in 6 files, `<li key={index}>`, import-specifier allowlist) | PASS |
| **S7** supersession, D5, focus, announce-once | S7 supersession (seq guard wins even when transport ignores abort); S7 D5 (blank resubmit inert, no fetch, result survives); S7 focus (heading on arrival, resolving card on retry, never `<body>`); S7 announce (empty→arrival→cleared→re-set for identical retry); +transport-failure cards | PASS |
| **S8** no new deps/tokens; CI green | import-specifier allowlist (S6 test 3); className scan confirms all 15 classes are spec §5 reused tokens (no new CSS, `.suggestion-option` avoided via `missing-list`); package.json/lockfile untouched (git diff); CI web+web-e2e green | PASS (Finding 5 re: CSS half source-verified not test-asserted) |

## Anti-tautology assessment (ENGINEERING_RELIABILITY §3)

No assertion compares a value to itself through the component's own code path. Fixture-derived expectations (`resolved-bbl`, GRC/warning messages, suggestion names, re-query URL params, correlation-id, retry_after) all traverse JSON-serialize → stubbed fetch → `address-api` (`boundedText`/`boundedToken`/header extraction/`URLSearchParams`) → render before comparison against the raw fixture value, which is a genuine round-trip check. The test never imports `bounded.ts` or the address-api mapping to pre-compute an expected value. The repaired `retryAfterHint = "120"` single-source const (injected into the fixture and asserted) is correct anti-drift, not a tautology — the value still crosses serialize/fetch/bound/render. Spec-copy literals ("Nothing is wrong with your input", "not your input", "resolved to a single lot", "HTTP 422", "timed out") are cases where the copy IS the requirement (allowed by the module docstring's stated exception).

## Test hygiene

- `afterEach(cleanup + vi.unstubAllGlobals)`; sub-scenario tests do mid-test `cleanup()`+`unstubAllGlobals()` before re-render — correct.
- All `deferred()` promises are resolved (supersession L726, focus L778, announce L796) — no dangling. `resolveAddress` clears its 12 s real timer in `finally` on every path; every hung-fetch test resolves its deferred, so no leaked timers.
- Stubbed fetch never reaches the network: the component calls `resolveAddress` without `fetchImpl`, so it uses the stubbed global `fetch`.
- No test interdependence: fixtures are factory functions returning fresh objects; each mutation is local.
- jsdom focus assertions are sound: focus targets carry `tabIndex={-1}` (headings) or are inputs; async focus wrapped in `waitFor`.

## Regression / cross-suite findings

- Existing PropertyLookup suite byte-untouched; flag-off DOM block byte-identical (diff shows only added `{ruleEvalEnabled ? null : …}` wrapper + import + flag-on mount).
- M4-T009 cross-suite hazard checked: only `rule-evaluation.test.tsx` renders `<PropertyLookup ruleEvalEnabled />`. With the flag on the address surface now mounts, but `getByLabelText("BBL")`, `getByRole("button", { name: "Look up property" })`, and the `profile-view`/`rule-eval-panel` testids remain unique, and the address surface fires no fetch on mount, so the suite's fetch-URL assertions are unperturbed. CI's 27/27 confirms.
- New modules are consumed only by `PropertyLookup` (mount) and `announce.ts` (type import). The `PropertyLookup` prop change is additive (`ruleEvalEnabled?` default `false`); no positional/count assumption elsewhere. `OutcomeAnnouncer` coexistence uses the distinct `testId="address-outcome-announcer"` (M4-T005 pattern), no default-id collision.

## Findings (all LOW / informational — none blocking)

1. **(LOW) S1 own test is presence/absence + fetch-absence, not a DOM byte snapshot.** Byte-equivalence is delegated to the untouched existing PropertyLookup suite (which is exactly how the packet's own S1 defines it: "existing PropertyLookup tests still pass unchanged") plus the verified byte-identical flag-off diff. Adequate under the packet's definition; stated honestly because the phrase "byte-equivalently" is not a literal snapshot assertion in this module.
2. **(LOW) request_budget_exceeded is covered as a "renders honestly if it arrives" scenario, not a literal backend "not-emitted" assertion.** Backend non-emission is not UI-testable; the endpoint's unreachable-by-construction property belongs to M2-T022's suite. The (503, request_budget_exceeded) pair is correctly typed and rendered as its own honest card here, so the pair is covered.
3. **(LOW) S5 `it.each` pins testid/retry/correlation per state, and dedicated tests pin the honesty-critical copy (invalid_input message, key_missing/auth_failed no-blame, retry_after), but generic body COPY is not asserted for the same-HTTP retryable states.** A copy transposition among {source_unavailable, timeout, malformed_response, internal_error, request_budget_exceeded} generic bodies would survive. Cosmetic, low risk; card identity and all spec-critical postures are pinned.
4. **(LOW/INFO) S3 "gap" is represented by a codeless slot, and keyboard selection is asserted structurally.** The connector (`_extract_suggestions`) skips empty slots, so a literal empty slot can never reach the client — the codeless slot is the contract-faithful representation of the "gap." Keyboard-only selection is proven via native radio-group semantics (shared `name`, label-wrapped input, no default checked) rather than a simulated arrow keydown, which is the appropriate jsdom-level proof.
5. **(INFO) S8's "no new CSS tokens" half is source-verified, not test-asserted** (undefined CSS classes would not fail a build). I confirmed all 15 `className` values are spec §5 reused tokens; the optional `.suggestion-option` was avoided by reusing `missing-list`.
6. **(INFO) No genuine behavioral RED in the CI arc** — consistent with the accepted M5-T014 CI-only thin-client posture the packet documents (path_notes: named-mutant static verification substitutes for assertion-first red/green). I performed that static verification for 8 mutants; each named test fails under its mutant.

## Required rework

None. Findings 1–6 are advisory and do not block acceptance.

## Reviewer conclusion

**PASS.** The test module independently and adversarially satisfies S1–S8, kills its named mutants, avoids tautology, keeps regression posture clean (existing suite untouched, flag-off byte-identical, cross-suite flag-on interaction verified), and its CI green (captured at 6787445e) provably applies to the reviewed content at 9f679ff7 (control-plane-only delta). Recommend the orchestrator record G4 = PASS. The six findings are non-blocking advisories worth carrying into Packet 2 (particularly Finding 3: consider a per-state body-copy assertion when the confirm-card work touches the error matrix again).

**Reviewer files (absolute):** frozen-SHA copies I reviewed were extracted to the reviewer's transient scratchpad (`scratchpad\rev\`). Authoritative sources at SHA 9f679ff7: `apps/web/src/components/address/__tests__/address-resolution.test.tsx`, `apps/web/src/components/address/AddressResolutionScreen.tsx`, `AddressForm.tsx`, `SuggestionChooser.tsx`, `apps/web/src/lib/address-api.ts`, `apps/web/src/lib/announce.ts`, `apps/web/src/components/property/PropertyLookup.tsx`.

Note: I was unable to persist an agent-memory note — the memory write guard rejected my worktree path `...\worktrees\agent-a92a95873d7b68e6f\.claude\agent-memory\qa-engineer\MEMORY.md` as a "shared-checkout path" despite it being the exact directory named in my instructions and confirmed as my `git rev-parse --show-toplevel`. This is a harness path-guard quirk, not a review blocker; flagging it so the orchestrator is aware memory did not get written this session.
