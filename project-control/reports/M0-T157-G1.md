# M0-T157 — G1 Code Review Gate Report (verbatim)

Orchestrator preservation note: report returned by the independent code-reviewer agent
("G1 code review M0-T157") on 2026-09-12 and preserved verbatim below (transport
entity-decoding only). Reviewer was read-only. Verdict: PASS (no defects).

---

I have sufficient evidence. The full non-verbose suite buffers output to completion and the only affected class (ResolverTests) already passed independently; CI green is corroborated by the captured evidence file. Here is my gate report.

---

# Gate Report — G1 Independent Code Review

- **Gate ID:** G1
- **Task:** M0-T157 — Resolver-test fixture repair: derive the nonexistent directive id
- **Reviewed identity (frozen):** material commit `14bdcc73`; HEAD `b405d350`; branch `candidate/D-024-mrl-option-b`
- **Reviewer:** independent (read-only)
- **Result:** **PASS**

## Frozen-identity verification

- `git show 14bdcc73` diff shape is exactly as contracted: **one test function** in `tools/test_directive_compliance.py` (`test_s12_wrong_directive_reference_fails_closed`, −1/+9 lines) plus the producer report. The commit additionally carries orchestrator control-plane artifacts (`M0-T157-G0.json`, `M0-T157-G0.md`, `state.json`, `M0-T157.json`) — these are orchestrator-written lifecycle files (producer_agent = orchestrator), not producer code material, and the frozen identity scopes producer material to the test function + report. Not a violation; noted for transparency.
- **Forbidden production modules untouched:** `directive_registry.py`, `validate_directive_compliance.py`, `project_control.py` are NOT in the diff (byte-unchanged). Resolver/validator/registry logic is not modified — the change is fixture-only.
- **No later commit touches the 2 allowed_paths:** `git log 14bdcc73..b405d350 -- tools/test_directive_compliance.py project-control/reports/M0-T157-producer-report.md` returns empty. The 3 intervening commits (`18ee6b79`, `a53e7244`, `b405d350`) are M5-T021 / evidence / submit and leave both allowed paths at their `14bdcc73` content.

## Scenario verdicts

**S1 — derived_id_fails_closed: PASS**
- Derivation reproduced empirically against the live registry: 40 directives (D-001..D-042, gaps present), `max(numbers)=42` → `missing="D-542"`, and `assertNotIn("D-542", registry)` holds (D-042 IS present, confirming the old hardcoded value would now collide).
- The derivation is **collision-proof**: it recomputes `max + 500` at test time against the then-current registry, so it can never go stale the way the hardcoded `D-042` did. `isdigit`/`len>1` filters guard non-numeric or malformed id segments; `assertNotIn` makes the nonexistence premise explicit and fail-loud.
- **Contract not weakened:** the assertions still require `ev["ok"] is False` and an `invalid_refs` entry containing `"does not exist"` — the same fail-closed resolver behavior. Verified by execution against the current registry (incl. real D-042).

**S2 — regression: PASS**
- `python tools/test_directive_compliance.py ResolverTests -v` → **Ran 5 tests, OK** (all 5, including repaired s12).
- `python tools/validate_directive_compliance.py --check` → **EXIT 0**.
- `python -m ruff check tools/test_directive_compliance.py` → **All checks passed**.
- Control-plane CI: corroborated via `project-control/reports/M0-T157-ci-evidence.txt` — run **34726577993** at head `18ee6b79` (a descendant that contains `14bdcc73`) reports control-plane **SUCCESS, fully green**. Independent `gh` confirmation was not run (reviewer is read-only and `gh` is forbidden); per ADR-005 evidence-capture division of labor I verified the orchestrator-captured evidence file. The local full non-verbose suite exceeded 300s on the heavy git-subprocess identity tests and was backgrounded — this does not affect the verdict since ResolverTests is the only class the change touches and it passes.

## Adversarial findings

- **Does the derivation mask a resolver regression?** No. It only selects *which* absent id is fed; it does not touch the `assertFalse(ev["ok"])` / `"does not exist"` assertions. If the resolver stopped rejecting nonexistent refs, the test would fail. The fail-closed contract is fully exercised.
- **Empty-registry edge (`max()` over empty → ValueError):** the test would ERROR (fail-loud), not silently pass. Acceptable for a fixture over the real committed registry, which always contains the self-proving anchor D-001 (append-only, D-001-R022) and can never be empty here. A default-guard would be tidier but is unnecessary; erroring is the safe direction. Not a finding.
- **`%03d` at D-1000+:** `%03d` is a *minimum* width — verified `1500 → "D-1500"`, `7 → "D-007"`. No breakage above 999. Reasoning in the packet confirmed.
- **Collision-class scan:** the only other real-registry references are to **D-001** (an *existence* premise — the stable anchor, never absent) and the **D-004-R…** ids at lines 1300–1303 which are asserted absent from *source text*, not from the registry (no nonexistence-of-directive premise). Synthetic tmp-registry ids (`D-700`/`D-701`/`D-900`) run against synthetic registries and are exempt. No remaining hardcoded real-registry nonexistence probe of a directive id. Observation (out of scope, already hardened): line 323 uses a synthetic ledger *task* id `M0-T9099`, deliberately out-of-range after the earlier `M0-T099` went stale — same defect class, already defended with a wide margin; a task id, not a directive id, and pre-existing, so not a finding for this task.

## Modularity

N/A — test-only change, single function, +8 net lines; no production source created or grown. `modularity_check` not applicable to the reviewed diff.

## Directive regime

Task is in-regime (`directive_refs: D-001:ALL`, governance path). The D-001 requirement-to-evidence verification is the `directive-compliance-verifier`'s pass recorded in `verification.json` (producer ≠ verifier) and rides the required **G4** gate; it is outside this G1 code-review scope and does not block G1.

---

**VERDICT: PASS** — Frozen identity holds (one test function + report at `14bdcc73`, forbidden modules untouched, allowed paths unchanged through HEAD `b405d350`). The derivation is correct, collision-proof, and preserves the fail-closed contract without weakening it. S1 PASS, S2 PASS. ResolverTests 5/5 OK, validator EXIT 0, ruff clean, control-plane CI green (run 34726577993, corroborated via captured evidence). No defects.

Relevant paths:
- `C:\Users\MLFLL\Downloads\nyc-zoning\ctl24\tools\test_directive_compliance.py` (fix at lines 290–302)
- `C:\Users\MLFLL\Downloads\nyc-zoning\ctl24\project-control\tasks\M0-T157.json`
- `C:\Users\MLFLL\Downloads\nyc-zoning\ctl24\project-control\reports\M0-T157-producer-report.md`
- `C:\Users\MLFLL\Downloads\nyc-zoning\ctl24\project-control\reports\M0-T157-ci-evidence.txt`

---

# APPENDED REVIEWER ADDENDUM (verbatim, same reviewer agent — full-suite corroboration)

Full-suite corroboration complete. The backgrounded run finished: **`python tools/test_directive_compliance.py` → Ran 129 tests, OK, exit code 0** — the entire directive-compliance suite passes locally at the frozen material, not just the affected `ResolverTests` class. This closes the one open item in S2 (local regression breadth) with no change to the verdict.

No amendments to the gate report already returned. Restating the outcome:

**Gate G1 — Task M0-T157 — VERDICT: PASS** (S1 PASS, S2 PASS).

Evidence summary at frozen identity (material `14bdcc73`, HEAD `b405d350`):
- Diff shape correct: one test function in `tools/test_directive_compliance.py` + producer report; forbidden modules (`directive_registry.py`, `validate_directive_compliance.py`, `project_control.py`) byte-unchanged; no later commit touches the 2 allowed paths.
- Derivation collision-proof (`max D-nnn + 500`, empirically `D-542` against the current 40-directive/max-42 registry), `assertNotIn` premise explicit, fail-closed contract preserved (not weakened).
- `ResolverTests -v` → 5/5 OK; full suite → 129/129 OK; `validate_directive_compliance.py --check` → EXIT 0; `ruff check` → clean; control-plane CI green (run 34726577993, corroborated via `project-control/reports/M0-T157-ci-evidence.txt`).
- No defects. Modularity N/A (test-only, +8 net lines). D-001:ALL requirement verification is the G4 `directive-compliance-verifier` pass, outside G1 scope.
