# M5-T055 G3 — independent code review (original return + delta-attestation, both saved VERBATIM; the headers are the orchestrator's)

## Original return (at frozen head db0cf0f8)

All verification complete. Here is my gate report.

---

# G3 Gate Report — M5-T055 (DB-035 confirm-arc rider cluster)

**Gate:** G3 (independent code review)
**Reviewer:** code-reviewer (read-only)
**Task:** M5-T055 — DB-035 confirm-arc rider cluster (riders a–d, AS-1..AS-6)
**Reviewed identity:** frozen submission head `db0cf0f8`; material commit `db445634` (4 files). Live HEAD `59862f2c` adds only a disjoint ledger commit (`M5-T055 + M5-T056 awaiting_gate`) — verified byte-stable: working tree matches HEAD for all four material files (`git status --porcelain` empty for them).
**Verdict:** **PASS** (web behavior PENDING CI at the pushed head — acceptance is the orchestrator's to hold until green; see A3).

## Method

Read the packet, producer report, and all four material files at HEAD. Verified the report's claims against actual source with `git diff db445634^ db445634`, `git grep`, and direct reads. Did not run npm/vitest/Playwright (thin client; web proves only in CI). Judged code, not local runs.

## Explicit rulings on items 1–4

### Item 1 — Rider (b) reading order: **CONFIRMED SOUND**

Actual JSX child order in `AddressConfirmCard.tsx`: `confirm-continue` CTA (`:377-385`) → `not-my-property` button (`:392-399`) → record note `<p data-testid="record-address">` (`:420-430`) → `<Meta>` footer (`:431`). The note is a non-interactive `<p>` inserted between the last interactive element and the footer. On the late insert (`showRecordAddress` flips true), normal block flow pushes down only what is below the insertion point — i.e., only `<Meta>`. Both interactive elements sit above the note and cannot move at any width or wrap. The diff (`AddressConfirmCard.tsx:397-432`) shows the note moved from former last-child (after `<Meta>`) to before `<Meta>`; nothing else in the interactive region changed. The below-CTA boundary argument holds. AS-2 unit tests (`address-confirm.test.tsx:881-922`, `:924-998`) bind this via `compareDocumentPosition` against both actions and assert `<Meta>` (`correlation-id`) is `card.lastElementChild`.

### Item 2 — Rider (c) accessible-name honesty / cap removal: **CONFIRMED SOUND**

`ENTERED_TITLE_ATTR_MAX_LEN = 512` and its truncation logic are fully removed; `enteredTitle = enteredInput` (`:134`). `git grep ENTERED_TITLE_ATTR_MAX_LEN -- apps/web` returns empty — the constant was module-local and had exactly one consumer (now gone); **no consumer relies on it.** Residual "512/513" hits are only explanatory comments/test-names documenting the supersession, not cap logic.

- **Security/DoS regression: none introduced.** The visible text node `{enteredInput.trim()}` (`:221`) was ALREADY unbounded before this change; the cap ONLY ever applied to `title`/`aria-label`. Removing it adds no new unbounded-reflection surface — it only lets a secondary attribute reflection match a string already fully rendered as visible text (a constant-factor change, not a new order of magnitude). React escapes both text and attribute values (inert, non-executable), so there is no XSS/injection path. The report's rationale is accurate.
- **Announced == seen at every length: holds.** For inputs ≤512 chars `enteredTitle` is byte-unchanged from before (was `enteredInput`, still `enteredInput`), so no short-input test is affected — the only behavioral change in the entire component is for >512-char entries. For >512, visible = `enteredInput.trim()`, accessible name (accname computation, via `toHaveAccessibleName`) trims/flattens the `aria-label` to the same trimmed value. AS-3 test (`:1182-1204`) binds `toHaveAccessibleName(longEntry)` and `toHaveAccessibleName(strong.textContent)` for a 600-char entry with no "…" marker. The old dishonest rider-i test (asserting the 513-char cap) was removed in the same diff — no leftover red test.
- **Supersession authority:** the packet SCOPE line (c) explicitly authorizes superseding DB-033 rider i and permits "announce the full visible text," so this is packet-sanctioned, not an unauthorized rollback.

### Item 3 — Rider (d) copy trim + copy sweep: **CONFIRMED SOUND**

No-street branch trimmed to `". The identity we carry forward is the tax lot (BBL)."` (`:232`). Matched-present branch (`:231`) is byte-identical to the accepted wording (context line in the diff — unchanged). Independent sweep `git grep "We show it as the address you searched for" -- apps/web` returns exactly 2 hits: `AddressConfirmCard.tsx:229` (a comment) and `address-confirm.test.tsx:1141` (inside `.not.toContain`). **No rendered branch still carries the removed sentence.** The matched-present wording sweep matches the accepted string at `:231` and test `:1154`. Producer's copy-sweep section is accurate.

### Item 4 — Rider (a) CLS spec: **CONFIRMED SOUND**

`git diff --numstat db445634^ db445634 -- responsive-a11y.spec.ts` = `149 0` (149 added, 0 removed); the removed-lines grep is empty. **The extension is strictly additive; every pre-existing S6 test (`:36-143`) and the `VIEWPORTS` constant (`:15-19`) and `expectNoHorizontalOverflow` helper are byte-preserved.**

- Intercept-hold-release: `recordGate` promise + `page.route("**/record-address**")` awaits the gate then fulfills; `releaseRecord()` releases after the "before" measurement (`:222-234`, `:259`). Correct.
- Exact equality, no tolerance: `expect(ctaTopAfter, …).toBe(ctaTopBefore)` (`:277`). The producer's CLS-tolerance sweep and my own reading confirm the only other numeric comparison in the file is the pre-existing S6 `toBeLessThanOrEqual` (`:33`); no `Math.abs`/sub-pixel tolerance exists. The exact-equality expectation is honest because the note inserts strictly below the CTA.
- Isolation: the spec waits for the lot-outline surface (above the CTA) to reach a terminal state (`:242-250`) BEFORE measuring `ctaTopBefore`, so the only post-release layout change is the below-CTA insert.
- Long-address wrap at 360px: `LONG_RECORD_ADDRESS` (`:168-169`, 122 chars, under the 600-char bound) applied at all three viewports including 360.
- Note-below-CTA + above-footer: `:282-290` assert `noteBox.y >= ctaBox bottom - 1` and `metaBox.y >= noteBox bottom - 1` (1px tolerance). These positional assertions also guard against any CSS flex/order reversal, complementing the DOM-order unit tests.

## Item 5 — Preservation: **CONFIRMED**

- Gating/display semantics unchanged: `git diff` filtered for `showRecordAddress|useRecordAddress|recordAddressStatus|INTERNAL_RULE_EVAL|export function AddressConfirmCard` returns empty — the gating logic and the component's exported signature are untouched; only the JSX block moved and comments/copy/attribute changed.
- No api change: material commit touches exactly 4 files (spec, component, unit test, report); no `services/api/`, no `packages/contracts/`. Forbidden paths respected.
- Consumers (D-066-R001 chain) unaffected: `AddressResolutionScreen`, `ArchitectEntry`, `PropertyLookup` consume the same unchanged props. The only removed symbol was module-local and externally unreferenced. Consumer sweep confirms `address-resolution.test.tsx:74` only stubs `/record-address` as a 404 (no position/last-child assertion); `record-address.test.ts`/`condo-records.test.ts`/`address-search.test.ts` test the lib, not the card DOM. `git grep lastElementChild|lastChild -- apps/web/**/*.test.tsx` outside the edited file returns none — the note's move from last-child to second-to-last leaves no out-of-scope red consumer.
- Modularity: component is 434 lines (under the 600 warn threshold); net change small and cohesive. Producer reports `modularity_check --check` exit 0 (G4/G5 cover the mechanical confirmation).

## Findings

- **G3-1 (advisory, non-blocking):** `address-confirm.test.tsx:800-804` — the S11 describe-block header comment still lists "(i) title/aria length bound" as a covered rider, but rider-i's test was removed and superseded by rider-c. Cosmetic stale comment; no functional impact.
- **G3-2 (advisory, pre-existing, non-blocking):** `enteredInput` (from `typedInput`/`view.inputEcho`) has no upstream length bound inside this component. This change introduces no NEW exposure (the visible text was already unbounded before), and AS-3 explicitly accepts "announce the full visible text," so it is consistent with the accepted design. If the team later wants a uniform reflected-text bound applied to BOTH the visible node and the attributes (the AS-3 "truncate both" branch), that would be a fresh discovery item — out of scope here.
- **G3-3 (informational):** AS-6 web proof (vitest `address-confirm` suite + Playwright `responsive-a11y` suite) is correctly disclosed as PENDING CI at the pushed head. Not verifiable locally (thin client); this PASS judges code only. The orchestrator must hold acceptance until CI is green at the pushed head that includes all four files together.

## Verdict

**PASS.** All four DB-035 riders are correctly and honestly implemented; the CLS spec is strictly additive with exact-equality proof of the new placement; the copy sweep and consumer sweep are accurate; removing the 512-char cap introduces no security/DoS regression and no consumer depends on it; announced == seen holds at every length; gating, display semantics, the public interface, and other consumers are preserved. The two advisories (G3-1, G3-2) are non-blocking. Acceptance remains conditional on green web CI at the pushed head (G3-3), which is the orchestrator's to record.

---

## Delta-attestation (after the [ORCH-CORRECTED per G4-1 + CI 35493851185] spec fix)

Confirmed. The delta review is clean.

**Reply: my PASS carries to `05f1aea2`.** No findings on the delta.

Verification of the delta:

- **Material identity intact.** `git diff --stat db0cf0f8..05f1aea2 -- AddressConfirmCard.tsx address-confirm.test.tsx` is empty — the component, the unit tests, and the exact-equality assertion I reviewed are byte-unchanged. The correction is spec-only within the new CLS `for (const viewport…)` block (after `:249`); the only removal is `const ctaTopBefore` → `let ctaTopBefore` inside that new block, and every pre-existing S6 test remains untouched.

- **The fix targets a test-environment flake, not a component defect.** The 27px CI movement at 360/768 was the `LotOutlineMap` status line at `LotOutlineMap.tsx:471` (`.architect-map-status`, rendered ABOVE the CTA) settling between the two measurements — not the record note (which inserts below the CTA). Gating the "before" measurement on terminal map state correctly isolates the record-note insert as the only post-measurement change, which is exactly what AS-1 is meant to prove. This is the honest fix; the exact-equality expectation is preserved (and made more reliable), never weakened.

- **The poll matches the real component and is not flaky.** `LotOutlineMap.tsx:471` renders the status line whenever `context && drawable && !mapRenderFailed`, and it PERSISTS in terminal state showing `"…: loaded"` / `"…: unavailable"` — neither contains the substrings `"Preparing"` nor `"loading"` (`"loaded"` does not contain `"loading"`), so the terminal branch is genuinely reachable and won't hang at `"no-status-line-yet"`. The four fallback testids in the poll match the fallback surfaces (for which `:471` does not render). Fail-safe: if neither a terminal fallback nor a terminal live-map state is reached, the poll times out and FAILS rather than passing vacuously.

- **The 500ms double-read quiescence guard** reassigns the `let ctaTopBefore` cleanly and runs BEFORE `releaseRecord()`, so it settles only pre-insert layout and does not mask the record-note movement; the downstream `toBe(ctaTopBefore)` still compares against the settled value. No weakening of the assertion.

One accuracy note for the record: the `db0cf0f8..05f1aea2` range also carries disjoint peer commits (M5-T057 api/`proposal_checks_api.py`, D-078 directive, M5-T056 task edits) — not "only spec + ledger." Those are other tasks' material interleaved on the shared branch; M5-T055's material identity (component + unit tests) is byte-stable, and its only in-scope change is the spec delta. This does not affect the carry.

**Verdict: PASS at `05f1aea2`** (G3). Web behavior still proves only in CI — acceptance stays held by the orchestrator until the amended CLS spec is green at the pushed head. [Orchestrator note: satisfied — run 35494571792 success at bd408ab9; see M5-T055-ci-evidence.md.]
