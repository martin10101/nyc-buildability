# M5-T037 — G2 producer self-check record (recorded by orchestrator)

G2 is the producer self-check gate; the CLI rejects the producer's own name, so the
orchestrator records it over the producer's documented self-check evidence.

- **Producer self-checks (report, run persistent-local-44 in wt-m5t037, packet cwds):** all
  seven documented commands re-executed by the producer verbatim, one per invocation, with
  real cwd + real outcomes recorded (ruff clean; tests/api 426; provider 28;
  rules-integration 49; typegen --check OK; typegen tests 29; modularity failures 0). The
  report's honesty discipline is exemplary: backend items labeled "worker-tested,
  independently UNVERIFIED"; AS-5/6/7 explicitly PENDING orchestrator-captured pushed-head CI;
  no completion claim.
- **Orchestrator harvest re-run (same tree, before the material commit):** all seven commands
  green with identical tallies.
- **Rework cycle under this gate's window:** the HJ's PASS-with-required-corrections (F1
  report-gating, F2 calm-panel jargon) was applied as tagged orchestrator corrections
  (75daaebd + the one-line casing alignment), resubmitted at 1c89922c; all five reviewers
  attested their PASS carries at that head; CI ALL 20 GREEN at 1c89922c
  (M5-T037-ci-evidence.md addendum).
- **Identity:** frozen submission at 1c89922c; contract/api surface byte-stable from the
  material commit a6875731 through 1c89922c (verified independently by all five reviewers).

**G2 verdict: PASS.**
