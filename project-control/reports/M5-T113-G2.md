# M5-T113 - G2 producer self-check record (recorded by the orchestrator, role self_check)

Producer: backend-engineer (orchestrator-dispatched subagent) in wt-m5t113; one in-worktree commit 21956e4b (parent 275ef85f), cherry-picked to fcd0033e with every file MATCH by git blob identity (7 files: project-control/reports/M5-T113-producer-report.md cdc39739, services/api/app/drawings/sheet_interpreter.py 31ebc55e, services/api/app/drawings/sheet_marked_content.py 6faf7291, services/api/app/drawings/sheet_objects.py f99c5058, services/api/app/drawings/sheet_reader.py aaf4f651, services/api/tests/drawings/test_sheet_content_features.py bddceb42, services/api/tests/drawings/test_sheet_reader_split_equivalence.py 1d1f26de).

- **Scope:** services/api/app/drawings/sheet_objects.py (content/form stream /DecodeParms PNG predictor via the existing apply_predictor), sheet_marked_content.py (NEW bounded inline << >> property-list lexer: depth + byte caps), sheet_interpreter.py (minimal wiring), sheet_reader.py (marked-content bounds threaded; a _scan_only_refusal seam); tests/drawings/test_sheet_content_features.py (NEW, 28 tests) and the deliberate test_sheet_reader_split_equivalence.py golden revision.
- **Producer self-checks (report, OBSERVED):** ruff clean; modularity exit 0; the drawings suite 245 passed under Python 3.11 via a scratch stub shim that skips the app.documents package __init__ chain (app/documents/units.py uses 3.12-only PEP 695 syntax; the documented `pytest tests/drawings -q` cannot collect under the local 3.11 - CI runs 3.12); one transient tracemalloc flake in the read-only test_pdf_object_streams (DB-076 h), passing on rerun. Real corpus: sha256 verified, run1 == run2 for all six, copies deleted.
- **[ORCH-HARVEST] at the harvested head fcd0033e (cherry-pick fcd0033e; Python 3.11 local): `python -m ruff check .` (cwd services/api) -> exit 0: All checks passed!; `python C:\Users\MLFLL\AppData\Local\Temp\claude\C--Users-MLFLL-Downloads-nyc-zoning-ctl24\9f312b0f-9155-4436-b27d-d471179bfe2b\scratchpad\checks\shim311\runtests.py tests/drawings -q -p no:cacheprovider` (cwd services/api) -> exit 0: 252 passed in 10.08s; `python tools/modularity_check.py --check` (cwd .) -> exit 0:   warn review_signal: tools/agent_supervisor/gate_wave.py - above the justification threshold; record a cohesion justification in review.**
- **Discoveries routed (producer):** Items 1-2's next blocker is the per-DOCUMENT 200k operator budget (recommend a per-page / paired work budget, DB-055 c); item 3 needs the sh shading operator; item 4 an inline image (BI) stays refused per DB-055 d; DB-076 (e) both scan-only inconsistencies are DISCLOSED, not fixed (a read-only test pins the scan-only label; the classic-vs-resolver image-only fix is out of scope).

G2 verdict: PASS (self_check; the independent reviews and the DCV follow - dispatched by the successor session per the handoff).
