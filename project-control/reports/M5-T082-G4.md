# M5-T082 — G4 independent test-adequacy review (qa-engineer "qa-mass", read-only)

> Transmission history: pinned at 1414e1a7 (HEAD advanced to 71ab388a mid-review through disjoint peer
> commits; the reviewer proved the reviewed blobs byte-identical to material b9b0d31e), delivered as three
> SendMessage parts (1/3..3/3) ending with END-OF-REPORT; no truncation. Joined verbatim by the orchestrator.

---

M5-T082 G4 test-adequacy review — qa-mass (independent; producer 3d-massing-engineer). PART 1/3.

SETUP / IDENTITY
- Pinned HEAD 1414e1a7 advanced to 71ab388a during review (disjoint peer commits: gate records for M5-T081/T084/T085/T087/T077; none touch T082). Reviewed files BYTE-IDENTICAL across the move and == material b9b0d31e: massing_model.py blob 255de6ec, test blob 62948e7b. Worktree clean under services/ after all runs (.pytest_cache/__pycache__ gitignored).
- cwd services/api; Python 3.11.9. Baseline `python -m pytest tests/scenario/test_massing_model.py -q` = 26 passed. Method: full-module in-process mutation — mutated source injected into sys.modules, the real suite run in a FRESH interpreter per mutant. BASELINE injection reproduced 26/26 and the golden b7fa98.. held (faithful harness).

MUTATION TABLE (mutant -> caught? / reddened tests)
- M1a reverse bottom-cap winding [AS-2 named] -> CAUGHT (6 red): closure[rect,lshape,5-floor] + reversed-face + volume==area*h + golden.
- M2 mis-order floors via reversed level-sort [AS-3 named] -> CAUGHT, but ONLY by test_as3_five_floor_plate_elevations (golden uses the 1-floor RECT; the swap-hash test compares base-vs-swap so both shift and it stays green).
- M3 containment sign-flip (accept outside / refuse inside) -> CAUGHT broadly (16 red).
- M4 naive fan triangulation (concave-unsafe) -> CAUGHT ONLY by the RECT golden hash (triangle-order change). NO concave-specific assertion fired -> F1.
- M5 drop clockwise->CCW normalization in _prepare_ring -> SURVIVED GREEN 26/26 -> F2.
- M6 drop collinear-vertex collapse in _prepare_ring -> SURVIVED GREEN -> F3.
- M7 tolerance 1e-6 -> 500.0 -> CAUGHT (footprint_outside_lot test); but 1e-6 -> 50.0 SURVIVES -> F4.
- M8 signed volume computed in WORLD coords (drop local-origin reduction) -> SURVIVED GREEN -> F5.

Continues in 2/3 (probe results + findings F1-F4).

---

M5-T082 G4 review — qa-mass. PART 2/3.

EXTRA-SHAPE PROBES (real module, independent invariant re-derivation: directed-edge closure, signed volume>0, cap abs-area vs shapely, volume==area*h, min 3D triangle area)
- U-shape (concave, 2 reflex; suite has none): all invariants hold. Correct.
- Clockwise footprints (RECT/LSHAPE/USHAPE) + a CW LOT ring: all correct (normalized to CCW, +volume). Correct.
- Collinear insert: exact-collinear -> collapses to 4 corners; near-collinear (1e-6) -> kept (5 corners), mesh valid, no degenerate triangle, plate 2399.99998 (correct area of the perturbed polygon, NOT a defect).
- Per-level setback outline (ring_cache path, untested): 2 meshes, invariants hold. Correct.
- Out-of-order level_index: elevations sorted correctly [0,11,23]. Correct.
- Real self-touch lot: refused self_intersection (via duplicate-vertex). 2500 floors: refused over_cap_vertices.
=> NO code defect on any untested shape. Every gap is missing COVERAGE, not wrong output.

FINDINGS
F1 (ADVISORY, strong — recommend before wiring) Concave triangulation is under-tested. The L-shape fixture is star-shaped from vertex 0, so a naive fan passes EVERY concave assertion (closure, abs cap-area==poly 3000, n-2 count). Proof: fan on U-shape -> cap_abs_area 9400 != poly 5200 (would redden); fan on L -> 3000 (does not). M4 is caught only incidentally by the RECT golden; a golden regen would remove the sole guard on concave-safety. Fix: add a non-star-shaped concave fixture (U/plus/comb) to the cap-area + closure + count parametrizations.
F2 (ADVISORY) Clockwise-input orientation normalization untested — all fixtures CCW; M5 survives. Real MapPLUTO rings are frequently CW. Add a CW lot ring + CW footprint fixture.
F3 (ADVISORY) Collinear-vertex collapse untested — M6 survives; no fixture has a redundant collinear vertex (common in real rings). Add one; disabling collapse risks false refusals / degenerate ears.
F4 (ADVISORY, strong — recommend before wiring) Fail-closed containment tolerance boundary unpinned. The only test shifts 200 ft (lot is 100 ft wide), so any tol up to ~110 ft passes. At tol=50 a 30-ft overhang footprint is silently ACCEPTED, uncaught. Add a near-boundary case (e.g. ~0.5 ft outside -> refused; on-lot-line behavior pinned).

Continues in 3/3 (F5-F8 + strengths + verdict).

---

M5-T082 G4 review — qa-mass. PART 3/3.

F5 (ADVISORY, low) Volume local-origin conditioning untested. M8 (world coords) survives because the integer-coordinate fixtures make world==local EXACTLY (recomputed 28800.0 and 260000.0, abs err 0.0). The docstring's numerical-conditioning claim is unverified. Fix: a volume case with non-integer 2263 coordinates, or assert the local reduction is applied. (AS-1 already pins the world_to_local offset metadata; only the volume USE is unpinned.)
F6 (low) test_as4_lot_with_hole_is_refused passes a 1-vertex ring -> reason invalid_source; the lot_has_holes / poly.interiors branch is unreachable from a ring list (a real self-touch yields self_intersection). Branch is effectively defensive/dead; test is misnamed. Optional: a genuine invalid-lot fixture or a comment.
F7 (low) No golden pinned for the multi-floor model; floor ordering is guarded solely by test_as3_five_floor_plate_elevations (M2 was caught by it alone). Consider a 5-floor golden.
F8 (low) MAX_TOTAL_FLOORS cap untested (probe: 2500 floors -> over_cap_vertices; the reason label reads oddly for a floor overflow but it is a typed refusal).

STRENGTHS (why this is a PASS): the directed-edge closure check catches a winding break across all three shapes (M1a); the containment sign-flip is caught broadly (M3); volume==area*h, honest labels (proposed/generated_option, no permitted/maximum-allowed), every typed refusal (footprint_outside_lot, self-intersection, non-finite, over-cap, non-positive height, no_generated_candidate) and the determinism golden are all solid; and the implementation is provably correct on every probed shape (U-shape, all CW rings, collinear collapse, per-level setback, out-of-order levels, floor cap, self-touch).

ASSESSMENT: no code defect found. All AS-1..AS-5 explicit assertions pass and both AS-named mutations (reverse winding, swap heights) are caught. The eight surviving/weak-catch dimensions are additive robustness COVERAGE gaps, not broken required behavior; F1 and F4 are the two I recommend closing before the wiring packet (test-only fixture additions, no code change) — consistent with the G5 defense-in-depth advisories already noted for this task.

M5-T082 G4 VERDICT: PASS
END-OF-REPORT
