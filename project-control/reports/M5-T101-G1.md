# M5-T101 — G1 official-source verification (data-contract-verifier "dc-t101", read-only)

> Transmission history: pinned at c2c4f523, delivered as three SendMessage parts (1/3..3/3) ending with END-OF-REPORT;
> no truncation. Joined verbatim by the orchestrator (transport wrapper tags removed only). Verdict PASS: every registry
> field, both ground-elevation definitions, the NAVD88 attribution, the untrusted-text fields, cadence and terms verified
> against the LIVE official service/FGDC/dictionary/SODA metadata (2026-09-24). Advisories ADV-1..ADV-4 are wording only.

---

M5-T101 G1 official-source verification (data-contract-verifier, READ-ONLY) — part 1/3

HEAD pinned c2c4f523e25906a9528fce35352b653491e348e0 (matches expected; material 6a0f7f53). Read-only; live fetches 2026-09-24. Independently re-fetched: ArcGIS layer/0?f=json, /0/metadata (FGDC XML), the City dictionary raw MD, SODA /api/views/5zhs-2jue.json, plus two live /query probes.

VERIFICATION TABLE — identity & schema (claim | official source | result)

- source_id "nyc-oti-building-footprints-arcgis" == connector SOURCE_ID (building_footprints_arcgis.py:93) | connector + draft | MATCH.
- agency "NYC OTI (formerly DOITT)" | SODA attribution "Office of Technology and Innovation (OTI)"; dictionary Agency OTI | MATCH.
- official_url BUILDING_view/FeatureServer, layer 0 | live service returns layer name "BUILDING" | MATCH.
- companion id 5zhs-2jue; dictionary URL nyc-geo-metadata/Metadata_BuildingFootprints.md | live SODA id 5zhs-2jue; dictionary fetched live | MATCH.
- 12 requested LONG fields + esri types (OBJECTID/OID, DOITT_ID/Int, BIN/Int, BASE_BBL/Str, MAPPLUTO_BBL/Str, HEIGHT_ROOF/Double, GROUND_ELEVATION/Int, CONSTRUCTION_YEAR/Int, FEATURE_CODE/SmallInt, GEOM_SOURCE/Str, LAST_EDITED_DATE/Date, LAST_STATUS_TYPE/Str) | live layer/0 fields array | ALL 12 MATCH.
- NAME, Shape__Area, Shape__Length exist live but deliberately_not_requested; GLOBALID absent (RQ-6) | live fields array | MATCH.
- Truncated-name refusal still LIVE: outFields=HEIGHTROOF,GROUNDELEV,MPLUTO_BBL -> body error {code 400, "Cannot perform query. Invalid query parameters.", details ["'outFields' parameter is invalid"]}, no features | live /query probe | MATCH (connector F1).
- Long-name query BIN=2019299 -> HEIGHT_ROOF 33.49, GROUND_ELEVATION 197, BASE_BBL=MAPPLUTO_BBL "2033800084" (non-condo equal), FEATURE_CODE 2100, GEOM_SOURCE "Photogrammetric" | live /query probe | MATCH fixtures (independently corroborates the recorded bodies are genuine official responses).
- CRS: EXPECTED_WKID 102718 / latestWkid 2263; publication wkid 102100/3857 | live extent.spatialReference {102100,3857}; dictionary "epsg:2263 US foot"; M5-T089-G1 outSR=2263->102718/2263 | MATCH.
- maxRecordCount 2000, supportsPagination true, currentVersion 12, capabilities Query,Extract | live layer/0 | MATCH.

-> part 2/3.

---

M5-T101 G1 — part 2/3 (datum, units, cadence, terms, shape)

DATUM (DB-058 f) — the load-bearing item; BOTH definitions verified LIVE and correctly attributed:
- City dictionary GROUND_ELEVATION = "Lowest Elevation at the building ground level. Calculated from LiDAR or photogrammetrically." | live dictionary | EXACT MATCH.
- FGDC (service /0/metadata) GROUND_ELEVATION = "an interpolated elevation value at the centroid (center point) of the building... a bare-earth surface is generated from the 2010 LiDAR... transferred to the ground elevation attribute." | live FGDC XML | draft's "LiDAR bare-earth value interpolated at the building centroid" is FAITHFUL.
- NAVD88 "when collected photogrammetrically or from modern sources" is stated ONLY by the dictionary; ABSENT from FGDC (searched: NAVD/1988 not present in FGDC) | live both docs | MATCH. Both definitions disclosed, neither chosen -- connector GROUND_DATUM_BASIS (:163-170) and the record agree.
- Zero ground kept as a real 0.0 flagged ground_elevation_zero_unverified (never defaulted) -- honest typed unverified value.

UNITS (RQ-1): HEIGHT_ROOF/GROUND_ELEVATION = feet is an INFERENCE, not a tagged unit | live dictionary + FGDC carry NO per-field unit tag (FGDC Foot_US is the horizontal-CRS unit only) | correctly labeled INFERENCE, RQ-1 kept open. MATCH.

UNTRUSTED TEXT (DB-058 a): untrusted_text_fields ("attributes","geom_source","last_status_type") + escape-on-render notice | connector UNTRUSTED_TEXT_FIELDS (:211) identical; GEOM_SOURCE/LAST_STATUS_TYPE are esriFieldTypeString free-text live | MATCH, honest.

CADENCE: "weekly public release of daily internal edits" | dictionary "Last Update: Weekly" + "Features are updated daily by OTI staff"; SODA Update Frequency "Weekly" | MATCH (ambiguity recorded honestly). Live provenance stamps re-verified against RAW values: dataLastEditDate 1789870561226 = 2026-09-20T02:16:01Z; schemaLastEditDate 1760112642732 = 2025-10-10T16:10:42Z; SODA rowsUpdatedAt 1789972823 = 2026-09-21T06:40:23Z -- ALL match the draft's stated values.

TERMS: SODA license none set (licenseId null); NYC Open Data Terms of Use; Access Rights Public | live SODA license null | MATCH.

SHAPE: the primary record's 21 top-level keys are identical in order to dcm-street-centerline.json's ArcGIS record (draft_status, source_id, self_declared_source_id, ... open_questions); the SODA record omits self_declared_source_id exactly as dcm's SODA record does; superset of dtm-condo.json. MATCH.

-> part 3/3.

---

M5-T101 G1 — part 3/3 (findings + verdict)

No BLOCKING findings. My core mandate (DB-058 a untrusted-text, DB-058 f datum disclosure, DB-058 g registry draft) is fully satisfied and verified against live official sources.

ADVISORY only (none blocks G1; all safe-direction):

ADV-1 -- In the PRIMARY record's fields_available.note, the parenthetical "short shapefile/SODA names (HEIGHTROOF, GROUNDELEV, MPLUTO_BBL, ...)" wrongly lumps SODA with the truncated shapefile forms. The live SODA channel uses FULL lowercased names (height_roof, ground_elevation, mappluto_bbl -- verified in the live columns array), and the draft's OWN SODA record lists these correctly. The load-bearing F1 fact (the ArcGIS service rejects the truncated names; use the LONG names) is TRUE and live-verified. Suggest dropping "/SODA" or clarifying "shapefile-truncated names (the SODA channel uses full lowercased forms)".

ADV-2 -- RQ-4 (capture x-esri-org-request-units-per-min) is listed open, but M5-T089-G1 already captured it live (usage=3198;max=28800). published_limit: null stays correct (that header is an org usage counter, not a published API limit). Minor staleness, conservative direction.

ADV-3 -- The SODA record calls rowsUpdatedAt "~1 day behind" the ArcGIS dataLastEditDate, but SODA's 2026-09-21 stamp is numerically LATER than ArcGIS's 2026-09-20. Raw timestamps are correct and RQ-3 (sync semantics) is open; the "behind" gloss is directionally imprecise. Non-material.

ADV-4 (carry-forward from M5-T089-G1 A4) -- completeness threshold has an internal FGDC ">500 sq ft" variant vs the dictionary's ">400 sq ft"; the draft uses the dictionary's >400 (faithful to its cited source), not encoded in the connector. Immaterial.

Provenance: every draft claim I checked traces to an official publisher (OTI ArcGIS service JSON, service FGDC XML, City metadata dictionary, or NYC Open Data SODA metadata), retrieved live 2026-09-24. Inferences (height unit) and residuals (RQ-1..RQ-6) are labelled; nothing is asserted beyond what the sources state.

Files verified: docs/research/source-registry-drafts/building-footprints.json; services/api/app/connectors/building_footprints_arcgis.py (:93, :119-133, :153-215); cross-checked vs docs/research/building-footprints-source-2026-09.md, project-control/reports/M5-T089-G1.md, and the two existing drafts.

M5-T101 G1 VERDICT: PASS

END-OF-REPORT
