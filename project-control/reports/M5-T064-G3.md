# M5-T064 G3 gate — independent code review (verbatim reviewer return)

Reviewer: code-reviewer (claude-opus-4-8 per D-064), dispatched by the orchestrator at the
seq-124 T064 wave, pinned to the frozen submit head 812a5860 (material 409a8a7d). Recorded
by the orchestrator; the reviewer is read-only and records no gate itself. The return
arrived in four parts (the first transmission truncated in finding 2; the reviewer re-sent
the remainder in three labeled parts) — joined verbatim at the exact break point below.

---

VERDICT: PASS

# G3 Code Review — M5-T064 (D-082-R002 max-envelope engine slice 1)

**Reviewer:** code-reviewer (independent, read-only). **Gate:** G3 (correctness / maintainability).
**Verdict:** PASS (with three low-severity advisory follow-ups; none block acceptance).

## Identity verification (frozen)

- HEAD at review: `a371cae9`. Submit head `812a5860` exists.
- `git diff 812a5860..HEAD -- <5 allowed_paths>` → **empty** (frozen path-scoped content is byte-stable at HEAD).
- `git show 409a8a7d --numstat` → **exactly 5 files**, and the material's own diff equals precisely the 5 allowed_paths (engine 996+, route 340+, tests 274+/642+, report 321+). `409a8a7d` is an ancestor of HEAD.
- Disjoint-peer note honored: **my verdict binds to the frozen path-scoped content of the 5 allowed_paths**; no control-plane commit touches them.

## Evidence I ran (from `services/api`, cwd restored)

| Command | Outcome |
|---|---|
| `python -m ruff check .` | `All checks passed!` |
| `python -m pytest tests/scenario/test_max_envelope.py tests/api/test_max_envelope_api.py -q` | `56 passed` |
| `python tools/modularity_check.py --check` (repo root) | `selected 472 files; failures 0`; `max_envelope.py` = `review_signal` warn (justified), not a failure |
| `grep max_envelope services/api/app/main.py` | NONE — route is genuinely UNMOUNTED |

CI at harvest head `366a50be` reported SUCCESS (18 jobs) per the packet; my local run corroborates the scoped surface.

## G3 findings

### 1. Dimension-resolution chain — CORRECT and provenance-faithful (PASS)
`_select_binding` (max_envelope.py:448) picks the **tightest** usable allowance: `min(usable, key=(value, rule_id))` for MAXIMUM, `min(usable, key=(-value, rule_id))` for MINIMUM — the every-declaring-rule intersection, deterministic tie-break by lowest rule id. Fail-closed branches are honest: ≥1 applicable but 0 usable → `ALLOWANCE_UNRESOLVED`; 0 applicable → `NO_APPLICABLE_RULE` (never an invented ceiling). `out_competed` correctly excludes only the binding id. Verified by AS-1/AS-2 including the mutation-flip (`test_as2_mutation_flips_the_binding_rule_id`: cov 0.5/0.4 → binds cov-b) and the MINIMUM-largest-floor test.

Provenance is faithful: values are taken through `registry.evaluate(...).export()` (max_envelope.py:434), which fail-closes on any citation lacking `content_digest_sha256` (proposal_checks parity; models.py `export`). No hand-copied constants: `_USABLE_COVERAGE` is bound byte-identical to the accepted checker's by the drift-guard test (`test_usable_coverage_matches_accepted_checker`), and `CALLER_RULE_INPUT_NAMES` / `RULE_INPUT_LOT_AREA_SOURCE` are **imported** from `proposal_checks`, not duplicated.

### 2. Geometry core — CORRECT, deterministic, honest about the rectangle-prism simplification (PASS)
`_lot_rectangle` (max_envelope.py:624) proves the supplied geometry is a simple axis-aligned rectangle whose bbox area is consistent with the recorded `area_sq_ft` (the coverage denominator), refusing diagonal/zero-length segments, interior vertices, a missing side, or an area mismatch — each a typed `LOT_GEOMETRY_UNSUPPORTED` gap, never coerced into a schematic. `_shrink_depth_to_cap` shrinks **only depth** (width preserved, so lot containment cannot be broken by the fit) until the checker-computed coverage is ≤ cap; containment is then re-proven and the block re-validated with the accepted B0 validator as defense in depth. The rectangle-prism limitation and the FAR/rear-yard non-commensurability are disclosed in `ENVELOPE_DISCLOSURE` and the per-dimension `semantic_gap`.

**Parity independently confirmed** (load-bearing for AS-4): the engine's `_shoelace_area` is byte-identical in float order to `derivation._shoelace_area` (`total += x1*y2 - x2*y1; abs(total)/2.0`); coverage is `footprint_area / lot_area` in both; and cumulative height in `derivation._derive_heights` sums `floor_to_floor_ft * floor_count`, which the engine's `_height_levels` saturates at-or-under the binding value. So a candidate saturating the tightest allowance is what the checker re-computes.

### 3. Generator-checker consistency proof — GENUINE, not a stub (PASS — the packet's core proof)
`_verify_consistency` (max_envelope.py:900) calls the real `check_proposal` on the emitted candidate and raises a typed `MaxEnvelopeError(field="candidate.*")` if any saturating dimension (`lot_coverage_ratio`, `building_height`) returns `FAIL`. AS-4 proves both saturating checks PASS at the derived values and a +epsilon mutation FAILs each. The tolerance of `COULD_NOT_CHECK` is sound: it is only reachable via the multi-rule `AMBIGUOUS` case, where the engine binds the **tightest** value, which by construction satisfies every usable rule (`test_as4_tolerates_ambiguous_checker_outcome_for_multi_rule_family`).

### 4. Honest gaps + typed conflict advisory (PASS)
Every declared dimension is enumerated (AS-3); each is a binding value XOR a typed gap, never both/neither. `_conflict_advisory` surfaces the registry's own fail-closed FH-2 detector (getattr-guarded) as an advisory and **never resolves a winner**. Route serializes all four gaps into the 200 body.

### 5. Route discipline (PASS)
Flag-gated OFF by default via the reused `internal_rule_eval_enabled()`; absent flag → generic `{"detail":"Not Found"}` 404 with no correlation id (byte-indistinguishable from an unmounted path). UNMOUNTED confirmed (main.py reference-free; `test_route_is_unmounted_in_the_real_app` asserts absence from routes + OpenAPI). Bounded body via reused streaming primitives; strict-JSON/NaN guard with the response renderer's own encoder settings; typed bounded refusals (BP-2/3/4/5) reusing the accepted `_FieldRefusal`/`_build_lot_context`; single engine call off the event loop (`run_in_threadpool`); status matrix single-sourced in `MAX_ENVELOPE_STATUS_STATE_MATRIX`. Correctly routes a generator-checker inconsistency (`candidate.*`) → 500 (internal invariant) vs a precondition → 422 (caller fault). No persistence.

### 6. Modularity (PASS with recorded justification)
999 lines → `review_signal` (above 750 justify, below 1000 hard-fail), `failures 0`. One public entry, one responsibility, three tightly-coupled stages sharing the shoelace/float-order contract with the co-located consistency proof; splitting the geometry stage would fork that contract from its sole consumer. Justification is recorded in the report §"Cohesion" and is defensible for this slice.

## Advisory follow-ups (LOW severity — non-blocking)

1. **Multi-floor height path not covered by the full consistency proof.** The fixtures bind height = 60 ft (single floor), so `_height_levels` for height > `MAX_FLOOR_TO_FLOOR_FT` (100) is proven only by its unit invariant (`ftf*n <= height`), not by running `check_proposal` on a multi-floor candidate. The logic is sound (cumulative <= binding <= MAXIMUM allowance -> PASS), but a follow-up test driving a >100 ft binding height through the generator-checker proof would close it.
2. **`_conflict_advisory` populated branch (max_envelope.py:526-529) appears untested.** The fixture registry is single-rule-per-family and `_FakeRegistry` has no `detect_conflicts`, so the dict-returning advisory branch is not exercised. Advisory-only (never resolves), so low risk, but a test with a conflict-producing registry double would add mutation sensitivity.
3. **`_LOT_AREA_INPUT` re-defined, not drift-guarded.** The engine redefines `_LOT_AREA_INPUT = "lot_area_sq_ft"` (the checker's identical private constant is not imported/pinned). Low risk (stable rule-DSL input name), but a one-line drift-guard mirroring the `_USABLE_COVERAGE` test would close the theoretical divergence.

## Conclusion
The binding-selection, provenance, honest-gap, and geometry machinery is correct and faithful; the core AS-4 generator-checker consistency proof is genuine and fail-closed; the route mirrors the accepted sibling discipline and is truly unmounted. Ruff clean, 56 tests pass, modularity exit 0. The three notes are advisory test-coverage improvements, not defects. **G3: PASS.**

--- END OF REPORT ---
