# M0-T100 — G5 Independent Security Review

> Saved VERBATIM by the orchestrator from the security-reviewer agent-return channel
> (transport entity-decoding only: harness-neutralized `<`/`>` restored). Verdict: PASS.

**Task:** M0-T100 — passive statusLine wiring activation (D-027; activates M0-T099 report §4 owner step)
**Gate:** G5 (security review) · **Reviewer:** security-reviewer (independent, read-only) · **Date:** 2026-08-26 UTC
**Reviewed content identity (frozen):** commit `a0b945ec6b6e90843d72fd891ab7524bc2f781d6`; material identity `3089df1386eb5cdaafc3a35416b1521b7c67e14b13a388cdb48babcf7c4ea0cd` (recorded at G2)
**Live checkout HEAD:** `8749fae440e30d0c3f74f76a4977ab181551818d` — verified = frozen content + control-plane-only commits, zero wiring drift (see D1/D7)
**Repo:** PUBLIC · branch `control/D-024-fable-codex-loop`
**Verdict:** **PASS**

## Scope and method

Independent read-only reproduction (never trusting the producer's conclusion). All commands run at the frozen SHA / live checkout. The read-only guard correctly blocked every mutation attempt (including scratchpad file-writes); I reproduced write-dependent proofs in-memory instead. Commands executed: `git log/diff/status/ls-files/check-ignore/grep`, `python -m pytest tools/test_agent_supervisor_statusline_handler.py -q`, `python -c` (in-memory handler invocation), `hashlib`/`json` verifications.

## Dimension-by-dimension findings

**D1 — Wiring diff surface: PASS.** Task range `8bb829c..a0b945e` (12 files, +790/-1) touches ONLY: `.claude/settings.json` (+4, the statusLine block), `.gitignore` (+3, the telemetry rule), the D-027 directive records, the M0-T100 control-plane records (task/gate/reports), and `state.json`. `git diff --name-only … -- tools/ .claude/hooks/` is empty. The handler `tools/agent_supervisor/telemetry_statusline.py` has an empty diff across the range and was last touched by M0-T099's content commit `00f2519` (byte-identical, not modified). No M0-T099 accepted artifact (`M0-T099*` task/report/gate) changed. Frozen→HEAD delta (`a0b945e..HEAD`) is control-plane only (G2 gate/report/evidence-map/state/task-status).

**D2 — Public-repo leak surface: PASS.** The runtime dir `.claude/telemetry/` is reliably gitignored: rule at `.gitignore:89`; `git check-ignore -v` matches the sidecar, the journal, and the directory; `git ls-files .claude/telemetry/` is empty; `git status --porcelain .claude/telemetry/` is empty. The committed settings block and activation report contain no secrets/credentials. Committed home paths (`C:/Users/MLFLL/…`) appear in 3 new files (task `worktree` field + report §5 global-fallback reference + G0 readiness), matching pervasive existing convention (153 tracked files, incl. `CLAUDE.md`, `SESSION_HANDOFF.md`) and explicitly permitted by the review scope; they are NOT in the telemetry artifacts. `settings.json` parses as valid JSON. See Finding 2 (nit).

**D3 — Live runtime artifacts: PASS.** Inspected the real live sidecar and 73-record journal (this session `4333c462-…`, the running Fable-5 TUI). Masking present in both slash and dash-encoded forms (`cwd` → `[HOME]\Downloads\nyc-zoning\ctl24`; `transcript_path` → `[HOME]\.claude\projects\[HOME]-Downloads-nyc-zoning-ctl24\<uuid>.jsonl`); sidecar `redaction_count: 3` matches the 3 `[HOME]` tokens present (222 in the journal). Byte-level scans over BOTH full files: `MLFLL` 0, `Users` 0, `C:\` 0, `C--` 0, credential shapes (`ghp_|sk-|Bearer|AKIA|xoxb|-----BEGIN`) 0. The retained `session_id`/transcript UUID disposition is SOUND — see Finding 1.

**D4 — Execution surface: PASS.** The statusLine command is a fixed literal string in tracked settings: `python -m tools.agent_supervisor.telemetry_statusline --journal .claude/telemetry/statusline_journal.jsonl`. No shell metacharacters; relative journal path; no network; no subprocess. `main()` returns 0 unconditionally and catches every exception into a `telemetry ?` row (exit-0-always verified in code and by 23/23 tests). Critically, the sidecar/journal write paths come ONLY from argv defaults, never from the stdin payload — so there is no path-traversal or write-redirect vector from the (semi-trusted) status JSON. Imports are stdlib (`argparse`, `json`, `sys`, `typing`) plus in-repo relative modules only. Trust model: `python -m` with cwd = project dir resolves the in-repo module; changing what it executes requires landing a reviewed change to tracked repo source — the SAME trust boundary as the existing `PreToolUse`/`SessionStart`/`UserPromptSubmit` hooks. Attack surface is local-machine only (no remote vector).

**D5 — Passive posture: PASS.** The handler has NO actuation path: no agent-stop, no session rotation, no model change, no supervisory call — it ingests stdin, writes the sanitized sidecar/journal, prints one row. `tools/agent_supervisor/**` is unchanged (supervisor freeze not triggered; tree hash unchanged). Continuous/autonomous activation (D-024 §18, R595 prerequisite) is untouched; the supervisor remains SHADOW-ONLY.

**D6 — Precedence claim: PASS (internal consistency verified).** The project statusLine block exists only in this repo's `.claude/settings.json`. The owner's global `~/.claude/settings.json` sits OUTSIDE the repo root (`C:/Users/MLFLL/.claude/…` vs repo `C:/Users/MLFLL/Downloads/nyc-zoning/ctl24`), so it is structurally impossible for this task's diff to have modified it — the "global untouched" claim is guaranteed by the diff surface, independent of the report. I verified the claim's internal consistency (report cites sha256 `32c6fb00…`, mtime `04:29:57Z` predating the first task write ~`05:48Z`); I did not and cannot read the external file, and correctly did not attempt to.

**D7 — Report claim reproduction: PASS.** Reproduced read-only: (a) `git check-ignore` rule at `.gitignore:89`; (b) masking scans 0 hits (report captured 33 records; now 73, still 0); (c) `pytest … -q` → **23 passed / 0 failed** (Python 3.11.9); (d) source sha256 = `c9c203d63cd42548bf8aaa6a4eb2f69b84c8aab33c0d8ee4fba590aed2ef9a9a` (matches report's `c9c203d6…ef9a9a`); (e) two-output row reproduced in-memory from the committed REAL fixture: `Fable 5 xhigh | ctx 4% of 1.0M | sess $0.78 1m | 5h 29% 7d 33% | v2.1.220` with zero leak tokens in the row, and one-feed linkage (`ctx_used_pct=4`, `window=1000000`, `cost=0.7828…`) confirmed structurally (sidecar write precedes row return in `handle_status_line`) and by value; (f) no wiring drift `a0b945e..HEAD`. The global sha256 `32c6fb00…` is the only claim I could not independently reproduce (external file, correctly out of read scope) — internal consistency only.

## Findings

1. **[minor / advisory — NON-BLOCKING]** The local sidecar/journal retain `session_id` and the transcript-file UUID unmasked (e.g. `4333c462-2d61-4cd5-8440-56438b964771`). Owner requirement D-027-R007 lists "session identifiers" among items not to be written unmasked, but the SAME requirement provides an explicit escape valve: "any residual identifier class explicitly reported to the owner rather than silently passed." Report §4 does exactly that (dedicated "Explicit disposition — retained identifiers" paragraph). **Security assessment: SOUND.** The UUIDs are opaque, random, non-credential correlation tokens (not username/email/path/secret); they live ONLY in `.claude/telemetry/`, which is verified gitignored, untracked, and never committed/pushed — so the public-repo exposure surface is structurally nil, and the residual exposure is the owner's own machine. Retaining a stable session key is a legitimate need for future Codex per-session correlation. *Remediation: none required for security. Recommend the directive-compliance-verifier formally adjudicate R007's session-identifier clause against the disclosure escape valve; from security posture this disposition is acceptable as-is.*

2. **[nit — NON-BLOCKING]** Three new committed control-plane files embed the owner home path `C:/Users/MLFLL/…` (task `worktree` field; report §5's `…/.claude/statusline.ps1` precedence reference; G0 readiness). This is a public repo. *Assessment: within scope and not a new leak class* — it matches pervasive established convention (153 tracked files already contain this path, including `CLAUDE.md`) and is explicitly permitted by the review brief. Flagged only for awareness that the repo's public posture already exposes the username/home path repo-wide (pre-existing, outside this task's remit). *Remediation: none for this task.*

## Conclusion

The change is a minimal, correctly-scoped, passive wiring activation. It adds no code, does not touch the frozen M0-T099 handler or the supervisor tree, introduces no actuation surface, keeps the runtime telemetry reliably out of git, masks all home paths in the live artifacts (0 leak-token hits), and confirms exit-0-always with no injection/SSRF/path-traversal/network vector. The one retained-identifier disposition is explicitly disclosed per the owner's own requirement wording and is security-sound because the artifacts never enter the public repo. No blocking or major security issues.

**VERDICT: PASS** (2 non-blocking advisories; the R007 session-identifier interpretation is properly the directive-compliance-verifier's to record, and is security-acceptable).
