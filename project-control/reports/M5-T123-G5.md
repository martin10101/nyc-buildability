# M5-T123 — G5 security review (security-reviewer "sec-t123", read-only)

> Transmission history: pinned at d1970b06 (material de92182d; CI run 36115519503 at d1970b06: api job green, web-e2e
> red on one unrelated Playwright focus test - the zero-delta rerun 36116139678 at b0eb29f7 was fully green),
> delivered as 4 SendMessage messages ending with END-OF-REPORT (3 report parts + a post-verdict CI acknowledgement); no truncation. Joined verbatim by the orchestrator
> (transport wrapper tags removed only). Verdict PASS; 0 Critical / High / Medium; Finding 1 LOW (the
> report's DXF memory figure is optimistic - measured ~17x input, the max_lines-governed pairs list), Findings 2-3 INFO; the
> PKT-H mount carry list.

---

M5-T123 G5 security review [1/3] — scope + tests. READ-ONLY.

PIN: HEAD d1970b06089418d8c75af4c28650cf612aa8030f (matches expected). Material de92182d (cherry-pick of wt-m5t123 8d226b2f); the submit/G2 seam d1970b06 (and its disjoint peer parent, M5-T121 gate records) touch only ledger/report files — task file identity is byte-stable.

METHOD: read all 4 production + 4 test files + the producer report at HEAD; verified the enforcement primitives in proposal_validation.py and the read path in dxf_reader.py (both READ-ONLY, unchanged in this packet); ran the documented suite; ran an in-process tracemalloc probe on the DXF reader (nothing written to the repo).

TESTS (cwd services/api): `python -m pytest tests/resilience tests/scenario/test_scene_api.py tests/drawings/test_dxf_import_api.py tests/cad/test_export_api.py -q -p no:cacheprovider` -> 199 passed, 0 skipped. The slot-sum guard and principal-hygiene tests are non-vacuous (details in part 3).

SCOPE (AS-5) — all clean:
- Material diff = exactly the 9 allowed paths (4 prod, 4 test, 1 report); no forbidden path touched.
- app/main.py has NO reference to the three routers -> routes stay UNMOUNTED (confirmed by grep).
- proposal_validation.MAX_BODY_BYTES stays 262144 (256 KiB); the three routes import only the primitives (_bounded_message / _declared_content_length / _read_body_within_ceiling), NOT the constant.
- dxf_reader.py absent from every task commit -> READ-ONLY honored. _IMPORT_DXF_LIMITS raises max_bytes to 20 MiB, within the reader's 64 MiB hard clamp (_LIMIT_CEILINGS); every other DxfLimits field keeps its default.
- Zero new dependencies (stdlib + already-admitted starlette/anyio); no requirements change.
- No request body / caller text is logged: only correlation ids, state strings, bounded field names, a fixed service reject_code enum, and an int declared-length. X-Forwarded-For never trusted (test-proven).

Result: no CRITICAL / HIGH / MEDIUM findings. Three ADVISORY (mount-carry) items follow in parts 2-3.

---

M5-T123 G5 review [2/3] — DoS/enforcement + Finding 1.

DoS / ENFORCEMENT — SAFE:
- Order per route: flag-404 -> method -> rate limit -> body ceiling (declared fast-path + streamed) -> parse -> slot+deadline. The rate limit precedes any body read on all three (dxf via _gate_and_limit, ahead of _parse_assignment and _read_body_and_run) — DB-081(b) honored.
- Body never buffered past the ceiling: _read_body_within_ceiling (proposal_validation.py:203) counts each chunk and returns (b"", True) on the FIRST over-ceiling chunk WITHOUT appending it or draining the rest, so peak transient = ceiling + one Starlette chunk. Both the declared-Content-Length fast path and the streamed accumulator enforce; a lying/absent length is caught by the stream.
- Slots: 8+12+4 = 24 < anyio's 40 (read LIVE in-test), 16 headroom, heavier->fewer asserted. run_in_job_slot releases the slot EXACTLY once: worker `finally` when started, cancel-path when cancelled pre-start (the pre-token-wait case), idempotent via a `released` flag — no leak, no pool starvation; a held slot always implies an available token since 24<40, and the pre-start-cancel await is itself under the deadline.
- DXF reader work is bounded: single-pass O(n) tokenizer (the prior O(n^2) fixed), max_lines / max_line_chars enforced WHILE scanning (generator), max_entities/vertices in _walk. The raised 20 MiB byte ceiling opens no superlinear path.

FINDING 1 (LOW / ADVISORY / mount-carry) — the report's DXF worst-case memory arithmetic is optimistic; the ~312 MiB total is NOT credible for dxf. The report models dxf at ~1.2x raw buffer (~24 MiB/slot, ~96 MiB for 4 slots). But the DXF reader's peak is dominated by _to_pairs (dxf_reader.py:589-605), which builds a list[tuple[int,str]] of ALL group-code pairs before _walk, bounded by max_lines=2,000,000 (UNCHANGED by this packet). MEASURED (tracemalloc, dense DXF through read_dxf): peak ~17x input bytes (~32.6 MiB for a 1.91 MiB / 1,000,000-line input). Raising max_bytes 8->20 MiB makes the full 2M-line cap reachable for dense files (the old 8 MiB ceiling capped effective lines lower), so per-dxf-slot peak is ~65-150 MiB, not ~24. 4 slots alone >= ~260 MiB; the 3-route aggregate worst case EXCEEDS the report's ~312 MiB. BOUNDED (max_lines is a hard clamp) and routes UNMOUNTED -> advisory, not blocking. This is DB-D4's mechanism, which the memory table did not carry.

---

M5-T123 G5 review [3/3] — principal, mount-carry, verdict.

FINDING 2 (INFO / ADVISORY / mount-carry) — principal keyed on the RAW value. caller_key (rate_limit.py:225-226) correctly gates on `isinstance(principal, str) and principal.strip()` (the whitespace-collapse fix, test-proven: "", space/tab/newline/mixed, and non-string types all -> host key), but returns f"principal:{principal}" with the UNSTRIPPED value (" u " -> "principal: u "). Safe now (routes UNMOUNTED; the fix removes the collapse-into-one-bucket bug). Mount-carry: PKT-H must set request.state.principal to a canonical, server-authenticated, non-caller-influenced id; a caller-influenceable or whitespace-carrying principal could otherwise mint non-canonical / distinct keys. Already flagged in the docstring.

FINDING 3 (INFO / already-documented / mount-carry) — pre-auth host-keying collapses all callers behind Render's shared proxy into one bucket; X-Forwarded-For correctly not trusted (spoofable). This is exactly why the route family stays UNMOUNTED until PKT-H supplies auth. No action for this packet.

WHAT PKT-H MUST STILL CARRY:
1. Instance sizing from the max_lines-governed DXF peak (~65-150 MiB per dxf slot, not ~24) + scene ~144 MiB + export ~72 MiB; confirm the Render instance size (not in repo) covers the true aggregate (>~312 MiB) under a real load test.
2. The authenticated, server-set, non-empty, CANONICAL principal that flips caller_key from host to principal (real per-caller isolation; the pre-auth host key collapses the shared-proxy callers).
3. DB-D4 co-sizing: raise max_lines with max_bytes for genuinely large real files, OR lower max_lines to match the 20 MiB byte budget so the memory peak matches the sizing.
4. Confirm the app's other run_sync users stay within the 16-token anyio headroom when the three routes are live.
5. Register DXF_IMPORT_ENABLED in config.py (declared in-route; registration deferred to the mount).

SUMMARY: the packet's code is safe, bounded, correctly scoped (exactly the 9 allowed paths), routes UNMOUNTED, proposal_validation untouched at 256 KiB, zero new deps, no body logging, XFF untrusted, enforcement order correct, body never buffered past ceiling+1-chunk, slots summed under the pool with leak-free release, DXF reader bounds intact. All three findings are ADVISORY mount-carry items; none blocks. (Note: could not write agent-memory — Write disabled; skipped per rules.)

M5-T123 G5 VERDICT: PASS
END-OF-REPORT

---

M5-T123 G5 — CI note acknowledged; VERDICT UNCHANGED: PASS.

Independently verified the web-e2e failure is out of scope for M5-T123:
- Zero apps/ files in the T123 material (de92182d); the web-e2e app is Next.js and loads none of the changed services/api Python (app/main.py imports neither rate_limit.py nor the three unmounted routes).
- apps/ diff d1970b06..b0eb29f7 is EMPTY, so the failing spec (a11y-announcements.spec.ts:152) is byte-identical at both shas — the rerun is a clean zero-delta read of the SAME frontend.
- The four T123 production blobs are byte-identical (MATCH) at d1970b06 and b0eb29f7, so the rerun exercises the same reviewed code.
- The failure signature (activeElement == <body> during the loading stage) is the known tabUntil/focus-never-body false-match flake, not a regression.
- The relevant CI signal for this packet — the api (ruff + pytest) job — passed; my local run was 199 passed.

One nuance: b0eb29f7 is NOT a whole-tree zero-delta — it adds peer work under services/api/app/drawings/sheet_*.py + tests/drawings/test_sheet_*.py (a different task). That is disjoint from M5-T123 (all four T123 blobs MATCH; apps/ empty), so it does not affect the web-e2e judgment or my verdict; just don't read its api-job result as M5-T123's.

No re-send of the report parts. Final: M5-T123 G5 VERDICT: PASS.
