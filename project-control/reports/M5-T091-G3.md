# M5-T091 + M5-T092 — G3 independent code review (code-reviewer "cr-w2", read-only; joint review)

> Transmission history: one joint review pinned at 4c999e15 (six blobs verified identical to the material
> commits d0919de6 / 706ae62a), delivered as three SendMessage parts (1/3..3/3) ending with END-OF-REPORT;
> no truncation. Joined verbatim by the orchestrator (transport wrapper tags removed only); saved
> identically as M5-T091-G3.md and M5-T092-G3.md. Verdicts: both PASS (advisories only).

---

G3 CODE REVIEW (cr-w2, read-only) — M5-T091 + M5-T092. Part 1/3.

PIN: HEAD 4c999e15 at start. All six blobs byte-identical to their material commits (verified via git rev-parse HEAD:<path>):
- T091 d0919de6: pdf_sheet_writer.py 86b129c9, test 52f37aa9, report 3b402b53 — MATCH.
- T092 706ae62a: glb_writer.py 64568629, test 9166d160, report 8f390845 — MATCH.
Local (Py3.11, services/api): ruff clean; `pytest tests/cad -q` = 262 passed. CI 3.12 is the authority.

== M5-T091 (PDF site-plan writer hardening, DB-053 a-d) — correctness ==
(a) Malformed vertex — `_validate_ring` (:260) + `_coerce_vertex` (:291): non-sequence/wrong-arity → invalid_ring; non-Real/bool/complex/Decimal/numeric-string → non_numeric_coordinate; huge-int OverflowError → oversize_input; non-finite → non_finite_coordinate; magnitude → oversize_input. Never raises. The per-field "any hostile value returns bytes or refusal" sweep (test:388) enforces the docstring promise. CORRECT.
(b) Finite guard — `_num` (:453) raises the internal `_RenderRefused`, caught only at the public boundary (:211); every `_num` call site (`_build_content`, `_assemble_pdf`) is inside that try. Both paths forced by monkeypatch (test:407). CORRECT.
(c) Claim screen — `_screen_caller_text` (:224) runs before any drawing (:164), screens all four caller strings on TWO keys: raw and ASCII-sanitised, each with non-[A-Z0-9] runs collapsed to one space (`_claim_key` :252). I verified the PDF match set is a strict SUPERSET of the DXF writer's plain `word in upper` check across the whole barred vocabulary (space- and hyphen-forms included) — no honesty gap vs the sibling; the ſ→"?" printed-form case is caught only by the sanitised key and mutant M3b reddens exactly there. Word set pinned by identity (`is`) AND a hardcoded literal (test:458). Coupling = read-only import of dxf_writer.CLAIM_CLASS_WORDS, which the packet explicitly authorises and forbids only EDITING. I confirmed the tuple is byte-identical at all three sites (dxf source, glb dup, T091 pin). CORRECT & SOUND.
(d) Escaper — `_escape_pdf_text` (:424) is the sole text path; unbalanced parens round-trip through the REAL in-repo strict reader (test:517); paren-skipping mutants redden (test:542). CORRECT.

No regression: golden c38360f9 unchanged (test:40); all 19 original tests preserved & green; ~408 SLOC < 600; zero new deps; unwired (no production importer). Part 2 next.

---

G3 CODE REVIEW (cr-w2). Part 2/3.

== M5-T091 findings ==
F1 ADVISORY (disclosed Deviations 1-2): numeric-string / bool / Decimal coordinates and non-list/tuple ring containers (generators, sets, numpy arrays) are now REFUSED where accepted M5-T085 silently coerced them. This is off-contract input (declared types float / tuple), on-contract golden bytes are unchanged, and the direction matches ratified DB-054(o) ("parse-to-float belongs in the wiring packet"). Acceptable; wiring must surface these refusals honestly (already DISCOVERY D3).
F2 ADVISORY: PDF writer now imports the DXF module (module coupling). Sound, packet-authorised, drift-guarded; a shared honesty-vocabulary module would remove it (DISCOVERY D1).
F3 ADVISORY: no caller-text length bound (DISCOVERY D2) — a very long address overflows the title block. Pre-wiring rider, out of scope here.
No BLOCKING findings.

M5-T091 G3 VERDICT: PASS

== M5-T092 (zero-dependency GLB writer) — correctness ==
Identity verified (Part 1). 
AS-1 structure: header magic/version/total-length, chunk order JSON→BIN, 4-byte start/end alignment, JSON 0x20-pad / BIN 0x00-pad, asset.version "2.0". I re-parsed the bytes with my OWN parser (no writer helpers): all hold; golden 30d79d80 pinned + deterministic. CORRECT.
AS-2/AS-3 units+axes: (east,north,up) ft → glTF (x, z, −y) m × 1200/3937 (US survey foot, explicitly ≠ 0.3048). I INDEPENDENTLY recomputed a probe: east100→+X 30.480061, up300→+Y 91.44, north200→−Z −60.96 — exact. Map determinant +1, so winding is preserved (winding test confirms an up-face → +Y normal). Factor + axis map + local origin recorded verbatim in asset.extras; scene/generator/label carry "Proposed - not a city record". CORRECT & honest.
Float32 honesty: positions stored IEEE-754 single; accessor min/max computed from the float32 values and are float32-stable (I confirmed acc.min == per-axis min of the decoded BIN and f32(min)==min); ±100,000-ft local cap keeps the float32 step ≤ ~2 mm and refuses un-localized EPSG:2263 world coords; index accessor correctly omits min/max. Honestly handled.
AS-4 fail-closed: non-finite, index-out-of-range (also excludes the uint32 restart value), degenerate (sine ≤ 1e-6 on float32), empty, over-cap. Caps enforced from len() BEFORE any element is read — proven by the `_Unreadable`/`_Outrunning` tests (DoS-resistant). Typed GlbValidationError w/ registered code; no partial output (assemble spy). CORRECT.
AS-5 scope: stdlib-only (AST test), unwired (AST test + my grep), 3 allowed paths, ~450 SLOC < 600. Part 3 next.

---

G3 CODE REVIEW (cr-w2). Part 3/3.

== M5-T092 findings ==
F1 ADVISORY (the packet's raise-vs-return question): T092 RAISES typed GlbValidationError while T091 RETURNS SitePlanRefusal values. Each satisfies its own packet's "typed refusal" wording and is internally consistent — T091's DB-053(a) SPECIFICALLY requires never-raise on caller data, whereas the GLB/DXF siblings raise. Disclosed (Deviation 5). The later packet that consumes both writers must reconcile the two error surfaces; already routed (DB-054(o) / DISCOVERY 3). Not blocking.
F2 ADVISORY: the official Khronos glTF-Validator was NOT run (thin client, no local npm); routed to harvest with an exact recipe. Structural conformance is proven by the in-test parser against cited spec sections and by my independent re-parse. Recommend the wiring/mount packet run the official validator before any GLB is served to a browser. Not blocking G3.
F3 ADVISORY: default material (metallic 0 / rough 1) with doubleSided=false means callers must supply outward-CCW winding, and the vertical datum for local z=0 is unowned — both correctly deferred to the wiring packet (DISCOVERIES 1-2).
F4 INFORMATIONAL: app/cad/__init__.py docstring lists dxf_writer + pdf_sheet_writer but not glb_writer. __init__ is a forbidden/orchestrator-owned path, so the producer correctly left it untouched (this is also why the AS-5 not-wired test passes); the orchestrator should add glb_writer to that docstring at a later seam.
No BLOCKING findings.

M5-T092 G3 VERDICT: PASS

== Summary ==
Both tasks: identity verified, scope exactly the 3 allowed paths each, zero new dependencies, unwired, ruff clean, modularity 0 failures, 262 tests pass locally, every DB-053(a-d) guard and every GLB AS-1..AS-5 requirement is correct and mutation-guarded, no regression of accepted behaviour. All findings are ADVISORY (disclosed deviations already routed to DISCOVERIES / DB-054) — none blocks acceptance.

M5-T091 G3 VERDICT: PASS
M5-T092 G3 VERDICT: PASS

END-OF-REPORT
