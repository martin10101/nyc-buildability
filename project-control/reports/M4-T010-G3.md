<!-- Reviewer return preserved VERBATIM by the orchestrator (report-preservation rule,
.claude/rules/project-control.md). Reviewer: independent data-contract-verifier subagent
(G3), returned 2026-09-11, reviewed HEAD 79f06fde. -->

All verification items are confirmed. Here is my complete G3 data-contract gate report.

---

VERDICT: PASS

# G3 Data-Contract Gate Report — M4-T010 (rule-citation content-digest binding)

- **Task:** M4-T010 — "Rule-citation content-digest binding: additive schema property + fail-closed loader verification"
- **Reviewed HEAD:** `79f06fde3a56aae2ffef7016e24a5f138fc0940f` (branch `candidate/D-024-mrl-option-b`)
- **Producer material commit:** `7bac23c1`; orchestrator record commits `79734f11` (evidence map), `79f06fde` (submit)
- **Reviewer role:** independent G3 data-contract verifier (did not author). Read-only; no pytest/ruff/git-write/project_control.py run. All validation done with stdlib `python -c` + read-only `jsonschema` and read-only `git show/status`.
- **Working-tree drift:** none — all reviewed source files are clean at HEAD (`git status --porcelain` empty for the four reviewed paths).

## 1. ADDITIVITY — PASS
Enumerated `services/api/app/rules/rulesets/*.rule.json` myself: **11 files** (matches packet S1 "currently 11" and the test's `_KNOWN_RULE_FILES=11`). Validated every one against the NEW schema with `Draft202012Validator`: **0 invalid**. Confirmed **none carries `content_digest_sha256` yet** (carriers list empty across all 11). The change is a single optional property added to `citations[].items.properties`; `additionalProperties: false` is retained on the citation item; `required` for citations is unchanged (`snapshot_id`, `section`, `quote`). No existing property or constraint was altered. Purely additive — every committed rule stays valid.

## 2. PATTERN CORRECTNESS — PASS
Pattern `^[0-9a-f]{64}$` = exactly 64 lowercase-hex chars. Verified the named snapshots `zr-23-21` and `zr-23-22` in **both** locations (`services/api/app/_zr_snapshots/v1/` and `docs/research/zr-snapshots/v1/`): each `content_digest_sha256` matches the pattern **and** equals `sha256(verbatim_excerpt)` recomputed by me. I extended the check to all 7 snapshots in both trees — every one is `pat=True eq=True`. (zr-23-21 stored/recomputed = `b52771e6…a48ad8a`, consistent with the in-file DIGEST CORRECTION note that moved it to the sha256(verbatim_excerpt) v1 convention.)

## 3. SEMANTICS — PASS
`_check_refs` diff (dsl.py lines 61-76): the citation loop now binds `snap = snapshots.get(citation["snapshot_id"])` (the get() call is byte-identical to the prior discarded call — **SnapshotError precedence preserved**, resolution runs before any digest logic), then `recorded = citation.get("content_digest_sha256")` and raises `DSLError` only `if recorded is not None and recorded != snap.content_digest_sha256`.
- **Presence-gated:** `recorded is not None` — absent field skips the comparison entirely.
- **Absent-field path byte-identical:** the only additions are two side-effect-free assignments; when the field is absent, no new raise, no new resolution — behavior equals pre-change.
- **Compares against the snapshot's STORED digest, no rule-side recompute / no alternate basis:** the comparison target is `snap.content_digest_sha256`. In `snapshots.load_snapshot_file`, that value is the snapshot's own stored digest, tamper-verified (`stored != sha256(excerpt)` fails closed with `SnapshotError`) — it is the snapshot's authoritative stored digest, not a rule-side recomputation and not a derived alternate basis. The `DSLError` names the rule id, the snapshot id, and both digests. Fail-closed at LOAD (raised inside `build_rule_definition` before a `RuleDefinition` is returned) — the rule never becomes evaluable.

## 4. VERSIONING DISCIPLINE — PASS
The schema lives in `schemas/v1/`. An additive **optional** property that keeps all older payloads valid is the canonical non-breaking contract change and correctly stays inside v1 with no directory bump. This matches the established repo precedent: M4-T003 already added `minimum/maximum/exclusive_minimum/exclusive_maximum/effective_from/effective_to/release/determination` as optional properties to this same v1 schema. The house rule's "bump the version" governs breaking changes (a new major `v*/` directory); the schema carries no internal version field to bump, and rule documents version themselves via `rule_version`. Nothing about this change is non-additive: no new required field, no tightened constraint, no removed/renamed field, `additionalProperties:false` retained. Right call.

## 5. EVIDENCE-MAP + PACKET HYGIENE — PASS
Reproduced the empty derivation independently. M4-T010 has `directive_refs: [{D-038, ALL}]`. Evaluating all 7 D-038 requirements against M4-T010 (id `M4-T010`, type `rule_engineering`, milestone `M4`, its four allowed_paths): **applicable set = [] (empty)**. R001/R002/R005/R006/R007 bind only `D-038-BOOTSTRAP`; R003/R004 bind only `M5-T003…M5-T013`; none declares `task_types`, `milestones`, or `paths` that would catch M4-T010. Therefore `M4-T010-evidence-map.json = {"requirements": {}}` is the correct empty form, consistent with the cited M2-T021/M0-T057 precedent. (Note: this is the machine-applicable derivation for the G3 record; the directive's own `verification.json` is written by the independent directive-compliance-verifier, not this gate.)

Producer-report sentence-level check against the tree — no overstatement found:
- "all 11 committed rules stay valid" / "none carries the field yet" — verified true (11 valid, 0 carriers).
- "additionalProperties: false retained" — true (schema line 36).
- "compares … against the snapshot's STORED digest … fail-closed AT LOAD … absence unchanged … SnapshotError precedence unchanged" — all true.
- "8 collected cases" — verified: test_s1, test_s2, test_s3, test_s4 (parametrized ×4), test_s5 = 8.
- "dsl.py 288→~300" — actual 299 lines; well under modularity thresholds.
- Unverified-by-discipline (not a defect): the RED "3 failed/5 passed" and GREEN "367 passed" counts were not re-executed (no pytest per discipline). The RED narrative is internally consistent (pre-change `additionalProperties:false` rejects the field wholesale → S2/S3/S5 fail, S1/S4 pass; 3+5=8), and the current green code paths were verified structurally. Suite execution is the QA/G4 gate's scope, not this G3 data-contract gate.

## 6. SCOPE — PASS
- Producer commit `7bac23c1` touches exactly 4 files, all within `allowed_paths`: `services/api/app/rules/dsl.py`, `services/api/app/rules/schemas/v1/rule_definition.schema.json`, `services/api/tests/rules/test_rule_citation_digest.py`, `project-control/reports/M4-T010-producer-report.md`. The dsl.py diff is confined to the citation loop; the schema diff is the single added property line. **No `engine.py`, no ruleset, no fixture, no evaluation_trace schema.**
- Orchestrator record commit `79734f11`: only `project-control/reports/M4-T010-evidence-map.json`.
- Orchestrator record commit `79f06fde`: only `project-control/reports/M4-T010.json` + `project-control/tasks/M4-T010.json`.
All three commits touch only their declared files.

## Summary
Every G3 verification item passes. The contract change is purely additive (optional `citations[].content_digest_sha256`, pattern `^[0-9a-f]{64}$`, `additionalProperties:false` retained), all 11 committed rules remain valid and none yet carries the field, both named v1 snapshots (and all 7) satisfy the pattern and equal `sha256(verbatim_excerpt)` in both trees, the loader comparison is presence-gated against the snapshot's stored digest with byte-identical absent-field behavior and preserved SnapshotError precedence, the v1-no-bump decision is the correct non-breaking call, the D-038 applicable set correctly derives empty, and both producer and orchestrator commits stay within declared scope. No defects.

**VERDICT: PASS** (reviewed HEAD `79f06fde3a56aae2ffef7016e24a5f138fc0940f`)

Relevant absolute paths:
- `C:\Users\MLFLL\Downloads\nyc-zoning\ctl24\services\api\app\rules\schemas\v1\rule_definition.schema.json`
- `C:\Users\MLFLL\Downloads\nyc-zoning\ctl24\services\api\app\rules\dsl.py`
- `C:\Users\MLFLL\Downloads\nyc-zoning\ctl24\services\api\app\rules\snapshots.py`
- `C:\Users\MLFLL\Downloads\nyc-zoning\ctl24\services\api\tests\rules\test_rule_citation_digest.py`
- `C:\Users\MLFLL\Downloads\nyc-zoning\ctl24\project-control\reports\M4-T010-evidence-map.json`
- `C:\Users\MLFLL\Downloads\nyc-zoning\ctl24\project-control\directives\D-038-build-product-not-self\requirements.json`
