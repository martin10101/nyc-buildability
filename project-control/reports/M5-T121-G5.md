# M5-T121 — G5 security review (security-reviewer "sec-t121", read-only)

> Transmission history: pinned at ed229971 (material d865d977; CI run 36113132489 green at ed229971), delivered as
> 3 SendMessage parts ending with END-OF-REPORT; no truncation (each part was queued twice, byte-identical; the first copy is joined). Joined verbatim by the orchestrator (transport
> wrapper tags removed only). Verdict PASS; 0 Critical / High / Medium; two LOW advisories (LOW-1 echoed page
> floats unguarded - finite today via the lexer; LOW-2 C1 / bidi controls not escaped - parity with the DXF helper).

---

M5-T121 G5 security review (READ-ONLY, sec-t121) — Part 1/3. VERDICT: PASS.

HEAD pinned ed229971. Material identity CONFIRMED: `git diff --stat d865d977 HEAD` for both code files is EMPTY — the reviewed files are byte-identical to material d865d977 (cherry-pick of wt-m5t121 3fdb77b5). Baseline (cwd services/api): ruff clean on both target files; `pytest tests/drawings/test_sheet_import.py` → 31 passed; module 623 SLOC (under modularity thresholds). No CRITICAL, HIGH, or MEDIUM findings. Two LOW advisories, both non-blocking (Part 2).

The precedent's blocking finding is CLOSED and independently verified. M5-T108 G5 MEDIUM 1 was: huge-but-finite coords overflow measured_dimensions to inf → uncontrolled 500 on a future JSON route. This module fixes it two ways, both probe-verified:
- Candidates: `_measured_is_finite` guard (sheet_import.py:377) → typed `coordinate_out_of_range`; probe with 1e300 coords returned ImportRefusal, never a non-finite value.
- Draft: build_draft routes EVERY draft through `validate_proposed_massing`, whose `_is_real_number` (proposal.py:248, =math.isfinite) and NYC-2263 bounds check (proposal.py:254) reject a non-finite/huge/local vertex as typed `draft_invalid`. Probe: a near-origin local ring → `draft_invalid` naming `proposed_massing.outline.vertices[0]` — proving the contract fires and is NOT bypassed. The only DraftResult construction (sheet_import.py:623) is post-validation; there is no alternate draft path.

Numeric safety — VERIFIED by 12 hostile in-process probes; build_draft/list_candidates NEVER raise, every failure a typed ImportRefusal VALUE:
- page_index bool/float/str → `page_out_of_range` (bool correctly excluded, :273).
- known_length_ft nan/inf/bool/neg/0/>1e6 → `invalid_scale` (:470-485).
- scale_edge bool/huge/neg → `bad_edge` (:427); scale_candidate not-a-ring → `bad_candidate`; degenerate/zero-length edge → `invalid_scale` (:439).
- resolved scale bounded [1e-9,1e9], non-finite/out-of-range → `invalid_scale` (:490); overflow outline caught at scale-bound as `invalid_scale` before the contract even runs (defense in depth).
- floors=str, floor_to_floor_ft=nan → `draft_invalid` (validate_proposed_massing catches). No unvalidated numeric reaches output; no ZeroDivision (measured edge proven >0 before divide).

Continued in Part 2.

---

M5-T121 G5 — Part 2/3 (the two LOW advisories; both ADVISORY, non-blocking).

LOW-1 (defense-in-depth; NOT reachable today; file:line sheet_import.py:403, :333-334). list_candidates echoes reader-provided page floats into CandidatesResult WITHOUT the finiteness guard it applies to the per-candidate measured dict: `user_unit=page.user_unit` (:403) and `media_box`/`flatten_tolerance` inside `_disclosure` (:333-334). This is the same class as the MEDIUM 1 the module otherwise fixed — a non-finite echoed float would break a future allow_nan=False JSON render. It is NOT reachable today, verified in source: the PDF number lexer rejects exponent notation and caps tokens at 64 bytes (pdf_lexer.py:189-194) → max magnitude ~1e63, ALWAYS finite; `_media_box` (sheet_objects.py:226-236) requires 4 numbers; flatten_tolerance is guarded finite (sheet_reader.py:306). Probe confirmed a 1e63 user_unit echoes as a finite (JSON-safe) value. Residual fragility: sheet_reader.py:468 validates `/UserUnit > 0` but NOT `math.isfinite` — the finite invariant rides entirely on the lexer two modules away. Remediation (route to the mount / reader owner, NOT this packet): the future mount's pre-render allow_nan=False guard (already in producer DISC-B) is the real backstop; optionally add an isfinite check on echoed page metadata here and/or on `/UserUnit` in sheet_reader.py (M5-T120 owns the reader now). Not blocking: unmounted + provably finite input today.

LOW-2 (escape parity residual; matches the accepted precedent; file:line sheet_import.py:217-239). `_escape_drawing_text` escapes only ASCII C0 controls (<0x20) and 0x7F. It does NOT escape C1 controls (0x80–0x9F, e.g. NEL 0x85) or Unicode bidi controls (e.g. RLO U+202E) — probe confirmed both pass through RAW in a scale note. This is BYTE-IDENTICAL to the accepted DXF helper (dxf_import.py:186-205), so it is parity with the accepted G5 bar, not a regression — the known DB-070/DB-081 escaping residual. Output is JSON data, so C1/bidi are valid, JSON-encodable code points that do not break rendering; bidi controls could reorder echoed scale-note text in a future UI. Remediation (consolidated, both helpers together so they stay identical): escape the full Unicode Cc/Cf categories, or at minimum C1 + bidi. Track under the existing DB-070/DB-081 residual; not blocking.

Continued in Part 3 (passed checks, scope/isolation, final verdict).

---

M5-T121 G5 — Part 3/3 (passed checks, scope/isolation, final verdict).

PASSED (verified in source + probe):
- Untrusted text: every drawing-derived string escaped + length-bounded — scale_notes (:347), reader-refusal detail (:263), author (:575), draft_invalid detail from str(exc) (:597). The PDF page disclosure carries only integer counts, finite floats, and a CONSTANT note — no file-derived strings (narrower text surface than DXF's disclosed_unknown/skipped_sections). Scale-note output bounded: ≤50 notes × ≤200 chars (MAX_SCALE_NOTES / MAX_DRAWING_TEXT_CHARS).
- Resource bounds: all work is O(total page points), bounded by the reader's per-page point budget (≤500k); closed-ring count ≤ points/3; measured_dimensions is O(vertices); the largest-area sort is O(k log k) on precomputed finite areas (_area_of guards isfinite, :415); the scale-note scan is O(#text_runs), reader-bounded; MAX_LISTED_CANDIDATES=200 / MAX_SCALE_NOTES=50 bound OUTPUT size. Caps are applied post-measure, but total measure work is inherently reader-budget-bounded — no super-linear blowup, no unbounded output/response.
- Draft honesty/safety: kind stays "proposed" (contract-enforced, proposal.py:449); input_class=imported_pdf, precision "imported drawing - not survey-confirmed" never upgraded (mutation-proven, test :436); label "Proposed - not a city record"; LOCAL-frame limitation DISCLOSED in FRAME_NOTE + provenance.frame_note (:619), not hidden; no permit/approved/maximum-allowed wording (test _FORBIDDEN_WORDS).
- No I/O / network / logging / new deps: imports only math, dataclasses, and internal modules (dxf_import, sheet_primitives, scenario.proposal); grep found NO os/io/socket/logging/open/requests and NO logger — so no document content is ever logged; zero new dependencies; dxf_import.py, the reader modules and app/scenario untouched (forbidden paths clean); unmounted (no route/main change).
- Cross-tenant isolation / service-role secrecy / private storage / SSRF/injection / upload controls / prompt-injection / log redaction / least privilege: N/A here — pure in-memory function, no DB, no auth/tenant/credential context, no outbound I/O, no storage, no LLM in path; ingested drawing text is treated as untrusted DATA and escaped. Upload ceilings, rate limiting, body bounds and the pre-render allow_nan guard are the future mount's job (producer DISC-B / PKT-H) — correctly out of scope for this pure service.

Discoveries routed (not fixed in-packet): LOW-1 → mount pre-render guard + optional reader `/UserUnit` isfinite (M5-T120); LOW-2 → DB-070/DB-081 consolidated escape hardening across both import helpers.

M5-T121 G5 VERDICT: PASS
END-OF-REPORT
