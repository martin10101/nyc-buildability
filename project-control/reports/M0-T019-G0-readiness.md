# G0 Definition-of-Ready — M0-T019 (orchestrator, administrative)

**Task:** Frontend framework security upgrade + permanent dependency-admission policy (npm/Playwright ecosystem)
**Reviewer:** orchestrator (administrative) · **Result:** PASS · **Date:** 2026-07-20

G0 readiness confirmed before dispatch (owner authorization 2026-07-20 — M0-T019 dispatch, implementation, CI, and independent review):

1. **Objective unambiguous** — packet `project-control/tasks/M0-T019.json`: patch the vulnerable Next/React tree to the EXACT reviewed target (next==15.5.20, react==19.1.2, react-dom==19.1.2, eslint-config-next==15.5.20, `overrides.postcss`==8.5.10) and make the owner's dependency-admission rule permanent + machine-enforced for the npm ecosystem. Hard prohibitions: no Next 16, no canary, no `npm audit fix --force`, no automated major downgrade, no unrelated dependency rewrite.
2. **Dependencies** — `[M0-T018 ✓ accepted (38th), M0-T020 ✓ accepted (39th)]`. Both dependency-security predecessors are accepted; M0-T019 is the npm counterpart of the same permanent policy and must stay consistent with the Python enforcement M0-T020 shipped.
3. **File scope exclusive** — `apps/web/package.json`, `apps/web/package-lock.json`, `apps/web/.npmrc`, `.github/workflows/**` (npm tooling pin + blocking audit + scheduled npm re-audit; existing jobs stay green), `docs/DEPENDENCY_SECURITY_POLICY.md`, `CLAUDE.md` (append the concise permanent rule ONLY), own producer report. Forbidden: `apps/web/src/**` behavior changes, test/TS weakening, warning-only audit, advisory waiver, Next 16/canary/`audit fix --force`, `services/api/**` (M0-T020 Python work is preserved untouched), `project-control/**` except own report, `.claude/**` tooling, production/deployment.
4. **Inputs/outputs defined** — inputs: current `apps/web/package.json` (next 15.3.4, react/react-dom 19.1.0, eslint-config-next 15.3.4; no `overrides`; no `.npmrc`), `ci.yml` web/web-e2e jobs (`npm ci --no-audit --no-fund`, node 22, no npm pin, no audit), `generate-lockfile.yml` (remote `npm install --package-lock-only` lock regen), `scheduled-audit.yml` (Python-only today). Outputs: exact-pinned tree + regenerated lockfile with the postcss override; npm 11.18.0 pinned at every applicable CI npm entry point; `.npmrc` with min-release-age=7 + save-exact=true (effective, with machine evidence); a BLOCKING `npm audit --audit-level=low` with JSON total==0 including dev deps; a scheduled npm re-audit with a visible/actionable failure path; `docs/DEPENDENCY_SECURITY_POLICY.md`; a concise CLAUDE.md pointer; per-package publication-date/age/advisory/provenance evidence in the producer report.
5. **Acceptance scenarios exist** — FE-S1..FE-S8 (exact target + no Next 16/canary; blocking audit zero incl. dev; deterministic npm ci; npm 11.18.0 + effective .npmrc; per-package ≥7-day release age; full regression lint/typecheck/vitest/build/Playwright/secret-scan; scheduled audit failure path; permanent policy doc + CLAUDE.md pointer).
6. **Required gates assigned** — G0 (administrative), G2 (producer self-check), G3/G4 (code-reviewer), G5 (security-reviewer). Producer `frontend-engineer` ≠ reviewers `code-reviewer`/`security-reviewer`.
7. **Execution & disk (thin-client)** — GitHub Actions is authoritative; lockfile regeneration runs on a GitHub runner via `generate-lockfile.yml` (`npm install --package-lock-only`, never on the owner PC); no local `node_modules`/npm on the owner PC; disk-safe. Target versions are PROVISIONAL and re-verified against live npm-registry timestamps + current advisories at implementation (STOP + report on any drift; no silent substitution).
8. **Cleanup/storage routing** — no durable artifacts on the owner PC; all output source in Git; durable CI evidence in GitHub Actions; the regenerated lock is committed by the runner.

Ready-to-start criteria met. Dispatched to frontend-engineer in an isolated worktree after this control PR merges (owner Phase 2 → Phase 3 sequence).
