# M5-T049 G3 — independent code review (code-reviewer, saved VERBATIM from the agent return)

G3 CODE REVIEW — M5-T049 (named-street matcher extraction) — VERDICT: PASS

Reviewed at pinned HEAD f3bf4fd3d65ff8db17d91aba48e4b83c2670cff2 (confirmed via rev-parse). Material commit b5d3343f is an ancestor; no commit between it and HEAD touched the three modules (verified). Read-only throughout; no write-producing commands.

BASIS: This is a genuine pure extraction with zero behavior change. I proved verbatim movement structurally, not by trusting the report: parsing the pre-split original (e04e6aad, 853 lines) and the two new modules with Python's ast, all 40 definitions (module constants, the 7 value dataclasses/enum, the 6 normalization helpers, the NamedStreetOverrideMatcher class with all 14 methods, and load_default_matcher) map 1:1 with ZERO missing, ZERO extra, and ZERO AST mismatch. Class decorators/bases match too (@dataclass(frozen=True) and str,Enum preserved). A multiset line-diff of original vs. (table+matching) shows every removed line is original module-docstring prose and every added line is an import, re-export, docstring, section comment, or blank — no logic line dropped or added. Facade, acyclicity, scope, and modularity all check out. One advisory-only report-wording nit; nothing blocking.

FINDINGS:

1. [PASS] Verbatim movement / no logic drift — services/api/app/rules/named_street_override_table.py + named_street_override_matching.py. AST-identity 40/40 defs; decorators/bases identical; multiset diff isolates only docstring/import/comment/blank changes. MATCHED_OVERRIDE disposition semantics preserved (match/classify/_validate* methods byte-identical at AST level).

2. [PASS] Facade completeness — services/api/app/rules/named_street_override.py:36-79. Every name each of the 4 consumers imports resolves through the facade: status.py:33 {MatchStatus, NamedStreetOverrideMatcher, OverrideQuery, _normalize_cd}, wide_street_live_provider.py:104 {NamedStreetOverrideMatcher, OverrideQuery, load_default_matcher}, test_named_street_override.py:29 {BOUNDARY_OPEN_QUESTION, AlternateWidthResult, MatchStatus, NamedStreetOverrideError, NamedStreetOverrideMatcher, OverrideQuery, load_default_matcher}, test_wide_street_wiring.py:68 {MatchStatus, NamedStreetOverrideMatcher, OverrideQuery, load_default_matcher}. Private _normalize_cd re-exported. __all__ = 17 names, matches re-exports exactly. Import chain acyclic: table.py imports stdlib only (leaf); matching.py imports from _table + app.rules.snapshots; neither imports the facade. The new re-export identity test (test_named_street_override_matching.py:36-68) asserts all 17 facade names ARE the same objects as the impl modules — a strong AS-2 guarantee.

3. [PASS] Acceptance suite byte-identity intact — test_named_street_override.py is NOT in the cherry-pick diff, and git diff e04e6aad..f3bf4fd3 on it is empty (byte-identical). It is the behavior proof, unedited.

4. [PASS] Modularity — independently ran `python tools/modularity_check.py --check`: selected 449 files; failures 0; warnings 20; exit 0. No warning references named_street_override / _table / _matching, so all three are below the 600 SLOC warn tier (raw lines 79/223/656; the checker's SLOC excludes blanks/comments/docstrings). Seven boundary answers present in the report. AS-3 confirmed at HEAD, matching CI run 35435873943.

5. [PASS] Scope — cherry-pick b5d3343f changes exactly 6 files, all within allowed_paths (the 3 modules, the 2 focused test files, the report). Zero forbidden_paths touched; the in-scope acceptance suite left untouched.

6. [ADVISORY] Report wording — producer report §3/§Byte-identity says "The facade __all__ is unchanged (same 17 names)". The e04e6aad monolith had NO __all__ at all; the facade newly introduces it. "Unchanged" is true only relative to the earlier in-task increment (run 51), not the monolith baseline, and could mislead a reader who takes e04e6aad as the reference. Non-behavioral: no consumer uses `import *`, and the introduced __all__ is correct/complete. No fix required for acceptance; optionally reword to "the facade introduces an explicit __all__ enumerating the 17 re-exported names." Note also the report's §Validation is scrupulously accurate — it does NOT claim ruff/pytest ran locally (only modularity executed by producer), attributes those to CI, and withdraws a prior increment's unexecuted "clean" claim; check #6 (no unexecuted-command-as-executed) passes.

VERDICT: PASS. No blocking findings; the single advisory is a report-wording clarification only.
