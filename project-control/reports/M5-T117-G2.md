# M5-T117 - G2 producer self-check record (recorded by the orchestrator, role self_check)

Producer: backend-engineer (orchestrator-dispatched subagent) in wt-m5t117; one in-worktree commit 76a9ba28 (parent ba48acad), cherry-picked to cf6526e3 with every file MATCH by git blob identity (7 files: project-control/reports/M5-T117-producer-report.md 8dd9dc70, services/api/app/api/v1/scene_api.py fa6a1181, services/api/app/resilience/rate_limit.py 10916271, services/api/tests/cad/test_export_api.py d5500e8d, services/api/tests/drawings/test_dxf_import_api.py fccbf334, services/api/tests/resilience/test_rate_limit.py 2efb09a2, services/api/tests/scenario/test_scene_api.py d0851eab).

- **Scope:** services/api/app/resilience/rate_limit.py: Finding 1 - a lock-guarded started flag + idempotent release so a job cancelled before its worker starts releases its slot exactly once, while a started job holds it until the thread returns (the 3 x 16 = 48 slots vs anyio's 40-token pool relation documented); Finding 2 - _maybe_sweep throttles the full-table sweep to at most once per window; Finding 3 - caller_key keys on a principal only when it is a non-empty string. Tests: +7 in tests/resilience (pre-start no-leak, post-start no double release, sweep throttle, principal hygiene, XFF never trusted, limiter + slot contention) and HEAD/OPTIONS + an AST no-local-limiter guard in the three route test files; scene_api.py docstring paths.
- **Producer self-checks (report, OBSERVED):** ruff clean; the four target suites 190 passed (was 183); modularity exit 0 (run directly under 3.11 - the target files do not import the PEP 695 chain). Five named in-process mutants (never-release-on-pre-start-cancel, release-on-every-cancel, sweep-on-every-refused-key, empty-principal-accepted, trust-XFF) each RED, baselines GREEN. The contention tests are positive guards: a bare drop-lock mutant does not redden on CPython (the GIL serializes the check-then-act); a yield-widened no-lock variant over-admits (12 > 10).
- **[ORCH-HARVEST] at the harvested head cf6526e3 (cherry-pick cf6526e3; Python 3.11 local): `python -m ruff check .` (cwd services/api) -> exit 0: All checks passed!; `python -m pytest tests/resilience tests/scenario/test_scene_api.py tests/drawings/test_dxf_import_api.py tests/cad/test_export_api.py -q` (cwd services/api) -> exit 0: 190 passed in 12.71s; `python tools/modularity_check.py --check` (cwd .) -> exit 0:   warn review_signal: tools/agent_supervisor/gate_wave.py - above the justification threshold; record a cohesion justification in review.**
- **Discoveries routed (producer):** Open questions: (1) the summed slot caps (48) exceed anyio's 40-token pool - keep 16 per route and size at the PKT-H mount (recommended); (2) accept a once-per-window sweep throttle. DB-086 (d), (e), (k) stay with the PKT-H mount / by note; no new defects.

G2 verdict: PASS (self_check; the independent reviews and the DCV follow - dispatched by the successor session per the handoff).
