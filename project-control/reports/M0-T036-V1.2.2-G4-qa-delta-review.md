# M0-T036 V1.2.2 — G4 QA delta re-gate (verbatim reviewer return)

- Gate: G4 (independent QA) | Task: M0-T036 | Reviewed SHA: **b95ebf7** (V1.2.2) | Delta baseline: 98da15e
- Reviewer model: **claude-opus-4-8 / effort xhigh** (owner-approved Fable-out fallback, D-007-R609 lifting R607 for M0-T036 only)
- This is the gate that FAILED at V1.2 on F1; it now confirms F1 resolved.
- Recorded verbatim by the orchestrator; reviewer was read-only and did not record the gate.

---

I have complete evidence. All verification is done against content-verified frozen SHA b95ebf7 (blob hashes matched), the true delta `98da15e..b95ebf7` matches the producer diff scope exactly, and I wrote nothing to the repo (only the session scratchpad). Here is my gate report.

---

# G4 QA GATE REPORT — M0-T036 (Codex-Claude supervisor bridge), V1.2.2 delta re-gate

**gate=G4 · task=M0-T036 · reviewed_sha=b95ebf7 · delta_baseline=98da15e · model=claude-opus-4-8 / effort xhigh** (owner-approved Fable fallback while Fable is out)

## VERDICT: PASS

**F1 = CONFIRMED-RESOLVED.** The record-only defect the prior V1.2 G4 caught is closed: the model switch now reaches the actuation (the launched `--model` and `expected_model` both rebind), it is proven by a real OS process, and I independently reproduced the mutation check that shows the hard-requirement test fails if the fix is reverted.

---

## How I verified (reproducible)

The assigned worktree is a **sparse review checkout** — `tools/agent_supervisor/` and the test files are not on disk, and HEAD is `d5d9b50` (3 doc/control commits past the frozen SHA; none touch supervisor code). I therefore materialized the exact frozen tree from the object DB and ran everything against it:

- `git archive b95ebf7` → extracted to scratchpad. **Content identity confirmed by blob hash**: `loop.py=af76f2c`, `claude_runner.py=76ab3c0`, `config.py=a398bd6`, `test_agent_supervisor_loop.py=4159d1c`, `test_agent_supervisor_model_chain.py=60b3f22` — all equal to the b95ebf7 blobs and to the producer diff's `index` hashes.
- `git diff --stat 98da15e b95ebf7` for the packet paths touches exactly the 8 files in `M0-T036-V1.2.2.diff` (1801 insertions / 185 deletions) — the producer diff is the true delta; nothing hidden, no CI or cross-module edits.

## Test counts I observed (Python 3.11.9, full frozen tree as repo root)

- **Full supervisor suite** `python -m unittest discover -s tools -p "test_agent_supervisor_*.py"`: **Ran 1150 tests — OK, 0 failures, 0 errors, 2 skipped.**
  - The 2 skips are legitimate environmental guards: `test_symlink_escape_is_denied` (Windows symlink privilege not held) and `test_job_objects_report_unavailable_off_windows` (POSIX-only guard). Neither is in the delta.
  - (An earlier run in a `tools/`-only extraction produced 5 spurious failures in `replay`/`policy` — all provenance/path-existence checks needing the full repo root, e.g. `project-control/tasks/M0-T036.json`. They vanished when run in the full tree; not regressions.)
- Delta files: `test_agent_supervisor_model_chain` = **20 tests OK**; `test_agent_supervisor_loop` = **97 tests OK** (incl. `ModelChainSwitchTests` = **14 OK**).

## F1 closure — the actuation-proving test + mutation evidence

**Actuation-proving test (switch leg):** `tools/test_agent_supervisor_model_chain.py::RealProcessSwitchTests::test_a_real_process_comes_up_on_opus_4_8_after_a_fable_5_exhaustion`. Unlike the V1.2 FakeRunner tests (which ignored `--model` and only asserted the record side), this spawns a **real OS process** — a local Python fake-CLI launched via `sys.executable` through the *real* `build_argv` path — that reads `--model` from its own argv, refuses to come up when the id is marked exhausted, and appends `{pid, model, argv}` to a **launch-log file written by that other process**. The test asserts on that file plus `loop.launched_model()`, `record["launched_model"]`, and `durable["launched_model"]`.

I ran a standalone reproduction (`repro_switch.py`) that dumps the persistent launch log for this exact scenario. It proves the **actual dispatched worker unit** — not merely the availability probe — comes up on opus-4-8:
```
[0] started=True  fable-5      <- cycle-1 unit on the pin
[1] started=False fable-5      <- seam availability probe of the pin: refused (exhausted)
[2] started=True  opus-4-8     <- seam availability probe of opus-4-8 (real process)
[3] started=True  opus-4-8     <- cycle-2 WORKER UNIT on opus-4-8 (real process)
loop.launched_model(): claude-opus-4-8   run.stopped: max_cycles_reached (not paused)
```

**`expected_model` also rebinds** (the second half of the prior FAIL): `RunnerRebindTests::test_with_model_moves_both_model_and_expected_model` and `CrashResumeTests::test_the_runner_start_builds_is_configured_on_the_effective_model` assert `expected_model == claude-opus-4-8` ("a pinned expected_model would flag every event as a downgrade").

**Independent mutation check (I performed it, not just read the claim):**
- Reverting the switch actuation — `_actuate_model` made record-only (runner not rebound, the V1.2.1 bug) — makes the hard-requirement test **FAIL**: `AssertionError: 'claude-fable-5' != 'claude-opus-4-8'` at `assertEqual(loop.launched_model(), FIRST_FALLBACK)`.
- Reverting the crash-resume actuation — cli.py `launch_model = pinned_model` instead of `effective_model(...)` — makes `CrashResumeTests::test_the_runner_start_builds_is_configured_on_the_effective_model` **FAIL**: `captured[0].model 'claude-fable-5' != 'claude-opus-4-8'`.

Both halves of F1 are genuinely caught by the suite.

## Coverage of the other required semantics (all passing)

- **Switch / return-to-pin / crash-resume**, each with a real process: switch (above); return `RealProcessReturnTests::test_the_return_to_the_pin_launches_a_real_process_on_the_pin`; resume `CrashResumeTests::test_a_resumed_run_launches_a_real_process_on_the_effective_model` (clean, probe-free: `started[0]==opus-4-8`) + `test_effective_model_is_read_from_the_durable_record` + `test_a_clean_journal_still_launches_on_the_pin`.
- **Launch-probe availability**: `test_a_process_that_reports_another_id_does_not_make_that_model_available` (a launch that resolves to Opus 5 → `model_id_mismatch`, unavailable).
- **Non-chain ids unselectable**: `test_an_unlisted_id_is_never_selectable_even_if_it_launches` (Opus 5 launches fine and is still never probed/chosen; `_actuate_model('claude-opus-5')` raises `model_not_in_chain`) + `test_the_ids_are_exact_strings_with_no_aliasing`.
- **Chain-exhaustion STOP+notify, no silent substitute**: `test_no_chain_entry_launches_so_the_run_stops_and_notifies` (`CHAIN_EXHAUSTED_STOP`, PAUSED_RECOVERY, open ask, only the cycle-1 pin process ever started, no substitution record).
- **Worker/shadow never switch**: `test_a_non_orchestrator_role_pauses_and_launches_nothing_new`, `test_shadow_observes_and_never_switches`.
- **Three distinct triggers not conflated (R606)**: the switch record carries its own `reason_code=quota_exhausted` alongside a *separate* `rotation_reason=context_threshold`; the hard-requirement test asserts both, so the quota switch is not absorbed into the context-rotation (or security-downgrade) trigger. Reason codes/records are kept distinct at the test-adequacy level.

## Findings

**Acceptance-blocking: NONE.**

**Activation-only (do not block SHADOW-ONLY acceptance; gate live auto-switch):**
- **A1 (activation, expected/correct — Medium for activation):** The live-CLI account-quota exhaustion signal is *not captured* (`QUOTA_EXHAUSTION_SIGNAL_VERIFIED=False`), and `cli.py` wires `make_launch_probe(... )` with **no `classify_unavailable`** for orchestrator sessions. So in production the probe returns `""` (unknown), which is not `quota_exhausted`, so the switch branch never fires — it **fail-closes to PAUSE+notify**. The switch actuation is fully proven only with the test-injected `quota_classifier`. This is the safe posture and is honestly disclosed (doctor `model_chain_availability` UNVERIFIED, README caveat). Live auto-switch cannot fire until the real signal is captured and a classifier wired — an activation step, not acceptance.

**Non-blocking observations (test-design robustness, not defects):**
- **O1 (Low):** In the switch-leg test, the availability probe *itself* launches the candidate, so `assertIn(opus-4-8, started[1:])` and the `switched`-argv assertion are satisfiable by the probe alone; the *dispositive* actuation assertions are `loop.launched_model()` / `record["launched_model"]` / `durable["launched_model"]`. My mutation check confirms these catch the defect, and my repro confirms the actual worker unit (log `[3]`) — not just the probe — comes up on opus-4-8, and the clean probe-free proof lives in the crash-resume test. No gap in practice.
- **O2 (Low, pre-existing, process risk):** Per producer report §7, CI still does not invoke the supervisor test files (needs a forbidden `.github/**` edit). These 1150 tests run locally only, so a future regression in this area would not be caught by CI. Not introduced by V1.2.2; worth a follow-up wiring task.

## Reviewer discipline confirmation

- **Wrote nothing to the repository.** All work occurred in the session scratchpad (`git archive` extractions, two throwaway mutation copies, one repro script). No repo file, no control-plane state, no ledger/git/gh command was run. Git usage was read-only object reads scoped to my worktree (`cat-file`, `ls-tree`, `archive`, `diff`, `hash-object`).
- **Stayed within the delta**: reviewed `tools/agent_supervisor/{README.md, claude_runner.py, cli.py, config.example.toml, config.py, loop.py}` + `tools/test_agent_supervisor_loop.py` + `tools/test_agent_supervisor_model_chain.py` at content-verified b95ebf7; confirmed the producer diff equals the true `98da15e..b95ebf7` delta.

**Recommended orchestrator action:** record G4 = **PASS**. F1 is CONFIRMED-RESOLVED. Acceptance may proceed on the existing SHADOW-ONLY basis; keep A1 (live quota-signal capture + classifier wiring) and O2 (CI wiring) as separate activation/follow-up items — neither blocks this gate.
