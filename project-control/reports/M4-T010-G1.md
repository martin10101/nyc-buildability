<!-- Reviewer return preserved VERBATIM by the orchestrator (report-preservation rule,
.claude/rules/project-control.md). Reviewer: independent code-reviewer subagent (G1),
returned 2026-09-11, reviewed HEAD 79f06fde. -->

All verification complete. Here is my gate report.

---

VERDICT: PASS

# Gate Report

- Gate ID: G1 (independent code review)
- Task ID: M4-T010 — rule-citation content-digest binding (additive schema property + fail-closed loader verification)
- Reviewer: code-reviewer (independent; did not write this code)
- Producer: rules-engineer/orchestrator (producer-material commit `7bac23c1`)
- Result: PASS
- Reviewed HEAD: `79f06fde3a56aae2ffef7016e24a5f138fc0940f` (matches the assigned frozen SHA). Producer material verified at `7bac23c1`; the only changes between `7bac23c1..79f06fde` are control-plane files (evidence map, `M4-T010.json`), zero `services/api` changes after the producer commit.
- Environment: read-only review in the primary checkout. No writes, no `project_control.py`/git-write/`gh`. Behavior independently reproduced with offline `python -c` probes (`PYTHONDONTWRITEBYTECODE=1`, `PYTHONPATH=.` from `services/api`) against the real `build_rule_definition`/schema/loader; full-suite pass is CI's authority.

## Acceptance criteria reviewed

All six packet scenarios independently reproduced (not merely read):

| ID | Result | Independent verification |
|---|---|---|
| S1 additive back-compat | PASS | Loaded all **11** committed `*.rule.json` via `load_rule_file` with the docs SnapshotStore — all load; none carries `content_digest_sha256` (grep confirmed empty set). Absence is legal. |
| S2 matching digest loads | PASS | Set `citations[0].content_digest_sha256 = snap.content_digest_sha256` (zr-23-21) on the R5 pilot doc → loads. Snapshot's stored digest equals independently recomputed `sha256(verbatim_excerpt)` (`True`). |
| S3 mismatch fails closed | PASS | Flipped first hex char → `DSLError` at load; message contains `rule_id`, cited `snapshot_id`, and both digests; length 298 chars (bounded). `match=` regex distinguishes the mismatch message from a schema-shape rejection — confirmed the schema-rejection message ("rule schema violation at …") does NOT satisfy the S3 regex. |
| S4 malformed rejected by schema | PASS | uppercase/63/65/non-hex all raise schema `DSLError` at `citations/0/content_digest_sha256` before any snapshot comparison; valid 64-hex passes schema. |
| S5 missing-snapshot precedence | PASS | Nonexistent `snapshot_id` + valid digest → `SnapshotError` first (resolution precedes digest check); no new resolution path introduced. |
| S6 no engine/dep change | PASS | `git show 7bac23c1` touches exactly 4 files; `engine.py`, `requirements.txt`, `pyproject.toml`, and all `rulesets/**` untouched; new focused test module added. Full `tests/rules` pass is CI's authority (producer records 367 passed); zero existing test file modified, so regression risk to the suite is nil by construction. |

## Directive/requirement verification

M4-T010 is in-regime (`directive_refs`: D-038:ALL). Independent per-requirement directive verification is the `directive-compliance-verifier`'s remit recorded in the directive `verification.json`, not the G1 code gate. The orchestrator evidence-map records that D-038's applicable set computes **empty** for M4-T010 (M2-T021/M0-T057 precedent). From the code side I find nothing in this schema/loader change that conflicts with D-038 (product pivot); this unit is product rule-engineering work enabling M4-T009 AS-5. The empty applicable-set claim must still be confirmed by the independent directive verifier.

| Requirement ID | Reviewed content identity | Verdict | Note |
|---|---|---|---|
| D-038:ALL | `79f06fde` / material `7bac23c1` | Deferred to directive-compliance-verifier | Applicable set recorded empty; no code conflict observed. |

## Steps independently executed

1. `git show 7bac23c1` (full diff) and `git diff --stat 7bac23c1 79f06fde` — scope confirmed.
2. Offline jsonschema probe of the reconstructed **pre-change** citation subschema: adding `content_digest_sha256` under `additionalProperties:false` errors at path `['citations',0]` → `loc = citations/0`.
3. Offline probe of the **post-change** schema pattern for the four S4 malformed values and a valid digest.
4. End-to-end probe of `build_rule_definition` with the docs SnapshotStore for S2/S3/S5 and the absent-field (no-op) case.
5. S1 enumeration probe loading all 11 committed rules.
6. Grep of `services/api/**.py` for `build_rule_definition` / `load_rule_file` / `_check_refs` / `Draft202012Validator` to find bypass paths.

## Expected versus actual

- **Schema (`rule_definition.schema.json`)** — additive optional `content_digest_sha256` with `pattern ^[0-9a-f]{64}$`; `additionalProperties:false` retained; NOT added to `required`. Correct and minimal. sha256-hexdigest is always lowercase 64-hex, so the pattern matches what `snapshots.py` stores. Matches M4-T009 §2 AS-5 routed step 1.
- **Loader (`dsl.py::_check_refs`)** — keeps the resolved snapshot; `recorded = citation.get(...)`; compares only `if recorded is not None and recorded != snap.content_digest_sha256`, raising typed `DSLError` naming rule id, snapshot id, and both digests. `snapshots.get()` (which raises `SnapshotError`) runs first, so precedence is preserved. Absent field ⇒ prior behavior exactly. Matches routed step 2.
- **Red/green coherence** — mechanically consistent with the pre-change code: `additionalProperties:false` rejected the field wholesale, which explains S2/S3/S5 RED and S1/S4 GREEN, and reproduces the producer's "3 failed, 5 passed." The S4 regex (`rule schema violation at citations/0`) is satisfied by the pre-change extra-property error too (correctly claimed as pre-change green), while the S3 regex is not — so S3 is a genuine mismatch-anchored red case, not a schema-shape tautology.
- **Anti-tautology** — expected digest is loaded from the snapshot and cross-checked against a recomputed `sha256(verbatim_excerpt)`; no literal digests in the test. (Note: because `snapshots.py::load_snapshot_file` already fails closed unless stored == recomputed and then stores the recomputed value, the test's cross-check can never fail in practice — harmless, and still not a literal.)

## Evidence paths

- Task packet: `C:\Users\MLFLL\Downloads\nyc-zoning\ctl24\project-control\tasks\M4-T010.json`
- Producer report: `C:\Users\MLFLL\Downloads\nyc-zoning\ctl24\project-control\reports\M4-T010-producer-report.md`
- Schema: `C:\Users\MLFLL\Downloads\nyc-zoning\ctl24\services\api\app\rules\schemas\v1\rule_definition.schema.json` (lines 34–45)
- Loader: `C:\Users\MLFLL\Downloads\nyc-zoning\ctl24\services\api\app\rules\dsl.py` (`_check_refs`, lines 61–76; sole caller `build_rule_definition` line 227–230)
- Snapshot model/loader (authoritative digest = `sha256(verbatim_excerpt)`): `C:\Users\MLFLL\Downloads\nyc-zoning\ctl24\services\api\app\rules\snapshots.py` (lines 119–154)
- Tests: `C:\Users\MLFLL\Downloads\nyc-zoning\ctl24\services\api\tests\rules\test_rule_citation_digest.py`
- Sole production loader: `C:\Users\MLFLL\Downloads\nyc-zoning\ctl24\services\api\app\rules\registry.py` (line 131 → `load_rule_file`)
- Motivating requirement: `M4-T009-producer-report.md` §2 AS-5 (lines 96–152) + §4 item 4 (lines 249–253)

## Regression / security / provenance findings

- **No bypass of the fail-closed check.** The only production rule-loading path is `registry.py → load_rule_file → build_rule_definition → _schema_validate + _check_refs`. `_check_refs` is called exclusively from `build_rule_definition`, which runs `_schema_validate` first, so `recorded` reaching the error message is always a schema-validated 64-hex string — the error message is bounded and cannot leak arbitrary/large input. Other `jsonschema`/`Draft202012Validator` uses are for profile/scenario/trace/response contracts, not rule-definition loading.
- **Error quality (reliability §7).** Typed `DSLError`, stable message, names rule id + snapshot id + both digests (each 64 hex), no legal/body/quote text echoed. Bounded (298 chars measured).
- **No behavior change for digest-less rules** confirmed by S1 (all 11 load) and the explicit absent-field probe.
- **Additive contract** — backward compatible; consumers reading `citations[]` are unaffected; the `Citation` dataclass intentionally does not carry the digest forward (verification-at-load is the objective; the raw document is retained via `RuleDefinition.raw`).
- **Modularity** — change is +≈11 cohesive lines inside `_check_refs`'s existing responsibility; `dsl.py` is 299 lines (well under warn 600); no responsibility mixing, no new module. `modularity_check --check` exit 0 is CI's authority; passes by inspection.

## Defects

None at MEDIUM or above.

## Observations (informational / LOW — not blocking)

1. **`parameters[].citation_ref` carries no digest binding.** Parameters resolve snapshots (`_check_refs` line 81) but cannot record a digest (their subschema is `additionalProperties:false` with no digest property), so no comparison occurs there. I judge the citations[]-only scoping **sound**: M4-T009 AS-5's text targets a rule's *citation* digest (the `citations[]` representation), so this unit fully enables AS-5. Parameter-level digest binding would be purely additive future hardening and is not required by AS-5. Flagging so M4-T009's AS-5 closure is not read as covering parameters.
2. **"Absent digest ⇒ error" was in M4-T009's routed step-2 wording** but is correctly NOT enforced here: making the field required would invalidate the 11 committed rules at load. Mandatory recording is enforced downstream by M4-T009's AS-5 test + per-rule digest adoption (routed steps 3–4, `rulesets/**`, forbidden to this packet). This sequencing enables AS-5 without weakening it.

## Required rework

None.

## Reviewer conclusion

The unit implements exactly the schema/loader change M4-T009 §4.4 routed to close AS-5 without weakening it: an additive optional `citations[].content_digest_sha256` (64-hex, `additionalProperties:false` retained) and a fail-closed `_check_refs` comparison against the snapshot's stored digest, with `SnapshotError` precedence preserved and no behavior change for digest-less rules. Scope is clean (4 files, all within exact-file `allowed_paths`; nothing else under `services/api`). The red/green record is mechanically coherent and independently reproduced; the S3 assertion genuinely distinguishes a content mismatch from a schema-shape rejection; the tests avoid literal-digest tautology. No defect at MEDIUM+ and no unmet packet clause.

VERDICT: PASS
