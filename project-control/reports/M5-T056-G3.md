# M5-T056 G3 — independent code review (saved VERBATIM from the reviewer return; this header line is the orchestrator's)

All verifications complete. Material commit 5b7b4b30 is in the frozen head; the 7 code/test material blobs are byte-identical at the frozen head db0cf0f8 and the green-CI head bd408ab9 (run 35494571792), so the green web CI exercised the exact frozen content. `_capped_raw_value` is applied only at the 422 branch. Here is my gate report.

---

# G3 Independent Code Review — M5-T056 (DB-036 condo-surface rider cluster)

**Gate:** G3 (independent code review) · **Reviewer:** code-reviewer (read-only) · **Date:** 2026-09-20
**Task:** M5-T056 · **Verdict: PASS**

## Identity / reproducibility

- Repo `C:\Users\MLFLL\Downloads\nyc-zoning\ctl24`, branch `candidate/D-024-mrl-option-b`.
- Frozen submission head `db0cf0f8`; live head `bd408ab9`. `git diff --name-only db0cf0f8 bd408ab9` touches **none** of the 8 T056 material files (only the M5-T056.json ledger-status field, e2e, docs, and disjoint tasks T055/T057/D-078). Peer commits are disjoint as claimed.
- All 8 material files are byte-stable between `db0cf0f8` and the working tree (`git hash-object` == `git rev-parse db0cf0f8:<path>` for each).
- Material commit `5b7b4b30` (8 files) is an ancestor of `db0cf0f8` (verified `git merge-base --is-ancestor`).
- The 7 code/test material blobs are byte-identical at `db0cf0f8` and `bd408ab9`, so CI success at `bd408ab9` (run 35494571792) covers the exact frozen material. Per thin-client policy I verified this stored CI evidence rather than running npm locally.

## Findings

**G3-1 (advisory).** Substitution allow-path `entered` fallback in `PropertyOverview.tsx:228` reads `sub?.enteredBbl` (bounded via `boundedBbl`, a 20-char token bound) rather than the strict `validateBblInput` parser. This is the resolver's own substitution record on the single-lot allow path, not the rider-(b) entered-vs-billing labels, so it does not weaken rider (b). Non-blocking.

**G3-2 (advisory).** In the mixed-zoning multi-lot case (one recorded district + one unknown), the divergent-zoning notice renders on `anyZoningRecorded` even though only one district is actually recorded. This is the fail-safe (over-inclusive) direction for a legal-question flag and matches AS-3's wording ("shows only when zoning is actually recorded"). Non-blocking.

**G3-3 (advisory).** `condo-records.ts` is 620 raw lines and `condo_records.py` 648; both well under the 750/1000 thresholds. Producer reports `modularity_check --check` exit 0 with no findings on the eight files; control-plane CI enforces it. No giant functions or responsibility mixing introduced — changes are a small parser helper plus render-branch derivations. Non-blocking.

No blocking defects found.

## Explicit rulings on items 1–5

**Item 1 — Rider (b) entered-vs-billing BBL labels: PASS.**
`enteredBblValue` (`condo-records.ts:269-273`) parses via `validateBblInput` imported read-only from `./bbl` (`condo-records.ts:37`); returns `result.canonical` on success, explicit `null` otherwise — never re-implemented, honest null when unparseable. `bbl.ts` is byte-untouched (forbidden path, not in the material set). Render (`PropertyOverview.tsx:258-266`): `enteredLot = recordsView.enteredBbl ?? "not recorded (unknown)"`, and `billingLot` shows the BBL only when `billingBblStatus === "recorded" && billingBbl`, else "not recorded (unknown)". A unit input (`billing_bbl_status:"unknown"`, `billing_bbl:null`) therefore renders the parsed unit BBL under "Condo lot you entered" and "not recorded (unknown)" under "Condo billing lot" — never the entered unit BBL relabelled as billing. Bound by parser tests (`condo-records.test.ts:416-433`, `459-470`) and render tests (`condo-resolution-display.test.tsx:722-742`, incl. `billing.textContent not.toContain("1003031001")`).

**Item 2 — Rider (c) dangling reference: PASS.**
`grep "professional-review determination"` over `apps/web/src` excluding `__tests__` returns no matches — the dangling phrase is fully gone from production source. Replacements: `PropertyOverview.tsx:264` "shown for reference under the development limits above" and `:280` "The development limits above govern." Both point at the `DevelopmentLimits` component, which renders above `CondoRecordsChannelSection` on **both** surfaces: `PropertyOverview.tsx:316`→`:336` and `ReportView.tsx:91`→`:97`. Negative + positive assertions at `condo-resolution-display.test.tsx:446-450` and `744-749`.

**Item 3 — Rider (e) zoning-status parsing: PASS.**
`recordedZoningStatus` is mirrored from `recorded_zoning_status` with an honest fallback derived from the zoning value when the source omits it (`condo-records.ts:286-289`); `recordedZoningDependency` parsed at `:361-364`. Notice gating (`PropertyOverview.tsx:243-244, 251-253, 270-271`): divergent notice gated on `anyZoningRecorded`; ZTLDB gap note gated on `anyZoningUnknown && recordedZoningDependency`; `zoningGapNote` is scope-aware (partial vs full gap) and protects already-recorded districts. No invented values (`_recorded_zoning_for` returns `None` today; status labelled). Bound by `condo-records.test.ts:435-457` (parsing + fallback) and `condo-resolution-display.test.tsx:751-776` (no-zoning: divergent absent, gap present, ZTLDB named, no "out of scope" leak; mixed: both notices, "recorded districts shown above are unaffected").

**Item 4 — Riders (f)/(g)/(h): PASS.**
(f) Real `<h2>` at `PropertyOverview.tsx:231` and `:263`; asserted via `getByRole("heading",{level:2,...})` on screen (`condo-resolution-display.test.tsx:778-788`) and brief (`report-view.test.tsx:483-485`).
(g) `boundedTimestamp` (`condo-records.ts:235-241`) keeps `:` and `+`; value pins assert char-for-char `"2026-09-01T14:05:56Z"` (`condo-records.test.ts:185-187, 464-469`; `condo-resolution-display.test.tsx:790-795`; `report-view.test.tsx:486-491`). A `boundedTimestamp→boundedToken` revert would strip the colons and fail these assertions — a genuine mutation-style pin.
(h) `MAX_RAW_VALUE_REPR_CHARS=256` (`condo_records.py:142`); `_capped_raw_value` defined once (`:231-238`) and applied **only** at the 422 branch (`:596`, grep-confirmed single call site); byte-identical at/under the cap. Bound by `test_condo_records_api.py:572-625` (short byte-identical, oversized truncated to ceiling+marker with exact length, end-to-end 422 capped, end-to-end short uncapped/verbatim).

**Item 5 — Preservation (load-bearing): PASS.**
One monotone `deriveCondoSurface` decision (`PropertyOverview.tsx:197-214`) governs both surfaces; `PropertyOverview.tsx:308` and `ReportView.tsx:46` both consume it. The new locals (`anyZoningRecorded`, `anyZoningUnknown`, `enteredLot`, `billingLot`, `zoningGapNote`) live inside `CondoRecordsChannelSection` render branches and derive purely from already-parsed `recordsView` fields — they do not feed `withholdAllowances` and add no second decision point. Withhold direction unweakened: `withholdAllowances = profileWithholds || channelWithholds` (`:206`) unchanged; multi-lot/unresolved/resolver-error still withhold (`condo-records.ts:613-619`; `condo-records.test.ts:395-408`). `boundedToken` charset untouched (`bounded.ts` is a forbidden path, not in the material set). `ReportView.tsx` byte-unchanged (permitted but not in the 8 material files; commit stat lists only `report-view.test.tsx`), so the brief inherits every fix through the one shared `CondoRecordsChannelSection` — proven end-to-end at `report-view.test.tsx:477-492`. No allowance-class vocabulary in the records/conflict copy (`.not.toMatch(/allowance/i)` at `condo-resolution-display.test.tsx:451`).

## Modularity

Public interfaces preserved: `PropertyOverview`, `CondoRecordsChannelSection`, `deriveCondoSurface`, `deriveCondoDisplay`, `condoWithholdsAllowances` signatures unchanged; new `CondoRecordsView`/`CondoBaseLotRecord` fields are additive. No breakage for the five production consumers of `PropertyOverview` or the `lib/condo-records.ts` consumers named in the code-graph block.

## Verdict

**PASS** — no blocking defects. Three advisory notes (G3-1..G3-3) for the orchestrator's discovery sweep; none require rework. Web-suite green is anchored to CI run 35494571792 at `bd408ab9`, verified to carry byte-identical material blobs to the frozen head `db0cf0f8`.
