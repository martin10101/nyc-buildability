<!-- Orchestrator capture: qa-engineer G4 return for M5-T046 saved VERBATIM
     (transport entity-decoding only). Review pinned at frozen head c9113e09; reviewer
     verified content digests and answered the mutation-sensitivity question directly. -->

# G4 Gate Report — M5-T046 (Address-to-lot identity honesty, DB-026)

**Gate:** G4 (QA / test adequacy) · **Reviewer:** qa-engineer (read-only) · **Verdict: PASS** (2 advisory findings; no blocking findings)

**Reviewed identity:** frozen head `c9113e09` on `candidate/D-024-mrl-option-b`; live branch tip `665d3d0b` is exactly one control-plane commit later (the SUBMITTED record), task identity byte-stable. Material build `3182f0a2`; adjudication `c3c690f7`; test-only AS-5 correction `fb7af67d`. Read at the frozen SHA via git plumbing (my worktree is on an unrelated branch, `d8b3899f`).

**Content identity verified (LF-normalized sha256 vs `M5-T046-source-sections.md`):**
- `apps/web/src/lib/address-search.ts` → `0096e659…f55e3f` ✓ matches
- `apps/web/src/lib/__tests__/address-search.test.ts` → `973ec3c5…930f22` ✓ matches

**Scope discipline:** the full changed set from material parent → `c9113e09` touches only allowed_paths production/test files (address-search.ts, its test, AddressConfirmCard/AddressResolutionScreen/AddressAutocomplete + their tests, geosearch.json) plus control-plane records. No forbidden path (`services/api/`, `packages/contracts/`, PropertyOverview/AnalysisIdentityNotice/ReportView or their tests) was touched.

**Web execution note:** per thin-client policy I did not run vitest/node; I verified the orchestrator-captured CI evidence (`M5-T046-ci-evidence.md`): CI run `35425516645` **success** at `fb7af67d` (1038+1 tests green, incl. corrected AS-5 + new AS-5b). This is expected division-of-labor, not a BLOCKED cause.

---

## Surface 1 — `address-search.test.ts` DB-026 block: scenario → mutation-sensitive assertion

Each amended scenario maps to a real, mutation-sensitive assertion:

1. **AS-1** (`:233-247`) — resolves `SEARCH_1279_37_STREET_SIZE3` to `kind:"resolved"`, `bbl "3052960043"`, `matchedName "1279 37 STREET"`, `matchedHouseNumber "1279"`, `matchedStreet "37 STREET"`, `bin "3340270"`, `padVersion "26c"`, and records `matchType "fallback"`/`confidence 0.8`. The gate correctly selects feature[0] (feature[1] GARAGE has housenumber `"1279 GARAGE"` ≠ `"1279"`; feature[2] EAST 37 fails the street equality). Full-shape assertion — mutation-sensitive to any wrong-feature or wrong-field promotion. ✓

2. **AS-2** (`:249-254`) — `normalizeStreetForMatch("37th street")==="37 STREET"` and `("37 STREET")==="37 STREET"`, then `SEARCH_1279_37TH_STREET` resolves to the same bbl. A mutant that drops the `ORDINAL_SUFFIX` fold (`address-search.ts:249,258`) fails both the direct normalize assertion and the resolve. ✓

3. **AS-3** (`:256-273`) — reverse frontage `SEARCH_3622_13_AVENUE` resolves **through code** to the same `bbl "3052960043"` with a **different** `matchedName "3622 13 AVENUE"` (`≠ trueHit.matchedName`); GARAGE sibling asserted same bbl / different bin. Kills a mutant keying identity on address-string or bin. ✓

4. **AS-4** (`:275-282` + `:284-296`) — both nonsense probes (wrong street, out-of-range house) return `no_match` **and** `"lot" in result === false` (explicitly proves NO lot surfaced). The never-consult probe pins a feature at `confidence:1 / match_type:"exact"` on a wrong street and still asserts `no_match`. ✓

   **Reviewer question 1 answer — YES, a confidence-gating mutant FAILS these tests.** Replace the equality gate (`address-search.ts:320-321`) with a `confidence >= 0.8` (or `match_type` peek) gate: the fallback/0.8 wrong-street and out-of-range bodies (`:275`) would be promoted → `no_match` assertions fail; and the `confidence:1/exact` wrong-street feature (`:284`) would be promoted → `no_match` fails. The mutant dies on two independent probes. This is the load-bearing adequacy result and it holds.

5. **AS-5** (`:298-316`) — the abbreviated `AUTOCOMPLETE_1279_37_ST` body: suggestions parse permissively (`length > 0`, tolerating absent confidence/match_type) **and** `resolveLotFromGeoSearch(body)` → `no_match` via the body's own `"37 st"` default parse (fail-closed on an uncontracted abbreviation). Mutation-sensitive to a parser that chokes on missing keys, and to any loose match that would promote `"37 st"`. ✓

6. **AS-5b** (`:318-331`) — same untouched body with an explicit reference parse `{houseNumber:"1279", street:"37 street"}` → `resolved`, `bbl "3052960043"`, `matchType` **null**, `confidence` **null**, `padVersion "26c"`. Kills a mutant that requires the two keys to be present, or that records `""` instead of `null`. Together AS-5 + AS-5b are **non-vacuous**: they prove the AS-5 `no_match` is specifically the abbreviation fail-closed, not a structural parse failure (same body, only the parse differs). ✓

---

## Surface 2 — confirm-arc journeys (`address-confirm.test.tsx` + `autocomplete.test.tsx`)

- **Differing case** — `address-confirm.test.tsx` S8 (`:492-517`): entered `"1279 37 street"` (verbatim lower-case) preserved and asserted distinct from the canonical matched line `"37 STREET"`; identity carried forward is `resolved-bbl "3052960043"`, never the address string.
- **Equal case** — S8 (`:519-524`): happy path keeps the entered-input record present (`"120 BROADWAY"`).
- **Raw typed-input preservation through pick** — S9 (`:597-639`): types `"  1279 37 st bk  "` (with surrounding whitespace), picks the city-shaped `"1279 37 STREET"` suggestion, asserts `entered-input` `<strong>` textContent `=== typed` **including whitespace**, distinct from both the picked label and the matched `"37 STREET"`. Strongly mutation-sensitive: a trimming mutant fails the whitespace-exact `.toBe(typed)`; an inputEcho-instead-of-typedInput mutant renders `"1279 37 STREET"` and fails both the `.toBe(typed)` and the `not.toContain("37 STREET")`. This directly guards `AddressConfirmCard.tsx:96-103` (the `typedInput` preference) and the `AddressAutocomplete` capture path.
- **onPick raw-text capture** — `autocomplete.test.tsx` (`:11-32`): types `"120 Queens"`, picks `"QUEENS BOULEVARD"`, asserts `onPick(query, "120 Queens")` — raw text distinct from the picked street. Non-vacuous.
- **a11y-relevant assertions:** preserved and exercised — S1 heading is the focus target (`:190-192`, `[data-outcome-heading]`, `document.activeElement`), ZoLa link resolved by role+shared accessible name (`:463-470`), combobox `aria-expanded="false"` after pick (`autocomplete:31`), decorative glyph `aria-hidden` (`:310-317`). No a11y regression introduced.

---

## Surface 3 — regression (M5-T032 byte-identical-behavior claim)

Verified against the material diff (`3182f0a2~1..3182f0a2`) and the AS-5 correction diff (`fb7af67d~1..fb7af67d`):

- `address-search.test.ts`: DB-026 block is a **clean append after line 206**; the only change to an existing line is the shared `import` (adds `normalizeStreetForMatch`, `resolveLotFromGeoSearch`). All M5-T032 blocks (recovery, DB-006, AS-8 per-borough) are untouched. Behavior byte-identical.
- `address-confirm.test.tsx`: S8/S9 appended after line 483; S1–S7 (M5-T016/T023/T024) untouched.
- `autocomplete.test.tsx`: the M5-T032-labeled describes (`:50-308`) are untouched. Only one **base** test (`"debounces typing…"`) had its `onPick` assertion extended to carry the raw typed text — a necessary adaptation to the changed `onPick` signature, not a behavior regression; the debounce and `aria-expanded` assertions in that test are unchanged.
- CI evidence confirms all 1038+1 tests green at `fb7af67d`; the single pre-correction failure was exactly one test (AS-5) and the fix was **test-file-only** (`fb7af67d` diff touches only `address-search.test.ts`, matching the CI-evidence "no production code changed" claim). ✓

**Answer to reviewer question 3:** Yes — the pre-existing M5-T032 outcome tests are untouched and passing per the CI evidence; the only pre-existing assertion modified is the base autocomplete `onPick` call (contract adaptation, not a regression).

---

## Surface 4 — gaps / vacuity / untested seams

**No blocking gaps.** Every contracted, testable scenario (AS-1..AS-6, AS-9-adjacent) has a mutation-sensitive probe; no probe passes vacuously (AS-4/AS-5 `no_match` assertions are anchored by AS-1/AS-5b `resolved` assertions on the same or sibling bodies). AS-8 (walkthrough) and AS-9 (registry shape) are HJ / data-contract surfaces respectively, outside G4 test-adequacy.

**Advisory findings (non-blocking):**

- **A1 (advisory)** — Reviewer's specific concern "no probe that an ABBREVIATED /search input also fails closed": the abbreviation fail-closed IS exercised, but via the *autocomplete* body's default parse (AS-5, `:298`). Because `resolveLotFromGeoSearch`/`normalizeStreetForMatch` are endpoint-agnostic, this covers the behavior in substance; there is no dedicated `/search`-shaped abbreviation fixture. Recommend a future packet that grows the gate add one `/search` abbreviation body for symmetry. Not blocking — the seam is behaviorally covered.

- **A2 (advisory)** — Two thin spots: (1) the GARAGE sibling is asserted at fixture level (`:269-272`) rather than resolved through code (it is not independently query-resolvable in the corpus, so this is reasonable); (2) the `enteredInput ? … : null` absent branch (`AddressConfirmCard.tsx:130`, when both `typedInput` is blank and `inputEcho` is empty) is untested. Low risk (resolved docs always carry `input_echo` on the tested paths). Re-flag if a future promotion packet exercises a BBL-only confirm path.

---

## Conclusion

Test adequacy against the AMENDED AS-1..AS-6 (plus AS-5b) is strong and mutation-sensitive; the equality gate is provably resistant to a confidence/match_type-gating mutant; regression of the M5-T032 suites is preserved (behavior byte-identical, additive-only save the necessary import and one `onPick` signature adaptation); content identity is digest-verified at the frozen SHA; scope is clean. **Verdict: PASS**, with advisory findings A1 and A2 recorded for a future GeoSearch-promotion packet.

Requested status: this is my read-only verdict — the orchestrator records the G4 gate.
