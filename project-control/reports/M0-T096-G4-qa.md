# GATE REPORT — G4 (QA) — M0-T096 (D-024 unit I)

> Verbatim reviewer return (qa-engineer agent, read-only, dispatched at frozen HEAD
> `1a935fb`; transport entity-decoding only — `-&gt;`/`&lt;`/`&gt;` rendered as `->`/`<`/`>`).
> Recorded by the orchestrator.

## Identity verified

- **Frozen review identity:** `1a935fb2f6f5859da6418fd6750733be3e7589c7` (commit: "M0-T096 control: G2 recorded PASS at 9b8ad17 + submit to awaiting_gate"), branch `control/D-024-fable-codex-loop`. Confirmed reachable in the shared object store via `git cat-file -t` and `git log --oneline`.
- **Deliverable code commit:** `5ff7f08` confirmed reachable; `git show --name-only 5ff7f08` shows it touched exactly four files — `tools/agent_supervisor/cli.py`, `tools/agent_supervisor/golden_run.py`, `tools/agent_supervisor/live_observation.py`, `tools/test_agent_supervisor_golden_run.py` — all inside the packet's `allowed_paths`; **no forbidden path touched** (scope discipline PASS).
- **Reviewer environment note (transparency):** my dispatched worktree HEAD is `d8b3899f…` (a different, reduced branch that does not contain the deliverable files). I therefore did **not** checkout; I extracted the frozen tree read-only with `git archive 1a935fb tools project-control/reports/M0-T096-*.md project-control/campaigns/D-024-fable-codex-loop.json | tar -x` into an isolated scratch dir and ran pytest there. No git write, no `gh`, no `project_control.py`, no repo file edited.

## What I executed and results

```
python --version                         -> Python 3.11.9   (pytest 8.4.2)
python -m pytest tools/test_agent_supervisor_golden_run.py -q   (from frozen extraction root)
  first run:  1 failed, 39 passed in 18.24s
     -> the ONE failure was FileNotFoundError on project-control/campaigns/D-024-fable-codex-loop.json,
        which my partial extraction had not yet staged (env artifact, not a defect).
  after extracting the campaign file:
     40 passed in 15.99s
python -m pytest tools/test_agent_supervisor_golden_run.py --collect-only -q  -> 40 tests collected
```

**40/40 PASS reproduced at the frozen identity**, matching the report's section 4.4 claim. The three register meta-tests (`test_every_16_9_item_maps_to_a_real_named_test`, `test_every_r186_step_maps_to_a_real_named_test`, `test_every_test_rung_maps_to_a_real_named_test`) passed against the full frozen `tools/` tree — i.e. every cited `(file, fragment)` machine-resolves to a real test name. I did **not** re-run the whole suite (externally killed on this host per instructions); the 2,584/2-skipped supervisor total and 373+2+184 non-supervisor totals + CI-on-pushed-SHA are treated as orchestrator-captured stored evidence and are plausible/consistent with the clean 40-pack and the two clean new modules.

I independently spot-checked eight of the most load-bearing citations with `grep -oE "def test_…"` against the cited files (invariants 7/9, chain exhaustion+return-to-pin, turnover exactly-one-opus, loop forward-idempotency, github-flow confirmed-push, seam successor-handoff, reviewer read-only-git). All resolved to **exact, semantically appropriate** test names — no substring false-positives.

## Scenario matrix → test mapping (report section 1: P-1, GR-1..17, W-1..7 = 25 rows)

| Staged scenario | Covering test(s) | Status |
|---|---|---|
| P-1 prove-first register | `Section169RegisterTests` (2), `GoldenSequenceRegisterTests` (2), `LadderRegisterTests` (3) | COVERED |
| GR-1 exact-command two-unit golden run | `test_the_two_unit_golden_run_crosses_a_rotation_with_no_human_step` (+ `..._records_the_exact_owner_command_shape`) | COVERED |
| GR-2 real low-risk repo effect | asserted inside `test_the_two_unit_golden_run…` (`git log` counts each commit ==1 on task branch) | COVERED |
| GR-3 safe-seam rotation inside run | `test_the_two_unit_golden_run…` (`last_rotation` set, `rotation_pending` False, kinds `[work,ready,work]`) | COVERED |
| GR-4 injected controller restart, exact-once | `test_an_injected_controller_restart_continues_without_duplicate_work` | COVERED (see INFO-1) |
| GR-5 injected refusal | `test_an_injected_refusal_records_intent_and_holds` + `test_an_injected_refusal_cannot_actuate_the_bridge` | COVERED |
| GR-6 injected quota exhaustion | `test_an_injected_quota_exhaustion_is_detect_and_hold` | COVERED |
| GR-7 injected fallback sequence | CITED: `CHAIN::…opus_4_8_after_a_fable_5_exhaustion` + `TURNOVER::authorized_exhaustion_redispatches_exactly_one_opus_worker` (16.9-b register / ladder r05) | COVERED by citation (prove-first) |
| GR-8 ambiguous-effect recovery | `test_an_ambiguous_effect_blocks_the_restart_from_dispatching` | COVERED |
| GR-9 status/ask while running | `test_status_answers_read_only_while_the_producer_is_running` | COVERED |
| GR-10 double-start idempotency | `test_a_second_start_while_the_lock_is_held_is_a_typed_refusal` | COVERED |
| GR-11 multi-epoch × forced rotation | `test_three_epochs_each_end_through_a_forced_rotation` | COVERED |
| GR-12 extended pause, clean resume | `test_an_extended_pause_survives_restart_and_resumes_cleanly` | COVERED |
| GR-13 accelerated overnight + restart | `test_an_accelerated_overnight_campaign_survives_a_mid_campaign_restart` | COVERED |
| GR-14 bounded soak via accelerated counters | `test_the_bounded_soak_crosses_every_breaker_boundary_exactly` + `test_soak_tallies_survive_a_mid_soak_restart_without_growth` | COVERED |
| GR-15 autonomous bounded selection + correct-next | `test_an_unbounded_next_unit_selection_refuses` + `test_advance_moves_to_the_dependency_correct_next_task` | COVERED |
| GR-16 progression-ladder register | `LadderRegisterTests` (3 tests; 11 rungs, exactly one owner-gated) | COVERED |
| GR-17 on-demand retrieval after compact | `test_the_compact_handoff_omits_content_that_deep_retrieval_returns` | COVERED |
| W-1 watcher structurally passive | `test_the_watcher_module_is_structurally_passive` + `test_the_watcher_writes_only_its_own_register_keys` | COVERED |
| W-2 natural-event capture, idempotent | `test_capture_is_idempotent_and_carries_the_five_fields` | COVERED (partial — see MINOR-1) |
| W-3 injected never masquerades as live | `test_injected_session_provenance_labels_injected` + `test_the_harness_marker_wins_over_a_live_session_scan` | COVERED |
| W-4 no premature verification | `test_no_code_path_writes_verified_live_true` + `test_a_clean_live_scan_yields_only_a_candidate` | COVERED |
| W-5 pre-graduation natural event fail-closed | `test_a_pre_graduation_natural_event_holds_fail_closed` | COVERED |
| W-6 sanitization at register boundary | `test_register_rows_are_sanitized_at_the_boundary` | COVERED |
| W-7 refusal seam wired in production start | `test_the_refusal_seam_is_reachable_from_the_production_start_path` + behavioral `test_an_injected_refusal_records_intent_and_holds` | COVERED |

**No unmapped scenario.** Register self-consistency verified: 16.9 = 13 items, R186 = 15 steps, ladder = 11 rungs (exactly one owner-gated rung `r11`), all asserted by count meta-tests.

## Test quality assessment

- **Exactly-once is genuinely pinned, not inferred.** `golden_run.py` has the fake producer **child processes** append their own `{launch, pid, kind}` rows to `launch_log.jsonl`; the golden-run test asserts the child-written sequence `["work","ready","work"]`, then `git log` counts `unit-a.txt`/`unit-b.txt` each ==1 on the task branch, then `journal.unsent_outbound()==[]` and `journal.pending_effects()==[]`. The restart test asserts `["work","work"]` (unit A never re-runs). This is child-attested + git-attested + outbox/pending-attested — strong.
- **Rotation is independently asserted** via `journal.get_state("last_rotation")` not-None and `rot.rotation_pending()` False (usage 500k crossing a 100k threshold), not merely inferred from the "ready" launch. READY successor is exercised.
- **Watcher tests are predominantly behavioral** (record→read register rows, CAS re-scan==0, sanitization redaction_count, injected/live labeling, comparison refusal, graduation not_ready), with two complementary **source-scan** guards (structural passivity; no `verified_live: True` literal). I confirmed in `live_observation.py` that `verified_live` is a hard-coded constant `False` on every produced row (line 289) with no assignment path, CAS via `compare_and_swap_state(key, None, record)`, and a closed `EVIDENCE_CLASSES = (injected, live_candidate)` with no `live` value — the fail-closed direction is correct (session `injected` OR marker present ⇒ injected).
- **cli.py wiring is a real construction, not a string.** `_run_loop` builds `GuardrailBridgeIntegration(journal=…, authorized_task=AuthorizedTaskRecord(from packet fields))` and passes `guardrail_bridge=guardrail_bridge` to `SupervisedLoop`; a packet without authorization/criteria yields an unproven `AuthorizedTaskRecord` → classifier fails closed (`CONDITION_AUTHORIZATION_UNPROVEN`), exactly as the report claims. The behavioral refusal test drives the CLI-built loop end-to-end to the R070 journal record. The watcher epilogue is wired in a `finally` block with try/except so a watcher failure is audited (`live_observation_scan_failed`) and never breaks `start` — matches the claim.
- **Mutation matrix (12/12) is adequate and each mutant is plausibly killed** by its named test across the important gates: labeling (evidence-class default flip, marker-scan removal, verified_live flip), CAS (CAS→overwrite), wiring (bridge nulled in `_run_loop`; epilogue scan dropped), comparison side-check, provenance check, graduation-ready flip, refusal-source discovery removal, fake-refusal shape loss, READY-successor-never-READY. I could not re-execute the mutation harness (orchestrator-captured), but each named killing test's assertions do fail under its stated mutation.

## Deliverables + placeholder check

All four named packet outputs exist at the frozen identity: `golden_run.py` (harness + fault injection), `live_observation.py` (watcher/register), `tools/test_agent_supervisor_golden_run.py` (1,017 ln / 40 tests), `M0-T096-golden-run-evidence.md`, `M0-T096-activation-package.md`. Activation package **items 1–14 all present with concrete content, no placeholder/TODO/TBD text**; the `<path>`/`<id>`/`<digest>` tokens are legitimate operator command-argument templates. Item 11 forward-references the gate wave (including this G4) that records verdicts after delivery — expected. Amendment-8 sequencing (M0-T110/T111/T112 deferral, R246/R247) is stated honestly and matches the packet progress log.

## Amendment-7 lane discipline

Confirmed. `golden_run.py` fakes are local-only (git + file I/O; no network); `live_observation.py` docstring/code contain no subprocess/prompt/context path (behaviorally and source-asserted). Every harness-born record carries `INJECTED-GOLDEN-RUN`; the marker backstop forces `evidence_class="injected"` even under a `live` session scan (proven by `test_the_start_epilogue…injected`). No test contacts a provider, waits for, or provokes a natural event; the natural-event lane stays `pending_live_observation` and gates only the 4.8-bridge actuation (R228). `assert_actuation_permitted` double-gate untouched.

## Findings by severity

- **BLOCKING:** none.
- **MAJOR:** none.
- **MINOR-1 (W-2 partial event-type coverage):** `test_capture_is_idempotent_and_carries_the_five_fields` injects only 3 of the discovery source kinds (guardrail_refusal, usage-limit quota, provider-abort availability) and asserts `rows_written==3`. The `EVENT_MODEL_TURNOVER` discovery branch (`model_change_audit`, lines 187–193), the turnover-transition→quota branch (195–209), and the outage retry/blocked availability branch (174–185) of `discover_events` are **not exercised by any behavioral test that asserts a row is produced**, and the mutation matrix's "refusal-source discovery removal" covers only the refusal branch. Concrete failure scenario: a future edit renaming the `model_change_audit` state key or breaking `_model_change_entries` would pass all 40 tests, silently un-capturing a natural model-turnover event. Risk is bounded (passive read-only observer; `verified_live` always False; graduation owner-gated ⇒ cannot cause a false PASS or actuation), so this is non-blocking, but W-2 in the report lists "model-change audit" as one of the five captured kinds without proving that path. Recommend a follow-up thin test injecting a `model_change_audit` entry (and ideally a turnover transition) asserting one labeled row each.
- **INFO-1:** the "injected controller restart" test models a clean process-boundary restart (two sequential `cli.main` invocations over persisted durable state), not a hard mid-write crash. The harder crash-after-every-transition matrix is covered by citation to the existing CRASH pack (R186 s14 / ladder r05) and by GR-8's `record_before_effect`-then-restart test, so exact-once-across-crash is adequately covered in aggregate. Consistent with the matrix's own framing.
- **INFO-2:** the pack reads two repo paths (`project-control/campaigns/D-024-fable-codex-loop.json` and cli/live_observation source); against a partial extraction the campaign test errors with FileNotFoundError. Against the full frozen tree it passes (40/40 after extracting the campaign file). Reproducibility note only.
- **INFO-3:** I could not run the whole suite or the mutation harness on this host (per instructions / external kill); the chunked suite totals and CI-on-pushed-SHA remain orchestrator-captured stored evidence, verified for plausibility. Full 83-requirement directive verification is the DCV's scope, not G4's.

## Verdict rationale

Every unit claim in scope for G4 is independently reproduced at the frozen identity: 40/40 tests pass; all 25 staged scenarios map to covering tests; all three executable registers' citations machine-resolve and spot-check as semantically correct; the golden-run assertions genuinely pin exactly-once/rotation/restart; the watcher is behaviorally and structurally fail-closed with a hard-False `verified_live`; the discovered-defect cli.py wiring is real, fail-closed, and bounded; all four deliverables exist with no placeholders; scope is clean; lane discipline holds. The one MINOR finding is a bounded test-coverage gap that cannot produce a false PASS or actuation.

VERDICT: PASS
