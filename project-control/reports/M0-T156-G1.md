# M0-T156 — G1 Code Review Gate Report (verbatim)

Orchestrator preservation note: report returned by the independent code-reviewer agent
("G1 code review M0-T156") on 2026-09-12 and preserved verbatim below. Reviewer was
read-only; frozen review identity: producer commits 2bdae13f + 012ba064. Verdict: PASS
(three advisories A1–A3, none blocking, no required rework).

---

All verification complete. Here is my full gate report.

---

# Gate Report

- Gate ID: G1 (code review)
- Task ID: M0-T156 — Directive-registry digest normalization (CRLF/LF); retire the control-plane CI red
- Reviewer: code-reviewer (read-only, ADR-005)
- Producer: orchestrator
- Result: **PASS**
- Clean environment/worktree used: reviewed at frozen producer identity. Confirmed the 8 allowed_path files are **byte-identical** across `012ba064` (frozen producer head), `a27b1ecc` (reviewed_sha in the submit record), and current `HEAD b89fc5ba` (`git diff --stat` empty for all 8 paths in both `012ba064..a27b1ecc` and `012ba064..HEAD`). `git diff 012ba064..HEAD` touches only `docs/design/ui-prototype.html`, `project-control/reports/M0-T156-ci-evidence.txt`, `M0-T156-evidence-map.json`, `M0-T156.json`, `state.json`, `tasks/M0-T156.json` — none of the 8 allowed_paths. Frozen identity holds; my recomputations/tests at HEAD are valid for the reviewed content.

## Acceptance criteria reviewed

| AS | Verdict | Reproduced evidence |
|---|---|---|
| AS-1 digest line-ending independent (red/green) | PASS | `test_crlf_lf_and_lone_cr_hash_identically` ran OK (0.019s). In-process: LF/CRLF/lone-CR all normalize to `sha256(b"line one\nline two\n")`; raw CRLF hash ≠ expected (red-half load-bearing) — removing normalization fails the CRLF `assertEqual`. Lone-CR decision documented in code + test + audit notes. |
| AS-2 real registry green both representations | PASS | Local `python tools/validate_directive_compliance.py --check` → **EXIT 0** (CRLF working tree). CI control-plane job `103531898219` → **SUCCESS** at head `012ba064` (LF checkout). Registry-level `test_representation_flip_validates_clean` → OK. |
| AS-3 re-stamps match LF actuals | PASS | Recomputed all six via `sha256(CRLF→LF→ lone-CR→LF bytes)` from blob@012ba064 **and** working tree; **all six equal the re-stamped manifest values** and carry the CI-reported prefixes 3704add2 / d8158188 / d0ae01b4 / d2876943 / dfab73c0 / 0e9de4cc. |
| AS-4 sources immutable / append-only | PASS | `2bdae13f --stat` + directive-tree scan: only the 4 manifests changed; **zero** byte changes to any source-*.md / amendment / requirements.json / verification.json / index.json. Each manifest: 6 digest values (12 changed digest lines) + `updated_at` + one appended audit_log entry. `locked_requirement_ids` unchanged; **no non-ASCII** bytes in added manifest lines; notes cite D-040-R003. |
| AS-5 tampering still detected | PASS | `test_content_tamper_still_detected_under_crlf` → OK. In-process adversarial proof: normalization collapses only {CRLF, lone CR, LF}↔LF; any non-CR/LF byte change (`X` vs `\r`, `e`→`3`) yields distinct normalized bytes. |
| AS-6 whole control-plane suite green | PASS | Local: validator + `test_project_control` + `test_directive_reminder` + `test_readonly_agent_guard` + `test_agent_dispatch_guard` EXIT 0 (per captured evidence); `LineEndingNormalizationTest` 3/3 OK (238.4s, reproduced). CI: control-plane SUCCESS + 16 other jobs SUCCESS; only failure = owner-gated `web-dependency-security`. |

## Directive/requirement verification

| Requirement ID | Reviewed SHA / content identity | Verdict | Reproduced evidence |
|---|---|---|---|
| D-040-R003 | a27b1ecc / identity 73d2a750 (content byte-identical to 012ba064 for the 8 allowed_paths) | **PASS** | (1) Sources IMMUTABLE — AS-4 confirms zero source/requirements/verification/index byte changes; fix lives entirely in digest computation + recorded-digest re-stamps. (2) Append-only/audit-log mechanism — one appended `digest_normalization_restamp` audit entry per manifest citing D-040-R003; no requirement id added/removed/renumbered; `locked_requirement_ids` untouched. (3) `required_harness` — control-plane CI green at fix head + validator EXIT 0, both reproduced. (4) `required_evidence` — no source-*.md content edit in the diff. |

D-001:ALL is cited for s19 governance coverage; its 136 requirements remain non-applicable by the task_ids conjunction (applicable set = cited = [D-040-R003]), consistent with the submit record `applicable=["D-040-R003"]`. Formal directive verification.json is the independent directive-compliance-verifier's record; this G1 pass confirms the R003 constraint holds in the actual diff.

## Steps independently executed

1. Read packet, producer report, CI-evidence file, D-040 requirements.json (R003).
2. `git show 2bdae13f` / `012ba064` — diff shape of tools, tests, and 4 manifests.
3. Recomputed all six digests from blob@012ba064 and working tree (normalized) — all match recorded.
4. Traced every caller of `sha256_file` / `sha256_text_artifact` / `normalize_text_artifact_bytes` across `tools/`.
5. Confirmed identity paths (`directive_registry.py:1204` content_manifest, `:1757` MigrationManifest.content_sha256) still use raw `sha256_file`; `content_sha256` has no consumers.
6. Ran `validate_directive_compliance.py --check` (EXIT 0) and the full `LineEndingNormalizationTest` (3/3 OK, 238s).
7. In-process adversarial normalization analysis (`\r\r\n`→`\n\n`, mid-line CR, tamper collisions).
8. `gh run view 34685572048` (read) — head 012ba064; control-plane job SUCCESS; sole failure = web-dependency-security.

## Expected versus actual

- Normalization ordering `replace(b"\r\n",b"\n").replace(b"\r",b"\n")`: adversarial `b"\r\r\n"` → step 1 leaves `b"\r\n"`, step 2 → `b"\n\n"` (two line breaks → two LFs). No content masking. Two inputs collide **iff** identical after mapping every {CRLF, lone CR, LF} to LF — i.e. differ only in line-ending representation. Tamper-safe as required.
- Adoption at exactly three registry-integrity sites: `directive_registry.py:553` (c2), `validate_directive_compliance.py:262` (migration manifest), `:496` (c14). `sha256_file` (`directive_registry.py:386`) unchanged. `requirements_id_digest` (`:1705`, joined-string) untouched. Matches the packet's site list exactly.
- Migration manifest is LF on both blob and working tree (norm==raw==recorded `e14190fa…`); the normalized migration check still passes — no raw-vs-normalized divergence.
- Completeness of the six: proven by CI control-plane SUCCESS at the fix head (executable authority over the real registry on the LF side) plus the sound argument (local raw EXIT 0 ⇒ recorded==local bytes for all; CI reported exactly six LF mismatches ⇒ exactly six were CRLF-recorded). No hidden seventh site.

## Evidence paths

- `C:/Users/MLFLL/Downloads/nyc-zoning/ctl24/tools/directive_registry.py` (:386-408, :553, :1204, :1755-1757)
- `C:/Users/MLFLL/Downloads/nyc-zoning/ctl24/tools/validate_directive_compliance.py` (:262, :496)
- `C:/Users/MLFLL/Downloads/nyc-zoning/ctl24/tools/test_directive_compliance.py` (:348, :384-385, LineEndingNormalizationTest :568-631)
- `C:/Users/MLFLL/Downloads/nyc-zoning/ctl24/project-control/directives/{D-032-astra-loop-activation-and-review-reconciliation,D-033-supervisor-management-layer,D-034-architecture-context-and-review-skill,D-038-build-product-not-self}/manifest.json`
- `C:/Users/MLFLL/Downloads/nyc-zoning/ctl24/project-control/reports/M0-T156-producer-report.md`, `M0-T156-ci-evidence.txt`
- CI run 34685572048, control-plane job 103531898219 (SUCCESS)

## Human-style walkthrough findings

N/A — governance/tooling task, no UI (packet: no G3).

## Regression/security/provenance findings

- No production behavior beyond digest computation changed. Tamper detection (c2/c14) retains full strength; the only forgiven mutation is line-ending representation — verified adversarially.
- Recorded **material identities** deliberately preserved: identity paths still hash raw bytes, so no accepted-work content identity shifts.
- Modularity: two small cohesive helpers added to the module that owns registry digests; validator consumes via import. No responsibility mixing, no dumping ground, no threshold concern (~27 production lines added).

## Defects

None blocking. None requiring correction.

## Required rework

None.

Advisories (non-blocking; orchestrator's discretion):

- **A1 (advisory) — audit_log key drift.** The new `digest_normalization_restamp` entries use keys `at`/`actor`/`action`/`note`. The canonical schema `project-control/directives/schema/v1/directive_manifest.schema.json:77-89` declares audit_log items `required: ["at","by","note"]`; the new entries omit `by`. This matches the pre-existing `actor`/`action` style already in D-032 and D-038, but introduces intra-file inconsistency in D-033/D-034 (whose prior entries use `event`/`by`). It does **not** fail validation: `validate_directive_compliance.py` only asserts the schema files exist and that top-level `audit_log` is present (MANIFEST_REQUIRED, :50-53) — it never applies jsonschema to entries — so CI/local are green and the corpus already diverges. The schema file is outside allowed_paths, so it cannot be harmonized in this task. Latent schema-vs-data drift worth a future cleanup.
- **A2 (advisory) — parallel normalizer divergence.** `tools/status_projection.py:108-113` `_sha256_file` normalizes CRLF→LF but not lone-CR, unlike the new `normalize_text_artifact_bytes`. It serves review-report display digests, not registry-integrity cross-platform comparison, so it is out of scope and harmless today; noted only so no future caller assumes the two helpers are equivalent.
- **A3 (observation, not a defect).** `content_manifest` (`directive_registry.py:1166-1212`, explicitly LEGACY/non-authoritative, unused by the CLI) and `MigrationManifest.content_sha256` (`:1755-1757`, no consumers found) intentionally keep raw `sha256_file`. Correct per the packet's identity-preservation requirement.

## Reviewer conclusion

The normalization primitive is correct and provably tamper-safe (line-ending representation only). It is adopted at exactly the three registry-integrity sites and nowhere that would shift a recorded material identity. All six re-stamps recompute exactly to the manifest values and to the CI-reported LF actuals; the manifest diffs are surgical (six digests + `updated_at` + one append-only audit entry each), sources are immutable, and `locked_requirement_ids`/non-ASCII constraints hold. The three new tests are real red/green with sound error filters and no weakening of existing assertions. The control-plane CI job is SUCCESS at the frozen head — the standing red is retired — with the sole remaining failure being the owner-gated `web-dependency-security` (D-040-R004, preserved). D-040-R003 is satisfied. All six acceptance scenarios independently reproduced.

VERDICT: PASS
