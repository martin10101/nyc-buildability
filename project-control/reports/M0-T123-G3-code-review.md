# M0-T123 — G3 code review (VERBATIM reviewer return; saved by the orchestrator, transport entity-decoding only)

All checks reproduce. Compiling the G3 report.

---

# G3 CODE REVIEW REPORT — M0-T123 (resume-path defect: launch/resume enforcement seam)

**Task:** M0-T123 — D-024 Amendment 19 (R327–R347); close the resume-path defect class
**Producer:** supervisor-resume-path-producer (backend-engineer) — I am independent
**Frozen head:** `git rev-parse HEAD` = `10cebf0` (verified). Reviewed material identity = `6aada29`; `6aada29..10cebf0` touches only control-plane evidence (gates/reports/state/task), source tree identical.
**Reviewer:** code-reviewer (read-only).

## Method
Both root causes re-traced in pre-fix source (`7b78d6c`); every guard, the sweep, the fixture, and the terminal event reproduced from primary evidence.

---

## Findings

### F1 — Both root causes re-traced in pre-fix code — PASS
**(a) Ceiling never evaluated before the first dispatch.** In `git show 7b78d6c:tools/agent_supervisor/loop.py`, `run()`'s cycle loop dispatches the first unit unconditionally (`result = self.run_cycle(prompt, cycle=index)` at the top of `for index in range(start_index, …)`); the rotation seam fires only *between* cycles (`if index < self.config.max_cycles and (self.rotation_pending() …)`, ~loop.py:2582) and on the `FORWARD_PROMPT` branch (~2523). `_flag_rotation_if_needed` (loop.py:936-950) only *sets* `rotation_pending` durably on unit return. So an ordinary IDLE→PREFLIGHT→first-cycle start never consults `rotation_pending` before the first provider contact — the producer's account is correct.
**(b) cwd defaulted to the checkout.** `git show 7b78d6c:tools/agent_supervisor/cli.py` line 2642: `worktree = pathlib.Path(args.worktree or repo).resolve()` with `repo = args.repo or checkout` (2641); absent `--worktree` ⇒ worktree = primary checkout, then `RunnerConfig(cwd=str(worktree), …)` (2672) launches the worker there. Confirmed. The M0-T107 packet declares `"worktree": "C:/Users/MLFLL/Downloads/nyc-zoning/wt-m0t107"` (M0-T107.json:61), so the mismatch is real.

### F2 — One seam; sole worker Popen guarded; probe exemption sound — PASS
`claude_runner.py` has exactly two `subprocess.Popen`: line 1256 (`run_unit`, the worker dispatch) and 1689 (`probe_model_launch`). The run_unit Popen is preceded by `launch_seam.enforce_launch` at 1218-1230 (raising `RunnerError` on any non-proceed). The reachability sweep's `_dispatchers()` mechanically finds every function that BOTH calls `build_argv` and `Popen`, and `test_the_only_worker_dispatch_popen_is_run_unit_and_it_is_seam_guarded` asserts the set is *exactly* `{run_unit, probe_model_launch}` — so a third launch site would turn it RED (the M0-T121 F2 "hidden site" lesson is addressed by the set-equality assertion, and by per-site checks). The probe exemption is sound: `probe_model_launch` is a fresh launch (ceiling never applies to non-resume), brokers no permissions, resumes nothing, and appends no checkpoint contract — the test pins the exemption to observable properties (`assertNotIn enforce_launch`, `assertFalse …with_checkpoint_contract`). Other package Popens (`preflight.py:126`, `process.py:764`) do not call `build_argv` and are not worker dispatches, so they are correctly outside the sweep.

### F3 — Ceiling semantics — PASS
`evaluate_ceiling` (launch_seam.py:269-302): fresh launch → `None`; unknown telemetry (`None` or `usage_known` False) → `REFUSE`/`ceiling_telemetry_missing` (never assumed below); `tokens >= ceiling` → `ROTATE`/`over_ceiling_resume_forbidden` (exactly-at is at-or-above). Verified by `test_AS2_*` (400_000 exact → ROTATE; 399_999 → resume; `None`/stale → missing; fresh → none). The 400k ceiling is imported from `RotationThresholds().context_rotation_threshold` (single owner-policy source, no drift — `test_ceiling_is_the_single_owner_policy_400k`). Rotation routes through the real mechanism: the pre-first-dispatch shed (`_rotate_over_ceiling_before_first_dispatch`, loop.py:970+) clears the recorded provider session (`clear_provider_session` → old transcript frozen, R338), drops any runner resume binding, consumes `rotation_pending`, and appends a first-class `over_ceiling_session_shed` audit event — preserving task identity/budgets/audit/branch/worktree (it touches none of them) and is idempotent (`test_AS4_shed_is_idempotent`, exactly-once R337). Turnover threads telemetry (`loop_turnover.actuate_resume` reads the recorded session tokens onto `with_resume`), and `with_resume` (claude_runner.py) uses `dataclasses.replace` so it preserves `expected_worktree` and refuses an over-ceiling `--resume` at the runner. The unknown-telemetry live case is shed via the durable `rotation_pending`/`context_threshold` flag (`test_AS1_sheds_on_durable_flag_even_when_telemetry_unknown`; `context_threshold ∈ CONTEXT_SHEDDING_REASONS`).

### F4 — cwd binding, Windows-aware, no evasion — PASS
Production gate `evaluate_packet_worktree_binding` (called in `_run_loop` before the runner is built, raising `LoopError`) refuses when the bound worktree is not the packet's declared worktree, naming the primary-checkout case specifically. For the reproduced defect (bound=ctl24, packet=abs wt-m0t107, primary=ctl24) it returns `CWD_PRIMARY_CHECKOUT` (`test_packet_worktree_mismatch_on_primary_is_named`). `normalize_path` = `normcase∘normpath`, so drive-letter case and slash direction fold (`c:/…/wt-m0t107` == `C:\…\wt-m0t107`, `test_AS3_windows_drive_case_and_slashes_still_match`); empty cwd/expected → `CWD_UNBOUND`. I checked evasion directly: `same_path(ctl24, wt-m0t107)` = False, dot/trailing-slash forms fold, empty never matches. No symlink/UNC/relative form yields a false *proceed*: the supervisor sets `cwd = str(worktree.resolve())` (realpath) and the guard requires lexical equality of resolved paths — any ambiguity fails **closed** (REFUSE), never open. The runner-level `evaluate_cwd` re-checks at the Popen chokepoint.

### F5 — Bypass-sensitive reachability sweep — PASS (one INFO)
The sweep derives facts from source ASTs (nothing hand-listed): `_call_linenos` proves `enforce_launch` precedes the single Popen in `run_unit`; `_strip_stmts` prunes a seam statement and re-runs the checker for three per-site RED reproductions (`test_RED_removing_the_seam_statement_uncovers_run_unit`, `test_RED_run_loop_without_the_gate_is_a_bypass`, `test_RED_run_without_the_shed_statement_is_a_bypass`). Site-granular (each of the three enforcement points has its own removal-sensitivity test), so one guarded site cannot mask another.

**INFO (non-blocking):** the runner-level `enforce_launch` (both cwd *and* ceiling) is gated on `if self.config.expected_worktree:`. Production `_run_loop` always binds `expected_worktree` (+`primary_checkout`), and `with_resume` preserves it via `dataclasses.replace`, so both guards are always active in production — but `test_run_unit_seam_precedes_the_only_worker_popen` verifies only textual order, not that the call is unconditional, so a future regression that left `expected_worktree` unbound on a production resume would skip the runner guard without turning the sweep RED. This is mitigated by defense-in-depth: the ceiling is independently enforced by the *unconditional* pre-first-dispatch shed and by turnover telemetry threading, and cwd by the unconditional CLI packet-worktree gate. The producer documents this as "Risk (low)". Recommend (not required) a follow-up test asserting the production launch path binds `expected_worktree`.

### F6 — Fixture integrity — PASS
I independently hashed the live preserved transcripts: cycle-2 (`798d2f00…jsonl`) SHA-256 = `3c9185687f12e86a2e066b18e8347a15840be94f981a52af3965f01394adbfaf` and cycle-1 (`02b014ee…jsonl`) = `3a0d1f30664b1deba7b6cd47a0a69bdc84906332eb3ed180aea5e74e2f8b9b17` — **byte-identical** to the fixture's recorded `source_sha256` (read-only derivation confirmed, R341/R329; sources never written). The tests consume only `source_sha256`, `durable_journal_state_at_cycle2_start`, `audit_excerpt`, `worker_transcripts` (grep-verified). The orchestrator's disclosed provenance edit touches `_provenance.runtime_dir_name` (a truncated SHA-256 of the checkout path — a naming digest, not a secret, PUBLIC), which **no test references** — so it changed no test-consumed data.

### F7 — Regression surface + tests — PASS
`git diff 7b78d6c 6aada29` leaves `policy.py`, `broker.py`, `start_gate.py`, `recovery_probes.py`, `recovery.py`, and `restart_channel.py` **byte-unchanged** (restart_channel is in allowed_paths but was not touched — its semantics are intact). Reproduced: `test_agent_supervisor_launch_seam.py` → **45 passed**; `restart_channel + golden_run` → **76 passed**; my broader run `loop + loop_turnover + session_continuity + claude_runner_env + rotation` → **203 passed**. `modularity_check --check` → failures 0; `launch_seam.py` (338 SLOC, a properly extracted focused module — extract-before-growth honored) not among warnings; loop.py/claude_runner.py additions did not breach a threshold. The producer's own count correction (46→45 bundled-run misattribution; 2869→2870 pre-final-test) is disclosed in the report and G2 and matches my independent 45.

### F8 — Terminal-evidence honesty (R343/R344) — PASS
I read the primary cycle-2 transcript directly: record 94 is `attachment {"type":"max_turns_reached","maxTurns":12,"turnCount":13}` at `cwd=C:\Users\MLFLL\Downloads\nyc-zoning\ctl24`; the file has 96 records, **no `result`-type record** (independently enumerated: types = queue-operation/attachment/user/atis-latch/assistant/last-prompt), and **every** record's cwd is the primary checkout (uniform wrong-cwd defect). This matches the producer's recovered finding: the worker exhausted its 12-turn budget (turn 13) re-orienting in the wrong cwd. The producer report §6 and `test_AS8_recovered_terminal_event_is_max_turns_not_a_provider_rejection` explicitly **contradict and abandon** the earlier "probable provider context-limit rejection" inference (M0-T107 journey §3.3, which labeled it "probable") as unproven. Honest, correct.

### F9 — Directive requirement rows (D-024 ALL; resolver set R327–R347) — PASS
Reproducibly evidenced: R327–R329 (S16.7 disposition; no budget change — policy/broker unchanged, touch history preserved) — governance + F7; R330 (authorized, cited in packet+commit); R331 (both live failures closed) — F1/F3/F4; R332 (ceiling on every launch/resume path) — runner seam always-on in production + shed + turnover telemetry; R333/R334 (never resume ≥400k; rotate or fail closed) — F3; R335/R336 (cwd bound; primary/unexpected fails closed) — F4; R337/R338 (rotation preserves lineage/identity/budgets/audit/branch/worktree/exactly-once; old transcript frozen; distinct new session in wt-m0t107) — F3; R339/R340 (call-site enumeration + bypass-sensitive test) — F2/F5; R341 (read-only fixtures) — F6; R342 (matrix: bands/missing-telemetry/Windows/cwd-mismatch/removal-sensitivity/provider-failure-as-typed-refusal all have named passing tests); R343/R344 (terminal honesty) — F8; R345 (no live restart/clear-recovery/journal edit/budget reset/PR #241) — sources byte-unchanged, no such calls in the diff, PR #241 untouched; R346/R347 (recert M0-T124 + R276 preflight + STOP for owner live-start decision) — recorded in producer report §10, not executed (correct — this task does not recert). No gap between a named requirement and reproducible evidence.

---

## Assessment
Both reproduced root causes are correctly diagnosed and closed by a single, focused, pure-decision enforcement module routed through the sole worker `Popen`, the CLI packet-worktree gate, and an unconditional pre-first-dispatch shed, with the resume/turnover path carrying ceiling telemetry to the runner backstop. Ceiling semantics (at-or-above never resumed, unknown fails closed), Windows-aware fail-closed cwd binding, exactly-once idempotent rotation with transcript freeze, a mechanical site-granular bypass-sensitive sweep, and read-only fixtures (source hashes independently verified byte-identical to the live transcripts) all reproduce. The terminal event is independently confirmed as `max_turns_reached`, with the prior context-limit hypothesis honestly contradicted. Regression surface (policy/broker/start_gate/recovery_probes/recovery/restart_channel) is untouched; 45 + 76 + 203 tests pass; modularity is clean. The one INFO (runner-seam guards coupled to `expected_worktree`, mitigated by three-layer defense-in-depth and documented) is a non-blocking robustness recommendation. No defects; no required corrections.

VERDICT: PASS

---

# G3 DELTA ATTESTATION (VERBATIM reviewer return; hardening 6aada29 -> 16e1b3b; saved by the orchestrator)

## DELTA ATTESTATION — M0-T123 hardening round

**Head:** `git rev-parse HEAD` = `16e1b3b` (verified). **Delta vs reviewed `6aada29`:** within `tools/`, only `claude_runner.py` and `test_agent_supervisor_launch_seam.py` changed (`git diff --stat` confirmed).

**(1) claude_runner is unwrap-only.** `git diff -w 6aada29 16e1b3b -- claude_runner.py` shows the sole logic change is removal of the `if self.config.expected_worktree:` wrapper; the `enforce_launch(...)` call (1224-1230) and the `if not _decision.ok: raise RunnerError(...)` (1231-1235) are byte-identical, now unconditional. The 26+/21- stat is comment expansion + re-indentation. I read the region: no other change in run_unit. F5-INFO closed — the ceiling guard now runs on every worker path (the seam internally skips only the cwd guard when `expected_worktree` is empty).

**(2) Re-wrap detection — genuine, with an honestly-noted narrowness.** `_enforce_nested_under_expected_worktree_if` walks for any `ast.If` whose test references a Name/Attribute `expected_worktree` containing an `enforce_launch` call; `test_RED_re_wrapping...` feeds a synthetic re-wrapped run_unit and it flags True, the real run_unit flags False. HONEST LIMITATION (as asked): a re-wrap under a *differently-named* condition (e.g. `ew = self.config.expected_worktree; if ew:`) would evade this AST check, since the `If.test` would reference `ew`, not `expected_worktree`. BUT the behavioral test `test_R332_worktree_less_over_ceiling_resume_still_refuses` pins the actual invariant — an unbound over-ceiling runner refuses at the chokepoint — so ANY re-wrap that suppresses the ceiling on unbound runners fails behaviorally regardless of naming. The AST test adds targeted syntactic sensitivity; the behavioral test is the robust guard. The pair is sound.

**(3) G5 hypothetical refuses.** `test_R332_worktree_less_over_ceiling_resume_still_refuses`: runner with `resume_session_id`, 640_224 tokens, `usage_known`, and `expected_worktree==""` raises `RunnerError(OVER_CEILING_RESUME_FORBIDDEN)` before Popen; the unknown-telemetry variant (`..._unknown_telemetry_still_fails_closed`) is also present. Reproduced.

**(4) Suite = 64 passed** (`pytest tools/test_agent_supervisor_launch_seam.py -q`). All 45 originals present by name (independent set-diff: 0 missing); +19 new = 64. No prior test weakened/renamed/deleted.

**(5) R337 seven properties, individually asserted:** checkpoint-lineage (`_last_checkpoint_id` + `_last_checkpoint` identity), task-identity (`config.task_id`/`authority.task_id`/`run_id`), budgets (`touches.report()` unchanged AND durable-state diff is exactly `{provider_session, rotation_pending, rotation_reason}` — proving no budget key moved), audit (chain verifies before+after, `before` a subset of `after`, len +1, last event = `over_ceiling_session_shed`), branch + worktree (`authority.branch`/`authority.worktree`), exactly-once (first True/second False, exactly one recorded shed, shed id `798d2f00` captured-not-lost, new session distinct). Genuinely per-property.

**(6) OSError limitation asserts TRUE behavior.** `test_raw_popen_failure_records_no_child` (class `MatrixR342ProviderFailureAtLaunch`, `_runner` uses `executable = tmp/missing.exe`, journal-wired): a missing exe makes `Popen` raise `FileNotFoundError` (OSError) *after* the seam passes and *before* any child record, so `account_for_children == 0`. This is real behavior, honestly documented; the follow-up framing is sound — exe existence is preflight's responsibility (guarded upstream in production), while the seam owns only the ceiling+cwd guards, so not converting a missing-exe OSError into a typed RunnerError is a correct boundary, not a gap.

**New findings introduced by the delta:** none. Behavior-only strengthening (ceiling now unconditional) plus test additions; no production behavior narrowed, no prior coverage lost.

DELTA VERDICT: PASS
