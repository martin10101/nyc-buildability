# M5-T115 — G4 test review (qa-engineer "qa-t115", read-only)

> Transmission history: pinned at ba48acad (material 75c03f25; CI run 36102902944 green at bfefd934, byte-identical
> sources), delivered as 3 SendMessage parts ending with END-OF-REPORT; no truncation. Joined verbatim by the
> orchestrator (transport wrapper tags removed only). Verdict PASS; all nine exit-gate scenarios proven
> green in CI; three ADVISORY items (aria-invalid positive state unasserted, a near-tautological regex, report wording).

---

M5-T115 G4 (independent test/regression review) — PART 1 of 3

PIN: HEAD at start = ba48acad (matches expected). Material 75c03f25 confirmed an ANCESTOR of the pushed head bfefd934 (the M5-T115 submit commit), so CI run 36102902944 tested exactly this material. bbl.ts four messages verified against the test literals (hardcoded in the test, NOT imported — non-tautological).

CI PER-TEST EVIDENCE (gh run 36102902944 --job 107969060540 web-e2e, GREEN):
- vitest: 55 files / 1468 tests passed, 0 failed.
- Playwright: 124 passed, 0 failed. New tests printed individually (ran, not skipped):
  • confirm-journey.spec.ts:142 "D-086 P2 (AS-2) ... entered vs matched vs BBL identities distinct" ✓
  • responsive-a11y.spec.ts:350 "D-086 P2 (DB-083 f) ... env badge + review line visible at phone-360 / tablet-768 / desktop-1280" ✓ (the ≤700px guard executed green).
All 18 CI jobs green; annotations are only Node-20 deprecation + MapLibre vendor-file lint noise, not failures.

EXIT-GATE SCENARIO MAP — every scenario has a cited test that EXISTS, ASSERTS the property, and ran green:
1. Keyboard+touch — autocomplete.test.tsx:22 (ArrowDown+Enter pick); address-confirm.test.tsx:639 (S9 click pick); a11y-announcements.spec.ts:180 (S6 keyboard-only journey). ✓
2. No auto-pick — autocomplete.test.tsx:125,143 ("never auto-accepts"/"Enter no highlight"); address-resolution.test.tsx:369 (S3: no radio checked, use-suggestion disabled). ✓
3. Manual fallback keeps typed text — address-resolution.test.tsx:962 (S8: street.value preserved verbatim, stale house/borough/zip cleared); autocomplete.test.tsx:114 (onFallback carries typed text). ✓
4. Every error/retry category — address-resolution.test.tsx:438 (S4) + :529 (S5 eight-state ERROR_MATRIX, distinct body copy + retry posture per state); autocomplete.test.tsx:51 (6 reasons, own copy each). ✓
5. Delayed PLUTO w/o CTA shift — address-confirm.test.tsx:966 (S11 rider a: childIds invariant, CTA + everything above byte-identical across the delayed record insert) + responsive-a11y.spec.ts:212 (DB-035 pixel CLS, document-top EXACT equality at 3 widths). Both strong. ✓
6. Invalid/missing BBL (four errors) — entry.test.tsx new four-error test (hardcoded literals per bbl.ts empty/non_numeric/wrong_length/invalid_borough). ✓
7. No WebGL — lot-outline-map.test.tsx:431. ✓
8. Multiple/invalid parcel — lot-outline-map.test.tsx:386 (review) + :398 (invalid). ✓
9. Absent source — lot-outline-map.test.tsx:409 (route_absent). ✓
Continued in part 2.

---

M5-T115 G4 — PART 2 of 3 (mutant analysis + findings)

NEW TESTS — do they fail for the right reason?
- MUTANT "hide env line at ≤700px": responsive-a11y.spec.ts:350 loops VIEWPORTS incl. phone-360 (360px, ≤700) and asserts toBeVisible() + toContainText("nothing here is a legal determination") + search-review visible. In a real browser a `display:none` @media(max-width:700px) mutant REDDENS at phone-360. Confirmed executed green in CI. GOOD. (The jsdom unit test entry.test env-badge would NOT catch a CSS hide — but the e2e is the right guard and it ran.)
- MUTANT "render identity label late": the "City-matched address" label is a SYNCHRONOUS conditional on first paint (AddressConfirmCard.tsx:195-199), not async. New label test (address-confirm.test.tsx:529) reddens if the label is dropped, moved, or its literal drifts (hardcoded "City-matched address" + compareDocumentPosition). AS-3's actual delayed element (record-address) is guarded by S11 childIds + DB-035 pixel. A late-label mutant isn't naturally constructible from synchronous JSX; adequately covered.
- MUTANT "drop aria-invalid": GAP. entry.test four-error test asserts aria-invalid ONLY as `not.toHaveAttribute("aria-invalid")` AFTER the valid input; it never asserts the POSITIVE errored state. Dropping `aria-invalid={error ? true : undefined}` entirely (always absent) leaves that assertion green → the mutant SURVIVES. The test's own comment ("set only while an error is shown") overclaims what it proves.

TAUTOLOGY HUNT:
- Label literal, env-badge literals, and the four bbl messages are all HARDCODED in the tests (not imported) → they redden on source drift. Correct, non-tautological.
- entry.test asserts the env note does NOT contain the §29 sentence ("This platform provides preliminary development and zoning feasibility") — a real negative guard that §29 was not retyped here. Good.
- WEAK: confirm-journey.spec.ts:142 asserts data-record-address-status against /loading|equal|absent|route-absent|error|shown/ — that regex matches ALL SIX enum values, so it only fails if the attribute is absent/garbage (near-tautological). Acceptable because the real AS-3 proof is DB-035 pixel + S11 childIds, and the test's load-bearing assertions (matched label, entered-input, resolved-bbl, Continue) are strong.

FINDINGS:
- ADVISORY-1 (test adequacy): aria-invalid positive state unasserted (entry.test four-error test). A mutant dropping the errored aria-invalid ships green. Not blocking: aria-invalid is an additive a11y attribute, NOT a packet exit-gate scenario, and the required "four distinct BBL errors" behaviour IS proven. Recommend adding `expect(input).toHaveAttribute("aria-invalid","true")` inside the error loop so the report §3 claim is actually guarded.
- ADVISORY-2: confirm-journey data-record-address-status regex is near-tautological (see above).
- ADVISORY-3 (report accuracy only): report §1/§2 mention "map CSS polish", but the architect.css diff contains NO map selectors (only search/confirm/env/bbl-error/label). Harmless — less change than claimed, no map regression risk.
Continued in part 3.

---

M5-T115 G4 — PART 3 of 3 (E2E stability, regression, verdict)

E2E STABILITY:
- No fixed sleeps in the NEW specs: confirm-journey AS-2 and responsive-a11y env-badge use only web-first assertions (toBeVisible/toContainText/toHaveText/toHaveAttribute) with a bounded 15s toBeVisible on the confirm card. No page.waitForTimeout in either new test. (The pre-existing DB-035 CLS test uses waitForTimeout only inside a layout-quiescence .poll() — belt-and-braces, not a flaky gate; unchanged by this task.)
- No activeElement.textContent focus matching in the new tests. The keyboard journey uses the shared tabUntil helper (its <body> textContent false-match was fixed earlier in helpers.ts).
- Stable selectors throughout: getByTestId (search-environment, search-review, confirm-matched-label, confirm-address, entered-input, resolved-bbl, confirm-continue, not-my-property) and getByRole heading — no positional/text-fragile locators.
- Resolver emits only 'resolved': ACCEPTABLE. The producer honestly discloses this (report §6, DISC-P2-2). warnings-above-Continue is unit-covered — address-confirm.test.tsx:217 (S1: compareDocumentPosition warnings→Continue + verbatim grc messages) and address-resolution.test.tsx:340 (S2). A resolved_with_warnings journey isn't elicitable from the synthetic harness, so unit coverage is the correct home.

REGRESSION:
- All test-file changes are purely ADDITIVE (git diff: +39 address-confirm, +77 entry, +55 confirm-journey, +27 responsive-a11y; no existing test modified or deleted).
- The 6 source deletions are attribute-augmenting line replacements in ArchitectEntry.tsx (added architect-search-bbl class, aria-invalid, data-testid, className-on-error) and preserve the existing id="architect-bbl-error", role="status", and aria-describedby contract, plus AddressConfirmCard's additive label. No user-visible string/testid/role/DOM-order removed.
- architect.css only APPENDS new search/confirm-scoped selectors (no shared selector edited; @media(max-width:700px) changes padding/flex-basis only, never display:none — consistent with the "visible at every width" claim).
- Whole web suite green at the material: 55 files/1468 vitest passed, 124 Playwright passed, all 18 CI jobs green.

CONCLUSION: Every packet exit-gate scenario is proven by a real, property-asserting test that ran green in CI at the exact material. Meaning-preservation holds (§29 stays in the footer via REQUIRED_DISCLAIMER; no permitted/approved/maximum wording). The three findings are all ADVISORY (no false behaviour ships, no meaning lost, no required scenario unproven, no regression). aria-invalid ADVISORY-1 is the one worth applying as a follow-up but does not block acceptance.

M5-T115 G4 VERDICT: PASS

END-OF-REPORT
