<!-- Reviewer return preserved VERBATIM by the orchestrator (report-preservation rule,
.claude/rules/project-control.md; transport entity-decoding applied: &amp; -> &).
Reviewer: independent code-reviewer subagent (G1), returned 2026-09-12, reviewed HEAD f5a30e51. -->

VERDICT: PASS

# G1 Code Review Gate Report — M5-T014

- **Task:** M5-T014 — Repair the compare-screen empty-from assertion (exercise the mixed-date branch)
- **Reviewed HEAD:** f5a30e51ab6965cc7fd0f7926df8ddfebf21daa5 (branch candidate/D-024-mrl-option-b)
- **Producer commit:** 31d3683e ("M5-T014 producer material…") — working tree clean for both output files (matches reviewed content identity)
- **Reviewer:** independent G1 code reviewer (did not write this); read-only, ADR-005. No vitest/pytest/ruff run (thin client, no node_modules by policy).

## 1. Diagnosis chain (each link confirmed against source)

- **Fixture both-null-end:** `packages/contracts/fixtures/valid/scenario/no_scenario_conflict.json` — both competing rules (`r5-residential-far`, `r5-residential-far-alt`) carry `effective_from: "2024-12-05"`, `effective_to: null`. CONFIRMED (lines 28–47).
- **Test blanks from → both-absent:** test line 398 sets `rules[0].effective_from = ""`; the fixture already had `effective_to: null`, so before the repair rule[0] was (from="", to=null). CONFIRMED.
- **`isAbsent` collapses null and "" alike:** `NoScenarioBlock.tsx:43-45` `isAbsent` returns true for `null || ""`; `:127-128` `isAbsent(from) && isAbsent(to)` renders "no effective dates are recorded for this rule". So both-absent → collapse. CONFIRMED.
- **CI DOM diff matches:** producer-quoted received line "rule id not stated (version not stated) - no effective dates are recorded for this rule" is exactly what the collapse branch emits given rule_id="   "→"" (via `boundedText`), rule_version="", and both dates absent. Mechanism matches; primary source is gh run 34669078475 web-e2e (externally recorded, not re-run here). CONFIRMED.

## 2. Repair correctness (traced through the component)

- Change: `rules[0].effective_to = rules[1].effective_from as string;` → sibling value is fixture "2024-12-05" (never invented), inserted after the blanking. Post-repair rule[0] = (from="", to="2024-12-05").
- Trace: `isAbsent("") && isAbsent("2024-12-05")` = true && false = **false** → per-slot branch → `recorded effective dates: from not stated, to 2024-12-05`.
- All three assertions pass: `rule id not stated` (rule_id ""), `version not stated` (rule_version ""), `from not stated` (empty-string from through per-slot `stated`). The assertion is scoped to the `scenario-competing-rules` node.
- **Genuine pin preserved:** if `isAbsent` stopped covering "" (null-only), `stated("", "not stated")` would return "" → "from not stated" would not render and the assertion fails again. The test still guards empty-string-labeling, which is its stated intent (docstring :389-391). CONFIRMED meaningful.

## 3. No weakening

- `git show 31d3683e` on the test file: **+8 lines, 0 deletions** (7-line mechanism comment + 1 code line). The three original expectations (:410-412) are untouched; nothing deleted or relaxed. CONFIRMED.

## 4. Scope

- `--stat`: exactly two files — the test file (+8) and `project-control/reports/M5-T014-producer-report.md` (+50). No component/lib/e2e/`packages/**`/services change. Matches `allowed_paths`; no `forbidden_paths` touched. CONFIRMED.

## 5. Adjacent-test non-interference

- `conflictScenarioBody()` returns `structuredClone(noScenarioConflict)` (scenario-fixtures.ts:86-88), a fresh deep clone per call. The both-null-collapse test (:437 "says so plainly when a competing rule records no dates at all") and the "reports recorded dates" test (:415) each get their own body object; the mutation in the repaired test cannot perturb them. CONFIRMED.

## 6. CI-only verification posture

Acceptable under the thin-client policy (`docs/LOW_STORAGE_CLOUD_DEVELOPMENT_POLICY.md`) and this packet's S1/S4. The RED half is externally recorded on every web-e2e run since the M5-T004 acceptance (documented, reproducible from CI history — not something a reviewer can or should re-run locally). The GREEN half (S4: vitest 390/390, web-e2e green) is the CI web-e2e job on the pushed head and is **G4's executable evidence** — my source analysis confirms the change is logically sufficient to flip the assertion, but the actual green must be confirmed at G4 from orchestrator-captured CI output. This is not a BLOCKED condition: the code correctness that G1 owns is fully verifiable from source.

## Notes for the record

- Test-only change; no handwritten production source grew, so modularity boundary answers / `modularity_check` are N/A.
- Task is in-regime (`directive_refs` D-038 ALL, empty-applicable form). The requirement-to-evidence pass is the independent `directive-compliance-verifier`'s (producer ≠ verifier); this G1 report is confined to code correctness, scope, provenance, and non-weakening.

**VERDICT: PASS** — reviewed HEAD f5a30e51. Diagnosis chain, intent-preserving repair, no-weakening, scope, and adjacent-test isolation all verified against source. Recommend G4 confirm the web-e2e GREEN (S4) from captured CI output before acceptance.
