# M5-T098 — G3 geometry review (geospatial-engineer "geo-t098", independent reviewer, read-only)

> Transmission history: pinned at c4d27e1e, delivered as two SendMessage parts (1/2, 2/2) ending with END-OF-REPORT;
> no truncation. Joined verbatim by the orchestrator (transport wrapper tags removed only). The producer was
> 3d-massing-engineer. Verdict PASS; advisories (collinear-collapsed vertices are not NYC-range-checked; the wrap catches
> only GEOSException; NYC_2263_* outside proposal.py __all__) are routed to the wiring packet at the accept seam.

---

M5-T098 G3 geometry review — geospatial-engineer (reviewer, READ-ONLY). Part 1/2.

HEAD pinned c4d27e1e. Material commit 4b25c60c: git show --stat = EXACTLY the 3 allowed paths. Evidence [OBSERVED] from services/api: `ruff check .` = All checks passed; `pytest tests/scenario/test_massing_model.py -q` = 65 passed. Memory write blocked (read-only) — skipped per protocol.

NYC LOT RANGE CHECK (AS-2) — PASS:
- Bounds INCLUSIVE both sides and byte-consistent with the B0 footprint check: massing_model.py:668-669 `NYC_2263_X_MIN <= x <= NYC_2263_X_MAX and NYC_2263_Y_MIN <= y <= NYC_2263_Y_MAX` == proposal.py:255-256. Same four constants (X 900000/1100000, Y 100000/300000).
- Placement correct: massing_model.py:684-686 — `_prepare_ring` -> `_require_lot_ring_in_nyc_bounds(ring)` -> `Polygon(...)` (after prepare, before any shapely construct; _prepare_ring is pure-python).
- 4326 and UTM-metre lots [OBSERVED] refuse reason=lot_ring_out_of_nyc_bounds field=lot_ring (NOT mislabelled footprint_outside_lot); the producer mutation table reproduces the footprint_outside_lot mislabel when the check is removed. A 2e6-wide lot (vertices <1e8 magnitude but > NYC X-max) also [OBSERVED] refused lot_ring_out_of_nyc_bounds — the NYC check covers the (1.1e6, 1e8] band the magnitude bound misses.
- City-edge lots inside the generous bounds (pyproj unavailable on this thin client — reasoned from established EPSG:2263 extents): SI Tottenville ~X915k/Y121k, Bronx city line ~Y271-273k, Far Rockaway ~X1067k — all inside [900k,1100k]x[100k,300k] with ~15-30k ft margin. These are the accepted B0 constants (stable since M5-T048; M5-T095 (a2dcc60f) changed proposal.py's simplicity-check internals + docstring only — the four NYC_2263_* assignment lines are byte-identical at HEAD, verified).

DEVIATION 1 — PASS (minimal, honest): git diff 64fce622..HEAD removed EXACTLY 3 lines from test_t088_as4_coordinate_magnitude_bound_is_inclusive (`build ±1e8 lot` / `area == 4e16` / `to_json()`). That assertion WAS the latent wrong-CRS-lot-accepted bug this task fixes. Replaced with a direct `_prepare_ring` at-1e8 inclusivity proof (CRS-agnostic, where the magnitude bound lives). No magnitude coverage lost; the edge+0.5 (>1e8 -> coordinate_out_of_range/lot_ring[1]) and the 1e9 f[0] refusals are unchanged. Both goldens (b7fa9862, e23b5cbc) byte-identical — no `-` line touches either; the one `+` b7fa9862 line is the new re-assertion test. Part 2 follows.

---

M5-T098 G3 geometry review — Part 2/2.

GEOS WRAP (AS-3) — PASS (with ADVISORY-2): `@_wrap_geos_errors` on build_massing_model (:722); build_from_generated_option (:864) delegates -> covered transitively. Both tested. Typed refusals pass through untouched: MassingModelError is a ValueError, not a GEOSException (MRO disjoint below Exception), so `except GEOSException` never catches it — tested (typed_refusal_passes_through, footprint_outside_lot survives).

SCAN-CHARGE ORDERING (AS-4) — PASS: massing_model.py:364 `budget.charge(m-3, field)` is BEFORE the inner `any(_point_in_triangle ...)` scan at :365-369. Mutant ME (charge after) reddens — the test counts pit calls 0 (before) vs n-3 (after), a clean discriminator.

Y-AXIS MAGNITUDE BOUND (AS-1) — PASS: :263 `abs(x) > MAX_COORD_ABS or abs(y) > MAX_COORD_ABS` checks x OR y; mutant MD (drop the y clause) reddens the y-only probe; inclusive at exactly 1e8. Correctly isolated on _prepare_ring directly (the lot path's NYC gate / B0's range check would otherwise mask MD by refusing a small-x ring for a different reason).

SCOPE (AS-5) — PASS: material commit touches exactly 3 allowed paths. Zero new deps (AST import-root test: roots ⊆ stdlib+numpy+shapely; functools is stdlib; GEOSException/NYC_2263_* from admitted shapely + the B0 proposal module). Unwired — no production importer of massing_model (grep clean); new reason string only in module+test; no fastapi/route/app.main/app.api/app.cad/http import (asserted). Modularity: 894 SLOC — WARN/justify band, under the 1000 hard cap, cohesion justification recorded; non-blocking.

ADVISORY-1 (geometry, non-blocking): NOT every vertex is NYC-range-checked. `_prepare_ring` collapses collinear vertices (:286-294) BEFORE `_require_lot_ring_in_nyc_bounds` iterates the collapsed ring (:667). [OBSERVED] a lot with a collinear spike at x=2e6 (out of NYC X-range, <1e8) BUILDS identically to the clean lot — the spike is silently collapsed and never NYC-checked. Benign (the 1e8 magnitude bound still guards overflow on raw vertices; the collapsed polygon is geometrically correct; the 4326/metric wrong-CRS cases the rider targets have all-out-of-range, non-collinear vertices and ARE caught) and beyond AS-2's stated scope, but an asymmetry vs B0 (which range-checks raw vertices before collapse). Recommend the user-geometry wiring packet range-check raw vertices (or check before collapse).

ADVISORY-2 (geometry engine, non-blocking): _wrap_geos_errors catches only GEOSException. In shapely 2.0.7 [OBSERVED] GEOSException subclasses ShapelyError, but siblings TopologicalError/GeometryTypeError/DimensionError/EmptyPartError/UnsupportedGEOSVersionError do NOT — they would escape untyped. None reachable on current bounded/validated input (the documented real crash — ~1e154 overflow -> empty centroid -> GEOSException — IS caught); AS-3's scope is GEOSException. The decorator/docstring phrase "no untyped error can escape" is scoped to GEOSException in practice. Recommend catching ShapelyError before wiring real user/MapPLUTO geometry.

ADVISORY-3 (very minor): NYC_2263_* are public module-level names in proposal.py but not in its __all__; explicit import is unaffected by __all__ and the values are byte-identical at HEAD — acceptable single source of truth (forbidden to change proposal.py here anyway).

M5-T098 G3 VERDICT: PASS
END-OF-REPORT
