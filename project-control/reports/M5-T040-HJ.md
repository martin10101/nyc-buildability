# M5-T040 — human-journey review (verbatim reviewer return; transport entity-decoding only)

Saved by the orchestrator from the human-journey-reviewer agent return, 2026-09-18 (seq-118
wave, pinned at submission sha b4865d1c). Nothing added or removed beyond decoding transport
HTML entities.

---

Human-journey (G3) review complete. Returning the report verbatim.

---

# G3 Human-Journey Gate Report — M5-T040

**Task:** M5-T040 — Named-street override wiring + DB-025(a-c) display gating
**Reviewed identity:** pinned submission `b4865d1c`; material commit `751a1826` (display work); CI success `4d8847c6` (run 35407153078, web+web-e2e). `git diff 751a1826..4d8847c6 -- apps/ services/` empty (bookkeeping only), so the CI conclusion speaks for the reviewed material identity.
**Reviewer:** human-journey-reviewer (read-only; judged from pinned code + tests + CI + accumulated surface knowledge; thin client — no local browser/npm).
**Scope of this gate:** the human journey and the DB-025(a-c) display contract (AS-6) plus the new named-street refusal journey. Server truth-table / hardening (AS-1..AS-5, AS-7/8) belong to the data-contract / code / security / directive verifiers.

## Verdict: PASS

No BLOCKING findings. Three ADVISORY findings, all non-blocking and routable to discovery. My prior M5-T037 findings **F3 and F4 are both CLOSED**.

## Answers to the five journey questions

**(1) F3 — do both surfaces gate uniformly on `determination_state`, not one-on-state / one-on-null-FAR? → CLOSED.**
The `determination_state` union is a closed 3-enum (`rule-evaluation-contract.ts:116-119`: within / not_within / professional_review_required). Both surfaces now key the review LABEL on state:
- `DevelopmentLimits.tsx:68` `review = wide.determination_state === "professional_review_required"`; the entire calm presentation branches on that + `within` (:69), never on null-FAR.
- `CalculationEvidence.tsx:17` `wideReview = wideDeterminationState === "professional_review_required"`; the status-line "Professional review required" now appears only when `wideReview` (:42). Pre-fix this line was UNCONDITIONAL and the withheld value keyed on null-FAR — that was exactly F3.

**(2) F4 — no "Wide-street conditional FAR" heading over the conservative value in the not-within case? → CLOSED.** In not-within, both surfaces read **"Governing floor-area ratio"**: `DevelopmentLimits.tsx:73/:74/:79` (valueLabel, headingText, and the section `aria-label` all follow), caption ":86 Outside 100 ft of a wide street; the conservative floor-area ratio governs"; `CalculationEvidence.tsx:44` heading + value caption both "Governing floor-area ratio". Honest, 'Governing floor-area ratio' class exactly. (The professional-review case correctly KEEPS the "Wide-street conditional FAR" heading over the *withheld* value — that value is not conservative, it is withheld pending review.)

**(3) DB-025(a) — "Professional review required" only when state says so; what does a confident within show? → Satisfied for the wide-street label.** On the calm answer-first surface (`DevelopmentLimits`) a confident within shows the FAR value (:84), a "Draft" badge, ":86 Applies within 100 ft of a wide street", and ":93 Draft — pending qualified legal review (not verified)" — NO "Professional review required". DRAFT-pending-legal-review framing intact; no verified claim anywhere (confidence is never mapped to a badge). See ADVISORY 2 for the one residual place a within still reads "Professional review required".

**(4) New named-street journey (Broadway W94-97 / Allen St) — calm and non-jargon, or does raw provision/section text leak? → No leak on the calm panel.** MATCHED_OVERRIDE and named_pending both route to a professional-review refusal (`wide_street_wiring.py`). The calm panel renders a FIXED plain-language refusal (`DevelopmentLimits.tsx:82` "Professional review required — the higher wide-street floor-area ratio is withheld until a qualified reviewer confirms the determination.") plus the draft note and a "Wide-street sources and provenance →" link. It renders NONE of the raw server `reason`/`draft_label`/`fallback_direction_note` — the [ORCH-CORRECTED per M5-T037 HJ F2] comment documents this and the M5-T037 F2 lesson holds. The named-street specifics (C5-3/C6-4/C6-6, CD7, "exceptions_checked", verbatim override row) surface ONLY on `CalculationEvidence` — the evidence disclosure, where raw strings belong — via `reason` and the "Named-street override pending" dl row. Grep confirms only these two components render `wide_street` at all.

**(5) Report/screen parity (ReportView unchanged; parity tests changed to hidden-heading queries) → Values and captions identical.** ReportView embeds the SAME components fed the SAME `evaluation` prop (`ReportView.tsx:72` DevelopmentLimits; `:96` CalculationEvidence inside a closed `<details id=brief-calculations>`). Parity is structural — no second data path. The test change adds `{ hidden: true }` to heading queries only because the closed `<details>` places the evidence heading in the a11y tree as hidden; value captions asserted via `textContent` are unaffected, and the DB-025c test proves screen value === report value (2.20) from one `validateRuleEvaluationDocument(doc).ok`-checked document. Test adaptation, not defect-masking.

## Findings

**ADVISORY 1 — `CalculationEvidence.tsx:44` null-FAR value fallback reintroduces a null-FAR-keyed "professional review required" phrase.** The value expression is `wideReview ? "withheld …" : (governing_max_residential_far ?? "withheld — professional review required")`. In a non-review state with null FAR this prints "withheld — professional review required" while `DevelopmentLimits.tsx:84` prints "Not calculated" — a wording mismatch AND a "professional review required" phrase on a non-review state. This is UNREACHABLE via the real server: `integration.py:525-528` + `select_far_row_value` make `governing_max_residential_far` null IFF state==professional_review_required. Defensive-only. Recommend aligning the fallback to "Not calculated" to fully honor "gate on determination_state, never null-FAR."

**ADVISORY 2 — `CalculationEvidence.tsx:30` top-level `Draft · Professional review required` is unconditional.** A confident within/not-within still reads "Professional review required" at the TOP of the "How this was calculated" surface. This is a pre-existing whole-evaluation banner (unchanged in this diff; honest under D-045-R009 pre-G6 posture — nothing is Verified until G6), and the producer scoped DB-025(a) to the wide-street subsection (the test deliberately scopes AROUND this banner: "so the panel-level draft banner is not read"). Not blocking because it is pre-existing, out of the wide-street scope, and honest at the product level. Flag for the owner to confirm DB-025(a)'s intent does not extend to the whole-evaluation banner.

**ADVISORY 3 — modularity signal (server-side, self-disclosed).** The producer report/commit note that `wide_street_wiring.py` is now above the modularity warning threshold ("cohesion signal flagged for review"). Outside my HJ lens; owned by the code-reviewer / modularity check. Route to discovery.

## Prior findings status
- **M5-T037 F3 — CLOSED** (both surfaces gate the review label on `determination_state`).
- **M5-T037 F4 — CLOSED** (not-within reads "Governing floor-area ratio" on both surfaces; conditional heading never sits over the conservative value).

## Evidence relied upon
- Code at material `751a1826`: `apps/web/src/components/architect/DevelopmentLimits.tsx`, `.../CalculationEvidence.tsx`, `.../ReportView.tsx`, `.../__tests__/development-limits.test.tsx`, `.../__tests__/report-view.test.tsx`; `apps/web/src/lib/rule-evaluation-contract.ts`; `services/api/app/rules/wide_street_wiring.py`, `services/api/app/rules/integration.py`.
- CI: `project-control/reports/M5-T040-ci-evidence.md` (run 35407153078 success @ 4d8847c6; diff to material empty).
- Grep: only `DevelopmentLimits.tsx` + `CalculationEvidence.tsx` render the `wide_street` block.

**Verdict: PASS** (3 ADVISORY findings above; none blocking).
