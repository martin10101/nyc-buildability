# M5-T058 HJ — independent human-journey review (verbatim reviewer return)

Reviewer: human-journey-reviewer (opus-4-8 xhigh per D-064), dispatched by the orchestrator at
the bc703fcc seam, pinned to material 7d02eddc at reviewed sha 817a6546. Recorded by the
orchestrator; reviewer is read-only and records no gate itself.

---

Verdict: **PASS** (with 5 non-blocking advisories routed as discoveries)

# G3 Human-Journey Gate Report — M5-T058 (D-078 substrate-substitution stamp; rule_evaluation contract 1.2.0)

## Identity reviewed
Frozen material 7d02eddc (19 files) at reviewed sha 817a6546 (+ the [ORCH-CORRECTED] frozen-contract-guard fix). **Reviewed at current HEAD bc703fcc.** Confirmed via read-only plumbing that all 19 material paths are byte-identical between 817a6546 and HEAD (`git diff --stat` empty), and that the two schema copies (`packages/contracts/schemas/v1/…` and `services/api/app/_contract_schemas/v1/…`) are byte-identical (`git show` diff empty). Thin client: web behavior judged from components + specs + fixtures (executable proof); CI at the reviewed head reported green by the orchestrator.

## Journey walked (as a skeptical analyst)

**J1 — Condo billing BBL that resolves to a single base lot (the headline, AS-1/AS-4/AS-5).**
`live_provider.build_live_substrate_resolved` (live_provider.py:336-414) resolves the billing BBL, and on `substitutes_base_lot` rides the CondoResolution across the seam and builds the stamp (`build_substrate_substitution_stamp`, :309-333). Route wires it in (rule_evaluation.py:343-354, 404-409); `evaluate_property(substrate_substitution=…)` carries it onto the 1.2.0 document (integration.py:648-651, 723/905). `evaluated_input.bbl` stays the entered billing BBL (fixtures.ts:60; schema desc line 569). On screen (ArchitectEntry.tsx:168-172) and brief (ReportView.tsx:84-85), `AnalysisIdentityNotice` renders ABOVE DevelopmentLimits. Because `evaluated_input.bbl === profile.identity.bbl`, `identityEvaluation` is non-null (ArchitectEntry.tsx:72) so the computed FAR on the base-lot substrate is shown, with the substitution record above it. Notice copy (AnalysisIdentityNotice.tsx:65-67): "*{label} analyzed on the base lot … You entered BBL X; this … was analyzed on the recorded base tax lot BBL Y. The billing lot and the base lot are recorded as entered versus analyzed — a record of the city's documented resolution, not a computed allowance.*"

**J2 — Condo billing BBL that is multi-lot / unresolved / typed-error (AS-2, honest refusal).**
`build_live_substrate_resolved` fail-safes to an absent substrate carrying `CONDO_BASE_LOT_UNRESOLVED_CAUSE` and NO stamp (live_provider.py:359-368); the route sets `spatial_absent_condo_unresolved=True` (rule_evaluation.py:352-354); `evaluate_property` emits `fail_safe_reason=condo_base_lot_unresolved` with honest prose naming the WHY and the fail-safe (integration.py:674-682). Web label "Condo base lot needs site confirmation" (development-limits.ts:162-164) renders on the DevelopmentLimits status line (:113) with values "Not calculated" (dev-limits test :538-548). No base lot is auto-selected on any path (D-078-R002 — verified in source, not just tests).

**J3 — Non-condo genuinely-absent substrate (AS-2 companion).** Cause stays `None` on the `no_candidate_districts` / `district_page_partial` / `connector_error` paths (live_provider.py:394-420), so `spatial_intersection_absent` and its generic label "Zoning boundary check unavailable" survive (dev-limits test :550-558). The condo cause does not leak into non-condo refusals.

**J4 — Guard direction (safety).** `stampLegitimate` (AnalysisIdentityNotice.tsx:54-61) requires entered_bbl == requestedBbl == evaluated_input.bbl AND analyzed_bbl a non-empty *different* string. Any non-correspondence falls through to the UNCHANGED withhold `role="alert"` branch. The spec binds both directions plus three non-corresponding-stamp variants, an unstamped mismatch, and a missing-BBL case (analysis-identity-substitution.test.tsx:48-161). The guard is closed by ADDING a branch, never by relaxing the unstamped path.

## Findings

- **HJ-1 (ADVISORY, clarity/honesty).** The `mixed_substrate` visibility (lot area/geometry describe the base lot; PLUTO identity facts describe the billing lot) is honest but **machine-readable only** — it lives in the CapturedRecord disclosure and the schema. The prominent notice copy does not surface the split, and DevelopmentLimits shows a PLUTO reference FAR for the **billing** lot (development-limits.ts residentialReference on `profile.identity.bbl`) directly beside an evaluated FAR computed on the **base** lot substrate, with no on-screen caveat that they describe different lots. Recon Q5 deliberately made `mixed_substrate` machine-readable, so this is not a spec miss — route as a product discovery for human surfacing.

- **HJ-2 (ADVISORY, recovery).** The refusal names a next step ("needs site confirmation" / API: "a site-definition confirmation is required") that has **no live control today**: the D-078 site-definition confirmation flow is the paired packet (M5-T059), which is dormant in prod (unmounted route + empty in-memory store). Honest, fail-safe, and an explicit scoped deferral (D-078-R002/R003), but a partial dead-end for the analyst. The informational path forward (the condo-records section lists the base lots; the analyst can inspect evidence or manually enter a base-lot BBL) exists but is not offered from the refusal surface.

- **HJ-3 (ADVISORY, accessibility).** The screen-reader OutcomeAnnouncer copy is less specific than the visible label: `announcementForRuleEvaluation` (lib/rule-evaluation.ts — **outside this packet's allowed_paths**) classifies `condo_base_lot_unresolved` as generic "missing_evidence" ("professional review required; the evidence needed … is missing") and the substitution as generic "applicable_draft"; neither mentions condo/substitution. The visible DevelopmentLimits label and the AnalysisIdentityNotice text carry the specifics, so nothing is hidden — but the announced text trails the visible text. Out-of-scope path; discovery-route.

- **HJ-4 (ADVISORY, consistency).** Two base-lot surfaces render for a single-base-lot condo: AnalysisIdentityNotice "analyzed on the base lot" (from the rule_evaluation stamp) and CondoRecordsChannelSection "Recorded base lot for this condo" (from the condo_records channel), via separate endpoints over the same resolver seam. Redundant. In ReportView the channel withhold is monotonic (it nulls `evaluation`, ReportView.tsx:46-49) while AnalysisIdentityNotice reads the raw `returnedEvaluation`, so a theoretical channel/stamp divergence stays fail-safe (numbers withheld, honest record shown). Consistent-domain wording.

- **HJ-5 (ADVISORY, jargon).** The notice introduces "billing lot" / "base tax lot" without first telling the analyst that their entered BBL is a condo billing lot. Acceptable for the professional architect audience and consistent with the accepted condo-records vocabulary ("Condo billing lot" / "Recorded base lot", M5-T052/T056).

No required corrections. No blocking defects. No violation of D-078-R002 (no auto-pick), the accepted withhold safety behavior, or evaluated_input.bbl identity.

## Explicit answers

**(1) Clarity.** Yes. The "analyzed on the base lot" record reads plainly for the professional audience and states two distinct BBLs labelled *entered* vs *analyzed* unambiguously (AnalysisIdentityNotice.tsx:66), and it disclaims itself as "a record … not a computed allowance." Residual: the mixed-substrate split is not surfaced in human copy (HJ-1) and "billing lot" is used without inline definition (HJ-5).

**(2) Correctness / honesty.** The stamped branch never softens the withhold — it is gated on strict correspondence and the unchanged unstamped/absent branches still withhold with `role="alert"` (verified in source lines 54-79 and five spec cases). `evaluated_input.bbl` remains the entered billing BBL (fixture + schema + response.py contract). The `condo_base_lot_unresolved` refusal is honest about WHY (multiple base lots / no match / upstream error) and about the fail-safe (integration.py:676-682). No copy implies the system chose the lot — schema ("NEVER an auto-selected base lot"), API prose ("no base lot is auto-selected"), and the notice ("the city's documented resolution") all disclaim selection.

**(3) Recovery.** The refusal is a correct fail-safe, not a crash, and the analyst retains evidence inspection and the condo-records base-lot list. But the named next step (site confirmation) has no live control yet (HJ-2) — a scoped, honest partial dead-end pending the paired packet.

**(4) Accessibility.** Correct: the new record branch is a non-alarming `<section class="architect-note">` with no `role` and `data-identity-state="substituted"`; the withhold keeps `role="alert"`. The bold label is `<strong>` (not a heading), consistent with the existing alert branch's pattern — the M5-T056 heading upgrade applied to the condo-records SECTION, not these inline notices. No nameless landmark region. One advisory: the announcement copy trails the visible label (HJ-3).

**(5) Consistency.** Consistent with the accepted condo-records view (M5-T052/T056) and with prior identity notices (m5t046-address-identity-honesty): entered-vs-analyzed neutrality, records-not-allowances framing, monotonic withhold, same resolver seam. The only friction is the two parallel base-lot surfaces (HJ-4), which remains fail-safe.

Relevant files: `apps/web/src/components/architect/AnalysisIdentityNotice.tsx`, `apps/web/src/components/architect/__tests__/analysis-identity-substitution.test.tsx`, `apps/web/src/lib/architect/development-limits.ts`, `apps/web/src/lib/rule-evaluation-contract.ts`, `apps/web/src/test-support/rule-evaluation-fixtures.ts`, `services/api/app/spatial/live_provider.py`, `services/api/app/rules/integration.py`, `services/api/app/api/v1/rule_evaluation.py`, `services/api/app/_contract_schemas/v1/rule_evaluation.schema.json`.
