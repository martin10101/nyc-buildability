# M5-T100 — G5 security review (security-reviewer "sec-t100", read-only)

> Transmission history: pinned at 83f86ce0, delivered as two SendMessage parts (1/2, 2/2) ending with END-OF-REPORT;
> no truncation. Joined verbatim by the orchestrator (transport wrapper tags removed only). Verdict PASS: str(exc) never
> logged; every tested control/line-separator vector repr-escaped; truncation after sanitisation; no exc_info. The one
> advisory (the caller-supplied correlation id is logged raw, pre-existing and unreachable while unwired) stays with the
> DB-058 (d) wiring packet.

---

M5-T100 G5 security review (read-only, sec agent) — part 1/2.

PIN: HEAD at start 83f86ce03607921b51f4bc1836088726239fc6cb. Material commit 2624adec. Method: source read + `git show 2624adec` + in-memory probe from services/api (no network, no writes).

SCOPE VERIFIED: diff touches exactly the 3 allowed paths. Connector change = 1 import (`sanitize_retry_after`, from the already-imported app.resilience.transport), 1 helper `_sanitized_bounded` + 2 constants (building_footprints_arcgis.py:410-420), and the catch-all except expansion (bf:1083-1107). No forbidden path touched (app/resilience/, fixtures/, mappluto, routes, main, requirements all clean). Zero new dependencies. Connector still UNWIRED (grep: no app/ module imports it; only self + tests). Refusal shape byte-identical.

FINDINGS:

No CRITICAL / HIGH / MEDIUM.

[ADVISORY-LOW, NON-BLOCKING] Caller-supplied correlation_id logged RAW. bf:1098 logs `correlation_id=%s` with `cid` (cid = correlation_id or uuid4, bf:1059). Unlike the class name and the fixed message, `cid` is NOT routed through `_sanitized_bounded`. Repro (OBSERVED, in-memory): cid="cid\nFORGED" -> the rendered line contains a real newline = a second forged log line. Assessment: (1) pre-existing and consistent with the shared transport, which already logs cid raw (transport.py:459-496) — this packet adds a second site with the same treatment, not a new vector class; (2) not reachable in production: connector is unwired, default cid is uuid4, only test callers exist and all pass safe ids; (3) already flagged by M5-T089 G5 A4 (INFO) and carried in this packet's DISCOVERIES as a wiring concern for DB-058(d), which is NOT in this packet's scope (e/h/l/m). Remediation (for the DB-058(d) wiring packet, not in-packet): generate cid server-side, or route cid through `_sanitized_bounded` at BOTH log sites (connector + transport).

(part 2/2 follows: leak/forge/bound/exc_info verifications + verdict)

---

M5-T100 G5 security review — part 2/2.

POSITIVE VERIFICATIONS (OBSERVED via source + in-memory probe):

1. No upstream-text / secret leak. `str(exc)` is NEVER rendered (bf:1095-1098 logs only `type(exc).__name__`, a FIXED constant, and cid). The exception MESSAGE — the one place upstream body text or a secret travels into a genuine bug (e.g. ValueError echoing an upstream literal) — never reaches the log. Test `test_t100_internal_error_is_findable_and_logs_no_upstream_text` proves injected "AdminSecret123"/UPSTREAM_BODY/FORGED/newline all absent. Load-bearing reason confirmed by probe: `sanitize_retry_after("AdminSecret123")` returns it VERBATIM (allowlist ^[A-Za-z0-9,: +\-]{1,64}$), so sanitizing str(exc) would NOT stop a short-secret leak — excluding str(exc) entirely is the correct choice, not a gap.

2. Log-injection / forging on the class-name field. `_sanitized_bounded` -> `sanitize_retry_after` (allowlist else repr) neutralized every vector I tested: CR, LF, CRLF, U+2028, U+2029, NEL (U+0085), VT, FF, ANSI ESC (\x1b), NUL, BS — each repr-escaped, none survived as a raw control char. The allowlist branch admits no control/line-break char. repr also escapes any non-ASCII class name.

3. The bound. Truncation is applied AFTER sanitization (bf:419-420: `safe = sanitize_retry_after(text)` then slice). Slicing only removes chars, never un-escapes one; probe on "\n"*300 -> no raw newline, ends with "...(truncated)", len 214 <= 200+marker. No repr escape is cut into a real control char.

4. exc_info / chaining. `logger.error(...)` is called WITHOUT exc_info and is not `logger.exception()`; no __cause__/__context__/traceback is rendered. Grep: no app logging config (basicConfig/dictConfig/addFilter/setFormatter) that could attach exc_info to records.

5. Refusal + noise. Refusal byte-identical: error_type="internal_error", message unchanged, detail={"exception": type(exc).__name__} — the class-name-only refusal M5-T089 G5 named the safe choice. Only genuine (non-typed) bugs are logged at ERROR; typed refusals are not re-logged, so findability is targeted, not noisy.

6. Findability vs leakage. class + fixed message + cid, matching the refusal's cid + class detail, is adequate to locate a bug at ERROR level without a traceback — a deliberate, documented non-disclosure tradeoff.

Tests are offline (injected transports; no network). ruff/pytest are the orchestrator/CI's to record.

M5-T100 G5 VERDICT: PASS

END-OF-REPORT
