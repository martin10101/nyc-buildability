# M5-T052 G3 — independent code review (code-reviewer, saved VERBATIM from the agent returns)

M5-T052 G3 (code) — VERDICT: PASS, with two BLOCKING-for-acceptance report corrections (code is correct; the defects are false evidentiary claims in the producer report/source-sections, not behavioral). Reviewed read-only at HEAD 6e3e1a40. Findings below.

BLOCKING (report accuracy, not code) — must correct the producer report before accept:

1. "condoWithholdsAllowances / deriveCondoDisplay byte-unchanged" is FALSE. Diff vs M5-T045 (28082288): the guard was REFACTORED. Old body (28082288) = `if (condoHasMultiLot(profile)) return true; return condoResolutionNotes(profile).some(n => n.outcome !== OK)`. HEAD body (PropertyOverview.tsx:165-167) = `return deriveCondoDisplay(profile).withholdsAllowances`, and `deriveCondoDisplay` (PropertyOverview.tsx:143-155) is a NEW function (0 occurrences in M5-T045 — it cannot be "unchanged"). The refactor is provably SEMANTICS-preserving: `.some(pred)` ≡ `.filter(pred).length>0`, same `CONDO_RESOLUTION_FIELD`, same `CONDO_ALLOWANCE_OK_OUTCOME`, and the derivation stays profile-only/channel-independent — so the packet's binding requirement ("fail-safe allow-list guard semantics UNCHANGED") IS met and behavior is correct. But producer-report §1 ("the profile guard condoWithholdsAllowances is byte-unchanged"), §2, and §5 over-claim "byte-unchanged"; correct to "semantics-preserving refactor (extracted deriveCondoDisplay)."

2. "PropertyOverview.tsx (which SHRANK — a component removed)" (producer-report §6 + source-sections) is FALSE. `git show 365f492f --numstat` = +165/−56 on PropertyOverview.tsx; base 210 → HEAD 319 lines (+109, ~+52%). It GREW (deriveCondoSurface + CondoRecordsChannelSection + new types/comments outweigh the removed CondoResolutionRecords). The modularity CONCLUSION is nonetheless correct — modularity_check exit 0, failures 0, 319 SLOC well below the 600 warn tier, condo_records.py (624 raw but comment-heavy) also absent from the 20 warnings — so no modularity violation; only the "SHRANK" claim must be corrected.

ADVISORY:

3. Route-test count inconsistency: producer-report §6 says "14 passed" for test_condo_records_api.py; source-sections/ci-evidence say "15 route tests." Harmonize (cosmetic).

PASS basis (the load-bearing ruling is genuinely implemented; verified in source, not just report):
- SINGLE SOURCE OF TRUTH: `deriveCondoSurface(profile, outcome)` (PropertyOverview.tsx:197-214) is the only condo decision. Both the screen (PropertyOverview.tsx:283, section mounted :311) and the printed brief (ReportView.tsx:45-46, section :97) render the SAME `CondoRecordsChannelSection` from it. No competing conditional gates the records/notice/fail-safe (grepped — the only `withholdAllowances` consumers are PropertyOverview:284-286 and ReportView:47-49, both off `condo.withholdAllowances`).
- MONOTONE: `withholdAllowances = profileWithholds || channelWithholds` (:206); channel `unavailable`/route-absent never withholds (condo-records.ts:560-566, deriveCondoChannelState:534-537 maps route_absent→non_condo). Nothing un-withholds. Profile guard stays authoritative and profile-only.
- CONFLICT fail-safe AND surfaced: `conflict = profileState.present && channelHasSignal && profileWithholds !== channelWithholds` (:205); the OR guarantees withhold; surfaced inline in the records section when multi-lot (:241) and via the standalone `condo-records-conflict` notice otherwise (:253-257). showSubstitution requires `!withholdAllowances`, so it's mutually exclusive with conflict — no un-safe allow path.
- TOKEN PIN real: web literals condo-records.ts:43-47 exactly equal resolver condo_base_lot.py:75-79 (not_condo_billing/resolved_single_base_lot/multi_lot_set/unresolved/error); route emits them (condo_records.py:77-84); pinned by tests both sides (test_condo_records_api asserts OUTCOME_* ; condo-records.test / condo-resolution-display).
- ROUTE (condo_records.py): normalize_bbl before any resolver call (:561, flag-gate 404 first :554); resolver consumed read-only via injected accepted entry points (resolve_condo_billing / dtm_resolve / classify_lot); typed 422/500, typed resolver failure = NORMAL 200 `error` doc (no 5xx); unit-BBL path present (:594-602). Tests fully offline via dependency_overrides with billing/unit "landmine" cross-guards.
- CLIENT (condo-records.ts): credentials:"omit" (:354), bounded reflected strings, typed outcome union; boundedTimestamp fix applied to ALL timestamp sites (:267 retrieved_at, :268 rows_updated_at, :294 retrieved_at; datasetVersion derived from bounded rowsUpdatedAt :290) — no timestamp field still runs through boundedToken; no sanitizer-vocabulary mismatch remaining.
- CONSUMER FIX (46bf1105, report-view.test.tsx, OUTSIDE allowed_paths, own tagged commit, producer-routed per §5): genuinely binds shared-decision behavior — resolved-single stub → substitution record + wide-street-result + NO records (:417-429); MISSING/UNKNOWN token route-absent stubs → profile guard withholds, no records (:432-459); multi_lot channel → records + 2 base-lot records + withheld allowance under waitFor (:462-470); non-condo byte-identical (:473-479). Matches report §5.
- AnalysisIdentityNotice.tsx BYTE-UNCHANGED (last touched 28082288/M5-T045; not in any of the 3 commits) — verified; the substitution explanation living in the records-view substitution record instead is coherent with the actual render.
- SCOPE: 365f492f touches only the 9 allowed_paths; main.py = import (:32) + one include_router (:174); the consumer fix is its own commit outside allowed_paths (deliberate); a6f35a0f = condo-records.ts (in scope).
- Display tests strong: deriveCondoSurface disagreement (profile-withholds/channel-benign-single → conflict surfaced, no records) and channel-only-multi-lot (conflict false, records shown) cases present; `not.toMatch(/allowance/i)` grep gate on records/substitution/conflict sections (D-073-R006).

Net: the code satisfies the ruling and all AS; correct findings 1–2 in the report text (and mirror in source-sections) before acceptance.

## Delta-attestation (verbatim)

ATTESTATION — my M5-T052 G3 (code) PASS CARRIES to e9abeaaf. Verified: git diff 6e3e1a40..e9abeaaf touches ONLY report/control-plane files (producer-report.md, source-sections.md, reports/M5-T052.json, state.json, tasks/M5-T052.json) — zero .tsx/.ts/.py code or test files, so every code path is byte-identical to the head I reviewed. Both required corrections state the facts I found: F1 at all three sites (byte-unchanged → semantics-preserving refactor delegating to the NEW deriveCondoDisplay, test-proven equivalent), F2 at both sites (GREW 210→319, +165/−56, below the 600 warn tier), A3 harmonized to 15 route / 454 api. Digest row resynced (bf225ba6→fa7c0412, pre-correction digest noted). Corrections satisfied; PASS stands at e9abeaaf.
