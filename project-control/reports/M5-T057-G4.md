<!-- Saved VERBATIM from the qa-engineer agent return (transport entity-decoding only),
     orchestrator seq 122, 2026-09-20. Ruling identity: frozen submission 2b5edbc3. -->

# Gate Report

- Gate ID: **G4**
- Task ID: **M5-T057**
- Reviewer: **qa-engineer** (independent; read-only)
- Producer: **backend-engineer** (material commit `3dbd056d`; worktree branch commit `fcc58e24`)
- Result: **PASS with one non-blocking finding + four recorded coverage gaps**
- Clean environment/worktree used: **`C:\Users\MLFLL\Downloads\nyc-zoning\wt-m5t057`** (branch `task/M5-T057-proposal-check-route` @ `fcc58e248a4de1ca0bd0c40525803defec6db69c`, `git status --porcelain` empty)

## Frozen identity confirmation

`git diff 2b5edbc3 HEAD -- <the six files>` at `ctl24` HEAD (`18616ff5954395401a7ac4f6e8aa757ba8e93eee`): **EMPTY** (no output, exit 0). Not BLOCKED.

Per-file blob identity (frozen `2b5edbc3` vs review worktree `fcc58e24`), all **SAME**:

| File | blob |
|---|---|
| `services/api/app/api/v1/proposal_checks_api.py` | `e7712e20` |
| `services/api/app/main.py` | `bf2c658d` |
| `services/api/tests/api/test_proposal_checks_api.py` | `eb2d9e2e` |
| `services/api/tests/api/test_proposal_validation_api.py` | `eddd9655` |
| `services/api/tests/rules/test_proposal_checks.py` | `83135b71` |
| `project-control/reports/M5-T057-producer-report.md` | `10201966` |

Producer commit `fcc58e24` touches **exactly 6 files**, all inside `allowed_paths`; no `forbidden_paths` entry touched. Report digest for the route module independently recomputed: `sha256 = e75bec6a3256aa5332054eaec4a207717eaadb7dab8dab22a2da56d7904186c9`, 593 lines — **matches** the report's §8 claim (raw == LF-normalized; file is LF).

## Steps independently executed / Expected versus actual

| # | Command | cwd | Expected | Actual |
|---|---|---|---|---|
| 1 | `python -m ruff check .` | `wt-m5t057/services/api` | exit 0 | `All checks passed!` **RUFF_EXIT=0** |
| 2 | `python -m pytest tests/api -q` | `wt-m5t057/services/api` | 540 passed | **540 passed in 29.48s** |
| 3 | `python -m pytest tests/rules -q` | `wt-m5t057/services/api` | 726 passed | **726 passed in 28.85s** |
| 4 | `python tools/modularity_check.py --check` | `wt-m5t057` | exit 0 | `selected 456 files; failures 0; warnings 20` — **exit 0**; `proposal_checks_api.py` is **not** in the warn list |
| 5 | `python -m pytest tests/api tests/rules -q -rsxX` | `wt-m5t057/services/api` | no skip/xfail | **1266 passed**, zero `s`/`x`/`X` in the `-rsxX` summary |

All four required counts/exits reproduce exactly as the packet expects and as the producer report §4/§9 claims.

## Acceptance criteria reviewed

### AS-1 (end-to-end, exact arithmetic) — PASS, value-pinned not shape-only

`tests/api/test_proposal_checks_api.py:169-198` (attested) pins every number, not shapes:
- `body["summary"] == {"pass": 1, "fail": 1, "could_not_check": 2, "total": 4}` (:177) — an exact dict equality, so a drift in any count fails.
- coverage: `provided 0.625` / `required 0.5` / **`shortfall == approx(0.125)`** / `direction == "maximum"` (:182-186).
- height: `outcome pass`, `provided 30.0`, `required 60.0`, `shortfall is None` (:188-191).
- both non-commensurable checks: `could_not_check` + `reason == "provided_fact_not_commensurate"` + `required_value is None` (:193-197).

Unattested variant `:200-210` pins the flipped summary `{"pass": 0, "fail": 1, "could_not_check": 3, "total": 4}` and `height.could_not_check_reason == "allowance_unresolved"`.

Flag-off parity: `:155-159` (generic 404, exact body `{"detail": "Not Found"}`, **no** `X-Correlation-ID`) and the cross-route equivalence at `test_proposal_validation_api.py:706-724`.

### AS-2..AS-5 — PASS; guard-deletion sensitivity proved by executed mutation, not by reading alone

I rebound each guard's module attribute in a throwaway `python -c` process (no file edits) and drove the live route with `TestClient`, then compared against what the suite asserts:

| Guard (file:line) | Mutation applied in memory | Observed result | Asserting test | Caught? |
|---|---|---|---|---|
| BP-3 cap `proposal_checks_api.py:224` (`_bounded_message`) | `mod._bounded_message = lambda m: m` | 422, **len 696, 600-char attacker string present in body** | `test_bp3_uncapped_vertex_repr_is_length_capped:288-291` (`attacker not in message`, `len < 500`) | **YES** |
| BP-4 street cap `:434-440` | `ROUTE_MAX_STREET_LINES = 10**9` | **200**, no field | `test_bp4_over_street_line_cap_refused:354-363` (asserts 422) | **YES** |
| BP-4 lot-line cap `:427-433` | `ROUTE_MAX_LOT_LINE_SEGMENTS = 10**9` | **200**, no field | `test_bp4_over_lot_line_cap_refused:340-351` | **YES** |
| BP-4 wall cap `:421-426` | `ROUTE_MAX_EXTERIOR_WALLS = 10**9` | 422 but field `proposed_massing.exterior_walls[0].start_vertex_index`, `ROUTE_MAX_EXTERIOR_WALLS` **not** in message | `test_bp4_over_wall_cap_refused:335-337` asserts field `== "proposed_massing.exterior_walls"` **and** the cap name in the message | **YES** (the message assertion is the decisive one — field-only would have been near-missed) |
| BP-2 charset `:268-273` | `_LABEL_CHARSET = re.compile(r"(?s).+")` | **200** | `test_bp2_bad_charset_scenario_label_refused:227-231` | **YES** |
| BP-2 length `:263-267` | `MAX_LABEL_LEN = 10**9` | **200** | `test_bp2_over_length_scenario_label_refused:216-224` | **YES** |
| BP-5 types `:277-293` | `_validate_lot_rule_fact_types = lambda f: None` | **200** | `test_bp5_bad_bool_fact_refused:384-393` | **YES** |
| BP-5 domains `:318-333` | `_validate_lot_rule_fact_domains = lambda f,r: None` | **200** | `test_bp5_out_of_domain_street_width_class_refused:439-455` | **YES** |

**AS-3 spot-check (requested):** the arithmetic is bound as a first-class test, not just a comment — `test_bp4_documented_worst_case_product_under_1e6:313-325` asserts `500 * (800 + 400) == 600_000 < 1_000_000` **and** that each route cap is strictly below its library counterpart (`MAX_EXTERIOR_WALLS`, `MAX_LOT_LINE_SEGMENTS`, `MAX_STREET_LINES`). Ordering is correct at `proposal_checks_api.py:523` — the O(1) `len` caps run **before** the input gate (`:529`) and before `_build_lot_context` (`:531`), so no unbounded work precedes the refusal. At-cap completion is covered for lot lines (`:366-378`, exactly `ROUTE_MAX_LOT_LINE_SEGMENTS`, returns 200).

**AS-4:** unmapped-key behavior is positively bound — `test_bp5_unmapped_fact_is_surfaced_not_fed:424-436` asserts the key appears in `unmapped_lot_facts` **and** is absent from `rule_input_bindings` (proves "never fed", not just "surfaced"). Domain refusals echo the accepted set and never the offending value (`:454-455`). Positive in-domain path also covered (`:470-475`).

**AS-5:** `test_bp1_only_check_proposal_entry_is_called:481-485` — source grep `"check_proposal(" in source`, `"derive_proposal(" not in source`, plus `not hasattr(mod, "derive_proposal")` (catches the import, not only the call). BP-6 non-emission bound at `:488-498` (three forbidden keys **and** a whole-body lowercase blob scan for `contract_version` / `1.1.0`).

### AS-6 (four T053 residuals) — PASS with one non-blocking finding on (a)

`proposal_validation.py` is **byte-unchanged** (no diff vs contract head `0099bbb2`). I verified this is correct rather than a miss: all four branches pre-existed (`_bounded_message` truncation at `proposal_validation.py:120-129`, the generic 500, the strict-JSON/surrogate guard, the shared flag), and the packet's SCOPE 2 phrases each residual as "gets a binding test". Test-only closure is the right shape.

- **(a) 400-cap truncation** — `test_proposal_validation_api.py:635-651`. Binds truncation (`"truncated" in message`, raw 600-char value absent, `400 < len < 500`). **FINDING F-1 (see Defects): the cap's exactness is not pinned.** Measured live: the real response is `len == 431` (400-char prefix + the 31-char marker `...<truncated; 696 chars total>`), longest attacker run 316. Rebinding `_MAX_REFUSAL_MESSAGE_CHARS` anywhere in **[401, ~468]** keeps both assertions green, so cap drift is not caught. Guard *removal* is caught (MUT1 above).
- **(b) forced-500 bounded body** — `:653-673`. Strong: monkeypatches `app.api.v1.proposal_validation.validate_proposed_massing_input` to raise, asserts `set(body) == {"state","message","correlation_id"}` (exact key set, so any added leak field fails) and scans for `boom`/`runtimeerror`/`traceback`/`/services/api`.
- **(c) lone-surrogate refusal** — `:675-685`. Raw `\ud800` escape, asserts 422 + `"surrogate" in message`.
- **(d) shared-flag coupling equivalence** — `:687-711`. Both-off: identical generic 404 with no correlation id **for both URLs**; both-on: both become reachable and return their typed 422. Both halves present as required.

### AS-7 (four G4 fold-ins) — PASS

Engine preservation independently confirmed by blob identity, not by the diff alone: `services/api/app/rules/proposal_checks.py` = `46f080d5a7fd4ce973359992ac6179d5db34e88b` at contract `0099bbb2`, at frozen `2b5edbc3`, and at `fcc58e24` — **identical at all three**; the file is absent from the changed-file list. PRESERVATION input satisfied.

- **G4-1 AMBIGUOUS_RULE** `tests/rules/test_proposal_checks.py:668-687`: two usable same-family traces → `COULD_NOT_CHECK` / `AMBIGUOUS_RULE`, **`required_value is None`** (no winner), and the provided fact still recorded at `0.625`. A mutation that picked a winner would flip `outcome` and populate `required_value` → caught.
- **G4-2 NO_APPLICABLE_RULE** `:689-702`: `applicability_outcome=False` → `NO_APPLICABLE_RULE`, `required_value is None`. Distinguished from the neighbouring `ALLOWANCE_UNRESOLVED` branch, which the engine reaches via a different path (`app/rules/proposal_checks.py:592-599`) — the fake trace is applicable-false, so the assertion genuinely separates the two.
- **G4-3 commensurable MINIMUM** `:704-761`: hand-shown values against the derived `min_lot_line_setback = 10.0` ft (west wall `x=1000000` minus lot line `x=999990`), driven **end-to-end through `check_proposal`**, not only `_compare`: PASS at required 5.0 (`shortfall is None`), **equality boundary** PASS at 10.0, FAIL at 15.0 with **`shortfall == 5.0 == required - provided`**, plus `pass_count`/`fail_count` and a one-result invariant. Direction of the shortfall subtraction is therefore pinned.
- **G4-4 finiteness + type guard** `:763-782`: non-`LotContext` lot → `ProposalCheckError` field `"lot"`; a NaN **street**-line start → field `"lot.street_lines['W-S'].start"` (the exact dotted field, so a guard that refused with a generic field fails).

Test-double fidelity checked against the real engine: `_FakeRegistry.family_coverage`/`evaluate(...).export()` match `app/rules/proposal_checks.py:557-562`, and the hand-authored trace keys (`applicability_outcome`, `coverage_status`, `outputs`) are exactly the keys `_select_allowance` reads at `:578-581`. `_USABLE_COVERAGE_STATUS` is read **from the module** (`:601` of the test) so the fixture cannot drift from the accepted `_USABLE_COVERAGE` set.

### AS-8 (proof) — PASS

Steps 1-5 above. CI run `35498218087` green at `f86ae735` is orchestrator-supplied evidence for the pushed head; I did not and may not run `gh`. The local reproduction at content-identical material is independent and complete.

## Directive/requirement verification

| Requirement ID | Reviewed SHA / content identity | Verdict | Reproduced evidence |
|---|---|---|---|
| D-066-R001 | `2b5edbc3` ≡ `fcc58e24` (blob-identical) | PASS (QA scope) | Navigation block honoured: the route's only engine import is `check_proposal` (`proposal_checks_api.py:68-72`); `proposal_input_gate` consumed read-only (`:81`, `:529`); no edit to `app/rules/**` or `app/scenario/**` — confirmed by the 6-file commit stat |
| D-076-R001 / R002 | same | PASS (QA scope) | BP-6 non-emission enforced at `:586-593` (report dict + correlation id only) and bound by `test_bp6_...:488-498`; no `contract_version`, no scenario document anywhere in the response |
| D-077-R002 / R003 | same | PASS (QA scope) | Report references files/line anchors and carries one concise evidence pass (§4, §9); suite counts reproduce exactly |

The authoritative in-regime verdict for all four directives belongs to `directive-compliance-verifier` per ADR-005; the above is the G4-scope corroboration only.

## Regression/security/provenance findings

- No regression: the full `tests/api` + `tests/rules` run is **1266 passed**, zero failures, zero skips, zero xfails.
- Modularity: `proposal_checks_api.py` (593 lines) does not appear in any of the 20 warnings; `failures 0`. The module owns one responsibility (HTTP trust boundary) and delegates all computation to the accepted engine.
- Provenance: producer report digest verified byte-exact; suite counts verified independently; the `main.py` change is the single documented mount line plus its comment.
- Security-relevant behavior I re-derived live (not merely read): the uncapped B0 vertex `repr` (G5-3 class) reaches the client at 696 chars when the cap is removed and at 431 chars with it — the cap is load-bearing and active.

## Defects

**F-1 (non-blocking, MINOR) — AS-6(a) "exactness" is not pinned; the 400-char cap can drift undetected.**
`services/api/tests/api/test_proposal_validation_api.py:650` asserts `400 < len(message) < 500`, and `:649` asserts `"q" * 450 not in message`. Measured live, the true response is `len == 431` with a 31-char marker. Rebinding `_MAX_REFUSAL_MESSAGE_CHARS` (`services/api/app/api/v1/proposal_validation.py:82`) to any value in **[401, ~468]** leaves both assertions green. AS-6 asks for "400-cap truncation **exactness**"; what is bound is "bounded below 500". The security property (uncapped value cannot reach a client) **is** mutation-caught, which is why this is not a FAIL. The same band applies to the sibling assertion at `tests/api/test_proposal_checks_api.py:291`.
One-line fix, if the orchestrator elects to take it: import the constant and assert `len(message) == _MAX_REFUSAL_MESSAGE_CHARS + len(f"...<truncated; {orig_len} chars total>")`, or simply `assert message.endswith("chars total>") and len(message) - len(marker) == 400`.

## Coverage gaps (requested item 6 — untested branches of the NEW route module)

None of these is an acceptance-scenario miss; all are named here for the record and for B3 slice 2.

1. **The new route's own 500 path is untested.** `_internal_error_500` has three call sites — registry-unavailable (`proposal_checks_api.py:551`), unexpected engine defect (`:582`), serialization-unsafe (`:592`) — and no test drives any of them. The only mention of 500 in `test_proposal_checks_api.py` is the matrix membership assertion at `:511`. The *validation* route's 500 is well covered (residual b), so the pattern exists and transfers directly.
2. **Lone-surrogate half of the new route's strict-JSON guard is untested.** The checks route carries its **own** copy of the guard (`:493-500`); only the NaN half is tested (`test_nan_body_is_422:545-550`). I verified the branch behaves correctly by live probe — 422 / `validation_error` / `"surrogate"` in the message — but it rests on the validation route's test, not its own.
3. **At-cap-exact is only covered for one of four boundaries.** Lot lines at exactly `ROUTE_MAX_LOT_LINE_SEGMENTS` is covered (`:366-378`); **exterior walls at exactly 500**, **street lines at exactly 400**, `MAX_LABEL_LEN` at exactly 200, and `MAX_BODY_BYTES` at exactly the ceiling are not. I probed the label boundary live: 200 chars → HTTP 200 (off-by-one is correct).
4. **`_build_lot_context` shape refusals are untested** — six typed refusal sites at `:351`, `:358-360`, `:361-363`, `:366-368`, `:371-376`, `:386-398` (`lot must be an object`, `lot.area_provenance`, `lot.lot_line_segments`/`street_lines` not-an-array, per-element not-an-object, `attestation` not-an-object), plus `lot_rule_facts must be an object` at `:514`. Only the well-formed-lot paths are exercised.
5. **BP-1 "called exactly ONCE" is not bound.** `test_bp1_only_check_proposal_entry_is_called` proves *which* entry, not *how many times*. A mutation calling `check_proposal` twice would pass the whole suite. A counting spy would close it.

Empty-payload and flag-off — the other two you asked me to check — **are** covered (`test_empty_body_is_422:531-535`; `test_flag_off_is_generic_404_no_leak:155-159` plus the cross-route equivalence).

## Placeholder hygiene

No empty test files: both changed test files are substantive (`test_proposal_checks_api.py` 560 lines / 46 tests; the two appended blocks are 84 and 188 lines). `-rsxX` reports **no skipped, no xfail, no xpass** across all 1266 tests. No `pytest.mark.skip`/`xfail` anywhere in the proposal test files — the only marks are `parametrize`. No smuggled early `return`s.

## Required rework

**None blocking.** Recommended (orchestrator's call, all test-only and all inside the packet's existing `allowed_paths`): close F-1's cap exactness, and pick up coverage gaps 1, 2, and 5, which are the three with real fail-closed significance. Gaps 3 and 4 are reasonable to route to `docs/DISCOVERY_BACKLOG.md` for B3 slice 2 rather than reopen this packet.

## Reviewer conclusion

Frozen identity confirmed empty; the review worktree is byte-identical to the material. All four documented commands reproduce the expected results exactly (ruff 0 / 540 / 726 / modularity exit 0), with 1266 combined passes and no skips. Every acceptance scenario AS-1..AS-8 is met: AS-1's arithmetic is value-pinned down to the 0.125 shortfall and the exact summary dicts; each of the eight BP guards was shown by executed in-memory mutation to be genuinely bound by a test that flips red when the guard is neutered; the four T053 residuals each bind their named branch; the four G4 fold-ins carry hand-shown values with the engine `app/rules/proposal_checks.py` proved byte-unchanged across contract, frozen, and review heads. The one finding (F-1) concerns the *precision* of a cap assertion, not the presence or correctness of the cap, and the five coverage gaps are additive hardening for the next slice rather than unmet criteria.

**VERDICT: PASS**


---

<!-- DELTA ATTESTATION saved VERBATIM from the resumed qa-engineer agent (transport entity-decoding only), orchestrator seq 122, 2026-09-20. Ruling identity: frozen resubmission a0cb3b79. -->

Verification complete. Here is the delta report.

---

# Gate Report — G4 delta attestation

- Gate ID: **G4 (delta)** | Task: **M5-T057** | Reviewer: **qa-engineer** (independent, read-only)
- Frozen resubmission: **`a0cb3b795ebc84792673ab5f43d3300f0ea90f33`** | rework material `ca5c8895` | reviewed in **`wt-m5t057` @ `c09f87d6`**, `git status --porcelain` empty
- Result: **PASS — my original G4 PASS carries to the corrected identity**

## Identity (independently re-verified, as before)

| File | blob @ `a0cb3b79` | blob @ `c09f87d6` | match |
|---|---|---|---|
| `proposal_checks_api.py` | `127864e3` | `127864e3` | ✓ |
| `test_proposal_checks_api.py` | `5401d5d3` | `5401d5d3` | ✓ |
| `test_proposal_validation_api.py` | `efcaa8fb` | `efcaa8fb` | ✓ |

Byte-unchanged claims confirmed against the **original** frozen `2b5edbc3`: `app/main.py` = `bf2c658d`, `tests/rules/test_proposal_checks.py` = `83135b71` — both identical, so AS-7's fold-in evidence is untouched. **PRESERVATION holds**: `app/rules/proposal_checks.py` = `46f080d5a7fd4ce973359992ac6179d5db34e88b`, still identical to contract head `0099bbb2` and to the original frozen sha.

LF-sha256 + line counts recomputed, all three match your claims exactly: `ccfe7c99…3b96f6` / 721, `0d1cd1ac…362219` / 767, `cd3bf12e…be0a05` / 714.

## Counts re-run from `wt-m5t057/services/api`

| Command | Expected | Actual |
|---|---|---|
| `python -m ruff check .` | 0 | `All checks passed!` |
| `python -m pytest tests/api -q -rsxX` | 552 | **552 passed in 29.13s**, no skip/xfail |
| `python -m pytest tests/rules -q -rsxX` | 726 | **726 passed in 30.50s**, no skip/xfail |
| `python tools/modularity_check.py --check` (repo root) | 0 | `selected 456 files; failures 0; warnings 20` |

Modularity note against the seven boundary answers: the route grew 593 → 721 lines (+128) and is **still absent from all 20 warnings**. The growth is all boundary-enforcement within the module's single declared responsibility (three bound helpers + two offloads), no new responsibility, `__all__` extended additively only — public interface stable.

## New tests: adequacy ruling — ADEQUATE

I mutation-probed the four new production guards in memory (attribute rebinding, no file edits). Every one flips the asserting test red, and three of them expose exactly the leak the rework closed:

| Guard | Mutation | Observed | Asserting test | Caught |
|---|---|---|---|---|
| lot-side id bound (`:485`, `:511`) | `_require_label = lambda v,f: v` | **200** with a **106,411-byte** body — the 100k attacker id republished verbatim | `test_lot_line_id_over_cap_refused_and_bounded` (422 + field + `len(blob) < 2000`) | ✓ |
| provenance/attestation ceiling (`_require_bounded_object`) | neutered | **200**, 8,497-byte body | `test_area_provenance_over_ceiling_refused`, `test_street_attestation_over_ceiling_refused` | ✓ |
| fact-key bound (`_validate_lot_rule_fact_keys`) | neutered | **200** with `<img src=x onerror=alert(1)>` **echoed in the response body** | `test_lot_rule_facts_key_bound_and_unmapped_stays_bounded` | ✓ |
| outline position cap | `ROUTE_MAX_TOTAL_OUTLINE_POSITIONS = 10**9` | 422 but field `proposed_massing.outline.vertices`, cap name absent from message | `test_bp4_outline_position_over_cap_refused` asserts field `== "proposed_massing"` **and** the cap name in the message | ✓ |

The test file's own claim — "Removing the id bound flips this red: the payload would then return 200" — is verified true, not asserted. The G5-F1 200-path exposure and the G3-F3 XSS-shaped-key exposure were both real and are both now closed and bound.

Per new test, ruling:
- **Field-cap helper unit test** — adequate; binds prefix, marker, `None` passthrough, and the under-cap identity case.
- **Forced-500 (my gap 1)** — adequate and *better* than the validation-route sibling: drives a **real** emission from this route (`get_proposal_check_registry` raising → `_internal_error_500` at `:551`), with `set(body) == {state, message, correlation_id}` so any added leak field fails.
- **Lone surrogate (my gap 2)** — adequate; hits this route's **own** guard copy (`:493-500`), asserts `"surrogate" in message`. Matches the behavior I measured live during the first pass.
- **Counting spy (my gap 5)** — adequate; `len(calls) == 1` binds arity, which no source grep can. The spy correctly intercepts through `functools.partial` because the global is resolved at call time.
- **Threadpool double-offload spy** — adequate; asserts **both** `validate_proposed_massing_input` and `check_proposal` in `offloaded`, so dropping either offload flips red. `getattr(fn, "func", fn).__name__` correctly unwraps the partial.
- **Corrected worst-case arithmetic** — adequate; pins `600_000`, `1_438_800`, the `< 2.1e6` total, and `n < MAX_TOTAL_VERTICES`, backed by an import-time guard at `:169-174`.

**My F-1 is fully closed.** `assert message.index("...<truncated;") == 400` is present in **both** cap tests (`test_proposal_checks_api.py:292-294`, `test_proposal_validation_api.py:656-658`) and is exact: the [401, ~468] drift window I demonstrated now fails immediately.

## Findings (both minor, non-blocking)

**F-2 (new, MINOR) — the changed source assertion in `test_bp1_only_check_proposal_entry_is_called` is partly vacuous.** `"check_proposal," in source` is satisfied by the **import block alone**: the token occurs at `proposal_checks_api.py:81` (`    check_proposal,` inside `from app.rules.proposal_checks import (...)`) as well as at the real call site `:690`. So that clause no longer proves invocation. This does **not** weaken AS-5 on net — the runtime counting spy is a strictly stronger binding than the source grep it replaced, and the negative half (`"derive_proposal(" not in source`, `not hasattr(mod, "derive_proposal")`) is unchanged. Optional tightening: assert on the two-line sequence, or drop the clause and let the spy own the property.

**Observation (not a defect) — `_bounded_field`'s *wiring* is unit-tested, not integration-tested.** The helper is directly bound, but no test drives a >200-char field through `_validation_error`. That is by design: the root fix (bounding caller ids) makes the belt-and-braces layer nearly unreachable, and the reachable exposure is closed and mutation-proved. Correct layering, no action needed.

**Observation — behavior tightening B3 slice 2 must honor:** `lot.lot_line_segments[].id` and `lot.street_lines[].wall_id` are now **required** non-empty charset-conforming strings (previously a missing id fell through to the engine). No regression (552 green), but it is a narrowed caller contract worth carrying into the slice-2 editor payload.

**Observation — private import** of `_total_position_count` from the read-only `proposal_input_gate`. This is reuse, not an edit; it is consistent with the already-accepted private-reuse pattern in this file (`_bounded_message`, `_declared_content_length`, `_read_body_within_ceiling` from `proposal_validation`), and the stated rationale is sound — the cap must count positions exactly as the DB-034(a) budget does rather than fork a second definition.

My gaps **3** (at-cap-exact for walls/street-lines/label/body) and **4** (`_build_lot_context` shape refusals) are correctly routed to **DB-039**; both are additive hardening, neither is an acceptance-scenario miss. I note that gap 4 is now *partially* closed as a side effect — the new id/attestation tests exercise several `_build_lot_context` paths.

## Conclusion

The corrected identity is content-verified, all four command counts reproduce exactly (0 / 552 / 726 / 0), the engine remains byte-unchanged, and the 12 new tests are individually adequate — each binds its branch with assertions I proved mutation-sensitive by execution rather than by reading. The rework strictly strengthens the AS-1..AS-8 surface I passed originally: nothing that carried my first PASS was weakened, my F-1 is exactly closed, and three of my five recorded gaps are now covered. The one new finding concerns a redundant clause in an assertion whose property is bound more strongly elsewhere.

**My G4 PASS carries to `a0cb3b79`.**

**VERDICT: PASS**
