# M5-T054 G0 — contract-readiness record (phase B2 proposal-conditioned rule checks)

**Task:** M5-T054 — proposal-conditioned rule checks (first production caller of
`derive_proposal`; the T051-G5 wiring preconditions bind in-packet).
**Contracted at:** 2026-09-20 (session seq 121, the D-077 three-lane contract seam;
B1/M5-T051 accepted as the 236th, making B2 contractable per the plan's order).
**Reviewer of record:** orchestrator.

## Packet completeness (start-controlled-task checklist)

- **Requirement identifiers:** docs/PROPOSAL_EDITOR_PHASED_PLAN.md phase-B B2 row (SCOPE
  enumerates it verbatim-faithfully); the T051-G5/T053-seam wiring preconditions
  (LotContext list ceiling, coordinate finiteness, area_sq_ft bounded-repr) named in
  SCOPE 1/2 item (1); DB-034(d) disposition in SCOPE 2/2; D-051 fallback discipline
  cited for the COULD_NOT_CHECK path.
- **Directive refs:** D-066:R001 + D-076:R001,R002 + D-077:R002,R003,R004 — bound this
  seam (task_ids appends + digest resyncs + audit entries, same commit);
  `evaluate_task_refs` → ok, applicable == cited (6 ids). D-076-R003 deliberately NOT
  cited: no contract-schema touch (contracts + scenario/ are forbidden paths).
- **Evidence files named in SCOPE:** the plan's B2 row; DB-034 + the T053/T051 seam
  sweeps; derivation.py; evaluator.py + integration.py (read-only study).
- **allowed_paths / forbidden_paths:** four allowed (new module + new test + new
  fixtures glob + report); forbidden pins the boundary structurally (no evaluator/
  integration/registry/response/rulesets/scenario/api/spatial/connectors/web edit).
- **Acceptance scenarios:** AS-1..AS-8 in-packet (hand-computed primary, COULD_NOT_CHECK
  first-class, the three wiring preconditions, facts-vs-allowances grep gate, explicit
  fact mapping, DB-034(d) no-emission proof, purity+bounds, proof).
- **Gates:** G0,G2,G3,G4,G5; reviewers code-reviewer, qa-engineer, security-reviewer,
  directive-compliance-verifier (G2 recorded by orchestrator).
- **Code graph (D-066-R001):** regenerated at this seam (stale fingerprint →
  regenerated); NAV BLOCK embedded in inputs (derivation.py's sole consumer = its own
  test, proving THIS packet is the first production caller; evaluator/integration
  consumer sets with line anchors, verified in source).
- **Modularity boundary answers:** proposal_checks.py is a NEW single-responsibility
  module (pure wiring + evaluation, stdlib only); boundary pinned by forbidden paths
  and the caller-supplied-parameters rule; checker in the documented commands.
- **Seeds:** all four new allowed_paths entries seeded this commit (placeholder module,
  placeholder py test, fixtures README, report placeholder).

## Pairwise disjointness (mechanical, D-072)

Lanes contracted this seam: M5-T054, M5-T055, M5-T056 (no other live lane; every
in-flight M5 task through T053 is accepted).
- T054 ∩ T055 = ∅; T054 ∩ T056 = ∅ (set intersection computed over allowed_paths:
  EMPTY / EMPTY). T054 is services/api rules-tree only; T055/T056 never touch it.
- Parked legacy tasks (M0-*/M4-*/M5-T001 non-terminal statuses) hold ZERO overlapping
  allowed_paths (checked mechanically; NONE).

## Ledger/scope hygiene

- New allowed_paths files seeded this commit — no zero-tracked-file refusal.
- No open blocker `affects`/`detail` references M5-T054.

**Verdict: PASS — packet is contract-complete; claim may proceed at this head.**
