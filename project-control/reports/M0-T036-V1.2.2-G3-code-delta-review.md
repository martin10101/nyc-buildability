# M0-T036 V1.2.2 — G3 code delta re-review (verbatim reviewer return)

- Gate: G3 (code-reviewer) | Task: M0-T036 | Reviewed SHA: **b95ebf7** (V1.2.2 model-chain rework)
- Reviewer model: **claude-opus-4-8 / effort xhigh** (owner-approved Fable-out fallback, D-007-R609 lifting R607 for M0-T036 only)
- Delta baseline: 98da15e → b95ebf7
- Recorded verbatim by the orchestrator; reviewer was read-only and did not record the gate.

---

I have completed my independent verification. All evidence is gathered and reproduced. Here is my gate report.

---

## G3 CODE-REVIEW GATE REPORT

**gate=G3 | task=M0-T036 | reviewed_sha=b95ebf7 | model=claude-opus-4-8/xhigh (owner-approved Fable-out fallback) | scope=V1.2.2 delta re-review (baseline 98da15e → b95ebf7)**

### VERDICT: PASS

Frozen-SHA integrity confirmed: `git diff --stat b95ebf7 HEAD -- tools/agent_supervisor/ tools/test_agent_supervisor_loop.py tools/test_agent_supervisor_model_chain.py` returns EMPTY (only two SESSION_HANDOFF doc commits followed b95ebf7); the working-tree reviewed files equal b95ebf7. The delta touches exactly 8 files (claude_runner.py +259, cli.py +79/-, config.py +105, config.example.toml +18, loop.py +424/-, README.md +1, test_loop.py, test_model_chain.py new +807) — contained, no collateral edits.

### F1 (record-vs-actuation divergence) — CONFIRMED-RESOLVED

The prior blocking finding is genuinely closed in code, not just in records. Both halves of F1 (effective model AND expected_model must reach the real relaunch, plus an actuation-side regression test) are satisfied:

1. **Rebind reaches the launch config, both fields.** `ClaudeRunner.with_model` (claude_runner.py:761-789) returns a copied runner whose `config = dataclasses.replace(self.config, model=model, expected_model=model)` — `build_argv` emits `config.model`, so the next `--model` really changes, and `expected_model` moves with it so stream verification no longer flags every event as a downgrade (kills the perpetual model_mismatch). Id used verbatim; non-stripped/empty ids raise `bad_model_rebind`.
2. **Actuation happens BEFORE any record, on every seam.**
   - Switch seam: `_switch_at_seam` calls `self._actuate_model(selected)` FIRST, then writes the `model_substitution` event + durable record (loop.py; diff hunk "ACTUATION FIRST"). Records now carry `launched_model=self.launched_model()` read off the rebound config.
   - Return-to-pin: `_return_to_pinned` calls `self._actuate_model(self.pinned_model)` at loop.py:1203 before `complete_rotation` and before record writes (1211-1222).
   - Crash-resume: `SupervisedLoop.__init__` sets `self._current_model = effective_model(journal, run_id, pinned_model)` and, when it differs from the pin, `self._actuate_model(self._current_model)` (loop.py:653-655) — the rebuilt runner launches on the model the journal says the run is on, not the exhausted pin.
   - cli.py `_run_loop` builds the runner on `effective_model(journal, run_id, pinned_model)` with `expected_model = args.expected_worker_model or launch_model` — closing the exact line G4 flagged (the unconditional rebuild-on-pin).
   - `_actuate_model` (loop.py:765-789) refuses (`LoopError model_actuation_unavailable`) rather than recording a switch a runner cannot perform — no record without a launch.
3. **Actuation-side regression tests spawn REAL OS processes and read `--model` off the argv the child actually received** (test_agent_supervisor_model_chain.py): `test_a_real_process_comes_up_on_opus_4_8_after_a_fable_5_exhaustion` (asserts `argv[argv.index("--model")+1]==claude-opus-4-8` from a process-written launch log), `test_a_resumed_run_launches_a_real_process_on_the_effective_model`, `test_the_runner_start_builds_is_configured_on_the_effective_model` (asserts `captured[0].expected_model==claude-opus-4-8`), `test_with_model_moves_both_model_and_expected_model`, and the actuation-impossible refusal test. These distinguish a real launch from a mere event — exactly what F1 demanded.

Reproduced: `python -m unittest tools.test_agent_supervisor_model_chain -v` → **20/20 OK**; `python -m unittest tools.test_agent_supervisor_loop` → **97/97 OK**; broad regression `... loop model_chain invariants crash adversarial` → **287/287 OK**. No regressions.

### Directive requirements — verified at code level

- **D-007-R603 (detect-and-SWITCH supersedes detect-and-hold):** old `SUBSTITUTE_MODEL` constant + `_substitute_at_seam` fully removed (grep confirms zero residual references); orchestrator quota-exhaustion now walks the chain via `_switch_at_seam`; non-quota reasons keep the fail-closed hold/pause. PASS.
- **D-007-R605 (HARD — relaunch must ACTUALLY launch on effective model):** verified above; proven by real-process argv assertions. PASS.
- **D-007-R606 (three DISTINCT triggers not conflated):** quota switch → reason_code `quota_exhausted`, event `model_substitution`; 400k context rotation → `context_threshold`; mid-task security downgrade → `model_downgrade`. Distinct reason codes, distinct records; they share only the safe seam actuator. `test_a_real_process...` asserts `durable["reason_code"]==quota_exhausted` AND `durable["rotation_reason"]==context_threshold` simultaneously — proving the quota switch is not absorbed into the rotation that carried it to the seam. PASS.
- **D-007-R608 / R751-R760 (governance):** chain order `("claude-fable-5","claude-opus-4-8","claude-opus-4-7")` exact (config.py `DEFAULT_ORCHESTRATOR_MODEL_CHAIN`); immutable/owner-editable-only via `[model_chain]` in the manifest-covered controller config, excluded from `_RUNTIME_ONLY_KEYS`, frozen dataclass; availability by ACTUAL LAUNCH PROBE (`probe_model_launch` launches the exact id and requires the process to report that exact id — a process reporting another id → `model_id_mismatch`, unavailable); exact-string membership, no normalize/alias/trim (`ModelChain.__contains__`, `_load_model_chain` whitespace/dup rejection); unlisted ids (Opus 5) never probed nor actuated (`candidates_after` stays in-chain; `_actuate_model` raises `model_not_in_chain`); chain exhaustion = clean STOP+notify with no substitution record (`_stop_chain_exhausted` → PAUSED_RECOVERY + queue_ask + handoff refresh); orchestrator-role only. Tests `test_an_unlisted_id_is_never_selectable_even_if_it_launches`, `test_a_process_that_reports_another_id...`, `test_no_chain_entry_launches...`, `test_a_non_orchestrator_role_pauses...`, `test_the_ids_are_exact_strings_with_no_aliasing`, config suite (empty/unknown-key/duplicate/whitespace refusals) all PASS. PASS.

Error paths are fail-closed: bare `False`, `unknown`, `probe_error` (probe raises), and every non-orchestrator role → PAUSE+notify, never a switch (`_probe_model` loop.py:732-757; verified by `test_a_probe_that_raises_fails_closed_to_a_pause`, `test_bare_false_probe_is_not_quota_and_pauses`, `test_quota_exhausted_non_orchestrator_still_pauses`).

### Findings

- **A1 — activation-only, informational (NOT acceptance-blocking).** `QUOTA_EXHAUSTION_SIGNAL_VERIFIED = False` (claude_runner.py:42) and cli.py wires `make_launch_probe(...)` WITHOUT a `classify_unavailable`, so in production the probe's classifier defaults to `""` (unknown). Consequence: a REAL Fable-5 account-quota exhaustion is currently classified `unknown` → fail-closed PAUSE, so the live chain-SWITCH will not actually fire until the installed CLI's real exhaustion stderr/exit bytes are captured and a classifier is wired. This is disclosed honestly in code and surfaced by `doctor` (`_check_model_chain_disclosure`, status UNVERIFIED). Correct fail-closed posture; the switch machinery is proven by an injected classifier in tests. This is an activation prerequisite, consistent with the shadow-only acceptance — not acceptance-blocking.
- **A2 — informational.** Availability probing is by design an actual process launch per seam (R752/R753); against a live CLI this consumes a small amount of quota on each pin probe and adds launch latency (bounded by `MODEL_PROBE_TIMEOUT_SECONDS=120`). This is the owner-mandated "launch probe, never a picker" behavior, not a defect. Worth the orchestrator's awareness at activation.
- **A3 — minor/maintainability, non-blocking.** Crash-resume `__init__` will raise (`model_not_in_chain`/`model_actuation_unavailable`) at construction if a durable active-switch names a model the owner has since removed from the chain, or if the injected runner lacks `with_model`. This is fail-closed and arguably correct (never resume on a now-unsanctioned model), but it surfaces as a constructor exception rather than a graceful pause. Acceptable; noting for completeness.

No acceptance-blocking correctness, contract, error-handling, or test-integrity defects found in the V1.2.2 delta. Tests are strong (real-process argv assertions, not event-only), fail-closed paths are covered, and records now provably agree with actuation.

### Scope attestation

I stayed strictly within the V1.2.2 delta (`git diff 98da15e b95ebf7 -- tools/agent_supervisor/ tools/test_agent_supervisor_loop.py tools/test_agent_supervisor_model_chain.py`), verified the working tree equals frozen SHA b95ebf7, did not re-litigate the settled F1 finding but verified its fix, and treated the prior G3 B-1..B-4 activation corrections and the quota-signal verification as activation-only (this task is accepted SHADOW-ONLY; activation is a separate owner decision). I made no writes, ran no control-plane/git-write/gh commands, and did not record the gate.
