# M5-T121 — G3 code review (code-reviewer "cr-t121", read-only)

> Transmission history: pinned at ed229971 (material d865d977; CI run 36113132489 green at ed229971), delivered as
> 3 SendMessage parts ending with END-OF-REPORT; no truncation (each part was queued twice, byte-identical; the first copy is joined). Joined verbatim by the orchestrator (transport
> wrapper tags removed only). Verdict PASS; two ADVISORY items (no test for a scale ring different from the
> outline ring; build_draft has no huge-but-finite pre-check - still a typed refusal).

---

M5-T121 G3 code review (PDF sheet import service) — part 1/3. READ-ONLY.

HEAD pinned ed229971 (branch candidate/D-024-mrl-option-b). Material d865d977 = the 3 allowed files ONLY (sheet_import.py, test_sheet_import.py, producer report); HEAD adds only ledger/gate artifacts — no forbidden path touched, dxf_import.py and the sheet_* reader modules unchanged.

Evidence run (verified independently, not just trusting the pack):
- ruff check services/api → All checks passed.
- pytest test_sheet_import.py + test_dxf_import.py → 60 passed (31 sheet_import) on local Python 3.11.
- CI on pushed head ed229971 (Python 3.12, the authority): api "CI" job success 5m48s + secret-scan + context-budget all success.
- tools/modularity_check.py --check → EXIT 0; sheet_import.py (623 raw lines, SLOC under the 600 warn) NOT in the warning list.
- In-process probes (python stdin; nothing written to the repo) A–E, results in part 2.

Part 2/3 (correctness) follows.

---

M5-T121 G3 — part 2/3, correctness (all confirmed; file:line):

- Stable-index addressing. `_closed_ring_candidates` (sheet_import.py:290) pairs each closed >=3-pt ring with its STABLE polyline index; `build_draft` validates roles against ALL rings via `_rings_by_index` (301, 554-567), not the 200-cap display list (398, const 123). `_select_ring` (508-520) searches BY stable index — correctly diverging from DXF's positional `rings[index][1]` (dxf_import.py:465), which would be wrong under this scheme. Probe A: a ring at stable idx 1 behind an open polyline at 0 is listed as 1 and selected correctly.
- Deviation 1 safety (the display cap never hides an assignable ring). Probe B: 202 in-bounds rings, cap 200, stable idx 201 absent from the display list, yet build_draft(building_outline=201) → ok with the correct outline. NOT covered by any shipped test (they use 1–2 rings); now independently verified.
- Scale (418-505): `(edge_index+1)%n` includes the closing edge; zero-length/non-finite edge → invalid_scale; scale = known/measured with finite + [1e-9,1e9] bounds; exact arithmetic (edge 40, known 80 → 2.0) confirmed (Probe E).
- Draft = the ASSIGNED ring scaled with explicit closure (523-531, 574); validate_proposed_massing called exactly as DXF (591-599); block provenance kind stays "proposed" (589); input_class lives ONLY in the separate provenance block, not the massing block (Probe E).
- Deviation 3 / OQ-1: `_outline_vertices` scales, never translates; a genuinely local frame fails the UNCHANGED NYC-2263 bounds (proposal.py:254-264) → draft_invalid naming the outline field; FRAME_NOTE (102-105) discloses the local frame; contract not weakened or bypassed. Honest.
- Reuse: only dxf_import PUBLIC names imported (55-63, all in DXF __all__); `_escape_drawing_text` reimplemented locally — Probe C shows byte-for-byte identity with the private DXF helper across 9 samples incl. control bytes, 0x7f, and truncation.
- No exception escapes to a caller (Probe D: None/bool/float/str/neg/oob page_index → all typed page_out_of_range). Closed-vocab typed refusals; `_select_ring` KeyError is unreachable (index validated first).
- Tests behavior-focused; 8 mutations each redden the right guard for the right reason (e.g. rows 6/7 flip selection to ring A; row 4 forces a supplied measurement → scale 1.0 → draft_invalid; row 8 upgrades precision). No vacuous assertions.

Part 3/3 (findings + verdict) follows.

---

M5-T121 G3 — part 3/3, findings + verdict.

BLOCKING: none.

ADVISORY (non-blocking; consistent with the accepted DXF precedent):
- A1. No test exercises scale_candidate != building_outline (scale measured on ring X, applied to a DIFFERENT outline ring Y). The design is sound — a uniform per-sheet feet/unit factor — and tests/Probe B cover the same-index case; a mixed-index test would close the coverage gap. Not a correctness defect.
- A2. build_draft has no huge-but-finite pre-check like list_candidates (377-387); an overflow outline returns draft_invalid (or invalid_scale via the tiny measured scale) — still a typed refusal, no crash, and the module is pure/unmounted. Already captured by the producer's DISC-B (the mount packet adds the allow_nan=False pre-render guard). Fine to defer.

Neither advisory weakens the validate_proposed_massing contract or the honesty/provenance discipline. The module faithfully mirrors the accepted DXF import, correctly adapts to stable-index addressing and a service-measured edge scale, keeps the draft in a disclosed LOCAL frame that must pass the unchanged NYC bounds, reuses only public DXF names, and returns a typed refusal on every failure. Scope, ruff, tests (local 3.11 + CI 3.12), and modularity all clean.

M5-T121 G3 VERDICT: PASS
END-OF-REPORT
