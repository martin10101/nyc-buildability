# M5-T108 — G4 test-adequacy review (qa-engineer "qa-t108", read-only)

> Transmission history: pinned at f4c4ab3a, delivered as three SendMessage parts (1/3..3/3) ending with END-OF-REPORT;
> no truncation. Joined verbatim by the orchestrator (transport wrapper tags removed only). Verdict FAIL on F1: the draft
> never proves it uses the USER-ASSIGNED candidate (always-ring-0 and largest-ring mutants survive); advisories A1-A3 join
> the rework round.

---

M5-T108 G4 test-adequacy review (qa-engineer, READ-ONLY, in-memory mutation) — PART 1/3

PIN: `git rev-parse HEAD` at start = f4c4ab3a696f52fb32e460699a82236ef4457c7a (expected f4c4ab3a or later — OK).
Baseline: from services/api, `python -m pytest tests/drawings/test_dxf_import.py tests/drawings/test_dxf_import_api.py -q` → 41 passed. Reproduced 3x (41 passed each, 4.4–4.8s).

METHOD: for each mutation I patched the CONSUMING namespace in-memory (app.api.v1.dxf_import_api / app.drawings.dxf_import), ran the full 41-test suite in-process, and restored (fresh process per mutation). Suite fully green under a mutation = SURVIVED (adequacy gap); ≥1 failure = CAUGHT. The producer's 9 `test_mutation_*` guards are in-suite and non-vacuous (each applies its mutation and asserts the FLIPPED outcome, which differs from the unmutated outcome — e.g. `assert status != 413`), so I focused my own mutations on the AS controls and the producer table's blind spots.

CAUGHT — suite adequately constrains these (7 of my independent mutations):

| # | Mutation (consuming namespace) | AS | Result |
|---|---|---|---|
| 1 | `mod.MAX_BODY_BYTES = 1e9` (raise upload ceiling) | AS-1(b) | CAUGHT (test_seam_passes / over_ceiling) |
| 2 | `mod.sniff_dxf_media = lambda ct,raw: None` (media/magic gate off) | AS-1(d) | CAUGHT (test_wrong_content_type_refused_415) |
| 3 | `mod._client_key = uuid-per-request` (rate limiter keyed unique → never trips) | AS-1(c) | CAUGHT (test_rate_limit_429) |
| 4 | `svc.DRAFT_INPUT_PRECISION = "survey-confirmed - authoritative"` | AS-3 | CAUGHT (route test_draft_200 asserts the LITERAL, not just the import) |
| 5 | `svc.DRAFT_SOURCE_LABEL = "Approved maximum allowed building"` | AS-3 | CAUGHT (2 tests) |
| 6 | `_resolve_scale` returns empty discrepancies (auto-reconcile) | AS-2 | CAUGHT (test_units_mismatch + route test_draft_shows_discrepancy) |
| 7 | `mod._not_found` adds a feature hint to the 404 body | AS-5 | CAUGHT (test_flag_off_is_generic_404) |

Note on #3: keying-by-unique (the dangerous direction) is caught; only over-broad keying (safe) would survive. Note on #4/#5: NOT tautological — the route test asserts the literal honesty strings and the service test captured the constant by value at import, so both flip when the source constant is mutated (matches the repo's hardcoded-literal-honesty rule).

Continued in part 2 (surviving mutants + BLOCKING finding).

---

M5-T108 G4 — PART 2/3

SURVIVED — suite does NOT constrain these (adequacy gaps):

| # | Mutation (consuming namespace) | AS | Result |
|---|---|---|---|
| 8 | `mod.build_draft` forces building_outline→0 (ignore user role) | AS-2 | SURVIVED (41 green) |
| 9 | `mod.build_draft` auto-picks the LARGEST-area ring | AS-2 | SURVIVED (41 green) |
| 10 | `svc._resolve_scale` drops the finite/strictly-positive guard on known_length_ft/measured_length | AS-1/AS-4 | SURVIVED (41 green) |
| 11 | `svc._refusal_from_read` skips `_escape_drawing_text` on the reader-refusal detail | AS-4 | SURVIVED (41 green) |

BLOCKING — F1: the draft never proves it uses the USER-ASSIGNED candidate (mutants #8 and #9, same root cause). AS-2 is "a draft is built ONLY from user-assigned roles," yet EVERY draft fixture (service and route) uses a SINGLE candidate ring at index 0, so a regression that silently always selected ring 0 — or auto-picked the largest ring — ships completely green across all 41 tests. The echo (`assigned_roles.building_outline`) and the range check (test_bad_candidate_index_refused) are tested; the GEOMETRY-selection is not.

Discriminating probe against the REAL (unmutated) code — proves this is untested-correct behavior, not a latent bug:
- 2 distinct in-bounds rings, `building_outline=1` → real draft `outline.vertices[0] == [990000.0, 200000.0]` (ring B), `assigned_roles.building_outline == 1`. An always-ring0 / largest-ring mutant would yield ring A `[985000.0,195000.0]` and no test would notice.

Cheap fix (closes F1): add ONE draft test (service level, ideally route too) with a 2-candidate DXF of two distinct closed in-bounds rings and `building_outline=1`, asserting `proposed_massing.outline.vertices[0]` equals the SECOND ring's first vertex (scaled) — and a reddening mutant that selects rings[0]/largest. This is the missing "reddening mutation" for the core AS-2 selection behavior.

Continued in part 3 (advisories, determinism, verdict).

---

M5-T108 G4 — PART 3/3

ADVISORY (mutant survived but lower severity — the code is correct; these are recommended tests, not blockers):

- A1 (mutant #10): the known-dimension scale guard is fail-closed on CALLER-CONTROLLED numeric query params, yet has ZERO coverage. Real code correctly refuses measured_length=0/<0, known_length_ft<0/inf, measured=nan (all → ImportRefusal unsupported_units, verified directly). Without the guard, measured_length=0 → ZeroDivisionError → 500, or nan → garbage scale. test_known_dimension_scale_path only covers 80/80. Add a refusal test for measured_length=0 and a non-finite value.
- A2 (mutant #11): reader-refusal detail escaping is untested. The route's `_bounded_message` backstops LENGTH (400 chars) but does NOT strip control chars, so `_escape_drawing_text` in `_refusal_from_read` is the only control-char defense for that path; the reader repr-escapes most embedded values, so exposure is narrow. Add a test that a reader refusal whose detail carries a control char is escaped at the service/route.
- A3 (boundary): the upload ceiling edge is untested. Direct probe: `_read_body_within_ceiling` accepts exactly MAX_BODY_BYTES (262144, too_large=False) and refuses MAX+1 (0, True) — correct strict `>`. Existing tests only send MAX+1; a `>=` off-by-one (safe direction) would survive. Add an exactly-at-limit accepted case.

DETERMINISM: no wall-clock flakiness. The deadline tests use wide margins (0.6s sleep vs 0.1s deadline; 0.2s vs 5.0s) and the rate-limit test injects a monotonic fixed-window limiter — 3x full runs all 41 green.

DISPLACED-VALUE / NON-VACUITY: honesty tests assert the displaced value (route asserts literal precision/label; mutants #4/#5 flipped them → CAUGHT), not "some error". Discrepancy, media, ceiling, rate-limit tests all assert the specific (status,state) pair.

FORBIDDEN SUITES: `tests/drawings/test_dxf_reader.py tests/drawings/test_dxf_roundtrip.py` → 61 passed, unchanged (forbidden files not touched).

Rationale for verdict: mutants #8/#9 leave a surviving mutant on the CENTRAL AS-2 acceptance behavior (draft built from the user-assigned role) across the entire suite — exactly what a G4 adequacy gate exists to catch, and specifically one of the checks I was asked to probe. The fix is one small discriminating test. The implementation itself is correct; this is a test-coverage FAIL.

M5-T108 G4 VERDICT: FAIL

END-OF-REPORT
