# M5-T065 HJ — independent human-journey review (verbatim reviewer return)

Reviewer: human-journey-reviewer (claude-opus-4-8 per D-064), dispatched by the orchestrator
at the seq-124 T065 wave, pinned to the frozen submit head 65faee54 (material d3793c2e).
G3-class acceptance evidence; recorded by the orchestrator. The return arrived in four
parts (the first transmission truncated mid-HJ-2; the reviewer re-sent the remainder in
three short labeled chunks) — joined verbatim at the exact break points below.

---

VERDICT: PASS

# M5-T065 — Human-Journey (G3) Review

**Task:** D-082 map-drawing input slice — keyboard-operable outline entry over the accepted read-only lot map, server-side EPSG:4326→2263 correspondence bridge (residual disclosed, typed refusals), additive adoption into the accepted numeric draft (manual stays the option).
**Frozen identity reviewed:** submit head `65faee54` (material `d3793c2e`); working tree equals the frozen surface for the 12 task paths. Web behavior relied upon as CI-proven: e2e green at `a3173987` (run 35554705276) and `93b5943a` (run 35555871945).
**Scope ruling honored:** I walked the journey AS DELIVERED (keyboard/numeric vertex entry), did not fail for the absence of click-to-place (recorded `[ORCH-SCOPE-DISPOSITION]`; click layer is contracted as M5-T066), and judged honestly whether the delivered keyboard journey is usable, clear, and honest — noting where the missing pointer path hurts (feeds T066).

## Journey walked (entry → drawing → convert → adopt → check → report)

1. **Entry.** From `/property?ruleeval=on` → open property (BBL) → "Architect workspace" nav → "Proposal editor" link (`ArchitectEntry.tsx:142-143` mounts `<ProposalEditor bbl={bbl}/>`; e2e `proposal-editor.spec.ts:118-128`). The drawing section renders only when a BBL is present (`ProposalEditor.tsx:383-387`). The whole surface sits behind the internal `INTERNAL_RULE_EVAL_ENABLED` flag (off by default) — a preview surface, not live to ordinary architects yet.

2. **Drawing-section honesty.** `ProposalOutlineDraw.tsx:129-143` leads with `<h3>Draw the outline</h3>` and "Proposed — your sketch, not a city record… approximate proposed input with its fit accuracy disclosed — not a survey. The numbers table stays editable; typing coordinates is always an option." A section note states the map is display-only and nothing is measured from it. Honesty framing is correct and matches the D-076-R002 doctrine.

3. **Add / edit / delete points by keyboard.** "Add drawn point" appends a row of aria-labeled `longitude`/`latitude` number inputs (`:156-195`, `:198-200`); Delete is aria-labeled per row (`:176-184`). Focus-on-delete is **managed from the start** via a `pendingFocus` nonce + `useEffect` (`:71-89`, `:99-107`) — focus lands on the delete control that slid into the index, else the Add button when the list empties (proven `proposal-outline-draw.test.tsx:119-132`). This directly closes the HJ-2 MEDIUM I raised on M5-T060 (delete-focus falling to body).

4. **Convert.** Gated to ≥3 points (`MIN_DRAWN_VERTICES`, `:40,110`); POSTs the drawn 4326 points to the flag-gated **UNMOUNTED** `/api/v1/outline-bridge` (`:112-123`). The client enforces the exact (status, state) matrix mirrored from the route and decodes every typed refusal distinctly (`outline-bridge-api.test.ts` — bridged / feature_unavailable / payload / invalid+reason / out_of_neighborhood / correspondence+reason / residual+bound / source / internal / validation_failure / unexpected). On `bridged`, `onAdopt` receives the server's 2263 vertices; refusals adopt nothing (`:120-122`).

5. **Bridge honesty (endpoint).** `outline_bridge.py` is a genuinely careful correspondence design: it fits an affine between the SAME parcel's two accepted rings and enforces a **necessary-not-sufficient** trust rule — emit only with ≥4 control points AND RMS ≤ 2.0 ft AND a ≥ 2.0 ft margin over the runner-up alignment; otherwise `ambiguous_correspondence`/`residual_too_high`, never guessed (`:739-771`). No CRS library, no hand-rolled Lambert math; residual/margin/alignment/both source-ring identities are all disclosed (`:789-819`). Flag-off/unmounted returns a generic 404 byte-indistinguishable from an absent path (`:561-564,636-637`).

6. **Adoption + re-check.** Bridged vertices land in the numeric authority table exactly as if typed (`adoptOutlineVertices` `proposal-draft.ts:277-279`; `adoptDrawnOutline` `ProposalEditor.tsx:107-114`), the stale check outcome is cleared, and the announcer tells the user to run the check. The re-check runs on the adopted shape (e2e AS-4 `proposal-editor.spec.ts:282-348`: draw → stubbed bridge → 2263 in the table → run check).

## Honesty judgment — PASS

Nothing false is presented. Drawn input is consistently labeled proposed, never a city record; the bridge output is disclosed as approximate with its residual, never survey-grade; the announcer never says "verified"/"best". Crucially, the surface **degrades honestly**: because the endpoint is UNMOUNTED and flag-off by design in every real environment, Convert always resolves to `feature_unavailable` → "Map-drawing conversion is not available in this environment. Enter coordinates in the table instead." (`outline-bridge-api.ts:531-532`), and the fully-functional numeric authority table sits right beside it. Drawn points persist across a refusal, and `out_of_neighborhood`/`residual_too_high` copy tells the user to adjust — real recovery paths.

## Accessibility judgment — PASS

Every action is keyboard-operable (Add/Convert/Delete buttons, lng/lat number inputs, all aria-labeled); the drawn-points table uses `scope="col"`/`scope="row"` + `<caption>`; `OutcomeAnnouncer` carries the AT message; the status region is `role="status"` for a bridged result and `role="alert"` for refusals (`:212-218`). Heading order is clean (h1 property → h2 "Proposal editor" → h3 "Draw the outline"). The bridged-success card uses `<strong>` rather than a heading, consistent with the accepted strong-not-heading pattern elsewhere in this workspace. Keyboard journeys are CI-proven (e2e drives Add/Convert/run-check via real Tab+Enter).

## Findings

All findings are **advisory** — none blocks acceptance of the delivered slice, and most feed M5-T066 (the click-to-place layer).

- **HJ-1 (MAJOR advisory — feeds T066; where the missing pointer path hurts).** The copy "Draw the outline" / "Place points over the lot below, then convert" / "Add at least 3 points over the lot" (`ProposalOutlineDraw.tsx:129,133,190`) describes a spatial map gesture that does not exist as delivered — the user types WGS84 longitude/latitude decimal degrees, which an architect does not know for building corners. Absent click-to-place, the drawing section provides essentially no value over typing 2263 directly in the authoritative table beside it, and the composed map is decorative (and in Playwright/CI Chromium may itself be the no-WebGL fallback, per my `lot-outline-map-web` notes, doubling the mismatch). Recommendation for T066: land the map-click seam, or until then reword to describe numeric lng/lat entry / suppress the section. This is the single biggest usability gap and is exactly the T066-feeding note the disposition asked for.

- **HJ-2 (MEDIUM advisory — adoption correctness; latent, CI-green masks it).** `adoptOutlineVertices` replaces only `vertices`, leaving `exterior_walls` (and `lot_line_segments`) referencing possibly-removed indices. The seed draft is a 5-vertex rectangle with 4 walls (indices 0-3); adopting a drawn triangle (3 vertices) leaves walls `W-N(2→3)` and `W-W(3→0)` dangling. `validateDraft` does not check wall→vertex validity, so the client would POST an inconsistent draft that the real check route would refuse. The AS-4 e2e asserts only `outline.vertices` and returns a canned report (`proposal-editor.spec.ts:302-313`), so CI stays green while the adopted draft is internally inconsistent. Recommendation: on adoption, reconcile or clear `exterior_walls`/`lot_line_segments` (or surface a "walls reference removed vertices" warning). This inherits T060's lack of wall/vertex reconciliation, but adoption makes dangling walls the default outcome of any drawn outline whose vertex count ≠ 5.

- **HJ-3 (MEDIUM advisory — sequencing, orchestrator).** The drawing section is rendered whenever a BBL is present, ungated on the bridge endpoint being mounted; Convert is therefore dead (`feature_unavailable`) in every real environment until the mount seam lands. The fallback copy is honest, but before this preview flag is flipped on for real users, gate the visible surface on the endpoint being mounted (or land the mount first).

- **HJ-4 (minor advisory).** With 1-2 drawn points, Convert is disabled with no visible reason — the "add at least 3" hint only appears in the empty-state row at 0 points (`ProposalOutlineDraw.tsx:187-193`). Consider a persistent "need ≥3 points" hint while the button is disabled.

- **HJ-5 (minor advisory).** Recoverable bridge faults announce "safe to retry" but there is no dedicated Retry control; the user re-clicks Convert (drawn points persist, so recovery works). Consistent with the T060 run-check pattern — noting for parity only.

## Consistency with prior notes

Reuses the accepted `card`/`failure-state`/`failure-meta`/`section-note` classes, `OutcomeAnnouncer`, and details-disclosure framing — strong consistency with the M5-T060 proposal editor. The focus-on-delete remedy I flagged as a MEDIUM in M5-T060 is applied here from the start. The honesty tone matches the address-identity/record-address channels reviewed earlier.

## Bottom line

The delivered slice — keyboard-operable vertex entry, focus-managed deletes, honest proposed-not-a-record labeling, disclosed correspondence residual, distinct typed refusals, honest `feature_unavailable` degradation, and additive adoption into the visible/editable/authoritative numeric table with a fresh check on the adopted shape — is usable, clear, honest, recoverable, and keyboard-accessible for the surface it actually is. Its incompleteness (no click-to-place; numeric lng/lat entry is a poor standalone input) is legitimized by the recorded scope disposition and is the substance of the T066 follow-up. PASS, with the advisories above routed to M5-T066 and adoption hardening.

VERDICT: PASS (advisory findings only; none blocking).

[END of M5-T065-HJ report]
