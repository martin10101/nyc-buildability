# M5-T080 — G3 independent source-fidelity review (code-reviewer "cr-p0", read-only)

> Transmission history: pinned at 33662211 (the four deliverables byte-equal to e8a2e9f1), delivered as four
> SendMessage parts (1/4..4/4) ending with END-OF-REPORT; no truncation. Joined verbatim in delivery order by
> the orchestrator (transport wrapper tags removed only). Verdict: FAIL (F1-F3 BLOCKING).

---

M5-T080 G3 source-fidelity review (code-reviewer, read-only), part 1 of 4.

PIN: HEAD 33662211 (producer pin b56f3d5b; material 5aadf182 + 170b942c; seam e8a2e9f1). The four deliverables at HEAD are byte-equal to e8a2e9f1.

PASSING CHECKS
- AS-1: I re-enumerated independently by parsing every markdown table row in assessment lines 57-923 and matching an ID on the first cell (a different method from the producers' grep). Result: 381 ids, 0 duplicates. Per family: SH15 PE10 DR23 PC6 PV3 ME17 SW3 ZC4 AD28 AC11 M13 LC22 (=155); A15 C11 F8 E16 R5 LS-P16 LS-C28 LS-E11 LS-F17 LS-T15 SR62 DB22 (=226). The ledgers (155 + 226) are an exact bijection with this set, and every row has exactly the 18 packet keys. The other first cells in range are only headers and test/lib anchor rows.
- AS-2: 344 L rows, none with an empty field among the five. The 4 consolidations resolve (LS-C08→LS-C24, LS-C23→LS-C22, LS-C27→LS-C06, SR27→SR12). There are no retire rows, and replacement_ref is empty on every other row. I compared all 381 marks with the assessment's triage text, by hand and with a script: none is weakened.
- AS-5: 5aadf182 touches only the 3 PART-A paths. 170b942c touches only ledger-b, whose blob c93b2f3f is the same at b51cba02, 170b942c and HEAD. There is no source, test, copy, rule or assessment edit. json.tool exits 0 on both ledgers. The assessment's LF sha256 is c6d1b257…, matching D-086-R001.
- AS-4 coverage: all 43 DB items the assessment cites have a row. The Section-13 matrix covers all 10 findings, each with steps and an owning slice. The boundary table covers D-040, D-076, D-082 and D-087. The drift table is correct: apps/web has 0 changed files dc5a763e→b56f3d5b, the backlog grew by 8 lines and the hold file by 10.
- Spans: I checked 67 current_source spans (44 in PART A, 23 in PART B) plus whether every cited path exists. They include SH-01/05/07, PE-01/07/08, DR-01/02/05 and DR-10..23, PC-01/02/05, PV-02, ME-03/04/08..17, SW-01..03, ZC-01, AD01/11/12, AC03/10, M01-M13, LC11/17/22, A05/07/08/10, C04/05/06/10, F03, R01, E11, LS-P01, LS-C10, LS-E10, LS-F03/F17, LS-T01, SR22/27 and DB07/21/22. The DR and ME spans were checked at b56f3d5b. All of them show the stated text except those listed in F1.
- Authorities: I checked about 50 cells. The 9 that are wrong are listed in F2.

---

M5-T080 G3 report, part 2 of 4: BLOCKING findings F1 and F2.

F1 (AS-3): wrong current_source in 11 rows.
- 10 rows cite a file that does not exist at dc5a763e, b56f3d5b or 33662211, in both assessed_source and current_source:
  - SR11 cites apps/web/src/components/survey-review/lib/surveyReview/model.ts:130-145.
  - SR52-SR56 cite …/labels.ts.
  - SR58-SR61 cite …/errorCopy.ts.
- The real path is apps/web/src/lib/surveyReview/*.ts. For example, model.ts:130-145 there shows the SR11 text. The ledger recorded drift "unchanged" against a missing file.
- SH-02 cites apps/web/src/app/page.tsx:6, but the text is on line 5 (line 6 is `}`). The error comes from the assessment, but the row is recorded as checked.
- Repro: `git cat-file -e 33662211:apps/web/src/components/survey-review/lib/surveyReview/model.ts` fails; `git show HEAD:apps/web/src/app/page.tsx | sed -n 5,6p`.

F2 (AS-3): 9 authority cells cite a real anchor that does not support the row. In R01, R03, C01, C10, E08 and LS-C03 the same anchor is also the proof_owner, so the row's future proof points at a test that does not cover it.
- PE-07: proposal-editor.test.tsx:24-42 tests the arithmetic and variations. The not-sent/_LABEL_CHARSET pin is at :83-88.
- R01: report-view.test.tsx:50-107 is the M5-T037 wide-street parity test. It says nothing about brief identity, not-saved or the audit appendix, which are pinned at workspace.test.tsx:56-85 and e2e architect-workspace.spec.ts:185-213.
- R03: report-view.test.tsx:278-320 is the printed identity-withhold test (A15's content), not the Source and review appendix. The captured/current notice is pinned at source-links.test.tsx:35.
- C01: condo-resolution-display.test.tsx:389-407 is the multi-lot records test (C02's content). The substitution and its heading are tested at :410-419, :548-563 and :781-792.
- C10: cites the same :389-407. The channel-disagreement test is at :435-455.
- E08: workspace.test.tsx:36-50 renders CalculationEvidence determinations (E13's content), not the evidence index. E13's own row then lacks this pin.
- LS-C03: compare-screen.test.tsx:90-180 tests document-vs-URL identity. The "No property selected" / invalid-BBL state is pinned in compare-entry.test.tsx:42-76 and e2e compare-journey.spec.ts:147-150.
- AD15: "PRD 4" is Users and roles. The fitting PRD text is §5 step 5 ("System displays possible matches if ambiguous"). §4 is also not in the assessment's §11 table, which the producer report says it drew from.
- M07: "DB-002/DB-032" is unrelated. DB-032 is the confirm-card record-address channel and DB-002 is the ZTLDB billing-BBL gap; neither concerns the condo-unit polygon. The assessment cites no DB for this row.
- Repro: `git show HEAD:<test> | grep -nE '^\s*(it|describe)\('`.

---

M5-T080 G3 report, part 3 of 4: BLOCKING F3, then advisories A1-A3.

F3 (AS-4): wrong facts in P0-RECONCILIATION.md.
- §4, D-076 row: it says the D-076-R003 post-B3 checkpoint was "REACHED, owner review QUEUED". But D-082-R001 records "Post-B3 checkpoint PASSED", and the same table's D-082 row says the work is released.
- §3, DB-046: marked RESOLVED. The backlog row (line 169) is still OPEN. Items (a)-(f) closed at the 251st acceptance (line 175), (g)/(h) are WATCH, and (i) is routed to the public-exposure packet. The assessment itself treats those remaining concerns as separate.
- §2, route map: it puts AC* under /property/confirm. In fact AddressConfirmCard renders inside AddressResolutionScreen on the /property search state (ArchitectEntry.tsx:65-67; AddressResolutionScreen.tsx:247), and its Continue link goes to /property/confirm (AddressConfirmCard.tsx:380). M* also renders on /property (AddressConfirmCard.tsx:305) and inside the proposal drawing wrapper.

ADVISORY
- A1, in-flight drift at 33662211. M5-T078 (5879cdfa) and M5-T079 (006f3589) landed after the pin.
  - The 26 flagged rows (DR-01..09, ME-01..17) are exactly the rows that cite the four edited files, so the flagging is complete.
  - But their spans have moved: ME-08..17 announcer 579→629, ME-03 168→200, ME-04 192→228, DR-01 180→243, DR-02 195→258, DR-08 151-158→178-185.
  - T078 also added new copy, e.g. "Point {i} is selected but has no coordinates yet" at ProposalOutlineMap.tsx:181. The re-pin must add states, not only move lines.
  - Test anchors in unflagged rows moved too: M01/M05's lot-outline-map.test.tsx:827-841 is now at :863 (+36).
  - Re-pin before P2/P4 use these rows.
- A2, boilerplate fields.
  - PART A uses one accessibility string on 128 of 138 L rows.
  - It uses one print_destination on 127 of 138 ("prints beside the number; full record in the report appendix"). That includes address-entry, map, drawing and legacy-confirm rows that have no print surface.
  - PART B says the legacy compare route has no print surface, while PART A says the legacy confirm rows print.
  - These fields are non-empty and marked P0-proposed, so they pass AS-2, but they are not yet real per-row destinations. P1 should resolve them.
- A3, authority form.
  - SH-07 has no class prefix, and its test covers the planned views, not the Documents card.
  - A01, LS-P01 and DB01 call blocker B-001 (the missing Supabase access token) a "reviewer finding".
  - PART B combines authority classes although the packet says "one of".
  - Many cells read "returned contract (<paraphrase>)" and name no contract; some sit on UI-only copy (DB21, DB09, SR57, SR21, SR47).
  - A05's exact pin is development-limits.test.tsx:457-463, and LS-C05's is compare-screen :125-133.
  - SR24 cites :71-84, which does not assert its text.

---

M5-T080 G3 report, part 4 of 4: advisories A4-A7, reproduction, corrections, verdict.

- A4, span precision.
  - LS-T02, LS-T04 and LS-T06 merge the assessment's disjoint spans into 90-180, 267-466 and 520-706.
  - ZC-04's span 30-53 runs past the end of its 49-line file.
  - M08 uses a derived 683-724; the actual branch is 694-702, with the text on line 699.
  - The LS-F rows use a mixed "3 paths :: P/C/E" form.
  - SR11's second path, DB08's "ui.tsx" and DB05's "CI labels 6-10" have no full path.
- A5, smaller reconciliation points.
  - DB-038 lists "(f) remain", but backlog lines 160 and 165 leave only (a), (d) and (e) open.
  - DB-043's note "contract AFTER the D-076-R003 checkpoint" is stale.
  - §1 credits the assessment with a "byte-identical" note that the assessment does not contain.
  - §5 gives steps and owning slices but names no concrete fixtures.
  - ZC* and ME* are ArchitectEntry views reachable from all three architect routes, not /property/compare only.
- A6: LS-C27→LS-C06 is really a two-target consolidation. The disclaimer's destination (LS-C05) appears only in prose.
- A7, method. Both parts inferred "unchanged" from byte identity of the whole apps/web tree. That proves nothing changed since dc5a763e. It does not prove each span is right (SH-02) or was copied correctly (the SR paths). PART A content-checked 3 spans and PART B 14, so the report's AS-3 "MET" overstates what was checked.

REPRODUCTION (read-only)
- Paths: `git cat-file -e <sha>:<path>` for each current_source path.
- Test anchors: `git show HEAD:<test> | grep -nE '^\s*(it|describe)\('`.
- Checkpoint: project-control/directives/D-082-max-envelope-first-and-map-drawing/requirements.json, D-082-R001.
- DB-046: `git show HEAD:docs/DISCOVERY_BACKLOG.md | sed -n '169p;175p'`.
- Route: ArchitectEntry.tsx:65-67; AddressConfirmCard.tsx:305 and :380.

REQUIRED CORRECTIONS (docs only, within allowed paths)
1. Fix the 10 SR paths (to apps/web/src/lib/surveyReview/…) and the SH-02 line (5).
2. Re-anchor authority and proof_owner for PE-07, R01, R03, C01, C10, E08 (and add the pin to E13), LS-C03, AD15 and M07.
3. In the reconciliation, correct the D-076 checkpoint status, the DB-046 status, and where AC* and M* appear in the route map.
After that, a delta check of only those rows and cells is enough.

M5-T080 G3 VERDICT: FAIL

END-OF-REPORT
