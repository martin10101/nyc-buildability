# M5-T065 G2 gate — producer self-check (recorded by the orchestrator)

G2 is the producer self-check gate; the CLI records it under --reviewer orchestrator (it
rejects the producer's own name). Recorded at the frozen submit identity 65faee54
(material d3793c2e).

## Producer self-check record (loop-3 worker, runs persistent3-local-10..12)

The loop worker built the 12-file map-drawing material and closed at STOP_FOR_OWNER with
the honesty-banner evidence handoff: the producer report separates DEMONSTRATED behavior
(§1–§2, file+line anchored) from OUTSTANDING evidence and the real scope gap (§3), and
states up front that click-to-place ON the map is NOT delivered and cannot be delivered
inside the packet's own compose-only wall (§3.1) — surfaced for a decision, never claimed
done. Fresh worker validation outcomes with explicit cwd (report §4-class discipline):

- `python -m ruff check .` (services/api) — exit 0
- bridge suite `tests/api/test_outline_bridge.py` — 38 passed
- full `tests/api` — 632 passed
- `python tools/modularity_check.py --check` (repo root) — exit 0

The producer report (part of the material) is the self-check artifact.

## Orchestrator scope disposition (recorded pre-submit; wave concurrence)

The [ORCH-SCOPE-DISPOSITION] (progress_log, 2026-09-21T00:52Z, Tier A): the delivered
surface — keyboard/numeric vertex entry + correspondence bridge + additive adoption — IS
this slice; the map-CLICK interaction layer is contracted as M5-T066 (claim seam c40c60db)
WITH the map surface in scope. The packet's scoping error (objective vs its own
forbidden_paths) is the orchestrator's, honestly surfaced by the producer. All wave
reviewers were briefed on the disposition and judged the delivered material against it:
G3 PASS ("did not fail for the absence of map click-to-place"; the honest surfacing
explicitly credited), G4 PASS (AS-1 residual recorded under the disposition), HJ PASS
(journey walked as delivered).

## Orchestrator corroboration at harvest + the CI chain

- Cherry-pick to d3793c2e: 12 files, ALL-MATCH LF-normalized digest verification post-pick;
  the material commit's own diff is exactly the 12 allowed_paths (G3 re-verified).
- CI red at d3793c2e (run 35549026019) root-caused NOT to the material: the SHARED e2e
  helper tabUntil false-matched <body> textContent at the Tab wrap; fixed [ORCH-CORRECTED]
  a3173987 in apps/web/e2e/helpers.ts — OUTSIDE the 12 task paths; the task's own e2e spec
  content is byte-stable.
- CI fully green at a3173987 (run 35554705276) and at the pushed submit-seam head 93b5943a
  (run 35555871945). The second red job at d3793c2e (exact-production-install age gate) was
  a PyPI connection-reset transient — fail-closed by design, green on the reruns.

**G2 verdict: PASS** (producer self-check complete, honest, and corroborated; recorded by
the orchestrator, role self_check).
