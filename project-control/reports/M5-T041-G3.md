# M5-T041 — G3 code review (verbatim reviewer return; transport entity-decoding only)

Saved by the orchestrator from the code-reviewer agent return, 2026-09-18 (seq-118 wave,
pinned at submission sha 843e23c4). Nothing added or removed beyond decoding transport
HTML entities.

---

I have everything I need. All checks are complete. Let me confirm the final structural fact — that PropertyOverview renders `ZoningContextPanel` outside `.architect-map-card` (confirmed above: the panel renders after the `</div>` closing `.architect-overview-grid`, so the M5-T040 scoped `within(siteCard())` queries stay singular). The review is complete.

---

# GATE REPORT — M5-T041 (G3 independent code review)

**Task:** M5-T041 — Address-flow + validator polish cluster: DB-024 copy/a11y quartet (a–d) + DB-025(e) web-validator wording correction
**Reviewed identity:** material commit `740b6f2f` (11 files, +818/-32), pinned submission `843e23c4` on `candidate/D-024-mrl-option-b`
**Reviewer:** code-reviewer (read-only)
**Verdict: PASS** (2 ADVISORY notes; no blocking defects)

Material identity verified by content-addressed `git show 740b6f2f` (immutable), so the branch HEAD having advanced to `412edd98` past the pin does not affect this review. Confirmed `git diff 740b6f2f..843e23c4 -- apps/ services/ packages/` is empty (later commits are control-plane/reports only).

## Findings

**F1 — Scope discipline: clean. (informational)**
All 11 changed files are inside `allowed_paths`. `apps/web/src/lib/address-search.ts` is untouched (not in the diff); no `services/api` path; no schema/generated-TS file touched (`rule-evaluation-contract.ts` is the handwritten runtime validator that *imports* generated types, listed in allowed_paths — its executable code is unchanged, see F5). The three allowed-but-optional files (`AddressResolutionScreen.tsx`, `address-resolution.test.tsx`, `workspace.test.tsx`) were legitimately not needed.

**F2 — DB-024(a) non-circular recovery: correct, no stale/race. (informational)**
`AddressAutocomplete.tsx:112` `fullSearchActive = searchResult !== null && searchResult.query === text`; `:113` `outcome = fullSearchActive ? searchResult!.outcome : typedOutcome`; `:149` selects `(fullSearchActive ? FULL_SEARCH_ERROR_MESSAGES : ERROR_MESSAGES)[outcome.reason]`. The message table and the displayed outcome are gated by the *same* flag, so a full-search error can only ever be paired with `FULL_SEARCH_ERROR_MESSAGES` and a typed error with `ERROR_MESSAGES` — no cross-wiring. Stale full searches are triple-guarded: `onChange` (`:162`) sets `searchResult(null)` + `cancelFullSearch()`; the resolve callback in `runFullSearch` drops superseded/aborted replies via the `fullSearchSeq` monotonic guard; and even a surviving stale write fails the `query === text` equality. All six `FULL_SEARCH_ERROR_MESSAGES` entries omit `FULL_ADDRESS_SEARCH_LABEL` and name "manual entry"/"BBL". The twin test correctly proves the typed-path copy deliberately still names the button (that path is genuinely non-circular).

**F3 — DB-024(b)/(d) shared constants: byte-identical, no cycle. (informational)**
`ABSENT_BBL_MAP_LINK_NOTE` and `ZOLA_LOT_LINK_LABEL` are single exports homed beside `FULL_ADDRESS_SEARCH_LABEL` in `AddressAutocomplete.tsx:22,32`. All three surfaces render the constant, not a literal (`AddressConfirmCard.tsx:150,151`; `PropertyOverview.tsx:60,72`; `ZoningContextPanel.tsx:59,63`). No import cycle: `AddressAutocomplete.tsx` imports only react/`address-api`/`address-search`/`use-address-suggestions` — none of its three consumers. Module boundary is contract-sanctioned ("home it with … `FULL_ADDRESS_SEARCH_LABEL`") and acyclic.

**F4 — DB-024(c) aria-hidden: correct. (informational)**
Address-hint `↗` and ProfileViews "Inspect calculation evidence" `→` are each wrapped `<span aria-hidden="true">` (`AddressAutocomplete.tsx:199`; `ProfileViews.tsx:53`); accessible names are arrow-free and asserted via `getByRole("link", { name })`.

**F5 — DB-025(e) validator: wording-only, zero behavior change, and the new wording is accurate. (informational)**
The `rule-evaluation-contract.ts` diff touches only the module doc block and the function JSDoc — no executable line changed (`validateRuleEvaluationDocument` only calls `checkEnum`/`problems.add` on named documented keys, never iterates `Object.keys(body)`, returns `ok:true` on empty problem list). The corrected claim ("unknown/extra top-level key is NOT rejected; no client-side additionalProperties enforcement; server owns the closed schema") matches the actual code. All recorded 1.0.0/1.1.0 fixture verdicts are unchanged; the additive `contract-versions.test.ts` DB-025(e) case documents the pre-existing behavior.

**F6 — Copy-change sweep: complete, not accidental. (informational)**
Grep across `apps/web` (including `e2e/`) for the old strings `Open in ZoLa`, `View this lot on the city`, `this result did not provide` returns only two *comment* mentions (`AddressAutocomplete.tsx:29`, `address-confirm.test.tsx:468`) — zero surviving assertions. The one e2e status assertion (`architect-workspace.spec.ts:226`) asserts the *typed-path* `ERROR_MESSAGES.rate_limited`, which this packet did not change. Out-of-scope M5-T040 test `development-limits.test.tsx:185,198` uses `within(siteCard())` scoped to `.architect-map-card`; `PropertyOverview` renders `<ZoningContextPanel>` *outside* that section (after the `.architect-overview-grid` div), so the newly-shared "Open ZoLa" label adds no second match inside the scoped card — the singular queries stay valid. Corroborated by green CI.

**F7 (ADVISORY) — objective vs binding-scope wording mismatch on PropertyIssuesSummary.**
The task *objective* names "ProfileViews/PropertyIssuesSummary arrow", but the binding "SCOPE (binding; DB-024 letter map)" (c) names only the address-hint arrow and the ProfileViews "Inspect calculation evidence" arrow — both correctly wrapped. `PropertyIssuesSummary` still has two unwrapped decorative `→` glyphs (`PropertyOverview.tsx:26` "Review conflicting source values →", `:34` "Review missing inputs →"), plus "All property facts →" (`:82`). The binding letter-map is satisfied; flagging so the orchestrator can confirm no residual HJ DB-024(c) finding was intended for those arrows. Not blocking (binding scope wins on internal conflict).

**F8 (ADVISORY) — producer report §3 slightly overstates the import baseline.**
Report §3 says the three consumers "already import from the architect address module"; the pre-image (`740b6f2f^`) shows none of `AddressConfirmCard`/`PropertyOverview`/`ZoningContextPanel` imported `AddressAutocomplete` before this commit — the imports are new. Harmless (the boundary is contract-sanctioned and cycle-free), but the "already import" phrasing is inaccurate.

## Evidence (read-only commands run)
- `git show 740b6f2f --stat` / `git show 740b6f2f -- apps/ services/ packages/` — 11 files, all in allowed_paths; full code diff inspected.
- `git diff 740b6f2f..843e23c4 -- apps/ services/ packages/` — empty (later commits control-plane only); `git diff --stat 740b6f2f..843e23c4` shows only reports/state/tasks JSON; `git rev-parse HEAD` = `412edd98` (past the pin — material verified content-addressed).
- `git show 740b6f2f:apps/web/src/components/architect/AddressAutocomplete.tsx` — full `fullSearchActive`/`runFullSearch`/`onChange` lifecycle and constant/message tables read.
- `git show 740b6f2f:apps/web/src/components/architect/PropertyOverview.tsx` — confirmed `ZoningContextPanel` renders outside `.architect-map-card`.
- `git show 740b6f2f^:<file>` for the three consumers — confirmed the AddressAutocomplete imports are new (F8).
- Grep `Open in ZoLa|View this lot on the city|this result did not provide` over `apps/web` — 2 comment-only hits (F6).
- Grep `ZoLa|valid BBL|did not provide|Search this full address|manual entry|full-address` over `apps/web/e2e` — only the unchanged typed-path assertion at `architect-workspace.spec.ts:226`.
- Grep `Open ZoLa|architect-map-card|siteCard` in `development-limits.test.tsx` — scoped queries at `:185,:198`.
- Read `M5-T041.json`, `M5-T041-producer-report.md`, `M5-T041-ci-evidence.md` — CI (incl. web + web-e2e) `success` at `740b6f2f` (run 35402081543), modularity check exit 0. Web behavior not run locally (thin-client); relied on orchestrator-captured CI evidence per protocol.
