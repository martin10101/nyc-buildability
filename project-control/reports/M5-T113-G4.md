# M5-T113 — G4 test review (qa-engineer "qa-t113", read-only)

> Transmission history: pinned at 7b3686f0, delivered as 3 SendMessage part(s) ending with END-OF-REPORT;
> no truncation. Joined verbatim by the orchestrator (transport wrapper tags removed only). Verdict PASS; five producer mutants +
> two cap-boundary sweeps red; golden revision exactly scoped; no flake in five runs; four ADVISORY notes.

---

M5-T113 G4 independent test review (qa-engineer, READ-ONLY) — part 1/3.

HEAD pinned at start = 7b3686f0 (as expected). Material = fcd0033e vs parent 0c24a8b5: EXACTLY the 7 packet files (sheet_objects.py, sheet_marked_content.py NEW, sheet_interpreter.py, sheet_reader.py, test_sheet_content_features.py NEW, test_sheet_reader_split_equivalence.py, producer report); test_sheet_reader.py untouched (a permitted subset). No forbidden path touched (verified via `git diff --name-only fcd0033e^ fcd0033e`). All runs go through the orchestrator 3.11 shim from services/api; I confirmed the shim stubs only package __init__ bodies and the REAL leaf modules execute.

Note: the read-only guard blocked BOTH the Write tool and bash file-writes to my scratch dir AND my agent-memory, so I ran every mutant inline via `python -c` (boot shim + importlib-load the producer test module by path) — no file written. Per the dispatch I skip the memory write and am not asking anyone to write it.

BLOCKING findings: NONE.

Test suite (shim): full tests/drawings = 252 passed. Per file: test_sheet_content_features 28, test_sheet_reader_split_equivalence 9, test_pdf_object_streams 44 (read-only, UNCHANGED), test_sheet_reader 46.

Duty 1 — each AS mutant fails for the right reason. I re-ran the producer's mutants IN-PROCESS against the CONSUMING namespace and confirmed the ACCEPTANCE test (not just the self-contained load-bearing test) flips RED:
- M1 sheet_objects.apply_predictor -> refuse: AS-1 png-predictor-draws -> RED
- M2 sheet_interpreter.read_inline_dict -> refuse: AS-2 BDC-inline-dict-draws -> RED
- M3 sheet_reader.MAX_MARKED_CONTENT_DEPTH=1: AS-2 nested-dict-draws -> RED
- M4 sheet_reader.MAX_MARKED_CONTENT_BYTES=4: AS-2 draws -> RED
- M5 sheet_reader._scan_only_refusal->None: AS-3 scan-only-reachable -> RED
All five target the consuming namespace correctly (read_inline_dict is imported by-value into sheet_interpreter; the caps are read from sheet_reader globals at read_sheet() call time and threaded into the interpreter limits) — no false survivors.

My OWN cap-boundary mutants (sweeps): byte cap on `<< /MCID 0 >>` (13 source bytes) -> REFUSES at max_bytes<=12, DRAWS at >=13 (exact `length > max_bytes`). Depth cap on depth-2 nesting -> REFUSES at max_depth<=1, DRAWS at >=2 (exact `depth > max_depth`). Both are exact off-by-one boundaries.

(continued in part 2/3)

---

M5-T113 G4 — part 2/3.

Robustness (my probe, not required but material): read_inline_dict terminates on 10 adversarial inputs — `<< } >>`, `<< ) >>`, unclosed `<<`, all-slashes (hits byte cap), binary junk, stray single `>`, single `<` hex string inside, nested arrays, `%` comments — each either refuses-typed or returns a valid span; NO infinite loop (5s watchdog each). lex_primitive always advances or errors, and the byte cap is a backstop. No DoS finding.

Duty 2 — golden revision is exactly-scoped. The diff changes EXACTLY the two intended cases: `refuse_decode_parms` (removed) -> `decode_parms_png_predictor` (now DRAWS) + `refuse_decode_parms_tiff` (TIFF predictor still refused), plus `_OVERALL` recomputed and corpus count 62->63. No other _GOLDEN entry changed (diff-verified). I did not need to check out 275ef85f: the equivalence test (9 passed) recomputes ALL 63 case digests from the CURRENT module and compares to the pinned golden, so the 61 unrevised cases are proven byte-identical — any behavior regression in the three edited source files would have reddened it.

Two non-tautological anchors on the revised cases:
- the new `decode_parms_png_predictor` digest (f1b2f835…) EQUALS the independent `flate_content` case's digest — i.e. the PNG-predictor content stream round-trips to the identical drawn output as a plain flate stream drawing the same line. A pure "capture whatever the code emits" golden could not produce that cross-match.
- the TIFF refusal is ALSO independently pinned in content_features by an explicit `feature=="predictor"` + `"TIFF" in detail` assertion, not only by the equivalence digest.
- test_corpus_shape_is_nontrivial guards against a vacuous all-pass (asserts len==63, docs>=25, refs>=25).

apply_predictor is a SINGLE implementation (sheet_objects.py:387) reused by _decode_stream (sheet_objects.py:190) — I grepped; there is no second predictor (packet requirement met). Its M5-T103 safety bounds are inherited unchanged: empty-data early return before row_len is derived; `row_len > absolute_cap` refused BEFORE any working buffer is sized (line 418-419); and _decode_stream charges the inflated bytes against the document-wide decoded-bytes budget BEFORE calling the predictor (test_as1_predictor_stream_shares_the_document_decoded_budget confirms this — passes).

(continued in part 3/3)

---

M5-T113 G4 — part 3/3.

Duty 3 — fixture realism. Predictor fixtures build a faithful per-row-filter-tag + flate stream with `/DecodeParms << /Predictor N /Columns C >>`; marked-content fixtures use `/OC << … >> BDC … EMC` (AutoCAD layers map to optional-content groups — realistic for PDFMaker output). Crucially, the producer's in-process ATTRIBUTION PROBE on the six real files shows items 1-2 genuinely require the predictor (else "decode parameters") and items 3-6 genuinely require inline dicts (else "marked-content dictionary") — both feature paths are validated as REACHED on real files, so the fixtures are anchored to real behavior, not synthetic-only. No case looks like a synthetic-only artifact that real files would never hit.

Duty 4 — flake. Ran the read-only test_pdf_object_streams 5 times: 44 passed every run. No tracemalloc flake (DB-076 h) observed.

Duty 5 — honesty. The real-corpus table honestly reports 0/6 read fully with distinct plausible next blockers (operator budget 1-2, `sh` shading 3, `BI` inline image 4, scan-only 5-6); the headline explicitly states nobody may tell the owner real architect PDFs can be read. Scan-only reachability is proven on synthetic resolver-path files (M5 + AS-3 tests, incl. a real-raster-image page). DB-076(e) is DISCLOSED not fixed, and the rationale is SOUND: I confirmed the read-only test_pdf_object_streams.py::test_scan_only_page_is_typed_refusal (line 443-448) genuinely pins `feature=="scan only"` for a TEXT-ONLY resolver doc, so relabeling would break a must-pass-unchanged suite. Nothing overclaims.

Scope (AS-5): 7 files all within allowed_paths; test_pdf_object_streams unchanged (44); modularity exit 0 (sheet_interpreter 690 < 750; new sheet_marked_content 171); zero new deps; unwired (no route/app change).

ADVISORY (non-blocking):
1. Fixtures are synthetic (offline requirement) — realism rests on the attribution probe, not the fixtures. Acceptable and documented.
2. The report cites the predictor digest as equal to "the plain `line` case"; it matches `flate_content` (same digest). Immaterial naming nit.
3. The report's self-check says "245 passed"; current-HEAD shim = 252 (sibling drawings suites grew at later commits). Not a defect; all green.
4. Items-1-2 per-document 200k operator budget and the DB-076(e) asymmetries are recorded OPEN QUESTIONS for the orchestrator/owner (out of P2 scope) — no test-adequacy impact.

M5-T113 G4 VERDICT: PASS
END-OF-REPORT
