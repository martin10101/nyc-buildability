# M0-T143 — G0 readiness (administrative)

- **Task:** M0-T143 — D-024 Amendment 46: Codex `--output-schema` strict Structured-Outputs
  subset repair, controller-side guarantee preservation, and reviewer failure observability.
- **Authority:** owner directive captured verbatim as
  `project-control/directives/D-024-fable-codex-loop/source-046-amendment.md`
  (requirements D-024-R700..D-024-R713, appended append-only; registry validated by
  `tools/validate_directive_compliance.py --check` at capture).
- **Qualifying evidence (supervisor-freeze §2):** reproduced live defect — canary-b5-02r2
  `review_unavailable` (runtime dir `9aca7075…`, audit seq 48: `codex_review_failed`,
  `no_decision`, exit 1) — plus the owner's diagnostic probe
  `%LOCALAPPDATA%\NYCBuildabilitySupervisor\ctl24-activation\codex-schema-probe-20260902-191655532`
  capturing the provider error verbatim: `invalid_request_error` / `invalid_json_schema`,
  status 400, param `text.format.schema`, message
  `In context=('properties', 'evidence_ref_ids'), 'uniqueItems' is not permitted.`
  Explicit D-024 requirement IDs cited: D-024-R700..R713 (Amendment 46).
- **Scope check:** one bounded follow-up task (R700); accepted tasks M0-T141/M0-T142 are not
  reopened; M0-T140 stays claimed awaiting its canary. Healthy surfaces (auth, model
  availability, executable identity, budgets, Fable worker leg, repository state) are out of
  scope by owner prohibition (R701).
- **Dependencies:** none new. The task edits `tools/agent_supervisor/**` under the
  defect-only maintenance lane with standard gates G0/G2/G3/G4 plus directive-compliance
  verification; the suite baseline re-establishment duty applies.
- **Readiness verdict:** READY — scope, authority, qualifying evidence, gates, and reviewer
  roster (code-reviewer, qa-engineer, directive-compliance-verifier) are all named; the
  producer is the orchestrator session in worktree `ctl24`.
