# M5-T101 — G3 code review (code-reviewer "cr-t101", read-only)

> Transmission history: pinned at c2c4f523, delivered as two SendMessage parts (1/2, 2/2) ending with END-OF-REPORT;
> no truncation. Joined verbatim by the orchestrator (transport wrapper tags removed only). Verdict PASS: the moved helpers
> are byte-identical and re-exported as the same objects; bounds charged before retention; one sanitised log site.
> Advisories A1-A4 (cid length bound, naive-deadline typing, metadata-before-deadline, the crude not-wired grep) are
> routed to the wiring packet at the accept seam.

---

M5-T101 G3 code review (read-only) — footprint connector wiring-hardening. Part 1/2. HEAD pinned c2c4f523; material commit 6a0f7f53.

VERIFICATION (all OBSERVED by me from services/api):
- ruff `All checks passed!`; `pytest tests/connectors/test_building_footprints_arcgis.py -q` -> 131 passed (117 pre-existing + 14 PKT-C); `modularity_check --check` exit 0.
- AS-1 split byte-identical: the moved helpers (_ring, parse_footprint_geometry, classify_query_relation, _safe_repr, _is_real, _finite, _signed_area, FootprintPart, GEOMETRY_* consts, COORD_ABS_MAX_FT) diff byte-for-byte between the 6a0f7f53^ connector and building_footprints_geometry.py. Re-exports are the SAME objects (test:982-984 asserts `is`); geometry imports nothing back (AST check:985-989; grep confirms one-way, acyclic). Connector body references no un-imported name. Test-file diff is PURELY additive (0 removed/modified lines) -> the 117 existing tests are genuinely unchanged.
- AS-2: vertex cap at _parse_page:764-770 BEFORE page.append:775; cumulative-bytes at _collect_pages:964-970 BEFORE collected.extend:981; _check_deadline:952 is the first stmt in the paging loop (before every page); interactive caps attempts at fetch:1024-1025 (INTERACTIVE_MAX_ATTEMPTS=1). ResourceBoundError/DeadlineExceededError subclass the connector error -> caught typed at 1050, no ERROR log.
- AS-3: single log site (logger.error:1061, internal_error path only); logs a FIXED _INTERNAL_ERROR_LOG_MESSAGE:428, never str(exc); class name + cid both sanitized (_sanitized_bounded:438, _safe_correlation_id:448); _CONTROL_CHAR_DELETE:435 covers C0/DEL/C1 incl CR/LF; typed refusals skip the ERROR log (1050-1051). Tests cover the class-__name__-trailing-newline case (test:1074) and the omission-vs-sanitization secret (test:1088) end-to-end.
- AS-4: zero ground stays real 0.0 flagged ground_elevation_zero_unverified (_ground_attr:844-847); GROUND_DATUM_BASIS:164-170 discloses both definitions, chooses neither; untrusted_text_fields+notice on the record:340-341. Datum code pre-existed M5-T089 (old connector already has GROUND_DATUM_BASIS); only the untrusted-text decl is new -> matches the report's honest deviation.
- AS-5: docs/research/source-registry-drafts/building-footprints.json mirrors the dcm/dtm shape, primary source_id == SOURCE_ID, cites M5-T087 throughout, RQ-1/RQ-2 listed open.
- AS-6: zero new deps (stdlib+shapely+app, no `import re` in either module), unwired (no app importer; only self-ref is the logger-name string), exactly the 5 allowed paths, no forbidden path touched.

BLOCKING findings: none. Advisories + verdict in Part 2/2.

---

M5-T101 G3 review. Part 2/2. All file:line refs are services/api/app/connectors/building_footprints_arcgis.py unless noted.

ADVISORY (none blocking):
- A1 (:455 / :1061-1064): _safe_correlation_id strips control chars but does NOT length-bound the cid. The class/message log fields are bounded via _sanitized_bounded (200 chars), but the cid is logged and echoed in full, so a very long caller correlation_id can still dump an unbounded log line. In-theme for a hardening packet; consider routing cid through _sanitized_bounded (or a length cap) at the log site. Not a stated AS-3 criterion (the forging vector is closed) and no untrusted caller exists while unwired.
- A2 (:722 / fetch deadline param :1002): a tz-NAIVE `deadline` vs the tz-aware default clock raises TypeError -> caught as unexpected -> internal_error + an ERROR log. Docstring says "tz-aware" but _validate_inputs never rejects a naive deadline. For the future wiring packet, reject it as a typed disallowed_request. Minor.
- A3 (_collect_pages): the deadline is checked before each query PAGE but not before the metadata fetch, so a past deadline still issues one metadata request (test:1020 confirms transport.calls==[metadata]). Consistent with the "before each page" wording; noted for completeness.
- A4 (producer DISCOVERY — I AGREE): test_as5_connector_is_not_wired_to_any_route_or_module (test:806) is a crude substring grep over app/**; it will fail BY DESIGN once PKT-E/PKT-H imports the connector, and it already forced building_footprints_geometry.py:11-13 to avoid naming the connector. Correctly routed as a note-for-later, out of PKT-C scope; replace with an import-graph/AST check before wiring lands.

MODULARITY: 865 SLOC / 47 symbols — both report-only warnings (symbol_ceiling 47>40, review_signal>750), not CI failures (modularity exit 0). Justification acceptable: one cohesive connector (typed-error taxonomy + result contracts + fetch/parse/paging pipeline), count dropped 50->47 after the required geometry split, below the 1000 hard cap. If it grows further, extract the attribute-typing helpers or the error taxonomy.

Assessment: clean seam, honest evidence (verified deviations), strong red/green guards + end-to-end no-leak probes, exact scope, zero new deps, unwired.

M5-T101 G3 VERDICT: PASS
END-OF-REPORT
