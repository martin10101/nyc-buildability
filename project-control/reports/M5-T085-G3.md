# M5-T081 + M5-T085 — G3 independent code review (code-reviewer "cr-cad", read-only; joint review)

> Transmission history: one joint review of M5-T081 and M5-T085 pinned at 1414e1a7, delivered as three
> SendMessage parts (1/3, 2/3, 3/3) ending with END-OF-REPORT; no truncation, nothing re-requested.
> Joined verbatim in delivery order by the orchestrator. The same joint report is saved as
> M5-T081-G3.md and M5-T085-G3.md (each task's gate cites it).

---

G3 CODE REVIEW (part 1/3) — M5-T081 (DXF writer). Repo at pinned HEAD 1414e1a7 (verified). Scope clean: `git show --stat 4fd58a98` = exactly the 3 allowed_paths; no route/__init__/web/sibling change.

F1 — BLOCKING (required correction): wrong drawing-unit code. `dxf_writer.py:72` sets `INSUNITS_FEET = 2` ($INSUNITS = 2 = INTERNATIONAL feet), emitted at `:441-442`. The committed official-source research `docs/research/dxf-format-reference-2026-09.md` (§0.2, §3, §7; primary sources [HDR2026]/[HDR2017]) states US Survey Feet is a distinct code — `$INSUNITS = 21` — and verbatim: "a writer that means survey feet should set $INSUNITS 21, not 2 (international feet)"; its prescribed G1 check asserts `$INSUNITS == 21`. The geometry is EPSG:2263 US survey feet, so the header declares the WRONG unit.

Corroborating defects: (a) the justifying comment `:66-71` ("$INSUNITS cannot distinguish [survey] from international feet") is factually FALSE per the repo's own research — code 21 exists precisely to distinguish them; (b) the file self-contradicts — header says international Feet while ANNOTATION `:111` prints "US SURVEY FEET".

The packet's FORMAT AUTHORITY input is binding ("never guess ... follows the Autodesk DXF Reference") and principle #3 forbids guessing units. This is squarely G1's format-authority remit and G1-as-specified would independently fail the current value; I flag it BLOCKING at G3 too because it is a measurement-honesty/correctness defect visible in source and ALREADY resolved by committed research (not speculation).

Fix: `INSUNITS_FEET = 21` (rename e.g. `INSUNITS_US_SURVEY_FEET`), correct the comment to cite code 21 per the research, re-anchor `GOLDEN_SHA256` (test:47 — header byte `2`→`21` changes the digest), and update the `# 2 = feet` comment at test:214. Note weak-test angle: `test_as2_header_declares_drawing_unit` binds to `str(d.INSUNITS_FEET)`, so it passes for ANY value and did NOT catch this — external review is the only guard on the specific code.

---

G3 CODE REVIEW (part 2/3) — M5-T081 advisories + verdict; M5-T085 findings.

T081 advisories (non-blocking):
- A2: `dxf_writer.py` is 739 raw lines (warn 600 SLOC). Producer reports modularity exit 0 (comments/docstrings dominate); responsibilities are cohesive (errors/sanitizer/model/serializer/builder). Acceptable now; consider splitting builder from serializer if it grows.
- A3 (strength, not a defect): the writer uses heavy POLYLINE/VERTEX/SEQEND, DIVERGING from the research's LWPOLYLINE suggestion (§4.2/§7). This is CORRECT — LWPOLYLINE is an R14+ entity, invalid in a strict R12/AC1009 file; POLYLINE/VERTEX is the right R12 primitive.

T081 verified strengths: single sanitizer choke point (`_GroupCodeStream.pair`→`_sanitize_value`; every value incl. numbers routes via `.real`→`.pair`; no `_lines.append` bypass); fail-closed (validate-then-assemble-in-memory, no partial output); injection test + necessity monkeypatch (test:274/289); honesty labels + claim-class grep (test:310); determinism (LF, `{:.6f}`, -0 normalize, golden test:167). All OTHER format constants match the research: $ACADVER=AC1009 (§1), 3DFACE 10-13/20-23/30-33 (§4.4), TEXT (§4.5), LAYER 2/70/62/6 (§5.1), LTYPE 72=65 (§5.2), section order HEADER/TABLES/ENTITIES/EOF (§6).

M5-T081 G3 VERDICT: FAIL

—

M5-T085 (PDF sheet writer). Scope clean: `git show --stat 519bc360` = exactly the 3 allowed_paths; `app/documents/` consumed READ-ONLY (test sideload, no modification). Zero new deps (math, dataclasses).

No BLOCKING findings. Advisories:
- A1: `_validate_ring` (`pdf_sheet_writer.py:201`) does `float(vertex[0])`/`len(vertex)` uncaught — a non-numeric/non-sequence coordinate raises ValueError/TypeError instead of a typed `SitePlanRefusal`. Declared input type is float and callers pass canonical 2263 geometry, so this is a low-risk defensive gap, not a fail-closed violation of the declared contract.
- A2: no claim-class-word screen on caller-supplied `address`/`bbl` passthrough (title block `:302-303`). T085's AS-5 does NOT require one (only fixed stamps + no true-north) and all fixed strings are clean, so it is compliant — but a future wiring packet feeding untrusted address text should add the claim-class screen T081 has.

---

G3 CODE REVIEW (part 3/3) — M5-T085 verified strengths + verdict.

- Single escaper `_escape_pdf_text` (`:329`) is the SOLE text path: only `_draw_text` (`:322`) calls it, and every label (edges/north/scale-bar/title-block) routes through `_draw_text`. ASCII-sanitise (non-0x20-0x7E→`?`) then backslash-escape `( ) \` — literal-string breakout closed; escaper-bypass in-process mutant reddens the round-trip (test:236); special-char round-trip is faithful+reversible (test:227).
- Deterministic: `generated_at`/`generator_version` are INPUTS not a clock (dataclass `:107-108`); `_num` (`:349`) rounds-then-trims, only IEEE `+ - * /`+`math.sqrt` feed it; golden sha256 pinned (test:119) + byte-equality test (test:115).
- PDF structure per ISO 32000-1 §7.5 (`_assemble_pdf :361`): 5 objects, classic xref with 20-byte entries + exact 10-digit offsets, trailer/startxref/%%EOF; independent xref-offset parse asserts each lands on `N 0 obj` (test:123) + offset-corruption mutant (test:261).
- Measurement honesty: dimensions = `math.sqrt` over EPSG:2263 inputs only (`_label_edges :266`); 30x100 lot + 20x60 footprint → 30.00/100.00/20.00/60.00 (test:169); scale, scale-bar (1 inch = feet_per_inch ft) and printed "1 in = N ft" are mutually consistent.
- Honesty: grid-north arrow "GRID N (EPSG:2263)" + title "EPSG:2263 grid north", never "true north" (test:287); "PROPOSED - NOT A CITY RECORD" + "Not for construction - professional review required" always present, incl. the no-building case (test:278).
- Round-trip counts internally consistent (segments 17, rects 2, text_runs 19) and [OBSERVED] via the real reader sideload on local 3.11; the plain-import path is the CI-3.12 authority (golden + structure tests need no reader and run on 3.11).

M5-T085 G3 VERDICT: PASS

Summary: T081 FAIL (one BLOCKING correction — $INSUNITS 2→21, ~4-line fix + golden re-anchor; everything else PASS-quality). T085 PASS (2 advisories only). Both scopes clean; zero new deps; sanitizer/escaper choke points sound; honesty labels correct.

END-OF-REPORT
