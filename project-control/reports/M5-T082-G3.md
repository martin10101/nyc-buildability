# M5-T082 — G3 independent geometry review (geospatial-engineer "geo-mass", read-only)

> Transmission history: one SendMessage transmission pinned at 1414e1a7, ending with END-OF-REPORT; no
> truncation. Saved verbatim by the orchestrator.

---

M5-T082 G3 GEOMETRY REVIEW (independent, read-only). Pinned HEAD 1414e1a7 verified (git rev-parse matches). Material b9b0d31e touches only the 3 allowed paths; max_envelope.py byte-untouched (last M5-T064 409a8a7d). Method: static review vs docs/3D_MASSING_ENGINE_ARCHITECTURE.md §2-4/10 + packet; independent no-repo-import numeric re-derivation (inline python); shapely edge-case probes; ran the suite (26 passed).

GEOMETRY CORE — all verified PASS:
- CRS frame complete/correct (massing_model.py:79-86,536-552): EPSG:2263 NAD83/NY-Long-Island ftUS; axis_order easting_northing matches the data magnitudes (x~1e6, y~2e5 for NYC); local origin at parcel centroid; exact world->local = subtract origin (offset [-ox,-oy,0]); precision grid 1e-6. Containment computed in world 2263.
- Ear-clip concave-safe: my independent re-derivation of LSHAPE -> 4 tris = n-2, cap area 3000.0 == polygon area, every triangle centroid inside the L (no notch-spanning). Collinear collapse (:219-233) is area-preserving.
- Prism closed + outward: side-edge winding yields outward normal (dy,-dx); signed volume 28800 = area x height > 0; per-prism directed-edge manifold holds. Volume reduced in LOCAL coords (:347-355) for conditioning — sound at NYC magnitudes.
- Containment tolerance sensible + DECLARED (FOOTPRINT_OUTSIDE_LOT_TOL_FT 1e-6, :107,593-606): shapely-confirmed — footprint==lot and shared-edge allowed (zero-lot-line), 1e-7 ft outside tolerated, 0.001 ft outside refused footprint_outside_lot, never clipped.
- Plates from shapely 2263 ring (translation-invariant); GFA=Σplates, height=Σfloors reconcile. B0 levels->bands faithful (floor_count x floor_to_floor, z from 0, per-level outlines, fail-closed on non-positive/empty/over-cap).
- Generated-option path mirrors the REAL max_envelope.as_dict (candidate dict|None + candidate_placement.detail; None -> typed no_generated_candidate) — verified vs max_envelope.py:368-386 and _build_candidate:848-863.

FINDINGS (all ADVISORY, none blocking):
F1 (massing_model.py:82) vertical_unit=us_survey_foot: B0 floor_to_floor_ft declares no foot-variant; module assumes it equals the horizontal CRS unit. Declaring vertical==horizontal is the internally-consistent choice (volume dimensionally coherent); US-vs-international-foot 2 ppm delta is immaterial at building heights (~0.0002 ft/100 ft). Note the undeclared-B0-unit assumption for the wiring packet.
F2 (test-gap, :216) CW-input footprint normalization is not exercised — all fixtures are CCW. I confirmed the sign logic (ccw +2400 / cw -2400) and that a mis-oriented ring fails closed at _triangulate; add a CW-footprint test with the MapPLUTO wiring.
F3 (:593-606) per-level outline is contained-checked against the LOT, not nested within the floor below, so a within-lot cantilever/overhang is permitted. Acceptable for this slice (buildable region ~= lot); revisit with real buildable-region primitives.
F4 (= producer discovery, CONFIRMED) stacked prisms share coincident interface caps. My check: the index-based closure test returns manifold=True per-prism AND for a naive union (caps have distinct indices), so it structurally cannot catch the position-space double-surface — the real GLB/DXF weld risk. Correctly routed to a later packet; not a defect for per-floor solids.
F5 (:340) exterior_walls (B0 metadata) are not used; side walls re-derived per ring edge — geometrically complete for a solid prism; party/exterior distinction lost in the mesh (fine for massing).

Producer's other discovery (open MapPLUTO rings, string coords) verified robust: string coords fail closed as non_finite; parse-to-float belongs in the wiring packet — correctly characterized.

M5-T082 G3 VERDICT: PASS
END-OF-REPORT
