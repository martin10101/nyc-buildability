<!-- Orchestrator capture: security-reviewer G5 return for M5-T045 saved VERBATIM
     (transport entity-decoding only). Pinned at frozen head 2b851c18; reviewer verified
     byte-identity to HEAD and the 15-file change-set scope proof. -->

I have completed my independent G5 security/privacy review. All primary source files are byte-identical between the frozen SHA `2b851c18` and current HEAD `7c935d25` (verified via `git diff --stat`, empty output), and the change-set is exactly 15 files, all within `allowed_paths`. Here is my gate report.

---

# G5 Security/Privacy Gate Report — M5-T045

**Task:** M5-T045 — Condo billing-BBL live wiring (DB-029)
**Reviewer:** security-reviewer (read-only, ADR-005)
**Reviewed identity:** frozen head `2b851c18144aee5cd84cbfa9b011dfa5346c7a0c` (branch `candidate/D-024-mrl-option-b`)
**Live HEAD at review:** `7c935d25ebd1ba842e4d0418fd82f44963b79ef1` — T045 production surface verified **byte-identical** to the frozen SHA via `git diff --stat 2b851c18 HEAD -- <surface>` (empty output); later commits are disjoint control-plane only.
**Material change-set:** cherry-pick `28082288`, 15 files / +2276 −67. CI-covered at `21ee7379` (run 35431367744 success, incl. web-e2e; secret-scan + context-budget success).
**Verdict: PASS** (no blocking findings; 3 advisory)

---

## Method

I re-derived each of the 7 named G5 surfaces from source at the frozen identity rather than trusting the producer's conclusions. I read `condo_base_lot.py`, the `live_provider.py` condo step, the `zoning_crosscheck.py` `condo_resolution_report`, the `dtm_condo_soda.py` riders, the four web components, the registry draft, and the fixture/test bodies. I ran a secrets/PII sweep across the change-set and a scope-proof over the material commit.

---

## Findings by surface

### 1. `condo_base_lot.py` — seam policy (PASS)

- **Canonical BBL discipline / no raw interpolation.** The seam performs **zero** URL construction — it only classifies (`classify_lot`, `condo_base_lot.py:164`) and delegates to the injected resolver (`:185`). No new network surface, no string interpolation of input into any URL. The resolver it calls (`dtm_condo_soda.resolve`) interpolates only `normalize_bbl(bbl).canonical` (`dtm_condo_soda.py:647,662`), never the raw input.
- **Typed error map leaks no `str(exc)`.** On `DtmCondoConnectorError` (`condo_base_lot.py:186-198`) the outcome carries only `error_type=exc.error_type` (a controlled class-attribute string such as `"rate_limited"`) plus a hardcoded English note. It does **not** read `exc.message`, `exc.detail`, or `exc.to_payload()`. Confirmed no `str(exc)` / `repr(exc)` anywhere in the module.
- **Provenance carries no secrets.** `_from_result` (`:212-235`) copies `result.provenance` verbatim; each provenance entry (`dtm_condo_soda.py:565-586`) is `{dataset_id, query_kind, request_url, retrieved_at, record_count, rows_updated_at}`. The `request_url` embeds only the constant host + a validated canonical BBL/condo_key — the app token is header-only (`_build_headers`, `dtm_condo_soda.py:326-332`) and never enters the URL or provenance.
- **Injected resolver seam is key-handling-free.** `CondoResolver` is `Callable[..., CondoBaseLotResult]` (`:145`); the seam never touches an app token, header, or secret. Default is the accepted leaf resolver; tests inject doubles.

### 2. `live_provider.py` condo step (PASS)

- **Fail-safe `None` path leaks nothing.** `build_live_substrate` (`:262-267`): on `is_fail_safe`, `_fail_safe(f"condo_{condo.outcome}", correlation_id)` is called **without** the `exc` argument, so `_fail_safe` (`:210-218`) logs `error_type=none` — event token (controlled `OUTCOME_*` vocabulary) + correlation id only. No payload, no `str(exc)`. This preserves the M1-T002 G5 F5 policy. Verified by `test_m5t045_condo_fail_safe_yields_absent_substrate_zero_lookups` (`test_live_provider.py:647-665`): exactly one warning line, `event=` + `correlation_id=` only, and **zero** downstream lookups.
- **Substitution path cannot smuggle an unvalidated BBL.** `substrate_bbl = condo.resolved_base_bbl or canonical_bbl` (`:269`). `resolved_base_bbl` originates from the resolver's `_collect_base_lots`, which rejects any `condo_base_bbl` not matching `_STRICT_BBL_RE.fullmatch` (exact 10 ASCII digits, `dtm_condo_soda.py:541-548`). So the substituted base lot is already validated canonical form before it reaches `fetch_ztldb`/`fetch_lot`. `test_m5t045_as1_...` (`test_live_provider.py:602-631`) proves both zoning-lot fetches run on `_BASE_LOT`, not the billing BBL.
- **Substitution log line is payload-only** (`_record_condo_substitution`, `:221-234`): `input_bbl`, `base_bbl`, `condo_key` (digit strings or `"none"`), `correlation_id`. Asserted at `test_live_provider.py:622-631`.
- **Non-condo / flag-off byte-identical.** Pass-through path (`:276-278`) uses `canonical_bbl` unchanged; `default_live_substrate` (`:318-319`) returns `None` with zero calls when the flag is off. Proven by `test_m5t045_non_condo_passthrough_default_resolver_byte_identical` (`:668-677`).

### 3. `zoning_crosscheck.py` `condo_resolution_report` (PASS)

- **Only validated/canonical identifiers embedded.** All reported branches (RESOLVED_SINGLE `:555`, MULTI_LOT `:564`, UNRESOLVED `:612`, ERROR `:621`) interpolate `resolution.input_bbl` — which reached a reported outcome only after `classify_lot` enforced the 10-ASCII-digit shape (`condo_base_lot.py:164`). `base_bbls` are resolver-validated 10-digit strings; `condo_key` is response-guarded to 6 ASCII digits or `None`; `source_id`/`dataset_ids`/`divergent_zoning_notice` are module constants. No raw echo of unvalidated input.
- **Non-condo pass-through records nothing** (`:631-633`, returns an empty `CrosscheckReport`) — non-condo profile byte-identical.
- **No logging of full responses.** Grep for `logger.|logging.|print(|str(exc` in the module returned **no matches** — the seam builds report strings only.

### 4. `dtm_condo_soda.py` riders (PASS)

- **Response-side `condo_key` fullmatch guard.** `_CONDO_KEY_RE = re.compile(r"\d{6}", re.ASCII)` (`:198`); the unit-expansion path refuses to interpolate a drifted key (`:737-743` → `SchemaDriftError`). `resolve_by_condo_key` input is guarded the same way (`:815`).
- **`$limit` off by default → byte-identical URLs.** `_limit_suffix(None)` returns `""` (`:366`); a non-positive/non-int (bool-excluded) value fails closed with `SchemaDriftError` (`:367-372`). Default `row_limit=None` keeps every URL byte-identical — no new query surface / SSRF vector.
- **`_ERROR_CODE_SAFE_RE` fullmatch.** `_sanitize_error_code` (`:347-356`) uses `.fullmatch` (not `.match`), so an error code safe only as a prefix (e.g. `"no-such-column\n<injected>"`) is `repr()`'d rather than logged verbatim. `detail` dicts are never surfaced through the seam (item 1).

### 5. Web display (PASS)

- **Escaped React text nodes.** `CondoResolutionRecords` (`PropertyOverview.tsx:135-158`) renders `note.text`, `formatValue(entry.value)`, `zoning`, `derivation`, `entry.source_id` through JSX text/attribute nodes; **no** `dangerouslySetInnerHTML` and **no** `href` derived from note text anywhere in the component. Same one shared path in the printed brief (`ReportView.tsx:81`).
- **Hostile note strings render inert.** `CONDO_RESOLUTION_NOTE_RE = /^condo_resolution:\s*\[([a-z_]+)\]\s*/` (`PropertyOverview.tsx:67`) extracts only a `[a-z_]+` token; any remaining prose is escaped text. A note that fails the regex yields `outcome: null` (`:94`).
- **Allow-list fail-safe direction confirmed — unknown WITHHOLDS, no bypass.** `condoWithholdsAllowances` (`:116-123`) returns `true` if a multi-lot conflict exists **or** any condo note's `outcome !== "resolved_single_base_lot"`. Missing/malformed token (`null`) and unknown tokens both trip it; allowances survive **only** when every condo note is positively the recognised success. `PropertyOverview` (`:175-177`) and `ReportView` (`:38-40`) share this one guard, so screen and brief cannot disagree. `AnalysisIdentityNotice.tsx` states identities neutrally (entered vs analyzed), infers no billing/base-lot relationship, and withholds fail-safe on mismatch/absence (`:29,:37`).

### 6. Fixtures / PII (PASS)

Fixture bodies (`test_dtm_condo_soda.py:126-212`) contain only public NYC DOF tax-map fields: `condo_base_bbl`, `condo_key`, `condo_number`, `condo_name` ("THE 128 HESTER STREET CONDO"), unit/block/lot. No PII, no secrets. The `# gitleaks:allow` on the fixture alias (`:148`) is the documented fixture-name false-positive class. Secrets sweep (`secret|password|api_key|bearer|service_role|AKIA|ghp_|sk-live|xox…|X-App-Token`) over `condo_base_lot.py` and the change-set returned **no matches** in any changed T045 file.

### 7. Scope (PASS)

`git show --stat 28082288` lists exactly 15 files, all in `allowed_paths`. **No forbidden path touched:** `profile/builder.py`, `spatial/adapter.py`, `_contract_schemas/`, `rules/**`, `packages/contracts/` are all absent from the change-set. `NO contract-schema change` confirmed: the profile seam maps onto the existing contract-1.3.0 conflict/note channels only (`zoning_crosscheck.py:474-483`).

---

## Cross-cutting security checklist

- **Cross-tenant isolation:** No tenant-scoped surface introduced; the seam operates on validated BBL identifiers only. N/A → no defect.
- **Service-role secrecy / private storage / upload controls:** No Supabase, storage, service-role, or upload code touched. N/A.
- **SSRF / injection defenses:** URLs built from a constant host with strictly ASCII-digit-validated BBL/condo_key; `$limit` off by default; `re.ASCII` + `fullmatch` guards on BBL, condo_key, and error code. No user-controlled host/scheme. PASS.
- **Prompt-injection defenses:** SODA response text treated as untrusted — unknown columns recorded as advisory notes, never trusted for values (`dtm_condo_soda.py:537-539`); no AI in this path; web renders connector notes as escaped text. PASS.
- **Least privilege:** App token env-only, header-only, never logged/payloaded. PASS.
- **Log redaction:** Payload-only lines (event/error-class + correlation id + validated digit identifiers); no `str(exc)`, no full-response logging. PASS.

---

## Advisory (non-blocking)

- **A1 (trust-boundary note).** `condoWithholdsAllowances` re-enables computed allowances only when every `condo_resolution:` note carries the exact token `resolved_single_base_lot`. This is safe under the current model because `connector_notes` are backend-authored contract data with no user free-text injection path (the note text interpolates only validated BBLs/dataset ids). If a future change ever lets user-influenced text reach `reproducibility.connector_notes`, this token check would become a spoofable allow-enable; keep note authorship strictly server-side. No action required now.
- **A2 (defense-in-depth, optional).** `live_provider._record_condo_substitution` logs `input_bbl=canonical_bbl` on the trust that the caller passes an already-canonical BBL. This matches the pre-existing pipeline log lines (no new leak surface), but a `normalize_bbl(...).canonical` assertion at the seam entry would make the invariant explicit. Advisory only.
- **A3 (modularity, out of G5 scope).** `dtm_condo_soda.py` remains a justify-band `review_signal` warning (modularity exit 0, 0 failures per producer §4/§7); the DB-029(k) shared-`soda_errors`/`soda_request` hoist is correctly routed to the orchestrator as a separate change. Not a security issue; flagged for the code/modularity reviewer.

---

## Verdict

**PASS.** All 7 named G5 surfaces satisfy the security/privacy requirements at the frozen reviewed identity `2b851c18` (production surface byte-identical to HEAD). No secrets, no PII, no injection/SSRF vector, no cross-tenant or log-leak defect, no forbidden-path touch. The fail-safe directions (backend `None`, display allow-list WITHHOLD) are correct and independently reproduced in the committed tests. Three advisories recorded; none block acceptance.
