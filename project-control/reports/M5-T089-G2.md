# M5-T089 - G2 producer self-check record (recorded by the orchestrator, role self_check)

Producer: geospatial-engineer (orchestrator-dispatched subagent) in wt-m5t089; one in-worktree commit 5fb4c99e (parent 114e5e56), cherry-picked to fe254eee with every file MATCH by git blob identity (16 files).

- **Scope:** the connector, its test, 11 recorded fixture bodies + MANIFEST.json + README under services/api/tests/fixtures/building_footprints/, and the producer report; mappluto_geometry_arcgis.py and every scenario/api module untouched; no dependency change; unwired.
- **Producer self-checks (report, OBSERVED):** ruff clean; 110 passed (also clean under -W error); the whole connector suite 1029 passed; modularity exit 0 with the connector's size justified; 16 mutants (height defaults, ground substitution, relative-base error, outSR dropped, short field names, paging ignored, CRS gate removed, over-page check removed, error object accepted, generic catch removed, holes filled, multipart reduced, touch-as-overlap, condo flag removed, placeholder untyped) all RED.
- **[ORCH-HARVEST] at the cherry-picked head fe254eee (Python 3.11.9, cwd services/api): `python -m ruff check .` -> All checks passed!; `python -m pytest tests/connectors/test_building_footprints_arcgis.py -q` -> 110 passed; repo root modularity exit 0 (the connector is 891 SLOC with 49 top-level symbols - above the justification threshold, below the 1000 hard limit; cohesion justification in the producer report).**
- **Fixtures:** captured once with curl from the official endpoint on 2026-09-24 (retrieved_at = HTTP Date header), 38,561 bytes total, each sha256-pinned; two bodies are replayed against a different URL and labelled so.
- **Discoveries routed (producer):** short field names return HTTP 200 with an ArcGIS error object (code 400), not an HTTP 400; RQ-1 height unit still open; two official GROUND_ELEVATION definitions disagree (lowest point vs centroid-interpolated); RQ-4 answered (request-units headers); missing heights published as 0; placeholder footprints carry lot-9999 BBLs; the Dakota courtyard hole confirms the massing hole gap; FGDC capture-threshold self-contradiction; lot-polygon queries return only the subject (neighbours need an envelope); stale code graph.

G2 verdict: PASS (self_check; G1 data-contract-verifier, G3 geospatial-engineer, G4 qa-engineer, G5 security-reviewer and the DCV follow).
