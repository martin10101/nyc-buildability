# M5-T052 G0 — contract-readiness record (DB-031 records-view packet)

**Task:** M5-T052 — multi-lot condo records-view channel + display; guard-coherence ruling
required first.
**Contracted at:** 2026-09-19, session seq 119, the M5-T050 acceptance seam (D-075 window).
**Reviewer of record:** orchestrator.

## Packet completeness (start-controlled-task checklist)

- **Requirement identifiers:** docs/DISCOVERY_BACKLOG.md row DB-031 (the channel-design
  successor recorded at the T045 run-49 wave, consolidated at the 229th/230th seam with
  the T045-wave riders it carries: guard-coherence G3-A2 — the load-bearing design
  question, required ANSWERED FIRST as AS-1 — route propagation G1-A2/G3-A1/HJ-A1,
  cross-boundary token pin G4-A1/HJ-A2, unit-BBL increment G3-A3, substitution
  substrate-record G3-A4, records-heading semantics HJ-A3); DB-029 fixture semantics
  (key absence); D-073-R006 records-vs-allowances for the display.
- **Directive refs:** D-066:D-066-R001 + D-073:D-073-R006 — bound this seam (task_ids
  appends + digest resyncs + audit entries, same commit); `evaluate_task_refs` → ok,
  applicable == cited.
- **Evidence files named in SCOPE:** DB-031/DB-029 rows; M5-T045-{G1,G3,G4,HJ}.md rider
  findings; condo_base_lot.py (read-only consumption target).
- **allowed_paths / forbidden_paths:** ten allowed (new route + its test + new client +
  its test + the three T045-surface components + their test + main.py mount + report);
  forbidden pins the resolver, profile/spatial wiring, contract schemas, and the address
  surface. main.py is allowed for the ONE mount line (risk-listed).
- **Acceptance scenarios:** AS-1..AS-8 in-packet (design-answer-first is AS-1; multi-lot
  primary, single substitution record, unit BBL, absence/error honesty, records
  discipline, compat, proof).
- **Gates:** G0,G1,G2,G3,G4,G5 (G1 = data-contract on the route's report shape — the
  channel is deliberately an existing-shape proof, no schema change); six-reviewer
  roster incl. HJ (display) and DCV.
- **Code graph (D-066-R001):** regenerated at this seam; NAV BLOCK embedded in inputs[3]
  (the six PropertyOverview consumers + three AnalysisIdentityNotice consumers with line
  anchors, verified in source; the resolver's accepted entry points named).
- **Modularity boundary answers:** two NEW focused modules (route, client) mirror the
  accepted T047 channel pattern; component edits stay within the owning components; the
  producer answers the seven questions in the plan; checker in documented commands.
- **Seeds:** all five new allowed_paths files seeded this commit (py placeholders, ts
  client stub, one trivial passing vitest test — the empty-.test.ts rule — and the
  report placeholder).

## Pairwise disjointness (mechanical, D-072)

Live lanes at contract time: M5-T051 (in_progress, loop-2 run 15: scenario derivation).
- T052 ∩ T051 allowed_paths = ∅ (T051 = scenario derivation/proposal + scenario tests +
  derivation fixtures; T052 = condo route/client + architect components). Zero overlap.
- M5-T050 accepted this seam (no live scope); the architect components T052 touches were
  T045's surface — T045 accepted (230th), no live lane holds them.

## Ledger/scope hygiene

- New allowed_paths files seeded this commit — no zero-tracked-file refusal.
- No blocker `affects`/`detail` references M5-T052.

**Verdict: PASS — packet is contract-complete; claim may proceed at this head.**
