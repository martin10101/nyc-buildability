# G4 — INTEGRATION / REGRESSION EVIDENCE VERIFICATION — M5-T013

**Verdict: PASS** (no blocking conditions; one expectation correction and one closed producer open-item recorded below; two pre-existing out-of-packet CI conditions restated as facts)

| Field | Value |
|---|---|
| Task | M5-T013 — evidence / provenance endpoint: surface the assembled evidence trail behind a property's rule evaluation |
| Reviewed SHA | `29ca7bca83c163957f6b0aa2ddc9a48b7f158a57` (confirmed via `git rev-parse HEAD`) |
| Branch | `candidate/D-024-mrl-option-b` (fast-forwarded from `task/M5-T013-evidence-endpoint`) |
| Commit arc | `9a84d392` (G0 packet) → `29ca7bca` (producer output). Confirmed: `9a84d392` is an ancestor of HEAD |
| Predecessor | M5-T012 accepted (175th) at `b3fa1736`; its 312 api tests are this task's regression baseline |
| Gate | G4 — integration / regression evidence (independent) |
| Verifier | ci-evidence-verifier (read-only, independent) |
| Date | 2026-09-10 (UTC) |
| Environment | Windows 11, Python 3.11.9, pytest 8.4.2, ruff 0.13.0, rootdir `services/api` (configfile `pyproject.toml`) |

## 0. Statement of independence

I am not the producer of M5-T013. I wrote, edited, and advised on nothing in this change, and I fixed
nothing. Every figure below comes from a command I ran myself at `29ca7bca`, in the owner's worktree,
after confirming HEAD. I ran both documented suites to completion before opening the producer's report;
the claim audit in section 9 is a post-hoc comparison, not a source. My only write is this file. I ran
no `tools/project_control.py`, no git write command, no `gh` write command, and no `pip install`.

## 1. Claim-by-claim result

| # | Material claim | Result | Observed |
|---|---|---|---|
| 1 | `python -m pytest services/api/tests/api` terminates and is green, **349 passed** | **CONFIRMED** | 349 passed in 43.00s, exit 0 (re-run: 349 in 42.77s) |
| 2 | `python -m pytest services/api/tests/scenario` terminates and is green, **388 passed** unchanged | **CONFIRMED** | 388 passed in 4.49s, exit 0 (re-run: 4.29s) |
| 3 | 37 evidence tests + 312 pre-existing | **CONFIRMED** | `test_evidence_api.py` collects **37**; baseline 312; 312 + 37 = 349 |
| 4 | "the api suite is back to ~10s from M5-T012's ~36s" | **CONTRADICTED (non-material)** | the documented command takes **43.00s / 42.77s** here — *slower* than M5-T012's 38.35s, not faster. The ~10s figure is this task's test file **in isolation** (9.52s measured). See section 2 |
| 5 | `python tools/modularity_check.py --check` passes | **CONFIRMED** | `selected 367 files; failures 0; warnings 16`, **exit 0** |
| 6 | The `review_signal` warn naming an `evidence.py` is `tools/agent_supervisor/evidence.py`, not the new route | **CONFIRMED** | the only warning line containing "evidence" names `tools/agent_supervisor/evidence.py`; the new route draws none. Section 4 |
| 7 | Change confined to the four `allowed_paths` | **CONFIRMED** | 4 files, 1549 insertions, **0 deletions** |
| 8 | Nothing in `forbidden_paths` | **CONFIRMED** | all 20 probed paths: 0 changed files (section 5.1) |
| 9 | `main.py` additive only, registrations and order unchanged | **CONFIRMED** | `8 insertions / 0 deletions`; one import, one `include_router` (appended last), a 6-line comment |
| 10 | `app/config.py` untouched, existing flag reused, no new flag | **CONFIRMED** | empty diff; route calls `internal_rule_eval_enabled` / `INTERNAL_RULE_EVAL_ENABLED` |
| 11 | OpenAPI byte-identical with the flag absent / empty / unknown / true, no evidence path in any state | **CONFIRMED** | one SHA-256 across all four states; 2 paths; no evidence path (section 5.3) |
| 12 | Zero pre-existing test regressed | **CONFIRMED** | 312 baseline passes; seven pre-existing files keep 4/41/34/3/32/171/27 |
| 13 | `ruff` debt count has not moved; this task's files are clean | **CONFIRMED** | still exactly 27 errors in the same 9 pre-existing files; owned files "All checks passed" |
| 14 | 15 `tests/documents` collection errors are the PEP 695 / 3.11 gap | **CONFIRMED** | same 15; collection total 2192 → 2229 (+37) |
| 15 | A green CI run exists for this SHA | **NOT APPLICABLE / NOT CITABLE** | zero GitHub Actions runs for `29ca7bca`; branch local-only by owner hold (section 7) |
| 16 | The module introduces no new (HTTP status, state) pair | **CONFIRMED** (contract-surface regression) | its nine pairs are an identical set to the accepted `scenario_analysis.py` matrix (section 6) |

## 2. Reproduced evidence — documented command 1

Run from the repo root `C:/Users/MLFLL/Downloads/nyc-zoning/ctl24`, at `29ca7bca`.

```
$ python -m pytest services/api/tests/api
============================= test session starts =============================
platform win32 -- Python 3.11.9, pytest-8.4.2, pluggy-1.6.0
rootdir: C:\Users\MLFLL\Downloads\nyc-zoning\ctl24\services\api
configfile: pyproject.toml
plugins: anyio-4.10.0, beartype-0.2.0, cov-6.0.0
collected 349 items

services\api\tests\api\test_contract_schema_packaging.py ....            [  1%]
services\api\tests\api\test_evidence_api.py ............................ [  9%]
.........                                                                [ 11%]
services\api\tests\api\test_properties_v1.py ........................... [ 19%]
..............                                                           [ 23%]
services\api\tests\api\test_property_contract.py ....................... [ 30%]
...........                                                              [ 33%]
services\api\tests\api\test_provenance_boundary_api.py ...               [ 34%]
services\api\tests\api\test_rule_evaluation_api.py ..................... [ 40%]
...........                                                              [ 43%]
services\api\tests\api\test_scenario_analysis_api.py ................... [ 48%]
........................................................................ [ 69%]
........................................................................ [ 89%]
........                                                                 [ 92%]
services\api\tests\api\test_scenario_api.py ...........................  [100%]

============================ 349 passed in 43.00s =============================

real    0m45.688s
EXIT CODE = 0
```

Wall clock: started `2026-09-10T04:28:32Z`, ended `2026-09-10T04:29:17Z` (45.7s process, 43.00s
in-session). **The command TERMINATED normally.** Second run: `349 passed in 42.77s`
(`real 0m45.540s`), exit 0 — deterministic, same count.

### Correction to the expected timing

The expectation that "the api suite is back to ~10s from M5-T012's ~36s" does **not** hold, and the
reason is structural rather than a defect: `services/api/tests/api` still contains M5-T012's 171
`test_scenario_analysis_api.py` tests, at-cap boundary cases included, so the documented command cannot
drop back to ~10s. Measured on this host:

| Command | Time |
|---|---|
| `pytest services/api/tests/api` (documented, 349 tests) | **43.00s** / 42.77s |
| same command at M5-T012's `12fda82f` (312 tests) | 38.35s / 36.63s |
| `pytest services/api/tests/api/test_evidence_api.py` alone (37 tests) | **9.52s** |

So the suite got *slower* by ~4.5s — exactly this task's 37 new tests — and the ~10s figure matches
**this task's own test file in isolation**, not the documented command. The producer's own report is
consistent with my measurement (it records 42.63s, within 1% of mine), so the ~10s expectation appears
to be a conflation of the file-level and suite-level numbers rather than anything either of us
observed. Non-material to the verdict; recorded so the cited number is right.

## 3. Reproduced evidence — documented command 2

```
$ python -m pytest services/api/tests/scenario
============================= test session starts =============================
platform win32 -- Python 3.11.9, pytest-8.4.2, pluggy-1.6.0
rootdir: C:\Users\MLFLL\Downloads\nyc-zoning\ctl24\services\api
configfile: pyproject.toml
plugins: anyio-4.10.0, beartype-0.2.0, cov-6.0.0
collected 388 items

services\api\tests\scenario\test_json_safety.py ........................ [  6%]
......                                                                   [  7%]
services\api\tests\scenario\test_scenario_breakeven.py ................. [ 12%]
................................................                         [ 24%]
services\api\tests\scenario\test_scenario_comparison.py ................ [ 28%]
.......................................................                  [ 42%]
services\api\tests\scenario\test_scenario_contract.py .................. [ 47%]
.....                                                                    [ 48%]
services\api\tests\scenario\test_scenario_derive.py .................... [ 53%]
..................................................                       [ 66%]
services\api\tests\scenario\test_scenario_foundation.py ................ [ 70%]
...............                                                          [ 74%]
services\api\tests\scenario\test_scenario_ranking.py ................... [ 79%]
..............................                                           [ 87%]
services\api\tests\scenario\test_scenario_sensitivity.py ............... [ 91%]
..................................                                       [100%]

============================= 388 passed in 4.49s =============================

real    0m6.984s
EXIT CODE = 0
```

Wall clock: started `2026-09-10T04:29:29Z`, ended `2026-09-10T04:29:36Z`. Second run: `388 passed in
4.29s`, exit 0. **388 unchanged** — the engine suite is byte-untouched (`tests/scenario/**` is a
forbidden path with 0 changed files) and unaffected.

## 4. Modularity — and the `evidence.py` filename collision, resolved explicitly

```
$ python tools/modularity_check.py --check
selected 367 files; failures 0; warnings 16
  ...
  warn review_signal: tools/agent_supervisor/evidence.py - above the warning threshold; consider the module boundary before growing it further
  ...
EXIT CODE = 0
```

**Exit code 0, `failures 0`, warnings 16.** `selected` rose 366 → 367: the one new production module.

### Which `evidence.py` is which

There are two files named `evidence.py` in this repository. I checked every warning line containing
the string "evidence" — there is **exactly one**, and it is **not** this task's file:

| File | Status | SLOC | `WARN_SLOC` 600 | Modularity result |
|---|---|---|---|---|
| `tools/agent_supervisor/evidence.py` | **PRE-EXISTING** — last touched by `ed04c4bb` (M0-T149), and `tools/**` is a `forbidden_paths` entry with **0 changed files** in this arc | **603** | **exceeded by 3** | **this is the warned file** (`warn review_signal`) |
| `services/api/app/api/v1/evidence.py` | **NEW** in `29ca7bca` (this task) | **410** | 190 under | **no warning of any kind** |

Confirmed mechanically: `modularity_check --check | grep -E "api/v1/evidence|test_evidence_api"` returns
**no match** (grep exit 1). The supervisor file also carried this same warning in my two M5-T012 runs,
so it is unambiguously pre-existing and unrelated. SLOC measured with the tool's own
`modularity_check.source_lines`; 410 SLOC corresponds to the 518 physical lines of the new module
(blank lines and comments excluded).

For completeness against my prior reports: `scenario_analysis.py` (M5-T012) still carries its
`warn review_signal` at 666 SLOC. The warning total is unchanged at 16 — **M5-T013 added no modularity
signal of its own.** `test_evidence_api.py` and `app/main.py` draw no warning.

## 5. Diff-scope analysis

### 5.1 Scope and forbidden paths

```
$ git diff --stat 9a84d392..29ca7bca
 project-control/reports/M5-T013-producer-report.md | 145 ++++
 services/api/app/api/v1/evidence.py                | 518 ++++++++++++
 services/api/app/main.py                           |   8 +
 services/api/tests/api/test_evidence_api.py        | 878 +++++++++++++++++++++
 4 files changed, 1549 insertions(+)

$ git diff --name-status 9a84d392..29ca7bca
A  project-control/reports/M5-T013-producer-report.md
A  services/api/app/api/v1/evidence.py
M  services/api/app/main.py
A  services/api/tests/api/test_evidence_api.py
```

Exactly the packet's four `allowed_paths`, **zero deletions anywhere in the arc** (`--numstat`:
145/0, 518/0, 8/0, 878/0). Every `forbidden_paths` entry probed with
`git diff --name-only 9a84d392..29ca7bca -- <path>`; all return **0 changed files**:

| Probe | Changed files | | Probe | Changed files |
|---|---|---|---|---|
| `services/api/app/config.py` | 0 | | `services/api/app/resilience/` | 0 |
| `services/api/app/api/v1/properties.py` | 0 | | `services/api/tests/api/test_properties_v1.py` | 0 |
| `services/api/app/api/v1/rule_evaluation.py` | 0 | | `services/api/tests/api/test_rule_evaluation_api.py` | 0 |
| `services/api/app/api/v1/scenario.py` | 0 | | `services/api/tests/api/test_scenario_api.py` | 0 |
| `services/api/app/api/v1/scenario_analysis.py` | 0 | | `services/api/tests/api/test_scenario_analysis_api.py` | 0 |
| `services/api/app/profile/` | 0 | | `services/api/tests/scenario/` | 0 |
| `services/api/app/rules/` | 0 | | `packages/contracts/` | 0 |
| `services/api/app/scenario/` | 0 | | `apps/web/` | 0 |
| `services/api/app/spatial/` | 0 | | `supabase/` | 0 |
| `services/api/app/connectors/` | 0 | | `tools/` | 0 |

**Diff scope: CLEAN.** All four sibling routes are untouched, the profile / rules / scenario /
spatial / connectors / resilience packages are untouched (so they are read-only consumers as the packet
requires), and `config.py` is untouched — the route reuses the existing flag
(`from app.config import internal_rule_eval_enabled`, env var `INTERNAL_RULE_EVAL_ENABLED`), so **no new
flag exists**.

### 5.2 `main.py` is additive only

```
$ git diff --numstat 9a84d392..29ca7bca -- services/api/app/main.py
8       0       services/api/app/main.py
```

Eight added lines, **zero removed** — which is itself proof that no pre-existing line was altered. The
diff is one import, one six-line comment, and one `include_router`:

```
+from app.api.v1.evidence import router as evidence_v1_router
...
     application.include_router(scenario_analysis_v1_router)
+    # Internal, feature-flag-gated EVIDENCE / provenance endpoint (task M5-T013).
+    # SAME posture as the scenario / rule-evaluation routes above - ALWAYS
+    # registered but unreachable (generic 404, no OpenAPI entry) unless the
+    # EXISTING INTERNAL_RULE_EVAL_ENABLED flag is an explicit true token (it
+    # surfaces that route's trail verbatim, so it reuses that flag and adds no new
+    # one); absent/unknown -> disabled (fail safe). See app.api.v1.evidence.
+    application.include_router(evidence_v1_router)
```

Registration order, read off both blobs:

| | `9a84d392` | `29ca7bca` |
|---|---|---|
| 1 | `properties_v1_router` (L118) | `properties_v1_router` (L119) |
| 2 | `rule_evaluation_v1_router` (L123) | `rule_evaluation_v1_router` (L124) |
| 3 | `scenario_v1_router` (L128) | `scenario_v1_router` (L129) |
| 4 | `scenario_analysis_v1_router` (L135) | `scenario_analysis_v1_router` (L136) |
| 5 | — | `evidence_v1_router` (L143) *(new, appended last)* |

All four pre-existing registrations keep their exact relative order and the new router is appended
after them, so route-resolution order is unchanged. One honest detail: the new **import** is inserted
at line 31, alphabetically ahead of `properties` (as ruff's I001 isort rule requires), so the four
pre-existing import lines shift down by one line number. Their relative order and their bytes are
unchanged — the `8 insertions / 0 deletions` numstat proves no pre-existing line was rewritten.
(Blob OIDs necessarily differ here, `071f7755` → `128593c6`, because this task legitimately edits the
file; the blob-identity method applies to the files that must *not* change, and those all return empty
diffs above.)

### 5.3 OpenAPI document byte-identical in every flag state

Built the app in-process at `29ca7bca` with `INTERNAL_RULE_EVAL_ENABLED` absent, empty, an unknown
token, and explicitly `true`:

```
flag=None    paths=2  evidence paths in schema=[]  ['/api/v1/health', '/api/v1/properties/{bbl}']
flag=''      paths=2  evidence paths in schema=[]  ['/api/v1/health', '/api/v1/properties/{bbl}']
flag='yes'   paths=2  evidence paths in schema=[]  ['/api/v1/health', '/api/v1/properties/{bbl}']
flag='true'  paths=2  evidence paths in schema=[]  ['/api/v1/health', '/api/v1/properties/{bbl}']

sha256 flag=None    a5c042a6b74b915d7bef2c8efcfc6946cf4ea2cd4ae69e93f7ca7edd1426b739
sha256 flag=''      a5c042a6b74b915d7bef2c8efcfc6946cf4ea2cd4ae69e93f7ca7edd1426b739
sha256 flag='yes'   a5c042a6b74b915d7bef2c8efcfc6946cf4ea2cd4ae69e93f7ca7edd1426b739
sha256 flag='true'  a5c042a6b74b915d7bef2c8efcfc6946cf4ea2cd4ae69e93f7ca7edd1426b739
ALL FOUR BYTE-IDENTICAL: True
```

One digest across all four states, two paths, **no evidence path in the schema in any state** —
`include_in_schema=False` holds regardless of the flag. Worth noting for the record: this digest
`a5c042a6…` is the **same** digest I recorded at M5-T012's `12fda82f`, so the published contract is
unchanged across two consecutive tasks. No consumer-visible regression.

### 5.4 Regression — zero pre-existing test regressed

Baseline re-established on the reviewed tree with the new test file excluded (`--ignore`; `--deselect`
with forward-slash node ids silently no-ops on this platform):

```
$ python -m pytest services/api/tests/api --ignore=services/api/tests/api/test_evidence_api.py
collected 312 items
...
============================ 312 passed in 37.31s =============================
EXIT CODE = 0
```

Per-file collected counts, full run vs baseline:

| Test file | Full suite | Baseline (evidence file ignored) | Delta |
|---|---|---|---|
| `tests/api/test_contract_schema_packaging.py` | 4 | 4 | 0 |
| `tests/api/test_properties_v1.py` | 41 | 41 | 0 |
| `tests/api/test_property_contract.py` | 34 | 34 | 0 |
| `tests/api/test_provenance_boundary_api.py` | 3 | 3 | 0 |
| `tests/api/test_rule_evaluation_api.py` | 32 | 32 | 0 |
| `tests/api/test_scenario_analysis_api.py` | 171 | 171 | 0 |
| `tests/api/test_scenario_api.py` | 27 | 27 | 0 |
| `tests/api/test_evidence_api.py` | **37** | absent | +37 (this task's file) |
| **Total** | **349** | **312** | **+37** |

**312 + 37 = 349**, exactly. The seven pre-existing api files hold
**4 / 41 / 34 / 3 / 32 / 171 / 27 = 312** — identical to M5-T012's accepted total, which is the correct
baseline — and all 312 pass **with the new router registered in `main.py`**. Because the arc edits no
pre-existing test file (5.1), those 312 tests are byte-identical to their pre-change selves, so this is
a true before/after comparison.

Independent cross-check from the other direction: the CI-wide collection total rose **2192 → 2229
(+37)**, the same delta, with no other file's count moving and the error count unchanged at 15.

**Pre-existing tests regressed: 0. Newly skipped, xfailed, or deselected: 0.**

## 6. Contract-surface regression (new module)

Beyond the suites, I checked what the new module could add to the API's emitted surface:

* **No new (HTTP status, state) pair.** `evidence.py` defines a module-level `STATUS_STATE_MATRIX`
  whose pair set is **identical** to the accepted `scenario_analysis.py` matrix — the nine pairs
  `(200,None)`, `(422,"validation_error")`, `(404,"no_match")`, `(502,"schema_drift")`,
  `(503,"rate_limited")`, `(503,"source_unavailable")`, `(504,"timeout")`, `(500,"internal_error")`,
  `(500,"internal_contract_error")`. The accepted `rule_evaluation.py` route emits a subset of these
  (`validation_error`, `no_match`, `internal_error`, `internal_contract_error`), so nothing new enters
  the contract.
* **Flag reuse verified in code, not just in the diff:** `app/config.py` exposes
  `internal_rule_eval_enabled()` / `INTERNAL_RULE_EVAL_ENABLED_ENV_VAR` and the route calls that
  existing helper (`evidence.py:74`, gate at `:390`). No second flag, and `config.py` is byte-unchanged.
* **Whitespace / line endings:** 0 trailing-whitespace lines in `evidence.py`,
  `test_evidence_api.py`, `main.py`, and the producer report (measured on the committed blobs,
  CR-stripped). Both new blobs are CRLF (518 CR / 517 lines; 878 CR / 877 lines), matching the
  repo-wide convention recorded in my earlier reports. `.gitattributes` imposes no `eol` rule on
  `services/api/**` or `project-control/reports/**`. Ruff (which enforces the W-class whitespace rules
  under `select = ["E","F","I","UP","B"]`) passes on all three touched files.

## 7. CI honesty — no CI run is citable for this SHA, and why

**There is no CI run to cite for `29ca7bca`, and nothing here should be read as a green pipeline.**

* `git branch -r --contains 29ca7bca` → **empty**. No remote ref contains the reviewed commit.
* `git rev-parse --abbrev-ref --symbolic-full-name @{u}` → `fatal: no upstream configured for branch
  'candidate/D-024-mrl-option-b'`.
* `git rev-list --count origin/main..HEAD` → **871**; `git rev-list --count --all --not --remotes` →
  **357**. The branch is local-only by **owner hold**; GitHub is intentionally stale (`origin/main`
  head is `d8b3899f`, PR #240).
* `gh run list` → runs whose `headSha` is `29ca7bca`: **0**.

CI-readiness is therefore a local-evidence judgement. The two conditions affecting the CI job
`api (ruff + pytest)` are unchanged; **both pre-existing, both outside this packet's `allowed_paths`,
both byte-untouched by this arc, neither M5-T013's to fix.**

### 7.1 PRE-EXISTING — `ruff check .` debt, count unmoved (cleanup task queued separately)

Run exactly as CI runs it (`working-directory: services/api`, step `ruff check .`,
`.github/workflows/ci.yml` L187-L211) with ruff **0.13.0**, the CI pin:

```
$ cd services/api && ruff check .
Found 27 errors.
[*] 4 fixable with the `--fix` option.
EXIT CODE = 1
```

**The count has not moved: still exactly 27** — 23 × E501, 2 × I001, 2 × B905 — in the same nine
already-accepted M5 engine files: `tests/scenario/test_scenario_comparison.py` (9),
`app/scenario/comparison.py` (6), `tests/scenario/test_scenario_ranking.py` (3),
`tests/scenario/test_json_safety.py` (2), `app/scenario/sensitivity.py` (2),
`app/scenario/ranking.py` (2), `tests/scenario/test_scenario_sensitivity.py` (1),
`tests/scenario/test_scenario_derive.py` (1), `app/scenario/breakeven.py` (1). All nine are in
`forbidden_paths` for this task and all nine are byte-unchanged in `9a84d392..29ca7bca`. **M5-T013
added no lint debt and removed none** (it cannot: those files are off-limits to it).

The files this task owns are clean:

```
$ cd services/api && ruff check app/api/v1/evidence.py tests/api/test_evidence_api.py app/main.py
All checks passed!
EXIT CODE = 0
```

Recorded as a **fact, not a gate failure**, per the coordinator's note that a cleanup task is queued
separately: the CI job `api (ruff + pytest)` would still fail at its Ruff step on this branch,
independently of M5-T013.

### 7.2 PRE-EXISTING — CI's wider `pytest -q` uncollectable on Python 3.11

```
$ cd services/api && python -m pytest -q --collect-only
...
ERROR tests/documents/test_units.py   (15 errors, all tests/documents)
!!!!!!!!!!!!!!!!!! Interrupted: 15 errors during collection !!!!!!!!!!!!!!!!!!!
2229 tests collected, 15 errors in 11.04s
EXIT CODE = 2
```

Same **15** collection errors, all under `tests/documents`, from PEP 695 generic-function syntax at
`app/documents/units.py:276` (`def _match_unit[UnitT: enum.Enum](`) — valid on CI's Python 3.12, a
`SyntaxError` on this 3.11.9 sandbox. A precise import grep for
`^\s*(from|import)\s+app\.documents` in `app/api/v1/evidence.py` and `tests/api/test_evidence_api.py`
returns **no matches**, so neither this task's route nor its tests can reach it; both packet suites are
fully collectible and green on 3.11. Not a regression, unrelated to this task. The collected total
rising 2192 → 2229 while the error count stayed at 15 is the cross-check that the new tests were added
cleanly.

## 8. Worktree / read-only discipline

`git status --porcelain` was captured before any command I ran and compared after all of them:
**identical** apart from this report. Everything else present is pre-existing and none of it is mine:

```
?? .claude/agent-memory/qa-engineer/MEMORY.md                                  (pre-existing, left alone)
?? .claude/agent-memory/qa-engineer/feedback_probe_separator_deleting_normalizations.md
?? scratchpad/                                                                 (pre-existing, left alone)
```

No tracked file is modified. `git rev-parse HEAD` is still
`29ca7bca83c163957f6b0aa2ddc9a48b7f158a57`. No commit, push, stash, checkout, install, or ledger write
occurred. My scratch snapshots live outside the repo in the session scratchpad.

## 9. Producer-report claim audit — including one open item now closed

| Producer claim | My observation | Status |
|---|---|---|
| `tests/api` → **349 passed** in 42.63s (37 evidence + 312 pre-existing) | 349 passed in 43.00s / 42.77s, exit 0 | **CONFIRMED** (within 1% on timing) |
| `tests/scenario` → **388 passed** in 4.31s, 0 regression | 388 passed in 4.49s / 4.29s, exit 0 | **CONFIRMED** |
| 37 evidence tests, 312 pre-existing, 0 regression | 37 / 312 / 0 | **CONFIRMED** |
| `main.py` additive registration only | `8 insertions / 0 deletions`, order preserved | **CONFIRMED** |
| `evidence.py` is "well under the 600-SLOC warn threshold" | **410 SLOC**, 190 under; draws no warn | **CONFIRMED** |

**Open item closed.** The producer's report carries a section headed *"Modularity — ORCHESTRATOR ACTION
REQUIRED"*, stating that `python tools/modularity_check.py --check` is not in this packet's
`documented_test_commands` and that `tools/**` is a forbidden path for the producer, so it **did not and
could not** run the command, and asking for it to be run before the gate wave. That was the correct
call rather than a gap in evidence, and I have now run it independently: **exit 0, `failures 0`,
warnings 16, and no warning naming any M5-T013 file** (section 4). AS-8's modularity requirement is
satisfied and the item needs no further orchestrator action.

No claim I checked is contradicted. The one correction in this report (section 2) is to the *expected*
timing given to me, not to anything the producer stated — its 42.63s figure agrees with my measurement.

## 10. Verdict

**PASS at `29ca7bca83c163957f6b0aa2ddc9a48b7f158a57`.**

Both packet-documented commands run to completion and are green, reproduced twice each by me:
**349 passed** (`services/api/tests/api`, 43.00s then 42.77s) and **388 passed**
(`services/api/tests/scenario`, 4.49s then 4.29s), both exit 0. The change is confined to the packet's
four `allowed_paths` with **zero deletions**; all 20 `forbidden_paths` probes return 0 changed files —
the four sibling routes, `config.py`, and the profile / rules / scenario / spatial / connectors /
resilience packages are all byte-untouched, so the new module is a read-only consumer and introduces no
new flag. `main.py` is `+8/-0` additive with every pre-existing registration and its order intact and
the new router appended last. The published OpenAPI document is byte-identical (one SHA-256) with the
flag absent, empty, unknown, and true, with no evidence path in any state — and identical to the digest
at M5-T012's accepted SHA, so the contract is unchanged across two tasks. The module's
`STATUS_STATE_MATRIX` adds no (status, state) pair beyond the accepted precedent.
**Zero pre-existing tests regressed**: the 312-test baseline holds with per-file counts unmoved
(4/41/34/3/32/171/27), 312 + 37 = 349, CI-wide collection 2192 → 2229, and the 388 engine tests are
unchanged. `modularity_check.py --check` exits 0 with `failures 0`.

**Filename clarification, stated for the record:** the `warn review_signal` naming an `evidence.py` is
**`tools/agent_supervisor/evidence.py`** — a different, pre-existing file (603 SLOC, 3 over
`WARN_SLOC`, last touched by `ed04c4bb`/M0-T149, inside the `tools/**` forbidden path with 0 changed
files). This task's **`services/api/app/api/v1/evidence.py` is 410 SLOC and draws no warning at all**.
The warning total is unchanged at 16, so M5-T013 added no modularity signal.

**One correction to the expected figures:** the documented api command takes **43.00s** on this host,
not ~10s — the suite still carries M5-T012's 171 tests, so it is *slower* than M5-T012's 38.35s by the
~4.5s the new tests add. The ~10s figure corresponds to `test_evidence_api.py` run alone (9.52s).

Recorded for follow-up, **not** blocking M5-T013 (both pre-existing, both outside `allowed_paths`,
both byte-untouched by this arc):

1. **Fact, cleanup task already queued:** `ruff check .` from `services/api` fails with **27 errors**
   (23 E501, 2 I001, 2 B905) in nine already-accepted M5 engine files under `app/scenario/**` and
   `tests/scenario/**`. The count is unmoved from the two M5-T012 rounds; this task added none and may
   not touch those files. CI's `api (ruff + pytest)` job fails at its Ruff step independently of
   M5-T013.
2. **Informational:** CI's wider `pytest -q` cannot be collected on this 3.11 sandbox
   (`app/documents/units.py:276` needs 3.12 for PEP 695 syntax; 15 `tests/documents` collection
   errors). CI runs 3.12 and neither this task's route nor its tests import `app.documents` — the known
   environment gap, not a regression.

No CI run is citable for `29ca7bca`: the branch is local-only under the owner hold, no remote ref
contains the commit, and `gh` reports zero runs for this SHA.

---
*G4 integration / regression evidence gate — independent, read-only. Every figure above is reproducible
from the commands quoted, at `29ca7bca`.*
