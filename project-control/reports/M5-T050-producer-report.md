# M5-T050 producer report — placeholder

Seeded at the contract seam (gate fails closed on zero tracked files for a new
allowed_paths entry). The producer replaces this file with the one concise
evidence pass (per-rider file+line map) per the packet's REPORT DISCIPLINE input.
