# M5-T055 G4 — independent QA review (original return + delta-attestation, both saved VERBATIM; the headers are the orchestrator's)

## Original return (at frozen head db0cf0f8)

I have completed the independent G4 review. Here is my gate report.

---

# G4 Gate Report — M5-T055 (DB-035 confirm-arc rider cluster)

**Reviewer:** qa-engineer (independent; read-only)
**Task:** M5-T055 — CLS pixel-proof e2e + record-note reading order + accessible-name truncation honesty + no-street copy trim
**Frozen reviewed head:** `db0cf0f8` (material `db445634`, 4 files). Live `59862f2c` is one disjoint ledger commit (`db0cf0f8` = the M5-T055 + M5-T056 evidence-map commit); task identity byte-stable at the frozen head.
**Verification basis:** read the packet AS-1..AS-6, read the actual test/spec/component code, and independently ran `git diff db445634^ db445634` on all three source files plus a fresh copy sweep via the Grep tool. I did **not** trust the producer's conclusions.

## VERDICT: PASS

No blocking findings. Three advisory findings (G4-1..G4-3), none of which block the gate. AS-6 (green CI at the pushed head) is inherently an orchestrator-captured post-push artifact, not a G4 code defect — flagged so acceptance does not proceed without it.

## AS-to-test binding table

| AS | Binding test (file:line) | Binds? | Mutation that would fail it |
|---|---|---|---|
| AS-1 CLS pixel proof | `responsive-a11y.spec.ts:212-292` (per-viewport loop over `VIEWPORTS` 360/768/1280); assertion `expect(ctaTopAfter).toBe(ctaTopBefore)` at `:277`; placement boxes `:288-289` | Yes | Note moved above CTA → `:277` (CTA shifts) AND `:288` (`noteBox.y >= ctaBox bottom`) fail |
| AS-2 reading order | `address-confirm.test.tsx:881-922` (shown) + `:924-998` (delayed insert); e2e `:288-289` | Yes | Meta before note (old order) → `:911` `lastElementChild toContainElement(metaRef)` and `:908-910` (`note … metaRef FOLLOWING`) fail; delayed test `:982-988` (note = second-to-last, Meta = last) fails |
| AS-3 a11y honesty | `address-confirm.test.tsx:1182-1204`; core `:1199` `toHaveAccessibleName(longEntry)`, `:1201` `toHaveAccessibleName(strong.textContent)`, `:1203` `endsWith("…") === false` | Yes | Any cap reintroduced → all three fail (name becomes 513-char "…", ≠ 600-char visible text) |
| AS-4 copy trim + sweep | `address-confirm.test.tsx:1119-1149`; core `:1140` `not.toContain("We show it as the address you searched for")` + independent grep | Yes | Removed sentence restored → `:1140` fails |
| AS-5 no behavior drift | S1–S10 byte-preserved (diff touches only S11 lines 878-1204); consumer sweep clean; no api/route file in diff | Yes (regression preserved) | Any S1–S10 edit or api/ change would surface in diff |
| AS-6 CI green + modularity exit 0 | PENDING — orchestrator-captured CI at pushed head | N/A at frozen SHA | See G4-3 |

## Duty-by-duty findings

**Duty 1 — test adequacy vs AS-1..AS-6 (read the code).** Verified by reading. AS-1 is a real-browser per-viewport measurement of the Continue CTA's document-absolute top (`rect.top + window.scrollY`, `:205-210`) before and after the long-record insert, with exact equality `:277`. AS-2 has two independent jsdom bindings (shown-state child order `:898-914`; delayed in-place resolve with node-identity + child-order proof `:959-994`) plus the e2e box check. AS-3 binds announced-name-equals-visible-text at 600 chars. AS-4 asserts the old sentence absent + the trimmed identity sentence present. Every enumerated mutation (note above CTA; cap reintroduced; removed sentence restored; old Meta-first order) fails at least one assertion — see table.

**Duty 2 — exact-equality (`toBe(ctaTopBefore)`) robustness.** The assertion is appropriate, not over-strict, because the note inserts strictly BELOW the measured CTA in DOM order and the spec waits for deterministic settle signals before each measurement: the lot-outline surface terminal testid (`:242-250`) and `data-record-address-status` transitioning `loading`→`shown` (`:253`, `:260`). Above-CTA content is settled before the "before" measurement and does not change on the insert (the conditional note is below the CTA in JSX; React re-render leaves above-CTA elements byte-identical). Residual environmental risks are advisory (G4-1, G4-2). The component genuinely emits the settle signals the spec waits on (`AddressConfirmCard.tsx:170-177`, `:183`).

**Duty 3 — regression surface.** Confirmed via `git diff db445634^ db445634`:
- `responsive-a11y.spec.ts` diff is a **pure append** (`@@ -141,3 +141,152 @@` — inserts only after the last S6 test at line 141). All S6 tests (`:1-143`), `VIEWPORTS` (`:15-19`) and `expectNoHorizontalOverflow` (`:21-34`, `toBeLessThanOrEqual` at `:33`) are byte-preserved.
- `address-confirm.test.tsx` diff touches only lines 878-1204 — entirely inside the S11 describe (opens `:806`). S1–S10 untouched.
- Consumer sweep verified independently: `address-resolution.test.tsx:74` references `/record-address` only as a stubbed 404, and `correlation-id` only via `.textContent` equality (`:465`, `:500`, `:540`, `:953`) — no DOM-position/last-child assertion, so the (b) Meta-footer reading-order move cannot break it. Producer's consumer-sweep claim is accurate.

**Duty 4 — 600-char a11y honesty binding.** The test asserts announced-name == visible-text on the SAME node/value: `expect(strong).toHaveAccessibleName(strong?.textContent ?? "")` (`:1201`), plus both endpoints pinned to `longEntry` (`:1193`, `:1199`) and `endsWith("…") === false` (`:1203`). `toHaveAccessibleName` computes from `aria-label` (= `enteredTitle` = full raw `enteredInput`, `AddressConfirmCard.tsx:134`, `:220`). Reintroducing any cap makes the accessible name 513 chars with a trailing "…", failing all three assertions. Binding is solid. The old rider-i 512-cap test was transformed **in place** into this test (diff `:1179` line replaced), so no orphaned/contradictory test remains.

**Duty 5 — empty/placeholder hygiene.** `responsive-a11y.spec.ts` is the pre-existing M2-T002 S6 suite extended in place (append confirmed) — no seeded placeholder to remove. `confirm-journey.spec.ts` (in `allowed_paths`, unmodified) is a real pre-existing S1 suite (verified `:1-40`), not a placeholder. No leftover empty/trivial contract-seam test found in either e2e file or the unit suite. The `ENTERED_TITLE_ATTR_MAX_LEN` const and its 512-slice logic are fully removed (grep: only a comment reference at `AddressConfirmCard.tsx:127` and an unrelated `.slice(0,-2)` child-order test op remain).

## Findings

- **G4-1 (advisory).** AS-1 exact equality carries one environmental dependency: if the note insert pushes a viewport past the scroll threshold, a classic (space-taking) scrollbar would shrink usable width and reflow above-CTA content, moving the CTA. This is a **real, deterministic** shift the assertion correctly catches (not a flake). Given the fixed-height lot-outline map plus modest content, the card almost certainly already scrolls at 360px (scrollbar present and stable across the insert). CI at the pushed head is the definitive proof.
- **G4-2 (advisory).** A web-font swap occurring between the two CTA measurements is a theoretical flake source, but both measurements happen after the card is fully visible and the lot-outline surface has settled, so fonts are effectively loaded by the "before" measurement. Low risk.
- **G4-3 (informational, non-blocking).** AS-6 requires green vitest + Playwright CI and `modularity_check.py --check` exit 0 at the pushed head. Web behavior proves ONLY in CI (thin client); the producer correctly marks it PENDING. Modularity is credible without execution — `AddressConfirmCard.tsx` is 434 SLOC (under the 600 warn threshold) and the change removes a const while moving one JSX element. These are orchestrator-captured pre-acceptance artifacts, not G4 code defects.

## Notes on scope

Directive verification (D-066-R001, D-073-R006, D-077-R002/R003) is the independent `directive-compliance-verifier`'s pass, not G4. Within my scope I observed consistency: the D-066-R001 consumer sweep checks out; the rider-d why-note asserts `not.toMatch(/\d/)` (`address-confirm.test.tsx:1167`), consistent with D-073-R006 "record implies no computed value."

**Recommendation to orchestrator:** record G4 = PASS. Before acceptance, capture the AS-6 CI artifact (vitest `address-confirm` suite + Playwright `responsive-a11y` suite green + modularity exit 0) at the pushed head per the standard thin-client evidence division.

---

## Delta-attestation (after the [ORCH-CORRECTED per G4-1 + CI 35493851185] spec fix)

Confirmed against source. `LotOutlineMap.tsx:471` renders `<p className="architect-map-status" role="status">` whose text carries `"Preparing map… "` while `!mapReady` and per-layer `"loading"`/`"loaded"`/`"unavailable"` — exactly the above-CTA element whose text-settle the poll now waits out. The fix keys off real elements, not invented ones.

## Delta verdict: (a) PASS carries to 05f1aea2

**Judged:** `git diff db0cf0f8..05f1aea2 -- apps/web/e2e/responsive-a11y.spec.ts` (spec-only; assertion and the delta boundary confirm component + unit tests are byte-unchanged).

**Closes G4-1 (root cause).** The terminal-map-state poll blocks the "before" measurement until the map region is height-stable: short-circuits to `terminal` on any fallback surface, else requires `.architect-map-status` to exist and contain neither `"Preparing"` nor `"loading"`. That is the precise 27px culprit (status line settling between the two reads on the live-map path at 360/768 while desktop had already settled). The element and its exact substrings exist at `LotOutlineMap.tsx:471`.

**Closes G4-2 (residual settle incl. fonts).** The 500ms double-read CTA quiescence gate only accepts `ctaTopBefore` once two reads 500ms apart agree — catching any remaining above-CTA async settle (late tiles, font swap) independently of the class-based poll. This is a genuine belt-and-braces: it closes the same root cause even if the `.architect-map-status` locator were ever wrong.

**Mutation sensitivity intact.** The `expect(ctaTopAfter).toBe(ctaTopBefore)` assertion is byte-identical (exact equality, no tolerance). Both new gates run strictly BEFORE `releaseRecord()`, so they only stabilize the pre-insert baseline — they cannot absorb or mask a post-release insert shift. A note-above-CTA mutation still moves the single post-`shown` `ctaTopAfter` read → fails. No false-pass path introduced; the quiescence loop's final `ctaTopBefore` is the last stable read (correct semantics, no off-by-one).

**Advisory (non-blocking, no change requested).** The poll couples to the CSS class `.architect-map-status` and the literal substrings `"Preparing"`/`"loading"`; a future rename/copy change would silently degrade it to the `"no-status-line-yet"` branch and time out (a visible deterministic failure, never a false pass) — and the redundant CTA-quiescence gate would still close the root cause. Definitive proof remains the CI re-run at `05f1aea2` (AS-6, orchestrator-captured); the earlier G4 PASS and its findings G4-1..G4-3 carry forward unchanged to `05f1aea2`. [Orchestrator note: satisfied — run 35494571792 success at bd408ab9; see M5-T055-ci-evidence.md.]
