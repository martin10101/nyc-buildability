<!-- Reviewer return preserved VERBATIM by the orchestrator (report-preservation rule,
.claude/rules/project-control.md; transport entity-decoding applied: &amp; -> &, &lt; -> <, &gt; -> >).
Reviewer: independent qa-engineer subagent (G4), returned 2026-09-12, reviewed HEAD ec4b75dc. -->

VERDICT: FAIL

# G4 QA Gate Report — M2-T022 (address-resolution API endpoint)

- **Task:** M2-T022 — GET /api/v1/address-resolution
- **Gate:** G4 (QA — coverage & executability of `services/api/tests/api/test_address_resolution_api.py` against packet S1–S8). FIRST full G4 wave.
- **Reviewed HEAD:** `ec4b75dc` (branch `candidate/D-024-mrl-option-b`); producer material commit `e245b40b`.
- **Reviewer:** qa-engineer (independent; did not author). Read-only per ADR-005. No pytest/ruff run (discipline); static + read-only git + `python -c` probes with `PYTHONDONTWRITEBYTECODE=1`. CI at the pushed head is the execution authority.
- **Recomputed collected count (by AST at reviewed HEAD): 25** — 17 `def test_` functions; `test_s4_typed_failures_map_to_documented_pairs` parametrized ×6 (`_FAILURES`), `test_s4_hostile_retry_after_is_dropped_never_echoed` parametrized ×4; 15 + 6 + 4 = **25**. Matches the producer's claim.

## Verdict rationale

The S1–S8 coverage and test executability are, on substance, strong and non-vacuous (details below). The gate FAILs on **one reproducible, precisely-located defect**: a test line exceeds the enforced ruff `line-length=100`, which (a) makes the packet's documented_test_command `ruff check services/api/app/api services/api/tests/api` fail, and (b) directly contradicts the producer's self-check claim "ruff … All checks passed." Fail-closed discipline plus a materially false evidence claim = FAIL. The fix is a one-line wrap; the S1–S8 substance is otherwise gate-ready.

## BLOCKING defect

**B1 — E501 line-too-long in the new test module (contradicts producer ruff self-check; fails a documented_test_command).**
- Location: `services/api/tests/api/test_address_resolution_api.py:361` (at reviewed HEAD `ec4b75dc`).
- Content (107 chars): `            detail={"http_status": 401, "url": "https://api.nyc.gov/geoclient/v2/address?houseNumber=314"},`
- Config: `services/api/pyproject.toml` → `[tool.ruff] line-length = 100`, `[tool.ruff.lint] select = ["E","F","I","UP","B"]` (E ⊇ E501); no `per-file-ignores`, no `# noqa`. It is a multi-chunk line, so ruff's single-word/URL E501 exception does not apply.
- Empirical corroboration that E501 is genuinely enforced repo-wide (avoids a false FAIL): a `python -c` scan of **every** `.py` in `services/api/app/api` and `services/api/tests/api` (which includes multiple accepted, multi-thousand-line modules) found **exactly one** line over 100 chars — this one. Every accepted file conforms; the new file is the sole outlier.
- Impact: `ruff check services/api/app/api services/api/tests/api` (documented_test_command) does not pass at `ec4b75dc`; producer report line 62 claims it did. Also fails the G1 lint gate.
- Fix: wrap the `detail={...}` dict across lines (matches `ruff format` output); re-run the documented ruff command clean.

## What was verified PASS (coverage & executability)

**Scenario map S1–S8 → executable assertions (all present, non-tautological):**
- **S1 resolved e2e:** values are fixture-loaded via `fixture_address(G01)` (never literals — M5-T004 rule honored); 7 `source_facts` (bbl, bin, street, borough, zip, lat, lon) each validated against the REAL `source_fact.schema.json`; byte-equality to outcome fields; correlation header == body id and distinct connector id in provenance; transport actually called with the sentinel header (positive control) and key-free URL. Verified the G01 fixture (`bbl=1018887502`, both GRCs `00` → `resolved`) drives all 7 facts.
- **S2 ambiguous:** `status=="ambiguous"`, `selection_policy=="caller_selects"`, `source_facts==[]`, `canonical.bbl is None` (nothing fabricated), suggestions compared against an INDEPENDENT fixture reconstruction in the connector's documented `{street_name[, street_code]}` shape (no invented `slot` key — the producer's §3.1 test-side correction is present in the committed form).
- **S3 rejected + constructed not_found:** both are visible 200s with both GRCs and partial `firstStreetNameNormalized` transported, `source_facts==[]`; not_found constructed via a clearly-labeled G01 variant.
- **S4 typed-failure matrix:** 6 typed errors → `(503 key_missing, 502 auth_failed, 503 rate_limited, 503 source_unavailable, 504 timeout, 502 malformed_response)` each asserted `in STATUS_STATE_MATRIX`, bounded (`len<4096`), positive-controlled (`raised` non-empty), key-free; plus 422 invalid_input with `transport.calls==[]` (rejected pre-network); hostile Retry-After (4 cases) DROPPED not truncated-and-shipped; well-formed retry-after surfaced; and a leak-absence harvest across the success + real-401 paths (sentinel absent from all bodies AND `caplog.text`; upstream body "gateway says no" proven not to pass through).
- **S5 flag gating:** unset → generic `404 {"detail":"Not Found"}` with no `X-Correlation-ID`; set → serves; flag check is first in the route (before correlation minting / input handling), reusing the existing `INTERNAL_RULE_EVAL_ENABLED` per the M5-T013 precedent.
- **S6 reflected text:** recorded G02 message transported verbatim + machine-readable `unsanitized_reflected_input` warning naming `grc_message`/`suggestions` with "escape on render"; hostile `<script>…` variant arrives byte-exact in JSON and is absent from logs and headers.
- **S7 offline/stateless:** module-wide egress-seam guard (`http.client.*.connect` + `socket.create_connection`, evidence-pack pattern); two identical requests → two connector calls (no cache) with distinct HTTP and connector correlation ids.
- **S8 no request-derived budget:** runtime seam (`calls[0]["extra_kwargs"]=={}`) + source scan (`AnalysisBudget(` / `budget=` absent). The route imports no `AnalysisBudget` and passes only `borough`/`zip_code`.

**Anti-tautology / jsonschema non-vacuity (proven by probe):** rebuilt the test's exact `referencing.Registry` (source_fact `$id`, absolute `common_uri`, and literal `common.schema.json`) and confirmed the `common.schema.json#/$defs/{non_empty_string,date_time,bbl,digest_sha256}` `$ref`s resolve and their constraints APPLY — a good route-shaped fact validates clean; a malformed `bbl` is rejected (`'NOT-A-BBL' does not match '^[1-5][0-9]{5}[0-9]{4}$'`), an undocumented key is rejected (`additionalProperties:false`), and a missing required key is rejected. So S1's `iter_errors(fact)==[]` is a real oracle; broken wiring would raise `Unresolvable`, not silently pass.

**Matrix set-equality:** `test_matrix_documented_pairs_are_exactly_the_emitted_set` pins the frozen 9-pair set both directions; each of the 9 documented pairs is ALSO behaviorally driven by ≥1 test (enumerated: 200/None via S1/S2/S3/S5/S6/S7; the 7 error pairs via S4 + the 401 harvest; 500/internal_error via the unexpected-exception test). Not merely literal-vs-literal.

**Red/green coherence (item 4):** the `.canonical` fix is in the committed route (`normalize_bbl(outcome.bbl).canonical`, line 293); `normalize_bbl` returns a `NormalizedBBL` dataclass, so the pre-fix form would fail `_assert_json_safe` → assembly guard → 500, failing S1/S5 (which expect 200) — mechanism verified and credible. The RED state itself is not git-reproducible (single producer commit); acceptable as a §3.1 observation. The S2 slot-key correction is present in the committed test.

**Executability statics:** `ast.parse` OK on Python 3.11 for both files (no PEP 695 / 3.12-only syntax); deterministic parametrize ids; fixture/schema paths resolve (`parents[4]`=repo root, `parents[1]/fixtures/geoclient`); egress guard sound under TestClient; `client` fixture clears `app.dependency_overrides` per test; tests write no repo files. **Only line 361 > 100 (defect B1).**

**Regression surface (M4-T009 lesson — cleared):** `e245b40b` touches exactly the 4 allowed paths (`address_resolution.py` [new], `main.py` [+import +`include_router` +comment, registration-only], the new test module, the report); no sibling source/test modified. No test asserts a total route count or a full path-list. The two order tests — `test_evidence_api.py::test_as8_…` and `test_scenario_analysis_api.py::test_as7_…` — assert their route follows a specific PRE-EXISTING set (properties / rule-evaluation / scenario[/…]), NOT absolute-last, so appending the new router after evidence does not break them. `test_as4_…byte_identical_off_and_on` is unaffected because the new route is `include_in_schema=False` (never in OpenAPI). No cross-suite regression from the registration.

**Whole-suite claim:** producer reports `pytest services/api/tests -q --ignore=tests/documents` → 1856 passed, 1 failed (the known local-3.11 PEP-695 artifact in `test_contract_serializers`, unchanged by this diff; passes on CI 3.12). Not runnable here per discipline; consistent with the recorded 3.12 requirement. CI at the pushed head is the execution authority.

## Non-blocking observations (not gating; for the producer/next gate)

1. **S8 source-scan robustness:** `"budget=" not in source` is trivially dodgeable in isolation (a space, positional pass, or renamed kwarg). The runtime seam `extra_kwargs=={}` is the real behavioral guard (and `resolve_address`'s `budget` is keyword-only, so it cannot arrive positionally). Adequate as-is; the source scan is supplementary, not the primary guard.
2. **`request_budget_exceeded` undriven:** it is the one connector taxonomy member intentionally excluded from `STATUS_STATE_MATRIX` and not driven (the endpoint passes no budget, so it cannot arise). Defensible given S8. Latent gap only if a budget is ever introduced: it would map to the default 503 with a state NOT in the matrix, and no runtime membership assertion covers that path. Worth a note when the budget seam is revisited.
3. **S1 `value_digest`/`response_digest` assertions** are construction-tied (route computes them; test recomputes from the same source value / reads them back from the same provenance), so they pin "the digest is taken over the original value / equals the connector's response digest," not an independent oracle. Meaningful, not a defect.

## Required correction for re-submission
- Fix B1: wrap `test_address_resolution_api.py:361` to ≤100 chars and confirm `ruff check services/api/app/api services/api/tests/api` passes clean (and correct the producer report's ruff claim). No test logic change needed; re-verify the 25-test collection is unchanged.

## Reviewer evidence (reproducible)
- `git show --name-status e245b40b` → 4 files (report, `address_resolution.py` [A], `main.py` [M], test [A]); `main.py` diff = one import + one `include_router` + comment.
- `python -c` AST parse of both `ec4b75dc` blobs → OK on Python 3.11.9; 17 `def test_` in the test blob; over-100 lines: test `[(361,107)]`, route `[]`.
- `python -c` scan of `services/api/app/api` + `services/api/tests/api` → total lines >100 = 1 (test:361).
- `python -c` jsonschema probe (test's exact Registry) → good fact `[]`; bad bbl / extra key / missing-required each rejected with the expected message.

Files reviewed (absolute): `C:/Users/MLFLL/Downloads/nyc-zoning/ctl24/services/api/tests/api/test_address_resolution_api.py`, `C:/Users/MLFLL/Downloads/nyc-zoning/ctl24/services/api/app/api/v1/address_resolution.py`, `C:/Users/MLFLL/Downloads/nyc-zoning/ctl24/services/api/app/main.py`, `C:/Users/MLFLL/Downloads/nyc-zoning/ctl24/services/api/app/connectors/geoclient_address.py`, `C:/Users/MLFLL/Downloads/nyc-zoning/ctl24/services/api/app/connectors/bbl.py`, `C:/Users/MLFLL/Downloads/nyc-zoning/ctl24/services/api/app/config.py`, `C:/Users/MLFLL/Downloads/nyc-zoning/ctl24/packages/contracts/schemas/v1/source_fact.schema.json`, `C:/Users/MLFLL/Downloads/nyc-zoning/ctl24/packages/contracts/schemas/v1/common.schema.json`, `C:/Users/MLFLL/Downloads/nyc-zoning/ctl24/services/api/pyproject.toml`, and the three geoclient fixtures.

---

Return to orchestrator: **G4 = FAIL**, reviewed HEAD `ec4b75dc`, recomputed collected count **25**. Single blocking defect **B1** (E501 at `test_address_resolution_api.py:361`, 107 > 100) — reproducible, task-introduced, contradicts the producer's ruff self-check, fails the documented ruff command and G1. S1–S8 coverage/executability otherwise verified sound (jsonschema validation proven non-vacuous; no cross-suite regression). Requested next step: route to rework for the one-line wrap + ruff re-verify, then re-gate.
