{
 "requirements": {
  "D-038-R003": [
   "services/api/app/api/v1/evidence.py: new M5 backend PRODUCT surface serving GET /api/v1/properties/{bbl}/evidence - the Evidence view named in the D-038 pivot plan (Compare UI -> Evidence view -> rule families) and never previously built. It rebuilds the property profile and its rule_evaluation SERVER-SIDE from the bbl path parameter over the SAME injected seams the accepted M5-T003 route uses, then assembles the provenance trail those accepted documents already carry: profile closed-provenance records, per-citation provenance from the rule_evaluation trace, input provenance for the evaluated inputs, and a per-claim DRAFT-vs-Verified status. The provenance data existed in profile/builder.py::_closed_provenance, rules/evaluator.py::_citations_with_provenance and rules/integration.py::_input_provenance but NO API surface exposed any of it, so a user saw conclusions with no way to audit them. services/api/app/main.py is additive-only (8 insertions, identical blob OID 128593c6 across the rework); services/api/app/config.py is byte-unchanged with the existing internal_rule_eval_enabled flag REUSED and no new flag.",
   "Contracted as a normal G0 packet: project-control/gates/M5-T013-G0.json result=PASS (intake report M5-T013-G0-intake.md). required_gates G0/G1/G3/G4/G5 ALL PASS at reviewed_sha 209b9548a18fa45f27c5b87f4af7989939645d81 - G1 code-reviewer (M5-T013-G1-rereview.md), G3 qa-engineer (M5-T013-G3-rereview.md), G4 integration/regression evidence via ci-evidence-verifier recorded under the rostered label code-reviewer (M5-T013-G4-rereview.md), G5 security-reviewer (M5-T013-G5-rereview.md). Every reviewer independent and != producer backend-engineer; the DCV (M5-T013-DCV-rereview.md) is independent of both the producer and the four gate reviewers.",
   "Executable acceptance scenarios AS-1..AS-8 map to real passing tests in services/api/tests/api/test_evidence_api.py (59 items, up from 37 at the first submission). Reproduced by the orchestrator and independently by every reviewer: python -m pytest services/api/tests/api -> 371 passed (312 pre-existing UNCHANGED + 59 evidence, 0 regression; the seven pre-existing api files keep 4/41/34/3/32/171/27 = 312; CI-wide collection 2229 -> 2251, exactly +22 with nothing else moving); python -m pytest services/api/tests/scenario -> 388 passed (unchanged across all four rounds, no engine module touched); python tools/modularity_check.py --check -> failures 0, EXIT 0 with NO warning naming services/api/app/api/v1/evidence.py (418 SLOC, 182 under the 600 threshold; the only evidence.py warning is the pre-existing tools/agent_supervisor/evidence.py at 603 SLOC). G3 independently re-ran its mutation matrix: 37 applied, 30 caught, and every one of its seven original coverage holes now fires.",
   "TRANSPORT, NEVER INTERPRET - the correctness boundary of this task, verified by exhaustive leaf classification rather than by assertion. The DCV classified every leaf of the emitted document against its sources: 1,413 leaves -> 1,404 TRANSPORTED byte-equal, 9 server-authored, and NOT ONE of the 9 is a legal value (no cap, FAR, district, section reference, citation text, source id or timestamp is authored). Zero arithmetic in the module; build_scenario called 0 times; import math 0; evaluate_property exactly once (the AS-1-mandated accepted rebuild, not a new rule). G1 independently confirmed no str()/f-string/.strip()/round()/join/title-case/re-ordering of any material value. The canonical cap is transported VERBATIM and now travels WITH its qualifications. No new legal rule is introduced, so the task is NOT G6-blocked - and rule_release.verified_eligible=False is itself transported, so the response SHOWS the G6 posture rather than hiding it.",
   "GATE-FAIL -> REWORK -> RE-ATTEST -> ACCEPT arc, recorded because it is the substance of this task's value. At the first reviewed SHA 29ca7bca the wave returned G3/G4/G5/DCV PASS but G1 FAIL (report M5-T013-G1.md retained as evidence). BLOCKING-1: the evidence document was a SILENT PROJECTION - rule_evaluation root 14/20 keys and evaluation_trace 7/19, dropping 18 required contract fields including every qualification attached to the cap it presented: exceptions_applied ('A higher maximum residential FAR (up to 2.00 ... per ZR 23-21) applies ... the result is conditional'), notes ('it is NOT an evidence-based determination'), computation_steps (the 10000.0 x 1.5 = 15000.0 derivation) and rule_release (including verified_eligible: false). A QUALIFIED figure was rendered UNQUALIFIED in the one surface built to audit it - the endpoint whose purpose is to prove nothing is overstated was itself overstating - breaching both 'no summarisation that could change meaning' and 'never silently omitted'. The narrow byte-equality test passed only because it asserted over the keys that were kept. BLOCKING-2 (G1 HIGH-1): rule_conflict, the typed competing-rules object the engine deliberately preserves for reviewers, was dropped and _gap_markers had no branch for it, so a real conflict surfaced only as a generic professional_review_required with no statement of WHAT conflicts. Both fixed at 209b9548: root 20/20, trace 19/19, evaluated_input 4/4 via wholesale deep copies, with the two genuinely relocated fields declared IN THE RESPONSE as source_field_routing so 'relocated, not omitted' is machine-verifiable; and a {kind: data_conflict, subject: rule_conflict, reason: the conflict's own note, detail: the whole typed object} marker that is additive to the generic posture markers. G1 drove a schema-valid conflict through the ENTIRE HTTP route and confirmed the marker names both competing rule ids with their effective windows.",
   "The assertion holding the fix shut is schema-driven rather than hand-listed: key-set equality against the app's OWN bundled rule_evaluation.schema.json, so a field added to the SOURCE CONTRACT and then dropped also fails - not merely the fields a reviewer happened to name. G1 and G3 each re-ran the decisive mutations independently: dropping a SINGLE field (zoning_district, exceptions_applied, computation_steps - the realistic future regression rather than a wholesale projection) fires two to three independent assertions every time. G5 additionally established WHY wholesale copying is sound by schema rather than assumption: rule_evaluation.schema.json is CLOSED (additionalProperties: false at root, at the trace and at every named sub-object) and validate_rule_evaluation_document runs BEFORE assembly, so the response's key space is contract-bounded and a future rules change cannot smuggle an undeclared internal field in without first amending packages/contracts.",
   "Route posture mirrors the accepted M5-T003 / rule-evaluation routes and introduces no new public surface: reachable only when the EXISTING internal_rule_eval_enabled flag is an explicit true token, else a generic 404 byte-indistinguishable from an unmounted path in body AND header-set with no correlation header (verified across 8-9 non-true flag tokens); include_in_schema=False; G4 verified the OpenAPI document is byte-identical (one SHA-256, a5c042a6) across the flag absent / empty / unknown / true, with no evidence path in any state - and that it is the SAME digest as at M5-T012's accepted SHA, so the published contract is unchanged across three consecutive tasks. STATUS_STATE_MATRIX is 9 pairs, fully driven, equal to the accepted route's emitted set, and byte-identical across the rework so ZERO new (status, state) pairs entered.",
   "FAIL-CLOSED, with M5-T012's gate findings carried forward as acceptance criteria UP FRONT rather than discovered twice - and verified to be IMPLEMENTED rather than recited. Every stage (fetch, rebuild, assembly, serialisation) sits inside an exception guard: G5 forced raises in nine stages and G1 in all three plus _assert_json_safe, every one yielding a typed in-matrix JSON 500 with X-Correlation-ID and no leak of path, token, env var or traceback. The response must survive BOTH json.dumps(doc, allow_nan=False) AND json.dumps(doc, ensure_ascii=False, allow_nan=False).encode('utf-8'), because those two disagree about unpaired surrogates and the renderer uses the second - G5 proved the pack discriminates them by neutralising the second half in memory and observing exactly the unpaired-surrogate test fail (RED onlyfirstform -> 1 failed), and G3 confirmed the same degradation path. NaN, Infinity and unpaired surrogates all fail closed to internal_contract_error. A disabled _assert_json_safe STILL yields a typed 500 because the return sits inside the guard.",
   "Non-blocking residuals recorded to backlog by explicit reviewer judgement, none an unmet acceptance scenario. The headline one, found independently by THREE reviewers (G3 R-1 MEDIUM, G5 NEW LOW, DCV) with G3 proving the consequence against the running app: source_field_routing's destination is checked for key EXISTENCE but never content REACHABILITY, so parking a real root field the domain test does not name while updating the pinned literal in the same change would ship green on all 59 tests with the response declaring a location that does not hold the content. It is a future-regression hole rather than a present defect - the two shipped relocations were each verified byte-equal at their destinations by G1, G5 and the DCV - and all four reviewers rate it non-blocking; G1's narrower attack (parking a NAMED field) was correctly caught, and G3's stronger attack on an UNNAMED field is what exposed the gap. Also carried: the routing map has no slot for a reason though the module comment promises one (G1 LOW-3, G5, DCV - moot today since there are no genuine exclusions); value-equality assertions catch value drift but not ==-preserving type drift such as 15000.0 -> 15000 (G1 LOW-4, G3 H-7); the single-citation fixture gives no teeth against citation re-ordering or a title-cased section (G1 LOW-5, G3 H-6, now nearly free via a synthetic 2-rule case); a directly-read request: Request echoing a header escapes AS-3 because both comparison requests send identical headers (G3 R-2); verified: 1 survives an is-not-True assertion (G3 R-4); a 'verified' claim under an unenumerated key survives, which is the documented limit AS-7 tells the reader to expect (G3 R-3); five sub-objects inside the closed contract are themselves open so their contents publish unconstrained - clean against today's fixtures, a residual for future content (G5); the rebuild chain is now duplicated in a third route module, build_property_profile appearing in five files, which needs its own extraction task rather than a fifth edited copy (G1 MEDIUM-1, escalated from M5-T012); and the source_coverage key name now undersells its contents, which G1 ruled must NOT be renamed at this gate because renaming a key in a versioned document is consumer-visible and deserves its own change with the EVIDENCE_CONTRACT_VERSION implications weighed. The pre-existing branch-wide ruff failure (27 errors in already-accepted M5 engine files under forbidden_paths, count verified UNMOVED across all four rounds) is carried as a separate queued cleanup task."
  ],
  "D-038-R004": [
   "Fully OFFLINE and requiring NONE of the deferred owner-only credentials: no Supabase (B-001), no NYC Geoclient (B-004), no network, no persistence, no file or subprocess I/O. The endpoint is BBL-based over already-accepted PUBLIC connectors and its tests are fixture-based - both explicitly permitted by R004's own text - consuming the property profile and rule_evaluation rebuilt over the injected get_pluto_fetcher / get_spatial_substrate_provider seams backed by recorded-official PLUTO fixtures and faithful M2-T013 substrate dicts.",
   "Proven by execution rather than assertion, twice and independently. The pack asserts the offline guarantee at the EGRESS seams with landmines that RECORD as well as raise (the recording is load-bearing because the route converts a raise into a typed 500, so a raise-only landmine would be swallowed), and G5 confirmed egress == [] on all four recording seams. The DCV re-armed its OWN loopback-only egress block at the reviewed SHA: the new test file ran 59 passed with egress [], and the whole api suite ran 371 passed with egress [].",
   "No credential is read and none can leak. G5 re-ran its disclosure sweep on the ENLARGED document (59,561 -> 64,262 bytes after the rework restored the full trail) with SEVEN canary secrets in the environment including a fake postgres:// URL and a Sentry DSN: zero leakage of value OR name, in body or headers, with no CANARY substring anywhere. Each newly carried subtree was scanned individually - rule_release, effective_window, input_validation, determination, uncertainty, applicability_trace, computation_steps, evaluated_inputs, spatial_context, spatial_uncertainty, rule_conflict, gaps, evaluated_input, source_field_routing - all clean for object addresses, tracebacks, Windows/POSIX paths, repo fragments, source filenames, credential words, private hosts, module dotted paths and usernames. The URL inventory is still exactly TWO, both official, and the 200 body is the same size token-set or token-unset so there is no length oracle.",
   "No new configuration or credential surface: services/api/app/config.py is byte-unchanged and the existing INTERNAL_RULE_EVAL_ENABLED flag is reused, so the deliverable adds no environment variable, no secret and no deployment requirement, and the flag is not declared in render.yaml or any workflow - the endpoint is a generic 404 in every deployed environment. G5's flag-reuse ruling: correct here, because this endpoint publishes only the provenance behind the document that flag already governs and both surfaces are body-less GETs with structurally zero untrusted input, so the blast radius stays homogeneous (its M5-T012 objection was specifically about one token opening a body-less GET and four body-accepting POSTs - two threat models, one switch). Isolation verified: scenario flag ON with rule-eval OFF gives 404.",
   "G5's no-auth carry-forward, restated because it is the condition that matters when this service is eventually exposed: the flag is the only containment and is genuinely fail-safe, so this is latent rather than exposed. Amplification was re-measured on the enlarged document at 64,262 bytes / 58 ms (x1004) against the already-accepted COMPLETELY UNGATED /properties/{bbl} at 74,804 bytes / 54 ms - still smaller than the existing unauthenticated surface and still with no caller-controlled multiplier, so no new amplification class is introduced. When authentication lands (M0-T007 / M0-T008, themselves blocked on the deferred Supabase credential) this endpoint needs authn+authz WITH ACCESS LOGGING, because a provenance view is where 'who looked' matters and the restored trail now carries rule lifecycle and approval state that is more reconnaissance-useful than the projected version was, plus a per-caller rate limit shared with the other property routes - and nothing body-related, since there is no body.",
   "python tools/validate_directive_compliance.py --check -> EXIT 0 at the reviewed SHA (run to completion by both the orchestrator and the DCV). Directive-file integrity re-verified by the DCV at all four commits of the arc: the CRLF on-disk byte digest of requirements.json equals the manifest's recorded requirements_content_digest_sha256 f62c6fc8, the source digest a237dd50 is unchanged, locked_requirement_ids is [R001..R007] matching requirement_count 7 with no id added, removed or renumbered, and the audit-log note's 32f8d252 -> f62c6fc8 resync claim was verified against the actual bytes at both ends. The applicable set was re-derived three ways including a whole-registry sweep of 36 active directives / 3,641 requirement rows, with the un-cited-applicability trap explicitly re-cleared for the project-control/reports/ path entry that makes D-004-R222, D-004-R240 and D-007-R610 intersect - all three still fail their governance+M0 conjunction against this backend/M5 task."
  ]
 }
}
