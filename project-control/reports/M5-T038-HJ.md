# M5-T038 — Human-journey review return (VERBATIM; human-journey-reviewer, read-only, pinned d94bbcb6)

Orchestrator note: returned via the agent-return channel 2026-09-18T09:58:11Z; the transport
truncated the first delivery inside journey 6. Part 1 below is the truncated delivery verbatim;
Part 2 is the resent remainder, verbatim. VERDICT: PASS (four minor advisory findings).
(The reviewer's opening line notes its agent-memory write was refused by the read-only guard —
expected for named reviewer spawns; no repository write occurred.)

---

## Part 1 (first delivery, verbatim)

The memory write is refused for this read-only dispatch — I'll skip it per protocol and deliver the report directly. My review is complete.

---

# G3 Human-Journey Review — M5-T038 (address-flow + context-panel polish)

**Verdict: PASS**

Task: DB-006/007/009/019a-c address-flow and zoning-context-panel hardening. Reviewed at pinned material commit `d94bbcb6` (source + tests) plus the orchestrator's out-of-scope e2e copy correction at `8c089343`. Read-only, thin-client: I walked each journey from source and the recorded Playwright/vitest specs; I did not re-run CI and rely on the orchestrator-captured "20/20 green at 8c089343" fact per the reviewer protocol, having verified in source that each acceptance assertion is actually encoded.

All six journeys read honestly and accessibly to a first-time architect. Four minor advisory findings below are polish/discovery items, none blocking.

## Journeys walked

**1. Search fails with 404/422 (new `rejected` outcome) — PASS.** The retry gate in `apps/web/src/lib/address-search.ts:136-139` classifies `>=500` as `source_unavailable` and every other non-2xx as `rejected`; the retry loop (`address-search.ts:175`) only continues while `reason === "source_unavailable"`, so a 4xx is single-attempt (tests `address-search.test.ts` AS-1 assert `calls === 1` for 404/422/400 and `calls === MAX_ATTEMPTS` for 500). The copy is distinct and honest across the three cases (`AddressAutocomplete.tsx:25-28`): rejected = "didn't accept that search," source_unavailable = "temporarily unavailable," rate_limited = "rate-limited right now. Wait a moment." Typed input survives — `text` state is never cleared on any error path, and the manual-entry fallback prefills it via `onFallback(text)`.

**2. Timeout offers the full-address button, copy names it — PASS.** DB-009's shared constant `FULL_ADDRESS_SEARCH_LABEL` (`AddressAutocomplete.tsx:16`) is the single source for both the button text (`:174`) and every failure line that names it (`:25-30`), so copy and affordance cannot drift. The button renders whenever `trimmedLength >= 3 && !selected` (`:172`), independent of the error state. Vitest AS-2 asserts the button is present and *not disabled* on timeout; AS-4 asserts the status text and the button carry the exact same `FULL_ADDRESS_SEARCH_LABEL` literal.

**3. "Not my property" focus return — PASS.** `notMyProperty` (`AddressResolutionScreen.tsx:209-212`) clears the result and bumps `entryFocusNonce`; the effect (`:124-127`) focuses `autocompleteRef` in architect mode (the "Street address" box) via the nonce/`useEffect` pattern, correctly avoiding a synchronous `.focus()` on a remounting target. Vitest DB-007b asserts `activeElement` becomes the search box, not `<body>`, and that form values are retained.

**4. PropertyOverview with null/non-canonical BBL — PASS.** `siteZolaUrl = zolaLotUrl(bbl)` (`PropertyOverview.tsx:47`) returns null for a non-canonical or literal-null BBL, and the component renders **no anchor** — only the honest note (`:64`), byte-identical to the panel's note (test asserts `siteAbsent.textContent === panelAbsent.textContent`). A dedicated `null as unknown as string` probe proves no crash and no guessed link.

**5. Landmark/historic coverage badges — PASS.** The shown-vs-computed distinction stays explicit in the panel intro (`ZoningContextPanel.tsx:66-71`, "Nothing here is computed — the calculated result stays in Development limits"). `CoverageBadge` is never color-alone: symbol (`aria-hidden`) + verbatim PRD-§12 enum token + `visually-hidden` gloss (`CoverageBadge.tsx:12-16`). A badge appears only on a *present* designation (`:119`); tests confirm 2 badges on present designations and 0 (plus honest "Unknown — not supplied") when features are absent — nothing invented, no duplication of the full coverage table.

**6. ZoLa glyphs aria-hidden — PASS.** Both ZoLa links wrap `↗` in `<span aria-hidden="true">` (`ZoningContextPanel.tsx:58`, `PropertyOverview.tsx:59`); accessible names become[TRANSPORT TRUNCATION POINT]

---

## Part 2 (resent remainder, verbatim; delivered 09:58:46Z — report COMPLETE)

Resending only the truncated remainder, starting mid-journey-6.

---

**Journey 6 (continued) — ZoLa glyphs aria-hidden — PASS.** Both ZoLa links wrap the `↗` in `<span aria-hidden="true">` (`ZoningContextPanel.tsx:58`, `PropertyOverview.tsx:59`), so the accessible names become "Open in ZoLa" and "Open ZoLa" respectively; the tests resolve each by `getByRole("link", { name })`, confirming the arrow is out of the accessible name and the link still announces sensibly. The confirm-card ZoLa link (`AddressConfirmCard.tsx:141`) is a full-text button with no glyph, so DB-019a's "two glyphs" scope is complete — no ZoLa link was missed.

## Cross-cutting judgment (calm presentation / jargon / DRAFT posture)

Calm, answer-first presentation holds: the panel leads with the designation values and defers provenance behind `ProvenanceDisclosure`, and the failure surface shows one honest status line plus recovery affordances rather than stacking alarms. DRAFT posture is preserved — no copy over-promises certainty; the coverage glosses stay hedged ("Official source fact, not yet professionally reviewed"; "A qualified professional must review this before reliance"), and the deadline was untouched per the honesty rule. No jargon leaks unexplained: the coverage enum tokens are deliberate verbatim PRD-§12 vocabulary, each paired with a plain-language gloss (title + `visually-hidden`), and the panel intro explicitly states "Nothing here is computed — the calculated result stays in Development limits," keeping the shown-vs-computed line clear.

## Advisory findings (non-blocking)

- **F1 — circular recovery copy when the failure originates from the explicit full-address search itself.** When the user clicks "Search this full address" and *that* call fails, the recovery copy still reads "Use the 'Search this full address' button below," pointing back at the button just clicked; for a `rejected` (4xx) origin, re-clicking cannot help. Mitigated — the "manual entry or BBL" alternative and the manual-entry button both render via `offerRecovery` (`AddressAutocomplete.tsx:113`) — so a real path forward always exists. The copy is optimized for the autocomplete-failed → try-full-search path, not the full-search-failed path. Low severity; future copy tweak.

- **F2 — the three absent-BBL notes are not verbatim-identical.** PropertyOverview and ZoningContextPanel match exactly ("The city map link needs a valid BBL, which this lot did not provide."), but AddressConfirmCard (`:146`) reads "The city's map link... which this result did not provide." The task input asked for consistency across panel + confirm card; the confirm card was in `allowed_paths` but left unaligned. All three are honest and semantically identical — cosmetic only.

- **F3 — decorative glyphs outside DB-019a's scope remain un-hidden (discovery).** DB-019a legitimately scoped to the two ZoLa links, but a broader glyph-a11y sweep would still catch the address-hint link "NYC Planning address suggestions ↗" (`AddressAutocomplete.tsx:170`) and the "→" arrows in `PropertyIssuesSummary` links (`PropertyOverview.tsx:25,32`). Out of scope here; route to the discovery backlog.

- **F4 — trivial: inconsistent ZoLa link accessible names** ("Open in ZoLa" on the panel vs "Open ZoLa" on site-context). Pre-existing, both understandable.

## Notes for the orchestrator
- Adding `rejected` to `AddressSearchErrorReason` is safe: the only exhaustive consumer is the `Record<AddressSearchErrorReason, string>` `ERROR_MESSAGES` map, which includes it (compile-time enforced); no unhandled switch elsewhere.
- I did not run tests (read-only / thin-client); the "20/20 green at 8c089343" fact is orchestrator-captured, and I verified in source that each acceptance assertion (AS-1 through AS-7) is actually encoded and that the [ORCH-CORRECTED] e2e copy matches the shared constant. F1/F2 are copy-only follow-ups; neither blocks acceptance of M5-T038.

**Verdict: PASS.**
