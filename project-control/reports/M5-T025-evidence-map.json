{
 "task_id": "M5-T025",
 "assembled_by": "orchestrator",
 "reviewed_sha": "634954162449c4c94b30ff3037ac7b743ef1df73",
 "note": "D-056 web-fix packet; producer commit 9ad6a4f5 cherry-picked as the material commit 10aaedcc. Producer worktree had no node_modules (thin client) - CI web + web-e2e on the pushed head is the executable authority (M5-T023 precedent); local vitest impossible anywhere on this machine. F1 record correction applied 2026-09-14 (rework seam); reviewed_sha updated at resubmit.",
 "requirements": {
  "D-056-R001": [
   "apps/web/src/lib/provenance-link.ts: strict anchored Socrata id regex ^[a-z0-9]{4}-[a-z0-9]{4}$ + constant prefix https://data.cityofnewyork.us/d/; the function's only input is a dataset id - no code path from request_url to any href (structural property, not just tested)",
   "ProvenanceDisclosure.tsx: Dataset-id row renders <a target=_blank rel='noopener noreferrer'> when the id validates, else the prior plain text byte-identical; Retrieved-from host stays text-only",
   "RuleEvaluationResult.tsx: same guarded pattern on the open-schema citation provenance; producer disclosed that live ZR citation provenance never carries dataset_id today (forward-compatible, tested via mutated real fixture) - FLAGGED to G3 to confirm this reading matches intent",
   "18 R001 tests: 10 provenance-link.test.ts (anchored rejection incl. substrings/full URLs/case; hostile javascript:/query/fragment/whitespace/script payloads -> null) + 4 sections.test.tsx + 4 rule-evaluation.test.tsx incl. hostile request_url never in any rendered href in either surface"
  ],
  "D-056-R002": [
   "[ORCH-CORRECTED per M5-T025-G3 F1] Root cause (reachable mechanism): the entire draw step's sole contingency was the one-time map 'load' event, which requires a completed first visually-complete render and never fires on a degraded-GL device (the reporting device also failed to boot ZoLa, a GL map SPA; D-056-R004 names hardware acceleration) - with no style.load arm, no readiness check, and no error handler, addSource/addLayer/fitBounds silently never ran while the background layer + attribution still painted: exactly the reported symptom. The originally-claimed 'listener attached after load already fired' race is NOT reachable at this call site (listener attached synchronously with construction) - G3-verified; the isStyleLoaded() fast path is defense-in-depth, the style.load arm + error surface are the operative fix. Device-side trigger confirmed by the owner redeploy retest (D-056-R006).",
   "Fix: exported runOnStyleReady() - synchronous isStyleLoaded() check (draws immediately when true, categorically closing the race) else arms BOTH load and style.load with an idempotency guard; map.on('error') now routes to a new typed lot-outline-render-error fallback instead of a silent blank map; all six pre-existing fallback branches byte-identical",
   "Framing: fitBounds maxZoom 18 -> 19.5 (LOT_OUTLINE_MAX_ZOOM, exported) - canonical 25x100ft lot grows ~17x67px -> ~48x190px in the 320px panel (calc in producer report)",
   "Red-on-old/green-on-new: 3 pure runOnStyleReady unit tests against a mock with REAL one-time-event semantics (the old mock always fired via queueMicrotask and could never catch this class); +6 lot-outline-map.test.tsx total",
   "Limitation disclosed: no live browser/WebGL in the environment - root cause argued from code + documented MapLibre behavior, final confirmation = owner Manual Deploy + refresh (D-056-R006 owner step)"
  ],
  "D-056-R003": [
   "NavigationControl (zoom in/out, showCompass:false, justification in report) added in the map init path via the already-admitted maplibre-gl; keyboard-accessible native buttons; zero new dependencies; package.json/package-lock.json byte-unchanged"
  ],
  "D-046-R001": [
   "Parallel execution shape: exactly 2 concurrent writers during the build (loop worker on M4-T020 services/api scope; this producer on apps/web scope) - under the 3-writer ceiling; normal G0/G3/G4 gated process (G0 PASS recorded at 572fd1e8)"
  ],
  "D-046-R002": [
   "Disjointness: M5-T025 allowed_paths (9 files: apps/web/** + its own report) share ZERO files with M4-T020 allowed_paths (services/api connectors/tests + its own report) - verified by inspection of both packets"
  ],
  "D-040-R001": [
   "Lot-outline work stays inside the released increment: MapLibre GL JS on the address confirm card only (LotOutlineMap.tsx); display-only discipline preserved (geometry verbatim, no measurement, camera framing only); no expansion-pack task or master-plan change"
  ]
 }
}
