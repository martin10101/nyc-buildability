<!-- VERBATIM reviewer return (data-contract-verifier, 2026-07-17), preserved by the orchestrator per the report-preservation rule. Transport entity-decoding only (&amp;/&lt;/&gt; decoded); no other alteration. The reviewer's opening line about its denied memory write is retained. -->

Memory write was denied by the permission system — I will not work around it; I'll note the proposed memory addition in my report instead. All verification is complete. Here is my full G1 gate report and verdict.

---

# G1 GATE REPORT — M1-T007 (Official-source research: DOB NOW Open Data family)

- **Gate:** G1 — Source and data-contract gate
- **Task:** M1-T007, branch `task/M1-T007-dob-now-research` @ `65cef7a`, reviewed in worktree `c:\Users\MLFLL\Downloads\nyc zoning\nyc-development-feasibility-claude-pack\.claude\worktrees\M1-T007` (clean tree confirmed; `git status --short` empty)
- **Reviewer:** data-contract-verifier (independent; did not produce this work; read-only — no repo file edited, no git write, no project_control.py)
- **Review date/time:** 2026-07-17, live verification window 08:29–08:45 UTC (my own retrieval timestamp `2026-07-17T08:29:32Z` recorded from `datetime.now(timezone.utc)`)
- **Method:** independent live re-fetch of every dataset identity, schema, cadence, description, join, no-match, failure, and pagination claim via `data.cityofnewyork.us` (tokenless, throttled, native Python urllib batches), byte-comparison of committed fixtures against live responses, official Socrata developer docs, and adversarial probes of all seven open questions. I did not rely on the producer's fixtures for any verdict — every material value was re-observed live.

## Per-item verdict table

| # | Item | Verdict | Evidence (mine, live, 2026-07-17) vs producer claim |
|---|---|---|---|
| 1 | 11 dataset IDs + official titles | **PASS** | `api/views/<id>.json` fetched for all 11. Names match exactly: w9ak-ipjd "DOB NOW: Build — Job Application Filings", rbx6-tga4 "DOB NOW: Build — Approved Permits", pkdm-hqz6 "DOB NOW: Certificate of Occupancy", xubg-57si "DOB NOW: Safety — Facades Compliance Filings", 52dp-yji6 "DOB NOW: Safety Boiler", e5aq-a4j2 "DOB NOW Elevator Safety Compliance", xxbr-ypig "DOB NOW: Build — Limited Alteration Applications", dm9a-ab7w "DOB NOW: Electrical Permit Applications", xmmq-y7za "DOB NOW: Electrical Permit Details", kfp4-dz4h "DOB NOW: Build Elevator Permit Applications", juyv-2jek "DOB NOW: Build Elevator Device Details". All 11: `assetType: dataset`, `newBackend: true`, `provenance: official`, attribution "Department of Buildings (DOB)", exactly 1 data-dictionary attachment each. Catalog corroboration re-run: `api.us.socrata.com/api/catalog/v1?...q=DOB%20NOW&limit=30` → resultSetSize 21, same membership as producer's extract fixture. |
| 2 | Column counts / metadata | **PASS** | Live column counts: 95 / 46 / 24 / 40 / 21 / 24 / 26 / 83 / 40 / 82 / 167 — all eleven match the findings table and the registry drafts' `column_count` claims exactly. createdAt values match (e.g. w9ak 2016-05-26T13:34:09Z). |
| 3 | Row counts | **PASS** | Live `count(*)`: w9ak 930,325; rbx6 969,467; pkdm 79,236; xubg 86,683; 52dp 871,266; e5aq 120,137 — identical to `row_counts_core6.json`. Zero drift is expected corroboration, not suspicious: every `rowsUpdatedAt` is unchanged since the producer's fetch, so no update ran between us. Secondary counts (my new observations, point-in-time): xxbr 226,558; dm9a 578,543; xmmq 1,243,118; kfp4 70,066; juyv 115,226. |
| 4 | Cadence vs live rowsUpdatedAt (incl. both children of both combined records) | **PASS** | Live custom fields: Update Frequency "Daily" + Automation "Yes" on 10; "Every weekday" on xubg-57si — exactly as documented. Live rowsUpdatedAt (all 11, converted from raw epochs by machine): w9ak 2026-07-16T20:23:19Z, rbx6 18:42:18Z, pkdm 20:01:40Z, xubg 17:31:05Z, 52dp 19:59:02Z, e5aq 20:32:59Z, xxbr 18:35:04Z, dm9a 21:48:38Z, **xmmq 20:05:30Z (child)**, kfp4 16:32:00Z, **juyv 14:16:44Z (child)** — every value identical to the producer's table, all within ~40 h of my fetch (daily jobs run afternoon/evening UTC; 2026-07-17's run had not yet fired at 08:29Z). No staleness; "no degraded_suspected" verdict is correct. Rule-out corroboration: bty7-2jhb frozen at 2018-08-07T14:34:20Z with "Historical data"/Automation No — exactly as claimed. |
| 5 | Tokenless targeted queries (positive + no-match) | **PASS** | All my requests were tokenless. Re-ran producer's documented queries: `w9ak-ipjd?bbl=1004410016` → 2 filings incl. M00855935-I1/"LOC Issued"; `rbx6-tga4?$where=bbl=1004410016` → same 2 keys with -SG work permits; `52dp-yji6?$where=bin_number=1006014` → boiler_id 10000021095Y0001, "Accepted"; no-match trio (`pkdm-hqz6`, `xubg-57si`, `e5aq-a4j2` at bin=1006014) → HTTP 200 `[]` each; positives for those three datasets confirmed via their own BINs (pkdm 1001905 live-verified by me). All match fixtures byte-for-byte on shared fields. |
| 6 | Pagination/throttling claims | **PASS** | Default `$limit=1000` verified **empirically** (projection query with no `$limit` on pkdm-hqz6 returned exactly 1,000 rows) and verbatim in docs: "defaults to 1,000 records per request" (dev.socrata.com/docs/queries/limit.html, HTTP 200). "Version 2.0 endpoints have a maximum $limit of 50,000" + "Version 2.1 and 3.0 ... no maximum" bullets both present; all 11 datasets are newBackend:true (2.1). `docs/queries/order.html` (HTTP 200): "results of a query are not implicitly ordered, so if you're paging, make sure you provide an $order" — supports the `$order`/`:id` stable-paging claim. `dev.socrata.com/docs/paging` and `/docs/paging.html` → 404 ×2, exactly as recorded (D12). App-token page (HTTP 200): "Without an application token, we can only track usage and perform throttling based on ... source IP address", "requests that aren't using an application token come from a shared pool via IP address", and it links the 429-too-many-requests reference — the "shared pool / HTTP 429 / token lifts throttling" claims are supported. |
| 7 | All 5 BIS rule-outs with quoted official descriptions | **PASS** | Live descriptions match the producer's verbatim quotes: ipu4-2q9a "Note: This dataset only includes permits issued in the Buildings Information System (BIS), most current permits are now issued in DOB NOW -- see the ... [rbx6-tga4]" (rows updated 2026-07-16, still live — correctly framed as different family, not dead); ic3t-wcy2 "This dataset does not include jobs submitted through DOB NOW. See the [w9ak-ipjd] dataset for DOB NOW jobs"; bs8b-p36w "all Certificates of Occupancy issued from 7/12/2012 to March 2021 ... For COs issued since March 2021, see [pkdm-hqz6]"; e98g-f8hy "This dataset is being replaced. Please visit ... ipu4-2q9a for DOB permit data" + "- Now Retired" in the official name; bty7-2jhb "this historical dataset is now redundant" + frozen 2018-08-07. All five accurate. |
| 8 | Filing-to-permit join (job_filing_number both sides) | **PASS** | Live: same `job_filing_number` "M00855935-I1" in w9ak-ipjd (filed 2023-04-10, Alteration) and rbx6-tga4 (`work_permit` "M00855935-I1-SG", issued 2023-07-05). Bonus corroboration: second filing M00842067-I1 also joins both sides (permit M00842067-I1-GC pattern → -SG suffix observed: "M00842067-I1-SG", issued 2023-05-31). Permit-ID = job_filing_number + work-type suffix confirmed. Parent/child joins also live-proven by me: dm9a-ab7w "B00334201-I1-EL" → xmmq-y7za child rows (work_description "Elevators, Escalators, Material Lifts"); kfp4-dz4h "M00058658-I1" → juyv-2jek device rows (device_id 1P27870). |
| 9 | Boiler/BIN join semantics (52dp-yji6) | **PASS** | Live metadata: `bin_number` is `number`-typed and is the ONLY location-bearing column (only key-relevant fields in the whole 21-column schema: tracking_number text, inspection_date text, bin_number number). `$where=bin_number=1006014` returns that building's boiler filings; bin_number serializes as plain string "1006014", no decimals. No BBL/address/borough columns — exactly as documented. |
| 10 | Malformed job_filing_number placeholders | **PASS** | Live re-observed and quantified beyond the producer's single sample: `$where=job_filing_number='Permit is no'` → **count 278**; sample rows carry `work_permit`="Permit is not yet issued", `permit_status`="Signed-off" (first row bin 4117367/QUEENS is the producer's fixture record, byte-identical). The mandatory key-format-validation finding is confirmed and is in fact a whole population, strengthening the connector requirement. |
| 11 | Text zero-padded vs number BBL | **PASS** | Live column types: `bbl` text in w9ak-ipjd/pkdm-hqz6/e5aq-a4j2/kfp4-dz4h, `gis_bbl` text in dm9a-ab7w, `bbl` **number** in rbx6-tga4 and xxbr-ypig, no BBL in xubg-57si/52dp-yji6/xmmq-y7za/juyv-2jek. Serialization probe re-run: number-typed rbx6 bbl returns `"4051980021"` — no decimal artifact, matching the producer's careful "do NOT generalize / normalize defensively (OQ-3)" framing. Zero-padding inside BBL composition confirmed ("1004410016" = 1|00441|0016 while split `block`="441"/`lot`="16" are unpadded). |
| 12 | Field aliases + borough casing | **PASS** | Live examples: `bin` (w9ak et al.) vs `bin_number` (52dp) vs `location_bin` (xxbr, "5122289"); `bbl` vs `gis_bbl` (dm9a, "3031310023"); same property BBL 1004410016 returns borough "Manhattan" (w9ak) vs "MANHATTAN" (rbx6) in my paired live queries; xxbr `location_borough_name` "Staten Island" title case live. `house_no` vs `house_number` vs `location_house_no` confirmed in live column arrays. |
| 13 | Non-ISO dates | **PASS** | Live: 52dp-yji6 `inspection_date` (text type in metadata) = "02/22/2018 00:00:00"; pkdm-hqz6 `c_of_o_issuance_date` (text type) = "02/15/22 11:08:46 AM" — both re-observed exactly; sibling date fields are ISO `calendar_date`/floating_timestamp. Also confirmed kfp4-dz4h `incomplete_date` is text while its other dates are calendar_date (consistent with the per-field-parser recommendation). |
| 14 | OQ-1..OQ-7 genuinely open, scoped, actionable | **PASS** (with evidence enrichment noted) | OQ-1: my own attempt `www.nyc.gov/site/buildings/industry/dob-now.page` → **HTTP 403** — genuinely blocked, actionable (browser session). OQ-2: attachment live HEAD → 200, `application/octet-stream`, 74,405 bytes; binary XLSX content extraction legitimately deferred per low-storage policy; assetIds captured make it actionable. OQ-3: correctly open — my clean-serialization observation matches; PLUTO precedent justifies the defensive stance. OQ-4: correctly open on *completeness*; note my live probe `$select=cycle,count(*)$group=cycle` shows cycles **6,7,8,9,10 only** (n = 15,969 / 15,681 / 21,940 / 30,523 / 2,570) — no cycles 1–5, so migration depth demonstrably starts at cycle 6 (evidence worth attaching; see O2). OQ-5: correctly open on the general relationship; my one-probe cross-match found juyv-2jek `device_id` "1P27870" present in e5aq-a4j2 as `device_number` "1P27870" (same building, BIN 1014813) — first evidence FOR a shared namespace (see O1). OQ-6: genuinely open — my targeted catalog probes ("gas piping", "parking structures", "retaining wall") surfaced no DOB NOW Safety datasets, consistent with the name-based-search limitation. OQ-7: app token is a real human action (HUMAN_ACTIONS §7), correctly reconfirmed. |
| 15 | Dataset-count mapping (owner directive): 9 records = 7 single + 2 combined = 11 IDs | **PASS** | dob-now.json parsed: exactly 9 records. Electrical record's `api_dataset_identifier` explicitly carries "dm9a-ab7w (parent); xmmq-y7za (details child, 40 cols, joined ONLY via job_filing_number, no location fields ...)" with observed key formats; elevator record carries "kfp4-dz4h (parent, 82 cols); juyv-2jek (device details child, 167 cols, one row per device per filing, joined via job_filing_number + device_id; bis_nyc_device_id legacy cross-ref observed)". Each child's ID, join rule, schema size (40/167 — both live-verified), per-child rowsUpdatedAt (both live-verified to the second) and child-specific limitation ("child dataset has NO location fields" / "child dataset has no BIN/BBL — parent fan-out only") are individually represented. Findings doc documents the combination explicitly (§1 secondary list, §2.2 all-11 table, §3.1 per-child rows). Both parent→child joins re-proven live by me (item 8). Count reconciles: 7 single + 2 combined = 9 records covering 6 core + 5 secondary = 11 IDs. |
| 16 | PRD §8.2 registry-field completeness (S5) | **PASS** | Programmatic check: all 18 required fields (source_id, agency, name, official_url, source_type, api_dataset_identifier, authentication, rate_limits, update_frequency, geographic_coverage, fields_available, terms_usage_notes, connector_implementation, last_successful_ingestion, latest_source_version, health_status, known_limitations, fallback_source) present in every one of the 9 records; unknowns explicitly tied to OQ references (OQ-1/2/3/4/5); `last_successful_ingestion: null` is correct for a research-only task (no connector exists). |
| 17 | dob-now.json valid JSON/UTF-8 | **PASS** | `json.load` with `encoding="utf-8"` parsed cleanly → 9 records. |
| 18 | Provenance + fixture discipline | **PASS** | Findings doc: every material claim carries URL + 2026-07-17 retrieval date; §10 D1–D13 source register is complete and honest (records its own 403s and 404s; D13 transparently discloses app-tokens was cited from accepted M1-T001 evidence rather than re-fetched — I re-fetched it live myself, claims hold). Fixtures: 26 files, ~182 KB total, largest 128 KB (views extract) — KB-scale; VERBATIM vs EXTRACT classes explicitly distinguished in the README with exact generating commands; my re-execution of the README commands reproduced fixture content byte-identically on all stable fields (see O3 for the one expected unordered-row difference). Scope check (S6): `git diff --name-only 6392bc1..65cef7a` = 29 files, all inside adjudicated allowed_paths; no connector code; no existing file modified. Second-official-presentation cross-check re-verified: catalog.data.gov mirror page HTTP 200 with 16 literal `w9ak-ipjd` occurrences. Attachment assetIds in the registry drafts (978bdfe8…, 9a9d025e…, 90b77d47…, f2d45892…) all match live metadata. Null-omission semantics ("79 of 95 keys present on sample rec 1") re-verified live: 79 keys, same first record. |

## Defects

**D1 (LOW, documentation completeness — required correction C1):** Findings §2.1 dispositions only 20 of the 21 catalog results. It accounts for 11 family + 5 rule-outs + g76y-dcqj + 855j-jady + 2 HPD noise = 20; **`bf97-mjsy` "Construction-Related Incidents" (attribution: Department of Buildings (DOB))** is present in both live catalog queries and in the producer's own `catalog_dob-now_results_extract.json` fixture but is never mentioned in the findings doc (grep count: 0). The sentence "the remainder are unrelated hits (…)" is therefore an incomplete enumeration — a small unevidenced claim by omission under this project's evidence standard. Reproduction: re-run catalog query D1, list the 21 IDs, grep the findings doc for `bf97-mjsy`. Impact: none on family membership (incident data is clearly not a DOB NOW module dataset), but the discovery-completeness claim must account for all 21 results.

No other defects found. Every numeric, schema, join, cadence, failure-behavior, and rule-out claim I tested reproduced exactly.

## Required corrections (BLOCKING acceptance per gate-verdict semantics)

- **C1:** Add a one-line disposition of `bf97-mjsy` (Construction-Related Incidents, DOB) to findings §2.1 — e.g. "DOB-attributed but not a DOB NOW module dataset (incident reports); deferred to the DOB-wide/legacy task alongside g76y-dcqj and 855j-jady" — so all 21 catalog results are accounted for.

## Non-blocking observations (recommend attaching as reviewer evidence; no producer rework required)

- **O1 (OQ-5 evidence):** Live cross-probe: juyv-2jek device_id "1P27870" (job M00058658-I1, BIN 1014813) exists in e5aq-a4j2 as device_number "1P27870", same BIN, status Active — first concrete evidence the device namespaces are shared. OQ-5 correctly remains open for the general relationship.
- **O2 (OQ-4 evidence):** Live `$select=cycle,count(*)&$group=cycle` on xubg-57si: cycles 6/7/8/9/10 = 15,969 / 15,681 / 21,940 / 30,523 / 2,570; no cycles 1–5 present — migrated depth demonstrably begins at cycle 6. Completeness-of-migration remains open.
- **O3:** The serialization fixture's second row ("1005640039") differs from my live second row ("3004500019") because the query has no `$order` — a live demonstration of the documented unstable-ordering behavior, not a fixture defect. First rows match byte-for-byte.
- **O4:** The rbx6-tga4 placeholder pollution is a population of **278 rows** (live count), not an isolated record — strengthens the mandatory key-format-validation connector requirement.
- **O5:** My attempt to update my own agent memory (`.claude/agent-memory/data-contract-verifier/reference_nyc-source-verification-techniques.md`) was denied by the permission system (Edit and Write both denied). Proposed additions (M1-T007 G1 learnings: urllib space-encoding gotcha; 278-row pollution population; clean number-BBL serialization on rbx6/xxbr vs PLUTO; empirical default $limit=1000 + order.html citation; cheap group-by/cross-key OQ probes; frozen-count corroboration) are preserved in this report; the orchestrator may apply them or re-grant memory-path write for a future session.

## VERDICT: **PASS**

(Recorded as PASS per project gate-verdict semantics, with correction **C1** BLOCKING the next gate (G3) and acceptance until applied, validated, and committed. C1 is a one-line documentation fix inside `docs/research/M1-T007-dob-now-sources.md` §2.1 — producer scope, trivial effort, no re-research needed.)
