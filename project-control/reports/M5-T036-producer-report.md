# M5-T036 producer report — placeholder

Seeded at the contract seam (G0 tracked-file requirement). The producer's
verbatim return replaces this file at the build seam, written to the
REVIEWER HONESTY BAR named in the packet.
