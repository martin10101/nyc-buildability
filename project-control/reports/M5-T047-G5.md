<!-- Orchestrator capture: security-reviewer G5 return for M5-T047 saved VERBATIM
     (transport entity-decoding only). Review pinned at frozen head ca94b2c3. -->

I have completed my independent G5 security/privacy review. All evidence verified at the frozen head; digests match; CI-proven identity confirmed byte-identical to the reviewed surface. Here is my gate report.

---

# G5 Gate Report — M5-T047 (Record-address display channel, DB-032)

**Gate:** G5 (security / privacy)
**Reviewer:** security-reviewer (read-only, independent)
**Task:** M5-T047
**Frozen reviewed SHA:** `ca94b2c3` (submission); live HEAD `21ee7379` is one control-plane commit later (verified via plumbing — no material change to the T047 surface)
**CI-proven identity:** `d27a6a83` — confirmed an ancestor of `ca94b2c3`, and the entire T047 production surface is byte-identical between them (so CI run 35431045720 "success", incl. web-e2e vitest + Playwright hostile-fixture tests, covers exactly the reviewed code)
**Verdict: PASS** (no blocking findings; 3 advisory notes)

## Identity / evidence integrity
- All four production files hashed at `ca94b2c3` (LF-normalized sha256) match `M5-T047-source-sections.md` exactly: `lot_geometry.py 10046e29…`, `record-address.ts c9a15f82…`, `AddressConfirmCard.tsx b76d0a76…`, `address-search.ts 2d3b6ecf…`. On-disk working tree matches the frozen head.
- Material build `7ecdcbf4` touched exactly the 9 allowed-path files; no forbidden path. The `[ORCH-CORRECTED]` `d27a6a83` touched only `address-resolution.test.tsx` (out-of-scope consumer **test**, orchestrator action per CODING_RULES/M5-T023 precedent — no production or security impact).

## 1. Backend route — `services/api/app/api/v1/lot_geometry.py` (record-address section)
- **BBL validated before any fetch:** `normalize_bbl(bbl)` at `:380` precedes the connector `fetch(...)` at `:401`; on `BBLValidationError` a typed 422 returns with zero network I/O (`:381-396`). The SODA query URL is built solely from the canonical BBL in the connector (`url = BASE_URL?bbl={normalized.canonical}`, `pluto_soda.py:638`) — no injection of the raw path param. PASS.
- **Flag-off 404 leaks no hint:** `_not_found()` (`:128-132`) returns a generic `{"detail":"Not Found"}` with no correlation header, checked FIRST at `:373` before a correlation id is even minted — byte-indistinguishable from an unmounted path. `include_in_schema=False` (`:363`) keeps it out of OpenAPI. PASS.
- **Error taxonomy leaks no `str(exc)`:** validation 422 uses `exc.to_payload()` whose `raw_value` is `repr()`-sanitized (`bbl.py:63`) and logs only `code`+correlation_id, never the raw value (`:383-386`); connector faults surface only connector-authored `message`/`error_type`/`source_id`/`detail` (`:411-421`) — the connector guarantees "no stack traces, headers, or the app token" (`pluto_soda.py:283-284`) and sanitizes the upstream error code via `_sanitize_error_code` (`pluto_soda.py:472`); the unexpected path calls `_internal_error_500` (`:135-147`) with a generic message logging only exception type + correlation id. PASS.
- **Reflected PLUTO address JSON-safe / no secret ride-along:** `_assert_json_safe(document)` runs before send (`:470`), exercising both `json.dumps(allow_nan=False)` and the Starlette `ensure_ascii=False`→utf-8 render path, so a hostile address (NaN / unpaired surrogate) fails closed to a typed 500, never an untyped ASGI crash. The document carries only the bounded address plus fixed public provenance (`source_id`, `dataset_id`, `dataset_version`, `retrieved_at`, `request_url`); `request_url` carries **no** app token (`SOCRATA_APP_TOKEN` is header-only, `pluto_soda.py:24-26,410-415`). PASS.
- **No new auth surface:** reuses the existing `INTERNAL_RULE_EVAL_ENABLED` flag, adds no new one. PASS.
- **Rate-limit mapping:** `_PLUTO_ERROR_STATUS` maps `rate_limited→429`, `timeout→504`, `source_unavailable/schema_drift→502`, default `502` (`:309-315`); the (status,state) matrix is frozen and asserted in RA-6. PASS.

## 2. Client — `apps/web/src/lib/record-address.ts`
- **Reflected address bounded before display:** `documentView` bounds the address via `boundedText(record.address, "")` (`:162`, control-char strip + 600-char cap) and source fields via `boundedToken(..)` (`:167-170`); a blanked/dropped result becomes explicit `null`, never an empty line. PASS.
- **Branch on raw/validated outcome, never unbounded data:** 200 handling requires `document_kind === "record_address"` and `isKnownOutcome(record.outcome)` against a fixed allow-list (`:257-272`); non-200 routes only after the `(status|state)` pair is in `DOCUMENTED_PAIRS` (`:280-287`); anything else → typed `unexpected_response`. PASS.
- **URL / SSRF:** `` `${apiBaseUrl()}/api/v1/properties/${encodeURIComponent(bbl)}/record-address` `` (`:187`) — compile-time base (`api.ts:152-154`, `NEXT_PUBLIC_API_BASE_URL`), only dynamic segment is `encodeURIComponent(bbl)`. No URL injection. PASS.
- **No address logging:** no `console.*` in the module; error outcomes carry generic fixed copy, never the address. PASS.
- **Abort/timeout state safety:** single merged `AbortController`, `timedOut` flag disambiguates timeout vs abort, `finally` clears the timer and removes the listener (`:299-302`); the hook uses an `active` guard and aborts superseded requests, ignoring `aborted` results (`:336-361`). No state leak. PASS.

## 3. Display — `apps/web/src/components/address/AddressConfirmCard.tsx`
- **Escaped text nodes:** the record line renders `<strong>{recordAddress}</strong>` (`:200`) as an auto-escaped React text child; no `dangerouslySetInnerHTML`/`innerHTML` anywhere in the touched files (grep-confirmed). PASS.
- **No href from returned text:** hrefs derive only from the re-validated canonical BBL — ZoLa via `zolaLotUrl(canonicalBbl)` (`:73`, returns null for non-canonical) and the continue `Link` via `encodeURIComponent(canonicalBbl)` (`:350`). The record address never reaches an href. PASS.
- **title/aria attribute context:** `<strong title={enteredInput} aria-label={enteredInput}>` (`:188`) — React escapes attribute values, and `title`/`aria-label` are non-navigable text attributes, so no `javascript:`/script-injection risk even for hostile typed input. PASS.
- **Honest gating:** `showRecordAddress` requires a `document` outcome + `address_of_record` + `recordAddressDiffersFromMatched` (`:124-139`); equal/absent/error/loading render no line. PASS.

## 4. Riders
- **Length-bound refusal path:** `GEOSEARCH_RESOLVE_INPUT_MAX_LEN = 512` enforced at gate entry (`address-search.ts:316-320`) BEFORE normalization/compare, returning typed `no_match` — the gate never processes an unbounded reflected string. PASS.
- **Abbreviation fixture:** the G4-A1 `/search` fixture uses public-record values only; the G5-A1 length fixtures use synthetic `"A".repeat(513)` generated in-test. No unbounded/hostile data at rest. PASS.

## 5. Fixtures / PII
- No secrets (grep across all 8 touched files for token/key/password/bearer/service-role/private-key: none). No emails/SSN/phone. Corpus §6 values (`3622 13 AVENUE`, `1279 37 STREET`, bbl `3052960043`, bin `3340270`, dataset `64uk-42ks`) are public tax-lot record fields; no natural-person data introduced. PASS.

## 6. Scope / authority
- Material change confined to 9 allowed-path files; no forbidden path (`connectors/`, `profile/`, `spatial/`, `LotOutlineMap.tsx`, `architect/`, `contracts/`) touched; `pluto_soda.py`/`bbl.py` consumed read-only. `AddressResolutionScreen.tsx` in allowed_paths but byte-unchanged. No new authority path. The record address is DISPLAY-only — identity binds to `addendum.pad.bbl`, and the record address never reaches an href or an identity input. PASS.

## Advisory findings (non-blocking)
- **A1 — `record-address.ts:205-210`:** fetch does not set `credentials: "omit"`, relying on the fetch default (`same-origin`). This matches the accepted baseline `lot-geometry-api.ts` and poses no exposure (the API has no auth cookies; cross-origin default sends none). Consider an explicit `credentials: "omit"` for defense-in-depth consistency across the address clients (repo-wide, out of this packet's scope).
- **A2 — `lot_geometry.py:387-396`:** the 422 validation path does not pass through `_assert_json_safe` (unlike the 200 path), but it reflects only `repr()`-output which is always ASCII/UTF-8-safe, so it cannot produce an untyped 500. Matches the existing lot-geometry endpoint; observation only.
- **A3 — `AddressConfirmCard.tsx:188`:** `title`/`aria-label` carry the unbounded raw `typedInput` (the user's own input, React-escaped, non-navigable). Not exploitable; pre-existing M5-T046 behavior. Could length-bound for tidiness.

## Reproduction (read-only, at `ca94b2c3`)
- Digest check: `git show ca94b2c3:<path> | tr -d '\r' | sha256sum` → matches source-sections index for all four production files.
- Ancestry/byte-identity: `git merge-base --is-ancestor d27a6a83 ca94b2c3` → true; per-file LF-sha256 of the T047 surface equal at `d27a6a83` and `ca94b2c3`.
- Sink/secret/PII greps over the 8 touched files returned empty (dangerouslySetInnerHTML/innerHTML/eval/javascript:/data-derived href; token/key/secret/bearer; email/SSN/phone).

**Verdict: PASS.** No blocking security or privacy defect on the M5-T047 surface. Advisories A1–A3 are optional hardening, not conditions of acceptance.
