# M5-T060 G3 gate — independent code review (verbatim reviewer return)

Reviewer: code-reviewer (opus-4-8 xhigh per D-064), dispatched by the orchestrator at the
c314a29a submit seam, pinned to material d7d45e84. Recorded by the orchestrator; reviewer is
read-only and records no gate itself.

---

I have now reviewed all 18 files at the frozen head plus the read-only route contract and consumers. Identity, scope, correctness, tests, and consumer-compatibility are all verified. Here is my gate report.

---

**VERDICT: PASS**

# G3 Independent Code-Review Gate Report — M5-T060 (phase B3 slice 2: proposal editor UI)

## Identity (verified via read-only git plumbing)
- Material commit reviewed: `d7d45e84` (full `d7d45e84…44ebf`), author martin10101, subject "M5-T060 material (B3 slice 2 proposal editor UI…)".
- `git diff-tree -r d7d45e84` = exactly 18 files, byte-for-byte the packet `allowed_paths`. No forbidden path present.
- Submit seam / current HEAD: `c314a29a` — touches only ledger (`M5-T060.json`, `M5-T060-evidence-map.json`, `state.json`, task json); `git diff --name-only d7d45e84 HEAD -- apps/web/` is EMPTY, so the working-tree material I read is identical to the frozen head. Reviewed at HEAD `c314a29a` with material pinned to `d7d45e84`.
- T058-HELD `test-support/rule-evaluation-fixtures.ts`: last touched by `7d02eddc` (M5-T058), untouched here. Confirmed.
- Disjoint peer lanes (M5-T062/T063): no material overlap observed in the 18-file diff.
- CI at `d7d45e84` (CI / secret-scan / context-budget = success) is taken as orchestrator-attested; thin-client — I did not run npm/vitest/playwright and judged web behavior by reading (per hard rule).

## Scope conformance
- Numeric 2263 authority present; `outline: { srid: 2263, … }` fixed at `proposal-draft.ts:209`. No 4326→2263 transform, no map-drawing code, no scenario emission anywhere — all three D-076-R003 deferrals are ABSENT by design (grep-confirmed: no `contract_version`, `scenario-contract`, `scenario-bounds`, `scenario-api` in packet source; no `dangerouslySetInnerHTML`/`innerHTML`).
- Map is composed read-only: `ProposalEditor.tsx:369` `<LotOutlineMap bbl context />` with display-only copy (`:365-368`); LotOutlineMap.tsx not edited.

## Correctness (findings evidence-bound)
- **Mirror caps verified against the accepted route** `services/api/app/api/v1/proposal_checks_api.py` (read-only): walls `500` (`:171`), lot-lines `800` (`:172`), streets `400` (`:173`), positions `1200` (`:174`), `MAX_PROVENANCE_BYTES 2048` (`:146`); `MAX_LABEL_LEN 200` and `_LABEL_CHARSET [A-Za-z0-9 ._:\-]+` fullmatch at `_proposal_fact_domains.py:44,50`. Client mirrors (`proposal-draft.ts:84-95`) match every value and each `DraftProblem.routeConstant` names its exact route source; mirror is advisory, server remains authority (documented `:12-16`, `:138-144`). No invented limit.
- **Charset equivalence proven**: JS `/^[A-Za-z0-9 ._:-]+$/` ≡ Python `fullmatch`, incl. trailing-`\n` rejection (JS `$` w/o `m` flag rejects trailing newline) — pinned by `proposal-draft.test.ts:47-50`.
- **Decode matrix complete & exact**: `DOCUMENTED_PAIRS` (`proposal-checks-api.ts:195-201`) = route `PROPOSAL_CHECKS_STATUS_STATE_MATRIX` (`proposal_checks_api.py:186-193`): (200,None)/(404,None)/(413,payload_too_large)/(422,validation_error)/(500,internal_error). 422 `field` decoded (`:421`), flag-off 404→feature_unavailable (`:406-408`), size-bound-before-parse fail-closed (`:365-372`), non-JSON/over-budget/undocumented-pair→unexpected_response, network_error/client_timeout/aborted handled; recoverable-gating correct (`:178-186`). Every reflected string via `boundedText`/`boundedToken`; numbers verbatim.
- **Report honesty**: status by icon+text (`OUTCOME_META :50-54`), never color-only (CSS colors are supplementary; `architect.css:211-214`). Shortfall in plan phrasing `"<p> u provided; <r> u required; <s> u short"` (`:44-48`). CNC reasons first-class with `semantic_gap` behind closed `<details>` (`:107-112`). All echoes React-escaped text; markup-echo proven text-rendered with no injected node (`proposal-check-report.test.tsx:66-72`).

## Consumer compatibility (swept beyond file list)
- `WorkspaceView` gains `"proposal"`; only exhaustive `Record<WorkspaceView,string>` is `VIEW_LABELS`, which was updated (`navigation.ts:6`). All other refs are index-access or `WorkspaceView[]`; `ArchitectEntry` switch has a `default` arm (`:145`) — no exhaustiveness break.
- PropertyOverview (T059-HELD), ProfileViews, DevelopmentLimits import only `propertyHref` from navigation (grep-confirmed) — additive view has zero rendered impact on them. `showInspector`/`analysisView` edits (`ArchitectEntry.tsx:87,92`) are additive; existing-view branches byte-stable.
- Gating: `ArchitectEntry` is `ruleEvaluationSurfaceEnabled`-gated at all three property pages (`app/property/{page,compare/page,confirm/page}.tsx`, unedited); proposal view adds NO new flag. AS-6 holds.
- ScenarioWorkspace untouched; `.architect-map-status` untouched (`architect.css:76`; new `proposal-*` classes only).

## Test adequacy
All 7 AS map to material + tests: AS-1 (`proposal-check-report.test.tsx:26-38` render + `:147-188` separate arithmetic pin + `proposal-checks-api.test.ts:186-221` transport pairing), AS-2 honesty labels, AS-3 full matrix + bounded markup + in-suite grep proof (`proposal-editor.test.tsx:104-118`), AS-4 variations/no-emission, AS-5 keyboard-only e2e with route-stub asserting the POST body BEFORE fulfil (`e2e/proposal-editor.spec.ts:136-158`), AS-6 mount/gating (`entry.test.tsx:183-189`, `workspace.test.tsx:15-19`), AS-7 CI-attested. CODING_RULES conformant: typed DOM queries (`querySelectorAll<HTMLSelectElement>`, `querySelector<HTMLDetailsElement>`), `as unknown as typeof fetch` for shape probes, no empty specs, no `.focus()` in a remount handler.

## Modularity
Largest new file `proposal-checks-api.ts` 476 SLOC (< 600 warn); clean separation draft-model / transport-client / three presentation components / fixtures. Producer-reported `modularity_check --check` = 0 failures, packet files absent from the 20 pre-existing warnings (consistent with sizes/structure).

## Findings
- **F1 (Low, advisory — non-blocking, route to discovery):** A saved variation stores a snapshot `draft` at save time (`ProposalEditor.tsx:84-94`), but a later re-run while that variation is active overwrites only its `report` (`:79-81`). After editing the draft and re-running, a variation's stored `draft` and `report` can diverge; the compare view renders only label+summary+per-check outcome (`ProposalVariations.tsx:20-38`), so nothing false is shown and every value stays labeled "Proposed — not a city record." AS-4 is literally satisfied ("re-running updates the stored report"). Consider snapshotting the draft-at-check-time in a follow-up. No correction required for this gate.

## Directive note (code-level only)
In-regime (`D-066-R001`, `D-076-R001/R002`, `D-077-R002/R003`). Code-level evidence is consistent: flat numeric editor over the accepted route (R001), honesty/third-input-class + no emission/contract_version (R002), consumed read-only seams with no held-file edits (R001 D-066), scope inside released B3 slice-2 queue (R003). Formal per-requirement verification is the separate `directive-compliance-verifier` pass recorded in `verification.json` (producer ≠ verifier) — not this G3.

## Conclusion
No blocking defects. Draft model + mirror validation are correct and cite route constants; decode matrix is complete with typed refusals; report rendering is honest and injection-safe (text + bounded echoes, no `dangerouslySetInnerHTML`); 2263 numeric authority with no CRS/map/emission scope breach; T058/T059 held surfaces and ScenarioWorkspace untouched; additive mount breaks no consumer. **G3 PASS**, with F1 as a low-severity advisory for discovery routing. Web-behavior scenarios (AS-1/3/5 render, AS-7) rest on the orchestrator-attested green CI at `d7d45e84` per thin-client rules.

Relevant paths (absolute):
- C:\Users\MLFLL\Downloads\nyc-zoning\ctl24\apps\web\src\lib\architect\proposal-draft.ts
- C:\Users\MLFLL\Downloads\nyc-zoning\ctl24\apps\web\src\lib\proposal-checks-api.ts
- C:\Users\MLFLL\Downloads\nyc-zoning\ctl24\apps\web\src\components\architect\ProposalEditor.tsx
- C:\Users\MLFLL\Downloads\nyc-zoning\ctl24\apps\web\src\components\architect\ProposalCheckReport.tsx
- C:\Users\MLFLL\Downloads\nyc-zoning\ctl24\apps\web\src\components\architect\ProposalVariations.tsx
- C:\Users\MLFLL\Downloads\nyc-zoning\ctl24\services\api\app\api\v1\proposal_checks_api.py (route contract, read-only)
- C:\Users\MLFLL\Downloads\nyc-zoning\ctl24\services\api\app\api\v1\_proposal_fact_domains.py (label cap/charset, read-only)
