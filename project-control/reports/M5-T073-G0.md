# M5-T073 G0 — readiness gate (orchestrator, administrative)

Task: **M5-T073 — DB-045(a) bridge-mount precondition validation**: measure the T065
outline-bridge preconditions (equal vertex counts + 2.0-ft affine-correspondence RMS)
against REAL MapPLUTO parcel pairs across both accepted connectors, on recorded offline
fixtures, BEFORE any mount seam. Validation only — no mount, no production change.

## Authority

- The requirement is specified verbatim in docs/DISCOVERY_BACKLOG.md DB-045 row item (a)
  (M5-T065 G3-F1 advisory: "validate against real parcel pairs BEFORE mounting"), routed to
  "the future mount seam packet"; named as the lane-2 re-feed candidate in the seq-125
  handoff. Lane occupancy authorized by owner directive D-084 ("run 3 codex loops"), R002
  bound to this packet with a same-commit digest resync.
- `evaluate_task_refs`: **ok=true, applicable == cited (D-084-R002)**.
- Sequencing: depends on M5-T065 — ACCEPTED (247th). The mount seam itself stays a FUTURE
  packet; DB-045(b) (exposure/rate-limit) and (d) (surface gating) remain with that seam.

## Packet readiness

- allowed_paths: 3 entries — a new offline test module (placeholder seeded, non-empty py),
  the fixtures glob written as `fixtures/bridge_ring_pairs/**` with a tracked PROVENANCE.md
  placeholder (bare dir prefixes match nothing), and the report (placeholder seeded).
- 4 acceptance scenarios: provenance-stamped fixtures (>=8 pairs, >=3 boroughs, named
  geometry classes), measurement correctness proven on synthetic known-outcome pairs
  (passing AND violating), the honest bounded verdict with refusal-class findings, and a
  zero-production-change bound.
- Capture needs live egress ONCE (fixture recording); the inputs pre-route an egress block
  to [BLOCKED]-to-harvest honestly — fabricated fixtures are prohibited in the packet text.

## Disjointness (live/frozen neighbors)

| Neighbor | State | Overlap |
|---|---|---|
| M5-T070 (loop-3 LIVE) | in_progress 65 | none — apps/web + its own report |
| M5-T071 (submit pending CI) | in_progress 85 | none — apps/web drawing surface + its report |
| M5-T072 (sibling seam, lane-1) | contracting | none — tests/site_definition vs tests/connectors |

Pairwise overlap of allowed_paths vs every live lane: EMPTY. The two connectors and
outline_bridge.py are read-only inputs; no allowed path touches production source.

## Seam records

- Graph regenerated at this seam before authoring; navigation block pins the bridge/connector
  import edges (outline_bridge.py:264, mappluto_lot_outline.py:57) and the
  display-vs-measurement CRS rule (never measure with the 4326 display ring).
- Validator: EXIT 0 at the D-084 capture tree (ad456e83 parent); the packet + binds + this
  report land in ONE contract-seam commit, G0 recorded at that head.

Verdict: **PASS** — packet is contract-ready.
