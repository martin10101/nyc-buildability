# M0-T090 — Directive-Compliance Verification (DCV)

> Saved VERBATIM by the orchestrator from the directive-compliance-verifier agent-return
> channel (transport entity-decoding only). Verdict: PASS (46/46; one non-blocking R063
> advisory). Reviewer preamble: "I have reproduced all the primary evidence."

# DCV REPORT — M0-T090 (D-024 Phase C1: bounded subagent contracts + structural workload sizing)

**Verifier:** directive-compliance-verifier (independent; producer = orchestrator, producer ≠ verifier)
**Directive:** D-024-fable-codex-loop · **Task:** M0-T090
**Read-only:** no `project_control.py`/git/gh writes performed.

## Content identity observed (reproduced, not claimed)

- **Frozen content commit `e8b21d1`.** Deliverable blobs (`tools/agent_supervisor/{workload_classifier,subagent_contracts,startup_overhead,spawn_decision,workload_sizing}.py` + `tools/test_agent_supervisor_bounded_contracts.py`) are **byte-identical between `e8b21d1` and live HEAD** (`git ls-tree` blob SHAs match; `git diff e8b21d1..HEAD` over those paths is empty). Post-freeze commits (`68d6f02`, `db8d333`) touch only `project-control/**` control-plane files — verified via `git diff --name-only e8b21d1..HEAD`.
- **G2 material identity `3e726a0fcc3ea574337d8e2166d3710322586806232fc52506372362f820baba`** (`project-control/gates/M0-T090-G2.json` `content_manifest_sha256`) matches the prompt; stable across the control-plane commits because deliverable content is unchanged.
- **Applicable set = 46, resolver confirms.** `evaluate_task_refs(M0-T090)` → `ok=True`, `len(applicable)=46`; the 46 ids equal the `applicable_requirement_ids` in the `verification.json` M0-T090 row exactly (D-024:ALL resolution). No selective-citation gap.
- **Reproduced:** `pytest tools/test_agent_supervisor_bounded_contracts.py -q` → **53 passed** (0.16s). `python tools/validate_directive_compliance.py --check` → **EXIT=0**. `python tools/modularity_check.py --check` → **failures 0** (largest new module `subagent_contracts.py` 587 SLOC < 600 warn; no new module in the 5 pre-existing warnings).

## Per-requirement rows (all 46)

Legend: **impl** = implementation share owed by this packet objective (positive primary evidence); **conduct** = no-violation/conduct row; **deferred-share** = later-unit portion, PASS-for-this-task because the producer report §1–§2 honestly scopes it as deferred and nothing here forecloses it (verified).

| ID | State | Evidence (reproduced) | Note |
|---|---|---|---|
| **R002** | PASS | No readiness/activation claim anywhere; unit is pre-spawn planning records only. `subagent_contracts.py` docstring + report §1 "supervisor stays SHADOW-ONLY (R595 untouched)". | conduct; one-prompt continuity/transport/restart/golden-run owed to M0-T092/T096, not claimed. |
| **R003** | PASS | Phase C1 schemas/classifier/sizing delivered shadow-only; no `loop.py`/`cli.py` edit in frozen diff (7-file diff = 5 modules+test+report). Graph subsystem not replaced. | conduct/impl; loop architecture untouched. |
| **R004** | PASS | No Codex file in diff; all product code authored by Fable/orchestrator producer. `envelope.evidence_inspection_plan` names what Codex *inspects*, grants no write. | conduct; Codex control-plane-only intact. |
| **R007** | PASS | `M0-T090-G0-readiness.md`: produced from campaign NEXT + captured directive; no owner re-prompt. | conduct. |
| **R008** | PASS | No owner-question artifact in packet/reports; D-028 sleep instruction honored. | conduct. |
| **R010** | PASS | `git log --merges 8bb829c..HEAD` empty; no "241"/merge in campaign commits; branch `control/D-024-fable-codex-loop`. | conduct; PR #241 untouched. |
| **R017** | PASS | `D-024-R101` cited in packet objective (`tasks/M0-T090.json`), **every** one of the 5 module docstrings (closing line "Supervisor-freeze qualifying evidence: D-024-R101."), and commit `e8b21d1` header + body. | impl/conduct; freeze §3 satisfied. |
| **R018** | PASS | Report §4 records distinct-axis (rotation SMALL/MEDIUM/LARGE not overloaded) + reuse of `context_pack_budget` tiers + telemetry records; no duplicate machinery. | impl. |
| **R020** | PASS | Frozen diff confined to the 3 allowed_paths scope areas; commits on control branch only. | conduct. |
| **R021** | PASS | No Codex action surface changed; envelope `telemetry_sources` restricted to closed Phase-B read-only set (`TELEMETRY_SOURCES`, validated). | conduct/no-violation. |
| **R022** | PASS | No path grants Codex mutation; all new records are controller-side frozen dataclasses. | conduct/no-violation. |
| **R023** | PASS | Mechanical (not prose) enforcement delivered at contract level: `ROLES` vocabulary (read_only/review_only/write), `assert_grantable` refuses overlapping write scopes/resources + producer cap, `validate_pair` refuses lease-without-write. | impl (contract-level share); Codex-transport-specific mechanical enforcement (read-only worktree/allowlists) owed to M0-T092/T096 — nothing here weakens it. |
| **R025** | PASS | No dispatch/accept path added; `SpawnDecision.to_dict` records only — nothing acts on a decision. | conduct/no-violation. |
| **R026** | PASS | No activation/actuation; grep of 5 modules shows no subprocess/Popen/spawn/actuate; R595 restated. | conduct; owner authority preserved. |
| **R042** | PASS | `validate_envelope` checks `telemetry_confidence` ∈ `CONFIDENCE_LABELS` (closed vocab); `StartupObservation.to_record` labels measurements `estimated`/`Measurement.unknown` for `None` (unknown never zero); `test_envelope_validation_fail_closed`, `test_unmeasured_startup_values_stay_unknown_never_zero`. | impl (label share). |
| **R045** | PASS | `assert_worker_text_clean` runs over `worker_text_fields()` (**every** string + tuple-item field) AND re-scans the rendered prompt ("rendered_prompt"); 10 fail-closed pattern classes. Tests: `test_worker_prompt_contains_no_quota_language` (INDEPENDENT scan, line 262–278, not the guard's own table), `test_quota_language_rejected_fail_closed` (8 phrasings incl. "About 3.5k tokens should suffice"), `test_quota_guard_covers_every_worker_field`. | impl; positive primary evidence + rendered-prompt re-scan confirmed. |
| **R046** | PASS | Contract scaffolding present: `HealthBands` (occupancy ladder) + `DetectorPolicy` (no_progress_window, repeated_attempt_limit, scope_drift) as envelope fields. | deferred-share; the full runtime health-score evaluation against telemetry owed to M0-T091/T092 — report §1 scopes runtime enforcement as deferred (verified honest). |
| **R047** | PASS | Two linked records realized as `WorkerAssignment` (planning layer) + `SupervisionEnvelope` (invisible supervision layer); `validate_pair` links + separates them; `test_pair_ids_must_link`. | impl; layer-2 runtime watching deferred (honest). |
| **R048** | PASS | `workload_classifier.py`: `WORK_CLASSES` closed 4-tuple, deterministic rule precedence, conservative unknown (`no_objective_features`, `cohesion_unproven` → unknown-recon-first). Tests for all four classes + `test_cohesion_unproven_is_never_optimistic`. | impl. |
| **R049** | PASS | `StartupObservation` fields: packet_bytes, repeated_documents, startup_tokens/seconds, files_reopened, time_to_first_evidence, outcome; median calibration over KNOWN values; `test_startup_observation_measures_the_calibration_inputs`, `test_calibration_uses_known_values_only_and_filters`. | impl. |
| **R050** | PASS | `HealthBands` PRIVATE + configurable (`from_controller_config` fail-closed on unknown keys) + ordering invariant; leak guard bans band names AND numeric thresholds from worker text; `test_bands_from_config_fail_closed`, `test_envelope_numbers_never_leak_into_worker_text`. | impl (band-definition share); runtime Land/Emergency behaviors deferred (honest). |
| **R051** | PASS | Grep of 5 modules: no `maxTurns`/`maxBudget`/`max_turns`/`max_budget` used as a cap — the only `max...spend` token is the R045 *rejection* pattern that bans such phrasing from worker text. `emergency_ceiling_note` is a private envelope field. | impl; catastrophic-ceiling *recovery test* deferred with runtime units (report §1 honest). |
| **R052** | PASS | `OverheadLedger.calibration(size_class=, resolved_model=)` filters per resolved model + size class; `HealthBands` configurable per policy table. `test_calibration_uses_known_values_only_and_filters` proves per-model filter. | impl (calibration-mechanism share); live-runtime calibration deferred (honest). |
| **R053** | PASS | `ExistingSubagentHealth.overloaded_or_confused` blocks resume regardless of counters; `decide_spawn` → `unhealthy_not_resumed`; `test_overloaded_subagent_never_resumed_to_save_startup`. | impl; directly implemented at decision level. |
| **R055** | PASS | **Every s6 bullet has a field.** WorkerAssignment covers all 15 worker-facing items (IDs, exact_change, necessity, role, permitted_paths+tools, prohibited_areas+external_effects, deliverable_schema, acceptance_criteria, required_evidence, checkpoints, blocker_reporting, checkpoint_commit_allowed, extension_protocol, unrelated_discovery, handoff_requirements). SupervisionEnvelope covers all 10 controller items (size_class+cohesion_rationale, graph_files/symbols/tests+write_lease_paths/lease_resources, startup_overhead_note+packet_reuse_plan, resolved_model+model_context_window, telemetry_sources+confidence, health_bands+emergency_ceiling_note, detectors, landing_opportunities, extension_criteria, evidence_inspection_plan). `validate_pair`; `test_envelope_validation_fail_closed`. | impl; field-by-field cross-check against source-001 §6 bullets completed — no missing field. |
| **R056** | PASS | `assert_no_envelope_leak`: bans HEALTH_BAND_NAMES[1:], `HealthBands.numeric_strings()` (repr, `:g`, percent forms), model window, both detector counters; re-run on every `render_worker_prompt`; `test_envelope_numbers_never_leak_into_worker_text` (band name, window, `0.7` occupancy). | impl; both leak forms + rendered-prompt re-scan confirmed. |
| **R057** | PASS | `decide_spawn` Goldilocks precedence: split-first (oversized) / recon-first (unknown) / stay-main (micro-churn) / resume-existing (healthy same-assignment) / fork (clean+beneficial) / spawn-new. `test_fork_only_when_clean_and_beneficial`, `test_tiny_targeted_work_stays_in_main_session`. | impl. |
| **R058** | PASS | `startup_overhead` measures reload cost; `spawn_decision` resume-healthy vs never-resume-overloaded + fork-only-clean-beneficial (`ParentContextState`); `test_followup_resumes_healthy_subagent`, `test_fork_only_when_clean_and_beneficial`. | impl. |
| **R059** | PASS | Classifier keeps `requires_frequent_parent_decisions`/`phases_share_substantial_context` in main; `model_fit` routes lower tier only with window ≥ target×headroom AND demonstrated capability, conservative on unknowns; `test_lower_tier_model_needs_window_and_demonstrated_capability` (all 4 branches). | impl. |
| **R060** | PASS | `PRODUCER_CAP = 3`; `assert_grantable` refuses 4th writer; read-only agents pass at any count; nested children flow through the same check; `test_producer_cap_rejects_fourth_writer`. | impl; cap = refusal ceiling, not target. |
| **R061** | PASS | Classifier `requires_frequent_parent_decisions` → main-session; cohesive-subagent for self-contained; `deliverable_schema`/`return schema` bounds returned summary. | impl (classifier/design share); verbose-transcript-out-of-context runtime behavior deferred (honest). |
| **R062** | PASS | `DetectorPolicy.repeated_attempt_limit` controller-side (never in worker text); `DEFAULT_EXTENSION_PROTOCOL` authorizes in-scope self-correction and gates boundary changes. | deferred-share; runtime repeated-attempt detection owed to later units (honest). |
| **R063** | PASS | `DEFAULT_EXTENSION_PROTOCOL` carries the s6.1 return list (proven / uncertain / why-blocks-acceptance / least-costly-next / additional-scope+natural-completion-point / resume-vs-new / consequences / durable-partial-checkpoint); envelope `extension_criteria` for controller side. `test_rendered_prompt_carries_required_duties`. | impl. **Advisory (non-blocking):** the default worker text compresses s6.1's "additional scope, **likely evidence sources**, and natural completion point" to "additional scope and its natural completion point" — the explicit *likely-evidence-sources* clause is absent from the overridable default (7 of 8 sub-items verbatim). Field is present and overridable; Codex approve/deny runtime gate deferred. Recommend a follow-up unit add the clause. Does not weaken the guard. |
| **R064** | PASS | `DetectorPolicy` docstring pins progress = durable evidence; `no_progress_window_minutes` counts absence of NEW durable evidence, not text volume/tool activity. | deferred-share; runtime no-progress trigger owed to later units (honest). |
| **R079** | PASS | Graph subsystem NOT edited; `neighborhood_from_view` adapts `repo_views` dict shape by value; no graph/index import in the package (leaf preserved); report §4. | impl/no-violation. |
| **R080** | PASS | `WorkloadFeatures.graph_stale` → unknown-recon-first; `tier_signals` raises `stale_graph`; `neighborhood_from_view` fails closed on malformed views; `test_stale_graph_is_reported_never_used`, `test_malformed_view_fails_closed`. | impl. |
| **R081** | PASS | Tiers REUSED via **lazy** `from tools import context_pack_budget` in `_budget()`; `select_tier` called, no copied table; `tools/context_pack_budget.py` NOT in frozen diff and byte-unchanged HEAD-vs-parent (verified). `test_tier_selection_reuses_the_accepted_tiers_exactly`, `test_medium_without_justification_stays_withheld`. | impl; constants untouched confirmed. |
| **R092** | PASS | `PACKET_CATEGORIES` matches the s13 six-category list exactly; `packet_plan` records role-scoped smallest-complete set, mandatory justified omissions, STOP on unprovable sufficiency (empty/truncated neighborhood); `test_packet_plan_marks_omissions_or_stops`, `test_unprovable_sufficiency_stops_instead_of_dropping_constraints`. | impl. |
| **R098** | PASS | `assignment_id` distinct from `parent_task_id` in both records; classifier chooses seams from ownership/proof boundaries, not task-id counting. | impl/no-violation. |
| **R101** | PASS | Phase C items 1 (schemas), 2 (classifier), 3 (startup measurement + goldilocks/resume/fork), 5 (graph sizing + packet plans) delivered; item 4 contract-level guards (cap/lease/no-quota) delivered. | deferred-share; runtime enforcement + item 6 (landing direction, emergency stop, child handoffs) owed to remaining Phase C/D units — report §1 explicitly and honestly defers; nothing here forecloses them. |
| **R108** | PASS | s16.2 SIZING subset (15 rows) mapped to named tests in report §2 and reproduced in the test file (vague/oversized/tiny/resume/overloaded/fork/cohesive/split/unknown/stale/startup-metrics/lower-tier/producer-cap/lease-overlap/nested-children-no-evade + independent no-quota proof). | harness/deferred-share; runtime rows (landing behaviors, TaskStop-for-emergency, catastrophic-ceiling recovery proof, child turnover, 40-min investigation) honestly owed to later Phase C/D units per report §1–§2 (verified — not falsely claimed). |
| **R120** | PASS | Bounded rules as refusal guards (cap, leases, vague-assignment rejection, quota rejection) activation cannot bypass; no unlimited-subagent surface; no actuation. | impl. |
| **R125** | PASS | `M0-T090-G0-readiness.md`: Gate-0 primary-root verified this session (cwd = worktree root, branch `control/D-024-fable-codex-loop`), recorded before any write. | conduct. |
| **R126** | PASS | G0-readiness: MCP roster empty; project default-deny; recorded. | conduct. |
| **R127** | PASS | No Gate-0 failure occurred; no write under a failed/unknown gate; fail-closed restated for successors. | conduct/no-violation. |
| **R128** | PASS | Single session, clean lineage from handoff HEAD `8bb829c`; no failed-start adoption; no history rewrite. | conduct/no-violation. |

## Prohibited-action evidence

No merge commits in `8bb829c..HEAD`; no PR #241 merge; no actuation/dispatch/spawn/subprocess in the five modules; no dependency added; no forbidden path touched; `context_pack_budget.py` untouched; supervisor SHADOW-ONLY (R595 untouched). Nothing deployed/installed/purchased/closed.

## Overall verdict

**PASS.** All 46 applicable requirements independently verified at frozen content `e8b21d1` (material identity `3e726a0fcc3ea574337d8e2166d3710322586806232fc52506372362f820baba`), reproduced on primary evidence — 53/53 tests, validator EXIT=0, modularity 0 failures, byte-identical deliverables HEAD-vs-frozen, resolver applicable==46==cited. No VIOLATED, no UNVERIFIABLE, no BLOCKED. Deferred-portion rows (R046, R050 runtime, R051 recovery-test, R052 live-calib, R061 runtime, R062, R064, R101 runtime+item6, R108 runtime) are PASS-for-this-task: the producer report §1–§2 honestly scopes them as owed to later Phase C/D units, and no code here forecloses them (verified against the actual modules). One **non-blocking advisory** on **R063**: the default worker extension-protocol text omits the explicit "likely evidence sources" clause of source-001 §6.1 (field present and overridable; recommend a later unit add it).
