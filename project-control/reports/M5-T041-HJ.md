# M5-T041 — human-journey review (verbatim reviewer return; transport entity-decoding only)

Saved by the orchestrator from the human-journey-reviewer agent return, 2026-09-18 (seq-118
wave, pinned at submission sha 843e23c4). Nothing added or removed beyond decoding transport
HTML entities.

---

# G3 Human-Journey Review — M5-T041 (address-flow + validator polish cluster)

**Verdict: PASS**

Reviewed read-only at pinned submission `843e23c4`, material `740b6f2f`. Confirmed `git diff 740b6f2f..843e23c4 -- apps/ services/ packages/` is empty (the two intervening commits are evidence/bookkeeping only), so the orchestrator-captured CI run `35402081543` (success, 7m07s, web + web-e2e) is the executable authority for the reviewed identity. No local npm/node run (thin client); journey judged from pinned source + tests + accumulated arc knowledge.

All four of my prior M5-T038 findings (DB-024 a–d = my F1–F4) are **closed**. DB-025(e) satisfied. No blocking findings. Advisories are non-blocking.

---

## Findings

**F1 — DB-024(a) non-circular full-search failure recovery — CLOSED. ADVISORY only.**
`FULL_SEARCH_ERROR_MESSAGES` (AddressAutocomplete.tsx:53), selected only while `fullSearchActive` (line 151), never names the just-failed "Search this full address" button. Every entry points at genuinely different, reachable next steps: "Use manual entry with this address below, or the BBL lookup." Both are real — the `use-manual-entry` button (AddressAutocomplete.tsx:205, gated `offerRecovery && onFallback`; architect always passes `onFallback=openManualFallback`) opens the manual Geoclient resolver with the typed street prefilled and focused, and "the BBL lookup" is the `<details>Search by tax lot (BBL)</details>` disclosure rendered on the same page (ArchitectEntry → PropertySearch). The typed-suggestion path deliberately keeps `ERROR_MESSAGES` (still names the full-address button) — correct, because the user has not tried it yet; that path is not circular. Reads naturally. ADVISORY: none material — the word "below" in the copy resolves to the manual-entry disclosure lower on the page rather than the recovery button (which sits just above the status line), harmless.

**F2 — DB-024(b) one shared absent-BBL note across three surfaces — CLOSED. ADVISORY nuance.**
`ABSENT_BBL_MAP_LINK_NOTE` (AddressAutocomplete.tsx:23) = "The city map link needs a valid BBL, which this lot did not provide." rendered byte-identically at AddressConfirmCard.tsx:150, ZoningContextPanel.tsx:63, PropertyOverview.tsx:67, asserted through the imported constant (address-confirm S7:473-483; zoning-context-panel AS-6 ×2 both notes:267-287). Honest and accurate on all three — in every case there genuinely is no canonical BBL, so ZoLa (the city map link) cannot be built. ADVISORY: on the **confirm card** the "this lot did not provide" framing is slightly loose (no lot is confirmed there yet; the prior card-specific copy said "which this result did not provide"), but the meaning is clear and coherent with the card's own surrounding copy (`resolved-bbl-absent` uses "lot identifier"; `confirm-continue-absent` says "use the BBL lookup on this page instead"). Byte-identical unification is the explicit requirement, and the "lot" framing fits the panel/overview frames best. Not misleading — no correction required.

**F3 — DB-024(c) decorative arrow glyphs — CLOSED. ADVISORY (route to discovery).**
Both glyphs my prior F3 named ("both remaining") are now `<span aria-hidden="true">`: the address-hint ↗ (AddressAutocomplete.tsx:199) and the ProfileViews "Inspect calculation evidence" → (ProfileViews.tsx:53); accessible names are arrow-free ("NYC Planning address suggestions", "Inspect calculation evidence"), asserted in the suites. This meets the **binding** DB-024 letter-map scope (which named only these two). ADVISORY: a fresh accessibility walk of the overview screen finds three sibling `→` glyphs still bare and inside link accessible names — PropertyOverview.tsx:26 "Review conflicting source values →", :33 "Review missing inputs →" (both in `PropertyIssuesSummary`), and :77 "All property facts →". A screen reader hears "Inspect calculation evidence" (clean) next to "Review conflicting source values right arrow" on the same view — inconsistent with the DB-019a pattern. These are pre-existing (not regressed by this packet). The objective's parenthetical named "ProfileViews/**PropertyIssuesSummary** arrow", so lines 26/33 arguably fall under the loose objective even though the binding letter-map named only ProfileViews. Recommend a follow-up discovery item to finish the sweep; not blocking here.

**F4 — DB-024(d) one ZoLa accessible name — CLOSED. ADVISORY only.**
`ZOLA_LOT_LINK_LABEL = "Open ZoLa"` (AddressAutocomplete.tsx:31) at AddressConfirmCard.tsx:145 (no glyph), ZoningContextPanel.tsx:59 and PropertyOverview.tsx:60 (each with decorative `↗`). All three expose the identical accessible name "Open ZoLa", asserted via `getByRole("link", { name: ZOLA_LOT_LINK_LABEL })` in each suite. Clear enough where it goes: the target audience is NYC architects/analysts for whom "ZoLa" is a known city tool, and each site's surrounding context ("Is this the right lot?" + BBL / "Zoning context" / "Site context") anchors the destination to the current lot. ADVISORY: "Open ZoLa" is terser than the prior confirm-card "View this lot on the city's ZoLa map" (drops "this lot"/"map"); and because the ↗ is aria-hidden, the new-tab (`target="_blank"`) nature is not announced to SR users (pre-existing, applies equally to the old copy). Benign redundancy: on PropertyOverview the site link and the nested ZoningContextPanel link both read "Open ZoLa" and both resolve to `zolaLotUrl(same bbl)` → identical URL, so two same-named links point to the same place (harmless).

**F5 — Recovery-journey coherence (end-to-end walk) — COHERENT.**
Typed entry fails → copy names the full-address button (real next step) → user clicks it → full search fails → copy switches to `FULL_SEARCH_ERROR_MESSAGES` naming manual entry + BBL lookup, never the just-failed search → user either takes `use-manual-entry` (prefilled + focused manual resolver) or the BBL disclosure below. Editing the input clears `searchResult`/cancels the in-flight search (onChange, AddressAutocomplete.tsx:~162), so `fullSearchActive` goes false and the typed-path copy returns. No loop, no dead end, no stranding. Integration coverage exists (address-resolution.test.tsx:974 clicks `use-manual-entry`).

**DB-025(e) — validator wording matches behavior — CLOSED (AS-5).**
rule-evaluation-contract.ts module doc + function doc corrected to describe the actual behavior: a positive-shape, documented-key check with no client-side additionalProperties rejection (server owns the closed schema). The diff is docs-only — the validator body is unchanged (it checks only named keys, never iterates `Object.keys` to reject an unmodeled key, returns `ok:true` when the problem list is empty), so no recorded 1.0.0/1.1.0 fixture verdict can move. contract-versions.test.ts DB-025(e) proves an otherwise-valid document with an unknown top-level key still validates `ok`.

## Preservation / scope
- `apps/web/src/lib/address-search.ts` not among the 11 changed files (out of allowed_paths) — retry/outcome/DB-006 `rejected` semantics preserved (AS-6).
- No contract-schema/generated-TS change; DRAFT/not-verified vocabulary unchanged.
- Modularity: three shared constants homed beside the existing `FULL_ADDRESS_SEARCH_LABEL` (single-source-of-truth module, no new util/dumping ground); producer + orchestrator reproduced `modularity_check --check` = 0 failures.

## Verdict
**PASS.** Prior findings F1–F4 all closed; DB-025(e) satisfied; recovery journey coherent. No BLOCKING corrections. Advisories (all non-blocking): F2 confirm-card "this lot" framing nuance; F3 three residual bare `→` glyphs in PropertyIssuesSummary/overview (recommend a discovery follow-up); F4 terser label + new-tab not announced to SR.

Relevant files:
- `C:\Users\MLFLL\Downloads\nyc-zoning\ctl24\apps\web\src\components\architect\AddressAutocomplete.tsx`
- `C:\Users\MLFLL\Downloads\nyc-zoning\ctl24\apps\web\src\components\architect\PropertyOverview.tsx`
- `C:\Users\MLFLL\Downloads\nyc-zoning\ctl24\apps\web\src\components\architect\ZoningContextPanel.tsx`
- `C:\Users\MLFLL\Downloads\nyc-zoning\ctl24\apps\web\src\components\architect\ProfileViews.tsx`
- `C:\Users\MLFLL\Downloads\nyc-zoning\ctl24\apps\web\src\components\address\AddressConfirmCard.tsx`
- `C:\Users\MLFLL\Downloads\nyc-zoning\ctl24\apps\web\src\components\address\AddressResolutionScreen.tsx`
- `C:\Users\MLFLL\Downloads\nyc-zoning\ctl24\apps\web\src\lib\rule-evaluation-contract.ts`
