# M5-T041 — G2 producer self-check record (recorded by the orchestrator)

G2 is the producer self-check gate; the CLI records it under the orchestrator's name (it
rejects the producer's own label). Basis, 2026-09-18 seq-118:

1. **Producer self-checks (loop-2 run persistent2-local-06, worker claude-opus-4-8):** the
   producer report documents the packet's documented command (`python
   tools/modularity_check.py --check`) run in-worktree, and the resubmission embeds the
   changed sections verbatim for review. Web suites deliberately NOT run locally (thin
   client) — CI is the only web proof channel, as the packet requires.
2. **Orchestrator reproduction at harvest (in wt-m5t041):** `python
   tools/modularity_check.py --check` exit 0 (0 failures) — independently re-run before the
   material commit.
3. **CI at the material head `740b6f2f`:** run 35402081543 success (all jobs incl. web +
   web-e2e), secret-scan 35402081496 success, context-budget 35402081495 success
   (orchestrator-captured: `M5-T041-ci-evidence.md`).
4. **Scope check at harvest:** the material diff = exactly 10 apps/web files + the producer
   report, all inside allowed_paths; `address-search.ts` untouched.

**G2 verdict: PASS.**
