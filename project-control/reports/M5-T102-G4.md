# M5-T102 — G4 test-adequacy review (qa-engineer "qa-t102", read-only)

> Transmission history: pinned at cf4fd224, delivered as two SendMessage parts (1/2, 2/2) ending with END-OF-REPORT;
> no truncation. Joined verbatim by the orchestrator (transport wrapper tags removed only). Verdict PASS: all 4 producer
> mutants and 4 of the reviewer's 5 weakenings caught; byte-identity genuine. Advisories A1 (the DXF writer's per-word
> coverage is transitive, not a direct 11-word test), A2 (a report label) and A3 (relative imports) are routed at the
> accept seam.

---

M5-T102 G4 (test-adequacy, read-only) — PART 1/2

PIN: HEAD cf4fd2242807a4d4f19453b0d05a240801bdf803 at start. Material commit 01b52fe8.
Env: Python 3.11.9 (repo CI = 3.12). Documented suite from services/api:
`python -m pytest tests/cad tests/drawings/test_dxf_roundtrip.py -q` -> 345 passed in 3.86s (matches producer report). ruff not re-run (test-adequacy scope).

METHOD: in-process mutations against the CONSUMING namespace (each writer calls the bare imported `contains_claim_word`, so `d/g/writer.contains_claim_word` monkeypatch reaches the code — verified correct technique, not a by-value false-survivor). Detector mutations run through the REAL `_imported_modules` loaded from test_dxf_writer.py.

MUTATION TABLE (CAUGHT = the guard/test reddens):

Producer's 4, re-run:
- P1 GLB raw-substring revert — CAUGHT (real refuses "As_of_right tower"; raw mutant renders it).
- P2 DXF raw-substring revert — CAUGHT (real refuses; raw mutant emits "AS_OF_RIGHT BUILDING" into the DXF).
- P3 PDF title-block screen removal — CAUGHT (real refuses "12 maximum allowed st"; neutered screen renders).
- P4 DXF allowlist flags __import__/importlib — CAUGHT (const `__import__('json')`->{json}; dynamic `__import__(name)`->__import__; importlib ref flagged).

My 5 weakenings:
- M1 claim_key regex narrowed to `[ _]+` (drops hyphen/dot/tab) — CAUGHT: test_claim_key_collapses_separator_runs reddens on "Maximum.allowed"; and PDF "12 As.Of.Right PL" would slip -> test_claim_word_separator_variants_are_refused reddens too.
- M2 drop PERMITTED from the tuple — CAUGHT: PDF renders "12 permitted st" -> test_claim_word_in_caller_text..[PERMITTED] reddens + 3 by-value pins redden. (Note: GLB also renders "Permitted block" and has NO hardcoded per-word test — see A1.)
- M3 PDF drops the printed-form (2nd-arg) screen — CAUGHT: real refuses "AS ſOF RIGHT" via the ASCII-sanitised form; raw-only mutant renders -> the PDF sep-variant param reddens. Printed-form catch is load-bearing.
- M4 __import__ hidden in a NESTED function — CAUGHT: real `_imported_modules` flags nested 'socket' (ast.walk recurses); detector not fooled by nesting.
- M5 relative-import `from .x import y` — SURVIVES (see A3, low severity).

Part 2 follows: byte-identity, drift guard, findings, verdict.

---

M5-T102 G4 — PART 2/2

BYTE-IDENTITY (AS-4) genuine, not tautological: test_cad_owner_samples.py rebuilds all 3 samples through the writers' PUBLIC functions and asserts equality to the committed bytes, WITH an explicit anti-vacuity guard (test_as3_byte_identity_comparison_is_strict: build != committed[:-1]+b"X"). The three writer goldens each hash a fresh render() call. All re-passed in the 345-green run. The screen only reads (never mutates emitted text) and goldens use clean inputs, so refusal-only change => bytes unchanged. Confirmed.

DRIFT GUARD (AS-3 clause 2) real: 3 by-value pins (test_claim_words test_as3_canonical_word_list_pinned_by_value; dxf test_as4_claim_class_words_are_the_expected_set; pdf _PINNED_CLAIM_WORDS) assert the HARDCODED literal, plus PDF's 11x4 hardcoded-parametrized behavioral coverage. A dropped word reddens.

DETECTION SANITY (my probes): all 11 canonical words detected; no false positives on clean/near-miss text ("1 LEGACY PL" guards LEGAL vs LEGACY; "12 PERMIT ST" guards PERMITTED vs PERMIT). case-fold drop and separator-class narrowing both redden the claim_key unit test. Note: "AS-OF-RIGHT" is a behaviorally redundant tuple entry (claim_key collapses hyphens to "AS OF RIGHT"); removing it flips no refusal — only the by-value pins guard it. Harmless.

FINDINGS:
BLOCKING: none.
A1 (ADVISORY, recommended hardening) — AS-3 "every word tested against EACH writer": GLB (11), PDF (11x4) and the shared module (11) have full per-word coverage; DXF exercises only APPROVED, MAXIMUM ALLOWED + the as-of-right/maximum-allowed separator families through the screen. Current DXF correctness IS assured transitively (shared-fn all-11 + `dxf_writer.contains_claim_word is contains_claim_word` identity pin + P2). Only a contrived partial hand-inline (drop the shared call, keep the tested subset, leave the import) could ship green — both PLAUSIBLE DXF regressions (raw-substring, dropped screen/word) are caught, so non-blocking. Recommend a parametrized DXF test feeding all 11 words via monkeypatched GENERATOR_NOTE (mirror GLB test_as3_every_canonical_word_refused_as_name).
A2 (ADVISORY) — producer report's AS-3 DXF evidence cites the by-value pin (clause 2) as per-word coverage (clause 1); it is not. Accuracy only.
A3 (ADVISORY, low) — `_imported_modules` skips ImportFrom level>0, so `from .x import y` evades the allowlist; relative imports reach only app siblings and cannot admit a third-party dep. Optional: also flag level>0.

Verdict rationale: production code correct (all 3 writers route every word to one shared, identity-pinned screen), all producer mutants + all plausible reviewer weakenings caught, goldens genuinely regenerate, drift guard has teeth. Sole gap is DXF per-word coverage completeness (non-blocking, current behavior proven).

M5-T102 G4 VERDICT: PASS

END-OF-REPORT
