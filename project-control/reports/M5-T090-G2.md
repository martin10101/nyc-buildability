# M5-T090 - G2 producer self-check record (recorded by the orchestrator, role self_check)

Producer: frontend-engineer (orchestrator-dispatched subagent) in wt-m5t090; one in-worktree commit
0d6cc897 (package.json + report), then the reviewed generate-lockfile workflow (run 35985974452,
SUCCESS) committed 82d2e54d (package-lock.json only) on the pushed task branch. Both cherry-picked
(80f17a86, 2e444920) with every file MATCH by git blob identity.

- **Scope:** exactly apps/web/package.json (+"@react-three/fiber": "9.7.0", +"three": "0.186.0"; the
  react-dom line only gained a comma), apps/web/package-lock.json (bot) and the report. No src/, e2e/,
  .npmrc, services or .github change.
- **Producer evidence (report, OBSERVED):** registry publish times, integrity, tarballs, maintainers,
  install scripts (none), licences (MIT), dependencies and peer ranges (fiber peers react >=19 <19.3 ->
  19.1.2 fits; three >=0.156 fits); OSV + GitHub advisory queries empty for both versions with a
  positive control (three@0.124.0 returns GHSA-fq6p); necessity argued against MapLibre fill-extrusion.
- **[ORCH-HARVEST] lockfile check at the cherry-picked head:** three 0.186.0 and @react-three/fiber
  9.7.0 resolve with the report's integrity values; 13 new packages (fiber + nested scheduler,
  @types/react-reconciler, @types/webxr, base64-js, buffer, ieee754, its-fine, react-use-measure,
  suspend-react, three, use-sync-external-store, zustand); 0 changed and 0 removed versions vs the
  previous lock. The workflow itself ran npm ci (integrity), blocking npm audit at every severity, the
  committed-lock release-age gate and the npm CLI advisory check before committing.
- **Discoveries routed (producer):** D1 @types/three needed by the first consumer (its own G5 admission;
  pulls 6 runtime deps incl. a WASM physics engine); D2 fiber maintainer list 22 -> 8 with one new
  account (matters for future upgrades); D3 React 19.3+ bump must move fiber too; D4 @types/webxr '*'
  range floats at each regeneration; D5 stale code-graph cache.

G2 verdict: PASS (self_check; never independent review - G4 qa-engineer, G5 security-reviewer section-5
provenance review of all 13 new packages, and the DCV follow; CI web-dependency-security on the pushed head is AS-3).
