# M5-T074 — G2 producer self-check record (recorded by the orchestrator, role self_check)

Producer: qa-engineer (lane-3 worker, run persistent3-local-18-m5t074). The unit closed
clean with the test addition delivered and the scoped pytest explicitly routed
([BLOCKED → harvest] class — root-cwd collection is the known invocation artifact, not a
defect); the orchestrator executed the harvest and appended it verbatim as the report's
[ORCH-HARVEST] section.

- **Scope self-check (report, [OBSERVED]):** test-only additions inside
  `tests/api/test_outline_bridge.py` (:709–806 — the generic-500 constants, one shared
  oracle, four live-500 tests) + this report; every pre-existing assertion byte-unchanged;
  `git status --porcelain` confined to the two allowed paths; zero production edits (AS-4).
- **Design note (report):** the shared `_assert_bounded_internal_error` oracle pins the
  EXACT `{state, message, correlation_id}` body via `set(body)` — any widening of
  `_internal_error_500` reddens all four tests (the DB-046 leak-gate pattern); each test
  adds its own `# secretscan:allow` leak sentinel + type/traceback absence asserts;
  reachability corroborated against the bounded production excerpts (:605–614, :700–717,
  :783/:617–622/:820–824) inlined in the report.
- **Harvest execution of the recipe (orchestrator, cwd services/api, wt-m5t074):**
  `python -m ruff check .` → All checks passed (exit 0); `python -m pytest
  tests/api/test_outline_bridge.py -q` → **42 passed** (the four live-500 proofs green
  among them) — the AS-1/AS-3 [UNVERIFIED] rows elevated to OBSERVED.
- **Modularity:** [OBSERVED] failures 0 / warnings 22 (all pre-existing; the test file not
  flagged; `outline_bridge.py`'s review_signal warning is the pre-existing production
  standing watch, untouched by this test-only change).
- **Identity:** in-wt commit cherry-picked, MATCH x2 (LF-normalized); 11 stale asks denied
  from piped JSON at harvest; CI on the pushed head is the backstop.

G2 verdict: PASS (self_check; never independent review — G3/G4 follow; G4 carries the
report's body-widening-mutant instruction for backend-engineer).
