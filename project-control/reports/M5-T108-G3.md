# M5-T108 — G3 code review (code-reviewer "cr-t108", read-only)

> Transmission history: pinned at f4c4ab3a, delivered as three SendMessage parts (1/3..3/3) ending with END-OF-REPORT;
> no truncation. Joined verbatim by the orchestrator (transport wrapper tags removed only). Verdict PASS with advisories
> A1-A6; A2 and A6 go into the rework round with the G4/G5 findings, A1/A3/A4 are PKT-H mount items.

---

M5-T108 G3 code review (DXF import service + UNMOUNTED route): PASS with advisories — report in 3 parts.

PIN: HEAD f4c4ab3a696f52fb32e460699a82236ef4457c7a (includes material commit 2df81013). Reviewed READ-ONLY.

OBSERVED evidence (this HEAD; cwd services/api unless noted):
- `ruff check` on the 4 T108 files AND `ruff check .` (full tree): "All checks passed!" — the 4 sibling-placeholder E501s the producer report noted are resolved now that M5-T106/T107 landed.
- `pytest tests/drawings/test_dxf_import.py test_dxf_import_api.py -q`: 41 passed.
- `pytest tests/drawings/test_dxf_reader.py test_dxf_roundtrip.py -q`: 61 passed (reader untouched, no regression).
- repo root `tools/modularity_check.py --check`: exit 0 (dxf_import.py 509 / dxf_import_api.py 407 lines, both under WARN 600; the warns listed are unrelated files).
- `git show --stat 2df81013`: exactly the 5 allowed_paths files; dxf_reader.py, app/main.py, requirements*, config.py all untouched. Zero new deps.

Contracts verified in actual source (non-findings):
- Draft block matches validate_proposed_massing (proposal.py:507): outline{srid 2263, vertices}, levels[level_index/floor_count/floor_to_floor_ft], exterior_walls[], provenance{author,editor_version,kind:"proposed"}. The out-of-bounds test's exc.field starts "proposed_massing.outline" exactly as asserted.
- Candidate index is produced by the SAME `_closed_ring_candidates` in both list_candidates and build_draft (dxf_import.py:293) → the index a user sees in /candidates maps to the same ring in /draft.
- Unit vocabulary matches the reader's _INSUNITS_NAMES exactly (dxf_reader.py:136): us_survey_feet/meters/unitless/inches/feet/yards.
- Reused T053 primitives exist with matching signatures (proposal_validation.py:187 _declared_content_length, :203 _read_body_within_ceiling→(bytes,bool), :120 _bounded_message); MAX_BODY_BYTES=262144.
- 404 sentinel {"detail":"Not Found"} with no correlation id is byte-identical to the accepted sibling (proposal_validation.py:113; lot_geometry matrix :100).
- Deadline: asyncio.wait_for raises TimeoutError (3.11+ alias), caught; thread not force-killed but bounded by the clamped limits (disclosed) — sound.
- Honesty (D-083): label "Proposed - not a city record", precision "imported drawing - not survey-confirmed", set once and never upgraded (D-083-R006); every log line is correlation-id only.
- Scale-into-2263 does NOT invent georeferencing: an ungeoreferenced/local-origin drawing fails the NYC-bounds contract and is surfaced typed, never auto-corrected.

Advisories follow in part 2.

---

M5-T108 G3 — ADVISORY findings (none blocking):

A1 (rate-limiter memory is NOT bounded — the packet's explicit question). dxf_import_api.py:137-163. `_RateLimiter._hits` gains one entry per distinct client key for the process lifetime; a key's timestamp list is pruned only when THAT key returns, so keys that never return are never evicted. Per-entry cost is tiny (host string + ≤30 floats) but the key count is unbounded. Acceptable for an unmounted internal route; MUST be fixed at PKT-H when it mounts — add stale-key eviction (opportunistic sweep or max-keys/LRU). Answering the packet directly: memory is NOT bounded, and YES it should become a shared module at PKT-H (max_envelope/proposal_validation will want the same per-caller limiter rather than a route-local copy). Thread-safety (single lock) and the monotonic clock are correct.

A2 (fragile, untested disclosure counts). dxf_import.py:325-332. Entity-kind counts use `type(p).__name__ == "LinePrimitive"/"FacePrimitive"/"TextPrimitive"`; those classes are not imported and no fixture contains a LINE/3DFACE/TEXT, so the branches are unexercised and would silently report 0 if the reader renamed a class. Currently CORRECT — I verified the names match dxf_reader.py:239/262/271. Recommend isinstance against imported classes + a disclosure fixture.

A3 (malformed-param /draft skips the limiter). dxf_import_api.py:384-389. `_parse_assignment` runs before `_guarded_read`, so a bad-param request returns 422 without consuming rate budget or reading the body. Cheap path (query-param parse only), low value; consider rate-limiting before param parse at mount.

A4 (256 KiB ceiling may be tight for real DXF). dxf_import_api.py:98 reuses proposal_validation.MAX_BODY_BYTES=256 KiB; a real architect DXF footprint can exceed this. Fails closed (never opens); revisit the clamp for DXF at PKT-H.

A5 (SECTION sniff scans only the first 4096 bytes). dxf_import.py:260-275. A valid ASCII DXF with a large leading 999-comment block could be refused "no SECTION marker". Fails closed (safe), rare.

A6 (minor test gaps). The (500,"internal_error") matrix pair is neither asserted in test_status_state_matrix nor exercised; disclosed_unknown/skipped_sections escaping passthrough is untested; a closed polyline with <3 vertices is silently absent from both candidates and the open-polyline count.

DB-070(c) deferral is SOUND: max_line_chars (4096) caps a group-code line below CPython's int-str digit limit (default 4300), so no reader change is needed now; correctly routed as a DISCOVERY for the next reader touch.

Verdict follows in part 3.

---

M5-T108 G3 — verdict (part 3 of 3).

Summary: the service is pure, deterministic, and honest; the route mirrors the accepted unmounted/flag-gated sibling pattern (include_in_schema=False, generic 404 sentinel, absent from the real app + OpenAPI, verified by test_route_is_unmounted_in_the_real_app). Parse-time controls (b)-(e) are all present, tested, and each has a reddening consuming-namespace mutation. The second DXF_IMPORT_ENABLED flag is genuinely additive (AND-ed with INTERNAL_RULE_EVAL_ENABLED), strictly more conservative, config.py untouched, canonical registration correctly deferred to PKT-H. Discrepancies are shown, never auto-reconciled (units-mismatch tested on both service and route). Scope is exactly the 5 allowed files; zero new deps; persists nothing (no storage import anywhere; test_no_persistence_state_between_requests). All AS-1..AS-5 are met.

Every advisory targets the PKT-H mount seam (chiefly A1: bound the limiter's memory and extract it as a shared module) or minor test hardening; none blocks acceptance of an unmounted internal packet.

M5-T108 G3 VERDICT: PASS

END-OF-REPORT
