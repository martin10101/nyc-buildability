# M5-T043 — G2 producer self-check record (recorded by the orchestrator)

G2 is the producer self-check gate; the CLI records it under the orchestrator's name (it
rejects the producer's own label). Basis, 2026-09-19 seq-118:

1. **Producer self-checks (loop-1 run persistent-local-47, worker claude-opus-4-8):** the
   producer report documents all five documented commands worker-run green in-worktree
   (ruff clean; wiring+matcher, provider, and consumer suites; modularity failures 0), an
   explicit worker-vs-supervisor evidence split, and the honest uncommitted-working-tree
   framing.
2. **Orchestrator reproduction at harvest (in wt-m5t043):** the same five commands re-run
   independently green before the material commit — ruff clean; 101 passed (wiring +
   matcher); 39 passed (provider); 475 passed (tests/api + test_rules_integration, zero
   consumer edits); modularity exit 0 from the repo root with BOTH owned rules modules off
   the warn list (the extraction's measurable outcome).
3. **CI at the combined harvest head `a380a956`:** run 35412920210 success (all jobs incl.
   web + web-e2e), secret-scan 35412920196 success, context-budget 35412920224 success
   (orchestrator-captured: `M5-T043-ci-evidence.md`); no M5-T043 allowed_path differs
   between material `f6e88222` and that head.
4. **Scope check at harvest:** the material diff = exactly 7 files, all inside
   allowed_paths; contract files and every consumer source file byte-untouched.

**G2 verdict: PASS.**
