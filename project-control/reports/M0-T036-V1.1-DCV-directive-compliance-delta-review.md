# Directive-Compliance Delta Verification — M0-T036 V1.1 rows R577–R584 (frozen c193a52)

> Preserved VERBATIM by the orchestrator from the dcv-v11-m0t036 agent-return channel
> (2026-08-04). Transport entity-decoding only; no edits.

D-007 DELTA DIRECTIVE-COMPLIANCE VERIFICATION — M0-T036 V1.1, rows R577–R584 — VERDICT: **PASS** (6 SATISFIED-BY-EVIDENCE, 2 PENDING-BY-DESIGN, 0 VIOLATED, 0 UNVERIFIABLE). Verifier: dcv-v11-m0t036 (directive-compliance-verifier), read-only, producer ≠ verifier. HEAD verified frozen at `c193a52e0ac1bf4ceb7e0a1e67bb866d02b2605c` (git rev-parse, unmoved throughout). Every finding below reproduced by me from primary evidence; the producer report `project-control/reports/M0-T036-V1.1-producer-report.md` was treated as a claim and re-derived.

## 0. Registry integrity at c193a52 (verified first)

- `python tools/validate_directive_compliance.py --check` → exit 0.
- `source-008-amendment.md` present, append-only (new file in delta; no prior source edited — diff 43848bd..c193a52 shows only additions to the D-007 registry). manifest.json entry: sequence 8, amends source-007, recorded_at `2026-08-04T08:40:00+00:00`, content_digest_sha256 `107b1cd2…ee8ee2cb` — I recomputed the SHA-256 of the file myself: **exact match**. manifest.audit_log has the amendment-7 capture entry at the same timestamp.
- `locked_requirement_ids` includes D-007-R577..R584 (manifest lines 761–768). requirements.json rows R577–R584 all carry `source_ref: source-008-amendment.md#the-owner-message`, `amendment_sequence: 8`; each row's text is a faithful atomic decomposition of the owner message — no missing, weakened, combined, or invented item against the source (I compared row-by-row against the verbatim message).
- D-007 verification.json contains NO R577–R584 rows — correctly pending this verification, not pre-marked.
- Harness (all run by me at c193a52, exit 0): `test_directive_compliance.py` OK; `test_project_control.py` all 22 groups OK; `test_directive_reminder.py` OK.

## Per-row table (id | status | primary evidence)

| Row | Status | Primary evidence (reproduced) |
|---|---|---|
| R577 DO-NOT-ACTIVATE adopted; shadow-only; supervised+limited-auto OFF | **SATISFIED-BY-EVIDENCE** | Affirmative negative: full delta `git diff 43848bd..c193a52` contains NO mode enablement — no .toml/config change of any kind (only .json deltas are control-plane records; state.json delta = updated_at only). `tools/agent_supervisor/config.example.toml:22` `default_mode = "shadow"` unchanged; `config.py:57` `BOOTABLE_DEFAULT_MODES = (MODE_SHADOW, MODE_SUPERVISED)` — limited-auto can never be a config boot default; limited-auto remains refused by name (`LimitedAutoRefused` LoopError, cli delta comment). The supervised-path code fixes (B-1) are the owner-authorized corrections, not activation: no supervised run occurred (no new pilot-run artifact; PILOT-LOG delta is text-only erratum). |
| R578 §6 allowlist NOT adopted, inert | **SATISFIED-BY-EVIDENCE** | Affirmative negative: §6 exists only inside `M0-T036-PHASE5-DECISION-PACKET.md` and is headed "PROPOSAL ONLY — inert until the owner's separate activation decision". No AUTO/NOTIFY/ASK tier or standing grant written into any config/policy file — repo-wide grep for allowlist structures hits only the pre-existing per-provider MODEL allowlists (config.py/cli.py), a different mechanism. SESSION_HANDOFF §3 lists the proposal as NOT AUTHORIZED/inert (R578). |
| R579 V1.1 scoped to exactly B-1..B-4/F-2/F-4/F-5 | **SATISFIED-BY-EVIDENCE** | Every item located in the code diff myself: B-1 `sent_prompt` threading (loop.py, 10 added lines + ForwardedPromptThreadingTests); B-2 `POLICY_CHECK→PREFLIGHT cycle_closed` edge (state_machine.py, comment cites "V1.1 correction B-2") + cli refusal-report catch; B-3 `multiple_distinct_checkpoints` CheckpointError (claude_runner.py); B-4 `CODEX_REVIEW→PAUSED_RECOVERY unsafe_condition` edge (cites "V1.1 correction B-4") + per-checkpoint reset; F-2 `clear-recovery` CLI command; F-4 `CHECKPOINT_CONTRACT`/`checkpoint_contract` derived block (claude_runner.py, 12 added lines); F-5 `_check_timezone_database` doctor check (cli.py). Delta paths confined to 7 supervisor modules + 5 test modules + control-plane records + SESSION_HANDOFF (out-of-scope path grep: empty). Tests reproduced by me at c193a52: full suite (19 modules) **1091 passed, 2 skipped, exit 0** — exactly the producer's claimed figure; the 5 delta-touched modules alone: 307 passed. |
| R580 hardening only delta-scoped + separately cited; nothing else | **SATISFIED-BY-EVIDENCE** | L-1: `process.py::assert_argv_safe` `=`-form hard-deny, in-code citation "V1.1 hardening L-1 (G3 review)" (whole process.py delta = that one hunk). V-6: `audit_log.py` `head_anchor_corrupt` fail-closed, cited "V1.1 hardening V-6 (G3 review)"; absent-anchor stays legitimate. V-4 partial: loop.py precise not-OK reasons, cited "V1.1 correction V-4"; residual honestly documented. circuit_breakers.py delta is B-4 documentation-not-rename, cited "G3 finding B-4". Out-of-scope check: `grep` of all added lines for L-3/L-4/V-2/V-3/V-5 = **0 hits in code** (they appear only as SKIPPED in the producer report). |
| R581 re-gate at new frozen SHA, same scheme | **PENDING-BY-DESIGN (in execution, no violation)** | c193a52 verified as the new frozen SHA (rev-parse; commit message cites D-007-R579; team-lead binding names it). The same five-review scheme is running now — session roster shows g3-v11-m0t036, g4-v11-m0t036, g5-v11-m0t036, cpv-v11-m0t036 plus this DCV delta review. Satisfaction completes when the orchestrator records all five delta returns; nothing contradicts the obligation. Shadow-only in force throughout (R577 evidence). |
| R582 doctor --live + supervised rehearsal NOT authorized | **SATISFIED-BY-EVIDENCE** | Affirmative negative: the `--live` code PRE-DATES the delta (introduced at Phase 3 commit 3a09aff per `git log -S`); the cli.py delta contains zero `-live` lines — nothing added. No live probe was RUN: the producer's doctor output is the offline form (`python -m tools.agent_supervisor doctor`, no --live). No supervised rehearsal: no new run artifact anywhere in the delta; the only PILOT-LOG change is the append-only run-4 erratum required by the G4 QA review (text correction, cites its basis, preserves history). SESSION_HANDOFF §3 lists both as NOT AUTHORIZED pending post-re-gate owner return (R582). |
| R583 M0-T035 acceptance owner-typed only | **PENDING-BY-DESIGN, boundary AFFIRMATIVELY HELD** | `project-control/tasks/M0-T035.json`: status `awaiting_gate`, accepted_at null — no accept act anywhere. D-006 verification recorded: `D-006-dispatch-efficiency-and-graph-wiring/verification.json` task_verifications[M0-T035] state **PASS**, all 9 rows PASS at reviewed_sha 4265994, verifier directive-compliance-verifier ≠ producer backend-engineer; verbatim return preserved at `reports/M0-T035-D006-independent-verification.md` (commit 4110fe1). The "prepare acceptance summary + exact console lines" sub-obligation is NOT yet discharged (no such artifact in reports/ — grep empty for M0-T035); handoff sequences it after this verification recording, consistent with the amendment. No violation. |
| R584 handoff refreshed; message captured verbatim | **SATISFIED-BY-EVIDENCE** | `docs/SESSION_HANDOFF.md` refreshed 2026-08-04 (commit dba17c1, "D-007-R584" in message): reflects Phase 5 complete, the owner decision R577–R584, the V1.1 unit + delta re-gate, M0-T035 owner-typed acceptance, and the NOT-AUTHORIZED list. Capture: `source-008-amendment.md` exists, digest-locked (recomputed match), audit-logged at capture time. Caveat stated plainly: I cannot see the owner's original chat message, so "verbatim" is verified as digest-locked capture + full consistency with your binding message's description — not by comparison to the original channel. |

## Affirmative boundary findings (R577/R582/R583)

Within all repository evidence at c193a52: (1) nothing activates, enables, defaults, or runs supervised or limited-auto mode — shadow remains the only default and the only mode ever run; (2) no live-probe code was added and no live probe or supervised single-forward rehearsal was executed or recorded; (3) no accept act occurred for M0-T035 — status awaiting_gate, verification recorded as evidence only, acceptance authority untouched and owner-typed only.

## Unverifiable / limitations (stated plainly)

- Verbatim-ness of source-008 vs. the owner's original chat message: outside repository evidence (see R584 caveat); everything checkable about the capture (existence, digest, audit-log, decomposition fidelity) checks out.
- Runtime absence of a supervised rehearsal on the owner's machine (parked `%LOCALAPPDATA%` dirs) is verified only to the extent the repository records it — no run artifact, journal, or report entered the repo; nothing suggests one occurred.
- R581/R583 carry forward obligations that by design complete after this wave (record five returns; hand the owner the console lines) — pending, not violated.

No requirement VIOLATED or UNVERIFIABLE; PASS stands for the delta rows R577–R584 at c193a52.
