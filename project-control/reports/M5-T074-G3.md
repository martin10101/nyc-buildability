# M5-T074 — G3 independent code review (code-reviewer, read-only)

> Transmission history: returned across seven transmissions — Parts 1–2 (truncated at "The
> oracle's `resp.jso"), a restart chunk re-covering the typed-vs-untyped sentence, its
> completion, the Part-3 oracle-soundness chunk, the load-bearing-sentinel chunk, then a
> final message that skipped ahead to F5–F7 + assessment + verdict, and a gap-fill message
> carrying the missing F1–F4 span (requested by exact boundary phrases). Joined verbatim in
> report order; the duplicate typed-vs-untyped sentence from the restart chunk is joined
> once at the overlap. Saved verbatim otherwise by the orchestrator (transport
> entity-decoding only).

**M5-T074 — G3 independent code review (code-reviewer, read-only)**
Pinned head verified: `git rev-parse HEAD` = `9bc68dc733c17d60dfdb2add1672e0faceb47d73`. Material commit `ea2cdb65`. Surface: `services/api/tests/api/test_outline_bridge.py` :709–806 (+111/−0) and `project-control/reports/M5-T074-producer-report.md`.

**Part 1 — verification performed**

Identity and scope. `git show --name-only ea2cdb65` lists exactly the two allowed paths; `git show --numstat` gives 111 additions and **zero deletions** on the test file, so every pre-existing assertion is byte-unchanged. `git diff ea2cdb65 HEAD -- services/api/tests/api/test_outline_bridge.py services/api/app/api/v1/outline_bridge.py` is empty, so the reviewed content is identical at the pinned head and production `outline_bridge.py` was not touched by the material commit nor by anything after it. `git status --porcelain services/api project-control/reports` is empty — no live mutant in the working tree (the G4 body-widening mutant was properly reverted).

Re-run from `services/api` cwd: `python -m pytest tests/api/test_outline_bridge.py -q` → **42 passed**; `python -m ruff check .` → **All checks passed!**. No added line exceeds 100 characters (the only ≥101-char lines in the file, :117 and :303, are pre-existing). The `# secretscan:allow <note>` pragma form matches the repo convention (`test_max_envelope_api.py:369`).

Production corroboration. The report's inlined excerpts match the real file at the pinned head exactly: `_internal_error_500` at :605–614, the fetch `try`/`except RingUnavailable`/`except Exception` at :700–713, the CRS guard at :715–717, `display_identity` spread at :783, `_assert_json_safe` at :617–622, the tail guard at :820–824. `_json` (:555–558) is the only place `X-Correlation-ID` is set.

Branch exclusivity. `git grep -n "_internal_error_500" -- services/api/app` shows **exactly three** call sites in `outline_bridge.py`: :713, :717, :824. That makes the reachability argument deterministic rather than merely plausible, which is the core of this task's claim.

**Part 2 — reachability, branch by branch**

Test 1 (`test_500_fetch_stage_internal_defect_is_bounded_generic`, :733). The `client` fixture's `_seam` raises any `Exception` instance it is handed, and `display_ring(...)` is invoked at :701 *inside* the try block — not at dependency-resolution time. `RuntimeError` is not `RingUnavailable`, so :703 is skipped and :709 catches it → :713. No other 500 site is reachable when the exception is raised before the rings exist. Claim holds.

Test 2 (`..._display_side`, :746). Both seams return valid `ParcelRing`s, so :709 cannot fire; `display.crs == "EPSG:2263"` makes the first disjunct of :715 true → :717. Correctly noted in the comment that **no exception is raised** on this branch.

Test 3 (`..._authoritative_side`, :765). Display ring is the default `_display_ring()` (`EPSG:4326`, first disjunct false); `authoritative.crs == "EPSG:4326"` makes the **second** disjunct true. This is genuinely a different operand of the same `or`, not a duplicate of test 2.

Test 4 (`..._serialization_unsafe_tail_guard`, :782). Seams return normally (:713 excluded), CRS is correct on both (:717 excluded), the geometry is the happy-path geometry so `_outside_neighborhood`, `fit_correspondence`, the residual gate and the ambiguity gate all pass; the planted `float("nan")` lives only in `source_detail`, which is spread into `display_identity` at :783 and into `document["correspondence"]["source_display_ring"]`, where `json.dumps(..., allow_nan=False)` at :621 raises → :824. With :713 and :717 excluded by construction and only three sites existing, the 500 the test observes can only be :824.

Typed vs untyped. I confirmed empirically that an unhandled exception under `TestClient(raise_server_exceptions=False)` yields `500` with body `Internal Server Error`, `content-type: text/plain`, and **no** `X-Correlation-ID`. The oracle's `resp.json()`, message equality and header-identity asserts would therefore all fail on an untyped ASGI 500. Each of the four tests thus proves the route's own handler ran, which is the substantive claim in AS-1.

**Part 3 — oracle soundness and findings**

The shared `_assert_bounded_internal_error` (:715–730) is the right design: `set(body) == _GENERIC_500_KEYS` is an exact-shape check, so any added field — detail, type, traceback, echoed input — reddens all four tests at once. `_GENERIC_500_MESSAGE` is an independent restatement of the production literal rather than an import, which is correct for an oracle. Header-to-body correlation identity is asserted on every 500.

One genuinely load-bearing leak assert: test 4's sentinel, because the pre-existing 200 test at :273–275 asserts that `source_display_ring` echoes exactly the display ring's `crs`, `source_id` and `source_detail` — so that provenance *would* have reached the client on the success path, and its absence on the 500 path is a real proof, not a tautology.

F1 (low, advisory — `test_outline_bridge.py:803`). `assert "nan" not in resp.text.lower()` is sound today only by accident of vocabulary: the fixed body is `{state, message, correlation_id}`, the message contains no "nan" substring, and `uuid4().hex` cannot contain the letter `n`. It would false-positive if the generic message ever gained a word such as "maintenance". No action needed now; the exact-shape oracle already covers the case.

F2 (low, advisory — :741–743, :760–762, :777–779, :802–805). The per-test `RuntimeError` / `internal-only-src` / `ValueError` / `Traceback` / sentinel asserts are logically subsumed by the exact-shape plus fixed-message checks. Keep them (they document intent and would still gate if `message` ever became dynamic, as `_bounded_message` is on other states), but the real leak gate is the oracle — worth knowing when judging the G4 mutant.

F3 (low, advisory — :715–730). Leak absence is asserted against `resp.text` only, never response headers. No gap exists today because `_json` sets only `X-Correlation-ID`, but the assertion set would not notice a future leaking header.

F4 (low, advisory — :700–707, :734, :747, :766, :783). Test comments carry hard line-number anchors into production (`:709-713`, `:715-717`, `:820-824`, `:605-614`). All four verified accurate at `9bc68dc7`; they will silently drift on any edit to `outline_bridge.py`. Same class as the T075 F1 anchor-staleness item dispositioned at accept.

F5 (medium — `project-control/reports/M5-T074-evidence-map.json:3`, outside allowed_paths, orchestrator/DCV). `material_commit` is prose — "the wt-m5t074 in-wt commit cherry-picked onto the seq-126 chain (2 files, MATCH x2 LF-normalized; +381/-4, test file additions-only). CI backstop rides the next push." — rather than the literal SHA `ea2cdb65dfb2fa64c5312042f742a0abc73b23b4`. The commit is recoverable from git log, but the map does not itself bind the reviewed material identity, which is the one job that field does. Recommend an `[ORCH-CORRECTED]` fix to the literal sha.

F6 (low, advisory — evidence-map:7–9, route to DCV). The single `D-084-R003` row is largely work-content prose. R003 is an operational lane-occupancy obligation (`producer: orchestrator`; `required_evidence: "Run journal for persistent3-local-17 + the harvest/re-feed seam commits"`; its own `status_reason` says it is "Sentinel-bound only… never a work-content obligation"). The row does open with the re-feed claim, which is the relevant clause, but cites no run-journal path. Test content can neither satisfy nor violate R003, so this is a DCV question, not a G3 blocker. Same class as the T070 DCV finding that a row must argue its requirement's actual clause.

F7 (informational — :715, :730, :727). `_assert_bounded_internal_error` is annotated `-> dict` and returns `body`, but all four callers (:740, :759, :776, :801) discard it; and `assert "vertices" not in body` is subsumed by the exact-shape check. Harmless.

Coverage note (favourable). `OUTLINE_BRIDGE_STATUS_STATE_MATRIX` (`services/api/app/api/v1/outline_bridge.py:128–141`) has nine rows. The pre-existing membership-only test at :469–475 covers six; live proofs already existed for 404 (:456), 413 (:440–445) and 502 (:434). `(500, "internal_error")` was the **only** row with no live response anywhere in the suite, exactly as the producer report and DB-045(c) state. This addition closes the matrix's live coverage completely.

Assessment. Reachability is correct and exclusive for all four branches; the oracle is sound and distinguishes a typed 500 from an untyped ASGI 500; the shared-oracle design concentrates the leak gate in one place that a body-widening mutant reddens four times over; zero production edits, zero deletions, ruff clean, 42 passed re-run independently from `services/api`. The findings are advisories plus one evidence-map traceability correction (F5) that belongs to the orchestrator and does not touch the material surface.

G3 VERDICT: PASS

END-OF-REPORT
