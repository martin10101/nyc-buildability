# M5-T124 — G5 security review (security-reviewer "sec-t124", read-only)

> Transmission history: pinned at f2986fac (material c65a0724; CI green at f2986fac), delivered as 2 SendMessage parts
> ending with END-OF-REPORT; no truncation. Joined verbatim by the orchestrator (transport wrapper tags removed only).
> Verdict PASS; 0 Critical / High / Medium; two LOW advisories (LOW-1 a repr'd caller srid / kind echoed in an
> aligned_invalid detail - unreachable in the real flow; LOW-2 no pre-transform size cap on the draft outline - linear work).

---

M5-T124 G5 security review (READ-ONLY, security-reviewer) — Part 1/2. VERDICT: PASS. 0 Critical / 0 High / 0 Medium; 2 LOW advisories (both ADVISORY, non-blocking).

HEAD pinned f2986fac26948af907cb0a3c2cd118e74279470f. Material identity CONFIRMED: `git diff --stat c65a0724 HEAD` for both code files is EMPTY — the reviewed files are byte-identical to material c65a0724 (cherry-pick of wt-m5t124 ede877bb). c65a0724 touched EXACTLY the 3 allowed paths (drawing_alignment.py, test_drawing_alignment.py, the report); grep for app/api|app/main.py|app/scenario|app/connectors|sheet_import|dxf_import|requirements|.github = NO forbidden path touched. Baseline (cwd services/api): `pytest tests/drawings/test_drawing_alignment.py -q -p no:cacheprovider` → 39 passed.

The M5-T108 blocking finding (huge-but-finite coords → non-finite output → uncontrolled 500) is CLOSED here and independently verified. This pure service has TWO output surfaces and both are guarded: (a) the transformed outline goes through validate_proposed_massing (drawing_alignment.py:677) whose finiteness + NYC-2263 bounds check (proposal.py:248/254) refuses a nan/huge vertex as typed `aligned_invalid` — the ONLY ok=True construction (line 697) is post-validate, no alternate path (mutation-proven, test:389); (b) every scalar the alignment block emits passes `_finite_numbers` (line 598/690) → non-finite → typed `non_finite_result`. Discrepancy-dict numbers all mirror top-level checked values, so no non-finite escapes either surface.

Numeric safety — VERIFIED by 9 in-process hostile probes; align_draft_to_lot NEVER raises, every failure a typed AlignmentRefusal VALUE:
- huge-but-finite BOTH src+tgt (1e300) → transform goes nan → `aligned_invalid` (caught by re-validate), NOT an exception.
- huge target only → outline out of bounds → `aligned_invalid`.
- tolerance_ft = inf / nan → `non_finite_input` (line 629; `_is_real` excludes non-finite AND bool) — an infinite tolerance CANNOT be set to hide residuals.
- tolerance_ft = 1e300 (huge-but-finite) → ok, but every per-pair residual is STILL emitted (residuals_ft, max_residual_ft) — the tolerance gates only the boolean flag, never the raw data; no suppression by input choice.
- self-intersecting (bowtie) lot ring → ok, outside area computed via buffer(0) repair (line 393), no exception.
- huge-finite lot ring (1e200) → ok, outside stays finite (bounded by the in-bounds outline), no overflow.
- garbage batch (None block/lot/pairs, non-list pairs, nan coord, bool coord, malformed outline) → 0 exceptions, all typed refusals.
- normal alignment block json.dumps(allow_nan=False) → OK (the future route's contract holds).

Division guards all reachable: saq==0 → `coincident_source` (line 648), hypot(dot,cross)==0 → `indeterminate_rotation` (654), both BEFORE _rigid_fit/_implied_scale; _mirror_fit handles mnorm==0 (269); _rms/_centroid divide by n≥2. Caps ordered before expensive work: MAX_CONTROL_PAIRS=1000 before the coerce loop (483), MAX_LOT_RING_VERTICES=100000 before shapely (437). shapely GEOSException/ShapelyError/ValueError caught in _safe_polygon (398) and _outside_lot_area (414). No superlinear path.

Part 2 follows (2 LOW advisories, honesty, scope/isolation, verdict).

---

M5-T124 G5 — Part 2/2 (2 LOW advisories, honesty/integrity, scope/isolation, verdict).

LOW-1 (ADVISORY; escape/echo parity residual; drawing_alignment.py:680, str(exc) from proposal.py:218 / :451). The `aligned_invalid` refusal detail = `str(ProposedMassingError)`, which for a bad srid/kind echoes a repr of that caller-supplied value: probe with srid = "2263"+U+202E+"EVIL<script>" yielded detail `...got '2263‮EVIL<script>'`. Python `!r` escapes C0/bidi controls to `\uXXXX` (raw U+202E did NOT survive — good), but literal ASCII like `<script>` passes through, and the detail is not length-bounded. Assessment: NOT reachable in the real flow (importer draft blocks always carry srid=2263 / kind="proposed", which survive the deepcopy unchanged, so validate never echoes them); the detail is JSON DATA rendered by a future route, not HTML, so `<script>` is inert unless a UI renders it as raw markup; only a malformed CALLER-built block triggers it. This is the same class as the ACCEPTED M5-T121 LOW-2 escape-parity residual. Route to the mount/UI: escape + length-bound the refusal detail if ever rendered as markup, and/or have the aligner normalize/reject srid/kind before re-validate. Not blocking.

LOW-2 (ADVISORY; defense-in-depth; drawing_alignment.py:344 and :370-375). The draft-block footprint and every per-level outline are transformed with NO pre-transform size cap IN THIS MODULE; the ceiling (MAX_OUTLINE_VERTICES=1000 / MAX_TOTAL_OUTLINE_POSITIONS=20000, proposal.py:74/89) is enforced by validate_proposed_massing which runs AFTER _transform_block. Contrast the lot_ring, which IS capped (MAX_LOT_RING_VERTICES=100000) BEFORE its shapely work. Measured: a 200k-vertex outline transforms+validates in 0.706s — the work is LINEAR (not superlinear) and refused `aligned_invalid`. Bounded in practice because the real caller is the importer (sheet_import/dxf_import, budget ≤500k) and the function is pure + UNMOUNTED. For parity + defense-in-depth the module could cap total input positions before transforming; request-size ceilings + rate limiting are the mount's job (producer DISC-B). Not blocking.

Honesty / integrity — PASS: no path bypasses validate_proposed_massing (single post-validate ok=True; test_mutation_skip_revalidation proves it load-bearing); precision label read verbatim by _input_precision (420) and echoed unchanged, deep-copied into the result (700), never upgraded (mutation-proven test:436); discrepancies cannot be suppressed by input choice (tolerance bounded finite; raw residuals/implied-scale/mirror/outside-area always emitted regardless of the disclosure flags); no permit/approved/maximum-allowed wording (test:443); implied scale + mirror computed only to DISCLOSE, applied transform is always a proper rigid motion det=+1 (_choose_transform=rigid, _applied_scale=1.0, _final_outline_vertices=identity — all mutation-proven seams).

Scope / isolation — PASS: imports only copy, math, dataclasses, shapely, app.scenario.proposal — NO os/socket/urllib/open/requests/logging (test_module_imports_no_io_or_network); so no content is logged, no network, no persistence (deep-copies input, input not mutated, two identical calls equal). Zero new deps (shapely already admitted). Cross-tenant isolation / service-role secrecy / private storage / SSRF / upload controls / prompt-injection / least privilege / log redaction: N/A — pure in-memory function, no DB, no auth/tenant/credential context, no outbound I/O, no storage, no LLM in path; ingested drawing/caller text is treated as untrusted DATA (only echo path is the repr'd srid/kind of LOW-1). Upload ceilings, per-caller rate limiting and an allow_nan=False pre-render guard are correctly the future mount's job (DISC-B), out of scope for this pure service.

M5-T124 G5 VERDICT: PASS
END-OF-REPORT
