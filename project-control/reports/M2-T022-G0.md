# M2-T022 — G0 administrative contract check (orchestrator)

Packet completeness verified against the /start-controlled-task checklist before claim:

- Requirement identifiers: docs/MVP_AGENDA.md B3 / A2 row 4 (address entry, the top client-demo
  item) and section C4 (the three endpoint duties: source_fact mapping + consumability proof,
  reflected-text warning, analysis_id never from untrusted input); the AddressResolution contract
  in the accepted M2-T021 connector; the house internal-endpoint pattern (M5-T013 evidence.py).
- Directive refs: D-038:ALL (in-regime; empty-applicable row expected at accept per the
  established precedent).
- Evidence files: named exactly in inputs[] (connector, three recorded fixtures, registry record,
  C4, source_fact schema READ-ONLY, sibling endpoint pattern).
- Scope: EXACT-FILE allowed_paths per the M5-T013 endpoint template (new route module, main.py
  router registration only, new test module, producer report); packages/contracts/** forbidden
  (schema consumed read-only); connectors/resilience/config and every sibling route + test module
  forbidden; disjoint from every open packet.
- Acceptance scenarios S1-S8: primary (S1 resolved + source_fact validation), ambiguity honesty
  (S2), visible rejection (S3), failure taxonomy + leak-absence with positive controls (S4),
  flag gating (S5), reflected-text honesty (S6), offline/no-state (S7), budget hygiene (S8) -
  executable, anti-tautology forms named in the scenario text.
- Gates: G0/G1/G3/G4/G5 with code-reviewer, data-contract-verifier, qa-engineer,
  security-reviewer - all distinct from the producer. G5 mandatory (a NEW HTTP surface in front
  of the key-bearing connector); G3 mandatory (the source_fact mapping IS the data-contract
  question).
- Modularity boundary answers: owning responsibility = HTTP transport of the address-resolution
  outcome; owning module = the new services/api/app/api/v1/address_resolution.py (siblings run
  330-600 lines, one route module per concern); main.py gains registration lines only; focused
  tests = the new test module; modularity check runs before submission.
- Dependency: M2-T021 (ACCEPTED, 178th) - satisfied.

G0 verdict: PASS (administrative; recorded by the orchestrator per gate-class rules).
