<!-- Reviewer return preserved VERBATIM by the orchestrator (report-preservation rule,
.claude/rules/project-control.md; transport entity-decoding applied: &lt; -> <, &gt; -> >).
Reviewer: independent data-contract-verifier subagent (G3), returned 2026-09-12,
reviewed HEAD ec4b75dc. -->

I have completed my independent verification. Every mapping, schema-validation, digest recomputation, no-fact rule, failure-matrix pair, and provenance check passed my own recompute — except one: the `unsanitized_reflected_input` warning omits `input_echo`, a verbatim caller-input reflection surface (I proved `<script>alert(1)</script>` arrives byte-exact in `input_echo.street`), while it names the equivalent `provenance.request_params`. The producer report's claim that the warning "names every reflected surface" is therefore false. That is a blocking G3 finding.

---

VERDICT: FAIL

# G3 Data-Contract Gate Report — M2-T022 (address-resolution API endpoint)

- Reviewed HEAD: **ec4b75dc** (branch candidate/D-024-mrl-option-b)
- Producer material commit: e245b40b; submit commit: ec4b75dc
- Reviewer: data-contract-verifier (independent; did not author). Read-only per ADR-005.
- Method: independent in-process TestClient probes (PYTHONDONTWRITEBYTECODE=1, nothing written to the repo), driving the REAL connector (`resolve_address`) over fixture-replaying transports; every source_fact validated against the REAL `packages/contracts/schemas/v1/source_fact.schema.json` with my OWN `referencing.Registry` wiring; every digest recomputed by me with an independent canonical-json-1 implementation.

## Scope (item 7) — PASS
`git diff --name-only e9a1d6a0 ec4b75dc` touched only the four allowed source paths (`services/api/app/api/v1/address_resolution.py`, `services/api/app/main.py`, `services/api/tests/api/test_address_resolution_api.py`, `project-control/reports/M2-T022-producer-report.md`) plus orchestrator control-plane files (gates/reports/state/tasks). `git diff e9a1d6a0 ec4b75dc -- packages/` is EMPTY — `packages/contracts/**` is byte-untouched. `main.py` change is router registration only.

## 1. The mapping against the schema — PASS
Drove the recorded G01 body end-to-end (200, status `resolved`, 7 facts). Every emitted fact validated against the real schema (my own registry). Per-key semantics independently confirmed:
- `original_field_name` = exact source field names (`bbl`, `buildingIdentificationNumber`, `firstStreetNameNormalized`, `firstBoroughName`, `zipCode`, `latitude`, `longitude`).
- `original_value` byte-equal to the raw source value for every field (strings verbatim incl. double-space `"WEST  100 STREET"`; numeric lat/long preserved as floats).
- `normalized_value` == the canonical outcome field (equal to original by construction for this transport connector).
- `retrieved_at` == connector provenance timestamp; `bbl` == canonical `1018887502` (via `normalize_bbl(...).canonical`).
- `confidence` 1.0, `user_confirmed_or_overridden` "none", `conflict_status` "none", `effective_date` null — matching the platform convention in `app/profile/wave_integration.py::_source_fact`.
- `value_digest` == my independent canonical-json-1 recompute for every field; `response_digest` == the connector's whole-response digest and == my recompute of the parsed body; `fact_key` == `<source_id>:<bbl>:<field>` (stable form); `observation_id` == `<connector correlation id>:<field>` (per-retrieval).

## 2. dataset_version honesty — PASS
Value `"geoclient-v2; Geosupport release id not exposed by the /address response"`. I scanned all three fixture bodies (G01/G02/G03) for any key containing version/release/vintage — none exists. The claim inside the string is true; it satisfies the schema's `non_empty_string` intent while honestly disclosing the gap rather than inventing a quarterly id (e.g. '26B'). No invention.

## 3. No-fact rules — PASS
- G02 (ambiguous): status `ambiguous`, `source_facts == []`, `bbl` null, `selection_policy: caller_selects`, suggestions verbatim in connector shape/slot order.
- G03 (rejected, GRC 42) and constructed not_found (11/11): visible 200, both GRCs, partial `street_name_normalized` transported, `source_facts == []`.
- Success with drifted field (numeric `zipCode`): no fact for `zipCode`, other 6 facts still emitted, canonical zip null.
- Withheld-with-reason: constructed resolved body with malformed `bbl` "9999999999" → `source_facts == []` + `source_facts_not_emitted_reason` = "resolved bbl failed canonical BBL validation…". bbl-absent success → `[]` + the distinct "carried no bbl" reason.

## 4. Transport honesty — **FAIL (blocking)**
PASS on: both GRCs + reason codes + messages on every outcome; provenance transported verbatim with key-free `request_params` (keys ⊆ {houseNumber,street,borough,zip}, no `Ocp-Apim-Subscription-Key` anywhere), `response_digest` + `digest_canonicalization` spec present.

**FAIL — the `unsanitized_reflected_input` warning's field list is inaccurate (omits a reflected surface).** The warning names `["grc_message","grc2_message","suggestions","canonical.street_name_normalized","canonical.borough_name","provenance.request_params"]` but does NOT name `input_echo`. The response's `input_echo.{house_number,street,borough,zip}` transports the caller's raw typed input VERBATIM. Proven: a request with `street="<script>alert(1)</script>"`, `house_number="7<b>7"` yields `input_echo.street == "<script>alert(1)</script>"` byte-exact (and the identical value in `provenance.request_params.street`, which IS named). A consumer relying on this machine-readable list to know what to escape on render would ship `input_echo` unescaped → reflected-XSS on the echo of the user's own input. This defeats C4 duty 2 (the list is the authoritative escape contract) precisely on the most direct reflected surface, and is internally inconsistent (request_params named, input_echo — the same caller values — not).

## 5. Matrix honesty — PASS
Every documented `(status, state)` pair is reachable and states equal the connector taxonomy's `error_type` strings verbatim: key_missing→503, auth_failed→502, rate_limited→503, source_unavailable→503, timeout→504, malformed_response→502, invalid_input→422, internal_error→500, and (200,None) for outcomes. Payloads bounded (<4096B) and key-free (positive-controlled). Well-formed `retry_after "7"` surfaced; hostile (`"7\n"`, 200-char) dropped. `request_budget_exceeded` is genuinely impossible at the seam: the route passes NO budget (verified by source and by empty `extra_kwargs` at the resolver seam); when I force-raised it, it falls to the default 503 with an honest `state:"request_budget_exceeded"` and is deliberately absent from `STATUS_STATE_MATRIX`.

## 6. Producer report sentence-by-sentence — one FALSE universal claim
The report states the warning "names every reflected surface." This universal claim is FALSE (input_echo is a reflected surface not named) — the failure class flagged in the packet. Other checked claims hold: flag reuse (INTERNAL_RULE_EVAL_ENABLED), generic 404 when off, every geocoding outcome a visible 200, states == taxonomy strings, 12 required + 4 lineage keys, honest dataset_version, facts withheld-with-reason on malformed/absent bbl, NO AnalysisBudget constructed or passed, resolved_with_warnings emits facts (verified: 7 facts). (Minor, non-material: "~460 lines" vs actual 483.)

## Other checks
Statelessness PASS (two identical requests → two transport calls, distinct HTTP and connector correlation ids). Flag-off PASS (generic 404, no correlation header, no body hint).

## Required correction (bounded, single surface)
Add `input_echo` (or the four `input_echo.*` fields) to `_REFLECTED_INPUT_WARNING["fields"]` in `services/api/app/api/v1/address_resolution.py`; correct the producer report's "names every reflected surface" claim; add a test asserting `input_echo` is named in the warning and that hostile `input_echo` text arrives byte-exact. (Consider likewise flagging `source_facts[].original_value`/`normalized_value` for the street/borough facts, which carry the same reflected source text — secondary.)

Reproduction: enable the flag, override `get_address_resolver` with the real connector over the recorded G01 body, GET with `street="<script>alert(1)</script>"`; observe `input_echo.street` is byte-exact and `"input_echo"` is absent from `response.json()["unsanitized_reflected_input"]["fields"]` while `"provenance.request_params"` is present.

Relevant files:
- C:\Users\MLFLL\Downloads\nyc-zoning\ctl24\services\api\app\api\v1\address_resolution.py (warning at `_REFLECTED_INPUT_WARNING`, lines 199-214; `input_echo` assembled at lines 350-355)
- C:\Users\MLFLL\Downloads\nyc-zoning\ctl24\project-control\reports\M2-T022-producer-report.md (false "names every reflected surface" claim)
- C:\Users\MLFLL\Downloads\nyc-zoning\ctl24\services\api\tests\api\test_address_resolution_api.py (S6 tests do not assert input_echo coverage)
