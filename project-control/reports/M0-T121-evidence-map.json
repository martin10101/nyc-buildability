{
 "task_id": "M0-T121",
 "note": "Evidence per D-024 requirement applicable to M0-T121 (resolver set, 19 rows: R302-R314, R318/R319, R323-R326). Material identity: work commit 668c824 on control/D-024-fable-codex-loop (cherry-pick of producer commit 49efc90 at base 04216dd). Reviewers reproduce each claim at the frozen head; producer report + repair record are claims, the code/tests/transcripts are the evidence.",
 "requirements": {
  "D-024-R302": [
   "Authorization exercised exactly as bounded: one defect task (this one) + the recert window (M0-T122, created, depends_on T121). Amendment 16 verbatim: directives/D-024-fable-codex-loop/source-016-amendment.md; G0 readiness report cites the reproduced-defect qualifying evidence (M0-T107-cycle2-start-refusal.md)."
  ],
  "D-024-R303": [
   "Complete F-2 class closed, not just HALTED: the mechanical sweep found and closed a THIRD latent edge (owner_answer_validated, WAIT_FOR_OWNER->PREFLIGHT) beyond the two named ones. Surfaces: owner-restart, acknowledge-emergency-stop, resume-after-answer (tools/agent_supervisor/restart_channel.py; cli.py wiring +2 lines). Producer report section 3 records the discovery and scope reasoning."
  ],
  "D-024-R304": [
   "owner-restart command: HALTED->IDLE on owner_explicit_restart, explicit + audited (durable owner-recovery audit event + journal transition), fail-closed (see R311 row). Tests AS-1 (live-shape pre-fix journal restarts truthfully, no journal edit) + AS-2 (repeat invocation refuses cleanly) in tools/test_agent_supervisor_restart_channel.py."
  ],
  "D-024-R305": [
   "acknowledge-emergency-stop: DISTINCT command, materially stronger acknowledgment - requires an explicit flag AND a journal-derived --confirm-emergency-token (printed on mismatch so the operator must read the journal state); impossible via habit/script default. AS-4 tests."
  ],
  "D-024-R306": [
   "Ordinary owner-restart REFUSES on EMERGENCY_STOPPED; no automatic path (recovery classification, scheduler, succession, start) fires either edge - AS-4 negative tests + the reachability meta-test pin the surfaces as the only call sites."
  ],
  "D-024-R307": [
   "Complete enumeration in producer report: blocking={WAIT_FOR_OWNER,PAUSED_RECOVERY,EMERGENCY_STOPPED,HALTED}, terminal={COMPLETE,EMERGENCY_STOPPED,HALTED}, all recovery edges listed; a meta-test pins the mechanically derived set against the TRANSITIONS table."
  ],
  "D-024-R308": [
   "Per-EDGE proof: each of the four owner recovery-resume edges has exactly one callable CLI surface (the shared owner_explicit_restart TRIGGER backs two distinct edges, one surface each); intentionally terminal edges (e.g. COMPLETE->IDLE run_closed) explicitly excluded as non-resume edges. Producer report enumeration table; meta-test."
  ],
  "D-024-R309": [
   "Deterministic mechanical reachability test (derives operator-recovery triggers from TRANSITIONS, discovers CLI call sites by inspection, no hand-maintained list). RED on pre-fix registration: AssertionError naming ['owner_answer_validated','owner_explicit_restart']; GREEN post-fix: 31 passed. Verbatim outputs in producer report; in-suite removal-sensitivity tests per surface."
  ],
  "D-024-R310": [
   "No generic state-changing command (each verb has a fixed source-state and fixed target edge); no journal edits (AS-1 restarts the pre-fix journal shape untouched); no policy loosening, budget resets, or audit-history modification (AS-8 before/after snapshot assertions)."
  ],
  "D-024-R311": [
   "Fail-closed preconditions, each individually forcing a typed refusal: open asks, pending external effects, surviving children, provider identity drift (recorded pinned identity check; the LIVE drift probe remains start-preflight territory - documented deferral, not a silent skip), non-SAFE_CHECKPOINT recovery classification. Tests AS-5a..e; durable emergency-stop flag refusal AS-6."
  ],
  "D-024-R312": [
   "Matrix covered with named tests: pre-fix journals (AS-1), current journals (in-suite fresh-journal cases), repeated invocation (AS-2), stale runs + concurrent controllers (AS-7 lock contention, exactly-once under race), emergency-stop recovery (AS-4), audit-chain continuity (verified before AND after restart), removal sensitivity (per-surface deregistration tests). 31 tests total, re-run independently by the orchestrator: 31 passed (worktree AND control checkout)."
  ],
  "D-024-R313": [
   "Exactly-once: single-instance lock held across check+transition; AS-7 racing invocations produce one transition. Durable audited owner-recovery record appended per successful invocation (AS-1 asserts the audit event)."
  ],
  "D-024-R314": [
   "Standard process in flight at submit: G0 PASS (ef0d476), G2 self-check PASS (this wave), G3/G4/G5 dispatched to the independent reviewer roster; DCV + mutation-proof verification at accept; CI on the pushed tip; frozen-identity recertification = M0-T122 (held until this task is accepted). Suite evidence: 410 passed on the required minimum + golden run, re-run by the orchestrator."
  ],
  "D-024-R318": [
   "Producer never interrupted: one dispatch, natural completion notification; Amendments 17/18 were captured and the model-identity evidence gathered read-only while it ran."
  ],
  "D-024-R319": [
   "No continuous-operability claim anywhere: producer report explicitly disclaims it; the orchestrator narrative and this evidence map repeat the disclaimer. The live-journey standard (R320) is owed AFTER M0-T122 via the owner-typed cycle-2 act."
  ],
  "D-024-R323": [
   "Producer ACTUAL model identity recorded mid-run without touching the producer: 105/105 transcript events claude-opus-4-8, uniform, no second id. Durable authority: D-004-R735 (subagent model assignment) via .claude/agents/backend-engineer.md frontmatter, byte-stable since commit 8b1b386. Report: M0-T121-producer-model-identity.md. FINAL transcript re-read owed at accept time per the report's commitment."
  ],
  "D-024-R324": [
   "Not interrupted: evidence as R318; the identity evidence came from a read-only grep of the transcript file, never a message to or stop of the agent."
  ],
  "D-024-R325": [
   "Determination recorded: AUTHORIZED - explicitly permitted bounded subagent assignment (no substitution/fallback event exists in the window; orchestrator stayed on Fable 5). Fail-closed condition NOT met; re-evaluated at accept with the final transcript re-read."
  ],
  "D-024-R326": [
   "Authority claimed ONLY from the explicit D-004-R735 assignment + checked-in frontmatter; no allowlist consulted or cited as authorization (model-identity report section 4)."
  ]
 }
}
