# D-046 source-001 (original, verbatim) — owner directive, interactive chat (companion session), 2026-09-13

Capture head: `9f0f202f9cad4b25d9bad1b41d767854e352661b` (branch `candidate/D-024-mrl-option-b`).
Frozen origin/main baseline: `d8b3899f61efa6620e18a26541ced96020f5bef9`.
Captured by the companion session (ctl24-8e) per the Part-0 working model; D-042/D-043/D-045
peer-capture precedent. Companion relay of D-045 and its first contracted task (M4-T012) were
already live when this directive was given.

## owner-message-verbatim

Context question (owner, verbatim — asked after the D-045 duration estimate of ~6–10 weeks core /
2–4 months including special districts):

> Is there a way to cut the time in half ? More agents etc ?

The companion session answered that roughly half is realistic via (1) parallel producers on
disjoint rule families and (2) more running hours, with review independence, sequential
integration, §G, and legal-sourcing care explicitly NOT compressible, and a practical ceiling of
2–3 parallel producers. The offer the owner accepted, verbatim:

> Want me to make it official? Say the word and I'll capture a scale-up directive (parallel family
> production, 2–3 producers, within all existing gates) and relay it to the build session so the
> next A1 wave already runs wide.

Directive (owner, verbatim):

> Yes

## capture-context

- The owner authorizes SCALED PARALLEL EXECUTION of the D-045 citywide rule-coverage campaign:
  up to 3 concurrent writing producers on disjoint family/connector tasks, each in its own
  isolated worktree with non-overlapping file scopes, per the standing orchestration policy
  (`.claude/ORCHESTRATION_POLICY.md`) and permanent principle 10 (worktree isolation; parallel
  agents never edit overlapping files).
- Explicitly UNCHANGED (stated in the accepted offer): independent review (producer ≠ reviewer),
  all G0–G7 gates, dependency policy §G including the 7-day age gate, sequential integration by
  the single orchestrator, directive-compliance verification, and careful legal sourcing. The
  owner was told, and accepted, that these do not compress; parallelism buys ~1.5–2×, not more.
- Cost posture disclosed and accepted: daily token usage roughly doubles while total campaign
  cost stays about the same; if usage caps or model availability stall the pipeline, concurrency
  backs off rather than quality.
- "More running hours" (lever 2) is owner-side behavior (keeping build sessions up longer), not a
  repository requirement; recorded here as context only, not decomposed as a requirement.
- This directive changes EXECUTION SHAPE only. D-045's scope, sequencing judgment (D-045-R008),
  preservation row (D-045-R009), and every standing hold are untouched.
