# D-032 source-001 — verbatim owner directive (2026-09-05)

Channel: owner terminal prompt, session start 2026-09-05 (first message after `/clear`).
Captured at frozen base: origin/main `d8b3899f61efa6620e18a26541ced96020f5bef9`; ctl24 HEAD
`db7d561f1f40d173b64c8d02f897b7939ec024ab` (branch `candidate/D-024-mrl-option-b`, clean).

The message began with pasted successor-prompt context from the seq-83 handoff (items 2–4 of the
previous session's closing output, reproduced verbatim below as the owner sent them), followed by
the owner's own typed directive text. Everything between the BEGIN/END markers is verbatim,
including spelling.

---BEGIN VERBATIM OWNER MESSAGE---
 Saved commits (this session, local only — nothing pushed, per policy; branch has no upstream, PR #241 untouched): handoff at db7d561f; the session's substantive tips include M0-T146 code 431018cf, M0-T146 gates/DCV through de49a03b, M0-T144 accept 892c9fe1, M0-T107 accept 9dcbdd09. Tree is clean; no uncommitted files.
3. Clean-start command (from the detected worktree root):
cd C:\Users\MLFLL\Downloads\nyc-zoning\ctl24 && claude
   Bootstrap Gate 0 required: the session's primary cwd must be that root and /mcp must report no servers before any write.
4. COPY INTO THE NEW SESSION — the ready-to-paste successor prompt is at the bottom of docs/SESSION_HANDOFF.md. In short: resume from durable evidence; verify root/branch/HEAD + MCP-clean; read CLAUDE.md, the handoff, the M0-T146 report, and D-024-persistent-activation-terminal.md; run python tools/project_control.py status (ledger wins). i need you to have 1 role get codex up and running as full time loop no more monkey busnnes also coedex has a new model calld astra or something like that (it also has difrint setting like high extra high etc i want to set it at high and use that for main codex agent for the loop but i ned you to ucheck if the cli has already that modle or codex might need to be update in th ecli if yes update it but the goel is to get this done asap u dont have to run 5 millon checks you just have to get this done and power up the loop for the run "C:\Users\MLFLL\Downloads\NYC-Buildability-Review-and-Roadmap-2026-09-05.pdf"  ChatGPT completed a repository and competitor review of nyc-buildability dated September 5, 2026. Reconcile that review with our current local work and incorporate the valid findings into our existing plan.

The review used published GitHub snapshots:

* Main: d8b3899f61efa6620e18a26541ced96020f5bef9
* PR #241: 4174a3b2a547ae7d5df5d35cefb63767cbf84721
* Published control branch: 6f5d12a6203c2c89390a982657fa8d66a91a0c3d

These may lag our current local progress. Live git, task records, verification evidence and campaign continuity take precedence. Do not reset anything to match these snapshots or recreate completed work.

1. Establish the current state first.

Verify the repository root, branch, HEAD, worktrees, active tasks and applicable MCP policy. Read AGENTS.md, CLAUDE.md, docs/SESSION_HANDOFF.md, the session-handoff profile and its required current-state references.

Run the existing read-only project-control and campaign-continuity status checks. Reconcile previously reported M0-T107 and M0-T144 acceptance and determine the actual current status of M0-T109 and the active campaign.

Preserve all existing owner holds, including PR #241 and the applicable R595/Option-A and R603–R605 restrictions. Apply the deficit-convergence skill before edits when its actual trigger applies.

2. Verify these findings against current code.

A. Live spatial integration:
In the reviewed version, services/api/app/api/v1/rule_evaluation.py returned None from the default spatial provider. services/api/app/rules/integration.py consequently returned a professional-review outcome. The scenario pilot reused this dependency. Successful cases in apps/web/e2e/harness/fixture_api.py injected spatial fixtures.

Determine whether real source-backed spatial data now reaches the default runtime path. Fixture success alone does not establish that.

B. Persistent customer workflow:
The reviewed API lacked a production authentication/organization boundary; confirmations could not be saved; database migrations and production review storage remained incomplete. Establish what is now implemented, connected and proven.

C. Legal evidence and coverage:
The reviewed tree contained seven draft rule files, six legal snapshots without raw-HTML verification, and only residential FAR integrated into evaluation.

Specifically investigate ZR 23-21: the stored summary presents the single-dwelling-unit 0.60 equivalent-FAR provision without its table scope, while the official HTML reviewed attached footnote 1 to the 0.75 R1/R2/R3 grouping. Preserve the original table and footnote relationships and prepare this for qualified review. Do not assume this proves the emitted R5 FAR is wrong or make an unsupported legal correction.

D. Document isolation:
services/api/app/documents/isolation.py checked kernel capability presence. The reviewed extraction pipeline called the decoder directly. Determine where the actual worker restrictions are installed and self-verified. Capability detection is not proof of applied isolation.

E. Data and operations:
The September 5 live PLUTO query returned 26v2; reviewed fixtures used 26v1. Check current source versions and compatibility.
The September 4 Python audit failed on tooling pip 26.1.2; the control branch already contained a 26.2 update. The web audit failed on an npm advisory-service 503. Reuse existing fixes and distinguish vulnerabilities from unavailable audit services without weakening gates.
Check the browser’s 12-second timeout against backend retries.
GitHub reported public visibility despite “private” descriptions. Report the intended-versus-actual state; do not change visibility automatically.

3. Update the delivery plan using existing tasks.

Preserve both goals: a dependable autonomous Codex loop and a useful buildability product. Identify which remaining controller work actually blocks product delivery.

Map the confirmed gaps to existing tasks before proposing new ones. Sequence:

* Real source integration and durable analysis revisions.
* Authentication, storage and review workflow.
* Exact legal evidence, applicability closure and qualified approval.
* Constrained building options, saved comparisons and evidence reports.
* Professional benchmarks and a paid customer pilot.

The existing R5 pilot can be a bounded delivery phase while preserving the all-five-borough mission. Treat the suggested 30/60/90-day schedule as provisional, not a commitment.

Competitor research found that Envelope, PropertyScout and other products already advertise citations, massing or expert assistance. Test our differentiation through reproducible results, explicit coverage, useful options and reduced reviewer effort.

4. Return one consolidated assessment.

Provide:

* Findings classified as confirmed, already fixed, partly resolved, contradicted or unverified, with current file/line and commit evidence.
* A dependency-ordered plan mapped to existing tasks.
* The next concrete deliverable and its acceptance criteria.
* Genuine owner decisions and external dependencies, grouped together.

Continue work already authorized by the current campaign. Prepare concrete proposals before requesting any new authorization. Do not treat this review as permission to bypass gates, merge PR #241, deploy, publish legal rules, install a new agent framework or abandon the active campaign.
---END VERBATIM OWNER MESSAGE---
