# D-005 — source-001 (original owner message, verbatim)

Captured verbatim per `.claude/skills/directive-compliance` §1. Channel: owner_message (owner forwarded a GPT-drafted directive with their own review instructions).
Frozen baseline: `origin/main` = `58309bc984eadde10034f5c60a920de72f30ffc1` (post-B-016 resolve, PR #111).
Do not edit this file after the directive is active; corrections are new append-only `source-00N-amendment.md` files.

---

THIS IS FROM GPT YOUR JOB IS TO REVUEW SEE IF THIS IS A GOOD FIT FOR OUR PROGRAM SEE IF THERE IS ANY Potential issues that will come up with the stuff we already did so far. How are we gonna implement it in terms of it running the system that we implemented that we use now, the PowerShell, the terminal.Next thing you have to check is if everything of this matches our security implementation that we implemented recently. Is this gonna make any issues security-wise? Is there any potential, you know, problems that we need to adjust? We need to. Then you have to just do general think if this is the best way to do it or if there's a better, easier, more, better way. It might not be, but it might be that you have an easier approach that will do the same job. It doesn't mean that you have to bring me back, I think. It just means that you look it through and decide what the next step is. And based on your recommendation, we're gonna go ahead and implement it.

```text
OWNER DIRECTIVE — CODEBASE KNOWLEDGE GRAPH PILOT

We are in the middle of active NYC Buildability development.

I want to introduce a codebase knowledge graph so Claude/agents can navigate the repository faster, reduce repeated grep/read exploration, understand dependencies, and eventually support graph-first context routing.

However:

THIS IS A CONTROLLED EVALUATION FIRST.

Do NOT broadly install Graphify into the active Claude environment.
Do NOT change the product.
Do NOT change our six future design-platform PRDs.
Do NOT interrupt the current NYC Buildability development lane.
Do NOT weaken or bypass any existing control-plane, directive, worktree, gate, or protected-main requirement.

The purpose of this task is to determine whether a code graph should become part of our engineering infrastructure and, if yes, how to integrate it safely.

==================================================
PART 0 — CAPTURE AND ORIENT
==================================================

First:

1. Capture this owner directive through the existing directive-compliance process if the current regime requires it.

2. Reconcile against CURRENT repository truth:
   - origin/main
   - project-control ledger
   - active directives
   - active blockers
   - current tasks
   - CI
   - current Agent Teams / worktree / hook state

3. Pay special attention to all currently known teammate/worktree/hook confinement issues.

Recent work showed that teammate shell cwd/confinement and hook behavior have had real defects.

Do NOT assume those problems are resolved merely because this prompt references them.

Determine current truth from the repository.

4. Do not disturb:
   - any dirty checkout
   - any active producer worktree
   - any uncommitted files
   - any active task branch
   - current development work

Use a new isolated worktree/branch if this evaluation requires files or experimentation.

==================================================
PART 1 — DEFINE THE PROBLEM WE ARE SOLVING
==================================================

The specific engineering problem is:

Claude and multiple agents should not repeatedly rediscover the codebase by blindly performing broad:

- Glob
- Grep
- Read
- dependency chasing
- duplicate architectural analysis

for every task.

We want a machine-readable CODEBASE NAVIGATION GRAPH that can answer questions such as:

- Where is this concept implemented?
- Which functions call this function?
- Which modules depend on this contract?
- What is upstream/downstream of this component?
- What files are likely affected by changing this interface?
- What code path connects property facts to rule evaluation?
- What code path connects rule evaluation to scenarios?
- Where does source_fact originate and where is it consumed?
- Which modules participate in geometry processing?
- Which components depend on property_profile?

This is navigation/context infrastructure.

It is NOT the future NYC legal/evidence/benchmark knowledge graph.

Keep those concepts separate.

We eventually expect three related but distinct graph layers:

A. CODEBASE GRAPH
   Files, classes, functions, calls, imports, architecture.

B. PROJECT/CONTROL GRAPH
   Tasks, requirements, systems, blockers, dependencies, gates, PRs.

C. NYC EVIDENCE KNOWLEDGE GRAPH
   Properties, legal rules, sources, filings, plans, benchmark cases,
   solver runs, discrepancies, provenance, historical relationships.

This task concerns ONLY A initially, with recommendations for later connection to B.

Do not implement C.

==================================================
PART 2 — GRAPHIFY DUE DILIGENCE
==================================================

Evaluate the current official Graphify project.

Do not trust social-media instructions.

Verify from official/current sources:

- official repository
- official PyPI package
- current package name
- current CLI command
- current release/version
- release date
- license
- Python requirement
- dependencies
- optional dependencies
- what is local
- what can make network/model calls
- files it writes
- Claude/Cursor configuration it modifies
- hooks it can install
- incremental-update behavior
- output files
- whether it executes arbitrary repository code
- security-relevant behavior
- telemetry/network behavior if any
- known recent fixes relevant to data loss/settings/hooks

The official package appears to currently be:

    graphifyy

while the command appears to be:

    graphify

but independently verify this rather than trusting the prompt.

IMPORTANT:

Our existing dependency-age/security policy remains binding.

If the latest Graphify release is younger than the permitted package-age window, do NOT silently waive that rule.

Report the conflict.

An isolated, non-production evaluation may be proposed if allowed by existing policy, but adoption into canonical engineering infrastructure requires explicit compliance or an owner-approved exception.

==================================================
PART 3 — NO INSTALL INTO THE ACTIVE ENVIRONMENT
==================================================

Do NOT initially run:

    graphify install

against the active development checkout or user/global Claude configuration.

Do NOT initially install Graphify hooks.

Do NOT alter global Claude Code configuration.

Do NOT alter Cursor configuration.

Do NOT add it to production requirements.

Do NOT modify active .claude hook/configuration surfaces.

Reason:

We already have sensitive work around agent confinement, hooks, worktrees, and orchestration.

We cannot introduce another moving part into that layer before understanding exactly what it changes.

==================================================
PART 4 — ISOLATED PILOT
==================================================

If due diligence finds no blocking reason to stop:

Create a completely isolated CODE-GRAPH PILOT environment.

Requirements:

- separate clean worktree or disposable clone
- based on a known frozen origin/main SHA
- no active development branch
- no product commits
- no persistent user/global configuration mutation
- no existing Claude hooks modified
- no source-of-truth project-control data modified
- no push to main
- no merge
- no acceptance without the normal workflow

Prefer an isolated tool environment such as uv tool/pipx if appropriate rather than contaminating the repository Python environment.

Record:

- exact Graphify version
- package digest if practical
- exact Python version
- frozen repository SHA
- exact commands used
- files created outside graph output
- network connections if observable
- configuration modifications

==================================================
PART 5 — FIRST EXTRACTION: CODE ONLY
==================================================

For the first test, graph CODE ONLY where practical.

Do NOT feed:

- legal PDFs
- client documents
- plan sets
- proprietary uploads
- benchmark documents
- sensitive content

into an external model.

The first question is simply:

Can deterministic/local AST-based code graphing materially improve navigation of this repository?

Where Graphify allows local tree-sitter extraction, prefer that mode.

Do not enable optional model/API processing merely because it exists.

Generate the normal graph artifacts in the isolated pilot, such as:

- graph.json
- graph.html
- human-readable graph report

Do not assume these exact filenames if current Graphify differs; inspect current behavior.

==================================================
PART 6 — INSPECT THE GRAPH QUALITY
==================================================

Before benchmarking, inspect whether the graph actually understands this repository.

Evaluate:

1. languages parsed
2. packages detected
3. modules
4. functions/classes
5. imports
6. call relationships
7. cross-language boundaries
8. Next.js/TypeScript coverage
9. FastAPI/Python coverage
10. generated-contract handling
11. tests
12. project-control scripts
13. false-positive edges
14. missing edges
15. inferred vs explicit relationships
16. whether dynamic Python/TypeScript behavior creates blind spots

Produce examples of GOOD graph relationships.

Produce examples of WRONG or MISSING graph relationships.

Do not claim it is accurate because graph.html looks impressive.

==================================================
PART 7 — REAL BENCHMARK
==================================================

Create a small reproducible navigation benchmark.

Select approximately 15–25 questions representative of real NYC Buildability work.

Include questions like:

- Where is SourceFact defined and consumed?
- Trace property_profile from contract to backend consumer.
- Trace MapPLUTO geometry into spatial-intersection handling.
- Trace rule evaluation into scenario-generation consumers.
- Where is exact legal arithmetic implemented?
- Which code can affect rule rounding?
- Which modules know about stale/cache states?
- What consumes zoning_features?
- Which files would likely be affected by changing a rule_evaluation contract?
- Where are geometry validity/repair semantics implemented?
- Where do project-control acceptance checks live?
- Which CI job validates product-map integrity?
- Trace an API request from route through service/domain layers where possible.

Use current repository architecture rather than blindly using these examples if names changed.

For each question perform TWO approaches.

-------------------------
APPROACH A — BASELINE
-------------------------

Normal Claude repository navigation using the existing approved workflow.

Measure where practical:

- elapsed time
- searches
- grep/glob calls
- source files opened
- approximate retrieved/context volume
- correctness
- completeness
- mistaken detours

-------------------------
APPROACH B — GRAPH FIRST
-------------------------

Start with the graph.

Use graph output/query mechanisms to identify relevant nodes/paths/files.

Then verify important conclusions against actual source code.

Measure:

- elapsed time
- graph queries
- source files opened
- approximate retrieved/context volume
- correctness
- completeness
- mistaken graph paths

==================================================
PART 8 — SCORE IT HONESTLY
==================================================

Create a comparison table.

For each benchmark:

Question
Baseline result
Graph-first result
Correct?
Complete?
Files opened
Navigation steps/tool calls
Graph missed anything?
Graph invented anything?
Source verification required?
Winner

Then overall:

- navigation speed improvement
- reduction in unnecessary reads
- reduction in repeated discovery
- accuracy
- false-positive rate
- material false-negative cases
- usefulness for large-task context routing
- usefulness for independent reviewers
- usefulness for producers
- usefulness for orchestrator
- usefulness for a non-engineer owner

Do NOT invent precise token savings if we cannot actually measure tokens.

Use observable measures instead.

==================================================
PART 9 — DETERMINE THE RIGHT TRUST MODEL
==================================================

The code graph must NEVER become authoritative truth.

Canonical truth remains:

- Git content
- actual source files
- project-control
- schemas/contracts
- CI
- accepted gate evidence

The graph is an INDEX / NAVIGATION PROJECTION.

Required pattern:

TASK
  ↓
GRAPH QUERY
  ↓
LIKELY FILES / CALL PATH
  ↓
READ AUTHORITATIVE SOURCE
  ↓
IMPLEMENT / REVIEW

Forbidden pattern:

TASK
  ↓
GRAPH
  ↓
TRUST WITHOUT SOURCE VERIFICATION

Define what kinds of graph results require source verification.

At minimum, anything involving:

- security
- legal semantics
- control plane
- data contracts
- public interfaces
- dependency impact
- acceptance decisions

must still be confirmed from authoritative source.

==================================================
PART 10 — STALENESS MODEL
==================================================

A stale graph is dangerous because it could send an agent to obsolete code.

Design, but do not necessarily implement yet, a deterministic graph-freshness model.

The graph should know:

- source repository SHA it represents
- graph generation time
- tool/version
- extraction mode
- configuration
- whether the checkout was dirty
- whether uncommitted files were included

Before an agent uses it:

compare:

    graph_source_sha
        vs
    current working/frozen task SHA

If they differ:

STATE = STALE

Do not present stale graph information as current.

For active task worktrees, think carefully about whether graphing:

- origin/main
- frozen task base
- task HEAD

is appropriate.

Do not assume one answer fits every lifecycle stage.

==================================================
PART 11 — UPDATE STRATEGY
==================================================

Evaluate Graphify's incremental-update capability.

Design the safest initial refresh pattern.

Preferred initial pattern:

SESSION / TASK START
      ↓
check graph metadata
      ↓
graph current?
   YES → query
   NO  → controlled update
      ↓
continue

Do NOT automatically install Git hooks yet.

Especially do not add commit/push hooks while our existing hook/confinement work is still sensitive unless separately reviewed and owner-authorized.

Later automation may be proposed.

==================================================
PART 12 — CLAUDE CONTEXT ROUTING DESIGN
==================================================

If the pilot succeeds, propose how graph-first navigation should integrate with our existing context-routing approach.

Example desired behavior:

Claude receives controlled task
        ↓
reads minimal task/control capsule
        ↓
queries relevant graph neighborhood
        ↓
identifies subsystem/files/dependencies
        ↓
reads authoritative source files only
        ↓
implements

Do NOT force full graph.json into Claude's context.

That defeats the purpose.

Graph retrieval must be targeted.

Design query patterns such as:

- explain node
- neighbors
- upstream dependencies
- downstream dependents
- shortest path between concepts
- files implementing a concept
- callers/callees
- impact neighborhood

==================================================
PART 13 — MULTI-AGENT USE
==================================================

Evaluate how this could help Agent Teams.

Example:

ORCHESTRATOR
      ↓
task scope
      ↓
PROJECT/CODE GRAPH
   ↙    ↓    ↘
Agent A Agent B Agent C

Each teammate gets only its relevant graph neighborhood.

But do NOT change the current agent confinement model during this pilot.

The graph must not be used as justification to loosen safety controls.

Also evaluate whether parallel agents could safely share one graph artifact read-only.

==================================================
PART 14 — FUTURE PROJECT GRAPH
==================================================

Do NOT implement it now, but produce a design note for eventually adding project-control relationships to a graph projection.

Potential node types:

- task
- directive
- requirement
- blocker
- milestone
- gate
- PR
- product system
- file
- module
- contract

Potential edge types:

TASK -> IMPLEMENTS -> REQUIREMENT
TASK -> PART_OF -> SYSTEM
TASK -> DEPENDS_ON -> TASK
TASK -> BLOCKED_BY -> BLOCKER
TASK -> MODIFIES -> FILE
PR -> DELIVERS -> TASK
GATE -> REVIEWS -> TASK
CONTRACT -> CONSUMED_BY -> MODULE

This graph must be GENERATED FROM canonical project-control/Git sources.

It must not become a competing manually edited source of truth.

==================================================
PART 15 — MISSION CONTROL INTEGRATION
==================================================

Evaluate, but do not build unless separately authorized, whether the owner Mission-Control dashboard could later add:

ENGINEERING MAP / CODE MAP

For example, clicking:

Rules Engine

could show:

Owner view:
- purpose
- engineering %
- launch readiness
- blockers

Technical/code view:
- modules
- key files
- key contracts
- upstream dependencies
- downstream consumers

This should reuse graph output rather than duplicate architecture metadata unnecessarily.

Again: recommendation only in this pilot unless the controlled task explicitly authorizes implementation.

==================================================
PART 16 — FUTURE NYC KNOWLEDGE GRAPH BOUNDARY
==================================================

Record explicitly that Graphify/code graphing is NOT our future NYC evidence graph.

Future product graph entities may include:

- property
- BBL
- BIN
- zoning district
- overlay
- special district
- rule
- amendment
- effective-date regime
- legal source
- source snapshot
- fact
- conflict
- DOB filing
- permit
- Certificate of Occupancy
- plan set
- plan sheet
- building footprint
- benchmark case
- solver run
- candidate scenario
- discrepancy
- defect
- regression test

That future system needs its own data model/provenance/temporal semantics.

Do not ask Graphify to become the canonical data store for those.

==================================================
PART 17 — STOP CONDITIONS
==================================================

STOP and return to me before adoption if any of the following occurs:

- Graphify modifies active/global Claude settings unexpectedly.
- It installs hooks without explicit authorization.
- It touches an active development checkout.
- It modifies project source/control data unexpectedly.
- Code is transmitted externally during the code-only local pilot.
- Package/dependency policy blocks installation.
- Security review finds a material unresolved risk.
- It requires weakening existing Claude/worktree safety.
- Graph results have material false-negative dependency paths.
- Graph output cannot be deterministically tied to a source SHA.
- It creates more context overhead than it removes.
- Its installation conflicts with existing Claude skills/hooks.
- The pilot would require bypassing the existing control plane.

Do not work around a STOP condition silently.

==================================================
PART 18 — SUCCESS CRITERIA
==================================================

The pilot succeeds only if we can demonstrate that:

1. graph generation is reproducible enough for our use,
2. code can remain local during the code-only path,
3. generated graph is clearly non-authoritative,
4. graph freshness can be tied to repository identity,
5. important results can be verified against source,
6. it materially reduces unnecessary repository exploration,
7. it maintains or improves correctness,
8. it integrates without weakening project controls,
9. it can be updated safely as main changes,
10. it provides targeted context instead of dumping the whole graph,
11. there is a safe path for multi-agent read-only consumption,
12. no active product work was disturbed.

==================================================
PART 19 — DELIVERABLE
==================================================

Do not return a giant stream-of-consciousness report.

Return a structured decision packet:

CODE GRAPH PILOT — OWNER DECISION PACKET

A. Current repo/control state
- current main SHA
- current relevant active work
- confinement/hook considerations

B. Tool due diligence
- package
- version
- age
- license
- install mechanism
- local/network behavior
- config/hook effects
- risks

C. Pilot setup
- isolated location
- frozen SHA
- commands
- files created

D. Graph quality
- what it maps well
- what it misses
- false/inferred relationships

E. Benchmark results
- baseline vs graph-first

F. Security/control findings

G. Staleness/update strategy

H. Proposed Claude context-routing integration

I. Proposed multi-agent use

J. Mission-Control integration recommendation

K. Relationship to future project/evidence graphs

L. Recommendation:
choose exactly one:

GO — adopt now
GO WITH CONDITIONS
WAIT
REJECT

M. Exact next controlled task(s), if GO

==================================================
PART 20 — IMPORTANT OWNER BOUNDARY
==================================================

This evaluation is NOT authorization to:

- implement the six new design-platform PRDs
- build the generative solver
- build the historical benchmark laboratory
- build the NYC Evidence Knowledge Graph
- install Neo4j
- install a vector database
- modify Mission Control
- restructure the repo
- rewrite Claude configuration
- change global hooks
- merge Graphify integration

Those will be separate owner decisions.

The immediate goal is simply:

PROVE WHETHER A CODEBASE GRAPH CAN SAFELY MAKE CLAUDE/AGENTS NAVIGATE NYC BUILDABILITY FASTER AND MORE ACCURATELY.

Proceed with audit and isolated evaluation under the existing control plane.
```

### What happens after you send this

Claude should **not start the giant expansion**. The sequence should now be:

```text
CURRENT BUILD ───────────────────────────────► continues normally

                 CODE GRAPH PILOT
                        ↓
                 prove usefulness
                        ↓
                safe integration
                        ↓
                 graph-first Claude
                        ↓
             MASTER EXPANSION ARCHITECTURE
                        ↓
                      6 PRDs
                        ↓
                graph-assisted gap audit
                        ↓
              controlled implementation
```

One especially useful point: Graphify's current docs specifically support project-scoped installation and incremental `--update` operation, so if the pilot passes we do have a path to keep the graph aligned as the repository changes rather than regenerating everything from scratch. ([PyPI][1])

**Do not select or approve a merge merely because Claude comes back saying Graphify works.** Send me the resulting owner-decision packet first. I want to inspect its benchmark results and especially how it interacts with your existing Claude hooks and worktree confinement before we make it part of the pipeline.

[1]: https://pypi.org/project/graphifyy/?utm_source=chatgpt.com "graphifyy · PyPI"
