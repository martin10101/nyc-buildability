# D-007 — source-002 (owner amendment 1, verbatim) — Step 7 rulings and the M0-T036 build dispatch

Captured verbatim per `.claude/skills/directive-compliance` §1 (amendments are new append-only
files; committed sources are never edited). Channel: owner_message (direct prompt to the
orchestrator session, immediately after the Step 7 consolidated stop and the owner's draining of
the R721 merge queue). Amends `source-001.md`.

Head at capture time: `control/M0-T036-dispatch-capture` branched from
`origin/main` = `e7f5078b2c3668c255fe65bc22afde576f65c75e` (the merge of PR #148).

Requirement IDs added by this amendment start at `D-007-R532`; no existing source file or
requirement row is edited. Rulings 1–2 (R307 discharge; merges #143–#145 ratification) are
D-004 subject matter and are captured in the same batch as **D-004 amendment 21**
(`source-022-amendment.md`, rows D-004-R732–R733); this amendment carries rulings 3–9 and the
dispatch.

## External-fact anchors, verified at capture time

- Ruling 5's condition resolves: the M0-T036 packet DOES record a single recommendation — the
  Phase 0 return and the packet risk name "a dedicated read-only controller checkout outside
  every Claude-writable path" (D-007 §13.1 first option). That option is therefore ADOPTED; the
  first-ASK fallback is not engaged.
- Ruling 3's "proposed amendment text from the Phase 0 return" is the blockquote in
  `project-control/reports/M0-T036-PHASE0-return.md` §"ADR-005 reconciliation"; it is applied
  verbatim as a dated amendment note in `docs/adr/ADR-005-agent-permission-and-gate-workflow.md`
  in this same batch, and is IN FORCE as captured law from this amendment, binding before any
  pushing supervisor mode activates.
- The two standing grants activated by ruling 7 are recorded on the M0-T036 packet in this same
  batch (proposed → approved), byte-scoped exactly as the packet proposed them.
- R721 (every merge queues for the owner) is expressly unchanged by this dispatch.

## Redaction

No redaction required: no session identifier, machine username, absolute user path, or hostname.
Verified by scan before commit.

---

## THE OWNER MESSAGE (verbatim, complete)

OWNER RULINGS AND BUILD DISPATCH — Step 7 decisions + M0-T036.
Capture this message verbatim through directive compliance before acting on
any part of it.

RULINGS:
1. R307 — DISCHARGED effective 2026-08-03 on the recorded evidence (post-
   accept verifier and both M0-T035 gates ran pinned Fable 5 with honest
   disclosure). The temporary explicit-Opus-5 exception is closed.
2. Merges #143, #144, #145 — RATIFIED. Content was verified through
   independent paths; nothing is reverted; the incident finding R718-R724
   stands unmodified on the record.
3. ADR-005 — ADOPT the proposed amendment text from the Phase 0 return
   verbatim, captured through the recommended vehicle (D-007 amendment row
   plus ADR-005 doc note). It must be in force before any pushing supervisor
   mode activates.
4. Audit anchoring — Option A (controller-pushed anchor branch).
5. Controller location — adopt the packet's recommended option; if the
   packet records no single recommendation, queue the choice as the build's
   first ASK and continue independent work — do not stop the lane on it.
6. Behavioral probes (--max-turns, stream-json canUseTool) — run as the
   first Phase 1 acts under this task's own rules; record results in the
   Phase 1 checkpoint; stop only if a result contradicts the CLI-adapter
   decision.
7. Standing grants — the two grants proposed in the M0-T036 packet are
   APPROVED and ACTIVE, exactly as written, task-scoped, expiring with the
   task, never widened: (a) auto-approve python tools/
   test_agent_supervisor_*.py inside the task worktree; (b) auto-approve
   push to task/M0-T036-supervisor-bridge after a passing review — never
   main.
8. SESSION_HANDOFF.md — refresh it as part of normal practice once this
   dispatch is captured.
9. Everything else on the recorded backlog stays backlog. M0-T035 and
   M0-T034 acceptance runs happen later on my instruction.

DISPATCH: M0-T036 is dispatched. Execute Phases 1 through 5 per the captured
D-007 directive and the task packet: every Section 18 stop condition live,
R721 unchanged — every merge queues for me — all gates as contracted, and
the D-006 measurement discipline continuing. Phase 5 ends at the shadow-
pilot decision packet and STOPS for my activation decision. Limited-auto is
never enabled by this task. Build it.
