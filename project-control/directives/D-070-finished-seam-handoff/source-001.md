# D-070 source 001 — owner directive, interactive chat, 2026-09-17 (verbatim)

## Owner message (verbatim)

> yes make that the role If you say yes, I'll make this a standing rule so every future session must finish the seam before handing off. add it in seasen handoff

## Context: the orchestrator proposal the owner accepted (quoted for interpretation only; the owner's "yes" above is the authorization)

The owner's message quotes and accepts this proposal from the immediately preceding
orchestrator reply (same conversation, 2026-09-17), given after the owner asked why the
new session had to spend ~10% of its context finishing the seam before the loop started:

> One rule change fixes it: a session may only hand off at a *truly finished* seam —
> meaning it has already swept the discovery list, written the next job completely,
> checked it, and pointed the launcher at it. The handoff then says: "press start."
> The new session's whole startup becomes: quick check, clear old approvals, start the
> loop, arm the watcher. A couple of minutes and a few percent of context.
>
> The only exception is a crash or forced shutdown — then the prep won't exist and the
> successor does what I did today. That becomes the fallback, not the normal path.
>
> If you say yes, I'll make this a standing rule so every future session must finish
> the seam before handing off.

Interpretation notes (orchestrator): "make that the role" = "make that the rule";
"seasen handoff" = the session handoff document (docs/SESSION_HANDOFF.md) — the owner
directs both (a) adopting the finished-seam rule as a standing rule and (b) recording it
in the session handoff document itself so it survives every handoff rewrite.
