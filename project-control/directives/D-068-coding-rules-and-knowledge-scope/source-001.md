# D-068 source 001 — owner directive, interactive chat (voice-transcribed), 2026-09-17 (verbatim)

## Owner message (verbatim)

> I need to add that also the stuff u found that is useful for next part down the road should be added to the md not just the loop we can also make a role md file that will italy have very few pointer words or adjacent? That will just say this rules are coding rules like. Don't do this, don't do this, don't do this, do this, do this, do this? When it comes to let's say file preparation. Fireplace, man or any type of editor that he ran in, that you think down the line, we should like a c. L issue, or this had that, you know what I mean

Orchestrator reading at capture (voice-transcription noise interpreted in context; the
message arrived while the orchestrator was fixing five CI defects from the M5-T032 seam):
(1) knowledge discovered during work that is useful "down the road" must be added to the
shared knowledge markdown stores — not only loop-related knowledge ("the md" = the D-054
stores). (2) Additionally create a RULES md file ("role md" = rules md) that is extremely
terse — "very few pointer words" — containing plain do/don't CODING rules ("Don't do this…
do this…") for things like file preparation/placement and any error class we actually ran
into that could later cause a CI issue ("a c. L issue" = a CI issue). Seed it from real
defect classes already hit; keep appending as new classes appear.
