# D-050 source-001 (original, verbatim) — owner directive, interactive chat (companion session), 2026-09-13

Capture head: `e063b7c70dfcb9df8e251195146511dbfc63447d` (branch `candidate/D-024-mrl-option-b`).
Frozen origin/main baseline: `d8b3899f61efa6620e18a26541ced96020f5bef9`.
Captured by the companion session (ctl24-8e) per the Part-0 working model; D-042..D-049
peer-capture precedent.

## owner-message-verbatim

Directive (owner, verbatim):

> We could tell him that if he needs something research, we could do it manually. So this way to
> make it go faster, would that be, would there be a option like that we can add? So I can use
> astero's research ability to bring back the results. So to make it go faster if it needs answers

## capture-context

- "Him" = the build loop / build session. "Astero's" = the owner's external deep-research AI
  tooling (the same channel that produced tonight's §11-25 finding, the §23-421(g) verification,
  and earlier `docs/design/astra-presentation-research.md`), referred to elsewhere as Astra.
- The owner authorizes an OWNER-ASSISTED RESEARCH CHANNEL to accelerate the D-045 campaign:
  when the loop has research-shaped questions (source discovery, section maps, definition texts,
  cross-reference hunts), it may hand them to the owner, who runs external deep research and
  brings back results — instead of, or in advance of, the loop spending its own research cycles.
- Precedent proving the pattern (same day): owner research located ZR §11-25 and verified
  §23-421(g); the pipeline then verified every claim against official text (print/PDF capture,
  byte-hashed) before encoding under D-049. The channel formalizes exactly that division:
  owner research = DISCOVERY AID; the pipeline's official-source capture = the only provenance
  rules may cite.
- Non-blocking design: the loop never waits on the channel; requests are appended to a running
  document and the loop proceeds with its own research when an answer has not arrived.
- Unchanged: G1 data-contract verification, the capture-completeness rule (print/PDF-class
  artifacts), D-048's ambiguity pattern, D-049-style owner decisions for interpretation, G6, and
  every gate. Owner-supplied research is never a source of record, never a sign-off, and never
  bypasses verification.
