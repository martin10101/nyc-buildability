This is a new owner instruction.

We are deferring—not canceling—the autonomy/controller activation described in NYC_BUILDABILITY_AUTONOMY_ACTIVATION_HANDOFF_2026-08-19.md. Do not continue that activation work in this task.

Our priority now is to resume building the actual NYC Buildability product.

First, reconcile the live repository, GitHub, ledger, directives, accepted tasks, open PRs, and CI. Confirm this is a genuinely fresh Claude Code process started at the repository worktree root and that the active MCP roster is empty. If any MCP server or MCP tool is present, stop and report only the exact cause and smallest restart correction.

Preserve these holds:

- D-013-R060 remains PENDING.
- Do not run the controller-update bundle.
- Do not enable limited-auto.
- Do not modify protected controller configuration, model-selection configuration, tools/agent_supervisor/**, the context pipeline, or the MCP policy.
- Do not begin another context-management, autonomy, governance, or infrastructure-hardening project unless a directly reproduced blocker prevents the selected product task.
- Preserve unrelated dirty files exactly as found.
- Do not weaken tests, reviews, CI, security controls, or branch protection.

After reconciliation, read the accepted product roadmap, PRD, task ledger, and relevant implementation code. Identify the single next dependency-ready PRODUCT unit for the established first pilot:

Single-lot, low-rise R5 family-residential new construction, while preserving the five-borough architecture and staged future rule expansion.

Prefer an already-planned product task if one exists. Select the smallest meaningful end-to-end unit that advances something a future user can actually enter, calculate, verify, view, or export. Do not select documentation-only work, another broad discovery exercise, or a speculative refactor.

If there is exactly one clear next unit:

1. Capture this owner instruction verbatim as the next directive resolved from the live registry.
2. Decompose it into independently verifiable requirements.
3. Create exactly one bounded product task, one branch, and one PR.
4. Implement it end to end with deterministic calculations, provenance, coverage boundaries, tests, and honest failure states.
5. Use the existing independent producer/reviewer/verifier lifecycle.
6. Stop with the PR independently accepted and fully green, but unmerged, and return a plain-English owner report plus the exact merge identity.

If the repository reveals multiple materially different next units requiring an owner product decision, do not implement any of them. Return only the choices, their user-visible outcomes, dependencies, and your recommended first choice.

This is authorization for one bounded NYC product task only. It is not authorization for controller activation, R060 promotion, broad infrastructure work, deployment, or merging.
