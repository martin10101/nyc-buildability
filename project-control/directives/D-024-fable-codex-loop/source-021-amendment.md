# D-024 Amendment 21 — corrected-start continuation of the Amendment-20 attempt authorized; pre-dispatch refusal ruled non-consuming (owner instruction 2026-08-30)

Captured: 2026-08-30 UTC by the orchestrator (Fable 5), verbatim from the owner's typed
message, BEFORE acting on it (D-001). Base identity at capture: branch
`control/D-024-fable-codex-loop`, HEAD `9c6548b` (local == origin; tree clean; campaign
seq 46 — the Amendment-20 attempt refusal record). Context: the R350 preflight passed all
10 rows; the owner executed step 1 `clear-recovery` (SUCCESS as certified) and step 2 (the
verbatim M0-T124 §4 item-3 start), which was REFUSED PRE-DISPATCH exit 11
`cwd_primary_checkout` — no provider contacted, no cycle began, journal preserved at
PREFLIGHT (`M0-T107-amendment20-start-refusal.md`). Amends: `source-001.md` (owner
directive v4) and the Amendment-20 window (`source-020-amendment.md`). Requirement IDs
assigned: D-024-R363..D-024-R371.

Reconciliation: the owner CONTINUES the same Amendment-20 single attempt (nothing here
grants a second attempt) with exactly one command correction — the `--worktree` binding
the certified M0-T123 seam requires — and RULES the pre-dispatch refusal non-consuming on
the stated grounds (no provider contact, no cycle). Sequencing: four verifications BEFORE
presentation (journal still PREFLIGHT; worktree clean; packet declares that exact
worktree; the complete corrected command parses). The M0-T124 command record is corrected
(append-only correction; the report is not digest-pinned by any verification record) and
the applicable control-plane validation/CI runs; supervisor source stays untouched and no
recertification triggers unless the certified material identity actually changes (it does
not — this is a control-plane/document correction only). The orchestrator presents ONE
complete, unwrapped command; the OWNER executes it; the orchestrator never executes it and
never runs `clear-recovery` again. All Amendment-20 rows not modified here stay binding
unchanged (eight-point proof R352–R359, prohibitions R360, failure protocol R361/R362).

Forward trace: s1 (authorize continuation with the --worktree correction) → R363; s2
(refusal ruled non-consuming) → R364; s3 (four pre-presentation verifications) → R365;
s4a (correct the M0-T124 command record) → R366; s4b (run applicable control-plane
validation/CI) → R367; s4c (no supervisor-source alteration / no recertification unless
the certified material identity actually changes) → R368; s5 (present one complete,
unwrapped command for the owner to execute) → R369; s6 (do not execute it yourself) →
R370; s7 (do not run clear-recovery again) → R371.

Anchors: #continuation-authorization (s1–s2), #pre-presentation-verification (s3),
#record-correction-and-validation (s4), #presentation (s5), #prohibitions (s6–s7).

---VERBATIM-BEGIN---
I authorize continuing the same Amendment-20 attempt with the required --worktree C:/Users/MLFLL/Downloads/nyc-zoning/wt-m0t107 correction. Treat the pre-dispatch refusal as non-consuming because no provider was contacted and no cycle began. Before presenting the command, verify the journal remains at PREFLIGHT, the worktree is clean, the task packet declares that exact worktree, and the complete command parses correctly. Correct the M0-T124 command record and run the applicable control-plane validation/CI; do not alter supervisor source or trigger recertification unless the certified material identity actually changes. Then present one complete, unwrapped command for me to execute. Do not execute it yourself. Do not run clear-recovery again.
---VERBATIM-END---
