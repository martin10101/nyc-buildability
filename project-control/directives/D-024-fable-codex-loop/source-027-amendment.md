# D-024 Amendment 27 — OPTION A authorized: narrow one-time R413 lift for the wt-m0t107 fast-forward (owner instruction 2026-08-31)

Captured: 2026-08-31 UTC by the orchestrator (Fable 5), verbatim from the owner's typed
message, BEFORE acting on it (D-001). Base identity at capture: branch
`control/D-024-fable-codex-loop`, HEAD `250bf73` (local == origin; campaign seq 57).
Delivery context: the one-character reply "a" answers the Option-A/Option-B decision the
orchestrator presented this session (campaign seq-57 NEXT;
`reports/M0-T107-commissioning-start-refusal.md` section 4) after the commissioning
Step-2 start was refused pre-dispatch (`task_authority` / `ledger_record_missing` at the
presented `--repo`). Per that presented menu, "a" SELECTS OPTION A. The orchestrator's
presented Option A, restated: (1) a narrow, ONE-TIME lift of the R413 hold on
`wt-m0t107` solely to fast-forward its checked-out branch
`task/M0-T107-plugin-portability` from `796e18f` to the current control tip (clean
worktree, proven ancestor, pure fast-forward; only that branch ref moves); (2) the
orchestrator re-runs the full section-2 preflight and re-presents the Step-2 command
corrected in exactly one value (`--repo C:/Users/MLFLL/Downloads/nyc-zoning/wt-m0t107`);
(3) the owner re-types Step 2 only (Step 1 is not repeated; the journal already rests at
PREFLIGHT). Amends: `source-001.md` (owner directive v4). Requirement IDs assigned:
D-024-R417..D-024-R419.

Reconciliation: R413's preservation hold on `wt-m0t107` is lifted ONCE, narrowly, for
the fast-forward only — R413 otherwise stands (M0-T107 packet content authority, PR
#241, and the preserved journal evidence remain untouched; the journal's PREFLIGHT
state is the owner's own Step-1 act). R409/R414 are unchanged: the orchestrator never
executes either section-4 command; the corrected Step-2 command is presented for the
owner to type. No supervisor file changes (no R247 re-trigger). Option B (the AD-093
probe-root defect task) remains an un-selected candidate, not authorized by this
amendment.

Forward trace: menu selection "a" -> Option A item (1) (authorize the narrow one-time
R413 lift; fast-forward wt-m0t107 to the current control tip) -> R417; item (2)
(re-run the complete section-2 preflight after the fast-forward; re-present Step 2 with
the single corrected --repo value, mechanically validated) -> R418; item (3) (owner
re-types Step 2 only; Step 1 not repeated; orchestrator never executes) -> R419.

Anchors: #option-a-lift (item 1), #option-a-repreflight (item 2),
#option-a-owner-retype (item 3).

---VERBATIM-BEGIN---
a
---VERBATIM-END---
