# D-024 Amendment 28 — reserved-turn delivery defect fix authorized, fastest sound path (owner instruction 2026-08-31)

Captured: 2026-08-31 UTC by the orchestrator (Fable 5), verbatim from the owner's typed
message, BEFORE acting on it (D-001). Base identity at capture: branch
`control/D-024-fable-codex-loop`, HEAD `6b7000b7` (local == origin; campaign seq 59).
Delivery context: direct reply to the journey-3 consolidated assessment
(`reports/M0-T107-commissioning-journey-3.md`, campaign seq-59 NEXT), whose decision
item (1) proposed the bounded AD-093 defect task for the reserved-turn delivery /
unit-completion defect, offering the exact authorization line. "fix it fastest way"
AUTHORIZES that defect task and directs the fastest sound execution path. Amends:
`source-001.md` (owner directive v4). Requirement IDs assigned: D-024-R420..D-024-R424.

Reconciliation: the fastest SOUND path — never a gate bypass — is the M0-T108
precedent: a bounded defect packet (M0-T130) produced by the orchestrator in the
primary checkout (single writer, no parallel producers), with independent reviewers
(producer != reviewer preserved), the supervisor-freeze qualifying-evidence citation
(the journey-3 reproduced defect + measured provider-CLI behavior), removal-sensitive
coverage whose absorption fixture derives from the journey-3 LIVE measurement (no new
provider launches to re-provoke it), the standard gates, accept, then the R247
recertification at the new frozen identity (the supervisor tree moves), and
re-presentation of the journey command for the owner to type (R409/R414 unchanged).
S16.7 remains a measurement; this amendment does not alter or reset it.

Forward trace: "fix it" -> authorize the proposed bounded defect task -> R420; the
defect's two symptoms (premature reserved-turn delivery; completion-accounting wall
ride) -> R421, R422; sound-fix necessities carried from the assessment (measured
fixture + removal-sensitive coverage) -> R423; "fastest way" governs sequencing to the
re-presented journey (recert then owner-typed start, gates intact) -> R424.

Anchors: #fix-authorization (R420-R422), #fix-harness (R423), #fix-sequencing (R424).

---VERBATIM-BEGIN---
fix it fastest way
---VERBATIM-END---
