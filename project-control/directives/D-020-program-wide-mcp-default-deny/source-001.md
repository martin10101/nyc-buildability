NEW SESSION HANDOFF AND OWNER DIRECTIVE
NYC BUILDABILITY — PROGRAM-WIDE MCP DEFAULT-DENY

Read this entire instruction before acting.

This instruction authorizes exactly ONE bounded repository task for the
program-wide MCP policy described below. It does not authorize controller
activation, R060 promotion, or NYC application development.

============================================================
1. RECONCILE THE LIVE REPOSITORY FIRST
============================================================

Treat every identity below as a dated checkpoint. Verify all facts against
the live local repository, origin/main, GitHub, task ledger, directive
registry, and CI before creating or modifying anything.

Reported checkpoint:

- Repository: github.com/martin10101/nyc-buildability
- PR #239: MERGED
- Authorized PR head:
  93c80fb6ed47237f4808eebdf0ebf2458c07d6ed
- Merge commit:
  31c50a09bd1671d111f21923c6a2d739f51187dd
- M0-T076: accepted
- Expected main-branch accepted count: 99
- D-019/M0-T076 lifecycle: closed
- Post-merge CI: green
- D-013-R060: PENDING
- Controller-update bundle: not run
- Automatic supervisor packet consumption: not enabled
- limited-auto: disabled
- Program-wide MCP policy: not implemented

The post-merge web-e2e job reportedly encountered a stuck GitHub runner
twice but passed normally on healthy reruns. Reconcile this read-only and
do not treat infrastructure delay as a product defect without evidence.

If the live repository disagrees materially with this checkpoint, STOP
and report the discrepancy. Do not repair or reconcile it silently.

============================================================
2. WHAT HAS ALREADY BEEN BUILT
============================================================

The context-intelligence pipeline now provides deterministic, bounded,
evidence-grounded task packets containing:

- the task contract;
- exact applicable owner requirements;
- relevant source-code excerpts;
- relevant tests;
- repository graph relationships;
- subsystem placement;
- bounded advisory memory;
- provenance and sufficiency results;
- evidence-derived model-routing signals.

The latest corrections added:

- safe multi-writer memory locking;
- complete repository path containment and redaction;
- correct frozen-G0 reviewer diffs;
- shared repository-neighborhood consumption;
- changed-implementation-first graph seeding;
- honest “undetermined” risk signals;
- more useful advisory memory;
- clean-checkout reproducible benchmarking;
- permanent CI coverage.

Do not reopen, rebuild, or redesign this system during the MCP task.

============================================================
3. THIS TASK’S ONLY OBJECTIVE
============================================================

Every ordinary Claude Code session working in NYC Buildability must default
to having NO unrelated external MCP server active.

Examples currently known or previously observed:

- claude.ai Airtable;
- Microsoft 365;
- Pencil;
- Supabase.

These connectors must not accompany ordinary repository, control-plane,
testing, context, or application tasks merely because they are installed
globally or connected to the owner’s account.

A connector may be used only when a future task explicitly authorizes that
specific connector for a stated purpose.

Examples:

- Supabase may be authorized for a bounded database task.
- Pencil may be authorized for a bounded design task.
- Airtable may be authorized for a bounded Airtable integration task.

Installation or account availability is not task authorization.

This task itself requires no external MCP connector. Do not invoke Airtable,
Microsoft 365, Pencil, Supabase, or another external MCP during this work.

============================================================
4. CONTROL-PLANE CAPTURE
============================================================

After the read-only reconciliation:

1. Capture this complete owner instruction verbatim as the next available
   directive.

2. The expected identifier may be D-020, but do not assume it. Determine
   the next valid identifier from the live registry.

3. Create exactly one bounded task using the next valid task identifier.
   The expected identifier may be M0-T077, but do not assume it.

4. Use one task, one branch, and one PR.

5. Freeze the exact allowed paths before implementation.

6. Use the full existing lifecycle:
   reconciliation → directive capture → G0 → implementation → tests →
   frozen submission → fresh independent adversarial review → independent
   directive-compliance verification → gates → acceptance.

7. Do not merge the PR without a later explicit owner merge authorization.

============================================================
5. REQUIRED READ-ONLY MCP AUDIT
============================================================

Before choosing a configuration change, identify every visible MCP server
and record:

- exact configured name;
- displayed name;
- server source;
- local/project/user/plugin/managed/claude.ai scope;
- configuration file or account source;
- whether it is currently enabled;
- whether it follows all projects or only this repository;
- what capability it exposes;
- whether NYC Buildability currently requires it;
- whether it can be disabled at repository scope without changing the
  owner’s global account.

Inspect the applicable Claude Code settings and MCP configuration using
read-only commands.

Do not expose secrets, tokens, credentials, connection strings, database
keys, account contents, or private external data in reports or logs.

Use official Claude Code documentation when configuration behavior needs
confirmation.

============================================================
6. REQUIRED IMPLEMENTATION
============================================================

Implement the smallest supported repository-level policy that achieves the
following outcome:

1. Merge the supported setting that disables automatic claude.ai account
   connectors into the existing checked-in project settings.

2. Preserve every existing setting. Never replace the settings file
   wholesale.

3. Disable other audited MCP servers at repository scope using their exact
   audited identifiers and the officially supported mechanism.

4. The policy must apply to fresh ordinary Claude Code sessions opened
   from this repository and its clean worktrees.

5. Do not disconnect or delete account-wide connectors.

6. Do not modify the owner’s global Claude configuration merely to make
   this repository test pass.

7. Do not damage Airtable, Supabase, Pencil, or Microsoft 365 availability
   in unrelated projects.

8. Document the default-deny rule and how a future task can request a
   narrowly authorized connector.

9. Add the smallest durable automated validation necessary to detect if
   the repository policy is accidentally removed or weakened later.

10. Do not build a new general-purpose permissions framework.

If one type of MCP server cannot be blocked through a supported
repository-level mechanism, do not modify global settings as a workaround.
Report the exact limitation and the smallest owner-gated next step.

============================================================
7. FRESH-SESSION PROOF
============================================================

Do not claim success merely because MCP tool definitions are deferred or
unused.

Prove the policy from a fresh process or clean worktree, not only from the
already-running Claude session.

The evidence must show:

- Airtable is not active;
- Microsoft 365 is not active;
- Pencil is not active;
- Supabase is not active;
- no unexpected external MCP server is active;
- repository settings still load normally;
- unrelated settings were preserved;
- no global account connector was removed;
- the result survives closing and starting a fresh repository session.

If the current session cannot verify its own restart behavior, use the
smallest safe fresh-process verification available and state any remaining
limitation honestly.

============================================================
8. SUPERVISOR COMPATIBILITY — READ ONLY
============================================================

Inspect, without modifying, how the existing supervisor launches Claude
Code.

Determine whether supervised launches load the repository’s checked-in
Claude settings.

If they do, prove that read-only.

If they do not, or this cannot be established without changing protected
controller or supervisor code:

- do not change those files;
- do not claim full supervised enforcement;
- record the exact owner-gated integration gap;
- propose the smallest later controller change separately.

============================================================
9. SUB-AGENT USE
============================================================

Sub-agents are permitted and encouraged when they materially improve speed
or independent verification.

Good bounded uses include:

- MCP provenance discovery;
- configuration-scope analysis;
- test-gap analysis;
- adversarial review;
- directive-compliance verification.

Do not allow simultaneous edits to overlapping files.

The primary agent owns implementation and integration. The producer,
adversarial reviewer, and directive verifier must remain genuinely
independent.

============================================================
10. PROTECTED AND FORBIDDEN AREAS
============================================================

Unless the frozen task contract specifically includes an MCP configuration
file after the audit, treat it as out of scope.

Only narrowly necessary repository MCP configuration, policy
documentation, automated validation, and required project-control
artifacts may change.

Do not modify:

- tools/agent_supervisor/**;
- protected controller installation or configuration;
- model-selection configuration;
- tools/model_routing.py;
- application code under apps/**;
- services/**;
- packages/**;
- supabase data or configuration;
- Airtable or Microsoft 365 data;
- branch protection;
- security policy;
- unrelated workflows or tests;
- D-013-R060 state;
- previously accepted task evidence.

Do not enable limited-auto.
Do not rewrite history.
Do not weaken, remove, skip, or replace existing tests.

============================================================
11. REQUIRED RETURN
============================================================

Return:

1. Live starting main SHA and accepted-task count.
2. Directive, task, branch, PR, submitted SHA, and manifest identities.
3. Complete MCP inventory with source and scope.
4. Exact files changed and why.
5. Before-and-after fresh-session MCP results.
6. Proof global connectors were not removed.
7. Tests and CI results.
8. Independent reviewer result.
9. Per-requirement verifier result.
10. Whether supervised launches consume the repository policy.
11. Any remaining owner-gated supervisor limitation.
12. Explicit confirmation that R060 remains pending and the controller
    bundle was not run.

Stop after the PR is independently accepted and green. Do not merge it,
run the controller bundle, approve R060, or begin another task without my
explicit authorization.C:\Users\MLFLL\Downloads\nyc-zoning
