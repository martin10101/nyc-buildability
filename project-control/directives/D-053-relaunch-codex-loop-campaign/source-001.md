# D-053 source-001 (original, verbatim) — owner directive, interactive chat (companion session), 2026-09-14

Capture head: `859b34c926360e9800b3ce76bd71fc504d2eff7f` (branch `candidate/D-024-mrl-option-b`).
Frozen origin/main baseline: `d8b3899f61efa6620e18a26541ced96020f5bef9`.
Captured by the companion session (ctl24-8e) per the Part-0 working model; D-042..D-052
peer-capture precedent (D-051/D-052 were build-session captures; id sequence coordinated).

## owner-message-verbatim

Context (companion, immediately prior): after the owner corrected the companion's wrong account —
the codex loop WAS finished and working (live limited-auto dispatch + live Codex/Astra reviews;
first end-to-end delivery 2026-09-07; owner-relaunched on product work; only the FINAL push/merge
authority and the independent monitoring layer were deliberately kept manual/off to the side) —
the companion offered: "Want me to relay that to the build session — relaunch the loop on the
campaign queue, session goes back to monitor-and-stamp? Say the word and I'll capture it as your
directive (D-053) and send it over."

Directive (owner, verbatim):

> Yes. Because the whole point was to use Codex model Astro for the guide. Now the question is
> just for the research purpose, should we continue... should we use Astro? Or Astro is their
> model that we use. Or we should... or how is it that gonna work? Like, who's... how is how is
> it gonna communicate since it launches under a different PowerShell? You know what I mean?
> Typically, it can monitor it, but you know what I mean?

## capture-context

- "Astro" = Astra, in BOTH of its roles, which this directive keeps distinct:
  (a) the Codex reviewer model wired INSIDE the loop's bridge ("the guide") — resumes
  automatically with the loop relaunch; (b) the owner's external deep-research tooling — the
  D-050 channel, which stays owner-driven and manual, unchanged.
- The owner's communication question ("it launches under a different PowerShell") is answered in
  the decomposition: the detached loop communicates through the repository (commits/pushes,
  ledger, queue files); the build session becomes the quiet MONITOR + final authority (stamps,
  merges, pushes) and owns the owner-facing notification leg (D-050-R005), since the detached
  process has no owner-facing channel of its own; the companion session remains the owner's
  conversational leg and directive-capture channel.
- Authority boundary UNCHANGED: this directive does NOT activate live automated merging/acceptance
  (the R595 live-actuation path stays unbuilt/shadow; ADR-005/ADR-006 stand). The loop does the
  full production job; the monitor stamps.
- All campaign directives (D-045..D-052) bind inside the loop exactly as outside it, including
  D-046 disjointness, D-047 producer-model intent (applied within the loop's owner-approved model
  allowlist; a blocked allowlist is an owner settings item, not a bypass), D-048 ambiguity
  pattern, D-049/D-052 owner decisions, D-050 research channel, D-051 fallback validation.
