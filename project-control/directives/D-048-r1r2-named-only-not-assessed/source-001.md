# D-048 source-001 (original, verbatim) — owner directive, interactive chat (companion session), 2026-09-13

Capture head: `05b5721f45ecaa69d62662082d28ecc5c32fc2d2` (branch `candidate/D-024-mrl-option-b`).
Frozen origin/main baseline: `d8b3899f61efa6620e18a26541ced96020f5bef9`.
Captured by the companion session (ctl24-8e) per the Part-0 working model; D-042..D-047
peer-capture precedent. Context: blocker B-023 (ZR 23-421 group-label scope, Section 20 stop on
M4-T012) was relayed to the owner with three options: (1) "Cover only what the text names; mark
the variants not-assessed" (recommended; ambiguous variants show 'not assessed — needs
professional review' until qualified review; questions join the G6 list), (2) hold R1/R2 until a
professional answers, (3) owner's own wording.

## owner-message-verbatim

Directive (owner, verbatim):

> "Cover only what the text names; mark the variants not-assessed
> Do this but write in md what I have to check with architect

## capture-context

- The owner selects option 1 for B-023 AND orders an owner-facing Markdown document listing the
  questions to check with their architect / zoning professional.
- Effect on M4-T012 (R1+R2 height/setback): implement exactly the districts ZR 23-421 enumerates
  ("R1 R2 R3A R3X R3-1 R3-2 R4 R4-1 R4A R5A" — bare R1 and R2 are named; the R3/R4/R5A variants
  belong to their own family tasks). The lettered variants NOT named anywhere in the captured
  governing text — R1-1, R1-2, R1-2A, R2A, R2X — are rendered "not assessed — needs professional
  review" (five-tag discipline; never an inherited envelope, never zero). Per B-023 detail item
  (4), sloping-plane setback mechanics remain typed A2 gaps (D-045-R008 sequencing); only the
  numeric 25 ft perimeter-wall / 35 ft ridge caps are encoded for the named districts.
- The three B-023 interpretation questions are NOT answered by anyone here — they move to the
  owner-facing questions document and the G6 agenda. B-023's blocking decision is thereby made by
  the owner; the build orchestrator updates the blocker record and re-dispatches M4-T012 when its
  directory scopes are free (not while M4-T014 is in flight — D-046-R002).
- STANDING PATTERN (companion decomposition of "Do this" + the running-document order, flagged to
  the owner for veto): future D-045-campaign cases of the same shape — governing text ambiguous
  about whether a lettered variant/district is swept into a rule — receive the same treatment
  WITHOUT a new owner stop: encode the explicitly named districts, mark the unnamed ones
  "not assessed — needs professional review", and append the question with citations to the
  questions document. A genuine contradiction, or an ambiguity that guts an entire task, still
  stops per D-008.
- Honest consequence stated to the owner: most real R1-district lots are mapped R1-1/R1-2, so
  until the professional answers, many R1 lots will show heights "not assessed" — correct, and a
  reason to ask the architect sooner.
- The questions document is `docs/ARCHITECT_REVIEW_QUESTIONS.md` (created with this capture);
  answers come back as owner directives / G6 rulings, never as agent interpretations.
