I am attaching NYC_BUILDABILITY_AUTONOMY_HANDOFF_2026-08-21.md. Read it completely before acting.

My goal is one coordinated campaign that gracefully upgrades the existing NYC Buildability engineering system. Do not rebuild the project, replace its agent roster, install a third-party agent framework, or copy executable code from the reviewed community repositories.

First reconcile the live GitHub repository and, where available, the actual Windows controller host. Treat every SHA, task count, PR state, config hash, CLI version, and model ID in the handoff as dated evidence to verify, not as a current fact. Read the actual implementation, tests, directives, active task packets, CI, and protected configuration. Do not rely on Claude's reports alone.

This message is a substantive owner directive. Capture it verbatim through the repository's directive-compliance process before making repository changes. It authorizes investigation and a controlled implementation campaign for:

1. A genuinely bounded unattended mode capable of a measured 10-hour run, using the existing supervisor, Claude workers, Codex reviewers, project-control ledger, worktree isolation, and GitHub workflow.
2. Real task-branch commit/push/PR/ordinary-green-merge continuation only where the existing autonomy tiers and active directives permit it. It does not override any explicit DO-NOT-MERGE banner or active owner hold, including PR #241.
3. Reliable context/session turnover and model-quota fallback using only owner-approved, live-probed model IDs; no silent or unlisted model substitution.
4. Official Claude Code mobile Remote Control for monitoring and steering local sessions, without pretending it is the supervisor's authenticated approval adapter.
5. A small project-owned engineering-reliability standard/skill, written from scratch, that incorporates only the vetted principles in the handoff. Do not install or execute Superpowers, secondsky, wshobson, or any other downloaded agent/plugin code.

Complete the full investigation before sending Claude Code any correction or implementation prompt. Collect every serious issue, reproduce important claims adversarially, and then issue one consolidated directive—no drip-feeding one new problem after each correction.

Required investigation and proof rules:

- Reconcile origin/main, open PRs, worktrees, ledger, CI, and the live controller before review.
- Use a clean checkout and freeze one code/content identity. Producer, reviewers, verifier, and final re-review must test that same identity. Any material change invalidates prior reviews.
- Test clean checkout, Windows behavior, Linux supported-or-explicit-fail-closed behavior, concurrency, unsafe paths, stale state, crash windows, model exhaustion, session rotation, GitHub side effects, mobile flows, integration, and real user/browser journeys where applicable.
- Distinguish unit/fake-runner proof from live end-to-end proof.
- Classify findings as Must fix now, Important but deferrable, or Minor recommendation. Do not reopen work for cosmetic or low-impact issues.
- Never claim autonomy, correctness, savings, token reduction, reliability, or 10-hour readiness without measured evidence.
- After corrections, independently review the corrected frozen result once more before declaring completion.

Authorization limits remain in force. Do not deploy to production, expose or acquire credentials, approve legal/zoning interpretations, release G6 or other legal holds, weaken security/CI/review/branch protections, rewrite history, force-push, push directly to main, alter unrelated user work, or merge PR #241 without separate explicit authorization. Do not activate unattended mode until its stated prerequisites and live canaries pass. If an existing directive requires a post-evidence owner decision that this conditional authorization cannot legally satisfy, bundle every remaining owner-only action into one exact final checkpoint instead of interrupting me repeatedly.

Begin by returning a short reconciliation table: live identities, what is already real, what is simulated, what is blocked, and the single ordered campaign you will execute. Then continue autonomously within the authorization above.
