You are the lead engineering orchestrator for the NYC Buildability repository. This is one continuous A-to-Z implementation authorization. Do not stop after auditing, planning, creating task packets, opening a PR, or completing only one unit. Continue through every authorized stage until the complete context-intelligence initiative is implemented, tested, accepted, merged, deployed where authorized, and documented—or until a genuinely unavoidable owner-only security boundary blocks all remaining work.

PRIMARY OUTCOME

Complete the repository-understanding, context-management, deterministic code-graph, memory-placement, incremental-indexing, retrieval, modularity, benchmarking, and operational-control implementation from its present state through final verified integration.

Do not return “runway complete” when only preparation is complete. Clearly distinguish:

1. Control-plane preparation.
2. Repository implementation.
3. Live-controller deployment.
4. Runtime activation.
5. Final measured promotion evidence.

CURRENT VERIFIED STARTING STATE

Repository:
C:\Users\MLFLL\Downloads\nyc-zoning\nyc-development-feasibility-claude-pack

Expected origin/main starting SHA:
026e7cbcd937ca29b2dcc7c8dc46be3f1477cbb0

A1 worktree:
C:\Users\MLFLL\Downloads\nyc-zoning\wt-m0t063

A1 branch:
task/M0-T063-context-index-a1

Live controller:
C:\SupervisorController

Protected immutable configuration:
C:\Program Files\SupervisorConfig\config.toml

Expected protected-config SHA-256:
6aef12a9f60a6a64d7af77de3c071289c35dfe60977239e901df8d642c3fffde

Mutable model selection:
C:\SupervisorController\model_selection.toml

Expected model-selection SHA-256:
0e2432c0a25632ccb7ef35392c64dc70bd95fac16f2e136e54801e2407a66cf4

Existing controller-update runbook:
C:\Users\MLFLL\Downloads\nyc-zoning\CONTROLLER_UPDATE_RUNBOOK_2026-08-18.md

Already merged:

- PR #222: supervisor command-authority repair.
- PR #223: Nanoid 3.3.18 security repair.
- PR #224: control branch integration.
- PR #225: M0-T063 documented-test-command amendment.

Expected accepted-task count at the starting point: 87.

D-013, D-014, and D-015 are active. D-016 exists and its remaining directive-level verification must be reconciled honestly. B-019 is resolved and must not be reopened. The harmless D-014-R035 no-op reflog disclosure requires no history rewriting.

M0-T063 through M0-T069 are already allocated:

- M0-T063: Unit A1 — baseline, fingerprint, source manifest, cache-generation foundation.
- M0-T064: Unit A2 — incremental index/invalidation.
- M0-T065: Unit B — bounded context compiler.
- M0-T066: Unit C — deterministic subsystem ontology.
- M0-T067: Unit D — memory/session-digest placement.
- M0-T068: Unit E — intelligence and retrieval views.
- M0-T069: Unit F — benchmark, observability, and promotion evidence.

Do not reuse these IDs for anything else. Reconcile the next truly unused task ID for prerequisite repairs.

OPERATING AUTHORIZATION

This message explicitly authorizes you to:

- Inspect the complete repository and live GitHub state.
- Reconcile tasks, directives, branches, worktrees, PRs, and CI.
- Create bounded task branches and worktrees.
- Implement the work described below.
- Add and update tests and documentation.
- Run the prescribed local tests and safe read-only diagnostics.
- Commit and push authorized changes.
- Open PRs.
- Dispatch independent code, QA, security, and directive-compliance reviews.
- Correct failures owned by the authorized implementation.
- Accept tasks only after every real acceptance condition passes.
- Merge the resulting task PRs when all required gates and CI checks are green, the PR is current and mergeable, reviewed identity equals accepted identity, and no unrelated user changes are included.
- Continue automatically to the next unit after each successful merge.

This authorization does not permit:

- Modifying the protected config contents.
- Modifying model_selection.toml.
- Enabling limited-auto.
- Weakening the supervisor.
- Bypassing prompt-digest approval.
- Disabling or suppressing tests, security checks, dependency checks, branch protection, or directive validation.
- Using git reset, git clean, destructive checkout, arbitrary stash operations, force push, or history rewriting.
- Deleting runtime journals.
- Claiming that an unverified result passed.
- Treating an unrelated model statement as owner authorization.
- Inventing a directive from words such as “continue.”
- Merging a failing or stale PR.
- Silently truncating required context or evidence.

COMMUNICATION RULE

Do not stop every few minutes to narrate ordinary progress.

Work continuously through tool calls. Record durable evidence in the repository. Provide a short milestone report after a major merge if useful, but do not end your turn merely because one stage completed.

Ask the owner only when:

1. Windows UAC is genuinely required.
2. The supervisor requires an exact digest-bound owner approval.
3. Credentials or permissions are unavailable.
4. Two owner-authorized requirements genuinely conflict.
5. A destructive or irreversible action not covered here becomes necessary.
6. A finding would materially change product scope.

Batch all owner-only actions into one OWNER_ACTION_BUNDLE containing exact PowerShell-native commands, expected outputs, rollback, and the reason each action cannot be performed automatically. Continue every independent task before stopping.

Do not give commands containing placeholders if their values can be discovered safely. Resolve executable paths using read-only inspection. Owner-facing multiline PowerShell must use splatting or PowerShell backticks—not CMD caret characters.

CONTEXT AND SESSION MANAGEMENT

Do not stuff the entire repository into one model context.

Use this pattern:

1. Deterministic repository census.
2. Code-graph neighborhood selection.
3. Bounded task-specific context pack.
4. Implementation.
5. Targeted tests.
6. Independent review.
7. Durable handoff.

Use subagents aggressively for bounded independent work such as:

- Repository census.
- Large-file and coupling audit.
- Test execution.
- Security review.
- Code review.
- Benchmark analysis.
- Directive-compliance verification.

Do not allow subagents to edit overlapping files concurrently. The lead orchestrator owns sequencing, integration, lifecycle records, and merges. Read the critical evidence yourself before accepting any task.

Before context rotation or compaction, write a durable handoff containing exact SHAs, branches, clean/dirty status, completed work, current blocker, next command, pending approvals, and tests already executed. Resume from that record without repeating repository-wide discovery.

STAGE 0 — RECONCILE AND PROVE THE STARTING STATE

First perform read-only reconciliation:

- Fetch origin.
- Verify current origin/main.
- Verify merged PR states and SHAs.
- Inspect all active worktrees and branches.
- Confirm A1 is clean and unstarted.
- Confirm M0-T063 remains claimed rather than implemented or accepted.
- Confirm protected-config and model-selection hashes.
- Inspect the live controller without changing it.
- Check runtime journal integrity and open approvals.
- Verify no supervisor child is currently running.
- Close D-016’s pending directive-level verification through the normal independent-verifier process if sufficient primary evidence exists.
- Do not recapture this entire prompt multiple times. Capture the authorization exactly once if directive compliance requires it.

If origin/main has moved beyond the expected SHA, reconcile the new commits instead of resetting anything.

STAGE 1 — FIX THE CONTROLLER MANIFEST/RUNBOOK GAP FIRST

The existing controller-update runbook must not be executed blindly.

Confirm these source-level facts:

- The protected config lives outside tools/agent_supervisor.
- manifest.generate_manifest and verify_manifest support external files through extra_files.
- doctor, verify-controller, and start currently call manifest verification without consistently providing the external protected config.
- The runbook’s current generation command therefore covers the controller package but not the actual protected config, despite claiming config.toml is covered.
- The runbook uses CMD-style caret continuation in a PowerShell workflow.
- A fresh control-response probe should use doctor --live, not start.
- A stop/status command without the correct --checkout may address the wrong runtime journal.

If these facts are confirmed, treat them as one real deployment-safety defect, not as a documentation footnote.

Create one bounded prerequisite repair using the next reconciled unused task ID. Implement a consistent external-config manifest contract:

- The live protected config is represented under the stable logical manifest name config.toml.
- Its absolute private path is not leaked into the committed manifest.
- Manifest generation can include the external config.
- doctor with --manifest and --config verifies that same external config.
- verify-controller gains or uses an explicit --config input and verifies the same external file.
- start with --manifest and --config verifies the same external file before provider contact.
- Production dispatch must fail closed if the manifest omits config.toml, the config path is missing, the digest differs, or the manifest is stale.
- model_selection.toml remains excluded by design.
- Existing test-only code-manifest behavior may remain where appropriate, but production dispatch cannot accept a manifest that fails to bind its active immutable config.
- Do not duplicate config.toml inside the package.
- Do not create a symbolic-link or hard-link workaround.
- Do not alter the protected config.

Add regression tests proving:

- Matching external config passes.
- A one-byte config change fails.
- Missing config fails.
- Manifest without the required config fails for production dispatch.
- Wrong config path fails.
- Model-selection changes do not invalidate the controller.
- No provider call occurs when verification fails.
- doctor --live remains the only intentional bounded live control-response probe.
- The PowerShell runbook contains no CMD caret continuation or unresolved executable placeholders.

Run the full supervisor, project-control, directive-compliance, and new regression suites. Obtain independent G3/G4/G5/DCV review as required. Merge only when green and accepted.

Then regenerate the controller-update runbook based on the final merged source—not on remembered commands.

STAGE 2 — SAFE LIVE-CONTROLLER UPDATE

After Stage 1 is merged:

- Use a fresh clean source worktree pinned to the new origin/main.
- Derive the exact controller delta automatically.
- Confirm the live controller still matches the expected pre-update state.
- Confirm no active supervisor child exists.
- Create a unique timestamped backup under C:\SupervisorBackup.
- Back up the controller package and A1 runtime journal.
- Do not use a backup command that can erase or mirror-delete an existing backup.
- Copy only the accepted controller delta.
- Do not modify the protected config or model selection.
- Generate the corrected manifest including the external protected config.
- Verify every copied file against the accepted source.
- Verify the protected-config SHA and ACL.
- Verify the model-selection SHA.
- Run verify-controller with both manifest and protected config.
- Run ordinary doctor with explicit checkout, config, model-selection, and manifest.
- Run the bounded doctor --live control-response probe using the exact verified Claude executable.
- Verify the A1 runtime remains PREFLIGHT and has no live children.
- Verify revoked asks no longer appear as open.
- Leave limited-auto disabled.

Resolve executable paths read-only. Previously observed candidates include:

Claude:
C:\Users\MLFLL\.local\bin\claude.exe

Codex:
C:\Users\MLFLL\AppData\Roaming\npm\codex.cmd

Do not trust these merely because they are listed here. Verify exact existence, version, and identity before use.

If UAC is needed, finish every non-UAC operation first and return one exact elevated command with rollback. Do not tell the owner to hand-edit files.

STAGE 3 — M0-T063 UNIT A1

After the repaired live controller passes all verification, begin M0-T063 using its existing task packet and D-013 requirements.

The baseline must be captured before behavior changes.

Implement:

- Deterministic canonical checkout identity.
- Repository snapshot fingerprint.
- HEAD identity.
- Dirty-state digest.
- Configuration/parser/schema versions in the fingerprint.
- Per-file SHA-256 source manifest.
- Canonical serialization and domain-separated hashes.
- Explicit handling of case, Unicode, symlinks, newline differences, unreadable files, exclusions, untracked files, and deleted files.
- Complete census accounting: eligible, indexed, excluded, failed, stale.
- mtime may be a precheck but is never proof.
- Cache generations outside the worktree under the accepted per-checkout LOCALAPPDATA namespace.
- Locking, temporary generations, validation, atomic promotion, crash recovery, and quarantine.
- Prior valid generation remains loadable after every simulated failure.
- Append-only redacted JSONL telemetry outside the repository.
- Bounded sanitized committed evidence.
- Baseline output digests and counts from the existing unmodified code-graph generator.
- Documentation and rollback.

Run exactly the packet’s documented tests plus the full required gates. Do not edit forbidden paths.

If the supervisor reaches a digest-bound approval, return one consolidated, exact approval request. Do not claim this prompt replaces a digest-bound approval.

STAGE 4 — M0-T064 UNIT A2: INCREMENTAL INDEXING

Implement incremental indexing on top of A1:

- Reuse the existing deterministic code-graph namespace.
- Cache by repository and snapshot fingerprint.
- On an unchanged snapshot, reuse the validated generation.
- On changes, hash and re-index only changed eligible files plus deterministically affected dependents.
- Correctly process add, edit, delete, rename, dirty worktree, parser-version changes, config changes, and eligibility-rule changes.
- Never use mtime alone.
- Maintain complete generation manifests.
- Validate every generation before promotion.
- Keep previous valid generations for rollback under bounded retention.
- Prevent concurrent writers and half-visible indexes.
- Full rebuild remains available as a reference and recovery path.
- Compare incremental output against a clean full rebuild; material graph output must match.
- Record files examined, files hashed, files parsed, files reused, affected dependents, cache hit/miss, runtime, and nullable token estimates.
- Never fabricate a zero when telemetry is unavailable.

STAGE 5 — M0-T065 UNIT B: BOUNDED CONTEXT COMPILER

Extend the existing context-pack contract rather than creating a competing system.

Implement deterministic context assembly using:

- Task and requirement anchors.
- Relevant subsystems.
- File and symbol neighborhoods.
- Dependency edges.
- Recent accepted decisions.
- Validated memory digests.
- Directly requested files and functions.

Required behavior:

- A compact ordinary task tier targeting roughly 5,000–8,000 useful tokens where sufficient.
- Existing larger ceilings remain safety ceilings, not mandatory fill targets.
- Effective budget is the strictest applicable configured limit.
- Required material is never silently chopped.
- If required material cannot fit, produce a structured overflow plan identifying what needs another retrieval pass.
- Separate authoritative source excerpts from summaries.
- Every selected item records why it was selected.
- Output must be deterministic for the same snapshot, query, and configuration.
- Context packs must identify the repository fingerprint and generation used.
- Stale context is rejected.
- Secret and sensitive-file exclusions remain enforced.
- Measure useful-context ratio, irrelevant-context ratio, retrieval misses, pack size, and time.

STAGE 6 — M0-T066 UNIT C: DETERMINISTIC ONTOLOGY

Do not invent a second soft taxonomy.

Use existing namespaces:

- File paths.
- Symbols.
- Existing code-graph nodes.
- Task IDs.
- Requirement IDs.
- Milestones.
- Directives.
- Accepted subsystem definitions.

Implement a versioned deterministic subsystem resolver. Parent relationships are derived, never freely chosen by a model.

A model may suggest advisory leaf tags, but those tags:

- Cannot create canonical parents.
- Cannot block promotion by themselves.
- Must be normalized and marked advisory.
- Cannot create duplicate canonical entities.

Every mapping must be reproducible and testable.

STAGE 7 — M0-T067 UNIT D: MEMORY AND SESSION-DIGEST PLACEMENT

Implement the closed digest schema:

- task_id
- requirement_ids[]
- files[]
- agent
- outcome

Add only other fields that are explicitly versioned and justified.

Rules:

- Digest → task packet → milestone is derived.
- File → symbol/subsystem is resolved through the code graph.
- Requirement IDs must exist.
- Task IDs must exist.
- File paths must resolve against the indexed snapshot.
- Every link is validated in a deterministic second pass.
- Unresolved links go to quarantine with reasons.
- Unresolved required links never silently enter the graph.
- Advisory topic tags cannot quarantine an otherwise valid digest.
- No orphan canonical nodes.
- Detect canonical duplicates and near-duplicate advisory labels.
- Add CI metrics for orphan count, unresolved-link count, and near-duplicate count.
- Preserve source digest, schema version, repository fingerprint, and creation provenance.

STAGE 8 — M0-T068 UNIT E: REPOSITORY-INTELLIGENCE VIEWS

Provide deterministic queries for:

- Repository census.
- File card.
- Symbol card.
- Subsystem card.
- Dependency neighborhood.
- Task-to-code mapping.
- Requirement-to-code mapping.
- Change-impact view.
- “Where is this implemented?”
- “What calls or depends on this?”
- “What changed since fingerprint X?”
- “What should be loaded to answer question Y?”
- “Which files were not indexed, and why?”

Answers must include concrete paths, symbols, edges, confidence, fingerprint, and generation. Do not answer with generic prose when repository evidence exists.

Bound every query. Do not dump the whole graph into the model context.

STAGE 9 — MODULARITY AND LARGE-FILE CONTROL

Perform a deterministic source-size and responsibility census across handwritten production code. Exclude generated files, vendored code, lockfiles, schemas, migrations where appropriate, and fixtures whose size is inherently data-driven.

Use file size as a warning signal, not as the sole architectural rule.

Apply these guardrails:

- New handwritten production modules should normally remain below 600 source lines.
- Anything above 750 lines requires an explicit cohesion justification.
- Anything above 1,000 handwritten source lines is a mandatory architecture-review item.
- Large functions, high fan-in/fan-out, mixed I/O/domain/storage/CLI responsibilities, and frequent-change hotspots receive higher priority than line count alone.
- Prefer modules with one clear responsibility.
- Separate domain logic, storage, serialization, CLI wiring, external I/O, policy, and presentation.
- Preserve public imports through thin compatibility facades when splitting existing modules.
- Do not create giant generic utils.py files.
- Do not split cohesive code into meaningless tiny fragments.
- Add focused tests before moving behavior.
- Refactor files touched by this initiative and any severe high-risk hotspot discovered by evidence.
- Do not turn this into an unrelated whole-repository rewrite.

Add a CI regression rule that prevents new oversized handwritten files or significant growth of existing oversized files without an explicit reviewed exception. Legacy debt may initially be reported rather than making all CI fail, but new regressions must fail.

Produce a modularity report with:

- Largest handwritten files.
- Top-level symbol counts.
- Responsibility findings.
- Dependency/coupling findings.
- Files refactored.
- Files deferred and why.
- Before/after line counts and test coverage.

STAGE 10 — M0-T069 UNIT F: BENCHMARKS AND PROMOTION

Build reproducible before/after evidence.

At minimum measure:

- Cold full-index runtime.
- Warm unchanged runtime.
- One-file-change runtime.
- Multi-file-change runtime.
- Files rehashed.
- Files reparsed.
- Cache hit rate.
- Stale-generation detection.
- Crash-recovery behavior.
- Incremental-versus-full output equality.
- Context-pack tokens or deterministic token estimate.
- Useful versus irrelevant selected material.
- Retrieval precision on a frozen question set.
- Misses and quarantined links.
- Orphans, unresolved links, and duplicates.
- Storage usage and retention behavior.
- Large-file/modularity regression status.

Create a frozen benchmark question set containing exact questions about known files, functions, tasks, requirements, subsystems, and dependencies. Compare answers against primary repository evidence.

Do not claim token savings without measured before/after data. If actual provider token usage is unavailable, distinguish deterministic estimates from provider-reported usage.

STAGE 11 — FINAL INTEGRATION AND OPERATIONS

For every unit:

1. Reconcile the task packet and allowed paths.
2. Claim through the existing lifecycle.
3. Implement only the unit’s scope.
4. Run local tests.
5. Run independent G3/G4/G5/DCV review as applicable.
6. Correct owned findings.
7. Push.
8. Wait for final CI.
9. Accept only against the actual reviewed material identity.
10. Merge only when green and mergeable.
11. Sync origin/main.
12. Continue to the next unit automatically.

If an unrelated existing failure blocks CI:

- Prove whether it is pre-existing and unrelated.
- Do not suppress or waive it.
- If a bounded repair is necessary, create a separate task from current main.
- Complete it through normal review.
- Resume the original sequence.
- Do not mix unrelated implementation into the blocked PR.

FINAL COMPLETION CONDITIONS

Do not return A_TO_Z_COMPLETE until all of these are true:

- The external protected config is genuinely bound into live manifest verification.
- The live controller is updated, backed up, verified, and healthy.
- Protected config and model selection remain byte-identical.
- Limited-auto remains disabled.
- M0-T063 through M0-T069 are implemented—not merely reserved.
- Every unit is accepted and merged.
- Incremental indexing is proven equal to clean full rebuild output.
- Cache generations are crash-safe and outside the repository.
- Context packs are deterministic, bounded, and stale-safe.
- Ontology placement is deterministic.
- Memory links are validated and quarantined correctly.
- Repository-intelligence queries return evidence-backed answers.
- New giant-file regressions are prevented.
- Required CI and security checks are green.
- origin/main is clean and synchronized.
- Runtime state and rollback instructions are documented.
- A single operator guide explains ordinary use, rebuild, cache reset, rollback, diagnostics, and how to ask repository-specific questions.
- A final report gives exact PRs, merge SHAs, task statuses, accepted count, test totals, benchmark results, token measurements, remaining risks, and any owner action still pending.

FINAL RETURN FORMAT

Return exactly one of:

A_TO_Z_COMPLETE

followed by:

- Final origin/main SHA.
- Tasks accepted and merged.
- PRs and merge commits.
- Live-controller verification.
- Tests and CI.
- Before/after performance and context measurements.
- Modularity results.
- Remaining non-blocking risks.
- Exact everyday startup command.
- Exact rollback command.

Or:

OWNER_ACTION_REQUIRED

only when no further independent work can continue, followed by one consolidated owner-action bundle with exact PowerShell commands, expected output, rollback, and the precise remaining stages that resume afterward.

Or:

HARD_BLOCKED

only for a real contradiction, unavailable credential, failed security invariant, or irreconcilable repository state, with primary evidence and no invented workaround.

Begin now with Stage 0. Continue through the complete sequence. Do not stop after producing another plan.
