# D-004 — source-001 (original owner message, verbatim)

Captured verbatim per `.claude/skills/directive-compliance` §1. Channel: owner_message.
Frozen baseline: `origin/main` = `421265709f81a40e20f3d890609907ed932967dd` (post-D-003 reconcile, PR #105).
Do not edit this file after the directive is active; corrections are new append-only `source-00N-amendment.md` files.

---

OWNER DIRECTIVE — Agent-teams runtime adoption, staged with pilots.
Capture this verbatim via /directive-compliance before any other action.
D-003 integration of #100/#101 is complete; do not reopen it.

PHASE 0 — ORIENT AND VERIFY
- Start-of-session routine: python tools/project_control.py status; read
  SESSION_HANDOFF.md; reconcile ledger vs git/CI; confirm main is clean.
- Verify the CLI supports agent teams (v2.1.178+: teammates spawn with no
  setup step) and the env flag is active in THIS session; if not, STOP
  and tell me to relaunch with it exported.
- Persistence: if .claude/settings.local.json exists, READ it and MERGE
  {"env":{"CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS":"1"}} into it without
  altering unrelated keys; create it only if absent. Then run
  `git check-ignore .claude/settings.local.json` and show the output. If
  it is not ignored, STOP and report before writing anything.

STANDING CONSTRAINTS (every phase)
- Ledger + git + CI are sole truth. The runtime team task list holds only
  ledger task ID references.
- ADR-005 unchanged: you alone run project_control.py, git, gh; you
  perform every rebase and merge. Producers never push, never run gh,
  never rebase; after you rebase, the producer only re-runs checks, and
  supplies content resolutions in its own worktree if a conflict lands in
  its scoped files.
- Sequential integration: one PR at a time, reconcile main, re-run
  required gates before the next.
- Reviewers are spawned ONLY from named .claude/agents/ definitions
  (agent_type-gated guards must recognize them), only the reviewers the
  content requires, each spawn prompt stating the frozen 40-char SHA and
  explicitly instructing /run-quality-gate (skills frontmatter does not
  auto-apply to teammates).
- Every tracked file this directive produces (including pilot reports)
  must belong to a contracted task whose allowed_paths cover it. Nothing
  floats outside a task.
- Do NOT touch M0-T025 in any way.
- Evidence hygiene (public repository): record only teammate names and
  agent types from team configuration. Redact session IDs, pane IDs,
  absolute user paths, and all machine-specific data from anything
  written to the repo.
- /resume does not restore teammates: every new conversation spawns a
  fresh team; never message a prior team.
- After ANY hook or settings file change is merged, testing of it happens
  only in a completely fresh Claude session; never assume a running
  session reloaded it.

STEP 1 — READ-ONLY REVIEWER PILOT (on my GO)
- First contract a pilot-governance control task (own ID, normal packet
  flow) whose allowed_paths are exactly the pilot report files named
  below plus its own producer report. All pilot evidence lives there.
- Target: one frozen, already-accepted SHA (e.g., post-integration main
  head). Mechanism proof only; the ledger's product state is untouched.
- Spawn 2–3 reviewer teammates from named definitions appropriate to
  that SHA's content.
- Record in project-control/reports/AGENT-TEAMS-PILOT-1.md (redacted):
  1) each teammate's own git rev-parse output showing the reviewed SHA;
  2) teammate names + agent types only, from team configuration;
  3) negative test: one reviewer attempts to create a disposable
     sentinel (a Write-tool attempt AND a Bash redirection such as
     `echo x > ./PILOT_SENTINEL.tmp`) — capture the guard denials
     verbatim, then YOU verify with `test -e` that the sentinel was
     never created, and record that verification;
  4) confirmation each reviewer actually invoked /run-quality-gate;
  5) verdicts + full report content delivered to you, preserved verbatim.
- STOP. Present evidence.

STEP 2 — NO-WRITE WORKTREE CAPABILITY PROBE (on my GO)
Purpose: prove teammates can enter and REMAIN in pre-created worktrees
before any confinement system is built.
- You pre-create two worktrees + branches from a frozen base SHA.
- Spawn two teammates with strictly read-only instructions (no Write/
  Edit; Bash limited to pwd/git inspection). Give each its
  EXPECTED_WORKTREE / EXPECTED_BRANCH / EXPECTED_BASE_SHA.
- Each must attest twice — once on arrival and once after at least one
  further turn — returning: pwd; git rev-parse --show-toplevel;
  git branch --show-current; git rev-parse HEAD.
- Success = both teammates match their DISTINCT assigned worktrees on
  BOTH attestations. Record results (redacted) in
  project-control/reports/AGENT-TEAMS-PILOT-2-PROBE.md under the
  governance task; tear down the worktrees.
- If the probe FAILS: STOP. Producers stay on the existing default
  (background subagents with isolation: worktree); only review waves use
  teams; skip Steps 3–4 and tell me.

STEP 3 — PRODUCER-CONFINEMENT CONTROL TASK (on my GO, only if Step 2
passed)
Create a NEW task — own ID, scope, acceptance tests, independent
reviewers. Never touch or repurpose M0-T025. Guarantee model: best-effort
prevention + guaranteed detection before integration; never claim
out-of-scope writes are impossible. Layers:
1) Bind session → task ID → worktree → branch → frozen base SHA; refuse
   producer work when the binding is missing or ambiguous.
2) PreToolUse: block Write/Edit destinations outside allowed_paths.
3) PreToolUse: block obvious repository-mutating Bash outside the
   assigned worktree.
4) Containment diff on BOTH TaskCompleted AND TeammateIdle (task status
   can lag): compare against the frozen base covering committed, staged,
   unstaged, renamed (BOTH sides of every rename must be allowed),
   deleted, and non-ignored untracked files; refuse completion on any
   escape.
5) Residual: worktree isolation + your final diff review before
   integration (interpreter/test-runner writes cannot be statically
   blocked; layers 4–5 catch them).
Acceptance tests: Windows paths, "..", symlinks, absolute paths,
mixed-case paths, shell redirects, git -C, generated files,
scripting-language writes.
STOP after its gate passes.

STEP 4 — TWO-PRODUCER PILOT (on my GO; start a COMPLETELY FRESH session
first so the merged hooks load)
- Two producer teammates on genuinely disjoint, already-contracted
  second-wave tasks. You pre-create worktrees + branches from a frozen
  base and record the §C matrix (task ID, branch, worktree, allowed/
  forbidden paths, expected shared files: none, merge order).
- Mandatory attestation before any write: EXPECTED_WORKTREE /
  EXPECTED_BRANCH / EXPECTED_BASE_SHA vs the four returned values (pwd,
  toplevel, current branch, HEAD). Any mismatch: stop that producer and
  report.
- Producers stay inside allowed_paths, return evidence + requested
  status (awaiting_gate | blocked | needs_split). You perform all git
  integration. Evidence (redacted) to
  project-control/reports/AGENT-TEAMS-PILOT-3.md under the governance
  task. STOP. Present evidence.

STEP 5 — ADOPTION (on my GO after Step 4 passes)
Contract a final task to:
- Amend ORCHESTRATION_POLICY §1 (teams preferred wave runtime when the
  session supports them; background subagents remain the fallback), §1a
  (task list persists locally; /resume does not restore teammates; fresh
  team every conversation; ledger authoritative), and §C (mandatory
  producer worktree attestation + reviewer spawn rules as permanent
  procedure).
- Move the env flag into tracked .claude/settings.json.
- Keep teammateDefaultModel at "leader's model" via /config ONLY (it
  lives as teammateDefaultModel: null in global ~/.claude.json; it must
  never appear in any tracked project settings file).
Present exact diffs before committing; route through independent review.
From then on, standard waves: ≤3 producers, packet-applicable reviewers
in parallel on frozen SHAs, sequential merges, you rebasing surviving
branches after each merge while producers re-run checks in parallel.

AT EVERY STOP: present evidence and exact proposed diffs, then wait for
my explicit GO. Anything ambiguous, blocked, or hold-releasing becomes a
blocker, not an action.
