# D-004 — source-007 (owner amendment 6, verbatim)

Captured verbatim per `.claude/skills/directive-compliance` §1 (amendments are new append-only
files; committed sources are never edited). Channel: owner_message. Amends `source-001.md`.
Frozen baseline unchanged: `origin/main` = `421265709f81a40e20f3d890609907ed932967dd`.
Head at capture time: `origin/main` = local `main` = `88045b06ef12ccb9b994b4e8b38ffe40d9cadf04`
(post PR #123), reconciled live against GitHub before any write; it contains the M0-T028
implementation merge `9db4ab328ea7e1570e347ae19174041d199aedc8` as required by the capsule.
This message is the owner's explicit GO for D-004 Step 3 **Phase 8 only**, delivered as the first
prompt of the REQUIRED fresh Claude Code session (the session that post-dates the PR #121 merge,
satisfying the two-session execution boundary of source-006/D-004-R134/R135).

Requirement IDs added by this amendment start at `D-004-R287`; no existing source file or
requirement row is edited. Scoping note: the executable Phase-8 obligations are ALREADY bound to
M0-T028 by rows D-004-R168–R286 (amendment 5), and M0-T028's independent verification row was
created there with the registry-derived 177 applicable ids that the CLI re-derives and enforces
at accept(). The rows below therefore record the GO event and bind THIS session's conduct under
the non-ledger sentinel `D-004-PHASE8` (same mechanism as `D-004-PHASE0`/`D-004-PROCESS`); they
deliberately do not join M0-T028's applicable set, which would contradict the capsule's pinned
177-id verification surface after submission. They are verified against primary evidence in the
Phase-8 independent verification report.

C2 note: the capsule (§5) makes recording the owner's decision on G5 finding M-2 (the C2
follow-up-task proposal) a precondition of acceptance. This GO does not state that decision;
it is captured below as an unresolved owner decision (D-004-R296) to be obtained and recorded
before `accept` runs, per skill §2 item 5 (owner decisions are captured, never inferred).

---

Resume NYC Buildability as orchestrator. Execute D-004 Step 3 Phase 8 exactly per project-control/reports/M0-T028-FRESH-SESSION-CAPSULE.md — reconcile main live first, prove all four hook entries fire (C1), run the on-policy three-reviewer sentinel with explicit Fable 5 spawns, independently verify sentinel absence, then resolve B-015, complete the final directive verification, and accept M0-T028 via the CLI. Stop conditions and forbidden actions per the capsule. This is my GO for Phase 8 only.
