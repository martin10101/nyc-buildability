# D-002 — source-001 (original owner message, verbatim)

Captured verbatim per `.claude/skills/directive-compliance` §1. Channel: owner_message.
Frozen baseline: `origin/main` = `2d44b47a6dcd0a53d04c60753482dc5f53776c8d` (post-#97-merge).
Do not edit this file after the directive is active; corrections are new append-only `source-00N-amendment.md` files.

---

OWNER DIRECTIVE — ACTIVATE THE CONTROL SYSTEM, CONSOLIDATE THE PLAN, AND PREPARE THE FIRST BLIND-ISOLATED PARALLEL BUILD WAVE

You are the sole controller/orchestrator for this repository. Complete this directive as one continuous control-plane run. Do not stop after routine substeps, commits, reviews, CI reruns, or corrections.

The four future producer tabs will be separate manually opened Cursor/Claude Code windows using separate Git worktrees. They cannot see one another’s conversations, unmerged branches, discoveries, or work. You must design the work so they do not need that visibility.

Do not use internal Agent, teammate, or subagent dispatch as a substitute for these separate tabs. You may use read-only reviewers where the existing control system permits them, but do not launch product producers in this tab.

## 1. Owner approval for PR #97

This message is explicit owner authorization to complete the existing protected acceptance and merge lifecycle for PR #97, provided all of the following identities still match:

* PR: #97
* Expected final head: `9bc1f13711fabd3f115c667efb72a8da3262d48d`
* Reviewed code identity: `ed8a6776f89c9f2b17ecad35e528a0180fe6bfd1`
* Content-manifest identity: `9ab6c9601b1a60f903d1a399d57192f662d96e912fdc50304ba931894b3004be`
* Changes after the reviewed code identity must still be declared evidence/control-only changes.
* Required final-head checks and directive verification must remain green.
* No unreviewed product-code change may have entered the PR.

Use the existing project-control lifecycle. Do not bypass acceptance, producer/verifier separation, frozen-identity checking, branch protection, or any owner/legal gate.

If an identity differs or new substantive code appeared, stop and return only a concise mismatch report. Otherwise, accept and merge PR #97 correctly, update local main with `--ff-only`, and explicitly read the merged directive/control-system changes before continuing.

## 2. Preserve the remaining LOW-1 issue without delaying product work

Do not amend PR #97 again for the reported LOW-1 issue concerning containment validation of `requirements_file` and `verification_file`.

After #97 is merged:

* Record LOW-1 as a narrowly scoped control-plane backlog item using the next valid reconciled task/blocker mechanism.
* State that it concerns path containment for trusted checked-in registry references.
* Preserve the current fail-closed behavior.
* Do not claim, dispatch, or implement it during this directive.
* It is not a blocker for preparing the first parallel build wave unless reconciliation discovers a materially greater risk.

This recommendation must now live durably in project control so it is not forgotten in later conversations.

## 3. Capture this directive under the new system

Invoke and follow `/directive-compliance`.

Capture this entire owner message verbatim as the next directive, expected to be D-002 if reconciliation confirms that ID is available. Do not silently choose D-002 if it is occupied.

Decompose all requirements, including the blind-tab rules and compact-reporting rules. Use the new multi-task verification format where applicable.

All new or materially replanned task packets created by this directive must cite this directive and carry the required directive-regime stamp. Do not create a second ledger, second gate system, or competing coordination framework.

## 4. Reconcile the existing control PRs before building

After #97 is merged, fetch the latest remote state and inspect the full diffs, reports, task packets, checks, and present status of:

* PR #93 — five-packet M3 legal-corpus replan
* PR #94 — frontend security reconciliation
* PR #95 — whole-system trust replan
* PR #96 — current handoff refresh
* PR #91 — superseded footprint proposal
* PR #92 — superseded handoff
* PR #64 — stale/superseded frontend implementation

Do not merge these stale branches blindly and do not assume their chat summaries are authoritative.

Create one fresh control-consolidation task and branch from the new post-#97 `origin/main`. Port or restate only the currently valid, independently supported control changes from the open PRs.

The consolidation must preserve at least:

* The corrected five-task M3 chain and Document Evidence Verification Engine.
* Source authority, closure, capture, versioning, construction-code, and professional-review controls.
* The whole-system A–J defect and architecture analysis.
* Decimal/exact legal arithmetic as a publication blocker.
* Auth/RLS/tenancy, orchestration, persistence, compatible snapshot sets, reporting, invalidation, and canonical-contract deficiencies.
* The current frontend security target and its age rule.
* The owner’s previous decision not to use an age-only exception.
* M0-T019 must not begin before the recorded exact eligibility time of `2026-07-28T15:59:32.231Z`, unless a later owner directive changes that decision.
* All existing holds concerning G6, legal approval, credentials, deployment, survey expansion, and paid services.

For every old PR, record whether each unique material change was:

1. carried forward;
2. superseded with a reason; or
3. still unresolved.

Once the fresh consolidation PR is accepted and merged, you are authorized to close stale/superseded PRs only when all unique material changes have been accounted for. Each closure comment must link to its replacement. Do not close any PR whose unique changes remain unresolved.

Create or refresh one accurate handoff based on the consolidated main state. Do not preserve multiple contradictory handoffs.

## 5. Build the parallel-work contract before creating worktrees

The producer tabs are “blind islands.” Their compatibility must be guaranteed by committed contracts on main before dispatch.

Create a durable first-wave integration contract using the existing task packets, directive references, allowed paths, reports, and project-control system. It may include a collision matrix and interface map, but it must not become a new task ledger.

The contract must freeze:

* exact task ID;
* exact base SHA;
* task dependencies;
* complete outputs;
* precise allowed paths;
* precise forbidden paths;
* owned interfaces and schema versions;
* read-only interfaces consumed from main;
* generated artifacts and their sole owner;
* database/migration ownership;
* environment-variable ownership;
* service/port ownership, if applicable;
* test and harness responsibilities;
* exact producer-report path;
* acceptance evidence;
* explicit stop conditions;
* planned downstream integration task.

No first-wave producer may depend on an unmerged output from another first-wave producer.

If producer B needs producer A’s work, they are sequential tasks and may not run simultaneously.

Path separation alone is insufficient. Also check for semantic collisions involving:

* the same API contract or schema ID;
* the same database table or migration sequence;
* the same configuration variable;
* the same generated output;
* the same source-of-truth document;
* the same package or lockfile;
* the same deployment definition;
* the same production entrypoint;
* provider/consumer version incompatibility.

Different source paths that write the same generated file count as an overlap.

## 6. Reserve shared hotspots for the controller or a later integration task

Unless one producer is explicitly assigned sole ownership, first-wave producers must not modify:

* `CLAUDE.md`
* `.claude/**`
* `.github/**`
* directive registry or directive tooling
* project-control CLI, master ledger, shared gate code, or control tests
* root dependency and lock files
* shared deployment definitions such as root `render.yaml`
* common production entrypoints
* shared application startup/configuration files
* shared generated artifacts
* another task’s report
* another producer’s migrations
* another producer’s schema or interface

A producer may modify only its exact task packet’s allowed paths and its one exact producer-report path.

Producer modules should be implemented behind frozen interfaces. Wiring multiple modules into shared production entrypoints belongs to a later, controller-contracted integration task after the providers have been merged.

If a producer discovers that a frozen interface must change, it must stop and submit a short interface-change request. It must not “helpfully” edit the shared contract.

## 7. Select the first parallel wave safely

Prepare for a maximum of four producer worktrees plus this controller tab. Four producers are a ceiling, not a quota.

Evaluate these candidate independent lanes:

1. M3-T001 legal-source authority, coverage, registry, manifest, and policy foundation.
2. Exact Decimal/legal-units arithmetic and rounding foundation addressing DF-2.
3. Canonical `source_fact` and `analysis_state_transition` contract hardening.
4. Auth/RLS/tenant-isolation foundation, only if its real acceptance path is not prevented by B-001 or another credential/deployment blocker.

Assign exact task IDs only after reconciling the authoritative ledger. Do not invent task IDs from this message.

Important sequencing:

* M3-T002 depends on accepted M3-T001 and therefore cannot run simultaneously with M3-T001.
* M3-T003 depends on M3-T002.
* Closure and downstream legal-rule work must follow their accepted dependencies.
* Tasks that consume newly hardened canonical contracts cannot run in parallel with the task changing those contracts unless they consume an already frozen compatible version.
* Do not start M0-T019 before its recorded eligibility timestamp.
* Do not start a run orchestrator that requires unmerged auth, persistence, or contract outputs.
* Do not fill an unsafe fourth slot merely to use all five windows.

If only three producers are genuinely independent and ready, prepare three. State plainly why the fourth remains idle.

## 8. Create worktrees, but do not open or operate the producer tabs

Only after the control-consolidation PR and first-wave task contracts are accepted and merged:

1. Fetch and fast-forward local main.
2. Confirm the controller worktree is clean.
3. Record one frozen first-wave base SHA.
4. Inspect existing worktrees and branches.
5. Create one separate sibling Git worktree and unique task branch per approved producer.
6. Do not duplicate the repository.
7. Do not install duplicate dependency trees, local databases, Docker data, or citywide datasets merely to prepare the worktrees.
8. Do not launch Claude, Cursor, or producer agents inside them.
9. Return the exact absolute path of every worktree so the owner can manually open each folder in a separate Cursor window.

Each producer must start from the same frozen main SHA unless its accepted dependency requires a later wave.

## 9. Create one startup capsule for each producer

Save each capsule durably in its task/report area, but make the task packet the source of truth.

Each capsule must be concise and contain:

* “You are a producer, not the controller.”
* task ID and title;
* branch and expected worktree;
* frozen base SHA;
* exact files that must be read before starting;
* directive references;
* owned and consumed interfaces;
* allowed paths;
* forbidden paths;
* dependencies already proven accepted;
* required tests and harnesses;
* permitted report path;
* exact completion condition;
* stop conditions;
* compact final-return format.

At the beginning of a new producer session—and after chat compaction, rebase, or a new task—the producer must read:

1. root `CLAUDE.md`;
2. its authoritative task packet;
3. the first-wave integration contract;
4. its applicable directive requirements;
5. its frozen interface/schema files;
6. its most recent task-specific resume report, if any.

It does not need to reread everything repeatedly during one continuous unchanged session.

## 10. Producer behavior and reporting rules

The startup capsules must instruct every producer:

* Work through the entire authorized task without stopping after routine substeps.
* Do not merge, accept, replan the ledger, change another task, or operate another worktree.
* Do not read from or import an unmerged sibling branch.
* Do not assume another tab completed anything.
* Do not modify a shared contract.
* Commit and push only the assigned branch.
* Open one PR against main when the complete packet is ready.
* Keep detailed evidence, commands, matrices, and file lists in committed reports and CI—not in the owner chat.
* Stop only for a real owner decision, credential/payment requirement, legal ambiguity, security exception, destructive operation, contract collision, or impossible acceptance condition.

A blocker return must be no more than 200 words.

A completed producer return must be no more than 500 words and use at most eight bullets:

1. task and plain-English outcome;
2. PR link and current head;
3. what now works;
4. contract/interface impact;
5. tests and review result;
6. blockers or limitations;
7. exact next dependency unlocked;
8. whether the producer is now idle.

Do not return every intermediate SHA, command, file, test name, requirement row, or review transcript.

## 11. Controller integration process after the tabs begin

The controller must never rely only on a pasted producer summary.

Whenever the owner pastes a producer’s completion return into the main tab, the controller must explicitly:

1. fetch remote state;
2. inspect the PR metadata and full diff;
3. read the authoritative task packet and producer report;
4. confirm every changed file is within allowed paths;
5. check the frozen interface/schema versions;
6. inspect required CI and independent-review evidence;
7. detect collisions against changes already merged during the wave;
8. determine whether the branch must be rebased;
9. merge only one producer PR at a time;
10. fast-forward main and read the merged result before considering the next PR.

If a completed producer branch is behind main, do not modify its active worktree silently. Return a short rebase/revalidation capsule for that producer tab. Merge only after its final rebased head is green.

Other independent producer tabs do not need live updates after every merge. However, if a merge changes an interface, schema, migration, generator, or configuration they consume, freeze the affected producer and require explicit rebase and revalidation.

Parallel production never means parallel merging.

Use provider-before-consumer order and schema-before-integration order. Shared entrypoint wiring occurs only in a later integration task after all required providers are accepted.

## 12. Compact owner communication

This is one continuous authorized controller run. Do not return to the owner after:

* each file edit;
* each commit;
* each gate;
* each reviewer round;
* each CI retry;
* each stale PR inspected;
* each routine fix.

Continue automatically unless one of the explicit stop conditions requires an owner decision.

At the end of this directive, return one owner-facing summary of no more than 900 words, excluding the short startup capsules. Use plain English.

Include only:

* whether #97 was accepted and merged;
* what happened to #93/#94/#95/#96/#91/#92/#64;
* the new directive and control-consolidation task;
* the final frozen base SHA;
* the approved first-wave producer tasks;
* proof that their ownership and dependencies do not overlap;
* exact absolute worktree paths;
* one short startup capsule for each manually opened tab;
* unresolved blockers or intentionally idle slots;
* the owner’s next physical action.

Do not include full changed-file lists, complete SHA histories, complete requirement matrices, raw command output, duplicated test lists, or lengthy reviewer narratives. Those belong in the repository evidence.

Always distinguish:

* proposed;
* implemented on a branch;
* CI-verified;
* accepted;
* merged into main;
* production-invoked;
* deployed.

Stop after the worktrees and capsules are prepared. The owner will manually open each producer worktree in a separate Cursor window and paste its capsule. Do not begin product implementation in the controller tab.
