# D-003 — source-001 (original owner message, verbatim)

Captured verbatim per `.claude/skills/directive-compliance` §1. Channel: owner_message.
Frozen baseline: `origin/main` = `cc142081336f2dac0854a947694fec33559dcc8a` (post-D-002-consolidation).
Do not edit this file after the directive is active; corrections are new append-only `source-00N-amendment.md` files.

---

OWNER DIRECTIVE — INTEGRATE FIRST WAVE AND PREPARE SECOND WAVE

All three first-wave producers have opened PRs. Perform the D-002 sequential integration process now. Capture this direction under the active directive-compliance system if a new directive is required.

Current remote facts to independently reverify:

* main remains `cc142081336f2dac0854a947694fec33559dcc8a`.
* PR #99, M2-T017: head `fead3f386281ecef5210d6aee1ba6f568264711d`; open, mergeable, all workflows green.
* PR #100, M3-T001: head `c0fc5d1219f57ce7208d00c1259195ba6ba311e9`; open and mergeable, but CI is red.
* PR #101, M4-T007: head `f97e00643d99c2789e32a84d2e64a2075fef81d2`; open and mergeable, but CI is red.

Do not treat any producer summary as acceptance evidence by itself.

## PR #99

Independently inspect the complete diff, task packet, report, allowed paths, frozen interfaces, generated artifacts, and D-002 evidence.

Run the exact-head G3/G4/G5 reviews. If green, submit, accept, and merge #99 through the protected lifecycle, fast-forward main, and read the merged result.

Do not begin serializer wiring; that requires a new integration task.

## PR #100

Do not accept or merge while red.

GitHub currently shows two failures:

1. Contract validation treats the positive and negative `legal_source_manifest` fixture JSON files under the schema tree as if they were schema definitions. The files consequently fail schema-document requirements.
2. The context-budget guard identifies `docs/LEGAL_CORPUS_COVERAGE_MATRIX.md` as an unaccounted duplicate current-status board.

Inspect the full logs yourself and return one concise correction capsule to the idle M3-T001 producer.

The producer must resolve the fixture-discovery problem without weakening the global validator or disguising invalid fixtures. It must also resolve the coverage-matrix classification honestly. Do not label a current matrix "historical" merely to make CI pass.

If resolution requires changing a shared context-budget allowlist or another forbidden control file, the producer must not cross its ownership boundary. Handle that as a tiny controller-owned control amendment, then require the producer to rebase.

After #99 merges, require M3-T001 to rebase onto current main, rerun its complete local verification, push a new head, and obtain entirely green final-head CI before independent G3/G4/G5 review and acceptance.

B-011 remains an owner-controlled construction-code-scope decision. Accepting M3-T001 must not silently approve that draft.

## PR #101

Do not accept or merge while red.

The current CI failure is one Playwright accessibility-focus test in `a11y-announcements.spec.ts`; 65 other journeys passed, and the failing frontend path is outside M4-T007's allowed scope. All relevant API/rules/math checks passed.

Rerun the failed web-e2e job against the exact unchanged head.

* If an identical-head rerun passes, record it as a suspected unrelated transient and continue independent review.
* If it fails again, investigate reproducibility as a controller-owned frontend/CI problem.
* The M4-T007 producer must not modify `apps/web/**`.
* Do not dismiss a repeatable failure merely because it appears unrelated.

After #99 merges, require #101 to rebase onto current main and rerun final-head verification.

Independently verify that Fraction/rational arithmetic satisfies the task's permitted "Decimal/rational" contract, that the differential recomputation is truly independent, that legal comparisons contain no binary-float decision path, and that the unit/rounding limitation does not contradict AS-4. Resolve B-014 only if the accepted implementation actually satisfies it.

## Merge discipline

Merge only one PR at a time:

1. Review exact head.
2. Rebase after preceding merges.
3. Run final-head CI.
4. Run independent gates.
5. Accept.
6. Merge.
7. Fast-forward main.
8. Read the merged result.
9. Continue to the next PR.

Probable order: #99 first, followed by whichever corrected/rebased PR is ready. Prefer #100 before #101 if both become ready simultaneously because M3-T001 unlocks the sequential legal-corpus chain.

Do not modify active producer worktrees from the controller. Return compact correction/rebase capsules for the appropriate producer tabs.

## Prepare second wave only after all first-wave PRs are resolved

After all accepted first-wave work is merged, contract a second blind-isolated wave from one new frozen main SHA.

Evaluate these three successor lanes:

1. M3-T002 — immutable official-source capture, content-addressable storage, and byte-level version detection, only if B-001 is resolved and a real durable-storage acceptance path exists.
2. A new integration task wiring the accepted M2-T017 allowlist serializer into the property-profile builder with fail-closed tests.
3. A new rule-engine hardening task for DF-6: missing optional inputs inside exception predicates must produce indeterminate/professional-review behavior, never silently evaluate as false.

Assign exact task IDs only after ledger reconciliation. Prove that their dependencies and file ownership are disjoint.

If B-001 remains unresolved, do not run fixture-only M3-T002 merely to fill a tab if it cannot be accepted or unlock M3-T003. Select another genuinely independent, acceptance-ready backlog task, or prepare only two producers.

Do not begin M3-T003 before accepted M3-T002. Do not begin new legal-rule-content work while its M3 closure/G6 dependencies remain unresolved.

Create fresh worktrees from the new frozen main SHA and return concise startup capsules. Do not reuse old task branches or old worktrees for new tasks.

Keep interim owner communication concise. Return detailed evidence to repository reports, not chat.
