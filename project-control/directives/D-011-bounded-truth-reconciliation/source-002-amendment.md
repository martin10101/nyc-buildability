# D-011 — Amendment 002 (owner, 2026-08-11)

- Channel: owner_typed_instruction (follow-up in the same reconciliation exchange)
- Recorded: 2026-08-11
- Append-only amendment to D-011; does NOT edit source-001.md.

## Verbatim owner text

keep going and get things done so we can open Codex fully also check how Codex manage itself when one model is not available (token issues etc) I understand it falls back on a lazy model but I want each new season that it trys first the main model

## Interpretation notes (for the resolver; not owner words)

- "keep going and get things done so we can open Codex fully" = proceed with the bounded prerequisite
  corrections that lead toward full Codex actuation. It is forward INTENT, not an authorization to activate
  now: the D-011 source-001 prohibitions (no M0-T056, no R595 activation, no broad accept allowlist) REMAIN
  in force until the P1-P3 / P6 engineering corrections land, M0-T053 is accepted, and the owner flips the
  activation switch. "Open Codex fully" names the destination, not a lifted hold.
- "check how Codex manage itself when one model is not available ... I want each new season [session] that it
  trys first the main model" = a supervisor model-selection requirement: at the start of every NEW session/run,
  attempt the MAIN/primary model FIRST; fall back to the cheaper ("lazy") model ONLY when the main is
  unavailable (weekly cap / token exhaustion / provider outage); the fallback must NOT persist as a sticky
  default into later sessions — each new session re-attempts the main model first. ("season" read as "session".)
