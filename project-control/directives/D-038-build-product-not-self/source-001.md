# D-038 source-001 (verbatim) — owner directive 2026-09-08

## Owner terminal prompt (verbatim)

> i need the codex loop to run and build the softwere asap nonstop no more building itself

## Owner decisions in the same exchange (AskUserQuestion selections, verbatim option text)

Question 1 — "The product's data foundation (database, auth, storage, legal corpus) is blocked on a
missing Supabase management access token (B-001). Providing it is the single biggest unlock. Do you
want to set it up now?"
- Owner selected: **"Not right now"** — Skip credentials this session. The loop can only build
  product engineering that doesn't touch live data/storage.

Question 2 — "Regardless of credentials, I'm pivoting the loop OFF supervisor self-infrastructure.
What should it build instead — right now?"
- Owner selected: **"Unblocked product engineering"** — Loop builds real product that needs no
  credentials: user-facing screens (Compare/Evidence/report/reviewer UI), more deterministic rule
  families, and the scenario/optimization engine. Keep it nonstop on product.

## Interpretation anchor (not owner words; for tracing only)

"the software" = the NYC development-feasibility PRODUCT (M1–M6 user-facing/domain features), NOT the
supervisor/control-plane self-infrastructure (the M0-T036→T155 loop-tooling line, e.g. gate-wave /
acceptance engine / reviewer proportionality). "no more building itself" = stop assigning the loop its
own tooling as the build target while unblocked product work exists. "nonstop" continues the standing
D-035 monitor-and-restart nonstop duty.
