# D-087 source-001 — owner directive (verbatim), 2026-09-24, interactive chat

## Context

Seq-128 orchestrator session (the D-085 Opus 5.5 restart), ~07:05Z on 2026-09-24. The three
fresh DCVs for M5-T073/T074/T075 were running; the orchestrator was drafting the lane re-feed
packets and the D-086 P0 packet. The owner sent the message below mid-turn (voice dictation;
transcription errors are preserved exactly as received — the orchestrator's working reading of
each unclear phrase is recorded in requirements.json, never edited into this source).

## Verbatim (owner, 2026-09-24)

> I have now alot of token to use up before today's reset lunch as many loops and subagents as
> possible to get more done wholesome course notn interfering with each other, you can even give
> them to build features that we pushed over later, like 3D. An amazing that not an amazing 3D
> handling on buildings and lap size or buildings being able to import into autocat. Through that
> special problem that we discussed forgot the name of it that basically is like the middleman
> PDF. Writing blueprint, reading PDF Friday. I think we did that writing. But it can be probably
> improved. Writing editing exporting cat files dwgs all that stuff. As long as they don't
> interfere with each other, you can run like 10 loops in one shot

## Orchestrator reply at capture (trace only — not owner text)

The orchestrator answered in plain English with the plan (about ten concurrent builders: the
three loop lanes plus helper agents on separate files; new work = a 3D lot/building view, an
AutoCAD-openable export, and architect-PDF reading/drawing-sheet output) and asked two
questions: (1) whether "the middleman" format is DXF; (2) native DWG needs a paid/licensed
library — DXF is used until the owner decides. It also asked for a few real architect PDF
drawings (public examples otherwise).
